# names.yaml — knowledge base for the retailOS Rust port.
#
# Maps osos load addresses to semantic names, signatures, variable renames
# and notes. Addresses are load addresses (base 0x08000000); file offset in
# osos.dec = address - 0x08000000. Sizes from Ghidra (decomp/functions.csv)
# unless noted.
#
# module values are folder/file under src/ (older entries use the pre-reorg
# flat file stems — same names, now organized into libc/, runtime/, strto/,
# time/, printf/, scanf/, stdio/, fp/).
# status values: identified / ported
# "ported" means: Rust port exists in src/, behavioral tests vs a reference
# implementation pass on host (cargo test), and a match.py mnemonic diff
# report was reviewed (exact instruction matches are not expected:
# ARM ADS 1.0.1 vs modern LLVM).

base: 0x08000000

functions:
  # --- ARM ADS runtime: memory block ---
  - address: 0x080000d4
    ghidra: FUN_080000d4
    name: memmove
    size: 452
    sig: "void *memmove(void *dst, const void *src, size_t len)"
    module: libc
    status: ported
    vars: {param_1: dst, param_2: src, param_3: len}
    notes: Backward copy on overlap from above, 32-byte block fast paths,
      funnel shifts for misaligned src.

  - address: 0x08000188
    ghidra: FUN_08000188
    name: memcpy_forward_words
    size: 108
    sig: "void *memcpy_forward_words(void *dst, const void *src, size_t len) -> dst + len"
    module: libc/memcpy
    status: ported
    vars: {param_1/r0: dst, param_2/r1: src, param_3/r2: len, param_4/r3: scratch}
    notes: >-
      Word-aligned forward body of the ADS copy runtime. Two 16-byte
      ldmia/stmia pairs stream each 32-byte block, followed by 16/8/4-byte
      groups and a fully loaded 0-3 byte tail. r0 returns dst + len; r1 is
      also advanced but is caller-saved. Overlap is not repaired: each load
      group completes before its stores, so dst > src can consume prior stores.

  - address: 0x080005fc
    ghidra: FUN_080005fc
    name: byte_copy
    size: 24
    sig: "void byte_copy(void *dst, const void *src, u32 len)"
    module: libc/byte_copy
    status: ported
    vars: {param_1: dst, param_2: src, param_3: len}
    notes: Forward byte-at-a-time copy helper. The pre-decrement/CMN loop
      skips all reads and writes when len is zero; overlapping ranges retain
      forward-copy (not memmove) behavior.

  - address: 0x08038260
    ghidra: thunk_EXT_FUN_220005fc
    name: iram_byte_copy_veneer
    size: 8
    sig: "void iram_byte_copy_veneer(void *dst, const void *src, u32 len)"
    module: libc/byte_copy
    status: ported
    renames: {thunk_EXT_FUN_220005fc: iram_byte_copy_veneer,
      EXT_FUN_220005fc: byte_copy}
    notes: >-
      Ghidra's 4-byte extent omits this ADS literal veneer target word: raw
      osos.dec has ldr pc,[pc,#-4] (0xe51ff004) at 0x08038260 and literal
      0x220005fc at 0x08038264; the next veneer starts at 0x08038268, so
      the true size is 8 bytes. Decoding every ARM B/BL word finds exactly
      10 direct calls, all unconditional bl (0x082e1b2c, 0x082e1b3c,
      0x082e1b4c, 0x082e1b5c, 0x082e1b6c, 0x082e1b84, 0x082e1bb0,
      0x082e1bd0, 0x082e1bf0, 0x082e1c08); no predicated call, tail branch,
      or aligned raw word references this thunk, so it is neither
      caller-gated nor virtually dispatched. The relocator at 0x080046e0
      copies osos 0x08000000..0x0800aed8 into IRAM 0x22000000, making
      target 0x220005fc the already ported byte_copy @ 0x080005fc. The
      veneer forwards to that body through a volatile function pointer,
      retaining separate hook-target identity and preventing inlining/ICF;
      it preserves the void byte-copy ABI and forward overlap behavior.

  - address: 0x082c4f38
    ghidra: FUN_082c4f38
    size: 24
    sig: "void forward_byte_copy(void *dst, const void *src, u32 len)"
    module: libc/forward_byte_copy
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: src
      param_3/r2: len
      r3: byte
    notes: >-

      Raw osos.dec extent is exactly 24 bytes: six ARM words from
      0x082c4f38 through bx lr at 0x082c4f4c; the next separately linked
      function starts at 0x082c4f50. Decoding every ARM B/BL word finds 13
      direct call sites, all unconditional bl (zero predicated forms).
      sub r2,#1; cmn r2,#1 skips the post-increment ldrb/strb pair exactly
      when len is zero, otherwise copying forward one byte per iteration.
      This has void return and deliberately retains forward overlap
      propagation rather than memmove behavior. The target-only unique Rust
      text section distinguishes this byte-identical body from byte_copy at


      0x080005fc; volatile accesses prevent LLVM replacing the retail loop
      with a memcpy intrinsic.

  - address: 0x080e7ebc
    ghidra: FUN_080e7ebc
    name: byte_copy_signed
    size: 24
    sig: "void byte_copy_signed(const u8 *src, u8 *dst, i32 len)"
    module: libc/byte_copy_signed
    status: ported
    vars: {param_1: src, param_2: dst, param_3: len}
    notes: >-
      Forward byte-at-a-time copy leaf whose argument order is (src, dst,
      len) - reversed relative to byte_copy (0x080005fc) and the libc
      convention - and whose loop guard is SIGNED: `subs r3,r2,#0` then
      predicated ldrbgt/strbgt and bgt, so len <= 0 touches neither pointer
      (the unconditional r2 decrement is caller-saved and unobservable).
      Extent binary-verified at 24 bytes: the next function
      (jpeg_source_read_byte @ 0x080e7ed4) opens with `ldr r3,[pc,#84]`.
      Call count verified by decoding every BL word in osos.dec: exactly 15
      sites, all plain unconditional bl, none predicated; callers include
      the Finder/System/Desktop-DB/Desktop-DF record writers near
      0x080aab00/0x080b4550 and the counted-string copies in 0x080cab5c,
      none of which need a callee NULL guard. Void return; the advanced
      src/dst left in r0/r1 are not a defined result. Overlap retains
      forward-copy (not memmove) behavior. Distinct from byte_copy: that
      one takes (dst, src, u32) with a `len != 0` guard, under which a
      negative length would copy ~4 GiB instead of nothing. match.py diff
      is the expected LLVM shape shared with byte_copy (hoisted cmp/bxlt
      guard, fp frame, do-while) - structure and loop body match. Host
      tests prove the signed guard (0/-1/INT_MIN are no-ops on null
      pointers), guard-byte integrity across len 0..64 against an
      independent slice reference model, and forward-overlap behavior.

  # --- stack-sensitive literal tail dispatch ---
  - address: 0x08003818
    ghidra: FUN_08003818
    name: kernel_indirect_dispatch
    size: 8
    sig: "void kernel_indirect_dispatch(void *argument) __attribute__((noreturn))"
    module: kernel/thunks
    status: ported
    renames:
      param_1: argument
      DAT_0800381c: kernel_indirect_dispatch_target
    notes: >-
      Raw ARM is `ldr pc, [pc, #-4]` followed by literal 0x0815ca7c, a
      tail dispatch rather than Ghidra's inferred indirect call and return.
      The recovered caller 0x08005cf4 passes its pointer in r0; the target
      immediately pops {r4, lr} and branches to 0x0812b9a4, so it never
      returns to the veneer's immediate caller. The ARM port is verbatim
      global assembly; the host-only shim terminates because that stack
      transfer cannot be represented in a normal host call.

  # --- display command resource selectors ---
  - address: 0x0800483c
    ghidra: FUN_0800483c
    name: display_index_flags
    size: 40
    sig: "u32 display_index_flags(u32 first_resource, u32 second_resource)"
    module: drivers/display_index_flags
    status: ported
    renames:
      param_1/r0: first_resource
      param_2/r1: second_resource
      uVar1/r0: flags
    notes: >-
      The display-command builders 0x080048fc and 0x08004a20 use the result
      to choose table lookup versus direct resource words for each selector.
      Each unsigned selector below the 28-entry table limit contributes
      independently: first_resource -> 0x1000 and second_resource -> 0x0800.

  # --- I2S transfer submission default mode ---
  - address: 0x08008648
    ghidra: FUN_08008648
    name: queue_transfer_with_default_mode
    size: 36
    sig: "void queue_transfer_with_default_mode(u32 first, u32 second, u32 length, u32 transfer_type, u32 transfer_slot)"
    module: drivers/transfer_default_mode
    status: ported
    renames:
      param_1/r0: first
      param_2/r1: second
      param_3/r2: length
      param_4/r3: transfer_type
      param_5/stack: transfer_slot
      FUN_08004a20: submit_transfer
      callee_param_4/r3: control
    notes: >-
      The sole recovered caller, 0x080084dc, submits each transfer chunk with
      source, destination, chunk length, controller kind, and slot. Raw ARM
      reshuffles its five-word ABI into 0x08004a20's six-word ABI, setting the
      fourth callee word to literal zero and forwarding the last two words on
      the stack. The target/host ROM seam retains that exact argument order;
      a host recorder proves one call with the inserted zero and discarded
      callee result.

  # --- Record field configuration ---
  - address: 0x0800f1b4
    ghidra: FUN_0800f1b4
    name: configure_record_fields
    size: 40
    sig: "void configure_record_fields(void *record, u32 primary_value, u32 secondary_value)"
    module: util/record_fields_configure
    status: ported
    renames:
      param_1/r0: record
      param_2/r1: primary_value
      param_3/r2: secondary_value
      param_1_plus_0xf0: secondary_value_field
      param_1_plus_0xf4: primary_value_field
      param_1_plus_0xf8: ready_flag
    notes: >-
      The only recovered callers (0x0801271c and 0x08015f40) are
      image-processing routines. Both pass a record-like object, a computed
      primary value, and literal 8. Raw ARM normalizes r1 and r2 through a
      literal-pool add/sub pair without changing their values, then stores the
      primary value at +0xf4, the secondary value at +0xf0, and zero at +0xf8.
  # --- I2S transfer-slot teardown ---
  - address: 0x08004c4c
    ghidra: FUN_08004c4c
    name: i2s_transfer_slot_cleanup
    size: 40
    sig: "void i2s_transfer_slot_cleanup(i32 controller, i32 channel)"
    module: util/i2s_transfer_slot
    status: ported
    renames:
      param_1/r0: controller
      param_2/r1: channel
      DAT_08004c74: i2s_transfer_slot_table_base
      iVar1: transfer_handle_cell
      thunk_FUN_0804b614: i2s_transfer_cleanup
    notes: >-
      The I2S transfer setup path 0x080083c4 stores a handle through companion
      setter 0x08004c78; sole recovered teardown caller 0x080085fc invokes
      this helper before it clears the channel active flag. The literal base is
      0x2200ae7c and the cell is base + controller*0x20 + channel*4 - 0x20.
      Raw ARM loads the cell, calls 0x0804b614 only when nonzero (preserving
      cell value in r0 and channel in r1), and always clears it.

  # --- RAM stream-buffer configuration ---
  - address: 0x08006b48
    ghidra: FUN_08006b48
    name: stream_buffer_configure_words
    size: 28
    sig: "u32 stream_buffer_configure_words(StreamBuffer *stream_buffer, u32 word_count)"
    module: util/stream_buffer_configuration
    status: ported
    renames:
      param_1/r0: stream_buffer
      param_2/r1: word_count
      param_1_plus_0x1c: byte_capacity
      param_1_plus_0x20: free_word_count
      param_1_plus_0x30: configured_word_count
    notes: >-
      Sole recovered caller 0x080073f0 is the RAM stream-buffer constructor:
      it zeros the object bookkeeping then passes 0x400. Raw ARM stores the
      word count at +0x30, logical-left-shifts it by two and stores the
      resulting byte capacity at +0x1c, clears +0x20, and returns zero.

  # --- RAM stream-buffer lazy initialization ---
  - address: 0x08006e88
    ghidra: FUN_08006e88
    name: initialize_stream_buffer
    size: 136
    sig: "void *initialize_stream_buffer(void)"
    module: kernel/stream_buffer_initializer
    status: ported
    renames:
      DAT_08006ef8: stream_buffer_guard_state (target RAM @ 0x22008c8c)
      DAT_08006efc: stream_buffer_guard (target RAM @ 0x22008c90)
      DAT_08006f00: stream_buffer (target RAM @ 0x22010398)
      DAT_08006f04: dso_handle
      DAT_08006f08: stream_buffer_destructor (IRAM mirror @ 0x22007454)
      DAT_08006f0c: stream_buffer_pages_ready (target RAM @ 0x2200aed5)
      FUN_080036e0: cxa_guard_acquire_veneer
      FUN_080036e8: cxa_atexit_veneer
      FUN_080036f0: cxa_guard_release_veneer
      FUN_080072cc: stream_buffer_page_initialize
      FUN_080073f0: stream_buffer_construct
    notes: >-
      Raw ARM is 112 bytes of code ending with `pop {r4,pc}` at 0x08006ef4
      plus a 24-byte literal pool at 0x08006ef8..0x08006f0c; the next
      independently linked leaf starts at 0x08006f10, so the true extent is
      136 bytes rather than a code-only count. Decoding every ARM B/BL word
      in osos.dec found 16 direct callers: all are unconditional bl, with
      zero predicated forms. It tests guard bit 0 at 0x22008c90, then on
      cxa_guard_acquire success calls the unported stream-buffer constructor
      0x080073f0 for fixed object 0x22010398, registers its returned `this`
      with cxa_atexit using IRAM destructor 0x22007454 and __dso_handle
      0x089ca09c, and releases the guard. Independently, it calls unported
      page initializer 0x080072cc(object, 1, 0) under byte flag 0x2200aed5,
      then returns the fixed object on every path. Port calls the already
      ported cxa guard and shutdown-chain functions directly; constructor and
      page initialization remain target-only seams, while host tests use
      private fixed-state backing and recording seams. The IRAM destructor
      target is valid: relocator 0x080046e0 mirrors the 0x08007454 body.

  # --- RAM stream-buffer page contexts ---
  - address: 0x080071b0
    ghidra: FUN_080071b0
    name: stream_buffer_set_page_contexts
    size: 32
    sig: "void stream_buffer_set_page_contexts(StreamBuffer *stream_buffer, u32 page_context)"
    module: kernel/stream_buffer_page_contexts
    status: ported
    renames:
      param_1/r0: stream_buffer
      param_2/r1: page_context
      param_1_plus_0x34: page_contexts
      param_1_plus_0x13c: fallback_page_context
      iVar2/r2: page_index
    notes: >-
      The RAM stream buffer is initialized by 0x080072cc/0x080073b0 and
      uses 32 4-KiB pages in its 0x20000-byte circular allocation. This
      leaf stores the supplied page context in every per-page word
      (+0x34..+0xb0) and the active-event-loop fallback context at +0x13c.
      0x0800722c reads the fallback when its callback is active; otherwise
      it selects a per-page word from the producer position. Raw ARM is one
      +0x13c store followed by a count-up loop of 32 +0x34+i*4 stores.
      The port uses volatile stores to prevent a freestanding ARM libc fill
      call while retaining those exact writes. Host tests prove all 33 words
      receive the context and every byte outside those fields remains intact.

  # --- RAM stream-buffer producer-page reset ---
  - address: 0x08007284
    ghidra: FUN_08007284
    name: stream_buffer_reset_producer_page_count
    size: 16
    sig: "u32 stream_buffer_reset_producer_page_count(StreamBuffer *stream_buffer)"
    module: h264/stream_buffer_reset
    status: ported
    renames:
      param_1/r0: stream_buffer
      param_1_plus_0x28: producer_page_count
      r1: zero
      r0: success
    notes: >-
      One recovered call site is the successful media-startup path at
      0x080056d0: after initializing the RAM stream buffer through
      0x08006e88, it calls this leaf with the returned object in r0.
      The stream-buffer constructor 0x080073f0 initializes +0x28, and the
      producer path 0x08006b64 increments it when it reaches an exhausted
      page. Raw ARM is `mov r1,#0; str r1,[r0,#0x28]; mov r0,#0; bx lr`:
      exactly one word store, then the literal zero success return.

  # --- UI allocation-table slot creation ---
  - address: 0x08004c78
    ghidra: FUN_08004c78
    name: ui_allocate_table_slot
    size: 40
    sig: "u32 ui_allocate_table_slot(i32 row, i32 entry)"
    module: ui/table_slot_allocate
    status: ported
    renames:
      param_1/r0: row
      param_2/r1: entry
      thunk_FUN_0804b8b0: allocate_ui_table_entry
      DAT_08004ca0: ui_allocation_table_base
      uVar1/r0: allocated_word
    notes: >-
      Calls the no-argument veneer at 0x080036c0 (literal target
      0x0804b8b0), stores its returned word at
      0x2200ae7c + row*0x20 + entry*4 - 0x20, then returns 0. The sole
      recovered caller 0x080083c4 passes the selected UI mode as row and a
      signed entry byte as entry before setting the matching row's control
      word. The unported allocator remains behind a target/host callback
      seam; direct host tests prove its return is stored at the exact index
      and that this wrapper returns zero.

  # --- UI startup event-one sequence ---
  - address: 0x080056b0
    ghidra: FUN_080056b0
    name: ui_startup_sequence
    size: 32
    sig: "u32 ui_startup_sequence(void)"
    module: ui/startup_sequence
    status: ported
    renames:
      thunk_FUN_080037a0: setup_phase_one
      thunk_FUN_080eade0: setup_phase_two
      FUN_08007470: event_handler_source
      FUN_080076cc: dispatch_first_event
      uVar1/r0: source
    notes: >-
      Calls the 0x080037a0 veneer (literal target 0x080ea68c), then the
      0x080037c8 veneer (literal target 0x080eade0), retrieves the event
      handler source from 0x08007470, and calls 0x080076cc with that source
      in r0 and event 1 in r1. The latter's result is discarded and this
      wrapper returns 0. The neighboring 0x08005690 wrapper has the same
      orchestration but a different second setup veneer and event 0, which
      identifies this as the event-one UI startup sequence. Unported calls
      stay behind a volatile UI target/host ROM-dispatch seam; target defaults
      call their retailOS addresses and direct host tests prove call order,
      arguments, and the fixed zero return.


  # --- no-argument callback veneer ---
  - address: 0x08003790
    ghidra: FUN_08003790
    name: dispatch_no_argument_callback
    size: 16
    sig: "void dispatch_no_argument_callback(void)"
    module: kernel/thunks
    status: ported
    renames:
      DAT_08003794: no_argument_callback_dispatch_target
      FUN_081b0d08: no_argument_callback_tail_entry
      FUN_080054a8: media_event_completion_handler
    notes: >-
      The first 8 bytes are a literal veneer: `ldr pc, [pc, #-4]` with
      target 0x081b0d08; the following 8 bytes at 0x08003798 are a distinct
      adjacent veneer and are not part of this port. The sole recovered
      caller is `bl 0x08003790` at 0x08005524, after passing record+0x28 to
      the preceding 0x08003720 veneer; it requires only call-and-return
      behavior. The target is the tail entry after FUN_081b0cf4: it restores
      that callback's saved registers, branches to 0x081c8658, and writes
      the preserved callback words into the process-global context. Thus r0
      through r3 have no defined ABI at this entry and its return value is
      ignored. The Rust target implementation keeps the exact `ldr pc`
      veneer; the host seam calls an injected `unsafe extern "C" fn()`,
      whose direct test proves a single call and ordinary return.
  # --- event callback dispatch ---
  - address: 0x08005094
    ghidra: FUN_08005094
    name: dispatch_event_callback
    size: 12
    sig: "void dispatch_event_callback(SystemEventContext *system_context)"
    module: kernel/thunks
    status: ported
    renames:
      param_1/r0: system_context
      param_1_plus_8: callback_context
      DAT_0800370c: event_callback_dispatch_target
    notes: >-
      Raw ARM is `add r0, r0, #8; b 0x08003708`; the 0x08003708 literal
      veneer is `ldr pc, [pc, #-4]` with target 0x0815c8a0. This tail
      dispatch forwards the eight-byte callback subobject as the sole r0
      argument to the unported virtual callback dispatcher and returns void.
      The Rust port preserves the ABI through the target/host operation seam;
      its ARM default is the same literal veneer target.

  # --- callback-target getter veneer ---
  - address: 0x08003910
    ghidra: thunk_FUN_0818c740
    name: callback_target_getter
    size: 8
    sig: "void *callback_target_getter(void)"
    module: kernel/thunks
    status: ported
    renames:
      thunk_FUN_0818c740: callback_target_getter
      DAT_08003914: callback_target_getter_target
    notes: >-
      Raw bytes are 04 f0 1f e5 40 c7 18 08, i.e. the literal veneer
      `ldr pc, [pc, #-4]` plus target word 0x0818c740; Ghidra's 4-byte
      extent drops the literal, and the next veneer starts at 0x08003918.
      Loading PC preserves every register including LR, so the target
      returns straight to this veneer's caller and the port is the verbatim
      instruction and literal on ARM. All 21 B/BL words decoded in osos.dec
      that reach 0x08003910 are plain unconditional `bl` - zero predicated
      forms and zero tail `b` - and every one of them immediately executes
      `ldr r1, [r0]` with no NULL guard, so the getter cannot return NULL.
      The nine distinct vtable slots those sites dispatch are +0x00, +0x04,
      +0x08, +0x0c, +0x10, +0x14, +0x1c, +0x20 and +0x30.
      The veneer sits inside the 0xaed8-byte block the relocator at
      0x080046e0 mirrors to IRAM, so on device it is entered as 0x22003910
      just as often as 0x08003910; both name the same 8 bytes.
      Addressing note that this entry had to establish - the relocator
      copies 0xaed8 bytes to 0x22000000 and only then moves the retailOS
      image down to 0x08000000 (memmove to 0x08000000 of 0xa0fc88 bytes from
      image base plus 0xaed8, then 0xa84 bytes to 0x08a0fc88), so a retailOS
      runtime address A has its bytes at osos.dec file address A plus 0xaed8.
      Read at face value the literal 0x0818c740 lands on a mid-function
      `pop {r3, r4, r5, r6, r7, pc}`, which is why earlier entries described
      it as an unliftable return edge; at file address 0x08197618 it is a
      clean function entry. The same plus-0xaed8 correction makes 118 of the
      188 literal veneers in this block resolve onto function entries, and it
      makes the vtable at retailOS 0x089a37e0 resolve to 12 of 13 real entry
      points.
      That target is a mode-selected singleton accessor over a two-byte
      record at retailOS 0x089cfd28 - byte 0 a one-shot initialization flag,
      byte 1 the selected mode. On the first call it sets byte 0, calls the
      two guarded-static constructors at retailOS 0x081e9784 and 0x081db84c
      and invokes vtable slot 0 on each returned object, then tail-calls the
      accessor byte 1 selects (nonzero picks 0x081db84c, zero picks
      0x081e9784) and returns that object. Sibling veneers in the same table
      carry the mode - 0x08003908 returns 3 when byte 1 is set and 0
      otherwise, 0x08003918 sets it to (argument == 3) - and the singletons
      themselves live at fixed retailOS addresses 0x08ae48e8 and 0x08ae494c.
      Neither accessor is ported, so the ARM build keeps the retail literal
      and the host build resolves CALLBACK_TARGET_GETTER_OPS through a
      read_volatile seam that defaults to NULL. The three already-ported
      wrappers 0x08007788, 0x080077a8 and 0x08007bd4 keep their own inline
      copies of this veneer; they are unchanged by this entry.

  # --- callback-target virtual dispatch ---
  - address: 0x08007788
    ghidra: FUN_08007788
    name: callback_target_dispatch
    size: 32
    sig: "void callback_target_dispatch(void *unused, void *callback)"
    module: app/callback_target_dispatch
    status: ported
    renames:
      param_1/r0: unused
      param_2/r1: callback
      piVar1: callback_target
      thunk_FUN_0818c740: callback_target_getter
    notes: >-
      Raw ARM preserves r1 across the 0x08003910 literal veneer (target
      0x0818c740), loads the returned target's vtable and tail-dispatches its
      +0x1c slot with r0=target and r1=callback. r0 on entry is ignored. The
      getter returns the same callback target used by neighboring wrappers
      0x080076d4, 0x080077a8, and 0x08007bd4; its setup path is unported, so
      the ARM port retains the literal veneer and host tests use a volatile
      getter seam to exercise the exact virtual callback ABI.

  - address: 0x080077a8
    ghidra: FUN_080077a8
    name: callback_target_slot_14_dispatch
    size: 40
    sig: "void callback_target_slot_14_dispatch(void *unused, void *arg2, void *arg3)"
    module: app/callback_target_slot_14_dispatch
    status: ported
    renames:
      param_1/r0: unused
      param_2/r1: arg2
      param_3/r2: arg3
      piVar1: callback_target
      thunk_FUN_0818c740: callback_target_getter
    notes: >-
      Raw ARM preserves r1 and r2 across the 0x08003910 literal veneer
      (target 0x0818c740), loads the returned target's vtable, and
      tail-dispatches its +0x14 slot with r0=target, r1=arg2, and r2=arg3.
      r0 on entry is ignored. The getter is an unported framework setup edge
      shared with neighboring wrappers 0x080076d4, 0x08007788, and
      0x08007bd4; the ARM port retains the literal veneer and direct host
      tests install a volatile getter seam to exercise the exact virtual
      callback ABI.

  - address: 0x08007bd4
    ghidra: FUN_08007bd4
    name: callback_target_slot_20_dispatch
    size: 32
    sig: "void callback_target_slot_20_dispatch(void *unused, void *callback)"
    module: app/callback_target_slot_20_dispatch
    status: ported
    renames:
      param_1/r0: unused
      param_2/r1: callback
      piVar1: callback_target
      thunk_FUN_0818c740: callback_target_getter
    notes: >-
      Raw ARM preserves r1 across the 0x08003910 literal veneer (target
      0x0818c740), loads the returned target's vtable and tail-dispatches its
      +0x20 slot with r0=target and r1=callback. r0 on entry is ignored. The
      callback-target getter is an unported framework setup edge shared with
      neighboring wrappers 0x080076d4, 0x08007788, and 0x080077a8; the ARM
      port retains the literal veneer and direct host tests install a volatile
      getter seam to exercise the exact virtual callback ABI.

  # --- stream-buffer context initialization ---
  - address: 0x08005e5c
    ghidra: FUN_08005e5c
    name: stream_buffer_initialize_and_get_page_context
    size: 28
    sig: "u32 stream_buffer_initialize_and_get_page_context(u32 unused_first, u32 unused_second, u32 *page_context_out)"
    module: drivers/stream_buffer_context
    status: ported
    renames:
      param_1/r0: unused_first
      param_2/r1: unused_second
      param_3/r2: page_context_out
      FUN_08006e88: initialize_stream_buffer
      FUN_0800722c: current_page_context
      uVar1/r0: page_context
    notes: >-
      Calls the lazy retailOS stream-buffer initializer at 0x08006e88, then
      obtains the current page or fallback context through 0x0800722c and
      stores it through r2. The raw ARM saves r2 in r4 across both calls and
      returns literal zero; r0 and r1 are never read. Both callees remain
      target/host ROM seams, with a host recorder proving strict call order,
      output-store value, and fixed-zero return.

  # --- event-handler source lazy initialization ---
  - address: 0x08007470
    ghidra: FUN_08007470
    name: event_handler_source
    size: 100
    sig: "void *event_handler_source(void)"
    module: kernel/event_handler_source
    status: ported
    renames:
      DAT_080074d4: event_handler_source_guard
      DAT_080074d8: event_handler_source_instance
      DAT_080074dc: dso_handle
      DAT_080074e0: event_handler_source_destructor
      DAT_080074e4: event_handler_source_initialized
      FUN_080036e0: cxa_guard_acquire_veneer
      FUN_080036e8: cxa_atexit_veneer
      FUN_080036f0: cxa_guard_release_veneer
      FUN_08007e38: event_handler_source_setup
    notes: >-
      Raw ARM is 100 bytes ending with pop {r4,pc} at 0x080074d0 plus the
      five-word literal pool at 0x080074d4..0x080074e4; the independently
      linked leaf at 0x080074e8 confirms the boundary. Decoding every ARM
      B/BL word in osos.dec found 10 direct callers: all are unconditional
      bl, with zero predicated forms and zero tail b. It tests bit 0 of
      guard 0x22008c84, registers fixed instance 0x22010318 with
      cxa_atexit(instance, 0x2200803c, 0x089ca09c) after successful acquire,
      and releases the guard. Independently, it calls the sole-caller,
      still-unidentified FUN_08007e38(instance) under byte flag 0x2200aed4,
      then returns the instance on every path. The port calls the existing
      cxa guard and shutdown-chain routines directly; FUN_08007e38 remains a
      target-only seam, while host tests use private fixed-state backing and a
      recording setup seam. The relocator at 0x080046e0 mirrors this body to
      0x22007470, and the former literal thunk 0x08038060 now forwards to
      this port.
  # --- event-loop callback dispatch ---
  - address: 0x080074e8
    ghidra: FUN_080074e8
    name: dispatch_event_loop_callback
    size: 12
    sig: "u32 dispatch_event_loop_callback(const EventLoopCallbackSource *source)"
    module: drivers/event_loop_callback
    status: ported
    renames:
      param_1/r0: source
      param_1_plus_0x20: callback
      DAT_08003904: event_loop_callback_dispatch_target
    notes: >-
      Raw ARM is `ldr r0, [r0, #0x20]; b 0x08003900`; the 0x08003900 literal
      veneer is `ldr pc, [pc, #-4]` with target 0x0818c528. This tail
      dispatch forwards source +0x20 as the sole r0 argument and preserves
      the target's r0 result. Recovered event-loop callers treat a zero result
      as not-ready and retry. The Rust port preserves the ARM wrapper and
      target literal on ARM, with a volatile target/host operation seam for
      direct host behavioral tests.

  # --- first event-handler dispatch ---
  - address: 0x080076cc
    ghidra: FUN_080076cc
    name: dispatch_first_event_handler
    size: 8
    sig: "u32 dispatch_first_event_handler(const EventHandlerSource *source, u32 event)"
    module: heap/first_event_dispatch
    status: ported
    renames:
      param_1: source
      DAT_08003934: first_event_handler_veneer_target
    notes: >-
      Loads source + 0x20 into r0 and branches (not bl) to the 0x08003930
      veneer, preserving r1's event word and forwarding the target's r0
      result unchanged. The veneer literal enters unported retailOS code,
      represented by FIRST_EVENT_DISPATCH_OPS with a zero-result default.

  - address: 0x08003150
    ghidra: FUN_08003150
    name: sctlr_enable_instruction_cache
    size: 20
    sig: "u32 sctlr_enable_instruction_cache(void)"
    module: drivers/cp15
    status: ported
    vars: {uVar1: sctlr}
    notes: Reads ARM926EJ-S CP15 c1 (SCTLR) with MRC p15,0,<Rt>,c1,c0,0,
      sets instruction-cache-enable bit 12 (0x1000), writes the result with
      the paired MCR, and returns the stored control word. Host builds use a
      replaceable deterministic CP15 seam.

  - address: 0x08003164
    ghidra: FUN_08003164
    name: sctlr_enable_data_cache
    size: 20
    sig: "u32 sctlr_enable_data_cache(void)"
    module: drivers/cp15
    status: ported
    vars: {uVar1: sctlr}

    notes: Reads ARM926EJ-S CP15 c1 (SCTLR) with MRC p15,0,<Rt>,c1,c0,0,
      sets data-cache-enable bit 2 (0x4), writes the result with the paired
      MCR, and returns the stored control word. Host builds use the shared
      replaceable deterministic CP15 seam.

  # --- opaque FreeType-adjacent metadata halfword accessor ---
  - address: 0x0805128c
    ghidra: FUN_0805128c
    name: ft_metadata_u16_at_b26
    size: 16
    sig: "u16 ft_metadata_u16_at_b26(const FtMetadataOwner *object)"
    module: ft/metadata
    status: ported
    vars:
      param_1: object
      r0_after_ldr: metadata
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; add r0,r0,#0xb00; ldrh
      r0,[r0,#0x26]; bx lr`, hence an aligned metadata pointer word at
      object+0xf00 and an unsigned, zero-extended u16 at metadata+0xb26.
      Ghidra's C agrees exactly. The concrete owner and metadata types and
      ownership are not recovered; the port deliberately retains the raw
      dereferences with no NULL or bounds checks.

  # --- opaque FreeType service-context metadata halfword accessor ---
  - address: 0x0805129c
    ghidra: FUN_0805129c
    name: ft_service_metadata_u16_at_b1c
    size: 16
    sig: "u16 ft_service_metadata_u16_at_b1c(const void *service_context)"
    module: ft/service/metadata
    status: ported
    vars:
      param_1: service_context
      r0_after_ldr: metadata
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; add r0,r0,#0xb00; ldrh
      r0,[r0,#0x1c]; bx lr`, hence an aligned metadata pointer word at
      service_context+0xf00 and an unsigned, zero-extended u16 at
      metadata+0xb1c. Ghidra's C agrees exactly. Direct call sites cache
      this value in a u16 UI field and compare it with the nested metadata
      halfword; one wrapper updates the same halfword and its +0xb90 dirty
      byte. The concrete layouts and ownership are not recovered, so the
      port deliberately retains raw dereferences with no NULL or bounds
      checks.

  # --- opaque FreeType service-context metadata byte accessor ---
  - address: 0x080512ac
    ghidra: FUN_080512ac
    name: ft_service_metadata_byte_at_b50
    size: 12
    sig: "u32 ft_service_metadata_byte_at_b50(const void *service_context)"
    module: ft/service/metadata
    status: ported
    vars:
      param_1: service_context
      r0_after_ldr: metadata
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; ldrb r0,[r0,#0xb50]; bx lr`, hence an
      aligned metadata pointer word at service_context+0xf00 and an unsigned,
      zero-extended byte at metadata+0xb50. Ghidra's C agrees exactly. The
      sole recovered direct caller reads this byte from its +0xa8
      service-context field, compares it against a cached byte at +0x58, and
      re-reads it to update that cache before notifying its vtable callback.
      The concrete layouts and ownership are not recovered, so the port
      deliberately retains raw dereferences with no NULL or bounds checks.

  # --- opaque FreeType service-context metadata byte accessor ---
  - address: 0x080512b8
    ghidra: FUN_080512b8
    name: ft_service_metadata_byte_at_b89
    size: 12
    sig: "u32 ft_service_metadata_byte_at_b89(const void *service_context)"
    module: ft/service/metadata
    status: ported
    vars:
      param_1: service_context
      r0_after_ldr: metadata
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; ldrb r0,[r0,#0xb89]; bx lr`, hence an
      aligned metadata pointer word at service_context+0xf00 and an unsigned,
      zero-extended byte at metadata+0xb89. Ghidra's C agrees exactly. Its
      recovered direct caller reads this byte from its +0xa8 service-context
      field, compares it against a cached byte at +0x8d, and re-reads it to
      update that cache before notifying its vtable callback. The concrete
      layouts and ownership are not recovered, so the port deliberately
      retains raw dereferences with no NULL or bounds checks.

  # --- opaque FreeType service-context metadata pointer accessor ---
  - address: 0x080512c4
    ghidra: FUN_080512c4
    name: ft_service_metadata_pointer
    size: 8
    sig: "const void *ft_service_metadata_pointer(const void *service_context)"
    module: ft/service/metadata
    status: ported
    vars:
      param_1: service_context
      r0_after_ldr: metadata_pointer
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; bx lr`, hence this returns the aligned
      metadata pointer word at service_context+0xf00 without dereferencing it.
      Ghidra's C agrees exactly. The recovered direct caller obtains this
      pointer from the value it had tested nonzero, then updates the u64
      counter at metadata+0xc30/+0xc34. The concrete layouts and ownership
      are not recovered; the port deliberately retains the raw load with no
      NULL or bounds checks.

  # --- retailOS one-based indexed object element ---
  - address: 0x080512cc
    ghidra: FUN_080512cc
    name: indexed_object_element
    size: 92
    sig: "void *indexed_object_element(const IndexedObject *object, u32 index)"
    module: ui/object_state
    status: ported
    renames:
      param_1: indexed_object
      param_2: index
      iVar1: storage_base
      param_1[5]: storage_ready
      DAT_08051328: indexed_object_tag
      FUN_080aa828: indexed_object_storage_base
    notes: >-
      Raw ARM is 0x080512cc..0x08051324 followed by the 0x61726179 tag
      literal at 0x08051328; 0x0805132c begins the next function. It rejects
      a mismatched tag, zero index, an unsigned index greater than word +8,
      or a zero word +20 storage_ready gate; it then directly calls
      0x080aa828 and returns storage_base + word(+4) * (index - 1). Decoding
      every ARM B/BL word in osos.dec finds 11 direct BL call sites, all
      unconditional, and no B tail calls. This is a
      separately linked, register-allocation-different implementation of
      indexed_object_offset at 0x08055ed0.


  # --- opaque FreeType service metadata flag-word accessor ---
  - address: 0x080514e0
    ghidra: FUN_080514e0
    name: ft_service_metadata_flags_at_b00
    size: 12
    sig: "u32 ft_service_metadata_flags_at_b00(const void *object)"
    module: ft/service_metadata
    status: ported
    vars:
      param_1: object
      r0_after_ldr: metadata
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; ldr r0,[r0,#0xb00]; bx lr`, hence
      aligned pointer and u32 word loads at object+0xf00 and metadata+0xb00.
      Ghidra's C agrees exactly. Recovered direct callers use bits 0 and 1
      independently as service state flags, but the accessor itself returns
      the full unmodified u32. The concrete layouts and ownership are not
      recovered, so the port deliberately retains the raw dereferences with
      no NULL or bounds checks.

  # --- opaque FreeType service metadata word accessor ---
  - address: 0x080514fc
    ghidra: FUN_080514fc
    name: ft_service_metadata_word_at_af4
    size: 12
    sig: "u32 ft_service_metadata_word_at_af4(const void *object)"
    module: ft/service_metadata
    status: ported
    vars:
      param_1: object
      r0_after_ldr: metadata
    notes: >-
      Raw ARM is `ldr r0,[r0,#0xf00]; ldr r0,[r0,#0xaf4]; bx lr`, hence
      aligned pointer and u32 word loads at object+0xf00 and metadata+0xaf4.
      Ghidra's C agrees exactly. The concrete layouts and ownership are not
      recovered, so the port deliberately retains the raw dereferences with
      no NULL or bounds checks.
  # --- retailOS UI object backend resolver (kind-1 self / kind-2 proxy) ---
  - address: 0x08051dc4
    ghidra: FUN_08051dc4
    name: object_backend_for_kind
    size: 28
    sig: "const void *object_backend_for_kind(const void *object)"
    module: ui/object_state
    status: ported
    vars: {param_1: object, r1: kind}
    notes: >-
      Tag-dispatching accessor: `ldrb r1,[r0]; cmp r1,#1; bxeq lr; cmp
      r1,#2; ldreq r0,[r0,#0xefc]; movne r0,#0; bx lr`. Kind byte at
      object+0x00; kind 1 returns the object itself, kind 2 returns the
      pointer field at +0xefc, anything else returns NULL. No null guard,
      and the +0xefc field is read only for kind 2. Recovered class:
      the same UI object family ui/object_state.rs already serves — the
      mode dispatcher 0x0808cec4 reads the state word at +0xe38 (the
      word object_state_word @ 0x08055e80 exposes) on the very pointer
      it passes here. The kind-1 backend is the 0xfa4-byte object
      constructed by 0x08058590 (kind byte planted via base ctor
      0x080dae7c, instance installed at the app root object's +0x30 by
      0x08114c40); the kind-2 proxy is the 0xf00-byte object built by
      0x080586b8, storing the backend pointer at +0xefc — factory
      0x0813e474 hands out such a proxy for its mode 2. Call-site
      census: 49 `bl 0x08051dc4` sites in decomp/osos.asm, clustered in
      the 0x08052xxx..0x08055xxx helpers (20 sites), the
      0x08066xxx/0x08067xxx selection routines (6), the 0x0808/0x0809
      mode dispatchers (5), the 0x0813xxxx facade class whose +0x40
      field carries the argument (11), plus singletons at 0x0805cd54,
      0x080866b0, 0x080875a8, 0x0808cf04, 0x080ca398, 0x080d1a9c and
      0x08046xxx. Host tests cover kinds 0/1/2/255 plus other unknown
      kinds and prove the +0xefc read happens only for kind 2 (one-byte
      fixtures for non-proxy kinds). match.py structural (exit 1
      expected): identical dispatch shape — kind-byte load, cmp #1
      early-return self, cmp #2, ldreq from +0xefc, else 0; diffs are
      only the crate frame-pointer prologue/epilogue (popeq {fp,pc} for
      bxeq lr), the same pattern as every leaf in this module.

  # --- retailOS state transition notifier ---
  - address: 0x080050fc
    ghidra: FUN_080050fc
    name: set_state_and_notify
    size: 28
    sig: "void set_state_and_notify(StateRecord *record, u8 new_state)"
    module: runtime/state_transition
    status: ported
    renames:
      param_1: record
      param_2: new_state
      DAT_0800371c: state_notification_callback
    notes: >-
      Reads record+0x20 as current_state. Unless it is the special value 2,
      copies it to previous_state at +0x21; writes new_state to +0x20; then
      tail-calls the 0x0800371c literal veneer with the context word at
      record+0x1c. The veneer resolves to runtime-installed callback
      0x080ea6ec. The target default calls that callback directly; host tests
      install a recording callback and cover both prior-state branch cases and
      the exact context argument.

  - address: 0x08005320
    ghidra: FUN_08005320
    name: finalize_state_and_dispatch
    size: 40
    sig: "noreturn finalize_state_and_dispatch(StateRecord *record)"
    module: runtime/state_finalize
    status: ported
    renames:
      param_1: record
      "record+0x20": current_state
      "record+0x21": deferred_state
      thunk_FUN_082aad24: terminal_state_callback
    notes: >-
      Reads current_state at record+0x20. When it is 2, calls the ported
      set_state_and_notify @ 0x080050fc with deferred_state from record+0x21,
      then reloads and terminally dispatches the resulting current_state through
      thunk 0x082aad24. Direct target dispatch uses STATE_FINALIZE_OPS so host
      tests can record the diverging callback; target builds call the ROM thunk.

  # --- retailOS state wait ---
  - address: 0x08055e8c
    ghidra: FUN_08055e8c
    name: wait_for_nonzero_state
    size: 40
    sig: "int wait_for_nonzero_state(void)"
    module: runtime/state_wait
    status: ported
    renames:
      DAT_08055eb4: state_word
      thunk_FUN_080568e8: wait_or_yield
    notes: >-
      Literal 0x08055eb4 holds runtime state word 0x089ca8bc. Repeatedly
      reads that word and, while zero, calls the 0x080e9eb0 veneer with 2;
      that veneer dispatches to FUN_080568e8's nonzero-argument wait/yield
      path. Returns the first nonzero state word unchanged.

  # --- retailOS UI sequence identifier ---
  - address: 0x08055eb8
    ghidra: FUN_08055eb8
    name: sequence_id_next
    size: 16
    sig: "u32 sequence_id_next(void)"
    module: ui/object_state
    status: ported
    renames:
      DAT_08055ecc: sequence_id_global
    notes: >-
      The 0x08055ecc literal is 0x089cfcc4, a runtime sequence-counter
      word. The leaf loads that word into r0, stores its wrapping increment,
      and returns the original value. All five recovered call sites store
      the return value as the new record's identifier.

  # --- retailOS one-based indexed object offset ---
  - address: 0x08055ed0
    ghidra: FUN_08055ed0
    name: indexed_object_offset
    size: 80
    sig: "void *indexed_object_offset(const IndexedObject *object, u32 index)"
    module: ui/object_state
    status: ported
    renames:
      param_1: indexed_object
      param_2: index
      iVar1: storage_base
      DAT_08055f20: indexed_object_tag
      FUN_080aa828: indexed_object_storage_base
    notes: >-
      The literal at 0x08055f20 is 0x61726179. The helper returns NULL
      unless word +0 equals that tag, index is nonzero, and index is no
      greater than unsigned word +8. It then calls 0x080aa828 for the
      storage base and returns base + word(+4) * (index - 1). Callers at
      0x08066bb8, 0x080e2d70, and 0x080e2dc8 use it as a one-based
      fixed-record accessor; 0x080aa828 verifies the same tag and reads
      its storage word at +24.

  # --- retailOS indexed-object storage base ---
  - address: 0x080aa828
    ghidra: FUN_080aa828
    name: indexed_object_storage_base
    size: 28
    sig: "void *indexed_object_storage_base(const IndexedObject *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: indexed_object
      param_1[6]: storage_pointer_slot
      uVar1: storage_base
      DAT_080aa844: indexed_object_tag
    notes: >-
      Reference C and raw ARM agree: compare word +0 to literal
      0x61726179, then only on equality load the pointer slot at +0x18 and
      return its stored base pointer; otherwise return NULL. The literal's
      raw osos.dec word is 0x61726179.

  - address: 0x08003178
    ghidra: FUN_08003178
    name: sctlr_disable_mmu
    size: 20
    sig: "u32 sctlr_disable_mmu(void)"
    module: drivers/cp15
    status: ported
    vars: {uVar1: sctlr}
    notes: Reads ARM926EJ-S CP15 c1 (SCTLR) with MRC p15,0,<Rt>,c1,c0,0,
      clears the architectural M bit (MMU enable, bit 0/0x1), writes the
      result with the paired MCR, and returns the stored control word. Host
      builds use the shared replaceable deterministic CP15 seam.

  - address: 0x0800318c
    ghidra: FUN_0800318c
    name: sctlr_disable_instruction_cache
    size: 20
    sig: "u32 sctlr_disable_instruction_cache(void)"
    module: drivers/cp15
    status: ported
    vars: {uVar1: sctlr}
    notes: Reads ARM926EJ-S CP15 c1 (SCTLR) with MRC p15,0,<Rt>,c1,c0,0,
      clears instruction-cache-enable bit 12 (0x1000), writes the result with
      the paired MCR, and returns the stored control word. Host builds use the
      shared replaceable deterministic CP15 seam.

  - address: 0x080031a4
    ghidra: FUN_080031a4
    name: sctlr_disable_data_cache
    size: 20
    sig: "u32 sctlr_disable_data_cache(void)"
    module: drivers/cp15
    status: ported
    vars: {uVar1: sctlr}
    notes: Reads ARM926EJ-S CP15 c1 (SCTLR) with MRC p15,0,<Rt>,c1,c0,0,
      clears data-cache-enable bit 2 (0x4), writes the result with the paired
      MCR, and returns the stored control word. Host builds use the shared
      replaceable deterministic CP15 seam.

  - address: 0x080031b8
    ghidra: FUN_080031b8
    name: invalidate_entire_instruction_cache
    size: 20
    sig: "u32 invalidate_entire_instruction_cache(void)"
    module: drivers/cp15
    status: ported
    vars: {}
    notes: Sends zero through ARM926EJ-S CP15 MCR p15,0,r0,c7,c5,0
      (Invalidate Entire Instruction Cache, 0xee070f15), followed in order by
      MCR p15,0,r0,c7,c10,4 (ARMv5 Data Synchronization Barrier,
      0xee070f9a), then returns zero. Host builds use the shared replaceable
      deterministic CP15 cache-maintenance seam.

  - address: 0x080031cc
    ghidra: FUN_080031cc
    name: invalidate_entire_data_cache_and_drain_write_buffer
    size: 20
    sig: "u32 invalidate_entire_data_cache_and_drain_write_buffer(void)"
    module: drivers/cp15
    status: ported
    vars: {}
    notes: Sends zero through ARM926EJ-S CP15 MCR p15,0,r0,c7,c6,0
      (Invalidate Entire Data Cache, 0xee070f16), followed in order by MCR
      p15,0,r0,c7,c10,4 (Drain Write Buffer, 0xee070f9a), then returns zero.
      Ghidra calls the latter Data Synchronization; ARM926EJ-S documents this
      encoding as draining the write buffer. Host builds use the shared
      replaceable deterministic CP15 cache-maintenance seam.

  - address: 0x08005cf4
    ghidra: FUN_08005cf4
    name: heap_object_destroy_and_dispatch
    size: 36
    sig: "void *heap_object_destroy_and_dispatch(void *object)"
    module: heap/object_destroy_dispatch
    status: ported
    vars:
      param_1/r0: object
      iVar1/r0: embedded_result
      thunk_FUN_081b0f0c: embedded_object_destroy
      FUN_08005ffc: empty_base_destroy
      FUN_08003818: kernel_indirect_dispatch
    notes: >-
      Calls the 0x081b0f0c virtual destructor on object +0x28, rebases its
      r0 result by -0x18 through the empty 0x08005ffc base destructor, then
      passes the result -8 into the literal tail veneer 0x08003818. On ARM
      that veneer enters 0x0815ca7c, which pops this wrapper's {r4,lr} frame
      and tail-branches, so the apparent final result -8 is unreachable in
      retail execution. The device port preserves that sequence in ARM
      assembly; the host seam exposes Ghidra's normal-return interpretation
      solely to verify all three offsets and call order.

  - address: 0x08005d18
    ghidra: FUN_08005d18
    name: decoder_end_batch
    size: 16
    sig: "uintptr_t decoder_end_batch(void **decoder_slot)"
    module: cxx/decoder_end_batch
    status: ported
    vars: {param_1: decoder_slot}
    notes: Media decoder lifecycle wrapper. Dereferences decoder_slot to its
      object, loads that object's vtable, and tail-dispatches slot +0x3c;
      no NULL checks are present. Its only direct caller invokes it after
      processing a decoder batch and before the paired decoder teardown.

  - address: 0x08005e4c
    ghidra: FUN_08005e4c
    name: pending_event_dispatch
    size: 16
    sig: "void pending_event_dispatch(void **event_slot)"
    module: cxx/pending_event
    status: ported
    vars: {param_1: event_slot}
    notes: Pending-event virtual forwarding wrapper. Dereferences event_slot to
      its object, loads that object's vtable, and tail-dispatches slot +0x20
      with the object preserved in r0. No NULL checks are present; the
      terminal virtual method has the Ghidra-recovered void signature.

  - address: 0x08005e88
    ghidra: FUN_08005e88
    name: media_operation_status_set
    size: 12
    sig: "void media_operation_status_set(u8 status)"
    module: cxx/mode
    status: ported
    vars: {param_1: status, DAT_08005e94: media_operation_status}
    notes: Stores status as one byte to the media-operation status global at
      0x22008ca0 (the literal pool word at 0x08005e94). The media operation
      completion path sets status 5 after a callback reports completion; the
      media metadata-load failure path sets status 3 before error notification.
      The port preserves the unconditional, non-atomic byte store and uses a
      private host byte stand-in.

  - address: 0x08005e04
    ghidra: FUN_08005e04
    name: decoder_begin_batch
    size: 16
    sig: "uintptr_t decoder_begin_batch(void **decoder_slot)"
    module: cxx/decoder_begin_batch
    status: ported
    vars: {param_1: decoder_slot}
    notes: Media decoder lifecycle wrapper. Dereferences decoder_slot to its
      object, loads that object's vtable, and tail-dispatches slot +0x30 with
      the object preserved in r0. The only direct caller first ensures decoder
      activation, invokes this before processing, then invokes decoder_end_batch
      at +0x3c. No NULL checks are present; despite Ghidra's void signature,
      the tail branch preserves the virtual method's r0 return value.


  - address: 0x08005ea8
    ghidra: FUN_08005ea8
    name: decoder_poll
    size: 16
    sig: "uintptr_t decoder_poll(void **decoder_slot)"
    module: cxx/vtable
    status: ported
    vars: {param_1: decoder_slot}
    notes: Media decoder readiness/status poll wrapper. Dereferences decoder_slot
      to its object, loads that object's vtable, and tail-dispatches slot +0x1c
      with the object preserved in r0. The recovered event-loop caller drains
      queued work, then repeatedly invokes this until its nonzero result signals
      that processing may continue. No NULL checks are present; despite Ghidra's
      void signature, the tail branch preserves the virtual method's r0 return
      value.


  - address: 0x08005ef0
    ghidra: FUN_08005ef0
    name: decoder_cleanup
    size: 16
    sig: "uintptr_t decoder_cleanup(void **decoder_slot)"
    module: cxx/decoder_cleanup
    status: ported
    vars: {param_1: decoder_slot}
    notes: Media decoder cleanup lifecycle wrapper. Dereferences decoder_slot
      to its object, loads that object's vtable, and tail-dispatches slot
      +0x0c with the object preserved in r0. Its two direct callers invoke it
      only while their pending-cleanup flag is set, then notify UI and clear
      that flag. No NULL checks are present; despite Ghidra's void signature,
      the tail branch preserves the virtual method's r0 return value.

  - address: 0x08005d28
    ghidra: FUN_08005d28
    name: ui_dispatch_pending_cleanup
    size: 52
    sig: "int ui_dispatch_pending_cleanup(void *cleanup_slot)"
    module: ui/pending_cleanup
    status: ported
    vars: {param_1: cleanup_slot, DAT_08005d5c: ui_notification_context}
    notes: Pending UI decoder-cleanup wrapper. The target slot has its decoder
      pointer at +0x00 and active-cleanup byte at +0x06; when that byte is
      nonzero it invokes the slot's unported vtable +0x0c dispatcher, then
      calls the second unported UI notification target with 0x22008cb8,
      clears the byte, and returns zero. A clear byte performs no call/store.

  - address: 0x08005f00
    ghidra: FUN_08005f00
    name: decoder_initialize
    size: 16
    sig: "uintptr_t decoder_initialize(void **decoder_slot)"
    module: stdio/decoder_initialize
    status: ported
    vars: {param_1: decoder_slot}
    notes: >-
      Media decoder initialization lifecycle wrapper. Dereferences
      decoder_slot to its object, loads that object's vtable, and tail-dispatches
      slot +0x08 with the object preserved in r0. The recovered callers invoke
      it only when a decoder-owner initialization byte at +0x06 is clear, then
      set that byte after the dispatch; no NULL checks are present. Despite
      Ghidra's void signature, the tail branch preserves the virtual method's
      r0 return value. Target vtable cells are ARM words; the direct host-test
      seam uses pointer-width cells for native callbacks. match.py structural:
      double dereference, slot +0x08 load, indirect return-forwarding branch.

  - address: 0x08005f10
    ghidra: FUN_08005f10
    name: ui_operation_unavailable
    size: 8
    sig: "int ui_operation_unavailable(void)"
    module: ui/operation_unavailable
    status: ported
    vars: {}
    notes: >-
      Stateless UI leaf. `mvn r0, #0; bx lr` returns the signed -1
      unavailable/error sentinel without reading an argument or memory.

  - address: 0x0800027c
    ghidra: FUN_0800027c
    name: memzero_aligned
    size: 88
    sig: "void *memzero_aligned(void *dst, size_t len)"
    module: memzero
    status: ported
    vars: {param_1: dst, param_2: len}
    notes: Zero-fill block loop, entry expects aligned dst.

  - address: 0x080002d4
    ghidra: FUN_080002d4
    name: memzero
    size: 68
    sig: "void *memzero(void *dst, size_t len)"
    module: memzero
    status: ported
    vars: {param_1: dst, param_2: len}
    notes: Zero-fill with len<4 + misalignment prologue, then shared block loop.

  - address: 0x08001e70
    ghidra: FUN_08001e70
    name: cpsr_disable_irq_fiq
    size: 20
    sig: "u32 cpsr_disable_irq_fiq(void)"
    module: drivers/interrupts
    status: ported
    vars: {r0: saved_if_mask, r1: cpsr}
    notes: Snapshots CPSR, returns only the prior IRQ/FIQ I/F mask (0xc0),
      then sets both I/F bits with the MRS/ORR/MSR cpsr_c sequence. Its caller
      pwrcon_update_masks uses it as an atomic guard and discards the returned
      mask because the paired 0x08001e84 helper restores directly afterward.

  - address: 0x08001e84
    ghidra: FUN_08001e84
    name: cpsr_restore_irq_fiq
    size: 20
    sig: "void cpsr_restore_irq_fiq(u32 saved_if_mask)"
    module: drivers/interrupts
    status: ported
    vars: {r0: saved_if_mask, r1: cpsr_without_if}
    notes: Reads CPSR, clears IRQ/FIQ I/F bits (0xc0), ORs the supplied saved
      I/F mask, and writes CPSR control with the MRS/BIC/ORR/MSR sequence.

  - address: 0x08001e98
    ghidra: FUN_08001e98
    name: cpsr_irq_enabled
    size: 16
    sig: "u32 cpsr_irq_enabled(void)"
    module: drivers/interrupts
    status: ported
    vars: {r0: irq_enabled, r1: one}
    notes: Reads CPSR and computes 1 & ~(cpsr >> 7), returning one exactly
      when the IRQ-disable I bit is clear. The FIQ-disable F bit does not
      affect the result, and CPSR is not modified.

  - address: 0x08001f04
    ghidra: FUN_08001f04
    name: usec_timer_elapsed_millis
    size: 52
    sig: "u32 usec_timer_elapsed_millis(u32 start, u32 milliseconds)"
    module: drivers/timer
    status: ported
    vars: {param_1: start, param_2: milliseconds,
      DAT_080023d0: timer_register_base,
      DAT_080023d4: max_milliseconds,
      timer_register_base_plus_0xb4: usec_timer_count}
    notes: Reads Timer E TECNT before validation. Intervals greater than
      0x00418937 (floor(u32::MAX / 1000)) return u32::MAX; otherwise,
      computes the elapsed count with u32 wrapping and returns whether it is
      at least milliseconds * 1000, as 0 or 1.

  - address: 0x08001edc
    ghidra: FUN_08001edc
    name: usec_timer_read
    size: 12
    sig: "u32 usec_timer_read(void)"
    module: drivers/timer
    status: ported
    vars: {DAT_080023d0: timer_register_base,
      timer_register_base_plus_0xb4: timer_e_counter}
    notes: Returns the raw Timer E TECNT word at 0x3c7000b4. The literal
      global DAT_080023d0 contains the timer-controller base 0x3c700000;
      callers use unsigned wrapping subtraction from this microsecond count
      for timeout polling. Target uses volatile MMIO; host uses a
      deterministic driver-local counter seam.

  - address: 0x08086e24
    ghidra: FUN_08086e24
    name: read_usec_timer_into
    size: 20
    sig: "void read_usec_timer_into(u32 *out)"
    module: drivers/timer
    status: ported
    renames:
      param_1/r0/r4: out
      thunk_FUN_08056658: usec_timer_read_thunk
      FUN_0836af80: usec_timer_read
      uVar1/r0: timer_e_counter
    notes: >-
      Binary extent is 0x08086e24..0x08086e38: `push {r4,lr}; mov r4,r0;
      bl 0x08056658; str r0,[r4]; pop {r4,pc}`. The branch thunk at
      0x08056658 goes directly to 0x0836af80, whose body reads the Timer E
      TECNT MMIO word at 0x3c7000b4. Therefore this function samples exactly
      once and stores the raw word through its unguarded output pointer.
      Raw decoding of every ARM B/BL word in osos.dec finds 18 direct bl,
      all unconditional, with zero predicated bl and zero tail b call sites.
      The separately linked next function at 0x08086e38 is byte-identical.
      Rust calls existing usec_timer_read directly instead of adding a seam
      for the unported branch thunk; this preserves one volatile sample and
      one word store.

  - address: 0x08086e38
    ghidra: FUN_08086e38
    name: read_usec_timer_into_2
    size: 20
    sig: "void read_usec_timer_into_2(u32 *out)"
    module: drivers/timer
    status: ported
    renames:
      param_1/r0/r4: out
      thunk_FUN_08056658: usec_timer_read_thunk
      FUN_0836af80: usec_timer_read
    notes: >-
      Binary extent is 0x08086e38..0x08086e4c; the next function opens with
      its own `push {r3,lr}` at 0x08086e4c. Instruction-identical twin of
      read_usec_timer_into @ 0x08086e24: `push {r4,lr}; mov r4,r0;
      bl 0x08056658; str r0,[r4]; pop {r4,pc}` — the bl immediate differs
      only because the PC-relative offset to the same thunk shifts with the
      address. The thunk at 0x08056658 branches to 0x0836af80, whose body
      reads the Timer E TECNT MMIO word at 0x3c7000b4, so this samples
      exactly once and stores the raw word through its unguarded output
      pointer; there is no NULL guard. Raw decoding of every ARM B/BL word
      in osos.dec finds 15 direct bl call sites, all unconditional, with
      zero predicated bl and zero tail b call sites — Ghidra's 15-call
      count is exact. Call sites (0x0808cd10, 0x0812b3e0, 0x0812b5f8,
      0x08131530, 0x08159134, 0x0815942c, 0x0815f720, 0x08182800,
      0x08182b10, 0x081a0f5c, 0x081b7908, 0x081bb38c, 0x081bc024,
      0x082249fc, 0x0826c5f0) use it for the same elapsed-time
      instrumentation as the twin's. Rust calls the already-ported
      usec_timer_read directly rather than adding a seam for the unported
      branch thunk. A dedicated link_section (.text.read_usec_timer_into_2)
      keeps LLVM identical-code folding from collapsing this independently
      hookable copy onto the twin's body; both symbols verified present
      and unfolded in librustypod.a. match.py: structural match — LLVM
      adds sl/fp frame bookkeeping around the same push/mov/bl/str/pop
      spine.

  - address: 0x08001ee8
    ghidra: FUN_08001ee8
    name: usec_timer_elapsed
    size: 28
    sig: "bool usec_timer_elapsed(u32 start, u32 interval)"
    module: drivers/timer
    status: ported
    vars: {param_1: start, param_2: interval,
      DAT_080023d0: timer_register_base,
      timer_register_base_plus_0xb4: usec_timer_count}
    notes: Reads Timer E TECNT through usec_timer_read, computes
      usec_timer_count - start with u32 wrapping arithmetic, and returns
      whether the elapsed duration is at least interval.

  - address: 0x08001f78
    ghidra: FUN_08001f78
    name: usec_delay
    size: 44
    sig: "u32 usec_delay(u32 interval)"
    module: drivers/timer
    status: ported
    vars: {param_1: interval, uVar2: start, iVar1: elapsed,
      DAT_080023d0: timer_register_base,
      timer_register_base_plus_0xb4: initial_usec_timer_count}
    notes: Captures Timer E TECNT once, then repeatedly invokes
      usec_timer_elapsed(start, interval) until it succeeds. Subsequent
      counter reads occur only inside that predicate; returns zero.

  - address: 0x08001fa4
    ghidra: FUN_08001fa4
    name: configure_cpu_to_ahb_divider
    size: 104
    sig: "u32 configure_cpu_to_ahb_divider(u32 clock_source, u32 requested_divider)"
    module: drivers/clock_config
    status: ported
    vars: {param_1: clock_source_unused, param_2: requested_divider,
      DAT_080023d8: clkcon_base, DAT_080023d8_plus_4: clkcon1,
      DAT_080023dc: clock_manager_state, clock_manager_state_plus_4: requested_cpu_to_ahb_divider,
      uVar2: clkcon1_control_word, uVar3: encoded_cpu_to_ahb_divider}
    notes: Configures System Controller CLKCON1 (0x3c500004) CPU-to-AHB
      divider field [20:16]; divider one clears CLKCON1[23:16], otherwise
      ((divider >> 1) - 1) & 0x1f is installed in [20:16] with update bit 22.
      clock_source is unused in this mode. Stores the requested divider in
      clock-manager IRAM state 0x2200ad10, calls the retailOS CLKCON1
      control-word transform dispatch at 0x08003508, writes the transformed
      word, and polls CLKCON1[23:16] for acknowledgement. Target performs
      volatile MMIO/IRAM accesses and invokes the original dispatch stub;
      host tests use a deterministic driver-local seam.

  - address: 0x08003b6c
    ghidra: FUN_08003b6c
    name: dispatch_message_0x17
    size: 32
    sig: "u32 dispatch_message_0x17(u32 unused_r0, u32 arg2, u32 arg3, u32 arg4)"
    module: runtime/message_0x17
    status: ported
    renames:
      param_1/r0: unused_r0
      param_2/r1: arg2
      param_3/r2: arg3_and_return_word
      param_4/r3: arg4
      local_18: message_command
      uStack_14: message_arg2
      local_10: message_arg3_and_return
      uStack_c: message_arg4
      FUN_08003660: message_dispatch_veneer
      DAT_08003664: rom_message_dispatch
    notes: >-
      Builds the exact four-word stack record {0x17, r1, r2, r3}, calls the
      0x08003660 literal veneer (ROM target 0x0802dca8), then reloads word 2
      at sp+8 as r0. Incoming r0 is saved only to form the stack record and
      is overwritten by the command tag. The port keeps the unported
      dispatcher behind a volatile target-default callback seam; the direct
      host test proves record layout, callback order/count, and that a
      dispatcher mutation of word 2 becomes the return value. Reference C:
      decomp/c/000/08003b6c_FUN_08003b6c.c; raw ARM 0x08003b6c..0x08003b8b.

  - address: 0x08003b8c
    ghidra: FUN_08003b8c
    name: ks_alloc_timer
    size: 36
    sig: "u32 ks_alloc_timer(void)"
    module: runtime/message_0x10
    status: ported
    renames:
      local_1c: timer_allocation_request
      local_1c[0]: timer_alloc_service_selector
      local_8: timer_handle_and_return
      FUN_08003660: message_dispatch_veneer
      DAT_08003664: rom_message_dispatch
    notes: >-
      KS_alloc_timer wrapper. Reserves seven stack words, writes RTXC service
      selector 0x10 at sp+4, and dispatches the six-word request beginning
      there through the 0x08003660 literal veneer (ROM target 0x0802dca8).
      The intermediate words remain deliberately uninitialized; after the
      dispatcher returns, reloads sp+24 (request word five) as the timer
      handle/result. The port preserves the stack-record ABI with
      MaybeUninit and uses the shared volatile target-default dispatcher seam
      from runtime/message_0x17. Reference C:
      decomp/c/000/08003b8c_FUN_08003b8c.c; raw ARM 0x08003b8c..0x08003baf.

  - address: 0x08000318
    ghidra: FUN_08000318
    name: pwrcon_update_masks
    size: 72
    sig: "u32 pwrcon_update_masks(u32 pwrcon0_mask, u32 pwrcon1_mask, u32 enable)"
    module: drivers/pwrcon
    status: ported
    vars: {param_1: pwrcon0_mask, param_2: pwrcon1_mask, param_3: enable,
      DAT_08000360: pwrcon_base, pwrcon_base_plus_0x48: pwrcon0,
      pwrcon_base_plus_0x4c: pwrcon1}
    notes: Enters the 0x08001e70 IRQ/FIQ guard, atomically updates PWRCON0
      (0x3c500048) and PWRCON1 (0x3c50004c), then restores with 0x08001e84.
      enable == 0 ORs the masks (gates clocks); any nonzero enable clears
      them (enables clocks). Target uses volatile MMIO; host tests install a
      recording driver-local guard/MMIO seam.

  - address: 0x08037df0
    ghidra: thunk_EXT_FUN_22001f38
    name: iram_msec_delay_veneer
    size: 8
    sig: "u32 iram_msec_delay_veneer(u32 milliseconds)"
    module: drivers/timer
    status: ported
    renames: {thunk_EXT_FUN_22001f38: iram_msec_delay_veneer,
      EXT_FUN_22001f38: iram_msec_delay}
    notes: >-
      Ghidra's 4-byte extent omits the literal target word: raw osos.dec has
      ldr pc,[pc,#-4] (0xe51ff004) at 0x08037df0 and 0x22001f38 at
      0x08037df4; the next thunk starts at 0x08037df8, proving an 8-byte
      extent. The relocator at 0x080046e0 copies 0xaed8 bytes from
      0x08000000 to 0x22000000, so the IRAM target is the 64-byte
      0x08001f38..0x08001f78 millisecond busy-wait: values over
      0x00418937 return 9 before any timer read; valid values multiply by
      1000, sample Timer E TECNT, and poll usec_timer_elapsed until done,
      returning 0. Every ARM B/BL word was decoded: 16 direct bl calls,
      15 unconditional and one bleq at 0x0836e33c; no tail b and no data
      word contains 0x08037df0, so the predication is caller-side equality
      gating, not a callee NULL guard or virtual dispatch. The Rust hook
      embeds the unported mirrored target body and calls the existing Timer E
      helpers directly, preserving validation-before-read and poll order.

  - address: 0x08037de8
    ghidra: thunk_EXT_FUN_22000318
    name: iram_pwrcon_update_masks_veneer
    size: 8
    sig: "u32 iram_pwrcon_update_masks_veneer(u32 pwrcon0_mask, u32 pwrcon1_mask, u32 enable)"
    module: drivers/pwrcon
    status: ported
    renames: {thunk_EXT_FUN_22000318: iram_pwrcon_update_masks_veneer}
    notes: >-
      One stub of the osos -> IRAM thunk table (ROM_THUNKS in
      kernel/thunks.rs, registry name slot left None per the
      0x08037e20 precedent): ldr pc,[pc,#-4] + literal word 0x22000318,
      binary-decoded from osos.dec (0xe51ff004 @ 0x08037de8, target word
      @ 0x08037dec). Ghidra reports 4 bytes; the stub is 8 (next stub
      starts at 0x08037df0). Call count binary-verified by decoding
      every B/BL word in osos.dec for every condition code: 31 bl, ALL
      unconditional, plus 3 tail b (0x080923b4 / 0x080ce0c4 /
      0x0836d8c4) and 1 beq (0x0836e0a0 - a caller-side branch choice,
      not a NULL guard: the callee takes pointer-less mask arguments).
      No DATA word holds the thunk address, so it is not virtually
      dispatched. Target identified via the relocator mirror (0x080046e0
      copies 0xaed8 bytes from 0x08000000 to 0x22000000), so IRAM
      0x22000318 is byte-identical to the ALREADY PORTED
      pwrcon_update_masks @ 0x08000318 in this module; the veneer calls
      it directly (the iram_usec_timer_read_veneer precedent). Pure tail
      transfer: r0-r2 pass through untouched, no stack, lr preserved,
      returns the body's r0 (always 0). The callee is loaded through
      read_volatile and the veneer carries its own link_section, so LLVM
      can neither inline the body nor ICF-fold the veneer onto another
      same-shaped call - verified: both symbols resolve in
      librustypod.a. Host tests prove set-mask and clear-mask
      transparency through the pwrcon host seam (identical guarded
      transaction and r0 as a direct call) and symbol distinctness.
      match.py structural (exit 1, expected): the port is ldr/ldr/bx
      through a literal plus the crate's fp frame, r0-r2 untouched;
      the original's literal word shows as a data-decoded andcs.

  - address: 0x080003b8
    ghidra: FUN_080003b8
    name: startup_wait_status_clear
    size: 112
    sig: "u32 startup_wait_status_clear(u32 timeout_ticks)"
    module: runtime/startup_wait
    status: ported
    vars: {param_1: timeout_ticks, DAT_08000500: startup_status_base,
      iVar1: startup_status_base, uVar2: start_ticks, uVar4: result,
      iVar3: poll_result, FUN_08001edc: timer_read,
      FUN_08001ee8: timer_elapsed, FUN_08001e98: irq_enabled,
      FUN_08003d44: task_yield}
    notes: >-
      Polls status word +0x04 bit 5 until it clears. The entry fast path
      returns zero without reading the timer when the bit is clear; otherwise
      it snapshots the timer, checks status before each timeout predicate, and
      yields task_delay(0, 1) only with IRQs enabled. A final status re-read
      returns 0x15 when the bit remains set, but returns zero when it clears
      concurrently with expiry. The status, timer, IRQ, and yield boundaries
      use target-default dispatch seams in runtime/startup_wait; focused host
      tests script each dependency and verify the polling order.

  - address: 0x08000428
    ghidra: FUN_08000428
    name: i2c0_wait_transfer_done
    size: 196
    sig: "u32 i2c0_wait_transfer_done(u32 timeout_usec)"
    module: runtime/i2c0_transfer
    status: ported
    vars: {param_1: timeout_usec, DAT_08000500: i2c0_register_base,
      iVar1: i2c0_register_base, uVar2: start, uVar4: status,
      iVar3: poll_predicates, FUN_08001edc: usec_timer_read,
      FUN_08001ee8: usec_timer_elapsed, FUN_08001e98: cpsr_irq_enabled,
      FUN_08003d44: task_delay}
    notes: >-
      Timeout-and-flag sibling of i2c0_wait_bus_idle @ 0x080003b8 on the
      S5L8702 I2C bus-0 controller (0x3c600000): waits while IICSTAT
      (base+4) bit 5 (bus-busy) is set AND IICSTA2 (base+0x20) bit 8 (the
      byte-transferred interrupt flag) is clear, polling stat, sta2 and
      the ported usec_timer_elapsed in that order and yielding via the
      unported RTXC task-delay gateway 0x08003d44 as task_delay(0, 1)
      whenever cpsr_irq_enabled. Entry fast path skips the wait (and the
      timer snapshot) when the bus is already idle or the flag already
      set. A confirming elapsed re-check after the loop returns 0x1f when
      the deadline passed; otherwise a confirming stat/sta2 re-read
      returns 0x15 (the I2C family timeout code, cf. FUN_0806418c) for
      the race where the in-loop elapsed call fired but the bus is still
      busy with the flag still clear, else 0. Every path then drains
      IICCON (base+0x10) by reading until the word reads zero and sets
      IICSTA2 bit 8 with a read-modify-write. Deviation: the callees and
      the three register accesses dispatch through the I2C0_TRANSFER_OPS
      slot (ported defaults; ROM gateway on target, no-op stub on host)
      and host builds use driver-local register seams per the
      drivers/pwrcon precedent; the algorithm is the original's.

  - address: 0x08004ca4
    ghidra: FUN_08004ca4
    name: select_i2s_interrupt_register_base
    size: 28
    sig: "u32 select_i2s_interrupt_register_base(u32 interface_selector)"
    module: runtime/timer_constants
    status: ported
    vars: {param_1: interface_selector, DAT_08004cc0: i2s1_register_base,
      DAT_08004cc4: i2s0_register_base}
    notes: Selects an S5L8702 I2S interface register base for the three-way
      interrupt-mask updater at 0x0800881c. Decoded literal-pool words are
      I2S1 (0x3cd00000) and I2S0 (0x3ca00000); selector 2 returns the
      inlined I2S2 base (0x3d400000). Selector 1 returns I2S1, selector 2
      returns I2S2, and every other selector returns I2S0. Target volatile-
      loads the original literal-pool addresses; host builds use a
      deterministic replaceable literal seam because firmware ROM is unmapped.

  - address: 0x08004ccc
    ghidra: FUN_08004ccc
    name: stream_context_reset_and_notify
    size: 40
    sig: "void stream_context_reset_and_notify(StreamContext *stream_context)"
    module: app/stream_context_reset_notify
    status: ported
    vars: {param_1/r0: stream_context, param_1_plus_0x05: reset_pending,
      param_1_plus_0x44: stream_state, thunk_FUN_080746ec: reset_stream_state,
      DAT_080036dc: notify_stream_state}
    notes: >-
      The sole recovered caller, media-startup coordinator 0x080056d0, calls
      this immediately after its framework setup operation and before the
      paired renderer step 0x080eade0. Raw ARM preserves the context in r4,
      invokes veneer 0x080036d0 (literal 0x080746ec) with its embedded stream
      state at +0x44, clears reset_pending at +0x05, then restores r4/lr and
      tail-enters veneer 0x080036d8 (literal 0x080747c8) with the same
      embedded state. Both targets are inside a renderer continuation and rely
      on inherited framework register/stack context, so target builds retain
      literal veneer assembly; host builds use an ordered two-callback seam.

  - address: 0x080088d8
    ghidra: FUN_080088d8
    name: startup_relocation_offset
    size: 16
    sig: "u32 startup_relocation_offset(void)"
    module: util/startup_relocation_offset
    status: ported
    vars: {r0: linked_routine_address_then_relocation_offset,
      r1: relocated_routine_address, DAT_080088f0: relocated_routine_address_literal}
    notes: >-
      Position-independent startup relocation helper. `sub r0, pc, #8`
      materializes the linked entry address 0x080088d8; subtracting it from
      the decoded literal-pool word 0x220088d8 returns the fixed IRAM offset
      0x1a000000 in r0. The port represents those two image addresses as
      explicit u32 constants because its Rust code is linked independently;
      this preserves the helper's returned firmware offset.

  - address: 0x080004ec
    ghidra: FUN_080004ec
    name: startup_wait_for_status_clear
    size: 20
    sig: "int startup_wait_for_status_clear(void)"
    module: runtime/startup_wait
    status: ported
    vars: {DAT_08000504: startup_wait_timeout_ticks}
    notes: Calls the ported 0x080003b8 status-clear wait with its fixed
      100000-tick literal, discards that status, and returns zero.

  - address: 0x08030fe4
    ghidra: FUN_08030fe4
    name: memset
    size: 20
    sig: "void *memset(void *dst, int value, size_t len)"
    module: memzero
    status: ported
    notes: Broadcasts value byte to a word, tail-branches into the shared
      fill body (via an iRAM veneer to memzero+4). Original is ADS
      __rt_memset(dst, len, value); port uses classic memset arg order.

  # --- ARM ADS runtime: string block @ 0x08030f64-0x080312a0 ---
  - address: 0x08030f64
    ghidra: FUN_08030f64
    name: memcmp
    size: 128
    sig: "int memcmp(const void *a, const void *b, size_t len)"
    module: memcmp
    status: ported
    notes: Word-at-a-time compare when aligned (re-scan mismatching word
      byte-wise), byte tail; returns u8 difference.

  - address: 0x08030ff4
    ghidra: FUN_08030ff4
    name: strcpy
    size: 100
    sig: "char *strcpy(char *dst, const char *src)"
    module: strcpy
    status: ported
    notes: ADS __strcpy_arm. Word-at-a-time with 0x01010101 zero-byte-detect
      idiom when aligned; 2x-unrolled byte loop otherwise. 116 call sites.

  - address: 0x0803105c
    ghidra: FUN_0803105c
    name: strncmp
    size: 116
    sig: "int strncmp(const char *a, const char *b, size_t len)"
    module: strncmp
    status: ported
    notes: Word compare + in-word NUL detect (0x01010101 idiom); byte tail
      with ADS `cmp b,#1; cmpcs` idiom. 67 call sites. No standalone strcmp
      exists in retailOS — it always uses strncmp.

  - address: 0x080310d4
    ghidra: FUN_080310d4
    name: strncpy
    size: 104
    sig: "char *strncpy(char *dst, const char *src, size_t len)"
    module: strncpy
    status: ported
    notes: Word copy + NUL idiom, then zero-pads to len (tail-calls zero
      filler). 41 call sites.

  - address: 0x08031140
    ghidra: FUN_08031140
    name: __rt_uread4
    size: 32
    sig: "u32 __rt_uread4(const void *p)"
    module: rt_unaligned
    status: ported
    notes: Byte-wise little-endian unaligned u32 load (read_unaligned today).

  - address: 0x08031160
    ghidra: FUN_08031160
    name: __rt_uwrite4
    size: 32
    sig: "u32 __rt_uwrite4(void *p, u32 value)"
    module: rt_unaligned
    status: ported
    notes: Byte-wise LE unaligned u32 store. Original arg order (value, p).

  - address: 0x08031180
    ghidra: FUN_08031180
    name: memchr
    size: 52
    sig: "const void *memchr(const void *s, int c, size_t len)"
    module: memchr
    status: ported
    notes: Bounded byte scan for (c as u8).

  - address: 0x080311b4
    ghidra: FUN_080311b4
    name: strcat
    size: 40
    sig: "char *strcat(char *dst, const char *src)"
    module: strcat
    status: ported

  - address: 0x080311dc
    ghidra: FUN_080311dc
    name: strchr
    size: 36
    sig: "const char *strchr(const char *s, int c)"
    module: strchr
    status: ported
    notes: Match test before NUL test, so c=='\0' returns the terminator.

  - address: 0x08031200
    ghidra: FUN_08031200
    name: strncat
    size: 64
    sig: "char *strncat(char *dst, const char *src, size_t len)"
    module: strcat
    status: ported

  - address: 0x08031240
    ghidra: FUN_08031240
    name: strrchr
    size: 36
    sig: "const char *strrchr(const char *s, int c)"
    module: strchr
    status: ported
    notes: Full-string scan keeping last match.

  - address: 0x08031264
    ghidra: FUN_08031264
    name: strstr
    size: 60
    sig: "const char *strstr(const char *haystack, const char *needle)"
    module: strstr
    status: ported
    notes: Byte-at-a-time brute force; empty needle returns haystack.

  # --- retailOS safe helpers ---
  - address: 0x082770bc
    ghidra: FUN_082770bc
    name: strlen_safe
    size: 36
    sig: "size_t strlen_safe(const char *s)"
    module: strlen_safe
    status: ported
    notes: >-
      NULL-guarded strlen (returns 0 for null). Classic ADS strlen is
      genuinely absent from the binary; this is what retailOS uses.
      Variants: 0x0810b610 (identical), 0x08275e20 (count+1).

  - address: 0x08275e20
    ghidra: FUN_08275e20
    name: strlen_safe_plus1
    size: 36
    sig: "size_t strlen_safe_plus1(const char *s)"
    module: strlen_safe_plus1
    status: ported
    notes: >-
      NULL-guarded strlen + 1: returns 1 for a null pointer, else
      strlen(s) + 1 (buffer size including the NUL terminator). The
      "count+1" variant of strlen_safe @ 0x082770bc. 51 `bl` call sites
      (binary-scanned); sampled callers confirm the buffer-size reading:
      0x08115678 subtracts 1 to recover the plain length for a bounded
      copy, and the StringObject sibling @ 0x082a50a0 (`ldr r0,[r0,#4];
      b 0x08275e20`) tail-branches here over the string payload (that
      sibling is a separate function, not ported here). match.py
      structural: same NULL guard / byte loop / count+1 return; the diff
      is LLVM's fp frame and a peeled first iteration with the counter
      pre-offset to 1.

  # --- Batch 2: ARM ADS arithmetic / stdlib runtime ---
  - address: 0x08031568
    ghidra: FUN_08031568
    name: __rt_sdiv
    size: 356
    sig: "i32 __rt_sdiv(i32 num, i32 den)  // rem also in r1"
    module: rt_div
    status: ported
    notes: 1812 call sites. Unrolled restoring division + sign fix.
      Truncation semantics; div0 raises via __rt_raise (stubbed).

  - address: 0x08036f14
    ghidra: FUN_08036f14
    name: __rt_udiv
    size: 28
    sig: "u32 __rt_udiv(u32 num, u32 den)"
    module: rt_div
    status: ported
    notes: 487 call sites; joins the sdiv core mid-stream skipping sign handling.

  - address: 0x0803421c
    name: __rt_div0
    size: 12
    sig: "i32 __rt_div0(void)"
    module: rt_div
    status: ported
    notes: >-
      mov r0,#2; mov r1,#2; b __rt_raise(0x080320a8) — SIGFPE with the
      Divide By Zero reason code, through the ported __rt_raise (raise.rs).
      Entered via plain b from both divide cores, so a survived raise
      returns __rt_raise's 0 straight to the divide's caller (modeled as
      quotient/remainder 0 in the rt_div funnel).

  - address: 0x08037a84
    ghidra: FUN_08037a84
    name: fixed16_div_core
    size: 540
    sig: "i32 fixed16_div_core(i32 dividend, i32 divisor) -> i32  // Q16.16 (a<<16)/b"
    module: rt_div
    status: ported
    vars: {param_1: dividend, param_2: divisor}
    notes: >-
      Q16.16 fixed-point signed division core, quotient only: returns
      (dividend << 16) / divisor truncated toward zero. NOT an ADS
      integer sdiv — all 6 bl call sites pass Q16.16 values
      (0x080ea678/0x080ea820/0x080eae7c in FUN_080ea5a0, 0x080eb024 in
      FUN_080eade0, 0x080f78ac in FUN_080f780c which divides by 0xf0000
      = 15.0, 0x080f7a90 in FUN_080f79d8) plus the b tail call from
      fixed16_div_indirect @ 0x082a182c, whose sdiv32 seam in fp_misc.rs
      now targets this port on target_os=none. Ghidra's 344-byte size
      truncates the body at the computed jump; real extent is
      0x08037a84-0x08037c9f (540 bytes), recovered by disassembling
      osos.dec directly (the Ghidra asm export omits 0x08037ad0-0x08037b8f).
      Structure: sign-magnitude prologue (quotient sign track in r3),
      shift = clz(den) - clz(num), add pc,pc,r4 dispatch into sixteen
      12-byte blocks (subs r4,r2,r1,lsl#s / orrcs r0,#bit / movcs r2,r4)
      emitting quotient bits 31..16 against the pre-shifted divisor, then
      sixteen 16-byte blocks (add r2,r2,r2 / subs / orrcs / movcs)
      emitting fractional bits 15..0; bmi into the low blocks when
      num < den. Quotient negated when signs differed; ONLY r0 returned
      (remainder left in r2 is call-clobbered — the sibling remainder
      core ending @ 0x08037a80 shares the layout but keeps r2). No
      __rt_div0 funnel: den == 0 never traps, every compare succeeds so
      the result is (1 << (17 + bit_length(|dividend|))) - 1, negated
      when dividend < 0 (5/0 -> 0x000fffff, 0/0 -> 0x0001ffff). shift >
      15 (Q16.16 quotient overflows 32 bits) makes the original's
      computed jump land before its table and walk into its own
      prologue — undefined; the port clamps the high phase at shift 15.
      match.py structural: same sign-track prologue, clz dispatch and
      subs/orrcs conditional-subtract cascade; LLVM rolls the two phases
      into loops and partially unrolls the low 16 steps.

  - address: 0x0802eefc
    ghidra: FUN_0802eefc
    name: __aeabi_uldivmod
    size: 712
    sig: "u64 __aeabi_uldivmod(u64 num, u64 den)  // rem in r2:r3"
    module: aeabi_64div
    status: ported
    notes: clz-normalized shifting restore; 53 callers.

  - address: 0x0802ee34
    ghidra: FUN_0802ee34
    name: __aeabi_ldivmod
    size: 80
    sig: "i64 __aeabi_ldivmod(i64 num, i64 den)"
    module: aeabi_64div
    status: ported
    notes: Signed wrapper over the unsigned core; 52 callers.

  - address: 0x0802ee84
    ghidra: FUN_0802ee84
    name: __aeabi_llsl
    size: 40
    sig: "u64 __aeabi_llsl(u64 v, u32 shift)"
    module: aeabi_64shift
    status: ported

  - address: 0x0802eeac
    ghidra: FUN_0802eeac
    name: __aeabi_llsr
    size: 40
    sig: "u64 __aeabi_llsr(u64 v, u32 shift)"
    module: aeabi_64shift
    status: ported

  - address: 0x0802eed4
    ghidra: FUN_0802eed4
    name: __aeabi_lasr
    size: 40
    sig: "i64 __aeabi_lasr(i64 v, u32 shift)"
    module: aeabi_64shift
    status: ported

  - address: 0x080320d0
    ghidra: FUN_080320d0
    name: _ll_udiv10
    size: 152
    sig: "u64 _ll_udiv10(u64 num)  // rem in r2"
    module: ll_udiv10
    status: ported
    notes: >-
      Shift-add magic multiply by 10; callers: dtoa, %lld, strtoull.

  - address: 0x08030e7c
    ghidra: FUN_08030e7c
    name: srandom
    size: 68
    sig: "void srandom(u32 seed)"
    module: random
    status: ported
    notes: Seeds 55-word table @ 0x08b2f918 (LCG *69069 + 0x66d619e1,
      store seed+(seed>>16)); lag indices @ 0x08a0fb94 = 23/54.

  - address: 0x08030ec8
    ghidra: FUN_08030ec8
    name: random
    size: 84
    sig: "i32 random()"
    module: random
    status: ported
    notes: Additive lagged Fibonacci; result & 0x7fffffff.

  - address: 0x08030ec0
    name: srandom1_thunk
    size: 8
    module: random
    status: ported
    notes: srandom(1) tail call.

  - address: 0x0802f1bc
    ghidra: FUN_0802f1bc
    name: tolower
    size: 36
    sig: "i32 tolower(i32 c)"
    module: ctype
    status: ported
    notes: ctype table @ 0x08985f01 (EOF guard byte at -1); bit 0x10 =
      uppercase (adds 0x20). Table embedded in ctype.rs as CTYPE_TABLE.

  - address: 0x0802f1e0
    ghidra: FUN_0802f1e0
    name: toupper
    size: 40
    sig: "i32 toupper(i32 c)"
    module: ctype
    status: ported
    notes: bit 0x08 = lowercase (subtracts 0x20); 0xdf guard (dead under C locale).

  - address: 0x082d731c
    ghidra: FUN_082d731c
    name: isdigit
    size: 36
    sig: "i32 isdigit(i32 c)"
    module: ctype
    status: ported
    renames:
      param_1: c
      piVar1: ctype_table_slot
    notes: >-
      Raw extent is exactly 36 bytes at 0x082d731c..0x082d733c; the distinct
      following function begins at 0x082d7340. Calls the already ported
      __rt_ctype_table_addr @ 0x0802eca0, loads its LC_CTYPE table word, and
      returns 1 exactly when unchecked table[c] is 0x20, else 0. Decoding
      every ARM B/BL word in osos.dec verified 10 plain unconditional bl call
      sites (0 predicated forms, 0 tail b); callers therefore do not gate this
      classifier. No NULL or bounds guard exists in retailOS or this port.

  - address: 0x082d7340
    ghidra: FUN_082d7340
    name: isspace
    size: 28
    sig: "i32 isspace(i32 c)"
    module: ctype
    status: ported
    renames:
      param_1: c
      piVar1: ctype_table_slot
    notes: >-
      Raw extent is exactly 28 bytes at 0x082d7340..0x082d7358; the distinct
      following function begins at 0x082d735c. Calls the already ported
      __rt_ctype_table_addr @ 0x0802eca0, loads its LC_CTYPE table word, and
      returns unchecked table[c] bit 0. Decoding every ARM B/BL word in
      osos.dec verified 8 plain unconditional bl sites (0 predicated forms,
      0 tail b); callers therefore do not gate this classifier. No NULL or
      bounds guard exists in retailOS or this port.

  - address: 0x08032fec
    ghidra: FUN_08032fec
    name: _chval
    size: 32
    sig: "i32 _chval(u8 c, u32 base)"
    module: chval
    status: ported
    notes: ASCII to digit, -1 if invalid for base.

  - address: 0x08030e60
    ghidra: FUN_08030e60
    name: ldiv
    size: 28
    sig: "ldiv_t ldiv(i32 num, i32 den)"
    module: chval
    status: ported
    notes: Tail-calls __rt_sdiv, stores {quot, rem} via hidden ptr.

  - address: 0x08033088
    ghidra: FUN_08033088
    name: strtoul
    size: 232
    sig: "u32 strtoul(const char *s, char **endptr, int base)"
    module: strtoul
    status: ported
    notes: >-
      Conversion core merged with ws/sign wrapper @ 0x0802f87c.
      ADS quirk: "0x" with no hex digit = no conversion. errno skipped.

  - address: 0x0802f87c
    ghidra: FUN_0802f87c
    name: strtol
    size: 176
    sig: "i32 strtol(const char *s, char **endptr, int base)"
    module: strtol
    status: ported
    notes: Clamps i32::MAX/MIN (port follows ADS wrapper contract;
      literal core wraps). errno skipped.

  - address: 0x0802f990
    ghidra: FUN_0802f990
    name: atoi
    size: 52
    sig: "i32 atoi(const char *s)"
    module: strtol
    status: ported

  - address: 0x0802fba0
    ghidra: FUN_0802fba0
    name: atol
    size: 52
    sig: "i32 atol(const char *s)"
    module: strtol
    status: ported

  - address: 0x0802fbd4
    ghidra: FUN_0802fbd4
    name: stream_test_eof_flag
    size: 44
    sig: "u32 stream_test_eof_flag(AdsStream *stream)"
    module: stdio/stream_flags
    status: ported
    vars: "r4=stream, r0=flags word, r4'=masked result, r5=&file.lock"
    notes: >-
      feof core: returns FILE+0xc flags & 0x40 (masked value, not a
      normalized boolean). Byte-structure twin of stream_test_error_flag
      @ 0x080333f8 (ferror core, mask 0x80) — same nop'd lock acquire/
      release hooks on FILE+0x3c, only the mask constant differs. NOT a
      strto-area conversion wrapper despite sitting right after atol; it
      heads the buffered-stream cluster (sync core 0x0802fc00, fseek core
      0x0802fd04). Callers: 0x08029af4/0x08029c08/0x08029e40/0x08029f64
      (unidentified 0x08029xxx parser loops).

  - address: 0x08034e68
    ghidra: FUN_08034e68
    name: __strtoull
    size: 288
    sig: "u64 __strtoull(const char *s, char **endptr, int base)"
    module: strtoull
    status: ported
    notes: 16/32/16-bit split accumulation (umull only). No standalone
      strtoull wrapper exists; port reconstructs it from strtoll prologue.

  - address: 0x080332d0
    ghidra: FUN_080332d0
    name: strtoll
    size: 248
    sig: "i64 strtoll(const char *s, char **endptr, int base)"
    module: strtoull
    status: ported
    notes: >-
      Whitespace/sign prologue over __strtoull (bl 0x08034e68), then clamps
      the magnitude to i64::MAX/MIN; errno (ERANGE) skipped like the other
      stdlib ports. Also reached by atol @ 0x0802fba0. An earlier duplicate
      entry (module strtoll, self-contained core) was consolidated here.

  - address: 0x08031720
    ghidra: FUN_08031720
    name: setjmp
    size: 40
    sig: "i32 setjmp(JmpBuf *env)"
    module: setjmp
    status: ported
    notes: EXACT MATCH (instruction-for-instruction). Saves r4-r11,sp,lr
      in non-ascending order + unwinder self-ref slot.

  - address: 0x08031748
    ghidra: FUN_08031748
    name: longjmp
    size: 44
    sig: "void longjmp(const JmpBuf *env, i32 val)"
    module: setjmp
    status: ported
    notes: 10/11 instructions match (bl via relocation). val==0 -> 1.

  - address: 0x080336cc
    ghidra: FUN_080336cc
    name: stack_switch
    size: 12
    module: setjmp
    status: ported
    notes: EXACT MATCH. push {r0,r1}; ldm sp,{sl,sp}; mov pc,lr.

  - address: 0x08000020
    name: __rt_memcpy
    size: 180
    sig: "void *__rt_memcpy(void *dst, const void *src, size_t len)"
    module: rt_memcpy
    status: ported
    notes: THE hottest function (~620 call sites via veneer 0x08037db0).
      dst-align prologue, funnel shifts for misaligned src, tail-branches
      to the shared block body @ 0x08000188. Not in functions.csv.

  # --- Corrections / discoveries from batch 2 ---
  - address: 0x08985f01
    name: ctype_table
    size: 256
    module: runtime/ctype
    status: ported
    notes: >-
      C-locale ctype flags DATA (+0x10 uppercase, +0x08 lowercase, +0x20
      digit, +0x40 control, +0x01 whitespace, +0x80 hex digit); EOF guard
      byte 0x00 at 0x08985f00. CTYPE_TABLE in runtime/ctype.rs verified
      byte-exact against osos.dec (file offset 0x985f01). No literal-pool
      pointer to it exists anywhere in the image — the address is computed
      at runtime by the locale init (LC_CTYPE block header @ 0x08985ef4,
      size 0x110, name "C") and installed at libspace+0x24, whence
      tolower/toupper/wcrtomb and the scanf ctype hook load it.

  # --- Batch 3: stdlib control flow, time, errno, printf helpers ---
  - address: 0x08030cd4
    ghidra: FUN_08030cd4
    name: qsort
    size: 256
    sig: "void qsort(void *base, size_t count, size_t size, cmp_fn)"
    module: qsort
    status: ported
    notes: 7 callers. Always runs insertion-sort finish after qsort_inner
      when count > 10 (repairs abandoned small partitions). Previously
      misidentified as strtoull.

  - address: 0x08030998
    ghidra: FUN_08030998
    name: qsort_inner
    size: 828
    sig: "void qsort_inner(void *base, size_t count, size_t size, cmp_fn)"
    module: qsort
    status: ported
    notes: Median-of-three quicksort, explicit 32-entry partition stack,
      larger half pushed / smaller half looped. Previously misidentified
      as __strtoull.

  - address: 0x08031310
    ghidra: FUN_08031310
    name: mktime
    size: 584
    sig: "i32 mktime(struct tm *tm)"
    module: mktime
    status: ported
    notes: Normalizes out-of-range fields, computes wday/yday; time_t=i32.

  - address: 0x080312ac
    name: mktime_normalize
    size: 100
    module: mktime
    status: ported
    notes: 16-bit split + division carry propagation helper.

  - address: 0x080312a0
    name: localtime
    size: 12
    sig: "struct tm *localtime(const i32 *t)"
    module: localtime
    status: ported
    notes: Veneer to core via static tm @ 0x08b2f9f4 (not thread-safe).
      Ghidra size (256) wrong — it's a 12-byte branch.

  - address: 0x08033590
    name: gmtime_core
    size: 260
    module: localtime
    status: ported
    notes: time_t decomposition; div-by-10 magic helper @ 0x08033694.

  - address: 0x08035634
    ghidra: FUN_08035634
    name: raise
    size: 284
    sig: "i32 raise(i32 sig)"
    module: raise
    status: ported
    notes: ADS signal dispatcher; handler table dead-stripped in stock
      binary (constant-folds to default handler). SIG count 14.

  - address: 0x080320a8
    ghidra: FUN_080320a8
    name: __rt_raise
    size: 48
    module: raise
    status: ported
    notes: Calls raise then aborts via retailOS terminate @ 0x82b20a0 (stubbed).

  - address: 0x08030f2c
    ghidra: FUN_08030f2c
    name: exit
    size: 48
    module: exit
    status: ported
    notes: Tails into __rt_sys_exit @ 0x08032084.

  - address: 0x08032084
    ghidra: FUN_08032084
    name: __rt_sys_exit
    size: 36
    module: exit
    status: ported
    notes: stdio flush (semihost, dead) then retailOS terminate @ 0x82b20a0.

  - address: 0x08032058
    ghidra: FUN_08032058
    name: abort
    size: 44
    module: exit
    status: ported
    notes: Message printer @ 0x08035788 then exit path.

  - address: 0x0803171c
    ghidra: thunk_FUN_08033720
    name: rt_exit_thunk
    size: 4
    sig: "noreturn rt_exit_thunk(i32 code)"
    module: exit
    status: ported
    renames:
      param_1/r0: code
      FUN_08033720: __rt_exit
    notes: >-
      Pure dispatch alias: raw ARM word ea0007ff is `b 0x08033720`
      (`__rt_exit`), preserving code in r0. The next word, `stmia r0!,
      {r8, r9, r11, lr}` at 0x08031720, begins a separate function, so
      the 4-byte extent is confirmed. Decoding every ARM B/BL word in
      osos.dec found 26 direct callers: all unconditional bl, with no
      predicated bl or tail b callers. No separate Rust symbol; hook
      0x0803171c to the existing __rt_exit implementation in runtime/exit.rs.

  - address: 0x08033720
    name: __rt_exit
    size: 56
    module: exit
    status: ported
    notes: Runs the atexit handler chain.

  - address: 0x080358b4
    ghidra: FUN_080358b4
    name: atexit
    size: 104
    sig: "i32 atexit(void (*f)(void))"
    module: atexit
    status: ported
    notes: >-
      [libspace+0x3c] table ptr; malloc(32) on first use; LIFO run order.

  - address: 0x080316cc
    name: __cpp_initialise
    size: 56
    module: atexit
    status: ported
    notes: Walks pc-rel fn-ptr table calling each (C++ static init).

  - address: 0x080336d8
    name: __cpp_finalise
    size: 60
    module: atexit
    status: ported
    notes: mla-indexed destructor-array walk.

  - address: 0x0802e94c
    ghidra: FUN_0802e94c
    name: block_byteswap
    size: 844
    sig: "void block_byteswap(void *dst, const void *src, size_t len)"
    module: byteswap
    status: ported
    notes: retailOS-specific; eor/ror#16/and 0xff00 idiom, 8 words/pass.
      Word-aligned pointers only (ldm/stm).

  - address: 0x08035588
    ghidra: FUN_08035588
    name: wcrtomb
    size: 112
    sig: "i32 wcrtomb(char *s, u32 wc, void *state)"
    module: mbrtowc
    status: ported
    notes: >-
      Wide->multibyte via LC_CTYPE block (libspace+0x24). Retail quirk:
      C-locale block[0x101] reads mktime month table (29) so converter path
      is dead; reduces to valid iff wc < 0x80. Previously called 'mbrtowc'.

  - address: 0x08032f68
    name: printf_mem_putc
    size: 20
    module: printf_helpers
    status: ported

  - address: 0x08032f7c
    name: printf_bounded_putc
    size: 20
    module: printf_helpers
    status: ported

  - address: 0x0802f208
    ghidra: FUN_0802f208
    name: printf_pad_emit
    size: 84
    module: printf_helpers
    status: ported

  - address: 0x0802f25c
    ghidra: FUN_0802f25c
    name: printf_pad_emit_zero
    size: 72
    module: printf_helpers
    status: ported

  - address: 0x0802f2a4
    name: printf_widen_signed
    size: 36
    module: printf_helpers
    status: ported

  - address: 0x0802f2c8
    name: printf_widen_unsigned
    size: 36
    module: printf_helpers
    status: ported
    notes: h/hh length-flag argument widening.

  - address: 0x0803204c
    name: __rt_libspace
    size: 8
    sig: "Libspace *__rt_libspace()  // base 0x08b31774"
    module: errno
    status: ported
    notes: >-
      Offsets: +0x00 errno, +0x08 heap desc, +0x14/+0x1c alloc arena,
      +0x24 ctype/LC_CTYPE block ptr, +0x28/+0x2c lc_numeric, +0x3c atexit tbl.

  - address: 0x0802ecb4
    name: __rt_errno_addr
    size: 12
    sig: "i32 *__rt_errno_addr()"
    module: errno
    status: ported

  - address: 0x08032168
    name: errno_get
    size: 16
    module: errno
    status: ported

  - address: 0x08032178
    name: errno_set
    size: 20
    module: errno
    status: ported

  - address: 0x0802eca0
    name: __rt_ctype_table_addr
    size: 16
    module: errno
    status: ported
    notes: libspace+0x24 -> LC_CTYPE block (0x08985f01 for C locale).

  - address: 0x0803350c
    name: get_lc_ctype
    size: 52
    sig: "const void *get_lc_ctype(const void *locale_dir, const char *name)"
    module: runtime/locale
    status: ported
    notes: >-
      CORRECTION (was '__assert_fail'): ADS C-locale getter — returns the
      compiled-in LC_CTYPE block (0x08985f00, guard byte + 256 flags) for
      name NULL/""/"C", NULL otherwise; 0x08391e38 is the ADS strcmp,
      not an assert printer. r0 is the locale directory ptr 0x08985c06,
      ignored. Moved from assert_rt.rs (deleted) to runtime/locale.rs
      alongside setlocale_core; siblings get_lc_numeric @ 0x080355f8 and
      get_lc_monetary @ 0x08036b80 have their own entries now. Called by
      setlocale_core and by __rt_lib_init @ 0x08035788 to seed
      libspace+0x24/28/2c.

  - address: 0x080355f8
    name: get_lc_numeric
    size: 52
    sig: "const void *get_lc_numeric(const void *locale_dir, const char *name)"
    module: runtime/locale
    status: ported
    notes: >-
      C-locale LC_NUMERIC getter (was get_lc_c_block_080355f8 in
      assert_rt.rs) — same body as get_lc_ctype; block @ 0x08986254 (16
      bytes; offsets 0xc/0xe/0xf -> "." "" ""). Installed at
      libspace+0x2c by setlocale mask bit 8; localeconv_fill reads it.

  - address: 0x08036b80
    name: get_lc_monetary
    size: 52
    sig: "const void *get_lc_monetary(const void *locale_dir, const char *name)"
    module: runtime/locale
    status: ported
    notes: >-
      C-locale LC_MONETARY getter (was get_lc_c_block_08036b80 in
      assert_rt.rs) — same body as get_lc_ctype; block @ 0x0898654c
      (0x2b bytes; 8 CHAR_MAX char fields + 7 offsets 0x24..0x2a to
      empty strings). Installed at libspace+0x28 by setlocale mask bit
      4; localeconv_fill reads it.

  - address: 0x080306cc
    name: locale_name_for_mask
    size: 240
    sig: "const char *locale_name_for_mask(u32 mask)"
    module: runtime/locale
    status: ported
    notes: >-
      Sole caller: setlocale_core. Multi-bit mask -> '*' + 5x8 lowercase
      hex digits of ALL five libspace+0x20..+0x34 words, rendered into
      the 42-byte BSS buffer @ 0x08b2f8ec (literal @ 0x08030980).
      Single-bit -> empty slot names "C" (@ 0x0803097c); installed slot
      resolves name via the self-relative i32 at (block & ~3) - 4. For
      an installed LC_COLLATE/LC_TIME slot (directory ptr 0x08985c06,
      no name record) this computes the garbage pointer 0x08985c04 +
      0x45444342 — returned, never dereferenced.

  # --- Batch 4: printf converters, malloc veneers, scanf front-end ---
  - address: 0x0802f2e8
    ghidra: FUN_0802f2e8
    name: convert_s
    size: 156
    sig: "void convert_s(PrintfState *state, const char *s)"
    module: printf_s
    status: ported
    notes: "%s converter; takes min_len in r2 (-1=%s, 1=%c). Zero-pad NOT
      suppressed for strings. NULL deref on device (port: empty string)."

  - address: 0x0802f4b4
    ghidra: FUN_0802f4b4
    name: convert_d
    size: 176
    sig: "void convert_d(PrintfState *state, i32 value)"
    module: printf_d
    status: ported
    notes: "%d/%i/%u 32-bit; dispatched on conversion char. 0 flag cleared
      when precision given. Uses div10 helper @ 0x08033694."

  - address: 0x08033694
    name: div10_u32
    size: 44
    module: printf_d
    status: ported
    notes: Shift-add divide-by-10 (telescoping + fix-up).

  - address: 0x0802f384
    ghidra: FUN_0802f384
    name: convert_x
    size: 280
    sig: "void convert_x(PrintfState *state, u32 value)"
    module: printf_x
    status: ported
    notes: "%x/%X/%p; digit tables @ 0x08985be4/0x08985bf5; # prefix only
      for nonzero after widening; %#p odd '@' prefix @ 0x0802f4a8."

  - address: 0x080323ec
    ghidra: FUN_080323ec
    name: convert_o
    size: 224
    sig: "void convert_o(PrintfState *state, u32 value)"
    module: printf_o
    status: ported
    notes: "%o; # alt-form '0' prefix consumes one precision unit."

  - address: 0x0802f574
    ghidra: FUN_0802f574
    name: convert_lld
    size: 208
    sig: "void convert_lld(PrintfState *state, i64 value)"
    module: printf_ll
    status: ported
    notes: "%lld/%llu; digit loop via _ll_udiv10 @ 0x080320d0."

  - address: 0x080322cc
    ghidra: FUN_080322cc
    name: out_padded
    size: 288
    sig: "void out_padded(state, digits, num_digits, prefix, prefix_len)"
    module: printf_out
    status: ported
    notes: >-
      Shared integer emitter — TRUE 5-arg ABI (prefix_len on stack);
      prefix does NOT come from state.prefix. Order: space-pad, prefix,
      zero-pad, precision zeros, digits, trailing pad. Other converters
      (d/x/o/ll) currently inline private copies — dedup candidate.

  - address: 0x080324d4
    ghidra: FUN_080324d4
    name: store_n
    size: 60
    sig: "void store_n(const PrintfState *state, void *dest)"
    module: printf_out
    status: ported
    notes: "%n; width per flags 0x400 hh/0x100 h/0x80 ll."

  - address: 0x0803218c
    ghidra: FUN_0803218c
    name: convert_ls
    size: 316
    sig: "void convert_ls(PrintfState *state, const u16 *ws)"
    module: printf_wide
    status: ported
    notes: "%ls; two-pass (measure bytes via wcrtomb, then emit); precision
      in BYTES; chars >= 0x80 silently skipped (C-locale dead path)."

  - address: 0x0802edac
    ghidra: FUN_0802edac
    name: malloc
    size: 40
    sig: "void *malloc(size_t size)"
    module: malloc_rt
    status: ported
    notes: >-
      152 callers; veneer to retailOS allocator @ 0x080eb67c via the
      tail veneer 0x082ab188 (tag 1). HEAP_OPS.alloc defaults to the
      full ported chain (alloc_ported shim over malloc_wrapper,
      heap/veneers.rs).

  - address: 0x0802edc8
    ghidra: FUN_0802edc8
    name: free
    size: 52
    sig: "void free(void *ptr)"
    module: malloc_rt
    status: ported
    notes: >-
      150 callers; NULL-guard; retailOS free @ 0x080e7970 via the tail
      veneer 0x082ab19c (NULL re-guard, tag 1). HEAP_OPS.free defaults
      to the full ported chain (free_ported shim over free_wrapper).

  - address: 0x0802edec
    ghidra: FUN_0802edec
    name: realloc
    size: 92
    sig: "void *realloc(void *ptr, size_t size)"
    module: malloc_rt
    status: ported
    notes: >-
      25 callers; 0-size=free, NULL=malloc; retailOS realloc @
      0x080edbf0 via the tail veneer 0x082ab1b4 (tag 1, copy-on-move 1).
      HEAP_OPS.realloc defaults to the full ported chain (realloc_ported
      shim over realloc_wrapper).

  - address: 0x0802ed14
    ghidra: FUN_0802ed14
    name: __rt_heap_init
    size: 152
    module: malloc_rt
    status: ported
    notes: >-
      Aligns to 8, stores heap desc [libspace+8]; grow via 0x080336c0
      (wrapper over 0x0803571c), SIGRTMEM(9) via __rt_raise on failure.
      HEAP_OPS.grow/raise default to the ported heap_grow_arena_wrapper
      and __rt_raise @ 0x080320a8.

  - address: 0x0803300c
    ghidra: FUN_0803300c
    name: string_getc
    size: 56
    module: scanf_helpers
    status: ported
    notes: Sticky EOF; count budget; no advance at EOF.

  - address: 0x08033044
    ghidra: FUN_08033044
    name: string_ungetc
    size: 64
    module: scanf_helpers
    status: ported
    notes: No char arg — rewinds one; multi-level down to base; fails on
      sticky EOF/count==0/ptr==base.

  - address: 0x0803328c
    name: getc_advance
    size: 20
    module: scanf_helpers
    status: ported
    notes: Returns **cursor then *cursor += delta (1=consume, 0=peek, -1=back).

  - address: 0x0802f92c
    ghidra: FUN_0802f92c
    name: sscanf
    size: 92
    sig: "i32 sscanf(const char *s, const char *fmt, ...)"
    module: scanf_helpers
    status: ported
    notes: >-
      Builds 52-byte ScanfState; engine via SCANF_ENGINE pointer
      (real target: format-engine prologue @ 0x080332a4 → 0x0803484c,
      next batch). Variadic capture deferred (stable Rust).

  - address: 0x08033170
    ghidra: FUN_08033170
    name: vsscanf
    size: 108
    module: scanf_helpers
    status: ported
    notes: NOT ISO vsscanf — scans ONE numeric/float conversion, reports
      consumed length; engine veneer @ 0x08036348 (next batch).

  # --- Batch 5: printf core/API, strtod, scanf converters ---
  - address: 0x08034374
    ghidra: FUN_08034374
    name: _printf
    size: 1164
    sig: "i32 _printf(PrintfState *state, const char *fmt, const u32 *args)"
    module: printf_core
    status: ported
    notes: Format engine. Flag table @ 0x08986234. 64-bit args fetched
      8-aligned. Unknown chars print verbatim; %% via unknown-char path.
      %e/%f/%g via FLOAT_CONVERTER hook, which now defaults to the real
      convert_fe port (0x08032d70).

  - address: 0x08032f14
    ghidra: FUN_08032f14
    name: _vsnprintf_dispatcher
    size: 76
    sig: "i32 _vsnprintf_dispatcher(fmt, putc, putc_ctx, args)"
    module: printf_core
    status: ported
    notes: Builds 68-byte PrintfState, installs str_emit, tail-calls _printf.

  - address: 0x08034804
    name: str_emit
    size: 48
    module: printf_core
    status: ported
    notes: Default emit_str hook (slice -> putc loop).

  - address: 0x0802f654
    ghidra: FUN_0802f654
    name: vsprintf
    size: 60
    module: printf_api
    status: ported

  - address: 0x0802f694
    ghidra: FUN_0802f694
    name: printf
    size: 64
    module: printf_api
    status: ported
    notes: Binds stdout FILE @ 0x08b2f864 + post-flush (semihost-dead, stubbed).

  - address: 0x0802f6dc
    ghidra: FUN_0802f6dc
    name: fprintf
    size: 68
    module: printf_api
    status: ported

  - address: 0x0802f724
    ghidra: FUN_0802f724
    name: sprintf
    size: 64
    module: printf_api
    status: ported

  - address: 0x0802f768
    ghidra: FUN_0802f768
    name: snprintf
    size: 88
    module: printf_api
    status: ported
    notes: C99 semantics verified (bound, NUL, would-be length return).

  - address: 0x08032f94
    ghidra: FUN_08032f94
    name: vsnprintf
    size: 84
    module: printf_api
    status: ported

  - address: 0x080331dc
    ghidra: FUN_080331dc
    name: strtod_core
    size: 164
    module: strtod
    status: ported
    notes: Two-pass (measure, convert) over vsscanf machinery; endptr via
      consumed count; dangling-exponent EOF case re-runs bounded.

  - address: 0x08031774
    ghidra: FUN_08031774
    name: atof
    size: 68
    sig: "f64 atof(const char *s)  // soft-float bits"
    module: strtod
    status: ported
    notes: errno-neutral (save/restore via errno_get/errno_set).

  - address: 0x08030dd4
    ghidra: FUN_08030dd4
    name: bsearch
    size: 140
    sig: "void *bsearch(key, base, nmemb, size, compar)"
    module: strtod
    status: ported
    notes: Was misidentified as a strtod wrapper; it IS the ADS bsearch.
  - address: 0x082cf348
    ghidra: FUN_082cf348
    name: store_u16_le
    size: 16
    sig: "void store_u16_le(u8 *p, u32 value)"
    module: sqlite/store_u16_le
    status: ported
    vars: {param_1: p, param_2: value}
    notes: >-
      Alignment-free two-byte little-endian store. The original is exactly
      strb r1,[r0]; lsr r1,r1,#8; strb r1,[r0,#1]; bx lr: AAPCS arguments
      are destination p in r0 and value in r1, the low sixteen bits are
      stored at p+0 and p+1, and no NULL check occurs. The two recovered
      callers (0x082e39f4 and 0x082e3bcc) use this shared helper for their
      two-byte output path. Host tests cover byte order, high-bit truncation,
      every byte alignment, both buffer boundaries, and untouched neighbours.


  - address: 0x0802f9c4
    ghidra: FUN_0802f9c4
    name: scanf_convert_int
    size: 472
    module: scanf_int
    status: ported
    notes: "%d/%i/%u/%x/%o; base prefixes, width budget, wrap (no saturate),
      -1 EOF-before-field, -2 no-digits. No ll path in this build (nop'd).
      Real converter for the engine dispatch (assigned 0x08035d50 was a
      CORDIC float helper)."

  - address: 0x08036018
    ghidra: FUN_08036018
    name: scanf_float_convert
    size: 816
    module: scanf_float
    status: ported
    notes: Decimal-digits -> soft-double back-end; 12-byte extended-fp math
      via SOFTFLOAT_OPS (pow10 0x08034228, ext_mul 0x08037534, ext_div
      0x080374d4 — next batch). ERANGE on |exp|>500; canonical inf flatten.

  - address: 0x08036348
    ghidra: FUN_08036348
    name: scanf_float_engine
    size: 1052
    status: duplicate
    notes: >-
      Batch-5 scouting record, superseded by the batch-7 ported entry
      below (module scanf/scanf_float_engine) — same address, same
      function. One correction to this scout: the inf/nan and 0x
      hex-float matchers are NULL VENEERS in retailOS (mov pc, lr @
      0x083ed1c0/0x083ed1bc), so no literal ever matches.

  - address: 0x0803677c
    ghidra: FUN_0803677c
    name: fread
    size: 656
    module: stdio/fread
    status: ported
    notes: ADS stdio buffered read (48-byte stream struct; semihost SYS_READ
      backed — dead on device). NOT a scanf converter as first assumed.

  - address: 0x08036a10
    ghidra: FUN_08036a10
    name: stream_read_chars
    size: 360
    module: stdio/fread
    status: ported
    notes: fread helper; zero callers in image (dead-linked).

  # --- Soft-float / libm region map (porting queue, next batches) ---
  # fplib region: 0x083eae74 (__d2f) -> 0x083ed1ff, ~9.1KB, the ONLY copy.
  # __dmul 0x083eba48 (106 calls), __dadd 0x083eaf2c (70), __i2d 0x083eb908
  # (65), __d2i 0x083eb7d0 (55), __fdiv 0x083ec5fc (44), __ddiv 0x083eb238
  # (39), __dsub 0x083ec09c (39), __i2f 0x083ec96c (37), __u2d 0x083eb9b4
  # (32), __fadd 0x083ec4b4 (30), __f2i 0x083ec898 (29), __u2f 0x083ec9a0
  # (29), __fmul 0x083eca20 (28), __fscalb 0x083ed150 (26), __dcmpgt/ge
  # 0x083ebe38 (20), __dcmplt/le 0x083eb9c0 (18), __drsb 0x83ebed8 (16),
  # __f2d 0x083ec450 (16), __f2u 0x083ec90c (16), __dscalb 0x083ed0dc (16),
  # __dcmpeq 0x083eb748 (10), __ll2d 0x083eb940 (10), __ull2d 0x083eb938 (7),
  # __ieee_status 0x083ece54 (7), __frsb 0x83ecc28 (5), __d2u 0x083eb880 (4),
  # __d2ll 0x083ece5c (4), __fsub 0x83ecc60 (3), __d2f 0x083eae74 (3),
  # __fcmpgt/ge 0x083ecba4 (3), fmod core 0x083ebc48 (3), _dsqrt 0x083ebf28,
  # _ddiv_core 0x083eb45c, __fcmpeq 0x083ec820, __fcmplt/le 0x083ec9a8,
  # __d2ull 0x083ecf34, __f2ll 0x083ecffc, _frnd 0x83ecdc4,
  # exception dispatchers 0x083eb144/0x083eb154/0x083eb1d4, trap decode
  # 0x083ed080. ABSENT: __f2ull, __ll2f, __ull2f.
  # libm: ceil 0x080317c4 (NOT _drnd), ceilf 0x08031914 (NOT _frnd),
  # floor 0x08031ab8, ldexp 0x08031c10 (NOT fmod), sin 0x080319a8,
  # cos 0x08031d24, sqrt 0x08031e44 (-> _dsqrt), sqrtf 0x08031ec8
  # (-> 0x08033f40), __kernel_sin 0x0803375c, __kernel_cos 0x08033dc8,
  # __kernel_rem_pio2 0x080338f4, _dpoly 0x08035a48, libm raise wrapper
  # 0x08032178, float wrapper 0x08031f44 + decompose 0x080340fc.

  # --- Batch 6: soft-float primitives (region 0x083eae74-0x083ed1ff) ---
  - address: 0x083ece54
    name: __ieee_status
    size: 8
    module: fp_scalb
    status: ported
    notes: ADS default stub, always returns 0.

  - address: 0x083ed0dc
    name: __dscalb
    size: 116
    module: fp_scalb
    status: ported
    notes: scalbn double; NaN -> descriptor 0x0400009b -> canonical qNaN.

  - address: 0x083ed150
    name: __fscalb
    size: 164
    module: fp_scalb
    status: ported

  - address: 0x083eb144
    name: fp_exception_dispatchers
    size: 280
    module: fp_scalb
    status: ported
    notes: >-
      0x083eb144 double NaN/err, 0x083eb154 double unordered-cmp,
      0x083eb1d4 float NaN/err, trap decode 0x083ed080 (nibbles 8/9/10).
      Modeled via FP_TRAP_HANDLER hook (default: canonical NaN).

  - address: 0x083eb748
    name: __dcmpeq
    size: 260
    module: fp_compare
    status: ported
    notes: Packed-CPSR return (N=8,Z=4,C=2,V=1); unordered=0xA.
      Denormal inputs flush to signed zero (NOT IEEE). gt/ge = swapped b-vs-a.

  - address: 0x083eb9c0
    name: __dcmplt/__dcmple
    size: 132
    module: fp_compare
    status: ported

  - address: 0x083ebe38
    name: __dcmpgt/__dcmpge
    size: 156
    module: fp_compare
    status: ported

  - address: 0x083ec820
    name: __fcmpeq
    size: 216
    module: fp_compare
    status: ported

  - address: 0x083ec9a8
    name: __fcmplt/__fcmple
    size: 116
    module: fp_compare
    status: ported

  - address: 0x083ecba4
    name: __fcmpgt/__fcmpge
    size: 128
    module: fp_compare
    status: ported

  - address: 0x083eb7d0
    name: __d2i
    size: 184
    module: fp_dconv
    status: ported
    notes: Truncate, saturate MIN/MAX, NaN->0 via FP_TRAP_HANDLER.

  - address: 0x0826f418
    ghidra: FUN_0826f418
    name: double_to_u32_saturating
    size: 56
    sig: "u32 double_to_u32_saturating(u64 x_bits)"
    module: fp_dconv
    status: ported
    renames: {param_1/r0: x_low_word, param_2/r1: x_high_word}
    notes: >-
      Input is an IEEE-754 double in r1:r0. Raw instructions construct
      double(u32::MAX) through __u2d, then invoke __dcmplt(limit, x). `bhi`
      tail-calls __d2u for limit > x and unordered (NaN); equality and x >=
      limit return u32::MAX directly. Decoded all image B/BL words: 8 plain
      BL callsites (0x083d24fc, 0x083d264c, 0x083d2824, 0x083d2974,
      0x083d2b4c, 0x083d2c9c, 0x083d2e74, 0x083d2fbc), no predicated calls.
  - address: 0x083eb880
    name: __d2u
    size: 132
    module: fp_dconv
    status: ported

  - address: 0x083eb908
    name: __i2d
    size: 48
    module: fp_dconv
    status: ported

  - address: 0x083eb9b4
    name: __u2d
    size: 12
    module: fp_dconv
    status: ported

  - address: 0x083eb940
    name: __ll2d
    size: 116
    module: fp_dconv
    status: ported
    notes: Round-to-nearest-even with sticky.

  - address: 0x083eb938
    name: __ull2d
    size: 8
    module: fp_dconv
    status: ported

  - address: 0x083ece5c
    name: __d2ll
    size: 212
    module: fp_dconv
    status: ported

  - address: 0x083ecf34
    name: __d2ull
    size: 148
    module: fp_dconv
    status: ported

  - address: 0x083ec898
    name: __f2i
    size: 120
    module: fp_fconv
    status: ported

  - address: 0x083ec90c
    name: __f2u
    size: 92
    module: fp_fconv
    status: ported

  - address: 0x083ec96c
    name: __i2f
    size: 52
    module: fp_fconv
    status: ported

  - address: 0x083ec9a0
    name: __u2f
    size: 8
    module: fp_fconv
    status: ported

  - address: 0x083ecffc
    name: __f2ll
    size: 204
    module: fp_fconv
    status: ported
    notes: >-
      Quirks: negatives>=1 -> +0; [2^63,2^64) wraps; >=2^64/+Inf -> -1.

  - address: 0x083eae74
    name: __d2f
    size: 180
    module: fp_fconv
    status: ported
    notes: No float denormals produced (flush to +0 before rounding).

  - address: 0x083ec450
    name: __f2d
    size: 96
    module: fp_fconv
    status: ported
    notes: Float denormals flush to +0.

  - address: 0x083eaf2c
    name: __dadd
    size: 500
    module: fp_dadd
    status: ported
    notes: >-
      70 callers. IEEE-exact core; quirks: NaN canonicalization,
      Inf+-Inf -> 0x7ff8...0001, denormal in/out flush to +0 (sign dropped).

  - address: 0x083ec09c
    name: __dsub
    size: 660
    module: fp_dadd
    status: ported
    notes: Contains shared sub-core 0x83ec0c4 + specials 0x83ec274.

  - address: 0x83ebed8
    name: __drsb
    size: 76
    module: fp_dadd
    status: ported
    notes: Computes b-a (flips FIRST operand sign, dispatches to add/sub cores).

  - address: 0x083eba48
    name: __dmul
    size: 448
    module: fp_dmul
    status: ported
    notes: >-
      106 callers (hottest fp op). IEEE-exact; quirks: underflow flush
      +0 always, denormal-input sign quirk, Inf*denormal -> NaN 0x7ff8...0001.

  - address: 0x083eb238
    name: __ddiv
    size: 516
    module: fp_ddiv
    status: ported
    notes: Ported as restoring division (NR seed-table core 0x083eb45c
      documented, not mirrored). Inf/Inf, 0/0 -> 0x7ff8...0001.

  - address: 0x083ec4b4
    name: __fadd

    size: 308
    module: fp_fadd
    status: ported
    notes: >-
      Quirk: magnitude-add no-carry path rounds ties AWAY (not RNE) —
      verified in disassembly, pinned by tests.

  - address: 0x83ecc60
    name: __fsub
    size: 352
    module: fp_fadd
    status: ported

  - address: 0x83ecc28
    name: __frsb
    size: 56
    module: fp_fadd
    status: ported

  - address: 0x83ecdc4
    name: _frnd
    size: 140
    module: fp_fadd
    status: ported
    notes: >-
      DEAD-LINKED. Not a float round-to-int: treats input as fixed-point
      scaled by 2^-43, returns sign-magnitude integer. Ported as-is.

  - address: 0x083eca20
    name: __fmul
    size: 284
    module: fp_fmuldiv
    status: ported
    notes: Port implements full IEEE gradual underflow (original flushes
      denormals everywhere); canonical NaN 0x7fc00001.

  - address: 0x083ec5fc
    name: __fdiv
    size: 472
    module: fp_fmuldiv
    status: ported
    notes: Restoring division (seed-table NR documented, not mirrored).

  - address: 0x083ebf28
    name: _dsqrt
    size: 348
    module: fp_misc
    status: ported
    notes: Exactly rounded (host f64::sqrt oracle). Negative -> 0x7ff8...0001.

  - address: 0x083ebc48
    name: __dmod
    size: 484
    module: fp_misc
    status: ported
    notes: >-
      CORRECTION: this is IEEE remainder() (ties-to-even quotient),
      NOT fmod. Sole caller: __kernel_rem_pio2. Verified vs libm remainder.

  # --- Batch 7: libm, printf float, scanf engines ---
  - address: 0x080317c4
    ghidra: FUN_080317c4
    name: ceil
    size: 304
    module: libm/ceilfloor
    status: ported
    notes: fdlibm sign-magnitude bit-mask round (NOT _drnd as recon said).

  - address: 0x08031ab8
    ghidra: FUN_08031ab8
    name: floor
    size: 312
    module: libm/ceilfloor
    status: ported

  - address: 0x08031914
    ghidra: FUN_08031914
    name: ceilf
    size: 136
    module: libm/ceilfloor
    status: ported

  - address: 0x08031d24
    ghidra: FUN_08031d24
    name: sin
    size: 260
    module: libm/trig
    status: ported
    notes: >-
      CORRECTION — recon had sin/cos wrappers swapped: 0x08031d24 is
      sin (calls __kernel_sin), 0x080319a8 is cos.

  - address: 0x080319a8
    ghidra: FUN_080319a8
    name: cos
    size: 244
    module: libm/trig
    status: ported

  - address: 0x08033dc8
    ghidra: FUN_08033dc8
    name: __kernel_sin
    size: 364
    module: libm/kernel_trig
    status: ported
    notes: CORRECTION — uses S2..S6 + S1 with iy tail (netlib __kernel_sin).
      0x0803375c is __kernel_cos (C1..C6, qx tail, no iy).

  - address: 0x0803375c
    ghidra: FUN_0803375c
    name: __kernel_cos
    size: 384
    module: libm/kernel_trig
    status: ported

  - address: 0x08035a48
    ghidra: FUN_08035a48
    name: _dpoly
    size: 244
    module: libm/kernel_trig
    status: ported
    notes: Horner evaluator (coeff*, n, x).

  - address: 0x080338f4
    ghidra: FUN_080338f4
    name: __kernel_rem_pio2
    size: 1136
    module: libm/rem_pio2
    status: ported
    notes: pio2 hi/lo x3 pairs @ 0x089860d8-0x08986108 (textbook Sun values),
      HUGE_SCALE[18] @ 0x08986048 (NOT powers of two — 4 negative entries,
      |x| >= 2^106 deterministic-but-meaningless, ported bug-for-bug).
      Calls __dmod (fp_misc) x3 for large args.

  - address: 0x08031e44
    ghidra: FUN_08031e44
    name: sqrt
    size: 112
    module: libm/sqrt
    status: ported
    notes: NaN pre-filter + dcmp domain check, tails into _dsqrt (fp_misc);
      EDOM via errno_set.

  - address: 0x08031ec8
    ghidra: FUN_08031ec8
    name: sqrtf
    size: 112
    module: libm/sqrt
    status: ported

  - address: 0x08033f40
    ghidra: FUN_08033f40
    name: _fsqrt_core
    size: 420
    module: libm/sqrt
    status: ported
    notes: Newton on float via __fmul/__i2f/__f2d.

  - address: 0x08031c10
    ghidra: FUN_08031c10
    name: ldexp
    size: 224
    module: libm/misc
    status: ported
    notes: __ieee_status save/restore + __dscalb; ERANGE on over/underflow
      (NOT fmod as early recon said).

  - address: 0x08032178
    name: errno_set_dup
    size: 20
    status: duplicate
    notes: Disassembly shows this IS errno_set (already ported in
      runtime/errno), not a libm raise wrapper.

  - address: 0x08031f44
    name: modff_wrapper
    size: 52
    module: libm/misc
    status: ported
    notes: Calls decomposer 0x080340fc, tails into __fadd of two stack parts.

  - address: 0x080340fc
    ghidra: FUN_080340fc
    name: frexpf_decompose
    size: 276
    module: libm/misc
    status: ported

  - address: 0x08032514
    ghidra: FUN_08032514
    name: dtoa
    size: 724
    module: printf/printf_float_dtoa
    status: ported
    notes: soft-double -> decimal digits + exponent for %e/%f/%g.

  - address: 0x08034228
    name: pow10_ext
    size: 316
    module: printf/printf_float_dtoa
    status: ported

  - address: 0x08037024
    name: ext_fp_add
    size: 712
    module: printf/printf_float_dtoa
    status: ported
    notes: 12-byte extended-fp format (same as scanf_float SOFTFLOAT_OPS).

  - address: 0x08037564
    name: ext_fp_mul
    size: 612
    module: printf/printf_float_dtoa
    status: ported

  - address: 0x0802e118
    ghidra: FUN_0802e118
    name: block_sub_bytes
    size: 60
    module: printf/printf_float_dtoa
    status: ported
    notes: CORRECTION — with 0x0802e154/0x0802e190/0x0802e1f4 these are
      SubBytes/InvSubBytes/ShiftRows/InvShiftRows of an AES-like proprietary
      cipher (tables @ 0x0891ee4c/0x0891ef4c, NOT standard AES S-box).
      Misidentified as dtoa helpers.

  - address: 0x08032d70
    ghidra: FUN_08032d70
    name: convert_float
    size: 420
    module: printf/printf_float
    status: ported
    notes: "%e/%f/%g wrapper; FLOAT_CONVERTER hook in printf_core targets this."

  - address: 0x0803285c
    ghidra: FUN_0803285c
    name: fp_format
    size: 1264
    module: printf/printf_float
    status: ported
    notes: Locale decimal_point via libspace+0x2c; precision default 6.

  - address: 0x08036348
    ghidra: FUN_08036348
    name: scanf_float_engine
    size: 1052
    module: scanf/scanf_float_engine
    status: ported
    notes: >-
      Width-limited collection into 0xff-terminated digit buffers, calls
      scanf_float_convert. The inf/nan (@ 0x08036420) and 0x hex-float
      dispatches call NULL VENEERS (mov pc, lr) — literals never match
      in retailOS; see the module docs. Ghidra mis-decompiles the
      EOF-during-ws-skip return (0, not -1) @ 0x080363b8.

  - address: 0x0803484c
    name: _scanf_engine
    size: 1564
    module: scanf/scanf_engine
    status: ported
    notes: >-
      Format walker + %s/%c/%[ handling. Ghidra wrong at 0x080363b8:
      EOF during whitespace skip returns 0, not -1.

  - address: 0x080332a4
    name: _scanf_prologue
    size: 84
    module: scanf/scanf_engine
    status: ported
    notes: Fills fmt_cursor/fmt_getc/ctype/scanset, tail-calls engine.

  # --- Batch 8: retailOS heap (0x0819cd5c..0x0819d9d8) + utilities ---
  # Heap data structures: see src/heap/types.rs (block header 8B, descriptor
  # 0x398B, size-sorted free list with boundary-tag coalescing).
  # Default heap: 32KB @ 0x08a12710, descriptor @ 0x08a1a710 (lazy init).
  # C++ operator new (tag 2, 1797 call sites) is the dominant consumer.

  - address: 0x0819d048
    ghidra: FUN_0819d048
    name: heap_alloc_core
    size: 668
    module: heap/alloc_core
    status: ported
    sig: "void *heap_alloc_core(HeapDescriptor *desc, u32 size, u32 zero_fill, u32 tag, void *old_ptr, u32 copy_flag, u32 oom_trace_disable)"
    vars: {param_1: desc, param_2: size, param_3: zero_fill, param_4: tag, param_5: old_ptr, param_6: copy_flag, param_7: oom_trace_disable, uVar1: raw_size, uVar8: alloc_size, puVar3: result, puVar7: block, unaff_r10: old_size, uVar5: next_flags, puVar2: absorbed, FUN_0819cf68: heap_add_region, FUN_0819d6cc: heap_lock, FUN_0819cde4: heap_unlock, FUN_0819ce28: heap_freelist_alloc, FUN_0819cd5c: heap_stats_sub, FUN_0819d314: heap_free_insert, FUN_0819d4dc: heap_free, FUN_08037db0: __rt_memcpy, FUN_08030f44: heap_panic, FUN_082bc4fc: cg_timer_wait, DAT_0819d2e4: oom_trace_gate_ptr, DAT_0819d2e8: oom_trace_event, DAT_0819d2ec: oom_trace_ticks}
    notes: >-
      Combined alloc+realloc engine; round (n+3)&~3+8 min 16 then 8-align;
      in-place merge on realloc grow, tail split on shrink/merged; ROM
      memcpy for copies (= __rt_memcpy). Register contract: r0-r3 then
      three stack args (old_ptr, copy_flag, oom_trace_disable). Every
      callee is ported and called through the ALLOC_ENGINE_HOOKS /
      HEAP_LOCK_HOOKS dispatch tables whose defaults are the real ports:
      heap_add_region 0x0819cf68 (first-use init, auto_init slot),
      heap_lock 0x0819d6cc / heap_unlock 0x0819cde4 (lock pair),
      heap_freelist_alloc 0x0819ce28 (direct call), heap_stats_sub
      0x0819cd5c (stats_retag slot), heap_free_insert 0x0819d314
      (freelist_insert slot; host cfg(test) uses offset links and mocks
      this slot), heap_free 0x0819d4dc (free_core slot), __rt_memcpy
      0x08037db0 (direct, libc.rs), heap_panic 0x08030f44 (non-returning,
      veneers.rs; hook takes a port-added HEAP_PANIC_* reason the default
      shim drops; panic paths do not unlock, like the original).
      Deviation: the OOM low-memory trace — three cg_timer_wait 0x082bc4fc
      calls @ 0x0819d278/0x0819d284/0x0819d290 (ticks 0x4b0 / a global /
      0x960 against one event handle), gated by stack arg oom_trace_disable
      == 0, result == NULL and the byte @ 0x089caf6e — is omitted; it only
      emits debug output on failure. Host tests: size-rounding boundary
      table (0,1,3,4,5,8,9,0x10), 8-align return contract, auto-init
      taken/not-taken, NULL vs non-NULL old_ptr, panic routing captured
      via a siglongjmp seam.

  - address: 0x0819ce28
    ghidra: FUN_0819ce28
    name: heap_freelist_alloc
    size: 316
    module: heap/alloc_core
    status: ported
    notes: First-fit over size-sorted free list; split if remainder >= n+19&~3.

  - address: 0x0819d314
    ghidra: FUN_0819d314
    name: heap_free_insert
    size: 456
    module: heap/free_path
    status: ported
    notes: >-
      Boundary-tag coalescing (footer copy at block_end-4), sorted insert.
      Single insert helper for all three call sites (heap_add_region
      0x0819cf68, alloc split path 0x0819ce28, heap_free 0x0819d4dc); all
      heap dispatch-table defaults are now wired to the real ports.

  - address: 0x0819d4dc
    ghidra: FUN_0819d4dc
    name: heap_free
    size: 200
    module: heap/free_path
    status: ported
    notes: Header validation, stats debit, coalesce+insert.

  - address: 0x0819d5a4
    ghidra: FUN_0819d5a4
    name: heap_desc_init
    size: 212
    module: heap/init
    status: ported
    notes: Sentinel const @ 0x08a77c0c is beyond image — synthesized all-zero
      head node (consumers require NULL-terminated ascending list).

  - address: 0x0819cf68
    ghidra: FUN_0819cf68
    name: heap_add_region
    size: 200
    module: heap/init
    status: ported
    notes: Max 20 regions; zero-word terminator; one big free block.

  - address: 0x0819d7b4
    ghidra: FUN_0819d7b4
    name: heap_create
    size: 20
    module: heap/init
    status: ported

  - address: 0x0819d7c8
    ghidra: FUN_0819d7c8
    name: heap_create_empty
    size: 28
    module: heap/init
    status: ported

  - address: 0x0819d7e4
    ghidra: FUN_0819d7e4
    name: heap_destroy
    size: 32
    module: heap/init
    status: ported

  - address: 0x0819d67c
    ghidra: FUN_0819d67c
    name: heap_alloc
    size: 36
    module: heap/wrappers
    status: ported

  - address: 0x0819d2f0
    ghidra: FUN_0819d2f0
    name: heap_alloc_tag1
    size: 36
    module: heap/wrappers
    status: ported
    notes: The '1' goes to the OOM-report-suppress stack arg, NOT the tag.

  - address: 0x0819ce00
    ghidra: FUN_0819ce00
    name: heap_alloc_zero
    size: 36
    module: heap/wrappers
    status: ported
    notes: calloc-style zerofill.

  - address: 0x0819d6a0
    ghidra: FUN_0819d6a0
    name: heap_realloc
    size: 44
    module: heap/wrappers
    status: ported

  - address: 0x0819d6cc
    ghidra: FUN_0819d6cc
    name: heap_lock
    size: 72
    module: heap/wrappers
    status: ported
    notes: Lazy RTXC mutex create (0x080744a4) once kernel is up (0x0809444c).

  - address: 0x0819cde4
    ghidra: FUN_0819cde4
    name: heap_unlock
    size: 16
    module: heap/wrappers
    status: ported

  - address: 0x0819d804
    name: named_heap_registry
    size: 304
    module: heap/wrappers
    status: ported
    notes: 3-slot table @ 0x08ad7860, refcounted 16-byte name nodes
      (0x0819d804/0x0819d818/0x0819d858).

  - address: 0x0819d714
    ghidra: FUN_0819d714
    name: heap_stats_add
    size: 160
    module: heap/stats
    status: ported
    notes: Tag/class halfwords in header; per-tag/class/bin counters; peak.

  - address: 0x0819cd5c
    ghidra: FUN_0819cd5c
    name: heap_stats_sub
    size: 136
    module: heap/stats
    status: ported

  - address: 0x080e837c
    ghidra: FUN_080e837c
    name: log2_floor
    size: 52
    module: heap/stats
    status: ported
    notes: 5-step binary-search CLZ; table @ 0x083e9b60 outside image
      (canonical table reconstructed, behavior exact). Returns 0 for input 0.

  - address: 0x22003eb0
    name: size_to_class
    size: 0
    module: heap/stats
    status: ported
    notes: ROM call (mask ROM, not in image) — reimplemented as documented
      log2(size/8) clamped 0..79, UNVERIFIED.

  - address: 0x080eb67c
    ghidra: FUN_080eb67c
    name: malloc_wrapper
    size: 40
    module: heap/veneers
    status: ported
    notes: 96 call sites; lazy-init + default heap, tail heap_alloc.

  - address: 0x080e7970
    ghidra: FUN_080e7970
    name: free_wrapper
    size: 40
    module: heap/veneers
    status: ported
    notes: 62 sites; NO NULL guard here (heap_free handles NULL).

  - address: 0x0807b254
    ghidra: FUN_0807b254
    name: calloc_wrapper
    size: 44
    sig: "u8 *calloc_wrapper(usize size, usize tag)"
    module: heap/veneers
    status: ported
    notes: >-
      malloc_wrapper's zerofill sibling; lazy_init_default_heap
      (0x08077250), then tail branch to heap_alloc_zero (0x0819ce00)
      with (*0x089ca638, size, tag). No size*count multiply — callers
      pass a byte count. 14 bl call sites (among them name_node_alloc @
      0x0809388c, tag 8). functions.csv says 80 bytes, but that lumps in
      the unrelated 12-byte getter @ 0x0807b280 (*0x089d03bc +4 +0x3c);
      real extent is 40 code + 4 literal. New HEAP_OPS.alloc_zero slot
      (default shim over the ported heap_alloc_zero). match.py
      structural (lazy init + volatile ops-table loads inlined; tail
      branch through the slot).

  - address: 0x0807b180
    ghidra: FUN_0807b180
    name: default_heap_free_bytes_get
    size: 24
    sig: "u32 default_heap_free_bytes_get(void)"
    module: runtime/global_state
    status: ported
    renames:
      DAT_0807b198: default_heap_global_literal
      DAT_089ca638: default_heap_descriptor
      r0: default_heap_free_bytes
    notes: >-
      Raw ARM is `bl 0x08077250; ldr r0,[pc,#8]; ldr r0,[r0]; ldr
      r0,[r0]`: it first runs lazy_init_default_heap, then performs two
      unchecked loads through the default-heap global at 0x089ca638.
      The established HeapDescriptor layout names word zero free_bytes.
      The port calls the existing guard and follows the existing
      DEFAULT_HEAP seam with a raw u32 load; it has no null check,
      cache, or ownership behavior. Recovered callers include the
      photo-database metric code at 0x08068504 and the threshold check
      at 0x080765ec.

  - address: 0x0807b19c
    ghidra: FUN_0807b19c
    name: default_heap_init_return_high_word
    size: 40
    sig: "u64 default_heap_init_return_high_word(u32 request)"
    module: heap/veneers
    status: ported
    renames:
      param_1/r0: request
      r4: saved_request
      FUN_08077250: lazy_init_default_heap
      DAT_089ca638: default_heap_descriptor
    notes: >-
      Verified 36 instruction bytes plus the literal at 0x0807b1bc. Raw ARM
      is `push {r4,lr}; mov r4,r0; bl 0x08077250; ldr r0,[pc,#12]; mov
      r1,r4; ldr r0,[r0]; pop {r4,lr}; b 0x0819cdf8`. The destination's
      verified two-word body is `mov r0,#0; bx lr`; its identity is
      unrecovered, so the port does not invent one. The resulting 64-bit ABI
      pair is r0 = 0 and r1 = request, represented as `(request as u64) <<
      32`; no allocation or dereference follows lazy initialization. Whole-
      image B/BL decode finds exactly 9 inbound calls, all unconditional bl
      (none predicated): 0x08117a9c, 0x0812ba88, 0x0816d0a4, 0x0816d0d4,
      0x0816d2e0, 0x0816d3fc, 0x0816d7ac, 0x081cb22c, and 0x08289d28.
  - address: 0x0807b280
    ghidra: FUN_0807b280
    name: global_indirect_word_get
    size: 16
    sig: "u32 global_indirect_word_get(void)"
    module: heap/state
    status: ported
    renames:
      DAT_0807b290: global_indirect_holder_literal
      DAT_089d03bc: global_indirect_holder
      iVar1/r0: opaque_state
      uVar2/r0: state_word
    notes: >-
      The 12-byte instruction body at 0x0807b280 is `ldr r0,[pc,#8]`
      (literal 0x089d03bc at 0x0807b290), `ldr r0,[r0,#4]`, `ldr
      r0,[r0,#0x3c]`, `bx lr`; functions.csv's 16-byte extent includes
      the literal. It performs two unchecked pointer dereferences and
      returns the +0x3c raw u32, with no ownership or bit meaning inferred.
      The recovered direct call is `bl 0x0807b280` at 0x080a0ca4 in
      FUN_080a0c60: its result is held in r7 while that caller clears bit
      0 of a different object's +0x3c word, then restores the raw value.
      The 0x089d03bc runtime holder is modeled as a packed crate static
      preserving its +4 pointer slot; host tests rebind that exact slot,
      prove +0x38 is not read, retain all u32 bits, and prove each call
      rereads the indirection.

  - address: 0x080edbf0
    ghidra: FUN_080edbf0
    name: realloc_wrapper
    size: 56
    module: heap/veneers
    status: ported

  - address: 0x080ee2b4
    ghidra: FUN_080ee2b4
    name: global_indirect_status_get
    size: 16
    sig: "u32 global_indirect_status_get(void)"
    module: heap/state
    status: ported
    renames:
      DAT_080ee2c4: global_indirect_holder_literal
      DAT_089d03bc: global_indirect_holder
      iVar1/r0: opaque_state
      uVar2/r0: status_word
    notes: >-
      Raw ARM confirms 16 instruction bytes at 0x080ee2b4: `ldr
      r0,[pc,#8]` (literal 0x089d03bc at 0x080ee2c4), `ldr r0,[r0,#4]`,
      `ldr r0,[r0,#0x18]`, `bx lr`; 0x080ee2c8 is the independently linked
      next function. It performs two unchecked pointer dereferences and
      returns the opaque state object's +0x18 raw u32. Whole-image decoding
      finds exactly ten inbound B/BL-immediate calls, all unconditional `bl`
      (none predicated): 0x080774ec, 0x080774f8, 0x0807753c, 0x0807756c,
      0x0807759c, 0x08087a6c, 0x080a0b48, 0x080dbc48, 0x0836a440, and
      0x0836aad0. Callers test result bits but do not establish their
      meanings. The port reuses heap/state's existing packed holder model
      for 0x089d03bc and intentionally adds no null guard, cache, ownership,
      or bit interpretation.

  - address: 0x08077250
    ghidra: FUN_08077250
    name: lazy_init_default_heap
    size: 44
    module: heap/veneers
    status: ported
    notes: 32KB heap @ 0x08a12710, desc @ 0x08a1a710, global @ 0x089ca638.

  - address: 0x082aadd4
    name: operator_new
    size: 20
    module: heap/veneers
    status: ported
    notes: Tag 2; 1797 call sites — the dominant allocator. delete @
      0x082aad24 (tag 2, NULL-guarded), tag-3 pair 0x082aad74/0x82aad14,
      checked new 0x08266c70 (new-handler code 3).

  - address: 0x082aaddc
    ghidra: FUN_082aaddc
    name: placement_new
    size: 8
    sig: "void *placement_new(size_t size, void *location)"
    module: heap/veneers
    status: ported
    renames:
      param_1/r0: size
      param_2/r1: location
      r0: returned_location
    notes: >-
      Raw ARM is exactly `mov r0,r1; mov pc,lr` at 0x082aaddc..0x082aade0:
      C++ placement allocation ignores the requested size and returns its
      caller-supplied location unchanged, including NULL, without reading or
      writing that storage or touching the heap. Scanning every ARM B/BL word
      in osos.dec found 18 direct calls: all are unconditional `bl` (zero
      predicated forms): two from C++ runtime error-path scratch construction
      @ 0x08266b00/0x08266da8 and sixteen from 0x082a8f00's initialization
      routine. No dispatch seam is needed because this is a leaf identity.
      Deviation: Rust models C++ placement new as an explicit function;
      #[inline(never)] and its own target-only text section preserve a
      distinct device call target.

  - address: 0x082aad24
    ghidra: FUN_082aad24
    name: operator_delete
    size: 16
    sig: "void operator_delete(void *ptr)"
    module: heap/veneers
    vars: {param_1: ptr}
    status: ported
    notes: >-
      Tag 2, NULL-guarded (verified against osos.asm: `cmp r0,#0x0;
      movne r1,#0x2; bne 0x080e7970; bx lr` @ 0x082aad24..0x082aad34) —
      the dominant deallocator, 665 call sites. NULL is a no-op; non-NULL
      tail-branches into free_wrapper @ 0x080e7970 with tag 2. The port
      was already in veneers.rs (written alongside operator_new); this
      entry records it. Deviation: the free dispatch is indirect through
      the HEAP_OPS.free slot (the file's documented ops-table pattern)
      instead of the original's direct tail branch; the NULL guard is
      instruction-faithful. #[inline(never)] keeps the branch boundary so
      callers' matches survive (the operator_new rationale).
      Relationship: cxx_array_dealloc @ 0x08266f2c (ported) is a bare
      `b 0x082aad24`, so both entries' hooks now point at real symbols.
      match.py structural: `cmp r0,#0` guard + early return correspond;
      the rest is the inlined lazy-init + ops-slot free tail call, the
      documented deviation.

  - address: 0x082aad4c
    ghidra: FUN_082aad4c
    name: string_object_equals
    size: 40
    sig: "int string_object_equals(const StringObject *this, const StringObject *other)"
    module: cxx/string_object
    status: ported
    vars: {param_1/r0: this, param_2/r1: other, r4: this_saved, uVar2/r0: compare_result}
    notes: >-
      Raw ARM is exactly ten code words at 0x082aad4c..0x082aad70; the
      adjacent operator_new_tag3 starts at 0x082aad74. It saves this in r4,
      calls string_object_c_str @ 0x082a50b0 over other, then loads this's
      raw payload word and calls utf8_strcmp_safe @ 0x08276d64. The
      `rsbs r0,r0,#1; movcc r0,#0` result is 1 only when the comparator
      returned 0, otherwise 0. No object-pointer guard exists; NULL this or
      other faults. Decoding every B/BL word in osos.dec found 31 direct
      calls, all unconditional `bl` (none predicated), including
      0x0827eb80's compare of two enclosing StringObjects before its state
      transition. Both callees are ported, so the Rust port calls them
      directly; there is no dispatch seam or other deviation. Host tests
      cover equal decoded UTF-8, unequal decoded codepoints and lengths,
      both NULL payloads, and one NULL payload.

  - address: 0x082aad74
    ghidra: FUN_082aad74
    name: operator_new_tag3
    size: 8
    sig: "void *operator_new_tag3(size_t size)"
    module: heap/veneers
    vars: {param_1: size}
    status: ported
    notes: >-
      C++ `operator new` for caller tag 3 — the alloc half of the tag-3
      pair whose delete is 0x082aad14. Whole body, verified against the raw
      image (osos.dec, load base 0x08000000, words e3a01003 eaf9023f):
      `mov r1,#3; b 0x080eb67c`, i.e. pin the caller tag to 3 and tail-branch
      into malloc_wrapper @ 0x080eb67c (already ported). 111 `bl` call sites
      and 0 `b`, counted by scanning every ARM branch word in the image (101
      of the 111 fall inside a Ghidra-known function extent; the remainder
      sit in regions functions.csv does not cover). No NULL/zero guard: any
      size, 0 included, reaches the heap core, and the returned pointer flows
      back verbatim — NULL means the allocation failed and is not retried
      here (that is operator_new_checked @ 0x08266c70). Deviations: the tail
      branch is a plain call in Rust, and the alloc dispatch goes through the
      HEAP_OPS.alloc slot (veneers.rs's documented ops-table pattern) rather
      than branching to 0x080eb67c directly. #[inline(never)] keeps the
      branch boundary — an 8-byte body is exactly what LLVM inlines away,
      which would drag the lazy-heap-init path into all 111 callers and
      destroy their matches. match.py structural: `mov r2,#3` (the tag) plus
      the trailing `bx r0` tail call through the ops slot correspond; the
      body in between is the inlined lazy init, the documented deviation —
      instruction-for-instruction the same shape the accepted tag-2
      operator_new @ 0x082aadd4 produces (26 instructions each).

  - address: 0x082aad14
    ghidra: FUN_082aad14
    name: operator_delete_tag3
    size: 16
    sig: "void operator_delete_tag3(void *ptr)"
    module: heap/veneers
    vars: {param_1: ptr}
    status: ported
    notes: >-
      C++ `operator delete` for caller tag 3, and the immediate neighbour
      *below* the tag-2 delete @ 0x082aad24 — a distinct function, not the
      same one. Whole body, verified against the raw image (words e3500000
      13a01003 1af8f313 e12fff1e): `cmp r0,#0; movne r1,#3; bne 0x080e7970;
      bx lr` — NULL returns without touching the heap, non-NULL tail-branches
      into free_wrapper @ 0x080e7970 (already ported) with tag 3. 155 `bl`
      call sites (105 unconditional + 50 `blne`) plus 3 tail `b`, counted by
      scanning every ARM branch word in the image; 145 of the `bl` fall
      inside a Ghidra-known function extent. The NULL guard here against its
      absence in operator_new_tag3 @ 0x082aad74 is real and deliberate — the
      C++ standard requires it on delete and not on new, and both host tests
      pin that asymmetry. Deviations: the conditional tail branch is a
      guarded plain call, and the free dispatch goes through the HEAP_OPS.free
      slot instead of branching to 0x080e7970 directly. #[inline(never)] for
      the 155 `bl` targets. match.py structural: the `cmp r0,#0` guard and
      its early return correspond one-to-one; the rest is the inlined lazy
      init plus the ops-slot free tail call, matching the shape already
      accepted for the tag-2 operator_delete @ 0x082aad24.

  - address: 0x0804938c
    ghidra: FUN_0804938c
    name: free_tag4_if_nonnull
    size: 12
    sig: "void free_tag4_if_nonnull(void *ptr)"
    module: heap/veneers
    vars: {param_1: ptr}
    status: ported
    notes: >-
      NULL-guarded front-end of the existing tag-4 MemH deallocator
      free_tag4 @ 0x0805d070. Whole body verified against osos.dec:
      e3500000 1a004f36 e12fff1e (`cmp r0,#0; bne 0x0805d070; bx lr`).
      Ghidra's 12-byte extent is right: 0x08049398 begins the distinct
      `b 0x082aad24` function. Scanning every ARM B/BL word found 11
      callsites: 8 plain `bl` and 3 `blne` (at 0x0806d254, 0x0806d27c,
      and 0x0806d740). Thus NULL normally reaches this wrapper only from
      its eight unconditional callers; the three predicated callers add a
      redundant outer guard. The port keeps the guard and invokes the
      already-ported free_tag4 for non-NULL pointers. Deliberate deviation:
      Rust emits a guarded call rather than the original conditional tail
      branch, and free_tag4 dispatches through HEAP_OPS.free. Host tests
      prove NULL avoids lazy initialization and freeing, while a non-NULL
      value is forwarded unchanged with tag 4. #[inline(never)] preserves
      a real device-call target. match.py structural review against this
      worktree's ARM archive reports the same `cmp r0,#0` guard, conditional
      early return, and tail branch; Rust adds an ABI frame, so it is 6
      instructions rather than the original 3.

  - address: 0x0805d070
    ghidra: FUN_0805d070
    name: free_tag4
    size: 8
    sig: "void free_tag4(void *ptr)"
    module: heap/veneers
    vars: {param_1: ptr}
    status: ported
    notes: >-
      The tag-4 deallocation entry. Whole body, verified against the raw
      image (words e3a01004 ea022a3d): `mov r1,#4; b 0x080e7970` — an
      unconditional tail branch into free_wrapper @ 0x080e7970 (already
      ported) with caller tag 4. Extent confirmed by the neighbours: the
      word below @ 0x0805d06c is the literal 0x4d656d48 ("MemH") belonging
      to the handle destructor @ 0x0805d028, and 0x0805d078 starts the
      memset-style fill, so the function is exactly 8 bytes. Call sites
      counted by decoding every ARM B/BL word in osos.dec: 46 `bl` + 12
      `blne` = 58 `bl`-form, plus 9 `b` + 4 `bne` tail branches = 71 total.
      Unlike the tag-2/tag-3 operator deletes there is NO NULL guard here,
      which is why a fifth of the `bl`-form sites are `blne` — the callers
      guard. Tag 4 belongs to the "MemH" managed-buffer family around this
      veneer, whose header carries the magic 0x4d656d48 at +0x04, whose
      constructor is 0x0805d10c (16-byte header + payload, both tag 4),
      whose destructor is 0x0805d028, and whose alloc twins are the
      identically shaped 8-byte veneers 0x0805d1d4 (`mov r1,#4; b
      malloc_wrapper`) and 0x0805d1dc (`mov r1,#4; b calloc_wrapper`) —
      all still unported. Deviations: the tail branch is a plain call, and
      the free dispatch goes through the HEAP_OPS.free slot instead of
      branching to 0x080e7970 directly. #[inline(never)] for the 58 `bl`
      targets. match.py structural: the port is the inlined lazy heap init
      plus the ops-slot free tail call with `mov r2,#4` carrying the tag —
      the same shape already accepted for operator_new_tag3 @ 0x082aad74
      and operator_delete_tag3 @ 0x082aad14.

  - address: 0x0805d0f4
    ghidra: FUN_0805d0f4
    name: bcopy_guarded
    size: 24
    sig: "void bcopy_guarded(const void *src, void *dst, i32 len)"
    module: libc/bcopy_guarded
    vars: {param_1: src, param_2: dst, param_3: len}
    status: ported
    notes: >-
      BSD-argument-order memmove adapter with a signed-length guard.
      Whole body, verified against the raw image (words e1a03000
      e3520000 e1a00001 a1a01003 aaff6b3d e12fff1e): `mov r3,r0; cmp
      r2,#0; mov r0,r1; movge r1,r3; bge 0x08037e00; bx lr` — swaps
      src/dst and tail-branches the rom_memmove veneer @ 0x08037e00
      (`ldr pc,[pc,#-4]` onto ROM memmove @ 0x220000d4, IRAM mirror of
      the ported osos memmove @ 0x080000d4) when len >= 0 signed;
      negative len is a no-op. Extent confirmed by the neighbour:
      0x0805d10c starts a new function (`push {r4,r5,r6,lr}`, the
      "MemH" managed-buffer constructor), so the function is exactly 24
      bytes — Ghidra's size is right. Call sites counted by decoding
      every ARM B/BL word in osos.dec: exactly 15 `bl`-form (14 plain
      `bl` + 1 `blne` @ 0x0808d3ac — one caller additionally gates on a
      NE condition; the length guard itself is unconditional inside the
      callee), no tail `b`, matching Ghidra's count. Sibling of bcopy @
      0x08042cbc (same swap, no guard). Deviation (house precedent, see
      rom_memmove_keep_dst and bcopy): the port calls the Rust memmove
      directly instead of dispatching through the ROM veneer.
      #[inline(never)] for the 15 `bl` targets. match.py structural:
      same guard-first shape (`cmp r2,#0; bxmi lr` vs the original's
      predicated movge/bge — LLVM branches out early instead), then the
      same r0/r1 swap into __aeabi_memmove; LLVM uses a framed non-tail
      `bl __aeabi_memmove` where the original tail-branches the veneer.

  - address: 0x0805d1dc
    ghidra: FUN_0805d1dc
    name: calloc_tag4
    size: 8
    sig: "u8 *calloc_tag4(usize size)"
    module: heap/veneers
    vars: {param_1: size}
    status: ported
    notes: >-
      The zerofill-alloc entry of the "MemH" managed-buffer family (free
      half: free_tag4 @ 0x0805d070). Whole body, verified against the raw
      image (words e3a01004 ea00781b): `mov r1,#4; b 0x0807b254` — an
      unconditional tail branch into calloc_wrapper @ 0x0807b254 (already
      ported) with caller tag 4. Extent confirmed by the neighbour: the
      word below @ 0x0805d1e4 is `push {r4-r8,lr}` starting the MemH
      handle resize/realloc function, so the function is exactly 8 bytes
      (Ghidra's size is right for once). Call sites counted by decoding
      every ARM B/BL word in osos.dec: exactly 23 `bl`, none predicated,
      no tail `b` — matching Ghidra's count. They cluster at
      0x080585a0..0x080588c0 (5) and 0x0805df44..0x0805e580 (7), the rest
      scattered. Structural twin of the still-unported malloc veneer @
      0x0805d1d4 (`mov r1,#4; b malloc_wrapper`). No NULL/size guard:
      size 0 and a failed (NULL) allocation pass through untouched, like
      the operator_new veneers. Deviations: the tail branch is a plain
      call, and the alloc dispatch goes through the HEAP_OPS.alloc_zero
      slot instead of branching to 0x0807b254 directly. #[inline(never)]
      for the 23 `bl` targets. match.py structural: the port is the
      inlined lazy heap init plus the ops-slot alloc_zero tail call with
      `mov r2,#4` carrying the tag — the same shape already accepted for
      free_tag4 @ 0x0805d070 and operator_new_tag3 @ 0x082aad74.

  - address: 0x0805d1d4
    ghidra: FUN_0805d1d4
    name: malloc_tag4
    size: 8
    sig: "u8 *malloc_tag4(usize size)"
    module: heap/veneers
    vars: {param_1: size}
    status: ported
    notes: >-
      The plain-alloc entry of the "MemH" managed-buffer family (free
      half: free_tag4 @ 0x0805d070; zerofill half: calloc_tag4 @
      0x0805d1dc). Whole body, verified against the raw image (words
      e3a01004 ea023927): `mov r1,#4; b 0x080eb67c` — an unconditional
      tail branch into malloc_wrapper @ 0x080eb67c (already ported) with
      caller tag 4. Extent confirmed by the neighbour: the word below @
      0x0805d1dc is the `mov r1,#4` starting calloc_tag4, so the
      function is exactly 8 bytes (Ghidra's size is right). Call sites
      counted by decoding every ARM B/BL word in osos.dec: exactly 23
      `bl`, none predicated, no tail `b` — matching Ghidra's count.
      Unlike calloc_tag4's clustered sites these are scattered across
      the image (0x0803c148, 0x08057ce0, 0x0805de68, 0x0805e52c,
      0x0805f600, 0x080680d4, 0x080681f0, 0x0806e4f8, 0x080866fc,
      0x0808d0c4, 0x0808d0ec, 0x0809d9b8, 0x0809ea84, 0x080be6f0,
      0x080be874, 0x080ca020, 0x080ca790, 0x080ce588, 0x080ce5c0,
      0x080d1b50, 0x080d8cc0, 0x080da6b0, 0x080f0758). No NULL/size
      guard: size 0 and a failed (NULL) allocation pass through
      untouched, like the operator_new veneers. Deviations: the tail
      branch is a plain call, and the alloc dispatch goes through the
      HEAP_OPS.alloc slot instead of branching to 0x080eb67c directly.
      #[inline(never)] for the 23 `bl` targets. match.py structural:
      the port is the inlined lazy heap init plus the ops-slot alloc
      tail call (`bx r0`) with `mov r2,#4` carrying the tag — the same
      shape already accepted for calloc_tag4 @ 0x0805d1dc and
      free_tag4 @ 0x0805d070.

  - address: 0x08030f44
    name: heap_panic
    size: 32
    module: heap/veneers
    status: ported
    notes: raise(1,0) -> exit -> terminate @ 0x82b20a0 (non-returning).

  - address: 0x0805f500
    ghidra: thunk_FUN_08030f44
    name: heap_panic_entry
    size: 8
    sig: "noreturn heap_panic_entry(void)"
    module: heap/veneers
    status: ported
    renames:
      FUN_08030f44: heap_panic
    notes: >-
      Ghidra's reported four-byte extent omits the following `bx lr`: raw
      ARM is `bl 0x08030f44; bx lr`, so this separately linked entry is
      eight bytes and the return is unreachable because heap_panic is
      non-returning. Decoding every ARM B/BL word in osos.dec found 11
      direct calls, all predicated: blne; six blhi; two blcs; blgt; and
      blle. There are no tail-branch calls or data-word references. The
      Rust entry directly invokes heap_panic rather than branching to its
      retailOS address; its isolated host test verifies the resulting
      raise(1,0) -> exit -> terminate(1) sequence.
  - address: 0x080edb74
    ghidra: FUN_080edb74
    name: allocation_construct_guard
    size: 20
    sig: "void allocation_construct_guard(void *constructed)"
    module: heap/new_handler
    vars:
      param_1: constructed
      DAT_080edb88: allocation_failure_descriptor
      FUN_081b53e4: report_allocation_failure
    status: ported
    notes: >-
      Raw ARM is `cmp r0,#0; ldreq r1,[pc,#8]; moveq r0,#4; beq
      0x081b53e4; bx lr`, with literal 0x088f8c48 at 0x080edb88.
      Thus non-NULL constructed allocations have no side effects; NULL
      reports allocation failure with code 4 and that opaque descriptor,
      then returns only if the reporter returns. The reporter remains an
      established-style replaceable diagnostic seam with an inert default
      until its C++ exception machinery is ported. Focused host tests pin
      the non-NULL no-report path, the NULL code/descriptor forwarding, and
      the default seam wiring. match.py confirms the guard/report shape
      structurally (the indirect seam is the documented deviation).


  - address: 0x08266abc
    ghidra: FUN_08266abc
    name: cxx_new_handler_dispatch
    size: 164
    sig: "void cxx_new_handler_dispatch(usize code, usize arg1, usize arg2, usize arg3)"
    module: heap/new_handler
    vars: {param_1: code, param_2: arg1, param_3: arg2, param_4: arg3,
      DAT_08266b60: handler_global_ptr, piVar3: message, uVar4: text}
    status: ported
    notes: >-
      ADS C++ runtime error/new-handler dispatch (variadic: r0 = code,
      r1..r3 = message args, spilled at entry). Reads the handler global
      @ 0x08a0fc08 (literal @ 0x08266b60); NULL -> immediate return —
      the only state stock retailOS reaches (binary-verified: the only
      reference to 0x08a0fc08 in the whole image is this function's own
      literal, nothing ever registers a handler). code <= 3 (SIGNED
      `cmp r0,#3; bgt`): handler(code, NULL), no message built. code >
      3: 8-byte message object {vtable, text} constructed in place over
      the spilled args (placement-new identity 0x082aaddc + ctor
      0x082a7cf4, vtable DAT_082a7d0c), text filled by the builder
      0x082a7c20 and read back through vtable[2], handler(code, text),
      then vtable[0] releases the tag-3-allocated text. 10 call sites:
      operator_new_checked 0x08266c70 (code 3) + the std::ios/exception
      reporters (codes 4, 8, 9, 0xe..0x11, 0x14). Deviation: the
      builder subsystem (RWSTDERR catalog 0x08266d28 + growable
      vasprintf 0x08266ca0 over string table DAT_082a7cf0) is NOT
      ported — it sits behind CXX_NEW_HANDLER_OPS.format_message whose
      default stub yields NULL text (unobservable on stock); vtable[0]
      is modeled as operator_delete_tag3, inferred from the builder's
      0x082aad74/0x082aad14 alloc/free pair (the vtable lives in
      runtime-initialized data, body unrecoverable); the original's
      post-build reload of the handler global is folded into one load.
      veneers.rs's HEAP_OPS.new_handler default is now the real port
      via the one-argument cxx_new_handler_report (the code-3 call
      never carries the variadic tail). match.py structural: early
      return + signed code compare + both handler call sites
      correspond; the release tail is the inlined lazy-init + tag-3
      delete chain (veneers pattern). Gotcha: the handler-global read
      must be volatile — LLVM folded it to None and erased the whole
      function on the first ARM build.

  - address: 0x08266c70
    ghidra: FUN_08266c70
    name: operator_new_checked
    size: 40
    sig: "void *operator_new_checked(size_t size)"
    module: heap/new_handler
    vars: {param_1: size, iVar1: block}
    status: ported
    notes: >-
      The ADS checked operator-new wrapper behind which the code-3
      new-handler call sits (223 call sites). block = operator_new(size)
      (tag-2 dominant allocator 0x082aadd4, already ported in
      heap/veneers, called directly); on NULL invokes
      cxx_new_handler_dispatch 0x08266abc with code 3 (`cmp r4,#0;
      moveq r0,#3; bleq`) through the one-argument cxx_new_handler_report
      (the code-3 call never carries the variadic tail), then returns
      block as-is — still NULL when no handler freed anything; no retry
      at this level. Size: 48-byte span @ 0x08266c70..0x08266ca0, 40
      per functions.csv — Ghidra excludes the 8-byte unreachable bl
      pair @ 0x08266c84/0x08266c88 (branched over on every path,
      compiler outlining residue), not reproduced. Other deviations:
      the prologue/epilogue r0/r1 spill-restore stack scratch and the
      dead `mov r4,#0` pre-init collapse away. The port moved from
      heap/veneers.rs (where it predated the dispatch port and reached
      the handler through the HEAP_OPS.new_handler slot) into
      heap/new_handler.rs next to the dispatch; veneers.rs re-exports
      it so cxx/string.rs and the string maps keep their
      heap::veneers::operator_new_checked paths. cxx_new_handler_report
      gained #[inline(never)] to preserve the original's bleq branch
      boundary (otherwise LLVM folds the whole dispatch into the
      caller). Host tests: success returns the block with no handler
      call (tag-2 alloc), failure reports code 3 with NULL message and
      propagates NULL, and the stock no-handler failure is a silent
      NULL. match.py structural: 10 vs 10 instructions, identical
      alloc/NULL-check/code-3-bleq/return shape; the diff is LLVM's fp
      frame, a popne early-out for the success path, and intra-archive
      bl relocations — the accepted ADS-vs-LLVM noise.

  - address: 0x0826f3bc
    ghidra: FUN_0826f3bc
    name: stack_frame_binary_search_tail
    size: 24
    sig: "u32 stack_frame_binary_search_tail(u32 key, i32 remaining_count, const u32 *cursor, const u32 *end)"
    module: ui/view_base
    status: identified
    renames:
      r0: key
      r1: remaining_count
      r3: cursor
      r4: end
    notes: >-
      Raw extent is 0x0826f3bc..0x0826f3d4, not Ghidra's 88-byte
      0x0826f3bc..0x0826f414 extent. These six instructions are an interior
      tail of the 0x0826f340..0x0826f3d8 routine: while remaining_count is
      positive, it branches backward to the midpoint/comparison block at
      0x0826f398; when done it substitutes end-4 for cursor == end, loads the
      selected word, and executes the enclosing routine's
      `pop {r1,r2,r3,r4,pc}`. Therefore 0x0826f3bc has no independently
      callable ARM ABI: it requires r4 and a 16-byte callee stack frame
      established before 0x0826f3bc. A standalone Rust export would return
      through lr and cannot reproduce that stack-frame-dependent continuation.
      Decoding every ARM B/BL word in osos.dec finds 12 direct, unconditional
      BL instructions targeting this interior address (0x08269250, 0x08269ac4,
      0x083d217c, 0x083d235c, 0x083d23c0, 0x083d23dc, 0x083d26e8,
      0x083d2704, 0x083d2a10, 0x083d2a2c, 0x083d2d38, 0x083d2d54), no
      predicated BLs, no tail B references, and no aligned image data word
      holding the address. The direct calls do not establish that required
      frame (for example 0x083d217c follows a callee that explicitly clears
      r1), an unresolved binary/source anomaly. Status remains identified
      rather than inventing a callable function or a callee identity.
  - address: 0x0826f580
    name: aligned_pool
    size: 612
    module: heap/pool
    status: ported
    notes: create 0x0826f658/alloc 0x0826f6a0/destroy 0x0826f7e4; 0x418-byte
      ctl struct, embedded heap desc +0x7c; alignments 4/16/32/1024 (recovered
      from serialized copy @ 0x089d6094); tag 0x2b; uncached path = D-cache
      clean+invalidate + bit31 uncached alias. POOL_OPS is now fully wired
      to real ports, incl. the base-subobject ctor/dtor/fill and the seed
      walk's deque accessor + iterator copies (heap/block_deque).

  - address: 0x0826f758
    ghidra: FUN_0826f758
    name: pool_free
    size: 40
    sig: "void pool_free(PoolControl *pool, void *ptr)"
    module: heap/pool
    status: ported
    renames:
      param_1/r0: pool
      param_2/r1: ptr
    notes: >-
      Binary extent is exactly 40 bytes, 0x0826f758..0x0826f780: ready
      flag (+0x414) and NULL guards, unmark ptr's bit-31 uncached alias,
      reload the alignment delta from ptr-4, subtract it to recover the
      raw heap allocation, then tail-branch to heap_free 0x0819d4dc with
      tag 2. Call count verified by decoding every ARM B/BL word in
      osos.dec: exactly 14 plain unconditional bl sites, no predicated bl
      sites. A separate bne tail wrapper at 0x082aad44 is not a call site.
      Host behavior deliberately reads the delta through the unmarked alias
      and routes the tail dispatch through the pre-existing POOL_OPS
      heap_free seam; both preserve target-visible behavior.

  - address: 0x0826f780
    ghidra: FUN_0826f780
    name: pool_alloc_v1
    size: 28
    sig: "void *pool_alloc_v1(PoolControl *pool, size_t size, size_t align_class, size_t uncached)"
    module: heap/pool
    status: ported
    renames:
      param_1/r0: pool
      param_2/r1: size
      param_3/r2: align_class
      param_4/r3: uncached
    notes: >-
      Binary extent is exactly 28 bytes, 0x0826f780..0x0826f79c. The
      veneer preserves uncached in ip over its prologue, writes stack
      argument variant = 1, restores uncached to r3, and bl-calls
      pool_alloc 0x0826f6a0; it returns that result unchanged. Call count
      verified by decoding every ARM B/BL word in osos.dec: exactly 12
      plain unconditional bl sites and no predicated bl sites. The Rust
      veneer delegates to the existing pool_alloc port, whose plain heap
      allocation dispatches through POOL_OPS.heap_alloc for host tests;
      this is the deliberate indirect-dispatch deviation from the direct
      binary call.

  - address: 0x082141bc
    ghidra: FUN_082141bc
    name: pool_base_construct
    size: 100
    sig: "PoolBase *pool_base_construct(PoolBase *this, const char *name, usize flag)"
    module: heap/block_deque
    vars: {param_1: this, param_2: name, param_3: flag}
    status: ported
    notes: >-
      Base-subobject ctor of the aligned pool (1 bl call site @
      0x0826f7ac in pool_init, flag always 1, binary-verified). Parent
      ctor bl 0x081f0050, vtable literal [0x08214220] = 0x08993120
      (runtime-initialized RW data — image bytes stale, slots
      unrecoverable, no serialized copy in the image), empty deque
      (begin = end = NULL iters via a real 0x083d9eec call,
      count/map/map_cap = 0 covering +0x6c/+0x70/+0x74), mailbox_slot_create
      @ 0x0808e294 on +0x78. Ghidra's decompile is garbled (it invents a
      value-returning FUN_081f0050 and misses that r0 stays this); the
      asm is authoritative. Base layout (0x7c bytes): vtable +0x00,
      client_ref +0x04, C++ recursive mutex +0x08, parent mailbox +0x24,
      flag bytes +0x28/+0x29, name obj +0x2c, fill_block_count +0x44,
      fill_cap +0x48, deque +0x4c, mailbox +0x78.

  - address: 0x08214224
    name: pool_base_destroy
    size: 76
    sig: "PoolBase *pool_base_destroy(PoolBase *this)"
    module: heap/block_deque
    status: ported
    notes: >-
      Non-deleting dtor chain; NOT in Ghidra's function list (it
      decompiled the chain inlined into pool_destroy 0x0826f7e4, whose
      tail b @ 0x0826f800 decodes to exactly 0x08214224 from the raw
      word — the sole call site, binary-verified). Re-installs the class
      vtable ([0x0821426c] = 0x08993120, same as the ctor), bl
      0x08214158 (release blocks), mailbox_slot_delete @ 0x080a6bec on
      +0x78, drains the deque via 0x083ddbdc while [this+0x6c] != 0,
      tail b 0x081f00a0 (parent dtor, returns this).

  - address: 0x08214158
    ghidra: FUN_08214158
    name: pool_base_release_blocks
    size: 100
    sig: "void pool_base_release_blocks(PoolBase *this)"
    module: heap/block_deque
    vars: {param_1: this}
    status: ported
    notes: >-
      1 bl call site @ 0x08214238 (the dtor), binary-verified. Locks the
      base mutex (+0x8) via the C++ recursive mutex thunk 0x082621a8; if
      the deque is non-empty and the client handle (0x083d64f4 on
      this+0x4, re-read before every call) is non-NULL, hands the
      descriptors back: 0x081fc080(client, deque) then 0x081fc884(client)
      (both now ported — heap/client_erase.rs and heap/client_commit.rs,
      the shipped POOL_BASE_OPS.client_erase / client_erase_commit
      defaults; the commit's 0/1 verdict is discarded, as in the
      original);
      zeroes fill_block_count/fill_cap (+0x44/+0x48); tail b 0x082621ac
      (unlock — its status is the return value, ignored by the only
      caller; port returns void). Ghidra's decompile inlines the whole
      unlock body, making it look like mutex internals belong here.

  - address: 0x08213fc4
    ghidra: FUN_08213fc4
    name: block_deque_fill
    size: 280
    sig: "i32 block_deque_fill(PoolBase *this, usize size, usize max)"
    module: heap/block_deque
    vars: {param_1: this, param_2: size, param_3: max}
    status: ported
    notes: >-
      1 bl call site @ 0x0826f580 (pool_seed_regions, max always 2000),
      binary-verified. Under the base mutex; size compared SIGNED (ble).
      block_manager_get 0x0818ae48 (result discarded), block count =
      __rt_udiv(region_block_size() + size - 1, region_block_size())
      [two separate size reads] stored at +0x44, max at +0x48 (doubles
      as the wait timeout); gate = client attach 0x081efc8c(this);
      reserve 0x081fbe4c(client, count * block_size [32-bit mul], 0);
      waits at most once (queue-get 0x080b4adc on the +0x78 mailbox,
      timeout [this+0x48]) for 0x081fc3f4(client, 0x40000) headroom;
      populate 0x081fc298(client, count, this+0x4c) is the return value.
      Both post-reserve failure paths virtual-call [vtable+0x10]
      (fill_failed; target unrecoverable, stale RW data) before
      returning 0. The client handle is re-read via 0x083d64f4 before
      every client call.

  - address: 0x08214150
    ghidra: FUN_08214150
    name: deque_node_accessor
    size: 8
    sig: "BlockDeque *deque_node_accessor(PoolBase *this)"
    module: heap/block_deque
    status: ported
    notes: >-
      add r0, r0, #0x4c; bx lr — the base subobject's block deque. 1 bl
      call site @ 0x0826f59c (pool_seed_regions), binary-verified.

  - address: 0x08217318
    ghidra: FUN_08217318
    name: timer_reset_4000
    size: 40
    sig: "void timer_reset_4000(u8 *this)"
    module: app/timer_reset
    status: ported
    renames:
      param_1: this (unidentified controller class; its timer lives at +0xb8)
      FUN_0812c6b0: timer_stop (already ported, called directly)
      FUN_0812c63c: timer_start_after (already ported, called directly)
      FUN_0812bf4c: timer_restart (already ported; tail branch in stock)
    notes: >-
      40 bytes exactly (0x08217318..0x08217340), Ghidra's extent confirmed:
      the mov r0,#0; bx lr leaf at 0x08217340 is the next function. 24 call
      sites binary-verified by decoding every ARM B/BL word in osos.dec: 22
      unconditional bl plus 2 blne - the predicated pair are caller-side
      mode gates (each caller is an event handler returning 1 that skips
      the reset when FUN_0817ee04()+0x28 equals its mode constant), not a
      NULL guard; this routine dereferences this+0xb8 unconditionally and
      the port keeps that. Algorithm: stop the +0xb8 timer, reload the
      field, timer_start_after(timer, 4000), reload again, tail-branch to
      timer_restart - i.e. re-arm the object's timeout to a fresh 4000 ms.
      All three callees were already ported, so no dispatch seam was added.
      Rust renders the tail branch as a normal call and preserves the three
      ldr [this, #0xb8] reloads as volatile reads. Host tests use the unique
      TIMER_RESET_4000 slab: one proves the 4000 ms period store and the
      'run ' state after restart, the other swaps +0xb8 mid-stop via the
      TIMER_OPS trace slot and proves both later helpers observe the
      replacement. match.py structural review against this worktree archive
      (--lib): LLVM inlines all three ported helpers plus their ops-table
      paths (142 instructions), but the stock shape survives - three +0xb8
      reloads, the inlined stop sequences with the 'stop'/'run ' FourCC
      stores, the mov r0,#4000 delay store, and the indirect arm tail.

  - address: 0x08216d70
    ghidra: FUN_08216d70
    name: stop_progress_layout_transition
    size: 52
    sig: "void stop_progress_layout_transition(u8 *this)"
    module: app/progress_layout_transition
    status: ported
    renames:
      param_1/r0: this (progress-layout controller)
      param_1_plus_0xb0: activity
      param_1_plus_0xc1: active transition flag
      param_1_plus_0xc4: transition timer
      FUN_0812c6b0: timer_stop (already ported, called directly)
      FUN_081f9248: unported activity cleanup callee
    notes: >-
      True extent is 52 bytes: push {r4,lr} starts at 0x08216d70 after the
      preceding string's terminating NUL, and the sibling push {r4,r5,r6,lr}
      starts at 0x08216da4. Every ARM B/BL immediate in osos.dec was decoded:
      15 direct calls, 3 plain bl and 12 blne, no tail b. The predicated
      callers gate on this+0xc1; they are not NULL guards, and the body repeats
      the same byte-zero early return. Active path loads +0xc4 and calls
      ported timer_stop, loads +0xb0 and calls 0x081f9248(activity, 1), then
      clears +0xc1. Neither pointer is NULL-checked. The timer callee is
      direct; unported 0x081f9248 stays behind a volatile
      PROGRESS_LAYOUT_TRANSITION_OPS seam whose target default reaches the
      firmware address and whose host default panics. Its wider identity is not
      inferred. Host tests use the unique PROGRESS_LAYOUT_TRANSITION u32 slab
      to prove inactive calls touch neither pointer and active calls stop the
      timer before the literal-one activity call, then clear the flag.
      Structural match.py review against this worktree archive (--size 52,
      --lib) preserves the +0xc1 gate, +0xc4 timer path, +0xb0 literal-one
      activity call and final byte clear; LLVM inlines ported timer_stop.

  - address: 0x082199bc
    ghidra: FUN_082199bc
    name: update_dispatch
    size: 80
    sig: "u32 update_dispatch(void)"
    module: app/update_dispatch
    status: ported
    renames:
      FUN_0811b2c0: singleton_class_6280 (already ported, called directly)
      FUN_0811c564: flag_2c_is_clear (already ported, called directly)
      FUN_0811bf0c: class_6280_commit (UPDATE_DISPATCH_OPS slot)
      FUN_081115e4: root_slot_190_query (UPDATE_DISPATCH_OPS slot)
      FUN_08112bbc: root_pending_process (UPDATE_DISPATCH_OPS slot)
      DAT_08219a0c: app-root pool word (0x089ca674; the crate static
        APP_ROOT_OBJECT, the app/context_scope.rs RW-page deviation)
    notes: >-
      EXTENT CORRECTED - Ghidra's 80 bytes is the code only; the
      function owns one trailing literal-pool word (0x089ca674 @
      0x08219a0c, reached by `ldr r0, [pc, #36]` @ 0x082199e0), so the
      true extent is 84 bytes, 0x082199bc..0x08219a10, where the next
      function opens `push {r0-r9, sl, fp, lr}` (e92d4fff). CALL COUNT:
      Ghidra's 23 confirmed - binary-verified by decoding every B/BL
      word in work/firmware/osos.dec: exactly 23 unconditional bl call
      sites, 0 predicated and 0 plain b, and no DATA word references the
      address (direct-called only, never virtual). Callers cluster in
      the Silver UI region (two in the 0x08131380 method, the rest in
      0x08222xxx..0x0823axxx) and all test the result: 1 = declined
      (nothing dispatched, caller proceeds with its own handling), 0 =
      handled. ALGORITHM: fetch the registry-class-0x6280 singleton
      (unconditionally, BEFORE the branch - it is allocated/cached even
      on the root path, and neither it nor the root is NULL-checked),
      test its mode flag byte at +0x2c via flag_2c_is_clear; flag SET ->
      FUN_0811bf0c(object) (the class-0x6280 object-side handler,
      broadcasting its own event family 0x6280/0x6282/0x6283/0x6287/
      0x6288 - pool literals @ 0x0811bff8..0x0811c00c - through the
      vtable +0x58 poster and storing 2 into +0x2c), return 0; flag
      CLEAR -> root = *0x089ca674, query vtable slot +0x190 of the
      root's +0x888 sub-object through the four-instruction tail-call
      veneer FUN_081115e4 (`ldr r0,[r0,#0x888]; ldr r1,[r0]; ldr
      r1,[r1,#0x190]; bx r1` - Ghidra's C wrongly types it void and
      drops the argument; the caller passes the root and tests r0),
      query == 0 -> return 1, else FUN_08112bbc(root) (re-queries via
      the same veneer, calls vtable slot +0xf0, then works the
      singleton_class_8c00 + 0x98 list under mutex_lock/mutex_unlock),
      return 0. DEVIATIONS: the three handler callees are unported and
      ride UPDATE_DISPATCH_OPS (the EVENT_HUB_OPS /
      IAP_THREAD_SLOT_POLL_OPS pattern), read slot-by-slot through
      read_volatile - target defaults transmute ROM addresses
      0x0811bf0c / 0x081115e4 / 0x08112bbc (hook-faithful), host
      defaults are documented inert stubs (commit/process no-ops, query
      returns 0 -> decline, so the default port is a harmless no-op
      returning 1). match.py structural review against this worktree
      archive (--lib): same control flow - leading bl
      singleton_class_6280 (R_ARM_CALL verified), the +0x2c flag test
      (LLVM inlines flag_2c_is_clear to ldrb/cmp/beq; the exported
      symbol still resolves in the archive for its other callers), two
      blx seam calls with the query's zero test gating the root process,
      and the mov r0,#1 / mov r0,#0 return pair; deltas are the crate fp
      prologue and LLVM tail-merging the commit blx with the process
      blx.

  - address: 0x083dd9e4
    ghidra: FUN_083dd9e4

    name: deque_iter_copy
    size: 36
    sig: "void deque_iter_copy(DequeIter *dst, usize r1_unused, const DequeIter *src)"
    module: heap/block_deque
    vars: {param_1: dst, param_3: src}
    status: ported
    notes: >-
      Word-by-word 16-byte iterator copy; src travels in r2, r1 is never
      read (both call sites happen to leave the deque pointer there) —
      the export keeps the 3-argument ABI. 2 bl call sites @ 0x0826f5b0 /
      0x0826f61c (pool_seed_regions), binary-verified. A byte-pattern

      scan of the whole image finds this r2 form EXACTLY ONCE; the
      src-in-r1 twin has 17 byte-identical copies and is ported
      separately as deque_iter_assign (canonical 0x083da458).

  - address: 0x083d9ec0
    ghidra: FUN_083d9ec0
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    notes: >-
      mov r0, #0x20; bx lr — elements per deque segment; 0x20 * 0x28 =
      the 0x500-byte segment stride. 7 bl call sites, binary-verified.
      This body is one of 17 BYTE-IDENTICAL copies, one per deque
      instantiation, each immediately followed by that instantiation's
      deque_iter_assign (8 + 36 bytes, adjacent — which is what
      identifies the pair as the template's out-of-line members). Hook
      any of them to this symbol. bl counts - 0x083d9fcc 18, 0x083da344
      13, 0x083d9f5c 12, 0x083da1a8 12, 0x083da240 12, 0x083da450 11,
      0x083d9ec0 7, 0x083da3dc 7, 0x081a81bc 5, 0x083da2d8 5, and
      2 each at 0x083d9e94, 0x083d9f30, 0x083da17c, 0x083da214,
      0x083da2ac, 0x083da3b0, 0x083da424 (116 calls in total).
      0x081a81bc is the only copy outside the C++ block.

  - address: 0x083d9fcc
    ghidra: FUN_083d9fcc
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    renames: {}
    notes: >-
      Alias of the existing deque_seg_capacity Rust export: raw ARM is
      `mov r0, #0x20; bx lr`, returning 0x20 elements per deque segment.
      This is one of the 17 byte-identical per-template copies, so the
      canonical port deliberately serves it directly rather than adding an
      indistinguishable second export. Decoding every ARM B/BL word in
      osos.dec finds 18 plain bl call sites and no predicated calls:
      0x083da01c, 0x083da050, 0x083da0a8, 0x083da0bc, 0x083da0c4,
      0x083da100, 0x083da110, 0x083da150, 0x083de238, 0x083de354,
      0x083de37c, 0x083de3c4, 0x083de454, 0x083de4b4, 0x083de4d8,
      0x083de4f0, 0x083de504, and 0x083eada4. Ghidra's 18-call count is
      exact.

  - address: 0x083da344
    ghidra: FUN_083da344
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    renames: {}
    notes: >-
      Alias of the existing deque_seg_capacity Rust export: raw ARM is
      `mov r0, #0x20; bx lr`, returning 0x20 elements per deque segment.
      Decoding every ARM B/BL word in osos.dec finds exactly 13
      unconditional plain bl call sites and no predicated calls:
      0x08215784, 0x08215798, 0x082157a0, 0x082157dc, 0x082157ec,
      0x083da394, 0x083df308, 0x083df3a8, 0x083df404, 0x083df428,
      0x083df43c, 0x083df450, and 0x083df4f0. This is one of the 17
      byte-identical deque-template copies, so the canonical export
      deliberately serves it directly rather than adding an
      indistinguishable second Rust body.

  - address: 0x083da450
    ghidra: FUN_083da450
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    renames: {}
    notes: >-
      Alias of the existing deque_seg_capacity Rust export. Raw ARM words
      0xe3a00020 and 0xe12fff1e decode as `mov r0, #0x20; bx lr`,
      returning 0x20 elements per segment. Decoding every ARM B/BL word in
      osos.dec verifies exactly 11 unconditional plain bl call sites, zero
      predicated bl forms, and zero tail b forms: 0x083da4a0, 0x083da4d4,
      0x083da52c, 0x083df8fc, 0x083df95c, 0x083df990, 0x083dfab4,
      0x083dfad8, 0x083dfc44, 0x083dfe38, and 0x083eae0c. This is one of
      17 byte-identical deque-template copies, so the canonical 0x083d9ec0
      export deliberately serves this address instead of adding an
      indistinguishable Rust body.

  - address: 0x083da1a8
    ghidra: FUN_083da1a8
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    renames: {}
    notes: >-
      Alias of the existing deque_seg_capacity Rust export: raw ARM is
      `mov r0, #0x20; bx lr` (words 0xe3a00020 0xe12fff1e, byte-identical
      to canonical 0x083d9ec0), returning 0x20 elements per deque segment.
      Decoding every ARM B/BL word in osos.dec finds exactly 12
      unconditional plain bl call sites and no predicated calls:
      0x083da1f8, 0x083de850, 0x083de8b0, 0x083de8e4, 0x083dea08,
      0x083dea2c, 0x083deb68, 0x083e0088, 0x083e009c, 0x083e00a4,
      0x083e00e0, and 0x083e00f0. Ghidra's 12-call count is exact. No
      data-word references (not vtable-dispatched). Callers scale the
      result by 4 (the pointer-sized element stride of this
      instantiation) — e.g. FUN_083de8dc allocates capacity*4 = 128
      bytes. This is one of the 17 byte-identical per-template copies, so
      the canonical export deliberately serves it directly rather than
      adding an indistinguishable second Rust body.

  - address: 0x083da240
    ghidra: FUN_083da240
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    renames: {}
    notes: >-
      Alias of the existing deque_seg_capacity Rust export: raw ARM is
      `mov r0, #0x20; bx lr` (words 0xe3a00020 0xe12fff1e, byte-identical
      to canonical 0x083d9ec0), returning 0x20 elements per deque segment.
      Decoding every ARM B/BL word in osos.dec finds exactly 12
      unconditional plain bl call sites and no predicated calls:
      0x081da704, 0x081da718, 0x081da720, 0x081da75c, 0x081da76c,
      0x083da290, 0x083dec18, 0x083dec78, 0x083decac, 0x083dedd0,
      0x083dedf4, and 0x083def30. Ghidra's 12-call count is exact. This
      is one of the 17 byte-identical per-template copies (callers scale
      the result by 4, the pointer-sized element stride of this
      instantiation), so the canonical export deliberately serves it
      directly rather than adding an indistinguishable second Rust body.

  - address: 0x083d9f5c
    ghidra: FUN_083d9f5c
    name: deque_seg_capacity
    size: 8
    sig: "usize deque_seg_capacity(void)"
    module: heap/block_deque
    status: ported
    renames: {}
    notes: >-
      Alias of the existing deque_seg_capacity Rust export: raw ARM is
      `mov r0, #0x20; bx lr` (words 0xe3a00020 0xe12fff1e, byte-identical
      to canonical 0x083d9ec0), returning 0x20 elements per deque segment.
      Decoding every ARM B/BL word in osos.dec finds exactly 12
      unconditional plain bl call sites and no predicated calls:
      0x083d9fac, 0x083ddcf0, 0x083ddd50, 0x083ddd88, 0x083ddeb0,
      0x083dded4, 0x083ddf5c, 0x083ddf70, 0x083ddf78, 0x083ddfb4,
      0x083ddfc8, and 0x083de0a8. Ghidra's 12-call count is exact. No
      data-word references (not vtable-dispatched). Callers scale the
      result by 0xc, the 12-byte element stride of this instantiation:
      the iterator ctor at 0x083d9f88 builds
      `seg_end = seg_base + capacity * 0xc`, and the pop paths at
      0x083ddcf0/0x083de0a8 free a spent segment via
      `cxx_array_dealloc(seg, 0x20, 0)`. This is one of the 17
      byte-identical per-template copies, so the canonical export
      deliberately serves it directly rather than adding an
      indistinguishable second Rust body.

  - address: 0x083d9eec
    ghidra: FUN_083d9eec
    name: deque_iter_init
    size: 68
    sig: "DequeIter *deque_iter_init(DequeIter *iter, u8 *cur, u8 **slot)"
    module: heap/block_deque
    vars: {param_1: iter, param_2: cur, param_3: slot}
    status: ported
    notes: >-
      cur as given; seg_base = *slot, seg_end = *slot +
      deque_seg_capacity() * 0x28 (real call), both NULL when slot is
      NULL; seg_slot = slot; returns iter. 9 bl call sites,
      binary-verified (ctor, pop_front x2, 0x083ddxxx siblings).

  - address: 0x083d9f88
    ghidra: FUN_083d9f88
    name: deque_iter_init_elem12
    size: 68
    sig: "DequeIter *deque_iter_init_elem12(DequeIter *iter, u8 *cur, u8 **slot)"
    module: heap/block_deque
    status: ported
    renames: {param_1: iter, param_2: cur, param_3: slot}
    notes: >-
      Raw ARM stores cur, then either writes NULL seg_base/seg_end for a
      NULL slot or loads seg_base = *slot and seg_end = seg_base + 0x180;
      it finally writes seg_slot and returns iter. The 0x180 stride is the
      local capacity call at 0x083d9fac to the 0x083d9f5c twin (`mov r0,
      #0x20; bx lr`), followed by `add r0, r0, r0, lsl #1` and `add r0,
      r1, r0, lsl #2`, yielding 0x20 * 12. Decoding every ARM B/BL word in
      osos.dec finds exactly 9 direct unconditional plain bl call sites
      (0x083ddd24, 0x083ddd68, 0x083dde48, 0x083dde70, 0x083dde9c,
      0x083ddef8, 0x083de014, 0x083de0dc, 0x083de118) and no predicated
      forms. Rust deliberately calls the canonical deque_seg_capacity port
      because the local target is an already-ledgered byte-identical
      capacity twin returning 0x20.

  - address: 0x083ddbdc
    ghidra: FUN_083ddbdc
    name: deque_pop_front
    size: 204
    sig: "void deque_pop_front(BlockDeque *dq)"
    module: heap/block_deque
    vars: {param_1: dq}
    status: ported
    notes: >-
      Pops the front element: begin.cur += 0x28, count--, virtual-calls
      the element dtor (element vtable slot 0 — descriptors are
      polymorphic); when the segment is spent or the deque empties,
      frees the segment via 0x08266f2c(seg, 0x20, 0), advances the map
      slot, and re-anchors begin (or, empty, NULL-inits both iterators
      and frees the map via 0x08266f2c(map, map_cap, 0) — map/map_cap
      left dangling, faithful). Returns a garbage r0/r1 pair all 4
      callers ignore (0x0814c724, 0x081fc0c8, dtor @ 0x08214250, drain
      helper @ 0x083ddcbc — binary-verified); port returns void.

  - address: 0x083d64f4
    ghidra: FUN_083d64f4
    name: handle_deref_or_null_alias_64f4
    size: 16
    sig: "void *handle_deref_or_null(void ***slot)"
    module: cxx/handle
    status: ported
    notes: >-
      ldr r0,[r0]; cmp; ldrne r0,[r0] — the block-manager client handle
      through the base object's two-level client ref (this+0x4);
      NULL-guarded on the middle pointer. 97 bl sites. Originally ported
      under the deque-specific name `client_handle_get`; a byte-pattern
      scan then showed it is one of 22 byte-identical copies of the
      generic accessor, so the implementation moved to cxx/handle.rs as
      `handle_deref_or_null` (canonical address 0x083d604c) and
      block_deque's `base_client` calls it. Hook this address to that
      symbol.

  - address: 0x08266f2c
    ghidra: FUN_08266f2c
    name: cxx_array_dealloc
    size: 4
    sig: "void cxx_array_dealloc(u8 *ptr, usize count, usize elem)"
    module: heap/veneers
    status: ported
    vars: {param_1: ptr, param_2: count, param_3: elem}
    notes: >-
      The ADS C++ array deallocation entry (operator delete[] for
      elements without destructors) — a SINGLE instruction, `b
      0x082aad24`, i.e. a pure tail branch into the tag-2 operator
      delete. The previous scouting note here (220 bytes, lazy init,
      inline heap_free accounting, heap_panic) was wrong on every
      count: it described a different function. functions.csv agrees
      the extent is 4 bytes; 0x08266f30 is an unrelated `bx lr` and
      0x08266f34 starts FUN_08266f34 (344 bytes). count/elem are dead
      — operator delete overwrites r1 with the tag on its first
      instruction and never reads r2 — but the call sites do pass
      them (deque_pop_front @ 0x083ddc40/0x083ddc84 pass
      (seg, 0x20, 0) and (map, map_cap, 0)), so the export keeps the
      3-argument ABI. 274 bl + 2 b = 276 call sites, binary-verified.
      Wired as the real POOL_BASE_OPS.seg_dealloc default (its slot
      signature grew the third argument to match the original call
      sites). match.py: `b operator_delete` behind the crate's
      frame-pointer prologue — structurally the original's tail
      branch. operator_delete gained #[inline(never)] so the branch
      survives codegen (same rationale as operator_new).

  - address: 0x081f0050
    ghidra: FUN_081f0050
    name: pool_parent_construct
    size: 80
    sig: "PoolBase *pool_parent_construct(PoolBase *this, const char *name, usize flag)"
    module: heap/pool_client
    vars: {param_1: this, param_2: name, param_3: flag}
    status: ported
    notes: >-
      Ctor of the block-manager CLIENT BASE CLASS — the parent
      subobject of the pool base, owning bytes +0x00..+0x44 of the
      object. 2 bl call sites @ 0x082141c0 (pool_base_construct) and
      0x082236cc (a sibling derived class), binary-verified. Body, in
      order: stmia r0!,{vtable,0} writes the parent vtable (literal
      [0x081f009c] = 0x0898ff7c) and clears client_ref (+0x4); C++
      recursive-mutex ctor 0x082621b0 on +0x8; mailbox_slot_create @
      0x0808e294 on +0x24; strb 0 at +0x28 and strb flag at +0x29;
      registration-node ctor 0x081eff0c(this+0x2c, name); +0x3c and
      +0x40 = 0. Returns this (r0 is re-derived from the two callees'
      returns — both return their argument, so the port keeps `this`
      in hand instead of doing 64-bit-unsafe byte arithmetic). Ghidra
      says void; the asm's closing `sub r0, r0, #0x2c` is authoritative.
      Neighbours characterized while porting: 0x081eff0c = node ctor
      (base ctor 0x08207674 stores name at node+0x4 and vtable
      0x089919a8, then node vtable 0x089a74cc + node+0x8/+0xc = 0);
      0x082621b0 = recursive mutex ctor, returns its argument. Both
      vtable literals are stale RW data (spot-checked: their "slots"
      land mid-function / in a jump table) — modeled as statics.
      match.py structural: identical field offsets and instruction
      order, ops-table blx in place of bl.

  - address: 0x081efc8c
    ghidra: FUN_081efc8c
    name: pool_client_attach
    size: 232
    sig: "i32 pool_client_attach(PoolBase *this)"
    module: heap/pool_client
    vars: {param_1: this}
    status: ported
    notes: >-
      block_deque_fill's gate (POOL_BASE_OPS.client_attach slot, now
      the real port). 232 bytes incl. the singleton literal (the
      earlier 176 was short); 3 bl call sites @ 0x08214020
      (block_deque_fill), 0x08223530 and 0x082236ec, binary-verified.
      Under the base mutex (thunk 0x082621a8/0x082621ac -> 0x082e8390/
      0x082e83d8): if the memo at +0x3c is NULL, get a client — mode
      byte +0x29 nonzero picks the process-wide singleton behind the
      ADS static-init guard (inline `tst *g, #1`, then acquire
      0x082ab31c / release 0x082ab338 over the .bss pair guard@
      0x08a09704, client@0x08a09708 — the image holds no initializer
      there, so a zero-init static is faithful), otherwise a private
      one. Either way: operator_new(0x170) then client ctor
      0x081e6b34(storage, name) where name comes from the registration
      node's vtable slot +0xc virtual on this+0x2c. Store the result at
      +0x3c; NULL -> unlock and return 0. Otherwise node.client
      (+0x34) = the client, node.owner (+0x38) = this, and the return
      value is 0x081eff38(this) — the block manager's registration,
      which installs the two-level client ref at +0x4 that
      client_handle_get reads (it calls 0x08262b1c, a twin of
      0x083d64f4, then 0x0818a630(mgr, this+0x2c, 0, this+0x4)) —
      now ported, heap/client_register.rs.
      Ghidra's decompile of this one is accurate and agrees. A failed
      singleton is remembered as NULL and never retried (the guard is
      spent) — reproduced. Still unported block-manager client
      machinery: ctor 0x081e6b34, reserve
      0x081fbe4c, headroom 0x081fc3f4. (Erase
      @ 0x081fc080, commit @ 0x081fc884 and populate @ 0x081fc298 are
      now ported — heap/client_erase.rs, heap/client_commit.rs,
      heap/client_populate.rs.)
      match.py structural: same offsets (0x3c/0x29/0x2c/+0xc/0x170),
      same guard test and shared-vs-private split; ops-table blx in
      place of bl.

  - address: 0x081fc080
    ghidra: FUN_081fc080
    name: client_erase
    size: 136
    sig: "void client_erase(u8 *client, BlockDeque *deque)"
    module: heap/client_erase
    vars: {param_1: client, param_2: deque, local_24: front, local_14: block, iVar1: remaining}
    status: ported
    notes: >-
      The block-manager client's erase op — pool_base_release_blocks'
      block hand-back (with commit @ 0x081fc884). 2 bl call sites @
      0x0814bb54 (FUN_0814bb18) and 0x08214198 (pool_base_release_blocks),
      binary-verified. Under the CLIENT's own mutex (client + 0x24, not
      the pool base's +0x8 — same C++ mutex pair 0x082e8390/0x082e83d8
      via thunks 0x082621a8/0x082621ac, REGION_MUTEX_OPS boundary,
      now defaulting to the real ports in kernel/posix_mutex):
      presets the hand-back word 0 (dead store, kept), snapshots the
      deque count (+0x20) once into the loop counter (NOT re-read —
      deque_pop_front decrements the deque's own count in parallel),
      and per iteration: copies the 16-byte begin iterator onto the
      stack via the 0x083d9ec8 copy of the ported deque_iter_assign
      (cxx/templates.rs, called directly), reads the front element's
      block word (element word 1 = region object pointer,
      block_region.rs's ELEM_REGION_INDEX; the u32 at region + 0xc is
      the hand-back word — 0x081fc124 dereferences it further as a
      handle middle), pops the front element through the real
      deque_pop_front @ 0x083ddbdc (virtual element dtor + segment
      retirement happen there), then hands the word to the manager:
      0x081fc124(client, &block). Tail: 0x081fbf1c(client, 1) then
      0x081fc230(client), both verdicts discarded. The three unported
      client-machinery callees dispatch through the new CLIENT_ERASE_OPS
      slots (house pattern, blx in place of bl) with documented no-op
      defaults (no manager); both notification slots return () since
      every caller discards the 0/1 verdict. The port is the shipped
      POOL_BASE_OPS.client_erase default, replacing block_deque.rs's
      stub_client_erase (removed — behavior-identical under the no-op
      defaults: a drained deque, nothing handed back). Host tests cover
      the empty-deque lock/notify/unlock protocol, a full 3-element
      drain through the real pop_front (per-element dtor-then-hand-back
      order, block words in order, segment+map retirement, count 0,
      NULL iterators), the count snapshot, and the wired-default no-op
      path. match.py structural: same lock/count-snapshot/loop/
      pop/hand-back/notify/unlock shape; the slot calls are blx for bl,
      the two ported callees are direct bl, LLVM adds its fp frame, and
      the element/region word reads come out as byte loads from the
      read_unaligned host-testing idiom (the block_region.rs deviation).

  - address: 0x081fc884
    ghidra: FUN_081fc884
    name: client_commit
    size: 48
    sig: "i32 client_commit(u8 *client)"
    module: heap/client_commit
    vars: {param_1: client, uVar1: verdict}
    status: ported
    notes: >-
      The block-manager client's commit op — client_erase's companion,
      run by pool_base_release_blocks right after the deque drain. 5 bl
      call sites @ 0x0814bb60 (FUN_0814bb18), 0x081a7a88, 0x082141a4
      (pool_base_release_blocks), 0x08223664, 0x0828bdbc, plus 2
      tail-branch sites @ 0x08168160 and 0x081f7430, binary-verified.
      A pure mutex bracket: under the CLIENT's own mutex (client + 0x24,
      not the pool base's +0x8 — same C++ mutex pair
      0x082e8390/0x082e83d8 via thunks 0x082621a8/0x082621ac,
      REGION_MUTEX_OPS boundary, client_erase's CLIENT_MUTEX_OFFSET
      shared) it calls the commit body 0x081fc408(client, 1) — the
      constant 1 (mov r1, #0x1) arms the body's post-commit manager
      notification — saves the 0/1 verdict across the unlock (mov r5,
      r0) and returns it verbatim (mov r0, r5); committed (1) / refused
      (0). pool_base_release_blocks discards the verdict, the other
      callers test it. The commit body is unported block-manager
      machinery (a 0x40000/0x20000 headroom probe pair, state-word
      updates at client + 0x44/+0x50, manager calls through the
      two-level client ref at +0x4), so it dispatches through the new
      CLIENT_COMMIT_OPS.commit_body slot (house pattern, blx in place
      of bl) whose documented default reports the no-manager refusal 0
      (block_deque.rs's reserve/avail/populate stub contract).
      The port is the shipped POOL_BASE_OPS.client_erase_commit default,
      replacing block_deque.rs's stub_client_erase_commit (removed —
      behavior-identical under the no-op defaults: the only wired
      caller discarded the result, and 0 is the refusal the no-op
      implied); the slot's signature gains the verdict return (-> i32).
      The commit drops out of pool_client_attach's unported-machinery
      list. Host tests cover the lock/body(client, 1)/unlock protocol,
      verdict pass-through (0/1/-1/7) with the mutex released even on
      failure, and the fail-closed wired default. match.py structural:
      same push, client+0x24, lock/body/unlock shape with the verdict
      held in a callee-saved register across the unlock; the diff is
      blx-through-table for the three calls (the mutex pair included)
      and LLVM's fp frame, the accepted house deviations.

  - address: 0x081fc298
    ghidra: FUN_081fc298
    name: client_populate
    size: 348
    sig: "i32 client_populate(u8 *client, usize count, BlockDeque *deque)"
    module: heap/client_populate
    vars: {param_1: client, param_2: count, param_3: deque, iVar1: node, uVar2: pushed, local_20: first_node_dead_spill, auStack_70: scratch, auStack_48: temp, uVar4: result}
    status: ported
    notes: >-
      The block-manager client's populate op — the fill verdict
      block_deque_fill returns (client_erase drains the same deque the
      other way). 2 bl call sites @ 0x081a8434 (FUN_081a83b8) and
      0x082140b0 (block_deque_fill), binary-verified. Under the
      CLIENT's own mutex (client + 0x24, same REGION_MUTEX_OPS boundary
      as erase/commit): gate 1 = the 0x40000 state-flag bit (the REAL
      state_flags_contain @ 0x081fc3f4, util/state_flags.rs — called
      TWICE, once per gate) OR the +0x18/+0x1c counters covering count
      (signed subs/bmi); gate 2 (flagged path only) = count <=
      +0x50 - +0x18 (unsigned cmpne/bcc); then the manager hand-out
      0x0818b0c4(*(client+4), client+8, count); then per block a walk
      of the region list (head client+0xc; node +0x4 next, +0xc region)
      copy-constructing a 0x28-byte region temp (0x08280464; NULL node:
      default-construct 0x082804b8 + destroy 0x082804fc around the
      copy), growing the deque when empty or segment-spent (0x083dda08),
      copy-constructing onto end.cur only when non-NULL, end.cur += 0x28
      and count += 1 unconditionally, temp destroyed, node advanced —
      NULL at the advance tail-calls heap_panic @ 0x08030f44
      (non-returning, one default element already pushed). Returns 1 on
      success, 0 on every refusal. The five unported callees (manager
      hand-out, deque growth, region ctor/copy/dtor) dispatch through
      the new CLIENT_POPULATE_OPS slots (house pattern, blx for bl);
      the manager slot's default is now the REAL 0x0818b0c4 port
      (heap/block_mgr.rs's manager_take_blocks — stub_manager_take_blocks
      removed) whose own body default is now the REAL 0x0818b108 port
      (take_blocks_body) — unreachable with no block manager on device,
      the no-manager contract
      of block_deque.rs's stub_client_populate one boundary deeper, so
      the wired defaults are still fail-closed and the no-op
      grow/region stubs are unreachable. block_deque.rs is off-limits in the shared
      tree, so its stub stays the POOL_BASE_OPS default; the port is
      exercised through the slot by heap/mod.rs's integration tests
      (real port + mocked callees replacing the wholesale stand-in,
      which built the deque by hand). Deviations: the original's
      emptiness bl (0x083d75b0, a container_is_empty copy) reads the
      count at deque+0x20 — the port reads the typed BlockDeque::count
      (identical on target, correct under the 64-bit host layout, the
      block_deque.rs lesson); the head-node spill local_20 is dead,
      kept; client/list pointer fields are u32 reads zero-extended
      (host fixtures live in a low mmap slab). Host tests cover both
      gates (flagged/unflagged, counter independence), manager refusal,
      a 3-block populate in list order (temp/element copy pairing,
      grow-once, per-block destroy, unconditional advance), grow on a
      spent segment with a segment-installing grow, and the fail-closed
      wired default. match.py structural: same lock/gates/manager/
      walk/advance/panic/unlock shape; slot calls are blx for bl,
      state_flags_contain inlined (ldr+tst) instead of bl, the u32 word
      reads come out as ldrb chains from the read_unaligned idiom (the
      client_erase.rs deviation), LLVM adds its fp frame and
      zero-initializes both temps per iteration.

  - address: 0x081eff38
    ghidra: FUN_081eff38
    name: client_register
    size: 60
    sig: "i32 client_register(PoolBase *this)"
    module: heap/client_register
    vars: {param_1: this}
    status: ported
    notes: >-
      The block manager's client registration — pool_client_attach's
      return value and block_deque_fill's gate verdict. 1 bl call
      site @ 0x081efd30 (inside pool_client_attach), binary-verified.
      Presets the verdict 1 (mov r5, #0x1), reads the two-level
      client ref at this+0x4 through the 0x08262b1c copy of
      handle_deref_or_null (cxx/handle.rs — byte-identical to the
      other 21, the only copy outside the C++ block): a live handle
      means already registered and the preset 1 is returned (bne).
      Otherwise block_manager_get @ 0x0818ae48 (heap/block_mgr.rs,
      real port) fetches the manager and 0x0818a630(mgr, this+0x2c,
      0, this+0x4) links the registration node into the manager's
      client list and installs the two-level ref client_handle_get
      reads; its return is the verdict. The r2 zero is a notification
      flag inside 0x0818a630 (nonzero would bl 0x0818a3b4(mgr, 0xa)
      after linking; always 0 here). 0x0818a630 is unported, so it
      dispatches through the MANAGER_CLIENT_REGISTER slot (house
      pattern); the default reports 0 — the no-manager verdict that
      keeps pool_client_attach's wired defaults refusing. The port is
      the shipped POOL_CLIENT_OPS.client_register default, replacing
      pool_client.rs's stub_client_register (kept a slot so host
      tests can observe it — the mailbox_slot_create/client_alloc
      precedent). Host tests cover the live-handle short-circuit, the
      empty middle pointer falling through, the argument tuple
      (mgr/node/0/ref slot), verdict pass-through (0/1/-1/7) and the
      wired-default 0. match.py structural: same push, this+0x4,
      bl handle_deref_or_null, conditional verdict 1 (rendered
      branchless movne/popne — the queue_wait idiom), node/0/ref-slot
      argument setup and the indirect manager call; LLVM adds its fp
      frame, inlines the 12-byte block_manager_get into a direct
      global load, and tail-calls the registration slot (bx ip)
      since the verdict needs no further work.

  - address: 0x080b4adc
    ghidra: FUN_080b4adc
    name: queue_wait
    size: 44
    sig: "u32 queue_wait(Mailbox **slot, u32 timeout)"
    module: heap/queue_wait
    vars: {param_1: slot, param_2: timeout}
    status: ported
    notes: >-
      Queue-get wait wrapper of the block-manager client machinery (the
      +0x78 mailbox wait of block_deque_fill, timeout [this+0x48]). 9
      bl call sites, binary-verified: 0x080e2ee4, 0x080e4310,
      0x0813b1a8, 0x0814b9f0, 0x08160cd8, 0x081effa4, 0x08214068
      (block_deque_fill), 0x0822356c, 0x08223684. `ldr r0, [r0]`
      dereferences the caller's mailbox slot once, then bl @ 0x080b4ae4
      to the ported csem_wait @ 0x08056904 (the 8-byte mailbox block
      and CountingSem share the state+id layout); the timeout arrives
      untouched in r1, which is why Ghidra's decompile shows one
      parameter. Verdict remapped: 0 -> 0, 1 (timeout) -> 3, any other
      code -> 0 (cmp #0 / bne, cmp #1 / bne, mov r0, #3; the
      other-code fold is unreachable from the ported csem_wait, kept
      for the branch structure). Callers cmp r0, #0x3 (0x0813b1ac,
      0x0814b9f4), map nonzero to -1 (0x080e2ee8, 0x080e4314), or
      discard the result (the fill loop). The bl is kept as an indirect
      call through the QUEUE_WAIT_SEM slot (default: the real port) so
      LLVM cannot inline the whole 88-byte semaphore into the 44-byte
      wrapper; the port is the shipped default of
      POOL_BASE_OPS.queue_wait, replacing the no-op stub (identical
      verdict under stock hooks: an empty mailbox times out). Host
      tests drive the 0/1/other mapping through the slot and the
      timeout/zero-clamp/count-bookkeeping end-to-end through the real
      csem_wait. match.py structural: ldr r0,[r0] then blx-in-place-of-
      bl, the mapping folded to cmp #1 + moveq #3 (0/else -> 0) — the
      original's two-compare structure rendered branchless.

  # --- indexed UI mode-state flag ---
  - address: 0x080b649c
    ghidra: FUN_080b649c
    name: indexed_mode_flag
    size: 28
    sig: "u32 indexed_mode_flag(i16 index)"
    module: ui/mode_state
    status: ported
    renames:
      param_1/r0: index
      DAT_080b64b8: mode_state_object_table
      uVar1/r0: mode_flag
    notes: >-
      Raw ARM uses smulbb with 99 then an indexed ldr shifted by two, so
      the signed low halfword index selects an object-pointer table entry
      at 0x08b2f648 + index * 0x18c. It reads that object's word +0x860
      and returns only its low five bits. The table is modeled by the
      MODE_STATE_OBJECT_TABLE target-global seam; no bounds or null checks
      are present in the original or port.

  - address: 0x08037cc0
    ghidra: FUN_08037cc0
    name: invalidate_data_cache_line_by_mva
    size: 8
    sig: "void invalidate_data_cache_line_by_mva(u32 mva)"
    module: drivers/cp15
    status: ported
    renames:
      param_1/r0: mva
    notes: >-
      Sends the supplied MVA through ARM926EJ-S CP15 MCR p15,0,r0,c7,c6,1
      (Invalidate Data Cache Line by MVA, 0xee070f36) then returns directly;
      it has no alignment guard or follow-up barrier. The target is a naked
      function so match.py is instruction-exact; host builds route the MVA
      through the shared replaceable cache-maintenance seam for testing.

  - address: 0x08037cd0
    ghidra: FUN_08037cd0
    name: dcache_line_clean_invalidate
    size: 8
    sig: "void dcache_line_clean_invalidate(usize mva)"
    module: heap/dcache
    status: ported
    notes: >-
      mcr p15,0,r0,cr7,cr14,1; bx lr — ARM926EJ-S clean+invalidate
      D-cache single entry by MVA. Sole bl caller 0x08044c34. Port is
      cfg-gated real inline asm on the ARM target, no-op on host; the
      DCACHE_LINE_OP slot (semihost.rs SEMIHOST_SWI pattern) lets host
      tests record the flushed line addresses.

  - address: 0x08037cd8
    ghidra: FUN_08037cd8
    name: dcache_clean_all
    size: 52
    sig: "u32 dcache_clean_all(void)"
    module: heap/dcache
    status: ported
    vars: {uVar1: index, uVar2: way}
    notes: >-
      Clean ENTIRE D-cache by set/way, then drain write buffer. Inner
      sweep r0 = 0, 0x20, ... < 0x1000 (128 sets) inside an outer way
      accumulator r1 stepped 0x2000_0000 until wrap (8 passes), mcr
      p15,0,(r1|r0),c7,c10,2 per line — 1024 cleans — then mov r0,#0;
      mcr p15,0,r0,c7,c10,4; bx lr, so the drain's zero operand doubles
      as the return value. Sole bl caller FUN_0836a990 @ 0x0836aa94.
      Sibling @ 0x08037d0c is the same sweep with clean+invalidate
      (c7,c14,2). NOT the Q16.16 div core the ticket guessed — CP15
      cache maintenance, so it lives in heap/dcache with its neighbors
      0x08037cc8/0x08037cd0, not runtime/rt_div. Port dispatches the
      two CP15 ops through DCACHE_SET_WAY_CLEAN_OP and
      DCACHE_DRAIN_WRITE_BUFFER_OP slots (real mcrs in the slot
      defaults on target, bit-exact encodings ee070f5a/ee070f9a).
      match.py structural: LLVM unrolls the constant-trip-count outer
      loop into 8 inner-loop copies with the way folded into the orr
      immediate, blx through the slots in place of the inline mcr (the
      documented dispatch-slot deviation), trailing mov r0,#0 return
      matches.

  # --- opaque resource-object field access ---
  - address: 0x0804482c
    ghidra: FUN_0804482c
    name: opaque_object_flags
    size: 8
    sig: "u32 opaque_object_flags(const u8 *object)"
    module: util/field_access
    status: ported
    vars: {"param_1/r0": object, "r0 (return)": flags}
    notes: >-
      Raw ARM is exactly `ldr r0,[r0,#0xc]; bx lr`: an unchecked,
      unaligned-capable flags-word read. The immediately preceding
      0x0804472c tests bit 0 of the same +0x0c word on its 0x44-byte
      opaque object in r0 and tests returned mask 0x2; direct caller
      0x082a2e60 reaches a selected child through +0x14 and tests returned
      mask 0x1.
      The shared flag use establishes the field role, but not a stable
      ownership type; port therefore keeps the object opaque.

  - address: 0x08044190
    ghidra: FUN_08044190
    name: strlen_byte_loop
    size: 32
    sig: "u32 strlen_byte_loop(const u8 *s)"
    module: libc/strlen
    vars: {param_1: s, iVar1: len}
    status: ported
    notes: >-
      Unguarded plain byte loop, independent of the ADS strlen at
      0x08392478. Copies s to r1, clears the 32-bit r0 count, loads
      bytes through r1, and increments both r1 and r0 only for a non-NUL
      byte; the first NUL is read but not stepped over. Codegraph found
      no named direct callers; the function remains separately exported
      as strlen_byte_loop for its 32-bit return ABI.

  - address: 0x080413dc
    ghidra: FUN_080413dc
    name: ft_clear_selected_entry_index
    size: 24
    sig: "i32 ft_clear_selected_entry_index(FtSelectionPrefix *selection)"
    module: ft/selection
    vars: {param_1: selection}
    status: ported
    notes: >-
      The selection fallback's 24-byte guard: null returns -50
      (0xffffffce), otherwise only the word at +4 is zeroed and it
      returns 0. Callers 0x08041ae4 and 0x08041d48 read that word as the
      index passed to FUN_08053d6c; after a selected-entry lookup cannot
      be used, they clear it before taking the unselected path. The
      leading word and caller-owned trailing record remain untouched.

  - address: 0x08044200
    ghidra: FUN_08044200
    name: ft_round_add_clear_bit16
    size: 36
    sig: "u32 ft_round_add_clear_bit16(u32 left, u32 right)"
    module: ft/arith
    vars: {param_1: left, param_2: right}
    status: ported
    notes: >-
      ARM sequence: tst bit 0 of each operand; only for an odd operand,
      add #1 then bic bit 16; add the resulting u32 words; bic bit 16
      from the sum. The 0x08044a9c and 0x0806b734 callers form left from
      a byte/halfword plus 1/2, pass right in a full register, and use
      the result in unsigned comparisons. This supports u32 but not a
      more specific upstream representation, so the operation-based name
      is intentional. All adds wrap modulo 2^32.

  - address: 0x080442a0
    ghidra: FUN_080442a0
    name: tdat_dispatch_message
    size: 52
    sig: "u32 tdat_dispatch_message(u8 *element, u32 message, u8 *arguments)"
    module: ui/tdat_message_dispatch
    vars:
      param_1/r0: element
      param_2/r1: message
      param_3/r2: arguments
      param_1_plus_0x4c: handler_address
      param_1_plus_0x50: context
      FUN_0806aa3c: ui_element_is_tdat_class
    status: ported
    notes: >-
      Raw ARM extent is exactly 0x080442a0..0x080442d4: push {r4, r5, r6,
      lr}; call ui_element_is_tdat_class at 0x0806aa3c; on success load the
      virtual handler and its context from element+0x4c/+0x50 and tail-call
      handler(element, message, arguments, context). A rejected class,
      including NULL, returns 0xffffffce. Decoding every B/BL word in
      osos.dec verifies 11 direct unconditional bl callers and one predicated
      beq tail branch at 0x0806aaa0; the branch is the refcount transition
      path. The stored handler is a target-width dynamic function address, so
      the port's TDAT_MESSAGE_OPS seam transmutes it only on target builds;
      host tests record the raw handler word and all forwarded arguments.
  - address: 0x08045f14
    ghidra: FUN_08045f14
    name: cstr_to_counted_u8
    size: 64
    sig: "void cstr_to_counted_u8(const u8 *src, u8 *dst, u32 limit)"
    module: libc/counted_copy
    vars: {param_1: src, param_2: dst, param_3: limit, bVar1: source_byte, pbVar2: payload, in_lr: length}
    status: ported
    notes: >-
      Converts a NUL-terminated source into a byte-counted destination:
      dst[0] is first cleared and bytes are appended at dst+1 without a NUL
      terminator. The source NUL test precedes comparison of the zero-extended
      u8 length against u32 limit; a continued iteration stores the byte,
      advances both cursors, then increments the stored length modulo 256.
      Thus a limit greater than 255 is unreachable through the byte counter.
      Callers 0x08052228, 0x08054ea0, and 0x08055f50 pass its result to
      0x08046a88, which reads byte 0 as length and starts at byte 1.


  - address: 0x08045ee0
    ghidra: FUN_08045ee0
    name: strcpy_bounded_nul
    size: 52
    sig: "void strcpy_bounded_nul(const u8 *src, u8 *dst, i32 limit)"
    module: libc/bounded_copy
    vars: {param_1: src, param_2: dst, param_3: limit, iVar1: copied}
    status: ported
    notes: >-
      Reads source before testing its NUL and then comparing copied (starting
      at signed i32 zero) for equality with limit. Copies only while both
      checks pass, incrementing copied with ARM's wrapping 32-bit add, then
      always writes a NUL at the current destination. Thus zero writes only
      the terminator and negative limits copy an ordinary finite string
      through its NUL. Direct callers use it as source-first,
      destination-second bounded text copy (notably 0x08079f54,
      0x08093d64, and 0x080bd7f8).

  - address: 0x08044c10
    ghidra: FUN_08044c10
    name: dcache_clean_invalidate
    size: 56
    sig: "void dcache_clean_invalidate(u8 *addr, usize len)"
    module: heap/dcache
    vars: {param_1: addr, param_2: uncached/off}
    status: ported
    notes: >-
      24 bl call sites, binary-verified — osos.asm drops two lines (DMA
      buffer prep in ATA/USB/LCD drivers, plus
      pool_alloc's uncached path). Bit-31 addr (uncached DRAM alias) =
      early return; else flushes line (addr+off) & ~0x1f for off = 0,
      0x20, ... < len via bl 0x08037cd0. Walk steps from addr, not the
      line start, so a misaligned addr can leave the final spilled line
      unflushed (callers pass padded lengths). Port dispatches per line
      through DCACHE_LINE_OP (real mcr on target); wired as the
      POOL_OPS.dcache_flush default. The clean-only sibling loop without
      the bit-31 guard sits right after @ 0x08044c48 (ported as
      dcache_clean, same module).

  - address: 0x08037cc8
    ghidra: FUN_08037cc8
    name: dcache_line_clean
    size: 8
    sig: "void dcache_line_clean(usize mva)"
    module: heap/dcache
    status: ported
    notes: >-
      mcr p15,0,r0,cr7,cr10,1; bx lr — ARM926EJ-S clean (no invalidate)
      D-cache single entry by MVA. Sole bl caller 0x08044c64 (inside
      dcache_clean's walk). Port is cfg-gated real inline asm on the ARM
      target, no-op on host; the DCACHE_CLEAN_LINE_OP slot (same
      semihost.rs SEMIHOST_SWI pattern as DCACHE_LINE_OP) lets host
      tests record the cleaned line addresses. match.py structural:
      identical mcr, frame-pointer push/pop in place of bare bx lr.

  - address: 0x08044c48
    ghidra: FUN_08044c48
    name: dcache_clean
    size: 48
    sig: "void dcache_clean(u8 *addr, usize len)"
    module: heap/dcache
    vars: {param_1: addr, param_2: len}
    status: ported
    notes: >-
      6 bl call sites, binary-verified — display-layer plane/surface
      flushes (layer_install_planes @ 0x0811feb8 and friends). The
      clean-only sibling of dcache_clean_invalidate @ 0x08044c10: same
      walk (line (addr+off) & ~0x1f for off = 0, 0x20, ... < len,
      stepping from addr not the line start) but per line via
      bl 0x08037cc8 (clean only) and with NO bit-31 uncached-alias
      guard — callers guard themselves (layer_install_planes' tst on
      p0). Port dispatches per line through DCACHE_CLEAN_LINE_OP (real
      mcr on target); a separate slot from DCACHE_LINE_OP so recordings
      distinguish clean-only from clean+invalidate. match.py
      structural: same loop (bic #0x1f, #0x20 stride, off<len bcc),
      blx through the slot in place of bl, crate-wide frame-pointer
      setup. NOTE: layer_install_planes' LAYER_DRIVER_HOOKS.flush_range
      default (src/drivers/display_layer.rs, drivers/ territory) still
      defaults to a no-op — wiring it to dcache_clean is left to the
      drivers/ porter.

  - address: 0x0818ae48
    ghidra: FUN_0818ae48
    name: block_manager_get
    size: 12
    sig: "u8 *block_manager_get(void)"
    module: heap/block_mgr
    status: ported
    notes: >-
      Returns the block-manager object pointer from the global word @
      0x089cb1b4 (the "AMBlockManagerThread" object; the 0x089cb1xx page
      is runtime-initialized — the image shows UI strings there). 14 bl
      call sites, binary-verified. Port models the global as the crate
      static BLOCK_MANAGER (same deviation as DEFAULT_HEAP for
      0x089ca638), NULL until the manager thread is ported/installed.

  - address: 0x0818a364
    ghidra: FUN_0818a364
    name: region_block_size
    size: 24
    sig: "u32 region_block_size(void)"
    module: heap/block_mgr
    status: ported
    notes: >-
      [[0x089cb1b4] + 0x30] — the block manager's per-region block size;
      0 when the manager pointer is NULL. 16 bl call sites plus the
      alias thunk b @ 0x081a7a9c (1 caller), binary-verified. Wired as
      the POOL_OPS.region_block_size default (pool_seed_regions /
      block_deque_fill sizing).

  - address: 0x081a7a9c
    ghidra: thunk_FUN_0818a364
    name: region_block_size_thunk
    size: 4
    sig: "u32 region_block_size_thunk(void)"
    module: heap/block_mgr
    status: ported
    notes: >-
      ALIAS - `b 0x0818a364`, a bare tail-branch thunk of
      region_block_size (verified against osos.asm); 1 bl caller. No
      separate port; hook this address to the region_block_size symbol.

  - address: 0x081a8204
    ghidra: FUN_081a8204
    name: aligned_buffer_reset
    size: 40
    sig: "u8 *aligned_buffer_reset(u8 *buffer)"
    module: heap/aligned_buffer
    status: ported
    vars:
      param_1: buffer
      FUN_082aad14: operator_delete_tag3 (direct, ported in
        heap/veneers - no seam)
    notes: >-
      The reset half of the firmware's 32-byte aligned-buffer owner, a
      two-word value type {+0x00 data (the 32-aligned view into the
      block), +0x04 allocation (the raw owned block)}. 40 bytes
      exactly, 0x081a8204..0x081a822c, ten instructions, no literal
      pool. 53 bl call sites, 0 b, binary-scanned by decoding every
      B/BL word in osos.dec. Algorithm: if the allocation word is
      non-NULL, operator_delete_tag3(allocation); then zero the
      allocation word FIRST and the data word second (the original's
      store order); return buffer. The class occupies
      0x081a8198..0x081a8228: the align-up helper 0x081a8198 (pad =
      0x20 - (block & 0x1f), returns (block + 0x1f) & !0x1f), the
      alignment query 0x081a81bc (`mov r0, #0x20; bx lr` - a
      byte-pattern scan lumps it with the 17 deque_seg_capacity
      copies, but in this translation unit it is the buffer's
      alignment constant), the constructor 0x081a81c4 (29 bl sites:
      zero both words, operator_new_tag3(size + 0x20), raw block to
      +4, aligned view to +0), and this reset. 0x20 is the ARM926EJ-S
      cache-line size; observed users match - 0x0804fe68 (a
      FreeType-area file reader, cf. the 0x08278xxx stream functions)
      fills the buffer with a whole file via __rt_memcpy, the
      0x08072xxx cluster resets it on error paths. Deviation: none of
      substance - the callee is ported and called directly; the
      original's own cmp/blne guard is kept even though
      operator_delete_tag3 NULL-guards internally (the double guard is
      faithful). Both words are 32-bit target words. Host tests: an
      installed allocation is freed exactly once with the ALLOCATION
      word (tag 3, observed through the veneers.rs mock heap) and both
      words zeroed, object returned; a NULL allocation frees nothing
      and still zeroes both words. match.py structural: same
      test/conditional-delete/zero-both/return shape with the +4-then-+0
      store order; the diff is LLVM rotating the NULL path out of line
      and the crate fp frame.

  - address: 0x081a81c4
    ghidra: FUN_081a81c4
    name: aligned_buffer_init
    size: 64
    sig: "u8 *aligned_buffer_init(u8 *buffer, usize size)"
    module: heap/aligned_buffer
    status: ported
    vars:
      param_1: buffer
      param_2: size
      FUN_082aad74: operator_new_tag3 (direct, ported in
        heap/veneers - no seam)
      FUN_081a8198: align_up_to_cache_line (private sibling fn)
    notes: >-
      The constructor half of the firmware's 32-byte aligned-buffer
      owner, the two-word value type {+0x00 data (the 32-aligned view
      into the block), +0x04 allocation (the raw owned block)} whose
      reset half @ 0x081a8204 is ported in the same module. 64 bytes
      exactly, 0x081a81c4..0x081a8204, sixteen instructions, no
      literal pool; the reset body opens immediately after. 29 bl
      call sites, every one unconditional (0 predicated blne/bleq
      forms, 0 b), binary-scanned by decoding every B/BL word in
      osos.dec. Algorithm: zero the data word FIRST and the
      allocation word second (the original's str order), then raw =
      operator_new_tag3(size + 0x20), store the raw block at +0x04
      unconditionally (the str sits between the cmp and the beq), and
      only when raw is non-NULL store the 32-aligned view
      (raw + 0x1f) & !0x1f at +0x00 via the 0x081a8198 helper;
      return buffer. The +0x20 headroom keeps size usable bytes after
      alignment; 0x20 is the ARM926EJ-S cache-line size (cache/DMA
      staging buffers - see the reset entry's sibling evidence).
      Ghidra's C (decomp/c/017/081a81c4_FUN_081a81c4.c) matches the
      raw decode exactly this time. Deviations: the align-up helper
      0x081a8198 is a private inline(never) sibling fn rather than
      its own exported port - its pad out-store (0x20 - (block &
      0x1f)) targets the constructor's saved-r1 stack slot, which the
      epilogue pops into a discarded register, so the pad is dead at
      this call site and unmodeled; operator_new_tag3 is called
      directly (ported). Both words are 32-bit target words; the
      block pointer is only stored and masked, never dereferenced, so
      host tests use small integer stand-ins below 4 GiB (no slab
      mapping needed). Host tests: the alloc fires once with
      size + 0x20 and tag 3 and both views are installed on an
      aligned block; a residue sweep 0..0x1f proves the align-up view
      for every misalignment; the NULL path stores NULL at +0x04,
      leaves +0x00 zero, and still returns the object. match.py
      structural: same zero-both / add #32 / bl alloc / store +4 /
      guarded bl align / store +0 / return shape; the diff is the
      crate fp frame, LLVM taking size from r1 directly instead of
      the original's stack reload, and the two bl targets printing
      unresolved in the static-archive disassembly (operator_new_tag3
      and the private helper - both verified in the archive symbol
      table).

  - address: 0x0818b0c4
    ghidra: FUN_0818b0c4
    name: manager_take_blocks
    size: 60
    sig: "i32 manager_take_blocks(u8 *manager, u8 *client_state, usize count)"
    module: heap/block_mgr
    vars: {param_1: manager, param_2: client_state, param_3: count, uVar1: result}
    status: ported
    notes: >-
      The block-manager hand-out client_populate calls as
      0x0818b0c4(*(client+4), client+8, count) — its one bl call site @
      0x081fc310 (client_populate), binary-verified. A pure mutex
      bracket: lock the manager's own mutex object at manager+0x148
      (the C++ owner-tracked mutex pair 0x082e8390/0x082e83d8 via
      thunks 0x082621a8/0x082621ac, block_region.rs's REGION_MUTEX_OPS
      boundary), run the body 0x0818b108(manager,
      client_state, count) through the new BLOCK_MANAGER_OPS
      take_blocks_body slot (house pattern, blx for bl), unlock, return
      the body's verdict held in a callee-saved register across the
      unlock (the client_commit.rs shape). The body slot's default is
      now the REAL 0x0818b108 port (take_blocks_body, stub_take_blocks_body
      removed) — unreachable with no block manager on device,
      the fail-closed no-manager contract — so the
      CLIENT_POPULATE_OPS.manager_take_blocks default is now this REAL
      port (replacing client_populate.rs's wholesale stub_manager_take_blocks,
      removed): the wired configuration reports the same refusal 0, one
      boundary deeper. Host tests cover the lock/body/unlock ordering
      with argument pass-through, verdict pass-through (0/1/-1/7) with
      the mutex released even on refusal, and the fail-closed wired
      default. match.py structural: same push, manager+0x148,
      lock/body/unlock, verdict-in-callee-saved shape; the diff is
      blx-through-table for the three calls and LLVM's fp frame, the
      accepted house deviations.

  - address: 0x0818b108
    ghidra: FUN_0818b108
    name: take_blocks_body
    size: 264
    sig: "i32 take_blocks_body(u8 *manager, u8 *client_state, usize count)"
    module: heap/block_mgr
    vars: {param_1: manager, param_2: client_state, param_3: count, local_24: end, local_28: cursor, local_18: pos, local_1c: first, local_20: last, uVar3: remaining, iVar2: entry}
    status: ported
    notes: >-
      The block hand-out body the manager_take_blocks bracket (above)
      runs under the manager+0x148 mutex — the actual
      (*(client+4), client+8, count) work from client_populate's
      perspective. 2 bl call sites @ 0x0818b0e8 (the bracket) and
      0x0818b050 (FUN_0818b028, count always 1), binary-verified.
      Gate: the manager free list's count word (list object at
      manager+0x4, count at +0x10 -> absolute +0x14) must cover count
      (unsigned bcc, equality grants), else refuse 0. Then a stack
      end-iterator walks count steps from the free-list head
      (manager+0x8) through the 0x083d5e88 advance (ported in place as
      iter_advance; NULL current node tail-calls heap_panic
      0x08030f44, defensive/unreachable given the gate); the range
      [head, end) is spliced out of the manager's free list into the
      client state's own list (the client state IS a list object;
      position = its head word at +0x4) through the unported 316-byte
      0x083d5d20 — the new BLOCK_MANAGER_OPS.splice_blocks slot
      (documented no-op default, verdict discarded like the original);
      finally a stamping walk re-reads the client head AFTER the
      splice and, per node, derefs to the +0xc block entry
      (0x083d5ea0, ported in place as iter_entry), reads the entry's
      +0x4 owner handle (NULL -> heap_panic, non-returning) and stamps
      the handle's +0xc back-link with the node. Returns 1.
      0x083d5e88/0x083d5ea0 are inlined private helpers (their only
      call sites are this body). Deviations: the original's triple
      entry/owner re-read per node and the dead post-panic NULL
      re-check collapse to one read (0x08030f44 is non-returning); the
      intermediate iterator spills collapse into the two locals the
      splice consumes; list pointer fields are u32 target pointers
      zero-extended on read / truncated on the stamp (host fixtures in
      a low mmap slab, the client_populate.rs idiom). The port is the
      wired BLOCK_MANAGER_OPS.take_blocks_body default
      (stub_take_blocks_body removed); client_populate.rs's
      wired-default test now points its fake client at a mapped fake
      manager with a 0 free-count word (the real body dereferences the
      manager where the stub refused — same refusal 0, one gate
      deeper). Host tests: refusal on a short free count (no splice,
      no stamps), a 2-of-3 hand-out (splice args with the end iterator
      two advances in, owner back-links stamped in list order),
      free == count (strict bcc boundary, end advances off the list to
      NULL), count 0 (empty-range splice, grants, nothing stamped),
      and the wired-default refusal through the real body's gate.
      match.py structural: same gate/advance-loop/splice/stamping-walk/
      panic/return-1 shape; the splice is blx-through-table for bl,
      the u32 word accesses come out as ldrb/strb chains from the
      (read|write)_unaligned idiom, LLVM peels the first advance
      iteration and adds its fp frame — the accepted house deviations.

  - address: 0x083d5d20
    ghidra: FUN_083d5d20
    name: list_splice
    size: 316
    sig: "i32 list_splice(u8 *dst_list, u8 **dst_pos, u8 *src_list, u8 **src_first, u8 **src_last)"
    module: cxx/list_splice
    vars: {param_1: dst_list, param_2: dst_pos, param_3: src_list, param_4: src_first, param_5: src_last, local_28: cursor, local_24: probe, iVar1: moved, iVar2: range_last}
    status: ported
    notes: >-
      std::list node-range splice (the ADS checked-iterator C++
      library's list::splice(pos, other, first, last)) — the callee
      take_blocks_body (0x0818b108, above) hands block-node ranges
      through; 3 bl call sites @ 0x0818adb0, 0x0818ae34 and
      0x0818b1a0, binary-scanned. Layout recovered from the cluster:
      list object +0x0 identity word / +0x4 head / +0x10 node count,
      node +0x0 owning-list identity / +0x4 next / +0x8 prev / +0xc
      element; lists are circular through a sentinel (end() is the
      sentinel, never NULL). Algorithm: three ownership checks (the
      iterator-debug asserts compiled down to a refusal — pos's node
      must carry dst_list's identity, first's and last's must carry
      src_list's, NULL node's owner reads 0) return 0 untouched on
      mismatch; a dead validation (nonempty source -> compare first
      against the list object itself, verdict discarded, reproduced
      for fidelity); an adopt walk re-stamping each moved node's +0x0
      owner with dst_list's identity while counting (0x083d5e88
      advance, NULL current node panics); a NULL last is fatal
      (0x08030f44, unreachable for circular lists); then the six-store
      circular re-link (range_last = last->prev; first->prev->next =
      last, last->prev = first->prev, first->prev = pos->prev,
      range_last->next = pos, pos->prev->next = first, pos->prev =
      range_last, in the original's order) and both count fixups,
      return 1. Quirk: NO empty-range guard — first == last moves zero
      nodes but the re-link still runs, swapping the predecessor links
      (pinned by a host test). Helpers ported in place, private:
      iter_owner (0x083d5e5c, 20 bytes), iter_equal (0x083d5e70, 24
      bytes), iter_advance (0x083d5e88, 24 bytes — same in-place
      rationale as block_mgr.rs's copy). The crate adapter
      splice_blocks (verdict discarded, like the original's callers)
      is now the wired BLOCK_MANAGER_OPS.splice_blocks default
      (stub_splice_blocks removed — the only src/heap touch, three
      stale "unported" doc mentions updated). Host tests: a middle
      range (links both directions, counts, owner re-stamps of only
      the moved nodes), the whole source through the sentinels
      (emptied source self-links), three foreign-iterator refusals
      (full-slab snapshot unchanged), the empty-range quirk, and the
      adapter (grant + silent refusal). match.py structural: same
      ownership-check/adopt-walk/relink/count-fixup shape; the u32
      word accesses come out as ldrb/strb chains from the
      (read|write)_unaligned idiom, the private helper bls label as
      list_splice+off, LLVM adds its fp frame — the accepted house
      deviations.

  - address: 0x080751a0
    ghidra: FUN_080751a0
    name: crc32_table_gen
    size: 96
    module: util/crc32
    status: ported
    notes: MSB-first table, poly 0x04C11DB7, into RAM @ 0x08ad786c
      (guarded once-init @ 0x082905a4). Compute loops in image are
      slicing-by-4 over runtime tables (0x0807badc reflected, 0x080f4428 MSB).

  - address: 0x0894289c
    name: crc32_reflected_table
    size: 1024
    module: util/crc32
    status: ported
    notes: >-
      zlib/IEEE table (entry[0x80]=0xEDB88320); dead DATA in osos —
      zero literal-pool references to 0x0894289c in the whole image.
      All 256 entries verified identical to the compile-time
      CRC32_REFLECTED_TABLE in util/crc32.rs (same reflected poly
      0xEDB88320, same bitwise generator), so the port covers it exactly.
      Vector: "123456789" = 0xCBF43926 (reflected), 0xFC891918
      (MSB/BZIP2 init/xorout).

  - address: 0x080e799c
    ghidra: FUN_080e799c
    name: utoa
    size: 116
    sig: "char *utoa(u32 value, char *buf, u32 base)"
    module: util/utoa
    status: ported
    notes: Bases 2..36; sign only for base 10; digit table @ 0x080e7a10.

  - address: 0x080eb6dc
    ghidra: FUN_080eb6dc
    name: variant_compare
    size: 164
    module: util/utoa
    status: ported
    notes: C++ type-tagged union compare, tags 0-3; tag0 length-prefixed
      string via memcmp; tag3 i32; NULL conventions documented.

  # --- Corrections from batch 8 ---
  # 0x8391e38 (names.yaml earlier said "assert printer") is actually a
  # normalized 3-way strcmp (-1/0/+1); assert printer is elsewhere.
  # RTXC Quadros kernel lives in the S5L8702 MASK ROM (0x22000000), reached
  # via the thunk table 0x08037db0-0x0803829c (~124 thunks). RAM-side
  # wrappers: sem create 0x08056724, wait 0x08056510, signal 0x08056710,
  # mutex create 0x080744a4, lock 0x0807f5c4, unlock 0x0807f6a0,
  # kernel-running check 0x0809444c; IRQ dispatcher 0x080080ac.

  # --- Batch 9: kernel-service layer (RTXC lives in mask ROM 0x22000000) ---
  - address: 0x08056724
    ghidra: FUN_08056724
    name: sem_create
    size: 52
    module: kernel/sync_sem
    status: ported
    notes: >-
      ISR-context -> static slot, else heap slot; ROM create dispatch
      via thunk 0x08037e70 -> ROM 0x22003d70 (opcode 1 = semaphore).
      CORRECTION: only sem_delete uses the 0x08037e40/0x22003dc8 dispatcher.

  - address: 0x0805646c
    ghidra: FUN_0805646c
    name: sem_delete
    size: 68
    module: kernel/sync_sem
    status: ported
    notes: ROM delete via 0x08037e40 -> 0x22003dc8, opcode 1; frees slot.

  - address: 0x08056510
    ghidra: FUN_08056510
    name: sem_wait
    size: 20
    module: kernel/sync_sem
    status: ported
    notes: Guard then tail -> ROM 0x22003fd0. 14 call sites.

  - address: 0x08056710
    ghidra: FUN_08056710
    name: sem_signal
    size: 20
    module: kernel/sync_sem
    status: ported
    notes: Guard then tail -> ROM 0x220042b4. 11 call sites.

  - address: 0x080744a4
    ghidra: FUN_080744a4
    name: mutex_create
    size: 28
    module: kernel/sync_mutex
    status: ported
    notes: 87 call sites. Plain count-1 semaphore cell — NO recursive/owner
      state (the +4 word is write-only padding).

  - address: 0x080744c0
    ghidra: FUN_080744c0
    name: timer_set_delay
    size: 24
    sig: "void timer_set_delay(u8 *timer, u32 delay)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
      param_2: delay
    notes: >-
      Trace/assert helper 0x08076954(timer) (dispatched through
      TimerOps::trace_assert, as in timer_stop), then stores delay into
      the period word at +0x4 - the value the arm helper 0x0807a228
      multiplies by 1000 to compute the deadline. timer_start_after
      (0x0812c63c) tail-branches here; ported as its own function first
      so the start_after body is real code, not another stub slot.
      match.py structural (trace call goes through the ops table, the
      str to +4 is verbatim).

  - address: 0x08076954
    ghidra: FUN_08076954
    name: timer_trace_assert
    size: 40
    sig: "void timer_trace_assert(u8 *timer)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
    notes: >-
      Raw ARM confirms the true 40-byte extent: nine instructions (36
      bytes) from 0x08076954 through the tail branch at 0x08076974 and
      literal 0x089ca318 at 0x08076978; the next entry opens at
      0x0807697c. A complete scan of every ARM B/BL word finds 11 direct
      branch sites: 9 bl (2 bleq, 3 blne, 4 unconditional) and 2 tail b.
      It locks the pending-list mutex @ 0x089ca318 through ported
      mutex_lock @ 0x0807f5c4, calls the unported trace/validate walk
      0x0809e620 through TimerOps::trace_validate without a NULL guard,
      then tail-branches to ported mutex_unlock @ 0x0807f6a0. Deliberate
      deviations: the tail branch is a return-position call and the
      mutex is volatile-copied before locking so LLVM preserves the pair.

  - address: 0x08074ba0
    ghidra: FUN_08074ba0
    name: retail_vsnprintf
    size: 60
    sig: "i32 retail_vsnprintf(u8 *buf, usize size, const u8 *format, VaList args)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: buf
      param_2: size
      param_3: format
      param_4: args
      local_c: cursor
    notes: >-
      Second firmware vsnprintf veneer (distinct from the ported
      0x08032f94 implementation), named retail_vsnprintf to avoid that
      symbol collision. Raw ARM: size==0 returns 0 before touching buf or
      calling the core; otherwise it writes args to sp+0 and buf to sp+4,
      calls 0x08077c94 as (sink descriptor 0x0807ca58, &cursor, size-1,
      format, args), NUL-terminates at the final cursor, and returns the
      core count untouched. The 0x08077c94 conversion core remains an
      injectable RETAIL_VSNPRINTF_ENGINE seam; tests cover forwarding,
      bounded truncation/NUL, count propagation, and the zero-size path.

  - address: 0x0807a228
    ghidra: FUN_0807a228
    name: timer_arm
    size: 176
    sig: "void timer_arm(u8 *timer)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
    notes: >-
      7 call sites (5 bl + 2 tail b, incl. the timer_restart tail
      branch). functions.csv lists it as "default"; named for what it
      does. No-op when the armed flag at +0x1c is set; otherwise, under
      the pending-list mutex @ 0x089ca318 (NOT timer_stop's 0x089cb294),
      runs the trace/validate walk 0x0809e620 (the unlocked core of
      trace_assert 0x08076954), and when the period word +0x4 is nonzero
      computes the deadline +0x8 = tick() + period*1000 (original: *125
      then lsl #3; tick getter 0x08056658 -> 0x0836af80 reads the
      free-running counter @ 0x3C7000B4), stamps 'run ' into the
      queued-state word +0x18, and splices the timer into the
      singly-linked pending queue (head cell @ 0x089ca300, link at +0x0)
      ahead of the first entry whose deadline compares strictly greater
      (signed comparator 0x082a243c on the +0x8 words - ties stay FIFO),
      then sets the armed flag. Notify helper 0x0808e2a8 on the cell
      0x089ca310 runs on BOTH the queued and zero-period paths; the
      mutex unlock is the tail branch. The four callees are new TimerOps
      slots (trace_validate / tick / compare_deadlines / notify_pending,
      documented stubs); the arm_timer slot now defaults to this port.
      Pending-list link words are absolute u32 pointers on target,
      offset links from a per-test arena base on host (the
      heap/alloc_core.rs precedent). match.py structural: same early
      return, same walk/break loop, LLVM merges the strne/streq splice
      pair into one predicated store and emits mla for tick+period*1000.

  - address: 0x0807f5c4
    name: mutex_lock
    size: 8
    module: kernel/sync_mutex
    status: ported

  - address: 0x0807f6a0
    name: mutex_unlock
    size: 8
    module: kernel/sync_mutex
    status: ported

  - address: 0x0807f650
    name: mutex_delete
    size: 32
    module: kernel/sync_mutex
    status: ported

  - address: 0x0807f670
    ghidra: FUN_0807f670
    name: task_yield
    size: 16
    sig: "i32 task_yield(void)"
    module: kernel/condvar
    status: ported
    notes: >-
      Yield wrapper between mutex_delete and mutex_unlock: push {r4,lr};
      bl kernel_yield @ 0x080568fc (r0=0 -> ROM 0x22004260, exact RTXC
      service unidentified, yield-like); mov r0,#0; pop {r4,pc} — the ROM
      result is discarded and 0 is always returned. 1 bl call site.
      Already ported as `task_yield` by the condvar-layer region port
      (0x0807f5cc..0x0807f740 in kernel/condvar.rs, CONDVAR_HOOKS.
      task_yield slot); this entry records the per-address mapping. The
      stock callee is ported as kernel_yield (kernel/task). match.py
      structural: same call-then-return-0 shape; the diff is the crate fp
      frame and the dispatch-slot blx standing in for the direct bl —
       the documented condvar.rs deviation.

  - address: 0x0807f680
    ghidra: FUN_0807f680
    name: condvar_init
    size: 32
    sig: "void condvar_init(CondVar *condvar)"
    module: kernel/condvar
    status: ported
    renames:
      r0: condvar
      r4: condvar (saved)
    notes: >-
      Condvar initializer between task_yield (0x0807f670) and
      mutex_unlock (0x0807f6a0): push {r4,lr}; bl sem_create @
      0x08056724 (a.k.a. object_create in CONDVAR_HOOKS); store the new
      kernel object handle at condvar+0; store 0 at +4 and +8 (wait-queue
      head/tail = empty); pop {r4,pc}. 20 bl call sites. Already ported
      as `condvar_init` by the condvar-layer region port
      (0x0807f5cc..0x0807f740 in kernel/condvar.rs, CONDVAR_HOOKS.
      object_create slot); this entry records the per-address mapping,
      like the task_yield commit (9ac77aa) did. Sibling no-allocation
      initializer is condvar_bind @ 0x080ed9c8 (ported). match.py
      structural: same create-then-zero-queue shape; the diff is the
      crate fp frame, the dispatch-slot blx standing in for the direct
      bl, and LLVM merging the +0/+4 stores into one stm — the
      documented condvar.rs deviation.

  - address: 0x0809444c
    ghidra: FUN_0809444c
    name: kernel_running
    size: 72
    module: kernel/sync_mutex
    status: ported
    notes: flag @ 0x089ca848; returns current-task-id, calling notify helper
      0x08060f80 with callback @ 0x083e2e38 when id==0 but a task exists.

  - address: 0x080744d8
    ghidra: FUN_080744d8
    name: condvar_signal
    size: 32
    sig: "void condvar_signal(CondVar *condvar)"
    module: kernel/condvar
    status: ported
    notes: >-
      Single-shot condvar wake, next to the mutex layer: list_pop_front
      (0x080f10b8) on the wait queue at condvar+4; if a waiter came off,
      tail-branch waiter_wake (0x080567f8) on its object (node+4). Empty
      queue is a no-op. 10 bl call sites (mqueue_node_recycle 0x080ed958
      twice, pool paths 0x0809ec5c/0x8100218/0x8106698/...). Complements
      condvar_broadcast (0x0807f5cc), which drains the whole queue.
      match.py structural (pop inlined by LLVM; wake stays a tail call).

  - address: 0x0807f5cc
    name: condvar_layer
    size: 372
    module: kernel/condvar
    status: ported
    notes: >-
      Region 0x0807f5cc..0x0807f740: sleep queue over RTXC via intrusive
      singly-linked list (head/tail anchor). condvar_broadcast (0x0807f5cc,
      FIFO drain — NO single-shot signal), mqueue_receive (0x0807f5f4),
      condvar_init/destroy/wait (0x0807f680/0x0807f650/0x0807f6ac, wait
      returns 3 on timeout), task_yield (0x0807f670 -> ROM 0x22004260).
      List ops: list_pop_front 0x080f10b8, list_push_back 0x080f1158,
      list_remove 0x080f10ec.

  - address: 0x080ed9c8
    ghidra: FUN_080ed9c8
    name: condvar_bind
    size: 20
    sig: "void condvar_bind(CondVar *condvar, u32 *lock_obj)"
    module: kernel/condvar
    status: ported
    notes: >-
      The no-allocation condvar initializer (vs condvar_init 0x0807f680,
      which creates a fresh kernel object): stores the caller-provided
      lock word into +0 and clears the wait queue (+4/+8). Pure stores,
      no calls. 21 bl call sites, among them queue_pool_init twice
      (0x0809eadc backref pool base, 0x0809eb44 backref pool+0x2c).
      match.py exact modulo frame-pointer noise.

  - address: 0x082743dc
    ghidra: FUN_082743dc
    name: condvar_construct_with_lock
    size: 40
    sig: "CondVar *condvar_construct_with_lock(CondVar *condvar, u32 *lock_obj)"
    module: kernel/condvar
    status: ported
    renames:
      r0: condvar
      r1: lock_obj
      r4: condvar (saved)
    notes: >-
      Raw ARM establishes the exact 40-byte extent
      0x082743dc..0x08274404: the separately linked next function begins
      at 0x08274404. Decoding every ARM B/BL word in osos.dec finds exactly
      11 direct inbound calls, all unconditional plain bl
      (0x08101a04/30, 0x0814ad00, 0x0814b724, 0x0819c244/6c,
      0x081fba7c, 0x081fbdb4/cc, 0x082121a4/cc); no predicated or tail
      calls. It clears the three CondVar words, forwards its untouched r1
      lock-word argument to condvar_bind @ 0x080ed9c8, then returns the
      receiver. Ghidra incorrectly drops the r1 parameter. condvar_bind is
      already ported, so the Rust port calls it directly rather than adding a
      duplicate dispatch seam. No deliberate deviations.

  - address: 0x080ed9dc
    ghidra: FUN_080ed9dc
    name: condvar_wait_forever
    size: 76
    sig: "void condvar_wait_forever(CondVar *condvar)"
    module: kernel/condvar
    status: ported
    renames:
      param_1: condvar
    notes: >-
      Verified extent 0x080ed9dc..0x080eda24 (next entry 0x080eda28).
      Creates a stack waiter, appends it to condvar+4, signals the semaphore
      slot at *condvar->lock_obj, calls csem_wake (0x080569a4) on the waiter
      id, deletes the waiter, clears its stack handle, then waits on the same
      semaphore slot. It deliberately does not unlink the stack node: the
      raw csem service must return only after a waker removed it. Binary scan:
      30 unconditional bl call sites and zero predicated forms. Port directly
      calls existing waiter_create/waiter_delete, csem_wake, sem_signal, and
      sem_wait entries; no duplicate dispatch seam.

  - address: 0x08003080
    name: irq_entry
    size: 120
    module: kernel/irq
    status: ported
    notes: Naked-asm RTXC frame (IRQ stack 0x08b32b58, nesting counter
      0x22008a50). match.py 25/30 (rest = relocations/pool words).

  - address: 0x080080ac
    ghidra: FUN_080080ac
    name: irq_dispatch
    size: 32
    module: kernel/irq
    status: ported
    notes: >-
      enter -> vic_dispatch -> exit. VIC blocks at 0x38e00000/0x38e01000
      (PL192-style: STATUS+0, INTENABLE+0x10, INTENCLEAR+0x14, VECTADDR+0x100,
      ADDRESS+0xf00), shared clear block 0x38e02008; ack = write 0x01010101.

  - address: 0x08004d7c
    name: rtxc_irq_enter
    size: 72
    module: kernel/irq
    status: ported
    notes: Lazy one-time init gated on ROM flag 0x22008c70 bit 0.

  - address: 0x08004dfc
    name: vic_dispatch
    size: 100
    module: kernel/irq
    status: ported
    notes: VIC0 priority over VIC1; vic_vector_init programs each slot with
      its own irq number (VICADDRESS reads back as index).

  - address: 0x08001d10
    name: irq_exit
    size: 100
    module: kernel/irq
    status: ported
    notes: Nesting high-water, outermost -> frame->TCB, scheduler tail.

  - address: 0x081669dc
    ghidra: FUN_081669dc
    name: irq_binding_attach_enable
    size: 24
    sig: "void irq_binding_attach_enable(IrqBinding *binding, Option<IrqHandler> handler)"
    module: kernel/irq_binding
    status: ported
    renames:
      param_1: binding
      param_2: handler
    notes: >-
      Verified extent 0x081669dc..0x081669f4 (24 bytes); Ghidra reports 28
      by absorbing the `push {r4,lr}` of the next sibling FUN_081669f4.
      Front door of the IrqBinding family (8-byte POD: u8 irq @ +0, handler
      word @ +4; ctor 0x08166a2c, set 0x081669bc, enable 0x08166a14,
      disable 0x081669a4, unbind 0x081669f4, empty dtor 0x08166a3c).
      Attaches `handler` to `binding->irq` in the IRAM dispatch table
      0x22010200 (bl FUN_081669bc) then unmasks the source in the VIC
      INTENABLE (tail b FUN_08166a14). Ghidra's C drops the r1 handler
      argument entirely. Binary scan: 15 unconditional bl call sites, zero
      predicated forms; callers pass the handler from a literal-pool word
      (e.g. driver init 0x08058388 builds ten bindings for irqs 8, 0x15,
      0x21, 3, 2, 1, 0, 0x10, 0x11, 0x18). Both callees unported, so they
      ride the IRQ_BINDING_ATTACH_OPS read_volatile seam (animation.rs
      precedent): target defaults transmute 0x081669bc/0x08166a14
      (hook-ready), host defaults inert. No NULL guard, no irq-range guard
      at this level (table helpers ignore >= 64). match.py: structural
      diff only from the seam indirection (ldrd/blx vs bl); LLVM kept the
      enable as a tail call.

  - address: 0x08037db0
    name: rom_thunk_table
    size: 1264
    module: kernel/thunks
    status: ported
    notes: 158 entries (NOT ~124), all `ldr pc,[pc,#-4]` (0xe51ff004),
      targets 0x22000020..0x2200881c; exactly two duplicated targets.
      Registry in src/kernel/thunks.rs, machine-verified against the binary.

  - address: 0x08037e00
    name: rom_gateway_wrappers
    size: 256
    module: kernel/task_lock
    status: ported
    notes: >-
      32 wrappers @ 0x08037e00-0x08037f00, all ported. Identified:
      rom_memmove (e00), rom_sem_wait (e08), rom_sem_signal (e10),
      kernel_ticks (e20, 47 callers), kernel_op_dispatch (e40),
      task_lock/unlock (e48/e50), size_to_class (e60, UNVERIFIED),
      kernel_create_dispatch (e70), tick_elapsed (eb8), irq_fiq_disable
      (ed8), tick_delay (ef0, 23 callers).

  - address: 0x08037e08
    ghidra: thunk_EXT_FUN_22003fd0
    name: rom_sem_wait
    size: 8
    sig: "usize rom_sem_wait(usize sem)"
    module: kernel/task_lock
    status: ported
    notes: >-
      Already ported as rom_sem_wait in kernel/task_lock.rs under the
      rom_gateway_wrappers span entry above; this entry records the
      per-function verification. ldr pc,[pc,#-4] (0xe51ff004) at
      0x08037e08 + target word 0x22003fd0 at 0x08037e0c: Ghidra reports
      4 bytes, the stub is 8 - the sibling veneer to ROM 0x220042b4
      (rom_sem_signal) starts immediately after. A pure ADS literal
      veneer onto the RTXC kernel semaphore wait, r0 = kernel semaphore
      id, the r0 result word passing back untouched, lr never stored.
      The ROM body is recoverable through the boot-relocator IRAM mirror
      (0x2200XXXX == osos 0x0800XXXX, relocator @ 0x080046e0): mirror @
      0x08003fd0 (188 bytes) indexes the same semaphore record table as
      the signal (0x08a1be28 + id*32) under the kernel lock - a free
      record (owner word 0) is claimed (acquire count +24 bumped, owner
      set from the current-task global 0x2200acf4, count halfword +4
      set to 1) and returns 0; a record already owned by the current
      task is a recursive acquire (count +24 and halfword +4 both
      bumped) and returns 10; otherwise the lock is dropped and the
      task blocks through the gateway (service 14, frame {14, 0, id, 1,
      0}), the frame's result word returned. Call sites binary-verified
      by decoding every ARM B/BL word in osos.dec for every condition
      code: 24 unconditional bl (Ghidra's 24 is correct), zero
      predicated bl, 8 unconditional tail b - fixed-id shims mov r0,
      #(18, 5, 1, 0x11) @ 0x0806a498 / 0x0806a4a0 (kernel_sem5_wait) /
      0x0806a4a8 / 0x0806a4b0 (kernel_sem17_wait), bare pass-through
      aliases @ 0x08115310 and 0x081e6218, cache_lock_wait @ 0x082d792c
      and the ata table-indexed wait @ 0x082d793c - and 1 predicated
      bne @ 0x0805651c, the guarded tail of sem_wait @ 0x08056510 (cmp
      r0,#0; ldrne r0,[r0]; cmpne r0,#0; bne), the only flag-gated
      reach: callers holding a semaphore SLOT pointer guard it
      themselves before tail-branching, the veneer itself has no NULL
      guard. No data word in osos holds 0x08037e08 - never dispatched
      virtually. Deviation (module design): dispatches through the
      ROM_KERNEL hook (volatile slot read) instead of jumping into the
      mask ROM; match.py structural: an indirect call through the hook
      slot, not the 8-byte ldr pc veneer - expected, same family
      pattern as the other 31 wrappers.

  - address: 0x08037e40
    ghidra: thunk_EXT_FUN_22003dc8
    name: kernel_op_dispatch
    size: 8
    sig: "usize kernel_op_dispatch(usize op, usize arg)"
    module: kernel/task_lock
    status: ported
    renames:
      thunk_EXT_FUN_22003dc8: kernel_op_dispatch
      DAT_08037e44: kernel_op_dispatch_rom_target
    notes: >-
      Raw words establish the full ADS veneer: e51ff004 (`ldr pc,[pc,#-4]`)
      at 0x08037e40 and literal 0x22003dc8 at 0x08037e44. Ghidra reports
      only 4 bytes; the sibling task_lock veneer begins at 0x08037e48. The
      relocator at 0x080046e0 makes 0x22003dc8 the IRAM mirror of the
      56-byte osos body 0x08003dc8..0x08003dff. That body stores r0, r1,
      and zero at sp+16..24, stores service words 12 and 6 at sp+4 and
      sp+8, calls gateway 0x08003660 with r0 = sp+4, then returns the
      post-dispatch sp+8 status word. Decoding every ARM B/BL word in
      osos.dec finds exactly 9 unconditional bl callers at 0x08056488,
      0x080564d4, 0x08056508, 0x080566c0, 0x080566e8, 0x080860a0,
      0x0808b23c, 0x0809c7d4, and 0x080a3d1c; zero predicated calls, tail
      branches, or data-word references. Callers use object classes 1, 2,
      and 4; this veneer supplies no NULL or opcode guard. Deliberate
      deviation: the target is foreign mask-ROM code, so the port reads the
      volatile ROM_KERNEL hook instead of issuing the target-only ldr-pc
      transfer. Host tests cover opcodes 1/2/4 with NULL, all-ones, and
      ordinary slot words, and verify the hook's status result returns
      unchanged. match.py shows the expected hook-call-vs-literal-veneer
      structural difference.
  - address: 0x08037e50
    ghidra: thunk_EXT_FUN_2200408c
    name: task_unlock
    size: 8
    sig: "u64 task_unlock(u32 task_id)"
    module: kernel/task_lock
    status: ported
    renames:
      thunk_EXT_FUN_2200408c: task_unlock
      DAT_08037e54: task_unlock_rom_target
    notes: >-
      Raw words prove the full veneer is e51ff004 (ldr pc,[pc,#-4]) at
      0x08037e50 plus target literal 0x2200408c at 0x08037e54; Ghidra's
      reported 4-byte extent drops that literal, and sibling veneer
      0x08037e58 begins immediately after. It tail-dispatches the task id in
      r0 to the IRAM mirror 0x0800408c, whose 32-byte service-3 gateway body
      is already ported as heap/task_unlock_gateway.rs
      task_unlock_gateway: it writes {3, uninitialized_output, task_id},
      dispatches it, then returns the post-dispatch first two words in r0:r1
      as a u64. Every ARM B/BL word in osos.dec was decoded: 10
      unconditional bl callers at 0x080564c8, 0x080564fc, 0x0806450c,
      0x08084c7c, 0x0809c7c8, 0x080c9c78, 0x080e43d4, 0x080e43f4,
      0x08393690, and 0x08393730; zero predicated calls, tail branches, and
      data-word references. Therefore the caller handles any sentinel and
      the veneer has no guard. The port deliberately calls the already ported
      mirror body, not the target-only ldr-pc literal; it exposes only the
      documented r0 task id and r0:r1 return. Host test verifies delegation,
      both result words, zero, and the 0xffffffff id. match.py has the
      expected direct-call-vs-literal-veneer structural difference.

  - address: 0x08037e88
    ghidra: thunk_EXT_FUN_22003d44
    name: rom_task_delay
    size: 8
    sig: "usize rom_task_delay(usize task, usize ticks)"
    module: kernel/task_lock
    status: ported
    renames:
      thunk_EXT_FUN_22003d44: rom_task_delay
      DAT_08037e8c: rom_task_delay_target
    notes: >-
      Raw words are e51ff004 (`ldr pc,[pc,#-4]`) at 0x08037e88 and target
      0x22003d44 at 0x08037e8c; Ghidra's 4-byte extent omits the literal,
      and 0x08037e90 begins the next veneer. This is an ADS literal
      tail-dispatch veneer, not an empty destructor: it forwards task (r0),
      ticks (r1), LR, and the target's r0 return word unchanged to/from the
      RTXC timed task-delay gateway service 20. The relocator at 0x080046e0
      mirrors the target to 0x08003d44, whose 44-byte body is separately
      ported as kernel/task_delay.rs task_delay. Decoding every ARM B/BL
      word in osos.dec finds 20 bl call sites (18 unconditional plus two
      blne at 0x0836b988/0x0836bbbc) and one bne tail at 0x080568f4:
      task_sleep's checked nonzero-tick path; no DATA word holds the thunk
      address. The wrapper uses the established volatile ROM_KERNEL hook
      seam rather than issuing the target-only ldr pc ROM transfer. Host
      tests prove transparent forwarding for (0,1) and (usize::MAX,
      usize::MAX), including the hook result.

  - address: 0x08037e10
    ghidra: thunk_EXT_FUN_220042b4
    name: rom_sem_signal
    size: 8
    sig: "usize rom_sem_signal(usize sem)"
    module: kernel/task_lock
    status: ported
    notes: >-
      Already ported as rom_sem_signal in kernel/task_lock.rs under the
      rom_gateway_wrappers span entry above; this entry records the
      per-function verification. ldr pc,[pc,#-4] (0xe51ff004) at
      0x08037e10 + target word 0x220042b4 at 0x08037e14: Ghidra reports
      4 bytes, the stub is 8 - the sibling veneer to ROM 0x2200418c
      (mailbox_send_gateway_mode1) starts immediately after. A pure ADS literal
      veneer onto the RTXC kernel semaphore signal, r0 = kernel
      semaphore id, the r0 result word passing back untouched, lr never
      stored. The ROM body is recoverable through the boot-relocator
      IRAM mirror (0x2200XXXX == osos 0x0800XXXX, relocator @
      0x080046e0): mirror @ 0x080042b4 (168 bytes) indexes the
      semaphore record table 0x08a1be28 + id*32 under the kernel lock -
      owner mismatch returns 9, a count above 1 decrements and returns
      10, no waiters releases the record (count/owner = 0) and returns
      0, otherwise a waiter is woken through the gateway (frame
      {15, 0, id}) and its result word returned. Call sites
      binary-verified by decoding every ARM B/BL word in osos.dec for
      every condition code: 25 unconditional bl (Ghidra's 25 is correct
      for once; the module header's stale 24 callers fixed), 7
      unconditional tail b - fixed-id shims mov r0, #(18, 5, 1, 0x11,
      3) @ 0x080644e4 / 0x080645a8 (kernel_sem5_signal) / 0x080645b0 /
      0x08064604 (kernel_sem17_signal) / 0x0806cf74, plus
      cache_lock_signal @ 0x082d794c and ata_semaphore_signal @
      0x082d7954 - and 1 predicated bne @ 0x0805671c, the guarded tail
      of sem_signal @ 0x08056710 (cmp r0,#0; ldrne r0,[r0]; cmpne
      r0,#0; bne), the only flag-gated reach: callers that hold a
      semaphore SLOT pointer guard it themselves before tail-branching,
      the veneer itself has no NULL guard. No data word in osos holds
      0x08037e10 - never dispatched virtually. kernel/thunks.rs
      ROM_THUNKS name slot set to Some("sem_signal") on the same
      evidence that named sem_wait (the sem_signal wrapper @ 0x08056710
      tail-branches here once the slot pointer is non-NULL), lifting
      the named-entry count test to 18. Deviation (module design):
      dispatches through the ROM_KERNEL hook (volatile slot read)
      instead of jumping into the mask ROM; match.py structural: an
      indirect call through the hook slot, not the 8-byte ldr pc
      veneer - expected, same family pattern as the other 31 wrappers.

  - address: 0x08037e18
    ghidra: thunk_EXT_FUN_2200418c
    name: mailbox_send_gateway_mode1
    size: 8
    sig: "void mailbox_send_gateway_mode1(u32 mailbox, u32 message, u32 priority, u32 semaphore)"
    module: kernel/mailbox_send_gateway_mode1
    status: ported
    renames:
      thunk_EXT_FUN_2200418c: mailbox_send_gateway_mode1
      DAT_08037e1c: mailbox_send_gateway_mode1_target
    notes: >-
      Raw words at 0x08037e18 are e51ff004 (ldr pc,[pc,#-4]) followed by
      target 0x2200418c at 0x08037e1c, so Ghidra's 4-byte extent omits the
      literal and the true veneer is 8 bytes; the next sibling veneer begins
      at 0x08037e20. The boot relocator at 0x080046e0 mirrors the 64-byte
      target body at osos 0x0800418c..0x080041cc. It writes the sparse
      nine-word RTXC selector-4 record {4, uninitialized, semaphore=r3,
      mailbox=r0, uninitialized, priority=r2, message=r1, 1, 0} and calls
      the 0x08003660 message-dispatch veneer. Selector 4 is KS_send; the
      meanings of trailing mode words {1,0} are not established, so the
      port names only the documented mailbox-send operation and preserves
      those values exactly. Every ARM B/BL word in osos.dec was decoded:
      11 bl callers, all unconditional, at 0x08047ddc, 0x08047e3c,
      0x08047e98, 0x08047f2c, 0x08047f94, 0x08047fec, 0x08048050,
      0x0836b554, 0x083932a8, 0x083932e8, and 0x083933a8; there are no
      predicated calls, tail branches, or data-word references. All callers
      discard the post-dispatch r0, so the Rust ABI is void. Deviation: calls
      the existing installable message_dispatch_veneer seam rather than
      issuing the original PC-relative bl directly to its foreign dispatcher.

  - address: 0x08037e78
    ghidra: thunk_EXT_FUN_220041cc
    name: rom_svc_220041cc
    size: 8
    sig: "usize rom_svc_220041cc(usize object)"
    module: kernel/task_lock
    status: ported
    notes: >-
      Already ported as rom_svc_220041cc in kernel/task_lock.rs under the
      rom_gateway_wrappers span entry above (slot 15 of 32); this entry
      records the per-function verification. ldr pc,[pc,#-4] (0xe51ff004)
      at 0x08037e78 + target word 0x220041cc at 0x08037e7c: Ghidra reports
      4 bytes, the stub is 8 - the sibling veneer to ROM 0x22001cbc opens
      immediately after at 0x08037e80. A real ADS literal veneer, not an
      empty bx lr destructor. The ROM body IS recoverable through the boot
      relocator IRAM mirror (0x2200XXXX == osos 0x0800XXXX, relocator @
      0x080046e0): mirror @ 0x080041cc, 48 bytes, now ported in full as
      kernel/gateway_signal.rs (entry below). Call sites binary-verified by
      decoding every ARM B/BL word in osos.dec for every condition code:
      21 bl - 19 unconditional (0x08060928, 0x08060970, 0x080860d4,
      0x080c9c00, 0x080c9c44, 0x080cb718, 0x080cb738, 0x080cc4d0,
      0x080e43fc, 0x0812f4a0, 0x08165500, 0x08191140, 0x08192c20,
      0x081f49bc, 0x082d964c, 0x08392f70, 0x0839347c, 0x083934b0,
      0x0839385c) plus 2 blne (0x0811f808, 0x08393a1c), correcting the
      stale "16 callers" in the task_lock.rs span survey - and 10 tail
      branches: 8 fixed-id shims `mov r0, #N; b 0x08037e78` with N = 60
      (0x0808cd88), 28 (0x080abf70), 26 (0x082b2c38), 23 (0x082b2c40),
      53/52/51 (0x082dafd0 / 0x082dafd8 / 0x082dafe0) and 8 (0x082e4bcc),
      the bare alias kobj::waiter_wake @ 0x080567f8, and the conditional
      beq @ 0x080567c8 in csem_post @ 0x080567a8. Both predicated bl are
      caller-side sentinel guards, not a property of the stub, which has
      none: 0x0811f808 is `ldr r0,[r0,#0x14]; cmn r0,#1; blne` (skip the
      0xffffffff empty-slot sentinel) and 0x08393a1c is `ldr r0,[r1,#0x10];
      cmp r0,#0; blne` (skip a zero id). No data word in osos holds
      0x08037e78 - never dispatched virtually; the only 0x220041cc word in
      the image is the veneer's own literal at 0x08037e7c.
      kernel/thunks.rs ROM_THUNKS name slot set to Some("signal_object"),
      lifting the named-entry count test to 19. Deviation (module design):
      dispatches through the ROM_KERNEL hook (volatile slot read) instead
      of jumping into IRAM; match.py structural - an indirect call through
      the hook slot, not the 8-byte ldr pc veneer - same family pattern as
      the other 31 wrappers.

  - address: 0x08037ea8
    ghidra: thunk_EXT_FUN_22004368
    name: gateway_wake_object
    size: 8
    sig: "u32 gateway_wake_object(u32 object)"
    module: kernel/gateway_wake
    status: ported
    renames:
      thunk_EXT_FUN_22004368: gateway_wake_object
      DAT_08037eac: gateway_wake_rom_entry
      FUN_08003660: message_dispatch_veneer
    notes: >-
      Raw words prove the full veneer is e51ff004 (ldr pc,[pc,#-4]) at
      0x08037ea8 plus target literal 0x22004368 at 0x08037eac; Ghidra's
      4-byte extent drops the literal, and the sibling veneer begins at
      0x08037eb0. The relocator at 0x080046e0 mirrors this target to
      0x08004368, whose 52-byte body builds {1, 0, object, 0, _}, calls
      the 0x08003660 message-dispatch veneer, and returns the dispatcher
      status word. Every ARM B/BL word in osos.dec was decoded: 11 bl
      callers, 10 unconditional (0x0806a59c, 0x0806b9e8, 0x080860f8,
      0x080c9b98, 0x080cb730, 0x080e4404, 0x08368324, 0x0839346c,
      0x083939a0, 0x083939c0) and one blne at 0x081a595c. That caller
      gates the call with tst r5,#4 and supplies object 18; the stub has
      no guard. Two tails also reach it: bmi at 0x0805699c and the bare
      csem_wake alias at 0x080569a4. The only data word equal to
      0x22004368 is this veneer literal, so it is not virtual. "Wake" is
      call-site evidence from the csem paths; selector 1's RTXC service
      identity remains unproven. The port uses the existing installable
      message_dispatch_veneer seam instead of branching into IRAM; tests
      cover record construction, status propagation, unguarded u32 ids,
      and the thunk-table target.

  - address: 0x080041cc
    ghidra: null
    name: gateway_signal_object
    size: 48
    sig: "u32 gateway_signal_object(u32 object)"
    module: kernel/gateway_signal
    status: ported
    renames:
      FUN_08003660: message_dispatch_veneer
      local_14[0]: gateway_service_selector
      local_14[1]: status
      local_14[2]: object
    notes: >-
      The body behind thunk 0x08037e78 -> ROM 0x220041cc (entry above).
      Absent from Ghidra's functions.csv; extent 0x080041cc..0x080041fc
      taken from the raw words (the next gateway stub, selector 0x12,
      starts at 0x080041fc). Reachable at both addresses because the boot
      relocator @ 0x080046e0 copies 0xaed8 bytes of osos to 0x22000000 and
      the body's only branch is the PC-relative bl 0x08003660, which
      relocates to bl 0x22003660 - the copied veneer whose absolute
      literal 0x0802dca8 lands back in osos. Algorithm - push lr, reserve
      five words (sub sp,#0x14), store r0 at word 2, clear word 1, store
      the RTXC gateway service selector 2 at word 0, pass r0 = sp to the
      0x08003660 dispatch veneer, then reload word 1 as the return value.
      Words 3 and 4 are never written or read; the frame is five words
      only for 8-byte sp alignment across the bl, and MaybeUninit keeps
      them uninitialized (the ks_alloc_timer idiom). The status word is a
      real return value - 20 of the 21 bl sites drop r0, but the 36-byte
      wrapper @ 0x080860c0 does `bl 0x08037e78; movs r0,r0; movne r0,#0x27`,
      so 0 is success and any nonzero status becomes its error 39. Record
      shape confirmed by the family - the service-1 sibling @ 0x08004368
      (thunk 0x08037ea8) is the same stub with word 3 also zeroed, and the
      timed variant @ 0x080043c0 (thunk 0x08037ea0, behind kobj::waiter_wait)
      writes {1, status, id, timeout, sp}. Naming - selector 2 and the
      record layout are binary facts; the "signal" reading is call-site
      evidence already recorded in this file, namely csem_post @ 0x080567a8
      reaching the veneer exactly when a counting semaphore's count rises
      from -1 to 0 (one sleeper parked) and kobj::waiter_wake @ 0x080567f8
      being a bare alias of it. The RTXC service table is not in osos, so
      the kernel primitive behind selector 2 is NOT proven. Deviations -
      the foreign dispatcher is reached through the ported
      runtime::message_dispatch_veneer and its installable seam; match.py
      structural (LLVM keeps a frame pointer and a 24-byte frame, writing
      the same {2, 0, object} triple at sp+4/8/12 and reloading the status
      from sp+8 - identical shape, different frame offsets).

  - address: 0x0806a4a0
    ghidra: FUN_0806a4a0
    name: kernel_sem5_wait
    size: 8
    sig: "usize kernel_sem5_wait()"
    module: kernel/task_lock
    status: ported
    notes: >-
      Fixed-id semaphore-acquire shim: `mov r0, #5; b 0x08037e08`
      (rom_sem_wait veneer -> ROM 0x22003fd0), the r0 result word passing
      back through the tail branch. Extent verified against the sibling
      shims that follow immediately: 0x0806a4a8 (`mov r0, #1`) and
      0x0806a4b0 (`mov r0, #0x11`, the PMU OUTER lock), both separate
      8-byte functions with the same branch target, both unported. 37 bl
      call sites (binary-verified, all unconditional; zero predicated bl
      and zero b — callers never flag-gate the acquire): 28 in the PMU
      I2C family 0x082e53xx-0x082e5bxx, where semaphore 5 is the INNER
      lock of the wait(0x11)/wait(5) bracket around every raw transfer
      (see drivers/i2c pmu_i2c_read_regs), plus 0x080671ac, 0x08067f90,
      0x080aa06c, 0x080b28a0, 0x080da17c, 0x080da658, 0x0836d420,
      0x0836e380, 0x0836e3dc. Deviation: dispatches through the ported
      rom_sem_wait (the ROM_KERNEL hook) instead of branching to the ROM
      veneer — same family pattern as the other task_lock wrappers, so
      match.py shows an expected structural diff (indirect call through
      hook slot 1, `mov r0, #5` intact).

  - address: 0x0806a4b0
    ghidra: FUN_0806a4b0
    name: kernel_sem17_wait
    size: 8
    sig: "usize kernel_sem17_wait()"
    module: kernel/task_lock
    status: ported
    notes: >-
      Fixed-id semaphore-acquire shim: `mov r0, #0x11; b 0x08037e08`
      (rom_sem_wait veneer -> ROM 0x22003fd0), the r0 result word passing
      back through the tail branch. Semaphore 0x11 is PMU_I2C_OUTER_SEM,
      the OUTER lock of the wait(0x11)/wait(5) bracket around every raw
      PMU transfer (kernel_sem5_wait @ 0x0806a4a0 is the INNER lock; see
      drivers/i2c). Extent verified: the next function's
      `ldr r1, [pc, #156]` prologue starts at 0x0806a4b8; sibling shims
      0x0806a498 (`mov r0, #18`, unported), 0x0806a4a0 (ported) and
      0x0806a4a8 (`mov r0, #1`, unported) share the same branch target as
      separate 8-byte functions. No data word in osos references
      0x0806a4b0 - the shim is never dispatched virtually. 31 bl call
      sites (binary-verified, all unconditional; zero predicated bl and
      zero b - callers never flag-gate the acquire): 28 in the PMU I2C
      family 0x082e53dc..0x082e5bf8, plus 0x080671a8, 0x08067f8c,
      0x0836d41c. Deviation: dispatches through the ported rom_sem_wait
      (the ROM_KERNEL hook) instead of branching to the ROM veneer - same
      family pattern as kernel_sem5_wait, so match.py shows an expected
      structural diff (indirect call through hook slot 1,
      `mov r0, #0x11` intact).

  - address: 0x08064604
    ghidra: FUN_08064604
    name: kernel_sem17_signal
    size: 8
    sig: "usize kernel_sem17_signal()"
    module: kernel/task_lock
    status: ported
    notes: >-
      Fixed-id semaphore-release shim: `mov r0, #0x11; b 0x08037e10`
      (rom_sem_signal veneer -> ROM 0x220042b4), the r0 result word
      passing back through the tail branch. Semaphore 0x11 is
      PMU_I2C_OUTER_SEM, the OUTER lock of the wait(0x11)/wait(5)
      bracket around every raw PMU transfer; this shim is its release
      half, mirror of kernel_sem17_wait @ 0x0806a4b0 (see drivers/i2c).
      Extent verified: Ghidra's 8 bytes are correct - the next
      function's `push {r4, lr}` prologue starts at 0x0806460c. No data
      word in osos references 0x08064604 - the shim is never dispatched
      virtually. 31 call sites (binary-verified against osos.dec;
      Ghidra's 25 counts only the bl forms): 25 unconditional bl - 22 in
      the PMU I2C family 0x082e53f8..0x082e5c14 plus 0x080671bc,
      0x08068034, 0x0836d438 - and 6 unconditional tail b (error-exit
      releases inside the same family at 0x082e54b0, 0x082e54fc,
      0x082e5548, 0x082e57d0, 0x082e5a1c, 0x082e5b60); zero predicated
      forms, so callers never flag-gate the release. Deviation:
      dispatches through the ported rom_sem_signal (the ROM_KERNEL hook)
      instead of branching to the ROM veneer - same family pattern as
      kernel_sem5_signal, so match.py shows an expected structural diff
      (indirect call through hook slot 2, `mov r0, #0x11` intact).

  - address: 0x0806aa3c
    ghidra: FUN_0806aa3c
    name: ui_element_is_tdat_class
    size: 40
    sig: "u32 ui_element_is_tdat_class(const u8 *target)"
    module: ui/tdat_class_check
    status: ported
    notes: >-
      NULL-guarded 'tdat' UI-element class-tag predicate: returns 1 iff
      target is non-NULL and the word at target+0x4 equals the tag
      0x74646174 ('tdat'), held in the literal pool at 0x0806aa60.
      Ghidra's 36-byte extent drops that literal word; the true extent
      is 40 (the next function's push {r4, lr} prologue starts at
      0x0806aa64). Call count verified by decoding every B/BL word in
      osos.dec: 31 unconditional bl, zero predicated - the internal
      NULL guard is what all callers rely on. The tag is stamped at
      object+0x4 by the 'tdat' factory 0x08047bfc, which allocates
      0x948 bytes and stores the tag (literal word @ 0x08047cbc) via
      stmib r4, {r0, r6}. Callers gate real field access on the check:
      sibling wrapper 0x0806aa64 reads a halfword at +0x40, 0x0806aaac
      bumps the counter at +0x40, 0x0806a994 tears down lists at
      +0x54/+0x8. Byte-shape-identical sibling 0x080613e0
      (ui_element_is_plst_class) checks the 'plst' tag 0x706c7374
      instead; this port's own link_section keeps the two hook seams
      unfolded (both symbols verified present in librustypod.a).
      match.py: crate fp frame plus LLVM clz equality lowering vs ADS
      cmp/moveq; the core cmp r0,#0 / beq / ldr r0,[r0,#4] / ldr r1,[pc]
      sequence matches verbatim.

  - address: 0x0805c220
    ghidra: FUN_0805c220
    name: tdat_element_addref_counters
    size: 180
    sig: "void tdat_element_addref_counters(u8 *element)"
    module: ui/tdat_counter_addrefs
    status: ported
    renames:
      param_1/r0: element
      FUN_0806aa3c: ui_element_is_tdat_class
      FUN_0808e16c: tagged_counter_try_increment
    notes: >-
      Raw ARM extent is exactly 0x0805c220..0x0805c2d0; the next separately
      entered function begins at 0x0805c2d4. It first invokes the NULL-safe
      ported 'tdat' class predicate and immediately returns if it fails.
      On success it calls the ported tagged_counter_try_increment on the
      element's counters at +0xc0, +0x118, +0x170, +0x1c8, +0x220, +0x278,
      +0x2d0, +0x328, +0x380, +0x3d8, +0x7a0, +0x7f8, +0x8a8, and +0x850,
      then loops ten times over +0x430 + index*0x58. Every increment result
      is ignored, so invalid tagged counters remain unchanged independently.
      Decoding every ARM B/BL-immediate word in osos.dec verifies 11 inbound
      plain bl calls (0x080461b8, 0x08046e80, 0x08052b54, 0x08055b2c,
      0x0805cd70, 0x0808677c, 0x0808d148, 0x0809dc54, 0x0809dfe0,
      0x0809efc0, 0x080aa4d0), zero predicated forms, and no tail b callers.
      The Rust port directly invokes the already-ported predicate and counter
      increment rather than branching into stock code; otherwise no deliberate
      deviations. match.py structural review shows Rust's direct calls rather
      than retail bl targets and LLVM unrolling the fixed ten-counter group;
      both retain the same 24 attempted increments and ignored results.

  - address: 0x0806c0b8
    ghidra: FUN_0806c0b8
    name: tdat_element_release_counters
    size: 176
    sig: "void tdat_element_release_counters(u8 *element)"
    module: ui/tdat_counter_releases
    status: ported
    renames:
      param_1/r0: element
      FUN_0806aa3c: ui_element_is_tdat_class
      FUN_0809f744: tagged_counter_try_decrement
    notes: >-
      Raw ARM extent is exactly 0x0806c0b8..0x0806c168; the next separately
      entered function begins at 0x0806c16c, so Ghidra's 180-byte extent is
      four bytes too long. It first invokes the NULL-safe ported 'tdat' class
      predicate and immediately returns if it fails. On success it calls the
      ported tagged_counter_try_decrement on the element's counters at +0xc0,
      +0x118, +0x170, +0x1c8, +0x220, +0x278, +0x2d0, +0x328, +0x380, +0x3d8,
      +0x7a0, +0x7f8, +0x8a8, and +0x850, then loops ten times over +0x430 +
      index*0x58. Every decrement result is ignored, so invalid, zero, and
      negative tagged counters remain unchanged independently. Decoding every
      ARM B/BL-immediate word in osos.dec verifies 9 inbound plain bl calls
      (0x0804622c, 0x08055b54, 0x0805cdc8, 0x08086854, 0x0808d580,
      0x0809ddd4, 0x0809e154, 0x0809efdc, and 0x080aa5b0), zero predicated
      forms, and no tail b callers. The raw body contains fifteen static bl
      instructions: one predicate, fourteen fixed decrements, and one loop
      decrement call executed ten times. The Rust port directly invokes the
      already-ported predicate and counter decrement rather than branching into
      stock code; otherwise no deliberate deviations.

  - address: 0x080522d4
    ghidra: FUN_080522d4
    name: ui_tdat_first_plst
    size: 24
    sig: "u32 ui_tdat_first_plst(const u8 *element)"
    module: ui/tdat_first_plst
    status: ported
    renames:
      param_1/r0: element
      param_1_plus_0x34: first_plst
      FUN_0806aa3c: ui_element_is_tdat_class
    notes: >-
      Raw ARM is mov r2, r0; push {lr}; bl 0x0806aa3c; movs r0, r0;
      ldrne r0, [r2, #0x34]; pop {pc}. The 24-byte Ghidra extent is exact:
      the adjacent function begins with cmp r0, #0 at 0x080522ec. Returns
      the word at element+0x34 only when the already-ported 'tdat' class
      predicate succeeds; otherwise returns zero. Caller 0x08050ae0 starts
      its linked 'plst' sequence from this word and passes every result to
      the 'plst' successor selector 0x08053bd0; callers 0x0805473c and
      0x080561cc read the selected object's flag byte at +0x1ac. Call count
      verified by decoding every B/BL word in osos.dec: 17 unconditional bl,
      zero predicated. The Rust port directly calls ui_element_is_tdat_class
      rather than branching to stock 0x0806aa3c; aligned word load and
      zero-on-rejected-class behavior are unchanged.

  - address: 0x080645a8
    ghidra: FUN_080645a8
    name: kernel_sem5_signal
    size: 8
    sig: "usize kernel_sem5_signal()"
    status: ported
    notes: >-
      Fixed-id semaphore-release shim: `mov r0, #5; b 0x08037e10`
      (rom_sem_signal veneer -> ROM 0x220042b4), the r0 result word
      passing back through the tail branch. Extent verified: preceding
      function's `pop {.., pc}` at 0x080645a4, sibling shim 0x080645b0
      (`mov r0, #1`, same branch target, unported) starts immediately
      after; no data word in osos references the address (never
      dispatched virtually). 37 bl call sites (binary-verified against
      osos.dec, all unconditional; zero predicated bl and zero b —
      callers never flag-gate the release): 28 in the PMU I2C family
      0x082e53xx-0x082e5cxx, the inner-lock release of the
      wait(0x11)/wait(5) bracket around every raw transfer (mirror of
      kernel_sem5_wait, see drivers/i2c pmu_i2c_read_regs), plus
      0x080671b8, 0x08068030, 0x080aa090, 0x080b28b4, 0x080da190,
      0x080da66c, 0x0836d434, 0x0836e3b0, 0x0836e3f4. Ghidra's asm
      listing shows only 35 of them (misses 0x082e5a14 and one more).
      Deviation: dispatches through the ported rom_sem_signal (the
      ROM_KERNEL hook) instead of branching to the ROM veneer — same
      family pattern as kernel_sem5_wait, so match.py shows an expected
      structural diff (indirect call through hook slot 2, `mov r0, #5`
      intact).

  - address: 0x08047f40
    ghidra: FUN_08047f40
    name: gateway_request_timed
    size: 104
    sig: "void gateway_request_timed(void *payload, u32 timeout)"
    module: kernel/gateway_request
    status: ported
    vars: {param_1: payload, param_2: timeout}
    notes: >-
      Tag-2 member of the 4-helper request family @ 0x08047edc..0x0804805c
      (tags 3/2/4/0). Builds a 9-word stack frame {0,0,0,0, tag 2, payload,
      timeout+1, pad, 0} and sends it through the ported mode-1 mailbox-send
      gateway body (thunk 0x08037e18 -> mirror 0x0800418c) as
      (mailbox = 1, message = &frame, priority = 5, semaphore = 6),
      bracketed by kernel semaphore 9 (rom_sem_wait/rom_sem_signal). 69 bl
      call sites; timeout is 0x3e8 at 53 of them (else 0xfa/0x1f4/
      0x1f40/0/computed), payload an object pointer or small id. Deviations:
      semaphore calls use task_lock::ROM_KERNEL (family pattern), while the
      ported send body preserves r3 = 6 in its sparse RTXC record through the
      existing message_dispatch_veneer seam; the pad word is zeroed rather
      than left uninitialized.

  - address: 0x08048000
    ghidra: FUN_08048000
    name: gateway_request_blocking
    size: 100
    sig: "void gateway_request_blocking(void *payload, u32 flag)"
    module: kernel/gateway_request_blocking
    status: ported
    vars: {param_1: payload, param_2: flag}
    notes: >-
      Tag-0 member of the 4-helper request family @ 0x08047edc..0x0804805c
      (tags 3/2/4/0), sibling of gateway_request_timed (0x08047f40). Calls
      the gateway-ready wait FUN_080c8304 first, then builds a 9-word stack
      frame {0,0,0,0, tag 0, payload, pad, pad, flag} (payload at sp+0x14,
      tag byte at sp+0x10, flag at sp+0x20) and sends it through the ported
      mode-1 mailbox-send gateway body (thunk 0x08037e18 -> mirror
      0x0800418c) as (mailbox = 1, message = &frame, priority = 5,
      semaphore = 6), bracketed by kernel semaphore 9 (rom_sem_wait/
      rom_sem_signal). 64 bl call sites: payload (r0) is a small id
      (0x1/0x8/0x10/0x23/0x30/0x33/0x37 observed) or an object pointer, flag
      (r1) is immediate 0/1 at every immediate site. Deviations: the ready
      wait rides the GATEWAY_READY_WAIT dispatch slot (documented spin
      default) since FUN_080c8304's own callees are unported; semaphore calls
      dispatch through task_lock::ROM_KERNEL, while the ported mailbox-send
      body preserves r3 = 6 through the existing message_dispatch_veneer
      seam; pad words 6/7 zeroed rather than left uninitialized.

  - address: 0x080c8304
    ghidra: FUN_080c8304
    name: gateway_wait_ready
    size: 36
    sig: "void gateway_wait_ready(void)"
    module: kernel/gateway_request_blocking
    status: ported
    notes: >-
      Gateway-ready spin-wait called at the head of FUN_08048000 and the
      helper @ 0x08048064 (6 bl call sites total): entry b polls first,
      then loops { sleep one tick via thunk 0x080e9eb0; call FUN_081a5500
      (the lazily-created gateway state object); ldrb the object's byte at
      +0x6a; repeat until it reads exactly 1 }. Deviations: the state
      getter FUN_081a5500 is unported (allocator-backed lazy object init),
      so it dispatches through the new GATEWAY_STATE slot whose default
      hands back a static 0x6b-byte never-ready object (loud-spin
      missing-stub philosophy, matching GATEWAY_READY_WAIT's default); the
      sleep callee is the ported task_sleep (kernel/task) wired directly -
      thunk 0x080e9eb0 is a bare `b 0x080568e8` alias - with the result
      discarded like the original; the ready byte is read volatile. The
      callers in this module still dispatch through the pre-existing
      GATEWAY_READY_WAIT slot (documented spin default kept; install
      gateway_wait_ready to wire the real wait - the
      observable_set_observer dispatch-boundary precedent). Host tests:
      one-poll return when already ready, getter re-polled every iteration
      until the +0x6a byte reads 1, default stub stable and never-ready.
      match.py structural: same poll/ldrb-+0x6a/cmp-1/loop shape; the diff
      is the crate fp frame, the blx through GATEWAY_STATE standing in for
      bl 0x081a5500, and LLVM inlining task_sleep's TASK_HOOKS load with
      the ticks==0 cmp folded away (ticks is the constant 1) - the
      documented deviations.

  - address: 0x080567fc
    ghidra: null
    name: rom_svc_22001cbc_alias_67fc
    size: 4
    sig: "usize rom_svc_22001cbc_alias_67fc(usize a0)"
    module: kernel/task_lock
    status: ported
    notes: >-
      ALIAS - bare `b 0x08037e80` tail veneer onto the rom_svc_22001cbc
      thunk (ROM 0x22001cbc). Absent from functions.csv; extent verified
      from osos.asm (1 instruction; the next word @ 0x08056800 opens a
      new frame). 2 call sites: bl @ 0x08081968, bne @ 0x0809ece8. No
      separate port exists; hook this address to the rom_svc_22001cbc
      symbol (the container_is_empty alias precedent).

  - address: 0x080769b8
    ghidra: FUN_080769b8
    name: os_malloc
    size: 8
    sig: "void *os_malloc(u32 size)"
    module: kernel/os_heap
    status: ported
    notes: >-
      Pure tail veneer `mov r1,#0; b 0x080eb67c` (malloc_wrapper) — caller
      tag 0 = kernel-service allocations. 4 call sites, all in the
      kernel-object layer: sem_create 0x08056724, mailbox create 0x0805675c,
      object create 0x080563a0 (x2). Ghidra's C is misleading (it inlines
      the malloc_wrapper chain). match.py 0/24 by design — LLVM inlines the
      ported malloc_wrapper body instead of tail-branching; behaviorally
      identical.

  - address: 0x080f151c
    ghidra: FUN_080f151c
    name: os_free
    size: 8
    sig: "void os_free(void *ptr)"
    module: kernel/os_heap
    status: ported
    notes: >-
      Pure tail veneer `mov r1,#0; b 0x080e7970` (free_wrapper), tag 0 =
      kernel-service objects. Reached from sem_delete 0x0805646c (bne
      tail), kernel-object delete 0x080564b0 (bl), queue-node free
      0x080b4c4c (bl + tail b). Ghidra's FUN_080f151c C is misleading — it
      inlines the whole free_wrapper/heap_free chain. match.py 0/24 by
      design (LLVM inlines the ported free_wrapper body).

  - address: 0x080bead8
    ghidra: FUN_080bead8
    name: heap_mutex_creating
    size: 24
    sig: "u32 heap_mutex_creating(void)"
    module: kernel/os_heap
    status: ported
    notes: >-
      bl 0x08077250 (lazy_init_default_heap) then ldrb from
      *0x089ca638 + 0xb5 — mutex_state2 of the default-heap descriptor,
      heap_lock's (0x0819d6cc) "RTXC mutex creation in progress" byte.
      sem_create's recursion guard (static cell instead of heap alloc).
      NAMING CORRECTION vs earlier notes — this is NOT an ISR-context or
      early-boot check; the byte is written only by heap_lock around
      mutex_create. match.py 0/20 by design (LLVM inlines the ported
      lazy_init_default_heap; the tail ldrb pair is identical).

  # --- Batch 10: ADS runtime region 0x08030000-0x08036fff gap sweep ---
  - address: 0x08036e80
    ghidra: FUN_08036e80
    name: frexp
    size: 144
    sig: "f64 frexp(f64 x, i32 *exp)  // soft-float bits"
    module: fp/d2f_checked
    status: ported
    notes: >-
      Classic frexp (mantissa to [0.5,1), exponent field forced 0x3fe).
      Zero/Inf/NaN return unchanged with *exp = 0. Subnormals pre-scale
      via __dscalb(x, 54) with *exp biased -54 — retailOS __dscalb
      flushes subnormals to +0, so subnormal input deterministically
      yields (+0.5, -1076); ported bug-for-bug (path dead in practice).
      Sole caller: 0x08036d7c.

  - address: 0x08036d7c
    ghidra: FUN_08036d7c
    name: d2f_errno
    size: 204
    sig: "f32 d2f_errno(f64 x)  // soft-float bits"
    module: fp/d2f_checked
    status: ported
    notes: >-
      Checked double->float for scanf %f: frexp exponent + 126 selects
      the path. 1..254 = plain __d2f (whose own rounding may still
      overflow to Inf or flush to +0 WITHOUT errno — faithful quirk);
      <= 0 and x != 0 = underflow (+0, ERANGE, __ieee_status 0x808
      save/restore mirrored); >= 255 = overflow (ERANGE, +-Inf by
      __dcmpgt sign). Inf/NaN report exp 0, so they take the in-range
      __d2f path (canonical qNaN, no errno).

  - address: 0x08036e4c
    ghidra: FUN_08036e4c
    name: scanf_narrow_float
    size: 32
    sig: "void scanf_narrow_float(u32 *out_float, const u32 *in_double)"
    module: fp/d2f_checked
    status: ported
    notes: >-
      scanf %f float-store veneer: loads the double lo-word-first, calls
      d2f_errno, stores the float word. Now the SCANF_FLOAT_NARROW
      default in scanf/scanf_float_engine.rs (was a spin stub).

  - address: 0x0803571c
    ghidra: FUN_0803571c
    name: os_heap_grow_arena
    size: 108
    sig: "usize os_heap_grow_arena(usize min_size, usize *old_top_out)"
    module: runtime/malloc_rt
    status: ported
    notes: >-
      The sbrk-like ADS alloc-arena extension (HEAP_OPS.grow contract).
      Grows the break at libspace+0x14 toward the live sp, bounded by
      sp - reserve (reserve at libspace+0x1c); grant = min(required +
      0x1007 & ~7, 33-bit-average(limit, required) & ~7). Writes the
      pre-grow break to *old_top_out on BOTH paths; returns growth or 0.
      DISCOVERY: libspace+0x1c is a stack-guard distance below sp, not
      an absolute arena bound.

  - address: 0x080336c0
    name: heap_grow_arena_wrapper
    size: 12
    module: runtime/malloc_rt
    status: ported
    notes: push/pop call veneer around 0x0803571c (how __rt_heap_init
      reaches the arena extension). LLVM aliases the two symbols (ICF);
      match.py must be run against heap_grow_arena_wrapper.

  - address: 0x0803170c
    ghidra: FUN_0803170c
    name: cxx_vec_delete
    size: 16
    sig: "void cxx_vec_delete(void *array, void (*dtor)(void *))"
    module: heap/veneers
    status: ported
    notes: >-
      ADS C++ delete[] for arrays with destructors (4 call sites:
      0x08267070/0x082a7164/0x082a8bec/0x083b6c10). Cookie at array-8 =
      {elem_size, count}; runs __cpp_finalise (ported, runtime/atexit)
      last-element-first, then tag-3 operator delete on the cookie start.
      The cookie walk goes through cpp_finalise_null_guard @ 0x082ab254
      (own exported port in the same module).

  - address: 0x082ab254
    name: cpp_finalise_null_guard
    size: 16
    sig: "void *cpp_finalise_null_guard(void *array, void (*dtor)(void *))"
    module: heap/veneers
    status: ported
    notes: >-
      cmp r0,#0; ldmdbne r0,{r2,r3}; bne __cpp_finalise(0x080336d8);
      mov pc,lr — 16 bytes (size was recorded as 12; the mov pc,lr @
      0x082ab260 belongs to it). Null-guarded cookie-loading front-end of
      __cpp_finalise; returns array-8 (block start) or NULL for a NULL
      array. Was inlined in the first cxx_vec_delete port; now extracted
      as its own exported function which cxx_vec_delete calls.

  - address: 0x08036d60
    ghidra: FUN_08036d60
    name: __rt_fp_status_addr
    size: 16
    sig: "u32 *__rt_fp_status_addr()"
    module: runtime/errno
    status: ported
    notes: >-
      bl __rt_libspace; add r0,#4. DISCOVERY: libspace+0x04 is the ADS
      soft-float status word (Libspace.fp_status now named). Sole caller:
      float exception path @ 0x083ecb94 in the fplib gap
      0x083ecb3c-0x083ecba4 (unported).

  # --- Batch 10 scouting: recorded-but-previously-unlisted ports ---
  - address: 0x08036f30
    ghidra: FUN_08036f30
    name: double_to_ext
    size: 244
    module: printf/printf_float_dtoa
    status: ported
    notes: Double -> 12-byte extended-fp unpack for dtoa; was ported in
      printf_float_dtoa.rs but never listed here.

  - address: 0x080327e8
    ghidra: FUN_080327e8
    name: format_exponent
    size: 116
    module: printf/printf_float
    status: ported
    notes: "%e/%g exponent-suffix emitter; ported in printf_float.rs but
      never listed here. Sole caller: fp_format."

  - address: 0x08035994
    ghidra: FUN_08035994
    name: classify_double
    size: 64
    module: printf/printf_float
    status: ported
    notes: Sign extraction + NaN(1)/Inf(2)/finite(0) classification for
      fp_format, wrapping the _fpclassify primitive @ 0x08036d0c; folded
      into printf_float.rs's classify_double.

  - address: 0x08036d0c
    ghidra: FUN_08036d0c
    name: fpclassify_core
    size: 56
    module: printf/printf_float
    status: ported
    notes: ADS _fpclassify primitive (classes 3=NaN, 5=zero, 7=Inf,
      4/6=finite); folded into classify_double in printf_float.rs.

  - address: 0x080359d4
    ghidra: FUN_080359d4
    name: dtoa_split_double
    size: 116
    module: printf/printf_float_dtoa
    status: ported
    notes: Exponent-field extract + denormal renormalize (__dscalb by
      2^64) shared by dtoa and scanf_float_convert; folded into the
      printf_float_dtoa.rs engine.

  - address: 0x08036d70
    ghidra: FUN_08036d70
    name: store_double
    size: 12
    module: scanf/scanf_float
    status: ported
    notes: str hi,[r0,#4]; str lo,[r0]; result-store helper folded into
      scanf_float.rs.

  # --- Semihosting SWI family (ported: stdio/semihost) ---
  # ADS semihosting wrappers, all `swi 0x123456` with an operation code —
  # dead on device (no debugger; retailOS never traps them usefully).
  # Ports issue the SWI through the SEMIHOST_SWI dispatch hook (host tests
  # mock it; the firmware default keeps the original `swi 0x123456`).
  - address: 0x08031f84
    name: _sys_open
    size: 32
    sig: "i32 _sys_open(const u8 *name, i32 mode)"
    module: stdio/semihost
    status: ported
    notes: >-
      Semihost SYS_OPEN (op 1); block [name, mode, strlen(name)] with the
      length measured by the retailOS unguarded strlen @ 0x08392478
      (inlined as strlen_raw in the port). Returns the handle or -1.

  - address: 0x08031fa4
    name: _sys_close
    size: 24
    sig: "i32 _sys_close(i32 handle)"
    module: stdio/semihost
    status: ported
    notes: Semihost SYS_CLOSE (op 2); block [handle].

  - address: 0x08031fbc
    name: _sys_write
    size: 32
    sig: "i32 _sys_write(i32 handle, const u8 *buf, u32 len)"
    module: stdio/semihost
    status: ported
    notes: >-
      Semihost SYS_WRITE (op 5); block [handle, buf, len]. Returns the
      number of bytes NOT written (0 = success).

  - address: 0x08031fdc
    name: _sys_read
    size: 32
    sig: "i32 _sys_read(i32 handle, u8 *buf, u32 len, u32 mode)"
    module: stdio/semihost
    status: ported
    notes: >-
      Semihost SYS_READ (op 6); block [handle, buf, len, mode] — the stdio
      refill (0x08034f88) passes the stream flag word as mode. Returns
      bytes NOT read; bit 31 of the result signals EOF.

  - address: 0x08031ffc
    name: _sys_istty
    size: 24
    sig: "i32 _sys_istty(i32 handle)"
    module: stdio/semihost
    status: ported
    notes: Semihost SYS_ISTTY (op 9); block [handle].

  - address: 0x08032014
    name: _sys_seek
    size: 24
    sig: "i32 _sys_seek(i32 handle, i32 pos)"
    module: stdio/semihost
    status: ported
    notes: >-
      Semihost SYS_SEEK (op 0xa); block [handle, pos] (absolute).
      Negative result = failure (checked by stdio_writeback).

  - address: 0x08032034
    name: _sys_flen
    size: 24
    sig: "i32 _sys_flen(i32 handle)"
    module: stdio/semihost
    status: ported
    notes: Semihost SYS_FLEN (op 0xc); block [handle].

  - address: 0x08036d48
    name: _sys_writec
    size: 24
    sig: "i32 _sys_writec(i32 ch)"
    module: stdio/semihost
    status: ported
    notes: >-
      Semihost SYS_WRITEC (op 3); r1 points AT the character (the arg is
      parked in a stack word, little-endian first byte). Callers in the
      debug-print helper region 0x08036bb4-0x08036d08 (not in
      functions.csv).

  - address: 0x0803202c
    name: sys_stub_ret0
    size: 8
    sig: "i32 sys_stub_ret0()"
    module: stdio/semihost
    status: ported
    notes: mov r0,#0; ret. Weak semihost stub (tmpnam/ensure family).

  - address: 0x080320a0
    name: sys_stub_ret0_2
    size: 8
    sig: "i32 sys_stub_ret0_2()"
    module: stdio/semihost
    status: ported
    notes: >-
      mov r0,#0; ret. Second return-0 stub, several callers. Identical
      body to sys_stub_ret0 — objdump shows the two ARM symbols at one
      address (match.py finds it under sys_stub_ret0's label).

  # --- retailOS diagnostic printf (stdio/debug_printf) ---
  - address: 0x082bd4a8
    ghidra: FUN_082bd4a8
    name: debug_printf
    size: 28
    sig: "int debug_printf(const char *fmt, ...)"
    module: stdio/debug_printf
    vars: {param_1: fmt}
    status: ported
    renames:
      param_1/r0: fmt
      FUN_08396e1c: debug_vprintf (DEBUG_VPRINTF seam, not yet ported)
    notes: >-
      The varargs front end of retailOS's semihosting trace printf.
      Seven instructions, extent 0x082bd4a8..0x082bd4c4 confirmed from
      the raw words (0x082bd4c4 starts the next function with its own
      push), no literal pool. push {r0,r1,r2,r3}; push {r4,lr};
      ldr r0,[sp,#8]; add r1,sp,#12; bl 0x08396e1c; pop {r4};
      ldr pc,[sp],#20 - the textbook AAPCS variadic prologue (spill
      r0-r3 into a frame contiguous with the caller's stacked
      arguments, point a va_list just past the fixed parameter, call
      the v-flavored worker, then drop lr + the four spill words in one
      post-indexed load). In C: `int debug_printf(const char *fmt, ...)
      { return debug_vprintf(fmt, ap); }`.
      48 call sites, binary-scanned by decoding every B/BL word in
      osos.dec: 36 bl, 9 blne, 1 bleq, 1 tail b, 1 tail beq. No DATA
      word holds the address, so it is never dispatched virtually. The
      ten predicated sites are callers gating a trace on a status
      BITMASK (e.g. tst r4,#0x400000; addne r0,pc,#0x50; blne @
      0x08087a88), NOT on a NULL check - this entry point has no guard
      on the format at all. 43 of the 48 load the format with
      `add r0,pc,#imm`, i.e. inline PC-relative string literals:
      "mDS: write data CRC response error\n" @ 0x08087adc (storage
      driver), "cI: card failed HS_TIMING SWITCH\n" @ 0x08074e14 (card
      driver).
      The worker FUN_08396e1c @ 0x08396e1c (380 bytes,
      0x08396e1c..0x08396f98 plus a 4-byte digit-table literal;
      0x08396f9c starts the next function) is a hand-rolled mini-printf
      writing straight to the ARM Angel semihosting console - unrelated
      to the ADS printf family in printf/printf_api.rs, and
      debug_printf is its ONLY caller (1 bl, binary-verified). It scans
      for % and backslash, flushing literal runs through FUN_0808e2b8
      (parks a NUL over the run's last byte, semihost SYS_WRITE0 op 4,
      then SYS_WRITEC op 3 via 0x080769a0 for the saved byte, then
      restores it). Conversions: optional 0-prefixed decimal width,
      ignored `l`, then d (emit '-', negate, fall into the unsigned
      printer 0x080944d8), u, x/X (8-digit zero-padded hex built
      backwards in the spill frame, flushed with SYS_WRITE0), c
      (SYS_WRITEC - except at width 4, where it dispatches 0x0807a344,
      the FourCC tag printer, so %4c spells a tag out), s (SYS_WRITE0
      on the argument), %% literal, and any other letter degrades to a
      single space. Always returns 0.
      Deviations - the `...` becomes an explicit args: VaList
      (*const u32), the house convention already used by
      printf/printf_api.rs's sprintf/snprintf and cxx/string_object.rs;
      that IS what the original builds, since the spill frame exists
      only to manufacture this pointer, and a variadic C caller needs
      the capture trampoline printf_api.rs documents. The worker rides
      the DEBUG_VPRINTF slot (firmware-address default on target,
      panicking default on host). Host tests (4) cover verbatim
      forwarding of both arguments, propagation of a non-zero result
      (the port does not synthesize the stock 0), the unguarded NULL
      format, and the slot being re-read per call. match.py (--size
      28): same instruction count (7); with the va_list explicit the
      spill frame is gone, so the port is a tail dispatch through the
      seam while r0/r1 already hold what the original had to reload.

  - address: 0x082dcf7c
    ghidra: FUN_082dcf7c
    name: trace_printf
    size: 48
    sig: "int trace_printf(const char *fmt, ...)"
    module: stdio/trace_printf
    status: ported
    vars: {param_1: fmt}
    renames:
      param_1/r0: fmt
      DAT_082dcfa8 (0x08a1081c): TRACE_BUF, the static line buffer
      FUN_082cecfc: trace_format (TRACE_FORMAT seam, not yet ported)
      uStack_c/uStack_8/uStack_4: the spilled r1-r3 va_list frame
    notes: >-
      The trace-LINE printer: renders one diagnostic row into a fixed
      static buffer and emits it with a single Angel SYS_WRITE0. Sibling
      of debug_printf @ 0x082bd4a8 but a different channel - that one
      streams through mini-printf FUN_08396e1c; this one formats via the
      row interpreter FUN_082cecfc @ 0x082cecfc (formats like
      "(H'Clock rate is',I5,H' Hz, Tick interval is',I4,H' ms, ',N)"
      @ 0x082bed98) into buffer 0x08a1081c, then swi 0x123456 with r0=4
      (SYS_WRITE0) and r1 = buffer base reloaded from the literal -
      no parameter block, r1 IS the string.
      Extent verified from raw words: Ghidra says 44 bytes but the true
      size is 48 - both ldr r?,= share ONE literal word at 0x082dcfa8,
      which Ghidra dropped the trailing copy of; 0x082dcfac starts the
      next function with its own cmp r0,#0. Call count verified by
      decoding every B/BL word in osos.dec: 44 sites, 34 plain bl plus
      10 predicated blne, no tail b/bcc forms, and no DATA word holds
      the address (never dispatched virtually). The blne sites gate on
      VALUE comparisons in their own bodies (cmp r0,r1 then addeq
      r0,pc,#imm; bleq @ 0x082bfd88), not on a NULL check - like its
      sibling this entry point has no guard on the format at all.
      Behaviour facts proven in the raw asm: the formatter's 'N'
      conversion stores the string terminator itself (cmp r1,#0x4e;
      moveq r0,#0; strbeq r0,[r5],#1 @ 0x082cee00), so a well-formed
      row ends in N and the buffer is a C string by SWI time; and the
      formatter's return count is DISCARDED here (mov r0,#4 clobbers it
      before the SWI), so the observable result is whatever the SWI
      leaves in r0 - all 44 callers treat the call as a statement.
      The buffer's size is encoded nowhere in the image; it is bounded
      above by the next referenced global at 0x08a10870, giving 0x54 =
      84 bytes, and only this function's literal pool references
      0x08a1081c in the whole image.
      Deviations - the `...` becomes an explicit args: VaList (*const
      u32), the house convention (that IS what the spill frame
      manufactures); the formatter rides the TRACE_FORMAT slot
      (firmware-address default on target, panicking default on host);
      the SWI goes through the SEMIHOST_SWI dispatch hook (stdio/
      semihost.rs house pattern); the firmware globals are Rust statics.
      Host tests (6): render-into-TRACE_BUF-then-WRITE0 with content
      identity, SWI-result-not-formatter-count return, one static
      buffer reused across calls, unguarded NULL format with the SWI
      still firing, empty rendering still issuing WRITE0 on the buffer
      base, and r1 carrying the buffer pointer directly rather than a
      parameter block. match.py (--size 48): structural parity modulo
      the two volatile dispatch slots (house anti-const-fold pattern)
      and the vanished spill frame.

  # --- retailOS diagnostic pager key reader (stdio/debug_pager) ---
  - address: 0x082d997c
    ghidra: FUN_082d997c
    name: debug_pager_read_key
    size: 72
    sig: "int debug_pager_read_key(void)"
    module: stdio/debug_pager
    vars: {}
    status: ported
    renames:
      DAT_082d99c4 + 4 (0x089ca3c4): DEBUG_PAGER_GATE_ADDR
      DAT_082d99c8 (0x083e2484): DEBUG_PAGER_WRITE_BEFORE_READ
      DAT_082d99cc (0x083e2480): DEBUG_PAGER_WRITE_AFTER_READ
    notes: >-
      Raw code extent is exactly 72 bytes, 0x082d997c..0x082d99c0,
      followed by the three-word literal pool at 0x082d99c4..0x082d99cc;
      the distinct next function starts at 0x082d99d0. The gate word at
      0x089ca3c4 is read first: nonzero returns 0 before any SVC. A zero
      gate issues Angel SYS_WRITE0 (r0=4) with raw r1 literal 0x083e2484,
      calls SYS_READC (r0=7, r1=0), saves its r0, then issues a second
      SYS_WRITE0 with 0x083e2480 and returns the saved character, ignoring
      both write results. Decoding every ARM B/BL word in osos.dec finds
      exactly 8 direct sites, all plain unconditional bl
      (0x082beef0/0x082bf2fc/0x082bf454/0x082bf924/0x082bfcc4/
      0x082bffb8/0x082c00b8/0x082c0310), zero predicated calls, and no
      tail b site. Its callers page diagnostic table dumps and break on
      X/x, hence debug_pager_read_key. No image data word holds this
      address, so it is not virtually dispatched. The two SYS_WRITE0
      literals decode to live code at 0x083e2480; their intended string
      identity is not established and the port forwards them unchanged.
      Deliberate deviation: the target reads the original gate address
      volatile; host builds model only that word with a private static so
      the skip branch can be proven. Host tests cover suppression without
      SVC plus the exact op/pointer order and preservation of SYS_READC's
      return over both ignored SYS_WRITE0 results.

  - address: 0x082dd3d8
    ghidra: FUN_082dd3d8
    name: context_activity_enter
    size: 36
    sig: "void context_activity_enter(Context *context)"
    module: cxx/context_activity
    status: ported
    vars: {param_1: context}
    renames:
      param_1/r0: context
      "*(int*)(param_1+0xe0)": context->activity, the u32 lease count
      "*(int*)(param_1+0xdc)": context->marked, the sweep-mark flag
    notes: >-
      Enter half of the C++ context activity lease: increments the
      volatile u32 activity count at context+0xe0, then if the volatile
      mark flag at context+0xdc is nonzero AND the new count is 1,
      volatile-stores 1 into +0xe0 a second time. The final streq
      re-stores a value the first str already wrote - semantically a
      no-op, yet ADS emitted it, so both fields are volatile int in the
      source (a volatile store may not be elided); the port keeps the
      redundant store via read_volatile/write_volatile. Callers
      decrement +0xe0 inline on exit (e.g. FUN_0837dc38 wraps
      FUN_082dd05c); release_object @ 0x0837ee98 (cxx/release.rs) takes
      the same lease and now calls this port on target instead of
      transmuting the stock address. FUN_0837e920 sets the +0xdc flag on
      every context in the global registry list (linked at +0xd4) before
      an idle-context sweep and clears it afterwards. Extent verified
      from raw words: the next separately linked function starts at
      0x082dd3fc with push {r4-r10,lr}, so the Ghidra 36 bytes are
      exact; no trailing literal pool. Call count verified by decoding
      every B/BL word in osos.dec: exactly 24 sites, all unconditional
      plain bl, no predicated forms, no tail b; the address occurs in no
      image data word (never dispatched virtually). match.py: structural
      parity - LLVM adds a push {fp,lr} frame and folds the entered==1
      test into cmp-old/moveq against the original's cmp r1,#1; streq;
      same loads, stores, predicates and order. Host tests (6) cover
      unmarked increment, marked non-one increments (incl. 0x7fffffff),
      the marked 0->1 transition, non-boolean flag values, u32 wrap from
      0xffffffff to 0 skipping the transition path, and the unmarked
      entered==1 boundary; fixtures assert every word outside +0xe0 is
      untouched and +0xdc is never written.

  # --- Batch 10 scouting: identified only (ADS stdio / locale block) ---
  # The region 0x08030004-0x0803097c is the ADS buffered-stdio + setlocale
  # cluster (FILE struct: flags @ +0xc, buffer @ +0x10, size @ +0x1c,
  # chain link @ +0x40; the paired `mov r0,r0` sequences are patched-out
  # lock/unlock hooks around each op). Semihost-backed, dead on device.
  - address: 0x08030004
    name: stdio_stream_error_reset
    size: 64
    sig: "void stdio_stream_error_reset(AdsFile *file)"
    module: stdio/stream_file
    status: ported
    notes: >-
      flags = (flags & ~0x200000 dirty) | 0x80 error; unless string mode
      (0x1000000) also clears the count words +0x00/+0x08. The original
      returns &file->lock (+0x3c) in r0 — patched-out unlock residue,
      ignored by all callers, dropped in the port.

  - address: 0x08030044
    name: stdio_writeback
    size: 152
    sig: "i32 stdio_writeback(const u8 *buf, i32 len, AdsFile *file)"
    module: stdio/stream_file
    status: ported
    notes: >-
      Physical write core. If flags & 0x20010 (mask from pool @
      0x08030684; 0x20000 = fread's FLAG_GOT_REFILL), seeks to pos
      (+0x18) first — failure errors out before writing, success clears
      the bits. Then _sys_write; sets 0x40000 (last-op-write), pos +=
      len - (result & 0x7fffffff). Returns 0 iff result == 0, else
      error reset + -1.

  - address: 0x080300dc
    name: stdio_foreach_close
    size: 92
    sig: "void stdio_foreach_close(AdsFile *excluded)"
    module: stdio/stream_file
    status: ported
    notes: >-
      Walks the stream chain from static stdin (pool @ 0x0803068c =
      0x08b2f820) via the +0x40 links, calling the fclose core @
      0x0802fc00 (STREAM_CLOSE_CORE hook, ported as stream_sync_core in
      stdio/seek_core.rs) on every stream whose
      flags contain all of 0x202 (pool @ 0x08030690), with arg2 = 0 for
      `excluded`, 1 otherwise. List lock object @ 0x08a0fc04 (pool @
      0x08030688; lock hooks patched to nops).

  - address: 0x08030624
    name: stdio_close_all
    size: 96
    sig: "void stdio_close_all()"
    module: stdio/stream_file
    status: ported
    notes: >-
      RENAMED from stdio_flush_all — 0x080302d0 turned out to be fclose
      (the locked wrapper around the fclose engine 0x08030238), so this
      is the exit-path CLOSE-all walker. Runs the static
      stdin/stdout/stderr (0x08b2f820/64/a8, pool @ 0x0803068c/98/9c)
      through fclose @ 0x080302d0 (STREAM_FCLOSE hook, ported), then
      walks the dynamic chain hanging off stderr's +0x40 link — captured
      BEFORE the static closes — closing and then FREEING (free @
      0x0802edc8) each node. Reached from the exit-path cleanup
      0x08035878.

  - address: 0x080307bc
    name: setlocale_core
    size: 448
    sig: "const char *setlocale_core(u32 mask, const char *name)"
    module: runtime/locale
    status: ported
    notes: >-
      Full ADS setlocale engine (the install path @ 0x08030860 that
      errno.rs references is its second half). Categories are bits
      (collate 1 / ctype 2 / monetary 4 / numeric 8 / time 0x10), state
      is 5 words at libspace+0x20+4*i (ctype biased +1). NULL name =
      query via locale_name_for_mask; name[0]=='*' = raw-slot restore of
      five 8-hex-digit groups; else all-or-nothing install through the
      get_lc_* getters (collate/time install the directory ptr
      0x08985c06 directly, so any name succeeds for those two).
      Callers: C++ locale layer @ 0x082670fc/0x08267130/0x08267154.
      Port models the slots as LC_SLOTS [usize;5] (see module docs).

  - address: 0x080333c8
    name: stream_clear_error
    size: 48
    sig: "u8 *stream_clear_error(AdsStream *stream)"
    module: stdio/stream_flags
    status: ported
    notes: clearerr core (bic +0xc flags of 0x4000 and 0xc0) with nop'd
      lock hooks; returns FILE+0x3c (the lock address, an artifact of the
      nop'd unlock).

  - address: 0x080333f8
    name: stream_test_error_flag
    size: 44
    sig: "u32 stream_test_error_flag(AdsStream *stream)"
    module: stdio/stream_flags
    status: ported
    notes: >-
      CORRECTION (was stream_test_eof_flag): returns FILE flags & 0x80,
      the ERROR indicator — stdio_stream_error_reset @ 0x08030004 sets
      0x80 on the buffered-write failure path, and the printf front-end
      @ 0x0802f694 maps this result to printf's EOF return. ferror core;
      the EOF indicator is 0x40.

  - address: 0x08033424
    name: setvbuf_core
    size: 164
    sig: "u32 setvbuf_core(AdsStream *stream, u8 *buf, u32 mode, u32 size)"
    module: stdio/stream_flags
    status: ported
    notes: >-
      Validates flags&3 != 0 and !(flags & 0x400000), mode in {0x100
      full, 0x200 line, 0x400 none} and size-1 < 0xffffff; _IONBF uses
      the internal 1-byte buffer at FILE+0x24 with size 1; stores
      buf/+0x10, size/+0x1c, cursor/+0x4, flags = (flags & ~0xf00) |
      mode; returns 0 ok / 1 fail.

  - address: 0x080334dc
    name: localeconv
    size: 20
    sig: "Lconv *localeconv(void)"
    module: runtime/locale
    status: ported
    notes: Returns the static lconv @ 0x08b317d4 (pointer literal @
      0x080334f0) after refilling it via localeconv_fill @ 0x080354b8.
      Caller @ 0x082673f8 (C++ locale layer).

  - address: 0x080354b8
    name: localeconv_fill
    size: 208
    sig: "void localeconv_fill(Lconv *lconv)"
    module: runtime/locale
    status: ported
    notes: >-
      Fills an lconv from the LC_MONETARY (libspace+0x28) and LC_NUMERIC
      (libspace+0x2c) blocks, resolving block-relative i32 string
      offsets (numeric +0/4/8 -> decimal_point/thousands_sep/grouping;
      monetary +8..0x24 -> the 7 monetary strings; monetary bytes 0..8
      -> the 8 char fields). No null guard — a locale must be installed.

  - address: 0x080334f4
    name: ptr_or_deref_is_null
    size: 24
    sig: "u32 ptr_or_deref_is_null(const void **p)"
    module: runtime/locale
    status: ported
    notes: Returns 1 if p == NULL or *p == NULL else 0; locale-block
      presence check (caller @ 0x083d8dec).

  - address: 0x08035924
    name: stdio_stream_alloc
    size: 32
    sig: "AdsFile *stdio_stream_alloc()"
    module: stdio/stream_file
    status: ported
    notes: >-
      malloc(0x88) and clear the FIRST BYTE only (low byte of count);
      fresh FILE object (0x88 = the 0x44-byte AdsFile — 48-byte stream
      prefix + lock @ +0x3c + chain link @ +0x40 — plus buffer slack);
      NULL on allocation failure. No callers found in the (incomplete)
      osos.asm. Allocator boundary via STDIO_ALLOC slot (defaults to the
      ported malloc).

  - address: 0x08034f88
    name: stream_raw_read
    size: 100
    sig: "i32 stream_raw_read(u8 *dest, i32 len, AdsStream *stream)"
    module: stdio/stream_file
    status: ported
    notes: >-
      (was stdio_helper_08034f88) The raw read refill shared by the getc
      core 0x08034fec (call site 0x080353c8) and fread's STREAM_REFILL
      slot: _sys_read(handle +0x14, dest, len, flags +0xc as the block's
      mode word), clears 0x40000 (last-op-write). Result decode: -1 =
      error (stdio_stream_error_reset, return -1); bit 31 set = EOF
      (flags |= 0x4000, remainder = result & 0x7fffffff). Returns len -
      remainder = bytes actually read (0 = clean EOF). Type-compatible
      with fread.rs's StreamRefillFn; installing it there is init-time.

  - address: 0x08034fec
    name: stdio_fill_or_flush_core
    size: 1156
    sig: "i32 stdio_fill_or_flush_core(AdsStream *stream, i32 lock_mode)"
    module: stdio/getc_core
    status: ported
    vars: "r4=stream, r8=lock_mode, r5=avail, r6=dest, r5'=len, r10=handle, r11=&file.lock"
    notes: >-
      The __filbuf-scale getc core behind fread.rs's STREAM_GETC
      contract (returns next char, -1 EOF/error, -2 "do a bulk refill"
      when the caller set flags 0x800000); STREAM_GETC now DEFAULTS to
      this port. Second arg (r1) only gates the nop'd per-stream
      lock/unlock hook sites (behaviorally dead; both callers pass 0).
      Sets 0x400000 (in-getc) on entry, never clears it. CORRECTION to
      earlier notes (asm-verified): the entry "fast path" exists only
      for string-mode streams (masked pair 0x4820e1/0x400001, pool @
      0x08035474/70) and its avail comes out as min(0, ptr-lim) — never
      positive — so it always falls through; normal streams branch
      straight to the dispatch (beq 0x08035094) carrying count as
      avail; the *ptr++ delivery is really reached via the negative-
      avail ungetc walkback (count = -(count+1), flags |= 0x1000).
      flags&0x80000 set = ungetc-pending variant: clears the bit, for
      normal streams recomputes count from ptr/lim and +0x08 from the
      write-state pair (0x4816a2 pool @ 0x08035478 == 0x400002: size
      +0x30 - used if 0x2000&&0x10000 and used>=size, else used-size;
      0 outside write state), sets 0x200000 and returns the pushback
      byte at +0x25. Main path: drops 0x200000, syncs alt-offset via
      0x080301d4 under flags&0x20; non-read-eligible ((flags & 0x60c9
      pool @ 0x0803547c) != 1) zeroes count and either re-reports a
      latched EOF (0x4040 pool @ 0x08035480; 0x5000 pair -> 0x40,
      +0x08 = used - size+0x30) or error-resets via 0x08030004; else
      lazily mallocs the buffer (size +0x1c, result unchecked) with
      0x800(|0x100); seeks/flushes (0x10 pending: write-active via
      flush core 0x08030138, else ensure-stub 0x0803202c under 0x40000
      + _sys_seek to pos+(ptr-base)); write-active buffers drain via
      the flush core (partial buffers restore pos, re-seek past the
      drained bytes, set 0x20000, refill only the tail; pos advances
      under 0x800000), read-state buffers advance pos by ptr-base and
      refill whole; 0x600 flushes sharers via stdio_foreach_close @
      0x080300dc(self); 0x800000 answers -2 after ptr/lim = base; else
      stream_raw_read @ 0x08034f88 refills (count = n-1, ptr = dest+1,
      lim = dest+n, +0x30 = size, 0x20000) — NOTE any nonzero n
      delivers, including the raw-read error -1 (count becomes -2, a
      stale byte returns); n == 0 = clean EOF (0x1000 off, 0x40 on,
      count 0, ptr = dest, +0x08 = (dest-base)-size unless string mode
      or 0x600). Port: stdio/getc_core, callees all ported (malloc via
      the STDIO_ALLOC slot).

  - address: 0x08030138
    name: stdio_flush_buffer_core
    size: 156
    sig: "i32 stdio_flush_buffer_core(AdsFile *file)"
    module: stdio/getc_core
    status: ported
    notes: >-
      Buffered flush core. Clears flags 0x280000 (0x200000 buf-dirty +
      0x80000 ungetc-pending) up front; (flags & 3) == 1 (read-only) is a
      clean no-op returning 0; (flags & 0x82) != 2 (not write-mode or
      error latched) returns -1; write-mode without 0x10000
      (FLAG_WRITE_ACTIVE) returns 0. Otherwise drains the live extent
      max_u(ptr +0x04, lim +0x2c) - base +0x10 via stdio_writeback @
      0x08030044 (nonzero result = -1, buffer untouched), rewinds
      ptr/lim to base, zeroes +0x08 (write-side count) for non-string
      streams and clears 0x10000. Binary-verified callers (bl scan):
      fclose core 0x0802fc00 (@ 0x0802fc68), stdio_sync_alt_offset
      0x080301d4, and the getc core 0x08034fec (x3); the per-stream
      wrapper 0x080302d0 (fclose, ported) reaches it only indirectly
      (0x080302d0 -> fclose engine 0x08030238 -> 0x0802fc00), so the
      STREAM_FCLOSE hook is NOT satisfied by this port.

  - address: 0x080301d4
    name: stdio_sync_alt_offset
    size: 100
    sig: "void stdio_sync_alt_offset(AdsFile *file)"
    module: stdio/getc_core
    status: ported
    notes: >-
      Clears flags 0x20 (fread.rs's FLAG_ALT_OFFSET); when pos (+0x18)
      != alt_offset (+0x28) — an fseek-style override — flushes via
      0x08030138 (result IGNORED; a failed drain still repositions),
      replaces the buffer-live pair 0x3000 with 0x10 (seek-pending),
      adopts alt_offset as pos and rewinds ptr/lim (+0x04/+0x2c) to
      base (+0x10). Always ends by clearing the EOF pair 0x4040.
      Sole caller (binary-verified): the getc core 0x08034fec.

  # --- Batch 10 scouting: identified only (runtime / libm misc) ---
  - address: 0x08035788
    ghidra: FUN_08035788
    name: rt_lib_init_for_abort
    size: 240
    sig: "void rt_lib_init_for_abort(usize heap_base, usize heap_limit)"
    module: runtime/lib_init
    status: ported
    notes: >-
      The ADS library (re)initialization (__rt_lib_init). Call sequence
      (disasm-verified): _fp_init @ 0x083ecb90, heap_guard_size
      @ 0x082ab194 into the 3-word bounds block {base, limit, guard} on
      sp, stackheap_bounds_fetch @ 0x0800af04 (linked out to bx lr — the
      block comes back untouched), __rt_heap_init @ 0x0802ed14,
      rt_lib_init_post_heap_hook @ 0x0802ecc4 (nop), srandom1_thunk
      @ 0x08030ec0, then seeds the libspace LC slots: +0x20 (collate)
      and +0x30 (time) get ZERO — their getter calls are linked out to
      `mov r0,r0` nops with r0=0, NOT the directory pointer setlocale
      would install; +0x24 = get_lc_ctype(0,0)+1, +0x28 =
      get_lc_monetary(0,0), +0x2c = get_lc_numeric(0,0). Then
      stdio_init @ 0x080304a0 (five nop'd hooks before the call, two
      after) and __cpp_initialise @ 0x080316cc (self-relative ctor
      region 0x089d4f8c..0x089d51d8, 147 entries — CPP_STATIC_INIT hook
      in the port). r2/r3 are preserved and passed through to
      __rt_heap_init (dead there); the r0/r1 return is the linked-out
      bounds fetch's residue (dangling block pointer, 0) — dropped in
      the port along with the r2/r3 plumbing. LC seeding goes through
      runtime/locale.rs LC_SLOTS (slots 1..3 seeded; 0 and 4 written
      zero). Sole caller (binary-verified bl scan): abort @ 0x0803206c
      with (-1, -3) via the loader @ 0x08035894 — exit.rs's abort still
      reaches this via its no-op abort_report stub (unification is an
      init-time concern, see its docs).

  - address: 0x08035878
    ghidra: FUN_08035878
    name: exit_stdio_cleanup
    size: 24
    sig: "void exit_stdio_cleanup()"
    module: stdio/stream_file
    status: ported
    notes: >-
      Calls 0x082ab2b0 with 0 (the libspace+0x38 shutdown-handler chain
      runner — walks nodes {next, arg, fn, key}, calls fn(arg), unlinks
      and frees each; LIB_SHUTDOWN_CHAIN hook), then
      stdio_close_all @ 0x08030624, then 0x0802ecc0 (a mov pc,lr no-op).
      Reached from __rt_sys_exit and heap_panic; exit.rs still reaches
      it via its own no-op stub (unification is init-time, see its docs).

  - address: 0x082ab2b0
    ghidra: FUN_082ab2b0
    name: lib_shutdown_chain
    size: 108
    sig: "void lib_shutdown_chain(i32 key)"
    module: runtime/shutdown_chain
    status: ported
    vars: {param_1: key, iVar1/r8: libspace, puVar4/r5: prev_link,
      puVar3/r4: node, iVar5/r7: head_snapshot}
    notes: >-
      Walks the shutdown-handler node list {next +0, arg +4, fn +8, key
      +0xc} hanging off libspace+0x38 (libspace via 0x0803204c). key 0
      matches every node, nonzero only nodes with an equal key word.
      Per match, exact order — unlink, snapshot the head word (AFTER the
      unlink), fn(arg) (plain `mov pc,r1` with adr lr, no Thumb-bit
      mask despite Ghidra's & 0xfffffffc), free(node) @ 0x0802edc8,
      re-read the head — changed head (handler re-registered) restarts
      the scan from the head, unchanged continues from the same
      predecessor link. Sole caller (binary-verified bl scan, no
      literal refs): exit_stdio_cleanup @ 0x08035880 with key 0; the
      keyed path is dead in retailOS. Registration counterpart is
      cxa_atexit @ 0x082ab1c8 (identified and ported later, same
      module) — every registration passes the same key, the
      __dso_handle literal 0x089ca09c, which is why key-0 teardown
      reaches all of them. Default target of stream_file.rs's
      LIB_SHUTDOWN_CHAIN hook.

  - address: 0x08035b3c
    ghidra: FUN_08035b3c
    name: log_atanh_kernel
    size: 524
    sig: "u32 log_atanh_kernel(u32 u)"
    module: libm/cordic
    status: ported
    vars: {param_1: u, uVar17: y, uVar18: x, iVar16: acc, iVar15: e then
      loop_count, iVar3: table_index, uVar6: scale_log2, iVar14: min_shift,
      uVar8: sh_y, uVar11: sh_x, uVar19: sh_t}
    notes: >-
      Hyperbolic-CORDIC atanh, vectoring mode; u <= 2^-12 (signed)
      returns unchanged, else 9-row seed table @ 0x0898630c pre-rotates
      (smull + __aeabi_lasr), then 32 iterations over the Q30 atanh
      table @ 0x08986264 (repeats at indices 3/13) gather acc, returned
      as __fdiv(__i2f(acc), __fscalb(__i2f(1 << (scale-1)), 30)). ARM
      register-shift semantics (>= 32 sign-fills) are load-bearing late
      in the loop. Sole caller log_decompose @ 0x080340fc; wired as the
      LOG_ATANH_KERNEL default in libm/misc.rs. Within 1 ulp of host
      atanh across the caller domain u in [0, 0.6).

  - address: 0x08035d50
    ghidra: FUN_08035d50
    name: exp_frac_kernel
    size: 696
    sig: "void exp_frac_kernel(u32 frac, u32 *out_sinh, u32 *out_cosh,
      u32 *out_result)"
    module: libm/cordic
    status: ported
    vars: {param_1: frac, param_2: out_sinh, param_3: out_cosh, param_4:
      out_result, iVar13: x, iVar12: y, iVar8: z, unaff_r6: start_index}
    notes: >-
      Hyperbolic-CORDIC e^frac, rotation mode; writes sinh/cosh/e^frac
      through the three out pointers (caller reads only the third).
      frac <= 2^-12 short-cuts to (frac, 1.0f, __fadd(frac, 1.0f)); else
      the 12-pair seed table @ 0x089864d4 (indexed -exp-1) forms the
      residual angle, a fixed seed-rotation loop rebuilds (x, y), and 32
      iterations over the Q30 atanh table @ 0x0898642c (byte-identical
      to the log copy @ 0x08986264; repeats at 3/13) drive z to 0.
      Finale scales by 1/K consts @ 0x08036010/0x08036014 and rescales
      the sinh output by raw exponent-field subtraction. Sole caller
      expf_core @ 0x08033f40 (recorded as _fsqrt_core; call site
      0x08033fdc); wired as the EXP_FRAC_KERNEL default in
      libm/sqrt.rs. e^frac within 2 ulp of host exp over frac in [0, 1).

  - address: 0x0803357c
    name: rom_memcpy_keep_dst
    size: 12
    sig: "void *rom_memcpy_keep_dst(void *dst, const void *src, size_t len)"
    module: libc/rom_string
    status: ported
    notes: >-
      push {r0,lr}; bl thunk 0x08037dd0 (ROM __rt_memcpy @ 0x22000020);
      pop {r0,pc} — memcpy veneer that returns the original dst (the ROM
      routine advances r0). ROM 0x22000020 is the mirror of ported osos
      0x08000020, so the port calls the Rust __rt_memcpy directly.

  - address: 0x08033588
    name: memmove_u16
    size: 8
    sig: "u16 *memmove_u16(u16 *dst, const u16 *src, size_t count)"
    module: libc/rom_string
    status: ported
    notes: >-
      mov r2,r2,lsl#1 (element count -> bytes) then b 0x08035680, the
      dst-preserving ROM-memmove veneer — i.e. a wide-char (u16 element)
      memmove. Caller: the wide-string helper @ 0x083d9a2c.

  - address: 0x08035680
    name: rom_memmove_keep_dst
    size: 12
    sig: "void *rom_memmove_keep_dst(void *dst, const void *src, size_t len)"
    module: libc/rom_string
    status: ported
    notes: >-
      push {r0,lr}; bl thunk 0x08037dd8 (ROM memmove @ 0x220000d4);
      pop {r0,pc} — returns the original dst. Not listed in
      functions.csv (folded into the preceding raise range). ROM
      0x220000d4 is the mirror of ported osos 0x080000d4, so the port
      calls the Rust memmove directly.

  - address: 0x08036d08
    name: nop_stub
    size: 4
    sig: "void nop_stub()"
    module: stdio/semihost
    status: ported
    notes: mov pc,lr. Weak hook target (reached via 0x080358a0 from the
      abort report path).

  - address: 0x080358a0
    ghidra: FUN_080358a0
    name: abort_report_hook
    size: 12
    sig: "void abort_report_hook()"
    module: stdio/semihost
    status: ported
    notes: >-
      Weak-hook veneer of the abort report sequence (exit.rs
      abort_report): push {r4,lr}; bl nop_stub @ 0x08036d08 (ported);
      pop {r4,pc}. No direct bl call sites in osos.asm — the abort path
      reaches it as the report hook between the ADS library (re)init @
      0x08035788 and the retailOS log call @ 0x082d8fe8. Deviation: the
      callee address is read through a volatile pointer (house
      anti-const-fold trick) so LLVM keeps the call instead of inlining
      the empty nop_stub and dissolving the veneer into a bare bx lr.

  # --- Batch 12: the __rt_lib_init dependency chain (runtime/lib_init +
  # stdio/stdio_init) ---
  - address: 0x08030238
    ghidra: FUN_08030238
    name: fclose_core
    size: 152
    sig: "i32 fclose_core(AdsFile *file)"
    module: stdio/stdio_init
    status: ported
    notes: >-
      fclose engine minus FILE deallocation. flags&3==0 -> -1 untouched.
      Unless flags&8 (skip-close pseudo-stream marker, producer not yet
      identified) it syncs via 0x0802fc00 (STREAM_SYNC_CORE hook) with
      take_lock=0, _sys_close(handle) (negative -> -1, stream NOT
      reset), frees the buffer when flags&0x800 (heap-buffer bit), and
      skips a linked-out post-close hook gated on flags&0xfe000000 ==
      0xac000000 (nop'd bl @ 0x080302a4 whose r0 the original returns —
      still 0). Reset: IRAM memclr (veneer 0x08037db8 ->
      memzero_aligned) of the first 0x3c bytes (lock +0x3c and chain
      link +0x40 survive), then flags = old & 0x1000000 (string-mode
      bit only). Callers (binary-verified): the locked wrapper fclose @
      0x080302d0 and freopen_core @ 0x08030300.

  - address: 0x080302d0
    ghidra: FUN_080302d0
    name: fclose
    size: 48
    sig: "i32 fclose(AdsFile *file)"
    module: stdio/stdio_init
    status: ported
    notes: >-
      The public fclose entry — the (patched-out) per-stream lock
      bracket (mov r0, file+0x3c; mov r0,r0 on both sides) around
      fclose_core @ 0x08030238, whose result passes through. Verified
      from the machine code that it wraps the FCLOSE engine, not a
      buffer flush; the only callers in the image (binary bl scan) are
      the four call sites inside stdio_close_all @ 0x08030624
      (0x08030640/48/50/60), whose STREAM_FCLOSE hook now defaults to
      this port.

  - address: 0x0802fef0
    ghidra: FUN_0802fef0
    name: fseek
    size: 72
    sig: "i32 fseek(AdsFile *file, i32 offset, i32 whence)"
    module: stdio/stdio_init
    status: ported
    notes: >-
      Public fseek entry — the patched-out per-stream lock bracket
      around the seek core @ 0x0802fd04 (STREAM_SEEK_CORE hook), result
      passed through. Six callers (binary-verified) incl. scanf/C++
      layers and freopen_core's append path.

  - address: 0x0802ff38
    ghidra: FUN_0802ff38
    name: ftell
    size: 112
    sig: "i32 ftell(AdsFile *file)"
    module: stdio/ftell
    status: ported
    vars: {param_1: file, uVar1: flags, iVar3: pos, param_2/param_3: scratch loads of ptr/offset_end (Ghidra artifact — one real argument), FUN_0802ecb4: __rt_errno_addr (ported in runtime/errno.rs)}
    notes: >-
      Public ftell: returns the stream's logical position — (ptr - base)
      + offset_end (stream +0x04/+0x10/+0x18), or the alt_offset word
      (+0x28) under FLAG_ALT_OFFSET (0x20) — the exact `cur` formula
      fseek_core's SEEK_CUR arm offsets from — minus one when
      FLAG_UNGETC_PENDING (0x80000) is set and the position is positive
      (subgt: 0 and negative positions are left alone). A not-open
      stream (flags & 3 == 0) stores 1 (EPERM, ADS numbering) through
      __rt_errno_addr and returns -1; no state changes on success. The
      mov r0,file+0x3c; mov r0,r0 brackets are the patched-out stream
      lock — omitted (house precedent). Two callers (binary bl scan:
      0x0802907c and 0x0802910c, both inside FUN_08028ff4) feed the
      result straight into fseek — the classic tell-then-seek pattern.

  - address: 0x0802fd04
    ghidra: FUN_0802fd04
    name: fseek_core
    size: 492
    sig: "i32 fseek_core(AdsFile *file, i32 offset, i32 whence)"
    module: stdio/seek_core
    status: ported
    notes: >-
      The buffered fseek engine. Resolves the target (SET/CUR/END, END querying _sys_flen), rejects a negative target or a tty handle with 2, then either repositions ptr inside the buffered window (updating the count/field_08 pair) or parks the target in alt_offset with FLAG_ALT_OFFSET for a lazy reposition. Clears the sticky-EOF latch. Callers: fseek @ 0x0802fef0 and 0x0802fc7c (inside the sync core 0x0802fc00). Now the STREAM_SEEK_CORE default.

  - address: 0x0802fc00
    ghidra: FUN_0802fc00
    name: stream_sync_core
    size: 156
    sig: "i32 stream_sync_core(AdsFile *file, i32 take_lock)"
    module: stdio/seek_core
    status: ported
    notes: >-
      Flush/position-sync core with a take-lock r1 flag (nonzero
      brackets in the patched-out stream lock). Computes the logical
      position from ptr/offset/base (or +0x28 when flags&0x20) and
      drains/seeks accordingly; calls fseek_core @ 0x0802fd04. Callers
      (binary-verified): 0x0802fcb0/0x0802fcd4 (fflush family),
      0x08030118, fclose_core @ 0x08030238. Now the default of BOTH the
      STREAM_SYNC_CORE hook (stdio/stdio_init.rs) and — same routine,
      second arg carrying foreach_close's not_excluded flag —
      stream_file.rs's STREAM_CLOSE_CORE hook.

  - address: 0x0802fc9c
    ghidra: FUN_0802fc9c
    name: fflush
    size: 96
    sig: "i32 fflush(AdsFile *file)"
    module: stdio/stdio_init
    status: ported
    vars: "r0/param_1: file, r4: chain cursor, r5: result latch, DAT_0802fd00: 0x08b2f820 (static stdin FILE, chain head), DAT_0802fcfc: 0x08a0fc04 (stream-list lock object, patched-out bracket)"
    notes: >-
      The public fflush entry. fflush(file): sync core 0x0802fc00
      (STREAM_SYNC_CORE hook) with take_lock r1=1, result passed through
      verbatim — a nonzero failure is NOT normalized to -1 on this path.
      fflush(NULL): walks the whole stream chain from the static stdin
      FILE (head pointer 0x08b2f820 loaded from the literal pool @
      0x0802fd00 — the global IS the stdin object, not a
      pointer-to-pointer), syncing every stream (+0x40 links) with
      take_lock 1; the walk continues past failures and the return is
      -1 (mvnne latch) when ANY sync reported nonzero, else 0. The
      stream-list lock bracket (ldr r0,[0x0802fcfc] = 0x08a0fc04;
      mov r0,r0) is patched out — omitted, house precedent. Sole caller
      in the image (binary-verified bl scan): 0x083d84c0, inside
      FUN_083d8468 (C++-layer flush wrapper, passes a stream arg).

  - address: 0x0802ffa8
    ghidra: FUN_0802ffa8
    name: stream_write_block
    size: 92
    sig: "i32 stream_write_block(const u8 *buf, i32 size, i32 nitems, i32 ctx)"
    module: stdio/fwrite
    status: ported
    vars: {param_1: buf, param_2: size, param_3: nitems, param_4: ctx (putc context), iVar2/r5: written, FUN_082cf2c8: STREAM_WRITE_PUTC hook (line-buffer putc @ 0x082cf2c8, ported as linebuf_putc)}
    notes: >-
      Block writer through the retailOS line-buffer putc @ 0x082cf2c8:
      size * nitems bytes from buf, one putc(byte, ctx) call per byte,
      returns nitems when the whole block is accepted, 0 on the first
      putc answering -1 (all-or-nothing — no partial-item count, unlike
      fread). size == 0 short-circuits to nitems; the total is the ARM
      mul's mod-2^32 product and the loop bound compares signed (blt),
      so a nonpositive total writes nothing and still returns nitems.
      Sole caller in the image (binary-verified bl scan): 0x08266c04,
      inside FUN_08266be8 (retailOS log/formatter helper reporting
      result == nitems as bool). The putc is NOT the ADS stream layer —
      0x082cf2c8 owns the line buffer @ 0x08b31720 (Angel svc flush on
      '\n'/0x50 bytes, -1 for a null ctx); now ported as linebuf_putc
      (see its entry), which is the shipped default of the
      STREAM_WRITE_PUTC hook slot (house pattern, fread.rs's
      STREAM_REFILL).

  - address: 0x082cf2c8
    ghidra: FUN_082cf2c8
    name: linebuf_putc
    size: 88
    sig: "i32 linebuf_putc(i32 c, i32 ctx)"
    module: stdio/linebuf_putc
    status: ported
    vars: {param_1/r0/r4: c, param_2/r1: ctx (putc context — only tested for 0, otherwise ignored), r6: &line_pos, r2: pos (pre-increment), r0(after inc): new_pos, r5: 0 (NUL/reset scratch), DAT_082cf320: 0x08a0fc00 (line position counter, LINE_BUF_POS), DAT_082cf324: 0x08b31720 (line buffer base, LINE_BUF)}
    notes: >-
      RetailOS debug-console line-buffer putc — NOT the ADS stream
      layer; the sink behind stream_write_block @ 0x0802ffa8 (sole
      caller in the image, binary-verified bl scan: bl @ 0x0802ffd8).
      Null ctx answers -1 without touching state. Otherwise the position
      is incremented FIRST, the low byte of c is stored (strb) at the
      OLD position, and the line flushes — NUL written at the new
      position, Angel semihosting svc 0x123456 op 0x04 (SYS_WRITE0,
      r1 = buffer base directly, no parameter block, result ignored),
      position reset to 0 — when c == '\n' (FULL-WORD compare: c =
      0x0000010a stores 0x0a but does not flush) or the new position
      reaches 0x50 (80 chars, signed blt skipped; deepest write is the
      NUL at index 0x50, so the buffer is 0x51 bytes). Returns c
      verbatim (full word). PORT DEVIATIONS: the flush goes through the
      SEMIHOST_SWI dispatch hook (house pattern; dead on device anyway,
      no debugger traps the svc) instead of an inlined svc, and the two
      firmware globals are the exported statics LINE_BUF (0x08b31720)
      and LINE_BUF_POS (0x08a0fc00). match.py: structural match —
      null-ctx early -1, pos inc/store, strb at old pos, flush on
      newline or pos >= 0x50 (LLVM renders as > 0x4f), NUL store, op-4
      dispatch (blx through the hook vs inlined svc), pos reset, return
      c.

  - address: 0x08030300
    ghidra: FUN_08030300
    name: freopen_core
    size: 252
    sig: "AdsFile *freopen_core(const u8 *name, const u8 *mode, AdsFile *file)"
    module: stdio/stdio_init
    status: ported
    notes: >-
      fclose_core(file) (result ignored), then mode-string parse: 'r' ->
      flags 1/semihost-mode 0, 'w' -> 2/4, 'a' -> 0x8002 (literal pool
      @ 0x08030694)/8, else NULL; any run of '+' (flags|=3, mode|=2) and
      'b' (flags|=4, mode|=1); trailing 't' adds 0x10 to the mode. Then
      _sys_open(name, mode) — exactly -1 -> NULL. Re-init: base/ptr
      NULL, +0x1c = 0x200 default, handle stored, flags = (old &
      0x1000000) | parsed; append mode seeks to the end (fseek(file, 0,
      2), result ignored). Returns file. Callers (binary-verified):
      fopen @ 0x080303fc (ported) and stdio_init x3.

  - address: 0x080303fc
    ghidra: FUN_080303fc
    name: fopen
    size: 164
    sig: "AdsFile *fopen(const char *name, const char *mode)"
    module: stdio/stdio_init
    status: ported
    vars: {param_1: name, param_2: mode, iVar2/r4: file, iVar1/r5: fresh}
    notes: >-
      Scans the stream chain from static stdin (pool @ 0x0803068c) for
      the first FILE with flags&3 == 0 (closed statics are reused);
      when every slot is live, appends malloc(0x44) @ 0x0802edac to the
      tail (link stored BEFORE any null check — the original has NONE
      and would memzero address 0 on failure; the port returns NULL, a
      documented deviation), zeroes it via the IRAM veneer 0x08037db8,
      runs the per-stream lock init 0x080320a0 (return-0 stub in this
      build; nonzero would mark string-mode — dead), then passes the
      slot to freopen_core @ 0x08030300 and returns its result.
      List-lock bracket (object @ 0x08a0fc04, pool @ 0x08030688)
      patched to nops. NO callers in the image (binary bl +
      literal-pool scan): linked-in ADS library dead code.

  - address: 0x08037db8
    name: iram_memzero_aligned_veneer
    size: 8
    module: libc/memzero
    status: ported
    notes: >-
      ldr pc,[pc,#-4] -> 0x2200027c: the IRAM-relocated copy of
      memzero_aligned @ 0x0800027c (same mirror scheme as the ROM
      veneers @ 0x08037dd0/0x08037dd8 — the 0x22000000 image is the
      low-address osos libc block, offsets match). 320 bl sites
      firmware-wide (binary-verified); ports call the Rust
      memzero_aligned directly (stdio_init/fclose_core use typed field
      resets — equivalent on the 32-bit layout, portable on wider
      hosts).

  - address: 0x0803f350
    ghidra: FUN_0803f350
    name: memzero_20_aligned
    size: 8
    sig: "void *memzero_20_aligned(void *dst) -> dst + 20"
    module: libc/memzero
    status: ported
    renames:
      r0: dst
      r1: byte_count_20
    notes: >-
      Raw extent is exactly 0x0803f350..0x0803f358: `mov r1, #20; b
      0x08037db8`. The branch tail-dispatches through the ported
      iram_memzero_aligned_veneer to the 0x2200027c IRAM mirror of
      memzero_aligned. Binary decoding finds 18 direct BL call sites, all
      unconditional. The underlying ADS block fill advances r0, so this
      port clears five aligned words and returns dst + 20; it deliberately
      does not inherit the public memzero_aligned port's dst-return
      compatibility deviation.

  - address: 0x080304a0
    ghidra: FUN_080304a0
    name: stdio_init
    size: 388
    sig: "void stdio_init()"
    module: stdio/stdio_init
    status: ported
    notes: >-
      The ADS _initio (stdio arm of __rt_lib_init; sole caller
      rt_lib_init_for_abort @ 0x08035788). Zeroes the three static FILE
      objects (0x08b2f820/64/a8, 0x44 bytes each via the IRAM memclr
      veneer), chains stdin -> stdout -> stderr (+0x40 links), runs the
      per-stream and list lock-init calls — 0x080320a0 is the ported
      return-0 stub sys_stub_ret0_2, so the flags|=0x1000000 fallback
      marking is dead — then freopen_core on the semihosting console
      ":tt" (rodata copies @ 0x08985ee4/e8/ec; modes "r"/"w"/"w") and
      setvbuf_core(NULL, 0x200 line-buffered, 0x40/0x40/0x10). Every
      freopen NULL / setvbuf nonzero raises SIGRTRED (8, __rt_raise)
      with the ":tt" pointer as the detail word and falls through.

  - address: 0x083ecb90
    ghidra: FUN_083ecb90
    name: _fp_init
    size: 20
    sig: "void _fp_init()"
    module: runtime/lib_init
    status: ported
    notes: >-
      bl __rt_fp_status_addr (0x08036d60); mov r1,#0; str r1,[r0] —
      zeroes the soft-float status word at libspace+4. Sole caller
      (binary-verified): rt_lib_init_for_abort @ 0x08035788.

  - address: 0x082ab194
    ghidra: FUN_082ab194
    name: heap_guard_size
    size: 8
    sig: "usize heap_guard_size(usize request)"
    module: runtime/lib_init
    status: ported
    notes: >-
      mov r0,#1; bx lr — heap guard-size veneer, a stub returning 1 for
      any request. Callers (binary-verified): __rt_heap_init @ 0x0802ed14
      (port folds the constant as HEAP_GUARD_SIZE, malloc_rt.rs) and
      rt_lib_init_for_abort @ 0x08035788 (stores it into the bounds
      block).

  - address: 0x082ab188
    name: malloc_tail_veneer
    size: 12
    module: malloc_rt
    status: ported
    notes: >-
      mov r0,r1; mov r1,#1; b 0x080eb67c — drops the ADS heap descriptor
      and tail-calls the retailOS allocator with caller tag 1. Sole
      caller (binary-verified): malloc @ 0x0802edac. Folded into the
      HEAP_OPS default shim alloc_ported (malloc_rt.rs); not a separate
      export.

  - address: 0x082ab19c
    name: free_tail_veneer
    size: 16
    module: malloc_rt
    status: ported
    notes: >-
      movs r0,r1; movne r1,#1; bne 0x080e7970; bx lr — drops the
      descriptor, re-guards NULL and tail-calls the retailOS free with
      tag 1. Sole caller (binary-verified): free @ 0x0802edc8. Folded
      into the HEAP_OPS default shim free_ported (malloc_rt.rs).

  - address: 0x082ab1b4
    name: realloc_tail_veneer
    size: 20
    module: malloc_rt
    status: ported
    notes: >-
      mov r0,r1; mov r1,r2; mov r2,#1; mov r3,#1; b 0x080edbf0 — drops
      the descriptor and tail-calls the retailOS realloc with tag 1 and
      copy-on-move 1. Sole caller (binary-verified): realloc @
      0x0802edec. Folded into the HEAP_OPS default shim realloc_ported
      (malloc_rt.rs).

  - address: 0x0800af04
    ghidra: thunk_FUN_082ab11c
    name: stackheap_bounds_fetch
    size: 12
    sig: "usize *stackheap_bounds_fetch(usize *bounds_block)"
    module: runtime/lib_init
    status: ported
    notes: >-
      ldr r3,[pc]; add r3,pc,r3; mov pc,r3 — pc-relative thunk (literal
      0x2a020c) to 0x082ab11c, a bare `bx lr`. The stock-ADS stack/heap
      bounds fetch (__user_initial_stackheap family, r0 = 3-word block
      {heap_base, heap_limit, guard}, bounds returned in r0/r1) is
      LINKED OUT on this build; the call returns r0 (the block pointer)
      and leaves the block untouched. Sole caller: rt_lib_init_for_abort
      @ 0x08035788, which propagates the residue as its own return.

  - address: 0x082ab11c
    name: linked_out_ret_stub
    size: 4
    status: ported
    notes: >-
      `bx lr` (verified against osos.asm) — the target the linked-out
      stackheap_bounds_fetch thunk @ 0x0800af04 lands on. Nothing to
      port: the linked-out behavior (return r0, leave the block
      untouched) is reproduced by the stackheap_bounds_fetch port, and
      the stub is reachable only through that thunk, so hooking the
      thunk's address covers this one.

  - address: 0x0802ecc4
    ghidra: FUN_0802ecc4
    name: rt_lib_init_post_heap_hook
    size: 4
    sig: "void rt_lib_init_post_heap_hook()"
    module: runtime/lib_init
    status: ported
    notes: >-
      mov pc,lr — linked-out weak hook between __rt_heap_init and the
      rand seeding in rt_lib_init_for_abort (its sole caller,
      binary-verified). Twin of the exit-path no-op @ 0x0802ecc0.

  # --- Batch: RTXC kernel-object layer (opcode-2 objects, task objects,
  # --- message-queue nodes) ---
  - address: 0x0805675c
    ghidra: FUN_0805675c
    name: mailbox_create
    size: 44
    sig: "Mailbox *mailbox_create(void)"
    module: kernel/kobj
    status: ported
    notes: >-
      os_malloc(8); block->state=0; ROM create dispatch (thunk 0x08037e70
      -> ROM 0x22003d70) with opcode 2 into &block->id; returns block. No
      NULL check on the allocation (faithful). 2 callers (0x0808e29c,
      0x080e8760). Opcode 2 = second kernel-object class (1 = semaphore).

  - address: 0x080564b0
    ghidra: FUN_080564b0
    name: mailbox_delete
    size: 60
    sig: "void mailbox_delete(Mailbox *block)"
    module: kernel/kobj
    status: ported
    notes: >-
      Copies block->id to a stack slot; task_lock/task_unlock pair (thunks
      0x08037e48/0x08037e50) on the id; ROM delete dispatch (thunk
      0x08037e40 -> 0x22003dc8, opcode 2) on the stack copy; zeroes
      block->id; os_free(block). No NULL guard. 1 caller (blne @
      0x080a6bfc). match.py structural match (blx via hook table).

  - address: 0x0808e294
    ghidra: FUN_0808e294
    name: mailbox_slot_create
    size: 20
    sig: "void mailbox_slot_create(Mailbox **slot)"
    module: kernel/kobj
    status: ported
    notes: >-
      *slot = mailbox_create() — installs a fresh mailbox block into a
      caller-owned pointer slot. 46 bl/b call sites, binary-verified;
      among them the pool base-subobject ctor (bl @ 0x08214210, slot =
      pool + 0x78) and the pool parent-class ctor (bl @ 0x081f0074,
      slot = parent + 0x24).

  - address: 0x080a6bec
    ghidra: FUN_080a6bec
    name: mailbox_slot_delete
    size: 32
    sig: "void mailbox_slot_delete(Mailbox **slot)"
    module: kernel/kobj
    status: ported
    notes: >-
      NULL-guarded teardown twin of mailbox_slot_create; deletes *slot
      via mailbox_delete (the blne @ 0x080a6bfc — mailbox_delete's only
      call site) and zeroes the slot unconditionally. 28 bl/b call
      sites, binary-verified; among them the pool base-subobject dtor
      (bl @ 0x08214240).

  - address: 0x0808e2a8
    ghidra: FUN_0808e2a8
    name: mailbox_slot_post
    size: 8
    sig: "void mailbox_slot_post(Mailbox **slot)"
    module: kernel/kobj
    vars: {param_1: slot}
    status: ported
    renames:
      param_1/r0: slot
      FUN_080567a8: csem_post (already ported, kernel/csem.rs)
    notes: >-
      Two instructions, extent 0x0808e2a8..0x0808e2b0 confirmed from
      the raw words (the sibling mailbox_slot_signal starts at
      0x0808e2b0, its own entry at 0x0808e2b8 with push {r4,r5,r6,lr});
      no trailing literal pool, the tail branch is PC-relative. NOT a
      veneer — `ldr r0,[r0]` (0xe5900000) then `b 0x080567a8`
      (0xeaff213d), i.e. csem_post(*slot): load the mailbox block out
      of a caller-owned one-word slot and tail-call the already-ported
      add-and-wake V op of the counting semaphore. 59 branch sites,
      binary-scanned by decoding every B/BL word in osos.dec: 29 bl,
      7 predicated bl, 18 tail b, 5 predicated tail b — Ghidra's "36
      bl" counts only the link forms. No DATA word holds the address,
      so it is never dispatched virtually. The 12 predicated sites are
      the behavioural fact worth recording: this wrapper has NO NULL
      guard (the ldr and csem_post both dereference unconditionally),
      so callers that can hold an empty slot test it themselves and
      branch in conditionally.
      Identification: byte-twin sibling of mailbox_slot_signal @
      0x0808e2b0 (`ldr r0,[r0]; b 0x0805697c`, the csem_signal
      flavor); with mailbox_slot_create @ 0x0808e294 filling the slot
      they form the framework's construct/post/signal "event cell"
      idiom over one member pointer. Deviations — none in behaviour;
      the port re-views the Mailbox pointer as a CountingSem at the
      one call (same 8 bytes, both #[repr(C)] named fields, no literal
      byte offsets). Host tests (4) cover the token-adding path (count
      was >= 0, no ROM entry), the exact -1 -> 0 wake transition
      carrying this block's waiter id, deeper negative counts NOT
      waking (-3 -> -2, sleepers stay parked), and re-pointing the
      slot between calls (nothing is cached — the ldr is per call).
      match.py (--size 8): LLVM inlines csem_post's mrs/msr critical
      section and its conditional wake into the wrapper instead of
      tail-branching, so the port is the same deref followed by the
      callee's body verbatim (increment, adds/EQ early return, hook
      wake through KOBJ_HOOKS.rom_waiter_signal) — the documented
      hook-table dispatch deviation, same structure, identical to the
      mailbox_slot_signal precedent.

  - address: 0x0808e2b0
    ghidra: FUN_0808e2b0
    name: mailbox_slot_signal
    size: 8
    sig: "void mailbox_slot_signal(Mailbox **slot)"
    module: kernel/kobj
    vars: {param_1: slot}
    status: ported
    renames:
      param_1/r0: slot
      FUN_0805697c: csem_signal (already ported, kernel/csem.rs)
    notes: >-
      Two instructions, extent 0x0808e2b0..0x0808e2b8 confirmed from the
      raw words (the next function starts at 0x0808e2b8 with its own
      push {r4,r5,r6,lr}); no trailing literal pool, the tail branch is
      PC-relative. NOT a veneer — `ldr r0,[r0]` (0xe5900000) then
      `b 0x0805697c` (0xeaff21b0), i.e. csem_signal(*slot): load the
      mailbox block out of a caller-owned one-word slot and tail-call the
      already-ported wake side of the counting semaphore. 52 call sites,
      binary-scanned by decoding every B/BL word in osos.dec: 44 bl,
      1 blne, 1 bleq, 4 tail b, 2 tail bne. No DATA word holds the
      address, so it is never dispatched virtually. The 8 predicated
      sites are the behavioural fact worth recording: this wrapper has NO
      NULL guard (the ldr and csem_signal both dereference
      unconditionally), so callers that can hold an empty slot test it
      themselves and branch in conditionally.
      Identification: the slot is the same one mailbox_slot_create
      @ 0x0808e294 fills (`*slot = mailbox_create()`), and its byte-twin
      sibling FUN_0808e2a8 (`ldr r0,[r0]; b 0x080567a8`) is the csem_post
      flavor (ported as mailbox_slot_post) — construct/post/signal over
      one member pointer, the
      framework's "event cell" idiom. This wrapper is what proves the
      8-byte mailbox block {state @ +0, id @ +4} IS csem.rs's
      CountingSem {count: i32 @ +0, waiter_id: u32 @ +4}: state is the
      signed token count, id the RTXC waiter object slept on.
      Deviations - none in behaviour; the port re-views the Mailbox
      pointer as a CountingSem at the one call (same 8 bytes, both
      #[repr(C)] named fields, no literal byte offsets). Host tests
      (4) cover the token-spending path (count stays >= 0, no ROM
      entry), the negative-count wake carrying this block's waiter id,
      repeated signals driving the count further negative without
      saturating, and re-pointing the slot between calls (nothing is
      cached — the ldr is per call). match.py (--size 8): LLVM inlines
      csem_signal's mrs/msr critical section and its conditional wake
      into the wrapper instead of tail-branching, so the port is the
      same deref followed by the callee's body verbatim (decrement,
      cmn #1 / popgt early return, selector-1 gateway wake through
      gateway_wake_object) — the documented dispatcher-seam deviation,
      same structure.

  - address: 0x08056788
    ghidra: FUN_08056788
    name: waiter_create
    size: 32
    sig: "u32 waiter_create(void)"
    module: kernel/kobj
    status: ported
    notes: >-
      Zeroed stack slot; ROM create dispatch opcode 2; returns the kernel
      object id by value. Ignores its caller's r0. 4 callers, among them
      condvar_wait @ 0x0807f6ac (CONDVAR_HOOKS.waiter_create types the id
      as a pointer — cast at install time).

  - address: 0x080564ec
    ghidra: FUN_080564ec
    name: waiter_delete
    size: 36
    sig: "void waiter_delete(u32 id)"
    module: kernel/kobj
    status: ported
    notes: >-
      task_lock/task_unlock pair on the id, ROM delete dispatch (opcode 2)
      on a stack copy. Frees nothing. 2 callers (condvar layer's
      waiter_delete hook @ stock 0x080564ec).

  - address: 0x08056b10
    ghidra: FUN_08056b10
    name: queue_node_clear_data
    size: 16
    sig: "void queue_node_clear_data(QueueNode *node)"
    module: kernel/mqueue
    status: ported
    renames:
      param_1: node
    notes: >-
      Leaf reset for QueueNode's 8-byte message payload: stores zero at
      node+0x0c then node+0x08, leaving the +0x00 linkage, +0x04 owner,
      and +0x10/+0x11 flags untouched. The raw ARM caller at 0x080393d4
      passes an embedded queue node; these offsets align with QueueNode.data.
      Volatile Rust stores preserve that observable write order; no semantic
      deviation. match.py structural (two zero stores, reversed payload-word
      order).

  - address: 0x08056b20
    ghidra: FUN_08056b20
    name: queue_node_data_delta
    size: 16
    sig: "i32 queue_node_data_delta(QueueNode *node)"
    module: kernel/mqueue
    status: ported
    renames:
      param_1: node
    notes: >-
      Leaf arithmetic accessor for QueueNode's two-word message payload:
      raw ARM loads node+0x08 then node+0x0c and returns the signed,
      modulo-2^32 subtraction (first word minus second). QueueNode.data
      models exactly those payload words; this does not measure queue
      occupancy or mutate the node. Rust wrapping_sub is intentional to
      preserve ARM SUB overflow behavior. match.py structural (two payload
      loads and one subtraction).

  - address: 0x080b4a88
    ghidra: FUN_080b4a88
    name: mqueue_deliver
    size: 84
    sig: "u32 mqueue_deliver(QueueNode *node, u32 *out_data, QueueNode **out_node)"
    module: kernel/mqueue
    status: ported
    notes: >-
      Consumer accept step for popped queue nodes. Invalid node (+0x10
      byte 0) -> *out_node=0, recycle (0x080ed958), return 0 without
      touching out_data. Valid -> copy 8-byte payload (+0x8/+0xc);
      persistent (+0x11 byte) hands the node to the consumer, transient
      recycles it; return 1. No NULL guard on node (recycle guards its
      own). 2 callers (mqueue_receive @ 0x0807f63c, loop @ 0x0807a330).
      This IS CONDVAR_HOOKS.deliver. match.py 5/30 prefix, structure
      identical.

  - address: 0x080ed958
    ghidra: FUN_080ed958
    name: mqueue_node_recycle
    size: 112
    sig: "u32 mqueue_node_recycle(QueueNode *node)"
    module: kernel/mqueue
    status: ported
    notes: >-
      Returns a queue node to its owner pool (QueuePool ptr at node+4).
      NULL node is a no-op. If node->persistent (+0x11), brackets the
      return with sem_wait/sem_signal on *(pool+0x20) and one
      condvar_signal (0x080744d8) of *(pool+0x24). Core: sem_wait
      pool->lock (+0), list_push_back (0x080f1158) onto the free list @
      pool+0x14, condvar_signal &pool->notify (+0x08), sem_signal
      pool->lock. Always returns 0. 2 bl call sites (both in
      mqueue_deliver 0x080b4a88). Now the MQUEUE_HOOKS.node_recycle
      default; sem/cv ops route through MQUEUE_HOOKS slots whose
      defaults are the real ports. match.py structural match (identical
      offsets/order; push_back inlined, hooks as blx).

  - address: 0x080563a0
    ghidra: FUN_080563a0
    name: task_create
    size: 200
    sig: "TaskRecord *task_create(usize entry, u32 priority, usize stack_size, usize context, const char *name)"
    module: kernel/task
    status: ported
    notes: >-
      RTXC task create. Gateway id alloc (thunk 0x08037e28 -> ROM
      0x22003b6c, service 0x17); os_malloc stack + 0x44-byte record;
      memzero_aligned; record {id, entry, context, name[0x31] strncpy'd +
      forced NUL at +0x3d, stack at +0x40}; priority map 0x080e4348;
      gateway init (thunk 0x08037e30 -> 0x22003c98 with opaque literal
      0x0809c5c8 as 5th arg); gateway register (thunk 0x08037e38 ->
      0x22003d00, service 41); name-table insert 0x0806331c; task_start.
      No NULL checks on the allocations. 1 caller (spawn front-end
      0x0807a15c). match.py inflated by in-crate inlining of
      strncpy/memzero_aligned + hook-table ldrd loads; sequence verified
      by host tests.

  - address: 0x080b4c44
    ghidra: FUN_080b4c44
    name: task_start
    size: 8
    sig: "void task_start(a1, a2, a3, a4, a5, u32 id, a7)"
    module: kernel/task
    status: ported
    notes: >-
      ldr r0,[sp,#4]; b 0x08037f78 — forwards its SIXTH argument (task
      id) to ROM 0x22003e00 (gateway service 0x15 per the osos mirror @
      0x08003e00), ignoring the rest. 1 caller (task_create, which
      passes its full 7-arg frame).

  - address: 0x080b4c4c
    ghidra: FUN_080b4c4c
    name: task_destroy
    size: 40
    sig: "void task_destroy(TaskRecord *rec)"
    module: kernel/task
    status: ported
    notes: >-
      NULL no-op; gateway delete on rec->id (thunk 0x08037f80 -> ROM
      0x2200427c, service 0x18 per mirror 0x0800427c); os_free(rec->stack
      @ +0x40); tail os_free(rec). Ghidra's C inlines the whole os_free
      chain — trust the asm. 1 caller @ 0x080bbbd4.

  - address: 0x08060f80
    ghidra: FUN_08060f80
    name: task_notify
    size: 72
    sig: "u32 task_notify(usize callback)"
    module: kernel/task
    status: ported
    notes: >-
      0 while kernel-started byte @ 0x089ca848 is clear. Else if the
      current task's context word (0x0805665c) is 0, registers the task
      (0x080865e8 with the callback), fetches the context block
      (0x080cb828) and installs a 100-entry queue pool (0x0807a080) at
      ctx+0x1c; returns 1. Sole caller: kernel_running 0x0809444c
      (= sync_mutex ROM_KERNEL.notify — wire to this port).

  - address: 0x080613e0
    ghidra: FUN_080613e0
    name: ui_element_is_plst_class
    size: 40
    sig: "u32 ui_element_is_plst_class(const u8 *target)"
    module: ui/plst_class_check
    status: ported
    notes: >-
      NULL-guarded 'plst' UI-element class-tag predicate: returns 1 iff
      target is non-NULL and the word at target+0x4 equals the tag
      0x706c7374 ('plst'), held in the literal pool at 0x08061404.
      Ghidra's 36-byte extent drops that literal word; the true extent
      is 40 (the next function's stmdb prologue starts at 0x08061408).
      Call count verified by decoding every B/BL word in osos.dec: 32
      unconditional bl, zero predicated - the internal NULL guard is
      what all callers rely on. The tag is stamped at object+0x4 by the
      'plst' factory 0x0805e36c, copied from word +1 of the class
      descriptor at 0x0805e508 ({size 0x628, tag, fn words}). Callers
      pass the typed target of a UI element reference, e.g.
      ui_element_reference_is_current 0x082a6620 via its
      TARGET_CLASS_CHECK seam. Byte-shape-identical sibling 0x0806aa3c
      checks the 'tdat' tag 0x74646174 instead; the port's own
      link_section keeps the two hook seams unfolded. match.py: crate
      fp frame plus LLVM clz equality lowering vs ADS cmp/moveq; the
      core cmp r0,#0 / beq / ldr r0,[r0,#4] / ldr r1,[pc] sequence
      matches verbatim.

  - address: 0x08061378
    ghidra: FUN_08061378
    name: plst_element_notify_teardown
    size: 60
    sig: "void plst_element_notify_teardown(u8 *element)"
    module: ui/plst_element_teardown
    status: ported
    renames:
      param_1/r0: element
      DAT_080613b0: teardown_notify_tag
    notes: >-
      'plst' element teardown notifier. Raw ARM begins at 0x08061378 and
      ends with literal event key 0x63706c64 at 0x080613b0; the next
      function starts at 0x080613b4, so the true extent is 60 bytes
      although Ghidra reports 56. It calls the already ported
      ui_element_is_plst_class at 0x080613e0; on success it calls the
      unported tagged-handler walker 0x08066bb8 with list element+0x48,
      that raw event key, element context, and two zero arguments. The
      final call at 0x080613a8 targets 0x0806715c, verified from raw bytes
      to be the standalone `bx lr` no-op: it ignores element+8 and has no
      aligned data-word reference in osos.dec, so is deliberately elided
      rather than given an invented callee identity. Call
      count verified by decoding every B/BL word in osos.dec: 11 direct
      bl calls, 6 unconditional, 4 blne, and 1 bleq. The predicated
      callers gate their own state; the port relies on its internal class
      check. The unported walker is exposed through the
      PLST_ELEMENT_TEARDOWN_OPS seam (stock address on target, recorder
      seam on host).

  - address: 0x08052728
    ghidra: FUN_08052728
    name: ui_plst_slot_item_at
    size: 172
    sig: "u32 ui_plst_slot_item_at(u8 *element, u32 selector, u32 reverse_flag, u32 index)"
    module: ui/plst_slot_item
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: selector
      param_3/r2: reverse_flag
      param_4/r3: index
    notes: >-
      Indexed per-selector item fetch on a 'plst' UI element. Guards:
      ui_element_is_plst_class (0x080613e0, already ported) must pass
      AND selector != 0; both internal, so all 23 call sites are
      unconditional bl (verified by decoding every B/BL word in
      osos.dec: 23 uncond, 0 predicated). Item count is the u16 at
      header(+0x40)+0x2e; index >= count returns 0. The selector word
      and the LOW BYTE of reverse_flag then pass through the unported
      stock normalizer 0x080b48dc by pointer (0x34/0x37 -> slot 4,
      0x33/0x35/0x36 consult element bytes +0x18c/+0x18d and word
      +0xc8; may force the flag to 0). A nonzero flag byte
      reverse-indexes: count-index-1. Slot = element word at
      +0x3ac+selector*4 (49 slots); NULL slots are lazily materialized
      via unported 0x80df1b8 (return value discarded, like the
      original), and a still-NULL slot returns 0. Result is the word
      at slot+0x10+index*4, verbatim (callers such as 0x080aa498 use
      it as a sub-element pointer). Ghidra's 172-byte extent is exact
      (sibling prologue at 0x080527d4). Both unported callees sit
      behind the PLST_SLOT_ITEM_OPS seam (stock addresses on target,
      identity/never-materialize defaults on host). The port narrows
      the flag word to its low byte before the normalize call:
      bit-identical because the original only ever strb/ldrb that
      slot. match.py structural: LLVM fp frame, ldrd of the ops pair,
      mvn/add lowering of count-index-1; guard order, slot
      re-read-after-materialize, and the +0x3ac/+0x10 addressing all
      match.

  - address: 0x080e4348
    ghidra: FUN_080e4348
    name: task_priority_map
    size: 120
    sig: "u32 task_priority_map(u32 level)"
    module: kernel/task
    status: ported
    vars: {param_1: level}
    notes: >-
      Pure jump-table switch, level -> RTXC priority. 0->0x7e, 1->0x3c,
      2->0x34, 3->0x33, 4->0x31, 5->0x30, 6->10, 7->9, 8->8; levels
      outside 0..=8 fall through the `cmp r0,#8 / addls pc` guard and
      return unchanged. Sole caller task_create @ 0x08056404. Now the
      TASK_HOOKS.map_priority default, replacing the identity stub.

  - address: 0x0806331c
    ghidra: FUN_0806331c
    name: task_name_table_insert
    size: 60
    sig: "u32 task_name_table_insert(u32 id, const char *name)"
    module: kernel/task
    status: ported
    notes: >-
      Linear scan of the 0x50-entry {id, name} table @ 0x08a10870
      (pointer literal @ 0x08063358; crate static in the port); claims
      the first slot whose id matches or is 0; returns 1, or 0 when
      full. Name pointer stored, not copied. Faithful quirk kept: id 0
      stores the name but leaves the slot free. 1 bl call site
      (task_create @ 0x0805643c). Now the TASK_HOOKS.name_register
      default (stub removed). match.py structural (same scan/cmpne/dual
      store; LLVM strides by 8 instead of lsl #3).

  - address: 0x0805665c
    ghidra: FUN_0805665c
    name: current_task_context_word
    size: 20
    sig: "usize current_task_context_word(void)"
    module: kernel/task
    status: ported
    notes: >-
      task = current_task_record (0x080565f0); returns *(task+8) — the
      context word task_create stores at +0x08 — or 0. This is what
      sync_mutex's ROM_KERNEL.current_task_id hook reads ("id" is really
      the caller-context pointer, cf. 0x080cb828 dereferencing it +0xc);
      TASK_HOOKS.current_task_context defaults to this port.

  - address: 0x080565f0
    ghidra: FUN_080565f0
    name: current_task_record
    size: 96
    sig: "TaskRecord *current_task_record(void)"
    module: kernel/task
    status: ported
    notes: >-
      Thunk 0x08037e58 (ROM 0x22003ec4, gateway service 40, r0=0) returns
      the current task record; if NULL, lazily claims a slot from the
      static pool of 0x3c 0x44-byte records (counter @ 0x089cc8f4, pool @
      0x08ac5ccc — crate statics in the port), fills id via thunk
      0x08037e60 called with the post-increment counter (the
      "size_to_class" ROM op — this call site is evidence it is really an
      id/handle helper), zeroes ONLY +4/+8/byte +0xc, and registers via
      thunk 0x08037e38 with r0=0 (faithful quirk: id 0, not rec->id).
      Full pool returns NULL.

  - address: 0x080865e8
    ghidra: FUN_080865e8
    name: register_current_task
    size: 168
    sig: "void register_current_task(const char *name)"
    module: kernel/task
    status: ported
    notes: >-
      Attaches a NameNode to the current task. Registration block @
      0x089ca848 (literal @ 0x08086690) = {byte +0: the kernel-started
      flag, THE SAME byte as sync_mutex's KERNEL_STARTED; +8: lock sem
      slot (port static REGISTER_LOCK_SEM); +0xc: node cache (port
      static REGISTER_NODE_CACHE)}. Pre-kernel allocates a node
      (0x08093870, hook) with no locking; post-kernel takes the lock and
      consumes the cache. node->task (+0x10) = current_task_record
      (0x080565f0); links via 0x080568d0; nonzero name duplicated into a
      tag-7 heap block (strlen 0x08392478 inlined + 0x080eb67c +
      strcpy 0x08030ff4) at node+0. Tail path locks if not yet locked,
      refills the cache, sem_signals. 2 bl call sites (task_notify
      0x08060fa8, 0x080e873c). Faithful oddity kept: the notify chain
      passes CODE address 0x083e2e38 as the name. Now the TASK_HOOKS
      .register_current_task default. match.py inflated by in-crate
      inlining (current_task_record/link/strcpy); sequence proven by
      host tests.

  - address: 0x080568d0
    ghidra: FUN_080568d0
    name: current_task_link_node
    size: 24
    sig: "void current_task_link_node(NameNode *node)"
    module: kernel/task
    status: ported
    notes: >-
      current_task_record()->context (+0x08) = node; no record is a
      no-op. 2 bl call sites (0x0807f870, register_current_task @
      0x08086634). This is how the context word becomes the NameNode
      pointer that kernel_running/current_task_context_word report.

  - address: 0x08093870
    ghidra: FUN_08093870
    name: name_node_alloc
    size: 132
    sig: "NameNode *name_node_alloc(void)"
    module: kernel/task
    status: ported
    notes: >-
      Allocates the 0x18-byte NameNode (tag 5 via 0x080eb67c) plus a
      0x54-byte context block (via allocator 0x0807b254 with tag 8);
      seeds node+0 with the code/data address 0x080938f4, zeroes the
      rest, cross-links node+0xc = ctx and ctx+4 = node, ctx halfword+0
      = 1. 2 bl call sites, both in register_current_task. TASK_HOOKS
      .name_node_alloc stub (static scratch node) stands in until
      ported.

  - address: 0x080cb828
    ghidra: FUN_080cb828
    name: current_task_ctx_block
    size: 20
    sig: "TaskCtx *current_task_ctx_block(void)"
    module: kernel/task
    status: ported
    notes: >-
      kernel_running() (0x0809444c, ported) result is really the current
      task's NameNode pointer (the context word); returns node->ctx
      (+0xc) or 0. 46 bl call sites. Now the TASK_HOOKS.current_task_ctx
      default (dummy-block stub removed); TASK_HOOKS.kernel_running_node
      types the query result as the pointer (default adapter casts the
      ported i32). match.py structural (cmp/ldrne tail exact; prefix is
      the volatile hook-table ldrd noise).

  - address: 0x0807a080
    ghidra: FUN_0807a080
    name: queue_pool_create
    size: 72
    sig: "u8 *queue_pool_create(i32 capacity)"
    module: kernel/task
    status: ported
    notes: >-
      Allocates capacity*0x14+0x48 bytes (tag 10, wrapping 32-bit size
      from the raw capacity) and initializes the pool via
      0x0809eab8(capacity, base+0x48, base); capacity < 1 degrades to
      (0, 0, base). Returns the unchecked base. 22 bl call sites (among
      them task_notify @ 0x08060fb8). Now the TASK_HOOKS
      .queue_pool_create default (capacity retyped i32 to match the
      signed compare); the initializer routes through the new TASK_HOOKS
      .pool_init stub until ported. match.py structural (identical size
      math/tag/degrade branch; hook-table ldrd noise).

  - address: 0x0807a0c8
    ghidra: FUN_0807a0c8
    name: utf8_next_codepoint_permissive
    size: 116
    sig: "u32 utf8_next_codepoint_permissive(const u8 **cursor)"
    module: util/utf8_next_codepoint_permissive
    status: ported
    renames:
      param_1/r0: cursor
      pbVar1/r0: sequence
      uVar2/r0: lead
      uVar3/r2: second_byte
    notes: >-
      Raw ARM is 29 instructions at 0x0807a0c8..0x0807a13c, followed by the
      distinct 0x0807a13c entry; it is byte-identical to the decoder at
      0x08276214. It advances the cursor by one for ASCII, two for a
      0b110xxxxx lead, and three for a 0b1110xxxx lead, masking continuation
      payload bits without validation. All other high-bit leads, including
      4-byte UTF-8, advance by three and return zero. The binary scan finds
      17 direct BL call sites: 14 plain BL plus BLCC at 0x08263010,
      0x08263cec, and 0x0826400c. The separate target-only text section
      prevents LLVM from folding this independently hookable copy into the
      byte-identical 0x08276214 port. Host tests cover NUL/ASCII, malformed
      two- and three-byte continuation payloads, and invalid/four-byte leads.

  - address: 0x0807a13c
    ghidra: FUN_0807a13c
    name: singly_linked_list_count
    size: 28
    sig: "u32 singly_linked_list_count(SinglyLinkedNode **head_link)"
    module: util/linked_list_count
    status: ported
    renames:
      param_1/r0: head_link
      iVar1/r0: count
      param_1/r1: node
    notes: >-
      Raw ARM spans 0x0807a13c..0x0807a158 (7 instructions); 0x0807a15c
      starts the separately linked next function, despite Ghidra's 32-byte
      extent. It counts the non-NULL nodes reached by following the first
      word from the supplied head-link address. The head-link itself has no
      NULL guard. Decoding every ARM B/BL word in osos.dec finds 11 direct
      inbound calls, all plain unconditional BL; no predicated calls or tail
      branches target it. Host tests cover empty, single-node, and four-node
      chains. match.py review against the verified 28-byte extent finds the
      same load/follow/increment/NULL-test loop; LLVM folds the initial zero
      and branch into an all-ones counter plus increment after the first
      load.

  - address: 0x0809eab8
    ghidra: FUN_0809eab8
    name: queue_pool_init
    size: 160
    sig: "u32 queue_pool_init(i32 capacity, QueueNode *nodes, QueuePool *pool)"
    module: kernel/mqueue
    status: ported
    notes: >-
      Lays out the 0x48-byte QueuePool header (now fully modeled in
      kernel/mqueue.rs): pool->lock = sem_create (0x08056724);
      condvar_bind (0x080ed9c8) at +0x08 backref pool; zeroes
      +0x14..+0x28 and stores nodes at +0x1c; chains the capacity nodes
      (stride 0x14) onto the free list at +0x14 with node->owner = pool
      and node->valid (+0x10) = 1 (list_push_back 0x080f1158);
      mutex_create (0x080744a4) at +0x2c; condvar_bind at +0x34 backref
      pool+0x2c; zeroes +0x40/+0x44 (the LockedQueue anchor). Returns 0.
      1 bl call site (queue_pool_create @ 0x0807a0bc). Now the
      TASK_HOOKS.pool_init default (slot retyped to the QueueNode/
      QueuePool signature); sem_create/mutex_create route through new
      MQUEUE_HOOKS slots defaulting to the real ports; condvar_bind and
      list_push_back are plain calls like the original bls. match.py
      structural (stores/offsets all present; LLVM inlines the two list/
      bind helpers and rewrites the counted loop as a decrement).

  - address: 0x0807a15c
    ghidra: FUN_0807a15c
    name: task_spawn
    size: 196
    sig: >-
      u32 task_spawn(const char *name, usize entry, usize unused, usize
      stack_size, usize arg, u32 flags, SpawnRecord *record, usize hook)
    module: kernel/task
    status: ported
    notes: >-
      The sole task_create caller (2 bl call sites: 0x080e87a8 /
      0x080e9e9c). Maps the priority-flag argument to a level (0->0,
      8->1, 0x10->2, 0x18->3, 0x20->4, 0x40->5, 0x80->6, 0xc0->7,
      0x100->8; other <0x10 -> 1, >=0x10 -> 3), packs the caller
      SpawnRecord {name, entry, flags @ +8, arg @ +0xc, hook-or-default
      @ +0x14} and calls task_create(SPAWN_TRAMPOLINE 0x08074990 (lit @
      0x0807a224), level, stack_size, record, name); TaskRecord stored
      at record+0x10. hook default = 0x0808327c (lit @ 0x0807a220);
      both literals are mid-function code addresses, kept verbatim.
      Arg 3 is dead (r2 overwritten before use). Returns 0. match.py:
      LLVM lowers the flag switch as a jump table vs the cmp chain —
      mapping proven by the 13-case host test.

  - address: 0x0805695c
    ghidra: FUN_0805695c
    name: waiter_wait
    size: 32
    sig: "u32 waiter_wait(u32 id, u32 timeout)"
    module: kernel/kobj
    status: ported
    notes: >-
      Timed sleep on a waiter object via thunk 0x08037ea0 -> ROM
      0x220043c0 (id, timeout); zero timeout clamped to 1 tick; returns
      1 iff the ROM code is 5 (RTXC timeout). CONDVAR_HOOKS.waiter_wait.

  - address: 0x080567f8
    ghidra: thunk_EXT_FUN_220041cc
    name: waiter_wake
    size: 4
    sig: "void waiter_wake(u32 id)"
    module: kernel/kobj
    status: ported
    notes: >-
      b 0x08037e78 — pure alias of the thunk for ROM 0x220041cc (signal/
      wake the waiter object). CONDVAR_HOOKS.waiter_wake.

  - address: 0x080568e8
    ghidra: FUN_080568e8
    name: task_sleep
    size: 24
    sig: "u32 task_sleep(u32 ticks)"
    module: kernel/task
    vars: {param_1: ticks}
    status: ported
    notes: >-
      cmp r0,#0 splits two tail branches: ticks != 0 -> thunk
      0x08037e88 -> ROM 0x22003d44 (gateway service 20, the timed task
      delay) with (0 = current task, ticks); ticks == 0 -> thunk
      0x08037e90 -> ROM 0x220043f4 (gateway service 28 sub 13, no
      arguments, exact RTXC op unidentified). Both ROM results pass
      through the tail branch; call sites ignore them. The ROM services
      route through TASK_HOOKS .rom_timed_delay/.rom_reschedule
      (rom_yield precedent, no-op default stubs). 8 direct bl call
      sites plus three veneer thunks @ 0x0805f4a0/0x080a6b40/0x080e9eb0;
      observed counts are small polling delays (1, 10, 0x32, 100).
      match.py: LLVM loads the hook table then makes the same two-way
      cmp/beq split into the two indirect calls — same structure.

  - address: 0x080e9eb0
    ghidra: thunk_FUN_080568e8
    name: task_sleep_thunk
    size: 4
    sig: "u32 task_sleep_thunk(u32 ticks)"
    module: kernel/task
    status: ported
    renames: {}
    notes: >-
      Pure dispatch alias of task_sleep - one word, eafdb28c =
      `b 0x080568e8` (the next word, push {r4-r6, lr} @ 0x080e9eb4,
      starts the following function, so Ghidra's 4-byte extent is
      confirmed). Call traffic re-verified by decoding every B/BL word
      in osos.dec: 53 branch sites reach it - 47 plain bl, 1 predicated
      blne @ 0x08093a84 (tst r4, #0x400 gates a 790-tick sleep; callers
      do their own flag checks, no NULL guard involved), and 5 tail b's:
      the sibling veneers @ 0x0805f4a0 / 0x080a6b40, two mov r0, #20
      sleep(20) epilogues @ 0x081f5c28 / 0x081f5fe0, and another lone
      4-byte alias @ 0x082c6f90. One DATA word holds the address
      (@ 0x08a19800, amid adjacent code pointers), so the veneer is
      reachable indirectly too - hence a real exported symbol rather
      than the region_block_size_thunk hook-straight-to-callee pattern.
      Port is a pure forwarding tail call onto the ported task_sleep;
      r0 passes through unchanged like the original branch. Deviation:
      task_sleep gained inline(never) so LLVM stops cloning its body
      into the thunk and both stay real bl targets; its structure is
      otherwise unchanged. match.py: same single-tail-branch shape
      (fp/lr frame + b task_sleep via JUMP24 reloc); the merged
      task_yield_thunk shows the identical frame shape.

  - address: 0x080568fc
    ghidra: FUN_080568fc
    name: kernel_yield
    size: 8
    sig: "i32 kernel_yield(void)"
    module: kernel/task
    status: ported
    notes: >-
      mov r0,#0; b 0x08037e98 (ROM 0x22004260, exact RTXC service
      unidentified — yield-like). The stock target of condvar.rs's
      task_yield/task_yield_thunk wrappers (CONDVAR_HOOKS.task_yield).

  - address: 0x081b02b8
    ghidra: FUN_081b02b8
    name: irq_save
    size: 24
    sig: "void irq_save(u32 *saved_if_bits)"
    module: kernel/csem
    vars: {param_1: saved_if_bits}
    status: ported
    notes: >-
      mrs r1,cpsr; and r2,r1,#0xc0; orr r1,r1,#0xc0; str r2,[r0]; msr
      cpsr_c,r1 — masks IRQ and FIQ and stores the PREVIOUS I/F bits
      (cpsr & 0xc0) through the out-pointer (nothing returned in r0).
      Ghidra decompiles it as `*param_1 = 0` (it cannot model cpsr).
      2 call sites, the irqsafe atomics @ 0x08056328/0x08056364; the
      matching restore sequence (mrs; bic #0xc0; orr saved; msr cpsr_c)
      is inlined in each caller, not a separate function. Port is
      cfg-gated - real mrs/msr inline asm on ARM, simulated cpsr word +
      write log on host (host builds cannot mask real interrupts).

  - address: 0x08056328
    ghidra: FUN_08056328
    name: atomic_add_irqsafe
    size: 60
    sig: "i32 atomic_add_irqsafe(i32 amount, i32 *ptr)"
    module: kernel/csem
    vars: {param_1: amount, param_2: ptr}
    status: ported
    notes: >-
      Saves cpsr via irq_save @ 0x081b02b8 (IRQ+FIQ off), old = *ptr,
      *ptr += amount, restores the saved I/F bits with the inline mrs/
      bic/orr/msr sequence, returns old. Twin of 0x08056364. 4 call
      sites (binary scan; osos.asm drops one line): 0x080567b8,
      0x080567e0 (the add-and-wake V ops csem_post @ 0x080567a8 /
      csem_post_deferred @ 0x080567d0, both ported),
      csem_wait's timeout undo @ 0x08056948, and the bare alias thunk
      `b` @ 0x080f041c.

  - address: 0x08056364
    ghidra: FUN_08056364
    name: atomic_sub_irqsafe
    size: 60
    sig: "i32 atomic_sub_irqsafe(i32 amount, i32 *ptr)"
    module: kernel/csem
    vars: {param_1: amount, param_2: ptr}
    status: ported
    notes: >-
      Twin of 0x08056328 with *ptr -= amount; returns the old value.
      3 call sites: csem_wait @ 0x08056918, csem_signal @ 0x0805698c,
      alias thunk `b` @ 0x080f0420 (thunk_FUN_08056364).

  - address: 0x080f041c
    ghidra: null
    name: atomic_add_irqsafe_alias_f041c
    size: 4
    sig: "i32 atomic_add_irqsafe_alias_f041c(i32 amount, i32 *ptr)"
    module: kernel/csem
    vars: {param_1: amount, param_2: ptr}
    status: ported
    notes: >-
      ALIAS - bare `b 0x08056328` tail veneer onto atomic_add_irqsafe.
      Absent from functions.csv AND osos.asm (the disassembly listing
      drops the line); extent and target binary-verified from osos.dec
      (word eafd97c1; the next word @ 0x080f0420 is the twin alias
      `b 0x08056364` onto atomic_sub_irqsafe, NOT this entry). 1 call
      site (binary scan; osos.asm drops it too): the tail `b` @
      0x08147254 of an unnamed add-veneer @ 0x0814724c (add r1,r0,#4;
      mov r0,#1 — atomic_add_irqsafe(1, &obj->field4), also absent
      from functions.csv), mirrored by FUN_08147258 which bl's the sub
      twin. No separate port exists; hook this address to the
      atomic_add_irqsafe symbol (the rom_svc_22001cbc_alias_67fc
      precedent). Doc header placed on atomic_add_irqsafe in
      src/kernel/csem.rs.

  - address: 0x080f0420
    ghidra: thunk_FUN_08056364
    name: atomic_sub_irqsafe_alias_f0420
    size: 4
    sig: "i32 atomic_sub_irqsafe_alias_f0420(i32 amount, i32 *ptr)"
    module: kernel/csem
    vars: {param_1: amount, param_2: ptr}
    status: ported
    notes: >-
      ALIAS - bare `b 0x08056364` tail veneer onto atomic_sub_irqsafe.
      Listed in functions.csv as thunk_FUN_08056364 and present in
      osos.asm (unlike the add twin @ 0x080f041c, which both drop);
      extent and target binary-verified from osos.dec anyway (word
      eafd97cf). 1 call site (binary scan): the `bl` @ 0x08147268 of
      FUN_08147258 (add r1,r0,#4; mov r0,#1 —
      atomic_sub_irqsafe(1, &obj->field4); when old == 1 the object
      tail-dispatches the virtual at (*obj)+0x20 — the last-reference
      release, mirror of the unnamed add-veneer @ 0x0814724c that
      tail-branches the add twin). No separate port exists; hook this
      address to the atomic_sub_irqsafe symbol (the
      rom_svc_22001cbc_alias_67fc precedent). Doc header placed on
      atomic_sub_irqsafe in src/kernel/csem.rs.

  - address: 0x08056904
    ghidra: FUN_08056904
    name: csem_wait
    size: 88
    sig: "u32 csem_wait(CountingSem *csem, u32 timeout)"
    module: kernel/csem
    vars: {param_1: csem, param_2: timeout}
    status: ported
    notes: >-
      Counting semaphore over a waiter object; csem[0] = signed count,
      csem[1] = waiter id. old = atomic_sub_irqsafe(1, &count); if
      old-1 < 0 (no token), timed sleep on csem[1] (thunk 0x08037ea0 ->
      ROM 0x220043c0, zero timeout clamped to 1); on RTXC code 5 undoes
      the decrement (atomic_add_irqsafe) and returns 1 (timeout), else
      0. 1 call site: 0x080b4ae4 (queue-get wrapper 0x080b4adc). Port
      sleeps through the ported waiter_wait @ 0x0805695c (identical
      clamp and rc==5 mapping, same KOBJ_HOOKS.rom_waiter_wait ROM
      boundary).

  - address: 0x0805697c
    ghidra: FUN_0805697c
    name: csem_signal
    size: 40
    sig: "void csem_signal(CountingSem *csem)"
    module: kernel/csem
    vars: {param_1: csem}
    status: ported
    notes: >-
      old = atomic_sub_irqsafe(1, &count) — DECREMENTS, binary-verified
      (word ebfffe74 @ 0x0805698c = bl 0x08056364; an earlier note here
      claimed atomic_add — wrong). If old-1 < 0 (a sleeper may be
      parked) tail-branches thunk 0x08037ea8 -> ROM 0x22004368 with
      csem[1] (wake via gateway_wake_object's selector-1 record). Both
      halves
      decrementing still works as a wait/notify event - positive counts
      are a ROM-skipping fast path, and once drifted negative every
      wait parks and every signal wakes (ROM keeps the signaled state).
      2 call sites: tail `b` @ 0x0808e2b4 (deref wrapper FUN_0808e2b0),
      bl @ 0x080bbb90. The classic add-and-wake V ops are the ported
      csem_post @ 0x080567a8 / csem_post_deferred @ 0x080567d0.

  - address: 0x080569a4
    ghidra: thunk_EXT_FUN_22004368
    name: csem_wake
    size: 4
    sig: "void csem_wake(u32 id)"
    module: kernel/csem
    status: ported
    notes: >-
      b 0x08037ea8 — bare alias of the thunk for ROM 0x22004368 (wake
      the waiter object, r0 = raw waiter id, no count bookkeeping).
      2 call sites: 0x080eda08, 0x0810789c.

  - address: 0x080567a8
    ghidra: FUN_080567a8
    name: csem_post
    size: 40
    sig: "void csem_post(CountingSem *csem)"
    module: kernel/csem
    vars: {param_1: csem}
    status: ported
    notes: >-
      The classic add-and-wake V op: old = atomic_add_irqsafe(1,
      &count); if old+1 == 0 (adds/EQ — old exactly -1, one sleeper
      may be parked) tail-branches thunk 0x08037e78 -> ROM 0x220041cc
      with the waiter id. CORRECTION to the earlier scouting note:
      the wake target is kobj's waiter-signal entry (ported as
      kobj::waiter_wake @ 0x080567f8, alias of the same thunk), NOT
      thunk 0x08037ea8 / ROM 0x22004368 (now ported as
      gateway_wake_object's selector-1 record), and the condition is
      equality with -1, not a signed < 0.
      2 call sites: tail `b` @ 0x0808e2ac (deref wrapper
      FUN_0808e2a8, ported as kobj::mailbox_slot_post), bl @ 0x080b26b8. Twin 0x080567d0 (same shape,
      wakes via thunk 0x08037e80) ported as csem_post_deferred.
      match.py: LLVM inlines the atomic (identical mrs/msr critical
      section), tests old+1 via the adds carry flag (cc = old != -1
      early return), and tail-branches the hook — same structure.

  - address: 0x080567d0
    ghidra: null
    name: csem_post_deferred
    size: 40
    sig: "void csem_post_deferred(CountingSem *csem)"
    module: kernel/csem
    status: ported
    notes: >-
      Absent from functions.csv (no Ghidra name); extent verified from
      osos.asm (0x080567d0..0x080567f8, 10 instructions). Instruction-
      for-instruction twin of csem_post @ 0x080567a8 — old =
      atomic_add_irqsafe(1, &count); if old+1 == 0 (adds/EQ — old
      exactly -1, one sleeper may be parked) tail-branches with the
      waiter id — except the wake goes to thunk 0x08037e80 -> ROM
      0x22001cbc, a full ROM function (kernel lock, then a pending-id
      table walk/append; mirror 0x08001cbc) instead of the gateway
      signal stub: a deferred-wake flavor of the same V op, routed
      through the ported task_lock::rom_svc_22001cbc. 1 call site:
      tail `b` @ 0x080c692c of the deref wrapper FUN_080c6928 (whose
      Ghidra decomp is this function verbatim; FUN_0812c444 picks
      between this flavor and csem_post's wrapper FUN_0808e2a8 on a
      flag). match.py (--size 40): LLVM inlines the atomic (identical
      mrs/msr critical section), early-returns on cc (old != -1), and
      calls the wake indirectly through the ROM_KERNEL table — the
      documented ROM-thunk dispatch deviation; same structure.

  # --- RTC context dirty marker (time/rtc) ---
  - address: 0x0805e66c
    ghidra: FUN_0805e66c
    name: rtc_context_mark_dirty
    size: 12
    sig: "void rtc_context_mark_dirty(RtcContext *context)"
    module: time/rtc
    renames: {param_1: context}
    status: ported
    notes: >-
      Raw-binary extent is exactly 12 bytes: `mov r1,#1; strb
      r1,[r0,#0xb90]; bx lr`; the separately linked next function begins
      at 0x0805e678. This marks the nested RTC context's +0xb90 dirty
      byte true without reading it or guarding context against NULL.
      Decoding every ARM B/BL word in osos.dec found 16 direct bl sites:
      8 unconditional and 8 blne; there are also 34 direct b sites, 2
      unconditional tail branches and 32 bne. The predicated branches
      establish that their callers guard the context pointer themselves;
      unconditional callers require a valid context. Callers first write
      one of several configuration fields in this context, then invoke
      this marker. Deviation: none. Host test verifies that a prior 0xff
      dirty value becomes 1 while the adjacent modeled context regions
      and status byte remain untouched.

  # --- RTC context handle (time/rtc) ---
  - address: 0x08056124
    ghidra: FUN_08056124
    name: rtc_context_handle
    size: 12
    sig: "u32 rtc_context_handle(const RtcContextOwner *owner)"
    module: time/rtc
    vars: {param_1: owner}
    status: ported
    notes: >-
      Leaf read-only accessor: ldr r0,[r0,#0xf00]; ldr r0,[r0,#0xc];
      bx lr. owner is the context-bearing object, whose +0xf00 nested
      RTC-context pointer supplies an opaque 32-bit handle at +0xc.
      Its sole recovered caller is the tail-dispatch setup
      FUN_08160254: it gets owner through
      FUN_08289690()->+0x28->+0x30, stores this result to its +0x54,
      then passes the nested context's companion +0xb54 word to its
      vtable +0x84 method. ABI: r0 is the sole pointer argument and
      return register; no stack or writes. Port deviation: none.

  # --- RTC static fallback pair (time/rtc) ---
  - address: 0x08056130
    ghidra: FUN_08056130
    name: rtc_static_time_pair
    size: 28
    sig: "i32 rtc_static_time_pair(u32 *days_out, u32 *secs_out)"
    module: time/rtc
    vars: {param_1: days_out, param_2: secs_out, puVar1: static_time_pair}
    status: ported
    notes: >-
      Leaf static-pair copier, not an RTC bus access: ldr r3,[pc,#20]
      loads 0x089caa98; ldr r2,[r3]; str r2,[r0]; ldr r0,[r3,#4];
      str r0,[r1]; mov r0,#0; bx lr. Thus the source's first word is
      written to days_out (r0 on entry) and its adjacent second word to
      secs_out (r1), preserving the source order. The retail image's
      read-only words are 0x74614400 and 0x6d695465. The caller at
      0x08065d18 passes r0=sp+4 and r1=sp, then compares the first
      result with its context +0xb7c and the second with +0xb80; the
      system-time builder FUN_0806e3dc likewise consumes them as a
      (days, seconds) pair. ABI: two valid writable u32 pointers in
      r0/r1; no stack access; return 0 in r0. Port deviation: volatile
      source reads retain the original read-only two-load behavior.
  # --- RTC wall-clock read (time/rtc) ---
  - address: 0x08056150
    ghidra: FUN_08056150
    name: rtc_read_time
    size: 72
    sig: "i32 rtc_read_time(u32 *days_out, u32 *secs_out, u32 seed_lo, u32 seed_hi)"
    module: time/rtc
    status: ported
    vars: {param_1: days_out, param_2: secs_out, param_3: seed_lo, param_4: seed_hi, iVar1: read_status}
    notes: >-
      NOT a sync primitive despite the 0x08056xxx neighborhood — the
      device's wall-clock read. FUN_082e58f0(0, buf) fetches 7 BCD bytes
      from the PCF50635 PMU RTC (I2C slave 0x73, register block 0x59:
      sec/min/hour/weekday/day/month/year; the mutexed wrapper around
      FUN_0836d698), then FUN_0809e3e8 converts them to a (days,
      seconds-of-day) pair: Hinnant civil-to-days over the 2-digit year
      (adj = (14-month)/12 UNSIGNED, y = year-adj+0x1a90, days =
      365*y + day + (153*mp+2)/5 + y/4 - y/100 + y/400 - 0x7d2d; 365
      from the literal @ 0x0809e4cc; BCD decode FUN_080ed424 clamps
      >= 0x9a to 99), secs = hour*0xe10 + min*60 + sec. The saved r2/r3
      slots double as the RTC buffer, so the 3rd/4th argument words seed
      it (visible on failed reads); the outputs are written even on
      error; return 0 or -5 (mvnne r0, #4). 1 call site: bl @ 0x0806e400
      in the 64-bit system-time builder FUN_0806e3dc. The mutexed
      wrapper FUN_082e58f0 is now ported (drivers/i2c::pmu_i2c_read_regs)
      and is the shipped default of the RTC_READ_REGS slot; the bank
      mux under it (FUN_0836d698) is ported too
      (drivers/i2c::pmu_i2c_read_bank, the shipped PMU_READ_REGS
      default), and its raw transfer FUN_0836d3b8 is now
      drivers/i2c::pmu_i2c_read. That port calls the still-unported
      S5L8702 I2C write/read primitives FUN_0836bb84 / FUN_0836b950 at
      fixed target addresses, while host tests install volatile seams.
      pure-arithmetic conversion FUN_0809e3e8 is ported as
      time/civil::bcd_datetime_to_days_secs and called (it was an inline
      reproduction at this entry's porting). match.py: LLVM
      inlines the conversion and strength-reduces the divides to magic
      multiplies, but every formula element is present (rsb #14,
      +0x1a90, mla 365, 0x99, -0x7d2d, 0xe10, mvnne #4); the original's
      two bl calls become one blx through the slot.

  # --- RTC context status (time/rtc) ---
  - address: 0x08056198
    ghidra: FUN_08056198
    name: rtc_context_status
    size: 12
    sig: "u8 rtc_context_status(const RtcContextOwner *owner)"
    module: time/rtc
    vars: {param_1: owner}
    status: ported
    notes: >-
      Read-only nested-context byte accessor. The raw ARM body is
      ldr r0,[r0,#0xf00]; ldrb r0,[r0,#0xb24]; bx lr: owner supplies
      the RTC-context pointer at +0xf00 and the return is its status byte
      at +0xb24, zero-extended in r0. The sole recovered caller is
      FUN_08172c6c, which invokes it on its context-owner +0xa8, caches
      the returned byte, and publishes event 0x60ae when it changes.
      The nested owner/context relationship is shared with the adjacent
      RTC-context accessors FUN_08056124 (+0x0c handle) and
      FUN_0805603c (+0xb54 companion word). ABI: r0 is the only input
      and return register; no stack access, writes, or null check. Port
      deviation: none.

  # --- RTC context field (time/rtc) ---
  - address: 0x080561a4
    ghidra: FUN_080561a4
    name: rtc_context_field
    size: 16
    sig: "u8 *rtc_context_field(void)"
    module: time/rtc
    vars: {iVar1: current_context}
    status: ported
    notes: >-
      Context-field address accessor: push {r4,lr}; bl FUN_08369bec;
      add r0,r0,#0x38; pop {r4,pc}. FUN_08369bec is the retailOS lazy
      shared-context getter: it publishes DAT_08369c0c into the slot
      DAT_08369c08 when zero, then returns that context base. This function
      returns (without dereferencing) the opaque eight-byte field at +0x38.
      Three recovered callers consume it: FUN_08067e6c and FUN_08067eb4
      use its +4 word in their word-wise transforms; FUN_08068504 copies
      all eight bytes into its iPod_Control_iTunes message. ABI: no
      arguments, pointer result in r0, no writes except the stock getter's
      lazy initialization. The target port calls that unported stock getter
      at 0x08369bec; host tests inject its result. Port deviation: none on
      target.

  # --- PMU board-version status bit (drivers/pmu) ---
  - address: 0x082e5b64
    ghidra: FUN_082e5b64
    name: pmu_board_version_status_bit
    size: 76
    sig: "u32 pmu_board_version_status_bit(u32 incoming_r0, u32 incoming_r1, u32 incoming_r2, u32 incoming_r3)"
    module: drivers/pmu
    status: ported
    renames:
      uVar1/r0: board_version
      uVar2/r0: pmu_register
      local_10/sp: status_byte_seeded_from_incoming_r3
      FUN_080e624c: board_version
      FUN_0806a4b0: kernel_sem17_wait
      FUN_0806a4a0: kernel_sem5_wait
      FUN_0836d3b8: pmu_i2c_read
      FUN_080645a8: kernel_sem5_signal
      FUN_08064604: kernel_sem17_signal
    notes: >-
      Raw ARM extent is exactly 76 bytes, 0x082e5b64..0x082e5bac: the
      following push {r4,r5,r6,lr} at 0x082e5bb0 begins the next sibling.
      Every B/BL word in osos.dec was decoded: 8 direct calls, all plain
      unconditional bl (cond=e), at 0x080608a8, 0x08060958, 0x080609c0,
      0x080c8898, 0x080d3ce8, 0x080e36a8, 0x082e5798, and 0x08392ff8;
      zero predicated forms. The function saves incoming r3 in its stack
      byte, reads board_version(), waits semaphores 17 then 5, reads one
      PCF50635 byte through pmu_i2c_read, signals 5 then 17 unconditionally,
      and returns bit 0 for high board-version halfword 0x11 (register 0x4b)
      or bit 2 for every other version (register 0x12). It deliberately
      ignores the I2C status, so a failed transfer returns the selected bit
      of the saved incoming-r3 byte. The port explicitly declares the
      otherwise-undeclared incoming r0-r3 ABI words so r3 preserves that
      error-path behavior; its six direct callee edges resolve to the
      already-ported Rust functions. Host tests cover both board branches and
      the failed-write incoming-r3 result.
      match.py structural review against the worktree archive retains the
      board-version/read/5-then-17 release sequence; LLVM adds its frame and
      uses a conditional move plus variable shift instead of ARM predicates.

  # --- Mutexed PMU I2C register-block read (drivers/i2c) ---
  - address: 0x082e58f0
    ghidra: FUN_082e58f0
    name: pmu_i2c_read_regs
    size: 52
    sig: "i32 pmu_i2c_read_regs(u32 bank, u8 *buf)"
    module: drivers/i2c
    status: ported
    vars: {param_1: bank, param_2: buf, uVar1: status}
    notes: >-
      The mutexed I2C register-read entry feeding rtc_read_time. Two
      fixed-id RTXC kernel-semaphore waits bracket the transaction
      through 8-byte veneers onto the ROM semaphore services: bl
      0x0806a4b0 (mov r0, #0x11; b 0x08037e08 - rom_sem_wait(0x11),
      the outer lock) and bl 0x0806a4a0 (mov r0, #5; b 0x08037e08 -
      rom_sem_wait(5), the inner lock), then bl 0x0836d698(bank, buf),
      then the mirror signals bl 0x080645a8 (rom_sem_signal(5)) and bl
      0x08064604 (rom_sem_signal(0x11)) - both unconditional, no error
      path skips them - and the read status returns verbatim in r4.
      The same lock pair brackets every PMU I2C transaction of the
      0x082e5xxx family (~30 wait sites); the raw transfer functions
      take no lock themselves. 4 bl call sites (binary-verified):
      0x08054910 inside FUN_080548ec (seeds the PRNG state from the
      raw RTC bytes), 0x08056164 inside rtc_read_time, 0x080641e0
      inside FUN_0806418c (the struct-tm time getter; maps status 0x15
      apart from other errors), 0x0806e894 inside FUN_0806e7e4 (the
      time setter's read-back). PORT DEVIATIONS: the semaphore pairs
      are not ported (fixed-id veneers onto ROM 0x22003fd0/0x220042b4)
      - the port calls the same ROM services through
      kernel::sync_mutex::ROM_KERNEL (sema_wait/sema_signal, no-op
      default stubs); FUN_0836d698 (bank 0 -> reg block 0x59 time,
      bank 1 -> 0x60 alarm, else bad-bank 9) is now ported as
      drivers/i2c::pmu_i2c_read_bank and is the shipped default of the
      PMU_READ_REGS slot (the pre-port stub is kept for host tests).
      Its raw transfer FUN_0836d3b8 writes the register address to I2C
      slave 0x73, the PCF50635 PMU, through FUN_0836bb84 and reads
      seven bytes through FUN_0836b950; it is now ported as
      drivers/i2c::pmu_i2c_read, whose hardware primitives remain
      fixed-address target calls and volatile host-test seams.
      The port is the shipped default of time/rtc.rs's RTC_READ_REGS
      slot, replacing rtc_read_stub (kept for host tests). Constants:
      PMU_READ_BAD_BANK = 9. Host tests cover the lock order
      (wait outer, wait inner, read, signal inner, signal outer),
      bank/buf forwarding, status pass-through (0/9/0x15/-5), the
      unconditional unlock on read error, and the retained PMU_READ_REGS
      fail-closed stub. The raw transfer's own tests cover its write
      failure, signed-length, read-status, and no-transfer cases.
      match.py structural: same wait(0x11)/wait(5)/read/signal(5)/
      signal(0x11) sequence with the status kept in r4; the diff is
      blx-through-table for the five calls and LLVM's fp frame (the
      accepted deviations).

  # --- PMU I2C bank mux: RTC register-block read (drivers/i2c) ---
  - address: 0x0836d698
    ghidra: FUN_0836d698
    name: pmu_i2c_read_bank
    size: 40
    sig: "i32 pmu_i2c_read_bank(u32 bank, u8 *buf)"
    module: drivers/i2c
    status: ported
    vars: {param_1: bank, param_2: buf}
    notes: >-
      The bank mux under pmu_i2c_read_regs (its only bl callee, @
      0x082e590c): bank 0 selects the PCF50635 RTC time register block
      0x59 (cmp r0, #0 / moveq r0, #0x59), bank 1 the alarm block 0x60
      (cmp r0, #1 / mov r0, #0x60), any other bank returns the
      bad-bank code 9 (movne r0, #9 / bxne lr) with the buffer
      untouched; a valid bank tail-branches (b 0x0836d3b8) into the
      raw transfer FUN_0836d3b8(reg, 7, buf), which writes the
      register address to I2C slave 0x73 (the PCF50635 PMU) via
      FUN_0836bb84 and reads the 7 BCD bytes back via FUN_0836b950,
      returning its status verbatim. FUN_0836d3b8 is now ported as
      drivers/i2c::pmu_i2c_read: the Rust call replaces the retail tail
      branch, while its two raw S5L8702 primitive calls remain
      fixed-address target calls and volatile host-test seams. The port
      is the shipped default of i2c.rs's PMU_READ_REGS slot, replacing
      pmu_read_regs_stub (retained for host tests). Constants:
      PMU_RTC_TIME_BLOCK = 0x59, PMU_RTC_ALARM_BLOCK = 0x60,
      PMU_RTC_BLOCK_LEN = 7. Host tests cover the bank mux (0 -> 0x59,
      1 -> 0x60, 7 bytes, buf forwarded), the bad-bank fail-closed
      path (2/9/0xffffffff -> 9, no transfer, buffer untouched), and
      raw transfer write/read status behavior. match.py structural:
      same two-compare bank mux (0x59/0x60/movne #9 + conditional
      return) and a transfer call; the diff is LLVM's fp frame, the
      call instead of retail b 0x0836d3b8, and the reordered
      compare/select structure (the accepted deviations).

  # --- indexed payload lookup wrapper (app) ---
  - address: 0x0809e3d0
    ghidra: FUN_0809e3d0
    name: indexed_payload_lookup
    size: 24
    sig: "u32 indexed_payload_lookup(void *index, u32 entry, void **payload_out, u32 *encoded_length_out)"
    module: app/indexed_payload_lookup
    status: ported
    renames:
      param_1/r0: index
      param_2/r1: entry
      param_3/r2: payload_out
      param_4/r3: encoded_length_out
      r2_at_callee: zero_mode
      FUN_080d6d94: indexed_payload_lookup_backend (unported; concrete identity unknown)
    notes: >-
      Raw ARM establishes the exact 24-byte extent
      0x0809e3d0..0x0809e3e8: push {r3,lr}; str r3,[sp]; mov r3,r2;
      mov r2,#0; bl 0x080d6d94; pop {ip,pc}. The following push
      {r4-r8,lr} at 0x0809e3e8 starts bcd_datetime_to_days_secs, so
      there is no literal pool. It is a wrapper, not a veneer: forwards
      index and entry, shifts its third/fourth ABI arguments into the
      backend's fourth/fifth positions, forces the backend mode argument
      to zero, and returns the backend status unchanged. The backend's
      concrete identity is not established; its observed role is an
      indexed-payload lookup with optional payload/encoded-length outputs.
      Decoding every ARM B/BL word in osos.dec finds exactly 20 direct
      unconditional bl call sites and one unconditional tail b
      (0x08055afc), with zero predicated calls and no aligned or unaligned
      data-word reference to 0x0809e3d0. Host tests cover a nonzero
      backend error, u32::MAX entry, output mutation, exact mode-zero
      forcing, and both NULL optional outputs. On target the still-unported
      backend is invoked directly at 0x080d6d94; host builds substitute
      INDEXED_PAYLOAD_LOOKUP_BACKEND. match.py structural review requested.

  # --- BCD datetime -> (days, seconds-of-day) converter (time/civil) ---
  - address: 0x0809e3e8
    ghidra: FUN_0809e3e8
    name: bcd_datetime_to_days_secs
    size: 228
    sig: "void bcd_datetime_to_days_secs(i32 *out_pair, const u8 *buf)"
    module: time/civil
    status: ported
    vars: {param_1: out_pair, param_2: buf, iVar1: adj, iVar6: year}
    notes: >-
      Hinnant civil-to-days over a 7-byte BCD RTC register image (sec,
      min, hour, weekday, day, month, year; weekday ignored). BCD decode
      FUN_080ed424 (v - 6*(v>>4) for v < 0x9a, else 99) is now ported in
      its own right as time/civil::bcd_to_bin (see the 0x080ed424
      entry); the month byte is decoded
      TWICE in the original. adj = (14 - month) / 12 via UNSIGNED
      __rt_udiv (0x08036f14 — a month above 14 wraps to a huge
      quotient); y = year - adj + 0x1a90 (6800, a multiple of 400, so
      the 2-digit year's leap pattern is undisturbed); mp = month +
      12*adj - 3; days = 365*y + day + (153*mp + 2)/5 + y/4 - y/100 +
      y/400 - 0x7d2d (365 from the literal @ 0x0809e4cc via mla; /5,
      /100, /400 are signed truncating __rt_sdiv @ 0x08031568; /4 is
      the compiler's add-3-and-asr truncating idiom); secs = hour*0xe10
      + min*60 + sec (the *60 strength-reduced to (min*15) << 2).
      Returns void; days -> out[0], secs -> out[1]. 5 bl call sites:
      @ 0x08056174 (rtc_read_time, ported — now calls this port instead
      of its inline reproduction), @ 0x080641f0 (FUN_0806418c),
      @ 0x0806e850 / 0x0806e8a4 / 0x0806e8b0 (FUN_0806e7e4). The port
      calls the ported __rt_udiv/__rt_sdiv (inline(never)) so the bl
      boundaries survive. match.py: same structure — LLVM re-schedules
      the 365*y and /4 terms earlier and builds 365 as 0x16d via
      mov/mvn+orr instead of the pool literal, but every formula
      element is present (rsb #14, +0x1a90, mla 0x99, the /4
       asr/lsr/asr idiom, -0x7d2d, 0xe10, rsb *60, seven decode calls,
       one udiv, three sdivs).

  # --- Binary -> packed BCD helper (time/civil) ---
  - address: 0x080e88a4
    ghidra: FUN_080e88a4
    name: bin_to_bcd_clamped
    size: 44
    sig: "u32 bin_to_bcd_clamped(u32 value)"
    module: time/civil
    status: ported
    renames:
      param_1/r0/r4: value
      iVar1/r0: quotient
      FUN_08036f14: __rt_udiv
    notes: >-
      Raw ARM extent is exactly 44 bytes, 0x080e88a4..0x080e88d0: it
      clamps unsigned values above 99 to 0x99, otherwise calls
      __rt_udiv(value, 10) and returns value + 6*quotient, yielding packed
      BCD. Decoding every ARM B/BL word in osos.dec finds exactly twelve
      direct unconditional bl call sites (@ 0x0806e7fc, 0x0806e808,
      0x0806e814, 0x0806e820, 0x0806e82c, 0x0806e838, 0x0809e870,
      0x0809e87c, 0x0809e888, 0x0809e8a4, 0x0809e8c8, 0x0809e8d8), with
      no predicated calls. The Rust port preserves the division call to the
      existing __rt_udiv port; no deviations. Host tests exhaustively cover
      0..=99 plus the 100/101 and u32::MAX clamp edges. match.py structural
      review requested.
  # --- BCD byte -> binary decode helper (time/civil) ---
  - address: 0x080ed424
    ghidra: FUN_080ed424
    name: bcd_to_bin
    size: 24
    sig: "i32 bcd_to_bin(u8 v)"
    module: time/civil
    status: ported
    vars: {param_1: v}
    notes: >-
      BCD byte to binary: v - 6*(v>>4) for v <= 0x99 (unsigned cmp
      r0, #0x99; r1 = v>>4, r1 = r1 - r1*4 = -3*(v>>4), r0 = v +
      r1*2 = v - 6*(v>>4), all under the ls predicate), else clamped
      to 99 (movhi r0, #0x63). The clamp tests the RAW byte, so
      0x9a-0xff all return 99 while a low-nibble-above-9 byte like
      0x0f still decodes arithmetically (15). 13 bl call sites in
      osos.asm (grep -c "bl 0x080ed424"): the seven decode calls in
      bcd_datetime_to_days_secs @ 0x0809e3e8 (whose inline stand-in
      this port replaces, call sites unchanged, still out-of-line)
      plus six more elsewhere (e.g. four in FUN_0806418c @
      0x0806423c/0x0806424c/0x08064258/0x08064264). Host tests cover
      the full u8 range 0x00-0xff against the reference formula
      (valid BCD pairs, the 0x99/0x9a boundary, the >= 0x9a clamp,
      and low-nibble-above-9 arithmetic decodes). match.py
      structural: identical computation (lsr #4; sub r1, r1, r1 lsl
      #2; add r0, r0, r1 lsl #1; select 99 vs the computed value);
      the diff is LLVM's fp frame and cmp #0x9a + addcc/mov select in
      place of the original's cmp #0x99 with ls/hi predication —
      semantically identical boundary.

  # --- Block-deque element -> region accessors (heap/block_region) ---
  - address: 0x082801f8
    ghidra: FUN_082801f8
    name: region_ref_lock
    size: 20
    sig: "u32 region_ref_lock(const void *elem)"
    module: heap/block_region
    status: ported
    notes: >-
      Loads the deque element's region object (elem + 0x4); a NULL region
      returns 0 without touching a mutex, otherwise tail-branches to the
      C++ mutex lock @ 0x082e8390 (alias thunk @ 0x082621a8) on the
      region's mutex (region + 0x8). 14 bl call sites. The mutex pair is
      the REGION_MUTEX_OPS dispatch boundary, whose slots default to the
      real ports (kernel/posix_mutex: posix_mutex_lock / _unlock).

  - address: 0x082802cc
    ghidra: FUN_082802cc
    name: region_ref_unlock
    size: 20
    sig: "u32 region_ref_unlock(const void *elem)"
    module: heap/block_region
    status: ported
    notes: >-
      Unlock twin of region_ref_lock; tail-branches to the C++ mutex
      unlock @ 0x082e83d8 (thunk @ 0x082621ac). 10 bl + 1 tail b sites.

  - address: 0x08280464
    ghidra: FUN_08280464
    name: region_elem_copy_construct
    size: 80
    sig: "void *region_elem_copy_construct(void *dst, const void *src)"
    module: heap/block_region
    status: ported
    renames:
      param_1: dst
      param_2: src
      DAT_082804b4: region_elem_vtable
    notes: >-
      80 bytes: 76 bytes of instructions (0x08280464..0x082804b0) plus
      vtable literal 0x089a6444 at 0x082804b4; the sibling constructor
      0x082804b8 follows immediately, binary-verified against osos.dec.
      A full ARM B/BL decode finds 15 bl call sites and no tail b sites:
      11 unconditional, four blne (0x0814b988, 0x081a85a8, 0x081fc39c,
      0x083e9f14). The predicated callers provide the NULL guard; the
      callee dereferences both dst and src unconditionally. It plants the
      element vtable, constructs the embedded recursive mutex via
      0x082621b0, locks src's region, copies src +0x4 and +0x8 to dst,
      increments a non-NULL region's u32 refcount, unlocks src, and returns
      dst. The Rust port calls the unported 0x082621b0 body directly on
      device; its host model composes cxx_mutexattr_init with the decoded
      kind-2 settype and PosixMutex initializer, then clears the transient
      attribute magic as pthread_mutexattr_destroy does. Element members use
      word indices so host pointer fields and the mutex do not overlap.
      Host tests cover live-region copy with u32 refcount wrap, recursive
      mutex state, lock/copy/retain/unlock order, and NULL-region copy with
      no mutex traffic. match.py structural review (exit 1, diff expected):
      same vtable plant, mutex ctor, source lock -> two-word copy -> guarded
      refcount increment -> source unlock sequence; LLVM uses its frame
      pointer, `blx` for the original mutex-ctor address, and distinct Rust
      lock/unlock calls in place of the retail direct bl instructions.

  - address: 0x082804b8
    ghidra: FUN_082804b8
    name: region_elem_default_construct
    size: 40
    sig: "void *region_elem_default_construct(void *dst)"
    module: heap/block_region
    status: ported
    renames:
      param_1: dst
    notes: >-
      Binary-verified extent: 36 bytes of instructions
      (0x082804b8..0x082804dc) plus vtable literal 0x089a6444 at
      0x082804e0; the next sibling begins at 0x082804e4. Full ARM B/BL
      decoding finds 13 unconditional bl call sites and no predicated or
      tail-b sites. The constructor has no NULL guard: it stores the raw
      vtable address, clears dst +0x4 and +0x8, invokes the recursive-mutex
      constructor @ 0x082621b0 on dst +0xc, and returns dst. The device port
      calls that unported constructor directly; the host model composes its
      ported attribute/settype/PosixMutex dependencies, enabling the test to
      prove stale pointer/scalar references are cleared and the resulting
      mutex is live, recursive, and status-zero. Pointer fields use word
      indices, with a host-width NULL write so the target's 32-bit clear
      remains a real NULL pointer in 64-bit fixtures. match.py structural
      review is expected to differ: LLVM emits the helper call path rather
      than the retail direct bl and target builds use a function-address
      call for the unported mutex constructor.

  - address: 0x08280430
    ghidra: FUN_08280430
    name: block_to_region_start
    size: 48
    sig: "void *block_to_region_start(const void *elem)"
    module: heap/block_region
    status: ported
    notes: >-
      Locks the element's region, reads the region start (region + 0x4),
      substitutes the fallback address 0x089cb1b8 (the word after the
      block-manager global) when the region or its start is NULL, then
      unlocks and returns the start. 9 bl call sites, binary-verified —
      osos.asm drops one. Now the POOL_OPS.region_start default: the
      address a seeded pool heap block begins at. Host note — pointer
      fields are addressed by word index, not literal target byte
      offset, so the +0x4/+0x8 pair does not overlap on 64-bit hosts.

  - address: 0x082804fc
    ghidra: FUN_082804fc
    name: region_elem_destroy
    size: 48
    sig: "void *region_elem_destroy(void *elem)"
    module: heap/block_region
    status: ported
    vars: {param_1: elem}
    notes: >-
      The 0x28-byte block-deque element's C++ destructor (48 bytes +
      one literal-pool word @ 0x0828052c; next function @ 0x08280530,
      extent binary-verified). Plants the class vtable (literal
      0x089a6444) at elem + 0x0, locks the element's region via the
      ported region_ref_lock (0x082801f8, direct call), releases the
      region reference @ 0x082803c0, destroys the recursive-mutex
      member at elem + 0xc through the dtor thunk @ 0x082621dc (->
      mutex destroy @ 0x082e82a4), and returns this as
      member_result - 0xc (the closing `sub r0, r0, #12`). 23 bl +
      1 tail b (0x0814b830) call sites, binary-verified by decoding
      every B/BL word in osos.dec — no predicated forms; callers
      include the deleting destructor @ 0x082804e4, client_populate's
      original (0x081fc354/0x081fc3bc) and the deque teardown
      (0x083dcd9c). Deviations: the two unported callees dispatch
      through the new REGION_ELEM_OPS slots (house pattern, blx for
      bl) — region_release defaults to a documented no-op stub (exact
      for the NULL-region elements the wired configuration can hold;
      a live-region element would need the real 0x082803c0 port —
      refcount decrement, mutex unlock, tag-43 free of region + 0x4,
      operator_delete of the region, elem + 0x4/+0x8 NULLed),
      member_destroy to an identity stub mirroring the thunk's own
      `mov r0, r4` (the mutex member is opaque, pool_client.rs's
      stub_mutex_init precedent). The vtable words at 0x089a6444
      (0, 0x082900b8, 0x0828cfa4, 0) do NOT decode as function
      entries — both pointers land mid-instruction-stream — so the
      port stores the raw ROM address (ui/view_base.rs precedent) and
      never dispatches through it; the anomaly is recorded, no slot
      identities invented. client_populate.rs's region_destroy slot
      now defaults to this port through a ()-returning adapter (the
      real-ports-become-defaults precedent). match.py structural:
      same vtable store, same guarded region lock (inlined
      region_ref_lock — its read_unaligned mutex-field load shows as
      ldrb x4, the module's documented host-fixture idiom), blx slots
      for the two unported callees, same `sub r0, r0, #12` return.
      LLVM did not fold the body; the symbol resolves in
      librustypod.a next to region_ref_lock.

  - address: 0x082805b8
    ghidra: FUN_082805b8
    name: region_ref_is_empty
    size: 68
    sig: "u32 region_ref_is_empty(const void *elem, u32 skip_empty_check)"
    module: heap/block_region
    status: ported
    renames:
      param_1: elem
      param_2: skip_empty_check
      iVar1: region
      iVar2: region_start
      uVar3: is_empty
    notes: >-
      Binary-verified extent: 68 bytes of code at 0x082805b8..0x082805f8;
      the next sibling begins at 0x082805fc. Full ARM B/BL decoding of
      osos.dec finds eight unconditional bl call sites
      (0x08119d7c, 0x08167d74, 0x0818ab58, 0x081a7d90, 0x081a82e4,
      0x081fc1b8, 0x08235cf8, 0x08280610), no predicated forms, and no
      tail b sites. It locks elem's region reference, then returns one only
      when skip_empty_check is zero and either elem +0x4 or the live
      region's +0x4 start pointer is NULL; it always unlocks before return.
      The Rust port uses the existing lock/unlock ports and word-index
      pointer fields, preserving 32-bit target offsets without host-field
      overlap. Host tests cover NULL region (no mutex traffic), NULL start
      (lock/unlock bracket), live start, and the nonzero bypass flag (still
      brackets with lock/unlock). match.py structural review is expected to
      differ because LLVM emits calls to the Rust lock/unlock ports rather
      than the retail direct bl instructions.

  # --- byte-order swap cluster (0x0805dc24..0x0805dd88) ---
  - address: 0x0805dc24
    ghidra: FUN_0805dc24
    name: bswap32
    size: 48
    sig: "u32 bswap32(u32 value)"
    module: util/bswap
    status: ported
    vars: {param_1: value}
    notes: >-
      Byte-reverse of a 32-bit value via a stack spill/reload (store word,
      re-read 4 bytes reversed, ldr back). 166 call sites, binary-scanned
      b/bl words. The htonl/ntohl of retailOS's big-endian format parsers.

  - address: 0x0805dd48
    ghidra: FUN_0805dd48
    name: bswap16
    size: 32
    sig: "u32 bswap16(u32 value)"
    module: util/bswap
    status: ported
    vars: {param_1: value}
    notes: >-
      Byte-swap of the low 16 bits, zero-extended by the ldrh reload
      (the argument's top half is discarded). 133 call sites.

  - address: 0x0805dd10
    ghidra: FUN_0805dd10
    name: bswap32_inplace
    size: 56
    sig: "void bswap32_inplace(u8 *ptr)"
    module: util/bswap
    status: ported
    vars: {param_1: ptr}
    notes: >-
      Reverses the 4 bytes at ptr in place. Original reads two ldrh
      halfwords (so ptr must be 2-byte aligned on ARMv5); the port reads
      byte-wise, identical for every pointer the original supported.
      77 call sites.

  - address: 0x0805dd68
    ghidra: FUN_0805dd68
    name: bswap16_inplace
    size: 36
    sig: "void bswap16_inplace(u8 *ptr)"
    module: util/bswap
    status: ported
    vars: {param_1: ptr}
    notes: Swaps the 2 bytes at ptr in place via one stack byte. 27 call sites.

  - address: 0x0805cfb4
    ghidra: FUN_0805cfb4
    name: bzero
    size: 76
    sig: "void bzero(u8 *dst, i32 len)"
    module: libc/bzero
    status: ported
    vars: {param_1: dst, param_2: len}
    notes: >-
      Third zero-fill in the image, independent of the ADS runtime pair
      0x0800027c/0x080002d4 (libc/memzero). Signed length with len<0
      early-out; len>0x10 aligns dst byte-wise then word-fills while
      len>=4; remainder byte-filled (subs/bpl, exactly len bytes).
      len<=16 goes straight to the byte loop. 55 call sites,
      binary-scanned. Port uses write_volatile against memset idiom
      recognition; LLVM unrolls the 3-step alignment prologue.

  - address: 0x080a63e4
    ghidra: FUN_080a63e4
    name: zero_unwritten_suffix
    size: 20
    sig: "void zero_unwritten_suffix(u8 *dst, u32 total, u32 initialized)"
    module: libc/zero_unwritten_suffix
    status: ported
    vars: {param_1: dst, param_2: total, param_3: initialized}
    notes: >-
      Raw osos.dec confirms five ARM instructions from 0x080a63e4 through
      0x080a63f4; the next separately entered function begins at 0x080a63f8.
      If initialized is less than total by an unsigned comparison, advances
      dst by initialized and tail-branches to bzero (0x0805cfb4) for
      total-initialized bytes; otherwise it returns without touching memory.
      There are exactly 11 direct plain bl call sites
      (0x08063b9c, 0x08063d48, 0x080946b8, 0x08094764, 0x080949bc,
      0x08094dfc, 0x08094fb8, 0x0809510c, 0x080953c8, 0x08095488,
      0x08095b84), verified by decoding every ARM B/BL word in osos.dec;
      no predicated forms or direct tail-B sites target it. Calls construct
      variable-size on-stack records and clear the tail after their initialized
      prefix. The Rust port reaches the existing bzero port through a volatile
      function-pointer load so LLVM cannot inline the callee and remove this
      device hook target; unlike stock's direct tail branch. A difference with
      bit 31 set remains a no-op because bzero's signed-length guard returns
      first.


  # --- FreeType 2 character-map API ---
  - address: 0x0804c4d0
    ghidra: FUN_0804c4d0
    name: ft_get_char_index
    size: 48
    sig: "u32 ft_get_char_index(FtFace *face, u32 charcode)"
    module: ft/glyph_slot
    status: ported
    vars:
      param_1: face
      param_2: charcode
      iVar1: charmap
      uVar2: result
    notes: >-
      FreeType 2.3.0 FT_Get_Char_Index (src/base/ftobjs.c). The retail
      ARM body has the identical source shape: cmp face,#0; conditionally
      loads face->charmap at +0x5c; returns zero on either null; otherwise
      casts its FT_CharMapRec prefix to FT_CMapRec, loads cmap->clazz at
      +0x0c and clazz->char_index at +0x0c, then blx. Direct callers at
      0x080770d4/0x080770e4 put a byte character code in r1 before the
      call, confirming the decompiler-missed FT_ULong charcode argument.
      FT_CMap_ClassRec's third callback is char_index, so no callback-null
      gate exists in the original or port. Host tests cover a null face,
      absent selected charmap, and callback receiver, character code, and
      return propagation.

  # --- FreeType 2 module API ---
  - address: 0x0804c500
    ghidra: FUN_0804c500
    name: ft_get_module
    size: 96
    sig: "FtModule *ft_get_module(FtLibrary *library, const char *module_name)"
    module: ft/module
    status: ported
    vars:
      param_1: library
      param_2: module_name
      iVar1: strcmp_result
      puVar2: module_cursor
      puVar3: module_limit
    notes: >-
      FreeType 2.3.0 FT_Get_Module (src/base/ftobjs.c). Returns NULL for a
      null library or module name; otherwise scans exactly
      library->num_modules entries from the module table (+0x1c, count at
      +0x18) and returns the first whose clazz->module_name equals the
      requested NUL-terminated name. The 96-byte ARM routine delegates each
      comparison to the resident strcmp at 0x08391e38. Named callers request
      "truetype" and "pshinter"; 0x0804c560 is the matching
      FT_Get_Module_Interface wrapper.
  - address: 0x0804c560
    ghidra: FUN_0804c560
    name: ft_get_module_interface
    size: 24
    sig: "const void *ft_get_module_interface(FtLibrary *library, const char *module_name)"
    module: ft/module
    status: ported
    vars:
      param_1: library
      param_2: module_name
      piVar1: module
      uVar2: module_interface
    notes: >-
      FreeType 2.3.0 FT_Get_Module_Interface (src/base/ftobjs.c).
      FUN_0804c500 is the unported FT_Get_Module: it consumes r0/r1, and
      0x0804c560 preserves both registers through its bl before returning
      NULL on a null module or module->clazz->module_interface. The ARM
      body is push; bl 0x0804c500; cmp; ldrne [module]; ldrne [clazz,#0x14];
      pop. The target port calls the resident lookup at 0x0804c500; host
      tests inject it and cover lookup failure plus argument forwarding and
      the nested class-interface read.
  - address: 0x0804c578
    ghidra: FUN_0804c578
    name: ft_get_next_char
    size: 80
    sig: "u32 ft_get_next_char(FtFace *face, u32 charcode, u32 *agindex)"
    module: ft/charmap
    status: ported
    vars:
      param_1: face
      param_2: charcode
      param_3: agindex
      iVar1: glyph_index
      iVar3: charmap
      local_10: code
      uVar2: result
    notes: >-
      FreeType 2.3.0 FT_Get_Next_Char (src/base/ftobjs.c). The ARM body
      loads face->charmap at +0x5c, its private FT_CMapRec class at +0x0c,
      and FT_CMap_ClassRec::char_next at +0x10, then invokes it with the
      mutable 32-bit character code. It writes the callback's glyph index
      to agindex when supplied and returns the resulting code only for a
      nonzero glyph index. The upstream FT_Get_First_Char calls this API
      after an unmapped character zero. Host tests cover both null gates,
      callback receiver and input, mutated-code propagation, and zero-index
      suppression.
  # --- FreeType 2 ftcalc fixed-point kernels ---
  - address: 0x0804d2cc
    ghidra: FUN_0804d2cc
    name: ft_mulfix
    size: 124
    sig: "i32 ft_mulfix(i32 a, i32 b)"
    module: ft/calc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      FreeType FT_MulFix (2.1.x pre-FT_Int64 ARM build), 16.16 multiply
      rounded half-up on magnitude. Identity fast-out returns a when
      a==0 or b==0x10000. Abs via (x^(x>>31))-(x>>31) so i32::MIN stays
      0x80000000. Single 32-bit mul when abs(a)<=0x800 and
      abs(b)<=0x100000 (max intermediate 0x80008000, never wraps);
      else three partial products, all 32-bit wrapping (big products
      truncate, preserved). 116 call sites, binary-scanned.

  - address: 0x0804c2d4
    ghidra: FUN_0804c2d4
    name: ft_divfix
    size: 140
    sig: "i32 ft_divfix(i32 a, i32 b)"
    module: ft/calc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      FreeType FT_DivFix. (a<<16)/b with abs(b)/2 rounding bias; b==0
      gives 0x7fffffff (0x80000001 when a<0). abs(a)<0x10000 takes one
      __rt_udiv (0x08036f14) of (ua<<16)+(ub asr 1) - that add wraps at
      2^32, preserved; else 64-bit path with hi = ua asr 16
      (arithmetic - a==i32::MIN gives hi 0xffff8000, forcing the
      clamp), FT_Add64 (0x080ed3b4) bias add, ft_div64by32 (0x0807c5b8)
      with 0x7fffffff clamp when hi>=ub. Port inlines __rt_udiv (u32
      division) and FT_Add64 (carry add) bit-identically and calls the
      real ported ft_div64by32. 56 call sites, binary-scanned.

  - address: 0x0804d1a8
    ghidra: FUN_0804d1a8
    name: ft_muldiv
    size: 288
    sig: "i32 ft_muldiv(i32 a, i32 b, i32 c)"
    module: ft/calc
    status: ported
    vars: {param_1: a, param_2: b, param_3: c}
    notes: >-
      FreeType FT_MulDiv (32-bit build). a*b/c with abs(c)/2 rounding
      bias on the magnitude; identity fast-out when a==0 or b==c; sign
      is (a^b^c)>>31, magnitudes via wrapping rsblt. Bound checks are
      SIGNED on the wrapped magnitudes (upstream FT_ABS quirk, so
      abs==0x80000000 passes the small test) - abs(a),abs(b)<=46340
      (literal @ 0x0804d2c8) and 0<abs(c)<176096 takes one mul plus
      __rt_sdiv (0x08031568); both small with abs(c)>=176096, or either
      large with abs(c)>0, takes inlined ft_multo64 (exact 64-bit
      product), FT_Add64 (0x080ed3b4) bias add, ft_div64by32
      (0x0807c5b8) with its 0x7fffffff hi>=c clamp; else 0x7fffffff.
      Quotient truncated to u32 then sign-restored with wrapping rsb,
      so unclamped quotients in 2^31..2^32 come back sign-flipped
      (stock overflow behavior, preserved). Port inlines ft_multo64 and
      FT_Add64 bit-identically (one u64 mul+add) and calls the real
      ported ft_div64by32. 33 call sites, binary-scanned.

  - address: 0x0804d0dc
    ghidra: FUN_0804d0dc
    name: ft_matrix_multiply
    size: 160
    sig: "void ft_matrix_multiply(const FtMatrix *a, FtMatrix *b)"
    module: ft/calc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      FreeType FT_Matrix_Multiply - b = a*b in 16.16, each cell the
      wrapping sum of two ft_mulfix (0x0804d2cc) products. Null a or b
      is a no-op. All eight products complete before the first store
      (stmia at the end), so a==b aliasing reads pre-multiply values -
      port snapshots both matrices up front. FtMatrix layout
      xx/xy/yx/yy @ +0/+4/+8/+12 (ft/types). 1 call site,
      binary-scanned.

  - address: 0x0804d054
    ghidra: FUN_0804d054
    name: ft_matrix_invert
    size: 136
    sig: "i32 ft_matrix_invert(FtMatrix *matrix)"
    module: ft/calc
    status: ported
    vars: {param_1: matrix}
    notes: >-
      FreeType FT_Matrix_Invert - in-place 16.16 inverse. delta =
      mulfix(xx,yy) - mulfix(xy,yx) (wrapping sub); null matrix or
      delta==0 returns 6 (FT_Err_Invalid_Argument) untouched. Else
      xy=-divfix(xy,delta), yx=-divfix(yx,delta) (wrapping rsb), xx_old
      stashed, xx=divfix(yy,delta), yy=divfix(xx_old,delta); returns 0.
      1 call site, binary-scanned.

  # --- FreeType 2 cffgload glyph-outline builder ---
  - address: 0x080bfd14
    ghidra: FUN_080bfd14
    name: cff_builder_add_point
    size: 100
    sig: "void cff_builder_add_point(CffBuilder *builder, i32 x, i32 y, i32 on_curve)"
    module: ft/cff_builder
    status: ported
    vars: {param_1: builder, param_2: x, param_3: y, param_4: on_curve}
    notes: >-
      FreeType cff_builder_add_point (cffgload.c) - the point recorder the
      Type 2 charstring interpreter cff_parse_charstrings (unported @
      0x080df818; scaled siblings 0x080ea434/0x080ea5a0/0x080eade0 share
      the LAB_080e0928/LAB_080e0c48 tails) feeds per decoded point:
      rrcurveto triplets pass on_curve=0 for the two cubic control points
      and 1 for the endpoint. Body decodes verbatim from raw ARM (extent
      0x080bfd14..0x080bfd78 confirmed by the next function's push
      prologue): outline = builder->current @ +0x14; if load_points byte
      @ +0x51 is set, store x>>16/y>>16 (ARITHMETIC shifts = floor for
      negatives) at points[n_points] (n_points sign-extended via ldrsh,
      so a negative count indexes backwards), tag = on_curve ?
      FT_CURVE_TAG_ON(1) : FT_CURVE_TAG_CUBIC(2) at tags[n_points],
      builder->last @ +0x18/+0x1c = the truncated point (stored, then
      reloaded via ldm); n_points @ outline+2 is incremented
      (ldrh/add/strh, 16-bit wrap) OUTSIDE the load_points guard, so the
      counting pass still advances it. 36 call sites verified by decoding
      every B/BL word in osos.dec: 35 bl + ONE bleq @ 0x080c8a54 (wrapper
      0x080c8a28 calls it with on_curve=1 only when the 0x0807ba0c
      point-capacity check returns 0 - a gated call, not a missing NULL
      guard; this function itself has no NULL guards). No DATA word holds
      the address: not virtually dispatched. Host tests (8) prove floor
      truncation of 16.16 inputs, tag selection, the counting-pass
      no-store path, nonzero and NEGATIVE n_points indexing, i16 wrap of
      the count, and that no other builder/outline bytes move. match.py
      differs only in the crate frame-pointer prologue and register
      allocation.

  - address: 0x0807ba0c
    ghidra: FUN_0807ba0c
    name: cff_check_points
    size: 52
    sig: "i32 cff_check_points(CffBuilder *builder, i32 count)"
    module: ft/cff_builder
    status: ported
    vars: {param_1: builder, param_2: count}
    notes: >-
      FreeType cffgload.c check_points - the Type 2 charstring
      interpreter's "will count more points fit?" gate, i.e.
      FT_GlyphLoader_CheckPoints(builder->loader, count, 0) with the
      loader head partially inlined by the ADS compiler. Extent is
      EXACTLY Ghidra's 52 bytes (0x0807ba0c..0x0807ba40): the next
      function starts at 0x0807ba40 with `mov r3,r0 / push {lr}`. Body
      verified against raw ARM: `cmp r1,#0 / beq ret` (count==0 returns
      0 BEFORE builder is dereferenced - a NULL builder with count 0 is
      safe), `ldr r0,[r0,#0xc]` (builder->loader), `ldrsh` of
      base.outline.n_points @ loader+0x16 and current.outline.n_points
      @ +0x36 (sign-extended: negative counts SUBTRACT), wrapping adds
      with count, then a SIGNED `cmp/bgt` against max_points @ +0x04
      (equal-to-capacity FITS). On overflow: `movgt r2,#0` and
      tail-branch the out-of-line FT_GlyphLoader_CheckPoints @
      0x0804c638 (unported), whose return value becomes ours. 13 call
      sites verified by decoding every B/BL word in osos.dec: all
      unconditional `bl` @ 0x080c8a3c, 0x080e0170, 0x080e0218,
      0x080e0354, 0x080e0448, 0x080e0528, 0x080e0614, 0x080e0794,
      0x080e088c, 0x080e0968, 0x080e0a60, 0x080e0b90, 0x080e0c94 (the
      cff_parse_charstrings interpreter and its scaled siblings); no `b`
      tails, no DATA word holds the address - not virtually dispatched.
      The unported grow path sits behind the GLYPH_LOADER_CHECK_POINTS
      seam (observable_array precedent), wired to 0x0804c638 on target
      and a panicking default on host. match.py structural: same head
      (count guard, loader deref, ldrsh pair, adds, signed cmp); LLVM
      adds the crate frame-pointer prologue, inverts the branch, and
      resolves the seam through a literal-pool static load into an
      indirect `bx` tail where the original direct-branches 0x0804c638.
      CffBuilder gained the named `loader` field @ +0x0c (was inside
      the reserved head words). Host tests (7) prove the count==0 early
      return on a NULL builder, the fits/exactly-at-capacity paths,
      sign-extension of both n_points (a u16 reader would wrongly
      grow), negative-count and negative-current-count subtraction, and
      that overflow calls the seam with (loader, count, 0) and
      propagates its return.

  - address: 0x080cca60
    ghidra: FUN_080cca60
    name: cff_builder_start_point
    size: 76
    sig: "i32 cff_builder_start_point(CffBuilder *builder, i32 x, i32 y)"
    module: ft/cff_builder
    status: ported
    renames:
      param_1/r0/r4: builder
      param_2/r1/r5: x
      param_3/r2/r6: y
      "builder+0x50": path_begun
      FUN_080cc9d8: cff_builder_add_contour (unported seam)
      FUN_080c8a28: cff_builder_add_point_on_curve (inlined wrapper)
    notes: >-
      FreeType cffgload.c cff_builder_start_point. Raw ARM extent is
      0x080cca60..0x080ccaac (76 bytes): the next function starts at
      0x080ccaac with ldr r0,[r0,#0x8c]. It returns 0 immediately if the
      builder's path_begun byte at +0x50 is nonzero. Otherwise it sets that
      byte to 1, calls the unported cff_builder_add_contour @ 0x080cc9d8,
      and returns that error unchanged when nonzero. On success it restores
      builder/x/y and tail-branches to 0x080c8a28. Raw decoding verifies
      that wrapper runs cff_check_points(builder,1) and, only on success,
      cff_builder_add_point(builder,x,y,1); the Rust port expresses that
      wrapper directly while retaining 0x080cc9d8 as a target direct-call
      seam. CffBuilder gained the named path_begun field at +0x50 (the
      existing load_points remains +0x51). 12 call sites verified by
      decoding every B/BL word in osos.dec: all unconditional bl at
      0x080e0158, 0x080e0204, 0x080e033c, 0x080e040c, 0x080e04ec,
      0x080e05d8, 0x080e0780, 0x080e0874, 0x080e0954, 0x080e0a4c,
      0x080e0b7c, and 0x080e0c80; no tail B or DATA word holds the address,
      so it is not virtually dispatched. Host tests (4) prove the begun
      early return, contour-error propagation, successful on-curve 16.16
      point append, and capacity-error no-append behavior.

  # --- FreeType 2 ftoutln outline helpers ---
  - address: 0x0804fe08
    ghidra: FUN_0804fe08
    name: ft_vector_transform
    size: 96
    sig: "void ft_vector_transform(FtVector *vector, const FtMatrix *matrix)"
    module: ft/outline
    status: ported
    vars: {param_1: vector, param_2: matrix}
    notes: >-
      FreeType FT_Vector_Transform - v = matrix*v in 16.16 via four
      ft_mulfix (0x0804d2cc) products with wrapping adds; both results
      computed before either store. Null vector or matrix is a no-op.
      13 call sites, binary-scanned.

  - address: 0x0804e0cc
    ghidra: FUN_0804e0cc
    name: ft_outline_transform
    size: 64
    sig: "void ft_outline_transform(const FtOutline *outline, const FtMatrix *matrix)"
    module: ft/outline
    status: ported
    vars: {param_1: outline, param_2: matrix}
    notes: >-
      FreeType FT_Outline_Transform - ft_vector_transform (0x0804fe08)
      on points..points+n_points. n_points sign-extended (ldrsh) and
      the limit compare is unsigned (bcc), so negative n_points wraps
      the limit below points and the loop runs zero times (preserved
      via wrapping usize pointer math). Null outline or matrix is a
      no-op. 4 call sites, binary-scanned.

  - address: 0x0804e10c
    ghidra: FUN_0804e10c
    name: ft_outline_translate
    size: 72
    sig: "void ft_outline_translate(const FtOutline *outline, i32 x_offset, i32 y_offset)"
    module: ft/outline
    status: ported
    vars: {param_1: outline, param_2: x_offset, param_3: y_offset}
    notes: >-
      FreeType FT_Outline_Translate - wrapping-adds the offsets to
      every point, index counted 0..n_points with a signed compare (so
      n_points<=0 is a no-op). The original's bic 0x10000 FT_UShort
      counter mask can never fire (n_points is ldrsh, max 32767) and is
      omitted. Deviation: the original loads outline->points before its
      null check (reads NULL+4 when outline==0); port checks first -
      identical behavior for all non-null outlines. 9 call sites,
      binary-scanned.

  - address: 0x0804deb4
    ghidra: FUN_0804deb4
    name: ft_outline_get_cbox
    size: 136
    sig: "void ft_outline_get_cbox(const FtOutline *outline, FtBBox *acbox)"
    module: ft/outline
    status: ported
    vars: {param_1: outline, param_2: acbox}
    notes: >-
      FreeType FT_Outline_Get_CBox - signed min/max of x and y over all
      points, seeded from points[0], scan from points[1] with the same
      unsigned wrapped-limit compare as ft_outline_transform (negative
      n_points still reads points[0] and returns it as all four
      extremes - preserved). n_points==0 yields the zero box. Null
      outline or acbox is a no-op. BBox layout
      x_min/y_min/x_max/y_max @ +0/+4/+8/+12. 8 call sites,
      binary-scanned.

  # --- FreeType 2: identified, not yet ported ---
  # Version evidence: license blob @ 0x087fc3cc says "copyright
  # 2000-2006, 2007 The FreeType Project" (a 2.3-era tree); sources
  # compiled from "...\Firmware\Silver\3rdParty\freetype\src\..." (path
  # strings for autofit, base, bdf, pfr, psaux, sfnt, truetype, type1,
  # cff modules survive). Debug/assert build: FT_Stream_* trace strings
  # @ 0x0804f43c..0x0804fd50 plus "assertion failed on line %d" FT_ASSERT
  # strings are live in the stream readers.
  - address: 0x080ed3b4
    ghidra: FUN_080ed3b4
    name: ft_add64
    size: 68
    sig: "void ft_add64(const FtInt64 *x, const FtInt64 *y, FtInt64 *z)"
    module: ft/calc
    status: ported
    vars: {param_1: x, param_2: y, param_3: z}
    notes: >-
      FreeType FT_Add64 (ftcalc.c) - 64-bit add of two {lo,hi} FT_Int64
      structs through pointers. lo = x.lo+y.lo wrapping, hi =
      x.hi+y.hi+(lo<x.lo); the original spells the carry test as lo <
      max(x.lo,y.lo) (movhi/movls then cmp), same predicate. Both
      halves are register-computed before either store, so z may alias
      x or y (tested). Size is 68 bytes per functions.csv, not the 40
      of the earlier scouting note. 2 bl callers, both in ported code
      (ft_divfix @ 0x0804c33c, ft_muldiv @ 0x0804d29c) which keep
      inlining it bit-identically. FtInt64 layout lo/hi @ +0/+4
      (ft/types). Port lowers to adds/adc.

  - address: 0x0807c5b8
    ghidra: FUN_0807c5b8
    name: ft_div64by32
    size: 64
    sig: "u32 ft_div64by32(u32 hi, u32 lo, u32 divisor)"
    module: ft/calc
    status: ported
    vars: {param_1: hi, param_2: lo, param_3: divisor, iVar1: remainder, uVar2: quotient}
    notes: >-
      FreeType ft_div64by32 (ftcalc.c, static) - 32 rounds of restoring
      division of the 64-bit (hi:lo) by divisor. Out-of-range results
      short-circuit to 0x7fffffff via "cmp r0,r2 / mvncs r0,#0x80000000
      / bxcs lr"; because the compare is unsigned this also swallows
      divisor == 0, so there is no divide-by-zero path at all. The loop
      is r = (r<<1)|(lo>>31); q <<= 1; if (r >= divisor) { r -= divisor;
      q |= 1; } lo <<= 1.
      CORRECTION to the earlier scouting note, which claimed this equals
      u64 division whenever hi < divisor - it does NOT. The remainder is
      one 32-bit register, so r <<= 1 drops its top bit. That is
      harmless exactly while divisor <= 0x80000000 (then r <= divisor-1
      <= 0x7fffffff and 2r+1 still fits), and diverges above it -
      ft_div64by32(0x90000000, 0, 0xffffffff) is 0 where the true
      quotient is 0x90000000 (pinned by a test). Both callers divide by
      an i32 magnitude and so stay inside the safe range, which is why
      the previous inlined u64 stand-in in ft_divfix/ft_muldiv was
      behaviorally right; it is nonetheless not this function, so the
      restoring loop is what got ported and both callers now route
      through the real symbol. 2 bl call sites, binary-scanned:
      ft_divfix @ 0x0804c34c and ft_muldiv @ 0x0804d2ac.

  - address: 0x0804c1a0
    ghidra: FUN_0804c1a0
    name: ft_angle_diff
    size: 36
    sig: "i32 ft_angle_diff(i32 angle1, i32 angle2)"
    module: ft/trig
    status: ported
    vars: {param_1: angle1, param_2: angle2, extraout_r1: remainder, iVar1: delta}
    notes: >-
      FreeType 2.3 FT_Angle_Diff (fttrigon.c, VER-2-3-0) - computes the
      ARM-wrapping angle2-angle1, calls the retail signed-remainder helper
      FUN_08031568 with FT_ANGLE_2PI (0x1680000), lifts a negative
      remainder by one turn, then subtracts one turn only when delta is
      strictly above FT_ANGLE_PI (0xb40000). Thus the result is
      (-180,180] in 16.16 degrees and both half-turn representations
      normalize to +180. Reference C:
      decomp/c/003/0804c1a0_FUN_0804c1a0.c; source identity and semantics
      confirmed against FreeType VER-2-3-0 fttrigon.c. Focused host tests
      cover negative remainders, both half-turn boundaries, and wrapped
      subtraction; match.py confirms the structural subtract/remainder/
      repair sequence.

  - address: 0x0804c1c4
    ghidra: FUN_0804c1c4
    name: ft_atan2
    size: 44
    sig: "i32 ft_atan2(i32 x, i32 y)"
    module: ft/trig
    status: ported
    vars: {param_1: x, param_2: y, local_c: vector.x, local_8: vector.y}
    notes: >-
      FreeType FT_Atan2 (fttrigon.c) - angle of (x,y) in 16.16 degrees,
      range (-180,180]. orrs x,y == 0 returns 0; else the pair goes on
      the stack as an FtVector through ft_trig_prenorm (0x080908a0,
      shift result discarded) and ft_trig_pseudo_polarize (0x080cce10),
      and vec.y is the answer. 7 call sites, binary-scanned (two from
      ft_outline_get_orientation @ 0x0804df3c, the rest in 0x0808axxx /
      0x080e1xxx). LLVM inlines both helpers into the port; they remain
      separately exported symbols. Host tests cross-check the whole
      chain against f64::atan2 (within 0.005 deg).

  - address: 0x080908a0
    ghidra: FUN_080908a0
    name: ft_trig_prenorm
    size: 140
    sig: "i32 ft_trig_prenorm(FtVector *vector)"
    module: ft/trig
    status: ported
    vars: {param_1: vector, uVar1: z, uVar2: msb, uVar3: x, uVar4: y}
    notes: >-
      FreeType ft_trig_prenorm (fttrigon.c, 2.2/2.3 shape) - parks the
      MSB of abs(x)|abs(y) on bit 27 (FT_TRIG_SAFE_MSB) and returns the
      shift applied, negated when the vector had to be scaled down
      (msb>27 takes asr by msb-27, else lsl by 27-msb). FT_MSB is
      inlined as the 16/8/4/2/1 binary search with SIGNED compares and
      asr, and the magnitudes come from rsblt (wrapping negate), so
      x==i32::MIN keeps abs negative, every compare fails, msb==0 and
      the vector is shifted up by 27 - quirk preserved and pinned by a
      test. Zero vector likewise returns 27. 1 call site (ft_atan2),
      binary-scanned.

  - address: 0x080cce10
    ghidra: FUN_080cce10
    name: ft_trig_pseudo_polarize
    size: 176
    sig: "void ft_trig_pseudo_polarize(FtVector *vector)"
    module: ft/trig
    status: ported
    vars: {param_1: vector, iVar2: x, iVar1: y, iVar7: theta, piVar3: arctan_cursor, uVar8: i}
    notes: >-
      FreeType ft_trig_pseudo_polarize (fttrigon.c) - CORDIC
      vector-to-polar. x<0 folds the vector (negate both, theta =
      0xb40000 = 180deg in 16.16); y>0 then flips that seed's sign;
      fixed pre-rotation by arctan(2) (y>=0 - x+=2y, y-=2x, theta +=
      table[0]; y<0 the mirror), then 23 pseudo-rotations i=0..22 by
      arctan(2^-i) accumulating table[i+1]; finally theta is
      FT_PAD_ROUND(theta,32) on its magnitude. Writes vec.x = pseudo
      length, vec.y = angle. CAUTION - Ghidra renders the table[0] sign
      condition as y<=0; the asm (rsbgt/addge/sublt off one cmp lr,#0)
      says y>=0 adds and y<0 subtracts, while the rotation direction
      also splits at y>=0. Table: 24 entries at runtime 0x089011fc via
      literal DAT_080ccec0, ROM image @ 0x0890c0dc (RW image = runtime +
      0xaee0; the RW image is at a different file offset than its
      runtime address, and the neighbouring FreeType __FILE__ pointer
      0x089012e8 lands in the same relocated block at +0xaed7, on the
      freetype/src/base/ftstream.c path string); contents are
      byte-identical to upstream
      ft_trig_arctan_table (4157273, 2949120, 1740967, ...) so the port
      embeds them as const. The earlier note's "22-entry table @
      0x082ad54c" was a partial/aliased match. 1 call site (ft_atan2),
      binary-scanned.

  - address: 0x0804df3c
    ghidra: FUN_0804df3c
    name: ft_outline_get_orientation
    size: 400
    sig: "i32 ft_outline_get_orientation(const FtOutline *outline)"
    module: ft/outline
    status: ported
    vars: {param_1: outline, local_2c: xmin, piVar9: xmin_point, piVar10: xmin_first,
      piVar11: xmin_last, piVar7: first, piVar8: last, iVar2: on_curve_count,
      iVar3: contour_xmin, piVar12: prev, piVar6: next}
    notes: >-
      FreeType FT_Outline_Get_Orientation, pre-2.4 FT_Atan2 flavor
      (before the FT_Outline_Get_CBox/area rewrite). Null outline or
      n_points<=0 returns 0 (FT_ORIENTATION_TRUETYPE). Per contour
      first..=last, contours with last < first+2 points are skipped
      (UNSIGNED pointer compare, bcc); the on-curve points (tag bit 0)
      are counted and the leftmost one kept, seeded x<32768; a contour
      with count>2 whose leftmost x beats the running minimum wins and
      carries its first/last bounds. No winner -> 0. Otherwise the
      previous/next on-curve neighbours are found by stepping within
      the contour, wrapping first<->last (the count>2 test is what
      guarantees termination), and the result is
      ft_atan2(prev-point) > ft_atan2(next-point) ? 1
      (FT_ORIENTATION_POSTSCRIPT, counter-clockwise) : 0 - a raw angle
      compare, NOT FT_Angle_Diff. The contour cursor limit is compared
      unsigned like the other ftoutln walkers, so negative n_contours
      runs zero iterations. Tag index is the byte distance from points
      arithmetic-shifted by 3 (ldrb [tags, r, asr #3]). 2 bl to
      ft_atan2 @ 0x0804c1c4; 1 call site (0x080bf31c), binary-scanned.

  - address: 0x0804d17c
    ghidra: FUN_0804d17c
    name: ft_error_trace
    size: 40
    sig: "void ft_error_trace(const char *format, u32 arg1, u32 arg2, u32 arg3)"
    module: ft/trace
    status: ported
    vars: {param_1: format, param_2: arg1, param_3: arg2, param_4: arg3}
    notes: >-
      The FT_ERROR/FT_TRACE sink of this debug build - varargs shim
      that homes r0-r3, builds a va_list at sp+12 and hands the format
      string to the firmware logger (0x0802f654 with the context word
      0x08b1c9dc from the literal @ 0x0804d1a4), then terminates the
      record via 0x08050f80. Busiest FreeType routine in the image:
      359 bl + 7 b call sites, binary-scanned. Neither logger routine
      is ported, so the port reproduces the CONTRACT - it tail-calls an
      installable FtTraceSink (ft_set_trace_sink, an ADDITION) and
      discards the message when none is installed, which is what retail
      hardware observes anyway. Second deviation - the port takes the
      three variadic slots AAPCS passes in r1-r3 instead of a real
      va_list; every call site in the image passes at most two (the
      widest FreeType format is "... pos = 0x%lx, size = 0x%lx").

  - address: 0x0804e154
    ghidra: FUN_0804e154
    name: ft_panic
    size: 40
    sig: "void ft_panic(const char *format, u32 arg1, u32 arg2, u32 arg3) -> !"
    module: ft/trace
    status: ported
    vars: {param_1: format}
    notes: >-
      FreeType FT_Panic (ftdebug.c), the FT_ASSERT failure path - same
      varargs forwarder shape as ft_error_trace but with its own logger
      context 0x08b1e9dc (literal @ 0x0804e178) and, instead of
      returning, exit(1) via the ported exit @ 0x08030f2c. Callers pass
      "assertion failed on line %d of file %s" plus __LINE__/__FILE__;
      the code after each call site is unreachable, which is why the
      stream readers dereference the stream they just asserted on.
      Ghidra's decompilation of this address is garbage (it runs on
      into the next function). 28 bl + 2 b call sites, binary-scanned.
      Port shares ft_error_trace's sink (deviation) and tail-calls the
      ported exit.

  # --- ftstream.c frame cursor (cursor @ +0x20, limit @ +0x24). The
  # FT_Stream_Get* family reads the window FT_Stream_EnterFrame mapped,
  # bounds-tests by POINTER compare against limit, and on overrun
  # silently returns 0 without moving the cursor - no FT_Error, no
  # trace. All assert FT_ASSERT(stream && stream->cursor).
  - address: 0x0804f05c
    ghidra: FUN_0804f05c
    name: ft_stream_get_char
    size: 64
    sig: "i32 ft_stream_get_char(FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream}
    notes: >-
      FreeType FT_Stream_GetChar (ftstream.c) - one byte from the frame,
      cursor += 1, SIGN-EXTENDED (ldrsbcc @ 0x0804f094). Bound is
      cursor < limit. FT_ASSERT __LINE__ 328 (moveq r1,#328 @
      0x0804f070), which places it first in the Get family. 13 call
      sites, binary-scanned.

  - address: 0x0804f15c
    ghidra: FUN_0804f15c
    name: ft_stream_get_short
    size: 76
    sig: "i32 ft_stream_get_short(FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, puVar2: p}
    notes: >-
      FreeType FT_Stream_GetShort (ftstream.c) - FT_NEXT_SHORT, signed
      big-endian 16-bit ((i8)p[0] << 8 | p[1]; the ldrsb @ 0x0804f198
      takes the FIRST byte). Bound cursor+1 < limit, cursor += 2. Note
      the "str r1,[r4,#32]" writing the cursor back is UNCONDITIONAL -
      harmless, since r1 is unchanged when the bound fails, and it is
      upstream's unconditional "stream->cursor = p". FT_ASSERT __LINE__
      345 (literal @ 0x0804f1ac). 50 call sites, binary-scanned - the
      busiest function in ftstream.c.

  - address: 0x0804f1d8
    ghidra: FUN_0804f1d8
    name: ft_stream_get_short_le
    size: 76
    sig: "i32 ft_stream_get_short_le(FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, puVar2: p}
    notes: >-
      FreeType FT_Stream_GetShortLE (ftstream.c) - FT_NEXT_SHORT_LE,
      signed LITTLE-endian 16-bit ((i8)p[1] << 8 | p[0]). Byte-for-byte
      ft_stream_get_short with the ldrsb moved to the second byte
      (0x0804f214 vs 0x0804f198) - that offset is the only difference.
      FT_ASSERT __LINE__ 364 (moveq r1,#364 @ 0x0804f1ec). 5 call
      sites, binary-scanned.

  - address: 0x0804f0c8
    ghidra: FUN_0804f0c8
    name: ft_stream_get_long
    size: 100
    sig: "i32 ft_stream_get_long(FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, puVar2: p}
    notes: >-
      FreeType FT_Stream_GetLong (ftstream.c) - FT_NEXT_LONG, big-endian
      32-bit, all ldrb (so identical to FT_NEXT_ULONG at this width).
      Bound cursor+3 < limit, cursor += 4, cursor written back
      unconditionally as in ft_stream_get_short. FT_ASSERT __LINE__ 401
      (literal @ 0x0804f130). 10 call sites, binary-scanned.

  # DEAD-STRIPPED from this build, confirmed by an exhaustive scan of
  # 0x0804c000-0x08051000 for their assert __LINE__ values and trace
  # strings: FT_Stream_GetOffset (~383), FT_Stream_GetLongLE (~419) and
  # FT_Stream_ReadShortLE (~520). The surviving line numbers 304, 328,
  # 345, 364, 401, 437, 475, 569, 616, 662 leave exactly those three
  # gaps and match upstream ftstream.c's function order.

  - address: 0x0804ed9c
    ghidra: FUN_0804ed9c
    name: ft_stream_close
    size: 20
    sig: "void ft_stream_close(FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream}
    notes: >-
      FreeType FT_Stream_Close (ftstream.c) - "if (stream &&
      stream->close) stream->close(stream)", a tail bxne with stream
      still in r0. Pre-2.4 body: it does NOT clear close/base/size
      afterwards, so calling it twice calls the callback twice (tested).
      4 call sites, binary-scanned.

  - address: 0x0804edb0
    ghidra: FUN_0804edb0
    name: ft_stream_enter_frame
    size: 308
    sig: "i32 ft_stream_enter_frame(FtStream *stream, u32 count)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: count}
    notes: >-
      FreeType FT_Stream_EnterFrame (ftstream.c) - maps a count-byte
      window as cursor (+0x20) / limit (+0x24) for the FT_Stream_Get*
      family: memory streams point straight into base+pos after checking
      pos < size && pos + count <= size (a wrapping 32-bit add), disk
      streams ft_mem_qalloc (0x082cfaf8) a buffer into base (+0) and
      read() into it. Owns the trace strings "FT_Stream_EnterFrame:"
      (0x0804ef10), " invalid read; expected %lu bytes, got %lu"
      (0x0804ef28) and " invalid i/o; pos = 0x%lx, count = %lu,
      size = 0x%lx" (0x0804ef54, args pos/count/size in r1/r2/r3).
      FT_ASSERT __LINE__ 235 for `stream && stream->cursor == 0`,
      upstream's nested-frame check and the lowest surviving line in the
      file. 35 bl call sites, binary-scanned. Upstream quirks kept: the
      short-read path frees base and nulls it, then FALLS THROUGH to the
      shared tail, so cursor becomes NULL, limit becomes count-as-a-
      pointer and pos still advances by read_bytes; and count is an
      FT_ULong handed to ft_mem_qalloc's signed FT_Long, so 2^31 and up
      come back FT_Err_Invalid_Argument without a read.

  - address: 0x0804ef8c
    ghidra: FUN_0804ef8c
    name: ft_stream_exit_frame
    size: 68
    sig: "void ft_stream_exit_frame(FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream}
    notes: >-
      FreeType FT_Stream_ExitFrame (ftstream.c) - if stream->read
      (+0x14) the frame was heap-allocated, so FT_FREE(stream->base)
      via ft_mem_free (0x082cfae8) with memory @ +0x1c and base = 0;
      then cursor (+0x20) = limit (+0x24) = 0 unconditionally, which is
      what re-arms ft_stream_enter_frame's nested-frame assert.
      FT_ASSERT __LINE__ 304 (moveq r1,#304 @ 0x0804ef98). 33 bl + 1 b
      call sites, binary-scanned.

  - address: 0x0804effc
    ghidra: FUN_0804effc
    name: ft_stream_extract_frame
    size: 44
    sig: "i32 ft_stream_extract_frame(FtStream *stream, u32 count, u8 **pbytes)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: count, param_3: pbytes}
    notes: >-
      FreeType FT_Stream_ExtractFrame (ftstream.c) - calls
      ft_stream_enter_frame (0x0804edb0) and on success hands the frame
      to the caller: *pbytes = stream->cursor, then cursor = limit = 0
      (an ExitFrame that skips the free, so the caller owns the block
      and must ft_stream_release_frame it). *pbytes is untouched when
      the enter fails. 12 bl + 1 b call sites, binary-scanned.

  - address: 0x0804f250
    ghidra: FUN_0804f250
    name: ft_stream_new
    size: 232
    sig: "i32 ft_stream_new(FtLibrary *library, const FtOpenArgs *args, FtStream **astream)"
    module: ft/stream
    status: ported
    vars: {param_1: library, param_2: args, param_3: astream}
    notes: >-
      FreeType FT_Stream_New (ftobjs.c, not ftstream.c) - allocates an
      FT_StreamRec (mov r1,#40, which pins sizeof(FT_StreamRec) and the
      whole FtStream layout) from library->memory (+0) via ft_mem_alloc
      (0x082cfaa4) and dispatches on args->flags (+0): bit 0
      FT_OPEN_MEMORY -> ft_stream_open_memory (0x0804f354) with
      args->memory_base/+4 and memory_size/+8; bit 2 FT_OPEN_PATHNAME ->
      ft_stream_open (0x0804f338) with args->pathname/+12, which is then
      stored into the stream whether or not the open succeeded; bit 1
      FT_OPEN_STREAM with a non-null args->stream/+16 -> free the fresh
      stream and adopt it. Null library returns 0x21
      (FT_Err_Invalid_Library_Handle), null args or an unusable flag set
      returns 6 (FT_Err_Invalid_Argument); both checks run BEFORE
      *astream is cleared, which is where this build's source differs
      from the upstream text. Any failure frees the stream and leaves
      *astream NULL; success re-stamps library->memory into the stream
      ("just to be certain", which is what gives an adopted stream its
      allocator). Independently confirms the ft_stream_open_memory
      identification. 3 bl call sites, binary-scanned.

  - address: 0x0804f028
    ghidra: FUN_0804f028
    name: ft_stream_free
    size: 52
    sig: "void ft_stream_free(FtStream *stream, i32 external)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: external}
    notes: >-
      FreeType FT_Stream_Free (ftobjs.c) - null stream is a no-op;
      otherwise stash memory (+0x1c) BEFORE the close (the callback may
      scrub the record), ft_stream_close (0x0804ed9c), and when
      external == 0 tail-branch ft_mem_free (0x082cfae8) on the stream
      itself. 3 bl call sites, binary-scanned.

  - address: 0x0804f5d0
    ghidra: FUN_0804f5d0
    name: ft_stream_read_fields
    size: 508
    sig: "i32 ft_stream_read_fields(FtStream *stream, const FtFrameField *fields, void *structure)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: fields, param_3: structure}
    notes: >-
      FreeType FT_Stream_ReadFields (ftstream.c) - the table-driven
      struct loader the sfnt/truetype/type1 drivers use. Walks a
      FT_Frame_Field array (4 bytes per row: value @ +0, size @ +1,
      offset @ +2), decoding one value per row out of stream->cursor and
      storing it at structure + offset. Identified beyond doubt by its
      jump table over `value - 4` with `cmp #21`, whose 22 entries
      reproduce upstream's opcode encoding `command << 2 | little << 1 |
      signed` exactly: 4 = ft_frame_start (bl ft_stream_enter_frame),
      8/9 byte/schar, 12..15 the shorts, 16..19 the longs, 20..23 the
      3-byte offsets, 24/25 bytes/skip - with 5..7 and 10..11 falling to
      `default:` because those bit patterns name no opcode. Values are
      read unsigned and sign-extended by lsl/asr with a shared
      24/16/8/0 shift, then stored strb/strh/str by fields->size. Two
      distinct exits: `default:` writes cursor back into the stream
      first, both error exits (a failed EnterFrame, or a bytes/skip run
      with cursor + len > limit) do not. A frame opened by an
      ft_frame_start row is always closed with ft_stream_exit_frame on
      the way out. 21 bl + 1 b call sites, binary-scanned. This was the
      last unported function in the alphabetically-sorted ftstream.c run
      0x0804ed9c..0x0804fd94. Port deviations: the original speculatively
      loads stream->cursor before the null-argument test (ldr r1,[r0,#32]
      @ 0x0804f5e0 sits above the bne), and its FT_MEM_COPY is the
      misalignment-capable ADS forward copy at 0x08000020 through the
      iRAM veneer 0x08037db0, reproduced with the ported memmove.

  - address: 0x0804f338
    ghidra: FUN_0804f338
    name: ft_stream_open
    size: 28
    sig: "i32 ft_stream_open(FtStream *stream, const char *pathname)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: pathname}
    notes: >-
      FreeType FT_Stream_Open (the ftsystem.c platform hook) - forwards
      (stream, pathname) unchanged to the firmware's file opener at
      0x082d3ddc and normalizes its answer to 0 or 1, i.e.
      FT_Err_Cannot_Open_Resource, so the opener's several distinct
      failures all reach FreeType as the same "no". 1 bl call site
      (ft_stream_new @ 0x0804f250), binary-scanned.

  - address: 0x082d3ddc
    ghidra: FUN_082d3ddc
    name: ft_platform_stream_open
    size: 120
    sig: "i32 ft_platform_stream_open(FtStream *stream, const char *pathname)"
    module: ft/system
    status: ported
    vars: {param_1: stream, param_2: pathname}
    notes: >-
      The firmware's FT_Stream_Open body - Apple's ftsystem.c, sitting
      immediately after its own close (0x082d3d40) and read (0x082d3d7c)
      callbacks, exactly the three-function shape upstream's ANSI
      ftsystem.c has. Null stream returns 9. Otherwise byte 0 of
      pathname is read as a signed char volume index (ldrb, -'0',
      lsl #24 / asr #24), byte 1 is skipped unconditionally and the path
      proper is pathname+2; that goes to the file opener at 0x082d3cb4
      with an out-parameter, and a NULL handle returns 20. On success
      the length query at 0x082a5418 writes stream->size (+4) directly,
      then pathname+2 -> +0x10 (a pointer INTO the caller's string),
      handle -> +0x0c, 0 -> +8, and the two callbacks -> +0x14 / +0x18.
      base (+0) is deliberately untouched - a disk stream has no image
      until ft_stream_enter_frame allocates one. The stored callback
      literals are RUNTIME addresses (0x082c8ea4 / 0x082c8e68), 0xaed8
      below their file positions; the 9 and 20 are the firmware's own
      status numbering, not FreeType's. PORT DEVIATION: its three wired
      file-layer entries remain an installable FtPlatformFileOps table
      (the ft/trace.rs sink pattern) while the file constructor/read
      subsystem is incomplete; the length query at 0x082a5418 is now
      ported directly and no longer has a dispatch entry. The opener's
      own logic is reproduced exactly.

  - address: 0x082a5418
    ghidra: FUN_082a5418
    name: ft_platform_file_length
    size: 116
    sig: "i32 ft_platform_file_length(void *handle, u32 *size)"
    module: ft/system
    status: ported
    vars: {param_1: handle, param_2: size, local_10: counted_lock, iVar1: query_result, uVar2: result}
    notes: >-
      Platform C++ file length query. Raw ARM proves the two-argument ABI
      r0=handle/r1=size despite Ghidra retaining two unused phantom
      parameters. It builds a counted-lock guard from *(handle+4)+0x44,
      acquires it, and reads the state byte at +8. A nonzero state returns
      2 after unlock. Otherwise it tests the directory-entry index at
      +0x18 against -1: on -1, size is untouched and the +0x1c open-status
      word is returned; on every other signed value, +0x20 cached entry
      length is written through size before unlock and return 0. Every exit
      calls mutex_unlock_counted, whose hold-counter decrement precedes
      semaphore signal. The target layouts assert the recovered offsets;
      the host model uses native pointers while exercising the same named
      fields. 20 bl call sites, binary-scanned.

  - address: 0x082a539c
    ghidra: FUN_082a539c
    name: ft_platform_file_tell
    size: 116
    sig: "i32 ft_platform_file_tell(void *handle, u32 *position)"
    module: ft/system
    status: ported
    vars: {param_1: handle, param_2: position, local_10: counted_lock, iVar1: query_result, uVar2: result}
    renames:
      param_1/r0/r4: handle
      param_2/r1/r5: position
      local_10: counted_lock
      "handle+0x08": length_query_state
      "handle+0x18": directory_entry_index
      "handle+0x1c": open_status
      "handle+0x34": cursor
      FUN_0818a144: counted_mutex_guard_acquire
      FUN_0809449c: mutex_unlock_counted
      FUN_082a548c: file_has_directory_entry
    notes: >-
      Platform C++ file cursor query. Raw ARM establishes the true 116-byte
      instruction extent from 0x082a539c through 0x082a540c; the next
      function starts at 0x082a5410. It receives only r0=handle and
      r1=position despite Ghidra retaining two phantom parameters, builds a
      counted-lock guard from *(handle+4)+0x44, and acquires it. A nonzero
      state byte at +0x08 returns 2 after unlock. Otherwise, the
      file_has_directory_entry predicate checks the signed +0x18 index for
      -1: on -1, position is untouched and the +0x1c open-status word is
      returned; every other index writes the +0x34 cursor to position before
      unlock and return 0. Raw decoding of every ARM B/BL word in osos.dec
      found 11 direct call sites, all unconditional bl: 0x0805a610,
      0x080e53d4, 0x080e5414, 0x080e5480, 0x080e5504, 0x080e5574,
      0x080e55d8, 0x080e5750, 0x080e579c, 0x080e58a4, and 0x081f51f4.
      Target code has no deliberate deviations. The native-pointer host
      model locks its named mutex field directly because the target guard's
      fixed +0x44 arithmetic would address host padding; tests cover cursor
      extremes, every non-sentinel index class, error output preservation,
      and balanced counted-lock release.

  - address: 0x082a548c
    ghidra: FUN_082a548c
    name: file_has_directory_entry
    size: 16
    sig: "i32 file_has_directory_entry(const void *file)"
    module: codegen/file_directory_entry
    status: ported
    vars: {param_1: file}
    notes: >-
      Platform C++ file object's directory-entry-index sentinel predicate.
      Its constructor at 0x08278dc4 initializes file+0x18 to -1, and its
      close path at 0x08278894 restores that value after closing the
      directory-entry-backed handle. Its exact 16-byte ARM body is `ldr
      r0, [r0, #0x18]; adds r0, r0, #1; movne r0, #1; bx lr`: the
      flag-setting wrapping addition leaves zero only for -1 and the
      conditional move returns 1 for every other i32 value, including both
      signed extremes. It is a read-only r0=file -> r0=i32 ABI leaf; 11
      direct C call sites use it before file virtual I/O or return the
      file's open-status word at +0x1c.

  - address: 0x082d3c5c
    ghidra: FUN_082d3c5c
    name: least_common_multiple
    size: 72
    sig: "u32 least_common_multiple(u32 left, u32 right)"
    module: util/least_common_multiple
    status: ported
    renames:
      param_1/r0: left
      param_2/r1/r4: right
      r5: signed_low_half_product
      FUN_08036f14: __rt_udiv
    notes: >-
      Raw osos.dec extent is exactly 72 bytes, from push {r4-r6,lr} at
      0x082d3c5c through the tail b __rt_udiv at 0x082d3ca0; the next
      separately linked function begins at 0x082d3ca4. It first computes
      smulbb(left,right), so the zero-product guard uses signed low 16-bit
      halves and returns 1; every known caller passes ldrb values. Otherwise
      it performs unsigned full-u32 Euclid division, swapping operands when
      necessary, and tail-divides that signed-low-half product by the GCD.
      Decoding every ARM B/BL word in osos.dec found 8 direct call sites, all
      unconditional bl and no predicated forms: 0x08043608, 0x08043618,
      0x08043630, 0x08043640, 0x08043658, 0x08043668, 0x08043680, and
      0x08043690. These are four duplicate byte-pair evaluations in
      FUN_080435f0 and consume r0 only. The port gets each Euclidean
      remainder via the existing __rt_udivmod out-parameter rather than
      the retail __rt_udiv r1 remainder; r0's returned quotient is unchanged.

  - address: 0x082d3cb4
    ghidra: FUN_082d3cb4
    name: ft_platform_file_open
    size: 112
    sig: "i32 ft_platform_file_open(i32 volume, const char *path, void **handle)"
    module: ft/system
    status: ported
    vars: {param_1: volume, param_2: path, param_3: handle, piVar2: file, uVar3: result}
    notes: >-
      The open behind ft_platform_stream_open @ 0x082d3ddc.
      operator_new(84) (0x082aadd4, the real heap::veneers port) then
      the file object's constructor at 0x08278dc4 with (raw, path, 1,
      volume, 0x10000, 2, 0); *handle takes the result unconditionally.
      A non-null object whose status word at +28 is non-zero means the
      open failed, so slot 1 of its vtable is called to destroy it and
      *handle is nulled; the return value is 1 only when the object
      survives. PORT DEVIATION: the constructor is a whole C++ file
      class subsystem and stays unported, so it is called through the
      FT_PLATFORM_FILE_CTOR dispatch slot (the app/singletons.rs
      pattern) whose default stub fails the open closed by returning
      null; everything else - the 0x54 allocation, the literal
      immediates, the status-word test, the destroy-and-null path - is
      reproduced exactly. match.py shows the same shape, the only delta
      being the blx-through-slot indirection.

  - address: 0x082d3d7c
    ghidra: FUN_082d3d7c
    name: ft_platform_stream_read
    size: 96
    sig: "u32 ft_platform_stream_read(FtStream *stream, u32 offset, u8 *buffer, u32 count)"
    module: ft/system
    status: ported
    vars: {param_1: stream, param_2: offset, param_3: buffer, param_4: count, uVar2: handle, local_1c: transferred, iVar1: read_status}
    notes: >-
      The FT_Stream_IoFunc ft_platform_stream_open plants in
      stream->read (+0x14); its runtime address 0x082c8ea4 is the
      literal at 0x082d3e54. Seeks the descriptor (+0x0c) to offset via
      0x082787b8, called as (handle, offset, offset, 0, 0) - the doubled
      offset register reproduced verbatim, its meaning not recovered -
      then - only when buffer and count are both non-zero - reads
      through 0x082784b8 with the argument order (handle, count, buffer,
      &transferred), the count stored through the out-parameter zeroed
      again if the read reports an error (any non-zero return). Returns
      the transferred count, so a NULL buffer or a zero count is the
      pure seek FT_Stream_Seek probes with. Unlike the close callback,
      stream is never null-tested. The 0x082787b8 seek body is now ported
      directly, so its real state, sentinel, range, and cursor behavior runs
      even in host tests. The deeper read body at 0x082784d4 remains behind
      retail_file_read's typed resident-target/host boundary; its default
      reports an error rather than inventing filesystem I/O.

  - address: 0x082d3d40
    ghidra: FUN_082d3d40
    name: ft_platform_stream_close
    size: 60
    sig: "void ft_platform_stream_close(FtStream *stream)"
    module: ft/system
    status: ported
    vars: {param_1: stream}
    notes: >-
      The FT_Stream_CloseFunc ft_platform_stream_open plants in
      stream->close (+0x18); its runtime address 0x082c8e68 is the
      literal at 0x082d3e58. Null stream is a no-op; otherwise the
      descriptor (+0x0c) - when itself non-null - is destroyed through
      slot 1 of its vtable and the record is scrubbed: descriptor,
      pathname (+0x10), size (+4) and base (+0) all set to 0, in that
      store order. Note it leaves stream->close itself in place, so
      ft_stream_close would happily run it twice. The destructor is
      read out of the object's own vtable (same call as
      ft_platform_file_open's failure path), so unlike the opener and
      the read callback this one carries no hard-coded C++ file-class
      address and is ported directly.

  - address: 0x0804f354
    ghidra: FUN_0804f354
    name: ft_stream_open_memory
    size: 28
    sig: "void ft_stream_open_memory(FtStream *stream, const u8 *base, u32 size)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: base, param_3: size}
    notes: >-
      FreeType FT_Stream_OpenMemory (ftstream.c) - points a stream at an
      in-memory image. stm r0,{r1,r2} writes base @ +0 and size @ +4,
      then zero to pos (+8), cursor (+0x20), read (+0x14) and close
      (+0x18); limit (+0x24) and everything else is left alone. That
      field set in that order IS upstream's function body, so this is
      the independent confirmation of the FtStream layout the readers
      were reverse-engineered against. 3 call sites, binary-scanned.

  - address: 0x0804f370
    ghidra: FUN_0804f370
    name: ft_stream_pos
    size: 8
    sig: "u32 ft_stream_pos(const FtStream *stream)"
    module: ft/stream
    status: ported
    vars: {param_1: stream}
    notes: >-
      FreeType FT_Stream_Pos (ftstream.c) - "ldr r0,[r0,#8]; bx lr",
      i.e. return stream->pos. 17 call sites, binary-scanned.

  - address: 0x0804f378
    ghidra: FUN_0804f378
    name: ft_stream_read
    size: 20
    sig: "i32 ft_stream_read(FtStream *stream, u8 *buffer, u32 count)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: buffer, param_3: count}
    notes: >-
      FreeType FT_Stream_Read (ftstream.c) - ft_stream_read_at(stream,
      stream->pos, buffer, count). Four register moves and a NOP that
      falls straight through into ft_stream_read_at @ 0x0804f38c, which
      the linker placed immediately after - there is no branch, which is
      why a naive call-site scan of 0x0804f38c misses this caller.
      15 call sites, binary-scanned. LLVM inlines the read_at body into
      the port instead of falling through - same behavior.

  - address: 0x0804f38c
    ghidra: FUN_0804f38c
    name: ft_stream_read_at
    size: 176
    sig: "i32 ft_stream_read_at(FtStream *stream, u32 pos, u8 *buffer, u32 count)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: pos, param_3: buffer, param_4: count, iVar4: transferred}
    notes: >-
      FreeType FT_Stream_ReadAt (ftstream.c) - bulk read of count bytes
      at pos. Two distinct failures and only the first is fatal: pos >=
      size (checked for BOTH stream flavors, unlike the seek's bounds
      test) traces "FT_Stream_ReadAt: invalid i/o; pos = 0x%lx, size =
      0x%lx" (0x0804f43c, one merged FT_ERROR as in FT_Stream_Seek) and
      returns 0x55 with nothing read and pos untouched; a SHORT transfer
      still copies what it got, still stores pos+transferred, and only
      then traces the tag (0x0804f478) plus " invalid read; expected
      %lu bytes, got %lu" (0x0804f48c, args count then transferred) and
      returns 0x55. Disk streams take whatever read() returns; memory
      streams clamp count to size-pos (so they short-read by
      construction near EOF) and memcpy via the ROM thunk 0x08037db0
      (mirror of the ported __rt_memcpy). Note pos is advanced as
      argument-pos + transferred, NOT re-read from the struct the way
      ft_stream_try_read does it. 1 bl (0x082c3398) + 1 b (0x0809cdd4)
      call site plus the fall-through from ft_stream_read @ 0x0804f378,
      binary-scanned.

  - address: 0x0804fd94
    ghidra: FUN_0804fd94
    name: ft_stream_try_read
    size: 116
    sig: "u32 ft_stream_try_read(FtStream *stream, u8 *buffer, u32 count)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: buffer, param_3: count, uVar4: transferred}
    notes: >-
      FreeType FT_Stream_TryRead (ftstream.c) - ft_stream_read_at's
      forgiving twin at the CURRENT pos: returns the byte count actually
      transferred, never traces and has no error return at all. pos >=
      size yields 0 with pos untouched; otherwise disk streams take
      read()'s answer and memory streams clamp to size-pos and memcpy
      (thunk 0x08037db0). The final advance RE-LOADS pos from the struct
      (ldr/add/str @ 0x0804fdf4) rather than adding to the value it
      captured, so a read callback that moves pos itself compounds with
      the advance - the one behavioral difference from ft_stream_read_at
      beyond the error reporting. 1 call site, binary-scanned. Port
      matches instruction-for-instruction at 29 vs 29 (match.py).

  - address: 0x0804fcb8
    ghidra: FUN_0804fcb8
    name: ft_stream_release_frame
    size: 48
    sig: "void ft_stream_release_frame(FtStream *stream, u8 **pbytes)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: pbytes}
    notes: >-
      FreeType FT_Stream_ReleaseFrame (ftstream.c) - if stream->read
      (+0x14) is non-null the frame was heap-allocated, so free *pbytes
      through ft_mem_free(stream->memory @ +0x1c, *pbytes) = 0x082cfae8,
      and *pbytes = 0 either way. The doubled "str r5,[r4]" at
      0x0804fcdc / 0x0804fce0 is the FT_MEM_FREE macro's own nulling
      followed by the explicit one - which is exactly upstream's source.
      No FT_ASSERT on stream (this is the pre-2.4 body). 19 bl + 1 b
      call sites, binary-scanned. LLVM folds the two stores into one and
      merges the null test the inlined ft_mem_free contributes; the
      observable behavior is identical.

  - address: 0x0804fce8
    ghidra: FUN_0804fce8
    name: ft_stream_seek
    size: 104
    sig: "i32 ft_stream_seek(FtStream *stream, u32 pos)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: pos, uVar2: size}
    notes: >-
      FreeType FT_Stream_Seek (ftstream.c) - stores pos at stream+8
      unconditionally, then validates: memory streams (null read fn @
      +0x14) require pos <= size (+4), so seeking to the first position
      past the last byte is legal; disk streams call read(stream, pos,
      NULL, 0) and fail on a non-zero return, with no size check at
      all. Failure traces "FT_Stream_Seek: invalid i/o; pos = 0x%lx,
      size = 0x%lx" (0x0804fd50) via ft_error_trace and returns 0x55
      (FT_Err_Invalid_Stream_Operation); pos keeps the rejected value
      either way. This build merged upstream's two FT_ERROR calls into
      one. 65 bl + 2 b call sites, binary-scanned (one b is
      ft_stream_skip). FtStream layout base/size/pos @ +0/+4/+8, read @
      +0x14 (ft/stream).

  - address: 0x0804fd88
    ghidra: FUN_0804fd88
    name: ft_stream_skip
    size: 12
    sig: "i32 ft_stream_skip(FtStream *stream, i32 distance)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: distance}
    notes: >-
      FreeType FT_Stream_Skip (ftstream.c) - three instructions: load
      pos (stream+8), add distance, tail-branch ft_stream_seek @
      0x0804fce8. This pre-2.4 version has no distance<0 rejection, so
      a negative distance seeks backwards and an underflow past 0
      becomes a huge u32 that the seek's bounds check then rejects
      (tested). 12 call sites, binary-scanned. LLVM inlines the seek
      into the port instead of tail-branching - same behavior.

  - address: 0x0804f7cc
    ghidra: FUN_0804f7cc
    name: ft_stream_read_long
    size: 204
    sig: "i32 ft_stream_read_long(FtStream *stream, i32 *error)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: error, pbVar2: p, uVar5: result, iVar4: pos}
    notes: >-
      FreeType FT_Stream_ReadLong (ftstream.c) - RENAMED from the
      earlier ft_stream_read_ulong guess - the trace string inside the
      function reads "FT_Stream_ReadLong: invalid i/o; pos = 0x%lx,
      size = 0x%lx" (0x0804f8c4), and the four byte loads are plain
      ldrb, so the assembled 32-bit pattern is identical either way
      (FT_NEXT_LONG == FT_NEXT_ULONG at 32 bits). Clears *error, bounds
      check pos+3 < size (unsigned, WRAPPING add - pos near 0xffffffff
      wraps and passes, quirk pinned by a test), then base+pos for
      memory streams or read(stream,pos,buf,4) requiring exactly 4 for
      disk streams; big-endian assembly p[0]<<24|p[1]<<16|p[2]<<8|p[3]
      (skipped, but pos still advanced, when the frame pointer is
      NULL); pos += 4 re-read from the struct. Failure emits ONE trace
      call and only then sets *error = 0x55 (the reverse of
      ft_stream_read_short's order). Live FT_ASSERT(stream): a null
      stream calls ft_panic (0x0804e154) with line 616 and the
      ftstream.c __FILE__ pointer 0x089012e8, and never returns.
      13 call sites, binary-scanned.

  - address: 0x0804fb88
    ghidra: FUN_0804fb88
    name: ft_stream_read_short
    size: 188
    sig: "i32 ft_stream_read_short(FtStream *stream, i32 *error)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: error, puVar3: p, iVar5: result, iVar4: pos}
    notes: >-
      FreeType FT_Stream_ReadShort (ftstream.c) - RENAMED from the
      earlier ft_stream_read_ushort guess - the high byte is loaded
      with LDRSB (0x0804fc00), so the result is SIGN-EXTENDED
      (p[1] | (i8)p[0] << 8) and this is the signed FT_Short reader,
      matching its own trace tag "FT_Stream_ReadShort:" (0x0804fc74).
      Port returns the sign-extended word the original leaves in r0.
      Otherwise the 2-byte twin of ft_stream_read_long @ 0x0804f7cc:
      bounds check pos+1 < size with the same wrapping add, memory
      frame or read(...,2), pos += 2, and on failure *error = 0x55
      FIRST, then TWO trace calls (tag @ 0x0804fc74, then " invalid
      i/o; pos = 0x%lx, size = 0x%lx" @ 0x0804fc8c - upstream's two
      FT_ERRORs both survived here). Live FT_ASSERT(stream): null
      stream calls ft_panic (0x0804e154) with line 475 and the
      ftstream.c __FILE__ pointer, and never returns. 17 call sites,
      binary-scanned.

  - address: 0x0804f4b8
    ghidra: FUN_0804f4b8
    name: ft_stream_read_char
    size: 172
    sig: "i32 ft_stream_read_char(FtStream *stream, i32 *error)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: error, uVar2: result}
    notes: >-
      FreeType FT_Stream_ReadChar (ftstream.c) - one byte at pos,
      pos += 1, returned SIGN-EXTENDED (lsl #24 / asr #24 @
      0x0804f538, upstream's "return (FT_Char)result"). Named by its
      trace string "FT_Stream_ReadChar: invalid i/o; ..." @ 0x0804f594
      and confirmed by the FT_ASSERT __LINE__ literal 437 @ 0x0804f568,
      which puts it first in ftstream.c ahead of ReadShort (475),
      ReadOffset (569), ReadLong (616), ReadLongLE (662) - upstream's
      order exactly. Unlike every other reader it has NO shared bounds
      test: the memory path checks pos < size itself (note < , not the
      siblings' pos+n < size) while the DISK PATH CHECKS NOTHING - a
      read callback that returns 1 is believed arbitrarily far past
      size. That asymmetry is upstream's and is pinned by a test.
      Failure sets *error = 0x55 then emits ONE trace call (this build
      merged upstream's two FT_ERRORs, as it did for FT_Stream_Seek).
      Live FT_ASSERT(stream) -> ft_panic (0x0804e154), never returns.
      11 call sites, binary-scanned, all in the sfnt/type1 loaders
      @ 0x0808xxxx-0x080a1xxx.

  - address: 0x0804fa48
    ghidra: FUN_0804fa48
    name: ft_stream_read_offset
    size: 204
    sig: "i32 ft_stream_read_offset(FtStream *stream, i32 *error)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: error, pbVar2: p, uVar5: result}
    notes: >-
      FreeType FT_Stream_ReadOffset (ftstream.c) - the 3-byte sibling of
      ft_stream_read_short. Trace tag "FT_Stream_ReadOffset:" @
      0x0804fb44, FT_ASSERT __LINE__ 569 @ 0x0804fb18. Result is
      upstream's FT_NEXT_OFF3, a SIGNED big-endian 24-bit offset -
      ((i8)p[0] << 16) | (p[1] << 8) | p[2]; the ldrsb on the top byte
      @ 0x0804fac0 is what makes it OFF3 rather than UOFF3. Bounds
      check pos+2 < size with the same wrapping add as its siblings,
      memory frame base+pos or read(stream,pos,buf,3) requiring exactly
      3, assembly skipped (but pos still advanced) when the frame
      pointer is NULL, pos += 3 re-read from the struct. Failure sets
      *error = 0x55 FIRST, then two trace calls (tag, then " invalid
      i/o; pos = 0x%lx, size = 0x%lx" @ 0x0804fb5c). 1 call site
      (0x080a32c4), binary-scanned.

  - address: 0x0804f900
    ghidra: FUN_0804f900
    name: ft_stream_read_long_le
    size: 212
    sig: "i32 ft_stream_read_long_le(FtStream *stream, i32 *error)"
    module: ft/stream
    status: ported
    vars: {param_1: stream, param_2: error, pbVar2: p, uVar5: result}
    notes: >-
      FreeType FT_Stream_ReadLongLE (ftstream.c) - ft_stream_read_long
      @ 0x0804f7cc with the bytes the other way round: FT_NEXT_ULONG_LE,
      p[0] | p[1]<<8 | p[2]<<16 | p[3]<<24, all ldrb. Trace tag
      "FT_Stream_ReadLongLE:" @ 0x0804fa04, FT_ASSERT __LINE__ 662 @
      0x0804f9d8 (last reader in the file, as upstream). Same pos+3 <
      size wrapping bounds test, same read(...,4) exact-count
      requirement, same pos += 4. Like ReadLong the failure path traces
      BEFORE setting *error = 0x55; unlike ReadLong this build kept
      both FT_ERRORs, so two trace calls fire (tag, then " invalid i/o;
      pos = 0x%lx, size = 0x%lx" @ 0x0804fa1c). 1 call site
      (0x0807d52c), binary-scanned.

  - address: 0x08042cbc
    ghidra: FUN_08042cbc
    name: bcopy
    size: 16
    sig: "void bcopy(const void *src, void *dst, size_t len)"
    module: libc/bcopy
    status: ported
    vars: {param_1: src, param_2: dst}
    notes: >-
      BSD-argument-order memmove adapter - swaps r0/r1 and tail-branches
      thunk 0x08037e00 onto ROM memmove @ 0x220000d4 (mirror of ported
      osos 0x080000d4). 47 call sites, binary-scanned. Port calls the
      Rust memmove (via LLVM's __aeabi_memmove lowering, whose weak
      builtin forwards to the strong ported memmove at link - same
      linkage shape as rom_memmove_keep_dst).

  - address: 0x08042ccc
    ghidra: FUN_08042ccc
    name: ft_buffered_stream_buffered_bytes
    size: 48
    sig: "u32 ft_buffered_stream_buffered_bytes(const FtBufferedStream *stream)"
    module: ft/buffer
    status: ported
    vars: {param_1: stream, uVar2: cursor, iVar1: buffered_bytes}
    notes: >-
      The 48-byte `buff`-tagged buffered-I/O record's byte count. Its
      reader at 0x08042fd4 copies from and advances +0x18 by this result;
      its output flush at 0x08042d68 uses the zero-flag span. ldrb +5
      selects output `cursor - buffer_start` (+0x18 - +0x1c, wrapping)
      when zero, or input `buffer_end - cursor + 1` (+0x20 - +0x18,
      inclusive) when nonzero. cmp/sub{ls}/add{ls}/mov{hi} make the
      input result zero exactly when the unsigned cursor exceeds the
      inclusive end; the permitted sub/add also wraps for the full
      0..=u32::MAX input range.

  - address: 0x0813b714
    ghidra: FUN_0813b714
    name: berec_test_bit
    size: 24
    sig: "u32 berec_test_bit(const u8 **rec, isize offset, u32 bit)"
    module: util/berec
    status: ported
    vars: {param_1: rec, param_2: offset, param_3: bit}
    notes: >-
      Big-endian record accessor family head - rec is a handle whose
      first word is the buffer base. Returns 0/1 for bit `bit` of the
      byte at offset (ands + movne). Shift is by register, so ARM
      register-shift rules apply (amount = bottom byte, 32..255 gives
      0); port replicates them. 17 call sites, binary-scanned (callers
      cluster @ 0x080f9xxx-0x080fdxxx record parsers).

  - address: 0x0813b72c
    ghidra: FUN_0813b72c
    name: berec_extract_bits
    size: 32
    sig: "u32 berec_extract_bits(const u8 **rec, isize offset, u32 shift, u32 width)"
    module: util/berec
    status: ported
    vars: {param_1: rec, param_2: offset, param_3: shift, param_4: width}
    notes: >-
      (byte-at-offset >> shift) & ((1 << width) - 1) - a bit-field from
      one byte of the record. ARM register-shift semantics on both
      amounts (width >= 32 wraps the mask to all-ones via sub #1),
      replicated in the port. 8 call sites, binary-scanned.

  - address: 0x0813b74c
    ghidra: FUN_0813b74c
    name: berec_read_u16
    size: 24
    sig: "u32 berec_read_u16(const u8 **rec, isize offset)"
    module: util/berec
    status: ported
    vars: {param_1: rec, param_2: offset}
    notes: >-
      Big-endian 16-bit field read (byte at offset is the high byte).
      Ghidra's CONCAT11 return; the asm keeps it as a 32-bit orr.
      5 call sites, binary-scanned.

  - address: 0x0813b764
    ghidra: FUN_0813b764
    name: berec_read_u24
    size: 36
    sig: "u32 berec_read_u24(const u8 **rec, isize offset)"
    module: util/berec
    status: ported
    vars: {param_1: rec, param_2: offset}
    notes: Big-endian 24-bit field read. 3 call sites, binary-scanned.

  - address: 0x0813b788
    ghidra: FUN_0813b788
    name: berec_read_u32
    size: 44
    sig: "u32 berec_read_u32(const u8 **rec, isize offset)"
    module: util/berec
    status: ported
    vars: {param_1: rec, param_2: offset}
    notes: Big-endian 32-bit field read. 4 call sites, binary-scanned.

  - address: 0x081f3b30
    ghidra: FUN_081f3b30
    name: load_be32
    size: 36
    sig: "u32 load_be32(const u8 *p)"
    module: util/beload
    status: ported
    vars: {param_1: p}
    notes: >-
      Alignment-free big-endian 32-bit load (4 ldrb + orr). Raw-pointer
      twin of berec_read_u32. 4 call sites, binary-scanned, all in the
      format-header parser cluster @ 0x081f3b54..0x081f3c80.

  - address: 0x081bd910
    ghidra: FUN_081bd910
    name: path_lengths_ok
    size: 96
    sig: "u32 path_lengths_ok(void *this_unused, const u8 *path)"
    module: fs/path_limits
    status: ported
    vars: {param_1: this_unused, param_2: path}
    notes: >-
      FAT/Win32-style path length validation - every component split on
      backslash, slash or colon must be <= 255 bytes and the whole path
      <= 259 bytes (MAX_PATH 260 incl. NUL); NULL fails. First arg is
      an unread `this` (r0 overwritten immediately), preserved for ABI.
      14 call sites, binary-scanned, all in the file-path cluster
      @ 0x081bc9f8..0x081bded8 (owns the literal
      "iPod_Control/Device/PlayCounts" @ 0x081bc840).

  - address: 0x081bd9b8
    ghidra: FUN_081bd9b8
    name: string_object_normalize_volume_path
    size: 44
    sig: "void string_object_normalize_volume_path(const u8 *volume_record, StringObject *path)"
    module: cxx/string_object_normalize_volume_path
    status: ported
    renames:
      param_1: volume_record
      param_2: path
      FUN_08279198: string_object_normalize_separators
      FUN_0827916c: string_object_prepend_volume_prefix
    notes: >-
      Raw extent is 44 bytes (0x081bd9b8..0x081bd9e3), not Ghidra's
      incorrect 88: the next sibling begins with push {r4,lr} at
      0x081bd9e4. Every ARM B/BL word in osos.dec was decoded: 18 direct
      call sites, all plain bl; zero predicated forms and zero plain B
      branches. It calls the unported separator normalizer @ 0x08279198
      as (path, '/', 0), then ldrb volume_record +0x10 and tail-calls
      unported @ 0x0827916c. Raw ARM shows that target constructs UTF-16
      [volume_code, ':', '\\', 0] and inserts it at index 0, so the port
      normalizes separators before prefixing the volume path. Deliberate
      deviation: both unported helper boundaries use
      STRING_OBJECT_PATH_OPS; target defaults transmute their retailOS
      addresses and host tests record ordering and arguments. Ghidra's
      shown 0x08276aa0 call is the tail target's callee, not this
      function's direct branch.

  - address: 0x082e19cc
    ghidra: FUN_082e19cc
    name: path_node_release
    size: 32
    sig: "u8 *path_node_release(u8 *node)"
    module: fs/path_node
    status: ported
    renames:
      param_1: node
      FUN_082e1960: shared_data_release
      FUN_082e2f04: path_node_pool_recycle
    notes: >-
      NULL-guarded release of a path-resolution node (0x1c bytes; the
      pool pop path zeroes exactly 0x1c): hands the shared refcounted
      0x54-byte data block at node +0x04 to the release @ 0x082e1960
      (refcount at data +0x24, unlinked from the list headed at
      0x08a0a720 and pushed to the data pool at 0x08a0a738 on the
      zero transition), then tail-calls the pool recycle-or-pop @
      0x082e2f04, which pushes the node onto the freelist headed at
      0x08a0a73c (pointer literal @ 0x082e2f70). Returns NULL on every
      path: the pool's push path returns NULL, and a NULL node
      short-circuits before either call (movs r4,r0; popeq {r4,pc}).
      Size byte-verified: next function's push {r4,r5,r6,lr} sits at
      0x082e19ec, so Ghidra's 32 is exact. 32 call sites, verified by
      decoding every B/BL word in osos.dec: 23 plain bl + 9 blne
      (0x082e1950, 0x082e1d10, 0x082e1d1c, 0x082e20c4, 0x082e3690,
      0x082e369c, 0x082e36a8, 0x082e41d4, 0x082e6530) - the predicated
      sites are callers already gating on their own non-NULL check;
      the callee's own guard makes the skip semantics identical.
      Cluster context: node factory FUN_082e0100 (pool pop + data alloc
      0x082e00f8), cloning lookup FUN_082e1f74 (bumps data +0x24 under
      the cluster lock), path walk FUN_082e15d8. Deviation: both
      callees are unported, so the target build calls their retailOS
      load addresses directly through transmuted literals (LLVM emits
      blx/bx register-indirect against .word literals instead of
      bl/b); host builds route them through PATH_NODE_HOST_OPS
      recording boundaries. match.py structural: same NULL guard with
      early return, same ldr of node +4, same call order, same
      pass-through return of the pool result.

  - address: 0x082e2004
    ghidra: FUN_082e2004
    name: path_node_lookup_component
    size: 40
    sig: "u8 *path_node_lookup_component(u8 *node, u8 *parent, u8 *component, u8 *component_state, u32 flags)"
    module: fs/path_node_lookup
    status: ported
    renames:
      param_1: node
      param_2: parent
      param_3: component
      param_4: component_state
      param_5: flags
    notes: >-
      Raw extent is exactly 40 bytes, 0x082e2004..0x082e202b: the next
      separately linked function begins with push {r4-r10,lr} at
      0x082e202c. Every ARM B/BL word in osos.dec was decoded: 10 direct
      call sites, all plain bl at 0x082e16a8, 0x082e2344, 0x082e23fc,
      0x082e2e70, 0x082e3190, 0x082e3520, 0x082e4110, 0x082e415c,
      0x082e41b0 and 0x082e4840; there are no predicated calls or tail
      branches. The wrapper preserves all five ABI arguments, supplies
      -1 as the sixth stack argument to unported 0x082e202c, and returns
      that helper's r0 unchanged. Callers use the non-NULL return as a
      child path node while processing components, so the name describes
      the wrapper's observed role; the exact identity of 0x082e202c and
      its sixth argument's field meaning are deliberately not invented.
      Ghidra incorrectly declares this wrapper void; raw ARM and every
      caller demonstrate its pointer return. Target builds call the
      unported helper through its retailOS load address; host tests use a
      recording boundary to prove all argument pass-through, the -1
      default and return propagation.

  - address: 0x0812120c
    ghidra: (absent from functions.csv)
    name: ata_cmd_reset
    size: 136
    sig: "void ata_cmd_reset(u8 *cmd)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd}
    notes: >-
      Default constructor of the 0x58-byte ATA command block - the
      function that pins the whole layout. MISSING from
      decomp/functions.csv (Ghidra never made a function here); found by
      decoding its one reference, a tail `b` @ 0x08166078. Zeroes every
      field it owns and installs two nonzero defaults - device index
      +0x20 = 0xff (read back with ldrsb by the consumers, so -1 = no
      device) and block size +0x34 = 0x200 = one ATA sector. Deliberate
      gaps - +0x00..+0x0b, +0x0d..+0x0f, +0x1b, +0x1f..+0x23 and +0x4b
      are never written. The port stores volatile - plain stores let LLVM
      collapse the six trailing `strh #0` into __aeabi_memclr, which does
      not exist in osos (confirmed by reading the ARM disassembly of the
      first attempt).

  - address: 0x08121488
    ghidra: FUN_08121488
    name: ata_cmd_set_protocol
    size: 8
    sig: "void ata_cmd_set_protocol(u8 *cmd, u8 protocol)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: protocol}
    notes: >-
      `strb r1, [r0, #0x0c]`. 24 bl call sites, binary-scanned - the
      busiest member of the accessor family. Every one of the 22
      data-transfer command builders @ 0x082794xx..0x0827b1xx passes 1;
      the two non-data builders (0x0827b158, 0x08283490) pass 5, and
      those two also skip the buffer/offset/length/count setters and ask
      for a 30-second rather than a 10-second timeout. #[inline(never)] -
      a real bl target LLVM would otherwise inline away.

  - address: 0x081213ec
    ghidra: FUN_081213ec
    name: ata_cmd_set_device
    size: 8
    sig: "void ata_cmd_set_device(u8 *cmd, u8 device)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: device}
    notes: >-
      `strb r1, [r0, #0x20]`. 23 bl call sites, binary-scanned. Every
      builder feeds it the result of 0x08105a2c, which is a single
      `ldrsb r0, [r0, #8]` - a SIGNED byte - so the field is a device
      index whose "none" is -1, matching the 0xff ata_cmd_reset leaves.
      The store itself is a plain strb, so the port's parameter is u8.
      #[inline(never)].

  - address: 0x08105a2c
    ghidra: FUN_08105a2c
    name: ata_device_get_index
    size: 8
    sig: "i32 ata_device_get_index(const u8 *device)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: device}
    notes: >-
      `ldrsb r0, [r0, #8]; bx lr` - the device object's index getter,
      returning the SIGNED byte at device+8 sign-extended to 32 bits.
      42 bl call sites, verified by decoding every B/BL word in
      osos.dec: ALL unconditional, callers @ 0x082794f4..0x0827b160,
      every one an ATA command-block builder. The builders consume the
      result two ways, both visible in the builder @ 0x082794cc:
      verbatim as the command block's device index (ata_cmd_set_device,
      whose "none" is the -1 that the reset's 0xff reads back as), and
      as `(index & 0xf) << 4` for the device-select nibble of the
      taskfile's device/head byte. Ghidra's 8-byte extent is right for
      once: 0x08105a34 starts a different function (a 17-way jump
      table, `cmp r0, #0x11; addls pc, pc, r0, lsl #2`).
      #[inline(never)].

  - address: 0x081212c0
    ghidra: FUN_081212c0
    name: ata_cmd_set_timeout_ms
    size: 8
    sig: "void ata_cmd_set_timeout_ms(u8 *cmd, u32 timeout_ms)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: timeout_ms}
    notes: >-
      `str r1, [r0, #0x24]`. 23 bl call sites, binary-scanned. Observed
      arguments - 0x2710 = 10000 in every data-transfer builder, 0x7530
      = 30000 in the non-data ones, which is what identifies the field as
      a millisecond timeout. #[inline(never)].

  - address: 0x08121414
    ghidra: FUN_08121414
    name: ata_cmd_set_flags
    size: 8
    sig: "void ata_cmd_set_flags(u8 *cmd, u32 flags)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: flags}
    notes: >-
      `str r1, [r0, #0x10]`. 19 bl call sites, binary-scanned. A bit set,
      not an enum - the builders load 0x1000, 0x1080, 0x2000, 0x2080,
      0x40000, 0x41000 and 0x42000 as rotated immediates, and 0x08283474
      ORs 0x80000 into a value it already holds. #[inline(never)].

  - address: 0x081212e8
    ghidra: FUN_081212e8
    name: ata_cmd_get_lba48
    size: 76
    sig: "void ata_cmd_get_lba48(const u8 *cmd, u32 *lba_hi, u32 *lba_lo)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: lba_hi, param_3: lba_lo}
    notes: >-
      1 bl call site. Exact inverse of ata_cmd_set_lba48 - undoes the
      (current | previous << 8) interleave of the three extended
      taskfile pairs +0x4c/+0x4e/+0x50 into the same (hi = bits 32..47,
      lo = bits 0..31) split the packer takes, with the out-pointers in
      the original's register order (r1 = hi, r2 = lo). The port reads
      the halfwords aligned-volatile - read_unaligned lowers to pairs of
      ldrb on ARMv5, while the original uses ldrh, and the alignment is
      free because the same blocks receive word `str`s. LLVM finds a
      different mask/shift sequence for the same result; proven by a
      packer/unpacker round trip over six LBAs plus a hand-built
      taskfile.

  - address: 0x08121480
    ghidra: FUN_08121480
    name: ata_cmd_set_buffer
    size: 8
    sig: "void ata_cmd_set_buffer(u8 *cmd, void *buffer)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: buffer}
    notes: >-
      `str r1, [r0, #0x28]`. 16 bl call sites, binary-scanned. +0x28 is
      the only pointer field in the block; every builder sets it as the
      head of the (buffer, offset, length) triple - buffer here, offset
      +0x2c via ata_cmd_set_buffer_offset (usually 0), length +0x30 via
      ata_cmd_set_transfer_len - and the two non-data builders skip the
      whole triple. The port stores `buffer as u32` through the same
      volatile-word helper as the other word setters: exact on the
      32-bit target, a documented truncation on the 64-bit test host
      (the tests pin the low 32 bits, all the hardware ever sees).
      #[inline(never)] - a real bl target. match.py: the `str r1,
      [r0, #0x28]` matches; LLVM adds a frame prologue/epilogue, as with
      the sibling setters.

  - address: 0x081212a4
    ghidra: FUN_081212a4
    name: ata_cmd_set_transfer_len
    size: 8
    sig: "void ata_cmd_set_transfer_len(u8 *cmd, u32 length)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: length}
    notes: >-
      `str r1, [r0, #0x30]`. 16 bl call sites. Transfer length in bytes -
      the builders pass `sectors * bytes-per-sector`, the product of the
      caller's count argument and the device object's vtable slot +0x20.
      Not ported only because it is one instruction and its family is
      already represented; trivial to add.

  - address: 0x08121204
    ghidra: FUN_08121204
    name: ata_cmd_set_buffer_offset
    size: 8
    sig: "void ata_cmd_set_buffer_offset(u8 *cmd, u32 offset)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: offset}
    notes: >-
      `str r1, [r0, #0x2c]`. 16 bl call sites, always immediately after
      ata_cmd_set_buffer and immediately before ata_cmd_set_transfer_len,
      which is what makes the triple read as (buffer, offset, length).
      Usually 0; 0x0827a2b0 passes a caller argument.

  - address: 0x081213e4
    ghidra: FUN_081213e4
    name: ata_cmd_set_block_size
    size: 8
    sig: "void ata_cmd_set_block_size(u8 *cmd, u32 block_size)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: block_size}
    notes: >-
      `str r1, [r0, #0x34]`. 12 bl call sites, each passing the device
      object's bytes-per-sector (vtable slot +0x20) - the same value the
      transfer-length computation multiplies by. ata_cmd_reset defaults
      the field to 512.

  - address: 0x081213ac
    ghidra: FUN_081213ac
    name: ata_cmd_set_sector_count
    size: 8
    sig: "void ata_cmd_set_sector_count(u8 *cmd, u8 count)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: count}
    notes: >-
      `strb r1, [r0, #0x15]`. 10 bl call sites, each passing the caller's
      sector count masked to a byte (`and r1, r5, #0xff`), which is why
      the LBA28 flows also cap the count at 0x100 (0x082798e4).
      #[inline(never)].

  - address: 0x081211bc
    ghidra: FUN_081211bc
    name: ata_cmd_set_command
    size: 8
    sig: "void ata_cmd_set_command(u8 *cmd, u8 command)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: command}
    notes: >-
      `strb r1, [r0, #0x1a]`. 16 bl call sites. The ATA command register;
      the read-DMA flow @ 0x082798bc passes 0xC8. ata_cmd_set_lba48
      writes the same byte itself, which is why only the LBA28 flows need
      this setter. #[inline(never)].

  - address: 0x081211ec
    ghidra: FUN_081211ec
    name: ata_cmd_set_feature
    size: 8
    sig: "void ata_cmd_set_feature(u8 *cmd, u8 feature)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: feature}
    notes: >-
      `strb r1, [r0, #0x14]`. 4 bl call sites; 0x08279bec passes 0xD5.
      The ATA feature register. #[inline(never)].

  - address: 0x0812141c
    ghidra: FUN_0812141c
    name: ata_cmd_set_lba28
    size: 84
    sig: "u32 ata_cmd_set_lba28(u8 *cmd, u32 lba, u32 drive)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: lba, param_3: drive}
    notes: >-
      Legacy-taskfile LBA28 packer of the ATA command-block setter
      family @ 0x081211bc..0x08121488 (cmd byte +0x1a - the read flow
      @ 0x082798bc stores 0xC8 READ DMA there; flags +0x10; count byte
      +0x15; xfer bytes +0x30). Validates lba < 2^28 and drive <= 1
      (else 0xffffffff), stores lba[0..2] at +0x16..+0x18 and device
      byte 0x40|drive<<4|lba[24:27] at +0x19, returns 0. 7 call sites,
      binary-scanned, callers @ 0x082799c8..0x0827a800.

  - address: 0x0812134c
    ghidra: FUN_0812134c
    name: ata_cmd_set_lba48
    size: 96
    sig: "void ata_cmd_set_lba48(u8 *cmd, u32 lba_hi, u32 lba_lo, u32 drive, u32 count, u8 command)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: cmd, param_2: lba_hi, param_3: lba_lo, param_4: drive, param_5: count, param_6: command}
    notes: >-
      Extended-taskfile LBA48 packer - stores (current|previous<<8)
      register pairs +0x4c=lba[0]|lba[3]<<8, +0x4e=lba[1]|lba[4]<<8,
      +0x50=lba[2]|lba[5]<<8 (48-bit LBA split lba_hi=bits 32..47 /
      lba_lo=bits 0..31), count pair +0x52, command byte +0x1a, device
      halfword +0x56=(0x40|drive<<4)&0xff. No validation. Port writes
      the u16 fields write_unaligned (original strh needs 2-byte
      alignment; identical for every pointer it supported). 8 call
      sites, binary-scanned, callers @ 0x0827a2f4..0x0827b09c.

  - address: 0x080b3d40
    ghidra: FUN_080b3d40
    name: mov_atom_node_new
    size: 28
    sig: "MovAtomNode *mov_atom_node_new(u32 fourcc)"
    module: mov/atom_node
    status: ported
    vars: {param_1/r0: fourcc, r4: saved_fourcc}
    notes: >-
      MOV/MP4 demuxer atom-table node factory: operator_new(0x28) @
      0x082aadd4 (tag 2, ported in heap/veneers, called directly),
      then a tail branch (b, not bl) to the node initializer @
      0x0814d2b0 with (node, fourcc). Ghidra's 28-byte extent is
      exact (next function opens 0x080b3d5c with push {r4,lr});
      Ghidra emits NO C file for this function - the port is from
      raw bytes. 22 bl call sites, 0 predicated, 0 tail branches,
      verified by decoding every B/BL word in osos.dec; no DATA word
      references the address (never virtually dispatched). 19 sites
      load a fourcc literal into r0 immediately before the bl:
      tkhd/edts/elst/mdia/mdhd/minf/stbl/stsd/stco/co64/stsc/stsz/
      stts/stss @ 0x081c1d2c..0x081c1dbc and moov/mvhd/trak/hdlr/
      mdat @ 0x081c7d6c..0x081c7da8, each the file's ASCII bytes as
      a little-endian u32 ('tkhd' = 0x64686b74); the site @
      0x080a3f00 is inside the table search FUN_080a3ea0 itself,
      growing the tree with a placeholder on a populate miss. The
      tail callee @ 0x0814d2b0 (unported, 48 bytes, 12 instructions,
      stores only, no callees) is reproduced store-for-store in the
      original's register order as the private atom_node_init:
      dup_chain/child_a/child_b = NULL (+0x08/+0x00/+0x04), kind
      byte +0x21 = 0, offset and size u64s +0x10/+0x18 = -1
      (placeholder), fourcc at +0x24; it deliberately does NOT write
      +0x0c or the +0x20 flag byte (born as whatever the heap block
      held) and returns r0 = node unchanged. No NULL guard between
      the allocator and the init - on OOM the first store faults, so
      the factory never returns NULL on the stock build (the one
      caller that NULL-checks, FUN_080a3ea0, can never observe it).
      Node layout cross-checked three ways: the init, the getters @
      0x0814d230/0x0814d1ac/0x0814d270, and FUN_080a3ea0's word
      reads (puVar2[9] = +0x24 key, puVar2[5]/[4] = -1 placeholder
      test, puVar2[0..2] = links). Deviations: the tail branch
      becomes an ordinary call+return (ABI-identical result); the
      init is not a separate exported symbol. match.py --lib
      <worktree> --size 28: LLVM inlines the init body with the
      store order intact (str +8, +0, +4, strb +0x21, mvn, str
      +0x10/+0x14/+0x18, fourcc +0x24, str +0x1c) - the only deltas
      are the crate fp prologue and the archive bl relocation. Host
      tests drive the mock heap: allocation is exactly one tag-2
      operator_new(0x28), all initialized fields verified, and the
      +0x0c/+0x20/+0x22 poison bytes provably survive.

  - address: 0x081c8034
    ghidra: FUN_081c8034
    name: mov_atom_info
    size: 112
    sig: "void *mov_atom_info(void *this_unused, void *table, u32 fourcc, u64 *offset_out, u64 *size_out, u8 *flag_out)"
    module: mov/atom_info
    status: ported
    vars: {param_1: this_unused, param_2: table, param_3: fourcc, param_4: offset_out, param_5: size_out, param_6: flag_out}
    notes: >-
      MOV/MP4 demuxer per-atom info query. r0 arg dropped unread
      (mov r0,r1 @ 0x081c8040). Dereferences the table handle
      (0x08280114, ldr r0,[r0]), searches the fourcc-keyed atom table
      (0x080a3ea0, populate=1), and unpacks the node via three
      getters: u64 payload offset @ node+0x10 (0x0814d230), u64 total
      size @ node+0x18 (0x0814d1ac), u8 flag @ node+0x20 (0x0814d270).
      NULL node -> writes -1/-1/0 to the outputs and returns NULL;
      callers test the u64s against -1 for atom presence. All 33 call
      sites plain bl (0 predicated), binary-verified; no DATA word
      references the address. Keys are fourccs (elst/tkhd/mdhd/stts/
      stsd literals @ 0x081c3088..0x081c30a4 in caller FUN_081c2098).
      Node layout from getters/setters/init 0x0814d2b0: +0x00/+0x04
      child links, +0x08 duplicate-fourcc chain, +0x21 kind byte 0..5,
      +0x24 fourcc key; offset setter rejects < 0, size setter
      rejects < 8, both fields born -1. The payload-offset getter
      0x0814d230 is now ported directly as mov_atom_node_get_offset;
      the remaining four callees dispatch through MOV_ATOM_INFO_OPS.
      Ghidra C is faithful here (mvneq r0,#0 is move-NOT, i.e. -1).

  - address: 0x081c83b4
    ghidra: FUN_081c83b4
    name: record_manager_get
    size: 104
    sig: "void *record_manager_get(void)"
    module: app/record_manager
    status: ported
    renames:
      DAT_081c840c: record_manager_state (ready byte at 0x089cfd4c, guard word at +4)
      DAT_081c8410: record_manager (fixed object at 0x08ad1f70)
      FUN_081c8954: record_manager_constructor (unported RECORD_MANAGER_CTOR seam)
      DAT_081c8414: __dso_handle (0x089ca09c)
      DAT_081c8418: invalid_record_manager_destructor_pointer (0x081bdae8)
    notes: >-
      Binary-verified ADS function-local-static accessor. The raw ARM code is
      88 bytes at 0x081c83b4..0x081c840c, followed by four literal-pool words
      through 0x081c8418; the next function begins at 0x081c841c, making the
      true extent 104 bytes rather than Ghidra's code-only 88. It tests guard
      bit 0, calls cxa_guard_acquire @ 0x082ab31c, calls the unported fixed
      object constructor @ 0x081c8954, registers its return with cxa_atexit @
      0x082ab1c8 using __dso_handle 0x089ca09c, calls cxa_guard_release @
      0x082ab338, sets the adjacent ready byte if clear, then reloads and
      returns 0x08ad1f70. Decoding every ARM B/BL word in osos.dec found 20
      direct callers: 20 unconditional bl, 0 predicated bl, and no tail b.
      The object's constructor reaches its final mutex word at +0xcd4, so the
      crate-static replacement models the observed 0xcd8-byte fixed extent.
      The unported constructor is RECORD_MANAGER_CTOR with a documented
      zeroing default. The pool's handler pointer 0x081bdae8 is an interior
      tail into a frame teardown at 0x081bda64, not a callable function entry;
      its Rust cxa_atexit handler is therefore a no-op. retailOS never runs
      this shutdown chain. Host tests cover first construction/registration,
      bit-0 fast path, both guard edge states, constructor-result versus
      reloaded-object return, full default zeroing, and the inert handler.

  - address: 0x08167b54
    ghidra: FUN_08167b54
    name: recording_buffer_get
    size: 116
    sig: "void *recording_buffer_get(void)"
    module: app/recording_buffer
    status: ported
    renames:
      DAT_08167bb8: recording_buffer_state (ready byte at 0x089cfd44, guard word at +4)
      DAT_08167bbc: recording_buffer (fixed object at 0x08ad1c30)
      FUN_081681a4: recording_buffer_constructor (unported RECORDING_BUFFER_CTOR seam)
      DAT_08167bc0: __dso_handle (0x089ca09c)
      DAT_08167bc4: invalid_recording_buffer_destructor_pointer (0x0815d38c)
      FUN_08167e58: recording_buffer_followup_initialize (unported RECORDING_BUFFER_INITIALIZE seam)
    notes: >-
      Raw osos.dec confirms 100 instruction bytes from 0x08167b54 through
      0x08167bb8, followed by four literal-pool words through 0x08167bc4; the
      next function begins at 0x08167bc8, so the true extent is 116 bytes
      rather than Ghidra's code-only 100. This is an ADS function-local-static
      accessor: it tests guard bit 0, calls cxa_guard_acquire @ 0x082ab31c,
      calls the unported fixed-object constructor @ 0x081681a4, registers the
      constructor return with cxa_atexit @ 0x082ab1c8 using __dso_handle
      0x089ca09c, releases the guard @ 0x082ab338, then runs unported follow-up
      initializer @ 0x08167e58 while the adjacent ready byte is clear before
      setting that byte and returning the fixed object. Decoding every ARM B/BL
      immediate in osos.dec found exactly nine direct inbound call sites:
      0x08149268, 0x0827d950, 0x0827d964, 0x0827dac0, 0x0827dad4,
      0x0827db28, 0x0827dca8, 0x0827dcbc, and 0x0827dd5c; all are
      unconditional bl, with zero predicated bl and no direct tail b. The
      constructor's MeCCA_RecordingBuffer literal and its final store at +0x33c
      establish the 0x340-byte observed object extent. Target Rust calls both
      unported raw entries through seams; hosts install models. The handler
      word 0x0815d38c is a bl instruction inside a larger function rather than
      a callable entry, so the replacement cxa_atexit handler is intentionally
      inert. Host tests cover first construction/registration/initialization,
      both fast paths, the bit-0-clear nonzero-guard case, constructor-result
      versus fixed-object return, and the observed object extent.

  - address: 0x081c8a84
    ghidra: FUN_081c8a84
    name: list_find_by_id16
    size: 56
    sig: "IdNode *list_find_by_id16(IdNode *head, u32 key)"
    module: util/list_find
    status: ported
    vars: {param_1: head, param_2: key}
    notes: >-
      Singly-linked-list search by u16 id (node +0 next, +4 id, ldrh).
      Miss fallback - key 0 or 0xffffffff returns head ("any/default";
      -1 can never match a 16-bit id), other keys return NULL. 7 call
      sites, binary-scanned, all in the MPEG-4 container parser cluster
      @ 0x081c38e0..0x081c8a38 (owns the moov/trak/mdhd atom literals),
      which looks up track nodes by id.

  - address: 0x0810b610
    ghidra: FUN_0810b610
    name: strlen_safe
    size: 36
    sig: "usize strlen_safe(const u8 *s)"
    module: libc/strlen_safe
    status: ported
    vars: {param_1: s}
    notes: >-
      Byte-identical second link-time copy of the NULL-guarded strlen
      @ 0x082770bc (verified equal). Served by the existing
      libc/strlen_safe port - hook both addresses to the one symbol.
      5 call sites, binary-scanned (0x0810b538, 0x081132d8, 0x0811334c,
      0x08297d74, 0x08297d80).

  - address: 0x0817ee04
    ghidra: FUN_0817ee04
    name: app_controller_get
    size: 44
    sig: "void *app_controller_get(void)"
    module: app/singletons
    status: ported
    notes: >-
      Lazy singleton getter, 1108 bl call sites - the hottest function
      in the mid region. If the cache word @ 0x089cc648 (the +8 slot of
      the global @ 0x089cc640) is 0, allocates 0xE8 bytes via operator
      new @ 0x082aadd4 (already ported, called directly), constructs via
      FUN_081847fc, caches the ctor's return and RELOADS the slot before
      returning. The object is the application controller - views hand
      themselves to it (0x08124af4), callers poke a mode halfword at
      +0x80, and its ctor resolves a registry target through the manager
      0x081883fc and vtable slot +0xe8. Deviations - the ctor is behind
      the SINGLETON_CTORS dispatch table (house pattern) with a
      documented zeroing stub default, and the cache is the crate static
      APP_CONTROLLER. NOT HOOK-READY - until FUN_081847fc is ported the
      default hands out a zeroed block with no vtable, so branching
      stock code at 0x0817ee04 would break it.

  - address: 0x0817ee34
    ghidra: FUN_0817ee34
    name: primary_or_demo_mode_keyed_object
    size: 92
    sig: "void *primary_or_demo_mode_keyed_object(FallbackKeyedObjectContext *context, u32 key, u8 *used_demo_mode)"
    module: app/fallback_keyed_object
    status: ported
    vars:
      param_1: context
      param_2: key
      param_3: used_demo_mode
      iVar1: primary_or_fallback
      piVar2: demo_mode
      uVar3: fallback_candidate
    notes: >-
      True extent is 0x0817ee34..0x0817ee90: raw ARM starts with
      `push {r4,r5,r6,lr}`, ends in `pop {r4,r5,r6,pc}`, and the next
      separately entered function begins at 0x0817ee90. 11 direct BL
      call sites, all unconditional (0x0817eb0c, 0x0817ec40, 0x08181170,
      0x08181afc, 0x08182294, 0x081823c0, 0x08183664, 0x08183c7c,
      0x08183f44, 0x08184334, 0x08184670); no predicated or tail-B
      references. Clears a non-NULL output flag, returns the non-NULL
      result from the +0x38 primary resolver (FUN_081b9a68), otherwise
      calls the ported TCDemoMode accessor (0x081883fc), dispatches its
      +0xec slot with key, casts the candidate through ported
      object_cast_to_class (0x08275b9c) for class 0x3b80, and marks the
      flag only after a successful fallback cast. Target calls the
      unported primary resolver at its verified address; host tests use
      a volatile seam. No NULL guards were added for framework inputs or
      vtables. Ghidra incorrectly declared this pointer-returning body
      void.

  - address: 0x08173848
    ghidra: FUN_08173848
    name: app_screen_get
    size: 44
    sig: "void *app_screen_get(void)"
    module: app/singletons
    status: ported
    notes: >-
      Same lazy-singleton shape as 0x0817ee04 but a 0x850-byte object
      constructed by FUN_08177a78, cached in the word @ 0x089cc1bc. 140
      bl call sites. The object is the screen/layout side - callers load
      layout resources into it (0x08181110 next to the
      "GotoExtraInfoLayout"/"GotoGenius" literals, 0x08174300 with 0x80)
      and then hand it to the controller (0x08183950(controller,
      screen)); its ctor chains eleven 0x0810ebbc sub-ctors and
      registers the result with class id 0x7800. Same deviations and the
      same NOT-HOOK-READY caveat as 0x0817ee04.

  - address: 0x0817ceb4
    ghidra: FUN_0817ceb4
    name: media_player_get
    size: 44
    sig: "void *media_player_get(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0817cee0: media_player_holder (global @ 0x089ca7c8; instance in its +4 slot)
      DAT_0817cee4: MEDIA_PLAYER_SIZE
      FUN_082aadd4: operator_new
      FUN_0817d970: media_player_ctor (SINGLETON_CTORS.media_player slot)
    notes: >-
      Same lazy-singleton shape as 0x0817ee04 but a 0xA6C-byte object
      (the size is a pool literal @ 0x0817cee4, too big for an ARM
      immediate), cached in the word @ 0x089ca7cc - the +4 slot of the
      global @ 0x089ca7c8. 101 bl call sites. The object is the
      TPodMediaPlayer, the one singleton whose class name survives in
      the image: its ctor @ 0x0817d970 hands the "TPodMediaPlayer"
      literal (@ 0x0817db74) to the class-name factory @ 0x0822053c and
      chains sub-ctors out past +0x9c4 (vtable 0x089a75c8 at +0x14).
      Feeds the 264-call-site leaf FUN_08259594 (returns instance+0x14
      or NULL) - ported as media_player_interface_get (see 0x08259594).
      Same deviations and the
      same NOT-HOOK-READY caveat as 0x0817ee04: the ctor sits behind the
      SINGLETON_CTORS dispatch table with a zeroing stub default and the
      cache is the crate static MEDIA_PLAYER_INSTANCE.

  - address: 0x0817cab0
    ghidra: FUN_0817cab0
    name: media_player_post_kind
    size: 64
    sig: "u32 media_player_post_kind(MediaPlayer *this, u32 kind)"
    module: app/media_player
    status: ported
    renames:
      param_1/r0/r4: this (the TPodMediaPlayer object)
      param_2/r1/r5: kind
      FUN_082669e4: message_kind_arena_pool (ported)
      FUN_0826c0d8: fixed_block_pool_alloc (ported)
      FUN_08266a48: message_kind_construct (ported)
      FUN_08110fdc: queued_message_post (ported)
    notes: >-
      The TPodMediaPlayer's fire-and-forget kind notification. Exact
      64-byte extent, 0x0817cab0..0x0817caf0 - Ghidra's 64 is right for
      once; the next function opens at 0x0817caf0 with `ldrb r0, [r0,
      #0x93d]`. 23 bl call sites, 0 predicated, 0 tail b, no DATA word
      references - binary-verified by decoding every B/BL word and
      scanning for the address in osos.dec. Ghidra's listing shows only
      22: the 23rd site @ 0x08179db4 is absent from decomp/osos.asm
      entirely. Never dispatched virtually. Algorithm: pop an 8-byte
      block off the MessageKind arena (message_kind_arena_pool +
      fixed_block_pool_alloc(pool, 8); the pool's block_size == 8
      exact-equality test always matches, so the operator_new fallback
      only fires on exhaustion), run message_kind_construct(block,
      kind) over it - no payload, no derived vtable, the base message
      vtable 0x089a3788 stays final - then queued_message_post(message,
      [this + 0x9ac], no_wait = 1, reply_queue = 0, flags = 0) with the
      flags word passed in the pushed r3 stack slot, and collapse the
      poster's result with `cmp r0, #0; movne r0, #1` (1 for any
      nonzero, else 0). No NULL guard anywhere: a doubly-exhausted
      arena hands NULL to the base constructor, which stores through
      it - reproduced. Every call site sits inside a media-player
      method of the 0xA6C-byte TPodMediaPlayer singleton
      (media_player_get @ 0x0817ceb4): the callers use param_1's vtable
      at +0x14, the interface media_player_interface_get returns, and
      +0x9ac sits inside the 0xA6C object. All but one site pass a
      pool-literal kind tag from the contiguous cluster 0x80008,
      0x8000a..0x8000f (decoded from the literal words); the site in
      FUN_0817c9a0 forwards a computed value. Paired with the virtual
      call on the +0x14 interface callers make right after, these are
      player state-change notifications. Deviations - all four callees
      are ported and are the wired defaults of MEDIA_PLAYER_POST_OPS;
      the slots stay replaceable for host-side recording (the
      queued_message.rs precedent) because a direct call is untestable
      on host: the arena accessor's host constructor default panics by
      design and the poster's failure edge dispatches the release
      through the firmware vtable word 0x089a3788, unreadable on host.
      The +0x9ac message-target word is modelled as a raw u32 in a
      repr(C) prefix struct (a host pointer at logical offset 0x9ac
      would be padded to 0x9b0), read as the target's 4-byte word
      exactly like the `ldr r1, [r4, #0x9ac]`. Host tests (5) pin the
      pool -> alloc(8) -> construct(kind) -> post(1, 0, 0) call order
      and argument routing, the collapse of any nonzero poster result
      to 1 and of 0 to 0, the exact +0x9ac field read against poisoned
      neighbours, verbatim forwarding of every observed kind literal
      (0x80008..0x8000f), and the guardless NULL path into the base
      constructor and poster. match.py structural: the four-call
      sequence with the 8-byte size, [r4, #0x9ac] target load, stack
      flags slot, no_wait = 1 and the cmp/movne collapse all survive;
      expected differences are the ops table's literal load + blx
      dispatch and LLVM's fp frame.

  - address: 0x08160148
    ghidra: FUN_08160148
    name: lazy_singleton_0x8c
    size: 48
    sig: "void *lazy_singleton_0x8c(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_08160174: singleton_cache (0x089ca7f0)
      FUN_082aadd4: operator_new
      FUN_08160534: singleton_0x8c_ctor (SINGLETON_CTORS.singleton_0x8c slot)
    notes: >-
      Raw-byte extent is 48 bytes: 44 bytes of code followed by the
      literal-pool cache address 0x089ca7f0 at 0x08160174; the next
      function starts at 0x08160178. It is the four-step lazy singleton
      idiom: load cache, allocate 0x8c with the ported operator_new when
      NULL, call the unported constructor, store its returned pointer,
      then reload and return the cache. Decoding every ARM B/BL word in
      osos.dec finds exactly 32 call sites, all unconditional bl (zero
      predicated forms and zero plain b tails). The constructor plants the
      media-player interface vtable 0x089a75c8 and connects to
      TMediaNowPlayingCntlr, but no class name or registry id survives;
      lazy_singleton_0x8c therefore records only the verified shape. The
      port uses the app/singletons dispatch seam and crate-static cache
      instead of runtime-initialized RAM; its zeroing constructor default
      means it is not hook-ready until FUN_08160534 is ported. Host tests
      prove exact allocation, constructor argument, returned-pointer cache
      reload, cache hit, and NULL-result retries. match.py structural
      review requested for lazy_singleton_0x8c.

  - address: 0x08259594
    ghidra: FUN_08259594
    name: media_player_interface_get
    size: 20
    sig: "void *media_player_interface_get(void)"
    module: app/singletons
    status: ported
    renames:
      FUN_0817ceb4: media_player_get
      iVar1: player (TPodMediaPlayer instance, then instance + 0x14)
    notes: >-
      `push {r4,lr}; bl media_player_get; cmp r0,#0; addne r0,r0,#0x14;
      pop {r4,pc}` - fetch the TPodMediaPlayer singleton and return a
      pointer to its interface sub-object at +0x14, or NULL unchanged
      when the singleton is NULL (the `addne`: the NULL is NOT offset).
      264 bl call sites - one of the hottest unported leaves. The +0x14
      sub-object is the player's vtable-bearing interface base: the ctor
      @ 0x0817d970 forms `r6 = this+0x14` first thing and plants the
      vtable literal (pool @ 0x0817db6c) at [r6], and every caller uses
      the result purely as a `this` for virtual dispatch
      (`ldr r1,[r0]; ldr r1,[r1,#N]; blx r1`, slots out past +0x110 -
      e.g. the Coverflow album-play handler FUN_0816d6c0 dispatches
      slots +0x10c and +0xac through it). match.py: same 5-instruction
      push/bl/cmp/addne/pop skeleton; LLVM emits an fp/lr frame and an
      intra-archive bl relocation instead of ADS's r4/lr frame. Same
      NOT-HOOK-READY caveat as the singleton getters.

  - address: 0x0825a0c0
    ghidra: FUN_0825a0c0
    name: lazy_singleton_0x8c_interface_get
    size: 20
    sig: "void *lazy_singleton_0x8c_interface_get(void)"
    module: app/singletons
    status: ported
    renames:
      FUN_08160148: lazy_singleton_0x8c
      iVar1: singleton
    notes: >-
      Raw bytes are the five-instruction `push {r4,lr}; bl
      lazy_singleton_0x8c; cmp r0,#0; addne r0,r0,#0x14; pop {r4,pc}`
      leaf. Ghidra's 20-byte extent is exact: the next function opens at
      0x0825a0d4. It returns the unidentified 0x8c singleton's interface
      sub-object at +0x14, preserving NULL rather than offsetting it. The
      0x8c constructor installs the shared media-player interface vtable
      0x089a75c8 at offset zero, but recovers no class name or registry id,
      so the symbol names only the verified singleton/interface shape.
      Decoding every ARM B/BL word in osos.dec finds exactly 14 direct call
      sites, all unconditional bl (zero predicated forms and zero plain-b
      tails); callers immediately dispatch through the returned vtable
      (observed slots +0, +4, +0x28, +0x40, +0x44, +0x4c, +0x6c, and
      +0x70). Deviation: inherits lazy_singleton_0x8c's crate-static cache
      and constructor dispatch seam rather than the runtime cache word
      0x089ca7f0, so it is not hook-ready until FUN_08160534 is ported.

  - address: 0x0813b6f4
    ghidra: FUN_0813b6f4
    name: context_target_id
    size: 12
    sig: "u32 context_target_id(void)"
    module: app/context
    status: ported
    vars: {DAT_0813b700: APP_CONTEXT}
    notes: >-
      `ldr r0, =0x089cc7ec; ldr r0, [r0, #8]; bx lr` - the word at +8 of
      the global application context. 254 bl call sites (127 distinct
      callers, mostly view classes @ 0x0839xxxx..0x083bxxxx), the
      hottest function in the mid region after the two singleton
      getters. The field is a numeric class/resource id, not a pointer,
      and 0 means "none" - every reader has the shape `id = get(); if
      (id) { target = manager->vtable[0xe8](manager, id); ... }`, i.e.
      it names a pending target to resolve through the by-id registry
      (0x081883fc fetches the manager for the sibling id 0x8080;
      0x081d2184 is the lookup). Its one writer, 0x082028d4 @
      0x08202904, stores 0x7a88 (an id that also appears in the table @
      0x083f61b0 next to the "None" literal) right after raising the
      flag byte @ 0x089d0074. Rest of the context object - +0x4 is a
      trace nesting depth (inc @ 0x08134af8, dec + assert-nonnegative @
      0x081e9e4c, tested @ 0x08134674 to gate the "controller_..." event
      emit), +0xc is referenced @ 0x0829b088. Deviation - the object is
      the crate static APP_CONTEXT rather than living at 0x089cc7ec
      (that RW page is runtime-initialized; the image holds stale UI
      strings there), zeroed = the pre-init state.

  - address: 0x081ee17c
    ghidra: FUN_081ee17c
    name: cursor_init
    size: 16
    sig: "void cursor_init(Cursor *cursor, Collection *collection)"
    module: util/cursor
    status: ported
    vars: {param_1: cursor, param_2: collection}
    notes: >-
      Collection-cursor family with 0x081ee138/0x081ee18c. Init - stores
      the collection in slot 0 and sentinel -2 ("before first") in slot
      1. 111 bl call sites (re-scanned: the earlier note had the three
      counts rotated - the real split is advance 113 / init 111 /
      invalidate 129). The original leaves r0 = cursor; no caller
      consumes it, so the port is void.

  - address: 0x081ee138
    ghidra: FUN_081ee138
    name: cursor_advance
    size: 68
    sig: "u32 cursor_advance(Cursor *cursor, void *out)"
    module: util/cursor
    status: ported
    vars: {param_1: cursor, param_2: out}
    notes: >-
      Advance - index becomes 0 after the -2 init sentinel, else +1;
      calls the collection's vtable slot +0x3c (item_at(index, out))
      and invalidates the cursor (index = -1) on a 0 result. 113 bl call
      sites. Ghidra types it void, but the original never touches r0
      after `blx r3`, so the accessor's result IS the return value - the
      canonical loop @ 0x0810e0ac branches on it. Dispatch goes through
      the collection's own vtable pointer (faithful; test vtables are
      honored). Advancing an exhausted cursor restarts at 0 (-1 + 1),
      preserved.

  - address: 0x081ee18c
    ghidra: FUN_081ee18c
    name: cursor_invalidate
    size: 12
    sig: "void cursor_invalidate(Cursor *cursor)"
    module: util/cursor
    status: ported
    vars: {param_1: cursor}
    notes: >-
      Sets the cursor index (slot 1) to -1, leaving the collection slot
      alone. Pairs with 0x081ee17c/0x081ee138. 129 bl call sites.

  - address: 0x081ee660
    ghidra: FUN_081ee660
    name: class_8900_work_queue_enqueue
    size: 68
    sig: "void class_8900_work_queue_enqueue(Class8900WorkQueue *this, Class8900QueuedWork *work)"
    module: app/class_8900_work_queue
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: work
      param_1_plus_0x14: mutex
      param_1_plus_0x1c: work_available
      param_1_plus_0x28: head
      param_1_plus_0x2c: tail
      param_2_plus_0x18: next
      FUN_0807f5c4: mutex_lock
      FUN_0807f5cc: condvar_broadcast
      FUN_0807f6a0: mutex_unlock
    notes: >-
      Raw ARM extent is exactly 68 bytes: 17 instructions from 0x081ee660
      through the tail branch to mutex_unlock at 0x081ee6a0; the next
      separately linked function begins with push {r4, r5, r6, r7, r8, lr}
      at 0x081ee6a4. A complete ARM B/BL word scan finds exactly nine direct
      call sites (0x081ee5e0, 0x081ee640, 0x081ee998, 0x081eebdc, 0x081eec78,
      0x081eed10, 0x081eedec, 0x081eeef0, and 0x081eef44), all plain
      unconditional bl; no predicated BL, direct tail branch, or aligned
      data word references this entry. The queue locks its +0x14 Mutex,
      writes the incoming 32-byte work record into the empty FIFO head or
      the old tail's +0x18 next link, records it as tail, broadcasts the
      +0x1c CondVar, and tail-branches to mutex_unlock. It never clears the
      incoming record's next link; callers' constructors own that setup.
      Rust directly calls the already ported mutex/condvar functions; the
      original tail-call's scratch return is not part of this void ABI.

  - address: 0x08120604
    ghidra: FUN_08120604
    name: surface_config_init
    size: 80
    sig: "void surface_config_init(u8 *config)"
    module: drivers/surface
    status: ported
    vars: {param_1: config}
    notes: >-
      Default constructor for the 0x40-byte display-LAYER configuration
      descriptor - NOT storage. The earlier note filed it under the ATA
      command-block cluster purely on address adjacency; every one of
      the 15 bl call sites (+1 tail b) is display code, and 0x0816e500 /
      0x0829635c both init this block then store width 0x140 = 320 and
      height 0xf0 = 240 (the Classic 6G panel) before handing it to the
      layer applier 0x08120978, which copies it into the layer object
      from 0x081d9064(display, layer). Writes bytes +0=2 (pixel format;
      the applier gives 3 special handling), +1=3 (format variant),
      zeroes the geometry words +4/+8 (origin), +0xc/+0x10 (size),
      +0x14/+0x18 (buffer size), +0x24/+0x28 (source offset), +0x30, and
      the flag bytes +0x2c/+0x2d, and arms +0x1c/+0x20 with -1 = "auto"
      (the applier resolves -1 to the clamped buffer size and writes it
      back into the caller's block). Bytes +2/+3, +0x2e/+0x2f and
      +0x34..+0x3f are deliberately left uninitialized. Stores are
      write_volatile - plain writes let LLVM collapse the zero run into
      a nonexistent __aeabi_memclr and byte-store the rest.

  - address: 0x08138bd0
    ghidra: FUN_08138bd0
    name: registry_find_for_slot
    size: 88
    sig: "SlotRecord *registry_find_for_slot(void *this, u32 slot, u32 id)"
    module: util/table_find
    status: ported
    vars: {param_1: this, param_2: slot, param_3: id}
    notes: >-
      Linear scan of a 275-entry table of 12-byte records for the one
      whose u16 id (+4) equals the id argument AND whose mask byte (+6)
      has bit (1 << slot) set; returns the record or 0. 8 bl call sites.
      Both anchors are now resolved - the base 0x089ccc38 is a fixed
      address (a static array in the runtime-initialized RW page, not a
      pointer global) and the "count" DAT_08138c2c is a literal-pool
      immediate 0x113 = 275, not a global at all. Record layout - +0
      descriptor pointer (0x08138e94 reads a u16 at descriptor+0x3c),
      +4 u16 id, +6 u8 slot mask, +8 opaque handle (fed to the decoder
      0x082cdc94 by 0x08138650). `slot` is 0..2 - every caller asserts
      it (0x0813858c, 0x08193e84) and each slot owns a 0x114-byte state
      record in the array @ 0x08ad0f34 whose +4 field takes this
      function's result (the slot's current descriptor); id 0 is the
      default. param_1 is a `this` the original never reads (mov r0, #0
      kills it before the loop) but all 8 call sites pass it. Faithful
      details - ARM register shift (slot 32..255 gives mask 0) then
      & 0xff so slots >= 8 never match; the ldrh id compare is
      zero-extended against the full u32 argument; do-while, so index 0
      is examined before the count is tested. Deviation - the table is
      the crate static SLOT_RECORDS, zeroed (block_mgr.rs precedent).

  - address: 0x081fe954
    ghidra: FUN_081fe954
    name: slot_table_register
    size: 60
    sig: "u32 slot_table_register(void *this, i32 index, i32 kind, u32 value_a, u32 value_b)"
    module: app/slot_table
    status: ported
    renames:
      param_1/r0: unused_this
      param_2/r1: index
      param_3/r2: kind
      param_4/r3: value_a
      param_5/[sp+4]: value_b
      DAT_081fe990: SLOTS (0x08ac8b94 runtime-RAM registration table)
    notes: >-
      Raw ARM is 15 instruction words from 0x081fe954 through 0x081fe98c,
      followed by its 0x08ac8b94 literal at 0x081fe990; the next function
      begins at 0x081fe994. The code accepts only signed index < 17 and
      signed kind < 3, returning 9 before touching the table otherwise.
      It indexes 16-byte slots, tests occupied byte +0, and, only when zero,
      stores kind/value_a/value_b at +4/+8/+0xc with stmib before storing
      occupied = 1. A busy slot still returns 0 unchanged. Exactly 9 direct
      callers decode as unconditional bl (no predicated bl or tail b);
      they use in-range constant indices. The first this argument is never
      read. Negative index and kind pass the signed comparisons; the Rust
      port preserves the pointer calculation and signed kind semantics, but
      cannot host-test negative index without intentionally corrupting memory
      before the static table.

  - address: 0x081fea54
    ghidra: FUN_081fea54
    name: slot_table_clear
    size: 52
    sig: "u32 slot_table_clear(void *this, i32 index)"
    module: app/slot_table
    status: ported
    vars: {param_1: this, param_2: index, DAT_081fea88: SLOTS}
    notes: >-
      Releases one entry of the 17-slot registration table @ 0x08ac8b94
      (17 * 16 = 0x110 bytes, confirmed by the builder 0x081ff758 which
      memsets 0x110 then seeds every +4 with 0x7fffffff). Restores +4 =
      0x7fffffff, zeroes +8/+0xc and drops the occupied byte at +0;
      returns 0, or 9 when index >= 17. 10 bl call sites. Slot layout -
      +0 occupied flag, +4 kind (0..2 when registered, 0x7fffffff =
      free), +8/+0xc payload words. The registrar 0x081fe954 is the
      twin - it refuses index >= 17 OR kind >= 3 with the same error 9
      and stores kind/value_a/value_b with one stmib only if the slot is
      free. Every call site pairs them (`if (enabled) register(this,
      SLOT, ...) else clear(this, SLOT)`) with SLOT a constant (10 and
      16 seen), so index is a slot id. param_1 is an unread `this`. The
      bounds test is SIGNED (cmp #17 / bxge), so a negative index would
      write before the table - reproduced, not hardened. Deviation - the
      table is the crate static SLOTS (0x08ac8b94 is past the end of the
      image, pure runtime RAM); stores are write_volatile since no
      ported code reads the table and plain stores get dropped as dead.

  - address: 0x0811000c
    ghidra: FUN_0811000c
    name: node_list_construct
    size: 52
    sig: "NodeList *node_list_construct(NodeList *this)"
    module: app/node_list
    status: ported
    vars: {param_1: this, iVar1: drain_state, DAT_08110040: node_list_vtable}
    notes: >-
      Constructs the fixed view-list singleton in place. Installs vtable
      0x0898165c at +0x00; clears head +0x04, stop key +0x08, and the
      draining flag byte +0x0c; constructs its embedded drain state at
      +0x10 through 0x08271cec/0x08275bb8 (vtable 0x089a5d0c, then
      zeroes +0x14/+0x18/+0x1c); and clears callback words
      +0x20/+0x28/+0x2c. Returns this in r0. The word at +0x24 is
      deliberately untouched. Reference C:
      decomp/c/010/0811000c_FUN_0811000c.c and
      decomp/c/026/08271cec_FUN_08271cec.c. The Rust port uses target
      offsets and volatile stores, preserving both ordering and the
      partial-initialization behavior rather than zeroing the object.
      UPDATE: 0x08271cec is now ported (cxx/observable_array,
      observable_array_construct); the four inlined stores at +0x10..+0x1c
      were replaced by the real call, and the trailing stores are again
      addressed off ITS return value as the stock `sub r0, r0, #16` does.
  - address: 0x0810fa90
    ghidra: FUN_0810fa90
    name: list_count_until_match
    size: 84
    sig: "u32 list_count_until_match(NodeList *list)"
    module: app/node_list
    status: ported
    vars: {param_1: list}
    notes: >-
      Walks the linked list at list+4 counting nodes; when the stop key
      at list+8 is nonzero, stops after the node whose vtable+0x68 call
      returns it, so the result is that node's 1-based position, else
      the total count. 125 bl call sites. The list object is a C++
      singleton - its accessor 0x0810fa30 is a function-local-static
      initializer (guard word 0x089cc834, ADS guard helpers 0x082ab31c/
      0x082ab338) returning the fixed object 0x08a79c74; 0x0810fa88
      hands out its head. Callers are view classes @ 0x0839xxxx storing
      the result in a field. Sibling 0x0810fb48 drains the same list
      through the same +0x68 slot and stop key. Node layout - +0 vtable,
      +0x14 next. Faithful details - the counter is bumped BEFORE the
      key test so the matching node is included, and the stop key is
      re-read after the virtual call (the original issues two
      `ldr r0, [r5, #8]`), so a callee that rewrites it is honored.
      Dispatch goes through the node's own vtable pointer, no hook
      table.
  - address: 0x0810faf4
    ghidra: FUN_0810faf4
    name: list_count_unflagged_before_key
    size: 84
    sig: "u32 list_count_unflagged_before_key(NodeList *list, u32 stop_key)"
    module: app/node_list
    status: ported
    vars: {param_1: list, param_2: stop_key, piVar3: node, piVar1: node_flags, iVar2: node_key, iVar4: unflagged_count}
    notes: >-
      Counts clear +0x20 node flags before a matching nonzero node key.
      Starting at list+0x04, each iteration dispatches the current
      node's own vtable+0x68 key accessor first. A returned nonzero key
      equal to stop_key ends the walk without inspecting or counting that
      node; a returned key of zero never matches. Otherwise the function
      reads the +0x20 flag, increments the wrapping count only if clear,
      then follows +0x14. Reference C:
      decomp/c/010/0810faf4_FUN_0810faf4.c. Host tests cover a null head,
      match, miss (including a zero node key), and callback-before-flag
      ordering with mocked vtables/nodes.
  - address: 0x0810fb48
    ghidra: FUN_0810fb48
    name: node_list_drain
    size: 640
    sig: "void node_list_drain(NodeList *list, u32 transition_mode, u32 successor_mode, u32 successor_key)"
    module: app/node_list
    status: ported
    vars: {param_1: list, param_2: transition_mode, param_3: successor_mode, param_4: successor_key, piVar4: removed_node, cVar5: keep_draining, uVar2: transition_action, uVar3: preparation_result, local_28: retained_node, local_24: retained_value}
    notes: >-
      Drains the view list (reference C:
      decomp/c/010/0810fb48_FUN_0810fb48.c). Sets the +0x0c draining
      flag, removes and releases each current head, and leaves the first
      head whose +0x20 continuation byte is clear (or whose +0x68 key
      matches nonzero list+0x08 stop_key). A transition-marked (+0x21)
      node is evicted through vtable+0x58; otherwise its +0x74/+0x80
      preparation and +0x54 inactive/active lifecycle calls run. After
      a removal it derives the successor transition mode from +0x68 or
      +0x98, handles the successor's +0x6c/+0x78 capture, releases every
      retained node enumerated by the embedded +0x10 state's vtable+0x3c,
      and finishes it through +0xbc. Finally clears the flag, consumes
      the +0x20 completion context/byte through the list vtable, then
      invokes the +0x28 final callback. The null successor after a
      removal is the stock non-returning assertion path. Host tests
      verify lifecycle/capture/release order, retained-node ownership,
      terminal-head preservation, and completion callback order.
  - address: 0x0810fe7c
    ghidra: FUN_0810fe7c
    name: node_list_enqueue
    size: 232
    sig: "void node_list_enqueue(NodeList *list, Node *node, u32 mode, void *context)"
    module: app/node_list
    status: ported
    vars: {param_1: list, param_2: node, param_3: mode, param_4: context, iVar1: blocks_enqueue, piVar3: observed_head, piVar2: head, uStack_24: enqueue_scope_mode, uStack_20: enqueue_scope_context}
    notes: >-
      Enqueues a view-list node with a scope around the whole operation
      (reference C: decomp/c/010/0810fe7c_FUN_0810fe7c.c). When +0x0c
      says a drain is active, it defers the node in +0x20 and mode's low
      byte in +0x24; node_list_drain later passes that pair to its
      completion slot. Otherwise it advances a blocking head through
      node vtable +0x70/+0x7c until the head disappears, remains the
      same, or reports clear; fetches FUN_081d2204's observer and calls
      its vtable+0x1ec; then records the low mode byte at node+0x18,
      links node+0x14 to the old head, installs it as head, and dispatches
      node vtable+0x78. The optional list +0x28 callback runs only on
      that immediate path. Scope setup/teardown (FUN_08266a48 and
      thunk_FUN_08275bc8) and the observer accessor are explicit
      NodeListEnqueueOps seams, while the observer's +0x1ec and node
      slots remain true vtable calls. Focused host tests pin immediate
      callback order, head traversal/linkage, low-byte truncation, and
      drain deferral.
  - address: 0x0810fdc8
    ghidra: FUN_0810fdc8
    name: node_list_advance_until
    size: 164
    sig: "void node_list_advance_until(NodeList *list, i32 target, i32 threshold)"
    module: app/node_list
    status: ported
    vars: {param_1: list, param_2: target, param_3: threshold, iVar1: measurement, uVar2: is_subsequent}
    notes: >-
      Advances the list through vtable+0x90 until either its vtable+0x94
      measurement minus a nonzero threshold is no longer positive as a
      wrapping signed i32 difference, or,
      when the threshold is zero, the current head is NULL or its own
      vtable+0x68 key equals target. The +0x90 callback receives
      is_subsequent=0 initially, then 1, with its middle argument always
      zero; it owns the actual advancement. Reference C:
      decomp/c/010/0810fdc8_FUN_0810fdc8.c. Host tests cover wrapping
      signed threshold comparison, both virtual-dispatch modes, match/empty
      termination, and iteration-flag ordering.

  - address: 0x0810fa30
    ghidra: FUN_0810fa30
    name: node_list_get
    size: 72
    sig: "NodeList *node_list_get(void)"
    module: app/node_list
    status: ported
    vars: {iVar1: acquired, uVar2: this, DAT_0810fa78: guard-pool-word
      (0x089cc834 = NODE_LIST_GUARD), DAT_0810fa7c: object-pool-word
      (0x08a79c74 = NODE_LIST storage), DAT_0810fa80: DSO_HANDLE
      (0x089ca09c = __dso_handle), DAT_0810fa84: dtor-pool-word
      (0x0810516c = node_list_destructor)}
    notes: >-
      The view-list singleton's accessor - a textbook ADS
      function-local static over a fixed .bss object, NOT the
      operator_new cache pattern of app/singletons.rs (which is why it
      lives in app/node_list.rs beside the NodeList it returns and its
      consumers, not in singletons.rs). Literal pool binary-verified:
      guard 0x089cc834 (@ 0x0810fa78), object 0x08a79c74 (@
      0x0810fa7c), __dso_handle 0x089ca09c (@ 0x0810fa80), destructor
      0x0810516c (@ 0x0810fa84). 168 bl call sites, binary-verified.
      Shape: tst guard,#1 fast path; cxa_guard_acquire (0x082ab31c);
      ctor FUN_0811000c(object) returning this in r0; cxa_atexit(this,
      dtor, dso) (0x082ab1c8); cxa_guard_release (0x082ab338); reload
      the OBJECT address - never the ctor's return - and return it.
      The guard pair and cxa_atexit are ported and called directly;
      the ctor is not, so it sits behind the NODE_LIST_CTOR dispatch
      slot whose documented zeroing default installs no vtables (the
      SINGLETON_CTORS contract - NOT hook-ready until the ctor is
      ported). The ctor (decomp examined) plants vtable 0x0898165c at
      +0x00, zeroes head +0x04 / stop_key +0x08 / draining-flag byte
      +0x0c, builds a sub-object at +0x10 (vtable 0x089a5d0c via the
      0x08275bb8/0x08271cec pair) and zeroes the drain-callback words
      +0x20/+0x28/+0x2c - object size 0x30 (NODE_LIST_SIZE). The
      registered "destructor" 0x0810516c is binary-verified a shared
      `mov r0,#1; ldmia sp!,{r4,pc}` epilogue stub - as a shutdown
      handler it would pop a frame it never pushed, so the
      registration could never fire (and retailOS never runs exit's
      chain anyway); modeled as a no-op. Deviations: guard and object
      are crate statics, not the 0x089cc834/0x08a79c74 .bss words
      (block_mgr.rs RW-page precedent), both zero-init = the exact
      pre-init state. match.py structural: same
      guard-test/acquire/ctor/register/return-object sequence with the
      ported callees inlined by LLVM (the two-level guard test merges
      into one whole-word test - outcome-identical in every guard
      state, since a bit-0-clear nonzero guard is refused by both) and
      the empty cxa_guard_release call deleted - the accepted
      intra-crate inlining noise (class_registry_construct precedent).

  - address: 0x08189464
    ghidra: FUN_08189464
    name: media_command_facade_get
    size: 92
    sig: "void *media_command_facade_get(void)"
    module: app/media_command_facade
    status: ported
    vars: {iVar1: acquired, uVar2: this,
      DAT_081894ac: guard-base-pool-word (0x089ca3a4; the guard is its +0xc),
      DAT_081894b0: guard-pool-word (0x089ca3b0 = MEDIA_COMMAND_GUARD),
      DAT_081894b4: object-pool-word (0x08a107f8 = MEDIA_COMMAND_FACADE),
      DAT_081894b8: DSO_HANDLE (0x089ca09c = __dso_handle),
      DAT_081894bc: dtor-pool-word (0x0817f190)}
    notes: >-
      EXTENT CORRECTED - functions.csv says 72 bytes, which is the code
      only; the five pool words @ 0x081894ac..0x081894bc belong to the
      function and the next function opens at 0x081894c0 (`ldr r0,[r0];
      ldr r1,[r0]; ldr r1,[r1,#0x158]; bx r1`, a sibling facade method),
      so the true extent is 92 bytes. 76 bl call sites from 22 distinct
      callers, zero plain-b tail sites - verified by decoding every B/BL
      word in osos.dec.
      A textbook ADS function-local static over a FIXED object, the
      node_list_get idiom, NOT the operator_new cache pattern of
      app/singletons.rs - which is why it lives beside the object it
      returns. Shape - tst guard,#1 fast path (read as
      `[0x089ca3a4+0xc]`, the same word as the 0x089ca3b0 literal the
      slow path loads); cxa_guard_acquire (0x082ab31c); ctor
      FUN_0818a010(object) returning this in r0; cxa_atexit(this, dtor
      0x0817f190, __dso_handle 0x089ca09c) (0x082ab1c8);
      cxa_guard_release (0x082ab338); reload the OBJECT literal - never
      the ctor's return - and return it. The guard pair and cxa_atexit
      are ported and called directly; the ctor is not, so it sits behind
      the MEDIA_COMMAND_CTOR seam (documented zeroing default - NOT
      hook-ready).
      WHAT THE OBJECT IS - a thin facade over the media player. The ctor
      (binary-verified, 84 bytes) writes +0x00 = media_player_get
      (0x0817ceb4, ported), +0x04 = instance_of_class_6600 (0x08100b74,
      ported), zeroes the bytes +0x08/+0x09, the half +0x0a and the
      bytes +0x0c/+0x0d, sets +0x10/+0x14/+0x18 = -1, +0x1c = 0 and
      +0x20 = 0xffff63c0 (-40000, the pool word @ 0x0818a064). +0x20 is
      the last store, so the object extent is 0x24 - there is no
      operator_new here to confirm a size independently.
      The methods around the accessor are one-liners of the form
      `[strb #1,[r0,#0xd];] ldr r0,[r0]; ldr r1,[r0]; ldr r1,[r1,#N]; bx
      r1` - most first mark the facade dirty at +0x0d (0x081897a8,
      0x08189998, 0x081899f4...), a few do not (0x081892e8, 0x081892f8,
      0x08189500) - then tail-dispatch through the player's vtable (slots +0x58, +0x68, +0x78, +0x88, +0x120,
      +0x130, +0x158, +0x17c, +0x1a4 observed). 21 of the 76 sites are
      in the message pump FUN_08100bb4, whose switch keys are the
      class-0x6600 command codes 0x6600000d, 0x6600000e, 0x6600000f...,
      each case fetching the facade and calling one method; the rest are
      the command wrappers @ 0x082c6a40..0x082c6ed0.
      The registered destructor 0x0817f190 is NOT a function entry - it
      is 0xbc bytes inside FUN_0817f0d4 (0x0817f0d4, 228 bytes), on a
      `ldr r2,[pc,#36]` that falls through to `pop {r0-r8,pc}`; as a
      shutdown handler it would pop a frame it never pushed, so the
      registration could never fire (and retailOS never runs exit's
      chain anyway). Modeled as a no-op - the node_list_get 0x0810516c
      precedent.
      Deviations - the guard and the object are crate statics, not the
      0x089ca3b0 / 0x08a107f8 words (the block_mgr.rs RW-page
      precedent); both zero-init = the exact pre-init state.
      Host tests - first call constructs/registers/publishes the guard,
      second call takes the bit-0 fast path and preserves a
      post-construction mutation, a bit-0-set guard short-circuits
      everything, a nonzero bit-0-clear guard is still refused by
      acquire, the RELOADED literal is returned while the registration
      carries the ctor's (deliberately different) return, the chain runs
      the no-op destructor, and the zeroing default covers exactly 0x24
      bytes.
      match.py structural - same guard-test / acquire / ctor / register /
      return-object sequence with the ported callees inlined by LLVM
      (the two-level guard test merges into one whole-word test -
      outcome-identical in every guard state, since a bit-0-clear
      nonzero guard is refused by both; cxa_atexit's malloc(16) and node
      link open-code; the empty cxa_guard_release is deleted) plus the
      crate fp frame and the `blx` through MEDIA_COMMAND_CTOR standing
      in for `bl 0x0818a010` - the accepted intra-crate inlining noise
      (node_list_get / class_registry_construct precedent).

  - address: 0x081c93fc
    ghidra: FUN_081c93fc
    name: table6_find_by_key
    size: 52
    sig: "RequestSlot *table6_find_by_key(RequestSlot **table, u32 key)"
    module: util/table_find
    status: ported
    vars: {param_1: table, param_2: key}
    notes: >-
      Scans a fixed 6-slot table of 8-byte records (base at *param_1)
      for the slot whose key word (+0) equals the key; returns the slot
      pointer or 0. 3 bl call sites - 0x08202694 and the state accessor
      0x0829dd30 (which returns the slot's byte at +4, or 0 when
      unregistered). The record is a request registry - key at +0, state
      byte at +4 driven 0 (idle) -> 1 (running) -> 2 (done) by
      0x08202694; the table lives at owner+0x28+0x3c and every access is
      taken under the lock at owner+0x28+0x40 (0x08094404/0x0809449c).
      There is no occupied flag, so a key of 0 matches the first zeroed
      slot - preserved. Codegen deviation - LLVM fully unrolls the
      constant 6-iteration loop (29 instructions vs the original's 13).

  - address: 0x080ed738
    ghidra: FUN_080ed738
    name: read_u16_le
    size: 16
    sig: "u32 read_u16_le(const u8 *p)"
    module: util/le_read
    status: ported
    vars: {param_1: p}
    notes: >-
      Unaligned little-endian u16 load - two post-indexed ldrb ORed at
      shifts 0/8, zero-extended. ARM ADS __packed-dereference idiom (a
      misaligned ldrh rotates rather than faults on ARM926EJ-S). 34 bl
      call sites, binary-scanned; leaf, no hardware. Callers such as
      0x0805a3ac/0x0805afd8/0x0805b0d0 walk packed on-disk records and
      re-pack the bytes big-endian afterwards.

  - address: 0x080ed748
    ghidra: FUN_080ed748
    name: read_u32_le
    size: 32
    sig: "u32 read_u32_le(const u8 *p)"
    module: util/le_read
    status: ported
    vars: {param_1: p}
    notes: >-
      Unaligned little-endian u32 load - four post-indexed ldrb ORed at
      shifts 0/8/16/24. Behaviorally identical to __rt_uread4
      (0x08031140, already ported in libc/rt_unaligned); this is a second
      copy from a different object - post-indexed loads and a bx lr
      return instead of fixed offsets and mov pc,lr. 65 bl + 1 b call
      sites, binary-scanned. LLVM folds the two identical bodies, so the
      archive exports read_u32_le and __rt_uread4 at the same address -
      review codegen as `match.py 0x080ed748 __rt_uread4`.

  - address: 0x080ed768
    ghidra: FUN_080ed768
    name: read_u64_le
    size: 88
    sig: "u64 read_u64_le(const u8 *p)"
    module: util/le_read
    status: ported
    vars: {param_1: p}
    notes: >-
      Unaligned little-endian u64 load - the eight bytes at p ORed at
      shifts 0..56, low word returned in r0 and high word in r1. Only 2
      bl call sites (0x080743dc, 0x081f5294) but it completes the
      0x080ed738/748/768 family. The original's first half contains four
      provably-dead instructions (lsr r2,r1,#24 / lsr r3,r1,#16 / orr
      r2,r3,r2 / orr r1,r2,ip,lsr #8) - the high words of the 64-bit
      shifts of bytes 1..3, which a zero-extended byte shifted left by
      less than 32 can never reach. Always zero, so the port drops them;
      returned value unchanged.

  - address: 0x0807439c
    ghidra: FUN_0807439c
    name: read_u16_be
    size: 28
    sig: "u32 read_u16_be(const u8 *p)"
    module: util/be_read
    status: ported
    renames:
      param_1/r0: p
      r1: first_byte
      r0_after_load: assembled_value
    notes: >-
      Unaligned big-endian u16 load: `ldrb r1,[r0],#1; ldrb r0,[r0];
      orr r0,r1,r0,lsl #8; lsl r1,r0,#8; orr r0,r1,r0,lsr #8; bic
      r0,r0,#0xff0000; bx lr`, yielding p[0]<<8 | p[1] zero-extended in
      r0. Its raw extent is exactly 28 bytes: next sibling starts at
      0x080743b8. Decoding every ARM B/BL word in osos.dec finds 18 direct
      `bl` callers and no predicated calls (0x080ac42c/440/454, 0x080daf3c,
      0x081542d4/e0/ec, 0x081550a0, 0x08155104, and
      0x082024ec/2500/2554/2568/257c/2590/25a4/25b8/25cc). It has no NULL
      or bounds guard; no deliberate deviations. `unpack_be16`
      (0x08261750) has the same observable load but is a distinct function,
      so this export has a dedicated text section.
  - address: 0x080743b8
    ghidra: FUN_080743b8
    name: read_u32_be
    size: 24
    sig: "u32 read_u32_be(const u8 *p)"
    module: util/be_read
    status: ported
    vars: {param_1: p}
    notes: >-
      Unaligned big-endian u32 load: bl read_u32_le (0x080ed748), then a
      register-only byte reverse of the result - lsl #24, orr of
      (v & 0xff00) << 8, orr of (v & 0xff0000) >> 8, orr of v >> 24.
      Behaviorally identical to load_be32 (0x081f3b30/0x0837a158) and
      unpack_be32 (0x08261770) in util/beload, but a distinct firmware
      function from another object, so it keeps its own symbol and text
      section. Ghidra reports 36 bytes; the raw words show pop {pc} at
      0x080743d8 and a separately-linked sibling (bl read_u64_le + tail
      branch, the 64-bit twin) at 0x080743dc - extent decoded from
      osos.dec. 41 bl call sites, all unpredicated, binary-scanned;
      recovered callers (0x0813xxxx, 0x08155xxx, 0x081c5xxx, 0x08202xxx)
      walk packed big-endian record arrays with strides 4/8/0xc. LLVM
      inlines the read_u32_le call (codegen-units=1) and lowers
      swap_bytes to its rotate idiom instead of the original mask/shift
      chain, so match.py shows the folded gather+reverse rather than the
      bl; behavior is unchanged and host-tested against
      u32::from_be_bytes over every offset.

  - address: 0x08076f48
    ghidra: FUN_08076f48
    name: byteswap16
    size: 16
    sig: "u32 byteswap16(u32 value)"
    module: util/bswap
    status: ported
    vars: {param_1: value}
    notes: >-
      Register-only 16-bit byte swap - ((v << 8) | (v >> 8)) & 0xff00ffff.
      A second implementation of bswap16 (0x0805dd48) from a different
      object; the 0x0805dxxx pair spills through the stack, this one stays
      in registers. Contracts differ at the edges - bswap16 reloads with
      ldrh and zero-extends, this masks with bic #0x00ff0000 and so
      passes the argument's top byte through (byteswap16(0xffffffff) =
      0xff00ffff vs bswap16's 0xffff). The mask is the ADS narrowing of an
      unsigned short - for a u16 argument the left-shifted copy cannot
      reach bit 24, so only byte 2 needs clearing. Every observed call
      site feeds a zero-extended ldrh and stores with strh, so the
      difference is never visible in stock firmware. 60 bl call sites,
      binary-scanned. Port reproduces the mask bit-for-bit; match.py is
      instruction-for-instruction modulo the frame push.

  - address: 0x08076f58
    ghidra: FUN_08076f58
    name: byteswap32
    size: 28
    sig: "u32 byteswap32(u32 value)"
    module: util/bswap
    status: ported
    vars: {param_1: value}
    notes: >-
      Register-only 32-bit byte reverse - v << 24 | (v & 0xff00) << 8 |
      (v & 0xff0000) >> 8 | v >> 24. Semantically identical to bswap32
      (0x0805dc24); second copy from the same object as byteswap16
      (0x08076f48). 23 bl call sites, binary-scanned. LLVM folds the two
      identical bodies, so the archive exports byteswap32 and bswap32 at
      one address - review codegen as `match.py 0x08076f58 bswap32`.

  - address: 0x080e94f4
    ghidra: FUN_080e94f4
    name: bswap32_word
    size: 32
    sig: "u32 bswap32_word(u32 value)"
    module: util/bswap
    status: ported
    vars: {param_1: value}
    notes: >-
      Standalone register-only 32-bit byte reverse. The raw sequence is
      mov #0xff00; and value lsr #8; orr value lsr #24; orr value lsl #24;
      mov #0xff0000; and value lsl #8; final orr, returning
      ((value >> 8) & 0xff00) | (value >> 24) | (value << 24) |
      ((value & 0xff00) << 8). Semantically a full u32 byte reversal;
      no deliberate deviations.

  - address: 0x080e9514
    ghidra: FUN_080e9514
    name: format_signed_radix_integer
    size: 232
    sig: "void format_signed_radix_integer(i32 value, u32 radix, RadixFormatSpec *spec)"
    module: printf/printf_radix_integer
    status: ported
    vars:
      param_1: value
      param_2: radix
      param_3: spec
      iVar6: remaining_precision
      bVar7: negative
      auStack_3c: reverse_digits
    notes: >-
      Signed 32-bit radix formatter. Negative values use ARM's wrapping
      negate (so INT_MIN becomes magnitude 0x80000000); the unconditional
      divide/remainder loop writes one or more reversed digit bytes, applies
      an active precision capped at 31, then appends '-' or the optional '+'
      before recording text_len. It calls the pad helper before and after
      reverse-byte output, using 1-left_justify then left_justify. The
      source and original disassembly show direct calls to unsigned div/mod
      0x08036f14, bounded output 0x08280f7c, padding 0x080ec120, and length
      scan 0x08392478; literal pool 0x080e95fc contains 0x083ecb4c for the
      digit-byte lookup. The three unported external helpers are volatile
      formatter seams in printf/printf_radix_integer; focused host tests
      install deterministic counterparts and cover INT_MIN, sign/zero
      precision, leading/trailing width padding, radix conversion, count,
      and callback order. match.py confirms the same signed-magnitude,
      division/reverse-buffer, precision, sign, pre/output/post control
      flow; indirect seams make the Rust body longer than the direct BLs.

  - address: 0x080e9600
    ghidra: FUN_080e9600
    name: managed_handle_mode
    size: 72
    sig: "i32 managed_handle_mode(i32 mode, *mut *mut u8 handle_slot)"
    module: heap/managed_handle
    status: ported
    vars: {param_1: mode, param_2: handle_slot, iVar1: handle}
    notes: >-
      Two-mode managed-handle lifecycle dispatcher. Mode 0 calls the
      constructor @ 0x08062470, stores its result to *handle_slot, and
      returns 0 for NULL / 2 otherwise. Mode 2 calls the release helper @
      0x08062374 with *handle_slot, clears the slot, then returns 2. Every
      other mode returns 1 and leaves the slot untouched. The structural
      comparison confirms those exact two original BL targets and all three
      status paths. The helpers' resource subsystem remains unported, so
      heap/managed_handle.rs uses the established volatile ops-table seam:
      its documented default reports creation failure and makes release
      inert until target integration wires both helpers.

  - address: 0x080e965c
    ghidra: FUN_080e965c
    name: bswap32_guid_field
    size: 28
    sig: "u32 bswap32_guid_field(u32 value)"
    module: util/bswap
    status: ported
    vars: {param_1: value}
    notes: >-
      Register-only 32-bit byte reverse - v >> 24 | (v & 0xff0000) >> 8 |
      (v & 0xff00) << 8 | v << 24 (top byte accumulated first, the
      reverse operand order of byteswap32 @ 0x08076f58). Third
      independent copy of the same operation; semantically identical to
      bswap32 (0x0805dc24) and byteswap32 (0x08076f58). NOT byte- or
      instruction-identical to either - same 7-instruction mov/and/orr
      shape, different accumulation order. All 6 bl call sites (osos.asm)
      sit in FUN_080fab9c and FUN_080fc3d8, which each pull a 12-byte
      struct from a COM-style vtable object and swap its three
      consecutive words - the GUID first-three-fields endianness fix-up,
      hence the name. The dedicated `.text.bswap32_guid_field` section
      keeps its exported firmware entry point distinct in the archive.
      Review codegen as
      `match.py 0x080e965c bswap32_guid_field`; LLVM emits a rotate-based
      mask/orr byte swap with an fp frame vs ADS's straight mov/and/orr
      chain; both straight-line, no loops, no calls.

  - address: 0x080e9878
    ghidra: FUN_080e9878
    name: fixed16_mul
    size: 20
    sig: "i32 fixed16_mul(i32 a, i32 b)"
    module: util/fixed
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 fixed-point multiply - smull to the full signed 64-bit
      product, then return bits [47:16], assembled by the original as
      (hi << 16) | (lo >>u 16). Truncating toward negative infinity, no
      rounding and no clamp; overflowing products wrap. This is retailOS's
      own fixed-point multiply, NOT FreeType's - FT_MulFix/ft_muldiv bias
      by half an ulp and saturate, so they are not interchangeable and
      the addresses are unrelated. 94 bl call sites, binary-scanned. The
      caller that pins the semantics is 0x08076154, a Q16.16 inverse
      square root - 8-entry seed table indexed by three exponent bits
      (via the clz at 0x0824980c), then Newton-Raphson y = y*(3 - x*y*y)/2
      with rsb r1,r1,#0x30000 as the literal 3.0 in Q16.16; it open-codes
      this same smull/lsl/orr idiom inline twice before calling out.

  - address: 0x08079a44
    ghidra: FUN_08079a44
    name: mul_shift_i32
    size: 20
    sig: "i32 mul_shift_i32(i32 a, i32 b, u32 shift)"
    module: util/fixed
    status: ported
    renames:
      param_1/r0: a
      param_2/r1: b
      param_3/r2: shift
      r12: product_high
    notes: >-
      Raw osos.dec is exactly five ARM instructions from 0x08079a44 through
      0x08079a54; the push at 0x08079a58 begins the distinct next function.
      smull forms the signed 64-bit a*b product in r0 (low) and r12 (high);
      lsl high by 32-shift and orr with low lsr shift returns product bits
      [shift+31:shift], with signed-product extension entering through high.
      Decoding every ARM B/BL word finds 12 direct bl call sites, all
      unconditional and none predicated: 0x08240f70, 0x08240f84, 0x08241008,
      0x08241020, 0x08241110, 0x0824156c, 0x08241ea4, 0x08241eb8,
      0x08241f44, 0x08241f5c, 0x08242068, 0x082422a4. Their r2 values are
      4 or 7 in the 0x08240a5c/0x08241acc rasterization routines. The Rust
      port deliberately has no semantic deviation, including ARMv5
      register-shift low-byte and >=32-zero behavior outside that caller range.
  - address: 0x080f0fa4
    ghidra: FUN_080f0fa4
    name: mul_wide_i64
    size: 12
    sig: "i64 mul_wide_i64(i32 a, i32 b)"
    module: util/fixed
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Signed widening multiply - the full 64-bit product of two 32-bit
      operands, returned in r0 (low) : r1 (high) per AAPCS. Body is a bare
      smull plus the mov that lands the low word in r0. 49 bl call sites,
      binary-scanned; consumers use it as the multiply of a 64-bit
      accumulated dot product (e.g. 0x082a01e4 multiplies four field pairs
      at +0/+4/+8/+0xc and sums with adds/adc, confirming the return is
      genuinely 64-bit and not a truncated mul).

  - address: 0x08094404
    ghidra: FUN_08094404
    name: mutex_lock_counted
    size: 32
    sig: "void mutex_lock_counted(CountedMutex *lock)"
    module: kernel/sync_mutex
    status: ported
    vars: {param_1: lock}
    notes: >-
      Waits on the mutex, then increments the hold counter at +8. The
      original open-codes mutex_lock's body (ldr r0,[r0] then the guard
      thunk 0x08056510 -> ROM sem wait 0x22003fd0) instead of calling
      0x0807f5c4; the port calls the ported mutex_lock, whose body is
      exactly that, and LLVM inlines it back. The object is a Mutex with
      a counter bolted on - word 0 sem cell, word 1 the padding
      mutex_create zeroes, word 2 the counter. The counter does NOT gate
      the wait, so the lock is not re-entrant; it reads 1 while held and
      0 while free, and wraps as a bare 32-bit add. 77 bl + 2 tail b call
      sites, binary-scanned. Only known instance is the global at
      0x08a79c68 (users 0x08124cec / 0x08124d68 / 0x08124f7c /
      0x08124fe8); that global never reaches mutex_create, so its cell is
      installed elsewhere. Layout modeled as a repr(C) CountedMutex so
      the fields stay disjoint on a 64-bit test host.

  - address: 0x0809449c
    ghidra: FUN_0809449c
    name: mutex_unlock_counted
    size: 20
    sig: "void mutex_unlock_counted(CountedMutex *lock)"
    module: kernel/sync_mutex
    status: ported
    vars: {param_1: lock}
    notes: >-
      Decrements the hold counter at +8, then signals the mutex by
      tail-branching into the release guard 0x08056710 (-> ROM sem signal
      0x220042b4). The store lands before the branch, so the decrement is
      observably ordered before the release - preserved and tested. 147
      bl + 16 tail b call sites, binary-scanned; the most-called
      unclaimed function in 0x08060000..0x08100000. Counter movement is
      unconditional - a NULL cell or zero handle skips the ROM op but
      still decrements, and unlocking a free lock wraps to 0xffffffff.

  - address: 0x08206e40
    ghidra: FUN_08206e40
    name: path_probe_guard_construct
    size: 40
    sig: "InterfaceGuard *path_probe_guard_construct(InterfaceGuard *storage, u32 base_hint)"
    module: app/path_probe
    status: ported
    renames:
      param_1/r0: storage (16-byte scoped interface-guard storage)
      param_2/r1: base_hint (live shared-base-constructor hint)
      DAT_08206e68: INTERFACE_GUARD_VTABLE_ADDRESS (0x08991978)
      FUN_0818a0c4: interface-guard shared base constructor (unported boundary)
      FUN_0818a144: counted_mutex_guard_acquire (ported)
    notes: >-
      Raw ARM proves the 40-byte instruction extent from 0x08206e40 through
      0x08206e64, followed by literal-pool word 0x08991978 at 0x08206e68;
      the next distinct function begins at 0x08206e6c. Decoding every ARM
      B/BL word in osos.dec finds exactly 14 direct references, all
      unconditional BLs (no predicated forms and no tail B): 0x08084d64,
      0x08089110, 0x080891a0, 0x08089228, 0x08089278, 0x080892d4,
      0x08090b94, 0x08090c0c, 0x0809b618, 0x0809b6b4, 0x080a8eec,
      0x080b6b64, 0x080f4ae4 and 0x080f4b80. It calls the unported base
      constructor with storage, the live r1 base hint, and zero r2; installs
      the derived vtable; then calls the ported counted_mutex_guard_acquire
      on storage+0x0c and returns its result rebased by 12 bytes. The
      unresolved base constructor has a device fixed-address boundary and a
      host test seam. Device builds call the ported acquire helper directly;
      host builds manually preserve its lock-address store and acquisition
      because the target's raw 32-bit 16-byte guard frame cannot be passed to
      the helper's native-pointer host model.
  - address: 0x08206e6c
    ghidra: FUN_08206e6c
    name: path_probe_guard_destroy
    size: 28
    sig: "InterfaceGuard *path_probe_guard_destroy(InterfaceGuard *guard)"
    module: app/path_probe
    status: ported
    renames:
      param_1/r0: guard (16-byte scoped interface guard; its +0x0c word is
        the raw CountedMutex pointer)
      DAT_08206e88: INTERFACE_GUARD_VTABLE_ADDRESS (0x08991978)
      FUN_0818a164: counted-mutex release veneer (inlined through the
        ported mutex_unlock_counted)
      FUN_0818a0fc: interface_guard_base_destroy (retailOS boundary)
    notes: >-
      Raw ARM proves the 28-byte instruction extent: ldr literal, push,
      str r1,[r0],#0xc, bl 0x0818a164, pop, sub r0,#0xc, and tail b
      0x0818a0fc. The 4-byte literal pool at 0x08206e88 is 0x08991978;
      the next distinct function begins at 0x08206e8c. Decoding every
      ARM B/BL word in osos.dec finds exactly 14 direct references, all
      unconditional BLs (no predicated forms and no tail B): 0x08084d8c,
      0x08089144, 0x080891cc, 0x08089254, 0x080892a4, 0x08089308,
      0x08090bac, 0x08090c38, 0x0809b630, 0x0809b6d4, 0x080a8f14,
      0x080b6b94, 0x080f4b0c and 0x080f4bac. No word-aligned literal in
      osos.dec equals this address. The destructor re-plants the guard
      vtable, releases the CountedMutex pointer in guard word +0x0c
      through the 0x0818a164 veneer, then tail-runs the base teardown and
      returns this. The release veneer only loads that pointer and calls
      already-ported mutex_unlock_counted, so the port inlines it. The
      base teardown at 0x0818a0fc remains a retailOS boundary: target
      builds call its fixed address, while host tests deliberately treat
      it as a no-op returning this because no host interface graph exists.
      Host test path_probe::tests::guard_destroy_replants_vtable_releases_and_returns_this
      proves vtable replacement, release-to-zero, free-lock wrapping, and
      the returned guard address using a dedicated below-4-GiB fixture.
  - address: 0x08206e9c
    ghidra: FUN_08206e9c
    name: counted_mutex_guard_release
    size: 40
    sig: "CountedMutex **counted_mutex_guard_release(CountedMutex **guard)"
    module: kernel/sync_mutex
    status: ported
    vars: {param_1: guard, iVar1: lock}
    renames:
      FUN_0809449c: mutex_unlock_counted
    notes: >-
      Binary decoding establishes the 40-byte extent through 0x08206ec4,
      where the next function begins, correcting no Ghidra extent in this
      case. Exactly 20 direct BL words target this address in osos.dec
      (all unconditional; no predicated BL forms). This is the destructor
      for a one-word counted-mutex scope guard: load *guard, skip the
      release when it is NULL, otherwise call mutex_unlock_counted and
      write NULL back; return guard in either case. The existing callee at
      0x0809449c is ported, so no dispatch seam was added. No deliberate
      deviations.

  - address: 0x0818a128
    ghidra: FUN_0818a128
    name: counted_mutex_guard_acquire_lock
    size: 28
    sig: "CountedMutex **counted_mutex_guard_acquire_lock(CountedMutex **guard, CountedMutex *lock)"
    module: kernel/sync_mutex
    status: ported
    renames:
      param_1/r0: guard (the one-word scope guard; receives lock and is returned)
      param_2/r1: lock (CountedMutex acquired through mutex_lock_counted)
      FUN_08094404: mutex_lock_counted (ported, called directly)
    notes: >-
      Raw ARM establishes the 28-byte extent from 0x0818a128 through
      0x0818a140; the separately linked interface-based acquire helper
      starts at 0x0818a144. Decoding every B/BL word in osos.dec finds ten
      direct references at 0x08149fb4, 0x0814a174, 0x0814a1f0, 0x081e1f0c,
      0x081e1f7c, 0x081e1fc8, 0x081e1ffc, 0x081e2030, 0x082971d0, and
      0x08297260: all are unconditional BLs (condition 0xe), with no
      predicated forms or tail B. No word-aligned data word in osos.dec
      equals this address, so it is not virtually dispatched. The function
      is the direct-lock form of a one-word counted-mutex scope guard:
      writes its CountedMutex input to *guard, calls the existing
      mutex_lock_counted port, then returns guard. The store is before the
      call, so the guard is visible while waiting. Neither pointer is
      NULL-checked. LLVM may inline mutex_lock_counted rather than retain
      the firmware BL; no semantic deviation. Host tests:
      src/kernel/sync_mutex.rs::tests::guard_acquire_lock_stores_locks_and_returns_the_guard
      / guard_acquire_lock_stores_the_guard_word_before_waiting /
      guard_acquire_lock_null_cell_still_stores_and_counts.
  - address: 0x0818a144
    ghidra: FUN_0818a144
    name: counted_mutex_guard_acquire
    size: 32
    sig: "CountedMutex **counted_mutex_guard_acquire(CountedMutex **guard, u8 *const *owner)"
    module: kernel/sync_mutex
    status: ported
    renames:
      param_1/r0: guard (the one-word scope guard; receives the lock
        address and is returned verbatim)
      param_2/r1: owner (object whose word at +4 is the interface
        pointer; the interface embeds the CountedMutex at +0x44)
      FUN_08094404: mutex_lock_counted (ported, called directly)
    notes: >-
      32 bytes, extent binary-verified: the next function (the unported
      unlock twin 0x0818a164, ldr r0,[r0]; bl 0x0809449c) starts at
      +0x20. 16 bl call sites, binary-scanned by decoding every B/BL word
      in osos.dec - 0x081ef5f4, 0x081ef77c, 0x081ef9f0, 0x08206e5c,
      0x08277a04, 0x08277ac0, 0x08277c08, 0x08278264, 0x082784f8,
      0x082787d4, 0x082788a4, 0x08278948, 0x082789dc, 0x08278d38,
      0x082a53b0 and 0x082a542c - all plain bl (cond 0xe), no predicated
      forms, no tail b, and the address appears in no DATA word, so it is
      never dispatched virtually. Acquire half of the one-word
      counted-lock scope guard used across the Silver-controller/facade
      code (the release halves are counted_mutex_guard_release @
      0x08206e9c and the unported 0x0818a164 twin; several call sites,
      e.g. 0x082779ec, skip both and hand the guard word straight to
      mutex_unlock_counted). Body: interface = owner->word_at_+4;
      *guard = interface + 0x44 (the embedded CountedMutex);
      mutex_lock_counted(*guard); return guard. The store to the guard
      lands BEFORE the lock call (str r0,[r4] then bl), so the guard
      word is already valid while the wait runs - preserved and tested
      with a probing sema_wait mock. No NULL guards anywhere: a NULL
      interface word yields lock address 0x44, which the original would
      dereference; callers always pass a live owner. The interface word
      is read with typed pointer indexing (owner.add(1)), not a byte
      offset, so the field stays at word index 1 on a 64-bit test host;
      on the 32-bit target it is exactly ldr r0,[r1,#4]. Deviation:
      LLVM inlines the ported mutex_lock_counted body here (its own port
      documents the same for mutex_lock), so the ARM build shows the
      wait bl followed by the inlined hold-count increment at [lock+8]
      in place of the original's bl 0x08094404. Host tests:
      src/kernel/sync_mutex.rs::tests::guard_acquire_stores_locks_and_returns_the_guard
      / guard_acquire_stores_the_guard_word_before_waiting /
      guard_acquire_null_cell_still_stores_and_counts /
      guard_acquire_word_feeds_mutex_unlock_counted_directly. match.py
      (exit 1, diff expected): same ldr [r1,#4] / add #0x44 / str [r4]
      / wait-call / return-guard structure; the diff is the crate fp
      frame and the inlined counted-lock body.

  - address: 0x08094424
    ghidra: FUN_08094424
    name: mutex_delete_counted
    size: 40
    sig: "void mutex_delete_counted(CountedMutex *lock)"
    module: kernel/sync_mutex
    status: ported
    vars: {param_1: lock}
    notes: >-
      Counted-lock teardown, the destructor half of the
      mutex_lock_counted / mutex_unlock_counted pair. If the semaphore
      cell pointer (word 0) is non-NULL it runs the same cell-destroy
      thunk 0x0805646c that mutex_delete uses (guards cell/*cell, ROM
      semaphore delete kind 1 via 0x08037e40 -> 0x22003dc8, *cell zeroed,
      cell freed unless it is the static early-boot cell 0x089cc8f8);
      then all three words (cell pointer, padding, hold counter) are
      zeroed unconditionally - even with a NULL cell the object returns
      to its all-zero born state. 6 bl call sites, binary-scanned; every
      caller hands it an embedded 12-byte object inside a larger struct
      (decomp: this+0x18 @ 0x08209f20, this+0x74 @ 0x08133fe0, this+0x88
      @ 0x081b676c, task-0xc @ 0x0816f5c0 / 0x08297458, wrapper
      0x080f81f0). Ported as mutex_delete_counted over CountedMutex; the
      thunk is inlined via the ROM_KERNEL dispatch table like the rest of
      sync_mutex.rs.

  - address: 0x08076154
    ghidra: FUN_08076154
    name: fixed16_rsqrt
    size: 168
    sig: "i32 fixed16_rsqrt(i32 x)"
    module: util/fixed
    status: identified
    vars: {param_1: x}
    notes: >-
      Q16.16 inverse square root by Newton-Raphson. Fully decoded, NOT
      ported - blocked only on a seed table whose contents cannot be
      recovered from osos.dec (see below). Algorithm - x == 0 returns
      0x7fffffff; x == 0x10000 (1.0) returns 1.0 unchanged; otherwise
      lz = clz(x) (0x0824980c, a software clz that returns 31 rather than
      32 for zero), idx = (x >> (28 - lz)) & 7 selects one of 8 u16 seeds
      which is doubled, e = lz - 16, then y = seed << ceil(e/2) for
      e > 0 else y = seed >> ((-e) >> 1), and if e is odd y is corrected
      by fixed16_mul(y, 0xb504) - 0xb504 is 1/sqrt(2) in Q16.16
      (46340 vs the exact 46340.95). Three iterations of
      y = (y >> 1) * (3.0 - x*y*y) follow, with 3.0 appearing literally as
      rsb r1,r1,#0x30000; the two inner multiplies are fixed16_mul
      (0x080e9878) open-coded inline as smull/lsl/orr, the final one is a
      real bl to it. All shift amounts are bounded by 8 so no ARM
      register-shift >= 32 case arises. 7 bl call sites, binary-scanned
      (0x08079a28, 0x0824c030, 0x0824c1e4, 0x0824c67c, 0x0824f3a0,
      0x08256eac, 0x082a06d4). Blocker - the seed table pointer literal at
      0x080761fc is 0x083f18b2, which lies in a 0x442b-byte all-zero run
      (0x083ee1c1-0x083f25ec) in osos.dec. That run is not bss - 85
      distinct addresses inside it are referenced as absolute literals
      from code all over the image (e.g. 0x083f18c2/0x083f18d4 from the
      literal pool of 0x080e76b8, a strncmp pair), so it is a read-only
      constant/string pool that our decrypted image does not carry
      populated (ADS compressed-RO-data image, or a decrypt gap). The
      table is exactly 8 u16 entries - 0x083f18b2 + 0x10 = 0x083f18c2 is
      the next referenced object. Port this once the region's contents
      are recovered; do not guess the seeds. Recovery attempts
      (2026-07-29, still blocked): aupd.dec, bootrom.bin, iram.bin and
      rsrc.bin do not contain the routine at all (searched for its
      rsb #0x30000 idiom and its exact code bytes; the only rsb
      #0x30000 site in osos.dec is 0x080761e4 itself), and no
      duplicate of the seed table exists in the decrypted portion
      (byte-searched both the floor and the round hypotheses below).
      osos.recovery carries the same hole while osos.bin holds nonzero
      ciphertext there, so the region is simply still undecrypted -
      `make decrypt` with the DFU oracle should fill it in. Expected
      seed shape for instant verification once recovered:
      table[idx] ~= 65536/sqrt(2*(1+idx/8)) giving floor/round pairs
      {b504/b505, aaaa/aaab, a1e8/a1e9, 9a5f/9a60, 93cd, 8e00/8e01,
      88d6, 8432/8433}; note the odd-e correction constant 0xb504 is
      exactly floor(65536/sqrt(2)), which hints the table was floored
      too. Port attempt 2026-07-29: re-verified the blocker stands -
      the 16 bytes at file offset 0x3f18b2 in osos.dec are still all
      zero and no newer decryption artifact exists. The floor-vs-round
      ambiguity is NOT safely ignorable: a 1-ulp seed difference enters
      at ~2.2e-5 relative error, and although the three Newton
      iterations crush that below Q16.16 resolution for the vast
      majority of inputs, fixed16_mul's truncating (hi<<16)|(lo>>u16)
      step can flip a result ulp whenever an exact intermediate product
      lands within ~1e-9 relative of a boundary - rare but real, and
      indistinguishable from the original without the true table. Kept
      status: identified; port immediately once `make decrypt` fills
      the region (the floor hypothesis above then verifies or falsifies
      in one lookup). Port attempt 2026-07-30: blocker confirmed
      unchanged - osos.dec/osos.recovery untouched since 2026-07-22,
      the 16 bytes at file offset 0x3f18b2 are still all zero and the
      zero run is still exactly 0x083ee1c1-0x083f25ec; no copy of the
      routine exists in aupd.dec, bootrom.bin, iram.bin, rsrc.bin or
      hash.bin (searched for the exact 12-byte prologue
      stmdb;movs r4,r0;mvneq r0,#0x80000000). Re-quantified the seed
      sensitivity with an exact i64 simulation of the decoded
      algorithm, and it is harsher than the "rare" estimate above for
      small x: bumping a single table entry by 1 ulp flips ~58% of
      outputs for 0 < x < 0x10000 (values below 1.0 - there
      t = fixed16_mul(y,x) keeps only a few significant bits, so the
      truncation drives the Newton loop into a seed-dependent limit
      cycle rather than a clean fixpoint) and ~0.1% of outputs for
      x >= 0x10000; the floor-vs-midpoint table hypotheses disagree on
      ~48% of a 410k-input sweep. Guessing the seeds is therefore not
      a "few ulps on edge cases" risk but a wholesale behavioral
      divergence for fractional inputs. Still blocked; still status:
      identified. Do not port until the true 8 halfwords are
      recovered.
      Deferral reconfirmed 2026-08-01 from the raw ARM disassembly and
      osos.dec: the function's literal pool still resolves the seed base to
      0x083f18b2 and the following literal is 0x0000b504, while all sixteen
      bytes at the seed address remain zero. The ARM body confirms the
      three fixed16_mul Newton iterations and no saturation beyond the
      explicit x == 0 / x == 0x10000 exits. Since seed values change
      observable fractional-input results, retain status: identified rather
      than publishing a non-parity Rust implementation.
      This porting pass independently confirmed that `cmp -n 16 -i
      0x3f18b2 osos.dec /dev/zero` succeeds, so no behavioral Rust port can
      be verified until the seed bytes are recovered.
      Deferral reconfirmed 2026-08-14: `cmp -n 16 -i 0x3f18b2
      osos.dec /dev/zero` still succeeds. The seed table remains
      unavailable, so retaining `identified` is necessary to avoid
      publishing a behaviorally unverifiable implementation.

  - address: 0x08076214
    ghidra: FUN_08076214
    name: fixed16_round_64
    size: 20
    sig: "i32 fixed16_round_64(i64 acc)"
    module: util/fixed
    status: ported
    vars: {param_1: "acc (low word, r0)", param_2: "acc (high word, r1)"}
    notes: >-
      Round-to-nearest extraction of a 64-bit fixed-point accumulator to
      32-bit Q16.16: adds r0,r0,#0x8000 / adc r1,r1,#0 adds half an ulp
      with carry across the word boundary, then
      mov r0,r0,lsr#16 / orr r0,r0,r1,lsl#16 returns bits [47:16] of the
      sum - exactly a 32-bit truncation of (acc + 0x8000) >> 16. Ties go
      toward positive infinity (round half up, floor(acc + 1/2) sense,
      negative fractions included); results that do not fit in 32 bits
      wrap, no clamp. The port takes the accumulator as one i64 (AAPCS
      r0:r1, matching the original register use). This is the rounding
      counterpart to fixed16_mul @ 0x080e9878's truncation: all 12 call
      sites (11 bl plus one tail b at 0x082a0254) sit at the end of
      mul_wide_i64 @ 0x080f0fa4 dot-product chains in the 0x082a
      geometry code - FUN_082a05d4/FUN_082a1368/FUN_082a1544 are 4x4
      matrix-times-vector transforms that adds/adc four Q16.16 products
      into a 64-bit accumulator and round it here to a Q16.16
      coordinate. Host tests cover carry propagation at the low-word
      boundary (lo=0xffff8000), half-ulp ties both signs, high-word
      extraction/wrap, and identity values. match.py structural: the
      adds/adc/lsr/orr/return core is instruction-identical; LLVM only
      adds an fp frame.

  - address: 0x080377e4
    ghidra: FUN_080377e4
    name: fixed16_recip_unguarded
    size: 48
    sig: "i32 fixed16_recip_unguarded(i32 x)"
    module: util/fixed
    status: ported
    vars: {param_1: x, iVar1: negative}
    notes: >-
      Unguarded Q16.16 reciprocal body, 0xffffffff / |x| with the sign
      reapplied. CAVEAT: the listed 48 bytes end at the computed jump;
      osos.asm drops everything past it and Ghidra could not recover the
      jumptable at 0x08037810 ("Treating indirect jump as call"). The
      true body, decoded from raw osos.dec words, runs
      0x080377e4..0x080379a8 (452 bytes): push {r1-r4}; mov r3,r0,asr#31
      (sign); cmp/movpl/rsbmi folding x to |x| (rsbmi wraps i32::MIN to
      magnitude 0x80000000, V ignored); clz r0,r1; then add pc,pc,r4
      with r4 = 12 * (31 - clz(|x|)) jumps into a 32-entry unrolled
      restoring division at the entry for quotient bit clz(|x|), the
      highest settable bit. Each 12-byte entry is subs r4,r2,r1,lsl#bit
      / orrcs r0,r0,#1<<bit / movcs r2,r4 (r2 = remainder seeded with
      the dividend 0xffffffff, r0 = quotient). The skip is semantic: in
      skipped entries the shift pushes divisor bits off the register top
      and would corrupt the trial. Epilogue cmp r3,#0 / rsbne r0,r0,#0 /
      pop / bx lr. The 2^32-1 dividend truncates every exact reciprocal
      one ulp low: recip(1.0)=0xffff, recip(2.0)=0x7fff; i32::MIN -> -1.
      x == 0 is invalid (clz(0)=32 -> wild negative jump offset); the
      guard lives in the caller fixed16_recip. 11 call sites: the
      conditional tail bne from fixed16_recip @ 0x08076204 plus 10 bl
      sites (0x080ea69c, 0x080ea6e0, 0x080eaea0, 0x080eaee4, 0x080ed388,
      0x08160800, 0x081a0fa0, 0x08224a5c, 0x082537a4, 0x082539f4). The
      port rolls the division into a loop from bit clz(|x|) down to 0;
      the computed jump and unrolling are ADS codegen, not semantics.
      FIXED16_RECIP_UNGUARDED now defaults to this port on both target
      and host (the seam stays swappable for forwarding tests); the old
      retail-transmute target path and host panic stub are gone. Host
      tests: hand-computed reciprocals, all powers of two both signs,
      i32::MIN/i32::MAX, +/-1, a dense +/-1..=0x20000 sweep plus
      power-of-two neighbors against plain truncating division, and
      fixed16_recip end to end through the real body. match.py
      structural: only the first 12 original instructions survive in
      osos.asm, so the diff is expectedly wide; the Rust loop body keeps
      the original's cmp/lsl + orrcs quotient-bit shape and the rsbmi
      sign reapplication.

  - address: 0x08076204
    ghidra: FUN_08076204
    name: fixed16_recip
    size: 16
    sig: "i32 fixed16_recip(i32 x)"
    module: util/fixed
    status: ported
    vars: {param_1: x}
    notes: >-
      Guarded Q16.16 reciprocal, 1 / x. cmp r0,#0 / bne 0x080377e4 /
      mvneq r0,#0x80000000 / bx lr: a zero input returns 0x7fffffff
      (i32::MAX - the mvneq materializes the bitwise NOT of 0x80000000,
      saturating 1 / 0); any nonzero input tail-branches (b, not bl)
      unchanged to the unguarded body FUN_080377e4 @ 0x080377e4, which
      keeps the sign in r3 (mov r3,r0,asr#0x1f), negates x into |x|,
      and jumps into an unrolled 32-bit restoring division of
      0xffffffff by |x| at the entry for quotient bit 31 - clz(|x|)
      (add pc,pc,r4 with 12-byte entries), negating the quotient when
      x was negative. The dividend is 2^32 - 1 rather than 2^32 because
      2^32 does not fit in a register, so the result is the Q16.16
      reciprocal truncated one ulp low. 36 call sites (35 bl plus one
      tail b), all in the 0x082a geometry / 0x0824 transform code:
      FUN_082a0920 is Gaussian elimination scaling a pivot row by
      recip(pivot) via the open-coded fixed16_mul idiom, and
      FUN_082a06d4 normalizes a 3x3 matrix by its bottom-right term.
      The callee FUN_080377e4 is ported in the same module as
      fixed16_recip_unguarded (see its entry); fixed16_recip still
      routes through the FIXED16_RECIP_UNGUARDED seam
      (app/h264_decode_forwarder.rs pattern), which now defaults to the
      port on both target and host and stays swappable so host tests
      can record forwarding. Host tests prove x == 0 ->
      0x7fffffff without touching the body, nonzero inputs forward
      unchanged both ways, and, with a faithful model of the recovered
      division wired in, that fixed16_mul(x, recip(x)) lands within
      |x|/2^16 + 2 ulps of 1.0. match.py structural: the cmp/branch/
      mvn guard is identical; the tail b becomes an indirect bx through
      the seam pointer (the known seam deviation).

  - address: 0x0824980c
    ghidra: FUN_0824980c
    name: clz_31
    size: 68
    sig: "u32 clz_31(u32 x)"
    module: util/fixed
    status: ported
    vars: {param_1: x, iVar1: lz}
    notes: >-
      Software count-leading-zeros by branchless binary search over
      half-ranges: lz starts at 31, then for the masks 0xffff0000,
      0xff00, 0xf0, 0xc, 0x2 in turn, if the value has a bit in the
      upper half of the current window lz loses that half's width and
      the value shifts down (movs/movne/tst/subne chains - ADS 1.0.1
      predates reliable ARMv5TE clz codegen). The zero-input quirk
      names it: a hardware clz of 0 yields 32, this yields 31, because
      the first movs test fails and the initial 0x1f survives; callers
      rely on it (fixed16_rsqrt @ 0x08076154 computes
      idx = (x >> (28 - lz)) & 7 and e = lz - 16, where 32 would wrap
      the shift). For every nonzero input the result equals
      x.leading_zeros(). 3 bl call sites (0x08076170 in fixed16_rsqrt,
      0x082417e8, 0x08242530). match.py: structural match - same
      search, LLVM renders the 0xff00/0xf0 masks as cmp #256/#16 and
      folds the final bit-1 step into lsl #30 + add asr #31; no loops,
      no calls.

  # --- Batch: the nine-array element table (app/element_table) ---
  # FUN_08105ffc lazily builds one 0xd8-byte object holding nine 0x18-byte
  # container sub-objects (9 * 0x18 = 0xd8), each constructed with
  # (0, 10, 2) by a DIFFERENT constructor - nine instantiations of one
  # template, so nine distinct element types. Nine accessors read them,
  # one per array: 0x080f1038 (+0x00), 0x080f0fb4 (+0x18), 0x080f108c
  # (+0x30), 0x080f1060 (+0x48), 0x080f100c (+0x60), 0x080ed8fc (+0x78),
  # 0x080ed928 (+0x90), 0x080ed8d0 (+0xa8), 0x080f0fe0 (+0xc0).
  # The element types are NOT identified - the only names near the nine
  # constructors are runtime-data pointers, not strings. Note this table
  # has nothing to do with the by-id class registry; the earlier names
  # (registry_entry_by_index / _alt) were wrong and have been replaced.

  - address: 0x083d3e8c
    ghidra: FUN_083d3e8c
    name: element_array0_construct
    size: 160
    sig: "void *element_array0_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      First constructor called by element_table, so it creates the array-0
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d3f2c + 4 (0x0897baac + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d3cec with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x083d4354
    ghidra: FUN_083d4354
    name: element_array1_construct
    size: 160
    sig: "void *element_array1_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Second constructor called by element_table, so it creates the array-1
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d43f4 + 4 (0x0897bbc4 + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d41b4 with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x083d40f0
    ghidra: FUN_083d40f0
    name: element_array2_construct
    size: 160
    sig: "void *element_array2_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Third constructor called by element_table, so it creates the array-2
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d4190 + 4 (0x0897bab8 + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d3f50 with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x083d3c28
    ghidra: FUN_083d3c28
    name: element_array3_construct
    size: 160
    sig: "void *element_array3_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Fourth constructor called by element_table, so it creates the array-3
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d3cc8 + 4 (0x0897b904 + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d3a8c with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x083d56b8
    ghidra: FUN_083d56b8
    name: element_array4_construct
    size: 160
    sig: "void *element_array4_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Fifth constructor called by element_table, so it creates the array-4
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d5758 + 4 (0x0897cc8c + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d551c with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x083d51dc
    ghidra: FUN_083d51dc
    name: element_array5_construct
    size: 160
    sig: "void *element_array5_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Sixth constructor called by element_table, so it creates the array-5
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d527c + 4 (0x0897cbec + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d503c with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.


  - address: 0x083d4cf4
    ghidra: FUN_083d4cf4
    name: element_array7_construct
    size: 160
    sig: "void *element_array7_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Eighth constructor called by element_table, so it creates the array-7
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d4d94 + 4 (0x0897cbac + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d4b58 with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x083d4a94
    ghidra: FUN_083d4a94
    name: element_array6_construct
    size: 160
    sig: "void *element_array6_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Seventh constructor called by element_table, so it creates the array-6
      container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d4b34 + 4 (0x0897cba0 + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d48f8 with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.
  - address: 0x083d45b4
    ghidra: FUN_083d45b4
    name: element_array8_construct
    size: 160
    sig: "void *element_array8_construct(void *this, u32 options, u32 slots, u32 growth)"
    module: app/element_table
    status: ported
    vars: {param_1: this, param_2: options, param_3: slots, param_4: growth, uVar1: allocation, iVar2: tracker_name, uVar3: tracker_name_len, iVar4: tracker_source_offset}
    notes: >-
      Ninth and final constructor called by element_table, so it creates the
      array-8 container (the contained element type is still unknown). Stores
      {data=0, slots, used=0, options, growth, tracker_label=0}, allocates
      and zeroes slots * 4 bytes for data, then copies a shortened fTable
      name into a second allocation at +0x14. The source is read from
      DAT_083d4654 + 4 (0x0897bc00 + 4); `strlen <= 10` skips one source
      byte, otherwise it skips two, and `strncpy` copies the original
      length. It calls FUN_083d4418 with `Tracker<%s> fTable=%x,
      fSize=%d`, but that helper is a four-byte no-op. No allocation-failure
      branch exists; it returns this after storing and using each allocation
      result. match.py: structural match; both bodies initialize six words,
      allocate and zero the slot buffer, allocate/copy tracker metadata, and
      return this. Rust emits explicit byte loops and inlines the trivial
      fTable +4 accessor/tracker no-op rather than their retailOS calls.

  - address: 0x08105ffc
    ghidra: FUN_08105ffc
    name: element_table
    size: 224
    sig: "void *element_table(void)"
    module: app/element_table
    status: ported
    notes: >-
      38 bl call sites. `if (cache == 0) { r0 = operator_new(0xd8); r0 =
      ctor0(r0, 0, 10, 2); r0 = ctor1(r0 + 0x18, ...); ... r0 = ctor8(r0
      + 0x18, ...); cache = r0 - 0xc0; } return cache;` - the cursor is
      chained through each constructor's RETURN, and the final base is
      the ninth return minus 0xc0, not the allocated block. The nine
      constructors in call order - 0x083d3e8c, 0x083d4354, 0x083d40f0,
      0x083d3c28, 0x083d56b8, 0x083d51dc, 0x083d4cf4, 0x083d4a94,
      0x083d45b4; each lays its sub-object out as {+0x00 data buffer of
      slots*4 zeroed bytes, +0x04 slots (=10), +0x08 0, +0x0c the first
      argument (=0), +0x10 growth (=2), +0x14 a second allocation}.
      Deviations - the cache is the crate static ELEMENT_TABLE, not the
      word @ 0x089ca3bc; the nine constructors sit behind the
      ELEMENT_ARRAY_CTORS dispatch table with zeroing-stub defaults
      (which leave slots = 0, so every accessor returns NULL), so this is
      NOT HOOK-READY; LLVM rolls the nine dispatch-table calls into a
      loop where the original is unrolled (behaviourally identical).

  - address: 0x080f1038
    ghidra: FUN_080f1038
    name: element_array0_at
    size: 40
    sig: "void *element_array0_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      47 bl call sites, binary-scanned. Calls element_table(), then
      returns array0.data[index] when index >= 0 and array0.slots >
      index, else NULL. Both compares are SIGNED (`cmp r4,#0` +
      `ldrge/cmpge` + `movle`/`ldrgt`), so a negative index and index ==
      slots both yield NULL, and a slot count read back with the sign
      bit set rejects every index. The getter runs BEFORE the bounds
      test, so even an out-of-range index builds the table.

  - address: 0x080ed928
    ghidra: FUN_080ed928
    name: element_array6_at
    size: 44
    sig: "void *element_array6_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      64 bl call sites, binary-scanned (111 across the pair with
      0x080f1038). Identical to element_array0_at but reads the array at
      +0x90; the original is the same body plus one redundant `add r0,
      r0, #0x90`. Both are exported from one shared Rust body, which is
      why they are separate #[inline(never)] wrappers.

  - address: 0x080f0fb4
    ghidra: FUN_080f0fb4
    name: element_array1_at
    size: 44
    sig: "void *element_array1_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      2 bl call sites, binary-scanned. One of the seven per-array
      accessors ported as one-line wrappers against app/element_table's
      shared body; reads the array at +0x18. The set - 0x080f0fb4
      (+0x18), 0x080f108c (+0x30), 0x080f1060 (+0x48), 0x080f100c
      (+0x60), 0x080ed8fc (+0x78), 0x080ed8d0 (+0xa8), 0x080f0fe0
      (+0xc0).

  - address: 0x080f108c
    ghidra: FUN_080f108c
    name: element_array2_at
    size: 44
    sig: "void *element_array2_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      7 bl call sites, binary-scanned. Same body as element_array0_at;
      reads the array at +0x30.

  - address: 0x080f1060
    ghidra: FUN_080f1060
    name: element_array3_at
    size: 44
    sig: "void *element_array3_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      5 bl call sites, binary-scanned. Same body as element_array0_at;
      reads the array at +0x48.

  - address: 0x080f100c
    ghidra: FUN_080f100c
    name: element_array4_at
    size: 44
    sig: "void *element_array4_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      6 bl call sites, binary-scanned. Same body as element_array0_at;
      reads the array at +0x60.

  - address: 0x080ed8fc
    ghidra: FUN_080ed8fc
    name: element_array5_at
    size: 44
    sig: "void *element_array5_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      9 bl call sites, binary-scanned. Same body as element_array0_at;
      reads the array at +0x78.

  - address: 0x080ed8d0
    ghidra: FUN_080ed8d0
    name: element_array7_at
    size: 44
    sig: "void *element_array7_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      8 bl call sites, binary-scanned. Same body as element_array0_at;
      reads the array at +0xa8.

  - address: 0x080f0fe0
    ghidra: FUN_080f0fe0
    name: element_array8_at
    size: 44
    sig: "void *element_array8_at(i32 index)"
    module: app/element_table
    status: ported
    vars: {param_1: index}
    notes: >-
      6 bl call sites, binary-scanned. Same body as element_array0_at;
      reads the array at +0xc0.

  - address: 0x0810dfc0
    ghidra: FUN_0810dfc0
    name: view_event_complete
    size: 40
    sig: "u32 view_event_complete(u8 *this)"
    module: app/view_event
    status: ported
    vars:
      param_1: this (the view/controller)
      FUN_0810dfe8: stop_view_timer (ported helper @ 0x0810dfe8;
        VIEW_EVENT_OPS slot; fetches this+0x50 and tail-branches to the
        ported timer_stop @ 0x0812c6b0 when non-NULL)
      FUN_0810e170: commit_staged_flags (VIEW_EVENT_OPS slot; cursor
        walk of the +0x60 collection storing item.byte2 into the
        global byte table @ 0x08a77a8f[item.byte0] via 0x0819c9d0)
    notes: >-
      The shared event-handler epilogue of the retailOS
      view/controller base class. 40 bytes exactly (ten instructions,
      no literal pool; the timer-stop wrapper opens immediately at
      0x0810dfe8). 98 call sites binary-scanned by decoding every B/BL
      word in osos.dec: 52 bl + 46 b. Algorithm: if the word at
      this+0x50 (the view's timer) is non-NULL, call 0x0810dfe8(this)
      to stop it; then call 0x0810e170(this) unconditionally to commit
      the staged flag bytes from the +0x60 collection into the global
      byte table @ 0x08a77a8f; return 1 - the framework's "handled"
      verdict, which consuming sites forward verbatim (e.g.
      0x0839f6d0, 0x08125678). r1 is never touched: the handler
      argument tail-callers leave in r1 is dead (both callees ignore
      or overwrite it), so the recovered ABI takes one argument.
      Every one of the 98 sites is a UI view/controller event handler
      (0x080fxxxx..0x0827xxxx) finishing `mov r1, arg; mov r0, this;
      b/bl 0x0810dfc0` - each does its specific work then tails into
      this epilogue. Note the original tests this+0x50 here and the
      wrapper re-tests it; the double test is reproduced. Extent is
      exact on both sides (0x0810dfc0..0x0810dfe8). Deviations: the
      stop helper is ported and wired into VIEW_EVENT_OPS; the staged
      commit helper still rides the seam (event_list.rs pattern -
      transmuted firmware defaults on target, panicking defaults on
      host, recording mocks in tests), so the port is hook-ready on
      target. Host tests: timer word set - stop then commit fire in
      order with `this` passed through, return 1; timer word clear -
      only the commit fires, return 1. match.py structural: same
      load-test/conditional-call/unconditional-call/mov r0,#1 shape;
      the diff is the ops-table blx indirection and the crate fp frame.

  - address: 0x0810dfe8
    ghidra: FUN_0810dfe8
    name: stop_view_timer
    size: 12
    sig: "void stop_view_timer(u8 *this)"
    module: app/view_event
    status: ported
    vars:
      param_1: this (the view/controller)
      param_1_plus_0x50: timer
    notes: >-
      21 bl call sites, binary-scanned. Raw ARM is `ldr r0, [r0, #0x50];
      cmp r0, #0; bne 0x0812c6b0; bx lr`: load the optional timer pointer
      from the view at +0x50 and tail-call the ported timer_stop when one
      exists, otherwise return immediately. The Rust port keeps the same
      null guard; the only deliberate deviation is that the tail branch is
      spelled as a normal call.

  - address: 0x0810e02c
    ghidra: FUN_0810e02c
    name: view_timer_start_after
    size: 92
    sig: "void view_timer_start_after(u8 *view, u32 delay)"
    module: app/view_timer
    status: ported
    renames:
      param_1: view
      param_2: delay
    notes: >-
      Whole 92-byte extent, verified from osos.dec as
      0x0810e02c..0x0810e088; the `mov r0,#1; bx lr` leaf at
      0x0810e088 is the next function. A complete ARM B/BL-word scan
      finds 41 direct callers: 39 bl (38 unconditional bl + 1 bleq)
      and 2 tail b. The single predicated caller is caller-side gating;
      this routine has only the view.timer NULL test. On NULL it calls
      operator_new(0x2c) @ 0x082aadd4, stores the target pointer at
      view+0x50, then invokes timer_schedule_shim(view, timer, 0, 0)
      @ 0x0811108c, making the view the timer constructor's config
      word. It stores delay at view+0x54, calls the ported
      stop_view_timer helper @ 0x0810dfe8 through the default
      VIEW_EVENT_OPS slot, reloads +0x50 for timer_start_after(timer,
      delay) @ 0x0812c63c, reloads it again for the tail branch to
      timer_restart @ 0x0812bf4c. The +0x54 sibling re-armer @
      0x0810e090 establishes delay's meaning. No allocation-failure
      guard exists in stock or the port. The ported stop helper still
      uses the existing VIEW_EVENT_OPS seam so host tests can replace
      it; all other callees are already ported and direct. Rust
      represents the final tail branch as a call. Host tests use one
      unique low-4-GiB slab for the view/timer u32 target pointers:
      allocation constructs/sets/arms the timer, and a wrapper that
      replaces +0x50 proves both post-wrapper reloads use the
      replacement without allocating. match.py structural review against
      this worktree archive: LLVM inlines timer_schedule_shim,
      timer_start_after, and timer_restart plus their ops-table paths,
      but retains the stock lazy load/test/allocation/store ordering.

  - address: 0x0810de48
    ghidra: FUN_0810de48
    name: view_event_apply_mapped_staged_flags
    size: 16
    sig: "u32 view_event_apply_mapped_staged_flags(u8 *this)"
    module: app/view_event
    status: ported
    renames:
      param_1/r0: this
      FUN_0810e1c4: apply mapped staged flags
    notes: >-
      Exact 16-byte extent verified from raw osos.dec instructions
      0x0810de48 through 0x0810de54 with the next function beginning at
      0x0810de58. Decoding every ARM B/BL word finds 94 direct branch
      sites: 19 plain unconditional bl calls and 75 plain tail b calls,
      with no predicated calls. The body preserves r0 and r1 across its
      prologue, calls 0x0810e1c4, then returns 1 as the handled verdict.
      The callee walks this+0x60, maps item byte 0 through 0x0819c7a4
      into byte 2, and writes item byte 1 to the global flag-byte table
      at the original byte-0 index. r1 is dead because the callee saves
      without reading it. The unported callee is an explicit
      VIEW_EVENT_OPS seam: target builds dispatch to firmware
      0x0810e1c4 and host tests record a replacement invocation.

  - address: 0x0810dddc
    ghidra: FUN_0810dddc
    name: registry_observer_base_construct
    size: 20
    sig: "RegistryObserver *registry_observer_base_construct(RegistryObserver *this)"
    module: app/class_registry
    status: ported
    vars: {param_1: this, puVar1: observer}
    notes: >-
      Common 8-byte registry-observer C++ base constructor; 24 direct bl
      call sites. Raw ARM calls parent constructor 0x08147288, loads
      literal 0x089814fc from 0x0810ddf0, stores it at this+0x00, then
      returns the parent's unchanged r0. The parent store sequence is
      vtable 0x08986408 at +0x00 followed by zero at +0x04, so this port
      preserves the complete ordered base layout: parent vtable, cleared
      state, derived base vtable. Callers allocate/pass 8-byte observer
      objects and immediately overwrite +0x00 with their own vtables;
      0x082028a4 is the registry observer's derived constructor
      (0x089910ac). No null check exists; this must be writable.

  - address: 0x0810e64c
    ghidra: FUN_0810e64c
    name: class_registry_construct
    size: 96
    sig: "void *class_registry_construct(Registry *registry)"
    module: app/class_registry
    status: ported
    vars: {param_1: registry, piVar1: observer_cache, piVar3: observer, uVar2: registry}
    notes: >-
      The constructor of the global class registry @ 0x08a79ca4 (the
      crate's CLASS_REGISTRY), called from the static-init chain @
      0x082afb6c - 9 bl + 1 tail b call sites, binary-scanned. Calls
      0x08135308(this, 8, 4) (the container base ctor, capacity 8 /
      growth 4), then lazily builds a second 8-byte observer singleton
      (operator_new(8) + ctor 0x082028a4, vtable 0x089910ac, cached in
      the global word @ 0x089d01ac - the literal @ 0x0810e6ac) and
      dispatches its vtable[+0x18] attach; the cache store lands BEFORE
      the dispatch and the observer is re-loaded after it. Then
      0x08135040(this, observer) swaps it in for the container's default
      observer and 0x08134ff8(this, 1) enables notifications, firing
      the first change notification. operator_new,
      registry_container_construct, and observable_set_notify_enabled are
      already ported and called directly; the observer ctor and observer
      swap sit behind the CLASS_REGISTRY_OPS dispatch table (the
      app/singletons.rs SINGLETON_CTORS pattern). The documented remaining
      container-state initializer stub makes the port NOT hook-ready: it
      does not create the container's default observer, so the registry
      cannot faithfully enter stock's normal observer-swap path until
      0x08135110 is ported. match.py: identical call/store/dispatch
      structure; the only deltas are the observer seams and frame layout.

  - address: 0x0810e6b0
    ghidra: thunk_FUN_08135380
    name: registry_container_destruct
    size: 4
    sig: "Registry *registry_container_destruct(Registry *registry)"
    module: app/class_registry
    status: ported
    vars: {param_1: registry, "param_1[7]": auxiliary_allocation, "param_1[9]": observer}
    notes: >-
      Four-byte tail thunk: raw ARM is `b 0x08135380`. Binary-decoding all
      B/BL words in osos.dec found 10 plain bl callers, 0 predicated callers,
      and one tail b caller at 0x0812d314. The destination is a distinct
      72-byte destructor at 0x08135380..0x081353c8 including its literal
      0x08984770, not part of Ghidra's reported thunk extent. It installs
      that vtable, dispatches observer->vtable[+0x1c] for registry+0x24,
      conditionally frees the registry+0x1c auxiliary allocation via the
      ported free @ 0x0802edc8, clears both words, and tail-branches to
      observable_array_destruct @ 0x08271d2c. Host tests substitute only
      that final direct tail call because Registry's pointer fields widen on
      x86-64 while ObservableArray intentionally preserves target u32
      fields; the ARM build calls the existing port directly. match.py
      structural review is recorded with this port.

  - address: 0x08135110
    ghidra: FUN_08135110
    name: registry_container_initialize
    size: 168
    sig: "void registry_container_initialize(Registry *this, u32 capacity, u32 growth)"
    module: app/class_registry
    status: ported
    vars: {param_1: this, param_2: capacity, param_3: growth, piVar1: observer, piVar2: observer_cache}
    notes: >-
      Initializes the registry container fields: capacity +0x10, zero count
      +0x14, growth +0x18, zero auxiliary +0x1c, changed +0x20, disabled
      notifications +0x21, and null observer +0x24. Capacity 4 selects the
      lazily allocated/cached observer whose constructor 0x0816f70c installs
      vtable 0x08988eb0; every other capacity selects 0x081991f4 /
      0x08989ca4. On a cache miss raw ARM allocates eight bytes, constructs,
      caches, and dispatches vtable +0x18, then stores the observer at +0x24
      and dispatches +0x18 again. Neither allocation nor either dispatch is
      null-guarded; all returns are discarded.

  - address: 0x08135308
    ghidra: FUN_08135308
    name: registry_container_construct
    size: 48
    sig: "Registry *registry_container_construct(Registry *this, u32 capacity, u32 growth)"
    module: app/class_registry
    status: ported
    vars: {param_1: this, param_2: capacity, param_3: growth, puVar1: registry}
    notes: >-
      Concrete observable-array constructor with 6 bl call sites. Raw ARM
      saves capacity/growth, calls observable_array_construct
      (0x08271cec), writes literal vtable 0x08984770 at +0x00 of its
      return, restores the saved arguments, calls the unported state and
      default-observer initializer 0x08135110, then returns the saved
      observable-array result. The existing observable-array port is
      called directly; 0x08135110 remains a test-injectable ops boundary,
      so the outer wrapper is deliberately not the shipped default until
      that separate function is ported.

  - address: 0x0813533c
    ghidra: FUN_0813533c
    name: registry_container_construct_default
    size: 44
    sig: "Registry *registry_container_construct_default(Registry *this)"
    module: app/class_registry
    status: ported
    vars: {param_1: this}
    notes: >-
      The ADS default constructor of the same registry-container class as
      registry_container_construct @ 0x08135308: identical body shape and
      the identical vtable literal 0x08984770, differing only in
      hardcoding capacity/growth to 4 (mov r1, #4 / mov r2, #4 @
      0x08135350/0x08135354) instead of taking them in r1/r2. Raw ARM:
      push {r4,lr}; bl observable_array_construct @ 0x08271cec; str the
      vtable literal (loaded from 0x08135364) at +0x00 of the base ctor's
      return; bl registry_container_initialize @ 0x08135110 with (4, 4);
      return the saved r4. Size is 44: Ghidra's 40 covers the ten code
      words only and drops the trailing literal-pool word; the next
      function (a destructor) starts @ 0x08135368. 23 bl call sites, 0
      predicated, binary-verified by decoding every B/BL word in osos.dec.
      Capacity exactly four is the case that selects the lazily cached
      0x08988eb0 default observer inside registry_container_initialize,
      so all 23 callers build capacity-4-observer containers. Both
      callees were already ported: observable_array_construct is called
      directly and the initializer goes through the same
      CLASS_REGISTRY_OPS.container_initialize seam as the sibling ctor.
      match.py (--lib worktree build): same call/store/dispatch shape;
      deltas are the ops-table blx and frame layout, as in the sibling.

  - address: 0x082028a4
    ghidra: FUN_082028a4
    name: registry_observer_construct
    size: 20
    sig: "void *registry_observer_construct(void *allocation)"
    module: app/class_registry
    status: ported
    vars: {param_1: allocation, puVar1: observer}
    notes: >-
      Registry observer C++ derived constructor. Raw ARM is
      push {r4,lr}; bl 0x0810dddc; ldr r1, [pc,#4];
      str r1, [r0]; pop {r4,pc}, with the literal 0x089910ac at
      0x082028b8. Thus it forwards the allocation in r0 to the distinct
      unported base ctor 0x0810dddc, stores that vtable at +0x00 of the
      base return, and forwards the same r0 return value. Called after
      operator_new(8) from the lazy observer path in 0x0810e64c.

  - address: 0x080ed7c0
    ghidra: FUN_080ed7c0
    name: is_mpeg4_container_extension
    size: 180
    sig: "u32 is_mpeg4_container_extension(const u8 *path)"
    module: app/media_ext
    status: ported
    vars: {param_1: path}
    notes: >-
      File-extension test for the MPEG-4 container family. Takes strlen
      (0x08392478), copies the last 3 bytes of the string to a stack
      buffer with strncpy (0x080310d4), ASCII-lowercases them in place
      (the classic sub #0x41 / cmp #25 / add #32 range test), then
      strncmp (0x0803105c, n = 3) against four literals stored inline in
      the function's own constant pool at 0x080ed874 - "m4v", "m4p",
      "mp4", "mov" (verified against osos.dec). Returns 1 on any match,
      0 otherwise. 1 bl call site (0x0803bf48). The extension is taken
      as the last 3 characters unconditionally - there is no '.' check,
      so a 3-character filename with no extension is tested as if it
      were one, and a shorter name reads before the string start.
      Ported once strlen @ 0x08392478 landed (the blocker the scouting
      note recorded). Codegen deviation - LLVM inlines all three libc
      calls and folds the four strncmp tests into a constant decision
      tree; the behavior is proven by the host tests.

  - address: 0x0811fd98
    ghidra: FUN_0811fd98
    name: layer_set_alpha
    size: 16
    sig: "void layer_set_alpha(u8 *layer, u8 alpha)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: alpha}
    notes: >-
      Global-alpha setter on the live display-LAYER object (the one
      0x08120978 copies a surface_config block into). Stores the byte at
      +0x48 and raises the layer dirty flag at +0x1bc. 89 call sites
      (87 bl + 2 tail b, 45 distinct callers) - binary-scanned, the
      hottest function in this cluster. Identified from the cross-fade
      @ 0x0810da8c, which does `lsr r5, r0, #24` (the alpha channel of
      an ARGB word) then calls this with `r5` on the fading-in layer and
      `255 - r5` on the fading-out layer; other callers pass 0 or 0xff.
      Leaf, no lock (four instructions, no call).

  - address: 0x08120880
    ghidra: FUN_08120880
    name: layer_set_global_alpha_blend
    size: 16
    sig: "void layer_set_global_alpha_blend(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      Sets the layer blend mode byte (+0x49) to 1 and dirties the layer.
      17 bl call sites from 11 distinct callers. Mode 1 is the mode in
      which the render pass 0x0811f8c8 pushes the +0x48 global alpha to
      the driver - the full mapping it applies is mode 0 -> driver
      vtable[+0x40] code 1, mode 1 -> code 3 then vtable[+0x44](alpha),
      mode 2 -> code 2, mode 3 -> code 5 then vtable[+0x44](alpha).

  - address: 0x08120890
    ghidra: FUN_08120890
    name: layer_set_pixel_alpha_blend
    size: 20
    sig: "void layer_set_pixel_alpha_blend(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      Sets the layer blend mode byte (+0x49) to 2 and dirties the layer.
      6 bl call sites. Mode 2 does not push the global alpha; the
      applier 0x08120978 forces exactly this mode whenever the pixel
      format byte (+0x0a) is 3, which is why it reads as the per-pixel
      alpha format.

  - address: 0x0811f8c0
    ghidra: FUN_0811f8c0
    name: layer_is_dirty
    size: 8
    sig: "u8 layer_is_dirty(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      Reads the layer dirty byte at +0x1bc that every mutator in this
      cluster raises and the commit pair 0x08120654 / 0x0812068c clears.
      3 bl call sites.

  - address: 0x0811fda8
    ghidra: FUN_0811fda8
    name: layer_set_geometry
    size: 216
    sig: >-
      u8 *layer_set_geometry(u8 *layer, u32 origin_x, u32 origin_y,
      i32 source_y, i32 source_x, i32 width, i32 height,
      i32 display_width, i32 display_height)
    module: drivers/display_layer
    status: ported
    vars:
      param_1: layer
      param_2: origin_x
      param_3: origin_y
      param_4: source_y
      param_5: source_x
      param_6: width
      param_7: height
      param_8: display_width
      param_9: display_height
    notes: >-
      Installs a whole source/destination rectangle on a display layer,
      but only if it fits the layer's frame buffer; nothing at all is
      written when any check fails (all four comparisons happen before
      the lock is taken). 20 bl call sites, 16 distinct callers.
      Checks - buffer_height (+0x2c) >= source_y and buffer_width
      (+0x30) >= source_x (one cmp/cmpge pair, so a failed first test
      short-circuits the second), then display_width + source_x <=
      buffer_width and display_height + source_y <= buffer_height, with
      the plain width/height substituted whenever the corresponding
      display size argument is negative (the -1 = auto sentinel
      surface_config_init arms at config +0x1c/+0x20). A negative
      display size also leaves the stored +0x24/+0x28 untouched. On
      success it takes the layer's own mutex at +0x78 (mutex_lock @
      0x0807f5c4 / mutex_unlock @ 0x0807f6a0, both ported) and writes
      +0x24, +0x28, +0x44 = 1, +0x0c, +0x10, +0x18, +0x1c, +0x20, +0x14
      and +0x1bc = 1. Returns the layer on both paths. All comparisons
      are signed and the extent additions are plain `add`, so they wrap
      rather than saturate. The field map is the one recovered from the
      applier 0x08120978 (see drivers/surface).

  - address: 0x08120908
    ghidra: FUN_08120908
    name: layer_enable
    size: 76
    sig: "void layer_enable(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      Clears the layer disabled flag (+0x41) under the layer mutex.
      50 call sites (49 bl + 1 tail b, 20 distinct callers) - callers
      typically fetch several layers with 0x081d9064(display, index) and
      enable them in a row (e.g. 0x080feb68). Idempotent - an already
      enabled layer is left completely alone. On a real transition it
      also sets the enable-state-changed byte (+0x43) and the dirty flag
      (+0x1bc), and for format variant (+0x0b) 3 only, unbinds the
      layer's surface - layer_swap_surface(layer, NULL) @ 0x081206c4,
      ported in the same module and reached through the
      LAYER_DRIVER_HOOKS dispatch table (default - the port itself). The original
      releases the mutex with a tail branch. Ghidra's C for this
      function invents a jumptable at 0x08037e10 - that is just the
      mutex_unlock tail branch.
      CORRECTION to the first port - 0x081206c4 takes TWO arguments
      (its second instruction is `mov r4, r1`), and layer_enable leaves
      the `mov r1, #0` it used for the +0x41 store live across the
      `bleq`, i.e. it passes a NULL surface. The original hook slot was
      declared one-argument and silently dropped it; both the slot and
      the call now carry the surface.

  - address: 0x081208bc
    ghidra: FUN_081208bc
    name: layer_disable
    size: 52
    sig: "void layer_disable(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      Mirror of layer_enable - sets the disabled flag (+0x41). 3 bl call
      sites. Two asymmetries kept from the original - the disabled byte
      and the dirty byte are set unconditionally (re-disabling an
      already disabled layer still dirties it), while the
      enable-state-changed byte (+0x43) is raised only on an actual
      transition; and there is no alternate-commit notify on this side.
      Runs under the layer mutex, released with a tail branch.
      #[inline(never)] since the layer_install_planes port (the
      layer_bind_surface precedent - a real bl target LLVM would
      otherwise inline into its callers).

  - address: 0x081fc3f4
    ghidra: FUN_081fc3f4
    name: state_flags_contain
    size: 20
    sig: "u32 state_flags_contain(u8 *object, u32 mask)"
    module: util/state_flags
    status: ported
    vars: {param_1: object, param_2: mask}
    notes: >-
      Tests the state-flag word at +0x44 of the 0x081fbxxx state-machine
      class - `bics r0, mask, flags`, so it answers "does the word hold
      ALL bits of mask", returning 1 or 0. 34 bl call sites from 20
      distinct callers, always with a single bit; the masks scanned out
      of the binary are 0x40000 (20x), 0x10000 (4x), 0x1 (3x), 0x20000
      (2x), 0x80000 (1x), 0x100000 (1x) plus 3 sites whose mask comes
      from a register. An empty mask returns 1 (vacuous containment).
      The owning class is not otherwise identified - it is a progress
      state machine with a parent at +0x04 it notifies by code
      (0x0818a41c(parent, code, self); codes 5, 7, 10) and counters at
      +0x18 vs +0x1c / +0x50 compared as produced <= expected. Codegen
      deviation - LLVM normalizes the 0/1 with `clz`/`lsr #5` instead of
      the original's `movne`/`moveq`.

  - address: 0x081fc524
    ghidra: FUN_081fc524
    name: state_flags_set
    size: 24
    sig: "u32 state_flags_set(u8 *object, u32 mask)"
    module: util/state_flags
    status: ported
    vars: {param_1: object, param_2: mask}
    notes: >-
      Companion of state_flags_contain - ORs mask into the +0x44 state
      word and returns the subset of mask that was ALREADY set, i.e. a
      non-atomic fetch_or. 3 bl call sites; callers use a zero result as
      "this call is the one that set the bit" (0x081fc230) or compare it
      against the mask (0x081fc408 tests `!= 0x10000`). Plain
      load/modify/store - no lock, no LDREX, no interrupt guard, exactly
      as in the original.

  - address: 0x08184b24
    ghidra: FUN_08184b24
    name: load_cros_resource_list
    size: 56
    sig: "ResourceList *load_cros_resource_list(u32 resource_data)"
    module: util/resource_list
    status: ported
    renames:
      param_1/r0/r4: resource_data
      uVar1/r0/r5: application_resource_provider
      uVar2/r0: list
      DAT_08184b58: CROS_RESOURCE_TAG (0x534f5243)
      FUN_0819fe10: application_resource_provider
      FUN_082aadd4: operator_new
      FUN_08184fd4: load_resource_list
    notes: >-
      Raw extent is 0x08184b24..0x08184b5c: 52 instruction bytes followed
      by literal-pool word 0x534f5243, with the separately linked next
      function opening at 0x08184b5c. Ghidra's 52-byte size omits that pool
      word. A complete ARM B/BL decode of osos.dec finds ten inbound sites,
      all unconditional bl at 0x0811dbd8, 0x08180560, 0x08184bdc,
      0x08184c4c, 0x081856cc, 0x0819ee4c, 0x081a10bc, 0x081ea6b8,
      0x081eae74, and 0x08218630; there are no predicated forms or tail b
      callers. It gets the global application resource provider, allocates
      0x24 bytes through operator_new, and forwards the allocation plus
      provider, resource_data, parser FourCC CROS, and zero options to
      load_resource_list without checking the allocation. The port calls all
      three ported callees directly; host tests prove the allocation size/tag
      and every initializer argument.

  - address: 0x08184bd4
    ghidra: FUN_08184bd4
    name: copy_first_entry_payload
    size: 104
    sig: "void copy_first_entry_payload(void *owner_input, FirstEntryPayload *output)"
    module: util/first_entry_payload
    status: ported
    renames:
      param_1/r0: owner_input
      param_2/r1/r4: output
      piVar1/r5: temporary_owner
      iVar2/r0: first_entry
      FUN_08184b24: load_cros_resource_list (called directly)
      FUN_083d78c4: vector_size_elem4_alias_78c4
      FUN_08184f98: ptr_vector_at
      thunk_EXT_FUN_22000020: __rt_memcpy
    notes: >-
      Verified extent is 0x08184bd4..0x08184c3c: the next function begins
      with push {r4-r9,sl,lr} at 0x08184c3c. Raw decoding of every ARM B/BL
      word in osos.dec finds 28 direct calls: 27 unconditional bl plus one
      blne at 0x0819ef48. It obtains a temporary vector owner through the
      ported load_cros_resource_list, checks the elem4 vector at +0x14 through
      0x083d78c4, then either copies 12 bytes from its first entry +8 to the
      output through the 0x08037db0 __rt_memcpy veneer or stores 0, 0, and
      byte 6 at output +0, +4, and +8. The empty path deliberately leaves
      output +9..+11 untouched. It releases a non-NULL temporary by loading
      its vtable then tail-calling the function pointer at vtable +4. The
      factory result is dereferenced without a NULL guard before that tail;
      the function's callers commonly invoke it unconditionally; the sole
      blne at 0x0819ef48 guards one call. Rust preserves the result writes and
      direct calls to already ported vector/memcpy helpers, plus target virtual
      dispatch; host tests
      use the factory/release boundary because host pointers are wider than
      the target vtable words.

  - address: 0x08184f84
    ghidra: FUN_08184f84
    name: ptr_vector_count
    size: 20
    sig: "i32 ptr_vector_count(const u8 *owner)"
    module: util/ptr_vector
    status: ported
    vars: {param_1: owner}
    notes: >-
      ADS-inlined std::vector<T*>::size() - `(*(end @ +0x18) -
      *(begin @ +0x14)) asr 2`. 17 bl call sites from 10 distinct
      callers, every one of them a loop bound over the same owner
      (`n = count(owner); for i in 0..n { item = FUN_08184f98(owner, i) }`
      at 0x08185398, 0x081856a0, 0x0819ed78, 0x08147340). The owner is
      the 0x24-byte object built by FUN_08184fd4 - vtable at +0x00,
      the container at +0x14. The shift is arithmetic, so an inverted
      vector yields a negative count while the call sites compare
      unsigned (bhi); reproduced, not fixed. begin/end are read as
      32-bit words, so on a 64-bit host the function is only meaningful
      with synthetic values (the heap/block_region link-word precedent).

  - address: 0x08184f98
    ghidra: FUN_08184f98
    name: ptr_vector_at
    size: 60
    sig: "void *ptr_vector_at(u8 *owner, u32 index)"
    module: util/ptr_vector
    status: ported
    vars: {param_1: owner, param_2: index}
    notes: >-
      Bounds-checked element accessor for the same vector at owner+0x14
      that ptr_vector_count measures - calls FUN_083d78c4(owner + 0x14)
      twice (once for a non-empty test, once for the index bound) and
      returns `((void **)*(owner + 0x14))[index]`, or 0 when the vector
      is empty or the index is out of range. Not ported - its size
      helper 0x083d78c4 is outside the 0x08100000-0x081fffff sweep and
      unported, so the whole body would be dispatch-table indirection.
  - address: 0x08184fd4
    ghidra: FUN_08184fd4
    name: load_resource_list
    size: 76
    sig: "ResourceList *load_resource_list(ResourceList *list, u32 provider, u32 resource_data, u32 parser, u32 options)"
    module: util/resource_list
    status: ported
    renames:
      param_1/r0/r4: list
      param_2/r1/r5: provider
      param_3/r2/r6: resource_data
      param_4/r3/r7: parser
      param_5/stack/r8: options
      DAT_0818501c: RESOURCE_LIST_VTABLE_ADDRESS (0x08989508)
      FUN_083e472c: three_word_clear_seventh (already ported, called directly)
      FUN_08184f50: resource-list initializer (RESOURCE_LIST_OPS seam; not yet ported)
    notes: >-
      Size correction: raw extent is 0x08184fd4..0x08185020. Ghidra reports
      72 instruction bytes but omits the literal-pool vtable word at
      0x0818501c; the next function begins at 0x08185020. Decoding every ARM
      B/BL word in osos.dec finds 19 direct call sites: all are unconditional
      bl, with no predicated forms, tail b branches, or data-word occurrence.
      It stores vtable 0x08989508 at list+0, calls the already ported
      three_word_clear_seventh on the three-word vector at list+0x14, then
      calls unported FUN_08184f50(list, provider, resource_data, parser,
      options) and returns list. Ghidra invents surplus arguments for the
      clear helper; raw r0 is only list+0x14. Rust preserves this sequence and
      uses a direct 0x08184f50 target call on device; RESOURCE_LIST_OPS is the
      host-test seam for that unported initializer.

  - address: 0x08185038
    ghidra: FUN_08185038
    name: resource_list_destroy
    size: 184
    sig: "ResourceList *resource_list_destroy(ResourceList *list)"
    module: util/resource_list
    status: ported
    renames:
      param_1/r0/r4: list
      iVar2/r5: element_index
      iVar3/r6: vector_entry
      DAT_081850ec: RESOURCE_LIST_VTABLE_ADDRESS (0x08989508)
      FUN_08184d08: resource-list finalizer (unported)
      FUN_080e7970: free_wrapper (direct, tag 0x33)
      FUN_08144000: dynamic-state destructor (unported)
      FUN_082aad24: operator_delete (direct)
      FUN_083d78c4: vector_size_elem4_alias_78c4 (direct on target)
      FUN_08266f2c: cxx_array_dealloc (direct)
    notes: >-
      Verified raw extent is 0x08185038..0x081850f0: 180 instruction bytes
      plus the literal vtable word at 0x081850ec. Ghidra's 176-byte extent
      is short. Every ARM B/BL in osos.dec yields 14 direct BL call sites,
      all unconditional, plus one tail B at 0x081d0d40. The function replants
      the resource-list vtable; it calls unported 0x08184d08 only when both
      byte flags +0x21 and +0x22 are nonzero; then walks vector entries from
      +0x14, frees each +0x08 payload through tag-0x33 free_wrapper when
      +0x22 is set, and operator-deletes every entry. It destroys a non-NULL
      +0x10 state through 0x08144000 then operator_delete, and array-deallocs
      vector begin without clearing any descriptor word. The two unported
      direct calls use volatile host seams; target builds call them at their
      verified addresses. Host vector fixtures use target-width arithmetic;
      device builds directly call vector_size_elem4_alias_78c4.

  - address: 0x08120978
    ghidra: FUN_08120978
    name: layer_apply_config
    size: 344
    sig: "u32 layer_apply_config(u8 *layer, u8 *config)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: config}
    notes: >-
      Copies a surface_config_init-shaped 0x40-byte block into a live
      display layer, under the layer mutex (+0x78). 17 bl call sites
      from 12 distinct callers. This is the function whose stores
      recovered the layer field map that drivers/surface documents.
      Beyond the plain copy - every geometry word (origin x/y, source
      x/y, width, height) is clamped to >= 0 (the original stores then
      re-stores 0 with a predicated strlt; LLVM emits the branchless
      `bic r0, r0, r0, asr #31`, same result); buffer height is raised
      to at least the clamped height and buffer width to at least the
      clamped width; a display width of -1 (and ONLY the width is
      tested) makes it resolve both display sizes from the layer and
      write them BACK into the caller's block before forwarding them to
      +0x24/+0x28, which is how callers learn the resolved size; pixel
      format 3 forces blend mode 2 at +0x49; and for a kind-5
      (+0x08 == 5, +0x09 == 0) layer it drives the byte global
      @ 0x089ca8ac to `surface_flag(+0x34) == 0`. That global is the
      render-suppression latch the flush @ 0x0811fcc4 tests before
      rendering the kind-5 layer; it is modeled as the crate static
      KIND5_RENDER_SUPPRESSED (the app/context.rs deviation - 0x089ca8ac
      is runtime-initialized RW data). Sets +0x44 (first thing), +0x40
      and the dirty flag +0x1bc; returns 0. The config pointer is typed
      *mut because of the -1 write-back.

  - address: 0x0811f8c8
    ghidra: FUN_0811f8c8
    name: layer_render
    size: 892
    sig: "u8 *layer_render(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      The render pass of the display layer - the consumer of every field
      the ported setters write. 5 bl call sites (binary-scanned)
      @ 0x0811fd38, 0x08120674 (layer_force_enable), 0x081206ac
      (layer_restore_enable), 0x081d8b3c and 0x081d8bb4. Returns the
      layer on every path. Returns early unless the dirty byte at +0x1bc
      is set, swaps the active surface (+0x64 released through its
      vtable slot +0x04, +0x50 stored in its place and retained through
      slot +0x00 - with a subtype-1 guard that bails out of the whole
      render when +0x34 is set and +0x60 differs from +0x64), then
      either takes the short path (enable-state-changed +0x43 set while
      the layer is enabled - driver vtable[+0x3c], clear +0x43, notify
      0x081d892c, and for a kind-5 subtype-0 layer set the byte @
      0x089ca8ac, modeled as KIND5_RENDER_SUPPRESSED) or builds the
      0x50-byte descriptor on the stack (0x0828302c inits it), fills it
      from the layer fields, maps the pixel format into +0x3c through
      0x08120ad4, refreshes the plane block through layer_query_planes
      @ 0x081203c4 (into the layer's own +0x1c8..+0x1d4), aliases the
      plane words by pixel format (0: planes 0/2/1, 1: 0/1, 2|3: 0),
      notifies instead when the change happened on a disabled layer,
      hands a kind-5 layer's +0x34 to driver vtable[+0x18], and pushes
      a copy of the descriptor behind the class word 0x089a651c through
      driver vtable[+0x14]. It then translates the blend mode byte +0x49
      into the driver's code - mode 0 -> vtable[+0x40](code 1); mode 1
      -> code 3 then vtable[+0x44](alpha @ +0x48); mode 2 -> code 2;
      mode 3 -> code 5 then the alpha push; anything else skips both.
      The tail re-tests +0x43/+0x41 (the callees may have mutated the
      layer) and closes out a change on an enabled layer. The blocker's
      resolution: the densely-packed pointer fields (+0x04 driver, +0x60
      previous, +0x64 last-handed) are native-width and, on 64-bit hosts
      only, parked in the object's own unaccounted gaps at 0x70/0x58/0x68
      (the heap/block_region.rs word-index rule applied field by field);
      +0x00 (notify target) and +0x50 keep their literal offsets. On the
      32-bit target every offset is literal and the other eleven ports
      are untouched. The driver vtable is modeled as repr(C)
      DriverVtable with native slots at word indices 5/6/15/16/17 (the
      SurfaceVtable precedent); the class word is the address of the
      LAYER_RENDER_CLASS static (the REGION_START_FALLBACK precedent).
      0x0828302c, 0x08120ad4 and 0x081d892c dispatch through new
      LAYER_DRIVER_HOOKS slots with documented no-op defaults (the
      install_planes precedent); the render hook's default is now the
      port itself. match.py: LLVM inlines layer_query_planes and lowers
      the zeroed descriptors to __aeabi_memclr; the dirty gate, kind-1
      guard and surface release are instruction-identical.

  - address: 0x0811fc4c
    ghidra: FUN_0811fc4c
    name: layer_reconfigure
    size: 120
    sig: "u32 layer_reconfigure(u8 *layer, u8 *surface)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: surface}
    notes: >-
      74 call sites (57 bl + 17 tail b, 46 distinct callers, plus the
      alias thunk @ 0x08120134) - the hottest function of the
      display-layer cluster. Points a layer at a surface and re-derives
      everything downstream of it. Under the layer mutex - (1)
      layer_bind_surface @ 0x0811fe80, a no-op unless the format variant
      (+0x0b) is 3, in which case the layer's +0x50 surface is released
      and the argument retained in its place; (2) layer_query_planes
      @ 0x081203c4 reads the now-current surface's four plane addresses
      into four stack words; (3) layer_install_planes @ 0x0811feb8
      writes those same four words into the layer plane block
      +0x1c8..+0x1d4. Steps 2 and 3 are a pure pass-through - the i-th
      out-parameter of the query becomes the i-th argument of the
      install, with no inspection. Returns 0. Callers pass a stored
      object pointer as the surface (e.g. 0x0810dcb0 - `ldr r1, [r4,
      #28]`). The three callees dispatch through LAYER_DRIVER_HOOKS.

  - address: 0x0811fe80
    ghidra: FUN_0811fe80
    name: layer_bind_surface
    size: 56
    sig: "void layer_bind_surface(u8 *layer, u8 *surface)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: surface}
    notes: >-
      1 bl call site (layer_reconfigure). Re-checks the format variant
      (+0x0b) and returns immediately for anything but 3; otherwise
      takes the layer mutex and calls layer_swap_surface @ 0x081206c4,
      releasing the mutex with a tail branch. Deviation kept from the
      original - the sole caller already holds that mutex, so this
      re-locks it. The layer mutex is a plain RTXC counting semaphore
      (kernel/sync_mutex.rs - no recursion counter), so the nesting only
      survives because these layers' +0x78 cell is never created; the
      port reproduces the instruction sequence rather than fixing it.
      #[inline(never)] - a real bl target small enough for LLVM to
      inline away.

  - address: 0x081206c4
    ghidra: FUN_081206c4
    name: layer_swap_surface
    size: 60
    sig: "void layer_swap_surface(u8 *layer, u8 *surface)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: surface}
    notes: >-
      2 bl call sites (layer_enable @ 0x08120944 with a NULL surface,
      layer_bind_surface @ 0x0811fea8 with the caller's). Refcounted
      swap of the layer's bound surface at +0x50 - releases the old one
      through its vtable slot +0x04 (skipped when NULL), stores the new
      pointer, and tail-dispatches its vtable slot +0x00 to retain it
      (skipped when NULL). A NULL-to-NULL swap is a single dead store.
      The earlier "cannot hold a pointer field on a 64-bit host" blocker
      is sidestepped because +0x50 is followed by unaccounted padding up
      to +0x60, so a native-width pointer access at the literal byte
      offset fits on a 64-bit host (the densely packed +0x04/+0x60/+0x64
      fields lack that slack; layer_render parks them in the object's
      unaccounted gaps on 64-bit hosts - see its own entry); the surface vtable slots are modeled native-width (the
      cxx/templates.rs precedent, SurfaceVtable). The port is wired as
      LAYER_DRIVER_HOOKS.swap_surface's default, so layer_enable /
      layer_bind_surface now exercise it; tests interpose a recording
      mock. match.py - same structure, LLVM uses branches instead of
      predication and adds a frame-pointer setup.

  - address: 0x082978bc
    ghidra: FUN_082978bc
    name: surface_plane_address
    size: 200
    sig: "u32 surface_plane_address(u8 *surface, u32 index)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: surface, param_2: index}
    notes: >-
      39 bl call sites (including every plane lookup layer_query_planes
      @ 0x081203c4 makes). The surface object's plane getter: a
      seven-way jump table (addls pc,pc,r1,lsl#2) on index, each arm
      gating on the surface's pixel-format byte at +0x08 and returning
      one of the plane-address words +0x24/+0x28/+0x2c, or 0 on a miss:
      idx0 fmt{0,1}->+0x24, idx1 fmt1->+0x28, idx2 fmt0->+0x28, idx3
      fmt0->+0x2c, idx4/5/6 fmt2/3/4->+0x24, else 0. Format 0 is planar
      three-component (YUV 4:2:0, per the installer's flush pattern),
      1 two-plane, 2/3/4 packed single-plane. Belongs to the surface
      object cluster but lives in drivers/display_layer because its
      only ported caller is layer_query_planes and the porting task is
      confined to that module. Ported as a callee-first commit for
      layer_query_planes. Deviation - returns the plane word as u32
      (a target 32-bit address) so the surface block stays pure 4-byte
      fields on a 64-bit host; callers cast. match.py - same structure:
      LLVM lowers the jump table to a comparison chain.

  - address: 0x081203c4
    ghidra: FUN_081203c4
    name: layer_query_planes
    size: 224
    sig: "u8 *layer_query_planes(u8 *layer, u8 **p0, u8 **p1, u8 **p2, u8 **p3)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: p0, param_3: p1, param_4: p2, param_5: p3}
    notes: >-
      2 bl call sites (layer_reconfigure, layer_render). Reads the four
      plane addresses of the layer's bound surface (+0x50) through
      surface_plane_address @ 0x082978bc (ported in the callee-first
      commit) and writes them to the four out-pointers, selecting and
      aliasing by the layer's pixel format (+0x0a) - format 0 - p0=plane
      0, p1=plane 2, p2=plane 3; format 1 - p0=p2=plane 0, p1=p3=plane
      1; format 2 - p0=plane 4; format 3 - p0=plane 5; anything else
      leaves the four words zeroed. An unbound layer (+0x50 NULL)
      returns without touching them at all - the four zeroing stores
      happen only on the bound path, before the format dispatch.
      Returns the layer. Ported; the earlier "reads the +0x50 pointer
      field" blocker dissolved with the layer_swap_surface precedent
      (+0x50 has unaccounted slack up to +0x60, so a native-width
      access at the literal byte offset fits a 64-bit host). Now the
      default of LAYER_DRIVER_HOOKS.query_planes; tests interpose a
      recording mock. Deviations - the accessor's u32 plane words are
      zero-extended into native-width out-pointers on a 64-bit host;
      LLVM inlines surface_plane_address (no bl edge remains) and uses
      a jump table for the format dispatch. match.py - same structure:
      NULL guard, four zeroing stores, four-arm dispatch, conditional
      plane loads.

  - address: 0x0811feb8
    ghidra: FUN_0811feb8
    name: layer_install_planes
    size: 212
    sig: "void layer_install_planes(u8 *layer, u8 *p0, u8 *p1, u8 *p2, u8 *p3)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: p0, param_3: p1, param_4: p2, param_5: p3}
    notes: >-
      2 bl call sites (layer_reconfigure, and the kind-5 flush @
      0x0811fcc4). Disables the layer first if it is not disabled yet
      (layer_disable @ 0x081208bc) then flushes the pixel range(s)
      through 0x08044c48(addr, len) with lengths built from height
      (+0x20) * buffer width (+0x30) = AREA. Kind (+0x08) 5 with pixel
      format (+0x0a) 0 and bit 31 clear in p0 flushes THREE ranges -
      p0 for AREA, p1 for AREA/4, p2 for AREA/4, i.e. planar YUV 4:2:0,
      which is what pins these four words as component plane addresses.
      Any other kind flushes p0 alone, for AREA*2 (format 2) or AREA*4
      (format 3), and not at all for the other formats. Finally stores
      the four plane words into +0x1c8..+0x1d4 with one `stm` and raises
      the dirty flag (+0x1bc). Ported; the earlier "writes four
      pointer-typed fields" blocker dissolved by storing the block as
      four u32 words - exactly what the original's stmia writes - which
      truncates the (zero-extended u32, per the surface_plane_address
      deviation) native pointers losslessly on a 64-bit host. The flush
      callee 0x08044c48 (the clean-only range walk, no bit-31 guard of
      its own - hence the caller-side tst on p0) is not ported; it
      dispatches through a new LAYER_DRIVER_HOOKS.flush_range slot
      (default - no-op, the heap/dcache.rs cache-op rationale), and the
      port is now the install_planes default. layer_disable gained
      #[inline(never)] (the layer_bind_surface precedent - a real bl
      target LLVM would otherwise inline away). match.py - same
      structure: predicated disable bl, kind/format dispatch, per-flush
      AREA reload+mul with asr#2 / lsl#1 / lsl#2 lengths, four word
      stores at +0x1c8..+0x1d4 (LLVM splits the stm), dirty store; blx
      through the hook slot in place of bl, branches instead of
      predication, frame-pointer setup.

  - address: 0x08120134
    ghidra: thunk_FUN_0811fc4c
    name: layer_reconfigure_thunk
    size: 4
    sig: "u32 layer_reconfigure_thunk(u8 *layer, u8 *surface)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: surface}
    notes: >-
      A single `b 0x0811fc4c` - an ADS alias veneer for
      layer_reconfigure, called once (bl @ 0x080f58e8). Ported as the
      same forward-and-return (a pure alias). #[inline(never)] on both
      the thunk and layer_reconfigure: without the latter LLVM inlines
      the whole callee body into the veneer. match.py - the crate-wide
      frame-pointer deviation: LLVM emits bl + mov r0,#0 + return
      instead of the bare b (no sibling-call fold with a kept frame
      pointer); the call-graph edge is identical. A hook on the stock
      veneer should point straight at layer_reconfigure.

  - address: 0x08120654
    ghidra: FUN_08120654
    name: layer_force_enable
    size: 56
    sig: "u8 layer_force_enable(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      RENAMED from the earlier scouting note's `layer_suspend` - this
      half *enables*, so "suspend" read backwards. Temporarily makes a
      hidden layer visible and remembers to put it back. 1 bl call site
      @ 0x081d8a20, inside a loop that walks all six layers of a display
      object. An already-enabled layer is left completely untouched
      (returns 0, the +0x41 byte it just read). A disabled one is
      enabled (layer_enable @ 0x08120908), rendered at once
      (layer_render @ 0x0811f8c8, LAYER_DRIVER_HOOKS.render), its dirty
      flag cleared and the parked flag +0x42 raised for
      layer_restore_enable. Returns the parked flag on both paths; the
      sole caller ignores it.

  - address: 0x0812068c
    ghidra: FUN_0812068c
    name: layer_restore_enable
    size: 56
    sig: "u8 layer_restore_enable(u8 *layer)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer}
    notes: >-
      RENAMED from the earlier scouting note's `layer_resume`. Mirror of
      layer_force_enable - if the parked flag +0x42 is set, disables the
      layer again (layer_disable @ 0x081208bc), renders, clears the
      dirty flag and clears +0x42. Returns the parked flag as it was on
      entry; the sole caller @ 0x081d8ac0 ORs the six layers' answers to
      learn whether the display changed at all (and only then sets the
      display's +0x98 byte). Asymmetry worth keeping - it reads +0x42
      and never re-reads +0x41, so a layer someone else enabled in
      between is still disabled here.

  - address: 0x08120ad4
    ghidra: FUN_08120ad4
    name: layer_plane_format_code
    size: 60
    sig: "void layer_plane_format_code(u8 *layer, u8 *out)"
    module: drivers/display_layer
    status: ported
    vars: {param_1: layer, param_2: out}
    notes: >-
      1 bl call site (the render pass layer_render @ 0x0811f8c8). Maps
      the layer's pixel format byte (+0x0a) to the driver's plane-format
      code and stores it through out - descriptor +0x3c at the call
      site: 0->8, 1->9, 2->3, 3->7; anything else returns without
      storing (bxne lr), so the descriptor's zeroed byte survives. A
      compare/moveq chain with a shared strb. Now the shipped default
      of LAYER_DRIVER_HOOKS.plane_format_code (the layer_query_planes
      precedent); the slot stays for recording mocks, and the render
      suite's counting mock delegates to the port instead of
      re-implementing the mapping. match.py - same structure (ldrb
      +0x0a, <=3 gate, code select, strb); LLVM packs the four codes
      into a 32-bit constant 0x07030908 with a shift-select, plus the
      usual frame-pointer prologue.

  # --- Batch: the by-id class registry (app/registry) ---
  # One statically-allocated associative container @ 0x08a79ca4 maps a
  # 0x80-aligned class id to that class's single live instance; 45
  # constructors publish themselves into it through 0x081d23f8. The same
  # number is the runtime type id, so every per-class accessor looks the
  # id up and then casts the result to the SAME id through 0x08275b9c.
  # Ids identified from the class name each constructor hands to the
  # name factory @ 0x0820b230 in the same basic block:
  #   0x4180 TCNotesDispatcher, 0x5680 TCSportTimer, 0x5800 TRadioCntlr,
  #   0x7600 TSearchCntlr, 0x8080 TCDemoMode.

  - address: 0x08187ad0
    ghidra: FUN_08187ad0
    name: demo_mode_keyed_object
    size: 20
    sig: "void *demo_mode_keyed_object(DemoMode *demo_mode, u32 key)"
    module: app/registry
    status: ported
    vars: {param_1: demo_mode, param_2: key}
    notes: >-
      29 bl call sites, binary-scanned over osos.dec; no predicated
      forms, no tail b, no data-word references. Ghidra's 20-byte extent
      is exact (the next function starts @ 0x08187ae4); it emits no C
      for this one (functions.csv marks it unknown). Body, verbatim:
      `ldr r3,[r0]; mov r2,r1; ldr r3,[r3,#0x100]; ldr r1,[r0,#0x30]; bx r3`
      - a TCDemoMode method tail-dispatching vtable slot +0x100 (index
      64) as `vtable[64](this, this->keyed_registry, key)`, resolving
      the object registered under key in the manager's keyed registry.
      All 29 sites share one idiom: this comes from demo_mode_instance
      @ 0x081883fc (25 call it within the preceding dozen instructions,
      the other four reuse its result cached in a callee-saved
      register), every key literal sits in the 0x0dad0000..0x0dad0fff
      resource-id space, and the result feeds object_cast_to_class @
      0x08275b9c directly. The +0x30 field is zeroed by the TCDemoMode
      ctor @ 0x081889c0, filled from the freshly built 300-byte keyed
      registry at +0x2c by the sibling setter @ 0x8187b08
      (`ldr r1,[r0,#0x2c]; str r1,[r0,#0x30]`, unported). The slot's
      target is UNRECOVERABLE: the ctor installs vtable pointer
      0x08989718, which lands in the C++ mangled-name blob in the
      decrypted image (the same page mismatch demo_mode_instance's note
      records), so dispatch goes through the object's own vtable
      pointer. No NULL guard at either dereference or before bx,
      reproduced. Deviation: the terminal bx is a final Rust call whose
      result is propagated - no observable work follows the dispatch
      (the cxx/vtable.rs decoder_poll precedent). match.py structural:
      same load order and tail dispatch; LLVM adds the usual {fp,lr}
      frame that decoder_poll and object_cast_to_class also carry.

  - address: 0x081883fc
    ghidra: FUN_081883fc
    name: demo_mode_instance
    size: 28
    sig: "void *demo_mode_instance(void)"
    module: app/registry
    status: ported
    notes: >-
      328 bl call sites (binary-scanned; the earlier note said 312) -
      the busiest function in 0x08100000-0x081fffff. Loads the literal
      class id 0x8080 (the word @ 0x08188418), calls the registry
      lookup 0x081d2184 and tail branches to the checked-downcast
      veneer 0x08275b9c with the SAME id, i.e.
      `object_cast_to_class(registry_lookup_by_id(0x8080), 0x8080)`.
      Class identified as TCDemoMode - the constructor @ 0x081889c0
      registers its `this` under 0x8080 and, in the same basic block,
      hands the literal "TCDemoMode" (@ 0x08188af4) to the class-name
      factory singleton @ 0x0820b230, the same register-then-name
      pairing that identifies TCNotesDispatcher/TCSportTimer/
      TRadioCntlr/TSearchCntlr. Caveat - the vtable pointer that ctor
      installs (0x08989718) falls inside the C++ mangled-name blob in
      the decrypted image instead of the vtable region above
      0x0898a000, so that page of the image does not match the running
      device; the identification rests on the code, not on that
      pointer. Siblings with the same shape - 0x08172124 (id 0x6000, 72
      bl) and 0x08100b74 (id 0x6600, 36 bl), both ported, both classes
      unidentified (their ctors never name themselves to the factory).
      Ghidra renders the tail veneer as a recovered-jumptable warning.

  - address: 0x08171ff8
    ghidra: FUN_08171ff8
    name: instance_6000_settings_block
    size: 8
    sig: "u8 *instance_6000_settings_block(u8 *instance)"
    module: app/registry
    status: ported
    renames:
      param_1/r0: instance
    notes: >-
      `add r0, r0, #0x60; bx lr` - returns a pointer to the settings
      block embedded at +0x60 of the class-0x6000 singleton (the object
      instance_of_class_6000 resolves; the caller @ 0x08163148 feeds
      that accessor's result straight in, the twelve settings-view
      callers @ 0x081ebe80..0x081ede24 reach the same object through
      their context's +0x378 field). 13 bl call sites, binary-scanned
      over osos.dec - all unconditional, no predicated forms, no tail b,
      no data-word references (not virtually dispatched). Ghidra's
      8-byte extent is exact: the word @ 0x08172000 is a lone `bx lr`
      (an empty C++ destructor) and the next function starts @
      0x08172004. Ghidra's osos.asm listing of this page is misaligned
      garbage (entry rendered as `add r0,r0,#0x60` against a wrong
      label, 0x0817a8bc as `blx r1`); the raw bytes are the truth. The
      block is at least 0x11 bytes: seven signed enum bytes at
      +0x00..=+0x06 (the setter @ 0x081edddc writes the -2/-1
      sentinels), a u64 at +0x08 and a byte at +0x10 (default 25 =
      0x19); the sibling @ 0x0817209c mirrors all of them into the
      media-player state object via the set-then-notify family
      0x08067738..0x08067850 (the repeat/shuffle cluster), so the block
      is the playback-preferences record. No NULL guard, matching the
      original: a NULL instance yields 0x60 and the caller faults on the
      field read, as stock does (port uses wrapping_add so host arithmetic
      stays UB-free). match.py: the core `add r0, r0, #0x60` matches;
      LLVM adds the frame-pointer push/pop every tiny port in this crate
      shows (observable_set_changed precedent).

  - address: 0x08172124
    ghidra: FUN_08172124
    name: instance_of_class_6000
    size: 24
    sig: "void *instance_of_class_6000(void)"
    module: app/registry
    status: ported
    notes: >-
      72 bl call sites (binary-scanned; the earlier note said 63). Same
      lookup-then-cast accessor as 0x081883fc for class id 0x6000. The
      class registers itself @ 0x08173774 but never hands a name to the
      factory @ 0x0820b230, so the CLASS IS NOT IDENTIFIED - the id is
      the only handle the firmware gives it and the symbol says so
      rather than inventing a name.

  - address: 0x0802ad10
    ghidra: FUN_0802ad10
    name: operation_dispatch
    size: 40
    sig: "i32 operation_dispatch(u32 first, u32 second, u32 third, u32 operation, u32 fifth, u32 sixth)"
    module: util/operation_dispatch
    status: ported
    renames:
      param_1/r0: first
      param_2/r1: second
      param_3/r2: third
      param_4/r3: operation
      param_5/stack: fifth
      param_6/stack: sixth
      FUN_0802ac90: operation_dispatch_core
    notes: >-
      A pure 40-byte six-word ABI wrapper. Raw ARM saves r2/r3/r4/lr,
      reloads its fifth and sixth arguments from the incoming stack at
      sp+16/sp+20, places them at the outgoing stack positions, and calls
      0x0802ac90 with all six words unchanged; it returns that core's i32
      status in r0. The core validates operation +0xfc, validates first/sixth,
      and on success dispatches an eight-word request. This port uses the
      target ROM address directly and a replaceable host callback seam; its
      focused host test records each forwarded word and return status.

  # --- selector-controlled mode word transform ---
  - address: 0x0802b504
    ghidra: FUN_0802b504
    name: transform_word_for_mode
    size: 52
    sig: "u32 transform_word_for_mode(u32 mode, u32 word)"
    module: util/bswap
    status: ported
    renames:
      param_1/r0: mode
      param_2/r1: word
    notes: >-
      52-byte leaf (0x0802b504..0x0802b538), thirteen ARM instructions,
      extent byte-confirmed by the distinct sibling starting at 0x0802b538.
      Call count verified by decoding every B/BL word in osos.dec: 22 plain
      bl sites, zero predicated forms. Exactly mode 1 returns word with all
      four bytes reversed (assembled in registers via shift/EOR byte lanes,
      equivalent to u32::swap_bytes); every other 32-bit mode value returns
      word unchanged, so callers gate on an exact mode word, not a flag
      range. The 22 callers are the record-header parsing family at
      0x08028xxx..0x08029xxx (FUN_08028e4c, FUN_08028f14, FUN_08028ff4,
      FUN_08029144, FUN_08029630, FUN_0802974c, FUN_080298e0, ...), which
      convert big-endian record fields only when the record mode is 1.
      0x0802b538 is a byte-identical twin used by the checked-block family.
      It is separately ported as transform_checked_word_for_mode so its ten
      direct retail callers retain their own exported target rather than an
      inlined duplicate. No deliberate deviations. match.py
      reviewed: LLVM emits a branchless predicated rotate form (orreq on
      the mode==1 comparison) where ADS branched over the swap, but the
      mode compare, conditional transform, and passthrough structure match.

  - address: 0x0802b538
    ghidra: FUN_0802b538
    name: transform_checked_word_for_mode
    size: 52
    sig: "u32 transform_checked_word_for_mode(u32 mode, u32 word)"
    module: util/bswap
    status: ported
    renames:
      param_1/r0: mode
      param_2/r1: word
    notes: >-
      Raw ARM is exactly 52 bytes (0x0802b538..0x0802b56c), thirteen
      instructions, bounded by checked_byte_block_convert_core's distinct
      push at 0x0802b56c. It is byte-identical to
      transform_word_for_mode @ 0x0802b504: move mode to r2, compare exact
      value 1, pass word through in r0 for every other mode, otherwise
      construct its four-byte reversal in register lanes. Decoding every ARM
      B/BL immediate in osos.dec finds ten plain unconditional bl callers,
      zero predicated bl forms, and zero direct B entries: 0x0802b5ac,
      0x0802b608, 0x0802b62c, 0x0802b680, 0x0802b704, 0x0802b764,
      0x0802b794, 0x0802b83c, 0x0802b8ac, and 0x0802b8e8. These are the
      checked byte/word block conversion family; their unconditional calls
      confirm the callee itself owns no NULL or flag guard. The port uses
      u32::swap_bytes for the disjoint-lane XOR result and a distinct
      target-only text section so LLVM does not fold this address's symbol
      into its byte-identical sibling. No deliberate deviations. match.py
      structural review is expected to differ in the conditional-swap shape.

  - address: 0x0802b56c
    ghidra: FUN_0802b56c
    name: checked_byte_block_convert_core
    size: 104
    sig: "u32 checked_byte_block_convert_core(u32 mode, u8 **input_cursor, u8 **input_cursor_mirror, u8 **output_cursor, u8 **output_cursor_mirror, i32 byte_count)"
    module: util/checked_byte_block
    status: ported
    renames:
      param_1/r0: mode
      param_2/r1: input_cursor
      param_3/r2: input_cursor_mirror
      param_4/r3: output_cursor
      param_5/stack: output_cursor_mirror
      param_6/stack: byte_count
      pbVar3/r7: source
      pbVar2/r5: target
      iVar4/r6: byte_sum
      FUN_0802b538: transform_checksum_for_mode
    notes: >-
      Raw ARM is exactly 104 bytes (0x0802b56c..0x0802b5d4), bounded by the
      distinct following byte-wrapper function. Decoding every ARM B/BL word
      in osos.dec finds exactly 17 plain unconditional bl call sites, zero
      predicated bl forms, and zero direct B entries. The core loads its
      initial source/output cursor from input_cursor/output_cursor (not either
      mirror), copies byte_count bytes while accumulating their unsigned
      wrapping sum, then transforms and compares the following aligned u32
      checksum. Exactly mode 1 reverses that checksum word; every other mode
      is identity. On a match it advances source past the checksum and stores
      input_cursor, input_cursor_mirror, output_cursor, then
      output_cursor_mirror; it returns 4 without changing aliases on mismatch,
      after its output writes. Its blt loop treats byte_count as signed, so
      zero or negative counts require a transformed zero checksum. No
      deliberate deviations: the local mode transform is inlined as in the
      neighboring byte reader. Host tests cover mode 0/1, mismatch writes,
      negative count, and distinct primary/mirror cursor slots.

  - address: 0x0802b5d4
    ghidra: FUN_0802b5d4
    name: checked_word_block_convert_core
    size: 128
    sig: "u32 checked_word_block_convert_core(u32 mode, u32 **input_cursor, u32 **input_cursor_mirror, u32 **output_cursor, u32 **output_cursor_mirror, i32 word_count)"
    module: util/checked_word_block
    status: ported
    renames:
      param_1/r5: mode
      param_2/r4: input_cursor
      param_3/r12: input_cursor_mirror
      param_4/r3: output_cursor
      param_5/r10: output_cursor_mirror
      param_6/r11: word_count
      puVar4/r7: source
      piVar3/r6: target
      iVar6/r9: sum
      iVar5/r8: index
      FUN_0802b538: transform_checked_word_for_mode
    notes: >-
      Raw ARM is exactly 128 bytes (0x0802b5d4..0x0802b654), 32 instructions,
      bounded by the distinct following function prologue. Decoding every ARM
      B/BL immediate in osos.dec finds nine plain unconditional bl callers,
      zero predicated bl forms, and zero direct B entries at 0x0802b6a0,
      0x0802c728, 0x0802c924, 0x0802cb20, 0x0802cd38, 0x0802d148,
      0x0802d3f4, 0x0802d904, and 0x0802dbcc. The core loads source from
      input_cursor_mirror and target from output_cursor_mirror, transforms
      word_count aligned words for exact mode 1, stores them, and accumulates
      a wrapping u32 sum. It transforms and compares the next aligned word.
      Success advances input cursor, input mirror, output cursor, and output
      mirror in that store order; failure returns 4 with aliases unchanged
      after the output writes. The signed blt bound means zero and negative
      counts require a transformed zero checksum. No deliberate deviations.
      Host tests cover mode-one conversion, distinct mirror cursor selection,
      mismatch output writes, distinct untouched aliases, and a negative count.
      match.py structural review finds the same cursor loads, transform loop,
      checksum comparison, and success-store order; LLVM uses a decrementing
      loop and extra frame setup.

  - address: 0x0802b654
    ghidra: FUN_0802b654
    name: checked_byte_block_convert
    size: 84
    sig: "u32 checked_byte_block_convert(u32 mode, u8 **input_cursor, u8 **input_cursor_mirror, u8 **output_cursor, u8 **output_cursor_mirror, i32 byte_count, u32 *out_leading_word)"
    module: util/checked_word_block
    status: ported
    renames:
      param_1/r4: mode
      param_2/r5: input_cursor
      param_3/r12: input_cursor_mirror
      param_4/r6: output_cursor
      param_5/r7: output_cursor_mirror
      param_6: byte_count
      param_7/r8: out_leading_word
      FUN_0802b538: transform_word_for_mode
      FUN_0802b5d4: checked_byte_block_convert_core
      uVar1: leading_word
    notes: >-
      84 bytes exactly (0x0802b654..0x0802b6a8), 21 ARM instructions,
      54 bl call sites, no tail branches. Reads **input_cursor_mirror as an
      aligned u32, transforms that leading word with 0x0802b538, stores it
      through out_leading_word, then calls 0x0802b5d4 with the original cursor
      slots and signed byte_count. Exactly mode 1 reverses the leading word;
      all other modes preserve it. The flat byte core now has a direct Rust
      port, so this wrapper directly invokes checked_byte_block_convert_core.
      Host tests cover leading-word mode conversion and the core's copied bytes,
      checksum status, and cursor advances. match.py preserves the
      load/transform/store/core-call/return shape; expected differences are
      LLVM frame allocation and the inlined transform.

  - address: 0x0802b6a8
    ghidra: FUN_0802b6a8
    name: checked_byte_block_reader
    size: 132
    sig: "u32 checked_byte_block_reader(u32 mode, u8 **input_cursor, u8 **input_cursor_mirror, u8 **output_cursor, u8 **output_cursor_mirror, i32 row_count, i32 bytes_per_row)"
    module: util/checked_byte_block
    status: ported
    renames:
      param_1/r0: mode
      param_2/r12: input_cursor
      param_3/r4: input_cursor_mirror
      param_4/r3: output_cursor
      param_5/stack: output_cursor_mirror
      param_6/stack: row_count
      param_7/stack: bytes_per_row
      pbVar2/r1: source
      pbVar4/r5: target
      iVar5/r6: byte_sum
      FUN_0802b538: transform_checksum_for_mode
    notes: >-
      Raw ARM is exactly 132 bytes (0x0802b6a8..0x0802b72c): the following
      word starts the distinct three-dimensional word-block reader. Decoding
      every ARM B/BL word in osos.dec finds exactly 30 call sites, all plain
      unconditional bl, with zero predicated bl and zero direct B entries.
      It reads initial source/output cursors from input_cursor_mirror and
      output_cursor_mirror, respectively, copies row_count * bytes_per_row
      bytes in row-major order while accumulating their unsigned wrapping
      sum, then transforms the following aligned u32 via 0x0802b538 and
      compares it to that sum. Exactly mode 1 byte-reverses that checksum;
      all other modes preserve it. A match advances input_cursor,
      input_cursor_mirror, output_cursor, then output_cursor_mirror and
      returns 0. A mismatch returns 4 without advancing any alias, although
      copied bytes remain in output. Both loop bounds use signed blt, making
      zero or negative bounds an empty block that accepts only transformed
      zero. Deliberate deviation: the one transform call is reproduced
      inline, matching sibling checked-word-block ports. Host tests cover
      row copy/sum/ordered alias advances, mode-1 checksum reversal,
      mismatch-after-copy, and a negative bound. match.py reviewed against
      the ARM release build; expected differences are the inlined transform
      and LLVM loop shape. Reference:
      decomp/c/001/0802b6a8_FUN_0802b6a8.c.

  - address: 0x0802b7c4
    ghidra: FUN_0802b7c4
    name: checked_byte_block_reader_3d
    size: 164
    sig: "u32 checked_byte_block_reader_3d(u32 mode, u8 **input_cursor, u8 **input_cursor_mirror, u8 **output_cursor, u8 **output_cursor_mirror, i32 dim0, i32 dim1, i32 dim2)"
    module: util/checked_byte_block
    status: ported
    renames:
      param_1/r0: mode
      param_2/r1: input_cursor
      param_3/r2: input_cursor_mirror
      param_4/r3: output_cursor
      param_5/stack: output_cursor_mirror
      param_6/stack: dim0
      param_7/stack: dim1
      param_8/stack: dim2
      pbVar3/r1: source
      pbVar4/r5: target
      iVar5/r6: byte_sum
      FUN_0802b538: transform_checksum_for_mode
    notes: >-
      Raw ARM is exactly 164 bytes (0x0802b7c4..0x0802b868), bounded by the
      distinct following three-dimensional word-block reader. Decoding every
      ARM B/BL word in osos.dec finds exactly 14 plain unconditional bl call
      sites, zero predicated bl forms, and zero direct B entries; callers at
      0x0802bd6c, 0x0802bec8, 0x0802bfb8, 0x0802c258, 0x0802c3b4,
      0x0802c4d0, 0x0802c6ec, 0x0802c8e8, 0x0802cad0, 0x0802cce8,
      0x0802d10c, 0x0802d3b8, 0x0802d8b4, and 0x0802db7c. It reads initial
      source/output cursors from input_cursor and output_cursor, copies
      dim0*dim1*dim2 bytes in three signed blt loops while accumulating their
      unsigned wrapping sum, then transforms the following aligned u32 via
      0x0802b538 and compares it to that sum. Exactly mode 1 byte-reverses
      the checksum; every other mode preserves it. A match advances input
      cursor, input mirror, output cursor, then output mirror; mismatch
      returns 4 without advancing aliases after copying bytes. Zero or negative
      dimensions leave the corresponding nest empty and accept only a zero
      transformed checksum. Deliberate deviation: the byte-identical
      0x0802b538 transform is inlined, matching neighboring reader ports.
      Host tests cover three-dimensional copy/sum/advance, mode-1 reversal,
      mismatch-after-copy, primary-versus-mirror cursor selection, and a
      negative dimension. Reference:
      decomp/c/001/0802b7c4_FUN_0802b7c4.c.

  - address: 0x0802b868
    ghidra: FUN_0802b868
    name: checked_word_block_convert_3d
    size: 188
    sig: "u32 checked_word_block_convert_3d(u32 mode, u32 **input_cursor, u32 **input_cursor_mirror, u32 **output_cursor, u32 **output_cursor_mirror, i32 dim0, i32 dim1, i32 dim2)"
    module: util/checked_word_block
    status: ported
    renames:
      param_1/r0: mode
      param_2: input_cursor
      param_3: input_cursor_mirror
      param_4: output_cursor
      param_5/stack: output_cursor_mirror
      param_6/stack: dim0
      param_7/stack: dim1
      param_8/stack: dim2
      FUN_0802b538: transform_word_for_mode
      puVar3/r4: source
      piVar8/r12: target
      iVar6/r7: sum
      iVar7/r8: i
      iVar5/r6: j
      iVar4/r5: k
    notes: >-
      188 bytes exactly (0x0802b868..0x0802b924; Ghidra's extent is right
      this time - the body ends in pop {.., pc} and the next function
      opens at 0x0802b924, no literal pool). Decoding every ARM B/BL word
      in osos.dec finds exactly 31 call sites, all plain unconditional
      bl, zero predicated forms, zero direct B sites; all sampled callers
      pass stack-local input/output cursor alias pairs and positive
      constant dimensions (typically 8/2/256). This standalone
      three-dimensional word-block decoder converts
      dim0*dim1*dim2 words in a three-level nest. Each word passes the
      mode-controlled transform 0x0802b538 (exactly mode 1 reverses all
      four bytes, anything else is identity - byte-verified), is stored
      through the output cursor in row-major order, and accumulates into
      a wrapping 32-bit sum; the next input word is then transformed and
      compared against that sum. On a match it returns 0 and advances
      BOTH input aliases to one word past the checksum and BOTH output
      aliases past the last written word (store order: input cursor,
      input mirror, output cursor, output mirror); on a mismatch it
      returns 4 with all four aliases untouched, the transformed words
      already sitting in the output buffer. Initial positions are read
      once, input from input_cursor_mirror and output from
      output_cursor_mirror. Loop bounds are signed (blt): a zero or
      negative dimension empties its nest, leaving a zero-length block
      whose checksum word must transform to 0. Deliberate deviation:
      the three bl 0x0802b538 sites (inner loop plus checksum word) are
      reproduced inline like the sibling checked_byte_block_convert port
      rather than exported as a second function. match.py shows the same
      nest/sum/checksum/quad-store shape; expected diffs are LLVM's
      count-down inner loop, its own stack-arg frame, and the inlined
      transform. Host tests cover mode 0 copy/sum/advance, mode 1 byte
      reversal with transformed checksum, mismatch (status 4, no advance,
      output still written), zero and negative dimension empty blocks,
      and mod-2^32 sum wraparound. Reference C:
      decomp/c/001/0802b868_FUN_0802b868.c.

  - address: 0x08100b74
    ghidra: FUN_08100b74
    name: instance_of_class_6600
    size: 24
    sig: "void *instance_of_class_6600(void)"
    module: app/registry
    status: ported
    notes: >-
      36 bl call sites (binary-scanned; the earlier note said 38). Same
      accessor for class id 0x6600, registered @ 0x08101aa4. CLASS NOT
      IDENTIFIED, same reason as 0x08172124.

  - address: 0x08103464
    ghidra: FUN_08103464
    name: queued_message_construct
    size: 72
    sig: "QueuedMessage *queued_message_construct(QueuedMessage *storage, u32 message_code, const u8 *bytes, u32 byte_count)"
    module: app/queued_message
    status: ported
    renames:
      param_1: storage
      param_2/r5: message_code
      param_3/r6: bytes
      param_4/r7: byte_count
      puVar1/r4: message
      DAT_081034ac: QUEUED_MESSAGE_VTABLE
      FUN_08266a48: queued_message_base_construct
      FUN_082aadd4: operator_new
      FUN_081b9248: queued_message_payload_construct
      uVar2: payload
    notes: >-
      72-byte factory (0x08103464..0x081034ac, eighteen ARM
      instructions) with 56 bl call sites, no tail branches. It builds
      the standard queued-message envelope in caller-provided arena
      storage: FUN_08266a48(storage, 0x16), installs the derived
      vtable literal DAT_081034ac = 0x08980744, allocates exactly 0x10
      bytes through the already-ported tag-2 operator_new, constructs
      the nested payload with the caller's message_code/bytes/
      byte_count unchanged and in r1/r2/r3 order, stores the returned
      payload pointer at +0x08, and returns the base constructor's
      result. FUN_08266a48 is a base constructor, not an allocator:
      its five-instruction body runs framework_object_construct,
      installs its own vtable, and stores the 0x16 kind/tag at +0x04.
      The outer constructor then overwrites that vtable for this
      derived envelope. All inspected callers allocate the 12-byte
      outer storage from the current message arena
      (FUN_08103400 + FUN_0826c0d8(..., 0xc)) then hand this result
      directly to FUN_08110fdc, which posts it to a task queue.
      The 16-byte nested payload's constructor 0x081b9248 stores the
      message code and byte count and, only when byte_count != 0,
      retains a 0x27-tagged owned copy of bytes; its dtor
      0x081b92ac releases that copy and the outer dtor 0x0810351c
      virtual-dispatches that payload dtor. The base constructor
      0x08266a48 is now ported (app/message_kind,
      message_kind_construct) and is the wired default for the
      QUEUED_MESSAGE_OPS.construct_base slot; only the payload
      constructor 0x081b9248 remains behind the seam
      (firmware-address default on target, panicking default on
      host); operator_new is direct.
      Host tests pin the 0x16 kind argument, base-result adoption,
      derived vtable store, direct operator_new(0x10), full nested
      argument routing, returned payload store, and the zero-length
      NULL-payload forwarding path. match.py structural comparison
      retains the base ctor/vtable/allocate/payload ctor/store/return
      sequence; expected differences are the two seam loads/blx calls
      and LLVM's frame/register allocation.

  - address: 0x081034b0
    ghidra: FUN_081034b0
    name: queued_message_construct_word
    size: 68
    sig: "QueuedMessage *queued_message_construct_word(QueuedMessage *storage, u32 message_code, u32 payload_word)"
    module: app/queued_message
    status: ported
    renames:
      param_1/r0: storage
      param_2/r5: message_code
      param_3/uStack_10: payload_word
      puVar1/r4: message
      DAT_081034f0: QUEUED_MESSAGE_VTABLE
      FUN_08266a48: message_kind_construct
      FUN_082aadd4: operator_new
      FUN_081b9248: queued_message_payload_construct
    notes: >-
      True raw extent is 68 bytes: sixteen ARM instructions at
      0x081034b0..0x081034ef plus the derived queued-message vtable literal
      0x08980744 at 0x081034f0; the next function begins with cmp r0,#0 at
      0x081034f4. Ghidra's 64-byte size omits that literal pool word. A
      binary-wide decode of every ARM B/BL word found 20 incoming calls: all
      are unconditional bl, with no predicated bl or direct tail b. This
      sibling of queued_message_construct builds the same 12-byte
      kind-0x16 envelope in caller-provided arena storage, installs the
      derived vtable, allocates 16 bytes with operator_new(0x10), and calls
      the nested payload constructor with {message_code, &payload_word, 4};
      it stores that constructor's result at +0x08 and returns the base
      constructor result. message_kind_construct (app/message_kind) and
      operator_new (heap/veneers) are ported directly; the payload constructor
      0x081b9248 remains behind the existing QUEUED_MESSAGE_OPS slot, whose
      target default calls the firmware address and whose host default panics.
      Host coverage records the full high-bit word, non-NULL four-byte payload
      pointer, construction order, return adoption, vtable/payload stores, and
      tag-2 allocation.

  - address: 0x0811050c
    ghidra: FUN_0811050c
    name: vtable_two_pair_base_construct
    size: 40
    sig: "void *vtable_two_pair_base_construct(void *this, const void *src_pair_at_4, const void *src_pair_at_12)"
    module: cxx/vtable_two_pair_base_construct
    status: ported
    renames:
      param_1/r0: this (aligned writable 20-byte base object, returned)
      param_2/r1: src_pair_at_4 (8-byte pair source copied to this +0x04)
      param_3/r2: src_pair_at_12 (8-byte pair source copied to this +0x0c)
      FUN_081bb6a4: pair_copy (shared two-word copy leaf, inlined)
    notes: >-
      Raw extent is exactly 40 bytes, 0x0811050c..0x08110534, plus the
      literal-pool vtable word 0x08981718 at 0x08110534; the next separately
      linked function starts at 0x08110538. Ghidra's 40-byte size matches.
      Decoding every ARM B/BL word in osos.dec found exactly 14 inbound
      calls, all unconditional bl (0x08103e14, 0x0812bdd8, 0x0812e8ec,
      0x0812ea1c, 0x081558f0, 0x0815f1d0, 0x0815fbdc, 0x0816de28,
      0x081baaec, 0x081d0d9c, 0x081e8794, 0x081fadcc, 0x08203274,
      0x0820443c), with zero predicated bl, zero tail b, and zero aligned
      data-word references. The unguarded leaf installs vtable 0x08981718 at
      this+0, then runs the shared two-word copy at 0x081bb6a4 twice:
      (this+4, r1) and (this+12, r2), returning this in r0. All 14 callers
      are derived-class constructors (push {r4,lr}; bl; ldr derived_vtable;
      str [r0]) that never set r1/r2, so the pair sources are stale
      grandcaller registers and the pair fields transiently hold
      leftover-pointer data until later initialization. Ghidra's C drops all
      three arguments (FUN_0811050c()). Anomaly documented, not resolved:
      the vtable region 0x08981718 decodes as neither ARM/Thumb code nor a
      code-pointer table in osos.dec, yet consumer 0x08103dc8 dispatches
      through [vtable+8], so the region is presumably rewritten at runtime.
      Deliberate deviation: the 0x081bb6a4 pair copy is inlined with
      volatile ordered loads/stores per project precedent, preserving the
      vtable-store-before-first-pair-load ordering (a self/overlapping
      source observes the vtable store). Host tests (3): field layout and
      this return with neighbour preservation, overlapping first-pair source
      observing the vtable store, second-pair source aliasing the first
      pair's destination. match.py --lib worktree archive (exit 1, diff
      expected): same vtable store and four ordered load/store pairs at
      +0x04/+0x08/+0x0c/+0x10; deltas are the crate fp frame and the inlined
      pair copies replacing the two bl 0x081bb6a4.

  - address: 0x08110fdc
    ghidra: FUN_08110fdc
    name: queued_message_post
    size: 164
    sig: "u32 queued_message_post(QueuedMessage *message, MessageTarget *target, u32 no_wait, usize reply_queue, u32 flags)"
    module: app/queued_message
    status: ported
    renames:
      param_1/r4: message
      param_2/r5: target
      param_3/r8: no_wait
      param_4/r0: reply_queue
      param_5/r9: flags
      iVar2/r7: target_queue
      r6: posted
      DAT_0811107c: TASK_MESSAGE_TAG_MSGS
      FUN_080cb828: current_task_ctx_block
      FUN_0812bf70: task_message_post_sync
      FUN_0812c628: task_message_post_async (unported mirror; wait = 0)
    notes: >-
      The poster for the envelope queued_message_construct @ 0x08103464
      builds - the "hands it directly to FUN_08110fdc" that entry already
      names. True extent 164 bytes, 0x08110fdc..0x0811107f: 160 bytes of
      code plus the pool word @ 0x0811107c holding the FourCC tag
      0x4d736753 = 'MsgS'; the next function is the bare `bx lr` @
      0x08111080. 45 call sites - 44 unconditional bl and exactly one
      blne (@ 0x0814b080), no plain b - binary-verified by decoding every
      B/BL word in work/firmware/osos.dec. The single predicated site is
      the CALLER's guard; the callee's own NULL guard covers only
      `message`, and the target chain is dereferenced unguarded before
      any test. Algorithm - read the destination queue through
      target->+0x10->+0x0c->+0x1c (object, owner record, task context
      block, queue pool, the same +0x1c current_task_ctx_block's callers
      use); default reply_queue to the current task's own queue when the
      argument is 0, and bail if either queue is 0; build a tagged stack
      message {'MsgS', -, -, target, message} and post it - fire-and-
      forget through 0x0812c628 when no_wait != 0, else synchronously
      through the ported 0x0812bf70; return the helper's result when
      non-zero, otherwise release the envelope through its vtable +0x04
      slot (the destructor 0x0810351c occupies) and return 0. So the
      function takes ownership of the envelope: it is freed on every
      failure edge rather than leaked. Near-twin @ 0x081251cc is the
      same shape one level up, stamping 'MsgB' (0x4d736742) and adding a
      byte field at +0x14. Deviations - (1) the outgoing message is
      modelled as the full 28-byte cell the post helper's copy
      FUN_0827210c moves (seven words, three single loads plus one
      ldm/stm of four), whereas the original reserves only 20 bytes
      (`sub sp, sp, #0x14`) and lets that fixed copy read two words of
      its own saved-register area; the port makes every byte the copy
      reads defined storage. (2) 0x0812c628 is not ported - its whole
      20-byte body is the same helper call with the wait flag forced to
      0 - so the no_wait branch makes that call through the existing
      kernel/task_message TASK_MESSAGE_OPS.post_message slot rather than
      re-stubbing a function one immediate away from its ported sibling.
      (3) current_task_ctx_block may report NULL; the original loads
      +0x1c off it unconditionally, the port reads a NULL context as
      reply queue 0, which lands on the same cleanup edge the original's
      `cmp r0,#0; beq` takes. (4) Ghidra names five parameters but drops
      all four arguments from the FUN_0812c628 call. Host tests (7) pin
      the 'MsgS' tag and word placement, the wait flag in both branches,
      the ownership transfer on success, release-exactly-once on a
      rejected post and on a queue-less target, the NULL-envelope guard,
      the full owner/context chain, and the 28-byte cell size; the
      fixture lives in a sub-4GiB slab because the envelope's vtable
      word is a u32. match.py structural - the branch shape, the three
      chained loads, the three message stores and the predicated
      release-dispatch tail all survive; expected differences are the
      seam load/blx for the async branch and LLVM's register allocation.

  - address: 0x08103400
    ghidra: FUN_08103400
    name: message_arena_pool
    size: 100
    sig: "FixedBlockPool *message_arena_pool(void)"
    module: app/message_arena
    status: ported
    renames:
      FUN_0826c134: fixed_block_pool_construct (the FixedBlockPool ctor,
        MESSAGE_ARENA_CTOR seam - not yet ported)
      FUN_082ab31c: cxa_guard_acquire (ported, runtime/cxa_guard.rs)
      FUN_082ab338: cxa_guard_release (ported, runtime/cxa_guard.rs)
      FUN_082ab1c8: cxa_atexit (ported, runtime/shutdown_chain.rs)
    notes: >-
      The queued-message arena's singleton pool accessor - a textbook
      ADS C++ function-local static over a fixed .bss object, the
      node_list_get / media_command_facade_get idiom. EXTENT CORRECTED:
      Ghidra's 80 bytes is the code only; the five literal-pool words @
      0x08103450..0x08103460 belong to the function and the next
      function opens at 0x08103464 (queued_message_construct's push),
      so the true extent is 100 bytes, 0x08103400..0x08103464. 43 bl
      call sites, 0 predicated, 0 tail b, no DATA word references -
      verified by decoding every B/BL word and scanning for the address
      in osos.dec; never dispatched virtually.
      Shape: guard word 0x089ca840 (fast path loads it as [0x089ca7fc
      + 0x44] via pool word @ 0x08103450, slow path via the 0x089ca840
      literal @ 0x08103454 - same word); tst #1 fast path;
      cxa_guard_acquire; FixedBlockPool ctor 0x0826c134 on the object
      0x08a1b1d4 (pool word @ 0x08103458) with block_size 12 /
      block_count 128; cxa_atexit(ctor's return, dtor 0x082612d0,
      __dso_handle 0x089ca09c); cxa_guard_release; reload the OBJECT
      literal - never the ctor's return - and return it. 128 blocks of
      12 bytes is exactly the 12-byte envelope queued_message_construct
      @ 0x08103464 builds (vtable, kind, payload).
      The registered destructor 0x082612d0 is NOT a function entry: it
      sits 0x140 bytes inside FUN_08261190 (880 bytes per
      functions.csv) on a `ldr r0, [r6, r0, lsl #2]` - an interior
      load through its caller's registers, so the registration could
      never fire (and retailOS never runs exit's chain anyway). Same
      situation as node_list_get's 0x0810516c and
      media_command_facade_get's 0x0817f190; modeled as a no-op.
      The guard pair and cxa_atexit are ported and called directly.
      The ctor is not, so it rides the MESSAGE_ARENA_CTOR seam:
      firmware-address default (0x0826c134) on target, panicking
      default on host. Deviations - the guard word and the pool object
      are crate statics rather than the .bss words @ 0x089ca840 /
      0x08a1b1d4 (the block_mgr.rs deviation), both zero-init = the
      exact pre-init state; on target FixedBlockPool is exactly the
      original's 0x1c bytes. Host tests (7 for the accessor) pin the
      construct/register/publish first call with the exact (12, 128)
      geometry, the bit-0 fast path and no-reconstruction, the
      bit-0-set short-circuit, the whole-word acquire refusal of a
      bit-0-clear nonzero guard, the reloaded-object-literal return vs
      the ctor's return going to the registration, and the real
      shutdown-chain registration running the no-op destructor.
      match.py structural: the guard-test / acquire / ctor(12, 128) /
      register / return-object sequence survives; expected differences
      are the seam's literal load + blx for the ctor, the ported
      callees inlined by LLVM (the two-level guard test merges into one
      whole-word test - outcome-identical in every guard state - and
      the empty cxa_guard_release call deleted; the node_list_get
      precedent) and LLVM's register allocation.

  - address: 0x08103550
    ghidra: FUN_08103550
    name: message_arena_alloc
    size: 24
    sig: "void *message_arena_alloc(size_t size)"
    module: app/message_arena
    vars: {param_1: size}
    status: ported
    renames:
      param_1/r0/r4: size
      FUN_08103400: message_arena_pool (the accessor, ported in the same
        module)
      FUN_0826c0d8: fixed_block_pool_alloc (already ported,
        heap/fixed_block_pool.rs)
    notes: >-
      The queued-message class's class-specific operator new. Six
      instructions, extent 0x08103550..0x08103568 confirmed from the raw
      words (the preceding function's literal pool ends at 0x0810354c;
      0x08103568 starts the next one with `cmp r1,#0x8300`), no trailing
      literal - the tail branch is PC-relative. push {r4,lr};
      mov r4,r0; bl 0x08103400; mov r1,r4; pop {r4,lr}; b 0x0826c0d8 -
      literally `void *operator new(size_t n) { return alloc(pool(), n); }`,
      the thin call site fixed_block_pool_alloc's own notes predicted.
      48 bl call sites, 0 tail branches, binary-scanned by decoding every
      B/BL word in osos.dec. No DATA word holds the address, so it is
      never dispatched virtually.
      Which arena - the accessor FUN_08103400 @ 0x08103400 (100 bytes,
      0x08103400..0x08103464; the next word is queued_message_construct's
      push) is a textbook ADS C++ function-local static: guard word
      0x089ca840 (loaded as 0x089ca7fc + 0x44), tst #1 fast path,
      __cxa_guard_acquire 0x082ab31c, FixedBlockPool ctor 0x0826c134 on
      0x08a1b1d4 with block_size 12 / block_count 128, __cxa_atexit
      0x082ab1c8 with (pool, dtor 0x082612d0, __dso_handle 0x089ca09c),
      __cxa_guard_release 0x082ab338, return 0x08a1b1d4. 128 blocks of
      12 bytes is exactly the 12-byte envelope queued_message_construct
      @ 0x08103464 builds (vtable, kind, payload). Every one of the 48
      call sites loads the size as `mov r0,#12` - verified by decoding
      the immediate at all 48, not sampled - so the pool's exact-size
      test always matches here and the global operator_new fallback is
      unreachable through this entry point. The parameter is still a
      real parameter and the port forwards it verbatim.
      Deviations - the accessor is now ported (message_arena_pool @
      0x08103400, same module) and is called directly; the
      MESSAGE_ARENA_POOL slot it rode while unported is gone.
      fixed_block_pool_alloc is called directly, no seam. Host tests (4)
      cover the stock 12-byte pop and relink, a non-12 size reaching the
      pool's size test (proving the argument is not folded to a
      constant), successive allocations walking the one singleton's
      free list, and draining the arena to exhaustion in free-list
      order; the host fixture widens the block stride to the host
      pointer alignment (FreeBlock.next is 8 bytes here, 4 on target)
      while keeping the declared block_size at the original's 12 - the
      allocator only follows next, never a stride. match.py (--size 24):
      identical skeleton (mov r4,r0 / accessor / mov r1,r4 / pop / tail
      b); the diffs are the accessor's direct bl and LLVM's fp frame.

  - address: 0x082669e4
    ghidra: FUN_082669e4
    name: message_kind_arena_pool
    size: 100
    sig: "FixedBlockPool *message_kind_arena_pool(void)"
    module: app/message_kind_arena
    status: ported
    renames:
      DAT_08266a34: guard base 0x089ca7fc (fast path reads [+0x38] = the
        guard word 0x089ca834)
      DAT_08266a38: MESSAGE_KIND_ARENA_GUARD (0x089ca834)
      DAT_08266a3c: MESSAGE_KIND_ARENA (the pool object, 0x08a1b180)
      DAT_08266a40: DSO_HANDLE (__dso_handle, 0x089ca09c)
      DAT_08266a44: the registered "destructor" 0x082612d0 (not a
        function entry - see notes)
      FUN_0826c134: fixed_block_pool_construct (the FixedBlockPool ctor,
        MESSAGE_KIND_ARENA_CTOR seam - not yet ported)
      FUN_082ab31c: cxa_guard_acquire (ported, runtime/cxa_guard.rs)
      FUN_082ab338: cxa_guard_release (ported, runtime/cxa_guard.rs)
      FUN_082ab1c8: cxa_atexit (ported, runtime/shutdown_chain.rs)
    notes: >-
      The MessageKind arena's singleton pool accessor - a textbook ADS
      C++ function-local static over a fixed .bss object, the exact
      shape of its message_arena_pool sibling @ 0x08103400 (Ghidra's C
      in decomp/c/025/082669e4_FUN_082669e4.c matches the raw ARM
      instruction for instruction). EXTENT CORRECTED: Ghidra's 80
      bytes is the code only; the five literal-pool words @
      0x08266a34..0x08266a48 belong to the function and the next
      function opens at 0x08266a48 (message_kind_construct's push,
      already ported), so the true extent is 100 bytes,
      0x082669e4..0x08266a48. 37 bl call sites, 0 predicated, 0 tail
      b, no DATA word references - verified by decoding every B/BL
      word and scanning for the address in osos.dec; never dispatched
      virtually.
      Shape: guard word 0x089ca834 (fast path loads it as [0x089ca7fc
      + 0x38] via pool word @ 0x08266a34, slow path via the 0x089ca834
      literal @ 0x08266a38 - same word); tst #1 fast path;
      cxa_guard_acquire; FixedBlockPool ctor 0x0826c134 on the object
      0x08a1b180 (pool word @ 0x08266a3c) with block_size 8 /
      block_count 256; cxa_atexit(ctor's return, dtor 0x082612d0,
      __dso_handle 0x089ca09c); cxa_guard_release; reload the OBJECT
      literal - never the ctor's return - and return it. 256 blocks of
      8 bytes is exactly the 8-byte object message_kind_construct @
      0x08266a48 builds (vtable word + the +0x04 kind tag). Its
      class-specific operator new @ 0x08266aa4 is now ported as
      message_kind_arena_alloc; the neighboring operator delete
      @ 0x08266a84 remains unported.
      Who calls it: all 37 sites feed the result to
      fixed_block_pool_alloc @ 0x0826c0d8 (ported) or its free
      counterpart. Decoding the r1 immediate at every site: exactly 8
      sites request 8 bytes and can hit the pool; the rest request
      12/16/20/24/28/32 or a variable size and always take the
      allocator's exact-equality fallback to the global operator_new
      (the `cmp r0, r1; beq` @ 0x0826c0e8, binary-verified) - sibling
      message classes sharing the accessor, where the mismatch costs
      only a redundant pool fetch.
      The registered destructor 0x082612d0 is NOT a function entry: it
      sits 0x140 bytes inside FUN_08261190 (880 bytes per
      functions.csv) on a `ldr r0, [r6, r0, lsl #2]` - an interior
      load through its caller's registers, so the registration could
      never fire (and retailOS never runs exit's chain anyway). The
      same registration every FixedBlockPool static carries
      (message_arena_pool @ 0x08103400); modeled as a no-op.
      The guard pair and cxa_atexit are ported and called directly.
      The ctor is not, so it rides the MESSAGE_KIND_ARENA_CTOR seam:
      firmware-address default (0x0826c134) on target, panicking
      default on host. Deviations - the guard word and the pool object
      are crate statics rather than the .bss words @ 0x089ca834 /
      0x08a1b180 (the block_mgr.rs deviation), both zero-init = the
      exact pre-init state; on target FixedBlockPool is exactly the
      original's 0x1c bytes. Host tests (7) pin the
      construct/register/publish first call with the exact (8, 256)
      geometry, the bit-0 fast path and no-reconstruction, the
      bit-0-set short-circuit, the whole-word acquire refusal of a
      bit-0-clear nonzero guard, the reloaded-object-literal return vs
      the ctor's return going to the registration, the real

      shutdown-chain registration running the no-op destructor, and
      the stock geometry/dso literals. match.py structural: the
      guard-test / acquire / ctor(8, 256) / register / return-object
      sequence survives; expected differences are the seam's literal
      load + blx for the ctor, the ported callees inlined by LLVM (the
      two-level guard test merges into one whole-word test -
      outcome-identical in every guard state - and the empty
      cxa_guard_release call deleted; the node_list_get precedent) and
      LLVM's register allocation.
  - address: 0x0811a4d0
    ghidra: FUN_0811a4d0
    name: message_handler_unhandled
    size: 8
    sig: "u32 message_handler_unhandled(void *receiver, MessageKind *message)"
    module: app/message_kind
    status: ported
    renames:
      param_1/r0: receiver
      param_2/r1: message
    notes: >-
      Exact 8-byte leaf body: mov r0,#0; bx lr. The next entry begins
      at 0x0811a4d8 with mov r0,#1, so no Ghidra extent was lost. A
      binary scan of every ARM B/BL word in osos.dec found 11
      unconditional plain bl callers, no predicated bl callers, and
      one tail b at 0x082988a4; no word in the image equals
      0x0811a4d0, so it is never vtable-dispatched. Callers provide a
      receiver in r0 and a MessageKind-derived event/message in r1,
      then interpret zero as unhandled or declined
      (0x081d1a70/0x081d24d8/0x081d251c) or discard it in the default
      callback teardown shape. The port preserves this false result
      and intentionally does not inspect either opaque argument.

  - address: 0x0811a890
    ghidra: FUN_0811a890
    name: stop_frequency_change_and_start_tuning_timer
    size: 36
    sig: "void stop_frequency_change_and_start_tuning_timer(u8 *controller)"
    module: app/tuning_timer
    status: ported
    renames:
      param_1/r0: controller
      param_1_plus_0xb0: tuning timer
      param_1_plus_0xb4: frequency-change pending byte
      param_1_plus_0xb8: frequency-change timer
      FUN_0812c6b0: timer_stop
      FUN_0811ac74: stop tuning timer and set its 4000 ms delay
    notes: >-
      Exact 36-byte extent, binary-decoded as push {r4,lr}; mov r4,r0;
      ldr r0,[r0,#0xb8]; bl timer_stop; mov r0,#0; strb r0,[r4,#0xb4];
      mov r0,r4; pop {r4,lr}; b 0x0811ac74. The next function opens at
      0x0811a8b4. Decoding every ARM B/BL word in osos.dec finds ten
      direct callers: ten unconditional bl, zero predicated bl, and zero
      tail b. It stops the frequency-change timer, clears the pending byte,
      then tail-branches to the 40-byte helper at 0x0811ac74. Raw decoding
      verifies that helper stops the tuning timer at controller+0xb0 and
      starts it after 4000 ms. The Rust port calls the already-ported
      timer_start_after directly instead of adding a seam for that unported
      wrapper; the timer effects and order are identical. Host test uses a
      unique low-address raw-u32 fixture and verifies both timer stops, the
      pending-byte clear, and the 4000 ms tuning delay.

  - address: 0x08266a48
    ghidra: FUN_08266a48
    name: message_kind_construct
    size: 24
    sig: "MessageKind *message_kind_construct(MessageKind *storage, u32 kind)"
    module: app/message_kind
    status: ported
    renames:
      param_1/r0: storage
      param_2/r1/r4: kind
      puVar1: this
      DAT_08266a60: MESSAGE_KIND_VTABLE
      FUN_08275bb8: framework_object_construct
    notes: >-
      24 bytes of code (0x08266a48..0x08266a60) plus the 4-byte
      vtable literal 0x089a3788 @ 0x08266a60, so 28 bytes of true
      extent; 45 bl call sites, 0 b, in decomp/osos.asm. Base
      constructor of the framework's kind-tagged message class, the
      class every task-queue message envelope descends from: push
      {r4,lr}; mov r4,r1; bl 0x08275bb8 (the already-ported
      framework_object_construct root ctor); ldr r1,[pc,#4]; stmia
      r0,{r1,r4}; pop {r4,pc} — overwrite the root vtable with the
      base message vtable and store the kind tag at +0x04, then
      return the root ctor's r0 for the caller's own derived-vtable
      store. Not an allocator: all 45 call sites pass storage they
      already own (FUN_0826c0d8 arena blocks, FUN_08266aa4(8)
      operator_new blocks, or stack slots) and immediately plant
      their own vtable over this one, keeping only the +0x04 kind
      store. Observed kind immediates: 0 (x4), 1, 2, 3, 4, 0x16
      (queued_message), 0x17, 0x22, 0x28, 0x20003, plus DAT-loaded
      tags. The vtable @ 0x089a3788 points its live slots at
      0x0828ef34 and the 0x08102f60..0x08103348 cluster beside the
      queued-message machinery, corroborating the message-base
      reading. The port calls framework_object_construct directly
      (no seam — the root ctor is ported in cxx/observable_array);
      its root-vtable store is dead exactly as in the original.
      queued_message.rs's QUEUED_MESSAGE_OPS.construct_base default
      is now this port on both target and host. Deviations - none
      beyond emitting the single stmia as two ordered volatile
      stores (the observable_array_construct idiom). match.py
      structural - identical push/mov/bl/ldr-literal/store/store/pop
      shape with the literal after the body; the diff is the crate's
      fp frame and the stm split.

  - address: 0x08266a80
    ghidra: thunk_FUN_08275bc8
    name: message_kind_destruct
    size: 4
    sig: "MessageKind *message_kind_destruct(MessageKind *this)"
    module: app/message_kind
    status: ported
    renames:
      param_1/r0: this
      FUN_08275bc8: framework_object_destruct (root dtor, a bare bx lr;
        ported in cxx/observable_array)
    notes: >-
      One instruction, b 0x08275bc8 - a true 4-byte tail-branch veneer
      (Ghidra's size is right), NOT a mis-sized body and NOT an ldr
      pc,[pc,#-4] veneer. It closes the message-kind cluster: ctor
      0x08266a48 (ported), NULL-guarded deleting dtor 0x08266a64
      (bl 0x08275bc8, then operator delete of 8 bytes via 0x08266a84),
      this non-deleting dtor, then the operator-delete veneer at
      0x08266a84. The target 0x08275bc8 is binary-verified a bare bx
      lr - the framework root destructor - so the net effect is the
      identity on r0 with no memory touched. Binary scan of every
      B/BL word in osos.dec: 17 plain bl sites, 0 predicated bl, 1
      tail b @ 0x0811ef8c (a derived dtor chaining into its base);
      Ghidra's 17 is the bl count only. 0x08266a80 appears in NO
      data word, so it is never vtable-dispatched - every caller
      binds it statically (its class vtable 0x089a3788's live slots
      point elsewhere, binary-verified). Two bl shapes: 8 sites are
      the NULL-guarded heap destroy push {r4,lr}; popeq {r4,pc}; bl;
      mov r4,r0 whose captured r0 feeds the operator-delete path
      (e.g. 0x0816f194: mov r4,r0; bl 0x082669e4; mov r1,r4) - the
      r0 pass-through is a real contract, so the port returns this
      despite Ghidra's void prototype; 5 sites are end-of-scope
      teardown of a stack message (add r0,sp,#N / mov r0,sp), the
      RAII scope teardown node_list_enqueue records as its
      thunk_FUN_08275bc8 seam. Port models the b+ bx lr pair as the
      plain return-this identity with no call, the same way the
      observable-array destructors model the same root dtor, and
      carries its own link_section against LLVM identical-code
      folding with the other empty-dtor exports (symbol verified
      present in the release archive). Host tests pin the r0
      pass-through (NULL/unaligned/extreme pointers) and the
      no-store behaviour over a poison-guarded 8-byte object.
      match.py structural - the original is the single b; the port
      is push {fp,lr}/mov fp,sp/pop {fp,pc}, LLVM's frame on the
      leaf identity: same net effect (r0 unchanged, no memory
      touched), expected non-match for a modeled-away veneer.

  - address: 0x08266aa4
    ghidra: FUN_08266aa4
    name: message_kind_arena_alloc
    size: 20
    sig: "void *message_kind_arena_alloc(size_t size)"
    module: app/message_kind_arena
    vars: {param_1: size}
    status: ported
    renames:
      param_1/r0/r4: size
      FUN_082669e4: message_kind_arena_pool (ported, app/message_kind_arena.rs)
      FUN_0826c0d8: fixed_block_pool_alloc (ported, heap/fixed_block_pool.rs)
    notes: >-
      The MessageKind class's class-specific operator new. Raw ARM
      extent is exactly 20 bytes, 0x08266aa4..0x08266abc: the preceding
      message_kind_construct ends with its vtable literal at
      0x08266aa0, and the next function begins with push {r0-r3} at
      0x08266abc. The six instructions are push {r4,lr}; mov r4,r0;
      bl 0x082669e4; mov r1,r4; pop {r4,lr}; b 0x0826c0d8: literally
      void *operator new(size_t size) { return
      fixed_block_pool_alloc(message_kind_arena_pool(), size); }.
      Binary-decoding every ARM B/BL word in osos.dec found 20
      unconditional bl call sites, zero predicated calls, and zero tail
      branches. No aligned DATA word equals 0x08266aa4, so it is not
      virtually dispatched. Both callees are already ported and called
      directly; the stock tail branch becomes a Rust call, which LLVM
      may tail-call. Host tests cover the stock 8-byte free-list pop,
      a 20-byte pool proving size forwards unchanged, and successive
      allocations walking the singleton free-list. match.py structural
      review is expected to retain the accessor / saved-size /
      fixed-block allocation sequence; LLVM frame and tail-call
      selection may differ.

  - address: 0x08103584
    ghidra: FUN_08103584
    name: instance_of_class_8300
    size: 24
    sig: "void *instance_of_class_8300(void)"
    module: app/registry
    status: ported
    notes: >-
      6 bl call sites. Same lookup-then-cast accessor as the ported
      0x081883fc for class id 0x8300 - `push; load id; bl 0x081d2184;
      pop; load the SAME id into r1; b 0x08275b9c`. Class identified as
      TPhotosSettingsSlideshowPlaylistCntlr from the literal its
      constructor hands to the class-name factory @ 0x0820b230 next to
      its `bl 0x081d23f8`; the port names it
      CLASS_ID_PHOTOS_SLIDESHOW_PLAYLIST_CNTLR. Seven siblings remain
      identified, all the same shape - 0x081353e8 (0x8f00 TCEQSetting,
      24), 0x0815e1ac (0x5680 TCSportTimer, 28), 0x0815ff34 (0x8700, 28),
      0x0827f218 (0x5780, 28), 0x08284e2c (0x6180, 28), 0x08289690
      (0x3280, 24), 0x0828ae68 (0x4180 TCNotesDispatcher, 28);
      app/registry.rs's `instance_of_class` body covers each in one
      line.
    renames: {}

  - address: 0x08103b88
    ghidra: FUN_08103b88
    name: instance_of_class_7600
    size: 24
    sig: "void *instance_of_class_7600(void)"
    module: app/registry
    status: ported
    notes: >-
      1 bl call site (binary-scanned). Same lookup-then-cast accessor as
      the ported 0x081883fc for class id 0x7600 - `push; load id;
      bl 0x081d2184; pop; load the SAME id into r1; b 0x08275b9c`.
      Class identified as TSearchCntlr from the literal its constructor
      hands to the class-name factory @ 0x0820b230 next to its
      `bl 0x081d23f8` (registers @ 0x08103d2c); the port names it
      CLASS_ID_SEARCH_CNTLR.
    renames: {}

  - address: 0x0812f0a4
    ghidra: FUN_0812f0a4
    name: instance_of_class_4e00
    size: 24
    sig: "void *instance_of_class_4e00(void)"
    module: app/registry
    status: ported
    notes: >-
      7 bl call sites (binary-scanned over osos.dec). Same
      lookup-then-cast accessor as the ported 0x081883fc for class id
      0x4e00 - `push; load id; bl 0x081d2184; pop; load the SAME id into
      r1; b 0x08275b9c`. Class identified as TCUnsupported from the
      literal "TCUnsupported" @ 0x0812f094, handed to the class-name
      factory @ 0x0820b230 by the naming block @ 0x0812f038 that sits
      immediately before this accessor in the image; the port names it
      CLASS_ID_UNSUPPORTED.
    renames: {}

  - address: 0x081353e8
    ghidra: FUN_081353e8
    name: instance_of_class_8f00
    size: 24
    sig: "void *instance_of_class_8f00(void)"
    module: app/registry
    status: ported
    notes: >-
      2 bl call sites (binary-scanned over osos.dec: @ 0x080ff318 and
      0x080ff38c). Same lookup-then-cast accessor as the ported
      0x081883fc for class id 0x8f00 - `push; load id; bl 0x081d2184;
      pop; load the SAME id into r1; b 0x08275b9c`. Class identified as
      TCEQSetting: the literal "TCEQSetting" @ 0x08135574 is handed to
      the class-name factory by the static-init block @ 0x08135518
      (alongside "TCSpeakers"), and the constructor @ 0x081356b0
      registers its `this` under 0x8f00 @ 0x081356e4; the port names it
      CLASS_ID_EQ_SETTING. Six siblings remain identified, all the same
      shape - 0x0815e1ac (0x5680 TCSportTimer, 28), 0x0815ff34 (0x8700,
      28), 0x0827f218 (0x5780, 28), 0x08284e2c (0x6180, 28), 0x08289690
      (0x3280, 24), 0x0828ae68 (0x4180 TCNotesDispatcher, 28).
    renames: {}

  - address: 0x0815e1ac
    ghidra: FUN_0815e1ac
    name: instance_of_class_5680
    size: 28
    sig: "void *instance_of_class_5680(void)"
    module: app/registry
    status: ported
    notes: >-
      10 bl call sites (binary-scanned over osos.dec). Same
      lookup-then-cast accessor as the ported 0x081883fc for class id
      0x5680, but 28 bytes rather than 24: it keeps the id in r4 across
      the lookup (`stmdb sp!,{r4,lr}; ldr r4,=0x5680; mov r0,r4;
      bl 0x081d2184; mov r1,r4; ldmia sp!,{r4,lr}; b 0x08275b9c`)
      instead of re-loading it from the literal pool. Class identified
      as TCSportTimer: the literal "TCSportTimer" @ 0x0815ea3c is handed
      to the class-name factory @ 0x0820b230 in the same basic block
      where the constructor registers its `this` under 0x5680 @
      0x0815e978; the port names it CLASS_ID_SPORT_TIMER. Five siblings
      remain identified, all the same shape - 0x0815ff34 (0x8700, 28),
      0x0827f218 (0x5780, 28), 0x08284e2c (0x6180, 28), 0x08289690
      (0x3280, 24), 0x0828ae68 (0x4180 TCNotesDispatcher, 28).
    renames: {}

  - address: 0x0815ff34
    ghidra: FUN_0815ff34
    name: instance_of_class_8700
    size: 24
    sig: "void *instance_of_class_8700(void)"
    module: app/registry
    status: ported
    notes: >-
      14 bl call sites (binary-scanned over osos.dec, no tail b). Same
      lookup-then-cast accessor as the ported 0x081353e8 for class id
      0x8700 - `push {r4,lr}; mov r0,#0x8700; bl 0x081d2184; pop
      {r4,lr}; mov r1,#0x8700; b 0x08275b9c`. functions.csv's 24 bytes
      is correct: 6 instructions, next function @ 0x0815ff4c (the 28 in
      the earlier sibling notes was a mis-count). Class NOT identified:
      the constructor @ 0x0815ffc4 registers its `this` under 0x8700 @
      0x08160078 but never names it to the class-name factory @
      0x0820b230, so the port names the id CLASS_ID_8700 (the
      0x6000/0x6600 precedent). Four siblings remain identified, all
      the same shape - 0x0827f218 (0x5780, 28), 0x08284e2c (0x6180,
      0x08289690 (0x3280, 24), 0x0828ae68 (0x4180
      TCNotesDispatcher, 28).
    renames: {}

  - address: 0x08160000
    ghidra: FUN_08160000
    name: instance_8700_owned_object
    size: 16
    sig: "void *instance_8700_owned_object(void)"
    module: app/registry
    status: ported
    notes: >-
      31 bl call sites (binary-scanned over osos.dec, zero predicated
      forms - Ghidra's 31 is exact). `push {r4,lr}; bl 0x0815ff34; ldr
      r0,[r0,#0x2c]; pop {r4,pc}` - returns the pointer field at +0x2c
      of the class-0x8700 singleton resolved through the ported
      instance_of_class_8700. The class's constructor @ 0x08160024
      fills +0x2c with a fresh operator_new(0xe4) block run through the
      constructor @ 0x081a4f64 (second argument = the 0x8700 object;
      that ctor registers the finished sub-object under class id
      0x8780), so the returned pointer is the 0x8780 sub-object the
      singleton owns; callers hand it back to the 0x081a3xxx/0x081a4xxx
      method cluster. Neither class is named to the class-name factory,
      so the port claims only the offset and the ownership. No NULL
      guard in the original and none in the port: an unregistered
      0x8700 faults on the ldr. Size 16 confirmed - next function @
      0x08160010 (a lone `bx lr` empty destructor, then the +0x34 byte
      flag getter/setter pair @ 0x08160014/0x0816001c). match.py diff
      shows only the LLVM frame-pointer prologue (push {fp,lr}; mov
      fp,sp) vs ADS's push {r4,lr}; the bl and the ldr [r0,#0x2c] line
      up.
    renames: {}

  - address: 0x0827f218
    ghidra: FUN_0827f218
    name: instance_of_class_5780
    size: 28
    sig: "void *instance_of_class_5780(void)"
    module: app/registry
    status: ported
    notes: >-
      10 bl call sites (binary-scanned over osos.dec, no tail b). The
      28-byte sibling shape of the lookup-then-cast accessor - `push
      {r4,lr}; ldr r4,=0x5780 (literal @ 0x0827f234); mov r0,r4; bl
      0x081d2184; mov r1,r4; pop {r4,lr}; b 0x08275b9c`: keeps the
      class id in r4 across the registry lookup, then tail-branches to
      the instance_of_class cast. Class NOT identified (no class-name
      literal handed to the factory @ 0x0820b230 next to its
      registration), so the port names the id CLASS_ID_5780 (the
      0x6000/0x6600/0x8700 precedent). Three siblings remain
      identified, all the same shape - 0x08284e2c (0x6180, 28),
      0x08289690 (0x3280, 24), 0x0828ae68 (0x4180 TCNotesDispatcher,
      28).
    renames: {}

  - address: 0x08284e2c
    ghidra: FUN_08284e2c
    name: instance_of_class_6180
    size: 28
    sig: "void *instance_of_class_6180(void)"
    module: app/registry
    status: ported
    notes: >-
      15 bl call sites (binary-scanned over osos.dec, no tail b). The
      28-byte sibling shape of the lookup-then-cast accessor - `push
      {r4,lr}; ldr r4,=0x6180 (literal @ 0x08284e48); mov r0,r4; bl
      0x081d2184; mov r1,r4; pop {r4,lr}; b 0x08275b9c`: keeps the
      class id in r4 across the registry lookup, then tail-branches to
      the instance_of_class cast. Class NOT identified (no class-name
      literal handed to the factory @ 0x0820b230 next to its
      registration), so the port names the id CLASS_ID_6180 (the
      0x6000/0x6600/0x8700 precedent). Two siblings remain identified,
      all the same shape - 0x08289690 (0x3280, 24), 0x0828ae68 (0x4180
      TCNotesDispatcher, 28).
    renames: {}

  - address: 0x08289690
    ghidra: FUN_08289690
    name: instance_of_class_3280
    size: 24
    sig: "void *instance_of_class_3280(void)"
    module: app/registry
    status: ported
    notes: >-
      37 bl call sites (binary-scanned over osos.dec, no tail b, no
      predicated form). Same lookup-then-cast accessor as the ported
      0x081883fc for class id 0x3280 - `push {r4,lr}; mov r0,#0x3280;
      bl 0x081d2184; pop {r4,lr}; mov r1,#0x3280; b 0x08275b9c`.
      functions.csv's 24 bytes is correct: 6 instructions, next
      function @ 0x082896a8. Class identified as TMediaNowPlayingCntlr:
      the constructor @ 0x08289ce4 registers its `this` under 0x3280 @
      0x08289d18 and hands the literal "TMediaNowPlayingCntlr" @
      0x08289f1c to the class-name factory @ 0x0820b230 in the same
      body (@ 0x08289e2c, the first of seven naming calls - the other
      six name its nested state classes _Music/_Video/_Play/_Idle/
      _TVOutAsk/_TVOutCableConnect); the port names it
      CLASS_ID_MEDIA_NOW_PLAYING_CNTLR. Callers dereference the
      returned singleton's +0x28->+0x30 (e.g. FUN_08160254). One
      sibling remains identified, same shape - 0x0828ae68 (0x4180
      TCNotesDispatcher, 28).
    renames: {}

  - address: 0x08289a5c
    ghidra: FUN_08289a5c
    name: media_now_playing_controller_noop
    size: 4
    sig: "void *media_now_playing_controller_noop(void *controller, unsigned int mode)"
    module: app/media_now_playing_controller_noop
    status: ported
    renames:
      param_1: controller
      param_2: mode
    notes: >-
      Raw osos.dec verifies one instruction, `bx lr`, exactly at 0x08289a5c:
      the preceding function ends at 0x08289a58 and the next separately
      linked function starts at 0x08289a60. It is not an ADS literal veneer
      and has no trailing pool word. Decoding every ARM B/BL word in the
      image found 8 direct, unconditional plain bl callers
      (0x08101560/0x0810176c/0x08101980/0x08130a34/0x081314cc/0x081314dc/
      0x08131528/0x08131584), with no predicated bl and no b tail callers;
      there are zero data-word references, so it is statically bound rather
      than vtable-dispatched. Each caller takes r0 from the ported
      TMediaNowPlayingCntlr singleton accessor 0x08289690 and supplies 0 or
      1 in r1, but the body ignores both words except for passing r0 through.
      The port consequently exposes the verified no-op controller/mode
      callback rather than inventing a setter or destructor identity.

  - address: 0x0828a0a4
    ghidra: FUN_0828a0a4
    name: object_sequence_id_assign
    size: 24
    sig: "void object_sequence_id_assign(void *object)"
    module: ui/object_state
    status: ported
    renames: {}
    notes: >-
      21 direct unconditional call sites, counted by decoding every ARM B/BL
      word in osos.dec. The function increments the object-sequence id word at
      0x089cfeb0, stores the incremented value back to the global, and stamps
      the same value into the caller's word at +0x0c. Host builds model the
      firmware word with OBJECT_SEQUENCE_ID.

  - address: 0x0828ae68
    ghidra: FUN_0828ae68
    name: instance_of_class_4180
    size: 28
    sig: "void *instance_of_class_4180(void)"
    module: app/registry
    status: ported
    notes: >-
      23 bl call sites (binary-scanned over osos.dec, no tail b, no
      predicated form, no data-word references). The 28-byte sibling
      shape of the lookup-then-cast accessor - `push {r4,lr}; ldr
      r4,=0x4180 (literal @ 0x0828ae84); mov r0,r4; bl 0x081d2184;
      mov r1,r4; pop {r4,lr}; b 0x08275b9c`: keeps the class id in r4
      across the registry lookup, then tail-branches to the
      instance_of_class cast. Ghidra's 28 bytes covers the code
      exactly; the id sits in a trailing literal-pool word and the
      next function starts @ 0x0828ae88. Class identified as
      TCNotesDispatcher: the constructor @ 0x0828bf6c registers its
      `this` under 0x4180 (literal @ 0x0828c0e8, bl 0x081d23f8 @
      0x0828c05c) and hands the literal "TCNotesDispatcher" @
      0x0828c0f4 to the class-name factory @ 0x0820b230 in the same
      body (@ 0x0828c074, the first of several naming calls); the
      port names it CLASS_ID_NOTES_DISPATCHER. Last of the thirteen
      per-class accessors; no siblings remain identified.
    renames: {}

  - address: 0x0828a700
    ghidra: FUN_0828a700
    name: notes_dispatcher_prepare_and_read_status
    size: 44
    sig: "u32 notes_dispatcher_prepare_and_read_status(void)"
    module: app/notes_dispatcher_status
    status: ported
    renames:
      FUN_0828ae68: instance_of_class_4180
      FUN_081178cc: notes_dispatcher_request_start
      FUN_082724e8: status_byte_read
      "[notes_dispatcher + 0x4fc]": request
      "[request + 0x39]": request_active
    notes: >-
      Raw osos.dec extent is exactly 44 bytes: eleven ARM instructions from
      0x0828a700 through tail `b 0x082724e8` at 0x0828a728; the next
      separately linked function begins at 0x0828a72c. Decoding every ARM
      B/BL word finds exactly nine direct callers, all unconditional bl
      (0x0810c204, 0x0810c4e8, 0x081561a8, 0x08156304, 0x0821d810,
      0x0821dc54, 0x0821eb34, 0x08230694, 0x0828be80), with no predicated
      or tail-branch callers. It gets the already ported TCNotesDispatcher
      singleton, skips both field access and call when it is NULL, otherwise
      reads its request byte at +0x535 and calls 0x081178cc with the
      request subobject at +0x4fc only while that byte is zero. It then
      tail-calls 0x082724e8, which zero-extends the global byte at
      0x089cffac. The 0x081178cc callee is not ported: its stronger identity
      is deliberately not invented; target Rust calls its address directly
      while host tests pass a recorder to the shared gate body.

  - address: 0x0828b1d4
    ghidra: FUN_0828b1d4
    name: field_dc_as_class_4a80
    size: 12
    sig: "void *field_dc_as_class_4a80(void *owner)"
    module: app/registry
    status: ported
    renames:
      param_1/r0: owner
      "[owner + 0xdc]": class_4a80_object
      DAT_0828b1e0: CLASS_ID_4A80
      FUN_08275b9c: object_cast_to_class
    notes: >-
      Raw osos.dec is exactly three ARM instructions at 0x0828b1d4:
      `ldr r1,[pc,#4]` loads the literal 0x4a80 from 0x0828b1e0, `ldr
      r0,[r0,#0xdc]` loads the owner's target-width object field, then
      `b 0x08275b9c` tail-branches to the already ported
      object_cast_to_class. The next function begins at 0x0828b1e4; its
      reported 12-byte size excludes the trailing 4-byte literal-pool word.
      Decoding every aligned ARM B/BL word in osos.dec finds exactly nine
      direct, unconditional bl callers (0x0828a21c, 0x0828a3ac,
      0x0828a51c, 0x0828b000, 0x0828b12c, 0x0828b46c, 0x0828b664,
      0x0828b86c, 0x0828be08), zero predicated forms, and zero tail
      branches. It has no NULL guard: a NULL owner faults on the field load;
      a NULL field reaches object_cast_to_class, which returns NULL. Class
      0x4a80 is not otherwise identified, so the port names only the
      verified field and class id. Host test maps one fresh low-u32 slab,
      proves the +0xdc target field reaches its own vtable's checked-cast
      slot for 0x4a80, and proves a target accepting 0x4a00 returns NULL.

  - address: 0x081d2408
    ghidra: FUN_081d2408
    name: framework_root_post_message
    size: 44
    sig: "u32 framework_root_post_message(u32 message_code)"
    module: app/framework_root_message_post
    status: ported
    renames:
      param_1/r4: message_code
      FUN_08103400: message_arena_pool
      FUN_0826c0d8: fixed_block_pool_alloc
      FUN_08103464: queued_message_construct
      FUN_081d1ff4: framework_root_queued_message_post_wrapper
      FUN_08110fdc: queued_message_post
      DAT_081d2014: FRAMEWORK_ROOT_HOLDER_ADDRESS (0x089cc858)
    notes: >-
      True extent is 44 bytes, 0x081d2408..0x081d2433: eleven ARM
      instructions from push {r4,lr} through the tail b 0x081d1ff4.
      Ghidra's reported 76-byte extent is wrong; raw bytes at 0x081d2434
      begin the next function's push {r3,lr}. A binary-wide decode of every
      B/BL word in osos.dec finds 14 unconditional bl call sites, zero
      predicated bl, and eleven tail entries (nine unconditional b plus two
      bne); no aligned data word contains this address. Algorithm: allocate
      12 bytes from message_arena_pool through fixed_block_pool_alloc,
      build a queued_message with message_code, NULL bytes, and byte_count
      zero, then tail through the 28-byte sibling wrapper @ 0x081d1ff4 to
      queued_message_post(message, framework_root_instance(), 1, 0, 0).
      The wrapper's raw ldr r1,[pc,#24]; ldr r1,[r1,#4] reads the +4 slot of
      the framework-root holder @ 0x089cc858 before posting. The post result
      remains in r0 despite Ghidra's void declaration, so the port exposes
      u32. All dependencies are already ported and called directly. Host
      tests cover the 12-byte allocation, NULL/zero payload, exact
      fire-and-forget post arguments, returned status, zero message code,
      and a high-bit message code.

  - address: 0x081d23f8
    ghidra: FUN_081d23f8
    name: registry_register
    size: 16
    sig: "usize registry_register(void *instance, u32 class_id)"
    module: app/registry
    status: ported
    notes: >-
      45 bl call sites. `mov r2, r0; ldr r0, =0x08a79ca4; b 0x0810e4ac`
      - publishes `instance` in the global class registry under
      `class_id`. This is the call every framework singleton's ctor
      makes; scanning its 45 sites is what recovered the class-id space
      (0x2700, 0x2980, 0x3280, 0x3680, 0x4180, 0x4e00, 0x5680, 0x5780,
      0x5800, 0x5a00, 0x6000, 0x6180, 0x6280, 0x6380, 0x6600, 0x6d00,
      0x6e00, 0x6f80, 0x7600, 0x7680, 0x7800, 0x7a00, 0x7a80, 0x7f00,
      0x7f80, 0x8080, 0x8180, 0x8300, 0x8400, 0x8480, 0x8700, 0x8780,
      0x8900, 0x8a00, 0x8c00, 0x8c80, 0x8d00, 0x8e80, 0x8f00, 0x9280,
      0x9300, 0x9400 plus three set out of line).

  - address: 0x081d2184
    ghidra: FUN_081d2184
    name: registry_lookup_by_id
    size: 36
    sig: "void *registry_lookup_by_id(u32 class_id)"
    module: app/registry
    status: ported
    vars: {param_1: class_id}
    notes: >-
      62 call sites (60 bl + 2 tail b, binary-scanned; the earlier note
      said 61+1). Zeroes a stack slot, calls 0x0810e4c8(registry @
      0x08a79ca4, class_id, &slot) and returns the slot - the by-id
      instance lookup that app/context.rs names as the resolver for the
      pending target id. Deviation - the registry object is the crate
      static CLASS_REGISTRY, not the address 0x08a79ca4 (past the end
      of the decrypted image, pure runtime RAM), and it starts all-zero
      i.e. with a NULL vtable, so this is NOT HOOK-READY until the
      container ctor 0x0810e64c is ported. Ghidra's C for 0x0810e4ac
      and 0x0810e460 is FABRICATED (it invents vtable chains and
      inlines callees); the ports were written from the assembly.

  - address: 0x0810e40c
    ghidra: FUN_0810e40c
    name: registry_find
    size: 84
    sig: "i32 registry_find(Registry *registry, u32 class_id, RegistryEntry *out)"
    module: app/registry
    status: ported
    notes: >-
      3 bl call sites. Spills the key to the stack, dispatches registry
      vtable[+0x4c] `index_of(this, &key)` and, when that is not -1,
      dispatches vtable[+0x3c] `entry_at(this, index, out)` to copy the
      whole (class_id, instance) pair into the caller's buffer; returns
      the index. The `str r0, [sp]` after the second dispatch lands in
      the r1 spill slot and is dead (popped into a scratch register).
      Faithful - the key is passed by pointer to a stack COPY.

  - address: 0x0810e4c8
    ghidra: FUN_0810e4c8
    name: registry_lookup
    size: 40
    sig: "u32 registry_lookup(Registry *registry, u32 class_id, void **out)"
    module: app/registry
    status: ported
    notes: >-
      16 call sites (15 bl + 1 tail b). Calls 0x0810e40c with a stack
      pair, then `adds r0, r0, #1; movne r0, #1` turns the index into a
      found flag and, only when found, copies the pair's VALUE word
      ([sp, #4]) to *out. Returns the flag. Deviation - the port zeroes
      the stack pair the original leaves uninitialized; unobservable
      (it is only read on a hit, after entry_at wrote it).

  - address: 0x0810e4ac
    ghidra: FUN_0810e4ac
    name: registry_insert
    size: 28
    sig: "usize registry_insert(Registry *registry, u32 class_id, void *instance)"
    module: app/registry
    status: ported
    notes: >-
      10 call sites (8 bl + 2 tail b). `push {r2,r3,r4,lr}; stm sp,
      {r1,r2}` builds the container's (key, value) pair on the stack and
      dispatches registry vtable[+0x1c] `insert(this, &pair)`,
      forwarding whatever that returns. Ghidra's C for this function is
      FABRICATED; ported from the assembly.

  - address: 0x0810e4f0
    ghidra: FUN_0810e4f0
    name: registry_assign
    size: 60
    sig: "u32 registry_assign(Registry *registry, u32 class_id, void *instance)"
    module: app/registry
    status: ported
    notes: >-
      3 call sites (2 bl + 1 tail b). registry_find into a stack pair;
      on -1 returns 0 without touching anything; otherwise overwrites
      only the pair's value word (`str r4, [sp, #4]`, so the EXISTING
      key is what the container sees) and calls 0x0810e460 with the
      index and that pair, returning 1. Never adds an entry.

  - address: 0x0810e460
    ghidra: FUN_0810e460
    name: registry_assign_at
    size: 76
    sig: "void *registry_assign_at(Registry *registry, i32 index, const RegistryEntry *entry)"
    module: app/registry
    status: ported
    notes: >-
      1 bl call site (from 0x0810e4f0). Brackets one indexed write with
      the change-notification pair - 0x08134ff8(this, 0) to switch
      notifications off, registry vtable[+0x24] `assign_at(this, index,
      entry)`, 0x08135038(this, 1) to raise the changed byte, then a
      TAIL BRANCH to 0x08134ff8(this, 1) which switches them back on and
      fires exactly one notification. Ghidra's C for this function is
      FABRICATED; ported from the assembly.

  - address: 0x08134ff8
    ghidra: FUN_08134ff8
    name: observable_set_notify_enabled
    size: 64
    sig: "void *observable_set_notify_enabled(Registry *observable, u32 enabled)"
    module: app/registry
    status: ported
    notes: >-
      6 call sites (5 bl + 1 tail b). Stores the LOW BYTE of `enabled`
      at this+0x21 while branching on the FULL WORD (so 0x100 writes 0
      and still takes the enable path - reproduced). Disabling returns
      `this`; enabling dispatches vtable[+0x64] and, only when that
      returns NULL, tail-calls vtable[+0x68]. A base-class method of the
      framework's observable, ported against the registry layout (which
      is that base layout). The +0x64 / +0x68 slot names
      (notify_deferred / notify_changed) are INFERRED from the batching
      idiom in 0x0810e460; the slot offsets are exact.

  - address: 0x08135038
    ghidra: FUN_08135038
    name: observable_set_changed
    size: 8
    sig: "void observable_set_changed(Registry *observable, u32 changed)"
    module: app/registry
    status: ported
    notes: >-
      1 bl call site. `strb r1, [r0, #0x20]; bx lr` - the observable's
      changed byte. Its neighbour 0x08135040 installs the registry observer
      at this+0x24 (ported as registry_set_observer).

  - address: 0x08135040
    ghidra: FUN_08135040
    name: registry_set_observer
    size: 96
    sig: "void *registry_set_observer(Registry *observable, RegistryObserver *observer)"
    module: app/class_registry
    status: ported
    renames:
      param_1: observable
      param_2: observer
      iVar1: pending_changes
    notes: >-
      10 direct call sites. Conditional old-observer vtable[+0x1c] detach,
      then `str r5,[r4,#0x24]`, unconditional new-observer
      vtable[+0x18] attach, observable vtable[+0x60] query, and conditional
      vtable[+0x68] tail dispatch. The old detach and new attach results are
      discarded; NULL from +0x60 returns directly, otherwise the +0x68
      result returns. The +0x24 write precedes attach, and the notification
      path re-loads the observable vtable after the query. `r6` is saved but
      unused. Ported in app/class_registry to cut over the constructor seam;
      the 10 call-site families show the underlying base-observable ABI, but
      the local signature models its registry-observer caller.

  - address: 0x08105a34
    ghidra: FUN_08105a34
    name: image_format_for_kind
    size: 196
    sig: "u32 image_format_for_kind(u32 kind)"
    module: app/image_format
    status: ported
    vars: {param_1: kind}
    notes: >-
      7 call sites (6 bl + 1 tail b, binary-scanned; the earlier note
      said 8 + 2). A dense ARM `addls pc, pc, r0, lsl #2` jump table
      over kind 0..17 - UNSIGNED bound, so everything above 17 takes the
      default - returning a constant id per kind or 0xffffffff for kinds
      9, 0xb, 0xd and out of range. Values from the literal pool at
      0x08105af8 - kind 0,1 -> 0x42a; 2 -> 0x3fd; 3 -> 0x400 (the
      immediate `mov r0, #1024`, not a pool word); 4 -> 0x428; 5,6 ->
      0x42b; 7 -> 0x429; 8 -> 0x425; 10 -> 0x424; 12 -> 0x40b; 14 ->
      0x41c; 15 -> 0x41f; 16 -> 0x405; 17 -> 0x41e. THE ID SPACE IS NOW
      IDENTIFIED - the result feeds 0x08105b58, which subtracts 0x3f1
      and switches on a 61-entry table (`cmp r6, #0x3c`, so ids
      0x3f1..0x42d), builds a 32-byte descriptor on its stack from image
      geometry (352, 320, 270, 220, 180, 176, 128, 100, 90, 84, 56, 41,
      37 stored to +0x00 and +0x04, often the SAME value to both; a
      fixed 16 at +0x18 = bit depth, i.e. RGB565; a halfword at +0x1c)
      and memcpys it to the caller; 0x08079d38 then returns
      `desc[0] * desc[8]`, a byte size. So these are image/artwork
      FORMAT ids and this table says which format each kind of artwork
      uses. The reading is an inference from the constants - nothing in
      the image names the kinds or the formats. Neighbours left unported
      - 0x08105b28 overrides kind 4 to 0x400 and tail-branches here,
      0x08105b38 is an argument swap, 0x08105b58 is the descriptor
      table itself. Deviation - LLVM emits a data table plus one indexed
      load where the original emits a jump table; behaviourally
      identical.

  - address: 0x08105b28
    ghidra: FUN_08105b28
    name: image_format_for_kind_kind4_override
    size: 16
    sig: "u32 image_format_for_kind_kind4_override(u32 kind)"
    module: app/image_format
    status: ported
    vars: {param_1: kind}
    notes: >-
      Four-instruction wrapper around image_format_for_kind
      (0x08105a34): `cmp r0,#4; moveq r0,#0x400; bne 0x08105a34;
      bx lr` - kind 4, which the plain jump table maps to 0x428, is
      answered 0x400 (kind 3's format) directly; every other kind
      tail-branches into the jump table unchanged. 5 bl call sites, no
      tail b (binary-scanned): 0x081b725c, 0x0821514c, 0x08215274,
      0x08223b98, 0x08223cc0 - all feed the result straight into the
      descriptor builder FUN_08105b58, so this is the same
      kind-to-format question asked through a variant table where kind
      4 shares kind 3's geometry. Why kind 4 is special-cased here but
      not in the main table is not visible from the image - ported on
      observable behavior. Neighbours 0x08105b38 (argument swap) and
      0x08105b58 (descriptor table) remain unported. Host tests: kind
      4 -> 0x400 (and equal to plain-table kind 3), every other kind
      0..=17 delegating to the decoded jump table, out-of-range kinds
      -> 0xffffffff, neighbours 3/5 unaffected. match.py structural:
      same cmp/moveq/conditional-return/tail-branch shape (the b's
      R_ARM_JUMP24 targets image_format_for_kind); the diff is LLVM's
      crate-standard fp frame - the accepted house deviation.

  - address: 0x08105b38
    ghidra: FUN_08105b38
    name: image_format_descriptor_for_kind
    size: 32
    sig: "void image_format_descriptor_for_kind(u8 *descriptor, u32 kind)"
    module: app/image_format
    status: ported
    vars: {param_1: descriptor, param_2: kind}
    notes: >-
      Argument swap around image_format_for_kind (0x08105a34) that turns
      the kind-to-format question into the descriptor question in one
      call: `mov r2,r0; push {r4,lr}; mov r0,r1; bl 0x08105a34; mov
      r1,r0; pop {r4,lr}; mov r0,r2; mov r0,r0` - NO bx lr: popping lr
      and falling through (the last mov is a pad nop) into FUN_08105b58
      is a tail call, so the descriptor builder returns straight to this
      function's caller. Semantics: image_format_descriptor(descriptor,
      image_format_for_kind(kind)). Ghidra folds the fall-through into
      one C function. 15 bl call sites, no tail b (binary-scanned):
      0x08119d28, 0x081b1e34, 0x081b71d0, 0x081b7d88, 0x081cc030,
      0x081cd218/0x081cd234/0x081cd250/0x081cd26c/0x081cd288,
      0x0820a4ec/0x0820a508/0x0820a524, 0x08220218/0x08220234 - all
      pass a 32-byte stack buffer and a kind (the `auStack_2bc [32]`
      pattern in FUN_081cd1fc & co.), so this is the one-step "format
      descriptor for artwork kind N" entry point. Consults the PLAIN
      table: kind 4 forwards 0x428 here, where the kind4-override
      wrapper would answer 0x400. Deviations - (1) the fall-through
      becomes an explicit tail call to image_format_descriptor
      (FUN_08105b58, ported below in the same module; the recording
      shim that stood in on the host while it was unported was removed
      when it landed), LLVM emits it as a `b` - the same
      control-flow shape; (2) LLVM's crate-standard fp frame. Host
      tests: descriptor pointer passes through unchanged, every kind
      0..=17 forwards the decoded jump table's format, out-of-range
      kinds forward 0xffffffff, kind 4 forwards 0x428 (plain table, not
      the override). match.py structural: same save-pointer/push/
      swap/bl/shuffle/pop/tail-branch shape; the bl's R_ARM_JUMP24
      target is image_format_for_kind and the tail b targets
      image_format_descriptor.

  - address: 0x08105b58
    ghidra: FUN_08105b58
    name: image_format_descriptor
    size: 444
    sig: "void image_format_descriptor(u8 *descriptor, u32 format)"
    module: app/image_format
    status: ported
    vars: {param_1: descriptor, param_2: format, local_38: width, local_34: height, local_30: height_bytes, local_2c: aux_tag, local_28: reserved_10, local_24: reserved_14, local_20: bit_depth, local_1c: pixel_format, DAT_08105fe4: PIXEL_FORMAT_TAG_RGB565, DAT_08105ff4: PIXEL_FORMAT_TAG_2565, DAT_08105ff8: PIXEL_FORMAT_TAG_1888, DAT_08105ff0: PIXEL_FORMAT_TAG_C422, DAT_08105fec: PIXEL_FORMAT_TAG_C420}
    notes: >-
      The one-step descriptor builder behind every artwork geometry
      query: caller passes a 32-byte stack buffer and a format id
      0x3f1..=0x42d (usually from image_format_for_kind) and gets back
      the image geometry: u32 width @+0x00, u32 height @+0x04, u32
      height_bytes @+0x08 (= height * bytes-per-pixel in every arm, so
      width * height_bytes is the buffer byte size FUN_08079d38
      computes as desc[0]*desc[8]), u16 aux tag @+0x0c (0 everywhere
      except the 220x176 pair 0x3f5/0x3fc where it is 0x10e; semantics
      unknown, ported on observable behavior), u32 zero @+0x10 and
      @+0x14 (always), u32 bit depth @+0x18 (16/32/12), u16
      pixel-format tag @+0x1c. Algorithm: zero the +0x10/+0x14 words,
      preload shared constants (0x140, 0x80, 0xdc, 0x54, 0x64, 0xb0,
      0x160, 0x10e) and the 0x565 tag from the literal pool, then a
      dense 61-entry jump table on format - 0x3f1 (`cmp r6,#0x3c;
      addls pc,pc,r6,lsl#2`, unsigned bound; holes and out-of-range ids
      take the default arm); each arm stores its fields and rejoins
      through shared store tails at 0x08105e94/0x08105f70/0x08105fd4/
      0x08105ccc (shared arms: 0x403/0x404, 0x3f9/0x40b/0x41a,
      0x41f/0x42c); finally the 32-byte stack image is copied to the
      caller's buffer (bl 0x08037df8, the ROM memcpy veneer). 18 bl
      call sites, no tail b (binary-scanned), plus the fall-through
      tail call from image_format_descriptor_for_kind. Literal pool
      (read from osos.dec, absent from the asm listing): 0x08105fe4 =
      0x565, 0x08105fe8 = 0x438 (height_bytes for 0x42b), 0x08105fec =
      0xc420, 0x08105ff0 = 0xc422, 0x08105ff4 = 0x2565, 0x08105ff8 =
      0x1888. Deviations - (1) the original initializes only 26 of the
      32 descriptor bytes: +0x0e/+0x0f and +0x1e/+0x1f are never
      stored, and the default arm stores nothing but the two zero
      words, so the copy leaks stack garbage there; the port zero-fills
      the whole descriptor instead (strictly safer) and reproduces
      every byte the original does store exactly; (2) the ROM veneer @
      0x08037df8 targets memcpy; the ported memcpy is called directly
      per house pattern (LLVM then inlines/elides the whole local +
      copy, see below). Host tests: every covered id's 32-byte
      descriptor vs an independently decoded arm table, the 29 table
      holes and out-of-range ids (unsigned bound) -> all-zero
      descriptor (deviation 1), the reserved words always zero,
      height_bytes * width = byte size at all three bit depths, and
      image_format_descriptor_for_kind building the plain table's
      descriptor for every kind (kind 4 -> 0x428's, not the override's
      0x400). match.py structural: NOT close at instruction level -
      LLVM converts the match into per-field constant tables indexed by
      format - 0x3f1 (zeros for the holes, folding the default arm in),
      stores the fields straight into the caller's buffer and calls
      __aeabi_memclr for the +0x0e..+0x17 zero gap, eliding the stack
      image and the memcpy entirely; same bounds check (cmp r3,#0x3c;
      bhi) and the same field values. The geometry reading of the id
      space (inferred when 0x08105a34 was ported) is confirmed by this
      table.

  - address: 0x0809d900
    ghidra: FUN_0809d900
    name: pixel_format_tag_to_ostype
    size: 140
    sig: "u32 pixel_format_tag_to_ostype(u32 pixel_format_tag)"
    module: app/image_format
    status: ported
    renames:
      param_1/r0: pixel_format_tag
      DAT_0809d970: PIXEL_FORMAT_TAG_C420
      DAT_0809d974: OSType_Y420
      DAT_0809d978: OSType_L565
      DAT_0809d97c: OSType_ARGB
      DAT_0809d980: OSType_B565
      DAT_0809d984: OSType_2VUY
      DAT_0809d988: OSType_V408
    notes: >-
      Maps six exact internal image pixel-format tags to host-facing QuickTime
      OSType values: 0x0565 -> 'L565', 0x1888 -> 'ARGB', 0x2565 -> 'B565',
      0xc420 -> 'y420', 0xc422 -> '2vuy', and 0xd444 -> 'v408'; all other
      u32 inputs return zero. Raw ARM has a 112-byte executable compare/
      return body at 0x0809d900..0x0809d96c followed by its 28-byte literal
      pool at 0x0809d970..0x0809d988; the next `push {r4,r5,r6,lr}` function
      begins at 0x0809d98c, so the true extent is 140 bytes rather than
      Ghidra's 112. Decoding every ARM B/BL word in osos.dec found 10 direct
      call sites, all unconditional bl (0x081508d4, 0x0815098c, 0x08150a48,
      0x08150b1c, 0x08150bd4, 0x08150d04, 0x08150dbc, 0x08150e74,
      0x08150f78, 0x0815102c), with no predicated or tail-B callers. The
      comparison order uses a signed `bgt` split around 0xc420, but all
      unmatched values converge at the same zero return. No deviations.

  - address: 0x0814d51c
    ghidra: FUN_0814d51c
    name: record_body_size
    size: 44
    sig: "u32 record_body_size(const u32 *record, u32 align)"
    module: util
    status: ported
    vars: {param_1: record, param_2: align, uVar1: size}
    notes: >-
      11 bl call sites from 9 distinct callers, all of them passing
      align = 4. Returns `(record[0] & 0x00ffffff)` - the low 24 bits of
      the record header are its size - minus, when align > 4, the
      padding needed to align `record + 12` up to `align`
      (`(align - ((record + 12) & (align - 1))) & (align - 1)`). With
      align = 4 the padding term is skipped entirely, so every live call
      is just the 24-bit size field. Callers use it as an offset -
      0x0814d6c8 reads `*(record + size + 0x0c)`, 0x0814d74c writes it,
      0x0814d7a4 forms `record + size + 0x10`. The record class and what
      lives at its +0x0c/+0x10 trailer remain unidentified; the
      alignment path is dead in this image but ported anyway for
      structural parity. Ported in src/util/record_body_size.rs.
      Deviation - the original is fully predicated (addhi/subhi/andhi/
      subhi straight-line); LLVM emits a conditional branch plus a frame
      push/pop. Same structure otherwise (load, cmp, align arithmetic,
      bic of the flags byte, subtract).

  - address: 0x081eb0c4
    ghidra: FUN_081eb0c4
    name: singleton_class_8900
    size: 44
    sig: "void *singleton_class_8900(void)"
    module: app/singletons
    status: ported
    notes: >-
      88 bl call sites from 64 distinct callers (binary-scanned; the
      earlier note said 86). The idiom app/singletons.rs already ports -
      `if (*slot == 0) *slot = ctor(operator_new(SIZE)); return *slot;`
      - with SIZE 0x380, the cache slot at 0x089cc3a8 + 4 and the
      constructor 0x081ee0c0. IDENTIFIED ONLY BY CLASS ID - that ctor
      publishes the object in the by-id class registry under id 0x8900
      (`bl 0x081d23f8` @ 0x081ee0f0), but unlike TCDemoMode and friends
      it never hands a name to the class-name factory @ 0x0820b230 and
      carries no name literal, so the CLASS COULD NOT BE NAMED and the
      symbol says only what the firmware says. Same NOT-HOOK-READY
      caveat as the other four getters - the ctor is a dispatch slot
      with a zeroing stub.

  - address: 0x081ec268
    ghidra: FUN_081ec268
    name: class_8900_cached_property_6031
    size: 40
    sig: "u32 class_8900_cached_property_6031(Class8900 *this)"
    module: app/class_8900
    status: ported
    renames:
      param_1/r0: this
    notes: >-
      17 bl call sites, binary-verified by decoding every B/BL word in
      osos.dec - all plain bl, no predicated forms, no tail b sites,
      matching the scouting figure. Ghidra's 40-byte extent is exact:
      the next function opens `push {r4, lr}` @ 0x081ec290. A method of
      the registry-class-0x8900 object (the singleton from
      app/singletons.rs; every call site hands it the class-0x8c00
      object's +0xd0 cache of that object). The class-0x8900 ctor @
      0x081ee0c0 zeroes +0x30 and stores instance_of_class_6000() at
      +0x378 (the last word of the 0x380-sized object), so +0x30 is a
      cached property value and +0x378 is the class-0x6000 store the
      cache fronts. Algorithm: `ldr r1, [r0, #0x30]; cmp r1, #0` -
      nonzero returns the cached word verbatim (`movne r0, r1; bx lr`);
      zero tail-dispatches the store's vtable slot +0xdc (`ldreq r0,
      [r0, #0x378]; moveq r2, #0x6000; ldreq r1, [r0]; ldreq r3, [r1,
      #0xdc]; addeq r1, r2, #0x31; bxeq r3`) as read(store, 0x6031,
      0x6000) - r2 = 0x6000 is live into the tail call, so it is a real
      third argument, matching the class-0x6000 read convention of the
      sibling dispatcher @ 0x08171fdc (slot +0xe0, (store, key, 0x6000,
      kind) with kind "Ui32" = 0x55693332). The cold path does NOT write
      the answer back into +0x30 - the cache is filled elsewhere.
      Ghidra's C drops both tail-call arguments ("Could not recover
      jumptable ... treating indirect jump as call"); the listing is
      ground truth. Property 0x6031 is not named in the image, but the
      class-0x8c00 state-machine callers @ 0x081a5530..0x081a70d4 pin
      its domain: they compare the result against the enum constants
      0x6032 / 0x6033 / 0x6038 / 0x603b, so 0x6031 is a mode/state word
      of the class-0x6000 0x60xx family whose value is itself a 0x60xx
      constant. Deviations: receiver and store modeled as #[repr(C)]
      structs with native pointers (the app/resource_chain.rs
      precedent), so +0x30 / +0x378 and vtable slot +0xdc land on their
      original offsets on the 32-bit target and stay self-consistent on
      the 64-bit host; the original's loads are unguarded (a NULL store
      faults) and so are the port's; the concrete class-0x6000 vtable
      stays stock firmware - the port performs the slot +0xdc dispatch
      itself, like resource_chain_find's inline blx through slot +0x64,
      so no dispatch seam. Host tests (5): warm cache returns verbatim
      (incl. 1, the enum constants, u32::MAX) without touching the
      store (NULL store must not fault); cold cache dispatches slot
      +0xdc once with (store, 0x6031, 0x6000) and propagates the
      answer, including a 0 answer; and the cold read does not fill the
      cache, so a second call dispatches again. match.py structural
      (exit-1 diff expected): same loads at the same offsets (+0x30,
      +0x378, vtable, +0xdc), same constants, same conditional
      early-return plus tail-dispatch shape; the diff is the crate
      {fp,lr} frame and LLVM's branchy form plus mov/orr synthesis of
      0x6031 standing in for the original's fully predicated eq
      sequence and addeq key formation.
  - address: 0x081ed86c
    ghidra: FUN_081ed86c
    name: class_8900_set_cached_property_6031
    size: 8
    sig: "void class_8900_set_cached_property_6031(Class8900 *this, u32 value)"
    module: app/class_8900
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: value
      "[this + 0x30]": cached_6031
    notes: >-
      14 bl call sites, binary-verified by decoding every B/BL word in
      osos.dec: all 14 are plain bl, with no predicated forms and no
      tail-b sites. Ghidra's 8-byte extent is exact: raw bytes are
      `str r1, [r0, #0x30]; bx lr`, and the next function begins at
      0x081ed874. The receiver is the registry-class-0x8900 singleton:
      its ctor zeroes +0x30 and the adjacent port
      class_8900_cached_property_6031 identifies it as the cached
      class-0x6000 state-property 0x6031, where zero deliberately means
      cold. Port writes value verbatim through Class8900's existing
      repr(C) field, with no NULL guard or other deviation. Host test
      covers zero plus 0x603b and u32::MAX, and confirms nonzero writes
      immediately take the getter's warm path without dereferencing a
      NULL store. match.py structural review of the worktree archive
      finds the same +0x30 str; LLVM adds only a conventional fp/lr
      frame around it.


  - address: 0x0810a7b8
    ghidra: FUN_0810a7b8
    name: singleton_class_6200
    size: 44
    sig: "void *singleton_class_6200(void)"
    module: app/singletons
    status: ported
    notes: >-
      47 bl call sites (binary-scanned; the earlier note said 38). Same
      getter idiom - SIZE 0xd0, cache slot 0x089cb304 + 4, ctor
      0x0810ab3c. The ctor registers the object under class id 0x6200
      (`mov r1, #0x6200` @ 0x0810abc4 with no intervening bl, `bl
      0x081d23f8` @ 0x0810ac1c - the 22-instruction gap is why an
      8-instruction backward scan reports the id as unknown). It also
      parks the layout-resource names Menu_AboutID_Template_iPod_Layout,
      ResetAllSettings_Language_Layout and
      DialogNotice_InsufficientDiskSpace_Layout in the object, placing
      it in the settings/about area - not enough to name the class.

  - address: 0x081b803c
    ghidra: FUN_081b803c
    name: singleton_class_7f80
    size: 44
    sig: "void *singleton_class_7f80(void)"
    module: app/singletons
    status: ported
    notes: >-
      38 bl call sites (binary-scanned; the earlier note said 37). Same
      getter idiom - SIZE 0x1d4, cache slot 0x089cc61c + 0, ctor
      0x081b80b4, which registers the object under class id 0x7f80 (`bl
      0x081d23f8` @ 0x081b8194). CLASS NOT NAMED, same reason as
      0x081eb0c4.

  - address: 0x0816df60
    ghidra: FUN_0816df60
    name: lazy_singleton_0x3c
    size: 44
    sig: "void *lazy_singleton_0x3c(void)"
    module: app/singletons
    status: ported
    notes: >-
      38 bl call sites (binary-scanned; the earlier note said 36). Same
      getter idiom - SIZE 0x3c, cache slot 0x089d0124 + 0xc, ctor
      0x0816e2ac. Unlike its three siblings this ctor registers NOTHING
      in the class registry and names nothing, so the allocation size is
      the only identifying fact the firmware offers and the symbol says
      exactly that. The ctor builds a small object - flag byte 1 at
      +0x10, zeroed +0x14..+0x38, a sub-object at +0x20 handed to
      0x083c78b4.

  - address: 0x0812c72c
    ghidra: FUN_0812c72c
    name: lazy_singleton_0x58
    size: 48
    sig: "void *lazy_singleton_0x58(void)"
    module: app/singletons
    status: ported
    vars: {iVar1: globals block 0x089cc168, uVar2: instance,
      DAT_0812c758: cache-pool-word (0x089cc168; the cache is its +4 slot)}
    notes: >-
      The eleventh member of the lazy-singleton family, and NOT a member
      of the timer family it sits next to (timer_stop @ 0x0812c6b0 shares
      only the address page). 46 bl call sites, 0 predicated and 0 plain
      b, binary-verified by decoding every B/BL word in
      work/firmware/osos.dec. True extent 48 bytes - 44 of code plus the
      pool word @ 0x0812c758 (0x089cc168) that Ghidra drops; the next
      function opens at 0x0812c75c with `ldr r0, [r0, #0x30]`. Same
      idiom - if the cache is 0, operator_new(0x58) then the ctor @
      0x0812ce88, store, then RE-LOAD the cache slot. The cache is the
      +0x04 slot of a shared three-slot globals block @ 0x089cc168 whose
      +0x00 (0x68 object, getter 0x0819bea8, ctor 0x0819c1d4) and +0x08
      (0x22c object, getter 0x081b5440, ctor 0x081b6628) are two more
      singletons of exactly this shape; only +0x04 is ported here.
      Class NOT named - the 44-byte ctor @ 0x0812ce88 runs the base ctor
      0x0819c550, plants vtable 0x08983b7c, initializes a sub-object at
      +0x3c via 0x08270394(this+0x3c, 0, 0) and zeroes a flag byte at
      +0x54 (which is what makes 0x58 the size), and neither it nor the
      base ctor reaches a name factory or the by-id registry; the vtable
      address lands in the region where the decrypted image holds the C++
      mangled-name blob (the app/registry.rs page mismatch), so it cannot
      be read for a name either. Callers use it as a `this` for virtual
      dispatch (slots +0x94, +0xac, +0x190) and as the first argument of
      the neighbours 0x0812ce60 / 0x0812ce74 / 0x0812c75c, clustered in
      the 0x0822xxxx-0x0823xxxx UI/menu code. NOT hook-ready - the ctor
      slot is a documented zeroing stub, like its ten siblings.

  - address: 0x081b5440
    ghidra: FUN_081b5440
    name: singleton_class_9400
    size: 48
    sig: "void *singleton_class_9400(void)"
    module: app/singletons
    status: ported
    vars: {iVar1: globals block 0x089cc168, uVar2: instance,
      DAT_081b546c: pool word (0x089cc168; the cache is its +8 slot)}
    notes: >-
      The nineteenth lazy singleton, and the third slot of the shared
      three-slot globals block @ 0x089cc168 whose +0x04 slot is
      lazy_singleton_0x58 @ 0x0812c72c (ported) and whose +0x00 slot (a
      0x68 object, getter 0x0819bea8, ctor 0x0819c1d4) remains stock.
      22 bl call sites, 0 predicated and 0 plain b, binary-verified by
      decoding every B/BL word in work/firmware/osos.dec (Ghidra's count
      agrees). True extent 48 bytes - 44 of code plus the pool word @
      0x081b546c (0x089cc168) that Ghidra drops; the next function opens
      at 0x081b5470 with `push {r4, r5, r6, lr}`. Same idiom - if the
      +8 slot (0x089cc170) is 0, operator_new(0x22c) then the ctor @
      0x081b6628, store, then RE-LOAD the cache slot. Class identified
      by registry id: the ctor (0x081b6628..0x081b6754, followed by the
      NULL-guarded deleting destructor @ 0x081b6754) publishes `this`
      under id 0x9400 (`mov r1, #0x9400; bl 0x081d23f8` @ 0x081b6740,
      the ported registry_register; 0x9400 is one of the ids the
      registry_register site scan recovered). The ctor runs the same
      base ctor 0x0819c550 the +0x04 sibling's ctor runs, plants vtable
      0x0898c1e4 (pool literal @ 0x081b6750) at +0x00, sets +0x3c = 0
      and +0x40 = 0xffffffff, builds three embedded context-scope
      records at +0x44/+0x58/+0x6c through the ported app/context_scope
      triplet (context_scope_init 0x082840e8 on a stack temp, copy ctor
      0x0828414c into the member, the empty bx-lr drop 0x08284188), runs
      the ported pair_header_base_construct @ 0x0810ebbc at +0x8c and
      +0x144, zeroes +0x1fc..+0x214 and bytes +0x218/+0x21d, builds a
      self-pointing map sentinel at +0x204 via FUN_083c63fc (parked at
      +0x214), and finishes with the ported construct_condition_variable
      @ 0x0807f680 at +0x220 - the last field written, which is what
      makes 0x22c the allocation size. Two call sites (0x081de604,
      0x081de64c) pick between THIS getter and lazy_singleton_0x58 on a
      mode flag byte at the caller's +0xe4, then dispatch the winner
      through vtable slot +0xac - the two shared-block siblings are
      alternative implementations of one interface; the rest cluster in
      0x08179xxx..0x0817dxxx and 0x08235xxx. Class NOT named - the ctor
      reaches no name factory - so the symbol carries the registry id.
      Deviations - the ctor rides the SINGLETON_CTORS dispatch table
      (class_9400 slot, documented zeroing default) and the cache is
      the crate static CLASS_9400_INSTANCE rather than the word @
      0x089cc170 (the block_mgr.rs RW-page deviation). NOT hook-ready,
      the family contract. match.py structural: same test / allocate
      (0x22c immediate) / construct / store / reload shape; LLVM keeps
      its frame-pointer prologue and reaches the ctor by blx through the
      dispatch slot, exactly like its eighteen siblings.
    renames: {}

  - address: 0x081533ec
    ghidra: FUN_081533ec
    name: settings_item_get
    size: 80
    sig: "u8 *settings_item_get(void)"
    module: app/settings_item
    status: ported
    renames:
      DAT_08153434: settings_item_guard
      DAT_08153438: settings_item
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab338: cxa_guard_release
    notes: >-
      EXTENT CORRECTED: Ghidra's 72 bytes are the instructions at
      0x081533ec..0x08153434; the 8-byte literal pool at 0x08153434
      (guard 0x089ca62c) and 0x08153438 (record 0x08a12700) belongs to
      this accessor, and the next independently entered function starts
      at 0x0815343c. Decoding every ARM B/BL word in osos.dec found
      exactly nine direct inbound call sites, all unconditional bl:
      0x08067fbc, 0x0806b894, 0x08165c64, 0x08170e44, 0x081a5428,
      0x081bbe68, 0x081bbeac, 0x081bbf10, and 0x08202280; there are no
      predicated bl forms or tail b branches. It tests guard bit 0,
      acquires through the ported cxa_guard_acquire, clears only byte +12
      of the fixed 16-byte record, stores 100 at aligned word +8, releases
      through cxa_guard_release, and returns the record on every path. A
      nonzero bit-0-clear guard reaches acquire but is refused, leaving the
      record untouched. Target builds use the original fixed addresses;
      host tests use private identically laid-out static storage. A volatile
      function-pointer load retains the original cxa_guard_release call
      boundary despite that already-ported helper's no-op body. The ported
      class_8c00_commit_mode now calls it directly rather than using its
      former getter dispatch seam.

  - address: 0x081a53c8
    ghidra: default
    name: class_8c00_commit_mode
    size: 124
    sig: "u32 class_8c00_commit_mode(u8 *this, u32 refresh, u32 mode)"
    module: app/class_8c00
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: refresh
      param_3/r2: mode
    notes: >-
      Ghidra's 120 bytes is the code only; the trailing literal-pool word
      @ 0x081a5440 (the app-root global 0x089ca674, reached by `ldr r0,
      [pc, #36]` @ 0x081a5414) belongs to it, so the true extent is 124,
      0x081a53c8..0x081a5444, where the next function opens `push {r4,
      lr}`. Call count binary-verified by decoding every B/BL word in
      osos.dec: 27 branch sites - 23 plain bl, 1 predicated blne (@
      0x081a68cc, gated on the result of a call on the +0xd0 object),
      and 3 tail b sites from sibling class-0x8c00 methods - i.e. 24 bl
      counting the predicated form, matching the scouting figure.
      A method of the registry-class-0x8c00 object (singleton_class_8c00
      @ 0x081a5500): if this+0xd8 (the mode word its ctor zeroes last)
      differs from `mode` AND mode is 1 or 2, it broadcasts event code
      61 (mode 1) or 11 (mode 2) through the service dispatcher
      0x0836afc0 (global @ 0x089ca458) and commits the new mode only on
      a zero return (the streq predicate); other mode values neither
      broadcast nor store. Then, unless refresh == 1, it reads property
      0x6056 of the class-0x6000 instance (0x081115cc - its root
      argument is dead, the body tail-calls vtable+0xe0 of
      instance_of_class_6000() with key 0x6056 and magic 0x55693332),
      scales it through the piecewise-linear 0..100 -> 6..55 curve
      0x080e676c (over the constant getters 0x080539cc = 6 and
      0x08051e30 = 55), and stores the result at +8 of the cxa-guarded
      settings record 0x081533ec/0x081534b8 (record @ 0x08a12700, init
      +8 = 100, +12 = 0) with a vtable+0x18 notify on the record's
      owner. Finally it tail-calls the global-mode commit 0x0815968c
      with 1 (no refresh) or 4 (refreshed), which stores the mode byte @
      0x089cc8f0 and notifies the guarded singleton @ 0x089cb208
      (vtable+0x20, flag = mode == 4), and propagates its return. Call
      sites pass mode 1 or 2 and refresh 1, 4, or a variable. The class
      is NOT named and neither the +0xd8 mode nor the byte @ 0x089cc8f0
      has a surviving name; the symbols name the observable mechanics.
      Five remaining unported callees ride the CLASS_8C00_COMMIT_SEAMS
      dispatch table (the SINGLETON_CTORS house pattern, volatile slot
      reads); settings_item_get is now ported directly in
      app/settings_item. Wired defaults are inert but never invent
      behavior: broadcast returns 11 (the original's exact service-absent
      path), the store returns the original's constant 0, and the
      global-mode commit returns the mode unchanged (the original's exact
      rejection path for modes outside {1, 4}); the property reader (0)
      and scale (identity) remain documented stubs, so the refresh != 1
      path is NOT hook-ready until the remaining settings chain is ported.
      The app-root word is the crate static APP_ROOT_OBJECT (the
      context_scope.rs RW-page deviation).
      Host tests prove: unchanged mode skips broadcast and store, modes
      1/2 broadcast 61/11 and commit only on a zero return, other modes
      reach neither, refresh == 1 tails with mode 1 while any other
      refresh value runs the property -> scale -> store chain and tails
      with mode 4, the tail seam's return propagates, and the wired
      defaults are inert and safe. match.py structural: same control
      flow (mode-field compare, {1, 2} gate, 61/11 select, conditional
      store, refresh select, five-call chain, tail commit); the diff is
      the crate fp prologue, blx through the seam table standing in for
      the six stock bls, and LLVM duplicating the register-save block
      into both branches of the 61/11 select.

  - address: 0x081a6368
    ghidra: FUN_081a6368
    name: class_8c00_rearm_timer_post_0x11
    size: 84
    sig: "void class_8c00_rearm_timer_post_0x11(u8 *this, u32 delay)"
    module: app/class_8c00
    status: ported
    renames:
      param_1: this
      param_2: delay
      FUN_0812c3b0: timer_is_running (ported in drivers/timer)
      FUN_0812c6b0: timer_stop (ported in drivers/timer)
      FUN_0812c63c: timer_start_after (ported in drivers/timer)
      FUN_0812bf4c: timer_restart (ported in drivers/timer)
      FUN_081de270: event_code_queue_post (ported in app/event_code_queue)
    notes: >-
      Ghidra's 84 bytes is exact for once: 0x081a6368..0x081a63bc, the
      sibling method opens `push {r4, lr}` at 0x081a63bc and there is
      no literal pool. 21 call sites binary-verified by decoding every
      B/BL word in osos.dec: 16 bl + 5 tail b (@ 0x08112bf0,
      0x081a6698, 0x0825b8d4, 0x08294a88, 0x08294b94), ALL
      unconditional - no predicated forms - and no data word anywhere
      in the image holds the address, so the method is never virtually
      dispatched. Every one of the 21 sites loads the same delay
      literal, 120000 ms (0x1d4c0, binary-verified at each site), so
      in practice this is the class's two-minute timeout refresh.
      A method of the registry-class-0x8c00 object (singleton_class_8c00
      @ 0x081a5500): if timer_is_running(&this->timer_98) - the
      predicate ported alongside it, comparing the embedded timer's
      +0x20 state word to 'run ' under the class mutex - then
      timer_stop, timer_start_after(timer, delay) and timer_restart on
      that timer (the stop/reprogram/restart triple; the inner stops
      are the original's redundancy, kept). Then, when the byte flag
      at +0xcc is 0, it tail-branches (moveq r1, #17; beq 0x081de270)
      to event_code_queue_post(this, 0x11) - `this` unoffset, because
      the object derives from the event-code-queue base at offset 0.
      The +0xcc flag is a suspend/resume marker: the ctor @ 0x081a71fc
      zeroes it and the sibling one-liners @ 0x081a60bc / 0x081a60cc
      set it to 1 / 0 while posting codes 9 / 0xa to the same queue.
      The +0x98 timer is the object's second EMBEDDED timer (the ctor
      builds it with timer_schedule_shim(this+0x48, this+0x98, 0, 0);
      the first, at +0x6c, is armed to 30000 ms at construction).
      Ghidra's C shows the tail INLINED (mutex/unlock around the queue
      push at param_1+0x34/param_1+8) - that is event_code_queue_post's
      body, not three separate callees. Ghidra's void is right: when
      the flag suppresses the post the original returns the flag byte
      in r0, and sampled callers (0x08113984, 0x08112bf0) discard it -
      garbage, not a result (the timer_restart precedent). Deviations:
      the predicated tail branch is spelled as a conditional call; all
      five callees are ported and called directly, no new dispatch
      seam. The post inherits event_code_queue_post's NOT-hook-ready
      caveat (the default EVENT_CODE_QUEUE_HOOKS.enqueue drops codes).
      Host tests prove: running + flag 0 runs the full triple on
      this+0x98 in stop/start_after/restart order, stores the delay in
      the period word, ends state 'run ' and posts 0x11 to this+0x08's
      queue; 'stop' and 'expi' states skip the rearm (no cancel fires
      for 'expi') but still post; a set flag suppresses only the post;
      stopped + flag set does nothing. match.py structural: LLVM
      inlines the whole timer triple through the TIMER_OPS dispatch
      slots (the timer_restart precedent), so the port shows the
      running-test, the three stop sequences, the delay store at
      +0x98+0x4, the 'run ' store, the arm blx, then the +0xcc flag
      test and the queue post - structurally the original.

  - address: 0x081a5500
    ghidra: FUN_081a5500
    name: singleton_class_8c00
    size: 48
    sig: "void *singleton_class_8c00(void)"
    module: app/singletons
    status: ported
    vars: {iVar1: pool base 0x089cc7bc, uVar2: instance,
      DAT_081a552c: cache-pool-word (0x089cc7bc; the cache is its +4 slot)}
    notes: >-
      EXTENT CORRECTED - functions.csv says 44 bytes, which is the code
      only; the function's own literal pool word (0x089cc7bc @
      0x081a552c, reached by `ldr r4, [pc, #32]`) is part of it, so the
      true extent is 48 bytes, 0x081a5500..0x081a5530, where the next
      function opens `push {r4, r5, r6, lr}`. 82 bl call sites from 60
      distinct callers, zero plain-b tail sites - verified by decoding
      every B/BL word in osos.dec, not by grep.
      The eighth member of the app/singletons family and idiomatically
      identical to the other seven - `if (cache == 0) cache =
      ctor(operator_new(0xdc)); return cache;` over the cache word
      0x089cc7c0. operator_new @ 0x082aadd4 is ported and called
      directly; the ctor FUN_081a71fc is not, so it rides a new
      class_8c00 slot in SINGLETON_CTORS (documented zeroing default -
      NOT hook-ready, the family contract).
      The class is registry id 0x8c00: FUN_081a71fc is a
      multiple-inheritance ctor that forms sub = this+0x48, plants the
      primary vtable 0x0898b39c at +0x00 and the secondary vtables
      (literal+0x50 / +0xcc / +0xdc) at +0x48 / +0x60 / +0x64, sets the
      ready bytes +0x68/+0x69/+0x6a to 1, caches singleton_class_8900
      (0x081eb0c4) at +0xd0, zeroes +0xc4/+0xc8/+0xcc/+0xcd/+0xd4..0xd8,
      and registers SUB - not `this` - under 0x8c00 (`mov r1, #0x8c00;
      add r0, r4, #0x48; bl 0x081d23f8` @ 0x081a731c). The last field it
      writes is +0xd8, which is what makes 0xdc the allocation size.
      CLASS NOT NAMED - the ctor never reaches the name factory @
      0x0820b230 (the singleton_class_8900/6200/7f80 precedent).
      This is the object the kernel gateway path waits on:
      gateway_wait_ready @ 0x080c8304 (kernel/gateway_request_blocking)
      polls its byte at +0x6a until it reads 1, and its `bl 0x081a5500`
      @ 0x080c8314 is one of the 82 sites. That caller deliberately
      still goes through its own GATEWAY_STATE slot: until FUN_081a71fc
      is ported the zeroing default never sets +0x6a, so wiring this
      getter in would swap one never-ready stub for another that also
      allocates 0xdc bytes per boot.
      Deviations - the cache is a crate static rather than the word @
      0x089cc7c0 (the block_mgr.rs RW-page deviation: the image's
      0x089cxxxx contents are stale UI strings, e.g. 0x089cc7bc reads
      "ting"); zero-init is the exact pre-init state.
      Host tests - allocation size is exactly the 0xdc immediate,
      construction happens once, a pre-seeded cache never allocates, a
      NULL-returning ctor caches NULL and re-allocates on the next call
      (no failure memory), and the zeroing stub clears 0xdc bytes and
      not one more. match.py structural - same
      test/allocate/construct/store/reload shape; the diff is the crate
      fp frame, `blx r5` through SINGLETON_CTORS standing in for `bl
      0x081a71fc`, `[r4]` instead of `[r4, #4]` for the crate cache, and
      LLVM rotating the cache-hit exit to the top of the function.


  - address: 0x081bb29c
    ghidra: thunk_FUN_0806e488
    name: singleton_class_8c00_ready_gate
    size: 4
    sig: "void singleton_class_8c00_ready_gate(void)"
    module: app/singletons
    status: identified
    notes: >-
      Raw bytes are a single long-branch thunk: `b 0x0806e488`. The target at
      0x0806e488 is a ready-byte gate on `singleton_class_8c00()` that returns
      immediately when byte +0x69 is zero and otherwise tail-branches into the
      longer 0x0805bb28 controller sequence. I could not port that chain
      faithfully in this turn because the downstream body still depends on
      unported helpers/constructors (`FUN_081f50b4`, `FUN_081b1068`,
      `FUN_081fd610`, `FUN_081fd61c`, `FUN_081f507c`, `FUN_08121e5c`,
      `FUN_08122220`, `FUN_081225f0`, `FUN_08087554`, `FUN_081b111c`) plus the
      indirect callback slot at `DAT_08037f34`. Verified call count: 21 `bl`
      sites.
  - address: 0x081de158
    ghidra: FUN_081de158
    name: keyed_event_binding_construct
    size: 76
    sig: "KeyedEventBinding *keyed_event_binding_construct(KeyedEventBinding *this, u32 key, u32 context, u32 kind)"
    module: app/keyed_event_binding
    status: ported
    renames:
      param_1: this
      param_2: key
      param_3: context
      param_4: kind
      FUN_0826c5a8: rect_clear (direct, ported in ui/rect)
      FUN_0816df60: lazy_singleton_0x3c (direct, ported in app/singletons)
      FUN_083db8f4: element_registry_slot_for_key (direct firmware boundary)
    notes: >-
      Exact 76-byte extent 0x081de158..0x081de1a4: the sibling constructor
      begins with `push {r3, r4, r5, lr}` at 0x081de1a4; no literal pool is
      present. Decoding every B/BL word in osos.dec finds 12 call sites, all
      unconditional bl: 0x0817f094, 0x0817f180, 0x08180b68, 0x08182074,
      0x081821bc, 0x081833e4, 0x081835bc, 0x08183ab4, 0x08183c10,
      0x08183ebc, 0x0818422c, and 0x08184610. No predicated or tail-branch
      sites. The constructor stores the key, low kind byte, and context word;
      clears +0x0c, the embedded Rect at +0x10, and +0x20; then registers
      this in the 0x3c element registry's map (+0x20) under a stack key copy.
      The raw `strb` leaves +0x05..+0x07 untouched, which the Rust layout and
      host test preserve. rect_clear and lazy_singleton_0x3c are direct
      ported calls. FUN_083db8f4 is still stock; device builds call its
      verified entry directly, while a host-only volatile function pointer
      records its observed map-slot ABI without inventing its identity. The
      host test proves truncating kind 0x1ab to 0xab, every initialized and
      untouched field, registry +0x20, stack-copy key value, slot write, and
      return-this result. match.py structural review: expected frame and
      call-shape differences around the direct 0x083db8f4 boundary.
  - address: 0x081de270
    ghidra: FUN_081de270
    name: event_code_queue_post
    size: 40
    sig: "void event_code_queue_post(EventCodeQueue *this, u32 code)"
    module: app/event_code_queue
    status: ported
    renames:
      param_1: this
      param_2: code
      FUN_0807f5c4: mutex_lock (direct, ported in kernel/sync_mutex)
      FUN_0807f6a0: mutex_unlock (direct, ported in kernel/sync_mutex)
      FUN_083dfeb8: queue push (EVENT_CODE_QUEUE_HOOKS.enqueue slot)
    notes: >-
      Exact 40-byte extent 0x081de270..0x081de298 - the base-class ctor
      opens `push {r4, r5, r6, r7, r8, lr}` at 0x081de298 and there is
      no literal pool, so Ghidra's 40 is right for once. 16 bl-form
      call sites verified by decoding every B/BL word in osos.dec: 11
      plain bl, 1 blne and 4 bleq (0x081a565c, 0x81a5680, 0x081a5844,
      0x081a5bfc, 0x081a7184); the predicated sites gate on the
      caller's own incoming-event kind/payload compares, never on
      queue state - the callee has no guards. No tail b sites, and a
      full data-word scan finds no reference to the address: never
      virtually dispatched.
      A method of the deferred event-code queue base class (base ctor
      0x081de298, vtable 0x0898e9b0), derived into registry class
      0x8c00 (singleton_class_8c00 @ 0x081a5500, ported in
      app/singletons) through intermediate ctor 0x08143efc. The body:
      push {r0, r1, r4, lr} makes a stack copy of `code` at sp+4,
      mutex_lock(&this->mutex_34), queue_push_u32(&this->queue_08,
      &code) @ 0x083dfeb8, mutex_unlock, pop discards the scratch
      slots. Void - r0 ends as mutex_unlock's result and no caller
      reads it. Observed codes: 0, 6, 7, 8, 0xb, 0xd, 0x14..0x17,
      0x1a, 0x1b; the push has exactly two callers image-wide (this
      method and the ctor's bulk-seed helper 0x083ea558), so this is
      the class's sole runtime enqueue point.
      Layout (target): vtable +0, pad +4, queue container +8..+0x30
      (count word at container+0x20 per the ported container_is_empty
      family), word +0x30, Mutex +0x34; base size 0x3c - the derived
      ctor @ 0x08143efc starts its own fields at +0x3c
      (binary-verified).
      Deviations - the push @ 0x083dfeb8 is unported (an ADS STL
      push_back with a grow path through 0x083df988 / operator_new),
      so it rides EVENT_CODE_QUEUE_HOOKS.enqueue; the default stub
      drops the code (inert, NOT hook-ready until the container family
      is ported). mutex_lock/unlock are called directly (the
      kernel/mutex_handoff precedent), so LLVM may inline their
      ROM-dispatch guards. Host tests prove the enqueue is bracketed
      by wait/signal on the mutex handle and receives the container
      base plus the exact code, that a NULL cell or zero handle still
      enqueues without touching the ROM, and that the default enqueue
      leaves the container untouched. match.py structural: same
      three-call shape with the mutex address held in a callee-saved
      register; the diff is the crate fp prologue and a `blx` through
      the hook table standing in for `bl 0x083dfeb8`.
  - address: 0x081ded14
    ghidra: FUN_081ded14
    name: lazy_singleton_0x28
    size: 48
    sig: "void *lazy_singleton_0x28(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_081ded40: singleton_0x28_cache (pool word holding 0x089cc7c8)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_081df284: singleton_0x28_ctor (SINGLETON_CTORS.singleton_0x28 slot)
    notes: >-
      Raw ARM is the app/singletons lazy-getter shape: load cache word
      0x089cc7c8; if NULL, operator_new(0x28), construct through
      FUN_081df284, store its result, reload and return cache. Ghidra's
      44-byte extent drops the pool word 0x089cc7c8 at 0x081ded40; raw
      bytes show the next function begins at 0x081ded44, so the true
      extent is 48 bytes. A full decoded B/BL scan finds exactly 26
      direct BL callers, all unconditional; there are no predicated BL
      forms or direct B tail sites. The constructor has no recovered
      class name; its +0xc queued-work subobject and its callers'
      tagged 12-byte work records support only the structural
      singleton_0x28 name. Deviations: the ctor remains behind the
      singleton_0x28 dispatch slot, whose default zeroes the 0x28-byte
      block; the cache is a crate static instead of runtime-initialized
      0x089cc7c8. Host tests prove exact-size first allocation,
      constructor-return caching, cache-hit no-allocation, and NULL
      result retry. match.py structural review confirms the
      test/allocate/construct/store/reload shape.
  - address: 0x081ded74
    ghidra: FUN_081ded74
    name: initialize_once
    size: 68
    sig: "void initialize_once(OnceInitializationState *state)"
    module: app/once_initializer
    status: ported
    renames:
      FUN_080e9cbc: fixed_once_initializer_dispatch (unported host/target seam)
      DAT_081dedb8: opaque_initializer_argument_1 (0x081d41fc)
      DAT_081dedbc: opaque_initializer_argument_0 (0x083ec9d0)
    notes: >-
      Raw ARM is a 68-byte flag guard with an eight-byte trailing literal
      pool; the next separately linked function starts at 0x081dedc0. It
      reads state byte +0x25 without a NULL guard, calls
      FUN_080e9cbc(0x083ec9d0, 0x081d41fc, 0, 0x20, 0x8000, 1, 0x32) only
      when that byte is zero, and stores one afterward. A full decoded
      B/BL scan finds exactly 10 incoming BL callers, all unconditional;
      there are no predicated BL forms. The first fixed argument does not
      decode as a valid function entry: its initial BMI depends on incoming
      flags, so it remains an opaque argument rather than an invented
      callee. Deviation: unported FUN_080e9cbc is a fixed retailOS call on
      target and a replaceable inert seam on host. Host tests prove the
      zero/nonzero gates, argument sequence, and post-call byte store.
  - address: 0x081df9ac
    ghidra: FUN_081df9ac
    name: command_dispatch_by_name_single_arg
    size: 116
    sig: "void *command_dispatch_by_name_single_arg(void *dispatcher, const char *name, void *callback_arg)"
    module: app/command_dispatch_name_callback
    status: ported
    renames:
      param_1: dispatcher
      param_2: name
      param_3: callback_arg
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_083db41c: command_map_find
      FUN_083d8b04: cxx_string_release
      FUN_083cf800: iterator_equals
      FUN_08030f44: heap_panic
    notes: >-
      Ghidra's 4-byte extent is wrong: 0x081df9ac is a public
      `mov r0, r0` entry that falls through into the implementation
      opening at 0x081df9b0, and the next distinct function starts at
      0x081dfa20. The true extent is therefore 116 bytes,
      0x081df9ac..0x081dfa20. A full raw scan decoding every ARM B/BL
      word in osos.dec found exactly 11 direct BL callers of the assigned
      entry (all unconditional), no predicated BL and no direct B; one
      additional BL enters the 0x081df9b0 body directly. The function
      COW-wraps name, finds it in the dispatcher +0x04 ordered map,
      releases the temporary, compares the result with the end sentinel
      at dispatcher +0x14, and returns NULL on a miss. On a hit it
      requires record +0x14 to be non-NULL (else heap_panic) and invokes
      it with callback_arg, returning that opaque callback result
      untouched. Nine recovered callers pass the same name pointer in
      r1 and r2 (TMediaNowPlayingCntlr variants); the raw scan also finds
      two calls in a Ghidra-omitted 0x082304b8/0x082304e8 region, so no
      argument identity is asserted for those sites. Deviations: the
      unported command_map_find and record callback ride a volatile ops
      seam; target defaults call 0x083db41c and the raw callback word,
      while iterator_equals is reproduced as its verified pointer
      equality. Host tests use a unique low-u32 dispatcher/record slab
      and prove map/key selection, miss-to-NULL without a callback, and
      distinct callback_arg forwarding with an untouched callback return.
  - address: 0x081dfa20
    ghidra: FUN_081dfa20
    name: command_dispatcher_get
    size: 44
    sig: "void *command_dispatcher_get(void)"
    module: app/singletons
    status: ported
    vars:
      iVar1: globals base (0x089cc824; the cache is its +4 slot)
      uVar2: instance
      DAT_081dfa4c: command_dispatcher_globals (pool word holding
        0x089cc824; the cache word is 0x089cc828)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_081dfc1c: command_dispatcher_ctor (SINGLETON_CTORS.command_dispatcher slot)
    notes: >-
      The ninth member of the app/singletons family, idiomatically
      identical to the other eight - `if (cache == 0) cache =
      ctor(operator_new(0x20)); return cache;` over the cache word
      0x089cc828. EXTENT: functions.csv says 44 bytes, which is the
      code only; the function owns one trailing literal-pool word
      (0x089cc824 @ 0x081dfa4c, reached by `ldr r4, [pc, #32]`), so
      the true extent is 48 bytes, 0x081dfa20..0x081dfa50, where the
      next function opens `push {r4, lr}`. CALL COUNT CORRECTED - 73
      direct bl, not the 69 an osos.asm grep reported, plus exactly one
      plain b (the veneer @ 0x0820b230, which carries 125 more bl), so
      198 call sites in total; re-verified by decoding every B/BL word
      in osos.dec and resolving its target.
      The object is the framework's command dispatcher: the ctor
      FUN_081dfc1c plants vtable 0x0898ebac at +0x00 and initializes an
      embedded libstdc++ ordered map at +0x04 (header pointer +0x14,
      flag +0x1c, comparator byte +0x1d - the container layout
      app/event_list documents) keyed by command-NAME string; 0x20 is
      exactly the map container's extent. 39 of the 69 call sites
      dispatch a command: stack-build a temporary Silver list table
      (FUN_081473d8, the silver_list_table ctor) for resource `id-1`,
      take the 'SCST'-resolved name handle at its +0x28, then
      FUN_081dfab0 -> FUN_081dfb10 inserts into the map under that name
      and invokes the handler at record+0x18; 9 sites register handlers
      by name string via FUN_081df9ac. The command ids live in a
      private 0x0dad0000 namespace (observed 0x0dad01d7..0x0dad0e01,
      pool-literal only - they select 'SCST' records in the rsrc
      volume, not addresses; the directory entry @ 0x0840b1fc counts
      0x95 of them). CLASS NOT NAMED - the ctor never reaches a name
      factory, so the symbol names the role the call sites prove.
      Deviations - operator_new @ 0x082aadd4 is ported and called
      directly; the ctor is behind the SINGLETON_CTORS dispatch table
      with a documented zeroing default (NOT HOOK-READY, the family
      contract); the cache is the crate static
      COMMAND_DISPATCHER_INSTANCE rather than the word @ 0x089cc828
      (the block_mgr.rs RW-page deviation).
      Host tests - first call allocates at exactly the 0x20 immediate,
      constructs on the raw block and caches the ctor's return; the
      second call returns the cache without re-allocating or
      re-constructing. match.py structural (exit 1, diff expected) -
      same test/allocate-0x20/construct/store/reload shape; the diff is
      the crate fp frame, `blx r5` through SINGLETON_CTORS standing in
      for `bl 0x081dfc1c`, `[r4]` instead of `[r4, #4]` for the crate
      cache, and LLVM rotating the cache-hit exit to the top.

  - address: 0x0820b230
    ghidra: thunk_FUN_0820b230
    name: command_dispatcher_get_veneer
    size: 4
    sig: "void *command_dispatcher_get_veneer(void)"
    module: app/singletons
    status: ported
    vars:
      FUN_081dfa20: command_dispatcher_get
    notes: >-
      Long-branch veneer to command_dispatcher_get @ 0x081dfa20 - one
      word, `b 0x081dfa20` (0xeaff51fa). Genuinely 4 bytes, NOT the 8
      of the `ldr pc, [pc, #-4]` + target-word form and not an empty
      `bx lr` destructor - the word was decoded from raw bytes, the
      preceding word 0x0820b22c is the `bx lr` that ends the previous
      function and the following word 0x0820b234 opens the next with
      `push {r3, r4, r5, lr}`.
      125 bl call sites, spanning 0x080ab5a0..0x0828e8d8 - MORE than
      the 73 that reach 0x081dfa20 directly, so this is the hotter of
      the dispatcher's two entry points (198 total). Binary-scanned by
      decoding every B/BL word in osos.dec and resolving its target,
      not from Ghidra xrefs or an osos.asm grep.
      Ported as its own #[inline(never)] symbol rather than a Rust
      alias (the app/service_manager veneer precedent), so a hook at
      0x0820b230 lands on a real veneer that branches on to the getter
      exactly as the image has it. No behavior of its own - the target
      carries the lazy `if (cache == 0) cache =
      ctor(operator_new(0x20))` and all of its deviations. NOT
      HOOK-READY for the same reason as its target - the ctor
      FUN_081dfc1c is unported and rides the zeroing SINGLETON_CTORS
      default.
      Host tests - the veneer allocates and constructs exactly once
      through the shared cache, both entry points hand out the same
      instance, and the two symbols are distinct addresses. match.py
      structural (exit 0 here, the bodies are one branch) - the built
      body is `push {fp, lr}; mov fp, sp; pop {fp, lr}; b
      command_dispatcher_get`, i.e. the original's tail branch plus the
      crate's mandatory non-leaf frame pointer.

  - address: 0x081dfab0
    ghidra: FUN_081dfab0
    name: command_dispatch_by_resource
    size: 96
    sig: "void *command_dispatch_by_resource(void *dispatcher, u32 resource_id, u32 command, u32 aux)"
    module: app/command_dispatch
    status: ported
    renames:
      param_1: dispatcher (this - the command_dispatcher_get singleton)
      param_2: resource_id (the 'SCST' record selector; call sites pass
        command_id - 1)
      param_3: command (the command id itself, 0x0dad0000 namespace)
      param_4: aux (observed 0 and 6)
      FUN_081473d8: silver_list_table_ctor (direct, ported in
        app/silver_list_table)
      FUN_081dfb10: dispatch_by_name (COMMAND_DISPATCH_BY_RESOURCE_OPS
        slot; unported - looks the name up in the dispatcher's +0x04
        string-keyed map via a temporary COW string and
        cxx_string_from_cstr 0x083d8b5c / FUN_083db41c /
        cxx_string_release 0x083d8b04, compares the iterator against the
        map end cached at dispatcher +0x14 through FUN_083cf800,
        answers NULL on a miss, else invokes the found record's +0x18
        handler slot with (name, resource_id, command, aux), a NULL
        slot itself fatal via bleq heap_panic)
      FUN_081474d0: silver_list_table_dtor
        (COMMAND_DISPATCH_BY_RESOURCE_OPS slot; unported - 252 bytes,
        0x081474d0..0x081475cc, re-plants the same vtable 0x08986434
        the ctor plants and tears down the map and the +0x28 name)
      FUN_08030f44: fail (COMMAND_DISPATCH_BY_RESOURCE_OPS slot; ported
        as heap_panic, non-returning)
    notes: >-
      40 bl + 0 b call sites, binary-scanned by decoding every B/BL
      word in osos.dec; all 40 unconditional (no predicated forms).
      Ghidra's 96-byte extent is CORRECT this time - the next function
      (dispatch_by_name, unported) opens `push {r0-r3, r4-r8, lr}` @
      0x081dfb10; Ghidra's C is right too, except its 40-byte
      `auStack_44` undersizes the 48-byte SilverListTable (it sized the
      buffer from the ctor's visible writes and missed the +0x2c store,
      so `local_1c` at sp+52 is really table.name at +0x28).
      A method of the command dispatcher singleton that dispatches a
      command selected by RESOURCE ID: stack-constructs a temporary
      Silver list table for resource_id UNPOPULATED (populate = 0 - no
      SLst items are ever loaded, the object exists only to carry the
      'SCST' resolution), reads the resolved name word at table +0x28
      once (ldr r0, [sp, #52]) and calls dispatch_by_name with the name
      in BOTH r1 and r2 plus resource_id, command and aux forwarded
      verbatim; a NULL answer is fatal (cmp r0, #0; bleq heap_panic,
      non-returning - the destructor never runs on that path),
      otherwise the temporary table is destroyed and the handler's
      return handed back untouched. All 40 call sites sit in Silver
      controller code: command_dispatcher_get, then this with
      (id-1, id, 0) (aux = 6 observed twice), the result handed to a
      virtual slot of the controller (vtable +4 / +0xd0 / +0x8c).
      Deviations - dispatch_by_name, the dtor and fail ride the
      COMMAND_DISPATCH_BY_RESOURCE_OPS read_volatile seam (the
      silver_list_table ctor-seam call-boundary decision); the target
      defaults transmute the real firmware addresses 0x081dfb10 /
      0x081474d0 / 0x08030f44, so the port IS hook-ready on device. A
      swapped-in fail hook that returns stops the dispatch at the NULL
      answer and returns it without running the dtor - faithful in
      shape, since the original never reaches the dtor on that path
      either. The temporary table is MaybeUninit stack storage, the
      original's `sub sp, sp, #60` frame.
      Host tests - the happy path proves ctor order (alloc -> registry
      -> resolve, NO populate), the name forwarded in both name slots
      with the resolved SCST bytes, dispatcher/resource_id/command/aux
      verbatim, dispatch -> dtor order, and the dtor running on the
      same table (fields snapshotted inside the mock - the table is
      dead stack storage once the port returns); the NULL path proves
      fail fires immediately after the NULL answer and the dtor is
      skipped; the failed-resolution path proves the dispatch still
      goes ahead with the shared empty-rep name. match.py structural
      (exit 1, diff expected) - same construct/load-name/dispatch/
      NULL-gate/destroy shape; the diff is the ops-table indirection
      (blx r8/r9/sl for bl 0x081dfb10 / 0x081474d0 / 0x08030f44), the
      crate fp frame, and LLVM keeping the result in r4 across the
      cmp.

  - address: 0x081f77a4
    ghidra: FUN_081f77a4
    name: volume_controller_get
    size: 44
    sig: "void *volume_controller_get(void)"
    module: app/singletons
    status: ported
    vars:
      DAT_081f77d0: volume_controller_globals (pool word holding
        0x089cc26c; the cache word is its +0x1c slot @ 0x089cc288 -
        deeper than this family's usual +4)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_081fa070: volume_controller_ctor (SINGLETON_CTORS.volume_controller slot)
    notes: >-
      The tenth member of the app/singletons family, idiomatically
      identical to the other nine - `if (cache == 0) cache =
      ctor(operator_new(0x3bc)); return cache;` over the cache word
      0x089cc288 (the +0x1c slot of the globals word @ 0x089cc26c).
      EXTENT: functions.csv says 44 bytes, which is the code only; the
      function owns one trailing literal-pool word (0x089cc26c @
      0x081f77d0, reached by `ldr r4, [pc, #32]`), so the true extent
      is 48 bytes, 0x081f77a4..0x081f77d4, where the next function
      opens `push {r4, lr}`. 52 bl call sites (binary grep over
      osos.asm).
      The object is the VOLUME CONTROLLER - registry class 0x7f00 (the
      ctor @ 0x081fa070 calls registry_register with id 0x7f00). The
      identification rests on its adjuster FUN_081f9120: a signed
      delta is clamped into a 0..100 range with a 100 cap (`if
      (current < 100 - delta) ... else delta = 100`) and committed
      through the player-side setter FUN_081116e0, with FUN_081f77fc
      the elapsed-paced sibling; the getter's call sites feed it wheel
      deltas from the "EnterVolume" menu handler (@ 0x0810c9dc, a
      wheel_sample_capture consumer), and the ctor queries the media
      player interface (media_player_interface_get @ 0x08259594 twice,
      plus 0x0825a0c0) and caches its vtable+0x108 answer as a byte.
      Class NOT named in the image (no name-factory call), so the
      symbol names the proven role. Deviations - operator_new is
      ported and called directly; the ctor rides a new
      SINGLETON_CTORS.volume_controller slot (documented zeroing
      default - NOT hook-ready, the family contract); the cache is the
      crate static VOLUME_CONTROLLER_INSTANCE (the block_mgr.rs
      RW-page deviation).
      Host tests - first call allocates at exactly the 0x3bc
      immediate, constructs on the raw block and caches the ctor's
      return; the second call returns the cache without re-allocating
      or re-constructing. match.py structural (exit 1, diff expected)
      - same test/allocate-0x3bc/construct/store/reload shape; the
      diff is the crate fp frame, `blx r5` through SINGLETON_CTORS
      standing in for `bl 0x081fa070`, `[r4]` instead of `[r4, #0x1c]`
      for the crate cache, and LLVM rotating the cache-hit exit.

  # --- Batch: the UI rectangle primitives (ui/rect) ---
  # Field layout recovered from the binary, QuickDraw order:
  # +0x0 top, +0x4 left, +0x8 bottom, +0xc right, all signed 32-bit.

  - address: 0x082a22d8
    ghidra: FUN_082a22d8
    name: rect_is_empty
    size: 36
    sig: "u32 rect_is_empty(const Rect *rect)"
    module: ui/rect
    status: ported
    vars: {param_1: rect}
    notes: >-
      Returns 1 when either span collapses - right <= left or
      bottom <= top - else 0. Signed comparisons (movle/movgt). 21 bl
      call sites, binary-verified. NOT the negation of rect_is_valid
      (0x082a22fc) - a zero-width rectangle is both empty and valid.

  - address: 0x082a22fc
    ghidra: FUN_082a22fc
    name: rect_is_valid
    size: 36
    sig: "u32 rect_is_valid(const Rect *rect)"
    module: ui/rect
    status: ported
    vars: {param_1: rect}
    notes: >-
      Returns 1 when neither span is inverted - left <= right and
      top <= bottom. 5 bl call sites; the interesting one is the tail of
      rect_intersect (0x0826c1c8), which uses it (not rect_is_empty) as
      the final guard, so a degenerate zero-area clip survives.

  - address: 0x082a2320
    ghidra: FUN_082a2320
    name: rect_width
    size: 16
    sig: "i32 rect_width(const Rect *rect)"
    module: ui/rect
    status: ported
    vars: {param_1: rect}
    notes: >-
      right - left (fields +0xc minus +0x4), wrapping. 38 bl call sites.
      This is what fixes the field order - the XRGB8888 to RGB565 blit
      loop @ 0x0816d4a0 uses the result as the pixels-per-row count
      (source pointer advances 4 bytes per inner iteration and
      result * 4 bytes per row) while rect_height (0x082a2330) drives
      the outer row loop. The screen rectangle {0, 0, 0xf0, 0x140}
      appears in the image data @ 0x0843a1f8 and 0x084e05e8 -
      bottom = 240, right = 320, the Classic 6G's 320x240 LCD.

  - address: 0x082a25d8
    ghidra: FUN_082a25d8
    name: ui_element_render_bounds
    size: 36
    sig: "void ui_element_render_bounds(UiElement *element, Rect *out)"
    module: ui/content_bounds
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: out
      "element+0x80": local_bounds
      FUN_082a2670: ui_element_resolve_render_context
      FUN_0828cb64: render_context_transform_rect
    notes: >-
      Extent is binary-verified: push {r4,lr} starts at 0x082a25d8 and the
      next function begins with ldr r0,[r0,#20] at 0x082a25fc. Every ARM
      branch-with-link encoding in osos.dec was decoded: eight direct
      unconditional bl call sites target this function and no predicated bl
      forms do. It copies the four-word local bounds rectangle at +0x80 to
      out, resolves the element's RenderContext via 0x082a2670, then
      tail-branches to the unported 0x0828cb64 transform with
      RenderContext and out. Rust uses the existing
      DRAW_STATE_RENDER_TRANSFORM volatile seam: device builds retain the
      retail target; host tests exercise its deterministic model. The tail
      branch becomes an ordinary Rust call with identical observable output.
  - address: 0x082a2670
    ghidra: FUN_082a2670
    name: ui_element_resolve_render_context
    size: 56
    sig: "RenderContext *ui_element_resolve_render_context(UiElement *element)"
    module: ui/render_context
    status: ported
    vars: {param_1: element, "element+0x34": parent, "element+0x3c": render_context, "vtable+0x5c": root_render_owner, "owner+0x104": render_context}
    notes: >-
      Walks the UI element parent chain (+0x34) and returns the first
      non-NULL direct render context (+0x3c). If every node's direct
      context is NULL, calls the root's vtable +0x5c method and returns
      its result's +0x104 render context. The 76 direct bl call sites
      use the result for coordinate-origin adjustment (0x0828cb64 /
      0x0828d9b4), render-enabled state (+0xf0), and bitmap surface
      access (+0x5c), establishing RenderContext rather than a generic
      owner. Representative callers: 0x0812b170, 0x0814274c,
      0x081576a0, and 0x0816d3f0.

  - address: 0x0828c534
    ghidra: FUN_0828c534
    name: coordinate_owner_set_opacity
    size: 36
    sig: "void coordinate_owner_set_opacity(CoordinateOwner *owner, u8 opacity)"
    module: ui/coordinate_origin
    status: ported
    renames:
      param_1/r0: owner
      param_2/r1: opacity
      "owner+0x3f": opacity
      "owner+0xec/+0xed": dirty_and_origin_changed
      "owner+0xfc": command_index
      FUN_0828d110: coordinate_owner_refresh
    notes: >-
      Raw osos.dec bytes verify the 36-byte extent from the byte stores at
      0x0828c534 through bx lr at 0x0828c554; FUN_0828c558 begins the next
      function. Decoding every ARM B/BL word in osos.dec finds nine direct
      bl callers: eight unconditional and one blne. A separately decoded
      unconditional tail branch at 0x0808ea68 moves a descriptor-derived
      owner into r0 and its low-byte opacity into r1 before branching here.
      This unguarded setter writes +0x3f, sets +0xec and +0xed to 1, and
      conditionally tail-calls FUN_0828d110 with r1 = 0 unless +0xfc is -1.
      FUN_0828d110 is an existing verified-address volatile seam: device
      builds call the retail address while host tests use a recorder.

  - address: 0x0828c600
    ghidra: FUN_0828c600
    name: coordinate_owner_set_origin
    size: 132
    sig: "void coordinate_owner_set_origin(CoordinateOwner *owner, const Point *origin)"
    module: ui/coordinate_origin
    status: ported
    renames:
      param_1/r4: owner
      param_2/r5: origin
      "owner+0x00": render_context
      "owner+0x20": local_bounds
      "owner+0x30/+0x34": previous_origin_x_y
      "owner+0xec/+0xed": dirty_and_origin_changed
      "owner+0xee": active
      "owner+0xfc": command_index
      FUN_0828d9b4: render_context_invalidate_rect
      FUN_0828d110: coordinate_owner_refresh
    notes: >-
      Extent is binary-verified from push {r0-r6,lr} at 0x0828c600 to
      the pop {r0-r6,pc} at 0x0828c680; the distinct next function begins
      at 0x0828c684. Decoding every ARM B/BL word in osos.dec finds 25
      direct bl call sites: 19 unconditional and 6 blne, no tail branches.
      The predicated callers supply nullable owners; this body has no NULL
      guard. For an active owner with a non-NULL render context, translates
      its +0x20 rectangle by the old +0x30/+0x34 Point and hands it to
      FUN_0828d9b4 before committing the incoming Point and setting +0xec
      and +0xed. A command index other than -1 invokes FUN_0828d110.
      Both callees are absent from the ledger: target seams call their retail
      addresses and host tests provide recording models. The +0xd4-lock /
      +0xdc-dirty-union role of FUN_0828d9b4 is verified from raw bytes;
      FUN_0828d110 is deliberately named only for its proven post-origin
      refresh role rather than assigning an unsupported identity. Host tests
      cover active old-bounds invalidation, inactive/contextless skips,
      flag stores, the -1 suppression branch, and a live refresh.

  - address: 0x0828cccc
    ghidra: FUN_0828cccc
    name: coordinate_owner_select_display_layer
    size: 132
    sig: "void coordinate_owner_select_display_layer(CoordinateOwner *owner, u32 selection, u32 refresh_hint)"
    module: ui/coordinate_origin
    status: ported
    renames:
      param_1/r4: owner
      param_2/r6: selection
      param_3/r7: refresh_hint
      "owner+0x54": presentation
      "owner+0xf4": display
      "owner+0xfc": layer_selector
      "owner+0x100": selection
      "owner+0x110": mutex
      DAT_0828cd50: layer_selector_table (0x083ecb28)
      FUN_0828d5f4: coordinate_owner_flush_pending
      FUN_081204a0: layer_commit_pending_state
      FUN_0828d110: coordinate_owner_refresh
    notes: >-
      EXTENT verified from raw osos.dec: 132 code bytes at
      0x0828cccc..0x0828cd50, followed by its literal-pool word
      0x083ecb28 at 0x0828cd50; the separate next function opens at
      0x0828cd54. Decoding every ARM B/BL word in the image finds 11
      direct calls, all unconditional bl; no predicated calls or tail
      branches. The live-presentation gate (+0x54) encloses the entire
      body. Under the recursive mutex at +0x110, it completes the pending
      handoff at 0x0828d5f4, accepts only selection values 0..4, loads one
      opaque u32 from the 20-byte table at 0x083ecb28, writes it to +0xfc
      and the original selection to +0x100, obtains a layer from the +0xf4
      display using the selector's low byte, commits that layer's pending
      state at 0x081204a0, then refreshes the owner at 0x0828d110. The
      latter ignores r1 entirely, so refresh_hint is ABI-preserved but has
      no behavior. The two unported callees use verified-address volatile
      seams; display_get_layer and the mutex pair are existing direct ports.
      Host tests cover the raw selector (including low byte 0xff), ordering
      of flush/commit/refresh, invalid selection, and the presentation gate.
      Deviation: the immutable 20-byte table is indexed directly instead of
      being copied through the ROM memcpy veneer into a private stack image;
      the original never exposes or mutates that scratch image.

  - address: 0x0828dda0
    ghidra: FUN_0828dda0
    name: render_context_set_suspended
    size: 148
    sig: "u32 render_context_set_suspended(RenderContext *context, u32 suspended)"
    module: ui/render_context_suspend
    status: ported
    renames:
      param_1/r4: context
      param_2/r1: suspended
      "context+0x20": bounds
      "context+0x3d": suspend_state
      "context+0x48": presentation_resource
      "context+0x54": presentation
      "context+0x58": presentation_in_use
      "context+0x60": rendering_enabled
      "context+0x78": owner
      FUN_0828c700: render_context_ensure_presentation (unported)
      FUN_0828d4e0: render_context_release_resource (unported)
      FUN_0828cd54: render_context_release_presentation (unported)
      FUN_0826ec9c: ui_element_invalidate
    notes: >-
      Raw osos.dec verifies the exact 148-byte extent: push {r4,lr} begins
      at 0x0828dda0, the closing pop {r4,pc} is at 0x0828de30, and the
      distinct next function starts at 0x0828de34. Decoding every ARM B/BL
      word finds nine direct calls: eight unconditional bl and one blne
      (0x0816e0b8); the predicated caller checks its nullable context before
      this body dereferences it. No image word contains the address, so it is
      not virtually dispatched. On a changed state while +0x60 is nonzero,
      a nonzero r1 releases +0x48 then +0x54 unless the latter is absent or
      +0x58 is nonzero. Zero r1 creates the presentation only when +0x54 is
      absent and rect_is_empty(+0x20) is false, then tail-calls
      ui_element_invalidate for +0x78. The byte store at +0x3d truncates r1
      but the release-vs-resume branch uses the full word; the port therefore
      retains u32 rather than narrowing the state argument. Unported setup
      and release calls use verified-address volatile seams on device and
      recording host models; their object identities are intentionally not
      inferred. Host tests cover unchanged and disabled exits, absent and
      in-use presentation gates, release order, degenerate bounds, and the
      full-word/truncated-byte edge case.
  - address: 0x082a26a8
    ghidra: FUN_082a26a8
    name: ui_element_is_fully_shown
    size: 32
    sig: "u32 ui_element_is_fully_shown(const UiElement *element)"
    module: ui/shown_state
    status: ported
    renames:
      FUN_082a26a8: ui_element_is_fully_shown
      param_1/r0: element
      "element+0x48": flags_48
      "flags_48 bits 9-10": auxiliary_state
      "flags_48 bits 11-12": shown_state
    notes: >-
      Exact raw extent is 0x082a26a8..0x082a26c8: its bx lr is at
      0x082a26c4, with the separate ui_element_is_shown sibling opening at
      0x082a26c8. It reads the aligned flags word at element+0x48 and returns
      1 only when bits 9-10 equal 0x200 AND bits 11-12 equal 0x800; the
      conditional and/cmp sequence means the second comparison is evaluated
      only after the first succeeds. Decoding every ARM B/BL word in osos.dec
      finds exactly 8 direct, unconditional bl call sites (0x081575bc,
      0x08158704, 0x0815c1e4, 0x0815c4f0, 0x081b83ec, 0x081b88c8,
      0x0829bafc, 0x082a24cc), with no predicated forms and no data-word
      references; callers supply a live pointer and the leaf has no NULL
      guard. Host tests cover each rejected encoding, the complete 4x4
      two-field truth table with unrelated flags set, and the +0x48 read
      extent. No deliberate deviations.
  - address: 0x082a26c8
    ghidra: FUN_082a26c8
    name: ui_element_is_shown
    size: 24
    sig: "u32 ui_element_is_shown(UiElement *element)"
    module: ui/shown_state
    status: ported
    vars: {param_1: element, "element+0x48": flags_48, "flags_48 bits 11-12": shown_state}
    notes: >-
      Pure leaf flag query: returns (flags_48 & 0x1800) == 0x800, i.e. 1
      when the two-bit shown-state field (bits 11-12 of the element flag
      word at +0x48) holds value 1, 0 for the other three states. Extent
      binary-verified: sibling flag query at 0x082a26a8 precedes it and
      the next function's push {r4, lr} opens at 0x082a26e0. Exactly 32
      direct bl call sites, none predicated, no data-word references
      (verified by decoding every B/BL word and scanning every word of
      osos.dec) - never dispatched virtually, no NULL guard, callers
      pass a live element. The setter at 0x0811e294 assigns the field
      with bic 0x1800; orr 0x800 when its state argument is non-zero and
      notifies vtable +0xa8; value 0x800 is the state in which an
      element participates in redraw (ui_element_invalidate's region
      form 0x0826ec14 gates dirty-rect accumulation on this exact test,
      0x0816e5bc early-outs on it). The compound sibling query at
      0x082a26a8 additionally requires bits 9-10 == 0x200. Host tests
      cover all four field values, neighbouring-bit independence, and
      read extent.

  - address: 0x0826eb5c
    ghidra: FUN_0826eb5c
    name: draw_state_configure_for_element
    size: 184
    sig: "void draw_state_configure_for_element(UiElement *element, DrawState *draw_state)"
    module: ui/draw_state_setup
    status: ported
    renames:
      param_1/r4: element
      param_2/r7: draw_state
      iVar1/r5: render_context
      iVar2/r6: render_owner
      local_24: transformed_bounds_and_clip
      FUN_082a25d8: ui_element_bounds_in_render_coordinates
      FUN_08264564: draw_state_set_origin
      FUN_08264550: draw_state_set_surface_and_clip
    notes: >-
      Extent binary-verified from the `push {r4,r5,r6,lr}` at 0x0826eb5c
      through the `pop {r4,r5,r6,r7,pc}` at 0x0826ec10; the distinct next
      function starts at 0x0826ec14. Decoding every ARM B/BL word in
      osos.dec finds exactly 32 unconditional direct bl call sites, zero
      predicated bl forms, zero tail b references, and no data-word
      references. Resolves the element render context, obtains its vtable
      +0x5c render owner, then for shown elements copies and transforms the
      +0x80 bounds, saves its pre-clip left/top as the draw-state origin,
      and intersects it with either context +0xb4 (owner byte +0x100 is
      non-zero) or parent +0xd0. It always stores context +0x5c as the
      surface and writes the final rectangle as the draw-state clip; hidden
      elements receive the zero clip without overwriting their origin.
      Deliberate deviation: FUN_082a25d8's unported tail target
      FUN_0828cb64 is reached through DRAW_STATE_RENDER_TRANSFORM. Target
      builds invoke its retail address; ordinary host calls fail loudly and
      test builds use a deterministic coordinate model. Host tests cover
      context clipping, hidden-origin preservation, and the parent-clip
      branch.

  - address: 0x082a24f4
    ghidra: FUN_082a24f4
    name: ui_element_bounds
    size: 24
    sig: "void ui_element_bounds(const UiElement *element, Rect *out)"
    module: ui/content_bounds
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: out
      "element+0x80": bounds
      FUN_0826c2e8: rect_move_to_origin
    notes: >-
      Raw extent is 0x082a24f4..0x082a2508: mov r2,r0; mov r0,r1;
      add r1,r2,#0x80; ldm r1,{r1,r2,r3,ip}; stm r0,{r1,r2,r3,ip};
      b 0x0826c2e8. It therefore copies all four bounds words before
      modifying out, then tail-dispatches to rect_move_to_origin, making
      top/left zero and preserving the signed wrapping spans in
      bottom/right. The next distinct function starts at 0x082a250c.
      Decoding every ARM B/BL word in osos.dec finds exactly 18 direct,
      unconditional bl call sites; zero predicated bl and zero tail-b
      references. Scanning every aligned word finds no data-word reference,
      so it is not a known virtual-dispatch target. Both arguments are
      dereferenced without NULL guards. Rust uses a normal call for the
      tail dispatch, preserving the observable output.

  - address: 0x082a2604
    ghidra: FUN_082a2604
    name: ui_element_content_bounds
    size: 84
    sig: "void ui_element_content_bounds(UiElement *element, Rect *out)"
    module: ui/content_bounds
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: out
      "element+0x48": flags
      "element+0x80": bounds
    notes: >-
      Copies the element's local bounds rectangle at +0x80 into out,
      normalizes it to the origin, and then applies a 1- or 2-pixel inset
      chosen from the flag word at +0x48. The inset classifier is the
      same bit test the firmware uses at 0x082a2468: certain flag
      combinations ask for 1 pixel, others for 2, and the rest for none.
      If the inset inverts the rectangle, rect_inset clears it. Raw
      decoding finds 21 direct bl call sites. The flag classifier is
      reproduced as a private Rust helper in the module; the retail
      0x082a2468 symbol stays unported rather than becoming a second
      exported seam.
  - address: 0x0826ec14
    ghidra: FUN_0826ec14
    name: ui_element_invalidate_region
    size: 132
    sig: "UiElement *ui_element_invalidate_region(UiElement *element, const Rect *region)"
    module: ui/invalidate
    status: ported
    renames:
      param_1/r4: element
      param_2/r6: region
      r5: parent
      stack_rect: clipped_region
      "element+0x48": shown_state_flags
      "element+0x80": bounds
      "element+0xa0": invalidate_suppressed
      "parent+0xd0": parent_clip
      "0x089cc888": redraw_enabled
      FUN_0828cb64: render_context_transform_rect
      FUN_0828d9b4: render_context_invalidate_rect
    notes: >-
      Extent is binary-verified: push {r0-r6,lr} starts at 0x0826ec14,
      pop {r0-r6,pc} ends at 0x0826ec94, the literal-pool word follows
      at 0x0826ec98, and the distinct whole-bounds thunk starts at
      0x0826ec9c. Decoding every ARM B/BL word in osos.dec finds 18
      direct call sites: 17 unconditional bl and one blne (0x0826f228),
      plus six tail b references; no image word contains the address, so
      it is never virtually dispatched. The blne caller checks its nullable
      element first; this body has no NULL guard and immediately reads
      +0x48 and +0xa0. If shown-state is exactly 0x800, the suppression
      byte is zero, and redraw_enabled is non-zero, it intersects region
      with element bounds and, when present, parent+0xd0. It resolves a
      render context, transforms the local clipped copy via 0x0828cb64,
      then submits it via 0x0828d9b4. The original restores element in r0;
      the port returns it. The retail global is read volatile at its fixed
      address on device and has a host replacement. Existing volatile seams
      preserve the two still-unported rendering calls; no duplicate seam
      was added. Tests cover all three gates, both clipping stages,
      transform-before-invalidate ordering, context selection, and the
      whole-bounds thunk.

  - address: 0x0826ec9c
    ghidra: FUN_0826ec9c
    name: ui_element_invalidate
    size: 8
    sig: "UiElement *ui_element_invalidate(UiElement *element)"
    module: ui/invalidate
    status: ported
    vars: {param_1: element, "r1 (out)": "&element->bounds (element + 0x80)"}
    notes: >-
      An 8-byte thunk, NOT a veneer and NOT an empty destructor:
      `add r1, r0, #0x80; b 0x0826ec14`. It tail-branches to the ported
      region form with the second argument forced to the element's own
      bounds rectangle at +0x80, marking the whole element dirty. Extent
      binary-verified (next function's push opens at 0x0826eca4). 59
      branch references, binary-verified by decoding every B/BL word in
      osos.dec: 32 bl + 3 blne + 17 b + 7 predicated b (Ghidra's "35 bl"
      is the 32+3 bl/blne split). No data-word references, so never
      dispatched virtually. The predicated blne sites (0x0812af7c,
      0x0812af88, 0x081bac1c) test the element pointer against NULL
      themselves; the target body has no guard. Its closing
      `pop {r0-r6,pc}` restores r0 = element and r1 = element + 0x80;
      r1 remains caller-saved scratch. The port calls the direct region
      form rather than tail-branching, preserving the returned element.

  - address: 0x0826ed44
    ghidra: FUN_0826ed44
    name: ui_element_set_flag_bit_3
    size: 44
    sig: "UiElement *ui_element_set_flag_bit_3(UiElement *element, u32 mode)"
    module: ui/set_flag_bit_3
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: mode
      r2: flags
      r3: current_flag_bit
      "element+0x48": flags
      "element+0x80": bounds
      FUN_0826ec14: ui_element_invalidate_region
    notes: >-
      Raw extent is exactly 44 bytes: ldr r2,[r0,#0x48] opens at
      0x0826ed44 and the tail b 0x0826ec14 ends at 0x0826ed6c; the
      independent next function starts at 0x0826ed70. The normalized
      bit-3 value (0 or 1) is compared directly with mode. Equal values
      return immediately; otherwise zero clears 0x08, any nonzero mode
      sets it, and the element's bounds at +0x80 are tail-passed to
      ui_element_invalidate_region. Decoding every immediate ARM B/BL
      word finds exactly eight direct bl call sites: seven unconditional
      (0x082034bc, 0x0820350c, 0x0820366c, 0x082036fc, 0x08203768,
      0x0820ade8, 0x0820ae38) and predicated bleq at 0x0816a804; there
      are no tail-b or data-word references. The bleq is gated by its
      caller's event condition, while this body has no NULL guard. The
      Rust call replaces the ARM tail branch while retaining its returned
      element pointer. Host tests cover boolean transitions, neighbouring
      flag preservation, non-boolean modes, and the shown-but-suppressed
      invalidation path.

  - address: 0x0826ef88
    ghidra: FUN_0826ef88
    name: view_base_set_resource_provider
    size: 100
    sig: "ViewBase *view_base_set_resource_provider(ViewBase *view, ResourceProvider *replacement)"
    module: ui/view_base
    status: ported
    renames:
      param_1/r4: view
      param_2/r5: replacement
      "view+0x38": resource_provider
      "resource_provider vtable+0x60": replacement_allowed
      FUN_08124dbc: resource_provider_detach_view (unported)
      FUN_08124af4: resource_provider_attach_view (unported)
      FUN_0826ec9c: ui_element_invalidate
    notes: >-
      Raw extent is exactly 100 bytes: push {r4,r5,r6,lr} begins at
      0x0826ef88, the pop and tail b to 0x0826ec14 end at 0x0826efec,
      and the distinct following function begins at 0x0826efec. The
      function asks the existing provider through vtable slot +0x60 whether
      replacement is allowed. A non-zero result detaches the provider
      re-read from view+0x38 through 0x08124dbc with (provider, view, 0),
      stores the requested provider, then invokes 0x08124af4 with
      (replacement, view) and invalidates the whole view. A zero result
      preserves view+0x38 and skips the detach, but still invokes
      0x08124af4 with the requested provider before invalidating. A NULL
      current provider stores the requested provider without a virtual call;
      the requested provider itself is never NULL-guarded. Decoding every
      ARM B/BL word in osos.dec finds exactly 10 direct call sites, all
      unconditional bl (0x0811dc60, 0x08142eec, 0x081806d8, 0x081a76dc,
      0x081dd9d8, 0x081dda30, 0x081dda7c, 0x081ddac8, 0x081ddae0,
      0x081ddaf8); there are no predicated bl forms, tail b references, or
      image-word references. The two unported fixed helpers remain behind a
      target-address/host-recorder seam; the now-named shared provider-vtable
      slot models +0x60 directly on both host and target. Host tests cover
      no-current, accepted, rejected, and NULL-replacement paths.
  - address: 0x082a5e48
    ghidra: FUN_082a5e48
    name: ui_element_reference_target_item_count
    size: 52
    sig: "u32 ui_element_reference_target_item_count(const UiElementReference *reference)"
    module: ui/element_reference_item_count
    status: ported
    vars: {param_1: reference, "reference+0x0": vtable, "vtable+0xc": resolve_slot, "reference+0x4": target, "target+0x40": collection, "collection+0x2e": item_count}
    notes: >-
      Raw extent is exactly 52 bytes, 0x082a5e48 through 0x082a5e78:
      push/mov, vtable slot-3 (+0xc) blx resolve, cmp, conditional target
      load and NULL check, target+0x40 load, then ldrh at collection+0x2e;
      the next separately linked function opens push {r4,r5,lr} at
      0x082a5e7c. Decoding every ARM B/BL word in osos.dec finds 16 direct
      call sites, all plain unconditional BL, with no predicated BL. One
      non-call BNE tail transfer at 0x0812c718 is a NULL-guarding wrapper:
      it passes reference+0x14 here only when present, otherwise returns zero.
      Resolve result zero returns zero without reading target; a nonzero result
      with a NULL target also returns zero. Otherwise target+0x40 is followed
      without a NULL guard and its +0x2e halfword is returned zero-extended.
      Callers ground this as an item count: 0x0816ebe0 accepts an index only
      when it is below this value before resolving it, while 0x0816ecd8 and
      0x0816eefc compare selection positions against it. The descriptor
      0x089a6600 slot +0xc is the unresolved 0x0826acd8
      selector/implementation data pair rather than a decodable entry; the
      port preserves raw vtable dispatch and does not invent a callee.
      Deliberate host deviation: fixtures hold the slot as a native-width
      function pointer while reference, target, and collection links remain
      target u32 words. Host tests prove resolve short-circuiting, NULL target
      handling, exact zero-extended halfword extraction, arbitrary nonzero
      resolve truthiness, and slot selection.

  - address: 0x082a5ee4
    ghidra: FUN_082a5ee4
    name: service_context_selection_construct
    size: 152
    sig: "void service_context_selection_construct(ScopedContext *destination, ServiceContextSelectionSource *source, u32 selection, u32 selector)"
    module: app/scoped_context
    renames:
      param_1/r0: destination
      param_2/r4: source
      param_3/r6: selection
      param_4/r5: selector
      "source vtable+0xc": selection_available
      "source+0x4": selection_element
      "source+0x10": context_mode
      "element+0x18c bit 0": default_selector_flag
      "element+0x18d bit 2": reverse_flag
      FUN_08052728: ui_plst_slot_item_at
      FUN_08054454: object_payload_if_available
      FUN_08270394: scoped_context_construct
      FUN_08270418: scoped_context_copy_fields
    status: ported
    notes: >-
      Raw ARM is exactly 0x082a5ee4..0x082a5f78: the following word
      0x082a5f7c is a distinct push {r4,r5,r6,lr} prologue, so Ghidra's
      152-byte extent is accurate and has no trailing literal pool. The
      function first constructs an ownerless ScopedContext in destination,
      then calls source vtable slot +0x0c. A zero response returns with that
      initialized token and never reads source+0x4. Otherwise selector zero
      maps element+0x18c bit 0 to selector 1 or 2; nonzero selectors pass
      through. Element+0x18d bit 2 is isolated as the reverse flag for
      ui_plst_slot_item_at(element, selector, reverse_flag, selection).
      object_payload_if_available turns the selected object's payload into
      the owner of a temporary ScopedContext built with source+0x10 mode;
      scoped_context_copy_fields copies its payload to destination while
      retaining destination's vtable. Decoding every ARM B/BL word in
      osos.dec finds exactly 8 direct callers, all unconditional plain BL
      (0x0816ec4c, 0x081758c8, 0x081b5de8, 0x081b620c, 0x081fd150,
      0x081fd1c4, 0x08219af0, 0x082c8784), with no predicated BL or
      direct tail-B sites. The source vtable's concrete slot target is not
      established, so the port preserves the observed virtual dispatch
      rather than inventing an identity. Host tests exercise the false
      availability gate, both default selectors, reverse indexing, explicit
      selector bypass, payload ownership, source mode, and the direct caller
      preserving its destination vtable. Deliberate host deviation: native
      pointer fields retain target offsets on ARM and stay self-consistent on
      the 64-bit host; the 'plst' fixture's firmware u32 links use a unique
      below-4-GiB slab.

  - address: 0x082a63c0
    ghidra: FUN_082a63c0
    name: ui_element_reference_target_field_210
    size: 44
    sig: "u32 ui_element_reference_target_field_210(const UiElementReference *reference)"
    module: ui/element_reference_target_field_210
    status: ported
    vars: {param_1: reference, "reference+0x0": vtable, "vtable+0xc": resolve_slot, "reference+0x4": target, "target+0x210": field_210}
    notes: >-
      Raw extent is exactly 44 bytes, 0x082a63c0 through 0x082a63e8:
      push/mov, vtable slot-3 (+0xc) blx resolve, cmp, conditional target
      load and target+0x210 word load, then conditional zero return. The next
      separately linked function opens push {r4,lr} at 0x082a63ec. Decoding
      every ARM B/BL word in osos.dec finds 15 direct call sites, all plain
      unconditional BL with no predicated forms. Resolve result zero returns
      zero without reading target; any nonzero result returns target+0x210
      with no target NULL guard. The field's semantic identity is not
      established, so the port names its verified storage location rather
      than inventing a meaning. The descriptor 0x089a6600 slot +0xc is the
      unresolved 0x0826acd8 selector/implementation data pair rather than a
      decodable function entry; the port preserves raw vtable dispatch and
      does not invent a callee. Deliberate host deviation: fixtures hold the
      slot as a native-width function pointer while reference and target links
      remain target u32 words. Host tests prove failed-resolve short
      circuiting, arbitrary nonzero resolve truthiness, exact word extraction,
      resolved zero preservation, and slot selection.
  - address: 0x082a6524
    ghidra: FUN_082a6524
    name: ui_element_reference_target_persistent_id
    size: 48
    sig: "u64 ui_element_reference_target_persistent_id(const UiElementReference *reference)"
    module: ui/element_reference_persistent_id
    status: ported
    vars: {param_1: reference, "reference+0x0": vtable, "vtable+0xc": resolve_slot, "reference+0x4": target, "target+0x30": persistent_id}
    notes: >-
      Raw extent is exactly 48 bytes (0x082a6524..0x082a6554): ARM words
      decode to push/mov, a vtable slot-3 (+0xc) blx resolve, cmp, conditional
      target load, conditional ldrd at target+0x30, and pop; the next
      separately linked function opens push {r4,r5,lr} at 0x082a6554. All 21
      direct call sites are unconditional plain BL (0x080b558c/0x080b55a8,
      0x0813cc50..0x0813cd58, 0x081b5558..0x081b64a4, and
      0x082375f4..0x0823792c); decoding every ARM B/BL word in osos.dec found
      no predicated or plain-B sites. The resolve result gates the entire
      target access: zero returns both AAPCS lanes zero, nonzero returns the
      aligned target +0x30/+0x34 pair as a u64. `playlistPersistentID` is the
      consuming key at 0x080b558c/0x080b55a8; 0x0813cc50/0x0813cc68 compare
      the pair with another object's virtual identity, grounding the
      persistent-ID name. The static vtable's +0xc word (0x0826acd8) is a
      selector/implementation data pair, not a decodable entry; a boot-time
      fixup plausibly replaces it, so the port preserves the raw vtable
      dispatch without inventing a callee. Deliberate host deviation: the
      slot is a native-width function pointer in fixtures while reference and
      target pointers remain target u32 words. Host tests prove failed-resolve
      short-circuiting, arbitrary nonzero resolve truthiness, full distinct
      AAPCS halves, and a resolved zero identifier. match.py structural review
      is recorded with this port's commit.

  - address: 0x082a6554
    ghidra: FUN_082a6554
    name: ui_element_reference_construct_string
    size: 144
    sig: "void ui_element_reference_construct_string(StringObject *out, const UiElementReference *reference)"
    module: ui/element_reference_construct_string
    status: ported
    vars: {param_1: out, param_2: reference, "reference+0x0": vtable, "reference+0x4": target, "vtable+0xc": resolve_slot, "vtable+0x20": string_route_slot, local_20c: utf16_unit_count, "local_20a": utf16_units, DAT_082a65dc: resource_chain_global, DAT_082a65e0: fallback_string_id}
    notes: >-
      Raw extent is exactly 144 bytes: 136 bytes of instructions from
      0x082a6554 through 0x082a65d8, plus literal-pool words at 0x082a65dc
      (0x089cc870) and 0x082a65e0 (0x0dad06c2); the next separately linked
      function opens push {r4,lr} at 0x082a65e4. The function default-constructs
      out through string_default_construct, resolves the reference by raw vtable
      slot +0xc, and returns that empty object when resolve yields zero. A
      resolved reference dispatches raw vtable slot +0x20: zero calls unported
      0x080544dc with reference+0x4 and stack count/buffer then assigns exactly
      that many UTF-16 units through string_object_assign_utf16; nonzero finds
      string resource 0x0dad06c2 from the chain pointer in global 0x089cc870
      then assigns the returned C string, including NULL. Decoding every ARM
      B/BL word in osos.dec finds 14 direct plain unconditional BL callers
      (0x08102d28, 0x0812cc50, 0x0813dc50, 0x08174d08, 0x081769d4,
      0x08176f28, 0x08177110, 0x081776d4, 0x081b5a0c, 0x081b61b8,
      0x0823288c, 0x08236f58, 0x08238f8c, 0x08239b98), zero predicated and
      zero direct B. The slot +0xc static value 0x0826acd8 decodes as the
      {0x801e,0x08ae5474} selector/implementation data pair, not a function
      entry; slot identities remain unestablished, so the port preserves raw
      dispatch rather than inventing callees. Deliberate host deviations:
      native-width vtable function-pointer fixtures, a modeled chain-global
      pointer, and a test-replaceable direct boundary for unported 0x080544dc;
      firmware builds call the latter at its fixed load address.

  - address: 0x082a65e4
    ghidra: FUN_082a65e4
    name: context_scope_subject_matches_context_field_f40
    size: 60
    sig: "u32 context_scope_subject_matches_context_field_f40(const ContextScope *scope)"
    module: app/context_scope
    status: ported
    vars: {param_1: scope, "scope+0x0": vtable, "vtable+0xc": resolve_slot, "scope+0x4": subject, "scope+0x8": context, "context+0xf40": field_f40}
    notes: >-
      Raw extent is exactly 60 bytes, 0x082a65e4 through 0x082a661c: the
      next separately linked function opens `push {r4,lr}` at 0x082a6620.
      Raw ARM calls vtable slot +0xc with scope; a zero resolve result returns
      zero without reading scope+0x4 or scope+0x8. Any nonzero result loads
      those two words with `ldmib`, loads context+0xf40 with no NULL guard,
      and returns their exact 0/1 equality. Decoding every ARM B/BL word in
      osos.dec found 14 direct call sites, all plain unconditional bl
      (0x0813ce7c, 0x081769a4, 0x0821945c, 0x08232854, 0x082328b4,
      0x08232a94, 0x08232ccc, 0x082378b4, 0x08238e24, 0x08239108,
      0x082391c0, 0x08239264, 0x0823952c, 0x082395fc), with no predicated
      BL or direct B references. The record is the 20-byte context scope
      built by 0x082840e8; descriptor 0x089a6600's +0xc word is unresolved
      0x0826acd8 selector/implementation data, not a decodable function
      entry, so the port preserves raw dispatch and does not invent a callee.
      Context+0xf40's semantic identity is unestablished; the name deliberately
      retains its verified storage location. Deliberate host deviation:
      fixtures hold the resolve slot as a native-width function pointer while
      scope/context links remain firmware u32 words. Host tests prove failed
      resolve short-circuiting, arbitrary nonzero resolve truthiness, slot
      selection, mismatch, and zero-word equality.

  - address: 0x082a6620
    ghidra: FUN_082a6620
    name: ui_element_reference_is_current
    size: 64
    sig: "u32 ui_element_reference_is_current(const UiElementReference *reference)"
    module: ui/element_reference
    status: ported
    vars: {param_1: reference, "reference+0x4": target, "reference+0x8": context, "reference+0xc": captured_element, "context+0xf60": active, "active+0x18": current_element}
    notes: >-
      Staleness predicate over the vtable-headed UI element reference
      built by 0x082840e8/0x08283f3c (which snapshots
      *(*(context + 0xf60) + 0x18) into +0xc at creation). Runs the
      stock class check 0x080613e0 (non-NULL arg whose +0x4 word equals
      the tag literal at 0x08061404) on the reference's target at +0x4;
      on success dereferences the context at +0x8 with no NULL guard and
      returns 1 only when the active sub-object at context+0xf60 is
      non-NULL and its current element at +0x18 still equals the
      captured snapshot at +0xc. 65 bl call sites; 0x08060fcc returns
      error 0xfffeffff when it fails, 0x0816eb38/0x0816f060 gate
      mutation through the embedded reference on it. The class check
      stays in retailOS behind the TARGET_CLASS_CHECK seam.

  - address: 0x082a66f0
    ghidra: FUN_082a66f0
    name: ui_element_reference_target_has_cookie
    size: 72
    sig: "u32 ui_element_reference_target_has_cookie(const UiElementReference *reference)"
    module: ui/element_reference_cookie
    status: ported
    vars: {param_1: reference, "reference+0x0": vtable, "vtable+0xc": resolve_slot, "reference+0x4": target, "target+0x218": cookie}
    notes: >-
      Resolve-gated cookie predicate over the same vtable-headed UI
      element reference ui_element_reference_is_current (0x082a6620)
      takes. 22 bl call sites, all unconditional (verified by decoding
      every B/BL word in osos.dec; none predicated). Calls the
      reference's vtable slot 3 (*(*reference + 0xc), blx) with the
      reference; on 0 returns 0 without touching the target, else
      dereferences the target at +0x4 with no NULL guard and returns 1
      only when the 64-bit cookie at target+0x218 is nonzero (ldrd +
      cmp r1,#0 / cmpeq r0,r2 / movne r0,#1 — either half set).
      Sibling property readers 0x082a63c0 (target+0x210), 0x082a66c0
      (target+0x1ac bit 0), 0x082a6738 (target+0x18c bit 6) share the
      identical slot-3 guard. The cookie is written at 0x0816edc8
      ({+0x110,+0x114} of the context's current object, flag
      target+0x18c |= 0x80) or set to -1 at 0x0816edd4; zero is the
      never-assigned state. Callers use it as a reference-identity
      component (0x0813cbd4 compares it against a second object's
      vtable slot +0x54) and as a wait gate (0x080b53b4, iTunes-control
      path). Unresolved anomaly, documented not invented: vtable
      0x089a6600 slot +0xc statically holds 0x0826acd8, which decodes
      as a {0x801e, 0x08ae5474} selector/impl data pair, not a function
      entry, and is referenced as data by hundreds of tables around
      0x0898b008 — a boot-time table fixup most plausibly installs the
      runtime target, so the port reproduces the raw load-and-blx and
      does not name the callee. Host fixtures read the vtable slot as a
      native function pointer (4 bytes on target, 8 on host; no other
      vtable word is read, so the wider read overlaps nothing).
      match.py: dispatch core ldr/ldr/blx/cmp/beq/ldr matches verbatim;
      LLVM lowers the cookie test to two ldr + orrs instead of
      ldrd + cmp/cmpeq and adds its sl/fp frame.

  - address: 0x082a2330
    ghidra: FUN_082a2330
    name: rect_height
    size: 16
    sig: "i32 rect_height(const Rect *rect)"
    module: ui/rect
    status: ported
    vars: {param_1: rect}
    notes: bottom - top (fields +0x8 minus +0x0), wrapping. 42 bl call sites.

  - address: 0x082a2340
    ghidra: FUN_082a2340
    name: rect_contains
    size: 68
    sig: "u32 rect_contains(const Rect *outer, const Rect *inner)"
    module: ui/rect
    status: ported
    vars: {param_1: outer, param_2: inner}
    notes: >-
      1 iff inner.left >= outer.left and inner.top >= outer.top and
      inner.bottom <= outer.bottom and inner.right <= outer.right.
      4 bl call sites. A pure edge-wise comparison with no emptiness
      test, so an inverted or empty inner still reports as contained -
      preserved.

  - address: 0x082a2384
    ghidra: FUN_082a2384
    name: rect_center
    size: 56
    sig: "Point *rect_center(const Rect *rect, Point *out)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: out}
    notes: >-
      Writes the midpoint into out and returns out (closing
      `mov r0, r1`). The horizontal midpoint goes to out+0 and the
      vertical to out+4, so the point is {x, y} - not QuickDraw's
      {v, h} - even though the rectangle itself is in QuickDraw field
      order. `add rN, rN, rN, lsr #31` before `asr #1` is C's
      truncate-toward-zero divide, so negative (inverted) spans round
      toward zero. 2 bl call sites (0x081dd370, 0x081dd464).

  - address: 0x082a23bc
    ghidra: FUN_082a23bc
    name: rect_equals
    size: 64
    sig: "u32 rect_equals(const Rect *a, const Rect *b)"
    module: ui/rect
    status: ported
    vars: {param_1: a, param_2: b}
    notes: C++ operator== - all four coordinates equal. 2 bl call sites.

  - address: 0x082a23fc
    ghidra: FUN_082a23fc
    name: rect_not_equals
    size: 64
    sig: "u32 rect_not_equals(const Rect *a, const Rect *b)"
    module: ui/rect
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      C++ operator!= - emitted as its own function (identical compare
      chain, inverted results) rather than as a negation of
      rect_equals. 8 bl call sites.

  - address: 0x082a22a0
    ghidra: FUN_082a22a0
    name: rects_intersect
    size: 56
    sig: "u32 rects_intersect(const Rect *a, const Rect *b)"
    module: ui/rect
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Overlap test that leaves both operands alone - copies a into a
      16-byte stack rectangle (push {r0-r3} as the frame), calls
      rect_intersect (0x0826c1c8) on the copy and returns
      1 - rect_is_empty(copy). 2 bl call sites. The original's
      `rsbs r0, r0, #1` / `movcc r0, #0` tail is a bool normalisation
      whose cc case is unreachable (rect_is_empty only returns 0 or 1),
      so it is dropped. LLVM inlines the whole chain and drops the
      stack copy - same semantics, 38 instructions vs 14.

  - address: 0x0826c1c8
    ghidra: FUN_0826c1c8
    name: rect_intersect
    size: 132
    sig: "void rect_intersect(Rect *dst, const Rect *src)"
    module: ui/rect
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      Clips dst to src in place. Either operand empty (rect_is_empty @
      0x082a22d8) clears dst outright via a tail branch to rect_clear
      (0x0826c5a8); otherwise dst.top/left take the max and
      dst.bottom/right the min, and the result is cleared unless
      rect_is_valid (0x082a22fc) still holds. 45 bl call sites. Void -
      the original leaves garbage in r0 (the constant 1 falling out of
      the rect_is_valid call on one path, dst on the cleared path), so
      no caller can depend on it. Because the final guard is
      rect_is_valid and not rect_is_empty, a valid-but-degenerate
      zero-area overlap (edge-to-edge neighbours) survives unchanged -
      preserved, and pinned by a test.

  - address: 0x0826c574
    ghidra: FUN_0826c574
    name: rect_offset
    size: 52
    sig: "void rect_offset(Rect *rect, i32 dx, i32 dy)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: dx, param_3: dy}
    notes: >-
      QuickDraw OffsetRect - the first argument moves the horizontal
      pair (+0x4 left, +0xc right) and the second the vertical pair
      (+0x0 top, +0x8 bottom), i.e. (dh, dv). The hottest of the
      family - 99 bl + 11 tail b call sites, binary-verified. Wrapping
      adds (the original's bare `add` never traps).

  - address: 0x0826c5a8
    ghidra: FUN_0826c5a8
    name: rect_clear
    size: 24
    sig: "void rect_clear(Rect *rect)"
    module: ui/rect
    status: ported
    vars: {param_1: rect}
    notes: >-
      Zeroes all four coordinates - the canonical empty rectangle.
      26 bl + 4 tail b call sites. Void; r0 is never written, which is
      exactly why rect_intersect can tail-branch here and still look
      like it returns its destination.

  # --- Batch: the ADS C++ static-initialization runtime ---

  - address: 0x082ab1c8
    ghidra: FUN_082ab1c8
    name: cxa_atexit
    size: 68
    sig: "i32 cxa_atexit(void *object, ShutdownHandlerFn destructor, i32 dso_handle)"
    module: runtime/shutdown_chain
    status: ported
    vars: {param_1: object, param_2: destructor, param_3: dso_handle,
      r4: node, r5: result}
    notes: >-
      The C++ ABI's __cxa_atexit and the registration counterpart of
      lib_shutdown_chain @ 0x082ab2b0. malloc(16) @ 0x0802edac, one
      `stmib r4, {r6,r7,r8}` filling {arg +4, fn +8, key +0xc} from the
      three arguments, then push onto the libspace+0x38 head
      (__rt_libspace @ 0x0803204c). 278 bl + 95 tail b call sites = 373,
      binary-verified. Two departures from the Itanium ABI - arguments
      are permuted to (object, destructor, dso_handle), and the return
      is 1 on success / 0 on allocation failure, the inverse of the
      standard. Every call site is the tail of a function-local static's
      one-time init, e.g. 0x0803c270 - guard fast path `tst g,#1`,
      cxa_guard_acquire (0x082ab31c), ctor (returns `this` in r0),
      cxa_atexit(this, dtor, 0x089ca09c), cxa_guard_release
      (0x082ab338). dso_handle is therefore always the same literal,
      0x089ca09c = __dso_handle. Deviations - node size is
      size_of::<ShutdownNode>() (16 on target, correct on a 64-bit
      host); allocation goes through the new SHUTDOWN_ALLOC slot
      (default = the ported malloc), paired with the existing
      SHUTDOWN_FREE.

  - address: 0x082ab31c
    ghidra: FUN_082ab31c
    name: cxa_guard_acquire
    size: 28
    sig: "u32 cxa_guard_acquire(u32 *guard)"
    module: runtime/cxa_guard
    status: ported
    vars: {param_1: guard}
    notes: >-
      __cxa_guard_acquire, single-threaded ADS variant. Guard word
      nonzero -> return 0; zero -> store 1 and return 1. 172 bl call
      sites, binary-verified. The flag is published by ACQUIRE, not by
      release, so a constructor that re-enters its own initialization
      is refused and sees a half-built object - preserved. The whole
      word is tested against zero here while the inlined fast path at
      each call site tests only bit 0. No locking, and there is no
      __cxa_guard_abort in the image.

  - address: 0x082ab338
    ghidra: FUN_082ab338
    name: cxa_guard_release
    size: 4
    sig: "void cxa_guard_release(u32 *guard)"
    module: runtime/cxa_guard
    status: ported
    vars: {param_1: guard}
    notes: >-
      __cxa_guard_release - a bare `mov pc, lr`, no-op because acquire
      already published the flag. 172 bl call sites, exactly matching
      cxa_guard_acquire's 172; the body is empty, so the identification
      rests on that 1:1 pairing plus the adjacency at every site (each
      release immediately follows the cxa_atexit that closes the
      static's initialization, e.g. 0x0803c2a4 after 0x0803c29c).
      Needs #[inline(never)] - an empty Rust fn is otherwise a prime
      candidate for elimination, and this is a real bl target.

  # --- Batch: the rest of the rect compilation unit (ui/rect) ---

  - address: 0x0826c24c
    ghidra: FUN_0826c24c
    name: rect_intersect_into
    size: 156
    sig: "void rect_intersect_into(Rect *out, const Rect *a, const Rect *b)"
    module: ui/rect
    status: ported
    vars: {param_1: out, param_2: a, param_3: b}
    notes: >-
      Three-operand rect_intersect - out = a intersect b, sources
      untouched. Same rules as the in-place form (0x0826c1c8),
      including the rect_is_valid final guard, so a degenerate
      zero-area overlap survives. 18 bl call sites. Void (r0 is 1 on
      the valid path, out on the cleared path). match.py - 39
      instructions against the original's 39.

  - address: 0x0826c2e8
    ghidra: FUN_0826c2e8
    name: rect_move_to_origin
    size: 48
    sig: "void rect_move_to_origin(Rect *rect)"
    module: ui/rect
    status: ported
    vars: {param_1: rect}
    notes: >-
      bottom -= top; right -= left; top = left = 0 - translates the
      rectangle to (0, 0) keeping its size, so the far edges become the
      height and width. Done by subtraction rather than through
      rect_offset, so it is exact even where negating the corner would
      overflow. 12 bl + 1 tail b call sites.

  - address: 0x0826c318
    name: rect_center_vertically_in
    size: 48
    sig: "void rect_center_vertically_in(Rect *rect, const Rect *container)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: container}
    notes: >-
      MISSING FROM decomp/functions.csv - Ghidra folded it into its
      predecessor 0x0826c2e8. Extent recovered from the binary,
      0x0826c318..0x0826c348, ending in the tail b to rect_offset.
      Computes dy = (container.top - rect.top) + (container.height -
      rect.height)/2 and tail-branches to rect_offset(rect, 0, dy) -
      the dx of 0 is a hard-coded `mov r1, #0`. 1 bl call site.

  - address: 0x0826c348
    name: rect_center_horizontally_in
    size: 48
    sig: "void rect_center_horizontally_in(Rect *rect, const Rect *container)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: container}
    notes: >-
      Horizontal twin of 0x0826c318, also MISSING FROM functions.csv
      (extent 0x0826c348..0x0826c378, binary-verified). Reaches the
      horizontal fields with a pre-indexed `ldr r2, [r1, #4]!` followed
      by `ldr r3, [r1, #8]` - i.e. container+0x4 and container+0xc.
      1 bl call site.

  - address: 0x0826c378
    ghidra: FUN_0826c378
    name: rect_set
    size: 12
    sig: "void rect_set(Rect *rect, i32 top, i32 left, i32 bottom, i32 right)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: top, param_3: left, param_4: bottom,
      param_5: right}
    notes: >-
      One `ldr ip, [sp]` + `stm r0, {r1, r2, r3, ip}` - the arguments go
      into the fields in declaration order, which is why the signature
      is in QuickDraw field order and not the toolbox SetRect order.
      20 bl call sites. FUN_0826c5c0 @ 0x0826c5c0 (1 bl call site) is
      BYTE-IDENTICAL - a duplicate emission from a second translation
      unit - and has no separate Rust symbol; a hook can target
      rect_set.

  - address: 0x0826c384
    ghidra: FUN_0826c384
    name: rect_inset
    size: 84
    sig: "void rect_inset(Rect *rect, i32 dx, i32 dy)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: dx, param_3: dy}
    notes: >-
      QuickDraw InsetRect - top += dy, left += dx, bottom -= dy,
      right -= dx, then clear (tail b to 0x0826c5a8) unless
      rect_is_valid still holds. Argument order matches rect_offset
      (dx in r1, dy in r2). Negative arguments grow the rectangle;
      inserting exactly to a point survives, overshooting clears.
      11 bl call sites. match.py - 21 instructions against 21.

  - address: 0x0826c3d8
    ghidra: FUN_0826c3d8
    name: rect_union
    size: 136
    sig: "void rect_union(Rect *dst, const Rect *src)"
    module: ui/rect
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      Grows dst to the bounding box of dst and src (min of the near
      edges, max of the far ones). The two emptiness tests come first
      so a zeroed rectangle does not drag the union to the origin -
      empty src returns with dst untouched, empty dst is overwritten by
      a whole-struct ldm/stm copy of src. 10 bl + 1 tail b call sites.
      Void. match.py - 34 instructions against 34.

  - address: 0x0826c460
    ghidra: FUN_0826c460
    name: rect_union_into
    size: 148
    sig: "void rect_union_into(Rect *out, const Rect *a, const Rect *b)"
    module: ui/rect
    status: ported
    vars: {param_1: out, param_2: a, param_3: b}
    notes: >-
      Three-operand rect_union. Differs from the in-place form in what
      an empty operand means - here EITHER empty operand makes out a
      plain ldm/stm copy of the other, which the in-place form cannot
      express when src is the empty one. 2 bl call sites.

  - address: 0x0826c4f4
    ghidra: FUN_0826c4f4
    name: rect_center_in
    size: 92
    sig: "void rect_center_in(Rect *rect, const Rect *container)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: container}
    notes: >-
      Centres rect inside container on both axes without resizing it -
      computes dx and dy the same way as the two single-axis siblings
      (0x0826c318, 0x0826c348) and tail-branches to rect_offset. 9 bl
      call sites. Both halves truncate toward zero, so an odd leftover
      lands on the near edge's side.

  - address: 0x0826c550
    ghidra: FUN_0826c550
    name: rect_inset_vertical
    size: 36
    sig: "void rect_inset_vertical(Rect *rect, i32 dy)"
    module: ui/rect
    status: ported
    vars: {param_1: rect, param_2: dy}
    notes: >-
      rect_inset's vertical-only sibling - left/right are neither read
      nor written. The overshoot guard is open-coded (`cmp top, bottom`
      / `bgt rect_clear`) instead of going through rect_is_valid, but
      it still clears ALL FOUR coordinates. 2 bl call sites.

  - address: 0x0826c5c0
    ghidra: FUN_0826c5c0
    name: rect_set_duplicate
    size: 12
    module: ui/rect
    status: ported
    notes: >-
      ALIAS - byte-identical duplicate of rect_set @ 0x0826c378 (`ldr
      ip, [sp]` / `stm r0, {r1,r2,r3,ip}` / `bx lr`), emitted a second
      time from another translation unit. 1 bl call site. No separate
      Rust symbol - point any hook at rect_set (the load_be32 alias
      precedent).

  - address: 0x0826c5cc
    ghidra: FUN_0826c5cc
    name: rect_unit_dead_stub
    size: 4
    sig: "void rect_unit_dead_stub()"
    module: ui/rect
    status: ported
    notes: >-
      A lone `bx lr` closing the rect compilation unit, with ZERO bl
      and ZERO b call sites in the whole image (binary-verified) - a
      linked-out empty virtual or an unreferenced inline. Ported as an
      empty body; a zero-instruction `black_box(())` barrier keeps
      LLVM's identical-function folding from aliasing the symbol onto
      stdio::semihost::nop_stub (same empty body). The ARM codegen is
      the crate-wide frame prologue + return, structurally a no-op
      either way.

  # --- Timer E whole-second counter ---
  - address: 0x0826c5e8
    ghidra: FUN_0826c5e8
    name: usec_timer_read_seconds
    size: 28
    sig: "u32 usec_timer_read_seconds(void)"
    module: drivers/timer
    status: ported
    renames:
      local_stack_word: counter_usec
      FUN_08086e38: read_usec_timer_into
      FUN_08036f14: __rt_udiv
    notes: >-
      Binary extent is 0x0826c5e8..0x0826c604; Ghidra incorrectly absorbs
      the tail branch into the 1000-divider. It takes one Timer E TECNT
      sample through the out-pointer wrapper at 0x08086e38 (which calls
      0x08056658 -> 0x0836af80), then tail-branches to __rt_udiv with the
      literal 1000, returning truncated unsigned whole seconds. 38 direct
      bl call sites, all unconditional, and no predicated bl call sites,
      verified by decoding every ARM B/BL word in osos.dec. The Rust port
      calls the already-ported usec_timer_read directly: raw bytes prove it
      performs the same single volatile TECNT read, while avoiding a new
      dispatch seam for the unported wrapper.

  # --- Vincent 3D software JIT: IR arena + node builders -------------
  # 0x082b0000-0x082e0000 is a mixed third-party block (SQLite, zlib,
  # OpenSSL, RTXCbug, FreeType). The cluster below belongs to the
  # Vincent 3D Rendering Library - the OpenGL ES 1.1 implementation
  # Apple shipped in retailOS. Identification evidence (all strings
  # binary-verified):
  #   0x082559fc "Hans-Martin Will" / "Software" / "OpenGL ES-CM 1.1"
  #              (Vincent's author; the glGetString VENDOR/RENDERER/
  #              VERSION table)
  #   0x082cb0f4 "1.1.0.APPLE Software JIT"
  # Vincent compiles the rasterizer/fragment pipeline to ARM at run
  # time; `codegen.*` builds the IR first. The pipeline generators live
  # in 0x0823a000-0x0826f5ff and emit the IR one node at a time, which
  # is why these builders dominate the image's call counts.
  # The earlier scouting note called this "a trace/event-record writer
  # and its kind-tagged factories". The mechanics it described were
  # right; the identity was not.
  #
  # Recovered layouts (target byte offsets):
  #   cg_heap_block_t  16 B  +0 next  +4 base  +8 total  +0xc current
  #   cg_heap_t         8 B  +0 current block  +4 default block size
  #   cg_module_t             +0 heap
  #   cg_proc_t               +4 module  +0x10 registers  +0x14
  #                           last_register  +0x20 num_registers
  #   cg_block_t              +4 proc  +0xc insts  +0x10 last_inst
  #   cg_virtual_reg_t 40 B  +0 next  +0x10 reg_no  +0x20 type (u8)
  #   cg_inst_t (base)       +0 next  +4 block  +8 kind (u8)
  #                          +9 opcode (u8)  +0xc first derived field

  - address: 0x082c1a08
    ghidra: FUN_082c1a08
    name: cg_heap_alloc
    size: 112
    sig: "void *cg_heap_alloc(cg_heap_t *heap, size_t size)"
    module: codegen/heap
    status: ported
    notes: >-
      19 bl + 1 tail b call sites (binary-scanned). The JIT's bump
      arena, and the blocker that kept this whole cluster unported.
      Rounds the request up to 8 (`add r0,r1,#7` / `bic r5,r0,#7`);
      if `block->total - block->current` is (unsigned) less than that,
      creates a block of max(heap->block_size, rounded) via
      cg_heap_block_create and pushes it on the front of the chain
      (the old block's unused tail is abandoned, never revisited);
      then carves `base + current`, advances current, and ZEROES the
      carved range through the ROM thunk 0x08037dc8 -> memzero @
      0x220002d4 (mirror of osos memzero @ 0x080002d4). The zero-fill
      is load-bearing - none of the record builders write their
      record's `next` link. No NULL checks anywhere; a failed malloc
      faults on the first field store. Port deviation - malloc/free/
      memzero go through the CG_HEAP_OPS dispatch table (house
      pattern) so host tests can supply an allocator; all three
      defaults are the real ports.

  - address: 0x082c56e4
    ghidra: FUN_082c56e4
    name: cg_heap_block_create
    size: 52
    sig: "cg_heap_block_t *cg_heap_block_create(size_t size)"
    module: codegen/heap
    status: ported
    notes: >-
      2 bl call sites (cg_heap_alloc and cg_heap_create).
      malloc(16) for the header, malloc(size) for the payload,
      `stmib` writes base+total, then current = 0 and next = NULL.

  - address: 0x082c1a78
    ghidra: FUN_082c1a78
    name: cg_heap_create
    size: 40
    sig: "cg_heap_t *cg_heap_create(size_t block_size)"
    module: codegen/heap
    status: ported
    notes: >-
      1 bl call site. malloc(8) for the arena head, one first block of
      block_size bytes, `stm r4,{r0,r5}` stores (current, block_size).

  - address: 0x082c1aa0
    ghidra: FUN_082c1aa0
    name: cg_heap_destroy
    size: 60
    sig: "void cg_heap_destroy(cg_heap_t *heap)"
    module: codegen/heap
    status: ported
    notes: >-
      1 bl call site. Walks the block chain newest-first freeing
      payload then header, then tail-branches into free() for the
      arena head itself.

  - address: 0x082b2f1c
    ghidra: FUN_082b2f1c
    name: cg_reg_append_bounded
    size: 16
    sig: "cg_virtual_reg_t **cg_reg_append_bounded(cg_virtual_reg_t *reg, cg_virtual_reg_t **cursor, cg_virtual_reg_t **end)"
    module: codegen/ir
    status: ported
    vars: {param_1: reg, param_2: cursor, param_3: end}
    notes: >-
      9 bl + 2 tail b call sites (cg_inst_visit_by_kind @ 0x082c1adc and
      its sibling collector @ 0x082c1bfc). Bounded-store helper: `cmp
      r1,r2; strne r0,[r1],#4; mov r0,r1; bx lr` - store reg at cursor
      and advance the cursor one slot, unless cursor == end; returns the
      (possibly advanced) cursor. The value is stored WITHOUT a NULL
      check. match.py: body identical (cmp/strne/mov), LLVM only adds a
      frame-pointer prologue/epilogue.

  - address: 0x082c2268
    ghidra: FUN_082c2268
    name: cg_proc_create
    size: 40
    sig: "cg_proc_t *cg_proc_create(cg_module_t *module)"
    module: codegen/ir
    status: ported
    vars: {param_1: module, puVar1: proc}
    notes: >-
      6 bl call sites (binary-scanned): 0x082439c4 / 0x082465d0 /
      0x0824873c / 0x082498e8 / 0x0824a488 / 0x0824afa4 - the JIT's
      procedure-lowering drivers (FUN_082439b0, FUN_082465bc,
      FUN_08248728, FUN_082498d4, FUN_0824a478, FUN_0824af94), one
      record per compiled procedure. The procedure factory: carves a
      52-byte (mov r1, #0x34) cg_proc_t out of module->heap (+0x00)
      and PREPENDS it to the module's procedure list (+0x04) - old
      head becomes proc->next (+0x00), head slot takes the record,
      record back-points to the module at +0x04. Register list
      head/tail (+0x10/+0x14) and the counter (+0x20) stay NULL by
      the arena's zero-fill. Ghidra types the return void, but r0
      survives the epilogue and FUN_082465bc feeds it straight to
      cg_virtual_reg_create (FUN_082c23dc) - which pins the record
      as cg_proc_t (only a proc is legal there) and the +0x04 field
      as proc->module. Physically adjacent to the emitted-code
      buffer pocket but semantically an IR builder. match.py: body
      identical, only LLVM's frame-pointer prologue/epilogue
      differs.

  - address: 0x082c23dc
    ghidra: FUN_082c23dc
    name: cg_virtual_reg_create
    size: 80
    sig: "cg_virtual_reg_t *cg_virtual_reg_create(cg_proc_t *proc, cg_virtual_reg_type_t type)"
    module: codegen/ir
    status: ported
    vars: {param_1: proc, param_2: reg_type, iVar1: reg, bVar2: list_was_nonempty}
    notes: >-
      835 bl call sites, binary-verified - THE most-called unported
      function in the image before this port. Carves a 40-byte
      cg_virtual_reg_t out of proc->module->heap (proc+4 -> module,
      module+0 -> heap), writes proc->num_registers (proc+0x20) into
      reg+0x10 and post-increments it, stores the type argument as a
      BYTE at reg+0x20, and appends the record to the procedure's
      register list (proc+0x10 head / proc+0x14 tail; the tail store
      is unconditional, the head store is the `streq`). reg->next is
      never written - it stays NULL by the arena's zero-fill.
      match.py 20/20 instructions, every constant and offset
      identical; the port folds the original's duplicate
      `ldr r1,[r4,#32]` (the record cannot alias the procedure).

  - address: 0x082d6c24
    ghidra: FUN_082d6c24
    name: cg_inst_create_base
    size: 72
    sig: "cg_inst_t *cg_inst_create_base(cg_block_t *block, size_t size, cg_inst_kind_t kind, cg_opcode_t opcode)"
    module: codegen/ir
    status: ported
    notes: >-
      11 bl + 1 tail b call sites - the twelve instruction factories
      at 0x082c17f4-0x082c19b8. Carves a size-byte record out of
      block->proc->module->heap (block+4 -> proc, proc+4 -> module,
      module+0 -> heap), stores the owning block at inst+4, appends to
      the block's instruction list (block+0xc head / block+0x10 tail),
      and writes kind and opcode as BYTES at inst+8 and inst+9.
      inst->next relies on the arena's zero-fill.

  - address: 0x082c0dec
    ghidra: FUN_082c0dec
    name: cg_label_create
    size: 52
    sig: "cg_label_t *cg_label_create(cg_codegen_t *codegen)"
    module: codegen/ir
    status: ported
    vars: {param_1: codegen, puVar1: label}
    notes: >-
      4 bl call sites (0x082c12dc, 0x082c12e8, 0x082c136c, 0x082cc11c),
      and every one consumes r0 immediately (`bl` + `str r0, [...]`),
      so the port returns the label even though Ghidra types the
      function void. The label factory of the JIT: allocates a 12-byte
      cg_label_t from codegen->heap (+0x08), NULLs label->fixups (+0x04)
      with an EXPLICIT store (unlike the sibling builders, whose `next`
      links ride the arena's zero-fill), stamps label+0x08 with the
      all-ones unbound sentinel (`mvn r1, #0`), and prepends the record
      to the codegen's label list (codegen+0x0c head; old head becomes
      label->next at +0x00). This is the creator cg_label_add_fixup's
      notes pointed at; the +0x00 next / +0x04 fixups / +0x08 offset
      layout is exactly what the fixup port assumes, cross-checked by
      the tests. match.py: identical field offsets, immediates and the
      bl to cg_heap_alloc; LLVM only adds its frame-pointer prologue
      and swaps the two disjoint constant stores.

  - address: 0x082c16e0
    ghidra: FUN_082c16e0
    name: cg_codegen_resolve_label_fixups
    size: 200
    sig: "void cg_codegen_resolve_label_fixups(cg_codegen_t *codegen)"
    module: codegen/ir
    status: ported
    vars: {param_1: codegen, puVar4: label, puVar3: fixup, uVar7: output, iVar5: label_offset, iVar6: fixup_offset, uVar1: emitted, uVar2: patched}
    notes: >-
      1 bl call site (0x08243204, inside the compile-and-patch driver
      FUN_08243138; its six clones tail-branch into the driver, so every
      JIT compilation ends here) plus a 4-byte literal pool entry at
      0x082c17a8 (0xfffff000, the 12-bit sign-extension mask). The
      label-fixup resolver: takes the CODEGEN (reads codegen+0x0c =
      labels head, codegen+0x10 = output buffer - cg_label_t is only 12
      bytes, so the argument cannot be a label) and walks every bound
      label's fixup list, patching each recorded instruction word in
      place in the emitted-code buffer via cg_buffer_read_word /
      cg_buffer_write_word (their sole caller). Tag byte 0 = 12-bit
      BYTE displacement (PC-relative literal loads): sign-extend the
      word's imm12 (tst #0x800, orr 0xfffff000), add
      label->offset - fixup->offset, mask to 12 bits, keep the top 20
      bits verbatim. Tag byte 1 = 24-bit WORD displacement (B<cond>):
      sign-extend imm24 (tst #0x800000, orr 0xff000000), add the byte
      delta logically shifted right by 2 (`add r1, r1, r2, lsr #0x2`),
      mask to 24 bits, keep the top byte (cond+opcode) verbatim. Any
      other tag is skipped without even reading the emitted word
      (`bne 0x082c178c`). Ghidra's decompile drops the tag-1 sign
      extension; the tst/orrne pair at 0x082c1728-0x082c172c is
      authoritative (it is arithmetically unobservable after the field
      mask, but the port replicates it). An unbound label still
      carrying the all-ones sentinel is patched against unchecked -
      parity, not sanitized.

  - address: 0x082c17ac
    ghidra: FUN_082c17ac
    name: cg_label_add_fixup
    size: 64
    sig: "void cg_label_add_fixup(cg_codegen_t *codegen, cg_label_t *label, cg_label_fixup_tag_t tag)"
    module: codegen/ir
    status: ported
    vars: {param_1: codegen, param_2: label, param_3: tag, puVar1: fixup, uVar2: output_offset}
    notes: >-
      4 bl call sites. Creates a 12-byte cg_label_fixup_t from
      codegen->heap (+0x08), calls FUN_082c23d4 on codegen->output
      (+0x10) to obtain its current offset (+0x804), then stores it at
      fixup+0x08. The exact remaining store order is `label->fixups`
      (+0x04) to fixup+0x00, tag as a BYTE to fixup+0x04, then fixup
      back to label->fixups. The label creator @ 0x082c0dec has the
      matching 12-byte layout: next label at +0x00, fixup head at
      +0x04, and bound offset at +0x08.
      The Rust port preserves that store sequence and scales pointer slots
      by word width so the target layout remains disjoint on host tests.

  - address: 0x082c17f4
    ghidra: FUN_082c17f4
    name: cg_create_inst_binary
    size: 48
    sig: "cg_inst_t *cg_create_inst_binary(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, cg_virtual_reg_t *source0, cg_virtual_reg_t *source1)"
    module: codegen/ir
    status: ported
    notes: >-
      419 bl call sites. cg_inst_create_base(block, 0x24, kind 2,
      opcode), then dest -> +0xc, source0 -> +0x14, source1 -> +0x18
      (source1 is the fifth argument, off the stack). +0x10
      (dest_flags) is deliberately left NULL - the sibling 0x082c1824
      is the variant that fills it.

  - address: 0x082c18ec
    ghidra: FUN_082c18ec
    name: cg_create_inst_load_immed
    size: 40
    sig: "cg_inst_t *cg_create_inst_load_immed(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, U32 value)"
    module: codegen/ir
    status: ported
    notes: >-
      201 bl call sites. cg_inst_create_base(block, 0x14, kind 6,
      opcode), then dest -> +0xc, value -> +0x10.

  # The remaining members of the same factory family. Each is
  # `cg_inst_create_base(block, <size>, <kind>, opcode)` followed by a
  # handful of field stores; all are trivially portable once someone
  # needs them, and every (kind, size) pair below is read straight off
  # the assembly. Call counts binary-scanned.
  - address: 0x082c1994
    ghidra: FUN_082c1994
    name: cg_create_inst_unary
    size: 40
    sig: "cg_inst_t *cg_create_inst_unary(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, cg_virtual_reg_t *source)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: dest, param_4: source}
    notes: >-
      36 bl call sites. kind 1, record size 0x20; dest -> +0xc,
      source -> +0x14 (+0x10 dest_flags left NULL by the arena
      zero-fill). match.py - same constants and store offsets; LLVM
      swapped the two strs (unobservable, they cannot alias).

  - address: 0x082c1824
    ghidra: FUN_082c1824
    name: cg_create_inst_binary_s
    size: 44
    sig: "cg_inst_t *cg_create_inst_binary_s(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, cg_virtual_reg_t *dest_flags, cg_virtual_reg_t *source0, cg_virtual_reg_t *source1)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: dest, param_4: dest_flags, param_5: source0, param_6: source1}
    notes: >-
      7 bl call sites. kind 2, record size 0x24 - the flags-writing
      twin of cg_create_inst_binary - `ldrd r6,[sp,#24]` then
      `add r1,r0,#12` / `stm r1,{r4,r5,r6,r7}` fills +0xc, +0x10,
      +0x14, +0x18 from four arguments. dest_flags and source0 arrive
      on the stack in the original.

  - address: 0x082c1898
    ghidra: FUN_082c1898
    name: cg_create_inst_compare
    size: 44
    sig: "cg_inst_t *cg_create_inst_compare(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, cg_virtual_reg_t *source0, cg_virtual_reg_t *source1)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: dest, param_4: source0, param_5: source1}
    notes: >-
      35 bl call sites. kind 3, record size 0x20; `stm` fills +0xc,
      +0x10, +0x14 (the third comes off the stack) - the sources sit
      one word earlier than binary's because compare has no dest_flags
      slot.

  - address: 0x082c18c4
    ghidra: FUN_082c18c4
    name: cg_create_inst_load
    size: 40
    sig: "cg_inst_t *cg_create_inst_load(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, cg_virtual_reg_t *address)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: dest, param_4: address}
    notes: >-
      34 bl call sites. kind 4, record size 0x18; dest -> +0xc,
      address -> +0x10. Field roles pinned by the caller @ 0x0823a8ac
      (the returned register goes in the dest slot).

  - address: 0x082c196c
    ghidra: FUN_082c196c
    name: cg_create_inst_store
    size: 40
    sig: "cg_inst_t *cg_create_inst_store(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *value, cg_virtual_reg_t *address)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: value, param_4: address}
    notes: >-
      5 bl + 2 tail b call sites. kind 5, record size 0x18; value ->
      +0xc, address -> +0x10. Field roles pinned by the caller @
      0x0823cc40 (a freshly computed value in the first slot).

  - address: 0x082c1878
    ghidra: FUN_082c1878
    name: cg_create_inst_branch_label
    size: 32
    sig: "cg_inst_t *cg_create_inst_branch_label(cg_block_t *block, cg_opcode_t opcode, cg_block_t *target)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: target}
    notes: "16 bl call sites. kind 7, record size 0x14; target -> +0xc."

  - address: 0x082c1850
    ghidra: FUN_082c1850
    name: cg_create_inst_branch_cond
    size: 40
    sig: "cg_inst_t *cg_create_inst_branch_cond(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *condition, cg_block_t *target)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: condition, param_4: target}
    notes: >-
      41 bl call sites. kind 8, record size 0x14. NOTE the swap, kept
      in the port - the third argument goes to +0x10 (condition) and
      the fourth to +0xc (target), the opposite order from every other
      factory in the family.

  - address: 0x082c1914
    ghidra: FUN_082c1914
    name: cg_create_inst_phi
    size: 40
    sig: "cg_inst_t *cg_create_inst_phi(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *dest, cg_virtual_reg_list_t *regs)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: dest, param_4: regs}
    notes: >-
      75 bl call sites. kind 9, record size 0x14; dest -> +0xc,
      register list -> +0x10 (built by cg_virtual_reg_list_create).

  - address: 0x082c193c
    ghidra: FUN_082c193c
    name: cg_create_inst_ret
    size: 16
    sig: "cg_inst_t *cg_create_inst_ret(cg_block_t *block, cg_opcode_t opcode)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode}
    notes: >-
      3 bl + 1 tail b call sites. kind 11, record size 0x10, no field
      stores - `mov r3,r1; mov r1,#16; mov r2,#11; b 0x082d6c24`. The
      port is a plain call; LLVM adds a frame around the tail branch.

  - address: 0x082c194c
    ghidra: FUN_082c194c
    name: cg_create_inst_ret_value
    size: 32
    sig: "cg_inst_t *cg_create_inst_ret_value(cg_block_t *block, cg_opcode_t opcode, cg_virtual_reg_t *value)"
    module: codegen/ir
    status: ported
    vars: {param_1: block, param_2: opcode, param_3: value}
    notes: >-
      2 bl call sites. kind 11, record size 0x10, one field store at
      +0xc - the value-returning twin of 0x082c193c.

  - address: 0x082c19bc
    ghidra: FUN_082c19bc
    name: cg_virtual_reg_list_create
    size: 76
    sig: "cg_virtual_reg_list_t *cg_virtual_reg_list_create(cg_heap_t *heap, ...)"
    module: codegen/ir
    status: ported
    vars: {param_1: heap}
    notes: >-
      75 bl call sites, every one feeding cg_create_inst_phi. Varargs:
      `push {r0,r1,r2,r3}` builds the argument frame, then walks the
      NULL-terminated list of register pointers from &arg1 upward,
      allocating an 8-byte cell {next @ +0x0, reg @ +0x4} per entry
      straight from the heap in arg0 (NOT from a module - the caller
      passes the arena) and chaining them via a trailing link pointer
      (r4: &head, then &prev->next); the tail cell's next stays NULL by
      the arena's zero-fill. DEVIATION: Rust cannot express the C
      varargs ABI on this target, so the port takes `regs`, a pointer
      to a NULL-terminated array of register pointers - the ported
      signature is `cg_virtual_reg_list_create(heap: *mut CgHeap, regs:
      *const *mut CgVirtualReg) -> *mut CgVirtualRegList`. Termination
      rule, cell layout and allocation sequence are unchanged;
      match.py shows the same loop body (mov r1,#8; bl cg_heap_alloc;
      str cell,[link]; str reg,[cell,#4]; advance) with only the
      argument-frame spill replaced by LLVM's prologue.

  - address: 0x082c1adc
    ghidra: FUN_082c1adc
    name: cg_inst_visit_by_kind
    size: 288
    sig: "cg_virtual_reg_t **cg_inst_visit_by_kind(cg_inst_t *inst, cg_virtual_reg_t **cursor, cg_virtual_reg_t **end)"
    module: codegen/ir
    status: ported
    vars: {param_1: inst, param_2: cursor, param_3: end}
    notes: >-
      4 bl call sites. Collects the registers an instruction DEFINES
      into the bounded output array cursor..end and returns the advanced
      cursor. Dispatches on the kind byte (inst+0x8) through TWO branch
      tables: kinds 0-20 via `cmp r0,#20; addls pc,pc,r0,lsl #2`, then
      the fall-through re-classified by `sub r0,r0,#4; cmp r0,#18;
      addls` which also reaches kinds 21-22 - so cg_inst_kind_t is dense
      over 0-22 (23 values; the first table alone suggested 21).
      Kinds 1, 2, 12-17 append +0xc then +0x10 when non-NULL (the
      binary_s dest_flags slot, NULL by arena zero-fill elsewhere);
      kinds 3, 4, 6, 9, 18-22 append only +0xc; kind 10 appends +0x14
      when non-NULL; kinds 0, 5, 7, 8, 11 and >22 append nothing. Every
      append goes through the bounded-store helper @ 0x082b2f1c (16
      bytes: `cmp r1,r2; strne r0,[r1],#4; mov r0,r1; bx lr`); the +0xc
      store is NOT NULL-checked. DEVIATION: 0x082b2f1c is ported as its
      own export (cg_reg_append_bounded) but the visitor keeps it inlined
      (append_defined_reg);
      match.py shows LLVM re-expressing the two tables as bitmask/range
      tests with the helper inlined (cmp/strne pairs) - same dispatch
      sets, same store sequence.

  - address: 0x082c1bfc
    ghidra: FUN_082c1bfc
    name: cg_inst_collect_used_regs
    size: 388
    sig: "cg_virtual_reg_t **cg_inst_collect_used_regs(cg_inst_t *inst, cg_virtual_reg_t **cursor, cg_virtual_reg_t **end)"
    module: codegen/ir
    status: ported
    vars: {param_1: inst, param_2: cursor, param_3: end, iVar1: source_reg, uVar2: intermediate_cursor, puVar3: reg_list_node, piVar4: result_cursor}
    notes: >-
      8 bl + 2 tail b call sites. Collects instruction INPUT registers into
      cursor..end through cg_reg_append_bounded and returns the advanced
      cursor. The 25-entry kind table (0-24) selects source fields and exact
      order: 1/14/15: +0x14; 2/13/17: +0x14,+0x18; 3/20/22:
      +0x10,+0x14; 4/8/18/21: +0x10; 5/19/23: +0xc,+0x10; 9/10:
      each `{next,reg}` cell from the +0x10 list; 11: non-NULL +0xc;
      16: +0x14,+0x18,+0x1c; 24: +0xc,+0x10,+0x14. Kinds 0, 6, 7,
      12 and >24 append nothing. All selected values except kind 11's
      optional return register are passed unconditionally, so a NULL selected
      field is stored and advances the cursor when capacity remains.

  - address: 0x082c214c
    ghidra: FUN_082c214c
    name: cg_index_registers
    size: 108
    sig: "void cg_index_registers(cg_module_t *module)"
    module: codegen/ir
    status: ported
    vars: {param_1: module, r6: module, r4: proc, r5: block, r0: reg (fill walk), r1: table, r2: reg_no, puVar3: proc, uVar1: table, piVar2: reg, puVar4: block}
    notes: >-
      1 bl call site - the compile-and-patch driver's eighth and last
      analysis pass (bl @ 0x08243188). Builds the register-number
      index the per-block liveness visitor FUN_082b7bbc (800 bytes,
      identified-but-unported; this pass's block loop is its only bl
      site) and the register allocator consume. Per proc in
      module->procs (+0x04): arena-allocate a num_registers-entry
      pointer table (ldr r0,[r4,#0x20]; mov r1,r0,lsl #0x2; ldr
      r0,[r6,#0x0]; bl 0x082c1a08 = cg_heap_alloc, called directly)
      and store it at proc +0x18 (first recovery of that field,
      CG_PROC_REG_TABLE); walk the register list (+0x10) storing
      table[reg->reg_no] = reg (ldrne r2,[r0,#0x10]; strne
      r0,[r1,r2,lsl #0x2]); then walk the block list (+0x08) invoking
      the visitor per block. A zero-register proc still gets a table
      - cg_heap_alloc(heap, 0) rounds up to one 8-byte granule - and
      the arena zero-fill leaves NULL entries for unclaimed numbers.
      No NULL guards beyond list termination. Port deviation: the
      visitor goes through the swappable CG_BLOCK_LIVENESS slot
      (default: no analysis, the CG_PROC_EMIT scheme) where the
      original bls FUN_082b7bbc directly; the table build is the
      original's exact body. Wired as the pass_index_registers default
      of CG_COMPILE_AND_PATCH_OPS (replacing the no-op stub without
      touching the driver). Host tests:
      src/codegen/ir.rs::index_registers_empty_module_is_a_no_op
      (NULL procs; zero-register proc still gets a table),
      ::index_registers_fills_the_table_by_register_number,
      ::index_registers_visits_every_block_in_list_order (recording
      mock behind the seam),
      ::index_registers_walks_every_proc_and_indexes_each_procs_own_registers
      (per-proc tables, per-proc numbering),
      ::index_registers_composes_with_resolve_phis_in_ops_table_order
      (driver stage-1 order through the ops table),
      ::index_registers_is_the_wired_default_of_its_pass_slot.

  - address: 0x082c21b8
    ghidra: FUN_082c21b8
    name: cg_resolve_phis
    size: 176
    sig: "void cg_resolve_phis(cg_module_t *module)"
    module: codegen/ir
    status: ported
    vars: {param_1: module, r7: proc, r6: block, r4: inst (reg in the self-link/compression walks), r5: phi_source_cell, r8: cell_reg, r9: dest_representative, puVar7: proc, puVar6: block, puVar4: inst, puVar5: source_cell, piVar1: reg}
    notes: >-
      2 bl call sites - the compile-and-patch driver's fourth pass
      (bl @ 0x08243168) and the register-allocation pass FUN_082c1d4c
      (bl @ 0x082c1d54), which re-runs it internally before allocating.
      SSA deconstruction: per proc in module->procs (+0x04), (1)
      self-link every register's +0x04 parent word (str r0,[r0,#0x4]
      over the +0x10 register list), (2) walk proc->blocks (+0x08, the
      ldr r6,[r7,#0x8] - first recovery of that field) and every
      instruction, matching ONLY the opcode byte at inst+0x09 against
      0x2f (ldrb/cmp - the kind byte is not consulted), and per
      {next,reg} source cell of the phi's +0x10 list union the source
      representative into the destination's: find(inst+0x0c dest) is
      recomputed for EVERY cell, then str dest_rep,[src_rep,#0x4];
      (3) re-walk the register list running find on every register to
      path-compress each web into a star (unions alone leave two-hop
      chains when a phi dest is itself another phi's source). The find
      helper is FUN_082ce51c, ported as the private cg_reg_find (all
      of its call sites are inside this pass). No NULL guards beyond
      list termination - a phi with NULL dest/source dereferences
      address 4 exactly as the original. Wired as the pass_resolve_phis
      default of CG_COMPILE_AND_PATCH_OPS (replacing the no-op stub
      without touching the driver). match.py structural: same four
      nested walks, same offsets (#4 procs, #16 registers, #8 blocks,
      #12 insts, #9 opcode byte vs 0x2f, #16 phi regs, #12 dest, #4
      cell reg/parent), same union store; the bls render as `bl 0`
      because the private cg_reg_find sits at offset 0 of its own
      archive member. Host tests:
      src/codegen/ir.rs::resolve_phis_empty_module_is_a_no_op (NULL
      procs; proc with no registers/blocks),
      ::resolve_phis_self_links_every_register_and_overwrites_stale_parents
      (stale non-NULL parents reset before any find; list links
      untouched),
      ::resolve_phis_ignores_instructions_without_the_phi_opcode
      (binary inst, and phi-KIND record with opcode 0x30, both
      skipped),
      ::resolve_phis_unions_a_single_source_phi_into_its_dest,
      ::resolve_phis_unions_every_source_of_a_multi_source_phi,
      ::resolve_phis_compresses_two_hop_chains_into_a_star (d1=phi(r);
      d2=phi(d1) - fails without the stage-3 compression walk),
      ::resolve_phis_phi_with_an_empty_source_list_leaves_the_web_alone,
      ::resolve_phis_walks_every_block_and_every_proc (second block via
      block+0x00, second proc via cg_proc_create prepend),
      ::resolve_phis_is_the_wired_default_of_its_pass_slot.

  - address: 0x082ce51c
    ghidra: FUN_082ce51c
    name: cg_reg_find
    size: 44
    sig: "cg_virtual_reg_t *cg_reg_find(cg_virtual_reg_t *reg)"
    module: codegen/ir
    status: ported
    vars: {param_1/r4: reg, r0: parent, then representative}
    notes: >-
      3 bl call sites, ALL inside FUN_082c21b8 (plus its own
      recursion), so the port is a private fn of codegen/ir, not an
      export. Union-find FIND over the register parent word (+0x04):
      if the parent is NULL or self, return the register itself
      (cmpne/moveq); else recurse, then path-compress - the parent is
      RELOADED after the recursive call and stored only when it
      differs (ldr r1,[r4,#0x4]; cmp r1,r0; strne r0,[r4,#0x4]).
      Ghidra renders the return void, but every caller consumes r0
      (mov r9,r0 / str r9,[r0,#0x4]) - the port returns the
      representative. Exercised through every cg_resolve_phis host
      test (single-hop unions, the two-hop chain, the compression
      walk).

  - address: 0x0827233c
    ghidra: FUN_0827233c
    name: task_ctx_field_0x30
    size: 16
    sig: "u32 task_ctx_field_0x30(void)"
    module: util/context_field
    status: ported
    vars: {FUN_080cb828: CURRENT_TASK_CTX_BLOCK, FUN_0809444c: kernel_running}
    notes: >-
      647 bl call sites - the hottest 16-byte leaf in the range.
      `push {r4,lr}; bl 0x080cb828; ldr r0, [r0, #0x30]; pop {r4,pc}`:
      field +0x30 of the current task's context block. 0x080cb828
      (46 bl call sites, ported as current_task_ctx_block in
      kernel/task.rs) is `node = kernel_running() (0x0809444c, ported);
      return node ? node->+0xc : 0` - so the whole chain is a two-level
      accessor onto the kernel task object graph; what +0x30 holds is
      not decoded (writers outside the ported set), hence the
      field-offset name. The callee's result is NOT NULL-checked -
      with no current task the original reads 0x30 and takes a data
      abort; the port keeps the unchecked load. Deviation: the callee
      sits behind the CURRENT_TASK_CTX_BLOCK dispatch slot (the
      util/inner_state.rs INNER_MATERIALIZE_COUNT pattern); the default
      stub models the callee's known prefix exactly (kernel_running as
      the name-node pointer, then the +0xc word, or NULL) via the
      ported kernel/sync_mutex.rs kernel_running. Host tests: recording
      mock's ctx pointer is dereferenced at +0x30 and returned, exactly
      one getter call; edge values 0/1/0x80000000/u32::MAX round-trip;
      the block is byte-compared untouched (read-only); the default
      stub returns NULL without dereferencing when the kernel is not
      started. match.py structural: tail `ldr r0,[r0,#0x30]` exact;
      the diff is the crate fp frame and the dispatch-slot ldr/blx
      standing in for the direct `bl 0x080cb828` - the documented
      deviation. The 20-byte sibling setter @ 0x0827234c
      (`str r4,[r0,#0x30]`) is ported as task_ctx_set_field_0x30 in
      the same module.

  - address: 0x0827234c
    ghidra: FUN_0827234c
    name: task_ctx_set_field_0x30
    size: 20
    sig: "void task_ctx_set_field_0x30(u32 value)"
    module: util/context_field
    status: ported
    vars: {param_1: value, FUN_080cb828: CURRENT_TASK_CTX_BLOCK}
    notes: >-
      2 bl call sites (0x081d22a8, 0x0829060c). The setter sibling of
      task_ctx_field_0x30 @ 0x0827233c: `push {r4,lr}; mov r4,r0; bl
      0x080cb828; str r4,[r0,#0x30]; pop {r4,pc}` - writes the value
      to field +0x30 of the current task's context block. The callee's
      result is NOT NULL-checked, exactly like the getter: with no
      current task the original stores to 0x30 and takes a data abort;
      the port keeps the unchecked store. Shares the
      CURRENT_TASK_CTX_BLOCK dispatch slot with the getter (same
      documented deviation - the slot's default stub models the
      callee's known prefix exactly). Host tests: the recording mock's
      ctx block receives the value at +0x30 with exactly one getter
      call; edge values 0/1/0x80000000/u32::MAX round-trip through the
      getter; a byte-compare proves only the +0x30 word is touched.
      match.py structural: `mov r4,r0` and tail `str r4,[r0,#0x30]`
      exact; the diff is the crate fp frame and the dispatch-slot
      ldr/blx standing in for the direct `bl 0x080cb828` - the
      documented deviation.

  - address: 0x08275b9c
    ghidra: FUN_08275b9c
    name: object_cast_to_class
    size: 20
    sig: "void *object_cast_to_class(FrameworkObject *object, u32 class_id)"
    module: app/registry
    status: ported
    notes: >-
      414 bl + 17 tail b call sites. `cmp r0,#0; ldrne r2,[r0]; ldrne
      r2,[r2,#0x14]; bxne r2; bx lr` - the framework's NULL-tolerant
      CHECKED DOWNCAST, dispatching vtable slot 5 with the wanted class
      id and short-circuiting a NULL object to NULL. Slot 5 identified
      as the cast operator, NOT a nullary virtual as the earlier note
      guessed - the veneer takes a SECOND argument. Evidence - (a) the
      13 per-class accessors that tail-branch here (0x081883fc,
      0x08172124, 0x08100b74, 0x08103598, 0x08103b9c, 0x0812f0b8,
      0x081353fc, 0x0815e1c4, 0x0815ff48, 0x0827f230, 0x08284e44,
      0x082896a4, 0x0828ae80) each pass the SAME class id they just
      looked up in the registry; (b) r1 across the 414 bl sites is a
      0x80-aligned constant from the class-id space (0x6800 at 126
      sites, then 0x1700, 0x1180, 0x1100, 0x0f00, 0x3500, 0x2b00,
      0x6380 - the last two are registered class ids); (c) the two
      functions immediately before it in the image are textbook castTo
      bodies - 0x08275b90 `cmp r1,#1; movne r0,#0; bx lr` (root class
      id 1) and 0x08275bcc `cmp r1,#0x5d00; cmpne r1,#1; movne r0,#0;
      bx lr`. Ported through the object's own vtable pointer, so
      subclass vtables work; no hook table.

  - address: 0x08258c58
    ghidra: FUN_08258c58
    name: string_id_record_default_construct
    size: 36
    sig: "StringIdRecord *string_id_record_default_construct(StringIdRecord *this)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, DAT_08258c78: STRING_ID_RECORD_VTABLE_ADDRESS, DAT_08258c7c: STRING_OBJECT_VTABLE_ADDRESS}
    notes: >-
      49 bl call sites (binary-scanned). 28 code bytes + two 4-byte
      literal-pool words @ 0x08258c78 (= 0x089a76f0) and 0x08258c7c
      (= 0x089a6044), both binary-verified against osos.dec. The
      DEFAULT constructor of the 0x10-byte polymorphic (string, id)
      record class whose destructor is string_id_record_destroy @
      0x08258c80 (ported beside it): one `stmia r0, {r1, r2}` plants
      BOTH vtables (the class vtable at +0 and the embedded
      StringObject's vtable at +4 - inlining string_default_construct,
      no call), `mvn r1, #0x0` stores -1 at +0xc (the default id),
      `mov r2, #0x0` NULLs the payload word at +8 (id stored BEFORE
      the payload - reproduced), returns this (r0 untouched, the ADS
      constructor convention). No allocation, no NULL guard. Siblings
      NOT ported here: assignment @ 0x08258c9c, equality @ 0x08258cc4
      (full family context in the 0x08258c80 entry; the (string,id)
      ctor @ 0x08258c08 and copy ctor @ 0x08258c2c are ported beside
      it). Host tests: both
      vtables/NULL payload/id -1 on a garbage record, no destructor
      body run (ops-slot recorder observes nothing), and a
      construct-then-destroy round trip. match.py structural: same
      four stores at offsets 0/4/8/12 with the two pool literals and
      the mvn/mov constants; the diff is LLVM scheduling the stores in
      offset order (the original's id-before-payload order is
      unobservable between distinct addresses) plus the crate fp frame
      - the accepted house deviations.

  - address: 0x08258c08
    ghidra: FUN_08258c08
    name: string_id_record_construct_from_string_id
    size: 32
    sig: "StringIdRecord *string_id_record_construct_from_string_id(StringIdRecord *this, const StringObject *source, i32 id)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: source, param_3: id, DAT_08258c28: STRING_ID_RECORD_VTABLE_ADDRESS}
    notes: >-
      7 bl call sites (binary-scanned: 0x0813b828, 0x0813b884,
      0x0813bd64, 0x0813bde0, 0x0813c3fc, 0x0813c478, 0x0813c9c8 - the
      name-plus-database-id record construction sites; full family
      context in the 0x08258c80 entry). 28 code bytes + the 4-byte
      literal-pool word @ 0x08258c28 (= 0x089a76f0, binary-verified
      against osos.dec). The (string, id) constructor of the 0x10-byte
      polymorphic (string, id) record class: `ldr r2,[0x08258c28];
      str r2,[r0],#0x4` plants the class vtable at +0 (the post-index
      add forms the this+4 argument, the embedded StringObject
      subobject), `bl 0x082773e0` chains to the StringObject copy
      constructor on this+4 with `source` still in r1 (the scouting
      note verified exact against osos.asm; the copy ctor's pool word
      @ 0x08277410 = 0x089a6044 also binary-verified), `sub r0,r0,#0x4`
      derives this back from the callee's return (the callee returns
      its argument), `str r4,[r0,#0xc]` stores the id argument (saved
      in r4 at entry). No allocation of its own, no NULL guard.
      Deviations: the class vtable is the modeled static
      STRING_ID_RECORD_VTABLE (ROM address, the sibling precedent);
      the StringObject copy ctor @ 0x082773e0 is NOT ported (its own
      payload-duplication callee @ 0x08276474 allocates and dispatches
      through vtable slot +0xc), so the chain goes through the new
      STRING_OBJECT_COPY_CONSTRUCT dispatch slot (the util/inner_state.rs
      INNER_MATERIALIZE_COUNT pattern) whose default stub is the copy
      ctor's empty-construction prefix (StringObject vtable + NULL
      payload, source ignored); the port derives this from the callee's
      return minus size_of::<usize>() (one vtable word) so the
      original's dataflow holds on 64-bit hosts too. Siblings NOT
      ported here: assignment @ 0x08258c9c, equality @ 0x08258cc4 (the
      copy ctor @ 0x08258c2c is ported beside it). Host tests: vtable planted before the
      dispatch (recorder reads the +0 word through subobject-1), the
      embedded subobject and source forwarded verbatim, the id stored
      (edge values -1/i32::MIN/i32::MAX/0/0x1f03), the return derived
      from the callee's return, the default stub's empty-string
      construction leaving the source byte-identical, and a
      construct-then-destroy round trip. match.py structural: same
      push/mov/ldr-pool/str-postindex/call/sub/str/pop shape; the diff
      is the crate fp frame, the blx-through-volatile-slot dispatch
      standing in for the direct bl 0x082773e0 (the documented
      deviation), and LLVM scheduling the id store before the sub
      (str r4,[r0,#8] on the not-yet-subtracted r0 - same effective
      +0xc address, unobservable reorder of an independent store).

  - address: 0x08258c2c
    ghidra: FUN_08258c2c
    name: string_id_record_copy_construct
    size: 40
    sig: "StringIdRecord *string_id_record_copy_construct(StringIdRecord *this, const StringIdRecord *source)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: source, DAT_08258c54: STRING_ID_RECORD_VTABLE_ADDRESS}
    notes: >-
      15 bl call sites (binary-scanned). 36 code bytes + the 4-byte
      literal-pool word @ 0x08258c54 (= 0x089a76f0, binary-verified
      against osos.dec). The COPY constructor of the 0x10-byte
      polymorphic (string, id) record class whose (string,id)
      constructor @ 0x08258c08, default constructor @ 0x08258c58 and
      destructor @ 0x08258c80 are ported beside it (full family context
      in the 0x08258c80 entry): `mov r4, r1` saves the source record,
      `ldr r1,[0x08258c54]; str r1,[r0],#0x4` plants the class vtable
      at +0 (the post-index add forms the this+4 argument, the embedded
      StringObject subobject), `add r1, r4, #0x4; bl 0x082773e0` chains
      to the StringObject copy constructor on this+4 with the SOURCE
      RECORD's embedded subobject (source+4, not the source record
      itself), `ldr r1,[r4,#0xc]` loads the source id, `sub r0,r0,#0x4`
      derives this back from the callee's return (the callee returns
      its argument), `str r1,[r0,#0xc]` copies the id. No allocation of
      its own, no NULL guard on this or source. Deviations: the same
      three as the (string,id) sibling - the class vtable is the
      modeled static STRING_ID_RECORD_VTABLE (ROM address, the sibling
      precedent); the StringObject copy ctor @ 0x082773e0 is NOT ported
      (its payload-duplication callee @ 0x08276474 allocates and
      dispatches through vtable slot +0xc), so the chain goes through
      the existing STRING_OBJECT_COPY_CONSTRUCT dispatch slot whose
      default stub is the copy ctor's empty-construction prefix
      (StringObject vtable + NULL payload, source ignored); the port
      derives this from the callee's return minus size_of::<usize>()
      (one vtable word) so the original's dataflow holds on 64-bit
      hosts too. Siblings NOT ported here: assignment @ 0x08258c9c,
      equality @ 0x08258cc4. Host tests: vtable planted before the
      dispatch (recorder reads the +0 word through subobject-1), the
      embedded subobject and the SOURCE's embedded subobject forwarded
      verbatim, the id copied (edge values -1/i32::MIN/i32::MAX/0/
      0x1f03), the return derived from the callee's return, the default
      stub's empty-string construction leaving the source
      byte-identical while the id copy still lands, and a
      copy-construct-then-destroy round trip. match.py structural: same
      push/mov/ldr-pool/str-postindex/add/call/ldr/sub/str/pop shape;
      the diff is the crate fp frame, the blx-through-volatile-slot
      dispatch standing in for the direct bl 0x082773e0 (the documented
      deviation), and LLVM scheduling the id store before the sub
      (str r1,[r0,#8] - same effective +0xc address, unobservable
      reorder of an independent store, as the sibling).

  - address: 0x08258c80
    ghidra: FUN_08258c80
    name: string_id_record_destroy
    size: 24
    sig: "StringIdRecord *string_id_record_destroy(StringIdRecord *this)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, DAT_08258c98: STRING_ID_RECORD_VTABLE_ADDRESS}
    notes: >-
      113 bl call sites (binary-scanned). 20 code bytes + the 4-byte
      vtable literal @ 0x08258c98 (= 0x089a76f0, binary-verified). The
      plain (non-deleting) destructor of a 0x10-byte polymorphic
      (string, id) record class built on the two-word StringObject
      class: `ldr r1,[0x08258c98]; str r1,[r0],#0x4` (plants the class
      vtable at +0, the post-index add forms the this+4 argument),
      `bl 0x08277484` (the ported string_object_destroy on the
      embedded StringObject subobject at +4/+8), `sub r0,r0,#0x4`
      (the callee returns its argument, so r0-4 = this). NOT a
      constructor - the callee is the StringObject DESTRUCTOR; the
      scouting shape notes had it backwards. The class is
      unidentified, the name structural (pair_header precedent); its
      0x08258cxx family: default ctor @ 0x08258c58 (`stmia` plants
      BOTH vtables - 0x089a76f0 at +0 and StringObject's 0x089a6044
      at +4, all four literal-pool words binary-verified - NULLs the
      +8 payload, stores -1 at +0xc), (string,id) ctor @ 0x08258c08
      and copy ctor @ 0x08258c2c (both chain to the StringObject copy
      ctor @ 0x082773e0 on this+4), assignment @ 0x08258c9c, equality
      @ 0x08258cc4 (compares via the ported string_object_c_str @
      0x082a50b0 plus the +0xc words). Sampled call sites: records
      are returned BY VALUE (sret) from virtual slot +0x13c of menu/UI
      objects and consumed through slot +0x44 (0x081026b4, 0x0813b9d4
      sites), heap-allocated via operator_new(0x10) @ 0x0807f160 and
      appended to the list at +0x60 of the element_table manager @
      0x08105ffc (ported, app/element_table), and constructed from a
      formatted name string plus a database id @ 0x0813b828/0x0813b884
      (0x080528ec reads the record store at r4+0x40). Vtable @
      0x089a76f0 is eighteen serialized words (modeled verbatim in the
      port): slot 0 is 0x082559ac, a get-descriptive-string query
      over ids 0x1f00-0x1f03 (the GLES VENDOR/RENDERER/VERSION/
      EXTENSIONS enums - "Hans-Martin Will", "Software", "OpenGL
      ES-CM 1.1", else stores 0x500 = GL_INVALID_ENUM and returns
      NULL) - a GL-flavoured named-object record; the rest undecoded.
      The 0x08258d00 neighbor plants 0x089a76fc (vtable+0xc) - a
      different 0x10-byte tagged-value class tail-merged onto the same
      storage. Port plants the modeled static vtable (ROM address a
      host cannot reproduce, house pattern), calls the ported
      string_object_destroy directly, and derives the return as the
      callee's return minus one size_of::<usize>() word (the
      original's sub r0,r0,#4 dataflow, value-identical on 64-bit
      hosts). match.py structural: same literal load / post-index
      vtable store / destroy call / return-this; the diff is LLVM's
      fp frame and LLVM folding the -4 into `mov r0, r4` (the
      documented deviation).

  - address: 0x08258c9c
    ghidra: FUN_08258c9c
    name: string_id_record_assign
    size: 40
    sig: "StringIdRecord *string_id_record_assign(StringIdRecord *this, const StringIdRecord *source)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: source}
    notes: >-
      12 bl call sites (binary-scanned: five consecutive @
      0x08177d7c-0x08177dac, five consecutive @ 0x0817a3dc-0x0817a40c,
      0x0822a164, 0x0822a2a8). 40 bytes, all code - no literal-pool
      word (it loads no vtable literal, unlike every ctor/dtor
      sibling). The ASSIGNMENT operator of the 0x10-byte polymorphic
      (string, id) record class whose three constructors and
      destructor are ported beside it (full family context in the
      0x08258c80 entry): `mov r5,r1; mov r4,r0` saves source/this,
      `add r0,r0,#0x4; add r1,r1,#0x4; bl 0x082774a8` chains to the
      StringObject assignment operator on this+4 with the SOURCE
      RECORD's embedded subobject (source+4, not the source record
      itself), `ldr r0,[r5,#0xc]; str r0,[r4,#0xc]` copies the id,
      `mov r0,r4` returns the saved this - NOT the callee's return,
      unlike the ctor siblings' `sub r0,r0,#0x4` derivation. NO vtable
      store (assignment never replants) and no self-assignment guard
      at this level (the guard lives inside the StringObject operator
      @ 0x082774a8: `cmp r0,r1` with the whole body conditional -
      `ldrne r1,[r1,#4]; movne r0,r4; blne 0x08276474` reassigning the
      payload from the source's payload word). The StringObject
      assignment operator @ 0x082774a8 is NOT ported (its payload
      reassignment helper @ 0x08276474 allocates and dispatches
      through vtable slots +0x8/+0xc), so the chain goes through the
      new STRING_OBJECT_ASSIGN dispatch slot (the
      STRING_OBJECT_COPY_CONSTRUCT pattern) whose default stub is the
      real operator's self-assignment-guard prefix - the cmp guard and
      the return, the unported helper call skipped, both objects
      untouched; the real port of 0x082774a8 replaces the stub when it
      lands. Neither of the ctor siblings' other deviations applies:
      no vtable is stored (nothing to model) and the original returns
      its own argument (no return derivation needed, exact on 64-bit
      hosts). Sibling NOT ported here: equality @ 0x08258cc4. Host
      tests: the dispatch receives the embedded subobject and the
      SOURCE's embedded subobject (recorder reads the +0 word through
      subobject-1 and observes the sentinel - no vtable store precedes
      the chain, the inverse of the ctor checks), the id copied (edge
      values -1/i32::MIN/i32::MAX/0/0x1f03), the return being the
      saved this even when the mock returns a bogus pointer, the
      default stub leaving both objects untouched except the id,
      self-assignment as a byte-identical no-op, and an
      assign-then-destroy round trip. match.py structural: same
      push/mov/add/add/call/ldr/str/mov/pop shape; the diff is the
      crate fp frame and the blx-through-volatile-slot dispatch
      standing in for the direct bl 0x082774a8 (the documented
      deviation).

  - address: 0x08258cc4
    ghidra: FUN_08258cc4
    name: string_id_record_equals
    size: 56
    sig: "int string_id_record_equals(const StringIdRecord *this, const StringIdRecord *source)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: source}
    notes: >-
      5 bl call sites (binary-scanned: five consecutive @
      0x081978ec-0x0819793c, comparing the records at
      +0x18/+0x28/+0x38/+0x48/+0x58 of two larger structures, each
      result consumed as a bool by `cmp r0,#0x0`). 56 bytes, all code
      - no literal-pool word (like the assignment sibling, it loads no
      vtable literal). The EQUALITY operator of the 0x10-byte
      polymorphic (string, id) record class whose three constructors,
      destructor and assignment operator are ported beside it (full
      family context in the 0x08258c80 entry): `mov r4,r0; mov r5,r1`
      saves this/source, `add r0,r1,#0x4; bl 0x082a50b0` runs the
      ported string_object_c_str over the SOURCE RECORD's embedded
      subobject (never NULL - a NULL source payload yields the shared
      empty string), `mov r1,r0; ldr r0,[r4,#0x8]; bl 0x08276d64`
      compares this's RAW payload word (NULL passed through untouched -
      the substitution guard lives inside the comparator, an asymmetry
      with the source side) against the source's C string through the
      UTF-8 comparator @ 0x08276d64 (identified below, NOT ported), and
      only when that returns 0 (`cmp r0,#0x0` with the rest
      conditional) are the id words compared (`ldreq r0,[r4,#0xc];
      ldreq r1,[r5,#0xc]; cmpeq r0,r1`), yielding 1 on equality, 0
      otherwise (`moveq r0,#0x1; movne r0,#0x0` - the whole register
      written, so the port returns the C++ bool widened to i32, exact).
      NO vtable store and no NULL guard on this/source. Deviation: the
      UTF-8 comparator @ 0x08276d64 dispatches through the new
      UTF8_STRCMP_SAFE slot (the STRING_OBJECT_COPY_CONSTRUCT pattern)
      whose default stub is a plain byte-wise strcmp behind the
      original's own NULL guard (its literal-pool word @ 0x08276db0
      holds 0x083e2e3a - the SAME shared empty string_object_c_str
      falls back to, binary-verified against osos.dec) - exact whenever
      both strings are pure ASCII (the UTF-8 decode @ 0x08276214
      collapses to the identity for bytes < 0x80), divergent only on
      multibyte sequences; the real port of 0x08276d64 replaces the
      stub when it lands. The ported string_object_c_str is called
      directly (no deviation on the source side). Host tests: the
      dispatch receives this's raw payload verbatim (NULL included)
      against the source's c_str result (NULL payload -> the modeled
      shared empty), a nonzero comparator result yields 0 even with
      matching ids, equal strings fall through to the id compare (edge
      values -1/i32::MIN/i32::MAX/0/0x1f00-0x1f03), the return is
      exactly 0/1, the default stub's byte-wise strcmp semantics
      (difference of the first differing pair, prefix-vs-NUL, the NULL
      -> shared-empty substitution on both arguments), and two
      end-to-end runs through the default stub (same content in
      different buffers; two NULL-payload default records equal until
      the id differs). match.py structural: same push/mov/add/bl/mov/
      ldr/call/cmp/conditional-id-loads/0-or-1-result shape; the diff
      is the crate fp frame, the blx-through-volatile-slot dispatch
      standing in for the direct bl 0x08276d64 (the documented
      deviation), and LLVM forming the id equality as sub/clz/lsr
      instead of cmpeq/moveq/movne (same 0/1 result).

  - address: 0x08259928
    ghidra: FUN_08259928
    name: settings_get
    size: 44
    sig: "void *settings_get(void)"
    module: cxx/settings
    status: ported
    renames:
      piVar1: cache (pointer to the cache word @ 0x089cc948)
      iVar2: constructed (the ctor's return)
      FUN_082aadd4: operator_new (ported in heap/veneers, called directly)
      FUN_08259eac: settings_ctor (unported; SETTINGS_CTOR dispatch slot, documented zeroing default)
    vars: {DAT_08259954: SETTINGS_INSTANCE_PTR (the pool literal holding &SETTINGS_INSTANCE; the cache word itself lives @ 0x089cc948)}
    notes: >-
      Lazy singleton getter, 61 bl call sites. If the cache word @
      0x089cc948 (pool literal @ 0x08259954) is 0, allocates 0x84 bytes
      via operator_new @ 0x082aadd4 (already ported, called directly),
      constructs via FUN_08259eac, caches the ctor's return and RELOADS
      the slot before returning (a NULL ctor result caches NULL, so the
      next call re-allocates - no failure memory). The 0x84-byte object
      is the firmware's SETTINGS STORE: its virtual setters (the
      0x08259970/0x082599d4/0x08259a64/0x08259da8/0x08259e18 family -
      no direct bl sites, so vtable-only) clamp small enum values into
      byte fields and post change notifications 0x50009-0x50013 to the
      listener list the ctor builds at +0x04 (dispatch through vtable
      slot +0x14), a string-keyed byte map sits at +0x40 (read @
      0x08259ac8/0x08259d24, write @ 0x08259b3c, iterate @ 0x08259b5c),
      and 0x08259e48 posts the 0x50001/0x50002 pair through slot +0x10.
      The event consumer @ 0x08172744 is a settings view: it re-reads
      the changed value (slots +0xc0..+0x108), repaints rows
      (resources 0x60bd/0x60f0-0x60f3), and mirrors enum values into
      the media-player interface (slot +0x30) - the repeat/shuffle
      style playback settings. The ctor plants primary vtable
      0x089a75c8 at +0x00 (the SAME interface base the TPodMediaPlayer
      carries at +0x14, see 0x0817ceb4) and the extended vtable
      0x089a7b14 over it, and names nothing (no class-name-factory
      call), so settings is the semantic name, not a recovered class
      name. Sibling getters fill the block: 0x082598f8 (0x44 bytes,
      ctor 0x08259450), 0x0825a16c / 0x0825a6b8 / 0x0825a6e8 (0x44,
      ctors 0x08259648 / 0x0825a8d4 / 0x0825aa58). Deviations (the
      app/singletons.rs contract): the cache is the crate static
      SETTINGS_INSTANCE - the 0x089cxxxx RW pages are
      runtime-initialized and the image holds stale UI strings there -
      and the ctor rides the SETTINGS_CTOR dispatch slot (house
      pattern) with a documented zeroing stub default. NOT HOOK-READY -
      until FUN_08259eac is ported the default hands out a zeroed block
      with no vtable and no listener list, so branching stock code at
      0x08259928 would break it. match.py structural: same
      push/ldr-pool/ldr/cmp/bne/mov-0x84/bl/bl/str/ldr/pop skeleton;
      the diff is the crate fp frame, the intra-archive bl for
      operator_new, and the blx-through-volatile-slot dispatch standing
      in for the direct bl 0x08259eac (the documented deviation).

  - address: 0x0825c1a0
    ghidra: FUN_0825c1a0
    name: plist_node_child_vector_push_back
    size: 48
    sig: "PlistNode *plist_node_child_vector_push_back(PlistNode *owner, const PlistNode *source)"
    module: fp/fp_misc
    status: ported
    vars: {param_1/r0: owner, param_2/r1: source, piVar1/r0: children, uVar9/r3: end, uVar6/r2: capacity, uStack_20/r2: next_end}
    notes: >-
      Raw ARM is exactly 12 instructions at 0x0825c1a0-0x0825c1cc;
      FUN_0825c1d0 starts immediately after it. It is the
      vector<PlistNode>::push_back fast path: owner+0x14 is the
      {begin,end,capacity} child vector, each element is 0x28 bytes, and
      the spare-capacity path stores end+0x28 before tail-branching to
      FUN_0825c61c(destination, source). If end is NULL it stores 0x28
      then returns NULL without construction. When end == capacity it
      rearranges r0/r1/r2 to
      FUN_083e32ac(&owner->children, end, source), an unported
      growth/insert helper, and tail-branches there. A complete raw ARM
      B/BL decode finds 14 incoming plain bl sites (0x08272ca0,
      0x08272d30, 0x08272e78/84, 0x08272f08/78/88, 0x08273178,
      0x082731e4, 0x082732bc, 0x08273330/a4/b0/bc), with no predicated
      bl or incoming tail branch. Both direct callees remain unported:
      device builds reach their exact retailOS addresses through a
      volatile operation table and host tests install recorders.
  - address: 0x0825c2c0
    ghidra: FUN_0825c2c0
    name: cxx_vector_find_equal
    size: 80
    sig: "int cxx_vector_find_equal(const CxxStringPairVector *owner, const char *const *needle, CxxStringPair **out)"
    module: cxx/templates
    status: ported
    vars: {param_1/r0: owner, param_2/r1: needle, param_3/r2: out, iVar2/r4: record}
    notes: >-
      Raw ARM (0x0825c2c0-0x0825c30f) loads begin/end from owner+4/+8,
      scans the half-open range as 8-byte records, and returns exactly 1
      after writing the first match to *out; a miss returns 0 without
      storing. The 4-byte prefix is not read. The only callee,
      FUN_083eac08 @ 0x083eac08, is the unported COW
      basic_string::operator==: it reads both _Rep lengths from data-4,
      rejects unequal lengths, then calls the ported memcmp @ 0x08030f64
      over the common length and maps a zero result to 1. Its redundant
      post-compare length ordering cannot change the result after the
      equality gate, so cxx_vector_find_equal represents it exactly with
      the established memcmp seam rather than a comparator stub. Callers
      in 0x0815aeac, 0x0815b31c, and 0x0815b478 construct a one-word COW
      string needle, release it after the call, and consume the found
      record's +4 string word, independently identifying the record and
      needle ABI. Host tests cover found, miss/no out-store, first match,
      and the exclusive end bound.

  - address: 0x0825c870
    ghidra: FUN_0825c870
    name: utf8_encode_codepoint
    size: 140
    sig: "void utf8_encode_codepoint(char *destination, unsigned int codepoint)"
    module: cxx/string_object
    status: ported
    vars: {param_1/r0: destination, param_2/r1: codepoint, uVar3/r2: final_continuation, bVar2/r12: second_continuation}
    notes: >-
      Raw ARM establishes ordinary one/two/three/four-byte output for
      normal inputs, but only the ASCII test (`ls`) is unsigned. The
      0x800, 0x10000, and 0x110000 tests are signed `blt`: values with
      bit 31 set therefore emit a two-byte prefix derived from an
      arithmetic right shift, while 0x110000..0x7fffffff stores only
      destination[0] = NUL. Surrogates are accepted as three-byte
      forms. The four-byte path reuses the three-byte second
      continuation (`codepoint >> 6`, rather than bits 12..17), then
      stores only +0, +1, +3, +4; destination[2] is untouched instead
      of receiving a third continuation byte. The function returns void.

  - address: 0x0825d318
    ghidra: FUN_0825d318
    name: xml_decode_skip_whitespace
    size: 60
    sig: "unsigned int xml_decode_skip_whitespace(void **reader_slot)"
    module: util/xml_decode_skip_whitespace
    status: ported
    renames:
      param_1/r0/r4: reader_slot
      uVar1/r2/r0: codepoint
      FUN_0825d5dc: xml_decode_codepoint (unported seam)
      FUN_0825d7c4: xml_decode_codepoint_and_reset (ported)
      FUN_0825d2fc: xml_codepoint_is_whitespace (unported seam)
    notes: >-
      Raw ARM is exactly 0x0825d318..0x0825d353: it loads *reader_slot and
      calls 0x0825d5dc once without clearing decoder state, duplicates the
      returned codepoint into r1/r2 for 0x0825d2fc, and on a nonzero predicate
      calls 0x0825d7c4 before reclassifying. Thus an initially non-whitespace
      codepoint leaves the decoder state as the raw decoder left it, whereas a
      terminal codepoint reached after whitespace is reset. The error/EOF
      sentinel 0xffffffff is tested by the predicate and returned normally.
      The next function begins at 0x0825d354. Decoding every B/BL word in
      osos.dec finds 10 static bl call sites, all unconditional. The two
      unported direct callees use their existing volatile seams: fixed raw
      firmware addresses on target and test-installed host callbacks.

  - address: 0x0825d354
    ghidra: FUN_0825d354
    name: xml_skip_whitespace
    size: 48
    sig: "unsigned int xml_skip_whitespace(void **reader_slot)"
    module: util/xml_skip_whitespace
    status: ported
    renames:
      param_1/r0/r4: reader_slot
      uVar1/r2/r0: codepoint
      FUN_0825d7c4: xml_decode_codepoint_and_reset (ported)
      FUN_0825d2fc: xml_codepoint_is_whitespace (unported seam)
    notes: >-
      Raw ARM is exactly 0x0825d354..0x0825d383: save reader_slot,
      load *reader_slot, call 0x0825d7c4, duplicate its codepoint into
      r1/r2, call the 0x0825d2fc predicate, and repeat while its result
      is nonzero. It returns the first non-whitespace codepoint unchanged,
      including the 0xffffffff decoder error/EOF sentinel. The confirmed
      extent ends before 0x0825d384. Decoding every B/BL word in osos.dec
      finds 26 static bl call sites: 25 unconditional plus one blne at
      0x0825d180. The decode-and-reset callee is ported in
      util/xml_decode_codepoint_and_reset.rs. The remaining direct callee
      uses a volatile seam: its fixed raw address on target and a
      test-installed host callback.
  - address: 0x0825d7c4
    ghidra: FUN_0825d7c4
    name: xml_decode_codepoint_and_reset
    size: 24
    sig: "unsigned int xml_decode_codepoint_and_reset(struct XmlUtf8Decoder *reader)"
    module: util/xml_decode_codepoint_and_reset
    status: ported
    renames:
      param_1/r0/r4: reader
      FUN_0825d5dc: xml_decode_codepoint (unported seam)
      param_1+4: reader.state
    notes: >-
      Raw ARM is exactly 0x0825d7c4..0x0825d7db: save reader, bl
      0x0825d5dc, set reader.state (+4) to zero, and return the decoder's
      unchanged r0 result. The following function starts at 0x0825d7dc.
      Decoding every B/BL word in osos.dec finds 11 static bl call sites:
      all 11 are unconditional, with no predicated direct calls. The decoder
      at 0x0825d5dc is not yet ported; this port reaches its fixed firmware
      address on target and uses a volatile callback seam in host tests.
  - address: 0x08276d64
    ghidra: FUN_08276d64
    name: utf8_strcmp_safe
    size: 56
    sig: "int utf8_strcmp_safe(const char *a, const char *b)"
    module: cxx/string_object
    status: ported
    vars: {param_1: a, param_2: b, local_c: a_cursor, local_10: b_cursor, iVar1: codepoint_a, iVar2: codepoint_b, DAT_08276db0: UTF8_STRCMP_SAFE_EMPTY_CSTR_ADDRESS}
    notes: >-
      ~20 bl call sites (decomp/c grepped: 0x0816c58c, 0x0816ddb4,
      0x0816ed0c, 0x08172afc, 0x0817f72c, 0x081bdb6c, 0x0828a9d4,
      0x0828b50c, 0x082a1df8, 0x082aad4c, 0x082aad94, 0x082c6ef0,
      0x083d6550, ... plus the ported record equality operator @
      0x08258cc4). NULL-guarded UTF-8-aware strcmp: a NULL argument is
      substituted with the shared empty C string (literal-pool word @
      0x08276db0 holds 0x083e2e3a - the SAME address
      string_object_c_str @ 0x082a50b0 falls back to, binary-verified),
      then both strings are walked CODEPOINT by CODEPOINT through the
      ported UTF-8 decoder @ 0x08276214, returning the first differing
      codepoint pair's difference (sub r0, r4, r0), 0 at a shared
      terminator. string_id_record_equals calls the port directly.

  - address: 0x08276214
    ghidra: FUN_08276214
    name: utf8_next_codepoint
    size: 112
    sig: "unsigned int utf8_next_codepoint(const char **cursor)"
    module: cxx/string_object
    status: ported
    vars: {param_1: cursor, pbVar1: sequence, uVar2: lead_or_codepoint, uVar3: second_byte}
    notes: >-
      Advances *cursor past one UTF-8 sequence and returns the decoded
      codepoint: ASCII (< 0x80) returned as-is; (b & 0xe0) == 0xc0
      decodes a 2-byte sequence ((b & 0x1f) << 6 | b1 & 0x3f);
      (b & 0xf0) == 0xe0 decodes a 3-byte sequence ((b & 0xf) << 12 |
      (b1 & 0x3f) << 6 | b2 & 0x3f); any other lead byte (4-byte
      sequences included) consumes THREE bytes and returns 0 - reading
      as a string terminator to its callers. Sole user in the equality
      path: the UTF-8 strcmp @ 0x08276d64 above.

  - address: 0x08276288
    ghidra: FUN_08276288
    name: utf8_prev_codepoint
    size: 116
    sig: "unsigned int utf8_prev_codepoint(const char **cursor)"
    module: cxx/string_object
    status: ported
    vars: {param_1: cursor, iVar1: sequence_end, uVar2: final_byte, uVar3: leading_byte, uVar4: preceding_byte}
    notes: >-
      Moves *cursor backward over one reverse-decoded UTF-8 sequence.
      The final byte is returned unchanged when ASCII. Otherwise the
      preceding byte selects a 2-byte form only for 0b110xxxxx; failing
      that, the third byte back selects a 3-byte form only for
      0b1110xxxx. Payload bytes are masked without continuation,
      overlong, or surrogate validation. Every failed high-bit
      lookbehind (including a malformed final lead and the tail of a
      4-byte sequence) consumes three bytes and returns 0. The function
      is a raw pointer cursor operation: invalid cursor or unreadable
      lookbehind faults as in retailOS.

  - address: 0x082762fc
    ghidra: FUN_082762fc
    name: utf16_utf8_byte_len_plus1
    size: 60
    sig: "int utf16_utf8_byte_len_plus1(const unsigned short *utf16)"
    module: cxx/string_object
    status: ported
    vars: {param_1: utf16, uVar1: code_unit, puVar2: next_code_unit, iVar3: utf8_byte_len}
    notes: >-
      Counts the UTF-8 byte length implied by NUL-terminated UTF-16 code
      units and includes one byte for the output NUL. NULL input returns 1.
      Values below 0x80 count as 1 byte, values below 0x800 as 2, and all
      remaining nonzero 16-bit values as 3. It does not combine surrogate
      pairs, so each surrogate counts as a separate 3-byte unit. The
      original's signed 32-bit sum wraps on overflow; the Rust port uses
      wrapping_add for that target-machine behavior.

  - address: 0x08276338
    ghidra: FUN_08276338
    name: utf16_utf8_byte_len_bounded_plus1
    size: 72
    sig: "int utf16_utf8_byte_len_bounded_plus1(const unsigned short *utf16, int max_code_units)"
    module: cxx/string_object
    status: ported
    vars: {param_1: utf16, param_2: max_code_units, uVar1: code_unit, iVar2: utf8_byte_len}
    notes: >-
      Counts the UTF-8 byte length implied by up to max_code_units UTF-16
      code units, including one byte for the output NUL. NULL input, zero,
      and negative bounds return 1. The bounded loop stops at either a
      UTF-16 NUL or the positive limit. Values below 0x80 count as 1 byte,
      values below 0x800 as 2, and all remaining nonzero 16-bit values as 3;
      surrogate pairs are not combined. The count and bound decrement happen
      only after a nonzero unit is processed. The signed 32-bit sum,
      including the final NUL, wraps on overflow.

  - address: 0x08276380
    ghidra: FUN_08276380
    name: utf8_codepoint_byte_width
    size: 28
    sig: "unsigned int utf8_codepoint_byte_width(unsigned int codepoint)"
    module: cxx/string_object
    status: ported
    vars: {param_1: codepoint, uVar1: byte_width}
    notes: >-
      The unsigned codepoint-to-UTF-8 byte-width classifier. The original
      first tests `0x7f < codepoint`: values 0x00 through 0x7f return 1.
      Larger values then test `codepoint < 0x800`: 0x80 through 0x7ff
      return 2, and 0x800 through 0xffffffff return 3. No Unicode scalar
      validation occurs, so surrogates and out-of-range values take the
      three-byte branch. The `u32` Rust argument preserves the original
      unsigned comparison behavior.

  - address: 0x0827639c
    ghidra: FUN_0827639c
    name: string_object_assign_cstr
    size: 100
    sig: "void string_object_assign_cstr(StringObject *this, const char *source)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: source, piVar1: source_first_byte, iVar2: destination}
    notes: >-
      Source: decomp/c/026/0827639c_FUN_0827639c.c. The function tests
      source itself and then its first byte, so NULL and empty strings
      dispatch only virtual slot +0xc with this. Nonempty input calls the
      NULL-safe strlen at 0x082770bc, adds the NUL byte, and invokes virtual
      slot +0x8 with (this, size, 0). A NULL result returns immediately:
      no source copy and no +0xc fallback. A non-NULL result receives the
      source copy through DAT_08037db4, including the terminator. The source
      remains caller-owned; the virtual slots own destination replacement and
      release protocol. Those two unported virtual callees are an injectable
      STRING_OBJECT_ASSIGN_CSTR_OPS boundary in the Rust port, while the
      existing libc strcpy port supplies the copy seam. match.py structural:
      source NULL/first-byte branch, strlen-plus-one request, NULL-result
      return, source copy, and +0xc fallback are retained.

  - address: 0x08276474
    ghidra: FUN_08276474
    name: string_object_assign_payload
    size: 100
    sig: "void string_object_assign_payload(StringObject *this, const char *payload)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: payload, piVar1: payload_first_byte, uVar2: requested_size, iVar3: destination}
    notes: >-
      Source: decomp/c/026/08276474_FUN_08276474.c. Called by the
      StringObject copy assignment operator (0x082774a8) with the
      source object's payload. NULL and empty payloads dispatch only
      virtual slot +0xc with this. A nonempty payload calls the
      NULL-safe inclusive-length helper at 0x08275e20, then invokes
      virtual slot +0x8 as (this, requested_size, 0). A NULL allocation
      result returns directly; otherwise DAT_08037db4 copies the payload
      including its NUL terminator. The +0x8 virtual method owns prior
      payload replacement/free bookkeeping, while this helper neither
      frees nor stores the payload field. The unported virtual callees
      are represented by the shared injectable STRING_OBJECT_ASSIGN_CSTR_OPS
      boundary; the existing libc strcpy port supplies the copy seam.
      match.py structural: NULL/empty branch, inclusive-size request,
      NULL-result return, source copy, and +0xc fallback are retained.

  - address: 0x08275d00
    ghidra: FUN_08275d00
    name: utf8_strcpy_bounded
    size: 116
    sig: "u32 utf8_strcpy_bounded(u8 *dst, const u8 *src, u32 limit)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: dst
      param_2: src
      param_3: limit
    notes: >-
      29 bl call sites (binary-scanned, all unconditional - no predicated
      forms). Ghidra's 116-byte extent confirmed against raw bytes: the
      next function (string_object_release_payload) starts @ 0x08275d74.
      Bounded NUL-terminated string copy that never splits a UTF-8
      sequence. Each iteration decodes one codepoint with the ported
      utf8_next_codepoint @ 0x08276214, classifies its encoded width
      with the ported utf8_codepoint_byte_width @ 0x08276380, and copies
      the sequence's RAW source bytes (trailing pointer to
      decoder-advanced cursor, not a re-encode) only while
      width < remaining, so one byte always stays in reserve for the
      terminator. On overflow it stores a NUL at the write cursor and
      returns total + 1; on the string terminator the NUL travels the
      same byte-copy path and is likewise counted, so the return value
      is bytes WRITTEN including the terminator, not strlcpy's would-be
      length. limit == 0 writes nothing and returns 0. Quirk retained:
      a four-byte or otherwise malformed lead makes the decoder consume
      three bytes and return 0, so the copy loop emits those three raw
      bytes, ends the copy with NO terminator written, and counts only
      width(0) == 1 for them. Callers: the 0x080d28e0 cluster copies
      "Str " resource strings into 0x20/0xa-byte fields (see
      resource_chain_find_string @ 0x0827239c) and 0x081ed670 fills a
      0x300-byte field. Both callees are real ports in the same module,
      called directly - no dispatch seam. match.py structural: same
      decode/width/truncate-with-NUL/copy-raw/count-and-continue loop;
      the diff is the crate fp frame and LLVM register allocation.
      Host tests (10 in cxx::string_object::tests): ASCII copy counts
      the NUL, empty string, limit 0 writes nothing, last-byte
      truncation, exact fit, 2- and 3-byte sequences copied whole or
      not at all, the malformed-lead raw-bytes-count-one quirk,
      mid-string truncation without splitting, limit 1.

  - address: 0x08275d74
    ghidra: FUN_08275d74
    name: string_object_release_payload
    size: 40
    sig: "void string_object_release_payload(StringObject *this)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this}
    notes: >-
      41 bl call sites (binary-scanned), among them both destructor
      siblings 0x08277458 and 0x08277484. The shared payload release of
      the same unidentified two-word string/buffer class as
      string_default_construct @ 0x08277440 / string_object_destroy @
      0x08277484: `ldr r0,[r0,#4]; cmp r0,#0; ldmiaeq` (NULL payload
      early-out), `mov r1,#0x34; bl 0x080e7970` (free_wrapper with
      caller tag 0x34, the TAG_STRING_OBJECT_PAYLOAD constant), `mov
      r0,#0; str r0,[r4,#4]` (NULLs the word). No NULL guard on `this`;
      returns void with r0 clobbered by the free path. Replaces the
      no-op leak stub as the wired default of the STRING_OBJECT_OPS
      release_payload slot (house ops-slot pattern; the slot stays so
      host tests can install recording mocks). match.py structural:
      same ldr-@+4 / NULL-guard / free-with-tag-0x34 / NULL-the-word
      sequence; the diff is LLVM's fp frame and the inlined
      free_wrapper lazy-init + HEAP_OPS dispatch standing in for the
      original's bl 0x080e7970 (the documented deviation).

  - address: 0x082a4b94
    ghidra: FUN_082a4b94
    name: strided_array_element_address
    size: 40
    sig: "u32 strided_array_element_address(const StridedArray *this, i32 index)"
    module: cxx/array_element_at
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: index
      param_1_plus_0x00: vtable
      vtable_plus_0x18: element_stride
      param_1_plus_0x08: storage
    notes: >-
      Raw osos.dec is exactly ten ARM words from 0x082a4b94 through
      pop {r4,r5,r6,pc} at 0x082a4bb8; the next separately linked leaf
      begins at 0x082a4bbc with ldr r0,[r0,#4]; bx lr. It saves this and
      index, calls this->vtable[+0x18](this), then returns storage +
      index * returned_stride through one mla, with all arithmetic modulo
      2^32. Decoding every ARM B/BL word finds eight direct call sites:
      0x08135250, 0x08135284, 0x08271798, 0x082a4884, 0x082a4944,
      0x082a4a38, 0x082a4b44, and 0x082a4c4c; all are plain
      unconditional bl (zero predicated calls). Two tail branches also
      target it: predicated bne at 0x082a47ac and unconditional b at
      0x082a4d0c. The vtable slot is identified only by role: sibling
      0x082a4c20 tests its return against four before choosing word-copy
      versus generic element copy. The observable-array vtable sampled at
      0x089a5d0c holds 0x08102f80 in that slot, which falls inside
      FUN_08102f44 with r4 live-in rather than decoding as an entry; no
      concrete callee identity is invented. Target code uses raw
      target-width vtable words; host tests use a named, widened vtable
      structure so native function pointers remain valid. Tests prove
      callback selection, positive and negative index multiplication,
      accumulation overflow, and array_element_at sentinel integration.
  - address: 0x082a4cf8
    ghidra: FUN_082a4cf8
    name: array_element_at
    size: 24
    sig: "u32 array_element_at(const StridedArray *this, i32 index)"
    module: cxx/array_element_at
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      Element-address accessor of the polymorphic strided-array class
      (vtable+0x18 = virtual element_size getter, +0x04 = signed count,
      +0x08 = storage base), with a last-element sentinel: 0x7fffffff
      becomes count - 1 when the signed count is positive (`cmn r1,
      #-0x7fffffff; bne; ldr r2,[r0,#4]; cmp r2,#0; subgt r1,r2,#1`),
      then tail-branches to the ported strided_array_element_address at
      0x082a4b94. 23 bl call sites, 0 predicated, binary-scanned by
      decoding every B/BL word in osos.dec. Extent verified against raw
      bytes: the next function's push starts exactly at 0x082a4d10, so
      Ghidra's 24 is right. Sentinel semantics confirmed by callers
      (FUN_081b92e4 fetches the last element as
      (array, 0x7fffffff)); slot +0x18 confirmed as the element size by
      sibling FUN_082a4c20 (`if (size != 4) <generic copy> else *out =
      *element`). The helper's host-widened vtable directly replaces the
      previous unported dispatch seam. Host tests in
      src/cxx/array_element_at.rs prove sentinel clamping, empty and
      negative-count pass-through, ordinary indices, and direct virtual
      stride calculation.

  - address: 0x082a4f58
    ghidra: FUN_082a4f58
    name: field_10_low_u16
    size: 16
    sig: "u32 field_10_low_u16(const u8 *this)"
    module: codegen/field_10_low_u16
    status: ported
    renames:
      param_1: this
    notes: >-
      Source: decomp/c/029/082a4f58_FUN_082a4f58.c. Raw ARM is
      `ldr r0,[r0,#0x10]; mov r0,r0,lsl #0x10; mov r0,r0,lsr #0x10;
      bx lr`, so the port loads the opaque record's word at +0x10 and
      returns exactly its low 16 bits, zero-extended to u32; it neither
      NULL-checks nor modifies the record. Its five direct call sites are
      the comparisons in FUN_08295e9c, against 0x6e, 0x10, 0x5a, 0x59,
      and 0x6f; that gives the recovered field an event/control-code role,
      but not enough object evidence to name the containing type. This
      body is distinct from the adjacent state-word byte-2 accessor
      wheel_event_button_code @ 0x082a4f78, not an alias. Host tests cover
      high/low-halfword combinations, boundary patterns, and aliased
      read-only access.

  - address: 0x082a4f78
    ghidra: FUN_082a4f78
    name: wheel_event_button_code
    size: 16
    sig: "u32 wheel_event_button_code(const u8 *this)"
    module: cxx/wheel_event
    status: ported
    renames:
      param_1: this
    notes: >-
      Extracts byte 2 (bits 16..23) of the state word at +0x10 of the
      control subsystem's click-wheel event sample - the record
      kernel/wheel_sample.rs's wheel_sample_capture @ 0x08292a88 fills
      (layout: kind +0x04, capture flag +0x08, elapsed +0x0c, state
      +0x10, rate +0x14). 16 bytes, all code, no literal pool
      (0x082a4f78..0x082a4f88). 51 bl call sites, binary-scanned. Raw
      ARM: `ldr r0,[r0,#0x10]; and r0,r0,#0xff0000; mov r0,r0,lsr
      #0x10; bx lr`; Ghidra (decomp/c/029/082a4f78_FUN_082a4f78.c)
      matches exactly. Class identification is call-site driven: the
      same handler cluster (0x0813f6c4.., 0x08140180..) calls
      wheel_sample_capture @ 0x08292a88 and its rate-getter sibling
      0x08292adc on the same record (0x0813f7c0/0x0813f7cc) and reads
      the elapsed word at +0x0c; at 0x0811a0b4 the same +0x10 word's
      byte 3 is flag-tested (`tst #0xff000000`, `and #0x1000000` -
      0x40000000 is the documented finger-on-wheel gate) before the
      kind == 0xd gate and this byte's dispatch. The byte is a
      small-integer CODE, not a character: range-compared (`cmp #0x59;
      bgt` @ 0x0811a0ec) and filter/dispatch-compared against
      0x4a..0x4c (skip filter @ 0x0810bdd4), 0x57/0x58 (list index
      -1/+1), 0x59/0x5a (select paths), 0x6e/0x6f; the kind word at
      +0x04 carries 8/0xd/0x15. Enum undecoded; "button code" is the
      evidenced role. NOT a string operation despite sitting among the
      StringObject accessors (ADS translation-unit packaging): the COW
      string rep (cxx/string.rs) is {refcount, capacity, length} plus
      inline data with no flags byte at +0x10, so the byte plays no
      part in any short/long string discrimination. ALIASES: the exact
      4-word pattern occurs exactly TWICE in osos.dec - here and at
      0x082a4f68 (26 bl call sites, binary-scanned), a byte-identical
      twin ADS emitted for a second translation unit (the
      handle_deref_or_null alias phenomenon), ported as
      wheel_event_button_code_alias_4f68 (its own entry below). The
      halfword sibling @
      0x082a4f58 (`ldr; lsl #0x10; lsr #0x10` - the low u16 of the
      same state word, 5 bl call sites) is a DIFFERENT body, not an
      alias, also unported. Host tests: a distinct value at each byte
      position in turn (only byte 2 survives), 0x00/0xff boundaries,
      and a read-only check with byte 3 and the low halfword at 0xff
      proving the mask. match.py structural: LLVM folds and+lsr into a
      single `ldrb [r0,#18]` - the same little-endian byte - behind
      the crate fp frame; 4 instructions vs the original's 4.

  - address: 0x082a4f68
    ghidra: FUN_082a4f68
    name: wheel_event_button_code_alias_4f68
    size: 16
    sig: "u32 wheel_event_button_code_alias_4f68(const u8 *this)"
    module: cxx/wheel_event
    status: ported
    renames:
      param_1: this
    notes: >-
      Byte-identical twin of wheel_event_button_code @ 0x082a4f78 - the
      same `ldr r0,[r0,#0x10]; and r0,r0,#0xff0000; mov r0,r0,lsr
      #0x10; bx lr` body extracting the same button-code byte (bits
      16..23) of the same wheel event sample's state word at +0x10.
      26 bl call sites, binary-scanned. ADS emitted the accessor twice
      (the parse_result_init_alias_3134 phenomenon); the call-site
      evidence shows NO semantic split - the twin's sites are the same
      control-subsystem event handlers keyed on the same codes: the
      0x57/0x58/0x5d value-adjust dispatch @ 0x0812eb1c (negate/add
      the halfword at r4+0xe8), the vtable prev/next/select dispatch @
      0x081408c0 (slots +0x16c/+0x170 on 0x57/0x58, +0x160 on 0x6f),
      the 0x4a..0x4c skip filter @ 0x0810bdd4, the command-table walk
      @ 0x08110868, and the wheel-sample statistics dispatch @
      0x0811a0e8 (which also flag-tests byte 3 of the same state word
      and gates on kind 0xd at +0x04). Ported as an independent body,
      not a delegation, exactly as the original is a second emitted
      copy rather than a branch to the first. Release-build note:
      LLVM MergeFunctions folds the two byte-identical bodies;
      wheel_event_button_code_alias_4f68 stays in the archive symbol
      table at the merged address (verified with nm), so hooks resolve
      it normally - match.py is therefore run as `match.py 0x082a4f68
      wheel_event_button_code`, which shows the same single-ldrb fold
      behind the crate fp frame, 4 instructions vs the original's 4.
      Host tests: same coverage as the canonical - per-byte-position
      extraction (only byte 2 survives), 0x00/0xff boundaries, and the
      read-only/mask proof with byte 3 and the low halfword at 0xff.

  - address: 0x082a37a4
    ghidra: FUN_082a37a4
    name: string_construct_from_vtable_slot_50
    size: 36
    sig: "void string_construct_from_vtable_slot_50(StringObject *out, StringSlot50Source *source)"
    module: cxx/string_from_vtable_slot_50
    status: ported
    renames:
      param_1/r0: out
      param_2/r1: source
      DAT_089a6044: STRING_OBJECT_VTABLE_ADDRESS
    notes: >-
      Exact raw extent is 36 bytes / nine ARM instructions from 0x082a37a4
      through the tail `bx r2` at 0x082a37c4; the separately linked next
      function begins at 0x082a37c8. It saves source in r4, calls the ported
      string_default_construct at 0x08277440 for out, then loads source's
      vtable and tail-dispatches its opaque +0x50 word as slot(source, out).
      The virtual callee has no established function identity, so this port
      preserves it as a raw typed vtable call rather than inventing a name or
      a replacement seam. Direct B/BL decoding across osos.dec found exactly
      eight direct callers at 0x080fec88, 0x08102c40, 0x081124dc, 0x08174db4,
      0x081763f0, 0x08176884, 0x08177188, and 0x081f4450: every one is a plain
      unconditional bl; none is predicated and no direct b references the
      entry. Each caller treats the interface as void despite the tail branch.
      Host coverage proves default construction happens before precisely one
      +0x50 dispatch, forwards the exact source pointer, and preserves the
      slot's output payload. Host vtable slots use native-width pointers;
      repr(C) keeps the target layout at the verified 32-bit word offsets.

  - address: 0x08277440
    ghidra: FUN_08277440
    name: string_default_construct
    size: 20
    sig: "StringObject *string_default_construct(StringObject *this)"
    module: cxx/string_object
    status: ported
    vars: {DAT_089a6044: STRING_OBJECT_VTABLE_ADDRESS}
    notes: >-
      280 bl call sites. `obj[0] = 0x089a6044; obj[1] = 0` - a two-word
      default constructor planting a vtable pointer and a NULL payload.
      Three siblings plant the SAME vtable (literal-pool words
      0x08277454/0x08277480/0x082774a4 all hold 0x089a6044, binary-
      verified) - 0x08277414 (ctor + a call to 0x08276620), 0x08277458
      (deleting destructor - vtable, 0x08275d74, then operator delete @
      0x082aad24) and 0x08277484 (990 bl call sites; vtable +
      0x08275d74, no delete) - so 0x089a6044 is one class's vtable and
      this is its trivial ctor. The class is UNIDENTIFIED: it sits in
      the 0x0827xxxx cluster next to the NULL-guarded strlen @
      0x082770bc (very likely a retailOS string/buffer class, but NOT
      the COW basic_string of cxx/string.rs), and the vtable is
      serialized in the image as six code pointers (0x0820c2dc,
      0x0821183c, 0x082116f8, 0x08213bfc, 0x08213818, 0x0820c5ec) of
      which Ghidra resolves only 0x08213bfc as a function start. Port
      models the vtable as the static STRING_OBJECT_VTABLE (pointer
      identity only, original address kept as the named constant
       STRING_OBJECT_VTABLE_ADDRESS) per the heap/pool_client.rs
       pattern. Original never writes r0, so the port returns `this`.

  - address: 0x08277484
    ghidra: FUN_08277484
    name: string_object_destroy
    size: 32
    sig: "StringObject *string_object_destroy(StringObject *this)"
    module: cxx/string_object
    status: ported
    vars: {DAT_089a6044: STRING_OBJECT_VTABLE_ADDRESS}
    notes: >-
      899 bl call sites (binary-scanned) - one of the hottest functions
      in the image. The plain (non-deleting) destructor of the same
      unidentified two-word class as string_default_construct @
      0x08277440: `obj[0] = 0x089a6044` (vtable literal @ 0x082774a4),
      then `bl 0x08275d74`, return `this` in r0; no operator delete
      (that is the 0x08277458 sibling's job). The callee 0x08275d74
      (40 bytes, ported as string_object_release_payload) is the shared
      payload release: NULL-guards
      the payload word at +4, frees it through free_wrapper @
      0x080e7970 with caller tag 0x34, then NULLs the word - hence the
      semantic name string_object_destroy. Port dispatches 0x08275d74
      through the STRING_OBJECT_OPS slot whose shipped default is the
      ported release itself (the slot stays so host tests can install
      recording mocks). match.py: same
       structure (vtable store, call, return this), the bl becoming a
       blx through the ops slot.

  - address: 0x08277458
    ghidra: FUN_08277458
    name: string_object_delete
    size: 40
    sig: "void string_object_delete(StringObject *this)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, DAT_089a6044: STRING_OBJECT_VTABLE_ADDRESS}
    notes: >-
      0 direct bl call sites (binary-scanned) - the deleting destructor
      is reached through delete expressions, not branches. 36 code
      bytes + the 4-byte vtable literal @ 0x08277480 (= 0x089a6044,
      binary-verified). The deleting-destructor sibling of
      string_object_destroy @ 0x08277484: `movs r4, r0` / `ldmiaeq
      sp!, {r4, pc}` (a NULL `this` returns untouched - unlike the
      destroy sibling there IS a NULL guard), `obj[0] = 0x089a6044`,
      `bl 0x08275d74` (the ported string_object_release_payload), then
      a tail `b 0x082aad24` with `this`. Verified against osos.asm:
      0x082aad24 is the tag-2 operator delete (`movne r1, #2; bne
      0x080e7970`), already ported as operator_delete in
      heap/veneers.rs, so the port calls it directly - the
      missing_free_p4 ops-slot rule for unported free contracts does
      not apply. The payload release dispatches through the
      STRING_OBJECT_OPS.release_payload slot (wired default: the ported
      release) exactly like the destroy sibling. Port returns void -
      the original tail-branches into the delete without rewriting r0,
      so the caller never sees a usable `this`. match.py structural:
      same NULL-guard early-out / vtable store / release call / tail
      branch into the delete; the diff is LLVM's fp frame and the bl
      becoming a blx through the ops slot (the documented deviation).

  - address: 0x08278f60
    ghidra: FUN_08278f60
    name: silver_controller_transition_addon_destroy
    size: 52
    sig: "u8 *silver_controller_transition_addon_destroy(u8 *this)"
    module: cxx/transition_addon
    status: ported
    renames:
      param_1: this
      DAT_08278f94: TRANSITION_ADDON_VTABLE_ADDRESS (0x089a60bc)
      DAT_0818a124: TRANSITION_ADDON_BASE_VTABLE_ADDRESS (0x0898994c)
      FUN_08278894: derived cleanup
      FUN_08278190: vector_destroy
      FUN_082792fc: string_object_destroy_veneer
      FUN_0818a0fc: base destructor tail target
    notes: >-
      Plain destructor of an as-yet-unidentified Silver-controller
      transition-addon class. The name is evidence-based, from the
      base-class descriptor/vtable literal 0x0898994c: the bytes there
      begin `TSilverCntlrTransitionAddon...` in osos.dec, and the paired
      constructor @ 0x08278e8c / destructor here both plant that literal
      or the derived sibling 0x089a60bc at +0. The raw ARM is 48 code
      bytes + the 4-byte literal pool word @ 0x08278f94 (= 0x089a60bc,
      binary-verified) and ends with a tail `b 0x0818a0fc`; Ghidra folds
      that tail target into this function, which is why the decompile
      appears to contain the base vtable store and deregistration call
      inline. Decoded from osos.asm, the real sequence is: install the
      derived vtable at `this+0`, call 0x08278894 for derived cleanup,
      destroy the embedded vector-like member at `this+0x40` through
      0x08278190, derive the embedded StringObject address from THAT
      callee's return (`result - 0x34` = `this+0x0c`) and destroy it
      through the ported string_object_destroy_veneer @ 0x082792fc, then
      recover `this` from the StringObject destructor's return
      (`result - 0x0c`) and tail into the base destructor @ 0x0818a0fc.
      The tail target installs 0x0898994c at +0 and calls
      `FUN_081e1fe8(*(this+4) + 0x24, this)`, returning `this`. Layout
      evidence from the paired constructor @ 0x08278e8c: the owner/base
      handle lives at +0x04, the embedded StringObject begins at +0x0c,
      and the vector-like member begins at +0x40 (its begin/end/capacity
      words are +0x48/+0x4c/+0x50 in 0x08278190's body). Call sites use
      it as a plain, caller-owned destructor: stack/object temporaries are
      constructed elsewhere then later passed here, with no operator
      delete afterward. Port calls the already-ported
      string_object_destroy_veneer directly, but the other three
      unresolved calls ride TRANSITION_ADDON_DESTROY_OPS slots with
      documented inert defaults (vector default faithfully returns its
      argument). Host tests pin the exact offset arithmetic and call
      routing via recorder mocks: derived-vtable store before cleanup,
      vector return -> StringObject via `-0x34`, StringObject return ->
      outer object via `-0x0c`, base deregistration through the stored
      owner pointer's +0x24 member, and the default seams preserving the
      same recovered `this` while the embedded StringObject destructor
      still plants its own host vtable pointer. match.py structural on
      the canonical symbol shows the expected widening from the crate
      frame plus the seam-slot blx calls and the already-ported direct
      StringObject veneer call, but preserves the same offset/dataflow
      chain and final return.

  - address: 0x08278e8c
    ghidra: FUN_08278e8c
    name: silver_controller_transition_addon_construct
    size: 188
    sig: "u8 *silver_controller_transition_addon_construct(u8 *this, const u8 *source, u32 flag, u32 base_hint, u32 quantum_arg, u32 scale_arg, u32 context)"
    module: cxx/transition_addon
    status: ported
    renames:
      param_1: this
      param_2: source
      param_3: flag
      param_4: base_hint
      param_5: quantum_arg
      param_6: scale_arg
      param_7: context
      DAT_08278f44: TRANSITION_ADDON_VTABLE_ADDRESS (0x089a60bc)
      DAT_082792c8: TRANSITION_ADDON_STRING_MEMBER_VTABLE_ADDRESS (0x089a60d8)
      FUN_0818a0c4: base_construct (base transition-addon constructor)
      FUN_082792b4: string_member_construct (derived string member veneer)
      FUN_08296efc: owner_capacity_query
      FUN_08277b64: transfer_quantum
      FUN_08277bd0: scale_class
      FUN_0818a0bc: facade_for_selector (ported, called directly)
      FUN_081a81bc: deque_seg_capacity (byte-identical copy, called directly)
      FUN_08278104: vector_member_construct
      FUN_08278250: register_with_owner
    notes: >-
      184 code bytes + the 4-byte literal-pool word @ 0x08278f44 (=
      0x089a60bc, binary-verified against osos.dec); the next function
      starts at 0x08278f48. 31 bl call sites, binary-scanned by decoding
      every B/BL word in osos.dec - all plain bl, no predicated forms and
      no tail b: 0x08047508, 0x0804fec0, 0x08058928, 0x080fe2e4,
      0x080ff974, 0x08104380, 0x08104660, 0x08104754, 0x08119dec,
      0x0813a578, 0x0813a5c0, 0x0813aca4, 0x0813ad00, 0x08149504,
      0x08149d34, 0x0814c470, 0x0815abfc, 0x081b1d4c, 0x081e1ec4,
      0x08210348, 0x08264b64, 0x08264d54, 0x08265860, 0x0826c630,
      0x0826c73c, 0x0827eff4, 0x0827f04c, 0x0827f824, 0x0827fbc0,
      0x0828fb00 and 0x082c7180. The seven-argument derived constructor
      paired with silver_controller_transition_addon_destroy @ 0x08278f60:
      four register arguments plus three stack arguments lifted by
      `ldm r8,{r6,r7,r8}` after `add r8,sp,#24`. The caller owns the
      storage (the ft/system.rs vtable_set call site operator_new(0x54)s
      it - 0x54 matches the vector member's +0x50 capacity word - and
      stashes the constructed object at its record+0x30). Sequence:
      base_construct(this, base_hint, flag ^ 1) @ 0x0818a0c4 plants the
      base vtable 0x0898994c, resolves the interface word at +0x04
      through the guard accessor 0x0818a06c (which forwards base_hint in
      r1 to the resolver 0x0814a130 - so param_4 is LIVE, not dead as
      Ghidra's base-ctor-only view suggests) and stores the inverted flag
      at byte +0x09; the constructor then overwrites +0x00 with the
      derived vtable 0x089a60bc and runs the string-member veneer
      0x082792b4 on this+0x0c (StringObject copy-ctor chain 0x082773e0
      plus the derived string vtable 0x089a60d8), storing the raw flag
      byte at the member's +8 (= this+0x14). Fields: +0x18 = 0xffffffff,
      +0x20 = 0, +0x24 = owner_capacity_query(+0x04 word) @ 0x08296efc
      (owner +0x04 interface NULL -> 0x200, else interface vtable slot
      +0x2c), +0x28 = context (stored BEFORE the helpers, which read it),
      +0x2c = transfer_quantum(this, quantum_arg) @ 0x08277b64 (context
      re-query 0x081a8198, 0x40000 clamp, capacity rounding through the
      divider 0x08036f14), +0x30 = scale_class(this, scale_arg) @
      0x08277bd0 (fully decoded: NULL context and scale-1 < 16 unsigned
      yields scale, else 1), +0x34 = 0, +0x38 = byte +9 of
      facade_for_selector(this, 1), +0x3c = the 0x081a81bc alignment
      constant 0x20, then the vector member constructor 0x08278104 on
      this+0x40 (owner word + zeroed +0x04/+0x08/+0x0c/+0x10 prologue,
      then an operator_new(24) node-fill loop through node ctor
      0x08277e2c) and the registration step 0x08278250 (result
      discarded). Every intermediate object address derives from the
      previous callee's RETURN (`sub r4,r0,#12` / `sub r4,r0,#64`),
      never from the entry this. Deviations: the seven unresolved
      callees cross TRANSITION_ADDON_CONSTRUCT_OPS slots - scale_class's
      default is the faithful decoded body, the base/string/vector
      defaults reproduce their decoded stores (interface-resolution
      result modeled as zero; the string default is the
      empty-construction prefix per the STRING_OBJECT_COPY_CONSTRUCT
      stub precedent; the vector default omits the node-fill loop), the
      capacity default reproduces the NULL-interface 0x200 fallback
      without dereferencing the owner, and the quantum/registration
      defaults are inert - while the two already-ported callees
      (facade_for_selector; deque_seg_capacity as the byte-identical
      0x081a81bc body, which the 0x083d9fc0 ledger entry sanctions
      hooking any copy to) are called directly. NOT hook-ready until
      the five non-faithful boundaries are ported and wired in. All
      object stores are 32-bit like the original's strs. Class identity
      caveat: the name rides the base class's TSilverCntlrTransitionAddon
      typeinfo string @ 0x0898994c; the derived vtable's RTTI pointer
      0x0897c5f4 does not decode as a typeinfo object (small-integer
      array bytes - recorded as an anomaly, not a named class), and the
      image string 21TCntlrTransitionAddonI11TCntlrBasicE @ 0x0898881c
      is unlinked. Host tests pin the call order and argument routing
      via recorder mocks with shifted string/vector returns (proving
      both container-of steps), the store-before-helper ordering of the
      context word, every default boundary's decoded stores end to end
      (with only the facade walk mocked - the default zero interface
      word would NULL-deref the real host walk), and the scale truth
      table including the arg-0 unsigned wrap. No construct-then-destroy
      round trip: on 64-bit hosts the string member's native payload
      word overlaps the +0x14 flag byte (the crate's face-word
      artifact). match.py (exit 1, diff expected): same seven-dispatch
      sequence, offsets and return-threading; the diff is the crate fp
      frame, the seam slots' ldr/blx dispatch standing in for the
      direct bls, and LLVM lowering the unaligned u32 stores/loads to
      strb/ldrb quads on ARMv5TE (the destructor sibling's accepted
      widening).

  - address: 0x08278dc4
    ghidra: FUN_08278dc4
    name: silver_controller_transition_addon_construct_from_cstr
    size: 200
    sig: "u8 *silver_controller_transition_addon_construct_from_cstr(u8 *this, const u8 *source, u32 flag, u32 base_hint, u32 quantum_arg, u32 scale_arg, u32 context)"
    module: cxx/transition_addon
    status: ported
    renames:
      param_1: this
      param_2: source (the C string the embedded string member is built from;
        the file-open path passes the volume-relative path)
      param_3: flag
      param_4: base_hint
      param_5: quantum_arg
      param_6: scale_arg
      param_7: context
      DAT_08278e84: TRANSITION_ADDON_VTABLE_ADDRESS (0x089a60bc)
      DAT_08278e88: TRANSITION_ADDON_STRING_MEMBER_VTABLE_ADDRESS (0x089a60d8)
      FUN_0818a0c4: base_construct (shared base transition-addon constructor)
      FUN_08277304: string_object_construct_from_cstr (ported, dispatched via
        STRING_OBJECT_CONSTRUCT_FROM_CSTR whose wired default is the port)
      FUN_08296efc: owner_capacity_query
      FUN_08277b64: transfer_quantum
      FUN_08277bd0: scale_class
      FUN_0818a0bc: facade_for_selector (ported, called directly)
      FUN_081a81bc: deque_seg_capacity (byte-identical copy, called directly)
      FUN_08278104: vector_member_construct
      FUN_08278250: register_with_owner
    notes: >-
      192 code bytes plus two 4-byte literal-pool words @ 0x08278e84
      (= 0x089a60bc) and 0x08278e88 (= 0x089a60d8), both binary-verified
      against osos.dec; Ghidra's 192-byte extent is the code span only
      and drops the pool as usual - the next function (the copy-construct
      overload silver_controller_transition_addon_construct @ 0x08278e8c)
      starts at 0x08278e8c. 23 bl call sites, binary-scanned by decoding
      every B/BL word in osos.dec - all plain bl, no predicated forms, no
      tail b, and no data-word references to the address: 0x0805a690,
      0x0808718c, 0x08096320, 0x080b5478, 0x080dc71c, 0x080e96f8,
      0x08149cd8, 0x08161af4, 0x08196444, 0x08196820, 0x081d2e18,
      0x081d42cc, 0x081d4374, 0x081eef94, 0x081f51a4, 0x081f525c,
      0x082658c0, 0x0826769c, 0x0827947c, 0x0828b6d0, 0x082d3cec,
      0x082d41d8 and 0x082d44e4. The from-C-string constructor overload
      of the Silver-controller transition-addon class: identical argument
      list (four register arguments plus three stack arguments lifted by
      `ldm r8,{r6,r7,r8}` after `add r8,sp,#24`), identical vtables and an
      identical field sequence to the 0x08278e8c overload - same base
      constructor 0x0818a0c4, same derived object vtable 0x089a60bc, same
      derived string-member vtable 0x089a60d8, same helpers. The only
      difference is the string-member step: this overload calls the
      already-ported string_object_construct_from_cstr @ 0x08277304
      directly (bl) and plants the derived string vtable 0x089a60d8 at
      the returned member's +0x00 ITSELF (str at 0x08278dfc) before the
      raw flag byte at the member's +8 (strb at 0x08278e00); the
      0x08278e8c overload delegates both steps to the unported veneer
      0x082792b4. Return-threading is identical: every intermediate
      object address derives from the previous callee's return (`sub
      r4,r0,#12` after the string constructor, `sub r4,r0,#64` after the
      vector constructor), never from the entry this; the context word at
      +0x28 is stored BEFORE the quantum/scale helpers that read it. This
      is the overload the platform file-open path uses:
      ft_platform_file_open @ 0x082d3cb4 calls it with (raw, path, 1,
      volume, 0x10000, 2, 0) over an 84-byte operator_new block (the
      +0x18 word it initializes to -1 is the directory-entry-index
      sentinel file_has_directory_entry @ 0x082a548c tests; the close
      path @ 0x08278894 restores it). Deviations: the six unresolved
      callees cross the SAME TRANSITION_ADDON_CONSTRUCT_OPS slots as the
      sibling overload (its string_member_construct slot is unused here),
      and the from-cstr string construction crosses the new
      STRING_OBJECT_CONSTRUCT_FROM_CSTR slot in cxx/string_object.rs (the
      STRING_OBJECT_COPY_CONSTRUCT pattern) whose wired default IS the
      ported string_object_construct_from_cstr. That slot exists because
      this constructor calls BOTH the string constructor on the +0x0c
      member AND facade_for_selector on the object base, and the two
      native-widened host dereferences demand incompatible fixture
      alignments on 64-bit hosts (StringObject at this+0x0c needs this =
      4 mod 8; the facade guard read at this+0x00 needs this = 0 mod 8),
      so no host fixture can run both real callees in one call - host
      tests install recording mocks on the string slot while the real
      callee's own behavior stays pinned by the string_object.rs tests.
      facade_for_selector and deque_seg_capacity (the byte-identical
      0x081a81bc alignment query) are called directly. NOT hook-ready
      until the five non-faithful boundaries are ported and wired in;
      ft/system.rs's FT_PLATFORM_FILE_CTOR slot deliberately keeps its
      fail-closed default - with register_with_owner inert the +0x1c
      status word would be operator_new garbage and the open could
      falsely succeed. Host tests pin the call order and argument
      routing with shifted string/vector mock returns (proving both
      container-of steps and that THIS constructor plants the derived
      string vtable), every default boundary's decoded stores end to end
      with the string stand-in (flag = 0 covering the other inversion:
      base byte +9 = 1, member +8 = 0), and the slot's wiring to the
      real port. No construct-then-destroy round trip (the face-word
      artifact, same as the sibling). match.py (exit 1, diff expected)
      against this worktree's archive via --lib: same eight-dispatch
      sequence, offsets and return-threading; the diff is the crate fp
      frame, the seam slots' ldr/blx dispatch standing in for the direct
      bls (now including the from-cstr string slot), and LLVM lowering
      the unaligned u32 stores/loads to strb/ldrb quads on ARMv5TE (the
      sibling's accepted widening).

  - address: 0x082a50b0
    ghidra: FUN_082a50b0
    name: string_object_c_str
    size: 16
    sig: "const char *string_object_c_str(const StringObject *this)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, DAT_082a50c0: STRING_OBJECT_EMPTY_CSTR_ADDRESS}
    notes: >-
      503 bl call sites (binary-scanned) - one of the hottest leaves in
      the image; a 4-byte tail-branch thunk @ 0x082a704c (`b
      0x082a50b0`) also reaches it and is NOT ported (one function per
      commit). The C-string accessor of the same two-word string class
      as string_default_construct @ 0x08277440 / string_object_destroy
      @ 0x08277484: `ldr r0,[r0,#4]; cmp r0,#0; ldreq r0,[0x082a50c0];
      bx lr` - the payload word at +4, or a shared empty C string when
      it is NULL. This pins the payload down as a heap-allocated
      NUL-terminated char buffer: sampled call sites feed the result
      to strtol (base 10), character-search and printf-family calls,
      the sibling @ 0x082a50a0 (`ldr r0,[r0,#4]; b 0x08275e20`) runs
      the count+1 strlen variant over it, and the 0x08279338 neighbor
      scans the class's characters for the path separators ':', '/'
      and '\\'. The shared default: the literal-pool word @ 0x082a50c0
      holds 0x083e2e3a (binary-verified against osos.dec) and the byte
      there is 0x00 - a NUL inside the 0x083exxxx stdlib code, so the
      default reads as "". Never returns NULL; no NULL guard on `this`.
      Port models the ROM default as the static STRING_OBJECT_EMPTY_CSTR
      (original address kept as STRING_OBJECT_EMPTY_CSTR_ADDRESS), the
      same simplification cxx/string.rs makes for its shared empty rep.
      match.py structural: same ldr-@+4 / NULL test / default-select /
      return; the diff is LLVM's fp frame and a movne standing in for
      the original's ldreq literal load (the documented deviation).

  - address: 0x082a5118
    ghidra: FUN_082a5118
    name: string_object_substring
    size: 88
    sig: "void string_object_substring(StringObject *out, const StringObject *source, int start_index, int max_codepoints)"
    module: cxx/string_object
    status: ported
    vars: {param_1: out, param_2: source, param_3: start_index, UNRECOVERED_JUMPTABLE: max_codepoints}
    notes: >-
      Decoded from raw osos.dec at 0x082a5118..0x082a516f: 88 bytes, all
      code (Ghidra's 88-byte extent is exact for once); the next
      function, string_object_find_utf8_prefix @ 0x082a5170, begins
      immediately after. 17 bl call sites, all plain and zero
      predicated, verified by decoding every ARM B/BL word in osos.dec.
      Bounded substring extraction of the two-word string class:
      default-constructs out FIRST (bl string_default_construct @
      0x08277440), then returns early when source's payload word at +4
      is NULL (popeq), when start_index is negative (poplt), and when
      the payload's codepoint count (utf8_codepoint_count_safe @
      0x082770e0) is unsigned lower-or-same than start_index (pople) -
      the index must address an existing codepoint, not the terminator.
      Otherwise it resolves the codepoint's byte pointer
      (string_object_codepoint_ptr @ 0x082a50c4 - never NULL here, the
      count guard precedes it) and tail-branches (pop + b) to the
      capped UTF-8 assignment @ 0x082764d8 with (out, start,
      max_codepoints). That tail (FUN_082764d8, 116 bytes, NOT ported):
      NULL/max<=0-guards into vtable slot +0xc clear, else counts bytes
      with the capped UTF-8 length helper @ 0x08275e44 (first
      max_codepoints codepoints + 1 for the NUL), allocates through
      vtable slot +0x8, memcpys @ 0x08037db0 and NUL-terminates - i.e.
      out receives at most max_codepoints codepoints starting at
      start_index, and max_codepoints <= 0 clears out. The early
      returns fire AFTER construction: out always leaves with the class
      vtable planted and a NULL payload. Ghidra's C for this function
      is garbage (an UNRECOVERED_JUMPTABLE parameter and phantom
      virtual dispatch from the unrelated jumptable at 0x082764fc
      inside the tail callee); the raw ARM is unambiguous. Deviation:
      the tail runs through the STRING_OBJECT_ASSIGN_UTF8_CAPPED
      dispatch slot (the STRING_OBJECT_COPY_CONSTRUCT pattern) whose
      default stub is a no-op - unlike string_object_assign_stub there
      is no argument-only prefix worth reproducing, since even the
      original's guard path dispatches the unportable +0xc virtual
      clear; the real port of 0x082764d8 replaces the stub when it
      lands. Minor codegen note: LLVM hoists the negative-index check
      above the payload-NULL check (both are early pops on disjoint
      conditions; the original reads [source+4] first, so a faulting
      source with a negative index faults in the original and returns
      quietly in the port). match.py structural: 22 original vs 27 Rust
      instructions; same ctor-first guard set, unsigned count compare
      and tail dispatch, the `b 0x082764d8` becoming a `bx` through the
      ops slot. Host tests: construction precedes every guard exit
      (NULL payload, negative index, index >= count incl. i32::MAX),
      the happy path dispatches (out, payload+byte_offset,
      max_codepoints) for each codepoint of a multibyte payload with
      max_codepoints pass-through at -5/0/1/i32::MAX, and the wired
      default stub leaves the fresh object untouched.

  - address: 0x082a5170
    ghidra: FUN_082a5170
    name: string_object_find_utf8_prefix
    size: 176
    sig: "int string_object_find_utf8_prefix(const StringObject *this, const char *needle, int start_index)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: needle, param_3: start_index, local_20: cursor, iVar1: needle_codepoint_count, uVar2: haystack_codepoint_count, iVar3: comparison, iVar4: index}
    notes: >-
      Decoded from raw osos.dec at 0x082a5170..0x082a521f: 176 bytes,
      all code; 0x082a5220 is the next function. 26 direct call sites,
      all plain bl and zero predicated, verified by decoding every ARM
      B/BL word in osos.dec. This is StringObject's UTF-8-cursor search:
      it NULL-checks the payload word at +4, counts needle and haystack
      through the ported utf8_codepoint_count_safe @ 0x082770e0, advances
      to the signed start index through the ported utf8_next_codepoint @
      0x08276214, then checks each remaining codepoint position. The
      decisive raw sequence is `mov r2,r7; mov r1,r8; bl 0x08030f64`:
      it passes the needle's CODEPOINT count to bytewise memcmp, not its
      byte length. Thus a multibyte needle may match only its initial
      bytes; this port deliberately preserves that retail behavior rather
      than implementing a conventional UTF-8 substring search. The
      unsigned `index + needle_count <= haystack_count` gate and empty
      needle behavior are likewise retained. No dispatch seam: all three
      callees are already ported and called directly. Host tests cover
      codepoint-index starts, the partial-multibyte-prefix result, NULL
      payload / NULL needle / empty-haystack guards, negative starts, and
      advance-past-terminator rejection. match.py structural review is
      expected to differ in frames and loop lowering.

  - address: 0x082a5220
    ghidra: FUN_082a5220
    name: string_object_find_codepoint
    size: 128
    sig: "int string_object_find_codepoint(const StringObject *this, int codepoint, int start_index)"
    module: cxx/string_object
    status: ported
    renames:
      param_1/r0: this
      param_2/r1/r5: codepoint
      param_3/r2/r6: start_index
      local_14: cursor
      iVar1/r0: decoded_codepoint
      iVar2/r4: index
    notes: >-
      Raw osos.dec decodes from 0x082a5220 through 0x082a529f, with the
      next function at 0x082a52a0: exactly 128 bytes, all code. Decoding
      every ARM B/BL word finds 9 direct call sites, all unconditional bl
      and zero predicated calls: 0x08119264, 0x081196ec, 0x08119708,
      0x08178728, 0x08178770, 0x081b2fb8, 0x0826c7b4, 0x082a5c24, and
      0x082a5c64. The function loads StringObject.payload at +4, returns
      -1 for NULL, advances its UTF-8 cursor to the signed start_index
      while rejecting a terminator, then returns the first later index
      whose utf8_next_codepoint result equals codepoint. Negative starts
      search from zero; a malformed four-byte lead yields searchable zero
      after its three-byte decoder advance. No dispatch seam: the sole
      callee utf8_next_codepoint @ 0x08276214 is already ported and called
      directly. Host tests cover multibyte codepoint indices, negative
      starts, absent/empty/NULL payload results, start-at-terminator, and
      malformed-lead zero matching. match.py structural review is expected
      to differ in frame and loop lowering.
  - address: 0x082a50a8
    ghidra: FUN_082a50a8
    name: string_object_utf8_strcasecmp_safe
    size: 8
    sig: "int string_object_utf8_strcasecmp_safe(const StringObject *this, const char *other)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: other, iStack_c: a_cursor, iStack_10: b_cursor, iVar1: folded_codepoint_a, iVar2: folded_codepoint_b, DAT_082760f0: UTF8_STRCASECMP_EMPTY_CSTR_ADDRESS}
    notes: >-
      32 bl call sites, binary-scanned over every B/BL word in osos.dec,
      ZERO predicated (matches Ghidra's count). 8 bytes, all code - the
      next function (string_object_c_str @ 0x082a50b0) starts right
      after. The case-folding compare-against-C-string accessor of the
      two-word string class (vtable @ +0, payload @ +4), `int
      StringObject::utf8_strcasecmp(const char *other)`: raw ARM is
      `ldr r0,[r0,#4]; b 0x0827609c` - a two-instruction thunk loading
      the payload word and tail-branching to the case-folding UTF-8
      compare core @ 0x0827609c (identified below, NOT ported) with
      the caller's r1 passed through untouched. No NULL guard on
      `this`; a NULL payload goes down untouched and the core's own
      NULL substitution (literal-pool word @ 0x082760f0 holds
      0x083e2e3a, binary-verified - the SAME shared empty C string
      string_object_c_str @ 0x082a50b0 and utf8_strcmp_safe @
      0x08276d64 fall back to) makes it read as "". Call sites pin the
      intent: nine consecutive sites @ 0x08117b8c-0x08117c38 compare a
      stack-built StringObject against the pc-relative literals "jpg",
      "jpeg", "jp2", "tif", "tiff", "png", "gif", "bmp", "pict" @
      0x08117ce8 on, each result consumed by `cmp r0,#0; beq` - a
      photo-import extension matcher (matching "JPG" against "jpg"
      REQUIRES the fold to work); further clusters @ 0x08118bf8 on,
      0x081193dc-0x0811949c, 0x08178138 on, 0x0826bde8 on, 0x0828aa0c
      on, 0x08298a0c on. Ghidra's C
      (decomp/c/029/082a50a8_FUN_082a50a8.c) shows the callee's body
      inlined and matches once the tail branch is followed. Port
      dispatches through the STRING_OBJECT_UTF8_STRCASECMP_CORE slot
      (the RETAIL_VSNPRINTF_ENGINE pattern) because the core is not
      ported; the default missing_utf8_strcasecmp_core returns the
      core's fold-equal exit (0) - arbitrary for real inputs, so this
      port is NOT hook-ready until 0x0827609c is wired in. match.py
      structural: same ldr-@+4 + tail-branch shape; the diff is
      LLVM's fp frame and the branch becoming a bx through the
      volatile-read slot (the documented deviation, same as
      string_object_len_plus1 / string_object_utf8_strcmp_safe).

  - address: 0x080ed884
    ghidra: FUN_080ed884
    name: string_object_copy_or_resource_on_casefold_match
    size: 76
    sig: "StringObject *string_object_copy_or_resource_on_casefold_match(StringObject *this, const StringObject *source, const char *compare_text, u32 resource_id)"
    module: cxx/string_object
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: source
      param_3/r2: compare_text
      param_4/r3: resource_id
      r4: source_saved
      r5: this_saved
      r6: resource_id_saved
      iVar1/r0: casefold_compare_result
    notes: >-
      Raw osos.dec extent is exactly 76 bytes: 19 ARM words from
      0x080ed884 through the tail branch at 0x080ed8cc; the next separately
      linked function starts at 0x080ed8d0. Decoding every ARM B/BL word
      finds 10 direct bl callers, all unconditional (zero predicated):
      0x082a596c, 0x082a5998, 0x082a59bc, 0x082a5a1c, 0x082a5a3c,
      0x082a5a5c, 0x082a5ad0, 0x082a5af8, 0x082a5b20, 0x082a5b48.
      Three tail callers also target it: unconditional b at 0x08298820 and
      0x0829acf0, plus bne at 0x08298adc. It loads source->payload and
      compares it with compare_text through utf8_strcasecmp_safe
      (0x0827609c). A nonzero result tail-calls the ported StringObject copy
      constructor; equality reads the current task's provider-chain head,
      resolves its Str resource by resource_id, and tail-calls the ported C
      string constructor. No path adds a NULL guard. The compare core remains
      identified because its runtime case-fold table is unresolved, so this
      port uses its pre-existing volatile STRING_OBJECT_UTF8_STRCASECMP_CORE
      dispatch slot; every other callee is ported and direct. Host tests
      prove the nonzero copy path and the equal resource-construction path,
      including the raw-u32 provider-chain field.
  - address: 0x0827609c
    ghidra: FUN_0827609c
    name: utf8_strcasecmp_safe
    size: 84
    sig: "int utf8_strcasecmp_safe(const char *a, const char *b)"
    module: cxx/string_object
    status: identified
    vars: {param_1: a, param_2: b, local_c: a_cursor, local_10: b_cursor, iVar1: folded_codepoint_a, iVar2: folded_codepoint_b, DAT_082760f0: UTF8_STRCASECMP_EMPTY_CSTR_ADDRESS, DAT_08276fd0: UTF8_FOLD_TABLE_ADDRESS}
    notes: >-
      7 direct bl call sites (binary-scanned: 0x0807a508, 0x080ed89c,
      0x08102994, 0x08135ca0, 0x08272520, 0x082989ec, 0x0829b8f0) plus
      the 32 of the string-object thunk @ 0x082a50a8 above. The
      case-insensitive counterpart of the ported utf8_strcmp_safe @
      0x08276d64: substitutes the shared empty C string (0x083e2e3a,
      pool word @ 0x082760f0, binary-verified) for either NULL
      argument, then walks both strings codepoint by codepoint through
      the ported utf8_next_codepoint @ 0x08276214, folds EACH decoded
      codepoint through the byte-map function @ 0x08276fbc (`cmp
      r0,#0x100; ldrcc r1,[0x08276fd0]; andcc r0,r0,#0xff; ldrbcc
      r0,[r1,r0]; bx lr` - codepoints >= 0x100 pass through
      unchanged), and returns the first unequal folded pair's
      difference (sub r0,r4,r0), 0 at a shared terminator. A bounded
      variant @ 0x082760f4 (112 bytes) adds a codepoint count limit to
      the same loop (its NULL default @ 0x08276164 is 0x083e2e3a too).
      BLOCKER / ANOMALY for a future port: the fold's 256-byte table
      base is the literal word 0x083ed1dd (pool @ 0x08276fd0,
      binary-verified; four other users at pool words 0x0806b87c,
      0x080ca930, 0x080dac54, 0x080f5414 - the last a byte-identical
      20-byte fold clone @ 0x080f5400, another an in-place
      string-byte-map loop @ 0x080dac30). In the static osos.dec that
      address sits MID-CODE inside the fplib region (0x083eae74-
      0x083ed1ff, the fp trap-control helper: the byte at the base is
      0xf0) followed by ADS register-dump format strings. fold(0) =
      0xf0 != 0 would break the compare loop's shared-terminator exit,
      and no classic 256-byte case table exists anywhere in osos.dec
      (scanned), so the runtime bytes at 0x083ed1dd almost certainly
      differ from the static image - patching vs relocation mechanism
      UNRESOLVED. Do not port the fold by modeling the static bytes.
      The photo-import call-site cluster on the thunk ("jpg"..."pict")
      pins the intended semantics as a case-insensitive compare.

  - address: 0x082a50a0
    ghidra: FUN_082a50a0
    name: string_object_len_plus1
    size: 8
    sig: "size_t string_object_len_plus1(const StringObject *this)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this}
    notes: >-
      1 bl call site (binary-scanned), @ 0x081ae498, which truncates the
      result to 16 bits (mov r6,r0,lsl #0x10; lsr #0x10) - a buffer
      size for a copy buffer. The length-plus-one accessor of the same
      two-word string class as string_object_c_str @ 0x082a50b0: a
      two-instruction thunk `ldr r0,[r0,#4]; b 0x08275e20` that
      tail-branches to the ported strlen_safe_plus1 @ 0x08275e20 over
      the payload word at +4 - the payload's buffer size including the
      NUL terminator, or 1 when the payload is NULL (the thunk guards
      nothing; the callee's own NULL guard yields 1, unlike c_str which
      substitutes a shared empty C string first). No NULL guard on
      `this`. Its 8-byte sibling @ 0x082a50a8 (`ldr r0,[r0,#4]; b
      0x0827609c`) runs the 84-byte FUN_0827609c over the payload and is
      NOT ported here. Port calls the ported callee through a volatile
      read (the house anti-const-fold trick, same as release_payload_op)
      purely to stop LLVM inlining strlen_safe_plus1 and dissolving the
      thunk. match.py structural: same ldr-@+4 + tail-branch shape; the
      diff is LLVM's fp frame and the branch becoming a bx through the
      volatile-read literal instead of the original's direct b (the
      documented deviation).

  - address: 0x082a5368
    ghidra: FUN_082a5368
    name: string_object_utf8_strcmp_safe
    size: 8
    sig: "int string_object_utf8_strcmp_safe(const StringObject *this, const char *other)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: this
      param_2: other
      iStack_c: a_cursor
      iStack_10: b_cursor
      iVar1: codepoint_a
      iVar2: codepoint_b
      DAT_08276db0: UTF8_STRCMP_SAFE_EMPTY_CSTR_ADDRESS
    notes: >-
      The compare-against-C-string accessor of the two-word string
      class (vtable @ +0, payload @ +4), `int
      StringObject::utf8_strcmp(const char *other)`. 8 bytes, all
      code (0x082a5368..0x082a5370). 39 bl call sites, binary-scanned
      over every B/BL word in osos.dec, ZERO predicated (matches
      Ghidra's count). Raw ARM: `ldr r0,[r0,#4]; b 0x08276d64` - a
      two-instruction thunk loading the payload word and tail-
      branching to the ported utf8_strcmp_safe @ 0x08276d64 with the
      caller's r1 passed through untouched. Returns the first unequal
      decoded codepoints' difference, 0 on equality; a NULL payload
      reads as the shared empty string (the thunk guards nothing; the
      NULL substitution guard lives inside the callee, the same
      asymmetry string_id_record_equals @ 0x08258cc4 documents). No
      NULL guard on `this`. Ghidra's C
      (decomp/c/029/082a5368_FUN_082a5368.c) shows the callee's body
      inlined and matches once the tail branch is followed. Opens the
      class's accessor cluster 0x082a5368-0x082a5398
      (string_object_is_empty @ 0x082a5370, the raw payload accessor
      `ldr r0,[r0,#4]; bx lr` @ 0x082a538c, payload ->
      utf8_codepoint_count_safe chain @ 0x082a5394). Call sites pin
      the use: twenty consecutive sites @ 0x0809f7b4-0x0809fa60
      compare a stack-built StringObject (constructed from a u16
      string through FUN_082765a8) against literal keywords
      ("Acoustic", ... @ 0x0809faa4 on, binary-verified), each result
      consumed by `cmp r0,#0; bne` - a genre-name -> ID dispatch
      chain (0x64 on the first match); a second nineteen-site cluster
      @ 0x0813ba04-0x0813cb88 runs the same shape, plus singletons @
      0x0809fb8c, 0x08118b84, 0x08118be0, 0x081192d4. Port calls the
      ported callee through a volatile read (the house
      anti-const-fold trick, same as string_object_len_plus1) purely
      to stop LLVM inlining utf8_strcmp_safe and dissolving the
      thunk. Host tests: equal strings compare 0 with the object and
      payload untouched, NULL payload passes through untouched (vs
      "" -> 0, vs "a" -> -97, vs NULL -> 0), multibyte comparison is
      decoded-codepoint (caf+e-acute vs caf+e-grave -> 1), and an
      8x8 sweep over ASCII/multibyte cases matches utf8_strcmp_safe
      called directly. match.py structural (worktree --lib): same
      ldr-@+4 + tail-branch shape; the diff is LLVM's fp frame and
      the branch becoming a bx through the volatile-read literal
      instead of the original's direct b (the documented deviation).

  - address: 0x082a5370
    ghidra: FUN_082a5370
    name: string_object_is_empty
    size: 28
    sig: "bool string_object_is_empty(const StringObject *this)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: this
      pcVar2: payload
      cVar1: first_byte
    notes: >-
      The emptiness predicate of the two-word string class (vtable @
      +0, payload @ +4), `bool StringObject::is_empty()`. 28 bytes,
      all code, no literal-pool word (0x082a5370..0x082a538c). 55 bl
      call sites, binary-scanned. Raw ARM: `ldr r0,[r0,#4]; cmp r0,#0;
      ldrbne r0,[r0]; cmpne r0,#0; moveq r0,#1; movne r0,#0; bx lr` -
      true exactly when the payload word is NULL (the
      default-constructed/cleared state) or points at a NUL byte; a
      NULL payload is never dereferenced (the ldrbne/cmpne are
      predicated on the pointer test). No NULL guard on `this`. Ghidra
      (decomp/c/029/082a5370_FUN_082a5370.c) matches the raw ARM
      exactly. Sits in the class's accessor cluster
      0x082a5368-0x082a5398 (payload -> utf8_strcmp_safe chain, the raw
      payload accessor `ldr r0,[r0,#4]; bx lr` @ 0x082a538c, payload
      -> utf8_codepoint_count_safe chain @ 0x082a5394). Call sites pin
      the class: at 0x0807a528/0x0807a53c the receiver is the object
      string_object_assign_cstr @ 0x0827639c just wrote, and at
      0x0810034c/0x08100354 the same receiver flows to
      string_object_c_str @ 0x082a50b0. ALIAS SCAN: the exact 7-word
      instruction pattern occurs exactly ONCE in osos.dec - unlike
      handle_deref_or_null and container_is_empty, this body has NO
      byte-identical twins. Host tests: NULL payload (true), payload
      pointing at NUL (true), nonempty payload (false; only the first
      byte is read, object untouched). match.py structural: same
      load/guard/return-1-or-0 flow; the diff is LLVM computing
      `byte == 0` as clz+lsr#5 behind the crate fp frame instead of
      ADS's predicated cmpne/movne pair.

  - address: 0x082a538c
    ghidra: FUN_082a538c
    name: string_object_raw_payload
    size: 8
    sig: "char * string_object_raw_payload(const StringObject *this)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: this
    notes: >-
      The raw payload accessor of the two-word StringObject class
      (vtable @ +0, payload @ +4). 8 bytes, all code
      (0x082a538c..0x082a5394): `ldr r0,[r0,#4]; bx lr` returns the
      payload word unchanged, including NULL. Unlike string_object_c_str
      @ 0x082a50b0, this accessor never substitutes the shared empty
      C string; it has no NULL guard on `this`. 34 direct bl call sites,
      all unconditional, plus 2 tail b callers (0x081a42b0 and
      0x081a4bfc), binary-scanned by decoding every ARM B/BL word in
      osos.dec. Those tail callers return a temporary StringObject's
      payload after their caller-side cleanup. The exact two-word body
      occurs 35 times in osos.dec, so the port has its own text section
      to preserve its exported hook seam if LLVM folds identical code.
      Host tests prove an invalid non-NULL payload is returned without
      dereference and NULL passes through rather than becoming the
      modeled empty C string. match.py structural review: same load of
      the second object word and immediate return.

  - address: 0x082cf9e4
    ghidra: FUN_082cf9e4
    name: ft_glyphslot_free_bitmap
    size: 72
    sig: "void ft_glyphslot_free_bitmap(FtGlyphSlot *slot)"
    module: ft/glyph_slot
    status: ported
    vars:
      param_1: slot
      uVar1: bitmap_flags
      uVar2: memory
    notes: >-
      FreeType ft_glyphslot_free_bitmap (ftobjs.c). FT_GlyphSlotRec has
      face at +0x04, bitmap.buffer at +0x58, and internal at +0x9c;
      FT_FaceRec::memory is +0x64 and FT_GlyphSlot_InternalRec::flags is
      +0x04. When FT_GLYPH_OWN_BITMAP (bit 0) is set, it calls
      ft_mem_free(memory, bitmap.buffer), zeros bitmap.buffer, then clears
      that bit. Otherwise it only zeros bitmap.buffer, preserving flags and
      never touching face or memory. Raw ARM confirms the conditional store
      before the early return and the ordered free/store/bic/store owned
      path. Four direct callers are 0x0804cbe8, 0x080a1f7c, 0x080a797c,
      and ft_glyphslot_set_bitmap @ 0x082cfa2c; the latter calls this
      helper before storing its replacement buffer and asserting no
      ownership bit remains. Host tests cover owned and non-owned paths,
      callback ABI, pointer clearing, and flag transition.

  - address: 0x082cfae8
    ghidra: FUN_082cfae8
    name: ft_mem_free
    size: 16
    sig: void ft_mem_free(FtMemory *memory, void *block)
    module: ft/memory
    status: ported
    vars:
      param_1: memory
      param_2: block
    notes: >-
      FreeType ft_mem_free (ftutil.c) - `if (block) memory->free(memory,
      block)`, a conditional tail branch through FT_MemoryRec's free slot
      at +8. 255 bl + 12 tail b call sites (binary-scanned), the busiest
      FreeType routine after the trace sink. Identified by its two
      callers inside the same translation unit - ft_mem_qrealloc @
      0x082cfb34 calls it exactly where upstream's `new_count == 0` arm
      does `ft_mem_free( memory, block )`, and ft_stream_exit_frame @
      0x0804ef8c / ft_stream_release_frame @ 0x0804fcb8 call it as the
      first half of upstream's FT_FREE macro (the macro's own nulling of
      the caller's pointer is the `str` that follows each call site).
      Freeing NULL never dereferences `memory`.

  - address: 0x082cfaa4
    ghidra: FUN_082cfaa4
    name: ft_mem_alloc
    size: 68
    sig: void *ft_mem_alloc(FtMemory *memory, i32 size, i32 *p_error)
    module: ft/memory
    status: ported
    vars:
      param_1: memory
      param_2: size
      param_3: p_error
    notes: >-
      FreeType ft_mem_alloc (ftutil.c) - ft_mem_qalloc (0x082cfaf8)
      followed by FT_MEM_ZERO when the allocation succeeded and size > 0.
      68 bl call sites (binary-scanned). The zero fill is ARM ADS'
      two-argument __rt_memclr reached through the iRAM veneer at
      0x08037dc8 -> 0x220002d4, i.e. memzero @ 0x080002d4 copied into
      fast memory. Confirmed by ft_mem_qrealloc @ 0x082cfb34, which calls
      it with (memory, new_count*item_size, &error) exactly where
      upstream's `cur_count == 0` arm does. Port deviation - LLVM inlines
      the memzero body, so match.py shows one bl plus an inlined
      __aeabi_memclr chain instead of the original's single bl.

  - address: 0x082cfaf8
    ghidra: FUN_082cfaf8
    name: ft_mem_qalloc
    size: 60
    sig: void *ft_mem_qalloc(FtMemory *memory, i32 size, i32 *p_error)
    module: ft/memory
    status: ported
    vars:
      param_1: memory
      param_2: size
      param_3: p_error
    notes: >-
      FreeType ft_mem_qalloc (ftutil.c) - the unzeroed allocation.
      size > 0 calls memory->alloc (FT_MemoryRec +4) and reports
      FT_Err_Out_Of_Memory (0x40) on a NULL result; size == 0 is a
      NULL block with FT_Err_Ok; size < 0 is FT_Err_Invalid_Argument
      (6) without ever asking the allocator. 3 bl call sites
      (binary-scanned) - ft_mem_alloc, ft_mem_realloc's chain and
      ft_stream_enter_frame @ 0x0804edb0 (upstream's FT_QALLOC).

  - address: 0x082cfa90
    ghidra: FUN_082cfa90
    name: ft_highpow2
    size: 20
    sig: u32 ft_highpow2(u32 value)
    module: ft/memory
    status: ported
    vars:
      param_1: value
    notes: >-
      FreeType ft_highpow2 (ftutil.c) verbatim - clear the lowest set bit
      (`value & (value - 1)`) until the result is 0, then return the last
      non-zero value, i.e. the highest power of two <= value (and 0 for
      0). 1 bl call site (@ 0x080b7aac; the single `b` to the address is
      the function's own loop back-edge). The third independent pin on
      this translation unit being ftutil.c, alongside the __FILE__
      pointer 0x08901290 (runtime; the text sits at file 0x0890c168) and
      the ft_mem_* family itself. match.py - same four-instruction loop;
      LLVM adds its frame and a redundant mov pair.

  - address: 0x082cfb34
    ghidra: FUN_082cfb34
    name: ft_mem_qrealloc
    size: 220
    sig: >-
      void *ft_mem_qrealloc(FtMemory *memory, i32 item_size, i32
      cur_count, i32 new_count, void *block, i32 *p_error)
    module: ft/memory
    status: ported
    vars: {param_1: memory, param_2: item_size, param_3: cur_count,
           param_4: new_count, param_5: block, param_6: p_error}
    notes: >-
      FreeType ft_mem_qrealloc (ftutil.c). Rejects cur_count < 0 ||
      new_count < 0 || item_size <= 0 with FT_Err_Invalid_Argument (the
      pre-2.3.5 `item_size <= 0` spelling, not the later `< 0`);
      new_count == 0 frees the block through ft_mem_free and yields NULL;
      new_count > FT_INT_MAX/item_size (an __rt_sdiv @ 0x08031568 against
      0x7fffffff) is FT_Err_Array_Too_Large (10); cur_count == 0 asserts
      block == NULL (FT_ASSERT __LINE__ 139, ftutil.c, through the ported
      ft_panic) and goes to ft_mem_alloc; otherwise memory->realloc
      (FT_MemoryRec +12) with (memory, cur_count*item_size,
      new_count*item_size, block) and FT_Err_Out_Of_Memory on NULL. 1 bl
      call site (ft_mem_realloc @ 0x082cfc3c). The assert's third vararg
      is the live r3 (new_count) - reproduced though the format ignores
      it. match.py - same arm structure; LLVM inlines __rt_sdiv and
      ft_mem_alloc (the ft_mem_alloc note's own deviation).

  - address: 0x082cfc3c
    ghidra: FUN_082cfc3c
    name: ft_mem_realloc
    size: 108
    sig: >-
      void *ft_mem_realloc(FtMemory *memory, i32 item_size, i32
      cur_count, i32 new_count, void *block, i32 *p_error)
    module: ft/memory
    status: ported
    notes: >-
      FreeType ft_mem_realloc (ftutil.c) - ft_mem_qrealloc (0x082cfb34)
      followed by FT_MEM_ZERO of the grown tail when the call succeeded
      and new_count > cur_count. 116 bl call sites (binary-scanned). Not
      ported - no ported caller needs it yet.
  - address: 0x082cfca8
    ghidra: FUN_082cfca8
    name: ft_module_get_service
    size: 216
    sig: >-
      void *ft_module_get_service(FtModule *module, const char *service_id)
    module: ft/module
    status: ported
    renames:
      param_1/r5: module
      param_2/r9: service_id
      iVar2/r0: module_class
      iVar3/r0: get_interface
      iVar6/r6: service
      puVar5/r4: module_slot
      puVar7/r7: module_slot_end
    notes: >-
      FreeType 2.3 ft_module_get_service (ftobjs.c). Raw ARM is exactly
      216 bytes from the push at 0x082cfca8 through pop {...,pc} at
      0x082cfd7c; its 0x082cfd80..0x082cfdb0 literal pool belongs to this
      function, while the three-word tail-call sibling starts at
      0x082cfdb4 (Ghidra instead starts its next function at 0x082cfdc0).
      A non-null module first calls its class's get_interface slot (+0x20)
      with (module, service_id). A NULL result triggers a linear scan of
      exactly library->num_modules entries from library->modules (+0x1c),
      excluding the source module, and returns the first non-NULL service.
      Module NULL returns NULL; service_id has no guard. The debug assertions
      report but do not repair a NULL class/callback before retailOS
      dereferences it; the port documents these as safety preconditions.
      Decoding every ARM B/BL word in osos.dec verifies eight direct callers,
      all plain unconditional bl (0x0807aa24, 0x080808d0, 0x0808257c,
      0x0808b598, 0x080928d4, 0x080a18ac, 0x080a809c, 0x080a81dc), with no
      predicated direct form. Host tests cover null module, the immediate
      primary answer, and null-primary fallback to a later module.
  - address: 0x082cfdc0
    ghidra: FUN_082cfdc0
    name: ft_service_list_lookup
    size: 76
    sig: >-
      void *ft_service_list_lookup(const FtServiceDesc *service_descriptors,
      const char *service_id)
    module: ft/service
    status: ported
    renames:
      param_1: service_descriptors
      param_2: service_id
      iVar1: strcmp_result
    notes: >-
      FreeType's internal ft_service_list_lookup (ftobjs.c): returns the
      service_data word from the first FT_ServiceDescRec whose serv_id
      strcmp-equals service_id. Raw ARM verifies r0/r1 NULL guards, an
      8-byte (two-word) record stride, a NULL serv_id terminator, and the
      call through thunk 0x08391e38 to retailOS strcmp @ 0x08391e44.
      The return is NULL for null inputs or no match. Host tests pin null
      inputs, a second-record match, missing identifiers, and case-sensitive
      strcmp comparison. match.py confirms the same guarded linear scan.

  - address: 0x082cfe0c
    ghidra: FUN_082cfe0c
    name: ft_synthesize_vertical_metrics
    size: 76
    sig: "void ft_synthesize_vertical_metrics(FtGlyphMetrics *metrics, i32 advance)"
    module: ft/metrics
    status: ported
    vars:
      param_1/r4: metrics
      param_2/r0: advance
      r5: height
      param_1[5]: vert_bearing_x
      param_1[6]: vert_bearing_y
      param_1[7]: vert_advance
    notes: >-
      FreeType 2.3-era ftobjs.c's ft_synthesize_vertical_metrics, exactly
      matching the historical upstream source and the FT_Glyph_Metrics
      eight-word layout. When advance is zero, computes wrapped signed
      `(height * 12) / 10` through __rt_sdiv; then stores only
      vert_bearing_x = -(width / 2), vert_bearing_y = (advance - height) / 2,
      and vert_advance = advance. Raw ARM uses signed truncate-toward-zero
      halves (`add sign-bit; asr #1`) and an ARM wrapping subtraction, both
      reproduced by the port. Direct glyph-loader callers at 0x0803d068,
      0x08080254, 0x08086ad8, and 0x080ae5dc pass a metrics record.

  - address: 0x082cfe58
    ghidra: FUN_082cfe58
    name: ft_validator_error
    size: 16
    sig: "void ft_validator_error(FtValidator *validator, i32 error) -> !"
    module: util/validator
    status: ported
    vars:
      param_1/r0: validator
      param_2/r1: error
      param_1[3]: error
      param_1+0x10: jump_buffer
    notes: >-
      FreeType ft_validator_error from ftobjs.c: stores error into the
      record at +0xc, then longjmp(&validator->jump_buffer @ +0x10, 1) —
      noreturn. Raw ARM body is exactly str r1,[r0,#12]; mov r1,#1;
      add r0,r0,#16; bl longjmp (0x08031748, already ported). Ghidra's
      16-byte extent is CORRECT: the 20 bytes after the bl are the adjacent
      out-of-line ft_validator_init body @ 0x082cfe68, not dead tail —
      Ghidra's decompile misreads them as this function's continuation,
      which is where its phantom param_3/param_4 come from; the real
      signature has two arguments. Call census from osos.dec: 41 sites, 8
      plain bl + 33 predicated (blcc/blhi/blne/...) — callers raise only
      when their own bounds/tag check fails; zero data-word references, so
      never dispatched indirectly. Port keeps the error store before the
      transfer and the val==1 argument; the divergence is modeled as -> !.
      Host tests drive a factored non-extern body with a mock longjmp
      dispatch (catch_unwind can't cross the extern frame — same pattern as
      runtime::exit's TERMINATE_HOOK) and pin the 60-byte record layout,
      the +0xc/+0x10 offsets, the env pointer, val==1, and store-before-
      transfer ordering. match.py: structural match modulo the crate-wide
      frame-pointer prologue (push {fp,lr}; mov fp,sp) that the accepted
      ft_validator_init sibling also emits.

  - address: 0x082cfe68
    ghidra: FUN_082cfe68
    name: ft_validator_init
    size: 20
    sig: "void ft_validator_init(FtValidatorPrefix *validator, u32 base, u32 limit, u8 level)"
    module: util/validator
    status: ported
    vars:
      param_1/r0: validator
      param_2/r1: base
      param_3/r2: limit
      param_4/r3: level
      param_1[0]: base
      param_1[1]: limit
      param_1[2]+0: level
      param_1[3]: error
    notes: >-
      FreeType `ft_validator_init` from ftobjs.c. Raw ARM is exactly
      `stmia r0,{r1,r2}; strb r3,[r0,#8]; str r1,[r0,#12]; bx lr`,
      initializing the 16-byte prefix of FT_ValidatorRec: two target-width
      table-bound addresses, the one-byte FT_ValidationLevel, and
      FT_Err_Ok. Bytes +9..+11 (the word-alignment padding) are deliberately
      untouched. The two direct CMap-table callers at 0x08078c70 and
      0x080b1bf4 pass a subtable base, its limit, and level 0. Its adjacent
      0x082cfe58 sibling is ft_validator_error: it stores an FT_Error into
      the record at +0xc and longjmps through the jump buffer at +0x10.


  - address: 0x08291c24
    ghidra: FUN_08291c24
    name: string_view_construct
    size: 148
    sig: "StringView *string_view_construct(StringView *view, ResourceProvider *resources, void *controller, void *parent, const StringViewSpec *spec)"
    module: ui/string_view
    status: ported
    renames:
      param_1: view
      in_stack_00000000: spec
      puVar1: view
      iVar2: member_cursor
      iVar3: view_recovered
    notes: >-
      Constructor of the 0x220-byte 'Str ' string view - the retailOS
      screen element that displays a resource-resolved string in a
      resource-resolved typeface and re-evaluates itself on an embedded
      timer. Ghidra's 148-byte extent is exactly right for once:
      0x08291c24..0x08291cb8 code ending `ldmia sp!,{r3,r4,r5,pc}`,
      then the 4-byte vtable literal 0x089a7178 @ 0x08291cb8, and the
      deleting destructor @ 0x08291cbc (which Ghidra does not list)
      starts immediately after. 32 bl call sites, all unconditional,
      verified by decoding every B/BL word in osos.dec; one is the new
      wrapper @ 0x082907a4 (operator_new(0x220), then this, forwarding
      all four register arguments with the spec stacked), the rest are
      inlined copies of that pair in the 0x0821xxxx..0x0826xxxx screen
      builders. Ghidra's C types it `int FUN_08291c24(void)` - it
      actually takes four register arguments plus the stacked spec and
      returns `view` (the closing sub r4,r0,#0x1e0 / mov r0,r4).
      Class identity comes from the post-construction resolve step
      0x08290f6c -> 0x08290d7c: it compares the view's class code
      (view+0x40, from spec+0x04) against 'Str ' (literal 0x53747220 @
      0x08290f5c), resolves spec+0x58 as 'Str ' or 'StSt'
      (string-style) and spec+0x5c as 'Type' (typeface) into the
      three-word resource reference at view+0xf8, and sets the
      resolved byte at view+0x1f0. Layout (confirmed by the unlisted
      destructor @ 0x08291cd4, which replants the same vtable from its
      own literal @ 0x08291d84): vtable +0x00, grand-base view
      subobject (ctor 0x0826f26c) to +0xa4, draw state +0xa4 (0x54),
      resource_ref +0xf8 (3 words), StringObjects +0x104/+0x10c, four
      owned pair-header service objects +0x114..+0x120 (born NULL, dtor
      pair_header_destruct + operator delete each), PairHeaderBase
      +0x124 (0xb8), cleared word +0x1dc, ObservableArray +0x1e0 (16),
      resolved flag +0x1f0, embedded 0x2c-byte timer +0x1f4 built by
      timer_schedule_shim(view, view+0x1f4, 0, 0) with the view as its
      config word. Callees: draw_state_construct, string_default_construct
      x2, pair_header_base_construct, observable_array_construct and
      timer_schedule_shim are already ported and called directly; the
      grand-base ctor 0x0826f26c and the resolver 0x08290f6c ride the
      new STRING_VIEW_OPS seam (firmware transmute on target, panic on
      host, event_list.rs pattern); the fully decoded 12-byte
      three-word clear 0x081e170c rides the same seam with a
      behaviour-identical default rather than a stub. Deviations: the
      port keeps `view` in hand instead of the original's r0 byte
      arithmetic (sub r0,r0,#0x10c / #0x124 / sub r4,r0,#0x1e0), which
      would be wrong on a 64-bit host (styled_text_view precedent);
      the vtable is the u32 ROM address STRING_VIEW_VTABLE_ADDRESS -
      the image words at 0x089a7178 are stale RW init data (they
      decode as mid-function fragments), same finding as
      styled_text_view's vtable. Host tests use the unique
      hints::STRING_VIEW slab (0x27000000) with the fixture at slab+4
      so the ported string_default_construct's host-width pointer
      stores land 8-aligned; the string payload stores' overlap of the
      following members on 64-bit hosts is covered by the later
      construction steps in both the original and the port and is
      documented in the module header. match.py structural: same
      offsets (+0xa4/+0xf8/+0x104/+0x10c/+0x114..+0x120/+0x124/+0x1dc/
      +0x1e0/+0x1f4), same call order, same vtable literal; the diff
      is the crate fp frame, the ops-table blx indirections and LLVM
      inlining timer_schedule_shim's argument rotation.

  - address: 0x08292a70
    ghidra: FUN_08292a70
    name: wheel_sample_is_touched
    size: 24
    sig: "u32 wheel_sample_is_touched(u8 *sample)"
    module: kernel/wheel_sample
    status: ported
    renames:
      param_1: sample
      FUN_08292a88: wheel_sample_capture
    notes: >-
      Raw ARM verifies 24 bytes exactly, 0x08292a70..0x08292a88, with no
      literal pool: `push {r4,lr}; mov r4,r0; bl 0x08292a88; ldr
      r0,[r4,#0x10]; and r0,r0,#0x40000000; pop {r4,pc}`. Binary scanning
      every ARM B/BL encoding found 13 direct BL call sites, all
      unconditional (no predicated BL); no image word points at this
      function, so there is no virtual-dispatch seam. Algorithm: lazily
      fill the wheel event sample, then return the state word's
      0x40000000 finger-on-wheel bit unchanged (zero or 0x40000000).
      The function has no NULL guard; callers branch on its bitmask result
      before acting on an active wheel. Deliberate deviations: none. Host
      tests cover first-use capture, masking unrelated state bits, and
      pre-captured touched and untouched states without helper calls.

  - address: 0x08292a88
    ghidra: FUN_08292a88
    name: wheel_sample_capture
    size: 64
    sig: "i32 wheel_sample_capture(u8 *sample)"
    module: kernel/wheel_sample
    status: ported
    vars:
      param_1: sample
      FUN_080dc8a4: sample_capture (WHEEL_SAMPLE_OPS.capture slot) -
        latches elapsed ticks + wheel state into sample+0xc/+0x10
      wheel_sample_rate: ported direct call - the 16-sample averaged
        inter-event tick delta
    notes: >-
      Lazy capture of the click-wheel event sample the control
      subsystem hands to UI controllers. 64 bytes exactly,
      0x08292a88..0x08292ac8, no literal pool (the next body opens at
      0x08292ac8). 54 bl call sites. Algorithm: if the flag byte at
      sample+8 is 0, call 0x080dc8a4(&sample[0xc], &sample[0x10])
      (elapsed + state), store 0x080bd8f0() at sample+0x14 (rate), set
      the flag to 1; then ALWAYS return the sign-extended low halfword
      of the word at +0xc (`ldr r0,[r4,#0xc]; mov r0,r0,lsl#16; mov
      r0,r0,asr#16`).
      The object is a wheel event sample: +0x10's bit 0x40000000 is
      the finger-on-wheel gate every consuming site tests (`tst
      r0,#0x40000000`, e.g. 0x0810c9f0); +0x14 is a velocity measure
      (0x080bd8f0 shifts the current deadline tick to its previous slot,
      expires the ring after a 1,000,000-tick gap, else averages it);
      ring is fed by sampler 0x080877a0 whose only caller 0x0809e550
      unwraps a 96-count rotary position delta (cmp #0x48; subgt
      #0x60; cmn #0x48; addlt #0x60) off timer 0x3c700000+0xb4 - the
      click-wheel evidence. Consumers are vtable-reached UI controller
      methods ("EnterVolume"/"EnterDefault" handlers, e.g.
      0x0810c9dc, which has no direct reference image-wide).
      NO CALL SITE CONSUMES THE RETURN: at all 54 sites the next
      instruction ignores r0 and re-reads the record's fields (binary
      survey) - the call's observable job is the lazy fill, hence the
      capture name; the halfword return is preserved because the
      original produces it. The sibling @ 0x08292adc (bl here; ldr
      r0,[r4,#0x14]) is the rate getter built on this capture.
      Deviation: only unported 0x080dc8a4 rides the WHEEL_SAMPLE_OPS seam;
      the ported rate helper uses that table solely to mock its ROM
      tick_elapsed dependency on host. Host tests prove the direct rate call
      follows capture, second/pre-seeded calls skip it, and the return
      sign-extends the low halfword (0x8000 -> -0x8000, high halfword
      ignored). match.py structural: flag-test/capture/rate/store/reload/lsl/
      asr shape; the capture dependency table and rate's deadline seam stand
      in for their direct firmware calls.

  - address: 0x08292adc
    ghidra: FUN_08292adc
    name: wheel_sample_get_rate
    size: 20
    sig: "u32 wheel_sample_get_rate(u8 *sample)"
    module: kernel/wheel_sample
    status: ported
    renames:
      param_1: sample
      FUN_08292a88: wheel_sample_capture
    notes: >-
      Raw ARM verifies 20 bytes exactly, 0x08292adc..0x08292af0, with no
      literal pool: `push {r4,lr}; mov r4,r0; bl 0x08292a88; ldr
      r0,[r4,#0x14]; pop {r4,pc}`. Binary scanning every ARM B/BL encoding
      found 19 direct BL call sites, all unconditional (no predicated BL).
      Algorithm: lazily capture the wheel sample through
      wheel_sample_capture(sample), then return the full u32 rate at
      sample+0x14. The getter has no NULL guard; the underlying capture
      owns the first-use fill and flag gate at sample+0x08. No data word in
      the image points at this function, so there is no virtual-dispatch
      seam. Deliberate deviations: none. Host tests cover first-use capture
      returning its computed rate and a pre-captured sample preserving an
      arbitrary full-width (0xfffffffe) rate without invoking either helper.

  - address: 0x080bd8f0
    ghidra: FUN_080bd8f0
    name: wheel_sample_rate
    size: 96
    sig: "u32 wheel_sample_rate(void)"
    module: kernel/wheel_sample
    status: ported
    vars:
      DAT_080bd950: inactivity_span_ticks (1000000)
      DAT_080bd954: wheel_timing_history (0x089caedc)
      DAT_080bd958: wheel_delta_ring (0x08a755fc)
      uVar3: previous_tick
      iVar4: history_expired
      uVar5: delta_sum
    notes: >-
      96 code bytes at 0x080bd8f0..0x080bd950 followed by a 12-byte
      literal pool. The direct control-subsystem caller is
      wheel_sample_capture @ 0x08292a88 (its 54 vtable-reached UI
      callers consume the stored result at sample+0x14); the sibling
      wheel position sampler @ 0x080877a0 feeds the ring but is not
      ported here. The function loads timing history +0x08, copies +0x04
      over it, then calls thunk 0x08037eb8 / ROM 0x22001ee8 with the old
      tick and 1000000. A nonzero result clears all 64 bytes at
      0x08a755fc through thunk 0x08037db8 / ROM 0x2200027c and returns
      zero. Otherwise it adds all sixteen u32 entries with ARM wrapping
      arithmetic and returns sum >> 4. It does not sample hardware or
      mutate the ring cursor. The Rust port reads those fixed RAM globals
      only on target; host tests provide backing storage and mock the ROM
      deadline check. They prove the history shift, deadline arguments,
      live average, expiration clear, and wrapping sum. match.py
      structural diff reviewed after the release ARM build.

  - address: 0x08292d08
    ghidra: FUN_08292d08
    name: controller_history_select_with_callback
    size: 20
    sig: "void controller_history_select_with_callback(void)"
    module: app/controller_history_select
    status: ported
    renames:
      DAT_08292d18: controller_history_resource_id
      FUN_08292d1c: controller_history_select
    notes: >-
      Raw ARM verifies a 20-byte extent at 0x08292d08..0x08292d1c: `ldr
      r0,[pc,#8]; mov r2,#0; mov r1,#1; b 0x08292d1c` followed by private
      resource-id literal 0x0dad073a. The next function begins at
      0x08292d1c, so Ghidra's 212-byte extent is incorrect. Scanning every
      ARM B/BL encoding finds 13 direct BL call sites: ten unconditional
      BL, two BLEQ, and one BLNE; it also finds one BNE tail caller. The
      wrapper has no guard; predicated callers supply their own gates.
      Algorithm: select the controller-history resource through the shared
      helper with history flag 1 and skip-callback flag 0, installing its
      CntrlHistoryFn callback after selection. Deliberate deviation: the ARM
      port uses a literal veneer for the stock helper because a payload
      cannot retain the retail PC-relative tail branch; host builds call a
      volatile callback seam. Host tests prove the exact resource id and
      complementary (1,0) policy flags. match.py structural review is
      recorded after the release ARM build.
  - address: 0x08292e58
    ghidra: FUN_08292e58
    name: controller_history_select_without_callback
    size: 20
    sig: "void controller_history_select_without_callback(void)"
    module: app/controller_history_select
    status: ported
    renames:
      DAT_08292e68: controller_history_resource_id
      FUN_08292d1c: controller_history_select
    notes: >-
      Raw extent is 20 bytes at 0x08292e58..0x08292e6c: four code
      instructions and the resource-id literal 0x0dad073a, contrary to
      Ghidra's 16-byte extent. A complete ARM B/BL word scan found 25
      direct bl callers, all unconditional; no predicated bl or tail b
      callers. It loads that private controller-history resource id,
      passes policy flags (0, 1), and tail-branches to the unported shared
      selection helper 0x08292d1c. Those flags make the helper select the
      controller but skip its CntrlHistoryFn callback allocation. The ARM
      port keeps the tail transfer through a literal veneer because the
      payload cannot retain the source-relative b; host tests replace that
      helper with a recorder and prove the id and both flags. match.py
      against this worktree archive and the verified 20-byte extent reports
      the same ldr/mov/mov shape plus the relocation-safe veneer; its exit 1
      is the expected structural diff.

  - address: 0x08292e6c
    ghidra: FUN_08292e6c
    name: control_state_flags
    size: 20
    sig: "u32 control_state_flags(void)"
    module: kernel/control_state
    status: ported
    notes: >-
      253 bl call sites. Loads the word at 0x089cc92c (= *0x089cc928
      literal + 4) and returns its low 12 bits (`lsl #20; lsr #20`) -
      a packed-field getter on the control-subsystem state object
      (callers test bits, e.g. flags & 0x10 @ 0x081f6344; the sibling
      setter @ 0x08292e84 stores the full word with bit 0x4000 as a
      conditional-store sentinel above this mask). Individual bit
      meanings stay with the unported writers of 0x089cc92c. Port reads
      the original firmware address on target (the global is not
      port-owned); host tests substitute a mock word. match.py - same
      load-and-mask leaf; LLVM materializes the mask and uses `and`
      instead of the lsl/lsr pair, plus its usual fp frame.

  - address: 0x08292e84
    ghidra: FUN_08292e84
    name: control_state_store
    size: 44
    sig: "void control_state_store(u32 word)"
    module: kernel/control_state
    status: ported
    renames:
      param_1: word
    notes: >-
      3 bl call sites (plus 1 blne @ 0x0839f6ec and 1 tail b @
      0x08292ce4). Guarded store of the control-state word @ 0x089cc92c:
      calls the lock-check helper @ 0x08292f58 (same word, bit 0x8000 =
      store lock); while locked the store is refused unless the caller
      sets bit 0x4000 in the argument as a "store anyway" override
      sentinel; the stored value always has 0x4000 stripped
      (`bic r0, r1, #0x4000`), so the sentinel never lands in the word
      and sits above the getter's low-12 mask. The save/force-0x10/
      restore dance in neighbors @ 0x08292c10..0x08292e64 (restoring via
      `saved | 0x4000`) uses exactly this override. Port reads/writes the
      original firmware address on target (global not port-owned, same
      precedent as control_state_flags); host tests substitute the mock
      word. The lock-bit read (bl 0x08292f58) goes through the ported
      control_state_is_locked; LLVM inlines it on ARM. match.py - same
      guard/branch/store structure; LLVM inlines the lock check,
      reorders the two bit tests, folds base+4 into a direct 0x089cc92c
      literal, plus its usual fp frame.

  - address: 0x08292f58
    ghidra: FUN_08292f58
    name: control_state_is_locked
    size: 20
    sig: "u32 control_state_is_locked(void)"
    module: kernel/control_state
    status: ported
    notes: >-
      1 bl call site (@ 0x08292e8c, from control_state_store; Ghidra's
      decompiled cross-references list further callers, but a binary
      scan of osos shows only the one). Store-lock query for the
      control-state word @ 0x089cc92c: loads the object base literal
      (0x089cc928), loads the word at +4, `and #0x8000; lsr #15` -
      returns the store-lock bit as a 0/1 value, the guard the setter
      consults before requiring the 0x4000 override sentinel. Port reads
      the original firmware address on target (global not port-owned,
      same precedent as control_state_flags); host tests substitute the
      mock word. match.py - same load-and-mask-to-0/1 leaf; LLVM folds
      base+4 into a direct 0x089cc92c literal and merges the and/lsr
      pair into one barrel-shifter `and r0, r1, r0, lsr #15`, plus its
      usual fp frame.

  - address: 0x0824f1e8
    ghidra: FUN_0824f1e8
    name: video_frame_dispatch_operation
    size: 96
    sig: "void video_frame_dispatch_operation(void *engine, void *frame, u32 operation)"
    module: util/video_engine
    status: ported
    renames:
      param_1/r0: engine
      param_2/r1: frame
      param_3/r2: operation
      r3: dispatch_context
      r4: frame
      r5: operation
    notes: >-
      Raw ARM extent is exactly 96 bytes at 0x0824f1e8..0x0824f248; the
      separately linked next function starts with push {r4,r5,r6,r7,lr} at
      0x0824f248, matching Ghidra's size. A complete scan of every ARM B/BL
      immediate in osos.dec finds 9 direct inbound bl calls, all
      unconditional, at 0x0824d720, 0x0824d768, 0x0824dc00, 0x0824dc1c,
      0x0824dc2c, 0x0824dc3c, 0x08251b94, 0x08251bb0, and 0x08251bc0;
      no predicated bl, immediate blx, direct tail b, or aligned DATA word
      targets the entry. It suppresses an operation only if its low two bits
      overlap frame+0x90, otherwise computes context as
      engine + (signed(engine+0xa98) >> 1), dispatches either the direct
      engine+0xa94 callback or a low-two-bit-masked table offset selected by
      engine+0xa98 bit 0, then re-reads frame+0x90 after the callback and
      ORs operation & 3 into that live value. The callback receives the full
      operation word. Target Rust reads the recovered target-width fields and
      raw callback word; host fixtures use a widened structural engine/table
      so native function pointers survive, translating the target byte table
      offset to a word slot. Tests cover direct dispatch, indirect tagged-table
      dispatch and low-bit masking, suppression of an already-pending
      operation, preservation of callback-mutated pending state, and
      zero-operation dispatch with no state change.
  - address: 0x0824f388
    ghidra: FUN_0824f388
    name: latch_first_error
    size: 24
    sig: "void latch_first_error(u32 *slot, u32 code)"
    module: util/error_latch
    status: ported
    renames:
      param_1/r0: slot
      param_2/r1: code
      r2: current
    notes: >-
      24-byte sticky first-error latch: `if (code != 0 && *slot == 0)
      *slot = code;` (cmp r1,#0 / bxeq lr / ldr r2,[r0] / cmp r2,#0 /
      streq r1,[r0] / bx lr). Zero doubles as "no error", so a zero code
      is never recorded and an already-latched slot is never overwritten -
      the earliest nonzero error sticks until the caller clears the slot.
      Extent self-verified: next function starts @ 0x0824f3a0, matching
      Ghidra's 24 bytes exactly; Ghidra's C is faithful. Call count
      binary-verified B/BL scan of osos.dec: 22 sites (20 plain bl + 2
      blcs @ 0x0824fb40 and 0x0825314c, both gating the call on their own
      unsigned range check `id - 0x84c0 >= 2` and passing the
      unknown-property code 0x500 - the callee itself has no flag guard,
      only the zero-code and first-wins guards). All 22 callers sit in
      the video-engine cluster 0x0824d224..0x082551e0 (see
      util/video_engine); 0x500 is that engine's unknown-property /
      out-of-range error code. No callees, no globals, no literal pool,
      no deviations. Host tests cover zero-code no-op, first-error-wins,
      zero-never-clears, boundary codes (1, 0x80000000, 0xffffffff), and
      a slot x code combination sweep against the recovered C expression.
      match.py (exit 1, diff expected): same cmp/bxeq/ldr/cmp/streq/bx
      core; the only diff is LLVM's usual fp/lr frame.

  - address: 0x08252bec
    ghidra: FUN_08252bec
    name: video_engine_get
    size: 12
    sig: "u8 *video_engine_get(void)"
    module: util/video_engine
    status: ported
    notes: >-
      146 bl + 1 tail b call sites (plus the 4-byte alias thunk
      `b 0x08252bec` @ 0x082cafbc). Bare singleton getter:
      `return *(void **)0x089ca8a8` (ldr literal / ldr [r0] / bx lr);
      NULL means "no video session" and every caller checks. The
      singleton is the VIDEO PLAYBACK ENGINE instance, named from its
      own methods and installers: the only setter @ 0x08252d4c
      (release-old/retain-new via 0x0824ddf0) is called from the media
      view controller's setup @ 0x08295ae0 while it builds a 320x240
      (0x140x0xf0, strh immediates @ 0x08295b58/0x08295b60) display-
      layer output path; the frame-advance method @ 0x08252aXX runs a
      ring of three frame slots (index @ +0xab4 mod 3), passing the
      current, (cur-1)%3 and (cur-2)%3 slots to the 1304-byte 3-plane
      fixed-point (20.12) scaler/compositor @ 0x08251894 - the
      prev/cur/next cadence of temporal deinterlacing of planar video;
      the object carries a property bag keyed by 16-bit ids (0x8892 ->
      +0xad8, 0x8893 -> +0xadc @ 0x0825360c; 0x88e4/0x88e8 @ 0x0824ce14;
      0x3059/0x305a -> +0xae0/+0xae4 @ 0x082cafc8) with 0x500 as the
      unknown-property error code (0x0824f388), and an active flag @
      +0xb50 whose clearing tears down the frame buffer. The bulk of
      the call sites are the public C wrappers @ 0x082d0c68..0x082d23ec,
      all `if (video_engine_get()) method(instance, ...)` thunks.
      Deviation (kernel/control_state.rs precedent): on target the port
      reads the original firmware slot @ 0x089ca8a8 (the owning setter
      is unported, so the port must not keep a copy); host tests
      substitute a mock pointer via set_mock_instance. match.py - same
      two-load leaf plus the usual LLVM fp frame; exit-1 diff is the
      frame only.

  - address: 0x082d2314
    ghidra: FUN_082d2314
    name: video_engine_set_property
    size: 48
    sig: "void video_engine_set_property(u32 command, u32 key, u32 value)"
    module: util/video_engine
    status: ported
    renames:
      param_1/r0: command
      param_2/r1: key
      param_3/r2: value
    notes: >-
      48-byte public C wrapper for the video engine's property-set
      command: `engine = video_engine_get(); if (engine)
      dispatch_0x08250498(engine, command, key, value)` — push r4-r6/lr,
      spill the three args, bl the getter, and on non-NULL restore and
      TAIL-branch (bne) to the dispatcher with the instance prepended;
      a NULL instance is a silent no-op (no error latch). Extent
      self-verified: next function @ 0x082d2344 is the sibling wrapper
      (identical prologue, tail target 0x08250628), so Ghidra's 48
      bytes are exact. Call count binary-verified B/BL scan of osos.dec:
      12 sites, ALL plain bl, zero predicated forms — the wrapper itself
      owns the NULL check, so callers never gate the call. All 12 sites
      pass command 0xde1: six in the engine setup @ 0x0825bd98 (as
      0x201 + 0xbe0; keys 0x2800/0x2801/0x2802/0x2803, the latter two
      computed as `0x2801 ^| (0xde1 >> 10)`, values 0x2600/0x2601/0x812f)
      and six in the 0x08281030 cluster (literal 0xde1). The tail target
      0x08250498 (unported) is the property dispatcher: any command other
      than 0xde1 latches the unknown-property error 0x500 via
      latch_first_error (0x0824f388); command 0xde1 resolves the current
      frame slot (0x08252bfc: table @ instance+0xa8c indexed by
      instance+0x24c) and writes one parameter byte per key — 0x2800 to
      slot+0x105 (1 for value 0x2600, 2 for 0x2601), 0x2801 to slot+0x104
      plus slot+0x106 (mapping values 0x2600/0x2601/0x2700/0x2701/0x2702),
      0x2802/0x2803 to slot+0x107/+0x108 via resolver 0x08260448, 0x8191
      to the instance bool @ +0x90a; unknown key/value pairs also latch
      0x500. Ghidra's C for FUN_082d2314 inlines the whole tail target
      into the wrapper body (its WARNING about an unreachable block at
      0x0824f38c is that inlining artifact) — the listing above is from
      the raw bytes. Deviation: the dispatcher is unported, so on target
      the port calls the original firmware body at 0x08250498 through the
      transmute seam (app/command_dispatch.rs precedent); host tests
      install a recording mock via set_mock_dispatch and the NULL path
      never touches the seam. Host tests cover NULL-instance silent
      no-op, instance-prepended verbatim argument pass-through (incl. 0
      and 0xffffffff), per-call instance re-read across swap and
      teardown. match.py (exit 1, diff expected): same structure — LLVM
      inlines the getter (two loads off literal 0x089ca8a8) and emits
      the tail dispatch as `bx ip` to literal 0x08250498 instead of
      `b`; frame is the usual fp/lr.

  # --- Region survey: 0x08300000-0x083fffff (agent hi6) ---
  # 3481 Ghidra functions, ~3439 unclaimed at survey time. Sub-blocks:
  #   0x08300000-0x0835ffff  sparse (~200 fns) - misc app/service code.
  #   0x08360000-0x0838ffff  (~650 fns) EMBEDDED SQLITE (~3.5.x), the
  #     media database - see src/sqlite/mod.rs. Identified from the
  #     string pool in the range: sqlite_master / sqlite_temp_master /
  #     sqlite_sequence / sqlite_stat1 / sqlite_autoindex_, the whole
  #     sqlite3ErrStr table (0x083763c4..0x083766e4), the PRAGMA name
  #     table (0x0837f5fc..0x083811ac), the parser diagnostics and the
  #     VACUUM script "ATTACH '' AS vacuum_db;". The "record container
  #     with 20-byte elements" of the earlier survey is the VDBE code
  #     builder: 20 bytes is sizeof(VdbeOp) and 0x08386840 is
  #     sqlite3VdbeAddOp3. Two NON-SQLite islands sit inside the span:
  #     the ATA/CE-ATA driver block ~0x08369000-0x0836c000 (strings
  #     "IAD: failed to write ATA task file", "MMC init failed"), which
  #     is where the S5L8702 GPIO helper 0x0836b5b0 lives, and
  #     FreeType/UI fragments above 0x0838f000.
  #   0x08390000-0x083bffff  (~1130 fns) C/C++ runtime + string cluster:
  #     plain byte-loop strcmp 0x08391e44 / strlen 0x08392478, a
  #     table-driven string hash 0x08391dec, tagged free veneers
  #     (0x08391d24 = free tag 0x39), array delete with byte accounting
  #     (0x083906f4).
  #   0x083c0000-0x083dffff  (~1000 fns) C++ standard library: a COW
  #     (libstdc++-style) basic_string with a 12-byte _Rep header
  #     {refcount, capacity, length} living 12 bytes below the char data,
  #     _S_empty_rep_storage in RAM at 0x08b31804 (data 0x08b31810);
  #     plus a deque/vector family and per-instantiation template member
  #     families. SWEPT (agent cxxlib): the whole string class is ported
  #     (construction, COW sharing, the mutation core @ 0x083d865c and
  #     the members on it, and std::less) and six template families are
  #     ported one canonical symbol each - see the cxx/templates block
  #     below for the alias maps. Correction to this survey: the
  #     "~60 byte-identical handle-deref accessors" is wrong; a
  #     byte-pattern scan of the image finds exactly 22, one of them
  #     outside this block, and they are all ported as
  #     handle_deref_or_null.
  #   0x083e0000-0x083edfff  ARM ADS fplib (already largely ported).
  #   0x083f0000-0x083fffff  (~100 fns) plus 256-byte permutation tables
  #     at 0x083ffd5c/0x083ffe5c.
  # Shared 256-byte byte-map at runtime address 0x088faa8b: indexed by
  # `ldrb [tbl, c]` from 9 sites (string hash 0x08391dec, sqlite3StrICmp
  # 0x08384f14 / sqlite3StrNICmp 0x08384fa0, 0x0838d874, 0x082cd880,
  # 0x082d77ec, 0x082dfdbc, 0x0836e600, 0x0836e9cc). RESOLVED (agent
  # media): it is sqlite3UpperToLower - the identity map except
  # 'A'..'Z' -> 'a'..'z'. It is NOT missing from the image; above the
  # read-only image the decrypted body's addresses are skewed, and the
  # datum a firmware pointer A names lives at image address A + 0xaed8.
  # Evidence:
  #   * 0x088faa8b + 0xaed8 = 0x08905963, which holds sqlite3UpperToLower
  #     byte-for-byte - and that byte sequence occurs exactly ONCE in the
  #     whole 10 MB image and is named by no pointer literal, i.e. it is
  #     the initializer for the address these functions use.
  #   * 0x089cb1bc + 0xaed8 = 0x089d6094, which holds the pool alignment
  #     table {4,!3} {16,!15} {32,!31} {1024,!1023} - the exact bytes
  #     heap/pool.rs documents as "recovered from a serialized copy @
  #     0x089d6094". So heap/pool.rs's "the 0x089cb1xx page is
  #     re-initialized at runtime" and this note describe ONE skew, not
  #     two runtime-init phenomena. The same applies to
  #     heap/block_region.rs's 0x089cb1b8 fallback.
  #   * Every pointer literal into 0x088e0000-0x08920000 resolves to a

  #     readable C string only at +0xaed8.
  # Rule of thumb for anything above ~0x088e0000: read the image at
  # `addr + 0xaed8`, not at `addr`.

  - address: 0x0835443c
    ghidra: FUN_0835443c
    name: copy_encoded_word_block
    size: 124
    sig: "u32 copy_encoded_word_block(EncodedWordBlock *destination, const EncodedWordBlock *source)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: source
      param_2_plus_0: encoded_count
      param_2_plus_4: words
      DAT_083544b8: encoded_count_multiplier
      iVar1/r0: decoded_count
      iVar2/r12: word_count
    notes: >-
      Raw ARM confirms the 124-byte instruction body at
      0x0835443c..0x083544b8, followed by its literal-pool multiplier
      0x0a7e377f at 0x083544b8; the next function starts at 0x083544bc.
      A complete B/BL decode of osos.dec finds exactly 38 direct call
      sites, all unconditional bl (no predicated calls or tail branches).
      The source header is {encoded_count, words}: `muls` decodes its
      signed count modulo 2^32, `rsbmi` takes the wrapping signed
      magnitude, and `cmp #0x1c; movgt` rejects magnitudes above 28 with
      status 1 before any destination write. A non-aliasing destination
      receives the raw encoded count then every decoded word in descending
      index order; aliasing headers succeed without a write. The
      otherwise-unidentified object family warrants a structural name.
      i32::MIN preserves the ARM wrapping-absolute-value corner case:
      its signed compare accepts it and a non-aliasing call attempts
      2^31 descending copies. match.py is structurally equivalent
      (multiply/range guard, backwards copy loop); its exit-1 mnemonic
      diff is expected.

  - address: 0x0833e83c
    ghidra: FUN_0833e83c
    name: copy_encoded_word_block_checked
    size: 124
    sig: "u32 copy_encoded_word_block_checked(EncodedWordBlock *destination, const EncodedWordBlock *source)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: source
      param_2_plus_0: encoded_count
      param_2_plus_4: words
      DAT_0833e8b4: encoded_count_multiplier
      iVar1/r2: decoded_count
      iVar2/r3: word_count
    notes: >-
      Raw ARM confirms a 120-byte instruction body at
      0x0833e83c..0x0833e8b4, followed by the literal-pool multiplier
      0x0a7e377f at 0x0833e8b4; the next separately linked function starts
      at 0x0833e8b8, for a 124-byte full extent. Decoding every ARM B/BL
      word in osos.dec finds exactly 11 direct inbound calls, all
      unconditional bl and zero predicated forms; no plain B or DATA-word
      reference targets this entry. It is the same bounded EncodedWordBlock
      copy algorithm as copy_encoded_word_block @ 0x0835443c: multiply the
      source encoded_count by 0x0a7e377f, reject when its signed wrapping
      magnitude exceeds 28 before touching destination, otherwise copy the
      raw header and payload words from high index to low. The range guard
      precedes the alias check, so an aliased out-of-range block returns 1;
      i32::MIN retains the ARM wrapping-negation corner case. The target-only
      unique Rust section prevents coalescing this separately linked,
      byte-identical sibling with copy_encoded_word_block. Host tests cover
      positive and negative decoded counts, both range edges, rejection
      without writes, descending overlapping copies, and the i32::MIN
      self-copy. match.py structural diff (exit 1 expected) was reviewed.

  - address: 0x083544bc
    ghidra: FUN_083544bc
    name: encoded_word_block_sign
    size: 76
    sig: "i32 encoded_word_block_sign(const EncodedWordBlock *block)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: block
      DAT_08354508: ENCODED_COUNT_MULTIPLIER (0x0a7e377f)
      iVar1/r0: is_zero (limb-zero scan result)
      uVar2/r4: sign
    notes: >-
      Sign of an EncodedWordBlock viewed as a signed multi-limb
      integer: 0 when every decoded limb is zero, +1 when the decoded
      header count is positive, -1 otherwise. Raw ARM confirms the
      76-byte instruction body 0x083544bc..0x08354508 followed by its
      literal-pool multiplier 0x0a7e377f at 0x08354508; the next
      function's push prologue starts at 0x0835450c, so the full
      extent is 80 bytes (Ghidra's 76 drops the pool word and
      mislabels it as the global DAT_08354508). Call count verified by
      decoding every ARM B/BL word in osos.dec: exactly 12 direct call
      sites, all unconditional bl, 0 predicated forms; the address
      occurs in no data word, so it is not dispatched virtually.
      Algorithm: call ported limb-zero scan encoded_word_block_is_zero and
      return 0 for ANY nonzero answer; otherwise reload the header and return
      the sign of encoded_count * 0x0a7e377f (signed; a non-positive product
      maps to -1). The 0x082fc770 scan decodes the count with wrapping
      absolute value, scans words high-to-low, and treats a word as nonzero
      iff word * 0x76b4197f != 0; that key is odd and therefore invertible
      modulo 2^32. A zero decoded count and i32::MIN return 1 without reading
      words; other negative decoded counts scan their absolute count. The
      port now calls that Rust export directly on host and target, retaining
      the post-call header reload. match.py is structurally equivalent
      (direct call, header reload, multiply, sign select; the zero scan's
      target-side volatile multiplier load preserves its mul/cmp loop);
      its exit-1 mnemonic diff is expected.

  - address: 0x08322bcc
    ghidra: FUN_08322bcc
    name: copy_encoded_word_block_from
    size: 124
    sig: "u32 copy_encoded_word_block_from(const EncodedWordBlock *source, EncodedWordBlock *destination)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: destination
      param_1_plus_0: encoded_count
      param_1_plus_4: words
      DAT_08322c44: encoded_count_multiplier
      iVar1/r2: decoded_count
      iVar2/r3: word_count
    notes: >-
      Raw ARM confirms the 120-byte instruction body at
      0x08322bcc..0x08322c44 followed by its literal-pool multiplier
      0x0a7e377f at 0x08322c44; the next function starts at 0x08322c48,
      so the full extent is 124 bytes (Ghidra's 120 drops the pool word).
      A complete B/BL decode of osos.dec finds exactly 33 direct call
      sites, all unconditional bl (no predicated forms); the address
      occurs in no data word, so it is not dispatched virtually.
      Same algorithm as copy_encoded_word_block (0x0835443c) with the
      parameter order swapped: r0 is the source (ldr ip, [r0] reads its
      header, the loop reads *(r0 + 4)) and r1 is the destination
      (str ip, [r1] writes its header, the loop writes *(r1 + 4)).
      Call sites confirm: 0x082ebbcc copies six blocks from an input
      array into a crypto context's 0x50..0x70 slots, source first.
      The range guard (signed wrapping magnitude of encoded_count *
      0x0a7e377f must be <= 28) runs BEFORE the alias check, so an
      aliased out-of-range header still returns 1; i32::MIN keeps the
      ARM wrapping-negation corner case. Not folded onto
      copy_encoded_word_block: both symbols resolve distinctly in
      librustypod.a. match.py is structurally equivalent
      (multiply/range guard, alias check, descending copy loop); its
      exit-1 mnemonic diff is expected.

  - address: 0x083555f8
    ghidra: FUN_083555f8
    name: encoded_word_block_set_int
    size: 116
    sig: "u32 encoded_word_block_set_int(i32 value, EncodedWordBlock *destination)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: value
      param_2/r1: destination
      DAT_0835566c: ENCODED_WORD_MULTIPLIER (0xd561a67f)
      DAT_08355670: ENCODED_COUNT_INVERSE (0xed99887f)
      iVar1/sl: ENCODED_WORD_MULTIPLIER (0xd561a67f)
      iVar2/r0: limb (wrapping absolute value, then shifted-out high part)
      iVar3/r4: count (limb count, negated when value < 0)
      iVar4/r5: high (arithmetic limb >> 31, then 0)
    notes: >-
      Producer half of the EncodedWordBlock family: initializes a block
      from a plain signed 32-bit integer, always returning 0. Raw ARM
      confirms the 116-byte instruction body 0x083555f8..0x0835566c with
      trailing literal-pool words 0xd561a67f at 0x0835566c and
      0xed99887f at 0x08355670; the next function's stmdb sp! prologue
      starts at 0x08355674, so the full extent is 124 bytes and
      Ghidra's 116 is the instruction body only. Ghidra mislabels both
      pool words as the globals DAT_0835566c/DAT_08355670; they are
      constants referenced from nowhere else. Call count verified by
      decoding every ARM B/BL word in osos.dec: exactly 17 direct call
      sites, all unconditional bl, 0 predicated forms; the address
      occurs in no data word, so it is not dispatched virtually.
      Algorithm: take the wrapping absolute value of value (subs/rsblt),
      then emit it as base-2^32 limbs low-to-high; each stored word is
      limb * 0xd561a67f mod 2^32 written to destination->words[count],
      and the header is signed_count * 0xed99887f written to
      destination->encoded_count LAST, after all word stores.
      0xed99887f is the multiplicative inverse mod 2^32 of the family
      reader key 0x0a7e377f (ENCODED_COUNT_MULTIPLIER), and 0x76b4197f
      (seen in reader literal pools, e.g. at 0x082eafa0 in FUN_082eaca4)
      inverts 0xd561a67f, so a block written here decodes back to value
      through the family's readers. The loop is while (high != 0 || limb
      != 0) { store; limb = high; high = 0; } with high = limb >> 31
      arithmetic, so every value strictly between -2^31 and 2^31 emits
      exactly one limb (zero emits none), but i32::MIN - whose wrapping
      absolute value stays 0x80000000 - shifts out a -1 high part and
      emits TWO limbs, [0x80000000 * key, 0xffffffff * key] with count
      -2. That is a faithful quirk of the original, not a general
      multi-limb conversion. Deliberate deviation: none. Host tests (4):
      zero writes header only with word sentinels untouched; positive,
      negative, and i32::MAX values store magnitude * 0xd561a67f as the
      single limb with decoded header count +/-1 and inverse-key
      round-trip; i32::MIN emits the two-limb quirk with decoded count
      -2; a produced block round-trips through copy_encoded_word_block
      (count accepted, header and limb preserved). match.py structural
      diff (exit-1 expected): same literal keys 0xd561a67f/0xed99887f,
      same rsbmi wrapping negation and stores through [r1, #4] and [r1];
      LLVM unrolls the at-most-two-iteration loop and precomputes
      0xffffffff * 0xd561a67f as the literal 0x2a9e5981 for the
      i32::MIN second limb.

  - address: 0x08391e44
    ghidra: FUN_08391e44
    name: strcmp
    size: 68
    sig: "int strcmp(const char *a, const char *b)"
    module: libc/strcmp
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Normalized three-way string compare returning exactly -1/0/+1, not
      the byte delta. Plain byte loop (ldrb, unsigned), continue test is
      the ADS two-flag idiom `cmp a,#0; cmpne b,#0` (the second test is
      redundant because the loop only continues when a == b, and the port
      drops it). Reached exclusively through the 4-byte entry veneer
      thunk_FUN_08391e44 @ 0x08391e38 (`b 0x08391e44`) - 756 bl call
      sites target 0x08391e38 and none target 0x08391e44, making it the
      most-called function in 0x08300000-0x083fffff. Not the ARM ADS
      strcmp (absent from osos); it belongs to the byte-loop string
      cluster 0x08391dec..0x083924a0. util/utoa's variant_compare
      previously replicated it as a private `strcmp3` helper - that copy
      is now deleted and the module calls this port directly. Hook both
      0x08391e38 and 0x08391e44 to the one symbol.

  - address: 0x08391e38
    ghidra: thunk_FUN_08391e44
    name: strcmp_veneer
    size: 4
    module: libc/strcmp
    status: ported
    notes: >-
      ALIAS - `b 0x08391e44` (verified against osos.asm), the address
      every one of strcmp's 756 callers actually branches to. No
      separate port; hook it to the strcmp symbol.

  - address: 0x08392478
    ghidra: FUN_08392478
    name: strlen
    size: 32
    sig: "size_t strlen(const char *s)"
    module: libc/strlen
    status: ported
    vars: {param_1: s}
    notes: >-
      Unguarded strlen - byte loop, no NULL check, no word-at-a-time
      trick, counter kept in r1 separately from the pointer walk. 419 bl
      call sites. Distinct from the NULL-guarded strlen_safe @ 0x082770bc
      (and its second link-time copy); the two `strlen_raw` helpers
      already inlined in stdio/semihost.rs and kernel/task.rs are callers
      of THIS function, so they can be redirected here later.
      Port uses read_volatile so LLVM's loop-idiom pass cannot rewrite
      the loop into a call to `strlen` - i.e. into itself.

  - address: 0x083995d8
    ghidra: FUN_083995d8
    name: scheduler_handle_label_lookup
    size: 76
    sig: "u32 scheduler_handle_label_lookup(i32 identifier)"
    module: util/scheduler_handle_label_lookup
    status: ported
    renames:
      param_1/r0: identifier
      local_8: label_out
      DAT_08399618: direct_label_limit
      DAT_0839961c: direct_label_base
      DAT_08399620: fallback_label
      FUN_080632a4: registered_handle_label_find
    notes: >-
      Raw osos.dec is 64 bytes of instructions plus the three-word literal
      pool at 0x08399618..0x08399620: 76 bytes from 0x083995d8 through
      0x08399623; the next separately linked function begins at
      0x08399624. Decoding every ARM B/BL word finds exactly 8 direct call
      sites, all unconditional bl (0x082bef60, 0x082bef8c, 0x082bf508,
      0x082bf538, 0x082bf568, 0x082bf598, 0x082bfce8, 0x082c03ec), with
      zero predicated forms. Scheduler-diagnostic callers use the result as
      an S16 label for semaphore and timer handle identifiers. A signed
      identifier strictly below the runtime word at 0x083e89d8 maps to the
      direct table address [0x083e7718] + identifier * 17 with wrapping ARM
      arithmetic. Other identifiers call unported 201-entry lookup
      FUN_080632a4(identifier, &stack_word); a nonzero return selects
      stack_word and zero selects [0x083e237c]. These runtime words are
      mutable retailOS data at run time despite decoding as instructions in
      the static image. The target port makes the same volatile absolute
      reads and call; host tests use private word and lookup seams to prove
      strict signed-bound exclusion, wrapping direct arithmetic, lookup
      forwarding, success, and fallback behavior.

  - address: 0x08399624
    ghidra: FUN_08399624
    name: scheduler_label_lookup
    size: 76
    sig: "u32 scheduler_label_lookup(i32 identifier)"
    module: util/scheduler_label_lookup
    status: ported
    renames:
      param_1/r0: identifier
      local_8: label_out
      DAT_08399664: direct_label_limit
      DAT_08399668: direct_label_base
      DAT_0839966c: fallback_label
      FUN_080632e0: registered_label_find
    notes: >-
      Raw osos.dec is 64 bytes of instructions plus the three-word literal
      pool at 0x08399664..0x0839966c: 76 bytes from 0x08399624 through
      0x0839966f; the next separately linked function begins at
      0x08399670. Decoding every ARM B/BL word finds exactly 10 direct
      call sites, all unconditional bl (0x082bef18, 0x082bef74,
      0x082bf348, 0x082bf4cc, 0x082bf9a4, 0x082bf9c8, 0x082bfd3c,
      0x082c0050, 0x082c0344, 0x082d0bf8), with zero predicated forms.
      The scheduler-diagnostic callers pass identifier fields and use the
      returned word as an S16 label. A signed identifier <= the runtime word
      at 0x083e89f0 maps to the direct table address
      [0x083e89dc] + identifier * 17 with wrapping ARM arithmetic. Higher
      identifiers call unported 80-entry lookup FUN_080632e0(identifier,
      &stack_word); a nonzero return selects stack_word and zero selects
      [0x083e237c]. These runtime words are mutable retailOS data at run
      time despite decoding as instructions in the static image. The target
      port makes the same volatile absolute reads and call; host tests use
      private word and lookup seams to prove signed-bound inclusion, wrapping
      direct arithmetic, lookup forwarding, success, and fallback behavior.

  - address: 0x0836b5b0
    ghidra: FUN_0836b5b0
    name: gpio_pin_configure
    size: 72
    sig: "u32 gpio_pin_configure(u32 pin_id, u32 mode, int level)"
    module: drivers/gpio_cmd
    status: ported
    vars: {param_1: pin_id, param_2: mode, param_3: level}
    notes: >-
      The single choke point through which retailOS configures S5L8702
      GPIO pins - 100 bl + 5 tail-b call sites. Writes one packed command
      word `port << 16 | pin << 8 | function` to GPIOCMD
      (0x3cf00000 + 0x200); `port = pin_id >> 3`, `pin = pin_id & 7`
      (8 pins per port). `mode` is the raw function code, passed through
      as `mode & 0xff`, except `mode == 1` (drive as output) with
      `level == 0` or `1`, which selects function 14 (low) / 15 (high)
      and skips the mask. `mode == 1` with any other level falls through
      to the mask and programs function 1 - the original's control flow,
      preserved. Always returns 0. Observed call sites use pin ids
      0x72/0x73/0x74 (port 14 pins 2/3/4) with mode 2 (alt function) and
      mode 1. Port splits the packing into the pure `gpio_command_word`
      (exhaustively tested on host) plus the register store, which is
      mocked off-target like kernel/csem.rs mocks the cpsr. Lives in
      drivers/gpio_cmd.rs, not drivers/gpio.rs - the latter is a stub
      reserved for another agent. Codegen deviation - LLVM packs the port
      field as `(pin_id << 13) & 0xffff0000` instead of
      `(pin_id >> 3) << 16`; same bits.

  - address: 0x0836b6cc
    ghidra: FUN_0836b6cc
    name: gpio_pin_read
    size: 44
    sig: "u32 gpio_pin_read(u32 pin_id, u32 *out)"
    module: drivers/gpio_pin_read
    status: ported
    renames:
      param_1/r0: pin_id
      param_2/r1: out
      DAT_0836b6f8: gpio_controller_base_literal (0x3cf00000)
      r2: gpio_data_word
      r0_after_ands: pin_level_mask
    notes: >-
      Verified raw extent is 44 bytes at 0x0836b6cc..0x0836b6f4; the following
      word at 0x0836b6f8 is the 0x3cf00000 GPIO base literal, and the next
      function begins at 0x0836b6fc. Reads the port data word at
      0x3cf00000 + (pin_id >> 3) * 0x20 + 4, normalizes bit
      (pin_id & 7) to 0 or 1, stores it through out, and returns 0. Every
      ARM B/BL word in osos.dec was decoded: 13 direct bl call sites, all
      unconditional; no predicated bl forms. Host tests use one volatile
      stand-in data word rather than mapped GPIO MMIO; ARM reads the exact
      volatile register address.

  # --- GPIO output pin drive ---
  - address: 0x0836b744
    ghidra: FUN_0836b744
    name: gpio_pin_write
    size: 52
    sig: "u32 gpio_pin_write(u32 pin_id, i32 level)"
    module: drivers/gpio_pin_write
    status: ported
    renames:
      param_1/r0: pin_id
      param_2/r1: level
      DAT_0836b778: gpio_controller_base_literal (0x3cf00000)
    notes: >-
      Write sibling of gpio_pin_read @ 0x0836b6cc with the identical
      address geometry: read-modify-write of the port data word at
      0x3cf00000 + (pin_id >> 3) * 0x20 + 4, bit (pin_id & 7).
      Verified raw extent is 52 bytes at 0x0836b744..0x0836b774 (48 code
      + the 0x3cf00000 literal at 0x0836b778); the next function begins
      at 0x0836b77c. level == 0 clears the bit (biceq), any nonzero
      level sets it (orrne); returns 0 unconditionally. Every ARM B/BL
      word in osos.dec was decoded: exactly 12 unconditional bl call
      sites plus 2 unconditional tail-b sites (0x08078c68, 0x082e5958);
      zero predicated forms. Two of the bl sites (0x080dbec4,
      0x080dbed8) are missing from Ghidra's asm listing. Observed uses:
      a 3x3 key-matrix scan (inferred from the two
      3-entry threshold tables in 0x080d3ce0) and pins
      0x5e-0x61 driven from 0x082e5aac / 0x082bc9ec (pin roles not
      identified). Port reuses gpio_data_address
      from gpio_pin_read and splits the bit edit into the pure
      gpio_pin_drive (exhaustively host-tested); the volatile RMW is
      mocked off-target like its sibling. Codegen deviation - LLVM
      computes the port address as (pin_id << 2) & ~0x1f added to a
      folded 0x3cf00004 literal, and emits both orr and bic selecting on
      the cmp flags; same address, same bits, plus a frame push/pop.

  # --- shared-cell handle constructor ---
  - address: 0x083b5090
    ghidra: FUN_083b5090
    name: shared_cell_construct
    size: 56
    sig: "SharedCell **shared_cell_construct(SharedCell **slot, void *value)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: slot
      param_2/r1: value
      iVar1: cell
    notes: >-
      Raw ARM extent is 56 bytes ending immediately before sibling
      0x083b50c8. It clears the one-word slot, returns without allocating
      for a NULL payload, otherwise invokes the already-ported tag-2
      operator_new(8) at 0x082aadd4 and fills the new cell as
      {value, refcount = 1}. It returns slot, not the cell. A full binary
      scan found exactly 29 incoming BL calls; every one is unconditional,
      with no predicated BL forms or direct tail branches. The allocator
      result is unguarded exactly as raw ARM: allocation failure faults
      while storing refcount. Host SharedCell uses usize for the payload
      word so host pointer fixtures remain representable; the allocation
      request remains the target's 8-byte cell size.


  # --- shared-cell handle copy assignment ---
  - address: 0x083b50e4
    ghidra: FUN_083b50e4
    name: shared_cell_assign
    size: 60
    sig: "SharedCell **shared_cell_assign(SharedCell **dst, SharedCell **src)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: src
      iVar1: cell
    notes: >-
      Raw ARM extent is 60 bytes ending immediately before sibling
      0x083b5120. The identity path returns dst without a release or retain.
      Distinct slots first BL the already-ported shared_cell_release at
      0x083b524c, then load src, store it in dst, and increment the
      non-NULL cell's signed refcount with wrapping ADD. The source read is
      deliberately after the destination release. A full binary scan found
      exactly 19 incoming BL calls; every one is unconditional, with no
      predicated BL forms or direct tail branches. Host SharedCell uses usize
      for the payload word so host pointer fixtures remain representable;
      source slots and refcounts preserve target-word semantics.
  # --- shared-cell handle slot comparison ---
  - address: 0x083b5120
    ghidra: FUN_083b5120
    name: shared_cell_slot_ne
    size: 20
    sig: "bool shared_cell_slot_ne(const SharedCellSlot *left, const SharedCellSlot *right)"
    module: cxx/value_compare
    status: ported
    vars: {param_1/r0: left, param_2/r1: right}
    notes: >-
      Compares the 32-bit cell-pointer words in two shared-cell handle slots
      and returns true when they differ. The adjacent constructor 0x083b5090
      allocates an eight-byte {value, refcount} cell and stores its address in
      a slot; the neighboring copy/release helpers 0x083b50e4 / 0x083b524c
      retain and release that cell. Direct application callers compare member
      slot +0x894 with a temporary slot before replacing or forwarding it.
  # --- shared-cell handle slot comparison, secondary call-site cluster ---
  - address: 0x083b51a8
    ghidra: FUN_083b51a8
    name: shared_cell_slot_ne_secondary
    size: 20
    sig: "bool shared_cell_slot_ne_secondary(const SharedCellSlot *left, const SharedCellSlot *right)"
    module: cxx/value_compare
    status: ported
    renames:
      param_1/r0: left
      param_2/r1: right
    notes: >-
      Raw ARM extent is 20 bytes, from 0x083b51a8 through 0x083b51b8, ending
      immediately before the distinct sibling at 0x083b51bc. It loads each
      shared-cell handle's 32-bit cell-pointer word and returns false for
      equal words or true for unequal words. It does not check either slot
      pointer for NULL, write either slot, or dereference either cell. A full
      decode of every ARM B/BL word in osos.dec found exactly 11 incoming
      calls: all are unconditional BL, with zero predicated BL forms and zero
      direct tail branches. It is semantically identical to
      shared_cell_slot_ne at 0x083b5120 but retained as a distinct callable
      Rust symbol in a target-only text section; that linker-preservation
      detail is the only implementation deviation.
  # --- shared-cell handle constructor sibling ---
  - address: 0x083b5134
    ghidra: FUN_083b5134
    name: shared_cell_construct_secondary
    size: 56
    sig: "SharedCell **shared_cell_construct_secondary(SharedCell **slot, void *value)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: slot
      param_2/r1: value
      iVar1: cell
    notes: >-
      Raw ARM extent is exactly 56 bytes, 0x083b5134..0x083b516c; the next
      separately linked sibling begins at 0x083b516c. It clears the one-word
      slot, returns without allocating for a NULL payload, otherwise invokes
      the already-ported tag-2 operator_new(8) at 0x082aadd4 and fills the
      new cell as {value, refcount = 1}. It returns slot, not the cell. Raw
      decoding of every ARM B/BL word in osos.dec found exactly 15 incoming
      BL calls, all unconditional: no predicated BL forms and no direct tail
      branches. This is byte-identical to shared_cell_construct @ 0x083b5090
      except for its address; the Rust sibling is isolated in its own text
      section so device hooks retain a distinct callable symbol. Host
      SharedCell uses usize for the payload word so host pointer fixtures
      remain representable; the allocation request remains the target's
      8-byte cell size.
  # --- shared-cell handle constructor tertiary sibling ---
  - address: 0x083b51bc
    ghidra: FUN_083b51bc
    name: shared_cell_construct_tertiary
    size: 56
    sig: "SharedCell **shared_cell_construct_tertiary(SharedCell **slot, void *value)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: slot
      param_2/r1: value
      iVar1: cell
    notes: >-
      Raw ARM extent is exactly 56 bytes, 0x083b51bc..0x083b51f0; the next
      separately linked sibling begins at 0x083b51f4. It clears the one-word
      slot, returns without allocating for a NULL payload, otherwise invokes
      the already-ported tag-2 operator_new(8) at 0x082aadd4 and fills the
      new cell as {value, refcount = 1}. It returns slot, not the cell. Raw
      decoding of every ARM B/BL word in osos.dec found exactly nine incoming
      BL calls, all unconditional: no predicated BL forms and no direct tail
      branches. The call sites are 0x0818a6ac, 0x0818a8ec, 0x0818a970,
      0x0818af40, 0x081a7f10, 0x081a7f98, 0x081a8788, 0x081a8b80, and
      0x081ba77c. This is byte-identical to shared_cell_construct @
      0x083b5090 except for its address; the Rust sibling is isolated in its
      own text section so device hooks retain a distinct callable symbol.
      Host SharedCell uses usize for the payload word so host pointer fixtures
      remain representable; the allocation request remains the target's
      8-byte cell size.
  # --- shared-cell handle copy constructor ---
  - address: 0x083b51f4
    ghidra: FUN_083b51f4
    name: shared_cell_copy_construct
    size: 28
    sig: "SharedCell **shared_cell_copy_construct(SharedCell **dst, SharedCell **src)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: src
      iVar1/r1: cell
      iVar2/r2: refcount
    notes: >-
      Raw ARM extent is exactly 28 bytes, 0x083b51f4..0x083b520c; the next
      separately linked sibling begins at 0x083b5210. It loads src's cell,
      stores it into dst, and increments the non-NULL cell's signed refcount
      with wrapping ADD. Although Ghidra declares void, raw execution leaves
      r0 (dst) unchanged, so the port returns dst. Raw decoding of every ARM
      B/BL word in osos.dec found eight incoming BL calls: seven
      unconditional at 0x0818a8e0, 0x081ba43c, 0x081f149c, 0x081f14a8,
      0x081f7454, 0x081f7480, and 0x081f748c, plus BLNE at 0x0818a704.
      There are no direct tail branches. The predicated caller is responsible
      for gating its call; the copy constructor itself does not NULL-check
      either slot pointer. Host SharedCell uses usize for the payload field,
      while the copied slot and 32-bit signed refcount retain target
      semantics.
  # --- shared-cell handle release ---
  - address: 0x083b524c
    ghidra: FUN_083b524c
    name: shared_cell_release
    size: 84
    sig: "void shared_cell_release(SharedCell **slot)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1: slot
      iVar1: cell
      iVar2: remaining
    notes: >-
      Raw ARM is 84 bytes ending immediately before sibling 0x083b52a0.
      It drops the intrusive signed refcount in an eight-byte
      {value, refcount} cell held by slot: an empty slot returns untouched;
      every non-empty path clears slot. A nonzero decrement result only
      releases this reference; the 1 -> 0 transition calls value's virtual
      deleting destructor through vtable word 1 (+4) when value is non-NULL,
      reloads slot after that call, and tag-2 operator_delete's the reloaded
      cell when it remains non-NULL. The ARM SUBS wraps: refcount 0 becomes
      -1 and does not clean up. Binary scan found exactly 38 incoming bl
      call sites, all unconditional (no predicated forms and no tail b).
      Ghidra's noreturn warning after the virtual call is wrong; raw control
      flow falls through to the cell delete. Sibling 0x083b52a0 has identical
      semantics (only its relative BL encoding differs); 0x083b52f4 is the
      distinct direct-destructor specialization. Structural review found
      LLVM's 30 instructions preserve the decrement, vtable[1] BLX,
      post-destructor slot reload, guarded cell delete, and final NULL store;
      its early BXEQ return and larger prologue differ from the 21-word ARM
      body.

  # --- shared-cell handle release, secondary call-site cluster ---
  - address: 0x083b52a0
    ghidra: FUN_083b52a0
    name: shared_cell_release_secondary
    size: 84
    sig: "void shared_cell_release_secondary(SharedCell **slot)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1: slot
      iVar1: cell
      iVar2: remaining
    notes: >-
      Raw ARM comprises 21 words from 0x083b52a0 through 0x083b52f0,
      ending immediately before the distinct direct-destructor specialization
      at 0x083b52f4. It is byte-identical in semantics to the sibling release
      at 0x083b524c: an empty slot is untouched; a non-empty cell has its
      signed refcount decremented with wrapping subtraction; every non-empty
      path clears slot. On the 1 -> 0 transition it conditionally dispatches
      the non-NULL payload through vtable word 1, reloads slot, and tag-2
      operator_delete's the reloaded non-NULL cell. A full ARM B/BL decode
      found exactly 19 incoming BL calls, all unconditional, with zero
      predicated BL calls and zero direct tail branches. The implementation
      has its own target text section so linker identical-code folding cannot
      erase this device-callable sibling symbol.

  # --- shared-cell handle release, direct payload teardown ---
  - address: 0x08262b84
    ghidra: FUN_08262b84
    name: shared_cell_release_direct
    size: 88
    sig: "void shared_cell_release_direct(SharedCell **slot)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: slot
      iVar1: cell
      iVar2: remaining
      FUN_081fc930: direct_payload_teardown
      FUN_082aad24: operator_delete
    notes: >-
      Raw ARM is exactly 22 words, 0x08262b84..0x08262bd8; the distinct next
      function starts at 0x08262bdc. A full decode of every B/BL word in
      osos.dec found exactly 10 incoming BL calls: all unconditional, with no
      predicated BL forms. The empty slot path returns untouched. A non-empty
      slot decrements its signed {payload, refcount} cell with wrapping SUBS
      and clears the slot even for a non-final or underflowed count. At the
      1 -> 0 transition, non-NULL payload takes the direct call to unported
      0x081fc930; its return feeds tag-2 operator_delete @ 0x082aad24, then
      the reloaded non-NULL cell is tag-2 deleted and the slot cleared. This
      is byte-identical in behavior to 0x083b52f4 except for direct-call
      displacement. The identity of 0x081fc930 is not inferred; host builds
      use an injectable return-preserving seam and target builds call it
      directly.

  # --- shared-cell handle release, direct payload teardown sibling ---
  - address: 0x083b52f4
    ghidra: FUN_083b52f4
    name: shared_cell_release_direct_secondary
    size: 84
    sig: "void shared_cell_release_direct_secondary(SharedCell **slot)"
    module: cxx/shared_cell
    status: ported
    renames:
      param_1/r0: slot
      iVar1: cell
      iVar2: remaining
      FUN_081fc930: direct_payload_teardown
      FUN_082aad24: operator_delete
    notes: >-
      Raw ARM is exactly 21 words, 0x083b52f4..0x083b5344; the next distinct
      function begins at 0x083b5348. A complete osos.dec B/BL-word decode
      found exactly 9 incoming calls, all unconditional BL (zero predicated
      forms and zero direct tail branches): 0x0818a6c4, 0x0818a770,
      0x0818a900, 0x0818a984, 0x0818a98c, 0x0818af58, 0x081a879c,
      0x083b5228, 0x083dd2c4. An empty slot returns untouched; every
      non-empty path wrapping-decrements the signed cell refcount and clears
      the slot. The final 1 -> 0 path passes a non-NULL payload to direct
      teardown 0x081fc930, operator_deletes its returned pointer, reloads and
      deletes the still-non-NULL cell, then NULLs the slot. This is
      byte-identical in behavior to shared_cell_release_direct @ 0x08262b84
      save the direct BL displacement. The direct callee's identity is not
      inferred; host builds use its return-preserving seam and target builds
      call the firmware address. A dedicated target text section retains this
      otherwise-identical device-callable symbol.

  # --- C++ COW basic_string<char> core (cluster 0x083d8518..0x083d8bac) ---
  # String object = one word, the char data pointer. 12-byte _Rep header
  # sits at data-12: {refcount i32 @0, capacity u32 @4, length u32 @8}
  # (field order recovered from the header stores, NOT the libstdc++ order).
  # refcount 0 = sole owner, -1 = destroy. Shared empty rep @ 0x08b31804
  # (RAM), its data @ 0x08b31810. Allocation is capacity + 14 bytes
  # (12 header + NUL + 1 spare) via operator_new_checked @ 0x08266c70;
  # deallocation via the 4-byte veneer 0x08266f2c = `b 0x082aad24`
  # (plain operator delete - the size and third argument are discarded).
  - address: 0x0825c8fc
    ghidra: FUN_0825c8fc
    name: cxx_string_pair_destroy
    size: 32
    sig: "CxxStringPair *cxx_string_pair_destroy(CxxStringPair *record)"
    module: cxx/string
    status: ported
    vars: {param_1: record}
    renames: {FUN_083d8b04: cxx_string_release}
    notes: >-
      C++ destructor for one 8-byte record containing two one-word COW
      strings. Raw ARM takes record in r0, calls cxx_string_release on
      record + 4 then record + 0, restores the saved record pointer to r0,
      and returns it. The pair fields are otherwise untouched.

  - address: 0x0825c91c
    ghidra: FUN_0825c91c
    name: cxx_string_pair_assign
    size: 36
    sig: "CxxStringPair *cxx_string_pair_assign(CxxStringPair *destination, const CxxStringPair *source)"
    module: cxx/string
    status: ported
    vars: {param_1: destination, param_2: source}
    renames: {FUN_083d8d1c: cxx_string_assign}
    notes: >-
      Raw ARM receives destination in r0 and source in r1, calls
      cxx_string_assign @ 0x083d8d1c first for their COW-string fields at
      +0x00 and then for their fields at +0x04, restores the saved
      destination pointer to r0, and returns it. It deliberately delegates
      to the COW assignment so source-reference ownership and
      self-assignment behavior are unchanged.

  - address: 0x083e2f48
    ghidra: FUN_083e2f48
    name: cxx_string_pair_range_destroy
    size: 48
    sig: "void cxx_string_pair_range_destroy(void *unused, CxxStringPairRangeEntry *first, CxxStringPairRangeEntry *last)"
    module: cxx/string
    status: ported
    vars: {param_1: unused, param_2: first, param_3: last}
    notes: >-
      A 12-byte-record half-open range destructor. The raw ARM entry leaves
      r0 unused and moves r2/r1 into the saved last/current registers before
      its first equality check. For every record it releases the COW string
      word at +4 via cxx_string_release @ 0x083d8b04, then the word at +0,
      advances current by 12, and terminates only when current == last.

  - address: 0x083d8b04
    ghidra: FUN_083d8b04
    name: cxx_string_release
    size: 80
    sig: "void cxx_string_release(char **string)"
    module: cxx/string
    status: ported
    vars: {param_1: string}
    notes: >-
      571 bl call sites - the second-most-called function in
      0x08300000-0x083fffff (the destructor half of every std::string in
      the OS). Returns immediately when the data pointer is the shared
      empty rep's. Otherwise: a refcount already at -1 destroys at once,
      else decrement and destroy when the result is -1. The second
      singleton test (`rep == 0x08b31804`) is redundant with the first
      (`data == 0x08b31810`) - kept in the port for fidelity. Destroy
      tail-branches to 0x08266f2c with (rep, capacity + 14, 0); that
      veneer is `b operator_delete`, so the port calls the ported
      heap::veneers::operator_delete directly and LLVM inlines it.

  - address: 0x083d8a64
    ghidra: FUN_083d8a64
    name: cxx_string_rep_create
    size: 152
    sig: "StringRep *cxx_string_rep_create(void *unused, u32 capacity, u32 length)"
    module: cxx/string
    status: ported
    vars: {param_1: unused, param_2: capacity, param_3: length}
    notes: >-
      99 bl call sites. Allocates a _Rep - capacity 0 returns the shared
      empty rep without allocating, otherwise operator_new_checked
      (capacity + 14), refcount 0, capacity, length, NUL at data[length].
      Two length-error reports call the C++ diagnostic/throw dispatch
      @ 0x08266abc with code 8 and two string arguments that both point
      at an empty string @ 0x083d8afc (diagnostics stripped from this
      build); the original reaches them with `bl` and falls through to
      allocate anyway, which the port reproduces via the CXX_STRING_OPS
      hook (volatile-read, or LLVM const-folds the no-op default away).
      Max capacity is 0xfffffff1 (`cmn r1, #15`). First argument is never
      read. DEVIATION - the original's header store `stm r0, {r1, r4, r5}`
      @ 0x083d8ae8 is unconditional and writes through NULL when the
      allocation fails; the port returns NULL instead. The original's
      redundant `stmiane` pre-zeroing of those same three words and the
      dead `str r1, [sp, #4]` are dropped.

  - address: 0x083d8518
    ghidra: FUN_083d8518
    name: cxx_string_rep_reserve
    size: 76
    sig: "char *cxx_string_rep_reserve(void *unused, u32 old_capacity, u32 needed, u32 length)"
    module: cxx/string
    status: ported
    vars: {param_1: unused, param_2: old_capacity, param_3: needed, param_4: length}
    notes: >-
      5 bl call sites. Growth policy in front of cxx_string_rep_create -
      new capacity is max(old + old/2 + old/8, old + 32, needed), all
      wrapping. The 1.625x term overtakes the +32 floor at old = 54
      (52 and 53 tie, and `movhi` keeps the 1.625x value). Returns the
      DATA pointer (rep + 12), not the rep. Note the original stages both
      candidates on the stack and selects with `addcc/movcs` on an
      address - a max, transcribed as such.

  - address: 0x083d8b5c
    ghidra: FUN_083d8b5c
    name: cxx_string_from_cstr
    size: 76
    sig: "char **cxx_string_from_cstr(char **string, const char *source)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: source}
    notes: >-
      495 bl call sites - `basic_string(const char *)`. strlen @ 0x08392478
      (ported), then either the shared empty rep data (length 0, no
      allocation) or cxx_string_rep_reserve(this, 0, len, len), which
      means a fresh string always gets at least 32 characters of
      capacity. Stores the data pointer into the one-word string object
      BEFORE the copy, then __rt_memcpy @ 0x08037db0 (ROM veneer, ported
      as __rt_memcpy). No NUL store here - rep_create already wrote it.
      Returns `string`.

  - address: 0x083d8bac
    ghidra: FUN_083d8bac
    name: cxx_string_from_buffer
    size: 108
    sig: "char **cxx_string_from_buffer(char **string, const char *source, u32 length)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: source, param_3: length}
    notes: >-
      7 bl call sites - `basic_string(const char *s, size_type n)`, the
      counted sibling of cxx_string_from_cstr and the constructor that
      makes the class binary-safe (embedded NULs survive). Same shape -
      length 0 parks on the shared empty rep with no allocation,
      otherwise cxx_string_rep_reserve(this, 0, n, n) gives a floor of 32
      characters. Two faithful quirks - the `cmn r2,#15 / blhi` length
      check fires and then FALLS THROUGH (and rep_create checks again, so
      an over-long request reports twice); and the memcpy is guarded on
      `source != NULL`, not on `n`, so a zero-length construction from a
      non-NULL pointer still calls memcpy with length 0, while `n != 0`
      with a NULL source allocates a rep of length n whose bytes are
      never written. Returns `string`.

  - address: 0x083d8c20
    ghidra: FUN_083d8c20
    name: cxx_string_default_ctor
    size: 12
    sig: "char **cxx_string_default_ctor(char **string)"
    module: cxx/string
    status: ported
    vars: {param_1: string}
    notes: >-
      19 bl call sites - `basic_string()`. Three instructions - load the
      literal 0x08b31810 (the shared empty rep's data pointer) and store
      it in the one-word string object. No allocation, no refcounting.
      Leaves `this` in r0, so the port returns it.

  - address: 0x083d8c30
    ghidra: FUN_083d8c30
    name: cxx_string_copy_ctor
    size: 92
    sig: "char **cxx_string_copy_ctor(char **dst, char *const *src)"
    module: cxx/string
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      64 bl + 6 b = 70 call sites - `basic_string(const basic_string &)`,
      the COW grab. Shares the source rep (store the same data pointer,
      then cxx_string_rep_add_ref @ 0x083b54f8) unless the rep is marked
      LEAKED, which is `refcount == -1` tested as `adds r1,r1,#1; beq`.
      A leaked rep has handed a mutable reference out, so the copy is
      deep - cxx_string_rep_create(this, length, length) then memcpy of
      `length` bytes, capacity exactly length (the growth policy is not
      consulted). Note -1 is the SAME sentinel cxx_string_release treats
      as "destroy me"; the two readings never collide because a leaked
      rep is the sole owner by definition. Returns `dst`.

  - address: 0x083d8c8c
    ghidra: FUN_083d8c8c
    name: cxx_string_dtor
    size: 20
    sig: "char **cxx_string_dtor(char **string)"
    module: cxx/string
    status: ported
    vars: {param_1: string}
    notes: >-
      5 bl call sites - `~basic_string()`. cxx_string_release @
      0x083d8b04 and nothing else, with `this` restored into r0 (the ADS
      destructor return convention). The 571 direct callers of
      0x083d8b04 against these 5 show the compiler inlined the wrapper
      away nearly everywhere.

  - address: 0x083b54f8
    ghidra: FUN_083b54f8
    name: cxx_string_rep_add_ref
    size: 24
    sig: "void cxx_string_rep_add_ref(StringRep *rep)"
    module: cxx/string
    status: ported
    vars: {param_1: rep}
    notes: >-
      `_Rep::_M_add_ref` - a NON-atomic `++refcount` that skips the
      shared empty rep by comparing against the literal 0x08b31804.
      Exactly 2 bl call sites, both in this string class (0x083d8c54 in
      the copy ctor, 0x083d8d3c in operator=), which is why the
      singleton can be immutable. Sits just below the nominal
      0x083c0000-0x083dffff sweep but is a member of this class and
      reachable from nowhere else.

  - address: 0x083d865c
    ghidra: FUN_083d865c
    name: cxx_string_replace_core
    size: 552
    sig: "char *cxx_string_replace_core(char **string, u32 pos, u32 n1, const char *source, u32 source_len, u32 source_pos, u32 n2)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: pos, param_3: n1, param_4: source}
    notes: >-
      The class's ONE mutation primitive - libstdc++'s `_M_replace`.
      Splices n2 characters from source[source_pos..] over the n1
      characters at string[pos..]. Seven arguments (r0-r3 plus three on
      the stack, read with `add fp,sp,#48; ldm fp,{r6,sl,fp}` after a
      12-register push); the source is described as
      (base, length, offset, count) so a substring of another string
      needs no temporary. Only 3 bl call sites - 0x083d8624,
      0x083d8564 and their users - but every mutating member funnels
      through those, so it carries ~90 call sites of traffic.
      Sequence - precondition `pos <= size()` and
      `source_pos <= source_len` (report code 9, then FALL THROUGH);
      clamp removed = min(n1, size()-pos) and
      inserted = min(n2, source_len-source_pos); length check
      `size()-removed <= 0xfffffff1-inserted` (code 8, also falling
      through); an empty result releases and parks on the shared empty
      rep; otherwise reallocate when the rep is shared
      (`refcount+1 > 1` unsigned, so a LEAKED -1 rep counts as unshared
      and IS mutated in place), when `capacity < new length`, or when
      `source` points into our own buffer (`data <= source <
      data+size()`); else memmove in place. GROWTH DIFFERS from the
      construction path - `max(1.625*old_LENGTH, old_LENGTH+128,
      new_length)`, whereas cxx_string_rep_reserve @ 0x083d8518 uses
      the old CAPACITY and a +32 floor. Returns `data + pos`, an
      iterator to the splice point, NOT `this` - the wrappers discard
      it. Faithful quirks - the tail copy sources from `data + pos + n1`
      with the RAW n1, harmless because clamping can only bite when the
      tail length is 0; falling through the code-9 report leaves
      `source_len - source_pos` wrapped, so the `inserted` clamp becomes
      a no-op and the splice reads past the declared end of the source;
      and a failed allocation is not guarded (the original copies to
      address 12).

  - address: 0x083d8624
    ghidra: FUN_083d8624
    name: cxx_string_replace_cstr
    size: 56
    sig: "char **cxx_string_replace_cstr(char **string, u32 pos, u32 n1, const char *source, u32 n2)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: pos, param_3: n1, param_4: source}
    notes: >-
      3 bl call sites - `replace(pos, n1, const char *s, n2)`. Pure
      argument shuffle onto cxx_string_replace_core with
      source_len = n2 and source_pos = 0 (so the core's clamp is a
      no-op), then returns `string` and discards the core's iterator.
      The three stack words it builds are {n2, 0, n2}.

  - address: 0x083d8564
    ghidra: FUN_083d8564
    name: cxx_string_append_substr
    size: 188
    sig: "char **cxx_string_append_substr(char **string, char *const *other, u32 pos, u32 n)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: other, param_3: pos, param_4: n}
    notes: >-
      3 bl call sites - `append(const basic_string &other, pos, n)`.
      Runs its own `pos <= other.size()` (code 9) and length (code 8)
      checks, both falling through on failure, then calls
      cxx_string_replace_core at pos = size() with n1 = 0 and the source
      range (other.data, other.size(), pos, n) - so `n` is clamped a
      second time inside the core. Returns `string`.

  - address: 0x083d8ca0
    ghidra: FUN_083d8ca0
    name: cxx_string_assign_cstr
    size: 120
    sig: "char **cxx_string_assign_cstr(char **string, const char *source)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: source}
    notes: >-
      20 bl + 4 b = 24 call sites - `operator=(const char *)`. strlen @
      0x08392478 first; a non-empty source is
      cxx_string_replace_cstr(this, 0, size(), s, len). An EMPTY source
      never touches the mutation core - a sole owner (`refcount == 0`,
      tested as `refcount+1 == 1`) is truncated in place, keeping its
      buffer, with length 0 and a NUL at data[0]; anything else (shared,
      or leaked at -1) is released and parked on the shared empty rep.
      Returns `string`.

  - address: 0x083d8d1c
    ghidra: FUN_083d8d1c
    name: cxx_string_assign
    size: 104
    sig: "char **cxx_string_assign(char **string, char *const *src)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: src}
    notes: >-
      42 bl + 4 b = 46 call sites - `operator=(const basic_string &)`.
      A shareable source rep is grabbed outright - cxx_string_rep_add_ref
      @ 0x083b54f8, then cxx_string_release on our own, then adopt the
      pointer. Self-assignment survives that path unguarded precisely
      because the bump precedes the release. A LEAKED source
      (`refcount == -1`) cannot be shared, so it goes through
      cxx_string_replace_cstr(this, 0, size(), src.data, src.size())
      instead, and only that path carries the explicit `this == &src`
      guard. Returns `string`.

  - address: 0x083d8d84
    ghidra: FUN_083d8d84
    name: cxx_string_append_cstr
    size: 56
    sig: "char **cxx_string_append_cstr(char **string, const char *source)"
    module: cxx/string
    status: ported
    vars: {param_1: string, param_2: source}
    notes: >-
      20 bl + 1 b = 21 call sites - `operator+=(const char *)` /
      `append(const char *)`. Captures size() BEFORE the strlen call
      (register scheduling the port keeps), then
      cxx_string_replace_cstr(this, size(), 0, s, len). Distinguished
      from cxx_string_assign_cstr @ 0x083d8ca0 only by which of pos/n1
      gets the old length - assignment passes (0, size()), append
      passes (size(), 0). Returns `string`.

  # --- tag-57 tracked allocator: free side (0x083906f4..0x08390774)
  #     plus the alloc tail @ 0x08390b8c ---
  # Block layout built by tracked_alloc_tail @ 0x08390b8c (ported):
  #   raw+0x00 i32 size, raw+0x04 i32 size>>31 (the pair is an i64),
  #   raw+0x08 = base, then padding so `data = (base + 36) & ~31` is
  #   32-byte aligned, with `pad = data - base` stored at data-4.
  # Accounting globals @ 0x08adc2c0 (9 code refs): +0x38 i64 current bytes,
  # +0x40 i64 peak. 0x08390c30 resets peak := current, 0x08390c54 returns
  # current. Allocator raises the peak; the free path only subtracts.
  - address: 0x08003500
    ghidra: thunk_FUN_08390bcc
    name: tracked_stats_raise_peak
    size: 8
    sig: "void tracked_stats_raise_peak(i64 current)"
    module: heap/tracked
    status: ported
    vars: {param_1: current, param_2: "current high word", unaff_r7: stats}
    notes: >-
      IRAM veneer: raw 0x08003500 is `ldr pc,[pc,#-4]` followed by target
      word 0x08390bcc, so its real extent is 8 bytes despite Ghidra's
      4-byte size. The target is an interior 28-byte fragment of
      sqlite3_malloc at 0x08390b14, continuing through its epilogue:
      it receives current bytes in r0:r1 and the stats block in ambient r7,
      compares signed i64 peak(+0x40) against current with subs/sbcs, then
      copies current to peak with ldrdlt/strdlt only when peak < current.
      Binary scan of every ARM B/BL encoding in osos.dec found 10
      unconditional bl call sites and no predicated bl; the sole plain b
      is the tail branch at 0x08001d0c. The port makes r0:r1 an explicit
      i64 current parameter and substitutes ALLOC_STATS for ambient r7
      (the established heap/tracked module simplification).

  - address: 0x083906f4
    ghidra: FUN_083906f4
    name: tracked_free
    size: 68
    sig: "void tracked_free(void *payload)"
    module: heap/tracked
    status: ported
    vars: {param_1: payload}
    notes: >-
      218 bl + 22 tail-b call sites. NULL-guarded. Reads the pad word at
      payload-4, backs up to the raw block (payload - pad - 8), subtracts
      the SIGNED 32-bit size cookie widened to 64 bits from the running
      byte counter (`subs`/`sbc rX, rX, r2, asr #31` on the i64 at
      0x08adc2c0+0x38), and tail-branches to the tag-57 free veneer
      0x08391d24. Every field it touches is a 32-bit scalar, so the
      literal byte offsets are valid on a 64-bit host too.

  - address: 0x08390734
    ghidra: FUN_08390734
    name: tracked_free_pointer_array
    size: 68
    sig: "void tracked_free_pointer_array(void **elements)"
    module: heap/tracked
    status: ported
    vars: {param_1: elements}
    notes: >-
      3 bl call sites. The block starts one word below `elements` with a
      WORD COUNT; the remaining count-1 words are pointers to tracked
      blocks. Frees each non-NULL element with tracked_free, then the
      block itself. NULL-guarded. Addressed by index through a
      `*mut *mut u8` (stride 4 on target, 8 on host) because the cookie
      and elements are pointer-sized - the word-index rule.

  - address: 0x08391d24
    ghidra: FUN_08391d24
    name: free_tag57
    size: 8
    sig: "void free_tag57(void *block)"
    module: heap/tracked
    status: ported
    notes: >-
      `mov r1, #57; b 0x080e7970` - free_wrapper with caller tag 57. One
      tail-b call site (tracked_free).

  - address: 0x08391d2c
    ghidra: FUN_08391d2c
    name: alloc_tag57
    size: 8
    sig: "void *alloc_tag57(size_t size)"
    module: heap/tracked
    status: ported
    notes: >-
      `mov r1, #57; b 0x080eb67c` - malloc_wrapper with caller tag 57.
      2 bl call sites, one of them the tracked allocator @ 0x08390b8c.

  - address: 0x082ccc74
    ghidra: FUN_082ccc74
    name: tracked_stats_arm_lock
    size: 24
    sig: "void tracked_stats_arm_lock(void)"
    module: heap/tracked
    status: ported
    vars: {DAT_082ccc8c: stats_plus_0x20}
    notes: >-
      The "scheduler-flag setter" used as the stats lock by the whole
      tracked-allocator cluster: 4 bl call sites (allocator entry
      0x08390b14, stats init 0x08390bf0, peak reader 0x08390c24,
      tracked_stats_current 0x08390c54). ldr r0,[lit 0x082ccc8c] loads
      0x08adc2e0; if [r0,#0x14] == 0 it is set to 8 (cmp/moveq/streq),
      else left alone - a one-way arm that keeps a running scheduler
      from tearing the ldrd reads of the i64 counters. Verified against
      osos.dec: 0x08adc2e0 is the 0x08adc2c0 accounting block + 0x20, so
      the flag word is 0x08adc2f4 = stats+0x34, INSIDE the same block,
      immediately before current_bytes(+0x38) - the RTXC scheduler is
      never touched, so it lives in heap/tracked (not kernel/), modeled
      as the new ALLOC_STATS.lock_flag field (reserved shrank 14->13
      words, layout unchanged). It is also the wired default of
      TRACKED_STATS_OPS.lock, replacing the documented no-op stub.
      Accesses are volatile: without it LLVM's globalopt narrowed the
      word to a boolean byte and dropped the 8 (ldrb/moveq #1/strbeq in
      ARM release). match.py: same ldr/ldr/cmp/moveq/streq structure;
      the diff is LLVM's fp frame and offset #0x34 vs #0x14 (the
      port-owned static vs the +0x20-based literal - the documented
      module simplification).

  - address: 0x08390b8c
    ghidra: null
    name: tracked_alloc_tail
    size: 92
    sig: "void *tracked_alloc_tail(int size)"
    module: heap/tracked
    status: ported
    vars: {r5: size, r6: "size + 0x24", r7: stats, r4: "raw, then payload"}
    notes: >-
      Tail of the tag-57 tracked allocator (not a functions.csv entry; the
      enclosing entry @ 0x08390b14 - 41 bl call sites - has the size<=0
      gate, the soft-limit check and the first alloc attempt, and warns
      via 0x0837d67c before falling into this retry). Retries
      alloc_tag57(size + 0x24 + 8) = size + 44 (8-byte header + up to 36
      bytes of alignment pad + payload); NULL in, NULL out. On success:
      raw+0 = size, raw+4 = size>>31 (the pair is an i64), data =
      (base + 36) & ~31 with base = raw+8, pad = data - base stored at
      data-4; current(+0x38) += (i64)(i32)size via adds/adc ..asr #31,
      and peak(+0x40) := current when peak < current (subs/sbcs +
      ldrdlt/strdlt). Port takes size as the argument (was r5) and reads
      the ALLOC_STATS static instead of literal 0x08adc2c0 (module
      simplification, same as the free side). match.py: same structure -
      LLVM folds base+8+36 into raw+44 and schedules the two cookie
      stores together; the peak raise keeps the lt condition.

  - address: 0x08390c24
    ghidra: FUN_08390c24
    name: tracked_stats_peak
    size: 44
    sig: "i64 tracked_stats_peak(i32 reset)"
    module: heap/tracked
    status: ported
    vars: {param_1: reset, r4: reset, r2: stats, DAT_08390c50: stats_base}
    notes: >-
      The COMPLETE peak reader of the tracked allocator's stats block
      (functions.csv size 44, verified against osos.asm and osos.dec -
      the literal @ 0x8390c50 is 0x08adc2c0, the stats base); the
      0x08390c30 entry below is its fall-through tail, not a separate
      callee. stmdb {r4,lr}; mov r4,r0; bl 0x082ccc74 (arms the stats
      lock, the ported tracked_stats_arm_lock); ldr r2,[lit]; cmp
      r4,#0; ldrd r0,r1,[r2,#0x40] - the PRE-reset peak into the return
      pair - then ldrne/strne copies current(+0x38) over peak(+0x40)
      word by word when reset is nonzero; ldmia {r4,pc}. The port
      absorbs the tail (the head falls through into it, so there is no
      fragment boundary to call across - unlike tracked_alloc_tail,
      which the enclosing entry @ 0x08390b14 (since ported as
      sqlite3_malloc) CALLS for its retry).
      Deviations: the ALLOC_STATS static instead of the literal
      0x08adc2c0 (module simplification), and the lock call dispatches
      through TRACKED_STATS_OPS.lock (house ops-slot pattern) whose
      default is the ported tracked_stats_arm_lock, same as
      tracked_stats_current. Host tests: the full-i64 peak read,
      reset=0 leaving both counters, reset!=0 copying current over
      peak but returning the pre-reset peak, lock-before-read/reset
      ordering via a recorder mock, and the wired default arming the
      flag. match.py: same push/mov/lock-call/ldr-stats/cmp/ldrd-peak/
      conditional-copy/pop; the diff is LLVM's fp frame, the
      blx-through-slot, and ldrdne/strdne in place of the split
      ldrne/strne word pairs - the documented deviations.

  - address: 0x08390c30
    ghidra: null
    name: tracked_stats_reset_peak
    size: 36
    sig: "i64 tracked_stats_reset_peak(i32 reset)"
    module: heap/tracked
    status: ported
    vars: {r4: reset}
    notes: >-
      Tail of the peak reader @ 0x08390c24 (the envelope head saves r4
      and calls the scheduler-flag setter @ 0x082ccc74 first), not a
      separate callee - the head falls through into it. Loads
      peak(+0x40) into the return pair, then when reset is nonzero
      (cmp r4,#0 + ldrne/strne) copies current(+0x38) over peak; the
      returned peak is the pre-reset value. The port of this logic
      lives INSIDE tracked_stats_peak @ 0x08390c24 (ported above),
      which absorbs the tail; there is no separate Rust symbol.
      (History: the earlier "the port IS the tail fragment" note
      described a standalone fragment port that was never committed -
      it was swept into names.yaml by an unrelated edit; the envelope
      port now covers both addresses.) Sibling 0x08390c54 (returns
      current after the lock call) ported as tracked_stats_current
      below.

  - address: 0x08390c54
    ghidra: FUN_08390c54
    name: tracked_stats_current
    size: 20
    sig: "i64 tracked_stats_current(void)"
    module: heap/tracked
    status: ported
    notes: >-
      1 bl call site (0x08391450). Verified against osos.asm: a
      COMPLETE 20-byte function, not a bare reader and not a tail
      fragment (unlike the 0x08390c24/0x08390c30 pair, where the lock
      call lives in the envelope head) - its body includes the lock
      call inline: stmdb {r4,lr}; bl 0x082ccc74 (scheduler-flag
      setter); ldr r1,[lit 0x08adc2c0]; ldrd r0,r1,[r1,#0x38];
      ldmia {r4,pc}. Arms the stats lock, reads current(+0x38, i64)
      with ldrd, returns it in r0:r1. Port deviations: reads the
      ALLOC_STATS static instead of literal 0x08adc2c0 (module
      simplification, same as the rest of heap/tracked), and the
      lock callee @ 0x082ccc74 (scheduler-flag setter, since ported
      as tracked_stats_arm_lock) dispatches through the
      TRACKED_STATS_OPS.lock slot (house ops-slot pattern, an indirect
      call in place of bl) whose default is that port, so host tests
      can still swap in a recorder. match.py: same
      push/call/ldr-stats/ldrd/pop structure; the diff is LLVM's fp
      frame and the blx-through-slot (the documented deviations).

  - address: 0x08390bf0
    ghidra: FUN_08390bf0
    name: tracked_stats_set_soft_limit
    size: 52
    sig: "i32 tracked_stats_set_soft_limit(void *callback, void *callback_arg, i64 soft_limit)"
    module: heap/tracked
    status: ported
    vars: {param_1: callback, param_2: callback_arg, "param_3,param_4": soft_limit, DAT_08390c20: stats_plus_0x20}
    notes: >-
      The ledger's "stats init"; verified against osos.asm it is really
      the soft-limit SETTER of the tracked allocator's stats block.
      1 bl call site (0x0839144c, inside the memory-pressure reclaimer
      @ 0x08391408; the init path reaches the same setter through the
      thunk @ 0x0813eb3c with a 0x80000 limit). Arms the stats lock
      (bl 0x082ccc74, the scheduler-flag setter), then strd (r0,r1) at
      [lit 0x08adc2e0]+0x8 and strd (r2,r3) at [lit]-0x20+0x20 - i.e.
      callback @ stats+0x28, callback_arg @ stats+0x2c, and the i64
      soft_limit @ stats+0x20 - and returns 0. Consumers verified in
      osos.asm: the allocator entry @ 0x08390b14 warns when the callback
      word is nonzero and current(+0x38)+size reaches the limit, and the
      warn helper @ 0x0837d67c blx's the callback as
      callback(callback_arg, current_bytes, size) under the +0x30
      reentrancy guard. Port deviations: the ALLOC_STATS static instead
      of literal 0x08adc2c0 (module simplification), the pointer words
      modeled as u32 (word-index rule, so the strd at +0x28 becomes two
      word stores), and the lock call dispatches through
      TRACKED_STATS_OPS.lock (house ops-slot pattern, same as
      tracked_stats_current) whose default is the ported
      tracked_stats_arm_lock. ALLOC_STATS gained the named fields
      soft_limit/soft_limit_callback/soft_limit_callback_arg/
      soft_limit_callback_active (reserved shrank 13->8 words, offsets
      unchanged). match.py: same push/arg-shuffle/lock-call/
      four-word-store/mov r0,#0/pop structure; the diff is LLVM's fp
      frame, the blx-through-slot, and the split strds (the documented
      deviations).

  - address: 0x0837d67c
    ghidra: FUN_0837d67c
    name: tracked_stats_warn_soft_limit
    size: 84
    sig: "void tracked_stats_warn_soft_limit(i32 size)"
    module: heap/tracked
    status: ported
    vars: {param_1: size, DAT_0837d6d0: stats_plus_0x20}
    notes: >-
      The soft-limit warn helper of the tag-57 tracked allocator's
      stats block. 4 call sites: blge/bl @ 0x08390b58/0x08390b84 in the
      allocator entry @ 0x08390b14 (warn-before-retry and warn-on-OOM)
      and blge/bl @ 0x08390f50/0x08390f84 in the tracked realloc
      @ 0x08390eec. Verified against osos.asm and osos.dec (literal
      @ 0x0837d6d0 = 0x08adc2e0, stats base + 0x20): when the callback
      word (+0x28) is nonzero and the reentrancy guard (+0x30) is
      clear, sets the guard, reads current_bytes (+0x38) word-wise, and
      blx's the callback as callback(callback_arg, current_bytes, size)
      - callback_arg in r0, the i64 in the aligned r2:r3 pair (r1
      skipped, still holding the callback pointer: Ghidra's phantom
      second parameter), size on the stack - then clears the guard.
      Port deviations: the ALLOC_STATS static instead of literal
      0x08adc2c0 (module simplification), the blx dispatches through
      the new TRACKED_STATS_OPS.invoke_soft_limit_callback slot (house
      ops-slot pattern; default blx_soft_limit_callback transmutes the
      callback word exactly like the original, host tests swap in a
      recorder since a real code address does not fit the u32 word on
      a 64-bit host), and volatile guard accesses (same globalopt
      narrowing hazard as lock_flag). match.py: same load-callback/
      branch/load-guard/branch/store-1/load-args/stack-arg/blx/
      store-0 structure; the diff is LLVM's fp frame and the
      blx-through-slot (the documented deviation).

  - address: 0x0837a158
    ghidra: FUN_0837a158
    name: load_be32
    size: 36
    sig: "u32 load_be32(const u8 *p)"
    module: util/beload
    status: ported
    notes: >-
      ALIAS - re-verified byte for byte: all 36 bytes at 0x0837a158 are
      identical to load_be32 @ 0x081f3b30, so the two are one function
      the linker emitted into two units. This copy is SQLite's
      sqlite3Get4byte (the b-tree page-header reader) and has 59 bl call
      sites versus 4 at the other address. NO separate port exists - hook
      BOTH addresses to the single `load_be32` symbol in util/beload.rs.
      Its write twin is store_be32 @ 0x083816cc (sqlite3Put4byte).

  - address: 0x08391dd0
    ghidra: raw ARM entry (runtime 0x08386ef8)
    name: sqlite_hash_str_compare
    size: 28
    sig: "int sqlite_hash_str_compare(const void *key1, int len1, const void *key2, int len2)"
    module: sqlite/find_element_given_hash
    status: ported
    vars: {param_1: key1, param_2: len1, param_3: key2, param_4: len2,
      r12: saved_len1}
    notes: >-
      SQLite 3.5.x hash.c's strCompare, selected for SQLITE_HASH_STRING
      by findElementGivenHash's literal at 0x082ce364. The literal is
      the runtime address 0x08386ef8; its actual raw body is at image
      0x08391dd0 under sqlite/mod.rs's +0xaed8 image/runtime skew. This
      distinction resolves the decompiler's erroneous 15996-byte
      FUN_08386ef8 extent: raw ARM at unskewed 0x08386ef8 is unrelated
      code. `osos.dec` 0x08391dd0..0x08391dec is exactly seven ARM
      instructions: mov r12,r1; cmp r12,r3; mov r1,r2; moveq r2,r12;
      beq sqlite3StrNICmp @ 0x08384fa0; movne r0,#1; bx lr. Thus it
      preserves the ABI `int(const void*,int,const void*,int)`, returns
      literal 1 when the lengths differ without reading either key, and
      otherwise tail-calls SQLite's ASCII-only str_nicmp with
      (key1,key2,len1). The port reuses sqlite::stricmp::str_nicmp,
      including its left-NUL stop and folded-byte-difference convention.
      Host tests cover the unequal-length no-read gate, equal-length ASCII
      folding, non-Latin-1 byte boundary, and the delegated NUL boundary.

  - address: 0x082b7870
    ghidra: raw ARM entry (runtime 0x082ac998)
    name: sqlite_hash_bin_compare
    size: 28
    sig: "int sqlite_hash_bin_compare(const void *key1, int len1, const void *key2, int len2)"
    module: sqlite/find_element_given_hash
    status: ported
    vars: {param_1: key1, param_2: len1, param_3: key2, param_4: len2,
      r12: saved_len1}
    notes: >-
      SQLite 3.5.x hash.c's binCompare, selected for every non-string
      keyClass by findElementGivenHash's literal at 0x082ce360. That
      literal holds runtime address 0x082ac998, while its actual raw body
      is at image 0x082b7870 under sqlite/mod.rs's +0xaed8 image/runtime
      skew. `osos.dec` 0x082b7870..0x082b788c is exactly seven ARM
      instructions: mov r12,r1; cmp r12,r3; mov r1,r2; moveq r2,r12; beq
      memcmp @ 0x08030f64; movne r0,#1; bx lr. Thus it preserves
      int(const void*,int,const void*,int): unequal lengths return literal
      1 without reading either key; equal lengths tail-call the ported
      memcmp(key1,key2,len1), preserving its first-differing-byte signed
      subtraction result. Equal zero lengths are null-safe through
      memcmp's no-load zero-length path. The target-only volatile function
      pointer prevents release LTO from inlining `memcmp`, preserving the
      call boundary at the cost of an indirect rather than direct tail
      branch; behavior and ABI are unchanged. Host tests cover the no-read
      length gate, equality, first-difference magnitude/sign, and this
      zero-length boundary.

  # --- Scouted in 0x08300000-0x083fffff, identified only ---
  - address: 0x08391dec
    ghidra: FUN_08391dec
    name: string_hash_tabled
    size: 72
    sig: "u32 string_hash_tabled(const u8 *s, i32 len)"
    module: sqlite/strhash
    status: ported
    vars: {param_1: s, param_2: len}
    notes: >-
      Table-driven byte hash - `h = table[c] ^ (h ^ (h << 3))` per byte,
      returning `h & 0x7fffffff`. A `len <= 0` measures the string with
      strlen @ 0x08392478 first. 3 bl call sites per the scouting note
      (none resolve in the osos.asm text - likely reached through a
      function pointer in SQLite's Hash machinery). The 256-byte table
      the literal at 0x08391e34 points at is sqlite3UpperToLower
      (runtime 0x088faa8b, image 0x088faa8b + 0xaed8 = 0x08905963 - see
      the skew note in sqlite/mod.rs), reproduced as
      sqlite/stricmp.rs's UPPER_TO_LOWER. This function is SQLite's
      strHash for SQLITE_HASH_NOCASE; its sibling at 0x08391df0 is the
      same loop without the length probe (a mid-function tail-entry
      alias, not a separate function). Port deviation - LLVM inlines
      the strlen (the cxx/string.rs precedent).

  - address: 0x08384f14
    ghidra: FUN_08384f14
    name: str_icmp
    size: 72
    sig: "int str_icmp(const char *left, const char *right)"
    module: sqlite/stricmp
    status: ported
    vars: {param_1: left, param_2: right}
    notes: >-
      SQLite's sqlite3StrICmp. 70 bl call sites. Folds each byte through
      sqlite3UpperToLower @ runtime 0x088faa8b and returns the DIFFERENCE
      of the folded bytes at the first mismatch or at left's NUL - not a
      normalized -1/0/+1. Not NUL-symmetric: only the left string's
      terminator stops the loop. The table was previously thought absent
      from the image; it is at 0x088faa8b + 0xaed8 = 0x08905963 (see the
      0xaed8 skew note in the region-survey block) and is plain ASCII
      tolower - identity except 'A'..'Z'. The earlier claim
      `table['A'] != table['a']` came from reading the image at the
      unskewed address.

  - address: 0x083d604c
    ghidra: FUN_083d604c
    name: handle_deref_or_null
    size: 16
    sig: "void *handle_deref_or_null(void ***slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      `cell = *slot; return cell ? *cell : NULL` - the offset-0 `T **`
      accessor of a handle wrapper, emitted once per template
      instantiation. Four words, `ldr r0,[r0]; cmp r0,#0; ldrne r0,[r0];
      bx lr`. NOTE the NULL test is on the INNER pointer, not on `slot`
      - a NULL `slot` faults. The earlier scouting note here said
      "roughly 60 copies from 0x083d604c to 0x083d62xx"; a byte-pattern
      scan of the whole image finds exactly 22, and one of them
      (0x08262b1c) is outside the C++ block entirely. Every one is
      byte-identical, so a single port serves all 22; hook any address
      that is needed to `handle_deref_or_null`. Binary-scanned bl counts
      (no b sites anywhere, 725 total) - 0x083d604c 253, 0x083d64f4 97,
      0x083d606c 69, 0x083d6190 69, 0x083d61d0 66, 0x083d60bc 25,
      0x083d64c4 18, 0x083d64e4 18, 0x083d602c 17, 0x083d61a0 16,
      0x083d6180 13, 0x083d607c 9, 0x083d64d4 9, 0x083d603c 8,
      0x083d609c 8, 0x083d60ac 7, 0x083d61b0 7, 0x083d60cc 5,
      0x083d608c 4, 0x083d605c 3, 0x083d61c0 2, 0x08262b1c 2. The
      canonical port cites 0x083d604c (most call sites); the alias
      entries below carry only their own counts. LLVM splits the
      conditional load into two basic blocks with a frame push instead
      of using `ldrne`; behavior is identical.

  # --- byte-identical aliases of handle_deref_or_null @ 0x083d604c ---
  # All 16 bytes, all `ldr r0,[r0]; cmp r0,#0; ldrne r0,[r0]; bx lr`,
  # all served by the one port in cxx/handle.rs.
  - {address: 0x083d602c, ghidra: FUN_083d602c, name: handle_deref_or_null_alias_602c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 17 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d603c, ghidra: FUN_083d603c, name: handle_deref_or_null_alias_603c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 8 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d605c, ghidra: FUN_083d605c, name: handle_deref_or_null_alias_605c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 3 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d606c, ghidra: FUN_083d606c, name: handle_deref_or_null_alias_606c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 69 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d607c, ghidra: FUN_083d607c, name: handle_deref_or_null_alias_607c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 9 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d608c, ghidra: FUN_083d608c, name: handle_deref_or_null_alias_608c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 4 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d609c, ghidra: FUN_083d609c, name: handle_deref_or_null_alias_609c,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 8 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d60ac, ghidra: FUN_083d60ac, name: handle_deref_or_null_alias_60ac,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 7 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d60bc, ghidra: FUN_083d60bc, name: handle_deref_or_null_alias_60bc,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 25 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d60cc, ghidra: FUN_083d60cc, name: handle_deref_or_null_alias_60cc,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 5 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d6180, ghidra: FUN_083d6180, name: handle_deref_or_null_alias_6180,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 13 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d6190, ghidra: FUN_083d6190, name: handle_deref_or_null_alias_6190,
     size: 16, sig: "void *handle_deref_or_null(void ***slot)", module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c (verified: same 16 bytes in osos.dec); 68 bl sites in decomp/osos.asm, 69 binary-scanned - the 69th site at 0x08236e48 (word 0xeb067cd0) sits in a block Ghidra left undisassembled. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d61a0, ghidra: FUN_083d61a0, name: handle_deref_or_null_alias_61a0,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 16 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d61b0, ghidra: FUN_083d61b0, name: handle_deref_or_null_alias_61b0,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 7 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d61c0, ghidra: FUN_083d61c0, name: handle_deref_or_null_alias_61c0,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 2 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d61d0, ghidra: FUN_083d61d0, name: handle_deref_or_null_alias_61d0,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 66 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d64c4, ghidra: FUN_083d64c4, name: handle_deref_or_null_alias_64c4,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 18 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d64d4, ghidra: FUN_083d64d4, name: handle_deref_or_null_alias_64d4,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 9 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - {address: 0x083d64e4, ghidra: FUN_083d64e4, name: handle_deref_or_null_alias_64e4,
     size: 16, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c; 18 bl sites. No separate port exists; hook this address to the single handle_deref_or_null symbol (the load_be32 alias precedent)."}
  - address: 0x08262b1c
    ghidra: FUN_08262b1c
    name: handle_deref_or_null_alias_2b1c
    size: 16
    module: cxx/handle
    status: ported
    notes: >-
      ALIAS - byte-identical to handle_deref_or_null @ 0x083d604c
      (verified against osos.asm: same four instructions); 2 bl sites.
      The only copy outside the 0x083c0000-0x083dffff C++ block. No
      separate port exists; hook this address to the single
      handle_deref_or_null symbol.

  - address: 0x083d60dc
    ghidra: FUN_083d60dc
    name: handle_elem_ptr
    size: 20
    sig: "u32 *handle_elem_ptr(u32 *const *const *slot, u32 index)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot, param_2: index}
    notes: >-
      The indexed member of the handle_deref_or_null family:
      `cell = *slot; base = cell ? *cell : NULL; return base + index`
      with 4-byte elements - five words: `ldr r0,[r0]; cmp r0,#0;
      ldrne r0,[r0]; add r0,r0,r1,lsl #2; bx lr`. The element add is
      UNCONDITIONAL: a NULL cell yields index*4 as a bare address, not
      NULL, and the NULL test is on the inner pointer (a NULL slot
      faults). Binary-scanned: exactly 2 byte-identical copies
      (0x083d60dc, 0x083d60f0), 3 unconditional bl sites each, and all
      six sites sit inside the two sibling cursor inits
      (plane_cursor_init @ 0x0839bae0 and its byte-similar sibling @
      0x0839bb58) - no other callers, no b sites, no data-word
      references. Ported alongside plane_cursor_init so that port's
      three bls keep the original's structure; hook both addresses to
      the one symbol. match.py - LLVM splits the predicated ldrne into
      a framed moveq/addeq/bxeq branch, the same deviation as
      handle_deref_or_null; behavior identical.

  - {address: 0x083d60f0, ghidra: FUN_083d60f0, name: handle_elem_ptr_alias_60f0,
     size: 20, module: cxx/handle, status: ported,
     notes: "ALIAS - byte-identical to handle_elem_ptr @ 0x083d60dc (same 20 bytes in osos.dec); 3 bl sites, all inside the sibling cursor init @ 0x0839bb58. No separate port exists; hook this address to the single handle_elem_ptr symbol (the load_be32 alias precedent)."}

  - address: 0x08386840
    ghidra: FUN_08386840
    name: vdbe_add_op3
    size: 132
    sig: "int vdbe_add_op3(Vdbe *p, int opcode, int p1, int p2, int p3)"
    module: sqlite/vdbe
    status: ported
    vars: {param_1: p, param_2: opcode, param_3: p1, param_4: p2}
    notes: >-
      SQLite's sqlite3VdbeAddOp3 - the busiest function in the database
      engine at 282 bl call sites. The earlier survey's "record container
      with 20-byte elements" is exactly this: 20 is sizeof(VdbeOp) and
      the owner is a Vdbe (prepared statement). Appends one bytecode
      instruction and returns the address it landed at (the pre-append
      nOp). Vdbe fields - db +0x00, nOp +0x0c, nOpAlloc +0x10, aOp +0x14,
      expired +0xff. When the array is full it resizes via 0x08367f88 to
      `nOpAlloc * 2`, or to 51 when empty (SQLite's 1024/sizeof(Op)), and
      returns 0 if the connection's mallocFailed byte (db +0x1e) is set
      afterwards. VdbeOp layout - opcode +0 (u8), p4type +1 (i8),
      opflags +2 (NEVER written), p5 +3 (u8), p1 +4, p2 +8, p3 +12
      (`stmib`), p4 +16 (zeroed). Also clears p->expired at +0xff.

  - address: 0x0839cf4c
    ghidra: FUN_0839cf4c
    name: refcounted_body_release_owned_variant
    size: 144
    sig: "void refcounted_body_release_owned_variant(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      19 direct bl call sites, all unconditional (verified by decoding every
      ARM B/BL word in osos.dec): no predicated forms, no tail b sites, and
      no data-word references - never virtually dispatched. Ghidra's 136-byte
      extent is short: raw ARM runs from 0x0839cf4c through 0x0839cfd8
      (144 bytes); the separately linked mutex-lock helper begins at
      0x0839cfdc and the mutex-unlock helper at 0x0839cfec. Owning template
      sibling of refcounted_body_release_owned @ 0x0839d2c0 over the same
      RefcountedBody layout (+0 implementation, +4 i32 refcount, +8 Mutex*).
      NULL body returns untouched. Otherwise it locks the optional mutex,
      subtracts one with plain wrapping subs, unlocks and NULLs the slot on a
      non-final result, and on exactly zero disposes the NULL-guarded
      implementation then tag-2 deletes its returned pointer, unlocks,
      mutex-deletes plus tag-2 deletes the mutex, clears body+8, tag-2 deletes
      the body, and NULLs the slot. The direct implementation disposer
      0x0816f5c0 is unported: raw ARM confirms its separate 124-byte body
      returns its input after teardown, but no class identity is invented.
      Target builds transmute-call that address; host tests use an identity
      seam solely to record the direct call. Codegen deviation: LLVM inlines
      the ported mutex and heap helpers, retaining the same
      guard/decrement/dispose/unlock/teardown order.

  - address: 0x0839d038
    ghidra: FUN_0839d038
    name: refcounted_body_release_slot1_copy
    size: 144
    sig: "void refcounted_body_release_slot1_copy(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r4: slot
      param_1_plus_0x0: body
      body_plus_0x0: implementation
      body_plus_0x4: refcount
      body_plus_0x8: mutex
      vtable_plus_0x4: virtual_destructor_slot_1
    notes: >-
      Raw ARM spans 0x0839d038..0x0839d0c8 (144 bytes); Ghidra's
      136-byte extent omits the trailing slot NULL store and return. The
      separately linked 16-byte mutex-lock and mutex-unlock helpers begin at
      0x0839d0c8 and 0x0839d0d8. Decoding every ARM B/BL word in osos.dec
      finds 13 direct bl callers, all unconditional: no predicated forms, no
      tail b sites, and no data-word references, so it is never virtually
      dispatched. Separately linked copy of the slot-1 RefcountedBody release:
      NULL body leaves the slot untouched; otherwise it locks the optional
      mutex, wrapping-decrements body+4, unlocks and NULLs the slot for
      non-final counts. On exactly zero it NULL-guardedly dispatches the
      implementation's vtable word 1, never frees that implementation block,
      unlocks, mutex-deletes and tag-2-deletes the mutex, clears body+8,
      tag-2-deletes the body, then NULLs the slot. Rust directly calls the
      already ported mutex and heap helpers rather than the local helpers;
      LLVM may inline them, retaining the same operation order. Host tests
      cover NULL, non-final, and final slot-1 teardown paths.

  - address: 0x0839ccac
    ghidra: FUN_0839ccac
    name: refcounted_body_release_dtor_copy
    size: 144
    sig: "void refcounted_body_release_dtor_copy(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r4: slot
      param_1_plus_0x0: body
      body_plus_0x0: implementation
      body_plus_0x4: refcount
      body_plus_0x8: mutex
      vtable_plus_0x4: virtual_destructor_slot_1
    notes: >-
      Raw ARM spans 0x0839ccac..0x0839cd3c (144 bytes); Ghidra's reported
      136-byte extent omits the final slot NULL store and return. The
      separately linked 16-byte mutex-lock and mutex-unlock helpers start
      at 0x0839cd3c and 0x0839cd4c. Decoding every ARM B/BL word in
      osos.dec finds exactly 9 direct bl callers, all unconditional:
      0x08280908, 0x08280aa4, 0x08280c9c, 0x08280d24, 0x08280d50,
      0x08280d68, 0x08280e38, 0x08280f68, and 0x0839ed20. No predicated
      forms, tail b sites, or data-word references occur, so it is never
      virtually dispatched. A separately linked copy of
      refcounted_body_release_dtor @ 0x0839cbc0: NULL body leaves slot
      untouched; otherwise optional mutex lock, wrapping refcount decrement,
      final vtable word-1 destructor dispatch, unlock, mutex delete/free,
      body free, then slot NULL. The implementation block is never freed.
      Rust directly calls the already ported mutex and heap helpers; LLVM may
      inline them rather than preserve the stock local helper calls, while
      retaining the guard/decrement/slot-1-dispatch/teardown order. Host tests
      cover NULL, non-final mutex, final dispatch/teardown, and wrapping-zero
      paths.
  - address: 0x0839cbc0
    ghidra: FUN_0839cbc0
    name: refcounted_body_release_dtor
    size: 144
    sig: "void refcounted_body_release_dtor(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      24 bl call sites (23 unconditional plus one blne at 0x083c64d0,
      verified by decoding every ARM B/BL word in osos.dec; no b sites,
      no data-word references - never virtually dispatched). Ghidra's
      136-byte extent is short: the body runs to 0x0839cc50 (144 bytes),
      where the separately linked 16-byte mutex-lock helper starts
      (0x0839cc50 locks, 0x0839cc60 unlocks; each loads body+8,
      NULL-checks it and tail-branches to mutex_lock 0x0807f5c4 /
      mutex_unlock 0x0807f6a0). A third template instantiation of
      refcounted_body_release @ 0x0839cd98 over the same RefcountedBody
      layout (+0 impl, +4 i32 refcount, +8 Mutex*): the bodies are
      byte-identical modulo bl displacements except ONE word - the final
      drop's virtual dispatch is `ldrne r1,[r1,#4]` (vtable slot 1, the
      plain virtual destructor) where the sibling loads slot 7 (+0x1c).
      Like the slot-7 sibling it never frees the impl block (unlike
      refcounted_body_release_owned @ 0x0839d2c0). Everything else
      matches the siblings: NULL body early-out, wrapping subs decrement
      (zero underflows to -1 and takes the shared path - tested),
      non-final drop unlocks and NULLs the slot, final drop unlocks
      through a fresh slot load, then mutex_delete (0x0807f650, ported in
      kernel/sync_mutex.rs) plus tag-2 operator delete (0x082aad24,
      ported) on the reloaded mutex, clears the mutex field, tag-2
      deletes the body, NULLs the slot. Codegen deviation: LLVM inlines
      the ported lock/unlock/delete (ROM_KERNEL dispatch included), so
      match.py shows a larger body with the same
      guard/decrement/vtable[1]/unlock/teardown structure.

  - address: 0x0839cd5c
    ghidra: FUN_0839cd5c
    name: refcounted_body_acquire
    size: 60
    sig: "void refcounted_body_acquire(RefcountedBody **dst, RefcountedBody *body)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1/r4: body
      body_plus_0x4: refcount
      body_plus_0x8: mutex
    notes: >-
      Raw ARM is exactly 60 bytes, 0x0839cd5c..0x0839cd98; the separately
      linked refcounted_body_release starts immediately at 0x0839cd98.
      Decoding every ARM B/BL word in osos.dec finds 10 direct bl callers,
      all unconditional, plus two unconditional tail b sites at 0x08131fbc
      and 0x08218e08; no predicated sites and no image word equal to this
      address, so it is never virtually dispatched. The unconditional
      `str r1,[r0]` stores body before the NULL early-out. Non-NULL body
      loads mutex at +8 and NULL-checks before mutex_lock, wrapping-increments
      signed refcount at +4, then reloads and NULL-checks mutex before
      mutex_unlock. A NULL mutex leaves the increment unguarded. This is a
      separately linked copy of refcounted_body_attach @ 0x0839d370; Rust
      preserves the dedicated target section to prevent folding and calls the
      already ported mutex helpers directly, which LLVM may inline while
      retaining the store/guard/increment/guard sequence. Host tests cover
      NULL overwrite, wrapping increment, and the non-NULL mutex path.

  - address: 0x0839cd98
    ghidra: FUN_0839cd98
    name: refcounted_body_release
    size: 144
    sig: "void refcounted_body_release(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      36 bl call sites (35 unconditional plus one blne at 0x081339f8;
      no b sites, no data-word references - never virtually dispatched).
      Ghidra's 136-byte extent is short: the body runs to 0x0839ce28
      (144 bytes), where the separately linked 16-byte mutex-lock helper
      starts (0x0839ce28 locks, 0x0839ce38 unlocks; each loads body+8,
      NULL-checks it and tail-branches to mutex_lock 0x0807f5c4 /
      mutex_unlock 0x0807f6a0). The teardown behind
      refcounted_ptr_release @ 0x0816cd44 and 34 other callers: NULL
      body early-out, lock the optional mutex at body+8, then a plain
      wrapping `subs` on the i32 refcount at +4 (zero underflows to -1
      and takes the shared path - tested). Non-final drop: unlock, NULL
      the slot. Final drop: run the implementation's virtual destructor
      through vtable word 7 (+0x1c) of the impl pointer at body+0
      (NULL-guarded), unlock, then mutex_delete (0x0807f650, ported in
      kernel/sync_mutex.rs) plus tag-2 operator delete (0x082aad24) on
      the reloaded mutex, clear the mutex field, tag-2 delete the body,
      NULL the slot. Body layout (+0 impl, +4 i32 refcount, +8 Mutex*)
      is struct RefcountedBody in cxx/handle.rs. Codegen deviation:
      LLVM inlines the ported lock/unlock (ROM_KERNEL dispatch included)
      and the helpers' guards, so match.py shows a larger body with the
      same guard/decrement/vtable[7]/delete structure. Port landed in
      commit b25c610 without this ledger entry; entry added after
      re-verifying extent, call count and tests.

  - address: 0x0839d2c0
    ghidra: FUN_0839d2c0
    name: refcounted_body_release_owned
    size: 144
    sig: "void refcounted_body_release_owned(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      28 bl call sites, all unconditional (verified by decoding every ARM
      B/BL word in osos.dec): no b sites, no predicated forms, and the
      address appears in no data word - never virtually dispatched.
      Ghidra's 136-byte extent is short again: the body runs to
      0x0839d350 (144 bytes), where the separately linked 16-byte
      mutex-lock helper starts (0x0839d350 locks, 0x0839d360 unlocks;
      each loads body+8, NULL-checks it and tail-branches to mutex_lock
      0x0807f5c4 / mutex_unlock 0x0807f6a0). A sibling template
      instantiation of refcounted_body_release @ 0x0839cd98 over the same
      RefcountedBody layout (+0 impl, +4 i32 refcount, +8 Mutex*) - the
      two bodies are byte-identical modulo bl displacements except for
      the final drop's implementation disposal. Where the sibling runs
      vtable slot 7 (+0x1c) and never frees the impl, this instantiation
      OWNS it: the NULL-guarded impl word is passed through the disposal
      @ 0x0827948c (44 bytes, unported: calls virtual slot 1 (+4) on the
      impl's first-word callback interface when non-NULL, then the ported
      string_object_destroy @ 0x08277484 on the two-word StringObject
      member at impl+4, returning this) and the disposal's return feeds
      tag-2 operator_delete @ 0x082aad24 directly. Everything else
      matches the sibling: NULL body early-out, wrapping subs decrement
      (zero underflows to -1 and takes the shared path - tested),
      non-final drop unlocks and NULLs the slot, final drop unlocks
      through a fresh slot load, then mutex_delete @ 0x0807f650 plus
      tag-2 delete on the reloaded mutex word, clears the mutex field,
      tag-2-deletes the body, NULLs the slot. Target dispatch to the
      unported 0x0827948c goes through a transmuted direct call
      (release.rs pattern); host builds model it faithfully - its whole
      chain is data-driven or ported. Codegen deviation: LLVM inlines
      the ported mutex helpers and deletes (ROM_KERNEL dispatch via
      literal-pool blx included), so match.py shows a larger body with
      the same guard/decrement/dispose/unlock/teardown structure.

  - address: 0x0839d370
    ghidra: FUN_0839d370
    name: refcounted_body_attach
    size: 60
    sig: "void refcounted_body_attach(RefcountedBody **dst, RefcountedBody *body)"
    module: cxx/handle
    status: ported
    vars: {param_1: dst, param_2: body}
    notes: >-
      6 bl call sites (verified by decoding every ARM B/BL word in
      osos.dec): 3 unconditional (0x081f0dbc, 0x081fcc30, 0x0839f2b0
      inside refcounted_ptr_copy_assign) plus 3 blne (0x083e8a60,
      0x083e8c28, 0x083e9538) whose callers predicate on the destination
      SLOT pointer (movs r0, r5; ldrne r1, [r4]; blne) - they NULL-check
      the slot themselves; this helper's only guard is on the body,
      after the store. No b sites, no data-word references: never
      virtually dispatched. Ghidra's 60-byte extent is exact:
      refcounted_body_release_dtor_variant starts at 0x0839d3ac. The
      shared-body attach half of the copy-assignment operators:
      refcounted_ptr_assign @ 0x0839eda0 with the obj = *src load
      hoisted into the second argument. *dst = body unconditionally
      (str r1, [r0] precedes the movs-flagged popeq), then a non-NULL
      body's refcount (+4) is bumped by one (wrapping add, tested at
      i32::MAX) under the optional mutex at +8 - the mutex word is
      loaded twice and NULL-checked separately around the bump. The
      original's return value is unreliable (r0 = dst only on the
      NULL-body popeq; the bump path tail-branches into mutex_unlock or
      pops r0 = mutex) and unused by callers, so the port returns
      nothing. Codegen deviation: LLVM inlines the ported lock/unlock
      and tail-duplicates the bump block, so match.py shows a larger
      body with the same store/guard/bump/guard structure; the symbol
      resolves in librustypod.a (no folding).


  - address: 0x0839d498
    ghidra: FUN_0839d498
    name: refcounted_body_release_retain_count
    size: 152
    sig: "void refcounted_body_release_retain_count(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r4: slot
      param_1_plus_0x0: body
      body_plus_0x0: implementation
      body_plus_0x4: refcount
      body_plus_0x8: mutex
      FUN_081f7328: direct_retain_count_disposer
    notes: >-
      Raw ARM establishes the true 152-byte extent
      0x0839d498..0x0839d530: Ghidra's 144-byte extent omits the final
      `str r6,[r4]` / `pop {r4,r5,r6,pc}`, and a separately linked
      16-byte mutex-lock helper begins immediately at 0x0839d530 (the
      matching unlock helper starts at 0x0839d540). Decoding every ARM B/BL
      word in osos.dec finds 11 direct bl callers, all unconditional: no
      predicated forms and no tail b sites. NULL body leaves the slot
      untouched; otherwise the optional mutex is locked, body+4 is
      wrapping-decremented, and the slot is NULLed. Non-final releases only
      unlock. On exactly zero, the NULL-guarded implementation is passed with
      the just-stored zero refcount to unported direct callee 0x081f7328,
      then the mutex is unlocked, destroyed and tag-2-deleted, body+8 is
      cleared, and the body is tag-2-deleted. The direct callee has a
      separately decoded 104-byte body but no inferred identity; target code
      calls it directly and host tests use a recording seam. LLVM may inline
      ported mutex and heap helpers while preserving the
      guard/decrement/dispose/unlock/teardown order.
  - address: 0x0839d3ac
    ghidra: FUN_0839d3ac
    name: refcounted_body_release_dtor_variant
    size: 144
    sig: "void refcounted_body_release_dtor_variant(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      23 bl call sites, all unconditional (verified by decoding every ARM
      B/BL word in osos.dec): no b sites, no predicated forms, and the
      address appears in no data word - never virtually dispatched.
      Ghidra's 136-byte extent is short again: the body runs to
      0x0839d43c (144 bytes), where the separately linked 16-byte
      mutex-lock helper starts (0x0839d43c locks, 0x0839d44c unlocks;
      each loads body+8, NULL-checks it and tail-branches to mutex_lock
      0x0807f5c4 / mutex_unlock 0x0807f6a0, both ported). A fourth
      separately linked copy of the slot-1 teardown: its 36 words are
      byte-identical to refcounted_body_release_dtor @ 0x0839cbc0 except
      the three absolute bl displacements to mutex_delete @ 0x0807f650
      and operator_delete @ 0x082aad24 (both ported). Same RefcountedBody
      layout (+0 impl, +4 i32 refcount, +8 Mutex*): NULL body early-out
      (slot untouched), lock the optional mutex, wrapping subs decrement
      (zero underflows to -1 and takes the shared path - tested),
      non-final drop unlocks and NULLs the slot, final drop dispatches
      the impl's plain virtual destructor at vtable word 1 (+4) - impl
      NULL-guarded, block never freed - unlocks through a fresh slot
      load, then mutex_delete plus tag-2 delete on the reloaded mutex
      word, clears the mutex field, tag-2-deletes the body, NULLs the
      slot on every path that reached the decrement. A dedicated
      link_section keeps LLVM from folding this hookable copy into its
      source-identical sibling; the symbol resolves in librustypod.a.
      Codegen deviation: LLVM inlines the ported mutex helpers and
      deletes (ROM_KERNEL dispatch included), so match.py shows a larger
      body with the same guard/destruct/unlock/teardown structure.

  - address: 0x0839d44c
    ghidra: FUN_0839d44c
    name: refcounted_body_mutex_unlock
    size: 16
    sig: "void refcounted_body_mutex_unlock(RefcountedBody *body)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0: body
      body_plus_0x8/r0: mutex
    notes: >-
      Raw ARM extent is exactly 16 bytes, 0x0839d44c..0x0839d45c: ldr
      r0,[r0,#8]; cmp r0,#0; bne mutex_unlock @ 0x0807f6a0; bx lr. The next
      separately linked function starts at 0x0839d45c. Decoding every ARM
      B/BL word in osos.dec finds eight direct bl callers, all unconditional
      at 0x0811ef44, 0x0811ef7c, 0x0816cf70, 0x0816cfa8, 0x08214520,
      0x08214558, 0x0839d3f8, and 0x0839d430; no predicated calls or direct
      tail b sites. No word-aligned image word equals this address, so it is
      not virtually dispatched. It loads the RefcountedBody's optional mutex
      at target +8 and tail-branches to the already ported mutex_unlock only
      when non-NULL. No deliberate behavioral deviations: structural review
      shows LLVM retains the tail branch after harmless frame setup, preserving
      the load/guard/unlock behavior. Host test covers a live mutex signal and
      the NULL-mutex early-out.

  - address: 0x0839e0e8
    ghidra: FUN_0839e0e8
    name: dma_aligned_byte_array_construct
    size: 124
    sig: "DmaAlignedArray *dma_aligned_byte_array_construct(DmaAlignedArray *array, u32 byte_count, u32 allocation_tag)"
    module: cxx/dma_aligned_array_destroy
    status: ported
    renames:
      param_1/r0/r4: array
      param_2/r1: byte_count
      param_3/r2: allocation_tag
      iVar1/r0: allocation
      uVar2/r0: aligned_data
    notes: >-
      Raw bytes establish the 124-byte extent 0x0839e0e8..0x0839e164: the
      DMA-aligned-array destructor starts immediately at 0x0839e164. Decoding
      every ARM B/BL word in osos.dec finds 26 direct bl callers, all
      unconditional: zero predicated forms and zero tail b sites. The
      byte-specialized constructor clears raw allocation +0x00 and aligned
      view +0x04, stores byte_count at +0x08 and clears the construction byte
      at +0x0c; it allocates byte_count + 0x40 through already ported
      malloc_wrapper (tag passed through), aligns the raw block up to 32 bytes,
      and, if bit 27 is set, clean-invalidates the aligned byte range through
      already ported dcache_clean_invalidate before setting bit 31 in the
      stored view. A count-sized empty trivial-element loop then completes
      before construction byte = 1. Port uses black_box to retain that otherwise
      empty loop; it has no memory effect. No dispatch seam was added because
      both callees are already ported.

  - address: 0x0839e164
    ghidra: FUN_0839e164
    name: dma_aligned_array_destroy
    size: 72
    sig: "DmaAlignedArray *dma_aligned_array_destroy(DmaAlignedArray *array)"
    module: cxx/dma_aligned_array_destroy
    status: ported
    renames:
      param_1/r0/r4: array
      uVar1/r0: element_index
    notes: >-
      The raw body is exactly 72 bytes (0x0839e164..0x0839e1ac); the next
      separately linked copy helper starts at 0x0839e1ac. Decoding every ARM
      B/BL word in osos.dec finds 26 direct bl callers, all unconditional:
      no predicated forms and no tail b sites. No image word equals
      0x0839e164, so it is statically bound rather than virtually dispatched.
      The array's 32-bit layout is raw tag-3 allocation +0x00, 32-byte-aligned
      data view +0x04, element count +0x08, and construction byte +0x0c. When
      constructed, it performs a count-sized empty trivial-element walk, clears
      the construction byte, and frees a non-NULL raw allocation through the
      already ported free_wrapper with tag 3, returning array. The nearby
      constructor at 0x0839e200 allocates count*4 + 0x40 and conditionally
      dcache-cleans the 32-byte-aligned view, which establishes the
      DMA-aligned-array identity. Codegen deliberately uses black_box to retain
      the original's count-dependent empty busy-wait; all object state and free
      behavior match the ARM body.

  - address: 0x0839eb8c
    ghidra: FUN_0839eb8c
    name: vtable_word_callback
    size: 24
    sig: "usize vtable_word_callback(void *receiver, u32 word, usize discarded_r2, usize forwarded_r3)"
    module: cxx/vtable_word_callback
    status: ported
    renames:
      param_1/r0: receiver
      param_2/r1: word
      param_3/r2: discarded_r2
      param_4/r3: forwarded_r3
      uStack_8: spilled_word
      piVar1: vtable
      uVar2: callback
    notes: >-
      Raw osos.dec extent is exactly 24 bytes: six ARM instruction words from
      0x0839eb8c through pop {r2,r3,r4,pc} at 0x0839eba0; the next separately
      linked sibling starts at 0x0839eba4. It spills incoming r1, loads
      receiver->vtable and slot +0x1c, then blx calls
      callback(receiver, &spilled_word, callback, forwarded_r3). The virtual
      method return remains in r0; mutation of the spilled word is discarded
      by the pop. Decoding every immediate ARM B/BL word finds exactly 11
      direct call sites: ten unconditional bl and one blcc at 0x08147fc8;
      there are no tail b sites. The predicated caller gates this deliberately
      NULL-unguarded wrapper. The Rust local models the ARM spill and a normal
      return models the final virtual result propagation; both deviations are
      ABI-observable equivalent.
  - address: 0x0839ebc4
    ghidra: FUN_0839ebc4
    name: refcounted_handle_construct
    size: 104
    sig: "RefcountedBody **refcounted_handle_construct(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: slot
      param_2/r1/r6: implementation
      param_3/r2/r8: want_mutex
      piVar1/r4: body
      puVar2: mutex
    notes: >-
      Raw bytes establish the exact 104-byte extent
      (0x0839ebc4..0x0839ec2c), where a separately linked function begins.
      Decoding every ARM B/BL word in osos.dec finds exactly 13 direct BL
      callers, all unconditional: no tail B sites, no predicated forms, and
      no image data-word references, so this template instantiation is not
      virtually dispatched. It clears slot first; with a non-NULL
      implementation, tag-2 operator_new(12) creates RefcountedBody {
      implementation, 1, NULL }; want_mutex allocates and zeroes tag-2
      operator_new(8), stores it at body+8, then calls mutex_create before
      publishing body. Returns slot. This is byte-identical to
      refcounted_ptr_construct @ 0x0839ed38 after normalizing the three BL
      displacements. Deliberate codegen deviation: LLVM may inline the ported
      mutex_create (including ROM_KERNEL dispatch), so the ARM body is larger
      but retains the alloc/init/store sequence. The dedicated target section
      prevents folding this separately hookable copy into its byte-identical
      siblings; host tests use oversized arenas because the host structs are
      wider than the target's 12/8-byte layouts.


  - address: 0x0839ec70
    ghidra: FUN_0839ec70
    name: refcounted_handle_copy_assign
    size: 48
    sig: "RefcountedBody **refcounted_handle_copy_assign(RefcountedBody **dst, RefcountedBody **src)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: dst
      param_2/r1/r4: src
    notes: >-
      Raw ARM establishes the exact 48-byte body 0x0839ec70..0x0839eca0,
      where a separately linked constructor begins. Decoding every ARM B/BL
      word in osos.dec finds exactly 11 direct BL callers, all unconditional
      (0x0812f6d4, 0x0812f940, 0x0812f99c, 0x081304d4, 0x081b5888,
      0x081b5d00, 0x081f79e8, 0x081f80c8, 0x081f8a4c, 0x081f8b2c, and
      0x081f8f78): no tail B sites, no predicated forms, and no image word
      equals the address, so it is never virtually dispatched. It compares
      slot addresses, and when distinct calls the ported slot-1 release
      refcounted_body_release_dtor @ 0x0839cbc0 before loading *src, then
      calls the separately linked attach helper @ 0x0839cb84. That helper is
      byte-identical to ported refcounted_body_attach @ 0x0839d370 modulo
      direct-branch displacements, so this port uses the already ported
      canonical helper. It returns dst unconditionally; host tests cover
      self-assignment and a non-final release followed by source attach. A
      dedicated target section prevents this hookable copy from folding with a
      sibling.
  - address: 0x0839eca0
    ghidra: FUN_0839eca0
    name: refcounted_handle_construct_variant
    size: 104
    sig: "RefcountedBody **refcounted_handle_construct_variant(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: slot
      param_2/r1/r6: implementation
      param_3/r2/r8: want_mutex
      piVar1/r4: body
      puVar2: mutex
    notes: >-
      Raw bytes establish the exact 104-byte extent
      (0x0839eca0..0x0839ed08), where another separately linked function
      begins. Decoding every ARM B/BL word in
      osos.dec finds exactly 13 direct BL callers, all unconditional: no
      tail B sites, no predicated forms, and no image data word references,
      so this template instantiation is not virtually dispatched. It clears
      slot first; with a non-NULL implementation, tag-2 operator_new(12)
      creates RefcountedBody { implementation, 1, NULL }; want_mutex
      allocates and zeroes tag-2 operator_new(8), stores it at body+8, then
      calls mutex_create before publishing body. Returns slot. This is
      byte-identical to refcounted_ptr_construct @ 0x0839ed38 after
      normalizing the three BL displacements. Deliberate codegen deviation:
      LLVM inlines ported mutex_create (including ROM_KERNEL dispatch), so
      the ARM body is larger but retains the alloc/init/store sequence. The
      dedicated target section prevents folding this separately hookable copy
      into its byte-identical siblings; host tests use oversized arenas
      because the host structs are wider than the target's 12/8-byte layouts.

  - address: 0x0839eda0
    ghidra: FUN_0839eda0
    name: refcounted_ptr_assign
    size: 68
    sig: "RefcountedBody **refcounted_ptr_assign(RefcountedBody **dst, RefcountedBody **src)"
    module: cxx/handle
    status: ported
    vars: {param_1: dst, param_2: src, iVar1: obj}
    notes: >-
      78 bl call sites. `obj = *src; *dst = obj`, and when obj is
      non-NULL bumps its refcount at +4 under the mutex at +8 (RTXC
      mutex lock 0x0807f5c4 / unlock 0x0807f6a0, both since ported in
      kernel/sync_mutex.rs, which is what unblocked this port). The
      mutex field is re-loaded and NULL-checked again before the
      unlock. Returns dst. Body layout recovered here: +0 opaque word,
      +4 i32 refcount, +8 Mutex* (struct RefcountedBody in
      cxx/handle.rs). Codegen deviation: LLVM inlines the ported
      lock/unlock (guards + ROM_KERNEL dispatch) rather than the
      original's bl pair - match.py shows the same guard/bump/guard
      structure inside a larger body.

  - address: 0x0839ef3c
    ghidra: FUN_0839ef3c
    name: refcounted_ptr_copy_construct
    size: 24
    sig: "RefcountedBody **refcounted_ptr_copy_construct(RefcountedBody **dst, RefcountedBody **src)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r4: dst
      param_2/r1: src
    notes: >-
      Raw ARM establishes the exact 24-byte extent 0x0839ef3c..0x0839ef54;
      a separately linked copy-assignment function begins at 0x0839ef54.
      Decoding every ARM B/BL word in osos.dec found exactly 10 direct BL
      callers: nine unconditional (0x08132d28, 0x08132ef4, 0x08133290,
      0x0813332c, 0x081333d0, 0x081334b4, 0x08133510, 0x08133584, and
      0x081336c8) and one blne at 0x083dc2fc; no tail B transfers and no
      image data word references, so it is not virtually dispatched. The
      predicated caller supplies the destination-slot guard. This C++ copy
      constructor loads *src, calls separately linked helper 0x0839cf10 to
      store the body in dst and increase its signed +4 refcount under the
      optional +8 mutex, then returns dst. Helper 0x0839cf10 is behaviorally
      byte-identical to ported refcounted_body_attach @ 0x0839d370, so the
      Rust port calls that canonical helper; its store, optional lock,
      wrapping increment, and freshly reloaded optional unlock preserve the
      stock ordering. The target section prevents linker folding with a
      byte-identical constructor sibling.

  - address: 0x0839f098
    ghidra: FUN_0839f098
    name: refcounted_ptr_construct_slot1
    size: 104
    sig: "RefcountedBody **refcounted_ptr_construct_slot1(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: slot
      param_2/r1/r6: implementation
      param_3/r2/r8: want_mutex
      piVar1/r4: body
      puVar2: mutex
    notes: >-
      Raw ARM establishes the exact 104-byte body 0x0839f098..0x0839f100:
      a separately linked forwarding copy-constructor wrapper begins at
      0x0839f100. Decoding every ARM B/BL word in osos.dec finds exactly
      eight direct bl callers, all unconditional: 0x08137614, 0x081377ac,
      0x081623d0, 0x081c0398, 0x081c03e8, 0x081c0460, 0x081c0520, and
      0x081c0598. It also has one beq tail transfer at 0x081623ac plus
      unconditional b transfers at 0x0820bd70, 0x0820bde8, 0x082104a8,
      0x082104e4, 0x082151c4, 0x0821533c, 0x08223c10, and 0x08223d88;
      no predicated bl forms or image data words equal the address. This
      separately linked C++ template copy clears slot first; a non-NULL
      implementation gets tag-2 operator_new(12) as RefcountedBody
      { implementation, 1, NULL }, with want_mutex optionally adding a
      zeroed tag-2 operator_new(8) Mutex initialized by mutex_create before
      the body is published. It returns slot. The Rust port uses the already
      ported direct callees and a dedicated target section to preserve this
      hookable entry; LLVM may inline mutex_create rather than retaining its
      stock direct bl.
  - address: 0x0839f148
    ghidra: FUN_0839f148
    name: refcounted_ptr_construct_variant
    size: 104
    sig: "RefcountedBody **refcounted_ptr_construct_variant(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: slot
      param_2/r1/r6: implementation
      param_3/r2/r8: want_mutex
      piVar1/r4: body
      puVar2: mutex
    notes: >-
      29 bl call sites, all unconditional, verified by decoding every ARM
      B/BL word in osos.dec: no b sites, no predicated forms, and no
      data-word references, so it is not virtually dispatched. Raw bytes
      establish the exact 104-byte extent: the next separately linked
      function begins at 0x0839f1b0. This C++ template instantiation is
      byte-identical to refcounted_ptr_construct @ 0x0839ed38 after
      normalizing the three bl displacements. It clears slot first; with a
      non-NULL implementation, tag-2 operator_new(12) creates
      RefcountedBody { implementation, 1, NULL }; want_mutex allocates and
      zeroes tag-2 operator_new(8), stores it at body+8, then calls
      mutex_create before publishing body. Returns slot. The dedicated
      .text.refcounted_ptr_construct_variant section prevents LLVM folding
      this separately hookable export into the canonical template instance.

  - address: 0x0839f1e0
    ghidra: FUN_0839f1e0
    name: refcounted_ptr_construct_secondary_variant
    size: 104
    sig: "RefcountedBody **refcounted_ptr_construct_secondary_variant(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: slot
      param_2/r1/r6: implementation
      param_3/r2/r8: want_mutex
      piVar1/r4: body
      puVar2: mutex
    notes: >-
      Raw ARM establishes the exact 104-byte body 0x0839f1e0..0x0839f248,
      where a separately linked sibling begins. Decoding every ARM B/BL word
      in osos.dec finds exactly 10 direct BL callers, all unconditional:
      0x0810ce24, 0x0810d040, 0x0814aeb8, 0x0814b29c, 0x081cc6e0,
      0x081cc784, 0x081f0c6c, 0x081f0dac, 0x081fcb88, and 0x081fcbb8.
      There are no tail B sites, predicated forms, or image words equal to
      this address, so it is not virtually dispatched. This C++ template
      instantiation is byte-identical to refcounted_ptr_construct @
      0x0839ed38 and refcounted_ptr_construct_variant @ 0x0839f148 after
      normalizing the three direct BL displacements. It clears slot first;
      with a non-NULL implementation, tag-2 operator_new(12) creates
      RefcountedBody { implementation, 1, NULL }; want_mutex allocates and
      zeroes tag-2 operator_new(8), stores it at body+8, then calls
      mutex_create before publishing body. Returns slot. The dedicated target
      section prevents LLVM from folding this separately hookable template
      copy into its byte-identical siblings; host tests cover null,
      unguarded, and mutex-backed construction.

  - address: 0x0839f1b0
    ghidra: FUN_0839f1b0
    name: refcounted_ptr_assign_owned
    size: 48
    sig: "RefcountedBody **refcounted_ptr_assign_owned(RefcountedBody **dst, RefcountedBody **src)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: dst
      param_2/r1/r4: src
    notes: >-
      Raw decoding establishes the exact 48-byte body
      (0x0839f1b0..0x0839f1e0), followed by a separately linked sibling.
      Decoding every ARM B/BL word in osos.dec finds 13 direct bl callers,
      all unconditional (0x0822398c, 0x08223a28, and
      0x08223df0..0x08223f58 at 0x24-byte strides): no b sites, no
      predicated forms, and no image word equals the address, so it is never
      virtually dispatched. It compares SLOT pointers and, when distinct,
      calls owning release 0x0839d2c0 before loading *src and attaching it
      through 0x0839d284, returning dst. The attach helper is byte-identical
      to ported refcounted_body_attach @ 0x0839d370 after normalizing its two
      branch displacements; that shared port preserves the unconditional
      store, optional-mutex refcount bump, and wrapping add. The owning
      release port preserves the old body's final disposal/deletion, so the
      Rust sequence has the source load after release like the ARM.

  - address: 0x0839ed38
    ghidra: FUN_0839ed38
    name: refcounted_ptr_construct
    size: 104
    sig: "RefcountedBody **refcounted_ptr_construct(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot, param_2: implementation, param_3: want_mutex, piVar1: body, puVar2: mutex}
    notes: >-
      30 bl call sites, all unconditional (verified by decoding every
      branch word in osos.dec): no b sites, no predicated forms, and the
      address appears in no data word - never virtually dispatched.
      Ghidra's 104-byte extent is exact this time: refcounted_ptr_assign
      @ 0x0839eda0 starts immediately after. Birth counterpart of
      refcounted_body_release @ 0x0839cd98: NULLs the slot first, and
      when the implementation word is non-NULL wraps it in a fresh tag-2
      operator_new(12) RefcountedBody { +0 impl, +4 refcount = 1, +8
      mutex = NULL }, optionally adding a tag-2 operator_new(8) Mutex
      (both words zeroed, then mutex_create @ 0x080744a4 fills the
      semaphore cell) at body+8; stores the body into the slot last and
      returns the slot (construct-and-return-this, mirroring
      refcounted_ptr_assign). Both callees were already ported
      (heap/veneers operator_new, kernel/sync_mutex mutex_create).
      Byte-identical siblings (modulo bl displacements to the same two
      callees) sit at 0x0839eca0 and 0x0839ee14 - per-type template
      instantiations, unported. Codegen deviation: LLVM inlines the
      ported mutex_create (ROM_KERNEL dispatch included), so the ARM
      body is larger but keeps the same alloc/init/store sequence -
      match.py structural confirms. Host tests back the 12/8-byte
      allocations with oversized arenas, since the host structs are
      wider than the 32-bit target layout.

  - address: 0x0839f28c
    ghidra: FUN_0839f28c
    name: refcounted_ptr_copy_assign
    size: 48
    sig: "RefcountedBody **refcounted_ptr_copy_assign(RefcountedBody **dst, RefcountedBody **src)"
    module: cxx/handle
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      14 bl call sites, all unconditional (verified by decoding every
      ARM B/BL word in osos.dec): no b sites, no predicated forms, and
      the address appears in no data word - never virtually dispatched.
      Ghidra's 48-byte extent is exact: a separately linked function
      begins at 0x0839f2bc. The C++ copy-assignment operator of a
      refcounted handle slot: if dst != src, release the old body
      through refcounted_body_release_dtor_variant, then attach *src
      through refcounted_body_attach; return dst. Both callees are
      ported (the slot-1 teardown @ 0x0839d3ac predates this port; the
      attach helper @ 0x0839d370 was ported alongside). The guard
      compares SLOT POINTERS, not bodies: two distinct slots holding
      the same body with refcount 1 destroy the body in the release and
      then re-attach the dangling pointer, bumping the freed body's
      refcount back to 1 - the aliasing hazard is reproduced faithfully
      and pinned by a host test. *src is loaded AFTER the release
      (ldr r1, [r4] follows the release bl), matching the original's
      ordering. Returns dst unconditionally, self-assign included. A
      byte-similar sibling copy-assign @ 0x0839f324 (6 bl sites) pairs
      two different helpers (release 0x0839d498, attach 0x0839d45c,
      both unported) and remains unported. Codegen: both calls remain
      real bls to the inline(never) exports; match.py shows the same
      guard/release/ldr/attach/return-dst structure (plus an LLVM
      frame-pointer prologue). The symbol resolves in librustypod.a.

  - address: 0x0839f2bc
    ghidra: FUN_0839f2bc
    name: refcounted_ptr_construct_tertiary_variant
    size: 104
    sig: "RefcountedBody **refcounted_ptr_construct_tertiary_variant(RefcountedBody **slot, usize implementation, u32 want_mutex)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0/r5: slot
      param_2/r1/r6: implementation
      param_3/r2/r8: want_mutex
      r4: body
      r7: null
    notes: >-
      Raw ARM establishes the exact 104-byte extent
      (0x0839f2bc..0x0839f324), with refcounted_ptr_copy_assign beginning
      immediately afterward. A full decode of every B/BL word in osos.dec
      finds nine direct BL callers, all unconditional: 0x08113eb8,
      0x08113edc, 0x0811710c, 0x0811711c, 0x081172b0, 0x081172d4,
      0x08211488, 0x082117d0, and 0x08211d7c; no tail B sites, predicated
      calls, or image data-word references exist, so this template
      instantiation is never virtually dispatched. It clears slot, returns
      it unchanged for a NULL implementation, otherwise calls ported
      operator_new(12) to construct RefcountedBody { implementation,
      refcount = 1, mutex = NULL }; nonzero want_mutex additionally calls
      operator_new(8), zeroes both Mutex words, stores it at body+8, calls
      ported mutex_create, and publishes the finished body last. The Rust
      port has a dedicated target section to prevent identical-code folding;
      LLVM may inline mutex_create rather than retain the original BL. Host
      tests cover NULL, unguarded, and mutex-backed construction.
  - address: 0x0816cd44
    ghidra: FUN_0816cd44
    name: refcounted_ptr_release
    size: 20
    sig: "RefcountedBody **refcounted_ptr_release(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    vars: {param_1: slot}
    notes: >-
      90 bl call sites (mostly the 0x0822xxxx application layer releasing
      stack- and member-slot handles). The drop counterpart of
      refcounted_ptr_assign @ 0x0839eda0 calls the ported
      refcounted_body_release @ 0x0839cd98 and returns its slot unchanged
      (the ADS destroy-and-return-this idiom). The teardown NULLs the slot
      on every non-NULL-body path. Filed under cxx/handle, not src/sqlite:
      0x0839cd98 sits above the SQLite span and is the same RefcountedBody
      family as the assign. match.py structural: same push/mov-r4/call/
      return-this frame; LLVM may inline the teardown helpers.

  # --- Embedded SQLite, 0x08360000-0x0838ffff (agent media) ---
  # The whole span is an amalgamated SQLite ~3.5.x - see src/sqlite/mod.rs
  # for the identification evidence and the recovered struct layouts
  # (sqlite3, Vdbe, VdbeOp, Btree, Parse, Token). Two non-SQLite islands
  # sit inside it: the ATA/CE-ATA driver block ~0x08369000-0x0836c000
  # (where the already-ported gpio_pin_configure @ 0x0836b5b0 lives) and
  # FreeType/UI fragments above 0x0838f000. The earlier survey's
  # "RTXC-mutex-guarded refcount setters" do NOT occur in this range -
  # the only refcount machinery found is 0x0837ee98 (a plain u16
  # decrement, no mutex); the mutex-guarded setter it meant is
  # 0x0839eda0, above the range.
  # Call-site counts below come from decoding every bl/b word in
  # osos.dec, not from osos.asm.

  # ---- SQLite string accumulator finish (src/sqlite/str_accum.rs) ----
  - address: 0x08384e14
    ghidra: FUN_08384e14
    name: str_accum_finish
    size: 112
    sig: "char *str_accum_finish(StrAccum *accum)"
    module: sqlite/str_accum
    status: ported
    vars: {param_1: accum, iVar2: z_text, cVar1: use_malloc}
    notes: >-
      SQLite's sqlite3StrAccumFinish. When zText is non-NULL it first writes
      its terminator at zText[nChar]. If useMalloc is set and zText still
      equals zBase, it requests nChar + 1 bytes through sqlite3_malloc
      (0x08390b14), installs the result in zText, sets mallocFailed on NULL,
      or copies zBase including its terminator on success. Returns zText.

  # ---- SQLite string accumulator (src/sqlite/vdbe_op.rs) ----
  - address: 0x08384e84
    ghidra: FUN_08384e84
    name: str_accum_init
    size: 44
    sig: "void str_accum_init(StrAccum *accum, char *base, int initial_capacity, int max_capacity)"
    module: sqlite/vdbe_op
    status: ported
    vars: {param_1: accum, param_2: base, param_3: initial_capacity, param_4: max_capacity}
    notes: >-
      SQLite's sqlite3StrAccumInit. Initializes the 23-byte recovered
      prefix of StrAccum: zBase and zText at +0/+4 both take base, nChar
      at +8 is zero, nAlloc and mxAlloc at +0xc/+0x10 retain the supplied
      capacities, and mallocFailed/useMalloc/tooBig at +0x14/+0x15/+0x16
      become 0/1/0. Leaf 44-byte store sequence; no validation or base
      buffer access.

  # ---- SQLite string accumulator reset (src/sqlite/str_accum.rs) ----
  - address: 0x08384eb0
    ghidra: FUN_08384eb0
    name: str_accum_reset
    size: 40
    sig: "void str_accum_reset(StrAccum *accum)"
    module: sqlite/str_accum
    status: ported
    vars: {param_1: accum}
    notes: >-
      SQLite's sqlite3StrAccumReset. If zText (+0x4) equals zBase (+0x0),
      it returns immediately and preserves every field. Otherwise it passes
      zText to sqlite3_free at 0x083906f4 (the ported
      heap::tracked::tracked_free) and then writes NULL to zText.
      The allocator call and post-call NULL store are verified against the
      40-byte disassembly.

  # ---- VDBE table-affinity materialization (src/sqlite/table_affinity.rs) ----
  - address: 0x08385004
    ghidra: FUN_08385004
    name: vdbe_attach_table_affinity
    size: 128
    sig: "void vdbe_attach_table_affinity(Vdbe *vdbe, Table *table)"
    module: sqlite/table_affinity
    status: ported
    vars: {param_1: vdbe, param_2: table, iVar1: affinity_cache, iVar3: column_index, uVar2: p4_copy}
    notes: >-
      SQLite's 3.6-era table-affinity helper. If table->zColAff (+0x28) is
      NULL, allocates table->nCol + 1 through sqlite3DbMallocRaw
      (0x08374960), copies every Column.affinity (+0x12, 20-byte Column
      stride), terminates the string, and caches it. It tail-calls the ported
      vdbe_change_p4 (0x08386aa4) with that cache, addr -1 and n 0; that seam
      applies the Vdbe/op/malloc-failure guards, frees and clears the former
      P4 through freeP4 (0x082cf388), duplicates the string through
      sqlite3DbStrNDup (0x08374a40), and tags it P4_DYNAMIC (-1). An
      affinity-cache allocation failure returns before modifying the op; a
      P4-copy allocation failure retains the dynamic tag with NULL P4.


  # ---- VDBE table-lock collection (src/sqlite/table_lock.rs) ----
  - address: 0x08385084
    ghidra: FUN_08385084
    name: vdbe_add_table_lock
    size: 176
    sig: "void vdbe_add_table_lock(Parse *parse, int database_index, int table_index, int is_write_lock, const char *table_name)"
    module: sqlite/table_lock
    status: ported
    vars: {param_1: parse, param_2: database_index, param_3: table_index, param_4: is_write_lock, param_5: table_name, iVar1: index, piVar2: lock, iVar3: count}
    notes: >-
      SQLite's sqlite3TableLock (the first argument is the Parse context,
      not a Vdbe: +0x00 db, +0x13c nTableLock, +0x140 aTableLock). Negative
      database indexes are ignored - the code generator's "no database"
      sentinel - and there is no shared-cache feature test, so the
      bookkeeping otherwise runs unconditionally. The existing 16-byte
      TableLock array (iDb +0, iTab +4, isWriteLock +8, zName +0xc) is
      scanned for the first matching (iDb, iTab) pair, whose write byte
      becomes the boolean OR of its old value and isWriteLock - promotion
      only, never demotion. A new pair reallocates through the ported
      sqlite3DbReallocOrFree (0x083749f4) to (nTableLock + 1) * 16 bytes,
      stores all four fields (zName is borrowed, not copied) and increments
      nTableLock. Note the asymmetry the port preserves: the fold path
      normalizes the flag to 0/1 while the append path `strb`s the low byte
      of isWriteLock verbatim. On allocation failure the old block is
      already freed, so it clears both aTableLock and nTableLock and
      explicitly sets db->mallocFailed (+0x1e) even though
      sqlite3DbRealloc has set it too.
  # ---- VDBE code builder (src/sqlite/vdbe.rs) ----
  - address: 0x083867f8
    ghidra: FUN_083867f8
    name: vdbe_add_op0
    size: 24
    sig: "int vdbe_add_op0(Vdbe *p, int opcode)"
    module: sqlite/vdbe
    status: ported
    vars: {param_1: p, param_2: opcode}
    notes: >-
      SQLite's sqlite3VdbeAddOp0: a 24-byte wrapper around vdbe_add_op3
      @ 0x08386840 that passes zero for p1, p2, and p3. Raw ARM decoding
      verifies 13 BL call sites: 8 unconditional, 3 BLNE, 1 BLEQ, and 1
      BLHI; the wrapper itself has no NULL guard, so predicated callers
      gate the call. Port preserves that direct delegation exactly.

  - address: 0x08386824
    ghidra: FUN_08386824
    name: vdbe_add_op2
    size: 28
    sig: "int vdbe_add_op2(Vdbe *p, int opcode, int p1, int p2)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeAddOp2. 126 bl + 1 tail b call sites. Pure
      front-end for vdbe_add_op3 @ 0x08386840 - preserves the incoming
      r3 (p2) through ip and zeroes the 5th (stack) argument p3.

  - address: 0x08386810
    ghidra: FUN_08386810
    name: vdbe_add_op1
    size: 20
    sig: "int vdbe_add_op1(Vdbe *p, int opcode, int p1)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeAddOp1. 56 bl + 1 tail b call sites. Front-end
      for vdbe_add_op3 with p2 = p3 = 0.

  - address: 0x083868c8
    ghidra: FUN_083868c8
    name: vdbe_add_op4
    size: 68
    sig: "int vdbe_add_op4(Vdbe *p, int opcode, int p1, int p2, int p3, const char *p4, int p4type)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeAddOp4. 76 bl call sites. Calls vdbe_add_op3
      with the first five arguments, then vdbe_change_p4
      @ 0x08386aa4 (now ported) with (p, returned address, p4, p4type),
      and returns that address.

  - address: 0x08367f88
    ghidra: FUN_08367f88
    name: vdbe_resize_op_array
    size: 48
    sig: "void vdbe_resize_op_array(Vdbe *p, int n_op)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's resizeOpArray. 2 bl call sites (both in vdbe_add_op3).
      `sqlite3DbRealloc(p->db, p->aOp, 20 * n_op)`; on success writes
      nOpAlloc (+0x10) and aOp (+0x14) with a base-writeback store pair.
      On failure it leaves both alone and the caller learns of it through
      db->mallocFailed. The `mov r0,#20; mul r2,r0,r1` is the literal
      sizeof(VdbeOp) that pins the 20-byte record size.

  - address: 0x08386a14
    ghidra: FUN_08386a14
    name: vdbe_change_p1
    size: 48
    sig: "void vdbe_change_p1(Vdbe *p, int addr, int value)"
    module: sqlite/vdbe
    status: ported
    renames:
      param_1: p
      param_2: addr
      param_3: value
    notes: >-
      SQLite's sqlite3VdbeChangeP1 - patches the first operand of the op
      at addr. 14 bl call sites (verified by decoding every B/BL word in
      osos.dec), none predicated. Byte-identical guard/store skeleton to
      vdbe_change_p2 @ 0x08386a44 - the two originals differ only in the
      store offset (+4 vs +8): four guards in order (NULL p via
      cmp/bxeq, addr < 0 and nOp <= addr via the ldrge/cmpge/bxle
      predicated chain, NULL aOp via predicated addne/strne), then
      aOp[addr].p1 = value at stride 20 (`add r1,r1,r1 lsl#2` then
      `lsl#2`). Ghidra's 48-byte extent is exactly right; the next
      function starts at 0x08386a44. match.py structural: LLVM emits
      branches instead of predication and keeps a frame, guards and
      store identical.

  - address: 0x08386a44
    ghidra: FUN_08386a44
    name: vdbe_change_p2
    size: 48
    sig: "void vdbe_change_p2(Vdbe *p, int addr, int value)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeChangeP2 - back-patches a jump target. 66 bl +
      2 tail b call sites. Four guards in order - NULL p, addr < 0,
      p->nOp <= addr, NULL p->aOp - then `p->aOp[addr].p2 = value`
      (`add r1,r1,r1 lsl#2` then `lsl#2` = stride 20, field at +8).

  - address: 0x0838b674
    ghidra: FUN_0838b674
    name: vdbe_jump_here
    size: 8
    sig: "void vdbe_jump_here(Vdbe *p, int addr)"
    module: sqlite/vdbe
    status: ported
    renames:
      param_1: p
      param_2: addr
    notes: >-
      SQLite's sqlite3VdbeJumpHere. 8 unconditional bl call sites and one
      conditional tail bne (verified by decoding every ARM B/BL word in
      osos.dec); no predicated BL call sites. The entire 8-byte function is
      `ldr r2,[r0,#0xc]` then `b 0x08386a44`: it snapshots p->nOp and
      tail-calls vdbe_change_p2(p, addr, nOp), thereby making the existing
      opcode at addr jump to the next emitted opcode. The original dereferences
      p before vdbe_change_p2 can guard it, so a NULL p is invalid.

  - address: 0x08386bd4
    ghidra: FUN_08386bd4
    name: vdbe_change_p5
    size: 32
    sig: "void vdbe_change_p5(Vdbe *p, u8 value)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeChangeP5 - sets P5 on the op just emitted.
      19 bl + 1 tail b call sites. Guards NULL p and NULL p->aOp, then
      stores at `aOp + nOp*20 - 17`, i.e. `&aOp[nOp-1].p5`. nOp is NOT
      checked (the original's assert(nOp>0) is compiled out); this store
      offset is what pins both the 20-byte stride and p5 at +3.

  - address: 0x0838b8fc
    ghidra: FUN_0838b8fc
    name: vdbe_make_label
    size: 88
    sig: "int vdbe_make_label(Vdbe *p)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeMakeLabel. 30 bl call sites. Returns the
      negative encoding `-1 - index` of a fresh forward-reference slot.
      nLabel (+0x18) is bumped unconditionally BEFORE the capacity check,
      so a failed growth still consumes the index. Growth is
      `nLabelAlloc*2 + 10` (`add r0,#10,r0 lsl#1`) through
      sqlite3DbReallocOrFree @ 0x083749f4 on 4-byte entries; the new slot
      is seeded with -1 only when the table exists. nLabelAlloc +0x1c,
      aLabel +0x20.

  - address: 0x0838cc04
    ghidra: FUN_0838cc04
    name: vdbe_resolve_label
    size: 24
    sig: "void vdbe_resolve_label(Vdbe *p, int x)"
    module: sqlite/vdbe
    status: ported
    notes: >-
      SQLite's sqlite3VdbeResolveLabel. 32 bl call sites.
      `if (p->aLabel) p->aLabel[-1-x] = p->nOp` (`mvn r2,r1` is the
      -1-x). p itself is not NULL-checked. Independently confirms nOp at
      +0x0c and aLabel at +0x20.

  - address: 0x08386aa4
    ghidra: FUN_08386aa4
    name: vdbe_change_p4
    size: 304
    sig: "void vdbe_change_p4(Vdbe *p, int addr, const char *value, int n)"
    module: sqlite/vdbe
    status: ported
    renames:
      param_1: p
      param_2: addr
      param_3: value
      param_4: n
    notes: >-
      SQLite's sqlite3VdbeChangeP4 - attaches the fourth operand to an
      op, copying/owning the value according to the P4_* tag in n.
      24 bl + 2 tail b call sites. This build's tag numbering is NOT
      upstream's (its KEYINFO is -6, not -5; freeP4's jump table spans
      -13..=0): -14 stores verbatim and keeps the tag even for NULL
      (checked first), -6 deep-copies a KeyInfo (nField word at value+8,
      5*nField+0x10 bytes, sqlite3_malloc + __rt_memcpy; OOM sets
      db+0x1e and tags NOTUSED), -9 stores verbatim but retags -6
      (upstream's P4_KEYINFO_HANDOFF behavior - both branches share the
      original's `strb r9`, r9 = -6), other negative n stores verbatim
      with tag truncated to a byte, n==0 measures strlen first, n>0
      duplicates via db_str_ndup and tags -1. Guards, in order: no aOp
      or db->mallocFailed drops the INCOMING payload through freeP4
      (unless it is a KeyInfo - the caller keeps that); a still-negative
      addr after nOp-1 is a silent no-op. freeP4 @ 0x082cf388 is not
      ported - it is the VDBE_P4_OPS dispatch slot (default: documented
      no-op that leaks rather than corrupts). strlen, __rt_memcpy and
      db_str_ndup are called through their ported twins, malloc through
      DB_MEM_OPS. match.py structural (slot blx vs direct bl, LLVM
      inlines the strlen, store order swapped in the n<0 branch).

  - address: 0x082cf358
    ghidra: FUN_082cf358
    name: free_ephemeral_function
    size: 20
    sig: "void free_ephemeral_function(void *funcdef)"
    module: sqlite/ephemeral_fn
    status: ported
    renames:
      param_1: funcdef
    notes: >-
      Upstream SQLite's freeEphemeralFunction, minus the db argument
      (this build's allocators are global). 1 bl + 1 tail-b call site,
      both inside vdbe_free_p4 @ 0x082cf388: the -7 branch calls it on
      the context's first word, the -5 branch tail-calls it on the
      payload. NULL returns immediately (`cmp r0,#0`); otherwise the
      FuncDef flags byte at +4 is loaded (`ldrbne r1,[r0,#0x4]`) and
      tested against the ephemeral bit 0x4 (`tstne r1,#0x4`, upstream's
      SQLITE_FUNC_EPHEM); only an ephemeral definition is freed via a
      conditional tail branch (`bne`) into sqlite3_free @ 0x083906f4,
      here its ported twin tracked_free. A non-ephemeral definition is
      borrowed from the function registry and left untouched. Now the
      default free_ephemeral_function slot of free_p4's
      FREE_P4_AUX_OPS table. match.py structural: the 3-instruction
      guard/test prologue matches exactly, then LLVM inlines
      tracked_free's body and tail-calls free_tag57 instead of the
      original's `bne sqlite3_free`. Ghidra's C merges this function
      with the following (unrelated) body and invents a second
      parameter.

  - address: 0x082cf388
    ghidra: FUN_082cf388
    name: vdbe_free_p4
    size: 148
    sig: "void vdbe_free_p4(int p4type, void *p4)"
    module: sqlite/free_p4
    status: ported
    renames:
      param_1: p4type
      param_2: p4
    notes: >-
      SQLite's freeP4 - the tag-dispatch destructor vdbe_change_p4
      (and the op-array teardown) call to release P4 payloads. NULL p4
      returns before the tag is examined (`cmp r1,#0` / `ldmiaeq`).
      Jump table over p4type -13..=0 (`add r0,r0,#0xd; cmp r0,#0xc;
      addls pc`): -13/-12/-11/-9/-6/-1 free the pointer raw,
      -10/-4/-3/-2 return untouched, -8 tail-calls a value destructor
      @ 0x08386504, -7 releases an ephemeral function @ 0x082cf358(*p4),
      sweeps the context's (arg, destructor) record array @
      0x08386e08(p4, 0) - a step the scout notes missed - then frees,
      -5 tail-calls 0x082cf358(p4). Anything outside -13..=0 (e.g. -14)
      returns unfreed. The raw free is sqlite3_free @ 0x083906f4,
      called through its ported twin tracked_free; the value
      destructor @ 0x08386504 has since been ported (sqlite/value_free)
      and is the default value_free slot, alongside the ephemeral
      release @ 0x082cf358 and the aux sweep @ 0x08386e08 as the
      default free_ephemeral_function / context_aux_cleanup slots.

  - address: 0x08386504
    ghidra: FUN_08386504
    name: value_free
    size: 32
    sig: "void value_free(void *value)"
    module: sqlite/value_free
    status: ported
    renames:
      param_1: value
    notes: >-
      Upstream SQLite's sqlite3ValueFree - the value destructor
      vdbe_free_p4's -8 (P4_MEM) branch tail-calls; 4 bl + 1 tail-b
      call sites total (the column-name / error-message teardowns
      @ 0x082beac8, 0x0836ed14, 0x083866ac, 0x0838feec call it
      directly). Three steps: NULL returns immediately (movs r4,r0 /
      ldmiaeq); the value's dynamic guts are released by
      sqlite3VdbeMemRelease @ 0x0838c04c (bl - aggregate-context
      finalize on flags bit 0x400 at +0x1c, xDel destructor at +0x20
      on bit 0x40, zMalloc free at +0x24, then z/xDel/zMalloc NULLed);
      the Mem shell itself is freed through the tag-57 tracked
      allocator via the tail branch b sqlite3_free @ 0x083906f4, here
      tracked_free called directly. 0x0838c04c has since been ported
      (sqlite/mem_release) and is the shipped default mem_release slot
      of VALUE_MEM_OPS; its own extern-release dependency @ 0x0838c074
      has since been ported (sqlite/mem_extern_release) and is the
      shipped default of that module's MEM_EXTERN_OPS slot; the extern
      release's aggregate finalize @ 0x0838bc38 has since been ported
      (sqlite/mem_finalize) and is the shipped default of
      MEM_AGG_FINALIZE_OPS, replacing the documented stub (clear the
      MEM_Agg/MEM_Dyn bits, free zMalloc, leak the external string
      rather than run the wrong destructor). Now the default
      value_free slot of free_p4's FREE_P4_AUX_OPS. Ghidra's C inlines
      the whole tag-57 free after the bl - that is the tail-called
      sqlite3_free's body, not this function's 32 bytes.       match.py
      structural: guard matches (cmp/bxeq vs movs/popeq), slot blx for
      the unported release, LLVM inlines tracked_free's stats-update
      prologue and tail-calls free_tag57 instead of b sqlite3_free.

  - address: 0x0838b5c4
    ghidra: FUN_0838b5c4
    name: vdbe_int_value
    size: 128
    sig: "i64 vdbe_int_value(Mem *p_mem)"
    module: sqlite/vdbe_int_value
    status: ported
    renames:
      param_1: p_mem
      uVar2: flags
      FUN_082c67f4: f64_to_i64
      FUN_083869f4: sqlite3VdbeChangeEncoding
      FUN_0838bfb0: sqlite3VdbeMemNulTerminate
      FUN_0836f82c: sqlite3Atoi64
    notes: >-
      SQLite 3.5.9's sqlite3VdbeIntValue (vdbemem.c), the signed
      64-bit projection of a Mem. Raw extent is 0x0838b5c4..0x0838b644:
      the 32 instructions end at the return @ 0x0838b640 and the next
      function starts with stmdb @ 0x0838b644; no literal pool. Flags at
      +0x1c are sampled once. MEM_Int (0x4) returns the signed i64 union
      at +0x00; else MEM_Real (0x8) tail-branches to FUN_082c67f4 after
      loading the f64 at +0x08. MEM_Str|MEM_Blob (0x12) adds MEM_Str,
      calls sqlite3VdbeChangeEncoding(pMem, SQLITE_UTF8), calls
      sqlite3VdbeMemNulTerminate, and calls sqlite3Atoi64(z, &value)
      only when both status results are SQLITE_OK; the parser status is
      ignored and all other paths return i64 zero. 13 `bl` call sites,
      all unconditional, binary-scanned from osos.dec: 0x083653f4,
      0x08368280, 0x08387b7c, 0x08387b8c, 0x08387fc8, 0x08387ff8,
      0x08389064, 0x0838a250, 0x0838a380, 0x0838bf58, 0x0838fa80,
      0x08392730, and 0x08392758; two unconditional tail b entries are
      0x08391778 and 0x0839177c. Deviations: unported
      sqlite3VdbeChangeEncoding @ 0x083869f4, sqlite3Atoi64 @
      0x0836f82c, and the identified f64-to-i64 ABI FUN_082c67f4 use a
      volatile local dispatch seam whose target defaults call retailOS
      and whose host tests record calls. The already ported
      vdbe_mem_nul_terminate @ 0x0838bfb0 is called directly rather
      than re-stubbed. Host tests cover integer precedence and signed
      extremes, the REAL conversion ABI including a NaN payload,
      text/blob promotion and ordered recode/terminate/parse calls,
      recode failure, and nonnumeric flags. match.py structural review:
      the original's three direct bl instructions and a tail b become
      volatile slot loads plus blx calls, while the flag-priority and
      short-circuit skeleton is preserved.

  - address: 0x0838bbb4
    ghidra: FUN_0838bbb4
    name: vdbe_mem_expand_blob
    size: 132
    sig: "i32 vdbe_mem_expand_blob(Mem *p_mem)"
    module: sqlite/vdbe_mem_expand_blob
    status: ported
    renames:
      param_1: p_mem
      iVar1: request
      FUN_0838bdb0: sqlite3VdbeMemGrow
      thunk_EXT_FUN_220002d4: iram_memzero_veneer
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeMemExpandBlob (vdbemem.c):
      materialize a MEM_Zero (0x800) cell. Raw extent
      0x0838bbb4..0x0838bc38 is confirmed by the closing
      ldmia sp!, {r4,pc} at 0x0838bc34 and the next function's stmdb
      at 0x0838bc38; no literal pool. The initial flags test returns
      SQLITE_OK untouched when MEM_Zero is clear. Otherwise the low
      word of Mem.u plus n yields a signed request with a one-byte
      minimum, sqlite3VdbeMemGrow @ 0x0838bdb0 runs with preserve = 1,
      nonzero maps to SQLITE_NOMEM (7), and success zeroes u's low-word
      count at z + old n through the ported IRAM memzero veneer
      @ 0x08037dc8, stores wrapped n + u, and clears MEM_Zero|MEM_Term
      (bic #0x820). 11 direct bl call sites decoded from every ARM B/BL
      word in osos.dec: 3 unconditional at 0x08387d58, 0x08389ca0,
      0x08391734; 7 blne at 0x08386748, 0x0838761c, 0x08387650,
      0x08387ec4, 0x08387ed8, 0x083885a8, 0x0838bb44; and 1 blgt at
      0x08386674. The predominant predication confirms callers normally
      gate expansion by MEM_Zero, while the callee retains its own
      unguarded flag check. Deliberate deviation: typed repr(C) Mem
      fields replace raw offsets, preventing 64-bit host pointer overlap;
      its target offsets are asserted in sqlite/vdbe.rs. The pre-existing
      raw-pointer value_text dispatch seam now defaults to this port and
      remains only for its recorder tests.
  - address: 0x0838bdb0
    ghidra: FUN_0838bdb0
    name: vdbe_mem_grow
    size: 232
    sig: "i32 vdbe_mem_grow(Mem *p_mem, i32 size, i32 preserve)"
    module: sqlite/vdbe_mem_grow
    status: ported
    renames:
      param_1: p_mem
      param_2: size
      param_3: preserve
      FUN_0837d374: sqlite3MallocSize
      FUN_083749f4: sqlite3DbReallocOrFree
      FUN_083906f4: sqlite3_free
      FUN_08374960: sqlite3DbMallocRaw
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeMemGrow (vdbemem.c),
      verified line-for-line against the public 3.5.9 source - the
      payload-space guarantee behind every Mem cell's z: nothing
      happens while the owned zMalloc block's tracked size covers the
      request; otherwise the request is floored to 32 bytes (signed
      movle) and the cell is regrown, by db_realloc_or_free
      @ 0x083749f4 in place when preserve && z == zMalloc (a failed
      resize stamps the WHOLE flags halfword MEM_Null (1) via
      strheq), or by sqlite3_free @ 0x083906f4 of the old block
      followed by db_malloc_raw @ 0x08374960 (the free runs BEFORE
      the malloc). A preserve grow with a borrowed payload then
      copies n payload bytes into the owned block (__rt_memcpy
      @ 0x08037db0), a MEM_Dyn payload with a non-NULL xDel is
      destructed with the OLD z (the z = zMalloc store comes later),
      and the common tail stores z = zMalloc, clears
      MEM_Static|MEM_Ephem (bic #0x180) UNCONDITIONALLY, NULLs xDel,
      and returns SQLITE_NOMEM (7) iff zMalloc is NULL. Extent
      0x0838bdb0..0x0838be98 confirmed from raw words (0x0838be94 is
      the closing ldmia; 0x0838be98 is MemHandleBom's stmdb; no
      literal pool). 13 bl call sites, binary-scanned from osos.dec:
      0x082b3960 in FUN_082b38e0; 0x083876d8, 0x08388788 and
      0x08389a40 in the 16 KB vdbe routine FUN_08386ef8; 0x0838b7f8
      and 0x0838b878 in FUN_0838b67c; 0x0838bb70 in
      sqlite3VdbeMemMakeWriteable @ 0x0838bb30; 0x0838bbec in
      sqlite3VdbeMemExpandBlob @ 0x0838bbb4; 0x0838bd40 in
      FUN_0838bcb4; 0x0838bfe4 in the ported vdbe_mem_nul_terminate
      @ 0x0838bfb0 (its ops-slot default is now this port, on target
      and host alike - the seam stays for recording mocks, and its
      test lock is pub(crate) so this module's integration test can
      serialize against its mock-swapping tests); 0x0838c200 in the
      ported vdbe_mem_set_str @ 0x0838c158 (left dispatching through
      its own seam); 0x0838c348 in the ported vdbe_mem_stringify
      @ 0x0838c32c; and 0x0838eed8 in FUN_0838ee8c. Deviations -
      sqlite3MallocSize @ 0x0837d374 (20 bytes, unported) is
      reproduced inline as the private tracked_block_size helper: the
      original's exact two-word walk of the tag-57 tracked header
      (pad at payload-4, size at payload-pad-8), NULL yielding 0, no
      ops slot; the allocator family is called directly through its
      ported twins (sqlite/mem's db_realloc_or_free and
      db_malloc_raw, heap/tracked's tracked_free,
      libc/rt_memcpy's __rt_memcpy). Two firmware deviations from
      upstream 3.5.9, kept as-is and pinned by host tests: the flags
      mask is bic #0x180 applied on success AND failure alike
      (upstream clears MEM_Ephem|MEM_Static|MEM_Dyn on success only),
      so MEM_Dyn (0x40) survives the grow with xDel NULLed, and a
      failed free+malloc grow keeps the type bits instead of becoming
      MEM_Null (only the failed realloc arm stamps MEM_Null).
      match.py structural: same skeleton - the capacity gate with the
      malloc-size walk inlined (ldr [r0,#-4]/sub/ldr [r1,#-8]), the
      cmp/movle #32 floor, the realloc arm with its strheq MEM_Null,
      the free+malloc arm (LLVM inlines tracked_free's stats update),
      the four-way cmpne memcpy condition, the xDel blx with the old
      z, and the unconditional and #0xfe7f/moveq #7 tail. Host tests
      (10) - sufficient capacity is a no-op (recording allocator
      proves no call), the 32-byte floor over requests
      -5..100, the NULL/NULL realloc arm, preserve-realloc in place,
      the borrowed-payload copy, no-preserve free-then-malloc with
      the freed raw pointer checked, NOMEM on both arms (realloc
      stamps MEM_Null, malloc keeps the type bits), the MEM_Dyn
      destructor running once on the OLD z with MEM_Dyn surviving,
      and the vdbe_mem_nul_terminate integration through the real
      grow - in-place and regrown (real tracked headers; the regrow
      half installs a copy-on-move bump realloc because the ported
      sqlite3_realloc finds the old payload INSIDE the new raw
      block, which the veneers recording realloc does not model).

  - address: 0x0838ee8c
    ghidra: FUN_0838ee8c
    name: aggregate_context
    size: 120
    sig: "*mut u8 aggregate_context(SqliteContext *context, i32 n_byte)"
    module: sqlite/aggregate_context
    status: ported
    renames:
      param_1: context
      param_2: n_byte
      puVar1: p_mem
      FUN_0838c074: vdbeMemClearExternAndSetNull
      FUN_0838bdb0: sqlite3VdbeMemGrow
      thunk_EXT_FUN_220002d4: iram_memzero_veneer
    notes: >-
      Upstream SQLite 3.5.9's sqlite3_aggregate_context. Raw extent
      0x0838ee8c..0x0838ef04 is exact: the closing ldmia is at
      0x0838ef00 and 0x0838ef04 begins the next leaf accessor; no literal
      pool. The context's pMem sits at +0x30. A pre-existing MEM_Agg
      (0x400) returns pMem->z untouched. A zero first request calls
      vdbeMemClearExternAndSetNull @ 0x0838c074, assigns flags = MEM_Null
      (1), and NULLs z. A nonzero request calls sqlite3VdbeMemGrow
      @ 0x0838bdb0 with preserve = 0; irrespective of grow status it
      assigns flags = MEM_Agg, stores context.pFunc into the low 32-bit
      word of Mem.u, then, only with a non-NULL z, clears exactly nByte
      bytes through the ported IRAM memzero veneer @ 0x08037dc8. It returns
      z on every path. 10 direct bl call sites, decoded from every ARM
      B/BL word in osos.dec: 0x082b5a64, 0x082c5014, 0x082c5044,
      0x082d2660, 0x082d26dc, 0x082d9700, 0x082d97d0, 0x083928d4,
      0x08392958, and 0x083943dc; all are unconditional, confirming that
      the callee's MEM_Agg test owns one-time initialization. Deviation:
      the recovered repr(C) SqliteContext uses named fields so host pointers
      do not overlap; the low Mem.u word is explicitly written, preserving
      its upper word as the stock str does. The target directly calls the
      ported extern-release, grow, and memzero functions without a dispatch
      seam; its raw-layout extern-release has an exactly equivalent typed
      host-only MEM_Dyn/xDel path. Host tests cover the pre-existing
      aggregate no-op, dynamic external release plus the zero-sized NULL
      transition, and a real sufficient-capacity grow that proves the
      MEM_Agg stamp, low-word FuncDef store, exact zero span, and untouched
      tail.

  - address: 0x0838bf50
    ghidra: FUN_0838bf50
    name: vdbe_mem_integerify
    size: 40
    sig: "i32 vdbe_mem_integerify(Mem *p_mem)"
    module: sqlite/vdbe_mem_integerify
    status: ported
    renames:
      param_1: p_mem
      uVar1: integer_value
      FUN_0838b5c4: vdbe_int_value
    notes: >-
      SQLite 3.5.9's sqlite3VdbeMemIntegerify (vdbemem.c). Raw extent is
      0x0838bf50..0x0838bf78: the closing pop {r4,pc} is at 0x0838bf74
      and the following word is the separate sqlite3VdbeMemSetNull entry;
      no literal pool. It calls the already ported sqlite3VdbeIntValue at
      0x0838b5c4, writes its signed i64 result to Mem.u (+0x00), then
      reloads flags at +0x1c and replaces only the five type bits
      (0x1f) with MEM_Int (0x4), returning SQLITE_OK (0). The 10 direct
      bl callers were decoded from every B/BL word in osos.dec:
      0x08387bf0, 0x08387ddc, 0x0838805c, 0x08388090, 0x08388ab8,
      0x08388c38, 0x0838927c, 0x0838952c, 0x0838a570, 0x0838a578.
      Every entry is unconditional; no predicated calls or tail branches.
      Deviation: named repr(C) Mem fields replace the raw offsets so host
      pointers stay disjoint; the shipped vdbe_int_value port is called
      directly rather than re-stubbing it. Host tests cover signed i64
      boundary values, every reachable old type-bit pattern, preserved
      attribute bits, return value, and untouched unrelated fields.
      match.py structural: LLVM adds a frame, but retains the helper call,
      strd result store, flags reload, flag stamp, and zero return; its
      orr #4 / bic #27 pair is equivalent to bic #31 / orr #4.

  - address: 0x0838bfb0
    ghidra: FUN_0838bfb0
    name: vdbe_mem_nul_terminate
    size: 116
    sig: "i32 vdbe_mem_nul_terminate(Mem *p_mem)"
    module: sqlite/vdbe_mem_nul_terminate
    status: ported
    renames:
      param_1: p_mem
      iVar1: grow_rc
      FUN_0838bdb0: sqlite3VdbeMemGrow
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeMemNulTerminate (vdbemem.c),
      verified line-for-line against the public 3.5.9 source - the
      double-NUL trailer guarantee for string Mem cells, immediately
      before vdbe_mem_realify @ 0x0838c024 in the vdbe Mem helper
      cluster. Extent 0x0838bfb0..0x0838c024 confirmed from raw words
      (0x0838c020 is the closing b 0x0838bfcc; 0x0838c024 is the next
      function's stmdb; no literal pool). Algorithm: flags at +0x1c is
      sampled once; MEM_Term (0x20) set or MEM_Str (0x2) clear
      short-circuits to SQLITE_OK with the cell untouched
      (upstream's `(flags & MEM_Term)!=0 || (flags & MEM_Str)==0`, in
      the disassembly's test order); otherwise sqlite3VdbeMemGrow
      @ 0x0838bdb0 guarantees n+2 bytes with preserve=1, a failed grow
      returns SQLITE_NOMEM (7) untouched, and on success the RELOADED
      z/n gain NULs at z[n] and z[n+1] (two strbs) and the reloaded
      flags gain MEM_Term by read-modify-write. 5 bl call sites,
      binary-scanned from osos.dec: 0x082b4710 in FUN_082b46f8 (the
      integer-coercion helper - nul-terminate, Atoi64-style parse @
      0x0837cb60, then vdbe_mem_realify on the failure arm),
      0x08386788 in sqlite3ValueText @ 0x08386718 (ported:
      sqlite/value_text), 0x083875b0 in the 16 KB vdbe routine
      FUN_08386ef8, 0x0838b618 in the sqlite3VdbeIntValue-style
      extractor FUN_0838b5c4, and 0x0838c84c in the ported
      sqlite/vdbe_real_value @ 0x0838c7ec. Deviations - the grow
      callee @ 0x0838bdb0 has since been ported
      (sqlite/vdbe_mem_grow) and is the shipped default of the
      module's VDBE_MEM_NUL_TERMINATE_OPS seam on target and host
      alike (the ABI type is still reused from
      sqlite/vdbe_mem_set_str, which owns it); host tests install a
      recording mock, and the test lock is pub(crate) so
      vdbe_mem_grow's integration test can serialize against the
      mock-swapping tests. vdbe_real_value's own nul_terminate slot
      can now default to this port but is left untouched here. The port
      goes through the typed repr(C) Mem (offsets asserted on 32-bit
      targets in sqlite/vdbe.rs). match.py structural: 29 vs 29
      instructions, same skeleton - single flags ldrh, the guard (LLVM
      folds the two tsts into and #0x22/cmp #2, both arms returning
      SQLITE_OK; proven equivalent by the exhaustive-flags test), blx
      through the ops slot for the original's bl 0x0838bdb0, the two
      strb zeros (LLVM uses writeback addressing for z[n]), the
      reloaded orr #0x20/strh, and LLVM's fp frame.

  - address: 0x0838c024
    ghidra: FUN_0838c024
    name: vdbe_mem_realify
    size: 40
    sig: "i32 vdbe_mem_realify(Mem *p_mem)"
    module: sqlite/vdbe_mem_realify
    status: ported
    vars: {param_1: p_mem}
    notes: >-
      Upstream SQLite's sqlite3VdbeMemRealify (vdbemem.c) - the in-place
      numeric-to-REAL helper immediately before mem_release @ 0x0838c04c.
      It calls sqlite3VdbeRealValue @ 0x0838c7ec to compute p_mem's value
      as double, stores the returned f64 into the REAL arm at +0x08, then
      read-modify-writes flags at +0x1c to (flags & !0x1f) | MEM_Real,
      preserving every attribute/ownership bit above the five type bits
      (`MEM_Null`..`MEM_Blob`). Unlike vdbe_mem_set_double @ 0x0838c0c4 it
      does not release anything and does not stamp value_type at +0x1e; it
      simply returns SQLITE_OK (0). 2 bl call sites, binary-scanned from
      osos.dec: @ 0x082b4784 inside FUN_082b46f8 and @ 0x08387e20 inside
      FUN_08386ef8. The callee sqlite3VdbeRealValue @ 0x0838c7ec is not
      ported yet, so the Rust module keeps it behind a
      VDBE_MEM_REALIFY_OPS seam: target builds call the retailOS address
      directly and host tests install a recording helper. match.py
      structural: the helper call becomes slot dispatch (literal load +
      blx) and LLVM adds its fp frame; the flag stamp still lands as one
      ldrh/orr/bic/strh group, with `bic #23` after `orr #8` standing in
      for the original `(flags & !0x1f) | 8` mask.
  - address: 0x0838c04c
    ghidra: FUN_0838c04c
    name: mem_release
    size: 40
    sig: "void mem_release(void *value)"
    module: sqlite/mem_release
    status: ported
    renames:
      param_1: value
    notes: >-
      Upstream SQLite's sqlite3VdbeMemRelease - the value guts release
      value_free @ 0x08386504 calls before freeing the Mem shell (~20
      bl call sites across the VDBE column/row teardowns plus one tail
      b @ 0x082d9728; the aggregate branch of the extern release @
      0x0838c074 tail-calls back into it). Three steps: the extern
      release @ 0x0838c074 runs first (bl - finalize an aggregate
      context on flags bit 0x400 at +0x1c, invoke the xDel destructor
      at +0x20 on the string at +0x14 on bit 0x40, clear xDel); zMalloc
      at +0x24 is freed raw (ldr r0,[r4,#0x24]; bl sqlite3_free @
      0x083906f4, here tracked_free - its NULL guard stands in for the
      original's, which also runs unconditionally); then z +0x14,
      zMalloc +0x24 and xDel +0x20 are NULLed in that order so a
      re-released Mem is inert. The extern release @ 0x0838c074 has
      since been ported (sqlite/mem_extern_release) and is the shipped
      default of the MEM_EXTERN_OPS slot (the slot is kept so host
      tests can intercept it); its aggregate finalize @ 0x0838bc38 has
      since been ported (sqlite/mem_finalize) and is the shipped
      default of that module's MEM_AGG_FINALIZE_OPS slot, replacing
      the documented stub (clear the MEM_Agg/MEM_Dyn bits, free
      zMalloc, leak the external string rather than run the wrong
      destructor). Shipped as
      the default mem_release slot of value_free's VALUE_MEM_OPS.
      match.py structural: slot blx for the extern release,
      LLVM inlines tracked_free's stats-update prologue and NULL guard
      and reorders the +0x20/+0x24 NULL stores; the three str zeros and
      the zMalloc load/free structure match.

  - address: 0x0838c074
    ghidra: FUN_0838c074
    name: mem_extern_release
    size: 80
    sig: "void mem_extern_release(void *value)"
    module: sqlite/mem_extern_release
    status: ported
    renames:
      param_1: value
      param_2: x_del
    notes: >-
      Upstream SQLite's vdbeMemClearExternAndSetNull (this build's
      variant checks the aggregate flag first and tail-calls
      mem_release) - the extern release mem_release @ 0x0838c04c runs
      first; 6 bl call sites (mem_release @ 0x0838c054, the row/column
      teardowns @ 0x083675fc, 0x08386fbc, 0x0838c2e0, 0x0838eeb4 and
      the make-owned copy helper @ 0x0838bae0). The flags halfword at
      +0x1c is loaded once (ldrh r0,[r0,#0x1c]): bit 0x400 (MEM_Agg)
      finalizes the aggregate context - the FuncDef pointer at +0x00
      (upstream Mem.u.pDef) goes to the finalize helper @ 0x0838bc38
      (ldr r1,[r4,#0x0]; bl - upstream sqlite3VdbeMemFinalize, which
      invokes the user xFinalize, frees the accumulation buffer and
      copies the 0x28-byte finalized result over the Mem, landing
      flags = 1) - and the function tail-calls mem_release @ 0x0838c04c
      (ldmia sp!,{r4,lr}; b), which re-enters this function on the
      finalized value before freeing zMalloc. Otherwise bit 0x40
      (MEM_Dyn) gates the xDel destructor at +0x20: when also non-NULL
      it is blx'd on the string pointer at +0x14 (ldr r0,[r4,#0x14];
      blx r1) and xDel is NULLed; when either check fails the function
      returns untouched (ldmiaeq sp!,{r4,pc}). The aggregate finalize
      @ 0x0838bc38 has since been ported (sqlite/mem_finalize) and is
      the shipped default of the MEM_AGG_FINALIZE_OPS slot, replacing
      the documented stub (clear the MEM_Agg and MEM_Dyn flag bits and
      return 0 without invoking xFinalize); the slot is kept so host
      tests can intercept it. mem_release
      @ 0x0838c04c IS ported (sqlite/mem_release) and is tail-called
      directly. The port is the shipped default extern_release slot of
      mem_release's MEM_EXTERN_OPS, replacing the
      missing_extern_release no-op. match.py structural: same two flag
      tests (tst 0x400 / tst 0x40 + ldrne/cmpne), conditional xDel blx
      with the z load, the mov r0,#0 / str xDel NULL and the tail b
      into mem_release all match; LLVM adds its fp frame, calls the
      finalize slot with blx through a literal-pool load (the slot, not
      the original's bl @ 0x0838bc38) and reloads value into r4 late.

  - address: 0x083675c0
    ghidra: FUN_083675c0
    name: release_mem_array
    size: 92
    sig: "void release_mem_array(void *mem, int n, int freebuffer)"
    module: sqlite/release_mem_array
    status: ported
    renames:
      param_1: mem
      param_2: n
      param_3: freebuffer
      iVar2: db
      uVar1: saved_malloc_failed
    notes: >-
      Upstream SQLite 3.5.9's releaseMemArray (vdbeaux.c) - the Mem
      array guts teardown; 6 bl call sites, all unconditional,
      binary-scanned from osos.dec (0x08044ccc, 0x08386db0,
      0x08386ddc, 0x0838afdc, 0x0838b6b4, and vdbe_set_num_cols @
      0x0838d0ac), no predicated or tail-b entries; extent
      0x083675c0..0x0836761c from functions.csv and the listing.
      Algorithm - NULL mem or zero n returns before anything is
      touched (movs r4,r0 / cmpne r5,#0 / ldmiaeq). Otherwise the
      owning connection is taken from the FIRST Mem's db field (ldr
      r6,[r4,#0x10]) and its sticky mallocFailed byte is saved (ldrb
      r8,[r6,#0x1e]) so releases mid-teardown never latch OOM into the
      caller; each of the n Mems then goes to mem_release @ 0x0838c04c
      when freebuffer is nonzero (cmp r7,#0 / beq) or to
      mem_extern_release @ 0x0838c074 when it is exactly zero, is
      stamped flags = MEM_Null (1) at +0x1c (mov r9,#0x1 / strh), and
      the walk advances 0x28 (add r4,r4,#0x28). The count check is
      signed (subs r0,r5,#0 / sub / bgt on the pre-decrement value),
      so a negative n walks nothing but the db save/restore still runs;
      the byte is restored after the loop (strb r8,[r6,#0x1e]).
      Deviations - both callees ARE ported (sqlite/mem_release,
      sqlite/mem_extern_release) and are called directly, no seams.
      Like the rest of the release cluster the port speaks the
      original's raw byte offsets (0x28 stride, db at +0x10, flags at
      +0x1c) rather than the typed repr(C) Mem; the two coincide on
      the 32-bit target, and on a 64-bit host the pointer-width z at
      +0x14 overlaps db at +0x10, so host tests never give the
      db-donor element a z (element 0's destructor identity is
      asserted instead). The port is the shipped default of
      vdbe_set_num_cols' SQLITE_MEM_ARRAY_RELEASE dispatch static,
      replacing the documented no-op stub (missing_mem_array_release
      is retained for tests); the slot stays for host recording mocks.
      match.py structural: same NULL/zero early-out (LLVM splits the
      fused movs/cmpne/popeq), same db load at +0x10 and ldrb/strb of
      the mallocFailed byte at +0x1e bracketing the walk, same
      freebuffer two-callee dispatch, same strh #1 at +0x1c and 0x28
      stride; LLVM recounts the loop as n+1 down to 1 (blt covering
      n < 1) and leaves the two callee bls as relocations.
      Host tests (9) - NULL array and zero count early-outs (the zero
      count provably before the db load), a negative count walking
      nothing while still bracketing the (empty) walk with the
      mallocFailed save/restore, freebuffer=1 releasing every zMalloc
      through the tracked allocator in array order and stamping
      MEM_Null with z/zMalloc/xDel NULLed, freebuffer=0 invoking each
      element's xDel in order with no zMalloc free, a destructor that
      latches OOM mid-walk rolled back to the pre-call byte, the walk
      matching a reference model on stride/order/flags for n = 1/2/5
      with untouched sentinel flags past the released prefix, a
      one-element array walking exactly once, and the shipped
      SQLITE_MEM_ARRAY_RELEASE default now being this port.

  - address: 0x0838c0c4
    ghidra: FUN_0838c0c4
    name: vdbe_mem_set_double
    size: 76
    sig: "void vdbe_mem_set_double(Mem *p_mem, f64 value)"
    module: sqlite/vdbe_mem_set_double
    status: ported
    vars: {param_1: p_mem, param_3: value_lo, param_4: value_hi}
    notes: >-
      Upstream SQLite's sqlite3VdbeMemSetDouble (vdbemem.c) - the
      early-3.x release-store-stamp form with the NaN-to-NULL
      guard: the helper @ 0x0837cb44 (upstream's sqlite3IsNaN,
      `return x != x;` spelled as a __dcmpeq @ 0x083eb748 of the
      value against itself, result normalized to 0/1) runs first;
      a NaN value tail-calls vdbe_mem_set_null @ 0x0838c13c
      (ldmiane/bne, so MemSetNull returns straight to this
      function's caller) and nothing is released, matching
      upstream's `if( sqlite3IsNaN(val) ){
      sqlite3VdbeMemSetNull(pMem); }` arm; otherwise mem_release
      @ 0x0838c04c frees the old guts, the f64 lands in the real
      arm of the value union at +0x08 (two strs, low word first),
      flags at +0x1c is ASSIGNED MEM_Real (0x8) outright - like
      the sibling vdbe_mem_set_int64 @ 0x0838c110 and unlike
      MemSetNull's read-modify-write, no attribute bit survives -
      and type at +0x1e becomes SQLITE_FLOAT (2). It sits
      immediately above vdbe_mem_set_int64 @ 0x0838c110 - the
      floating-point half of the numeric-datum setter pair.
      Extent 0x0838c0c4..0x0838c110 confirmed from raw words -
      0x0838c10c is the ldmia ...,{pc} return and 0x0838c110 is
      the stmdb entry of MemSetInt64; no literal pool. 8 branch
      references, binary-scanned from osos.dec (3 bl, 4 tail-b, 1
      bne): bl @ 0x082d74b4 inside FUN_082d7488; bl @ 0x083682e8
      inside FUN_08368254; bl @ 0x0838ef70 inside FUN_0838ef38
      (the counterpart of set-int64's bl @ 0x0838efbc fallback in
      the same column-Mem store run); tail b @ 0x082b2910 inside
      FUN_082b28b8; tail b @ 0x082b5ab0 inside FUN_082b5a58; tail
      b @ 0x08391140 inside FUN_08391124 (in the thunk run beside
      the embedded-Mem setters @ 0x08391200/0x08391218); tail b @
      0x083943f4 inside FUN_083943d0; bne @ 0x08392934 inside
      FUN_083928c8, the value copy whose byte at +0x19
      discriminates the payload arm (ldrd from +0x08 r and bne
      here, or from +0x00 u.i and beq to set-int64 @ 0x0838c110).
      Deviations - the port goes through the typed repr(C) Mem
      (offsets statically asserted on 32-bit targets in
      sqlite/vdbe.rs); the NaN predicate is Rust's f64::is_nan,
      the same self-inequality the unported helper @ 0x0837cb44
      spells through __dcmpeq (inlining it keeps this port to one
      function); the NaN route calls the ported vdbe_mem_set_null
      @ 0x0838c13c directly; mem_release runs through the
      MEM_SET_OPS slot owned by sqlite/vdbe_mem_set_int64 (its
      shipped default is the ported release), kept so host tests
      can intercept it - and that module's test-slot lock was
      exposed (pub(crate) ops_lock()) so both setter modules'
      tests serialize on the shared slot. match.py structural:
      same push/arg-save/NaN-check/cmp/release-call/f64-store
      @+0x08/strh #8 @+0x1c/strb #2 @+0x1e/pop shape with the NaN
      tail-branch preserved; LLVM spells is_nan as
      __aeabi_dcmpun, calls the release through the slot (literal
      ldr + blx, not the original's bl @ 0x0838c04c), swaps the
      two value-half strs and the strb/strh across disjoint
      offsets, and adds its fp frame. Host tests (6) - the value
      landing bit-for-bit in the real arm over edge cases
      (+-zero, +-1, subnormal boundaries, the infinities, finite
      extremes), flags assigned MEM_Real outright exhaustively
      over all 65536 prior u16 values, value_type forced to
      SQLITE_FLOAT over all 256 prior type bytes, the guts
      release running exactly once per non-NaN call on the cell
      itself (recording mock on MEM_SET_OPS), NaN payloads
      (canonical/negative/signaling/all-ones) routing to set-null
      semantics - (flags & !0x1f) | MEM_Null, SQLITE_NULL, no
      release, real arm untouched - and u/db/n/enc
      byte-for-byte untouched (z/xDel/zMalloc are the release's
      own contract, covered by sqlite::mem_release's tests).

  - address: 0x0838c110
    ghidra: FUN_0838c110
    name: vdbe_mem_set_int64
    size: 44
    sig: "void vdbe_mem_set_int64(Mem *p_mem, i64 value)"
    module: sqlite/vdbe_mem_set_int64
    status: ported
    vars: {param_1: p_mem, param_3: value, param_4: value_hi}
    notes: >-
      Upstream SQLite's sqlite3VdbeMemSetInt64 (vdbemem.c) - the
      early-3.x release-store-stamp form: mem_release @ 0x0838c04c
      frees the old guts, the i64 lands in the integer arm of the
      value union at +0x00 (stmia, low word first), flags at +0x1c is
      ASSIGNED MEM_Int (0x4) outright - no attribute bit survives,
      unlike the sibling vdbe_mem_set_null @ 0x0838c13c's
      read-modify-write - and type at +0x1e becomes SQLITE_INTEGER (1).
      It sits between sqlite3VdbeMemSetDouble @ 0x0838c0c4 (stores
      the f64 arm at +0x08 with MEM_Real 0x8 / SQLITE_FLOAT 2) and
      sqlite3VdbeMemSetNull @ 0x0838c13c - the numeric-datum pair of
      the vdbeMem setter family. Extent 0x0838c110..0x0838c13c
      confirmed from raw words - 0x0838c138 is the ldmia ...,{pc}
      return and 0x0838c13c is the ldrh entry of MemSetNull; no
      literal pool. 7 branch references, binary-scanned from osos.dec
      (2 bl, 4 tail-b, 1 beq): bl @ 0x08365590 inside FUN_0836554c
      (doubles a fetched 64-bit, zeros on overflow, embedded Mem at
      +8); bl @ 0x0838efbc inside FUN_0838ef84 (fallback path stores
      into a column-indexed Mem array element, *(p+0x38) + n*0x28 -
      0x28); tail b @ 0x082b2974 inside FUN_082b28b8 (embedded Mem at
      +8); tail b @ 0x082c5030 inside FUN_082c5008 (ldrd from a
      fetched cell, embedded Mem at +8); tail b @ 0x082d7a4c inside
      FUN_082d79b4 (sign-extends a computed 32-bit count, mov r3,r2,
      asr #0x1f); tail b @ 0x0839120c from the unlisted thunk @
      0x08391200 (mov r2,r1; mov r3,r1,asr #0x1f; add r0,r0,#8; b -
      the 32->64 sign-extending sqlite3VdbeMemSetInt analog, in the
      embedded-Mem setter run beside the set-null thunk @ 0x08391218);
      beq @ 0x0839292c inside FUN_083928c8, the value copy whose byte
      at +0x19 discriminates the payload arm (ldrd from +0x00 u.i and
      beq here, or from +0x08 r and bne to set-double @ 0x0838c0c4).
      Deviations - the port goes through the typed repr(C) Mem
      (offsets statically asserted on 32-bit targets in
      sqlite/vdbe.rs); mem_release @ 0x0838c04c IS ported
      (sqlite/mem_release) and is the shipped default of the new
      MEM_SET_OPS slot, kept so host tests can intercept it - the
      ported release speaks the original's raw byte offsets, which
      coincide with the typed Mem only on the 32-bit target (the same
      reason value_free keeps its VALUE_MEM_OPS slot). match.py
      structural: same push/arg-save/release-call/i64-store/strh #4
      @+0x1c/strb #1 @+0x1e/pop shape; LLVM calls the release through
      the slot (literal-pool ldr + blx, not the original's bl @
      0x0838c04c), splits the stmia into two strs, swaps the
      strb/strh order across disjoint offsets and adds its fp frame.
      Host tests (5) - the value landing bit-for-bit in u over edge
      cases (0, +-1, sign/word boundaries, i64 extremes), flags
      assigned MEM_Int outright exhaustively over all 65536 prior u16
      values (no attribute bit survives), value_type forced to
      SQLITE_INTEGER over all 256 prior type bytes, the guts release
      running exactly once per call on the cell itself (recording
      mock on MEM_SET_OPS), and r/db/n/enc byte-for-byte untouched
      (z/xDel/zMalloc are the release's own contract, covered by
      sqlite::mem_release's tests).

  - address: 0x0838c13c
    ghidra: FUN_0838c13c
    name: vdbe_mem_set_null
    size: 28
    sig: "void vdbe_mem_set_null(Mem *p_mem)"
    module: sqlite/vdbe_mem_set_null
    status: ported
    vars: {param_1: p_mem}
    notes: >-
      Upstream SQLite's sqlite3VdbeMemSetNull (vdbemem.c) - the "set
      the value stored in *pMem to NULL" primitive; 4 bl call sites
      plus one tail-b, binary-scanned from osos.dec, no predicated
      entries: 0x0838c178 inside sqlite3VdbeMemSetStr @ 0x0838c158
      (the z == NULL route documented in sqlite/value_set_str.rs),
      0x0838a2d0 and 0x0838ac38 inside the 16 KB vdbe routine
      FUN_08386ef8, 0x083911b4 inside the out-of-memory marker
      FUN_083911a8 (NULLs an embedded Mem at +8, stamps SQLITE_NOMEM
      at +0x34, sets the mallocFailed byte of the connection at
      +0x18), and a tail b @ 0x0839121c from the thunk @ 0x08391218
      (add r0,r0,#8; b) that NULLs the Mem eight bytes into its
      argument. Extent 0x0838c13c..0x0838c158 confirmed from raw
      words - 0x0838c154 is bx lr and 0x0838c158 is the stmdb entry
      of sqlite3VdbeMemSetStr; no literal pool.
      Algorithm - two stores, no branches, no calls: the flags
      halfword at +0x1c is read-modify-written, ldrh / bic #0x1f /
      orr #1 / strh - the low five type bits (this build's MEM_Null
      0x1 / MEM_Str 0x2 / MEM_Int 0x4 / MEM_Real 0x8 / MEM_Blob 0x10)
      are replaced by MEM_Null while every attribute bit above
      survives (MEM_Term 0x20, MEM_Dyn 0x40, MEM_Static 0x80, MEM_Agg
      0x400, MEM_Zero 0x800, per the sibling ports' recovered
      numbering) - the read-modify-write form of upstream's helper
      (later upstreams spell it MemSetTypeFlag(pMem, MEM_Null)); the
      textbook early-3.x form assigns flags = MEM_Null outright,
      which this binary does not do. Then type at +0x1e becomes
      SQLITE_NULL (5, mov r1,#0x5 / strb). Nothing is released:
      z/zMalloc/xDel stay put, so a cell that owns a buffer keeps
      owning it - releasing is mem_release @ 0x0838c04c's job.
      Deviations - none beyond the typed repr(C) Mem with named
      fields (offsets statically asserted on 32-bit targets in
      sqlite/vdbe.rs); MEM_NULL and SQLITE_NULL are reused from
      sqlite/value_new.rs, which owns them.
      match.py structural: same two stores at +0x1c/+0x1e with the
      same constants (LLVM reassociates (flags & ~0x1f) | 1 into
      orr #1 / bic #30 - identical - and emits its fp frame; the
      strb/strh order swap across disjoint offsets is unobservable).
      Host tests (4) - the flags transform verified exhaustively over
      all 65536 u16 values, value_type forced to SQLITE_NULL over all
      256 prior type bytes, the recovered attribute bits surviving
      individually and combined, and every other Mem field
      byte-for-byte untouched.

  - address: 0x0838c158
    ghidra: FUN_0838c158
    name: vdbe_mem_set_str
    size: 304
    sig: "int vdbe_mem_set_str(Mem *p_mem, const void *z, int n, u8 enc, void *x_del)"
    module: sqlite/vdbe_mem_set_str
    status: ported
    vars:
      param_1: p_mem
      param_2: z
      param_3: n
      param_4: enc
      param_5: x_del
      r4: n
      r5: p_mem
      r6: z
      r7: flags
      r8: enc
      r9: x_del_or_copy_len
      uVar1: value_type
      uVar2: ownership
      uVar5: flags
      iVar3: copy_len
      iVar4: rc
      bVar6: is_blob
    notes: >-
      Upstream SQLite 3.5.x's sqlite3VdbeMemSetStr (vdbemem.c), the
      string/blob installer for an existing Mem. Extent is
      0x0838c158..0x0838c288: 0x0838c280 is mov r0,#0,
      0x0838c284 is ldmia sp!,{r4-r10,pc}, and 0x0838c288 begins the
      next function; there is no literal pool. Binary scan of osos.dec
      finds 24 direct call sites: 23 bl plus the blne @ 0x08386710 in
      sqlite3ValueSetStr @ 0x083866ec; sqlite3VdbeSetColName @
      0x0838d004 is another caller through the shared dispatcher.
      NULL z calls sqlite3VdbeMemSetNull @ 0x0838c13c then returns 0.
      Otherwise flags start MEM_Str (0x2), or MEM_Blob (0x10) for
      enc == 0. Negative n scans a single UTF-8 NUL or a double NUL in
      two-byte units and sets MEM_Term (0x20). SQLITE_TRANSIENT
      ((void *)-1) calls sqlite3VdbeMemGrow @ 0x0838bdb0 with n plus
      the measured terminator and memcpy's z into the grown Mem buffer;
      other xDel values first release old guts via
      sqlite3VdbeMemRelease @ 0x0838c04c, then install borrowed
      MEM_Static (0x80) storage for NULL xDel or MEM_Dyn (0x40)
      external storage otherwise. It stores n, flags, enc, and API
      type (TEXT 3/BLOB 4) in that order. BLOBs retain UTF-8 encoding
      after their BLOB type is latched; non-UTF-8 text calls the
      BOM-strip helper MemHandleBom @ 0x0838be98. A nonzero result
      from Grow or HandleBom is normalized to SQLITE_NOMEM (7).
      The Rust port is the shipped default of value_set_str's
      SQLITE_VDBE_MEM_SET_STR slot, replacing its old NOMEM stub while
      retaining the slot for test mocks. Grow @ 0x0838bdb0 has since
      been ported (sqlite/vdbe_mem_grow) but this module's slot is
      unchanged: Grow and HandleBom remain
      typed dispatch seams: host defaults panic until tests supply
      mocks, target defaults call their retailOS addresses. The
      existing MEM_SET_OPS release seam is reused because mem_release
      speaks raw target offsets. Host tests (10) cover the NULL route,
      static/dynamic ownership, UTF-8 and UTF-16 measured copies,
      BLOB tagging, explicit copy lengths, Grow/BOM failure timing,
      empty strings, helper traces, and untouched union/db fields.

  - address: 0x0838c2d0
    ghidra: FUN_0838c2d0
    name: vdbe_mem_shallow_copy
    size: 92
    sig: "void vdbe_mem_shallow_copy(Mem *p_to, const Mem *p_from, u16 src_type)"
    module: sqlite/vdbe_mem_shallow_copy
    status: ported
    vars:
      param_1: p_to
      param_2: p_from
      param_3: src_type
      r4: p_to
      r5: p_from
      r6: src_type
      FUN_0838c074: mem_extern_release
      thunk_EXT_FUN_22000188: memcpy
    notes: >-
      Upstream SQLite 3.5.x's sqlite3VdbeMemShallowCopy (vdbemem.c),
      sitting between sqlite3VdbeMemSetStr @ 0x0838c158 and
      sqlite3VdbeMemStringify @ 0x0838c32c in the vdbe Mem helper
      cluster - how OP_Column-style code hands a value from the row
      cache to an output register without moving the payload. Extent
      0x0838c2d0..0x0838c32c confirmed from raw words - 0x0838c328 is
      ldmia sp!,{r4,r5,r6,pc} and 0x0838c32c is the stmdb entry of
      sqlite3VdbeMemStringify; no literal pool. 3 direct call sites,
      binary-scanned, all inside the 16 KB vdbe routine FUN_08386ef8:
      bl @ 0x083874c0 (src_type 0x80 = MEM_Static, column value into
      the [r5,#0x38] register array), bl @ 0x0838752c (src_type 0x100
      = MEM_Ephem, the [r5,#0x4c] array, followed by a MEM_Ephem test
      that makes the copy writeable through 0x0838bb30) and bleq @
      0x0838851c (src_type 0x80, gated on a sign-extended byte == -8).
      Algorithm - the extern release @ 0x0838c074
      (vdbeMemClearExternAndSetNull, ported as mem_extern_release)
      runs on the DESTINATION only - zMalloc is deliberately not
      freed, so the destination keeps its allocation buffer (freeing
      it is mem_release @ 0x0838c04c's job). Then 0x24 bytes are
      copied - the whole 0x28-byte Mem except zMalloc at +0x24
      (upstream's MEMCELLSIZE = offsetof(Mem, zMalloc)) - and xDel at
      +0x20 is NULLed: the destination never owns the source's
      external storage. The ownership restamp is conditional: when
      the source's flags have MEM_Dyn (0x40) OR its z == zMalloc
      (ldr r0,[r5,#0x14]! / ldr r1,[r5,#0x10] / cmp - the writeback
      reload trick), the copied MEM_Dyn|MEM_Static|MEM_Ephem bits
      (bic #0x1c0) are replaced by the caller's src_type (orr, no
      mask of its own); otherwise the copied flags stand and the
      function returns early (ldmiane). Deviations - the port goes
      through the typed repr(C) Mem (ptr::read/write of the struct
      with z_malloc saved and restored is the same field set as the
      raw 0x24-byte copy, and keeps the host build's wider pointer
      fields coherent); the ROM veneer @ 0x08037df8 targets
      memcpy_forward_words, which LLVM lowers itself (to
      __aeabi_memmove8 of 0x28 bytes with z_malloc held in a
      callee-saved register). mem_extern_release IS ported and is the
      shipped default of the SHALLOW_COPY_OPS slot; the slot is kept
      so host tests can intercept it (the ported release speaks raw
      target offsets, like MEM_SET_OPS's mem_release). match.py
      structural: same shape - extern release first (ldr/blx through
      the ops slot vs bl), struct copy, xDel NULLed, tst #0x40/bne,
      z==zMalloc cmp with early popne, then the bic #0x1c0 / orr /
      strh restamp (LLVM folds the mask to and with 0xfe3f). Host
      tests (6) - every field but z_malloc/x_del copied (z_malloc
      preserved, x_del NULLed, source untouched), the extern release
      running exactly once on the destination BEFORE the copy (mock
      captures the pre-copy flags/u), the MEM_Dyn restamp exhaustive
      over all 32768 flagged values x 4 src_types, the z==zMalloc
      restamp without MEM_Dyn (MEM_Agg/MEM_Term bits surviving), the
      borrowed-payload early return exhaustive over all 32768
      MEM_Dyn-less flags values with src_type irrelevant, and
      src_type stored verbatim with no mask of its own.

  - address: 0x0838c32c
    ghidra: FUN_0838c32c
    name: vdbe_mem_stringify
    size: 144
    sig: "int vdbe_mem_stringify(void *p_mem, u8 enc)"
    module: sqlite/vdbe_mem_stringify
    status: ported
    vars:
      param_1: p_mem
      param_2: enc
      r4: p_mem
      r5: flags_snapshot
      r6: sqlite_ok
      r7: enc
      FUN_0838bdb0: sqlite3VdbeMemGrow
      FUN_083913b4: sqlite3_snprintf
      FUN_08392478: strlen
      FUN_083869f4: sqlite3VdbeChangeEncoding
    notes: >-
      Upstream SQLite 3.5.x's sqlite3VdbeMemStringify (vdbemem.c),
      immediately after vdbe_mem_shallow_copy @ 0x0838c2d0 in the
      vdbe Mem helper cluster - how sqlite3ValueText-style code
      materializes text out of a cell that only carries a number.
      Extent 0x0838c32c..0x0838c3bc confirmed from raw words
      (0x0838c3b8 is ldmia sp!,{r2,r3,r4,r5,r6,r7,r8,pc}), plus two
      inline adr-addressed format literals at 0x0838c3bc..0x0838c3cc
      ("%lld" and "%!.15g", read from the decrypted image); no
      literal pool. 4 bl call sites, binary-scanned: 0x08386798 in
      the sqlite3ValueText original, and 0x08387634, 0x08387668,
      0x0838ad4c inside the 16 KB vdbe routine FUN_08386ef8.
      Algorithm - flags are sampled once BEFORE the grow (ldrh r5);
      sqlite3VdbeMemGrow(pMem, 32, 0) failure short-circuits to
      SQLITE_NOMEM (7) with the cell untouched; the MEM_Int (0x4)
      snapshot bit picks "%lld" over Mem.u at +0x00 or "%!.15g"
      over Mem.r at +0x08, read AFTER the grow and passed as the
      8-byte variadic payload at [sp,#0]; the rendered length
      (unguarded strlen @ 0x08392478, ported) lands in n at +0x18,
      enc at +0x1f becomes SQLITE_UTF8 (1), the RELOADED flags gain
      MEM_Str|MEM_Term (0x22), and sqlite3VdbeChangeEncoding(pMem,
      enc) recodes with the caller's enc UNMASKED (mov r1,r7 - the
      SQLITE_UTF16_ALIGNED bit survives), its return code discarded.
      Deviations - grow/snprintf/changeEncoding stay behind the
      VDBE_MEM_STRINGIFY_OPS seam (host defaults panic, target
      defaults transmute the retailOS addresses); the snprintf slot
      passes the variadic 8-byte payload as raw u64 bits through a
      non-variadic fourth parameter, ABI-identical under AAPCS
      (64-bit arg after three word args lands entirely on the stack
      either way); the format strings live in Rust rodata instead of
      inline adr literals. match.py structural: same shape - flags
      sample, grow, NOMEM short-circuit, branchless int/real arm
      select (addeq +0x8 / ldm / stm sp vs predicated ldrd/strd),
      inlined strlen byte loop (LLVM), n/enc stores, ldrh-orr #0x22-
      strh flags restamp, recode tail, SQLITE_OK return; calls go
      through the ops slots (blx) instead of direct bl. Host tests
      (10) cover int rendering across edge values (0, +/-1, 42,
      i64::MIN/MAX), real rendering with bit-exact payload check,
      MEM_Int winning when both type bits are set, grow-failure
      atomicity (no formatter/recode, cell untouched), enc passed
      unmasked (incl. 0x0a with the aligned bit), the discarded
      recode return code, the post-strlen flags reload, the pre-grow
      flags snapshot, the post-grow value read, and attribute-bit
      survival.

  - address: 0x0838c3cc
    ghidra: FUN_0838c3cc
    name: vdbe_mem_too_big
    size: 76
    sig: "int vdbe_mem_too_big(const Mem *p_mem)"
    module: sqlite/vdbe_mem_too_big
    status: ported
    vars:
      param_1: p_mem
      r4: p_mem
      r1: flags_snapshot
      r0: n_total
      r2: n_scratch
      iVar1: n_total
    notes: >-
      Upstream SQLite 3.5.x's sqlite3VdbeMemTooBig (vdbemem.c),
      immediately after vdbe_mem_stringify @ 0x0838c32c (and its two
      inline format literals) in the vdbe Mem helper cluster - the
      SQLITE_LIMIT_LENGTH gate the vdbe engine runs before
      materializing an oversized TEXT/BLOB result. Extent
      0x0838c3cc..0x0838c418 confirmed from raw words (0x0838c414 is
      ldmia sp!,{r4,pc} and 0x0838c418 is the next function's stmdb);
      no literal pool. 2 bl call sites, binary-scanned: 0x083874a4
      and 0x0838aca8, both inside the 16 KB vdbe routine
      FUN_08386ef8. Algorithm - flags at Mem+0x1c are sampled ONCE
      (ldrh r1) and both tests run off that halfword; only
      MEM_Str|MEM_Blob cells (tst #0x12) are checked, anything else
      returns 0 without touching db; the size is n at +0x18 plus,
      for a MEM_Zero (0x800) zero-tail blob, low32(Mem.u) at +0x00 -
      the original's ldrd/adds/adc computes a 64-bit sum but the high
      half is dead (r1 is reloaded with db before the cmp), so the
      observable op is the wrapping 32-bit add; the verdict is the
      signed limit < n_total against aLimit[SQLITE_LIMIT_LENGTH] at
      db+0x50 (ldr r1,[r4,#0x10]; ldr r1,[r1,#0x50]; cmp; movlt),
      strictly greater - limit == n_total is small enough.
      Deviations - the port goes through the typed repr(C) Mem with
      named fields; the dead 64-bit high half of the adds/adc is not
      reproduced; the parameter is const (the original never writes
      through r0). Leaf - no ops seam. match.py structural: same
      shape - flags sample, tst #0x12 early-out, tst #0x800
      conditional low-word add (LLVM uses predicated ldrne/addne
      instead of beq), db+0x50 limit load, signed cmp with movlt;
      LLVM keeps the frame off the non-zero path instead of pushing
      r4 up front. Host tests (6) cover the MEM_Str|MEM_Blob mask
      exhaustively over all 65536 flag values, the strictly-greater
      boundary (limit == n is OK), signedness on both sides
      (negative n never too big, negative limit gates), MEM_Zero
      adding low32(u) to n on both str and blob cells, the dead high
      half of u (2^32 adds nothing) with the wrapping 32-bit add
      (i32::MAX + 60 wraps negative), and the cell/connection being
      read-only.

  - address: 0x0838c7d4
    ghidra: FUN_0838c7d4
    name: vdbe_opcode_has_property
    size: 20
    sig: "int vdbe_opcode_has_property(int opcode, int mask)"
    module: sqlite/vdbe_opcode_has_property
    status: ported
    vars:
      param_1: opcode
      param_2: mask
      r0: opcode_then_result
      r1: mask
      r2: opcode_property_table
      DAT_0838c7e8: opcode_property_table_ptr
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeOpcodeHasProperty (vdbe.c:
      `return (opcodeProperty[opcode]&mask)!=0;`), identified by exact
      structural match of both callers against the public 3.5.9
      source: resolveP2Values (FUN_08368038) and sqlite3VdbeAddOpList
      (FUN_0838690c), each gating negative-P2 label fixup on
      (opcode, OPFLG_JUMP). Extent 0x0838c7d4..0x0838c7e8 confirmed
      from raw words (5 instructions; 0x0838c7ec is the next
      function's stmdb), plus the literal-pool word @ 0x0838c7e8
      holding the table pointer 0x08a0994c. 2 bl call sites,
      binary-scanned: 0x083680e8 and 0x0838699c. Algorithm - ldrb
      table[opcode], ands full-register mask, movne r0,#0x1:
      normalized 0/1. The opcodeProperty table (144 bytes, max opcode
      143, every byte drawn from OPFLG bits 0x01..0x20) is RECOVERED
      from the image at runtime base +0xaed8 = image 0x08a14824 (the
      sqlite/mod.rs skew): the 3.5.9 mkopcodeh.awk numbers non-TK
      opcodes by iterating an awk associative array (unspecified
      order), so this build's opcode numbering cannot be regenerated
      from upstream. Recovery cross-checked against vdbe.c property
      comments via caller-recovered opcode numbers: OP_Function=0x15
      (0x00), OP_Noop=0x17 (0x00), OP_VRename=0x1d (0x00),
      OP_Halt=0x26 (0x00), OP_Statement=0x2a (0x00), OP_AggStep=0x61
      (0x00), OP_VFilter=0x64 (0x01, /* jump */), OP_Destroy=0x6a
      (0x02, /* out2-prerelease */), OP_VUpdate=0x77 (0x00), plus the
      0x2c (IN1|IN2|OUT3) arithmetic run at 74..=83 - all consistent.
      The at-rest bytes at the unskewed pointer 0x08a0994c are a
      256-byte permutation repeated 16x inside a high-entropy
      FairPlay/cert region - NOT the runtime table; the +0xaed8 skew
      resolution is what makes the port possible. Deviations - the
      ROM table is the crate static OPCODE_PROPERTY; out-of-range
      opcodes (barred by upstream's compiled-out assert) read beside
      it via wrapping_offset; mask is i32 (full-register ands), not
      the decompile's byte. Leaf - no ops seam. match.py structural:
      same shape - literal-pool table base, ldrb [r2,r0], ands,
      movne #1; LLVM pushes {fp,lr} it does not need. Host tests (7)
      cover the skew arithmetic, the recovered table verbatim
      (independent copy + 6-bit sanity), all 144 opcodes x all 256
      byte masks exhaustively, full-register mask behavior (bit 8,
      sign bit, -1, 0), 0/1 normalization on multi-bit ANDs, both
      boundary opcodes, and the upstream property comments for every
      caller-recovered opcode number.

  - address: 0x0838c7ec
    ghidra: FUN_0838c7ec
    name: vdbe_real_value
    size: 136
    sig: "double vdbe_real_value(Mem *p_mem)"
    module: sqlite/vdbe_real_value
    status: ported
    renames:
      param_1: p_mem
      uVar3: integer_low_word
      uVar5: integer_high_word
      uVar6: integer_sign_bit
      local_18: parsed_value_low_word
      uStack_14: parsed_value_high_word
      DAT_0838c874: zero_real_low_word
      DAT_0838c878: zero_real_high_word
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeRealValue (vdbe.c), the
      double-returning helper vdbe_mem_realify @ 0x0838c024 calls before
      storing Mem.r. Extent 0x0838c7ec..0x0838c874: 33 ARM instructions
      through the return @ 0x0838c870 plus the 8-byte literal-pool
      fallback 0.0 @ 0x0838c874. Dispatch samples flags once: MEM_Real
      (0x8) returns r (+0x08); MEM_Int (0x4) signed-converts u.i (+0x00)
      to double through the inlined ADS soft-float path; MEM_Str|MEM_Blob
      (0x12) ORs in MEM_Str, runs sqlite3VdbeChangeEncoding(pMem,
      SQLITE_UTF8) @ 0x083869f4, then sqlite3VdbeMemNulTerminate(pMem) @
      0x0838bfb0, and finally sqlite3AtoF(z, &value) @ 0x0836f528,
      ignoring its consumed-byte return. Either checked helper failure,
      and all other storage classes including NULL, return literal 0.0.
      The three unported callees are VDBE_REAL_VALUE_OPS: target defaults
      call their retailOS addresses and host tests install recorders.
      5 direct bl callers in osos.asm at 0x0838788c, 0x08387898,
      0x08387dac, 0x083880b8 and 0x0838c02c; the thunk @ 0x08391774
      branches here. Host tests cover REAL precedence/no calls, signed
      integer conversion at both extrema, blob promotion and helper order,
      both checked-error short circuits, and NULL/other zero fallback.

  - address: 0x0838c87c
    ghidra: FUN_0838c87c
    name: vdbe_record_compare
    size: 372
    sig: "int vdbe_record_compare(int n_key1, const u8 *p_key1, const UnpackedRecord *p_key2)"
    module: sqlite/vdbe_record_compare
    status: ported
    renames:
      param_1: n_key1
      param_2: p_key1
      param_3: p_key2
      local_34: sz_hdr1
      local_64: serial_type1
      local_30: n_key1_stack_copy
      auStack_60: mem1
      local_50: mem1_db
      local_44: mem1_flags
      local_41: mem1_enc
      local_3c: mem1_z_malloc
      uVar2: idx1
      uVar7: d1
      iVar5: i
      iVar8: rc
      lVar11: compare_result_pair
      extraout_r1: '[invented — live r1 after the mem_release bl]'
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeRecordCompare (vdbeaux.c),
      verified against the 3.5.9 source: weigh the packed OP_MakeRecord
      blob {nKey1, pKey1} against a parsed UnpackedRecord key2 during
      index seeks. functions.csv's 372 bytes is EXACT (the mis-sizing
      suspicion in the assignment does not hold): the next stmdb is
      0x0838c9f0, the sqlite3VdbeRecordUnpack analog that builds the
      UnpackedRecord this one consumes; no literal pool. 3 bl call
      sites: 0x0837208c/0x083720cc in FUN_08371e54 (the
      sqlite3BtreeMoveto analog — upstream btree.c's two calls) and
      0x0838b450 in FUN_0838b380 (the sqlite3VdbeIdxKeyCompare analog).
      Struct layouts recovered from its own loads (typed repr(C) in the
      port, 32-bit offset asserts): UnpackedRecord +0x00 pKeyInfo, +0x04
      nField (u16), +0x06 needFree, +0x07 needDestroy, +0x08 aMem (raw
      0x28-byte Mems); KeyInfo +0x00 db, +0x04 enc, +0x05 incrKey,
      +0x06 prefixIsEqual, +0x08 nField (i32), +0x0c aSortOrder, +0x10
      aColl[i]. Algorithm: seed scratch mem1 (sp+0x08) db/enc from the
      KeyInfo, clear flags/zMalloc; decode the header-size varint with
      the build-wide idiom (first byte < 0x80 inline, else
      sqlite3GetVarint @ 0x0837ac30 — ported, called directly, u64 out
      truncated to the original's u32 store); while idx1 < szHdr1 && i
      < pPKey2->nField: decode the serial type the same way, break when
      the payload is exhausted (d1 >= nKey1, UNSIGNED) and
      sqlite3VdbeSerialTypeLen @ 0x0838cfe8 reports a nonzero length
      (SIGNED bgt — exact, the formula never sets bit 31), else
      sqlite3VdbeSerialGet @ 0x0838cc1c decodes into mem1 (d1 += its
      return) and sqlite3MemCompare @ 0x0837d47c weighs mem1 against
      aMem[i] under aColl[i] (NULL coll once i reaches the KeyInfo
      nField, loaded once before the loop); a nonzero rc breaks. Then
      mem1.zMalloc, if any, goes to the ported mem_release @ 0x0838c04c
      (the original's blne; retail serial_get never allocates, so the
      call is dead but present). Endgame: rc==0 → -1 when incrKey, else
      stays 0 when prefixIsEqual, else 1 when d1 < nKey1 (unsigned);
      rc!=0 → negated when aSortOrder exists && i < nField (reloaded)
      && aSortOrder[i]. Ghidra's decompile drops the third mem_compare
      argument and obscures the endgame as SBORROW4 — the disassembly
      is authoritative (movge r2,#0 / ldrlt r2,[r0,#16] is the coll
      select). The serial_type_len callee @ 0x0838cfe8 and serial_get
      callee @ 0x0838cc1c are now ported (sqlite/vdbe_serial_type_len
      and sqlite/vdbe_serial_get) and are the shipped defaults of their
      VDBE_RECORD_COMPARE_OPS slots; their missing_ panic stubs remain
      for tests needing unconfigured callees. Only mem_compare @
      0x0837d47c remains the seam (target defaults transmute its retail
      address; host defaults panic). Deviations: the serial type_len
      datum — the pointer 0x088fce10 in the literal-pool word at
      0x0838d000 — resolves through the +0xaed8 skew documented in
      sqlite/mod.rs to image offset 0x08907ce8, whose bytes ARE
      upstream's aSize[] verbatim (binary-verified; see the
      0x0838cfe8 entry); the scratch Mem is 0x30 bytes and zero-filled
      on host
      (mem_release's native-width zMalloc NULLing at +0x24 must stay in
      bounds; nothing reads the uninitialized-in-the-original fields
      before serial_get writes them). match.py: original 93 insns vs
      139 — same skeleton: ldrsb/cmn/ble sign-trick for the < 0x80
      fast paths, direct bl to the ported get_varint (×2) and
      mem_release (the original's predicated blne), blx through the
      three seam slots where the original has direct bl
      0x0838cfe8/0x0838cc1c/0x0837d47c, the ldrlt/movge coll select,
      and the endgame ldrb +0x05/+0x06, mvn rc=-1, movcc rc=1,
      ldr +0x0c/+0x08, rsbne negation; LLVM extras are the scratch-Mem
      zero-fill, a branchless clz/lsr/and for prefixIsEqual==0 &&
      d1<nKey1, and unsigned-equivalent loop compares (u16-bounded).
      Host tests (16): equal multi-field walks with exact serial_get
      payload addresses (idx1/d1 tracking), first-difference break,
      descending negation on both columns, sort order past nField
      ignored, trailing-payload/incrKey/prefixIsEqual endgames, NULL
      ordering, real fields, the exhausted-payload break (serial_get
      never runs), zero-length fields past key1 end still comparing,
      a 130-field record (two-byte header varint through the ported
      get_varint; per-field TypeLen(0) checks), two-byte serial types
      with 60-byte text + blob, bytewise text order with the prefix
      rule, coll selection past the KeyInfo nField, and zMalloc release
      through the ported mem_release with a recording heap.

  - address: 0x0838c9f0
    ghidra: FUN_0838c9f0
    name: vdbe_record_unpack
    size: 312
    sig: "UnpackedRecord *vdbe_record_unpack(KeyInfo *key_info, int n_key, const u8 *p_key, void *p_space, int sz_space)"
    module: sqlite/vdbe_record_unpack
    status: ported
    renames:
      param_1: key_info
      param_2: n_key
      param_3: p_key
      param_4: p_space
      param_5: sz_space
      local_38: sz_hdr
      local_3c: serial_type
      piStack_34: key_info_stack_copy
      local_30: n_key_stack_copy
      pbStack_2c: p_key_stack_copy
      piStack_28: p_space_stack_copy
      iVar3: n_byte_then_i
      piVar4: p_mem
      uVar1: idx
      uVar5: d
      iVar2: varint_or_type_len_scratch
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeRecordUnpack (vdbeaux.c),
      verified against the 3.5.9 source: parse the nKey-byte record at
      pKey into an UnpackedRecord over pKeyInfo, using pSpace when
      (nField + 2) * sizeof(Mem) fits in szSpace and
      sqlite3DbMallocRaw(pKeyInfo->db, nByte) otherwise. THE
      ASSIGNMENT'S sqlite3VdbeSerialPut SUSPICION DOES NOT HOLD: this
      is the record UNPACKER (the writer side's inverse) - it calls
      sqlite3VdbeSerialGet, which a put-side writer never would, and
      upstream 3.5.9 has no serial-put function in this span at all.
      functions.csv's 312 bytes is exact: the next stmdb is
      0x0838cb28; no literal pool. 2 bl call sites (binary-scanned):
      0x08371f2c in FUN_08371e54 (the sqlite3BtreeMoveto analog) and
      0x0838b430 in FUN_0838b380 (the sqlite3VdbeIdxKeyCompare
      analog), each passing a 200-byte stack buffer as pSpace; no
      tail b. Struct layouts the listing PROVES (and the
      vdbe_record_compare entry/comment now cite): UnpackedRecord
      +0x00 pKeyInfo, +0x04 nField (u16, strh), +0x06 needFree (strb,
      0 for pSpace / 1 for heap), +0x07 needDestroy (strb, always 1),
      +0x08 aMem = p + 0x28; the earlier +0x05/+0x06 guess in the
      record_compare port never read them and was wrong. KeyInfo
      +0x00 db, +0x04 enc, +0x08 nField (i32, ldr). Mem field writes
      per element: enc byte +0x1f from KeyInfo.enc, db word +0x10
      from KeyInfo.db, flags hword +0x1c = 0, zMalloc word +0x24 = 0.
      Algorithm: nByte = (nField + 2) * 0x28; pSpace branch stamps
      needFree = 0, heap branch needFree = 1 with NULL propagating;
      pKeyInfo/nField+1/needDestroy = 1/aMem seeded; header-size
      varint via the build-wide idiom (first byte < 0x80 inline, else
      the ported get_varint @ 0x0837ac30, u64 out truncated to the
      original's u32 store - the get_varint32_fast helper is shared
      with vdbe_record_compare, one idiom one home); while idx <
      szHdr (unsigned) AND i < p->nField (the u16 at +0x04 reloaded
      per iteration): decode the serial type the same way, break when
      d >= nKey (SIGNED blt to skip) and the ported
      vdbe_serial_type_len @ 0x0838cfe8 reports a nonzero length
      (SIGNED bgt, exact - the formula never sets bit 31), else seed
      the field Mem and let the ported vdbe_serial_get @ 0x0838cc1c
      decode into it (d += its return, pMem += 0x28). The
      exhausted-payload break stops a short record before the first
      field that would read past nKey, but trailing zero-length
      fields (NULL, the 0/1 constants) still unpack. p->nField = i on
      the way out. All four callees are ported and called directly,
      per the porting rules. Deviations: the aMem elements stay RAW
      0x28-byte Mems (the vdbe_serial_get convention), so on a 64-bit
      host the db word at +0x10 is the pointer's low 32 bits - the
      original's str is a 4-byte store and a host-width store would
      clobber z/n at +0x14/+0x18. match.py: 78 original vs 93 LLVM
      instructions, same structure (LLVM strength-reduces the *0x28
      to lsl pairs, folds the < 0x80 fast path into ldrsb/cmn, and
      reproduces the per-iteration ldrh nField reload). Host tests
      (8): every serial type 0..13 plus 5-byte text and 7-byte blob
      round-tripped byte-exact against the ported vdbe_serial_get
      over the same payload pointers (full 0x28-byte Mem compare,
      seeded buffers); the exact-fit caller-buffer boundary vs the
      one-byte-short db_malloc_raw path (recording allocator,
      needFree = 1) and its NULL/failure-latch endgame; the
      short-record break before the first overrunning field;
      trailing NULLs unpacking past the exhausted payload; the
      nField + 1 field cap; a 130-NULL record (two-byte szHdr varint
      through the ported get_varint); a 93-byte text field (two-byte
      serial-type varint).

  - address: 0x0838cc1c
    ghidra: FUN_0838cc1c
    name: vdbe_serial_get
    size: 484
    sig: "u32 vdbe_serial_get(const u8 *buf, u32 serial_type, Mem *p_mem)"
    module: sqlite/vdbe_serial_get
    status: ported
    renames:
      param_1: buf
      param_2: serial_type
      param_3: p_mem
      bVar1: payload_byte_1
      bVar2: payload_byte_0
      uVar3: mem_flags
      uVar4: payload_low_word
      uVar6: payload_high_word
      iVar5: is_nan
      DAT_0838ce00: mem_text_ephem_flags
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeSerialGet (vdbeaux.c): deserialize
      one serial-typed packed-record payload into a Mem. functions.csv's
      484-byte extent is exact: 121 ARM instructions from 0x0838cc1c
      through the final ldmia return at 0x0838cdfc; 0x0838ce00 is the
      following literal-pool word 0x00000102 (MEM_Str|MEM_Ephem), and
      the next function begins at 0x0838ce04. Binary-verified against
      osos.dec. Its jump table makes types 0/10/11 NULL; 1..=6 are
      1/2/3/4/6/8-byte signed big-endian integers; 7 is binary64,
      becoming NULL only when sqlite3IsNaN @ 0x0837cb44 says so; and
      8/9 are integer constants 0/1. A type >= 12 aliases buf through
      Mem.z (+0x14), stores n=(t-12)>>1 at +0x18, clears xDel (+0x20),
      and sets text|ephemeral 0x102 for odd t or blob|ephemeral 0x110
      for even t. The port uses the target's raw 0x28-byte Mem layout:
      host pointer words deliberately truncate to 32 bits at their ARM
      offsets, target pointers are complete. It is now the shipped
      serial_get default of vdbe_record_compare's
      VDBE_RECORD_COMPARE_OPS seam; the missing_ test mock is retained.
      Host tests use an independent jump-table reference model for every
      type 0..12, negative sign extension at every integer width, finite
      and NaN real bit patterns, and odd/even tail pointer/length/flag
      stamps. match.py structural review: original 121 instructions,
      Rust 159; both retain the serial-type jump-table split, fixed-width
      integer/real cases, NaN-to-NULL branch, and tail field stores.

  - address: 0x0838ce04
    ghidra: FUN_0838ce04
    name: vdbe_serial_put
    size: 188
    sig: "u32 vdbe_serial_put(u8 *dst, int dst_capacity, Mem *p_mem, int file_format)"
    module: sqlite/vdbe_serial_put
    status: ported
    renames:
      param_1: dst
      param_2: dst_capacity
      param_3: p_mem
      param_4: file_format
      uVar1: serial_type
      uVar2: payload_len
      uVar3: store_index
      uVar4: value_low_word
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeSerialPut (vdbeaux.c): serialize
      one Mem into its packed-record payload. functions.csv's 188-byte
      extent is exact: the final ldmia return is 0x0838cebc and
      sqlite3VdbeSerialType begins at 0x0838cec0; no literal pool.
      Binary scan finds one direct bl caller, 0x08388840 inside
      FUN_08386ef8's sqlite3VdbeExec/OP_MakeRecord loop. The
      sqlite3VdbeSerialType callee @ 0x0838cec0 is still unported and
      remains an explicit VDBE_SERIAL_PUT_OPS seam: target builds branch
      to retailOS and host builds require a test mock. Its serial-type
      dispatch writes types 1..=6 from Mem.u.i (+0x00) and type 7 from
      Mem.r (+0x08) in big-endian fixed-width form using ported
      vdbe_serial_type_len @ 0x0838cfe8; types 0/8/9/10/11 return zero;
      types >=12 copy Mem.n (+0x18) bytes from the raw 32-bit Mem.z
      (+0x14) through ported __rt_memcpy and, for MEM_Zero (0x800),
      append Mem.u.nZero zero bytes clamped to signed nBuf through
      ported memzero. Host tests mock every dispatch classification:
      1..=6, 7, 0/8/9/10/11 and both normal/clamped zero-tail >=12
      paths, comparing against an independent upstream model.

  - address: 0x0838cec0
    ghidra: FUN_0838cec0
    name: vdbe_serial_type
    size: 288
    sig: "u32 vdbe_serial_type(Mem *p_mem, int file_format)"
    module: sqlite/vdbe_serial_type
    status: ported
    renames:
      param_1: p_mem
      param_2: file_format
      uVar1: flags
      uVar2: value_high_word
      uVar3: magnitude_high_word
      uVar4: value_low_word
      bVar5: max_6byte_high_equal
      bVar6: max_6byte_low_equal
      bVar7: max_6byte_below
      DAT_0838cfe0: max_int16_threshold
      DAT_0838cfe4: max_int24_threshold
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeSerialType (vdbeaux.c): classify
      one Mem into its packed-record serial type. functions.csv's
      288-byte extent is exact: 72 instructions through the ldmia
      return at 0x0838cfdc, then the two literal-pool words at
      0x0838cfe0 (0x00007fff) and 0x0838cfe4 (0x007fffff)
      (binary-verified against osos.dec), then vdbe_serial_type_len's
      prologue at 0x0838cfe8. 3 bl call sites, all unconditional,
      binary-scanned from osos.dec: 0x08388680 and 0x083887d8 inside
      FUN_08386ef8's sqlite3VdbeExec/OP_MakeRecord loop, 0x0838ce1c
      inside vdbe_serial_put; no tail b. The Mem.flags (+0x1c)
      cascade runs MEM_Null (0x1) -> 0, MEM_Int (0x4) -> the integer
      arm, MEM_Real (0x8) -> 7, else the string/blob tail n*2 + 12 +
      ((flags & MEM_Str) >> 1) with Mem.n (+0x18) grown by the low
      word of Mem.u.nZero (+0x00) under MEM_Zero (0x800). The integer
      arm short-circuits exact 0/1 to types 8/9 when file_format >= 4
      (SIGNED blt skips; v + 8 via adds/adc, high half dead), then
      negates negative values (unconditional rsbs/rsc wrap, no
      upstream i < -MAX_6BYTE guard — the wrapped magnitude exceeds
      MAX_6BYTE and falls to type 6 anyway, host-test proven across
      the i64 domain) and walks the UNSIGNED ladder 0x7f / 0x7fff
      (pool word) / 0x7fffff (pool word) / 0x7fffffff / MAX_6BYTE =
      (0x8000 << 32) - 1 (the cmp high,#0x7fff / cmpeq low,
      #0xffffffff pair reusing the r6 literal) answering types 1..=6.
      Ghidra types the return ulonglong because the adds/adc pairs
      leave a high word in r1; all three call sites read r0 alone, so
      the port returns u32 (the narrowing vdbe_serial_put documents).
      The vdbe_serial_put VDBE_SERIAL_PUT_OPS seam is untouched (this
      port does not edit sibling files); wiring it to this classifier
      is a separate change. Host tests: NULL/INT/REAL/tail flag
      precedence, the 8/9 constants under file_format -1/0/1/3/4/5/
      100, every ladder rung +/-2 in both signs, i64::MIN/MAX, a
      4096-sample xorshift sweep of the i64 domain against an
      independent upstream model (including its -MAX_6BYTE guard),
      and the string/blob formula over length/zero-tail wrap edges.

  - address: 0x0838cfe8
    ghidra: FUN_0838cfe8
    name: vdbe_serial_type_len
    size: 24
    sig: "u32 vdbe_serial_type_len(u32 serial_type)"
    module: sqlite/vdbe_serial_type_len
    status: ported
    renames:
      param_1: serial_type
      uVar1: payload_len
      DAT_0838d000: a_size_base_ptr
    notes: >-
      Upstream SQLite 3.5.9's sqlite3VdbeSerialTypeLen (vdbeaux.c) —
      the payload byte length of one packed record field. functions.csv's
      24 bytes is exact: six instructions (cmp r0,#0xc; ldrcc r1,
      [0x838d000]; ldrbcc r0,[r1,r0]; subcs r0,r0,#0xc; movcs r0,r0,
      lsr #0x1; bx lr) plus the 4-byte literal-pool word at 0x0838d000,
      then the next function's prologue at 0x0838d004. 9 bl call
      sites, all unconditional, binary-scanned from osos.dec:
      0x083883bc, 0x083884a0, 0x08388688, 0x0838b534, 0x0838b5bc,
      0x0838c918 (inside vdbe_record_compare), 0x0838cac0, 0x0838ce38
      and 0x0838f5f0; no tail b. The pool word holds the table base
      pointer 0x088fce10 — a post-image address whose raw image bytes
      are unrelated, but through the +0xaed8 skew documented in
      sqlite/mod.rs the table sits at image offset 0x08907ce8 and reads
      00 01 02 03 04 06 08 08 00 00 00 00 (binary-verified against
      osos.dec), byte-for-byte upstream's aSize[]: type 0 NULL, 1..=4
      1/2/3/4-byte ints, 5 the 6-byte int, 6 the 8-byte int, 7 the
      8-byte float, 8/9 the constants 0/1, 10/11 reserved. Serial
      types >= 12 are the string/blob tail: even t a blob of
      (t-12)/2 bytes, odd t a text of (t-13)/2 — the single
      (t-12)>>1 the subcs/movcs pair encodes. The port keeps the
      unsigned lsr shape so bit 31 of the result stays clear (the
      property vdbe_record_compare's signed bgt length check relies
      on) and is the shipped default of vdbe_record_compare's
      VDBE_RECORD_COMPARE_OPS serial_type_len slot (the
      value_set_str/vdbe_set_num_cols flip pattern; the missing_
      panic stub is retained for tests needing an unconfigured
      callee). Host tests: the table against the recovered bytes,
      every serial type 0..12, the 12/13 zero-length boundary, an
      even/odd sweep 12..=4096 against both parities' formulas, and
      u32::MAX-1/u32::MAX staying unsigned.

  - address: 0x0838bc38
    ghidra: FUN_0838bc38
    name: mem_finalize
    size: 124
    sig: "int mem_finalize(void *value, void *func_def)"
    module: sqlite/mem_finalize
    status: ported
    renames:
      param_1: value
      param_2: func_def
    notes: >-
      Upstream SQLite's sqlite3VdbeMemFinalize - the aggregate-context
      finalize the extern release @ 0x0838c074 bl's with the FuncDef
      pointer at Mem+0x00; 2 bl call sites (the extern release's
      aggregate branch @ 0x0838c090 and the VDBE step machinery @
      0x0838a700 - both callers test the return == 1 for the error
      path, confirming the 0/1 contract). A NULL func_def or NULL
      xFinalize at FuncDef+0x18 returns 0 untouched (cmp r1,#0;
      ldrne r2,[r1,#0x18]; cmpne r2,#0; beq - xFinalize is loaded
      twice, once for the guard and once for the call). Otherwise a
      0x40-byte frame (sub sp,sp,#0x40) holds an old-style
      sqlite3_context with the result Mem embedded inline (upstream
      3.5.x layout: FuncDef *pFunc @ +0x00, Mem s @ +0x08, Mem *pMem
      @ +0x30, int isError @ +0x34): scratch.flags = 1/MEM_Null
      (strh @ctx+0x24), scratch.db = value->db @ Mem+0x10 (str
      @ctx+0x18), scratch.zMalloc = 0 (str @ctx+0x2c), ctx.pMem =
      value (str r4,[sp,#0x30]), ctx.isError = 0 (str @ctx+0x34),
      ctx.pFunc = func_def (str r1,[sp,#0x0]); xFinalize(ctx) is
      blx'd; value->zMalloc at +0x24 is freed raw (bl sqlite3_free @
      0x083906f4, here tracked_free); the 0x28-byte scratch Mem is
      memcpy'd over value (bl 0x08037df8, the ROM memcpy veneer, here
      the ported memcpy) - landing flags = 1 unless xFinalize set a
      result, which is what clears MEM_Agg for the re-entered extern
      release; the return is isError != 0 normalized by movne r0,#0x1.
      The original's compiler omitted upstream's memsets (only the
      seven stored fields are initialized); the port zero-fills the
      whole frame instead, and on 64-bit hosts additionally clears the
      upper half of the widened zMalloc field after the copy (cfg'd
      out on the ARM target). Shipped as the default agg_finalize slot
      of mem_extern_release's MEM_AGG_FINALIZE_OPS, replacing the
      missing_agg_finalize stub (which cleared the MEM_Agg/MEM_Dyn
      bits and returned 0 without invoking xFinalize). match.py
      structural: same NULL guard, 0x40 frame, seven context stores,
      blx xFinalize, ldr/free of zMalloc, 0x28-byte memcpy and the
      cmp/movne r0,#1 tail; LLVM emits its fp frame, a __aeabi_memclr4
      for the zero-fill, elides the dead zMalloc zero-store, and
      inlines tracked_free's NULL guard + stats-update prologue (the
      same accepted deviation as mem_release/value_free).

  - address: 0x08386e08
    ghidra: FUN_08386e08
    name: vdbe_context_aux_sweep
    size: 100
    sig: "void vdbe_context_aux_sweep(void *ctx, int exempt_mask)"
    module: sqlite/aux_sweep
    status: ported
    renames:
      param_1: ctx
      param_2: exempt_mask
      uVar3: index
      piVar4: record.arg
      pcVar2: record.destructor
    notes: >-
      Upstream SQLite's sqlite3VdbeDeleteAuxData loop, freestanding
      over a function context's aux-record array: owner pointer at +0
      (untouched), signed record count at +4, then 8-byte
      {arg, destructor} records from +8. 2 bl call sites (vdbe_free_p4
      @ 0x082cf3f4 with mask 0; the statement-exec path @ 0x08387b00
      with a live mask - Ghidra's C additionally attributes it to
      vdbe_change_p4's -7 branch, which shares those sites). Each slot
      with index <= 31 whose mask bit is set is exempt (`cmp r4,#0x1f;
      bgt` / `tst r7,r8, lsl r4` - indices past 31 cannot be
      exempted); a non-exempt slot with a non-NULL arg has its
      destructor, when non-NULL, blx'd WITH the arg in r0 (Ghidra
      shows `(*pcVar2)()` - the arg is loaded two instructions
      earlier), then the arg slot is zeroed. NULL-arg slots are
      skipped before the destructor word is even loaded. The count is
      reloaded every iteration (volatile in the port) so a destructor
      mutating it mid-sweep is honored. Wired as the default
      context_aux_cleanup slot of free_p4's FREE_P4_AUX_OPS. match.py
      structural (LLVM: tst r9,r4,lsr r6 instead of tst r7,r8,lsl r4,
      separate cmp/beq/blx instead of blxne, fp frame).
      vdbe_change_p4 still reaches this through its own VDBE_P4_OPS
      slot; flipping that default is a separate commit. match.py
      structural (LLVM emits a ldr-pc table instead of addls-pc,
      inlines tracked_free into the raw-free path, and calls the aux
      slots with blx).

  # ---- Case-insensitive compare (src/sqlite/stricmp.rs) ----
  - address: 0x08384fa0
    ghidra: FUN_08384fa0
    name: str_nicmp
    size: 96
    sig: "int str_nicmp(const char *left, const char *right, int n)"
    module: sqlite/stricmp
    status: ported
    notes: >-
      SQLite's sqlite3StrNICmp. 19 bl + 1 tail b call sites. str_icmp
      @ 0x08384f14 over at most n bytes, through the same
      sqlite3UpperToLower map. The count is decremented BEFORE the loop
      test (`subs r3,r2,#0; sub r2,r2,#1; ble`), so after the loop a
      negative count means the budget was exhausted, which is turned into
      a 0 result (`movlt r0,#0`). Faithful quirk - n = INT_MIN wraps the
      pre-decrement to INT_MAX, so that one value still compares instead
      of returning 0.

  # ---- Begin-write-operation marker (src/sqlite/begin_write_operation.rs) ----
  - address: 0x0837021c
    ghidra: FUN_0837021c
    name: begin_write_operation
    size: 124
    sig: "void begin_write_operation(Parse *parse, int set_statement, int i_db)"
    module: sqlite/begin_write_operation
    status: ported
    vars: {param_1: parse, param_2: set_statement, param_3: i_db}
    notes: >-
      SQLite's sqlite3BeginWriteOperation, the 3.5.x-era shape (works
      on pParse directly - no sqlite3ParseToplevel walk, no isWrite
      store). 17 bl call sites, zero predicated, verified by decoding
      every B/BL word in osos.dec. Extent verified from raw bytes: the
      next function starts at 0x08370298, so exactly 124 bytes as
      Ghidra reports. Per database i_db: fetch the Vdbe and bail when
      NULL, emit the schema-cookie verification, set bit i_db in
      Parse.writeMask (+0x100), then append OP_Statement i_db (opcode
      0x2a, this build's numbering) when set_statement is requested and
      Parse.nested (byte at +0x13) is zero. When i_db != 1 and the temp
      database is attached (db->aDb at +0x08, Db stride 0x18, pBt at
      +0x04 - the word at aDb+0x1c; the stride is corroborated by
      codeVerifySchema's iDb*24 indexing), the sequence repeats once
      for i_db = 1: upstream's tail recursion, folded into a loop by
      the firmware's compiler (movne r5,#1; bne). Callees:
      sqlite3VdbeAddOp1 @ 0x08386810 is ported and called directly;
      sqlite3CodeVerifySchema @ 0x083734bc is UNPORTED and rides the
      BEGIN_WRITE_OPS seam with a documented no-op default;
      sqlite3GetVdbe @ 0x0837acf4 also rides the seam because its
      ledger entry claims ported in sqlite/vdbe but the symbol exists
      on no branch - the entry was added as identified in dda6c20 and a
      later merge flipped the status without the code ever landing
      (flagged anomaly; the shipped stand-in returns Parse.pVdbe at
      +0x0c verbatim, exact whenever a statement is already under
      construction and matching the OOM end state otherwise, since the
      unported constructor's own default fails). The writeMask update
      uses checked_shl(i_db & 0xff) to match the ARM register-shift
      semantics (amounts 32..=255 yield 0) instead of Rust's
      overflowing shl. match.py shows the expected deviations: blx
      through the two seam slots instead of direct bl's, LLVM frame
      setup, and loop peeling of the temp-database second pass.

  # ---- Schema-cookie increment emitter (src/sqlite/change_cookie.rs) ----
  - address: 0x08372e54
    ghidra: FUN_08372e54
    name: change_cookie
    size: 108
    sig: "void change_cookie(Parse *parse, int database_index)"
    module: sqlite/change_cookie
    status: ported
    renames:
      param_1: parse
      param_2: database_index
    notes: >-
      SQLite 3.5.9 sqlite3ChangeCookie. Raw ARM establishes the true
      108-byte extent 0x08372e54..0x08372ebc: a separate sibling starts
      with push {r4-r6,lr} at 0x08372ec0. Binary decoding every ARM B/BL
      word finds 9 direct BL callers, not Ghidra's count alone: eight
      unconditional (0x0836f040, 0x083745e4, 0x08375634, 0x08375b98,
      0x08375dfc, 0x083760c0, 0x08379448, 0x0838d5e4) and one BLNE
      (0x0836eb78); that sole conditional call is caller-gated, as the
      callee has no guard. Algorithm: get_temp_reg(parse), load
      db->aDb[database_index].pSchema->schema_cookie, wrap-add one,
      emit OP_Integer (0x2f) value,temp_reg, emit OP_SetCookie (4)
      database_index,0,temp_reg, then tail-branch to release_temp_reg.
      Ghidra's four-argument void C misses the tail call; the raw ARM
      calling sequence and SQLite 3.5.9 build.c identify it decisively.
      Deliberate host-only layout adaptation: Parse, sqlite3, and Db links
      are u32 words in repr(C) structures, preserving every target offset
      despite host pointer widening; target behavior is unchanged. Host
      tests cover selected-db emission and cookie/register wrap with a full
      free-list.
      match.py (--size 108, worktree archive): the same get-temp/add-op2/
      add-op3/release call sequence and schema `+1`; expected differences
      are LLVM's frame, register scheduling, and relocation placeholders.
  # ---- B-tree lock counter (src/sqlite/btree_lock.rs) ----
  - address: 0x0837118c
    ghidra: FUN_0837118c
    name: btree_enter
    size: 28
    sig: "void btree_enter(Btree *p)"
    module: sqlite/btree_lock
    status: ported
    notes: >-
      SQLite's sqlite3BtreeEnter. 36 bl call sites. Non-shared handles
      return immediately; otherwise `p->wantToLock++`. Btree layout -
      db +0x00, pBt +0x04, inTrans +0x08, sharable +0x09, locked +0x0a,
      wantToLock +0x0c (SQLite's own field order). SQLITE_THREADSAFE is
      off in this build, so the mutex acquisition is compiled away and
      the body is only the counter. The original leaves p->locked in r0
      on the sharable path and the untouched p on the other - an
      inconsistent, dead value (the C function is void, and none of the
      36 call sites consume r0); ported as void.

  - address: 0x08371da4
    ghidra: FUN_08371da4
    name: btree_leave
    size: 36
    sig: "void btree_leave(Btree *p)"
    module: sqlite/btree_lock
    status: ported
    notes: >-
      SQLite's sqlite3BtreeLeave. 41 bl + 1 tail b call sites. Non-shared
      handles return immediately; otherwise `--p->wantToLock`, and
      p->locked (+0x0a) is cleared only when the counter reaches EXACTLY
      zero - an over-released handle keeps counting into negatives with
      the flag still set (the original's assert(wantToLock>0) is
      compiled out).

  # ---- B-tree cursor position restoration (src/sqlite/restore_cursor_position.rs) ----
  - address: 0x08372ae0
    ghidra: FUN_08372ae0
    name: btree_restore_cursor_position
    size: 112
    sig: "i32 btree_restore_cursor_position(BtCursor *cursor)"
    module: sqlite/restore_cursor_position
    status: ported
    renames:
      param_1/r0/r4: cursor
      iVar1/r0/r5: rc
      FUN_08371e54: sqlite3BtreeMoveto (BTREE_MOVETO_OPS.moveto seam)
      FUN_083906f4: tracked_free
    notes: >-
      SQLite 3.5.x sqlite3BtreeRestoreOrClearCursorPosition. Raw ARM ends
      at the separate sibling's push @ 0x08372b50, confirming the
      0x08372ae0..0x08372b50 112-byte extent and no literal pool. Binary
      decoding every ARM B/BL word in osos.dec found exactly eight direct
      BL callers: seven unconditional at 0x08370e24, 0x08370e8c,
      0x08370f74, 0x08371c68, 0x08371ce4, 0x083722a8, and 0x08372934,
      plus caller-gated BLCS at 0x08371314; no tail B. FAULT eState 3
      returns cursor +0x50 saved rc untouched. An isIncrblobHandle byte
      at +0x54 returns SQLITE_ABORT (4), likewise untouched. Otherwise it
      stores eState +0x43 = INVALID (0), calls unported sqlite3BtreeMoveto
      @ 0x08371e54 with cursor, saved key +0x44, zero, result +0x50,
      saved 64-bit nKey +0x48, zero, and result +0x50; only a zero result
      frees then clears saved key. The moveto ABI is raw-byte verified but
      remains a volatile BTREE_MOVETO_OPS seam because its target is
      unported. Target pointer fields are modeled as u32 loads for host
      layout parity; this is the only deliberate deviation. Host tests
      cover fault precedence, incrblob abort, exact movement arguments,
      successful key clearing, and failure retention.

  # ---- B-tree cursor data/key-size accessors (src/sqlite/data_size.rs) ----
  - address: 0x08370e6c
    ghidra: FUN_08370e6c
    name: btree_data_size
    size: 104
    sig: "i32 btree_data_size(cursor: *mut BtCursor, out: *mut u32)"
    module: sqlite/data_size
    status: ported
    vars:
      param_1/r0/r4: cursor
      param_2/r1/r5: out
      r6: rc
      FUN_08372ae0: btree_restore_cursor_position (direct ported callee)
      FUN_083727c8: btree_parse_cell (direct ported callee)
    notes: >-
      Verified against osos.asm 0x08370e6c..0x08370ed0 (104 bytes) and
      decomp/c/033/08370e6c_FUN_08370e6c.c. This is SQLite 3.5.x
      sqlite3BtreeDataSize, NOT sqlite3BtreeKeySize: `ldr r0,[r4,#0x30]`
      / `str r0,[r5]` transfers one u32 from cursor +0x30 = cached
      CellInfo +0x10 = nData (the leaf payload varint); its ported i64
      KeySize sibling at 0x08371cc4 does `ldrd r0,r1,[r4,#0x28]`
      (CellInfo.nKey at +0x08) and `strd` to its eight-byte out-param.
      The two call sites prove the split:
      0x083881e4 is vdbeExec OP_Column's non-index branch (the isIndex
      branch immediately beside it calls 0x08371cc4), and 0x08389a20 is
      OP_RowData's data-size/length-limit path.
      Fields, each verified by the target's loads/stores: BtCursor +0x18
      pPage (u32 target MemPage pointer, ldrd low word); +0x1c idx (u32,
      ldrd high word); +0x20 cached CellInfo base (r2 = cursor +0x20);
      +0x30 = CellInfo +0x10 nData (u32, the returned value); +0x3e =
      CellInfo +0x1e nSize (u16, `ldrh` cache-valid flag — zero causes
      parse); +0x42 validNKey (u8, `strb #1` only after parse); +0x43
      eState (u8: INVALID 0, VALID 1, REQUIRESEEK 2, FAULT 3); out +0x00
      receives nData with `str`. Algorithm: state < 2 executes
      `movcc r0,#0; bcc`, so it skips validation but DOES NOT return
      from the function; on rc == 0 it re-reads state, writes out=0 for
      INVALID, otherwise parses lazily on nSize==0 and returns cached
      nData. A nonzero validator rc branches directly to the epilogue,
      leaving out untouched.
      Callees: sqlite3BtreeRestoreOrClearCursorPosition @ 0x08372ae0 is
      now the direct ported btree_restore_cursor_position callee; its
      unported sqlite3BtreeMoveto dependency @ 0x08371e54 remains behind
      that module's documented BTREE_MOVETO_OPS seam.
      The cache-miss parser is the JUST-PORTED sqlite3BtreeParseCell @
      0x083727c8, called directly with (pPage, idx, cursor +0x20) per
      the ported-callee direct-call house precedent (not via its seam).
      Host tests cover skipped validation, movement failure/no output write,
      post-restore INVALID handling, cache hits, and cache misses. match.py
      (exit 1, diff expected): target has 26 instructions and the port 53;
      both have the same state gate, rc short-circuit, state re-read, nSize
      cache gate, parse-on-miss/validNKey store, and nData out-store.
      Expected differences are LLVM frame/register scheduling, the direct
      ported validator's unresolved `bl 0` relocation, and LLVM expanding
      raw u16/u32 reads to little-endian byte loads/orrs on ARMv5 rather
      than the original's aligned ldrh/ldrd/ldr.

  - address: 0x08371cc4
    ghidra: FUN_08371cc4
    name: btree_key_size
    size: 112
    sig: "i32 btree_key_size(cursor: *mut BtCursor, out: *mut i64)"
    module: sqlite/data_size
    status: ported
    vars:
      param_1/r0/r4: cursor
      param_2/r1/r5: out
      iVar1/r6: rc
      uVar2/r1: n_key_high_word
      FUN_08372ae0: btree_restore_cursor_position (direct ported callee)
      FUN_083727c8: btree_parse_cell (direct ported callee)
    notes: >-
      SQLite 3.5.x sqlite3BtreeKeySize, verified against
      decomp/c/033/08371cc4_FUN_08371cc4.c and the 112-byte function
      range. This is the 64-bit sibling of sqlite3BtreeDataSize:
      `ldrd r0,r1,[r4,#0x28]` loads cached CellInfo.nKey (+0x08; cursor
      +0x28) and `strd r0,r1,[r5]` writes its target-little-endian low
      and high words to the i64 out-parameter. eState +0x43 below 2
      skips validation; otherwise the direct ported
      sqlite3BtreeRestoreOrClearCursorPosition @ 0x08372ae0 runs. A
      nonzero rc returns immediately without touching either output word.
      On success, eState is re-read: INVALID writes i64 zero; otherwise
      CellInfo.nSize at +0x3e is the cache flag, and a zero causes the
      direct ported sqlite3BtreeParseCell @ 0x083727c8 call with
      (pPage +0x18, idx +0x1c, CellInfo +0x20), followed by
      validNKey +0x42 = 1. Host tests cover invalid-state zeroing,
      movement error propagation and no-write behavior, post-restore
      INVALID handling, signed pair cache hits, and parse/cache-state
      updates. match.py diff is expected because raw layout reads remain
      unaligned little-endian accesses.

  # ---- B-tree cell size query (src/sqlite/cell_size.rs) ----
  - address: 0x082c0a50
    ghidra: FUN_082c0a50
    name: cell_size_ptr
    size: 28
    sig: "u16 cell_size_ptr(const MemPage *page, const u8 *cell)"
    module: sqlite/cell_size
    status: ported
    vars: {param_1: page, param_2: cell}
    notes: >-
      SQLite 3.5.x cellSizePtr (btree.c): parse the cell and return
      CellInfo.nSize. The whole body is one call and one halfword load:
      stack CellInfo (0x24-byte frame; struct is 0x20), bl
      sqlite3BtreeParseCellPtr @ 0x083727ec, `ldrh r0,[sp,#0x1e]` — the
      +0x1e (nSize) offset is from the disassembly, NOT Ghidra's local_a
      arithmetic. 11 bl call sites: 0x082b6090, 0x082b6128, 0x082b6698,
      0x082b68a0, 0x082b6af4 (the b-tree insert/drop/defragment cluster),
      0x082c2dcc, 0x082c5ec0, 0x08371064, 0x083710ac, 0x08371154,
      0x08371768. The callee (identified, not yet ported) is SQLite's
      parseCellPtr: MemPage fields leaf +0x07 / intKey +0x03 /
      childPtrSize +0x09 / maxLocal +0x0a / minLocal +0x0c / pBt +0x40
      (whose +0x1e is usableSize), varint readers 0x0837ac30
      (sqlite3GetVarint) and 0x0837aab0 (sqlite3GetVarint32), overflow
      divisor via __rt_udiv @ 0x08036f14. CellInfo as it writes it:
      pCell +0x00, nKey/nData pair +0x08/+0x0c, payload u32s +0x10/+0x14,
      then u16s nHeader +0x18, nLocal +0x1a, iOverflow +0x1c, nSize +0x1e.
      The parse call goes through the BTREE_CELL_OPS dispatch boundary
      (house pattern). Follow-up: 0x083727ec is now ported
      (sqlite/parse_cell.rs) and is the shipped parse_cell default; the
      zero-fill stand-in remains for tests and the two unported varint
      readers (0x0837ac30/0x0837aab0) joined the same seam static.
      Nothing in the firmware consumes the port yet (no hooks.yaml
      entry).
      Deviations: the port zero-initializes its 0x20-byte buffer (the
      original's stack slot is uninitialized) so the default slot's
      result is defined. match.py: same stack CellInfo and the identical
      `ldrh r0,[sp,#30]` tail; expected frame diff — LLVM fp frame, the
      eight zero-init stores, and blx through the ops slot instead of the
      direct bl (callee unported). Host tests:
      returns_the_n_size_halfword_at_0x1e,
      passes_page_and_cell_through_verbatim,
      result_tracks_only_the_0x1e_field, zero_fill_stand_in_reports_zero,
      shipped_default_is_the_ported_parser.
      Follow-up: both varint readers are now ported — 0x0837ac30
      (sqlite/get_varint.rs) and 0x0837aab0 (sqlite/get_varint64.rs)
      are their slots' shipped defaults; the get_varint32 slot's type
      was corrected to *mut u64 for 0x0837aab0's true u64 out-param.

  # ---- B-tree cell parser, by index (src/sqlite/parse_cell.rs) ----
  - address: 0x083727c8
    ghidra: FUN_083727c8
    name: btree_parse_cell
    size: 36
    sig: "void btree_parse_cell(const MemPage *page, u32 index, CellInfo *info)"
    module: sqlite/parse_cell
    status: ported
    vars: {param_1: page, param_2: index, param_3: info}
    notes: >-
      SQLite 3.5.x sqlite3BtreeParseCell: the index-based cell-parse
      entry — translate a cell index to the cell pointer, then parse.
      5 bl call sites: 0x082b29d4, 0x082cdf08, 0x08370eb8, 0x08371d18,
      0x08371d88. Algorithm (9 instructions, verified against
      osos.asm): r3 = MemPage +0x0e u16 (cellOffset — the cell-pointer
      array's offset within the page data) + index*2; r1 = MemPage
      +0x44 (aData — the page data base); the array entry is a
      BIG-ENDIAN u16 cell offset (ldrb pair + `orr r3,r12,r3,lsl #0x8`:
      data[entry] is the high byte); r1 = aData + offset = cell
      pointer; then FALLS THROUGH (the `mov r0,r0` at 0x083727e8 is ADS
      padding, not logic) into 0x083727ec, the ported
      btree_parse_cell_ptr — the two original functions share one body.
      WARNING — functions.csv/Ghidra mis-size: the 36-byte extent and
      the standalone C body in
      decomp/c/033/083727c8_FUN_083727c8.c are misleading; Ghidra
      decompiled THROUGH the fall-through, so that file shows the whole
      parse body that actually lives at 0x083727ec. This function's
      true extent ends at the fall-through. The port calls the ported
      btree_parse_cell_ptr export directly, NOT through the
      BTREE_CELL_OPS parse_cell slot: the house precedent is direct
      calls for ported callees (expr_new -> db_malloc_zero,
      btree_parse_cell_ptr -> __rt_udivmod) with the seam reserved for
      identified-but-unported callees, and the original has no call
      here at all — the fall-through IS a direct hand-off of
      (page, cell, info) in r0/r1/r2. MemPage field names per upstream
      SQLite 3.5.x (cellOffset +0x0e, aData +0x44), now documented in
      the parse_cell.rs constants. Deviation: the +0x0e/+0x44 field
      reads go through the module's unaligned accessors (the port's
      *const u8 carries no alignment promise; LLVM lowers them to byte
      loads where the original has aligned ldrh/ldr — same house
      defensiveness as btree_parse_cell_ptr's CellInfo writes).
      match.py (--size 36): same structure — cellOffset + index*2 add
      (LLVM schedules it after the aData load), the big-endian ldrb
      pair + orr lsl #8 decode, aData + offset sum, then a TAIL CALL
      (pop; b btree_parse_cell_ptr) — the exact fall-through shape;
      expected diffs are the fp frame and the byte-expanded unaligned
      field reads. Host tests:
      cell_pointer_array_entry_is_decoded_big_endian,
      index_steps_the_cell_pointer_array_by_two,
      page_cell_and_info_reach_the_parser_verbatim.

  # ---- B-tree cell parser (src/sqlite/parse_cell.rs) ----
  - address: 0x083727ec
    ghidra: FUN_083727ec
    name: btree_parse_cell_ptr
    size: 296
    sig: "void btree_parse_cell_ptr(const MemPage *page, const u8 *cell, CellInfo *info)"
    module: sqlite/parse_cell
    status: ported
    vars: {param_1: page, param_2: cell, param_3: info}
    notes: >-
      SQLite 3.5.x sqlite3BtreeParseCellPtr (btree.c's parseCellPtr):
      decode one b-tree cell's header into the 0x20-byte CellInfo
      (layout in the cell_size_ptr entry above). 9 bl call sites:
      0x082b6624, 0x082b6914, 0x082c0a5c (the ported cell_size_ptr),
      0x082c2ad8, 0x082c3468, 0x082cdfe0, 0x082d6924, 0x082d98c4,
      0x082e87b0. MemPage fields read: intKey +0x03, leaf +0x07,
      childPtrSize +0x09, maxLocal +0x0a (u16), minLocal +0x0c (u16),
      pBt +0x40 (u32; BtShared.usableSize at its +0x1e). Algorithm,
      verified against osos.asm (Ghidra's decompile obscures the
      __rt_udiv remainder as extraout_r1 and flips the signed
      strhgt/strhle): info.pCell = cell; header cursor starts at
      childPtrSize; leaf pages decode the payload varint — first byte
      < 0x80 is inlined (value = byte, len 1), multi-byte calls
      sqlite3GetVarint @ 0x0837ac30 — internal pages skip it (0);
      table pages (intKey != 0) decode the rowid varint ALWAYS through
      sqlite3GetVarint32 @ 0x0837aab0 (no inline fast path) into +0x08,
      +0x0c untouched; index pages decode the nData varint through the
      same inline/GetVarint pair into +0x08, zero +0x0c, and add nData
      to the payload; +0x10 = payload varint, +0x14 = total payload,
      +0x18 = header length. nPayload <= maxLocal: nLocal = nPayload,
      iOverflow = 0, nSize = max(nPayload + header, 4) (signed movlt
      clamp). Overflow: nLocal = minLocal + (nPayload - minLocal) %
      (usableSize - 4) via __rt_udiv @ 0x08036f14 (the original uses
      the r1 remainder, not the r0 quotient), collapsed to minLocal
      when the surplus exceeds maxLocal; iOverflow = nLocal + header,
      nSize = iOverflow + 4. Callees: sqlite3GetVarint 0x0837ac30 and
      sqlite3GetVarint32 0x0837aab0 are identified but unported — they
      ride the BTREE_CELL_OPS seam (same house static cell_size_ptr
      dispatches through; zero-writing stand-in defaults returning
      length 1); __rt_udiv 0x08036f14 is ported (runtime/rt_div) and is
      called as __rt_udivmod for the remainder, which gained
      #[inline(never)] so the bl call boundary survives for match.py
      (the __rt_udiv precedent). Deviations: CellInfo writes go through
      unaligned accessors (the port's *mut u8 carries no alignment
      promise; LLVM lowers them to byte stores where the original has
      aligned str/strh — the shipped caller's stack CellInfo is
      aligned, so this is host-defensiveness only), and info.pCell
      stores the 32-bit pointer value (host fixtures live below 4 GiB
      to round-trip). match.py (--size 296): same branch skeleton —
      leaf fast path (LLVM: ldrsb/cmn/ble sign trick for the < 0x80
      test), blx through the seam slots where the original has direct
      bl 0x0837ac30/0x0837aab0 (unported), bl __rt_udivmod for the
      original's bl 0x08036f14, unsigned-equivalent compares (bls/movhi
      for bcc/strhgt-strhle, movls for the movlt floor — inputs are
      u16-bounded so signed/unsigned coincide), strb-expanded CellInfo
      stores. Host tests:
      int_key_internal_cell_is_rowid_varint_only,
      table_leaf_cell_single_byte_varints_skip_the_seam,
      table_leaf_multi_byte_payload_varint_calls_get_varint,
      index_leaf_cell_adds_n_data_to_the_payload,
      index_internal_cell_skips_the_payload_varint,
      payload_at_max_local_stays_local,
      overflow_collapses_to_min_local_when_surplus_exceeds_max_local,
      overflow_keeps_the_surplus_when_it_fits_max_local,
      n_size_is_clamped_to_four,
      n_size_lands_at_0x1e_where_cell_size_ptr_reads.
      Follow-up: 0x0837aab0 is now ported (sqlite/get_varint64.rs) and
      is the shipped get_varint32 slot default; the slot type was
      corrected to *mut u64 (the original's paired stores) and the
      rowid read now writes the hi word at CellInfo+0x0c exactly like
      the original's callee.

  # ---- Auto-vacuum pointer-map page locator (src/sqlite/ptrmap_pageno.rs) ----
  - address: 0x082e866c
    ghidra: FUN_082e866c
    name: ptrmap_pageno
    size: 80
    sig: "u32 ptrmap_pageno(const BtShared *bt, u32 pgno)"
    module: sqlite/ptrmap_pageno
    status: ported
    renames:
      param_1: bt
      param_2: pgno
    notes: >-
      SQLite 3.5.x ptrmapPageno (btree.c): locate the pointer-map page
      whose 5-byte entries (type byte + big-endian u32, cf. ptrmapGet @
      0x082e85d8 and ptrmapPut @ 0x082e86bc) cover database page pgno
      of the auto-vacuum b-tree. Ghidra's 80-byte extent is exact —
      FUN_082e86bc begins with its own push {r3-r9,lr} at +0x50, no
      swallowed sibling, no literal pool. 16 genuine bl call sites
      (binary-scanned, 0 predicated): 0x082b3834, 0x082b58f0,
      0x082b5944, 0x082b5994, 0x082bd9d8, 0x082bde48, 0x082ce068,
      0x082d088c, 0x082d4cdc, 0x082d4e70, 0x082e85ec, 0x082e861c,
      0x082e86e0, 0x082e8710, 0x08371a0c, 0x08371a50; a 17th decode at
      0x088f9b78 is a blge false positive in a data region (ASCII iPod
      adjacent). Algorithm (raw words; Ghidra's C agrees): per_map =
      BtShared.usableSize(+0x1e u16)/5 + 1 via __rt_udiv @ 0x08036f14;
      map_index = (pgno - 2 wrapping)/per_map; ret = per_map*map_index
      + 2 (the map page HEADS its own group); if ret ==
      PENDING_BYTE(0x40000000)/BtShared.pageSize(+0x1c u16) + 1 then
      ret++. All three divides are unsigned __rt_udiv, quotient only,
      called directly per house precedent (its inline(never) keeps the
      three bl boundaries for match.py); mul/sub/add wrap mod 2^32
      exactly like the ARM. No NULL guard and no pgno < 2 early-out —
      the upstream check is absent in this build. Deviations: none
      structural; aligned halfword reads match the original's ldrh.
      match.py structural: same three bl __rt_udiv, mul, +2, compare
      and conditional bump; LLVM folds the mul+add into mla and the
      bump into moveq r1,#3 / mla. Host tests (5): small-page group
      boundaries (map page heads its group), reserved-region span
      shrink, pending-byte-page bump (exact divisor geometry:
      pageSize 0x8000 / usable 1080 / map index 151), wrapping pgno
      below 2, reference-model sweep over span boundaries and u32
      extremes.

  # ---- Auto-vacuum pointer-map writer (src/sqlite/ptrmap_put.rs) ----
  - address: 0x082e86bc
    ghidra: FUN_082e86bc
    name: ptrmap_put
    size: 184
    sig: "u32 ptrmap_put(BtShared *bt, u32 pgno, u32 map_type, u32 parent_pgno)"
    module: sqlite/ptrmap_put
    status: ported
    renames:
      param_1: bt
      param_2: pgno
      param_3: map_type
      param_4: parent_pgno
      local_20: map_page
      iVar1: status
      iVar3: ptrmap_page
      iVar4: stored_parent_pgno
      iVar5: entry_offset
    notes: >-
      SQLite 3.5.x ptrmapPut (btree.c). Raw extent is exactly 184 bytes:
      `push {r3-r9,lr}` at 0x082e86bc through the branch at 0x082e8770;
      an independent `push {r4,lr}` begins sibling FUN_082e8774. Binary
      decoding finds 11 genuine direct callers, all plain unconditional bl
      (0 predicated): 0x082b5c64, 0x082b69bc, 0x082bdb20, 0x082ce0dc,
      0x082cf48c, 0x082d6978, 0x082e8804, 0x083677f4, 0x0836783c,
      0x08368eec, 0x08368f34. Return SQLITE_CORRUPT (11) if pgno is zero;
      otherwise resolve the pointer-map page at ptrmap_pageno(bt, pgno),
      map entry (pgno - ptrmap_page - 1) * 5, and only dirty/write it if
      its byte type or big-endian parent differs. The raw bleq at 0x082e8764
      gates both stores on the zero return from FUN_0837ef64, but release at
      0x0837ee98 follows unconditionally after every successful resolve.
      Ported callees: ptrmap_pageno (0x082e866c), load_be32 (0x0837a158),
      store_be32 (0x083816cc), release_object (0x0837ee98), and the leased
      resolver (0x0837dc38). FUN_0837ef64 remains retailOS-owned and is
      called directly only on target; host tests inject it and the resolver
      as test boundaries. Host tests: zero page early return, resolver
      failure avoids release, equal entry skips write, dirty failure preserves
      data but releases, and an eType above 255 proves ldrb compares all 32
      input bits before strb truncates.

  # ---- B-tree varint decoder (src/sqlite/get_varint.rs) ----
  - address: 0x0837ac30
    ghidra: FUN_0837ac30
    name: get_varint
    size: 192
    sig: "u8 sqlite3GetVarint(const u8 *p, u32 *v) [asm out-param; the seam slot it ships on widens the store to u64]"
    module: sqlite/get_varint
    status: ported
    vars: {param_1: p, param_2: out, param_3: '[invented — untouched stack slot reused by the tail call]', param_4: '[invented — same]', uVar3: a, uVar4: b1_accum, bVar1: b3, local_10: tail_low_word, DAT_0837acf0: PAIR_MASK}
    notes: >-
      SQLite 3.5.x sqlite3GetVarint (the repo's established
      identification — see the naming caveat below): the varint reader
      the b-tree layer is built on. 13 bl call sites: 0x08371ff8
      (FUN_08371e54), 0x08372834/0x08372880 (the ported
      btree_parse_cell_ptr), 0x083882e0/0x083883a4 (FUN_08386ef8),
      0x0838b504/0x0838b52c (FUN_0838b478), 0x0838b590/0x0838b5b4
      (FUN_0838b570), 0x0838c8d0/0x0838c900 (FUN_0838c87c),
      0x0838ca78/0x0838caa8 (FUN_0838c9f0). Algorithm, verified
      instruction-by-instruction against osos.asm (Ghidra invents
      param_3/param_4 from the stack slots the tail call reuses):
      big-endian base-128, cases unrolled shortest-first, length
      returned. NO one-byte case — the function reads p[0] and p[1]
      unconditionally and tests p[1]'s high bit; ALL 13 call sites
      guard with `cmp r0,#0x80; bcc` and keep the single byte inline
      (value = byte, len 1), so the export's contract, like the
      original's, is "p[0] has the continuation bit set". 2 bytes:
      v = p[1] | (p[0]&0x7f)<<7. 3: a = p[2]|p[0]<<14, v = (a&mask) |
      (p[1]&0x7f)<<7. 4: b = p[3]|p[1]<<14, v = (b&mask) |
      ((a&mask)<<7). mask = literal pool @ 0x0837acf0 = 0x001FC07F =
      (0x7f<<14)|0x7f, confirmed in osos.dec. 5: c = p[4]|a<<14,
      v = (c & ~0x0FE00000 & ~0x3F80) | ((b & ~0x0FE00000 & ~0x3F80)
      << 7) — the bic pairs strip each raw byte's continuation bit out
      of the shifted accumulators; the top group's bits above the low
      32 shift out, i.e. the u32 out-param truncates a 5-byte (35-bit)
      varint to its low word. 6+ (p[4] high bit set): rewinds to p
      (`sub r0,r1,#0x4`) and tail-calls the u64 decoder @ 0x0837aab0
      (identified, unported), copies only the LOW word of the callee's
      stack u64 into the out-param, returns the callee's byte count.
      The port inlines the tail's equivalent (decode_tail),
      group-by-group verified against 0x0837aab0's disassembly — 7-bit
      accumulation, 9th byte contributes all 8 bits (v<<8 | p[8]); an
      independent register-level Python simulation of both functions'
      exact u32 ops matched the port on 400k random 2..=9-byte inputs
      (lengths and low words everywhere, hi=0 for len<=5, full u64
      tail), sidestepping the classic Ghidra mis-decompile of the
      9-byte path. Naming caveat: this function's out-param is a u32
      (single str per case) and 0x0837aab0's is a u64 (paired
      stores), i.e. by upstream roles 0x0837ac30 is the truncating
      getter and 0x0837aab0 the full 64-bit one — the repo's docs
      (parse_cell.rs/cell_size.rs, kept for continuity) attach the
      upstream names the other way around; the seam SLOT-to-ADDRESS
      mapping is correct either way. Seam-default promotion (the
      vtable_slot_50_dispatch precedent): the export replaced
      missing_get_varint as DEFAULT_BTREE_CELL_OPS.get_varint;
      callers still reach it only through the slot
      (parse_cell::read_varint_fast truncates the u64 with `as u32`,
      exactly the original's u32 store); the zero stand-in remains
      for tests. Deviations: the slot's out-param is u64 where the
      original's is u32 — the port writes hi=0 for 2..=5-byte cases
      (bit-identical to the zero-extended u32 store, 5-byte
      truncation included) and the FULL u64 for the 6..=9 tail (the
      value the original computed in its callee and discarded the hi
      word of; low word bit-identical). match.py (--size 192):
      original 48 insns vs 108 — same shortest-first cascade with the
      same masks (0x1fc07f pair mask rebuilt as orr 0x7f|0x1fc000,
      the 0xfe00000/0x3f80 bic immediates) and the same group shifts;
      expected LLVM shape diffs: per-case epilogues instead of
      predicated blocks + shared tail, ldrsb/asrs/bmi sign tests for
      `tst #0x80`, strd r2,[r1] with r3=0 for the u64 slot store, and
      the tail inlined (no bl 0x0837aab0 — callee unported). Host
      tests:
      round_trips_every_encoded_length_against_the_reference_encoder,
      decodes_two_through_nine_byte_varints_directly,
      one_byte_varints_are_the_callers_inline_fast_path,
      boundary_values, ninth_byte_contributes_all_eight_bits,
      five_byte_varints_keep_only_the_low_word_like_the_original,
      bytes_consumed_stops_at_the_first_clean_high_bit,
      reads_no_byte_past_the_varint_end (PROT_NONE guard page),
      shipped_seam_default_is_this_port.
      Follow-up: 0x0837aab0 is now ported (sqlite/get_varint64.rs) and
      is the shipped default of the get_varint32 seam slot; this port's
      decode_tail remains inlined by choice and decodes identically.

  # ---- 64-bit b-tree varint decoder (src/sqlite/get_varint64.rs) ----
  - address: 0x0837aab0
    ghidra: FUN_0837aab0
    name: get_varint64
    size: 380
    sig: "u8 sqlite3GetVarint(const u8 *p, u64 *v) [asm out-param: paired lo/hi stores at [r1,#0]/[r1,#4] in every exit]"
    module: sqlite/get_varint64
    status: ported
    vars: {param_1: p, param_2: out, bVar1: b_next, bVar2: b7_raw, bVar3: cont_clear, uVar4: acc_a, uVar5: acc_b, uVar6: acc_c, DAT_0837ac2c: PAIR_MASK}
    notes: >-
      SQLite 3.5.x's full-width sqlite3GetVarint — the 64-bit varint
      decoder behind every table b-tree rowid read and the tail of the
      truncating u32 getter. 3 bl call sites: 0x08372008 (FUN_08371e54,
      reads the result back with ldrd r0,r1,[sp,#0x8] — a full u64),
      0x08372858 (the ported btree_parse_cell_ptr's rowid read,
      out-pointer = CellInfo+0x08 so the hi word lands at +0x0c),
      0x0837ace0 (the tail call from the ported get_varint @
      0x0837ac30, which keeps only the low word). NAMING INVERSION vs
      upstream, documented in the module header: upstream sqlite
      attaches sqlite3GetVarint to the u64 decoder (THIS function) and
      sqlite3GetVarint32 to the truncating u32 one (0x0837ac30); the
      repo's established naming has them swapped (0x0837ac30 was ported
      first as get_varint), so this port is get_varint64 and the
      BTREE_CELL_OPS seam slot it ships on keeps its established
      (upstream-inverted) name get_varint32 — with its type corrected
      from *mut u32 to *mut u64, the original's true signature
      (paired str/strd stores, Ghidra's param_2[1] hi-word writes).
      Algorithm, verified instruction-by-instruction against osos.asm:
      big-endian base-128, cases unrolled shortest-first, length
      returned. Unlike 0x0837ac30 there IS a one-byte case (v = p[0],
      len 1 — the rowid call site has no inline fast path). Each case
      k reads exactly p[0..k] and tests p[k-1]'s continuation bit.
      Cases 1..=8 accumulate 7-bit groups most-significant first
      (v = (v<<7) | (p[i]&0x7f)); the 9th byte contributes all 8 bits
      (v = (v<<8) | p[8] — the original re-reads p[4] via
      ldrb r2,[r2,#-0x4] to rebuild the hi word's top group). The
      original computes in a u32 lo/hi register pair (literal pool @
      0x0837ac2c = 0x001FC07F, the same PAIR_MASK as the sibling's @
      0x0837acf0; bic pairs 0x0FE00000/0x3F80 strip continuation bits
      from shifted accumulators); its paired stores equal
      (v as u32, (v>>32) as u32) of the port's plain u64 accumulation
      for every case — the masks drop exactly the bits the group
      accumulation never sets, and 7-bit-group bits above bit 31 fall
      off the 32-bit store identically (hand-checked for the 5/7/8/9
      cases; the sibling's decode_tail shares this function's shifts
      and was verified the same way). Unlike the u32 sibling nothing
      truncates: hi = v>>32 from the 5-byte case up. Seam-default
      promotion (the get_varint/cell_size_ptr precedent): the export
      replaced missing_get_varint32 as DEFAULT_BTREE_CELL_OPS.
      get_varint32; callers still reach it only through the slot. The
      parse_cell rowid read now decodes into a u64 and writes BOTH
      words like the original (lo at CellInfo+0x08, hi at +0x0c — the
      previous port left +0x0c untouched on a u32-slot reading of the
      callee); its test now asserts the +0x0c hi-word store. The zero
      stand-in remains for tests. Deviations: none in behavior — the
      unrolled cascade is written as plain u64 group accumulation
      rather than the original's paired-u32 shift/mask machinery
      (bit-exact, see above). match.py (--size 380): original 95
      insns vs 94 — same shortest-first unrolled cascade, same length
      constants 1..9, same per-byte continuation tests (LLVM:
      ldrsb/cmp/bmi sign trick for `tst #0x80`), same u64 shift
      carries into the hi word (lsr #25/orr pairs for v<<7 across
      words) and strd slot stores; expected LLVM shape diffs:
      predicated per-case epilogues vs the original's shared
      stmia/popeq blocks, and a late push once the register budget
      grows. Host tests:
      round_trips_every_encoded_length_against_the_reference_encoder,
      one_byte_varints_decode_in_the_export, boundary_values,
      five_byte_varints_keep_the_full_value_unlike_the_u32_sibling,
      ninth_byte_contributes_all_eight_bits,
      bytes_consumed_stops_at_the_first_clean_high_bit,
      reads_no_byte_past_the_varint_end (PROT_NONE guard page),
      shipped_seam_default_is_this_port.
  - address: 0x083749bc
    ghidra: FUN_083749bc
    name: db_realloc
    size: 56
    sig: "void *db_realloc(sqlite3 *db, void *p, int n)"
    module: sqlite/mem
    status: ported
    notes: >-
      SQLite's sqlite3DbRealloc. 9 bl call sites. Returns NULL WITHOUT
      calling the heap when db->mallocFailed (db +0x1e) is already set;
      otherwise sqlite3_realloc @ 0x08390eec and, on failure, sets the
      flag. db is dereferenced unconditionally here (unlike the malloc
      path). 0x08390eec is ported - it IS the wired default of the
      DB_MEM_OPS.realloc dispatch slot in src/sqlite/mem.rs.

  - address: 0x083749f4
    ghidra: FUN_083749f4
    name: db_realloc_or_free
    size: 32
    sig: "void *db_realloc_or_free(sqlite3 *db, void *p, int n)"
    module: sqlite/mem
    status: ported
    notes: >-
      SQLite's sqlite3DbReallocOrFree. 6 bl call sites. db_realloc, and
      when it fails, sqlite3_free @ 0x083906f4 on the OLD pointer (the
      ported heap::tracked::tracked_free, called directly).

  - address: 0x08374960
    ghidra: FUN_08374960
    name: db_malloc_raw
    size: 56
    sig: "void *db_malloc_raw(sqlite3 *db, int n)"
    module: sqlite/mem
    status: ported
    notes: >-
      SQLite's sqlite3DbMallocRaw. 19 bl call sites. Same sticky
      mallocFailed protocol as db_realloc, but guards BOTH the entry
      check and the failure record on db != 0 - a NULL connection is
      allowed and simply goes straight to sqlite3_malloc @ 0x08390b14
      (ported - it IS the wired default of the DB_MEM_OPS.malloc
      dispatch slot).

  - address: 0x08374998
    ghidra: FUN_08374998
    name: db_malloc_zero
    size: 36
    sig: "void *db_malloc_zero(sqlite3 *db, int n)"
    module: sqlite/mem
    status: ported
    notes: >-
      SQLite's sqlite3DbMallocZero. 34 bl call sites. db_malloc_raw then
      zero-fill of the whole request through the IRAM thunk @ 0x08037dc8
      (-> 0x220002d4 = the ported memzero @ 0x080002d4). Nothing is
      zeroed when the allocation fails.

  - address: 0x08390b14
    ghidra: FUN_08390b14
    name: sqlite3_malloc
    size: 184
    sig: "void *sqlite3_malloc(int n)"
    module: sqlite/mem
    status: ported
    vars: {param_1: n, r5: n, r4: "NULL, then payload", r6: "n + 0x24",
      r7: stats, DAT_08390be8: stats_plus_0x20, DAT_08390bec: stats_base}
    notes: >-
      SQLite's sqlite3_malloc, the tag-57 tracked allocator's entry.
      41 bl call sites (binary-scanned). functions.csv size 184; the
      visible body - this head, the 0x08390b8c fall-through tail (the
      ported tracked_alloc_tail) and the two stats literals @
      0x08390be8/0x08390bec - spans to 0x08390bf0. Verified against
      osos.asm and osos.dec (literal @ 0x08390be8 = 0x08adc2e0 = stats
      + 0x20, so [r0,#8] is the callback word at stats+0x28; literal
      @ 0x08390bec = 0x08adc2c0, the stats base). n <= 0 (signed,
      subs/ble) returns NULL before the lock is armed. Then: bl
      0x082ccc74 arms the stats lock (the ported
      tracked_stats_arm_lock, dispatched through the
      TRACKED_STATS_OPS.lock slot, house pattern same as the stats
      readers); GATED soft-limit check (unlike the realloc entry - the
      callback word must be nonzero): current(+0x38) + n >=
      soft_limit(+0x20) as signed i64 (ldrd/adds/adc ..asr #31,
      subs/sbcs, blge) warns with n through the ported
      tracked_stats_warn_soft_limit @ 0x0837d67c (called directly; the
      sum is only compared, accounting happens on success in the
      tail); the allocation-deny schedule @ 0x08378a44(0) can refuse
      with NULL before the heap is touched (ALLOC_PRESSURE_OPS slot,
      default is the BSS-inactive at-rest proceed). First attempt
      alloc_tag57(n + 44) via bl 0x08391d2c; on failure an UNGATED
      warn(n) and a single retry - the retry IS the ported
      tracked_alloc_tail @ 0x08390b8c the head falls through into.
      Deviations: BOTH attempts call the ported tracked_alloc_tail
      (the original's first attempt is a raw bl 0x08391d2c whose
      success path branches INTO the tail's rebuild @ 0x08390b98 -
      same alloc, same rebuild/accounting, one copy of the builder);
      the stats block is the ALLOC_STATS static (heap/tracked module
      simplification). The port is the wired DB_MEM_OPS.malloc
      default, replacing the documented always-fails stub, so the
      db_* wrappers and the realloc NULL branch now reach real
      behavior. Host tests: n <= 0 touching nothing (no lock arm, no
      heap, no warn, no deny), the callback-word and limit gating of
      the entry warn (incl. the current+n == limit edge), lock-before-
      warn ordering via recorders, deny refusal ordering (warn first,
      heap untouched, no accounting), success-path header/accounting/
      peak, the OOM warn-and-retry-once path, and the default wiring.
      match.py structural: same gate/lock/gated-warn/deny/alloc/
      warn-retry/rebuild/accounting branch structure; the diff is
      LLVM's fp frame, the blx-through-slot lock and deny calls, and
      the warn helper inlined (same bytes, different shape - the
      documented deviations).

  - address: 0x08390eec
    ghidra: FUN_08390eec
    name: sqlite3_realloc
    size: 284
    sig: "void *sqlite3_realloc(void *p, int n)"
    module: sqlite/mem
    status: ported
    vars: {param_1: p, param_2: n, r5: n, r7: old_pad, r6: "base, then data",
      r8: old_size, r9: "n + 0x24, then new_pad", sl: stats, r4: "raw, then data"}
    notes: >-
      SQLite's sqlite3_realloc, the tag-57 tracked-heap resize. 5 bl call
      sites. p == 0 tail-branches to sqlite3_malloc @ 0x08390b14
      (dispatched through the DB_MEM_OPS.malloc slot, whose wired
      default is the ported sqlite3_malloc). n <= 0 (signed) is sqlite3_free
      @ 0x083906f4 (the ported tracked_free, called directly) + NULL -
      a zero resize is a free. Otherwise: recover pad at p-4, base =
      p - pad, raw = base - 8, old_size at raw+0; UNGATED soft-limit
      check (unlike the malloc entry - no callback-word test, no
      stats-lock arm): current(+0x38) + n - old_size >= soft_limit(+0x20)
      as signed i64 warns with n - old_size through the ported
      tracked_stats_warn_soft_limit @ 0x0837d67c (called directly); the
      allocation-deny schedule @ 0x08378a44(0) can then refuse with NULL
      before the heap is touched. Raw resize is realloc_tag57(raw,
      n + 44) = the veneer chain 0x08391d34 -> 0x08081688 ->
      realloc_wrapper @ 0x080edbf0 with (tag 57, copy-on-move 1),
      retried once after a warn(n) on failure. Rebuild: raw+4 = n>>31,
      raw+0 = n, data = (raw + 8 + 36) & ~31, memmove of the FULL
      old_size from the old to the new alignment via the ROM thunk
      @ 0x08037e00 (ported libc memmove; a shrink can run the copy past
      the new block's end - the original's behavior), pad word at data-4
      AFTER the copy; current += (i64)(i32)(n - old_size), peak := current
      when it grew. The one unported pressure helper (the deny schedule
      @ 0x08378a44; the warn helper @ 0x0837d67c is ported) dispatches
      through the new ALLOC_PRESSURE_OPS.alloc_deny_check slot whose
      default reproduces the at-rest behavior (schedule records
      BSS-inactive -> proceed); the stats block is the ALLOC_STATS
      static (heap/tracked module simplification). match.py: same
      structure - LLVM folds raw+8+36 into raw+44, const-folds n>>31 to
      0 (n > 0 there), inlines realloc_wrapper + lazy_init_default_heap
      (the long HEAP_OPS ldrd midsection), tracked_free and the warn
      helper (the memzero precedent: same bytes, different shape) and
      routes the copy through __aeabi_memmove; the p==0 path stays a
      tail call (bx) through the malloc slot.

  - address: 0x0837d38c
    ghidra: FUN_0837d38c
    name: sqlite3_malloc_zero
    size: 36
    sig: "void *sqlite3_malloc_zero(int n)"
    module: sqlite/mem
    status: ported
    vars: {param_1: n, r5: n, r4: "NULL, then block"}
    notes: >-
      SQLite's sqlite3MallocZero: p = sqlite3_malloc(n) (bl 0x08390b14,
      the ported entry); if p != NULL, zero-fill the whole request
      through the IRAM thunk 0x08037dc8 -> memzero @ 0x220002d4 (mirror
      of osos memzero @ 0x080002d4) via blne - so a failed allocation
      is never zeroed (it would fill from address 0); return p either
      way. 14 bl call sites, all plain (no predicated forms),
      binary-scanned from osos.dec. Extent verified against osos.dec:
      the body is 9 instructions ending @ 0x0837d3ac, the next function
      (push {r4-r8,lr}) starts @ 0x0837d3b0 - exactly Ghidra's 36
      bytes. n <= 0 never reaches the fill: the malloc entry returns
      NULL for those before touching the heap, so the blne guard
      trips. Deviations: the malloc dispatches through the
      DB_MEM_OPS.malloc slot (house ops-slot pattern, same as
      db_malloc_raw and the realloc NULL branch; the wired default IS
      the ported sqlite3_malloc the original bl's), and the fill is
      the ported memzero called directly (per the porting rules) -
      LLVM inlines it and recognizes the body back into
      __aeabi_memclr/__aeabi_memclr4 chains, the db_malloc_zero
      precedent (same bytes written, different shape in the match.py
      diff). Host tests: exactly-n-bytes zeroed with the tail
      untouched, NULL result leaving memory alone, and n forwarded
      unchanged (0 and -16 reach the allocator and come back NULL).

  - address: 0x08374a14
    ghidra: FUN_08374a14
    name: db_str_dup
    size: 44
    sig: "char *db_str_dup(sqlite3 *db, const char *z)"
    module: sqlite/strdup
    status: ported
    vars: {param_1: db, param_2: z}
    notes: >-
      SQLite's sqlite3DbStrDup. 22 bl call sites. Calls str_dup
      @ 0x08384ed8 and, if z was non-NULL and the copy failed, sets
      db->mallocFailed (+0x1e) via sqlite::mem::set_malloc_failed.
      Lives in sqlite/strdup.rs next to str_dup, whose callees are
      now all reachable (strlen and __rt_memcpy through their ported
      twins, sqlite3_malloc through DB_MEM_OPS), so the earlier
      dispatch-slot objection no longer applies. match.py: same
      structure (str_dup tail call, NULL early-out, conditional
      flag store).

  - address: 0x08374a40
    ghidra: FUN_08374a40
    name: db_str_ndup
    size: 48
    sig: "char *db_str_ndup(sqlite3 *db, const char *z, int n)"
    module: sqlite/strdup
    status: ported
    vars: {param_1: db, param_2: z, param_3: n}
    notes: >-
      SQLite's sqlite3DbStrNDup. 12 bl call sites. Same shape as
      db_str_dup over str_ndup @ 0x08384f60: tail call, NULL-z
      early-out, conditional store of 1 to db->mallocFailed (+0x1e)
      via sqlite::mem::set_malloc_failed. Its only callee str_ndup was
      still missing, so it is ported alongside in the same module
      (precedent: db_malloc_raw landing with db_malloc_zero). match.py:
      same structure (callee bl, cmp/early-out, strbeq at +0x1e).

  - address: 0x083753d0
    ghidra: FUN_083753d0
    name: dequote
    size: 148
    sig: "void dequote(char *z)"
    module: sqlite/dequote
    status: ported
    vars: {param_1: z, cVar4: quote, cVar1: c, iVar3: read, iVar2: write}
    notes: >-
      SQLite's sqlite3Dequote. 3 bl call sites. NULL -> no-op; leading
      byte picks the quote (' and " self-quote, [ quotes with ], `
      self-quotes), anything else returns untouched. In-place compaction
      from read cursor 1 / write cursor 0: doubled quote collapses to
      one, a lone quote terminates (NUL stored at write cursor), and a
      NUL with no closing quote ends the loop with NO terminator store.
      Ported because name_from_token @ 0x0837d7ac needs it. match.py:
      same structure (quote classification, compaction loop with escape
      doubling and terminator store).

  - address: 0x08384ed8
    ghidra: FUN_08384ed8
    name: str_dup
    size: 60
    sig: "char *str_dup(const char *z)"
    module: sqlite/strdup
    status: ported
    vars: {param_1: z}
    notes: >-
      SQLite's sqlite3StrDup. 2 bl call sites. NULL in, NULL out;
      otherwise strlen @ 0x08392478, sqlite3_malloc @ 0x08390b14 of
      len + 1, and __rt_memcpy through the veneer @ 0x08037db0
      (terminator included). strlen and __rt_memcpy are called through
      their ported twins (LLVM inlines strlen); sqlite3_malloc goes
      through the DB_MEM_OPS dispatch slot like sqlite/mem.rs. No
      mallocFailed flag here - that is db_str_dup's job.

  - address: 0x08384f60
    ghidra: FUN_08384f60
    name: str_ndup
    size: 64
    sig: "char *str_ndup(const char *z, int n)"
    module: sqlite/strdup
    status: ported
    vars: {param_1: z, param_2: n}
    notes: >-
      SQLite's sqlite3StrNDup. 2 bl call sites (db_str_ndup and
      FUN_0838656c). NULL in, NULL out; otherwise sqlite3_malloc
      @ 0x08390b14 of n + 1, __rt_memcpy through the veneer
      @ 0x08037db0 of exactly n bytes (no strlen - the source need not
      be NUL-terminated in the span), and a terminator store at
      dup[n]. __rt_memcpy goes through its ported twin; sqlite3_malloc
      through the DB_MEM_OPS dispatch slot like str_dup. No
      mallocFailed flag here - that is db_str_ndup's job. Ported
      together with db_str_ndup, its assigned caller.

  # ---- Parse context (src/sqlite/parse.rs) ----
  - address: 0x08381f98
    ghidra: FUN_08381f98
    name: release_temp_reg
    size: 32
    sig: "void release_temp_reg(Parse *parse, int reg)"
    module: sqlite/parse
    status: ported
    notes: >-
      SQLite's sqlite3ReleaseTempReg. 36 bl + 3 tail b call sites.
      Pushes a finished VDBE register onto the parse context's free list
      - nTempReg (u8) at Parse +0x15, aTempReg[8] at Parse +0x18. Ignores
      register 0 (SQLite's "no register" sentinel) and silently drops the
      the release when the list is full, leaking the register for the rest of
      the statement. The `cmpne r1,#8` is unsigned, so a counter already
      past the cap cannot overrun the array.

  - address: 0x0837a354
    ghidra: FUN_0837a354
    name: get_temp_range
    size: 104
    sig: "int get_temp_range(Parse *parse, int n_reg)"
    module: sqlite/get_temp_range
    status: ported
    renames:
      param_1: parse
      param_2: n_reg
      iVar1: cached
      iVar2: first
    notes: >-
      SQLite 3.5.9's sqlite3GetTempRange (expr.c), verbatim - no
      deviation: reserve n_reg consecutive VDBE registers, from the
      range pool when possible, else carved off nMem. 11 bl call sites,
      no tail b sites (binary-scanned): 0x082c3b10, 0x082e8838,
      0x08368884, 0x083771a8, 0x08379b80, 0x08379f60, 0x0837c328,
      0x083836c4, 0x08385cb4, 0x0838e11c, 0x08396188. Algorithm: load
      iRangeReg (i32 @ Parse +0x3c) and nRangeReg (i32 @ +0x38) once at
      entry; if n_reg <= nRangeReg (signed - the bgt at 0x0837a36c
      short-circuits past the scan) AND usedAsColumnCache @ 0x083966a0
      (ported, called directly - no seam: here the verdict is
      load-bearing, unlike the get_temp_reg sibling) finds no cached
      register in the probe range, bump iRangeReg += n_reg and
      nRangeReg -= n_reg and return the old iRangeReg; else return old
      nMem + 1 and advance nMem (i32 @ +0x48) by n_reg ONLY (upstream's
      i = pParse->nMem+1; pParse->nMem += nReg;). Two upstream quirks
      kept: the cache probe covers the WHOLE remaining pool range
      (iRangeReg, iRangeReg + nRangeReg - 1), not just the requested
      sub-range, so a cached register anywhere in the pool vetoes it;
      and the 3.5.9 discarded-verdict bug of get_temp_reg @ 0x0837a3bc
      does NOT exist here (the cmp/eq-conditioned pool updates after
      the bl use the verdict). All arithmetic wraps like ARM add/sub.
      match.py (exit 1, diff expected): same structure - entry loads
      at +0x38/+0x3c, signed gate over the scan, probe-range add/sub,
      cmp after the bl, carve path returning old nMem + 1 with
      nMem += n_reg, pool path storing both pool fields; LLVM splits
      the eq-conditioned block into straight-line code at a second
      return instead of ADS' predicated ldr/str chain.

  - address: 0x0837a3bc
    ghidra: FUN_0837a3bc
    name: get_temp_reg
    size: 80
    sig: "int get_temp_reg(Parse *parse)"
    module: sqlite/get_temp_reg
    status: ported
    renames:
      param_1: parse
      uVar1: reg
      iVar2: i
    notes: >-
      SQLite 3.5.9's sqlite3GetTempReg (expr.c) - the code generator's
      single-register draw from the temp-register pool; release_temp_reg
      @ 0x08381f98 is the companion push. 35 bl call sites, no tail b
      sites (binary-scanned): 0x082c3d28, 0x082c5fd8, 0x082d0344,
      0x082d0350, 0x082da4b8, 0x082e8844, 0x0836899c, 0x08368a10,
      0x08368a54, 0x08368b04, 0x08368bcc, 0x08372e60, 0x083733bc,
      0x083733c8, 0x08373678, 0x0837746c, 0x083777a8, 0x08377878,
      0x08377884, 0x08377bac, 0x08379ce4, 0x0837bd30, 0x0837bd3c,
      0x0837c1f8, 0x0837c3f8, 0x0837d6f4, 0x0837d700, 0x0838195c,
      0x08383780, 0x08385bfc, 0x08385c08, 0x0838e564, 0x0838e948,
      0x08399100, 0x0839910c. Algorithm: if nTempReg (u8 @ Parse +0x15)
      is nonzero, probe every aTempReg[i] (i32[8] @ +0x18) through
      usedAsColumnCache @ 0x083966a0 as (r, r) - the count byte is
      re-read every pass (in-loop ldrb) and every verdict is DISCARDED -
      then return ++nMem (i32 @ +0x48; the incremented value, ARM add
      wrap semantics). The discarded verdicts and missing pop are not a
      decompile artifact: upstream 3.5.9's loop `continue` merges with
      its fall-through at the same i++, so the post-loop
      `if( i>=nTempReg )` is always true and the pop tail (with its
      increment-less infinite while) is unreachable - ADS 1.0.1 deleted
      it, making the famous 3.5.9 temp-register bug permanent: the pool
      is write-only and every draw bumps nMem. Struct identity is
      pinned by the callee's offsets: 0x083966a0 walks nColCache (i32 @
      +0x58) over 16-byte aColCache records (@ +0x60, iReg at record
      +0x0c, hence the ldr at +0x6c) - exactly SQLite 3.5.9's
      Parse/yColCache layout (version string "3.5.9" @ image 0x905834).
      Callee 0x083966a0 is ported (used_as_column_cache,
      sqlite/used_as_column_cache) and is the TEMP_REG_OPS seam's
      shipped default; the returns-0 stand-in remains for tests.
      Ghidra types the return as void; r0 carries ++nMem.

  - address: 0x083960e8
    ghidra: FUN_083960e8
    name: parse_release_deferred_vdbe
    size: 72
    sig: "void parse_release_deferred_vdbe(Parse *parse)"
    module: sqlite/parse_release_deferred_vdbe
    status: ported
    renames:
      param_1: parse
      uVar1: parse_error_message_or_vdbe
      bVar2: has_parse_error_message
    notes: >-
      The extent is 0x083960e8..0x08396130: the next word starts a distinct
      `push {r4-r11,lr}` function and there is no literal pool. All 8 direct
      binary-scanned callers are unconditional plain bl: 0x082bdba8,
      0x08370908, 0x08370b4c, 0x08370cc0, 0x08371880, 0x083718bc,
      0x08371a74, and 0x08372b8c; no predicated callers exist. Parse identity
      is established by the known SQLite 3.5.x fields zErrMsg at +0x08 and
      pVdbe at +0x0c. Algorithm: return while +0x30 or zErrMsg is nonzero, or
      pVdbe is NULL. Otherwise read db from +0x00 and, if its +0x48 pointer is
      signed-positive as a target-width word, call the ported
      release_via_field_0x48. Finally clear pVdbe and the byte at +0x10. The
      no-NULL-guard dereferences are intentional. Deliberate host-only layout
      deviation: pointer-word slots scale to the host pointer width to prevent
      widened fixture pointers from overlapping target fields.

  - address: 0x083966a0
    ghidra: FUN_083966a0
    name: used_as_column_cache
    size: 64
    sig: "int used_as_column_cache(Parse *parse, int start, int end)"
    module: sqlite/used_as_column_cache
    status: ported
    renames:
      param_1: parse
      param_2: start
      param_3: end
      iVar1: i
      iVar2: i_reg
    notes: >-
      SQLite 3.5.9's usedAsColumnCache (expr.c), verbatim - no
      deviation, including the absence of the iReg != 0 guard later
      SQLite versions add (no zero test in the 16 instructions).
      3 bl call sites, no tail b sites (binary-scanned): 0x083774dc
      in FUN_08376ef0 (unidentified; passes (iCurrent, iCurrent)),
      0x0837a380 in FUN_0837a354 (sqlite3GetTempRange; passes
      (iRangeReg, iRangeReg + nRangeReg - 1)), and 0x0837a3e8 in
      get_temp_reg @ 0x0837a3bc (discards the verdict - see its
      entry). Algorithm: load nColCache (i32 @ Parse +0x58) ONCE at
      entry (ldr into lr), walk the 16-byte aColCache records (@
      +0x60, iReg at record +0x0c, hence the ldr at +0x6c), return 1
      on the first record whose iReg lies inside the INCLUSIVE signed
      range [start, end], else 0. All comparisons signed (blt/movle,
      loop head bgt): a negative nColCache scans nothing. With the
      port landed, get_temp_reg's TEMP_REG_OPS seam default is now
      the real scan (both target and host); the returns-0 stand-in
      remains for tests. match.py (exit 1, diff expected): same
      structure - count load at +0x58, record walk at 0x10 stride
      with the iReg ldr at +0x6c, signed range test, early return 1
      vs fall-through return 0; LLVM renders the loop as a countdown
      with post-increment pointer walk in place of ADS' index form.

  - address: 0x08376b48
    ghidra: FUN_08376b48
    name: expr_cache_affinity_change
    size: 64
    sig: "void expr_cache_affinity_change(Parse *parse, int first_reg, int count)"
    module: sqlite/expr_cache_affinity_change
    status: ported
    renames:
      param_1: parse
      param_2: first_reg
      param_3: count
      iVar1: i
      iVar2: i_reg
      iVar3: record
    notes: >-
      SQLite 3.5.9's sqlite3ExprCacheAffinityChange (expr.c). Ghidra
      reports 68 bytes, but raw osos.dec words at 0x08376b48..0x08376b88
      end with pop {r4,pc}; the independent next entry starts at
      0x08376b8c, so the verified extent is 64 bytes. 15 unconditional
      bl call sites, no predicated bl or tail b sites (binary-scanned):
      0x082c3a30, 0x082c3c7c, 0x082c3c8c, 0x082d0428, 0x082d0504,
      0x08368b34, 0x08368c38, 0x08373460, 0x083736b0, 0x0837768c,
      0x083777dc, 0x08379c20, 0x0837a028, 0x08385da8, 0x0839628c.
      Algorithm: calculate last_reg as first_reg + count - 1 with ARM
      wrapping arithmetic; load nColCache once from Parse +0x58; for
      every 16-byte aColCache record at +0x60, set its affChange byte at
      +0x08 to one when its iReg word at +0x0c lies in the inclusive
      signed range [first_reg,last_reg]. No NULL guard exists. No
      deliberate deviations: count zero and integer overflow preserve
      the original wrapped endpoint semantics.

  - address: 0x08376bf4
    ghidra: FUN_08376bf4
    name: expr_code
    size: 52
    sig: "int expr_code(Parse *parse, Expr *expr, int target)"
    module: sqlite/expr_code
    status: ported
    renames:
      param_1: parse
      param_2: expr
      param_3: target
      iVar1: in_reg
      iVar2: p_vdbe
    notes: >-
      SQLite 3.5.9's sqlite3ExprCode (expr.c) - the "guaranteed
      placement" wrapper over the expression code generator: evaluate
      expr so the result ALWAYS sits in VDBE register target, and
      return target verbatim. 23 bl call sites, no tail b sites
      (binary-scanned): 0x082c3b24, 0x082c3b34, 0x082c3b44 (all in
      FUN_082c3a38), 0x082c4a7c, 0x082c4af8 (both in FUN_082c4a30),
      0x08373424 (FUN_08373180), 0x08376c38 (FUN_08376c28,
      sqlite3ExprCodeAndCache), 0x08376ccc
      (FUN_08376c94), 0x08377a28, 0x08377a78 (both in FUN_08376ef0,
      sqlite3ExprCodeTarget itself), 0x08379974 (FUN_083797fc),
      0x0837c2cc, 0x0837c398, 0x0837c55c, 0x0837c7a4 (all in
      FUN_0837b9ec), 0x08383850 (FUN_08382e28), 0x08385e70,
      0x08385f0c (both in FUN_0838581c), 0x0838e1e4, 0x0838e4ec,
      0x0838e7c0, 0x0838e8b0 (all in FUN_0838d8a4), 0x083962d0
      (FUN_08396130). Algorithm: in_reg = sqlite3ExprCodeTarget(parse,
      expr, target) @ 0x08376ef0; if in_reg != target, load pVdbe
      (Parse +0x0c - the ldrne) and when non-NULL append
      OP_SCopy in_reg, target via vdbe_add_op2 @ 0x08386824 (opcode
      literal 8, the movne r1,#0x8); return target unconditionally.
      The two upstream asserts (target in range, pVdbe or mallocFailed)
      are compiled out of the firmware (NDEBUG) and not modeled.
      Callee 0x08376ef0 (3168 bytes, the code generator proper) is
      UNPORTED and rides the EXPR_CODE_OPS seam; the shipped default
      stand-in reports "value already in target" (returns target), so
      no OP_SCopy is emitted. Callee vdbe_add_op2 is ported
      (sqlite/vdbe) and called directly. Ghidra's decompile drops the
      4th argument to vdbe_add_op2 (r3 = target, set by movne r3,r5);
      the disassembly is authoritative. match.py (exit 1, diff
      expected): same structure - generator call, in_reg vs target
      compare, conditional pVdbe load at +0xc, opcode-8 vdbe_add_op2
      call, return target; LLVM uses a branch + the seam's blx where
      ADS predicated (ldrne/cmpne/movne/blne).

  - address: 0x08377b9c
    ghidra: FUN_08377b9c
    name: expr_code_temp
    size: 84
    sig: "int expr_code_temp(Parse *parse, Expr *expr, int *out_temp_reg)"
    module: sqlite/expr_code_temp
    status: ported
    renames:
      param_1: parse
      param_2: expr
      param_3: out_temp_reg
      iVar1: temp_reg
      iVar2: in_reg
    notes: >-
      SQLite 3.5.9's sqlite3ExprCodeTemp (expr.c). Binary-scanned:
      16 unconditional bl call sites, zero predicated bl and zero tail b
      sites: 0x082c3cd0, 0x082c3d00, 0x08376fd0, 0x083773ec,
      0x08377400, 0x08377498, 0x083776dc, 0x083778d0, 0x083779a4,
      0x08378040, 0x083781a0, 0x083781d4, 0x083782f8, 0x08378440,
      0x08378474, 0x0838e42c. Algorithm: get_temp_reg(parse) @
      0x0837a3bc, call sqlite3ExprCodeTarget(parse, expr, temp_reg) @
      0x08376ef0, and return that register. When it is the requested
      temporary, write it to out_temp_reg; otherwise release_temp_reg
      @ 0x08381f98 returns the unused temporary to the free list, write
      zero (SQLite's no-register sentinel), then return the generator's
      register. No NULL guards precede either output store. The expression
      generator remains unported and uses the existing EXPR_CODE_OPS seam;
      its default returns temp_reg, so the shipped default takes the retain
      path. No new dispatch seam was introduced.

  # ---- Big-endian byte stores (src/util/beload.rs) ----
  - address: 0x08046b1c
    ghidra: FUN_08046b1c
    name: store_u32_be_bytes
    size: 40
    sig: "void store_u32_be_bytes(u8 *p, u32 value)"
    module: util/beload
    status: ported
    vars: {param_1: p, param_2: value}
    notes: >-
      Four alignment-free strb stores, most significant byte first. ARM
      spills the little-endian u32 argument then reloads stack offsets 3,
      2, 1, 0, making the source argument's logical bit order explicit.
      This is separate from SQLite's store_be32 @ 0x083816cc even though
      they implement the same byte-store operation. Three recovered
      decompiler call sites write 1 or 2 into a buffer at p + 2.

  # ---- Big-endian page fields (src/util/beload.rs) ----
  - address: 0x083816cc
    ghidra: FUN_083816cc
    name: store_be32
    size: 32
    sig: "void store_be32(u8 *p, u32 value)"
    module: util/beload
    status: ported
    notes: >-
      SQLite's sqlite3Put4byte. 38 bl call sites. Four strb, most
      significant byte first - alignment-free. The write twin of
      load_be32 / sqlite3Get4byte @ 0x0837a158.

  # ---- Scouted in 0x08360000-0x0838ffff, identified only ----
  - address: 0x083766f4
    ghidra: FUN_083766f4
    name: sqlite_error
    size: 136
    sig: "void sqlite_error(sqlite3 *db, int err_code, const char *format, ...)"
    module: sqlite/error
    status: ported
    vars: {param_1: db, param_2: err_code, param_3: format, param_4: "variadic word 0 (args[0])", r4: db, r5: err_code, iVar1: error_value}
    notes: >-
      SQLite 3.5.9's sqlite3Error (util.c). 27 unconditional bl call
      sites plus conditional blne 0x082c443c and bleq 0x082dc074 (29
      total binary-scanned): NULL db returns; otherwise pErr (+0xc8) is
      created through sqlite3ValueNew @ 0x083866c0 if absent, and an OOM
      return leaves even errCode unchanged. On success errCode (+0x14)
      receives err_code. A NULL format calls sqlite3ValueSetStr @
      0x083866ec with (pErr, 0, NULL, SQLITE_UTF8, SQLITE_STATIC); a
      format is expanded through sqlite3VMPrintf @ 0x08386454 with a
      va_list at the spilled r3 word, then installed with length -1 and
      the firmware's xDel pool literal 0x0838581c (upstream passes
      sqlite3_free). The post-format pErr reload is deliberate. ValueNew
      and ValueSetStr are unported house dispatch seams (OOM and no-op
      defaults); VMPrintf uses the existing SQLITE_VM_PRINTF seam. Host
      tests cover NULL db, cached/create/OOM pErr paths, clear versus
      formatted results, xDel, and the reload.
  - address: 0x082c43f0
    ghidra: FUN_082c43f0
    name: column_mem
    size: 88
    sig: "Mem *column_mem(Vdbe *statement, i32 column)"
    module: sqlite/column_mem
    status: ported
    renames:
      param_1/r0: statement
      param_2/r1: column
      param_1_plus_0xec: n_res_column
      param_1_plus_0xf8: result_set
      FUN_083900c4: sqlite_data_count
      FUN_083766f4: sqlite_error
      DAT_082c4448: fallback_null_mem
    notes: >-
      SQLite's private columnMem helper behind the sqlite3_column_* wrappers.
      Raw extent is exactly 88 bytes: code starts at 0x082c43f0, the
      literal fallback-Mem pointer 0x088fce20 is at 0x082c4448, and the next separately linked
      function starts at 0x082c444c. Decoding every ARM B/BL word in
      osos.dec finds 10 direct callers, all unconditional bl and none
      predicated: 0x0838f9b8, 0x0838f9dc, 0x0838fa00, 0x0838fa50,
      0x0838fa7c, 0x0838faa0, 0x0838faec, 0x0838fb10, 0x0838fb34, and
      0x0838fb54. Returns result_set + column*40 only for a non-NULL
      result_set, signed column >= 0, and signed column < n_res_column;
      otherwise calls the ported sqlite_error(statement->db, SQLITE_RANGE
      (25), NULL) when db is non-NULL and returns the shared SQL-NULL Mem
      at runtime address 0x088fce20 (flags = MEM_Null, type = SQLITE_NULL,
      z = the empty string at 0x089c4850). The successful ARM
      path calls 0x083900c4, a 24-byte unported sqlite3_data_count leaf
      that merely reloads pResultSet/nResColumn and returns the count; r0
      is overwritten before return, so its ignored pure result has no
      observable effect and the Rust port deliberately omits the call
      rather than adding a dispatch seam. The named repr(C) p_result_set
      field preserves the target's +0xf8 pointer slot without host pointer
      field overlap. Host tests cover first/middle/last records, negative,
      endpoint, and INT_MAX indices, and an unallocated result set.
  - address: 0x083767a0
    ghidra: FUN_083767a0
    name: sqlite_error_msg
    size: 76
    sig: "void sqlite_error_msg(Parse *parse, const char *format, ...)"
    module: sqlite/error_msg
    status: ported
    vars: {param_1: parse, param_2: format}
    notes: >-
      SQLite's sqlite3ErrorMsg - the engine's single error reporter, and
      after the VDBE builders the hottest thing in the range at 86 bl +
      4 tail b call sites. `pParse->nErr++` (+0x40); frees the old
      zErrMsg (+0x08) through sqlite3_free @ 0x083906f4; formats a new
      one with sqlite3VMPrintf @ 0x08386454 on pParse->db (+0x00); sets
      pParse->rc (+0x04) to 1 (SQLITE_ERROR) if it was 0. This pins the
      Parse layout - db +0x00, rc +0x04, zErrMsg +0x08, nErr +0x40.
      Ported with two dispatch-level deviations: the C varargs spill is
      an explicit `args: VaList` (pointer to the first variadic word,
      the printf/printf_api.rs convention), and the formatter is the
      SQLITE_VM_PRINTF dispatch static (default documented always-NULL
      stub) because sqlite3VMPrintf drags in the whole SQLite printf
      chain (0x08384e84 / 0x0839788c / 0x08384e14) - a batch of its
      own. sqlite3_free is the ported heap::tracked::tracked_free,
      inlined by LLVM. Parse is a typed repr(C) struct with 32-bit
      offset asserts.

  - address: 0x0837d358
    ghidra: FUN_0837d358
    name: sqlite_set_string_formatted
    size: 28
    sig: "char *sqlite_set_string_formatted(sqlite3 *db, const char *format, ...)"
    module: sqlite/set_string_formatted
    status: ported
    vars: {param_1: db, param_2: format, param_3: "variadic word 0 (args[0])", param_4: "variadic word 1 (args[1])"}
    notes: >-
      37 bl call sites. SQLite's sqlite3MPrintf: spills r0-r3 into a
      varargs home area, reloads the format (spilled r1), builds ap =
      &spilled-r2, and calls sqlite3VMPrintf @ 0x08386454 with
      (db, format, ap). The epilogue never touches r0, so the
      formatter's heap-owned result (NULL on allocation failure) is
      returned verbatim - call sites like 0x0836f26c use the return
      value ("SELECT idx, stat FROM %Q.sqlite_stat1" fed to the
      executor). Ported with the same two dispatch-level deviations as
      sqlite_error_msg: the C varargs spill becomes an explicit
      `args: VaList`, and the unported 0x08386454 is the shared
      SQLITE_VM_PRINTF dispatch static (default always-NULL stub). The
      match.py diff is exactly those deviations: the Rust body has no
      home area to spill, so LLVM tail-calls through the loaded slot.

  - address: 0x08384158
    ghidra: FUN_08384158
    name: sqlite3_set_string
    size: 148
    sig: "void sqlite3_set_string(char **pz, const char *z_first, ...)"
    module: sqlite/set_string
    status: ported
    vars: {param_1: pz, param_2: "z_first (args[0])", param_3: "variadic word 1 (args[1])", param_4: "variadic word 2 (args[2])"}
    notes: >-
      23 bl call sites (binary-scanned: 20 plain bl, one blne @
      0x08387314, two bleq @ 0x0838a868 / 0x0838b2a8 - the predicated
      sites gate on a computed condition, not a pointer; the callee has
      no NULL guard on pz). SQLite's sqlite3SetString: spills r0-r3
      into a varargs home area, then walks the NULL-terminated string
      list from the spilled r1 twice. Pass 1 sums 1 + sum(strlen(z))
      (unguarded strlen @ 0x08392478) into the request size; then
      sqlite3_free(*pz) (the ported tracked_free @ 0x083906f4, called
      unconditionally - its own NULL guard covers a fresh pz), then
      sqlite3_malloc @ 0x08390b14, stored through pz EVEN WHEN NULL (a
      failed allocation leaves *pz == NULL, never stale). On success
      dst[0] = 0 up front, pass 2 appends each string with __rt_memcpy
      (ROM veneer @ 0x08037db0) over exactly strlen(z) bytes - no
      per-string NUL - and one terminator closes the result. Epilogue
      returns void and pops the home area (ldr pc, [sp], #20). Ghidra
      drops the first-pass strlen argument in its C - the bytes show
      ldr r0,[r4],#4 feeding the bl. Extent self-verified: next
      function prologue at 0x083841ec, so 148 bytes exact. Deviations:
      the C varargs spill becomes an explicit args: VaList (=
      &spilled-r1, house convention), and the malloc goes through the
      DB_MEM_OPS dispatch whose wired default IS the ported
      sqlite3_malloc; strlen, tracked_free and __rt_memcpy are called
      directly through their ported twins. The size accumulator wraps
      like the 32-bit r6 it was. match.py diff is the expected one:
      LLVM inlines the strlen and tracked_free bodies and the explicit
      args pointer removes the home-area spill.

  - address: 0x082c2438
    ghidra: FUN_082c2438
    name: integrity_check_append_msg
    size: 176
    sig: "void integrity_check_append_msg(IntegrityCheck *check, const char *prefix, const char *format, ...)"
    module: sqlite/integrity_check_append_msg
    status: ported
    renames:
      param_1/r4: check
      param_2/r5: prefix
      param_3: format
      param_4: "variadic word 0 (args[0])"
      r6: old_message
      r7: formatted_message
    notes: >-
      SQLite 3.5.9's checkAppendMsg. 14 direct bl call sites verified by
      decoding every ARM B/BL word in osos.dec: 11 unconditional (0x082c25fc,
      0x082c2680, 0x082c283c, 0x082c2970, 0x082c2a48, 0x082c2a7c,
      0x082c2e04, 0x082c2e90, 0x082c2f1c, 0x082c2f54, 0x08371a34) and
      three blne (0x082c2bec, 0x08371a74, 0x08371abc), whose caller-side
      mismatch predicates gate diagnostic production. Extent verified from
      the raw bytes: code begins 0x082c2438, the next function prologue is
      0x082c24f0, and the two-word literal pool at 0x082c24e8 contains the
      default empty prefix and "\n". If check->mxErr is zero, return without
      touching anything. Otherwise decrement mxErr (+0x10), increment nErr
      (+0x18) with ARM wrapping arithmetic, format through sqlite3VMPrintf
      @ 0x08386454 using NULL db and &spilled-r3, then replace zErrMsg
      (+0x14). A NULL prefix becomes the empty literal. The no-old path joins
      prefix and formatted text; the old path first NULLs zErrMsg, joins old,
      newline, prefix, and formatted text, then frees old followed by the
      formatter result through sqlite3_free @ 0x083906f4. NULL formatter
      results terminate sqlite3SetString's list, retaining prior pieces.
      Deliberate deviations: explicit VaList replaces C varargs and the
      unported formatter uses the existing SQLITE_VM_PRINTF dispatch seam;
      sqlite3SetString and tracked_free are direct ports. Host tests map the
      target-width string-list fixture below 4 GiB so u32 vararg pointers
      cannot silently truncate.

  # ---- SQLite OS close wrapper, sqlite3OsClose (src/sqlite/os_close.rs) ----
  - address: 0x0837dae8
    ghidra: FUN_0837dae8
    name: sqlite_os_close
    size: 48
    sig: "i32 sqlite_os_close(sqlite3_file *file)"
    module: sqlite/os_close
    status: ported
    renames:
      param_1/r0/r4: file
      r1: "file->pMethods, then xClose"
      "*(code**)(*param_1 + 4)": sqlite3_io_methods::xClose
    notes: >-
      SQLite's sqlite3OsClose from os.c: rc = SQLITE_OK; if
      file->pMethods != NULL, rc = pMethods->xClose(file) (io_methods slot
      +0x04) and then pMethods is nulled, marking the file closed so a
      repeated call is a no-op returning 0. Raw ARM extent verified: 12
      words at 0x0837dae8..0x0837db14, the next separately linked function
      starts with push {r4,r5,r6,lr} at 0x0837db18 - Ghidra's 48 bytes are
      exact, no trailing literal pool. Call count verified by decoding
      every ARM B/BL word in osos.dec: exactly 12 direct sites, 10
      unconditional bl plus 2 blne (0x0837de14, 0x0837de2c, both gated on
      a flag byte of the owning object, not on file - this wrapper never
      NULL-checks file itself); the address occurs in no image data word
      (never dispatched virtually). Identity established by the adjacent
      0x0837db44..0x0837dc18 sqlite3_io_methods forwarding family
      (including the ported sqlite_os_write @ 0x0837dbf8); callers free
      the file object afterwards through sqlite3_free @ 0x083906f4 (the
      ported tracked_free) - the immediate sibling @ 0x0837db18 is exactly
      sqlite_os_close + sqlite3_free. The concrete xClose is runtime
      method-table data, so the port dispatches the supplied table rather
      than inventing a callee; the struct is shared with os_write, whose
      +0x04 slot this port resolved to xClose. match.py: structural parity
      - same load chain (file -> pMethods -> +0x04), same NULL-guard early
      return of 0, same blx dispatch and post-call NULL store; LLVM splits
      the exit into moveq/bxeq before the frame instead of ADS's popeq and
      spills r4/sl/fp instead of just r4. Host tests: NULL methods return
      0 without dispatch, xClose receives the exact file pointer with
      pMethods still set during the call and NULLed after (original
      ordering), and a second close on the same file is a no-op.

  # ---- SQLite file big-endian word reader (src/sqlite/read_be32.rs) ----
  - address: 0x08365598
    ghidra: FUN_08365598
    name: sqlite_read_be32
    size: 52
    sig: "i32 sqlite_read_be32(sqlite3_file *file, i64 offset, u32 *out)"
    module: sqlite/read_be32
    status: ported
    renames:
      param_1/r0: file
      r1: AAPCS alignment padding before offset
      param_3/r2: offset low word
      param_4/r3: offset high word
      param_5/stack_arg_1/r4: out
      local_10: read_buffer
      FUN_0837dba0: sqlite_os_read
      FUN_0837a158: load_be32
    notes: >-
      Raw ARM confirms Ghidra's 52-byte extent: the body runs from
      0x08365598 through 0x083655c8, and the next separately linked
      function begins with push {r2,r3,r4,r5,r6,r7,r8,lr} at 0x083655cc.
      It allocates a four-byte stack buffer, calls the ported
      sqlite_os_read(file, buffer, 4, offset), and only for status 0 calls
      the ported load_be32 before storing through out; a nonzero status
      returns with out untouched. Decoding every ARM B/BL word in osos.dec
      finds exactly 9 inbound calls, all unconditional bl
      (0x082de188, 0x082de22c, 0x08365aa4, 0x08365ac8, 0x08365ae4,
      0x08365b08, 0x08365b60, 0x08365bf0, 0x08365c2c), with no predicated
      call or tail branch. The 64-bit offset is r2/r3 because r1 is AAPCS
      alignment padding after the first pointer argument. Rust models the
      local as MaybeUninit: as in ARM, it is only consumed after xRead
      returns success, whose SQLite contract initializes all four bytes.

  # ---- SQLite OS read forwarding wrapper (src/sqlite/os_read.rs) ----
  - address: 0x0837dba0
    ghidra: FUN_0837dba0
    name: sqlite_os_read
    size: 32
    sig: "i32 sqlite_os_read(sqlite3_file *file, void *buffer, u32 amount, i64 offset)"
    module: sqlite/os_read
    status: ported
    renames:
      param_1/r0: file
      param_2/r1: buffer
      param_3/r2: amount
      r3: AAPCS alignment padding before offset
      stack_arg_1: offset low word
      stack_arg_2: offset high word
      "file->pMethods": sqlite3_io_methods
      "pMethods+0x08": xRead
    notes: >-
      Raw bytes are eight words at 0x0837dba0..0x0837dbbc; the next separately
      linked function begins at 0x0837dbc0, so Ghidra's 32-byte extent is
      exact with no literal pool. The wrapper copies its two incoming stack
      words into its outgoing stack-argument slots, loads file->pMethods and
      xRead at +0x08, blx-calls it, and returns its i32 status unchanged. The
      adjacent 0x0837db44..0x0837dc18 forwarding family is SQLite
      sqlite3_io_methods (xClose/xRead/xWrite/xTruncate...), establishing this
      as sqlite3OsRead. Its i64 offset is stack-passed under AAPCS because r3
      is the alignment pad. Decoding every ARM B/BL word in osos.dec finds
      exactly 9 direct calls, all unconditional bl (0x082dd6c4, 0x082dd944,
      0x082de1ac, 0x083655ac, 0x08365614, 0x08365a5c, 0x08365c58,
      0x08365cb8, 0x08372570); no predicated call, tail branch, or aligned raw
      data word targets this wrapper. The concrete xRead implementation is
      runtime vtable data, so the port dispatches the supplied method table
      rather than inventing a callee. Host tests prove it forwards the exact
      file and mutable buffer pointers, zero amount, i64::MIN offset, and
      method status unchanged.

  # ---- SQLite OS write forwarding wrapper (src/sqlite/os_write.rs) ----
  - address: 0x0837dbf8
    ghidra: FUN_0837dbf8
    name: sqlite_os_write
    size: 32
    sig: "i32 sqlite_os_write(sqlite3_file *file, const void *buffer, u32 amount, i64 offset)"
    module: sqlite/os_write
    status: ported
    renames:
      param_1/r0: file
      param_2/r1: buffer
      param_3/r2: amount
      r3: AAPCS alignment padding before offset
      stack_arg_1: offset low word
      stack_arg_2: offset high word
      "file->pMethods": sqlite3_io_methods
      "pMethods+0x0c": xWrite
    notes: >-
      Raw bytes are eight words at 0x0837dbf8..0x0837dc14; the next
      separately linked function begins at 0x0837dc18. The wrapper copies
      its two incoming stack words into the outgoing stack-argument slots,
      loads file->pMethods then xWrite at +0x0c, blx-calls it, and returns
      its i32 status unchanged. The adjacent 0x0837db44..0x0837dc18
      forwarding family is SQLite sqlite3_io_methods (xClose/xRead/xWrite/
      xTruncate...), establishing this as sqlite3OsWrite. Its i64 offset is
      stack-passed under AAPCS because r3 is the alignment pad. Decoding
      every ARM B/BL word in osos.dec finds exactly 13 direct calls, all
      unconditional bl; no predicated call or tail branch targets it. The
      concrete xWrite implementation is runtime vtable data, so the port
      dispatches the supplied method table rather than inventing a callee.

  - address: 0x0837dc18
    ghidra: FUN_0837dc18
    name: parse_expr
    size: 32
    sig: "Expr *parse_expr(Parse *parse, int op, Expr *left, Expr *right, const Token *token)"
    module: sqlite/parse_expr
    status: ported
    vars: {param_1: parse, param_2: op, param_3: left, param_4: right, param_5: token}
    notes: >-
      SQLite's sqlite3PExpr. 44 bl call sites, all grammar actions.
      Replaces its first argument with the db back-pointer at +0x00 of
      the parse context and tail-forwards to sqlite3Expr @ 0x08376808
      (ported as expr_new - see its entry; allocates a 0x44-byte Expr
      via the ported db_malloc_zero @ 0x08374998, stores the op byte at
      +0x00, operands at +0x08/+0x0c, the token span at +0x14/+0x18,
      recomputes height via the ported expr_set_height @ 0x083788fc; on
      OOM releases both operands via 0x08377e00 and returns NULL).
      0x08376808 is wired as the default of the SQLITE_EXPR_NEW
      dispatch static, replacing the documented always-NULL stub
      (mimics its OOM path; retained for host tests, which still swap
      the slot for recording mocks). The match.py diff is unchanged by
      the wiring: the Rust body tail-calls through the loaded slot
      instead of a direct bl. Old scouting name: sqlite_forward_via_db.

  - address: 0x08376808
    ghidra: FUN_08376808
    name: expr_new
    size: 216
    sig: "Expr *expr_new(sqlite3 *db, int op, Expr *left, Expr *right, const Token *token)"
    module: sqlite/expr_new
    status: ported
    vars: {param_1: db, param_2: op, param_3: left, param_4: right, param_5: token, puVar1: node}
    notes: >-
      SQLite 3.5.x's sqlite3Expr, the SQLITE_EXPR_NEW dependency of
      parse_expr (sqlite3PExpr). 6 bl call sites. Allocates a 0x44-byte
      Expr via the ported db_malloc_zero @ 0x08374998 (called
      directly); on OOM releases both operands through the expression
      destructor @ 0x08377e00 (left first, then right) and returns
      NULL. Otherwise stores the op low byte at +0x00 (strb of the full
      op register), iAgg = -1 at +0x30 (mvn r0,#0), the operands at
      +0x08/+0x0c; with a token, ldmia's its two words into BOTH token
      (+0x14/+0x18) and span (+0x1c/+0x20); without one and with both
      operands, merges the child spans through the helper @ 0x0837894c
      (called as (node, &left->span, &right->span) - the Token at +0x1c
      of each child) and propagates EP_CombBound (flags halfword +0x02
      bit 0x100, iTable word +0x04) from the right child (only inside
      the both-operands branch), then from the left child (under left
      != NULL alone, so the left wins a doubly-flagged join); finishes
      with the ported expr_set_height @ 0x083788fc (called directly).
      The destructor callee is ported: 0x08377e00 (sqlite3ExprDelete,
      88 bytes, 34 bl call sites - recursive teardown: dyn token/span
      strings and the node itself via sqlite3_free @ 0x083906f4,
      p_left/p_right recursion, p_list via 0x08378670, p_select via
      0x08383c88) is expr_delete (see its entry), the shipped default
      of SQLITE_EXPR_DELETE, so the OOM path really releases both
      operands; the documented no-op stub is retained for host tests
      (OOM leaks the operands; the NULL return is unchanged). The
      span-merge helper @ 0x0837894c (96 bytes, 5 bl call sites,
      the other four in the 0x0839a block) is ported: expr_span (see
      its entry), the shipped default of SQLITE_EXPR_SPAN_MERGE, so a
      token-less node with two spanned operands gets the merged span;
      the documented no-op stub is retained for host tests (with it
      installed the zeroed span survives - identical to the original
      whenever either child's span.z is NULL). The port is the shipped
      default of parse_expr's SQLITE_EXPR_NEW, replacing the
      always-NULL stub. Expr/Token are typed repr(C) structs with
      32-bit offset asserts.
      match.py: same structure - the alloc/NULL fork, the field stores,
      the token two-word copies, the conditional merge call, both
      EP_CombBound tst/ldr/str blocks, the height tail call; the diff
      is blx-through-slot for the two unported callees and LLVM frame
      setup.

  - address: 0x08377e58
    ghidra: FUN_08377e58
    name: expr_dup
    size: 184
    sig: "Expr *expr_dup(sqlite3 *db, const Expr *source)"
    module: sqlite/expr_dup
    status: ported
    vars: {param_1: db, param_2: source, iVar1: node, uVar2: clone}
    notes: >-
      SQLite 3.5.x's sqlite3ExprDup. Binary decoding fixes the function
      extent at 0x08377f10, so its 0x08377e58..0x08377f0c body is 184
      bytes. A whole-image ARM branch scan found exactly 34 references:
      all are unconditional bl instructions (no predicated forms and no
      tail b). NULL source returns NULL before allocation. Otherwise
      sqlite3DbMallocRaw @ 0x08374960 allocates 0x44 bytes; the IRAM
      memcpy veneer @ 0x08037df8 copies the source; non-NULL token text
      (+0x14) is duplicated with its packed length (+0x18 >> 1) through
      sqlite3DbStrNDup @ 0x08374a40 and its dyn bit is set; only span.z
      (+0x1c) is cleared. It then recursively duplicates pLeft (+0x08)
      and pRight (+0x0c), before cloning pList (+0x10) through
      sqlite3ExprListDup @ 0x083786c0 and pSelect (+0x38) through
      sqlite3SelectDup @ 0x08383cc4. Both latter callees were checked
      absent from names.yaml and src before this port, so their volatile
      dispatch slots default to NULL-returning stubs pending real ports;
      that is exact for NULL fields but deliberately drops non-NULL
      lists/selects until wired. The host test covers NULL, root OOM,
      token ownership/string copying, recursive child order, and both
      dispatches. On 64-bit host tests a typed widened Expr is copied;
      the target executes the original 0x44-byte memcpy.

  - address: 0x08377e00
    ghidra: FUN_08377e00
    name: expr_delete
    size: 88
    sig: "void expr_delete(Expr *expr)"
    module: sqlite/expr_delete
    status: ported
    vars: {param_1: expr}
    notes: >-
      SQLite 3.5.x's sqlite3ExprDelete, the single-argument build
      variant (no db parameter - the frees go straight to the global
      tracked allocator). 34 bl call sites. NULL is a no-op
      (movs/ldmiaeq). Frees the span text (+0x1c) when the span packed
      word (+0x20) has its dyn bit set (tst #1), then the token text
      (+0x14) on the token packed word (+0x18), both via sqlite3_free
      @ 0x083906f4 (the ported tracked_free, called directly);
      recurses into p_left (+0x08) then p_right (+0x0c) with direct
      self-calls; hands p_list (+0x10) to sqlite3ExprListDelete
      @ 0x08378670 and p_select (+0x38) to sqlite3SelectDelete
      @ 0x08383c88; the node's own block is the tail branch into
      sqlite3_free (post-order: every child fully released before the
      parent's block goes back). The list destructor is ported:
      0x08378670 (see its entry) is expr_list_delete, the shipped
      default of the SQLITE_EXPR_LIST_DELETE slot - the documented
      no-op stub is retained for host tests (with it installed the
      list teardown is skipped; the strings, operands and node are
      still released exactly as the original releases them). The
      select destructor 0x08383c88 is ported as
      [`crate::sqlite::select_delete::select_delete`], whose FROM-source
      cleanup now directly calls the ported `src_list_delete` at
      0x083843e8.
      The port is the shipped default of expr_new's SQLITE_EXPR_DELETE
      slot, replacing the documented no-op stub (retained for host
      tests, which still swap the slot for recording mocks) - the
      constructor's OOM path now really releases both operands. Expr
      reuses expr_new's typed repr(C) Token with 32-bit offset
      asserts; on a 64-bit host p_select keeps a cfg-split home at
      +0x48, matching expr_height::Expr's widened layout (the original
      +0x38 would land on the widened span's n_dyn word). match.py:
      same structure - the NULL guard, the two dyn-guarded frees in
      field order, the two recursive bl's, the two slot calls, the
      tail free; the diff is LLVM narrowing the tst #1 word loads to
      ldrb, inlining tracked_free's NULL guard + stats update (the
      accepted mem_release/value_free deviation), blx through the
      loaded slots, and LLVM's fp frame.

  - address: 0x0837857c
    ghidra: FUN_0837857c
    name: expr_list_append
    size: 208
    sig: "ExprList *expr_list_append(Parse *parse, ExprList *list, Expr *expr, const Token *name)"
    module: sqlite/expr_list_append
    status: ported
    vars: {param_1/r0: parse, r8: db, param_2/r4: list, param_3/r6: expr, param_4/r7: name, iVar2/r5: n_alloc, piVar3/r5: item}
    notes: >-
      SQLite 3.5.x's sqlite3ExprListAppend. Raw bytes confirm the
      208-byte extent 0x0837857c..0x0837864b; 0x0837864c starts the
      separately linked FUN_0837864c. A whole-image ARM B/BL scan found
      exactly 18 direct BL callers: 17 unconditional and one BLNE at
      0x0839ab20, with no tail B callers.
      The first instruction loads db from parse +0x00 before every
      allocator/name call; parse itself is dereferenced without a NULL
      guard. NULL list allocates a zeroed
      0x10-byte header through db_malloc_zero @ 0x08374998. When
      n_alloc <= n_expr it grows capacity to n_alloc*2+4 and reallocates
      the 0x0c-stride items through db_realloc @ 0x083749bc. A failed
      allocation first releases expr through expr_delete @ 0x08377e00,
      then list through expr_list_delete @ 0x08378670, and returns NULL.
      Otherwise non-NULL expr or name appends one all-zero item, obtains
      p_name through name_from_token @ 0x0837d7ac, then stores p_expr;
      both NULL leave the list unchanged. All five direct callees were
      already ported, so no dispatch seam was added. The Rust port shares
      typed repr(C) list/item layouts with the destructor; pointer fields
      widen on the host but 32-bit assertions retain the original
      0x10/0x0c target layouts. Host tests cover create/grow, token
      duplication and dequoting, full-list resize, two-NULL no-op,
      new-list OOM latching, and resize-OOM teardown.

  - address: 0x08383ca8
    ghidra: FUN_08383ca8
    name: select_dest_init
    size: 28
    sig: "void select_dest_init(SelectDest *destination, u8 destination_kind, int parameter)"
    module: sqlite/select_dest_init
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: destination_kind
      param_3/r2: parameter
      "destination+0x00": e_dest
      "destination+0x01": aff_sdst
      "destination+0x04": i_sd_parm
      "destination+0x08": i_sdst
      "destination+0x0c": n_sdst
    notes: >-
      SQLite 3.5.x's sqlite3SelectDestInit. Raw ARM confirms the exact
      28-byte extent: strb r1,[r0]; mov r1,#0; str r2,[r0,#4]; strb
      r1,[r0,#1]; str r1,[r0,#8]; str r1,[r0,#12]; bx lr. A complete
      osos.dec ARM B/BL decode finds 10 direct bl call sites, all
      unconditional (no predicated blne/bleq forms). It initializes the
      16-byte SelectDest: destination mode at +0x00, parameter at +0x04,
      and zeroes affinity/+0x01 plus the +0x08/+0x0c bookkeeping words;
      padding +0x02..+0x03 remains untouched. No dispatch seam: this
      function is a leaf with no direct callees. Rust preserves that
      store-only behavior and the unguarded destination dereference.
  - address: 0x083843e8
    ghidra: FUN_083843e8
    name: src_list_delete
    size: 112
    sig: "void src_list_delete(SrcList *source_list)"
    module: sqlite/src_list_delete
    status: ported
    renames:
      param_1/r0: source_list
      r4: item
      r5: index
      "source_list+0x00": n_src
      "item+0x00": z_database
      "item+0x04": z_name
      "item+0x08": z_alias
      "item+0x0c": p_table
      "item+0x10": p_select
      "item+0x1c": p_on
      "item+0x20": p_using
    notes: >-
      SQLite 3.5.x's sqlite3SrcListDelete. Raw ARM confirms the exact
      112-byte extent 0x083843e8..0x08384454: the next separately linked
      constructor opens at 0x08384458. A whole-image ARM B/BL decode finds
      9 direct BL call sites, all unconditional (0x082c36d8, 0x0836f164,
      0x083701cc, 0x08374760, 0x083751f8, 0x0837ca50, 0x08384268,
      0x083861b4, 0x0839aeb0), with zero predicated BL forms; five
      additional unconditional tail B callers are 0x0836eb84, 0x08375670,
      0x08375bfc, 0x08375cf0, and 0x083997a4. NULL returns without touching
      memory. Otherwise it re-reads signed n_src (+0x00) for a pre-tested
      bgt loop over inline 0x30-byte items beginning at +0x08. Each item
      frees zDatabase, zName, and zAlias through tracked_free @ 0x083906f4,
      then calls the table release entry @ 0x0837521c, ported select_delete
      @ 0x08383c88, ported expr_delete @ 0x08377e00, and the identifier-list
      release entry @ 0x0837b11c. It finally tail-branches to tracked_free
      with the source-list header. The two real but unported release entries
      are volatile no-op-default seams; all other calls are direct ports.
      Typed repr(C) header/item views retain target offsets and strides on
      ARM while intentionally widening pointer fields for host fixtures.
      Host tests prove NULL, signed negative-count, per-item ordering, and
      final-header-free behavior. match.py has the same NULL guard, signed
      inline-item loop, seven cleanup calls, and final free; LLVM adds its
      frame and emits blx through the two unported-release seams.

  - address: 0x08383c88
    ghidra: FUN_08383c88
    name: select_delete
    size: 32
    sig: "void select_delete(Select *select)"
    module: sqlite/select_delete
    status: ported
    vars: {param_1: select}
    notes: >-
      SQLite 3.5.x's sqlite3SelectDelete, the single-argument build
      variant (NULL is a no-op). Releases the select's owned
      substructures in field order: result list, FROM source list,
      WHERE, GROUP BY, HAVING, ORDER BY, prior select, LIMIT, OFFSET,
      then frees the Select block itself. The FROM-source list is now a
      direct call to ported src_list_delete @ 0x083843e8. match.py: same
      high-level structure - the NULL guard, owned-field teardown order,
      recursive prior deletion, and final free; the diff is LLVM's frame.
  - address: 0x08378670
    ghidra: FUN_08378670
    name: expr_list_delete
    size: 80
    sig: "void expr_list_delete(ExprList *list)"
    module: sqlite/expr_list_delete
    status: ported
    vars: {param_1: list, puVar6: item, iVar8: index}
    notes: >-
      SQLite 3.5.x's sqlite3ExprListDelete, the single-argument build
      variant (no db parameter - the frees go straight to the global
      tracked allocator). 22 bl call sites, binary-scanned: one in
      expr_delete @ 0x08377e3c, the rest across the parser/VDBE
      teardown paths. NULL is a no-op (movs/ldmiaeq). Otherwise walk
      the item array at +0x0c with stride 0xc under a signed bgt guard
      against n_expr (+0x00, re-read from the header every iteration):
      each item's p_expr (+0x00) goes back through sqlite3ExprDelete
      @ 0x08377e00 (the ported expr_delete, called directly - the same
      direct bl the original makes, mirroring expr_delete's own
      direct self-calls) and its alias name p_name (+0x04) is freed
      unconditionally through sqlite3_free @ 0x083906f4 (the ported
      tracked_free, whose internal NULL guard is the original's own
      sqlite3_free NULL check); then the items array and finally the
      list header, the latter as the tail branch into sqlite3_free.
      ExprList/ExprListItem are typed repr(C) structs with 32-bit
      offset and 0xc-stride asserts. The port is the shipped default
      of expr_delete's SQLITE_EXPR_LIST_DELETE slot, replacing the
      documented no-op stub (retained for host tests) - deleting an
      expression with an argument list now really releases the whole
      list instead of leaking it. Host tests cover the NULL no-op,
      empty/negative-count lists (array free, then header; a NULL
      array hits only the free's internal guard), in-order per-item
      teardown (expr, name, expr, name, then array and header), and
      the stride/n_expr walk boundary (a poisoned item past the count
      is never touched). match.py: same structure - the NULL guard,
      the pre-tested signed loop re-reading n_expr with the 0xc
      stride, the direct expr_delete call, the array and header frees;
      the diff is LLVM's fp frame, an early-out branch layout, and
      tracked_free's NULL guard + stats update inlined at each of the
      three free sites (the accepted mem_release/value_free
      deviation).

  - address: 0x0837b11c
    ghidra: FUN_0837b11c
    name: id_list_delete
    size: 68
    sig: "void id_list_delete(IdList *id_list)"
    module: sqlite/id_list_delete
    status: ported
    renames:
      param_1/r0: id_list
      r4: index
      r5: id_list
      "id_list+0x00": items
      "id_list+0x04": n_id
      "items+0x00": z_name
    notes: >-
      SQLite 3.5.x's sqlite3IdListDelete. Raw ARM confirms the exact
      68-byte extent 0x0837b11c..0x0837b15f; the separately linked
      identifier-list constructor opens at 0x0837b160. A whole-image ARM
      B/BL decode finds exactly nine direct BL callers, all unconditional:
      0x083701d4, 0x08375358, 0x083753b8, 0x0837b0f4, 0x0837ca68,
      0x08384330, 0x08384434, 0x08385204, and 0x08391a14. No predicated
      BL reaches it; 0x083997c4 is its sole unconditional tail-B caller.
      NULL returns before any dereference. Otherwise the signed pre-tested
      bgt loop re-reads n_id at +0x04, freeing each 8-byte item's z_name
      word at +0x00 in array order. It then frees items and tail-branches
      to sqlite3_free for the IdList header. All frees are direct calls to
      tracked_free @ 0x083906f4, including NULL names/items; its existing
      NULL guard is the original behavior. Typed repr(C) IdList/IdListItem
      views and 32-bit offset assertions preserve the target's 12-byte
      header and 8-byte item stride while avoiding overlapped host pointers.
      This replaces src_list_delete's former no-op identifier-list dispatch
      seam with the direct port. Host tests cover NULL, zero and negative
      counts, a NULL item array, NULL names, and ordered name/array/header
      release. match.py should retain the NULL early return, signed loop,
      three tracked frees, and final-header teardown; LLVM need not preserve
      the original tail branch.

  - address: 0x0837894c
    ghidra: FUN_0837894c
    name: expr_span
    size: 96
    sig: "void expr_span(Expr *expr, const Token *left_span, const Token *right_span)"
    module: sqlite/expr_span
    status: ported
    vars: {param_1: expr, param_2: left_span, param_3: right_span, param_4: scratch (phantom - r3 is dead on entry)}
    notes: >-
      SQLite 3.5.x's sqlite3ExprSpan, the span-merge dependency of
      expr_new (sqlite3Expr). 5 bl call sites: one in expr_new
      @ 0x08376894 (node, &left->span, &right->span), the other four in
      the 0x0839a grammar-action block. A NULL node or a NULL z in
      either child span is a no-op (predicated cmp/ldrne/bxeq chain -
      right->z checked before left->z); if either child span is
      heap-owned (dyn, bit 0 of the packed n:31|dyn:1 word at +0x04;
      left checked first via tst [r1,#4],#1) only span.z (+0x1c) is
      NULLed and the packed word (+0x20) is left alone; otherwise
      span.z = left_span->z and span.n_dyn = (old span.n_dyn & 1) |
      ((right.z - left.z + right.n) << 1) - the covering length with
      the node's own previous dyn bit preserved. Ghidra's decompile
      invents a fourth parameter (param_4, reused as a scratch slot);
      the original reads r0..r2 only, so the port is three-argument.
      Reuses expr_new's typed repr(C) Expr/Token (the 32-bit offset
      asserts live in expr_new.rs). The port is the shipped default of
      expr_new's SQLITE_EXPR_SPAN_MERGE slot, replacing the documented
      no-op stub (retained for host tests) - a token-less node with two
      spanned operands now gets the merged span instead of the
      allocator's zeroes. Host tests cover the NULL-expr and
      NULL-child-z no-ops (span untouched), both dyn orderings (only
      span.z cleared, n_dyn kept), the covering-length computation, and
      the dst dyn-bit preservation. match.py: same NULL/dyn guard
      chain, strne 0 to +0x1c, and the sub/add-shifted/and #1/orr
      length arithmetic into +0x20; the diff is LLVM's fp frame,
      branchy instead of predicated early returns, ldrb narrowing of
      the tst #1 word loads (the accepted expr_delete deviation), and
      a reassociated (bic #1; orr) form of the length<<1 | dyn merge.

  - address: 0x08378a44
    ghidra: FUN_08378a44
    name: alloc_deny_check
    size: 132
    sig: "int alloc_deny_check(int slot)"
    module: sqlite/mem
    status: ported
    vars: {param_1: slot, iVar1: record, iVar2: countdown, DAT_08378ac8: ALLOC_DENY_SCHEDULE (literal 0x08adc2cc = stats + 0x0c), DAT_08378acc: ALLOC_DENY_TOTAL (literal 0x08a09918, +0x18 word)}
    notes: >-
      SQLite's fault-injection allocation-deny schedule - the upstream
      test-harness sqlite3MemdebugFail deny-counter family, NOT
      sqlite3MallocAlarm (the soft-limit alarm is the ported
      tracked_stats_warn_soft_limit @ 0x0837d67c path). Verified against
      osos.asm and osos.dec: literal @ 0x08378ac8 = 0x08adc2cc = the
      stats block + 0x0c (exactly one 0x14-byte record fits before
      soft_limit @ +0x20); literal @ 0x08378acc = 0x08a09918, an
      otherwise-unreferenced BSS struct whose +0x18 word is a global
      denial counter. 2 bl call sites: sqlite3_malloc @ 0x08390b14 and
      sqlite3_realloc @ 0x08390eec, both with slot 0. Record layout
      (0x14 bytes, upstream's MemFault): +0x00 countdown, +0x04
      deny_budget, +0x08 benign_denies, +0x0c deny_count, +0x10 active
      (byte), +0x12 benign_mode (signed halfword). Inactive record:
      return 0 without draining the countdown. countdown > 0:
      decrement, proceed (cmp/subgt/strgt/bgt). Otherwise a denial:
      global counter++, deny_count++, benign_denies++ when benign_mode
      > 0 (ldrsh), deny_budget-- and the active byte cleared when the
      budget runs out (movle/strble), return 1 - the caller fails the
      allocation without touching the heap. Deviations: the table is
      the ALLOC_DENY_SCHEDULE static and the counter the
      ALLOC_DENY_TOTAL static instead of stats+0x0c / the 0x08a09918
      BSS struct (the heap::tracked ALLOC_STATS simplification). The
      port is the wired ALLOC_PRESSURE_OPS.alloc_deny_check default,
      replacing the documented at-rest stub; the slot stays for
      host-test mocks (at rest the schedule is BSS-inactive, so the
      default proceeds exactly like the stub did). Host tests: at-rest
      proceed touching nothing, the inactive-before-countdown gate
      ordering, countdown draining before the first denial, budget
      exhaustion self-deactivating the record, the signed benign_mode
      gate (positive/zero/negative), and the slot default identity.
      match.py structural: same add-shifted slot*0x14 index, ldrb
      active gate, subgt/strgt countdown, ldrsh/ldrgt benign path and
      sub/cmp/str/movle/strble budget tail - the diff is LLVM's fp
      frame, register shuffling, and ALLOC_DENY_TOTAL addressed
      directly (no +0x18, documented deviation).

  - address: 0x08378ad0
    ghidra: FUN_08378ad0
    name: find_collation_encoding
    size: 52
    sig: "CollSeq *find_collation_encoding(sqlite3 *db, unsigned encoding, const char *name, int name_len, int create)"
    module: sqlite/find_collation_encoding
    status: ported
    vars: {param_1: db, param_2: encoding, param_3: name, param_4: name_len,
      param_5: create, iVar1: collation_set, iVar2: selected_collation}
    notes: >-
      Exact raw extent 0x08378ad0..0x08378b04 (the next body starts at
      0x08378b04). 13 bl call sites, binary-scanned over every ARM B/BL word
      in osos.dec, all unconditional with no predicated forms: 0x082c5274,
      0x082c5340, 0x082d87fc, 0x082d89cc, 0x082dbf3c, 0x082dbf80,
      0x08378e54, 0x0837a1b4, 0x0837a1fc, 0x0837a244, 0x0837b7c0,
      0x0837d1c0, 0x08381e78. The movs on name selects either sqlite3 word
      11 (+0x2c) or the result of direct helper 0x082ce220(db, name,
      name_len, create); NULL remains NULL, otherwise the add/add-shift/sub
      tail returns collation_set + (encoding - 1) * 20. The helper is not
      independently ported or named in this ledger, so the port deliberately
      uses a volatile seam: target builds call its verified retail address and
      host tests record its four arguments. The 20-byte record stride and
      1-based encoding follow raw address arithmetic; valid encodings are
      SQLite's 1..3. Host tests cover NULL-name bypass/default selection,
      named forwarding and third-record selection, lookup failure, and all
      three record positions.

  - address: 0x0837b2f4
    ghidra: FUN_0837b2f4
    name: index_key_info
    size: 160
    sig: "KeyInfo *index_key_info(Parse *parse, const Index *index)"
    module: sqlite/index_key_info
    status: ported
    renames:
      param_1/r8: parse
      param_2/r7: index
      iVar4/r6: n_column
      puVar1/r5: key_info
      iVar3/r4: column
      FUN_08374998: db_malloc_zero
      FUN_0837d18c: sqlite3LocateCollSeq
      FUN_083906f4: tracked_free
    notes: >-
      Raw osos.dec fixes the exact extent at 0x0837b2f4..0x0837b394: 40
      ARM words ending in pop {r4-r8,pc}, followed immediately by the next
      separately linked stmdb at 0x0837b394; no literal pool. Decoding every
      ARM B/BL immediate in the image finds exactly 8 inbound direct calls,
      all unconditional bl (no predicated call): 0x082b41cc, 0x08378e9c,
      0x0837da08, 0x083818f0, 0x08385b20, 0x0838dfb0, 0x083992b0, and
      0x083992e0. This is SQLite 3.5.9's sqlite3IndexKeyinfo: it loads db
      from parse + 0x00, requests 5*nColumn+16 zeroed bytes from the ported
      db_malloc_zero, stores db, nField and aSortOrder = aColl+nField,
      resolves every azColl[i] through sqlite3LocateCollSeq(parse, name, -1),
      and copies aSortOrder[i]. The nColumn loop uses signed blt and completes
      even if lookup records parse->nErr; only after the loop does the
      original check its +0x40 error word, free the fresh KeyInfo through
      ported tracked_free, and return NULL. sqlite3LocateCollSeq
      @ 0x0837d18c has no ported ledger entry, so the deliberate volatile
      INDEX_KEY_INFO_HOOKS seam calls that verified retail address on target
      and a recording hook on host. Target-width pointers are read and written
      as u32 words, avoiding host pointer-width field overlap. Host tests
      cover multi-column layout and arguments, allocation failure, completion
      of all lookups before cleanup after a latched failure, and nColumn=0.

  - address: 0x082ce2e8
    ghidra: FUN_082ce2e8
    name: find_element_given_hash
    size: 120
    sig: "HashElem *find_element_given_hash(const Hash *hash, const void *key, int key_len, uint h)"
    module: sqlite/find_element_given_hash
    status: ported
    vars: {param_1: hash, param_2: key, param_3: key_len, param_4: h,
      piVar2: bucket, puVar3: elem, iVar4: count, pcVar5: compare,
      DAT_082ce360: BIN_COMPARE_ADDR (pool word 0x082ac998),
      DAT_082ce364: STR_COMPARE_ADDR (pool word 0x08386ef8)}
    notes: >-
      SQLite 3.5.x hash.c's findElementGivenHash with
      findCompareFunction inlined (cmp keyClass,#3 / ldreq strCompare
      / ldrne binCompare). 2 bl call sites, binary-scanned:
      sqlite3HashFind @ 0x0837ad88 (bl @ 0x0837add8 - the ported
      hash_find, whose HASH_FIND_HOOKS find_elem slot now defaults to
      this port, replacing the not-found stand-in; the port is the
      point of this entry) and sqlite3HashInsert @ 0x0837ae08 (bl @
      0x0837ae64 - the duplicate-key probe of the insert/replace
      path). Body: ht @ +0x10 NULL -> NULL (hash itself is never
      NULL-checked, both callers pass a live Hash); bucket = ht + h*8
      of {count @ +0x00, chain @ +0x04} pairs; while (count-- && elem)
      compare(elem->pKey @ +0x0c, elem->nKey @ +0x10, pKey, nKey) == 0
      -> return elem, else elem = elem->next @ +0x00; exhausted ->
      NULL. Pins HashElem.nKey @ +0x10 (exactly where hash_find's
      scouting note predicted; the field was added to hash_clear's
      HashElem struct with a 32-bit offset assert) and the 8-byte
      bucket pair (exported as the Bucket struct). The two comparator
      pool words are runtime addresses in Ghidra-undecoded gaps,
      decoded by hand to the same 28-byte upstream shape (if (n1 !=
      n2) return 1; return cmp(p1, p2, n1)): strCompare runtime
      0x08386ef8 / image 0x08391dd0 (directly before strHash @ image
      0x08391dec), tail-calling the ported str_nicmp @ 0x08384fa0;
      binCompare runtime 0x082ac998 / image 0x082b7870 (directly
      before binHash @ image 0x082b788c), tail-calling the ported
      memcmp @ 0x08030f64. Both identified, not yet ported - kept
      behind FIND_ELEMENT_HOOKS, which models the two literal-pool
      words as raw usize addresses (a host const cannot hold a fn
      pointer to firmware; the transmute happens at the call site,
      the hash_function convention), defaults transparent to
      on-target hooks. Deviations: the seam slot load replaces the
      two literal-pool ldrs but keeps the single indirect blx;
      `hash` is not NULL-checked, like the original. match.py
      structural: same ht NULL guard, ldrb keyClass / cmp #3 select,
      ht + h*8 bucket index, comparator blx'd with (elem+0x0c,
      elem+0x10, pKey, nKey), count-- chain walk, elem-or-NULL tail -
      the diff is LLVM's fp frame, register shuffling, and a rotated
      loop pre-test (count != 0 && elem != 0 hoisted, same semantics).
      Host tests (11): stock-address defaults, NULL-ht short-circuit,
      empty bucket (count == 0 never consults a live chain), single
      hit with exact comparator args, chain walk first/middle/last
      with stop-at-first-match, mismatch skip, not-found NULL,
      short-count truncation, long-count stopped by the NULL link,
      bucket-index selection, and binCompare selection for every
      class != 3 (incl. the length gate). hash_find gained an
      integration test through the real default walker: a live
      8-bucket table laid out via the ported string_hash_tabled,
      comparators stubbed as the stock shapes over the ported
      str_nicmp - hits, a case-folded hit, and an absent-key miss.

  - address: 0x082cf328
    ghidra: FUN_082cf328
    name: store_u32_le
    size: 32
    sig: "void store_u32_le(u8 *p, u32 value)"
    module: sqlite/store_u32_le
    status: ported
    vars: {param_1: p, param_2: value}
    notes: >-
      Alignment-free four-byte little-endian store. The original shifts
      value right by 8, 16, and 24, then issues strb to p+0 through p+3,
      placing bits 7..0 first. It has no NULL check and writes precisely
      that four-byte range. The two recovered callers (0x082e39f4 and
      0x082e3bcc) use it to materialize a 32-bit temporary before their
      format-specific writer. A scan of every 32-byte function in
      functions.csv found no byte-identical alias.

  - address: 0x082d2a1c
    ghidra: FUN_082d2a1c
    name: hash_function
    size: 16
    sig: "int (*hash_function(int key_class))(const void *key, int key_len)"
    module: sqlite/hash_function
    status: ported
    vars: {param_1: key_class}
    notes: >-
      SQLite 3.4.x/3.5.x hash.c's hashFunction: returns &strHash when
      keyClass == SQLITE_HASH_STRING (3 - this build's hash.h has
      SQLITE_HASH_INT=1/_POINTER=2 compiled out, STRING=3, BINARY=4),
      &binHash otherwise. 4 instructions (cmp/ldrne/ldreq/bx) plus an
      8-byte literal pool at 0x082d2a2c/0x082d2a30 holding the two code
      pointers. 3 bl call sites: rehash @ 0x0836741c, sqlite3HashFind
      @ 0x0837ad88, sqlite3HashInsert @ 0x0837ae08 - all ldrb keyClass
      from Hash+0x00, blx the returned pointer with (pKey, nKey), and
      mask with htsize-1. Targets: strHash @ runtime 0x08386f14 = image
      0x08391dec under the +0xaed8 skew (see sqlite/mod.rs), already
      ported as string_hash_tabled - this dispatcher is the function
      pointer its scouting note predicted; binHash @ runtime 0x082ac9b4
      = image 0x082b788c, in a Ghidra-undecoded gap, decoded by hand
      (h = byte ^ h ^ (h<<3) per byte, raw - no case fold, no strlen
      probe, return h & 0x7fffffff) - identified, not yet ported. The
      port returns the stock runtime addresses like the original's
      literal pool does, so it stays transparent to hooks planted on
      either hash's stock address. match.py: same cmp #3, same two
      literal-pool words, predicated select - the diff is LLVM frame
      setup and register shuffling instead of predicated loads.

  - address: 0x082d2a34
    ghidra: FUN_082d2a34
    name: expr_height_of
    size: 24
    sig: "void expr_height_of(Expr *child, int *height)"
    module: sqlite/expr_height_of
    status: ported
    vars: {param_1: child, param_2: height, param_3: _dead}
    notes: >-
      SQLite 3.5.x's exprHeight. Leaf, 6 instructions, no prologue:
      cmp/ldrne/ldrne/cmpne/strgt - a NULL-safe signed maximum fold of
      the child's cached n_height (+0x40) into the caller's accumulator
      (`if (child && child->nHeight > *height) *height = child->nHeight;`).
      Called from expr_set_height @ 0x083788fc (twice), heightOfExprList
      @ 0x082d2a4c (once) and heightOfSelect @ 0x082d2a8c (four times).
      Wired as the default of the SQLITE_EXPR_HEIGHT_FOLD dispatch
      static in sqlite/expr_height.rs, replacing the documented no-op
      stub (host tests still swap the slot for recording mocks). The
      match.py diff is LLVM frame setup and a branchy NULL check
      instead of predicated loads; the fold core (ldr +0x40, ldr, cmp,
      strgt) matches exactly.

  - address: 0x082d2a4c
    ghidra: FUN_082d2a4c
    name: expr_list_height
    size: 64
    sig: "void expr_list_height(ExprList *list, int *height)"
    module: sqlite/expr_list_height
    status: ported
    vars: {param_1: list, param_2: height}
    notes: >-
      SQLite 3.5.x's heightOfExprList. Called from expr_set_height @
      0x083788fc (once) and heightOfSelect @ 0x082d2a8c (three times).
      NULL-safe counted fold: if the list is non-NULL (movs/ldmiaeq
      early return), walk its n_expr entries (+0x00) - a 12-byte-stride
      item array at +0x0c whose first word is the Expr * - folding each
      item's p_expr through exprHeight @ 0x082d2a34 (ported as
      expr_height_of, called directly). The loop guard is a signed bgt,
      so a zero or negative n_expr folds nothing. Pins the ExprList
      layout: n_expr +0x00, items +0x0c, item stride 12, p_expr at item
      +0x00. Wired as the default of the SQLITE_EXPR_LIST_HEIGHT_FOLD
      dispatch static in sqlite/expr_height.rs, replacing the documented
      no-op stub (host tests still swap the slot for recording mocks).
      The match.py diff is LLVM frame setup and a branchy NULL check
      instead of predicated pops; the loop core (ldr +0x0c, 12-byte
      stride, bl exprHeight, ldr +0x00, cmp/bgt) matches exactly.

  - address: 0x082d2a8c
    ghidra: FUN_082d2a8c
    name: select_height
    size: 112
    sig: "void select_height(Select *select, int *height)"
    module: sqlite/select_height
    status: ported
    vars: {param_1: select, param_2: height}
    notes: >-
      SQLite 3.5.x's heightOfSelect. Called from expr_set_height @
      0x083788fc (once) and sqlite3SelectExprHeight @ 0x08383dd0 (once).
      NULL-safe fold over a compound-select chain: while the select is
      non-NULL (movs/ldmiaeq early return), fold its clause expressions
      p_where +0x10, p_having +0x18, p_limit +0x2c, p_offset +0x30
      through exprHeight @ 0x082d2a34 (ported as expr_height_of, called
      directly) and its lists p_elist +0x00, p_group_by +0x14,
      p_order_by +0x1c through heightOfExprList @ 0x082d2a4c (ported as
      expr_list_height, called directly) - the exact field order of the
      3.5.x source - then tail-loop onto p_prior +0x20 (b 0x082d2a90
      back to the NULL check). Pins the Select layout used by this fold:
      p_elist +0x00, p_where +0x10, p_group_by +0x14, p_having +0x18,
      p_order_by +0x1c, p_prior +0x20, p_limit +0x2c, p_offset +0x30.
      Wired as the default of the SQLITE_SELECT_HEIGHT_FOLD dispatch
      static in sqlite/expr_height.rs, replacing the documented no-op
      stub (host tests still swap the slot for recording mocks) - with
      this port all three expr-height fold helpers are real ports. The
      match.py diff is LLVM frame setup and a branchy top-of-loop NULL
      check instead of predicated pops; the fold core (same offsets,
      same clause/list order, the ldr +0x20 tail loop) matches exactly.

  - address: 0x082d2ba0
    ghidra: FUN_082d2ba0
    name: hex_to_int
    size: 24
    sig: "u32 hex_to_int(u32 h)"
    module: sqlite/hex_to_int
    status: ported
    vars: {param_1: h}
    notes: >-
      SQLite 3.5.x's sqlite3HexToInt (util.c, SQLITE_ASCII build).
      Called from hexToBlob @ 0x0837b00c twice (once per nibble of
      each output byte when packing an x'ABCD' blob literal).
      Branchless ASCII hex-digit decode: bit 6 of the character
      separates the digit band ('0'..'9', bit 6 clear) from the letter
      bands ('A'..'F'/'a'..'f', bit 6 set); add 9 when bit 6 is set and
      mask with 0xf, landing every band's low nibble on the value:
      (h + 9*(1 & (h>>6))) & 0xf, character-for-character the 3.5.x
      source. The original returns the full register (the (u8) cast in
      the source is just the 0xf mask) and validates nothing, so
      non-hex input yields whatever the bit formula gives. The match.py
      diff is cosmetic: LLVM folds the shift-out/shift-back bit-6
      extract into `tst r0,#0x40` + `addne r0,r0,#9` (same branchless
      +9 correction, same mask, 6 instructions each side).

  - address: 0x082d2bb8
    ghidra: FUN_082d2bb8
    name: blob_to_hex
    size: 160
    sig: "u8 *blob_to_hex(const u8 *blob, i32 len)"
    module: sqlite/blob_to_hex
    status: ported
    vars: {param_1: blob, param_2: len, puVar2: out, puVar4: cursor, iVar5: index, iVar1: digits}
    notes: >-
      2 bl call sites (0x080aedfc, 0x080aee4c), both in the keyid/serial
      formatter 0x080aede0, which hands the result to the string builder
      0x0806ea5c with "keyid"/"serial" and frees it through 0x08043994.
      Format a byte string as colon-separated uppercase hex ("01:23:AB"):
      NULL blob or len == 0 (cmpne; negatives pass) returns NULL without
      touching the heap; otherwise len*3+1 bytes (wrapping shift-add)
      from traced_alloc @ 0x08043c18 (ported, called directly), alloc
      failure records the packed tag triple (0x22, 0x6f, 0x41, 0, 0) in
      the diagnostic ring buffer @ 0x08049a84 and returns NULL; each
      byte then emits digits[b>>4], digits[b&0xf], ':' and the final
      ':' is overwritten with the NUL terminator. The digit table rides
      the literal-pool pointer @ 0x082d2c58 = 0x08a0eddc; at +0xaed8
      (the image/runtime skew documented in sqlite/mod.rs) the image
      holds exactly "0123456789ABCDEF\0\0\0\0" there, so it is the
      standard uppercase table (crate static HEX_DIGITS in the port).
      len stays signed end to end (blt loop); a negative len wraps
      len*3+1 negative and traced_alloc's own size<=0 guard takes the
      diagnostic path before any underlying alloc call - except for the
      negatives whose len*3+1 wraps back POSITIVE (e.g. -0x55555555 ->
      2): those allocate, skip the loop, and land the terminator store
      one byte before the block. Faithful hazard, reproduced and tested
      against a guard byte; no caller can reach it. Note also that the
      block is one byte larger than the string it holds (2n digits +
      n-1 separators + NUL = 3n written out of 3n+1 requested), so the
      last byte of every block stays uninitialized - reproduced, not
      tightened. Deviations: the ring-buffer logger 0x08049a84 is
      unported (walks a per-task record block from 0x080498f8 and packs
      its first three args as a0<<24 | (a1&0xfff)<<12 | (a2&0xfff)) and
      is the HEX_BLOB_HOOKS dispatch boundary, default a documented
      no-op recorder (the NULL return the caller sees is unchanged).
      match.py: same structure - entry guard, add r0,r4,r4,lsl#1 +1
      alloc with zero tags, the ldrb [table, b>>4] / and #15 / ':'
      loop, terminator at [r1,#-1], diag path with 0x22/0x6f/0x41 and
      the fifth arg on the stack; the diff is the LLVM frame pointer, a
      count-down loop with a 3-byte stride, the hook-slot blx for the
      diag call, and the diag block sunk past the return. Host tests
      (12) cover the reference, zero/NULL/one-byte, all 256 byte
      values, high-bit bytes, the exact allocation request, the
      untouched spare byte, alloc failure, and both negative-len paths;
      they mock TRACED_ALLOC_HOOKS under the new shared
      testing::TRACED_ALLOC_TEST_LOCK (the ata_cmd allocator tests now
      take the same lock instead of a private one).

  - address: 0x083787a8
    ghidra: FUN_083787a8
    name: sqlite_resolve_expr_names
    size: 196
    sig: "i32 sqlite_resolve_expr_names(NameContext *context, Expr *expr)"
    module: sqlite/resolve_expr
    status: identified
    renames:
      param_1/r0: context
      param_2/r1: expr
      DAT_0837889c: expression_visitor
    notes: >-
      Raw ARM confirms the 196-byte executable extent
      0x083787a8..0x0837886c, followed by its error string literal pool
      through 0x0837889c; the separately linked next function begins at
      0x083788a0. The code returns 0 for a NULL expr, rejects an expression
      when context->parse->expression_height + expr->n_height exceeds
      context->parse->db's limit at +0x5c, otherwise increments that height,
      invokes sqlite_expr_walk @ 0x0839866c with the literal visitor pointer
      0x082cf9f0, then decrements the height and updates the context and Expr
      flags. This matches the role of SQLite's expression-name resolver, but
      the visitor identity is deliberately not asserted. That literal points
      12 bytes into FUN_082cf9e4, not a callable function entry: raw bytes at
      0x082cf9f0 immediately use r4 inherited from sqlite_expr_walk and on
      its normal condition execute popeq {r4,r5,r6,pc}, consuming the walker
      frame rather than returning to its blx callback call site. A Rust call
      to the already-ported sqlite_expr_walk cannot preserve this stock-stack
      nonlocal exit. No dispatch seam is added because names.yaml has no
      ported callable identity for 0x082cf9f0. Exactly 13 direct BL words
      target this entry, all unconditional (none predicated): 0x082d9270,
      0x08367fe8, 0x083730c4, 0x08374d78, 0x08375f18, 0x0837be8c,
      0x08383f38, 0x08383f4c, 0x08383f8c, 0x08383fec, 0x08384000,
      0x08385564, and 0x08385974. This port is deferred until the
      stack-dependent visitor/continuation can be recovered faithfully.

  - address: 0x083788fc
    ghidra: FUN_083788fc
    name: expr_set_height
    size: 80
    sig: "void expr_set_height(Expr *expr)"
    module: sqlite/expr_height
    status: ported
    vars: {param_1: expr, local_10: height}
    notes: >-
      SQLite 3.5.x's sqlite3ExprSetHeight. 7 bl call sites, including
      the tail of sqlite3Expr @ 0x08376808. Seeds a running signed
      maximum at 0, folds the cached subtree heights of the node's four
      child slots into it - p_left +0x08 and p_right +0x0c via
      exprHeight @ 0x082d2a34 (ported as expr_height_of, see its
      entry), p_list +0x10 via heightOfExprList
      @ 0x082d2a4c (ported as expr_list_height, see its entry), p_select +0x38 via
      heightOfSelect @ 0x082d2a8c (ported as select_height, see its
      entry) - then stores max + 1 into n_height +0x40
      (plain wrapping add). All three fold helpers are ported; the call
      sites keep the dispatch-static shape (SQLITE_EXPR_HEIGHT_FOLD,
      SQLITE_EXPR_LIST_HEIGHT_FOLD, SQLITE_SELECT_HEIGHT_FOLD -
      defaults now the real ports, the documented no-op stub retained
      for host tests). The
      match.py diff is
      exactly that deviation: blx
      through the loaded slots instead of direct bl's, plus LLVM frame
      setup.

  - address: 0x0837acf4
    ghidra: FUN_0837acf4
    name: get_vdbe
    size: 56
    sig: "Vdbe *get_vdbe(Parse *parse)"
    module: sqlite/vdbe
    status: ported
    vars: {param_1: parse}
    notes: >-
      SQLite's sqlite3GetVdbe. 46 bl call sites. Returns pParse->pVdbe
      (+0x0c), creating it with sqlite3VdbeCreate @ 0x08386c44 on
      pParse->db and then calling 0x083867f8(v, 20) - an AddOp0-shaped
      helper emitting opcode 20 - when it was NULL. The pVdbe store is
      unconditional even on creation failure. The unported constructor
      @ 0x08386c44 goes through the VDBE_CREATE_OPS dispatch boundary
      (default slot always fails, mimicking OOM); the AddOp0 helper is
      semantically vdbe_add_op3(v, op, 0, 0, 0), so the ported
      vdbe_add_op3 is called directly.

  - address: 0x0837d918
    ghidra: FUN_0837d918
    name: open_table
    size: 132
    sig: "void open_table(Parse *parse, int cursor, int database_index, Table *table, int opcode)"
    module: sqlite/open_table
    status: ported
    renames:
      param_1/r0: parse
      param_2/r1: cursor
      param_3/r2: database_index
      param_4/r3: table
      param_5/[sp]: opcode
      r5: vdbe
    notes: >-
      SQLite 3.5.9's sqlite3OpenTable. Raw ARM has exactly 132 bytes
      (0x0837d918..0x0837d998; next function 0x0837d99c) and 10 direct
      BL sites, all unconditional: 0x082b56b8, 0x082b57f8, 0x08374dfc,
      0x08378db8, 0x0837d9f0, 0x0838193c, 0x08385aac, 0x0838df14,
      0x08399060, 0x083990e0. Virtual tables (Table.isVirtual at +0x39)
      return before touching the Parse state. Otherwise, record the
      shared-cache lock with table.tnum (+0x14), write flag (opcode == 9),
      and borrowed table name, then emit OP_SetNumColumns 0, table.nCol
      (literal 0x62) and the supplied open opcode cursor, table.tnum, iDb.
      The direct sqlite3GetVdbe call at 0x0837acf4 cannot target a Rust
      symbol: its ledger entry says ported but the tree has none, as
      begin_write_operation documents. This port reads Parse.pVdbe directly,
      matching that module's configured stand-in and the unported
      constructor's NULL/OOM end state.

  - address: 0x0837ad2c
    ghidra: FUN_0837ad2c
    name: hash_clear
    size: 92
    sig: "void hash_clear(Hash *hash)"
    module: sqlite/hash_clear
    status: ported
    vars: {param_1: hash, piVar2: elem, iVar3: next, iVar1: key, bVar4: copy_key}
    notes: >-
      SQLite 3.4.x/3.5.x hash.c's sqlite3HashClear, the destructor half
      of the Hash machinery hash_init @ 0x0837ade8 (ported, see its
      entry) constructs. 7 bl call sites in two functions, both
      binary-verified: sqlite3SchemaClear @ 0x08382c58 (4 sites -
      fkeyHash @ +0x40 and idxHash @ +0x18 directly, plus the two
      stack snapshots of tblHash/trigHash at sp+0x04/sp+0x18 taken
      before the delete loops; the live Schema hashes are re-stamped
      by hash_init right after) and closeDatabase @ 0x0838f890 (3
      sites - the db handle's +0x128/+0xf4/+0x114 aCollSeq/aModule/
      aFunc-family hashes on the sqlite3_close path). Body: detach
      first @ +0x0c and NULL it, free the ht bucket array @ +0x10
      through sqlite3_free @ 0x083906f4 (the ported
      heap::tracked::tracked_free, called directly - LLVM inlines it
      into the body, as in sqlite/mem.rs; same bytes, different shape
      in the match.py diff), NULL ht and zero htsize @ +0x08, then
      walk the chain: next = elem->next @ +0x00, and iff copyKey
      @ +0x01 (ldrb, reloaded each round) and elem->pKey @ +0x0c are
      both non-NULL (fused ldrne/cmpne/blne) free the key first, then
      free the node; count @ +0x04 is the final zero store. Confirms
      the 20-byte Hash layout hash_init pins and pins this build's
      HashElem node layout: next @ +0x00 and the strdup'd key @ +0x0c
      (the pKey of 3.4.x's {next, prev, data, pKey, nKey}). Deviation
      from upstream: no `if( pH->ht )` wrapper - the free runs
      unconditionally and tracked_free's internal NULL guard is the
      original's own. Host tests (5) cover the empty hash (nothing
      freed, flags survive), ht-first-then-chain free order, copyKey
      key-before-node order with a NULL-key node, the copyKey-clear
      never-loads-the-key case, and full field zeroing; they route
      frees through the shared veneers::tests::mock_heap with a
      recording free, against hand-built tag-57 tracked blocks.

  - address: 0x0837ad88
    ghidra: FUN_0837ad88
    name: hash_find
    size: 96
    sig: "void *hash_find(const Hash *hash, const void *key, int key_len)"
    module: sqlite/hash_find
    status: ported
    vars: {param_1: hash, param_2: key, param_3: key_len, iVar1: elem,
      pcVar2: hashfn, uVar3: data}
    notes: >-
      SQLite 3.4.x/3.5.x hash.c's sqlite3HashFind, the read path of the
      Hash machinery hash_init @ 0x0837ade8 and hash_clear @ 0x0837ad2c
      (both ported) bracket. 11 bl call sites, binary-scanned: the
      find-or-create helper @ 0x082ce220 (db +0x128 aCollSeq-family
      hash by strlen'd name, malloc+fill+sqlite3HashInsert on miss -
      the sqlite3FindFunction shape), the db +0xf4 aModule-family
      lookup @ 0x0838d52c (result stored at caller +0x3c), a +0x2c
      trigHash-style lookup @ 0x0836fecc, and 0x082c52d8 / 0x08375c98 /
      0x083761bc / 0x08378bbc / 0x08378ffc / 0x08379094 / 0x083794e0 /
      0x08385354. Body: NULL hash or NULL ht @ +0x10 -> NULL
      (predicated movs/ldrne/cmpne/moveq); otherwise ldrb keyClass
      @ +0x00, bl hash_function @ 0x082d2a1c (ported), blx the result
      with (pKey, nKey), bl __rt_sdiv @ 0x08031568 against htsize
      @ +0x08 and take the REMAINDER (mov r3,r1 - upstream's inlined
      sqlite3HashValue `% pH->htsize`, not a mask; both key hashes
      return h & 0x7fffffff, so the signed remainder is the unsigned
      one on every reachable path), then bl findElementGivenHash
      @ 0x082ce2e8 with (hash, pKey, nKey, bucket) and return
      elem ? elem->data @ +0x08 : NULL. Pins HashElem.data at +0x08
      (upstream 3.4.x {next, prev, data, pKey, nKey}, agreeing with
      hash_clear's next @ +0x00 / key @ +0x0c); the walker's disasm
      (bucket = ht + h*8 of {count, chain} pairs, comparator blx'd
      with (elem->key @ +0x0c, elem->nKey @ +0x10, pKey, nKey)) also
      pins HashElem.nKey @ +0x10 for its future port. findElementGivenHash
      @ 0x082ce2e8 is identified but unported - reached through the
      find_elem slot of HASH_FIND_HOOKS, whose shipped default is a
      "not found" stand-in; the hash_dispatch slot defaults to the real
      ported hash_function and exists so host tests can substitute a
      host-callable dispatcher (the real one returns firmware runtime
      addresses, callable on-target only). Deviations: both callees go
      through the volatile hook slots (indirect blx replaces two direct
      bls - the house seam convention, cf. sqlite/cell_size); LLVM
      inlines the ported __rt_sdivmod into the body, so the
      bl 0x08031568 boundary does not survive (the hash_clear /
      tracked_free precedent). match.py: same NULL-guard prologue,
      same key-hash blx, same cmp/ldrne data-or-NULL tail; the diff is
      LLVM's fp frame, the slot indirection, and the inlined divide.
      Host tests (8): default slot wiring, NULL-hash / NULL-ht
      short-circuits that never reach the hooks, keyClass dispatch
      verbatim (3 = STRING and 4 = BINARY), negative-nKey pass-through
      (the strlen probe contract), bucket = h % htsize as a remainder
      (13 % 8 = 5; htsize 1 buckets 0x7fffffff to 0), found element ->
      data word @ +0x08, not-found -> NULL.

  - address: 0x0837ade8
    ghidra: FUN_0837ade8
    name: hash_init
    size: 32
    sig: "void hash_init(Hash *hash, u8 key_class, u8 copy_key)"
    module: sqlite/hash_init
    status: ported
    vars: {param_1: hash, param_2: key_class, param_3: copy_key}
    notes: >-
      SQLite 3.4.x/3.5.x hash.c's sqlite3HashInit. 9 bl call sites in
      three functions: openDatabase @ 0x082dbda8 (3 sites - the fresh
      0x180-byte sqlite3 handle's aModule/aFunc/aCollSeq-family hashes
      at +0xf4/+0x114/+0x128, all (SQLITE_HASH_STRING, 0)),
      sqlite3SchemaClear @ 0x08382c58 (trigHash @ +0x2c and tblHash
      @ +0x04, re-initialized to pristine after the trigger/table
      delete loops), and sqlite3SchemaGet @ 0x08382d18 (all four
      Schema hashes @ +0x04/+0x18/+0x2c/+0x40 when file_format==0;
      fkeyHash alone gets copyKey=1). Body: strb keyClass @ +0x00,
      strb copyKey @ +0x01, zero the words at +0x04 (count), +0x08
      (htsize), +0x0c (first), +0x10 (ht) - 8 stores, no prologue.
      Pins this build's Hash layout (20 bytes; hence the 0x14-spaced
      Schema hashes and the 0x14 memcpys in sqlite3SchemaClear). The
      field order deviates from upstream's count/first/htsize: htsize
      is at +0x08 and first at +0x0c here, proven by sqlite3HashInsert
      @ 0x0837ae08 doubling the +0x08 word and by sqlite3HashClear
      @ 0x0837ad2c walking the +0x0c list and freeing the +0x10
      bucket array. Deviation from upstream: copyKey is stored
      verbatim (upstream ANDs it with keyClass>=SQLITE_HASH_STRING;
      the asserts are compiled out NDEBUG) - every observed caller
      passes the final 0/1 already. match.py structural: same two
      strb + four str-zero shape; the diff is LLVM's fp frame and
      register allocation.

  - address: 0x08382ddc
    ghidra: FUN_08382ddc
    name: schema_to_index
    size: 72
    sig: "int schema_to_index(sqlite3 *db, Schema *schema)"
    module: sqlite/schema_to_index
    status: ported
    vars: {param_1: db, param_2: schema}
    notes: >-
      SQLite 3.5.x prepare.c's sqlite3SchemaToIndex, identified by the
      -1000000 (0xfff0bdc0) literal-pool sentinel @ 0x08382e24 that
      upstream returns for a NULL pSchema (transient sub-select tables
      in expr.c). 72 bytes of code plus the 4-byte literal; the next
      function starts at 0x08382e28 (Ghidra omits this one from its C
      listing entirely - only its callers are decompiled). 33 bl call
      sites, binary-scanned over every B/BL word in osos.dec, matching
      Ghidra's count; all unconditional, no predicated forms. Body:
      if pSchema==NULL return -1000000 WITHOUT dereferencing db (the
      beq precedes the nDb load - a behavioural fact the host test
      proves with a NULL db); else scan db->aDb[i].pSchema for i in
      0..nDb (signed bgt guard), return i on match, nDb on no match
      (upstream's assert(i<nDb) is compiled out NDEBUG). Pins this
      build's Db layout: 24-byte stride (upstream 16) with pSchema
      at +0x14 (upstream +0x0c), proven by openDatabase @ 0x082dbda8
      pointing the 0x180-byte handle's aDb (+0x08) at handle+0x14c
      (an inline two-entry array, 2*0x18=0x30) and routing
      sqlite3SchemaGet's result into aDb[0]+0x14 / aDb[1]+0x14.
      Caller shape: FUN_082b45d4 and FUN_082b4124 feed the result to
      sqlite3VdbeAddOp* operands and index aDb[i]+0x00. match.py
      structural: same NULL-sentinel, same nDb guard, same 24-byte
      stride scan comparing +0x14, not-found returns nDb; LLVM
      hoists aDb into a post-increment pointer and splits the signed
      bgt into cmp/bxlt + bne - not observable. Host tests (7):
      NULL-schema sentinel with a NULL db, match at every index,
      first-match-wins on duplicate pSchema words, unknown schema
      returns nDb, entries past nDb unsearched, nDb==0 and nDb<0
      both return 0 (signed guard). Fixture: u32-word slab via
      testing::hints::SCHEMA_TO_INDEX.

  - address: 0x0837d7ac
    ghidra: FUN_0837d7ac
    name: name_from_token
    size: 48
    sig: "char *name_from_token(sqlite3 *db, Token *name)"
    module: sqlite/name_from_token
    status: ported
    vars: {param_1: db, param_2: name}
    notes: >-
      SQLite's sqlite3NameFromToken. 27 bl call sites. NULL token -> NULL;
      otherwise db_str_ndup(db, tok->z, tok->n) then dequote @ 0x083753d0
      (unconditional; a NULL duplicate is dequote's no-op case). Pins the
      Token layout - z at +0x00 and a packed `dyn:1 | n:31` word at +0x04
      that the code reads as `field >> 1` (`lsr r2,r2,#1`). Both callees
      are now ported (dequote landed first, in sqlite/dequote). Fields
      are addressed by word index (block_region.rs precedent) so the
      64-bit host keeps the pointer and packed words disjoint. match.py:
      same structure (NULL guard, ldr/ldr/lsr, both callee bls).

  - address: 0x0837d7dc
    ghidra: FUN_0837d7dc
    name: sqlite_nested_parse
    size: 168
    sig: "void sqlite_nested_parse(NestedParse *parse, const char *format, VaList args)"
    module: sqlite/nested_parse
    status: ported
    vars: {param_1/r4: parse, param_2: format, param_3: args, iVar1/r5: sql}
    notes: >-
      Raw extent is exactly 0x0837d7dc..0x0837d884 (the next function starts
      at 0x0837d884). SQLite 3.5.9's sqlite3NestedParse: if Parse.nested
      (+0x13) is nonzero, return without touching any other state. Otherwise
      call the ported sqlite_vm_printf @ 0x08386454 with Parse.db (+0x00);
      a NULL result writes 1 to db->mallocFailed (+0x1e). On a result,
      increment nested, copy Parse+0x14c..+0x1a7 (0x5c bytes) to the stack
      through the ported IRAM memcpy veneer, call sqlite3RunParser @
      0x08382580 with NULL pzErrMsg, free the SQL through ported
      tracked_free @ 0x083906f4, restore the 0x5c-byte region, then decrement
      nested. Decoding every ARM B/BL word finds 17 direct BL call sites:
      14 unconditional (0x082c6028, 0x082dc158, 0x082dc180, 0x0836eda4,
      0x0836f0e4, 0x0836f118, 0x0836f144, 0x083745b4, 0x083755f8,
      0x0837595c, 0x083760ac, 0x08379434, 0x0838d5c4), and 3 blne
      (0x08375628, 0x08375938, 0x08375988); no tail B. The predicated sites
      gate nested SQL on caller state. Deliberate deviation: unported
      sqlite3RunParser (764 bytes) is SQLITE_RUN_PARSER, a volatile dispatch
      seam whose default is its successful no-state-change outcome. Host tests
      prove the recursion no-op, formatter-failure OOM latch, parser depth and
      NULL error pointer, parser-state restore, and direct tracked free.
  - address: 0x0836761c
    ghidra: FUN_0836761c
    name: release_via_field_0x48
    size: 16
    sig: "*mut u8 release_via_field_0x48(owner: *mut u8)"
    module: cxx/release
    status: ported
    vars: {param_1: owner}
    notes: >-
      51 bl call sites. Raw ARM is exactly `cmp r0,#0; ldrne
      r0,[r0,#0x48]; bne 0x0837ee98; bx lr`: NULL owner returns NULL;
      otherwise the child is tail-called into release_object. The child is
      deliberately not guarded because the callee handles NULL. Ghidra
      incorrectly inlines the 200-byte tail target into this 16-byte
      forwarder. The child pointer field is addressed by word index 18 in
      host fixtures so widened pointers do not overlap adjacent fields.

  - address: 0x0837ee98
    ghidra: FUN_0837ee98
    name: release_object
    size: 200
    sig: "*mut u8 release_object(object: *mut u8)"
    module: cxx/release
    status: ported
    vars: {param_1: object, iVar3: context, iVar2: remaining_refcount, iVar1: remaining_children}
    notes: >-
      NULL returns NULL. Otherwise acquires the context at object+0x00,
      decrements the plain u16 refcount at object+0x22, and only at zero
      detaches the object nodes at +0x10 and (when context+0x14 is zero)
      +0x2c via 0x082d7fdc; invokes the optional context+0xc4 destructor
      with object and context+0x40; then decrements context+0x48. A zero
      child count calls 0x082dd800 when context+0x17 is zero or the signed
      i64 at +0x98 is positive. Finally it drops the context activity count
      at +0xe0 and returns NULL. Target builds call the three unported
      context helpers at their fixed retailOS addresses; host-only mocks
      exercise the direct function's transitions and virtual dispatch.

  - address: 0x0837dc38
    ghidra: FUN_0837dc38
    name: context_record_resolve_leased
    size: 52
    sig: "u32 context_record_resolve_leased(Context *context, u32 record_id, u32 **out_record, u32 flags)"
    module: cxx/context_record_resolve
    status: ported
    vars: {param_1: context, param_2: record_id, param_3: out_record}
    renames:
      param_1/r0: context
      param_2/r1: record_id
      param_3/r2: out_record
      r3 (dropped by Ghidra): flags
      "*(int*)(param_1+0xe0)": context->activity, the u32 lease count
    notes: >-
      Leased wrapper around the still-unported context record resolver
      FUN_082dd05c @ 0x082dd05c. Extent verified from raw words: the next
      separately linked function starts at 0x0837dc6c with push
      {r4,r5,r6,lr}, so the Ghidra 52 bytes are exact; no trailing literal
      pool. Call count verified by decoding every B/BL word in osos.dec:
      exactly 14 sites, all unconditional plain bl, no predicated forms,
      no tail b; the address occurs in no image data word (never
      dispatched virtually). Body: save r0-r2 in r4-r6, bl
      context_activity_enter (ported, 0x082dd3d8), restore r0-r2, bl
      0x082dd05c, then inline release: ldr/sub/str on the u32 at
      context+0xe0 and return the resolver status (r0 survives the
      decrement, which uses r1 only). Ghidra's C is wrong twice: it marks
      the wrapper void and drops the fourth argument - the wrapper never
      touches r3 and the 36-byte enter helper uses r0-r2 only, so the
      caller's r3 (flags, usually 0) reaches the resolver intact, and
      callers such as FUN_0837de5c test the returned status with cmp
      r0,#0. The release half is NOT a function call: a bare decrement
      with no sweep-mark re-check and no NULL guard. Deliberate
      deviations: the enter half calls the ported context_activity_enter
      on both targets (same as cxx/release.rs); the resolver remains
      retailOS-owned, so target builds transmute-call 0x082dd05c (LLVM
      emits blx via a literal-pool word, the expected absolute-address
      pattern) while host builds route only that boundary through a
      recording ops table; the inline decrement uses volatile accesses
      because the lease field is shared volatile state. Host tests (5):
      four-argument forwarding incl. the r3 flags word, exact lease
      acquire/release pairing (initial count restored, no leak or double
      release), nonzero status passthrough with the lease still released,
      marked-context net-zero with the +0xdc flag never written, and
      u32::MAX wraparound of the release decrement; fixtures assert every
      word outside +0xe0 is untouched. match.py: structural parity -
      same prologue/save/enter/restore/resolve/decrement/epilogue shape;
      expected diffs are LLVM's wider {r4-r7,fp,lr} frame and the blx +
      literal for the absolute resolver address.

  - address: 0x08371418
    ghidra: FUN_08371418
    name: context_child_handle_acquire
    size: 96
    sig: "u32 context_child_handle_acquire(u8 *owner, u32 child_id, u8 **out_handle, u32 prepare_mode)"
    module: cxx/context_child_handle
    status: ported
    renames:
      param_1/r0: owner
      param_2/r1: child_id
      param_3/r2: out_handle
      param_4/r3: prepare_mode
      local_18: record
      iVar1: status_or_handle
    notes: >-
      Extent verified from raw osos.dec: push {r3-r7,lr} starts at
      0x08371418 and its pop {r3-r7,pc} ends at 0x08371474; the next
      separately linked function begins at 0x08371478. Every ARM B/BL
      word in the decrypted image was decoded: exactly 18 direct call
      sites, all unconditional plain bl, with no predicated form; no
      image data word equals this address, so it is a static entry point.
      The owner word +0x00 supplies the Context for the unported lease
      wrapper FUN_0837dc38. That wrapper preserves r3 while acquiring
      and releasing the context activity lease around FUN_082dd05c, so
      prepare_mode is forwarded exactly as the fourth argument. On
      success the record's non-NULL +0x00 selects record +0x38 as the
      handle, then the function stores owner at +0x40, payload
      (record +0x34) at +0x44, record at +0x48, child_id at +0x4c, and
      byte +0x08 as 100 only when child_id is 1. A nonzero resolver
      status returns unchanged and does not map or initialize the handle.
      Ghidra invented a stack-derived fourth parameter and marks
      FUN_0837dc38 void, but raw r3 preservation and the cmp r0,#0 after
      bl prove the forwarded argument and returned status. Deliberate
      deviation: target builds call the still-unported 0x0837dc38
      directly; host tests install a recording boundary only for that
      external wrapper. Host tests cover primary and non-primary
      initialization, r3 forwarding, and the failure path leaving output
      untouched. match.py is structural only; its expected diff includes
      host-boundary code and LLVM's different frame layout.

  - address: 0x0806f1a0
    ghidra: switchD_0807a000::caseD_7
    name: ata_handle_field4_is_positive
    size: 24
    sig: "i32 ata_handle_field4_is_positive(const u8 *handle)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: handle, iVar2: field4_is_positive}
    notes: >-
      ATA candidate-selection leaf used by FUN_08070c34 after it finds a
      matching table entry. Returns 1 exactly when handle is non-NULL and
      its signed in-memory word at +0x04 is positive; NULL, negative, and
      zero return 0. ARM is `cmp r0,#0; ldrne r0,[r0,#4]; cmpne r0,#0;
      movle/movgt r0,#0/#1`; the plain aligned load is not MMIO. match.py:
      same NULL guard, +4 load, signed comparison, and 0/1 result shape.

  - address: 0x0806f1b8
    ghidra: FUN_0806f1b8
    name: ata_handle_first_word_or_zero
    size: 16
    sig: "u32 ata_handle_first_word_or_zero(const u32 *handle)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: handle, uVar1: first_word_or_zero}
    notes: >-
      ATA candidate-selection leaf. `cmp/ldrne/moveq/bx lr`: returns
      `handle[0]` through a plain aligned in-memory load, or 0 when the
      handle is NULL. match.py: same NULL guard and field-0 load shape.

  # ---- ATA/CE-ATA driver island inside the SQLite span ----
  # ~0x08369000-0x0836c000. Not SQLite; the strings at 0x0836a004+ are
  # the IAD/RMS/WMS ATA command logger. gpio_pin_configure @ 0x0836b5b0
  # (already ported) belongs to this island, not to the database engine.
  - address: 0x08369780
    ghidra: FUN_08369780
    name: namespace_provider_count
    size: 16
    sig: "i32 namespace_provider_count(const u32 *providers)"
    module: cxx/object_flags
    status: ported
    vars: {param_1: providers, uVar1: entry_count}
    notes: >-
      Namespace-provider array accessor used by registry_key_hash. The raw
      `cmp/ldrne/mvneq/bx lr` body returns the signed +0x00 entry-count
      word, or -1 when the providers object is NULL. The -1 sentinel keeps
      the registry's signed `count > index` gate closed for non-negative
      indices. match.py: same null guard and field-0 load shape.

  - address: 0x08369864
    ghidra: FUN_08369864
    name: namespace_provider_at
    size: 20
    sig: "const u32 *namespace_provider_at(const u32 *providers, u32 index)"
    module: cxx/object_flags
    status: ported
    vars: {param_1: providers, param_2: index, uVar1: provider}
    notes: >-
      Namespace-provider table accessor called by registry_key_hash after its
      signed count gate. The raw `cmp/ldrne/ldrne/moveq/bx lr` returns
      `((const u32 *const *)providers[1])[index]`, with index scaled by four,
      or null when providers is null. The table pointer itself has no null
      guard: a non-null providers object with a null table faults at the
      second load, as in retailOS. match.py: same null guard, +0x04 table
      load, and scaled indexed load shape; host tests represent the target's
      unaligned +0x04 pointer field explicitly.

  - address: 0x083697f0
    ghidra: FUN_083697f0
    name: namespace_provider_push
    size: 8
    sig: "u32 namespace_provider_push(usize *providers, usize value)"
    module: cxx/object_flags
    status: ported
    vars: {param_1: providers, param_2: value}
    renames:
      FUN_0836963c: namespace_provider_insert_at
    notes: >-
      SIZE CORRECTION - Ghidra reports 192 bytes, but the next function
      (the `table[index] = value` accessor 0x083697f8) starts 8 bytes in;
      the remaining 184 bytes are five separately linked siblings
      (0x083697f8 set, 0x08369810 comparator swap, 0x0836982c sort,
      0x08369864 namespace_provider_at, 0x08369878 tail) that Ghidra
      swallowed into this extent. True body: `ldr r2, [r0]; b
      0x0836963c` - the append entry point of the namespace-providers
      table, tail-branching to namespace_provider_insert_at with the
      entry count as the index. 33 `bl` call sites, all unconditional
      (0 predicated forms), counted by decoding every ARM B/BL word in
      osos.dec - no caller NULL- or flag-gates the call, matching the
      veneer itself: the count load at +0x00 runs BEFORE the callee's
      NULL guard, so a NULL providers faults on the load. There is no
      null-safe path through this veneer. Returns the callee's result
      (the new count, or 0 on grow failure). Deviation: the tail branch
      to the ported callee is an ordinary returning call, so the count
      is read twice on the appended path with no intervening store, as
      in retail. match.py (--size 8): same `ldr r2,[r0]` + tail-branch
      shape; the LLVM frame prologue is the only addition.

  - address: 0x083697ac
    ghidra: FUN_083697ac
    name: namespace_provider_each_then_destroy
    size: 68
    sig: "void namespace_provider_each_then_destroy(usize *providers, NamespaceProviderTeardown teardown)"
    module: cxx/object_flags
    status: ported
    vars: {param_1: providers, param_2: teardown}
    renames:
      FUN_08369618: namespace_provider_destroy
    notes: >-
      17 `bl` call sites, ALL unconditional (0 predicated forms, 0 tail
      `b`), counted by decoding every ARM B/BL word in osos.dec - no
      caller NULL- or flag-gates the call; the callee's own
      `movs r5,r0; popeq` guard is the only guard. Size verified: 68
      bytes, 0x083697ac..0x083697f0, confirmed exact because the next
      function is the ported namespace_provider_push veneer @
      0x083697f0. Ghidra's C is wrong in its usual ways: it inlines the
      tail callee's body (the traced_free descriptor dance belongs to
      FUN_08369618) and invents param_3/param_4. Algorithm: `for index
      in 0.. { reload count; if !(count >s index) break; entry =
      table[index]; if entry != 0 { teardown(entry) } }` - the count at
      +0x00 AND the table pointer at +0x04 are reloaded on EVERY
      iteration (volatile in the port) and the gate is the signed `bgt`,
      so a teardown that mutates the object genuinely redirects the
      remaining iteration (host test appends an entry mid-drain and sees
      it visited). The entry NULL check is the predicated `blxne r6`; the
      callback itself has no NULL guard - all 17 callers pass a real
      per-site teardown, so a NULL callback with a live entry jumps to 0
      as in retail. Finally r0 = providers and the function
      tail-branches (`b 0x08369618`) to the ported object destroy. Both
      functions have void ABI results; deviation: Rust makes that tail
      transfer an ordinary returning direct call. Object words are
      addressed by pointer-sized word index (byte-exact +0x00/+0x04 on
      the 32-bit target, disjoint slots on a 64-bit host - the
      registry_key_hash key model). match.py: same structure -
      null-guard, signed count gate, per-iteration count/table reloads,
      `lsl #2` indexed entry load, NULL-entry-skip around `blx`, then
      direct destroy call; LLVM peeled the first loop-gate test and grew
      the frame.

  - address: 0x08369618
    ghidra: FUN_08369618
    name: namespace_provider_destroy
    size: 36
    sig: "void namespace_provider_destroy(usize *providers)"
    module: cxx/object_flags
    status: ported
    vars: {param_1: providers}
    renames:
      FUN_08043994: traced_free
    notes: >-
      Raw ARM extent is exactly 36 bytes, 0x08369618..0x0836963c: the
      next independently linked function, namespace_provider_insert_at,
      starts with `push {r4,r5,r6,lr}` at 0x0836963c. Inbound branch scan
      of every ARM B/BL word in osos.dec found 11 `bl` calls (8
      unconditional, 3 `blne`) and two unconditional tail `b` calls.
      The mixed caller guards do not replace this function's own
      `movs r4,r0; popeq` NULL guard. Algorithm: for non-NULL providers,
      load the +0x04 table word, conditionally `bl traced_free` for a
      non-NULL table, reload providers into r0, then tail-branch to
      traced_free for the object. traced_free itself has no NULL guard,
      so both checks are material. Ghidra incorrectly ascribes
      traced_free's descriptor calls to this function and fabricates
      extra parameters. Deviation: the Rust port expresses the final
      tail branch as an ordinary direct returning call; both ABIs are
      void. Pointer-sized word indices retain target offsets +0x00/+0x04
      while avoiding host-pointer overlap. Host tests cover NULL object,
      NULL table, and table-before-object release order; match.py reports
      the same two conditional paths and two frees, with LLVM's frame
      setup replacing the retail tail branch.

  - address: 0x0836963c
    ghidra: FUN_0836963c
    name: namespace_provider_insert_at
    size: 184
    sig: "u32 namespace_provider_insert_at(usize *providers, usize value, i32 index)"
    module: cxx/object_flags
    status: ported
    vars: {param_1: providers, param_2: value, param_3: index}
    notes: >-
      0 `bl` call sites and exactly 1 inbound branch - the tail `b` from
      the namespace_provider_push veneer @ 0x083697f4 - counted by
      decoding every ARM B/BL word in osos.dec; Ghidra's listing has no
      separate entry for this body at all. Insert into the growable
      namespace-providers table {count @ +0x00, table @ +0x04,
      sorted-flag @ +0x08, capacity @ +0x0c, comparator @ +0x10}:
      NULL providers returns 0. If `capacity <=u count + 1` (unsigned
      `bgt` skips; grows when capacity == count + 1, keeping one spare
      slot), the table is reallocated to `capacity << 3` bytes (the
      doubled capacity) through ported traced_realloc FUN_08043f3c and
      the capacity word doubled; a failed realloc
      returns 0 with the object untouched. With the count reloaded:
      a negative index or `count <=s index` (signed `ble`) appends at
      table[count]; otherwise `for i in (index..=count).rev(): table[i
      + 1] = table[i]` shifts the tail up - INCLUDING the one-past-end
      word table[count], copied to table[count + 1] first, a faithful
      quirk the grow guarantee keeps in bounds - then table[index] =
      value. Finally count is stored incremented, the sorted flag at
      +0x08 is cleared (the sort sibling 0x0836982c sets it after
      qsorting with the +0x10 comparator), and the NEW count is
      returned. Deviation: the object words are addressed by
      pointer-sized word index (byte-exact on the 32-bit target,
      disjoint slots on a 64-bit host - the registry_key_hash key
      model). match.py (--size 184): same structure - NULL guard,
      unsigned grow gate with `lsl #3` byte size, direct traced_realloc
      call, table store + capacity doubling, signed append/shift
      selection, one-past-end-inclusive downward shift loop, count
      increment + sorted-flag clear, new-count return; LLVM swapped
      the two append predicates (both lead to the same path) and uses
      a pre-indexed ldr in the shift loop.

  - address: 0x08369778
    ghidra: FUN_08369778
    name: ata_call_with_zero
    size: 8
    sig: "u32 *ata_call_with_zero(void)"
    module: drivers/ata_cmd
    status: ported
    notes: >-
      24 bl + 1 tail b call sites. `mov r0,#0; b 0x083696f4` - the
      zero-argument entry point of the ATA handle factory 0x083696f4,
      which builds the 5-word handle ([0]=0, [1]=zeroed 4-entry table,
      [2]=0, [3]=4, [4]=param) read by ata_handle_first_word_or_minus1
      and ata_handle_table_entry; this veneer is the common case where
      the extra word (+0x10) stays 0. Deviation: the factory and its
      allocator/free (0x08043c18 / 0x08043994) are unported, so the port
      calls the factory indirectly through ATA_HANDLE_HOOKS whose
      default stub returns NULL - the original's own alloc-failure
      result, which every caller's NULL check already handles (e.g.
      0x0803bbb0). match.py: same mov r0,#0 + tail-call shape, plus the
      hook-table ldr/bx indirection and an LLVM frame.

  - address: 0x082d0ae8
    ghidra: FUN_082d0ae8
    name: ata_error_record
    size: 136
    sig: "u8 *ata_error_record(void)"
    module: drivers/ata_cmd
    status: ported
    vars: {iVar1: table, iVar2: id, iVar3: slot, iVar4: record}
    notes: >-
      6 bl call sites in osos.asm (0x082d0aa8, 0x082e1f7c, 0x082e1fa0,
      0x082e2258, 0x082e4074, ata_report_error @ 0x083690b0). The
      storage layer's per-task error-record allocator: owner id from
      thunk 0x08037e60 (ROM 0x22003eb0, the UNVERIFIED "size_to_class";
      called with no argument of its own - ATA_ERROR_HOOKS.current_id,
      default stub 0 = "no kernel"), then scan 8 slots of 0x38 bytes
      (stock table @ 0x08adb68c, pointer literal @ 0x082d0b74; crate
      static in the port) for owner==id, else claim the first owner==0
      slot: zero it, stamp owner (+0x00) and 1-based slot tag (+0x0c).
      Faithful quirks kept: a full pool reuses slot 0; id 0 matches the
      zeroed pool on the FIRST scan and returns slot 0 uninitialized.
      Deviations: zero fill is the ported memzero, not the island-local
      byte loop 0x08394120 (whose dst+n return the caller discards);
      LLVM fully unrolls both constant-trip scans (util/table_find.rs
      precedent) - slot order, early return and the slot-0 fall-through
      preserved. match.py structural only, as expected.

  - address: 0x082d0b80
    ghidra: FUN_082d0b80
    name: debug_task_selector
    size: 200
    sig: "i32 debug_task_selector(void)"
    module: kernel/debug_task_selector
    status: ported
    renames:
      param_1..param_4: input_words (16-byte stack receive buffer)
      iVar2: task_identifier
      piVar1: task_count
      puVar4: task_label
    notes: >-
      Raw osos.dec is 172 bytes of instructions at 0x082d0b80..0x082d0c2b
      plus this function's 28-byte literal pool at 0x082d0c2c..0x082d0c47:
      200 bytes through 0x082d0c47; the next separately linked function
      begins at 0x082d0c48. Decoding every ARM B/BL word finds exactly 9
      direct call sites, all unconditional bl and zero predicated forms:
      0x082bf6c0, 0x08393e7c, 0x08393e98, 0x08393ea8, 0x08393eb8,
      0x0839400c, 0x0839401c, 0x08394054, 0x08394094. No osos data word
      equals the entry address, so it is never indirectly dispatched.
      Semihost SYS_WRITE0 prints "\nTask (# or name)> ", then the unported
      debug-console reader 0x082d670c fills a 16-byte stack buffer and
      SYS_WRITE0 prints "\n". The reader's raw body opens ":tt" and issues
      SYS_READ; that establishes its narrow debug-console role but not a
      broader identity. '-' returns -1; a leading decimal token goes through
      atoi_dead_sign and must be in 1..=the signed mutable task count at
      0x22008a54; every other token is matched case-sensitively against
      scheduler_label_lookup's one-based labels. The target port uses the
      exact firmware reader, IRAM count word, semihost SWI, atoi, label
      lookup and strcmp; host tests substitute only the reader, mutable word,
      semihost SWI and existing label-lookup seam. match.py is structural:
      expected to differ because Rust expresses the semihost/read seam and
      loops differently.

  - address: 0x082d7924
    ghidra: FUN_082d7924
    name: cache_lock_wait
    size: 12
    sig: "usize cache_lock_wait(void)"
    module: fs/cache_lock
    status: ported
    renames:
      DAT_082d7930: cache_lock_sem_id_address_literal
      thunk_EXT_FUN_22003fd0: rom_sem_wait
    notes: >-
      Cache lock semaphore-acquire thunk, the wait half of the pair
      bracketed with cache_lock_signal @ 0x082d7944. Raw extent is three
      code words at 0x082d7924..0x082d7930; the following word is the
      BSS-address literal 0x08a096f8, not code (next separately entered
      function is the ATA-table wait sibling @ 0x082d7934, so Ghidra's
      12-byte code extent is exact). Body: `ldr r0, [0x082d7930]` (r0 =
      0x08a096f8), `ldr r0, [r0]` (r0 = cache lock kernel semaphore id),
      `b 0x08037e08` - tail-branches to the rom_sem_wait veneer (ROM
      0x22003fd0 via the IRAM mirror), whose r0 result word passes back
      through the tail branch. The load is intentionally unguarded. The
      global word at 0x08a096f8 has only two literal references in osos:
      this thunk and its signal sibling @ 0x082d7944 (same global,
      rom_sem_signal veneer 0x08037e10 -> ROM 0x220042b4); the pair is
      the acquire/release bracket of the fs/disk cache lock, e.g. around
      the refcount/owner mutation in cache_entry_release @ 0x082e18bc.
      23 branch references, verified by decoding every ARM B/BL word in
      osos.dec for every condition code: all 23 are unconditional bl,
      zero predicated forms, zero tail b - callers never flag-gate the
      acquire (0x082b1974, 0x082dfe18, 0x082e0098, 0x082e06fc,
      0x082e07dc, 0x082e0808, 0x082e0e28, 0x082e17c0, 0x082e1814,
      0x082e1844, 0x082e1850, 0x082e18c8, 0x082e196c, 0x082e1fcc,
      0x082e2170, 0x082e2eb0, 0x082e2f18, 0x082e2f2c, 0x082e2f84,
      0x082e2f98, 0x082e42c0, 0x082e4324, 0x082e48d8). No data word in
      osos references 0x082d7924 - the thunk is never dispatched
      virtually. Deviation: dispatches through the ported rom_sem_wait
      (the ROM_KERNEL hook) instead of branching to the 8-byte ROM
      veneer - the ata_semaphore family pattern; match.py (exit 1, diff
      expected) shows the indirect hook-slot call and the crate frame,
      with the BSS literal load/deref skeleton intact. Host builds read
      a private model word so tests exercise the reload-per-call
      behavior without mapping the firmware BSS address.

  - address: 0x082d7934
    ghidra: FUN_082d7934
    name: ata_semaphore_wait
    size: 12
    sig: "usize ata_semaphore_wait(usize index)"
    module: drivers/ata_semaphore
    status: ported
    renames:
      param_1/r0: index
      DAT_082d7940: ata_semaphore_table
      thunk_EXT_FUN_22003fd0: rom_sem_wait
    notes: >-
      Raw extent is three code words at 0x082d7934..0x082d7940; the
      following word is the BSS table literal 0x08adb66c, not code (the
      next separately entered function is cache_lock_signal @
      0x082d7944, so Ghidra's 12-byte extent is exact). Body: `ldr r1,
      [0x082d7940]` (r1 = 0x08adb66c), `ldr r0, [r1, r0, lsl #2]` (r0 =
      ata_semaphore_table[index], the unchecked table word), `b
      0x08037e08` - tail-branches to the rom_sem_wait veneer (ROM
      0x22003fd0 via the IRAM mirror), whose r0 result word passes back
      through the tail branch. The wait half of the pair bracketed with
      ata_semaphore_signal @ 0x082d7954 (same table, rom_sem_signal
      veneer 0x08037e10 -> ROM 0x220042b4). The table occupies 0x20
      bytes (eight ids) immediately before the ATA error-record pool at
      0x08adb68c and has only the wait/signal siblings' two literal
      references in osos. Call sites binary-verified by decoding every
      ARM B/BL word in osos.dec for every condition code: 22
      unconditional bl (Ghidra's 22 is correct), zero predicated forms,
      zero tail b - callers never flag-gate the acquire (0x082b19ac,
      0x082b1c48, 0x082c3028, 0x082e0638, 0x082e174c, 0x082e1d04,
      0x082e1d54, 0x082e22d0, 0x082e23dc, 0x082e310c, 0x082e34b8,
      0x082e40a8, 0x082e45a0, 0x082e4640, 0x082e46e8, 0x082e47c8,
      0x082e5e04, 0x082e5eb4, 0x082e6258, 0x082e62c8, 0x082e6380,
      0x082e662c); most load index via `ldrh r0, [controller, #0x78]`
      immediately before the call. No data word in osos references
      0x082d7934 - the thunk is never dispatched virtually. Deviation:
      dispatches through the ported rom_sem_wait (the ROM_KERNEL hook)
      instead of branching to the 8-byte ROM veneer - the
      ata_semaphore family pattern; match.py (exit 1, diff expected)
      shows the indirect hook-slot call and the crate frame, with the
      BSS table literal load and indexed deref skeleton intact. Host
      builds model the table's eight words so tests exercise the
      lookup without mapping the firmware BSS address.

  - address: 0x082d7944
    ghidra: FUN_082d7944
    name: cache_lock_signal
    size: 12
    sig: "usize cache_lock_signal(void)"
    module: fs/cache_lock
    status: ported
    renames:
      DAT_082d7950: cache_lock_sem_id_address_literal
      thunk_EXT_FUN_220042b4: rom_sem_signal
    notes: >-
      Cache lock semaphore-release thunk. Raw extent is three code words
      at 0x082d7944..0x082d7950; the following word is the BSS-address
      literal 0x08a096f8, not code (next separately entered function is
      the ATA-table sibling ata_semaphore_signal @ 0x082d7954, so
      Ghidra's 12-byte code extent is exact). Body: `ldr r0, [0x082d7950]`
      (r0 = 0x08a096f8), `ldr r0, [r0]` (r0 = cache lock kernel semaphore
      id), `b 0x08037e10` - tail-branches to the rom_sem_signal veneer
      (ROM 0x220042b4 via the IRAM mirror), whose r0 result word passes
      back through the tail branch. The load is intentionally unguarded.
      global word at 0x08a096f8 has only two literal references in
      osos: this thunk and its wait sibling cache_lock_wait @
      0x082d7924 (same global, rom_sem_wait veneer 0x08037e08 -> ROM
      0x22003fd0); the pair
      is the acquire/release bracket of the fs/disk cache lock, e.g.
      around the refcount/owner mutation in cache_entry_release @
      0x082e18bc. 28 branch references, verified by decoding every ARM
      B/BL word in osos.dec (Ghidra reports only the 24 bl forms): 24
      unconditional bl, 3 unconditional tail b (0x082e1800, 0x082e18f4,
      0x082e2ee8) and 1 conditional tail bne @ 0x082e1988 - that one site
      flag-gates the release on a compare; callers are the fs/disk cache
      family (0x082b1994/9c, 0x082dfe68..0x082e2fc8). No data word in
      osos references 0x082d7944 - the thunk is never dispatched
      virtually. Deviation: dispatches through the ported rom_sem_signal
      (the ROM_KERNEL hook) instead of branching to the 8-byte ROM
      veneer - the ata_semaphore family pattern; match.py (exit 1, diff
      expected) shows the indirect hook-slot call and the crate frame,
      with the BSS literal load/deref skeleton intact. Host builds read a
      private model word so tests exercise the reload-per-call behavior
      without mapping the firmware BSS address.

  - address: 0x082d7954
    ghidra: FUN_082d7954
    name: ata_semaphore_signal
    size: 12
    sig: "usize ata_semaphore_signal(usize index)"
    module: drivers/ata_semaphore
    status: ported
    renames:
      param_1/r0: index
      DAT_082d7960: ata_semaphore_table
      thunk_EXT_FUN_220042b4: rom_sem_signal
    notes: >-
      Raw extent is three code words at 0x082d7954..0x082d7960; the
      following word is the BSS table literal 0x08adb66c, not code. Loads
      the unchecked table word at index*4 then tail-branches to veneer
      0x08037e10 -> ROM 0x220042b4, preserving that result. Binary scan of
      every ARM B/BL word in osos.dec found 27 unconditional bl call sites
      plus one unconditional tail b at 0x082e1d28; zero predicated calls.
      The table occupies 0x20 bytes before the ATA error-record pool at
      0x08adb68c and has only the wait/signal siblings' two literal
      references in osos. Device builds access the original BSS address;
      host tests model its eight words and dispatch through the existing
      kernel/task_lock rom_sem_signal hook.

  - address: 0x083690a8
    ghidra: FUN_083690a8
    name: ata_report_error
    size: 24
    sig: "u32 ata_report_error(u32 error)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: error}
    notes: >-
      25 bl call sites. The storage layer's errno setter: fetches the
      caller's per-task record from ata_error_record (0x082d0ae8,
      ported first), stores `error` at record +0x04 and returns
      0xffffffff for the caller to propagate (the reader @ 0x082d0aa4
      returns record+0x04). match.py: body identical (`mov r4, r0; bl;
      str r4, [r0, #4]; mvn r0, #0`), only the LLVM frame prologue
      differs - the same shape as ata_handle_first_word_or_minus1.

  - address: 0x08043828
    ghidra: FUN_08043828
    name: crypto_add_lock
    size: 132
    sig: "i32 crypto_add_lock(i32 *pointer, i32 amount, i32 lock_type, const u8 *file, i32 line)"
    module: crypto/add_lock
    status: ported
    vars: {param_1: pointer, param_2: amount, param_3: lock_type, param_4: file, param_5: line, DAT_080438ac: services_descriptor}
    renames:
      FUN_08043b94: resource_op_dispatch
      DAT_080438ac: services_descriptor
    notes: >-
      SIZE VERIFIED - 128 bytes of code at 0x08043828..0x080438a7 plus the
      trailing services-descriptor literal 0x08a0e93c @ 0x080438ac; the
      separately linked resource_registry_release starts at 0x080438b0, so
      Ghidra's 132-byte extent is exact. All 12 call sites are unconditional
      `bl` forms (0x0803d3e8, 0x0804ae8c, 0x0805f758, 0x0805f860,
      0x08062394, 0x0806fce8, 0x0806fd1c, 0x0806fda8, 0x08070f28,
      0x08071008, 0x080a8f4c, 0x082b4c1c), with zero predicated `bl` forms
      and zero tail `b` sites, counted by decoding every ARM B/BL word in
      osos.dec. No image data word holds 0x08043828, so it is not dispatched
      virtually. The services-descriptor slot +0x0c is a whole-operation
      callback: when non-NULL it receives (pointer, amount, lock_type, file,
      line) and its return value passes through. The shipping slot is NULL,
      so the fallback calls resource_op_dispatch(9, lock_type, file, line),
      stores `*pointer + amount` with ARM's wrapping 32-bit arithmetic,
      calls resource_op_dispatch(10, lock_type, file, line), and returns the
      stored value. Deviation: descriptor slot +0x0c is
      RESOURCE_OP_HOOKS.add_lock_callback and the direct resource calls are
      ordinary Rust calls; host tests prove both fallback lock arguments and
      order, i32 wrapping, and callback replacement without fallback writes.

  - address: 0x08043c18
    ghidra: FUN_08043c18
    name: traced_alloc
    size: 168
    sig: "u8 *traced_alloc(i32 size, u32 tag1, u32 tag2)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: size, param_2: tag1, param_3: tag2, puVar2: descriptor, puVar3: block}
    notes: >-
      88 bl call sites (binary scan), among them the ATA handle factory
      0x083696f4 and its zero-arg veneer ata_call_with_zero. The
      firmware-wide traced allocator front-end over the RAM descriptor
      @ 0x08a0c2a4 (pointer literal @ 0x08043cc0): size <= 0 (SIGNED
      subs/ble) returns NULL without touching anything; else zero
      descriptor word +0x00, and if the trace hook (slot +0x28) is
      installed also zero +0x04 and call trace(NULL, size, tag1, tag2,
      0) - the phase is the 5th arg on the stack; then block =
      alloc(size, tag1, tag2) (slot +0x0c); the trace slot is RE-READ
      and, if still installed, trace(block, size, tag1, tag2, 1) runs;
      finally block != NULL && size > 0x800 (signed bgt, so exactly
      0x800 escapes) stamps block[0] = *(u8 *)0x08a0ea04 (pointer
      literal @ 0x08043cc4, 0x61 in the stock image - a large-
      allocation tag byte whose writer lives outside this function).
      tag1/tag2 are call-site tags passed through to the trace hook
      (0,0 nearly everywhere; 0x082d4474 passes two string literals).
      Deviations: the consulted descriptor slots live in
      TRACED_ALLOC_HOOKS (the ATA_HANDLE_HOOKS pattern; the default
      alloc stub fails every request, the original's own NULL path),
      the two status words and the tag byte are crate statics
      (TRACED_ALLOC_STATUS, LARGE_ALLOC_TAG = 0x61), and the status
      stores are write_volatile so LLVM cannot fold the descriptor
      handshake away. match.py: same structure - two status-zero
      stores, null-checked pre/post trace blx with the phase word on
      the stack, alloc blx, slot reload, signed >0x800 tag stamp; LLVM
      reordered the block-null/size guards into an equivalent
      short-circuit. The free twin 0x08043994 is ported as traced_free
      (see its entry).

  - address: 0x08043f3c
    ghidra: FUN_08043f3c
    name: traced_realloc
    size: 188
    sig: "u8 *traced_realloc(u8 *block, i32 size, u32 tag1, u32 tag2)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: block, param_2: size, param_3: tag1, param_4: tag2, iVar1: descriptor, puVar3: replacement}
    notes: >-
      True raw extent is exactly 188 bytes, 0x08043f3c..0x08043ff8;
      the next independently linked function starts at 0x08043ff8.
      Decoding every ARM B/BL word in osos.dec finds 9 direct inbound
      calls, every one an unconditional bl (0x0803a444, 0x08040c74,
      0x080420cc, 0x08049640, 0x08077c48, 0x080e8f94, 0x080eed4c,
      0x083694f4, 0x08369674), with zero predicated forms and zero
      direct tail branches. NULL block tail-branches to traced_alloc
      with (size, tag1, tag2). A non-NULL block with size <= 0 by the
      signed cmp/ble returns NULL without touching descriptor slots.
      Otherwise optional trace slot +0x2c receives (old, NULL, size,
      tag1, tag2, 0), descriptor slot +0x14 reallocates
      (old, size, tag1, tag2), then trace slot +0x2c is re-read and,
      if present, receives (old, replacement, size, tag1, tag2, 1),
      including a NULL replacement. The descriptor is RAM
      0x08a0c2a4 via literal 0x08043ff4. Deviation: the consulted slots
      are TRACED_REALLOC_HOOKS; its default realloc returns NULL, the
      retail allocation-failure path, and the NULL tail branch is a
      returning call to traced_alloc. Host tests cover positive success
      and failure trace order, signed nonpositive no-call handling, and
      NULL delegation. match.py (--lib local archive): 58 Rust versus
      46 retail instructions; it retains the NULL and signed-size
      early exits, pre/post optional trace calls with stack arguments,
      underlying realloc call, and trace-slot reload. Extra prologue and
      hook-table copies arise from the portable descriptor seam.
  - address: 0x08043994
    ghidra: FUN_08043994
    name: traced_free
    size: 72
    sig: "void traced_free(u8 *block)"
    module: drivers/ata_cmd
    status: ported
    vars: {param_1: block, iVar1: descriptor, UNRECOVERED_JUMPTABLE: trace}
    notes: >-
      80 bl + 44 blne + 2 bleq call sites in osos.asm, among them the
      keyid/serial formatter's hex-blob release @ 0x080aee4c and the
      ATA handle factory's teardown. The free twin of traced_alloc
      (0x08043c18) over the SAME RAM descriptor @ 0x08a0c2a4 (pointer
      literal @ 0x080439dc): if the free-trace hook (slot +0x30, not
      the allocator's +0x28) is installed, call trace(block, 0); then
      free(block) via slot +0x18, unconditionally - neither the slot
      nor block is NULL-checked (the 44 blne callers guard); then the
      trace slot is RE-READ and, if still installed, the post-free
      trace(NULL, 1) runs as a tail call (bxne r2 - Ghidra's
      "unrecovered jumptable"). Deviations: the consulted slots live
      in TRACED_FREE_HOOKS (the TRACED_ALLOC_HOOKS pattern; the
      default free stub is a no-op since the original never NULL-
      checks the slot), and the post-free trace is an ordinary
      returning call where the original tail-branches. match.py: same
      structure - null-checked pre-trace blx with (block, 0),
      unconditional free blx, slot reload, null-checked post-trace
      tail branch with (NULL, 1); LLVM frame and ldrd hook-table loads
      aside.

  - address: 0x08043b94
    ghidra: FUN_08043b94
    name: resource_op_dispatch
    size: 132
    sig: "void resource_op_dispatch(u32 op, i32 resource, u32 arg0, u32 arg1)"
    module: kernel/resource_op
    status: ported
    vars: {param_1: op, param_2: resource, param_3: arg0, param_4: arg1, iVar3: object}
    renames:
      FUN_08043a6c: resource_registry_acquire
      FUN_080438b0: resource_registry_release
      FUN_08044174: current_context_id
    notes: >-
      SIZE CORRECTION - Ghidra says 128, dropping the trailing literal-pool
      word 0x08a0e93c @ 0x08043c14 (the services-descriptor pointer). The
      next function, traced_alloc, starts at 0x08043c18, so the true extent
      is 132 bytes: 128 of code plus that word. 74 `bl` + 3
      `bleq` = 77 `bl`-form call sites plus 4 tail `b` = 81 total, counted
      by decoding every ARM B/BL word in osos.dec. The RAM-side entry
      point every caller uses to run a lock/unlock-style operation on a
      numbered resource - callers bracket critical sections with matched
      pairs, op 9 before and op 10 after (or the shorter op 5 / op 6
      ownership probes), always with arg0/arg1 zero; e.g. 0x080b6ba4 wraps
      a counter increment in (9,1)/(10,1) and 0x08092504 probes ownership
      with (5,0x13)/(6,0x13) before taking (9,0x12). The sign of
      `resource` is the entire dispatch (`subs r5,r1,#0` / `bge`)-
      non-negative ids are static resources handed to descriptor slot
      +0x08 as a tail call with the arguments untouched; negative ids are
      handles into the descriptor's refcounted registry (slot +0x04),
      guarded by slot +0x18 BEFORE the registry is touched, then resolved
      with resource_registry_acquire @ 0x08043a6c (a NULL object is fatal-
      `bleq heap_panic` @ 0x08030f44), dispatched to a RE-READ slot +0x18
      (`ldr ip,[r4,#0x18]` @ 0x08043bcc, not the cached guard value) with
      the object replacing the id in argument 1, and finally tail-branched
      to resource_registry_release @ 0x080438b0 with the RAW NEGATIVE ID,
      not the object. Void - the original leaves r0 holding whatever the
      last path touched (0 from an empty +0x18, `op` from an empty +0x08,
      the tail callee's result otherwise) and no caller reads it.
      Descriptor 0x08a0e93c slots seen from this cluster - +0x04 registry
      vector, +0x08 static handler, +0x10 current-context-id hook (read by
      0x08044174, which returns 1 when empty), +0x18 registered handler,
      +0x1c per-object destroy hook (run by the release path). Deviations,
      the TRACED_ALLOC_HOOKS pattern - the two descriptor slots and the two
      registry entry points (both unported) live in RESOURCE_OP_HOOKS with
      no-op/NULL defaults, so the stock all-NULL descriptor is reproduced
      exactly; the two tail branches are plain calls; and the re-read +0x18
      slot is called only when still installed, where the original would
      blx a NULL slot (unreachable in the stock image, nothing clears the
      slot while a resource is held). match.py structural - same shape,
      sign test (LLVM picks `cmn r1,#1; ble` for `resource < 0`), both
      slot-null guards, blx acquire, fatal NULL-object branch, the slot
      re-read after the acquire returns, blx handler, tail release; LLVM's
      wider frame and its ldrd hook-table loads aside.

  - address: 0x08044174
    ghidra: FUN_08044174
    name: current_context_id
    size: 28
    sig: "u32 current_context_id(void)"
    module: kernel/resource_op
    status: ported
    notes: >-
      SIZE CORRECTION - Ghidra says 24, dropping the trailing literal-pool
      word 0x08a0e93c @ 0x0804418c (the services-descriptor pointer, the
      same descriptor resource_op_dispatch @ 0x08043b94 reads). The next
      function, FUN_08044190, starts at 0x08044190, so the true extent is
      28 bytes: 24 of code plus that word. 13 `bl` call sites and ZERO
      predicated forms, counted by decoding every B/BL word in osos.dec -
      the NULL check lives entirely in the callee (contrast traced_free,
      where 44 of the callers guard with blne), so with the stock all-NULL
      descriptor every caller unconditionally sees context id 1. The whole
      body: load descriptor slot +0x10; if installed, tail-call it
      (`bxne r0`) with no arguments and return its result; if empty,
      return 1 (`moveq r0, #1`). Callers compare the id against a stored
      owner to decide whether they already hold a lock - 0x08092504
      computes `owned = current_context_id() == descriptor[5]` between op
      5/op 6 probes, and the refcounted lock @ 0x08043cc8 re-reads the id
      when the stored owner does not match. Deviations: the slot lives in
      RESOURCE_OP_HOOKS (the TRACED_ALLOC_HOOKS pattern) as the new
      `context_id` member, `None` reproducing the stock NULL slot; and the
      original's `bxne r0` tail call is a plain call (Rust has no
      guaranteed tail calls; the hook takes no arguments, so nothing rides
      through in registers). match.py structural - same shape: table load,
      slot NULL test, indirect `bx` to the hook on one arm, `mov r0, #1`
      on the other; LLVM's push/pop frame and the forced full-table ldrd
      volatile loads aside. The exported symbol resolves in librustypod.a
      (no fold).

  # --- Per-instantiation template members of the C++ block (cxx/templates) ---
  # This block is full of families the compiler emitted once per template
  # instantiation instead of sharing. Each family needs ONE port; every
  # copy in it can be hooked to that symbol. Families found by scanning
  # the image for the exact byte pattern of each body (osos.asm is
  # missing lines and undercounts, so all counts below are from decoding
  # bl/b words in work/firmware/osos.dec):
  #   handle_deref_or_null  22 copies  725 calls  -> cxx/handle
  #   container_element_at  30 copies  154 calls  -> cxx/templates
  #   deque_iter_assign     17 copies   99 calls  -> cxx/templates
  #   deque_seg_capacity    17 copies  116 calls  -> heap/block_deque
  #   vector_size_elem*     28 copies  276 calls  -> cxx/templates
  #   less_unsigned         12 copies   73 calls  -> cxx/templates
  #   container_is_empty     9 copies   92 calls  -> identified
  #   not_equal_deref        5 copies   35 calls  -> identified

  - address: 0x083e35a4
    ghidra: FUN_083e35a4
    name: cxx_record_range_destroy_8
    size: 40
    sig: "void cxx_record_range_destroy_8(void *unused, CxxStringPair *first, CxxStringPair *last)"
    module: cxx/templates
    status: ported
    vars: {param_1: unused, param_2: first, param_3: last}
    renames:
      FUN_0825c8fc: cxx_string_pair_destroy
      FUN_083d8b04: cxx_string_release
      iVar1: current
      param_2: first
      param_3: last
    notes: >-
      Structural half-open range destructor for an 8-byte vector element:
      raw ARM saves r1 as current and r2 as last, compares them before
      dereferencing, then calls 0x0825c8fc(current) and adds 8. That
      recovered element destructor is exactly `cxx_string_release(this + 4)`
      followed by `cxx_string_release(this)`, where the established release
      port is 0x083d8b04. The range port transcribes those two calls directly
      rather than inventing a second exported wrapper. Five decompiled direct
      callers use it as vector teardown: 0x083e35cc and 0x083e3724 destroy
      `{begin,end}` before freeing at an 8-byte capacity, while the outer
      destructor 0x0825c790 tears down its +4 vector the same way and releases
      adjacent COW strings at +0/+0x10. This identifies each element as two
      one-word COW string objects. Host tests validate an empty NULL range,
      all four releases across two records, and that the final free is the
      final record's first member, proving reverse member order.

  - address: 0x083e38a0
    ghidra: FUN_083e38a0
    name: cxx_record_range_destroy_16
    size: 40
    sig: "void cxx_record_range_destroy_16(void *unused, StringObjectPair *first, StringObjectPair *last)"
    module: cxx/templates
    status: ported
    vars: {param_1: unused, param_2: first, param_3: last}
    renames:
      FUN_082677e0: string_object_pair_destroy
      FUN_08277484: string_object_destroy
      iVar1: current
      param_2: first
      param_3: last
    notes: >-
      Structural half-open range destructor for a 16-byte vector element:
      raw ARM saves r1 as current and r2 as last, does its first
      `cmp r4,r5` before any dereference, then per iteration passes current
      in r0 to 0x082677e0 and adds 0x10. The passed r0 argument is otherwise
      unused. The element destructor is fully recovered and ported: its
      `string_object_destroy(this + 8)` followed by
      `string_object_destroy(this)` establishes a pair of adjacent
      two-word StringObjects, in reverse member destruction order.
      0x082677c8 constructs that pair at +0/+8 and 0x082680d8 moves those
      same subobjects separately. Five decompiled direct call sites
      (0x082679f8, 0x083e38c8, 0x083e3aa8, and two paths in 0x083e3ad8)
      use r0 as a vector header and its begin/end words as r1/r2;
      0x083e3aa8 immediately frees the storage as 16-byte elements.

  - address: 0x083da458
    ghidra: FUN_083da458
    name: deque_iter_assign
    size: 36
    sig: "u32 *deque_iter_assign(u32 *dst, const u32 *src)"
    module: cxx/templates
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      Word-by-word 16-byte deque-iterator copy (cur, seg_base, seg_end,
      seg_slot), dst in r0 and src in r1, returning dst untouched. 17
      BYTE-IDENTICAL copies, each sitting immediately after that
      instantiation's deque_seg_capacity (8 + 36 bytes, adjacent - which
      is what identifies the pair as the deque template's out-of-line
      members). Distinct from deque_iter_copy @ 0x083dd9e4, which is the
      same four-word copy with the source in r2 and exists exactly once.
      bl counts (99 total) - 0x083da458 31, 0x083d9fd4 22, 0x083da34c 6,
      0x0824c774 5, 0x083d9f64 4, 0x083da1b0 4, 0x083da248 4,
      0x083da42c 4, 0x083d9ec8 3, and 2 each at 0x0824c734, 0x083d9e9c,
      0x083d9f38, 0x083da184, 0x083da21c, 0x083da2b4, 0x083da2e0,
      0x083da3b8. 0x0824c734/0x0824c774 are the only copies outside the
      C++ block.

  - address: 0x083d9ff8
    ghidra: FUN_083d9ff8
    name: deque_iter_init_elem4_alias_9ff8
    size: 64
    sig: "DequeIter *deque_iter_init_elem4_alias_9ff8(DequeIter *iter, u8 *cur, u8 **slot)"
    module: cxx/templates
    status: ported
    vars: {param_1: iter, param_2: cur, param_3: slot, iVar1: seg_base}
    renames:
      FUN_083d9fcc: deque_seg_capacity
      param_1: iter
      param_2: cur
      param_3: slot
      iVar1: seg_base
    notes: >-
      Raw ARM establishes a 64-byte function from 0x083d9ff8 through
      `pop {r4,r5,pc}` at 0x083da034; the following function begins at
      0x083da038. It stores cur; for a NULL segment-slot pointer it writes
      NULL base/end; otherwise it loads the slot base, calls the adjacent
      `mov r0,#0x20; bx lr` capacity member at 0x083d9fcc, reloads the slot,
      writes base + 0x20 * 4 as end, stores the slot, and returns iter. A
      non-NULL slot containing NULL therefore yields end 0x80, not NULL.
      Decoding every ARM B/BL word in osos.dec finds exactly nine
      unconditional plain bl call sites (0x0815b3a4, 0x083de2e8,
      0x083de310, 0x083de340, 0x083de39c, 0x083de468, 0x083de490,
      0x083de4c4, and 0x083de528), zero predicated bl forms, and zero tail
      b sites. The port deliberately calls the existing deque_seg_capacity
      export rather than the local duplicate; wrapping_add preserves the raw
      NULL-base + 0x80 result. Its dedicated text section prevents LLVM from
      folding this independently hookable copy into another deque template
      instance.
  - address: 0x083da1d4
    ghidra: FUN_083da1d4
    name: deque_iter_init_elem4_alias_a1d4
    size: 64
    sig: "DequeIter *deque_iter_init_elem4_alias_a1d4(DequeIter *iter, u8 *cur, u8 **slot)"
    module: cxx/templates
    status: ported
    vars: {param_1: iter, param_2: cur, param_3: slot, iVar1: seg_base}
    renames:
      FUN_083da1a8: deque_seg_capacity
      param_1: iter
      param_2: cur
      param_3: slot
      iVar1: seg_base
    notes: >-
      Raw ARM confirms Ghidra's 64-byte extent: the entry at 0x083da1d4
      runs through `pop {r4,r5,pc}` at 0x083da210, with the next sibling
      capacity member beginning at 0x083da214. It stores cur; for a NULL
      segment-slot pointer writes NULL base/end; otherwise loads the slot
      base, calls the adjacent `mov r0,#0x20; bx lr` capacity member at
      0x083da1a8, reloads the slot, writes base + 0x20 * 4 as end, then
      stores the slot. A non-NULL slot containing NULL therefore writes end
      0x80, not NULL. Decoding every ARM B/BL word in osos.dec finds exactly
      nine unconditional plain bl call sites (0x083de884, 0x083de8c4,
      0x083de9a0, 0x083de9c8, 0x083de9f4, 0x083dea4c, 0x083dea80,
      0x083deb9c, and 0x083debd4), zero predicated bl forms, and zero tail b
      sites. The port deliberately calls the existing deque_seg_capacity
      export rather than the local duplicate; wrapping_add preserves the raw
      NULL-base + 0x80 result. Its dedicated text section prevents LLVM from
      folding this independently hookable copy into another deque template
      instance.

  - address: 0x083da26c
    ghidra: FUN_083da26c
    name: deque_iter_init_elem4_alias_a26c
    size: 64
    sig: "DequeIter *deque_iter_init_elem4_alias_a26c(DequeIter *iter, u8 *cur, u8 **slot)"
    module: cxx/templates
    status: ported
    vars: {param_1: iter, param_2: cur, param_3: slot, iVar1: seg_base}
    renames:
      FUN_083da240: deque_seg_capacity
      param_1: iter
      param_2: cur
      param_3: slot
      iVar1: seg_base
    notes: >-
      Raw ARM confirms Ghidra's 64-byte extent: the entry at 0x083da26c
      runs through `pop {r4,r5,pc}` at 0x083da2a8, with the next sibling
      capacity member beginning at 0x083da2ac. The body differs from the
      0x083da3e4 iterator constructor only in its relative bl displacement:
      it stores cur; for a NULL segment-slot pointer writes NULL base/end;
      otherwise loads the slot base, calls the adjacent `mov r0,#0x20; bx lr`
      capacity member at 0x083da240, reloads the slot, writes base +
      0x20 * 4 as end, then stores the slot. A non-NULL slot containing NULL
      therefore writes end 0x80, not NULL. Decoding every ARM B/BL word in
      osos.dec finds exactly nine unconditional plain bl call sites
      (0x083dec4c, 0x083dec8c, 0x083ded68, 0x083ded90, 0x083dedbc,
      0x083dee14, 0x083dee48, 0x083def64, and 0x083def9c), zero predicated
      bl forms, and zero tail b sites. The port deliberately calls the
      existing deque_seg_capacity export rather than the local duplicate;
      wrapping_add preserves the raw NULL-base + 0x80 result. Its dedicated
      text section prevents LLVM from folding this independently hookable
      copy into another deque template instance.

  - address: 0x083d6fdc
    ghidra: FUN_083d6fdc
    name: deque_iter_advance_copy_elem4
    size: 44
    sig: "DequeIter *deque_iter_advance_copy_elem4(DequeIter *dst, const DequeIter *source, i32 distance)"
    module: cxx/templates
    status: ported
    vars: {param_1: dst, param_2: source, param_3: distance, uStack_20: copy}
    renames:
      FUN_083d9fd4: deque_iter_assign_alias_9fd4
      FUN_083da088: deque_iter_advance_elem4
      param_1: dst
      param_2: source
      param_3: distance
      uStack_20: copy
    notes: >-
      Raw ARM establishes the 44-byte extent from the push at 0x083d6fdc
      through `pop {r0,r1,r2,r3,r4,r5,r6,pc}` at 0x083d7004; the next
      sibling begins at 0x083d7008. This is the 4-byte-element deque
      iterator non-mutating advance: it copies source to a 16-byte stack
      iterator via FUN_083d9fd4, passes that copy and the signed distance to
      FUN_083da088, then copies the returned iterator to dst and returns dst.
      Decoding every ARM B/BL word in osos.dec found exactly nine
      unconditional plain bl call sites (0x081cb444, 0x081cb480,
      0x083de640, 0x083de65c, 0x083de66c, 0x083de688, 0x083de714,
      0x083de7ac, and 0x083de7d4), zero predicated bl forms, and zero tail
      b sites. FUN_083da088 is still unported; the target default calls its
      surviving firmware address through DEQUE_ITER_ADVANCE_ELEM4_OPS, while
      host tests install a recording model. Typed DequeIter copies preserve
      the target four-word object while avoiding overlapping 64-bit host
      pointer fields.
  - address: 0x083da3e4
    ghidra: FUN_083da3e4
    name: deque_iter_init_elem4_alias_a3e4
    size: 64
    sig: "DequeIter *deque_iter_init_elem4_alias_a3e4(DequeIter *iter, u8 *cur, u8 **slot)"
    module: cxx/templates
    status: ported
    vars: {param_1: iter, param_2: cur, param_3: slot, iVar1: seg_base}
    renames:
      FUN_083da3dc: deque_seg_capacity
      param_1: iter
      param_2: cur
      param_3: slot
      iVar1: seg_base
    notes: >-
      Raw ARM establishes a 64-byte function from 0x083da3e4 through
      `pop {r4,r5,pc}` at 0x083da420; the adjacent 8-byte capacity member
      begins at 0x083da3dc and the next iterator-copy function begins at
      0x083da424. This is byte-identical to deque_iter_init_elem4 at
      0x083da47c after resolving its one relative `bl`: it writes cur,
      sets NULL base/end for a NULL segment slot, otherwise loads the slot's
      base, reloads it after calling the adjacent capacity member, and writes
      base + 0x20 * 4 as end. A non-NULL slot containing NULL consequently
      writes end 0x80. Decoding every ARM B/BL word in osos.dec found exactly
      nine unconditional plain bl call sites (0x083df5c0, 0x083df600,
      0x083df6dc, 0x083df704, 0x083df730, 0x083df788, 0x083df7bc,
      0x083df86c, 0x083df8a4), zero predicated bl forms, and zero tail b
      sites. It is exported in a dedicated text section so LLVM cannot fold
      this independently hookable address into its byte-identical twin.
  - address: 0x083da47c
    ghidra: FUN_083da47c
    name: deque_iter_init_elem4
    size: 64
    sig: "DequeIter *deque_iter_init_elem4(DequeIter *iter, u8 *cur, u8 **slot)"
    module: cxx/templates
    status: ported
    vars: {param_1: iter, param_2: cur, param_3: slot, iVar1: seg_base}
    renames:
      FUN_083da450: deque_seg_capacity
      param_1: iter
      param_2: cur
      param_3: slot
      iVar1: seg_base
    notes: >-
      Raw ARM confirms Ghidra's 64-byte extent: the entry at 0x083da47c
      runs through `pop {r4,r5,pc}` at 0x083da4b8 and the sibling deque
      iterator function begins at 0x083da4bc. 11 plain `bl` call sites
      (0x083df930, 0x083df970, 0x083dfa4c, 0x083dfa74, 0x083dfaa0,
      0x083dfaf8, 0x083dfc78, 0x083dfcb0, 0x083dfdc4, 0x083dfe68, and
      0x083dfea4), zero predicated `bl` forms, and zero tail `b` sites
      were counted by decoding every ARM B/BL word in osos.dec. This
      4-byte-element deque-iterator constructor writes cur; for a NULL
      segment-slot pointer writes NULL base/end; otherwise loads the
      slot's base, calls the adjacent `mov r0,#0x20; bx lr` capacity member
      at 0x083da450, reloads the slot, writes base + 0x20 * 4 as end, and
      stores the slot. A non-NULL slot containing NULL therefore yields
      end 0x80, not NULL. The port uses the existing
      heap/block_deque::deque_seg_capacity export rather than a redundant
      wrapper for the byte-identical capacity member; `wrapping_add`
      deliberately preserves the raw NULL-base + 0x80 result without
      Rust's in-bounds pointer-arithmetic requirement.

  - address: 0x083d9fd4
    ghidra: FUN_083d9fd4
    name: deque_iter_assign_alias_9fd4
    size: 36
    sig: "u32 *deque_iter_assign_alias_9fd4(u32 *dst, const u32 *src)"
    module: cxx/templates
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      ALIAS - byte-identical second instantiation of deque_iter_assign
      @ 0x083da458 (all 36 bytes verified against osos.dec: `ldr
      r2,[r1]; str r2,[r0]; ldr r2,[r1,#4]; str r2,[r0,#4]; ldr
      r2,[r1,#8]; str r2,[r0,#8]; ldr r1,[r1,#0xc]; str r1,[r0,#0xc];
      bx lr`), the same 16-byte deque-iterator copy (cur, seg_base,
      seg_end, seg_slot) returning dst in r0 untouched. The adjacency
      identification holds: the 8 bytes immediately before it @
      0x083d9fcc are `mov r0,#0x20; bx lr`, byte-identical to
      deque_seg_capacity @ 0x083d9ec0. 22 plain `bl` call sites, none
      predicated - verified by decoding every BL word in osos.dec (the
      Ghidra count of 22 is exact): 0x081cb434 and 0x081cb470 in
      FUN_081cb408/FUN_081cb458 (deque element-at and insert helpers on
      a `{header, deque}` object at +4), 0x083d6fc0/0x083d6fd4 in
      FUN_083d6fb0 and 0x083d6fec/0x083d7000 in FUN_083d6fdc (the
      deque-iterator retreat/advance pair), and 16 sites in
      FUN_083de544 (the deque insert-around-midpoint splitter).
      Reference C: decomp/c/038/083d9fd4_FUN_083d9fd4.c. Ported as its
      own exported symbol with an identical body under a distinct
      link_section (the vector_size_elem4_alias_76c8 precedent), so
      LLVM identical-function folding cannot merge the labels; both
      symbols resolve in librustypod.a. match.py: body is
      instruction-identical to the original; the only diff is LLVM's
      `push {fp,lr}` / `pop {fp,pc}` frame prologue replacing the bare
      `bx lr`.

  - address: 0x083d7d10
    ghidra: FUN_083d7d10
    name: vector_pair_copy_into
    size: 16
    sig: "u32 *vector_pair_copy_into(const void *vector, u32 *dst, const u32 *src)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector, param_2: dst, param_3: src}
    notes: >-
      NULL-guarded copy into an opaque two-word (8-byte) vector-element
      pair. Raw ARM is `movs r0,r1; ldmne r2,{r1,r2}; stmne r0,{r1,r2};
      bx lr`: `r0` is the ignored vector-header argument at entry, then
      becomes dst and is incidentally returned. The two source words are
      loaded before either destination store; a NULL dst neither reads src
      nor writes. Three bl call sites: 0x080d1e38 passes a vector header,
      insertion-slot iterator, and stack pair; 0x083e02f0 and 0x083e0370
      are the vector's 8-byte-element relocation path. The pair fields
      remain opaque, but the shared vector/slot/pair call shape identifies
      the helper.

  - address: 0x083d7580
    ghidra: FUN_083d7580
    name: less_signed
    size: 24
    sig: "u32 less_signed(void *this, const i32 *a, const i32 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: a, param_3: b}
    notes: >-
      45 bl call sites; the only copy of this body in the image.
      `std::less<int>::operator()(const int &, const int &)` - the
      operands arrive by reference so both are dereferenced, and `this`
      (r0) is overwritten by the first load. `movge 0 / movlt 1`.

  - address: 0x083d7598
    ghidra: FUN_083d7598
    name: less_unsigned
    size: 24
    sig: "u32 less_unsigned(void *this, const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: a, param_3: b}
    notes: >-
      less_signed with `movcs 0 / movcc 1` - an unsigned compare. That
      the unsigned form has 12 byte-identical copies and the signed form

      one is the only thing that distinguishes the two families in the
      image. bl counts (73 total) - 0x083d7598 20, 0x083d7404 7, and
      then 0x083d7434, 0x083d744c, 0x083d7464, 0x083d747c, 0x083d74ac,
      0x083d74c4, 0x083d74dc, 0x083d7568 at 5 each, 0x083d741c and
      0x083d7494 at 3 each.

  - address: 0x083d5efc
    ghidra: FUN_083d5efc
    name: container_element_at
    size: 24
    sig: "void *container_element_at(void *this, usize index)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      `T *operator[](size_t)`. Dispatches through the container's OWN
      vtable at byte offset 0x40 (slot 16) to get the address of the
      element slot, then loads the element pointer out of it -
      `*(vfn40(this, index))`. Confirmed against call sites - callers
      pass a loop counter in r1 and NULL-test the result (0x0839bdd4,
      0x081b1564). The `push {r4, lr}` saves nothing (r4 is never
      touched); it is stack alignment. 30 BYTE-IDENTICAL copies, 154 bl
      sites - 0x083d5efc 13, 0x083d5f14 11, 0x083d6bc0 10, 0x083d5f44 8,
      0x0829bac0 7, 0x083d6870 7, 0x083d6c2c 7, 0x083d5f2c 6,
      0x083d5f74 6, 0x083d5fec 6, 0x083d689c 6, 0x083d6bec 6,
      0x083d6908 5, 0x083d6a68 5, 0x083d6b14 5, 0x083d6b6c 5,
      0x083d5f5c 4, 0x083d5fbc 4, 0x083d6aa8 4, 0x083d6ad4 4,
      0x083d6b40 4, 0x083d6c6c 4, 0x083d68dc 3, 0x083d69d8 3,
      0x083d5f8c 2, 0x083d5fa4 2, 0x083d6818 2, 0x083d6844 2,
      0x083d6998 2, 0x083d5fd4 1. 0x0829bac0 is the only copy outside
      the C++ block. The port reads the vtable by SLOT INDEX (0x40/4),
      not byte offset, so it is correct on a 64-bit test host.

  - address: 0x083d5f14
    ghidra: FUN_083d5f14
    name: container_element_at_alias_5f14
    size: 24
    sig: "void *container_element_at_alias_5f14(void *this, usize index)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      Byte-identical instantiation of container_element_at @ 0x083d5efc:
      `push {r4,lr}; ldr r2,[r0]; ldr r2,[r2,#0x40]; blx r2; ldr r0,[r0];
      pop {r4,pc}`. It implements `T *operator[](size_t)` by dispatching
      through this container's vtable slot 0x40 to obtain an element-slot
      address, then loading its element pointer. All 11 decoded direct call
      sites are plain `bl` (none predicated): 0x081002e4, 0x08100408,
      0x08100624, 0x0810070c, 0x081008f4, 0x08100930, 0x081010a4,
      0x08101538, 0x08101550, 0x0839c004 and 0x0839c048. They pass the
      index in r1; callers NULL-test the returned element, including the
      0x0839bffc loop that searches for the first non-NULL entry. Exported
      in a distinct link section so LLVM retains a separately hookable
      symbol despite its byte-identical body.

  - address: 0x083d5f44
    ghidra: FUN_083d5f44
    name: container_element_at_alias_5f44
    size: 24
    sig: "void *container_element_at_alias_5f44(void *this, usize index)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      Byte-identical instantiation of container_element_at @ 0x083d5efc:
      `push {r4,lr}; ldr r2,[r0]; ldr r2,[r2,#0x40]; blx r2; ldr r0,[r0];
      pop {r4,pc}`. It implements `T *operator[](size_t)` by dispatching
      through this container's vtable slot 0x40 to obtain an element-slot
      address, then loading its element pointer. Ghidra drops the r1 index
      parameter, but raw `blx r2` preserves it. All eight decoded direct
      call sites are plain `bl` (none predicated): 0x08126ef0, 0x081272f4,
      0x081273ac, 0x08127de0, 0x08127e04, 0x08127e30, 0x0839c1f0, and
      0x0839c234. Callers pass loop or event indexes and NULL-test the
      returned element. Exported in a distinct link section so LLVM retains
      a separately hookable symbol despite its byte-identical body.

  - address: 0x083d68dc
    ghidra: FUN_083d68dc
    name: container_element_at_alias_68dc
    size: 24
    sig: "void *container_element_at_alias_68dc(void *this, usize index)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      ALIAS — byte-identical second instantiation of container_element_at
      @ 0x083d5efc (all 24 bytes verified against osos.dec: `push {r4,lr};
      ldr r2,[r0]; ldr r2,[r2,#0x40]; blx r2; ldr r0,[r0]; pop {r4,pc}`),
      the same `T *operator[](size_t)` dispatch through the container's
      own vtable slot 0x40. 2 `bl 0x083d68dc` sites plus one `b`
      tail-call (grep on decomp/osos.asm): 0x082848a0 in FUN_08284878
      (loops r4 as the index over this+0x34 and feeds each element on),
      0x083d0214 in FUN_083d01f4 (NULL-tests the element, then an
      indirect call through the element's own vtable), and 0x082a6770
      (tail-wraps this+0x34 as a plain accessor) — the ledger's
      binary-scanned "3 bl sites" counts the b. Reference C:
      decomp/c/037/083d68dc_FUN_083d68dc.c (Ghidra drops the r1 index
      parameter; the blx passes it through). Ported as its own exported
      symbol with an identical body under a distinct link_section (the
      vector_size_elem4_alias_76c8 precedent, NOT the ledger-only
      not_equal_deref aliases) so LLVM's identical-function folding
      keeps both labels hookable. match.py: same ldr/ldr/blx r2/ldr
      dispatch-and-deref shape as the original; LLVM emits its fp frame
      (push {fp,lr} / mov fp,sp) in place of the original's
      stack-alignment push {r4,lr}. Host tests:
      src/cxx/templates.rs::element_at_alias_68dc_dispatches_through_slot_0x40_and_derefs
      / element_at_alias_68dc_matches_primary.

  - address: 0x083d6908
    ghidra: FUN_083d6908
    name: container_element_at_alias_6908
    size: 24
    sig: "void *container_element_at_alias_6908(void *this, usize index)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      ALIAS — byte-identical third instantiation of
      container_element_at @ 0x083d5efc (all 24 bytes verified against
      osos.dec: `push {r4,lr}; ldr r2,[r0]; ldr r2,[r2,#0x40]; blx r2;
      ldr r0,[r0]; pop {r4,pc}`), the same `T *operator[](size_t)`
      dispatch through the container's own vtable slot 0x40. 5
      `bl 0x083d6908` sites, no b tail-call (grep on decomp/osos.asm):
      0x08123ff8, 0x08298b84 and 0x08298cb4 each reach a sub-container
      at this+0x6c (`add r0,r?,#0x6c` immediately before the bl) —
      0x08298b84's index is clamped by an `array_at_checked`-shaped
      signed bound check first — and 0x083d02b4 / 0x083d02f8 in
      FUN_083d02ac loop r4/r5 as the index and NULL-test each returned
      element (`cmp r0,#0; beq`) before use — the same usage shape the
      primary's call sites established. Reference C:
      decomp/c/037/083d6908_FUN_083d6908.c (Ghidra drops the r1 index
      parameter; the blx passes it through). Ported as its own exported
      symbol with an identical body under a distinct link_section (the
      vector_size_elem4_alias_76c8 / container_element_at_alias_68dc
      precedent, NOT the ledger-only not_equal_deref aliases) so LLVM's
      identical-function folding keeps all three labels hookable.
      match.py: same ldr/ldr/blx r2/ldr dispatch-and-deref shape as the
      original; LLVM emits its fp frame (push {fp,lr} / mov fp,sp) in
      place of the original's stack-alignment push {r4,lr}. Host tests:
      src/cxx/templates.rs::element_at_alias_6908_dispatches_through_slot_0x40_and_derefs
      / element_at_alias_6908_matches_primary.

  - address: 0x083d6bc0
    ghidra: FUN_083d6bc0
    name: container_element_at_alias_6bc0
    size: 24
    sig: "void *container_element_at_alias_6bc0(void *this, usize index)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: index}
    notes: >-
      ALIAS — byte-identical instantiation of container_element_at @
      0x083d5efc (all 24 bytes verified against osos.dec: `push {r4,lr};
      ldr r2,[r0]; ldr r2,[r2,#0x40]; blx r2; ldr r0,[r0]; pop {r4,pc}`).
      It implements `T *operator[](size_t)` by dispatching through the
      container's own vtable slot 0x40 for an element-slot address, then
      loading that slot's element pointer. All 9 decoded direct call sites
      are plain `bl` (none predicated): 0x08214878, 0x08214930, 0x08214a0c,
      0x08214e20, 0x08214e38, 0x08214e48, 0x08214e5c, 0x083d1a68, and
      0x083d1aa0. Ghidra drops the r1 index parameter, but the `blx r2`
      preserves it for the virtual method. Exported in a distinct link
      section so LLVM retains this separately hookable symbol despite the
      byte-identical body. Host tests:
      src/cxx/templates.rs::element_at_alias_6bc0_dispatches_through_slot_0x40_and_derefs
      / element_at_alias_6bc0_matches_primary.

  - address: 0x083d48dc
    ghidra: FUN_083d48dc
    name: array_at_checked
    size: 28
    sig: "void *array_at_checked(const PtrArray *array, i32 index)"
    module: cxx/templates
    status: ported
    vars: {param_1: array, param_2: index}
    notes: >-
      `index >= 0 && index < count ? base[index] : NULL` over a
      {base at +0, count at +4} pointer array. Both tests are SIGNED
      (`cmp r1,#0` then `cmpge r2,r1` with a gt/le split), so a negative
      index is rejected rather than wrapping. Fully predicated in the
      original - `base` is loaded only on the in-range path, so an empty
      array never touches it. 40 bl sites here plus 3 at the
      byte-identical copy 0x083d5500.

  - address: 0x083d7810
    ghidra: FUN_083d7810
    name: vector_is_empty
    size: 24
    sig: "u32 vector_is_empty(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `std::vector<T>::empty()` over the {begin, end} head. Verified
      against raw osos.dec: `ldr r1,[r0]; ldr r0,[r0,#4]; cmp r1,r0;
      movne r0,#0; moveq r0,#1; bx lr`. It takes the vector in r0 and
      returns an AAPCS word-sized 0/1 bool in r0; neither bound is
      dereferenced. Source C:
      decomp/c/037/083d7810_FUN_083d7810.c. Focused host tests cover
      equal non-NULL bounds, unequal adjacent bounds, and NULL values;
      only the two bound words are read.

  - address: 0x083d76c8
    ghidra: FUN_083d76c8
    name: vector_size_elem4_alias_76c8
    size: 16
    sig: "i32 vector_size_elem4_alias_76c8(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 4 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d76c8_FUN_083d76c8.c.

  - address: 0x083d76d8
    ghidra: FUN_083d76d8
    name: vector_size_elem4
    size: 16
    sig: "i32 vector_size_elem4(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::size()` for a 4-byte element - `ldm r0,{r0,r1}; sub
      r0,r1,r0; asr r0,r0,#2; bx lr` over the {begin, end} head. The
      shift is ARITHMETIC, so a reversed vector yields a negative count.
      17 BYTE-IDENTICAL copies, 140 bl sites - 0x083d76d8 20,
      0x083d7a48 18, 0x083d7944 17, 0x083d78c4 11, 0x083d7924 10,
      0x083d76e8 9, 0x083d77cc 7, 0x083d7904 7, 0x083d78e4 6,
      0x083d7914 5, 0x083d78f4 5, 0x083d7a68 5, 0x083d76c8 4,
      0x083d78d4 4, 0x083d7934 4, 0x083d7a38 4, 0x083d7a58 4.

  - address: 0x083d76e8
    ghidra: FUN_083d76e8
    name: vector_size_elem4_alias_76e8
    size: 16
    sig: "i32 vector_size_elem4_alias_76e8(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 9 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d76e8_FUN_083d76e8.c.

  - address: 0x083d77cc
    ghidra: FUN_083d77cc
    name: vector_size_elem4_alias_77cc
    size: 16
    sig: "i32 vector_size_elem4_alias_77cc(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 7 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d77cc_FUN_083d77cc.c.

  - address: 0x083d78c4
    ghidra: FUN_083d78c4
    name: vector_size_elem4_alias_78c4
    size: 16
    sig: "i32 vector_size_elem4_alias_78c4(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 11 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d78c4_FUN_083d78c4.c.

  - address: 0x083d78d4
    ghidra: FUN_083d78d4
    name: vector_size_elem4_alias_78d4
    size: 16
    sig: "i32 vector_size_elem4_alias_78d4(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 4 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d78d4_FUN_083d78d4.c.

  - address: 0x083d78e4
    ghidra: FUN_083d78e4
    name: vector_size_elem4_alias_78e4
    size: 16
    sig: "i32 vector_size_elem4_alias_78e4(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 6 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d78e4_FUN_083d78e4.c.

  - address: 0x083d78f4
    ghidra: FUN_083d78f4
    name: vector_size_elem4_alias_78f4
    size: 16
    sig: "i32 vector_size_elem4_alias_78f4(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 5 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d78f4_FUN_083d78f4.c.

  - address: 0x083d7904
    ghidra: FUN_083d7904
    name: vector_size_elem4_alias_7904
    size: 16
    sig: "i32 vector_size_elem4_alias_7904(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 7 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7904_FUN_083d7904.c.

  - address: 0x083d7914
    ghidra: FUN_083d7914
    name: vector_size_elem4_alias_7914
    size: 16
    sig: "i32 vector_size_elem4_alias_7914(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 5 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7914_FUN_083d7914.c.

  - address: 0x083d7924
    ghidra: FUN_083d7924
    name: vector_size_elem4_alias_7924
    size: 16
    sig: "i32 vector_size_elem4_alias_7924(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 10 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7924_FUN_083d7924.c.

  - address: 0x083d7934
    ghidra: FUN_083d7934
    name: vector_size_elem4_alias_7934
    size: 16
    sig: "i32 vector_size_elem4_alias_7934(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 4 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7934_FUN_083d7934.c.

  - address: 0x083d7944
    ghidra: FUN_083d7944
    name: vector_size_elem4_alias_7944
    size: 16
    sig: "i32 vector_size_elem4_alias_7944(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 17 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7944_FUN_083d7944.c.

  - address: 0x083d7a38
    ghidra: FUN_083d7a38
    name: vector_size_elem4_alias_7a38
    size: 16
    sig: "i32 vector_size_elem4_alias_7a38(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 4 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7a38_FUN_083d7a38.c.

  - address: 0x083d7a48
    ghidra: FUN_083d7a48
    name: vector_size_elem4_alias_7a48
    size: 16
    sig: "i32 vector_size_elem4_alias_7a48(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 18 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7a48_FUN_083d7a48.c.

  - address: 0x083d7a58
    ghidra: FUN_083d7a58
    name: vector_size_elem4_alias_7a58
    size: 16
    sig: "i32 vector_size_elem4_alias_7a58(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 4 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7a58_FUN_083d7a58.c.

  - address: 0x083d7a68
    ghidra: FUN_083d7a68
    name: vector_size_elem4_alias_7a68
    size: 16
    sig: "i32 vector_size_elem4_alias_7a68(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical vector_size_elem4 instantiation at 0x083d76d8:
      `ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; bx lr`. Its 5 bl
      call sites use the same {begin, end} vector head. The `asr` is
      arithmetic, so reversed spans retain their negative count. Source C:
      decomp/c/037/083d7a68_FUN_083d7a68.c.

  - address: 0x083d7a78
    ghidra: FUN_083d7a78
    name: vector_size_elem2
    size: 16
    sig: "i32 vector_size_elem2(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      vector_size_elem4 with `asr #1`. 16 bl call sites; the only copy
      of this shift (verified against osos.asm - no other `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr #1; bx lr` veneer exists). Source C:
      decomp/c/037/083d7a78_FUN_083d7a78.c - Ghidra's `return
      param_1[1] - *param_1 >> 1`. The `asr` is arithmetic, so reversed
      spans retain their negative count.

  - address: 0x0829db9c
    ghidra: FUN_0829db9c
    name: vector_size_elem2_clamped
    size: 24
    sig: "i32 vector_size_elem2_clamped(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      The unsigned-clamped vector<u16>::size() out-of-line body: `ldm
      r0,{r0,r1}; cmp r1,r0; subhi r0,r1,r0; asrhi r0,r0,#1; movls
      r0,#0; bx lr`, verified against osos.dec - 24 bytes ending in bx
      lr, next function at 0x0829dbb4 (the earlier 20-byte size in the
      wstr_case_eq notes dropped the trailing bx lr). 23 bl call
      sites, all plain, zero predicated (binary-scanned over osos.dec).
      The only copy of this guarded body in the image (byte-pattern
      scan). Unlike the unguarded vector_size_elem2 @ 0x083d7a78
      (`sub`/`asr #1`, negative counts for reversed spans), the cmp is
      UNSIGNED and the sub/asr are predicated on it, so empty or
      inverted heads clamp to 0. Was previously ported in place as the
      private wide_vec_len helper inside util/wstr_casecmp (see the
      wstr_case_eq entry); promoted to its own exported cxx/templates
      member, wide_vec_len now delegates. Caller shape: the
      FUN_081ba9ac family parses signed HH:MM:SS-style fields out of
      vector<u16> heads and tests this result for nonzero before
      dividing each field; wstr_case_eq takes its match length through
      it. match.py: same guarded clamp structure (LLVM merges cmp/sub
      into one flags-setting subs and keeps the mov #0/asrhi pair);
      the ldm comes out as byte loads from the family's read_unaligned
      host-testing idiom, the accepted deviation of
      vector_size_elem4.

  - address: 0x083d7860
    ghidra: FUN_083d7860
    name: vector_size_elem8
    size: 16
    sig: "i32 vector_size_elem8(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}

    notes: >-
      vector_size_elem4 with `asr #3`. 4 byte-identical copies, 50 bl
      sites - 0x083d7860 14, 0x083d76a4 14, 0x083d7a88 13,
      0x083d7664 9 (0x083d7664 is ported below and hooks this same
      symbol).

  - address: 0x083d7664
    ghidra: FUN_083d7664
    name: vector_size_elem8
    size: 16
    sig: "i32 vector_size_elem8(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem8 @ 0x083d7860 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#3; bx lr` - verified against
      Ghidra's `return param_1[1] - *param_1 >> 3` and the vector_capacity_elem20
      port's per-address osos.asm read, which pins this shift
      immediately after 0x083d7650). Served by the existing
      cxx/templates port - hook this address to the vector_size_elem8
      symbol (the vector_size_elem16 copy-at-0x083d78b4 precedent). 9 bl
      call sites. Source C: decomp/c/037/083d7664_FUN_083d7664.c.
      match.py - the ldm comes out as byte loads from the family's
      read_unaligned host-testing idiom, the same accepted deviation as
      vector_size_elem4; the sub/asr#3 structure matches.

  - address: 0x083d76a4
    ghidra: FUN_083d76a4
    name: vector_size_elem8
    size: 16
    sig: "i32 vector_size_elem8(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem8 @ 0x083d7860 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#3; bx lr` - verified against
      osos.asm per address; it sits immediately before the
      vector_capacity_elem8-shaped shift at 0x083d76b4, exactly like the
      primary sits before its own capacity sibling at 0x083d7870).
      Served by the existing cxx/templates port - hook this address to
      the vector_size_elem8 symbol (the vector_size_elem16
      copy-at-0x083d78b4 precedent). 13 bl call sites plus one b
      tail-branch at 0x0829c02c; the 0x083d7860 entry's "0x083d76a4 14"
      count includes that tail-branch. Source C:
      decomp/c/037/083d76a4_FUN_083d76a4.c - Ghidra's
      `return param_1[1] - *param_1 >> 3`. match.py - the ldm comes out
      as byte loads from the family's read_unaligned host-testing idiom,
      the same accepted deviation as vector_size_elem4; the sub/asr#3
      structure matches.

  - address: 0x083d7a88
    ghidra: FUN_083d7a88
    name: vector_size_elem8
    size: 16
    sig: "i32 vector_size_elem8(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem8 @ 0x083d7860 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#3; bx lr` - verified against
      osos.asm per address; it sits immediately after the
      vector_size_elem2 @ 0x083d7a78). Served by the existing
      cxx/templates port - hook this address to the vector_size_elem8
      symbol (the vector_size_elem16 copy-at-0x083d78b4 precedent). 12
      bl call sites plus one bne tail-branch at 0x08269c50; the
      0x083d7860 entry's "0x083d7a88 13" count includes that
      tail-branch. Source C:
      decomp/c/037/083d7a88_FUN_083d7a88.c - Ghidra's
      `return param_1[1] - *param_1 >> 3`. match.py - the ldm comes out
      as byte loads from the family's read_unaligned host-testing idiom,
      the same accepted deviation as vector_size_elem4; the sub/asr#3
      structure matches.

  - address: 0x083d7674
    ghidra: FUN_083d7674
    name: vector_size_elem16
    size: 16
    sig: "i32 vector_size_elem16(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem16 @ 0x083d7884 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#4; bx lr` - verified against
      osos.asm per address; the 0x083d7884 entry's "0x083d7674 11" bl
      count was stale, it is 8 bl sites plus one beq tail-branch at
      0x08054290). Served by the existing cxx/templates port - hook this
      address to the vector_size_elem16 symbol (the vector_size_elem16
      copy-at-0x083d78b4 precedent). Source C:
      decomp/c/037/083d7674_FUN_083d7674.c - Ghidra's
      `return param_1[1] - *param_1 >> 4`. match.py - the ldm comes out as
      byte loads from the family's read_unaligned host-testing idiom,
      the same accepted deviation as vector_size_elem4; the sub/asr#4
      structure matches.

  - address: 0x083d7684
    ghidra: FUN_083d7684
    name: vector_size_elem16
    size: 16
    sig: "i32 vector_size_elem16(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem16 @ 0x083d7884 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#4; bx lr` - verified against
      osos.asm per address, matching the adjacent 0x083d7674 and
      0x083d7694 copies). Served by the existing cxx/templates port -
      hook this address to the vector_size_elem16 symbol (the
      vector_size_elem16 copy-at-0x083d78b4 precedent). 11 bl call sites
      plus one beq tail-branch at 0x08054208; the 0x083d7884 entry's
      "0x083d7684 12" count includes that tail-branch. Source C:
      decomp/c/037/083d7684_FUN_083d7684.c - Ghidra's
      `return param_1[1] - *param_1 >> 4`. match.py - the ldm comes out as
      byte loads from the family's read_unaligned host-testing idiom,
      the same accepted deviation as vector_size_elem4; the sub/asr#4
      structure matches.

  - address: 0x083d7694
    ghidra: FUN_083d7694
    name: vector_size_elem16
    size: 16
    sig: "i32 vector_size_elem16(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem16 @ 0x083d7884 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#4; bx lr` - verified against
      osos.asm per address, matching the adjacent 0x083d7674 and
      0x083d7684 copies). Served by the existing cxx/templates port -
      hook this address to the vector_size_elem16 symbol (the
      vector_size_elem16 copy-at-0x083d78b4 precedent). 11 bl call sites
      plus one beq tail-branch at 0x080542ec; the 0x083d7884 entry's
      "0x083d7694 12" count includes that tail-branch. Source C:
      decomp/c/037/083d7694_FUN_083d7694.c - Ghidra's
      `return param_1[1] - *param_1 >> 4`. match.py - the ldm comes out as
      byte loads from the family's read_unaligned host-testing idiom,
      the same accepted deviation as vector_size_elem4; the sub/asr#4
      structure matches.

  - address: 0x083d76b4
    ghidra: FUN_083d76b4
    name: vector_capacity_elem8
    size: 20
    sig: "i32 vector_capacity_elem8(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_capacity_elem8 @ 0x083d7870 (same
      `ldr r1,[r0,#0x8]; ldr r0,[r0,#0x0]; sub r0,r1,r0; mov r0,r0,
      asr #0x3; bx lr` - verified against osos.asm per address; it
      sits immediately after the vector_size_elem8 copy at 0x083d76a4,
      exactly like the primary capacity sits after its own size
      sibling at 0x083d7860). Served by the existing cxx/templates
      port - hook this address to the vector_capacity_elem8 symbol
      (the vector_size_elem16 copy-at-0x083d78b4 precedent). 2 bl
      call sites: 0x083e07fc, 0x083e09f8; no tail branches. Source C:
      decomp/c/037/083d76b4_FUN_083d76b4.c - Ghidra's
      `return param_1[2] - *param_1 >> 3`. match.py - the ldr head
      comes out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as the primary;
      the sub/asr#3 structure matches.

  - address: 0x083d7884
    ghidra: FUN_083d7884
    name: vector_size_elem16
    size: 16
    sig: "i32 vector_size_elem16(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      vector_size_elem4 with `asr #4`. 6 byte-identical copies, 78 bl
      sites - 0x083d7884 19, 0x083d7798 14, 0x083d7684 12,
      0x083d7694 12, 0x083d7674 11, 0x083d78b4 10.

  - address: 0x083d78b4
    ghidra: FUN_083d78b4
    name: vector_size_elem16
    size: 16
    sig: "i32 vector_size_elem16(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem16 @ 0x083d7884 (same `ldm
      r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#4; bx lr` - verified against
      osos.asm and Ghidra's `return param_1[1] - *param_1 >> 4`). Served
      by the existing cxx/templates port - hook this address to the
      vector_size_elem16 symbol. 10 bl call sites. match.py - the ldm
      comes out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as
      vector_size_elem4; the sub/asr#4 structure matches.

  - address: 0x083d78a4
    ghidra: FUN_083d78a4
    name: vector_size_elem32
    size: 16
    sig: "i32 vector_size_elem32(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      vector_size_elem4 with `asr #5`. 8 bl call sites; the only copy of
      this shift.

  - address: 0x083d74f4
    ghidra: FUN_083d74f4
    name: cxx_string_less
    size: 116
    sig: "u32 cxx_string_less(void *this, char *const *a, char *const *b)"
    module: cxx/string
    status: ported
    vars: {param_1: this, param_2: a, param_3: b}
    notes: >-
      34 bl call sites; the only copy. `std::less<basic_string>` -
      lexicographic `a < b`, the ordering every string-keyed container
      in the OS sorts on. memcmp @ 0x08030f64 (ported) over
      min(a.size(), b.size()) bytes, staged on the stack and selected
      with `movcc/addcs` on an address (a min, transcribed as such); on
      a tie the shorter string wins. The original computes the full
      three-way result and then keeps only its sign bit
      (`lsr r0,r0,#31`), so the return is 0 or 1. Length-driven, so
      embedded NULs order correctly. `this` (r0) is overwritten by the
      first load.

  - address: 0x083d7008
    ghidra: FUN_083d7008
    name: strstreambuf_has_input_and_output
    size: 24
    sig: "bool strstreambuf_has_input_and_output(const u8 *this)"
    module: cxx/string
    status: ported
    vars: {param_1: this}
    notes: >-
      `ldr r0,[r0,#4]; mov r1,#12; bics r0,r1,r0; movne r0,#0;
      moveq r0,#1; bx lr`: true exactly when both mode bits 0x4 and 0x8
      are set. The 2 direct callers (0x083da85c and 0x083dab68) replace
      the put buffer, then refresh the get-area pointers only on true,
      identifying the bits as simultaneous input/output areas of a
      strstreambuf. Port reads the unconditionally dereferenced mode word
      at `this + 4`; no deviations.

  - address: 0x083d7020
    ghidra: FUN_083d7020
    name: strstreambuf_input_available
    size: 36
    sig: "i32 strstreambuf_input_available(const u8 *this)"
    module: cxx/string
    status: ported
    vars: {param_1: this, bVar2: input_active, iVar1: input_end}
    notes: >-
      One direct caller (0x083d954c). If bit 0x4 of the mode word at
      `this + 4` marks an active input area and its end cursor at +0x18 is
      non-NULL, returns `input_end - input_cursor` for the current cursor
      at +0x14; otherwise returns zero. The ARM conditional loads prove the
      current cursor is untouched outside that active, non-NULL-end path.
      No deviations.

  - address: 0x083d7044
    ghidra: FUN_083d7044
    name: strstreambuf_is_eof
    size: 16
    sig: "bool strstreambuf_is_eof(const u8 *this, i32 character)"
    module: cxx/string
    status: ported
    vars: {param_1: this, param_2: character}
    notes: >-
      `cmn r1,#1; movne r0,#0; moveq r0,#1; bx lr`: ignores `this` and
      returns true only for the signed 32-bit EOF sentinel -1. The 7 direct
      callers (0x083d8eb4, 0x083d916c, 0x083d9318 twice, 0x083da614,
      0x083da728, 0x083dab68) test stream-buffer read results before
      consuming or storing them. No deviations.

  - address: 0x083d7054
    ghidra: FUN_083d7054
    name: strstreambuf_is_eof_alias
    size: 16
    sig: "bool strstreambuf_is_eof_alias(const u8 *this, i32 character)"
    module: cxx/string
    status: ported
    vars: {param_1: this, param_2: character}
    notes: >-
      `cmn r1,#1; movne r0,#0; moveq r0,#1; mov pc,lr`: independently
      addressed EOF predicate that ignores `this` and returns true only for
      the signed 32-bit sentinel -1. Its three direct call sites
      (0x083d9898 and 0x083d9b20 twice) test buffered character results
      before consuming or storing them. The predicate matches the sibling
      at 0x083d7044, but this raw body uses the semantically equivalent
      `mov pc,lr` epilogue instead of `bx lr`; exported independently.
      No deviations.

  # --- scouted, not ported (0x083c0000-0x083dffff) ---

  - address: 0x083d75e0
    ghidra: FUN_083d75e0
    name: container_is_empty
    size: 16
    sig: "u32 container_is_empty(const u8 *container)"
    module: cxx/templates
    status: ported
    notes: >-
      `ldr r0,[r0,#0x20]; rsbs r0,r0,#1; movcc r0,#0; bx lr` - returns
      1 when the word at this+0x20 is zero, 0 otherwise (the ADS idiom
      for `return !x`). 9 BYTE-IDENTICAL copies, 92 bl sites -
      0x083d75e0 15 (the canonical port), 0x083d7610 13, 0x083d75c0 13,
      0x083d7630 12, 0x083d75f0 10, 0x083d7620 9, 0x083d7600 8,
      0x083d75d0 6, 0x083d75b0 6. Callers use it as an emptiness
      predicate (0x0815a8e4 does `if (!empty()) front()`, 0x083de868
      does `if (empty()) construct`), but whether the word at +0x20 is
      a count or a head pointer is NOT pinned down, so the class is not
      identified and the name is provisional - it says what callers use
      the answer for, which is all the firmware tells us. The port
      reads the field by word index (8); match.py - LLVM normalizes the
      0/1 with clz/lsr instead of rsbs/movcc (the state_flags_contain
      precedent).

  - address: 0x083d7610
    ghidra: FUN_083d7610
    name: container_is_empty_alias_7610
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 13 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d75c0
    ghidra: FUN_083d75c0
    name: container_is_empty_alias_75c0
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 13 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d7630
    ghidra: FUN_083d7630
    name: container_is_empty_alias_7630
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 12 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d75f0
    ghidra: FUN_083d75f0
    name: container_is_empty_alias_75f0
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 10 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d7620
    ghidra: FUN_083d7620
    name: container_is_empty_alias_7620
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 9 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d7600
    ghidra: FUN_083d7600
    name: container_is_empty_alias_7600
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 8 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d75d0
    ghidra: FUN_083d75d0
    name: container_is_empty_alias_75d0
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 6 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d75b0
    ghidra: FUN_083d75b0
    name: container_is_empty_alias_75b0
    size: 16
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to container_is_empty @ 0x083d75e0; 6 bl
      sites. No separate port; hook to the container_is_empty symbol.

  - address: 0x083d6f78
    ghidra: FUN_083d6f78
    name: not_equal_deref
    size: 28
    sig: "u32 not_equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    notes: >-
      `*a == *b` computed into 0/1 and then XORed with 1 - i.e.
      `*a != *b` with a redundant final `eor r0,r0,#1` the compiler
      never folded. BINARY predicate, not the (this, const T*, const T*)
      functor the scouting note guessed - r2 is never read and r0 is
      dereferenced on the first instruction. 5 byte-identical copies,
      35 bl sites - 0x083d6f78 14 (the canonical port), 0x083cf9e0 7,
      0x083d6f94 6, 0x083d6f40 4, 0x083d6f5c 4. match.py - LLVM folds
      the eor and emits subs/movne; same load-compare structure.

  - address: 0x083cf9e0
    ghidra: FUN_083cf9e0
    name: not_equal_deref_alias_c9e0
    size: 28
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to not_equal_deref @ 0x083d6f78; 7 bl
      sites. No separate port; hook this address to the
      not_equal_deref symbol.

  - address: 0x083d6f94
    ghidra: FUN_083d6f94
    name: not_equal_deref_alias_6f94
    size: 28
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to not_equal_deref @ 0x083d6f78; 6 bl
      sites. No separate port; hook this address to the
      not_equal_deref symbol.

  - address: 0x083d6f40
    ghidra: FUN_083d6f40
    name: not_equal_deref_alias_6f40
    size: 28
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to not_equal_deref @ 0x083d6f78; 4 bl
      sites. No separate port; hook this address to the
      not_equal_deref symbol.

  - address: 0x083d6f5c
    ghidra: FUN_083d6f5c
    name: not_equal_deref_alias_6f5c
    size: 28
    module: cxx/templates
    status: ported
    notes: >-
      ALIAS - byte-identical to not_equal_deref @ 0x083d6f78; 4 bl
      sites. No separate port; hook this address to the
      not_equal_deref symbol.

  - address: 0x083cf7b8
    ghidra: FUN_083cf7b8
    name: equal_deref_f7b8_copy
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    renames:
      FUN_083cf7b8: equal_deref_f7b8_copy
      param_1: a
      param_2: b
    notes: >-
      Raw osos.dec from 0x083cf7b8 through bx lr at 0x083cf7cc is exactly
      `ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1; bx
      lr`; the next separately linked equal_deref copy begins at 0x083cf7d0,
      confirming the 24-byte extent. It aligned-loads both u32 operands,
      compares them, and returns normalized 1 or 0 for equality. Decoding
      every ARM B/BL word in osos.dec finds exactly 8 direct callers, all
      unconditional bl: 0x0809dcf4, 0x0809e074, 0x083bcb08, 0x083bcf74,
      0x083bcf90, 0x083bd014, 0x083bd0d8, and 0x083db1fc; there are no
      predicated calls, tail branches, or aligned raw-word references. The
      body is byte-identical to the established equal_deref port at
      0x083cf968, so this ledger-only alias deliberately adds no duplicate
      dispatch seam; hook 0x083cf7b8 to equal_deref. Its shared host test
      covers equal values at distinct addresses, unequal values in both
      orders, zero, all-bits-set, and iterator-shaped records whose trailing
      word is not read.
  - address: 0x083cf8a8
    ghidra: FUN_083cf8a8
    name: equal_deref_f8a8_copy
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    renames:
      FUN_083cf8a8: equal_deref_f8a8_copy
      param_1: a
      param_2: b
    notes: >-
      Raw osos.dec from 0x083cf8a8 through bx lr at 0x083cf8bc is exactly
      `ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1; bx
      lr`; the separately linked equal_deref copy at 0x083cf8c0 follows
      immediately, confirming the 24-byte extent. It aligned-loads both u32
      operands, compares them, and returns normalized 1 or 0 for equality.
      Decoding every ARM B/BL word in osos.dec finds exactly 8 direct callers,
      all unconditional bl: 0x081bf838, 0x0839bc00, 0x083c6ff8, 0x083c7464,
      0x083c7480, 0x083c7504, 0x083c75c8, and 0x083db8ac; there are no
      predicated calls, tail branches, or aligned raw-word references. The
      body is byte-identical to the established equal_deref port at
      0x083cf968, so this ledger-only alias deliberately adds no duplicate
      dispatch seam; hook 0x083cf8a8 to equal_deref. Its shared host test
      covers equal values at distinct addresses, unequal values in both
      orders, zero, all-bits-set, and iterator-shaped records whose trailing
      word is not read.
  - address: 0x083cf8f0
    ghidra: FUN_083cf8f0
    name: equal_deref_f8f0_copy
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    renames:
      FUN_083cf8f0: equal_deref_f8f0_copy
      param_1: a
      param_2: b
    notes: >-
      Raw osos.dec from 0x083cf8f0 through bx lr at 0x083cf904 is exactly
      `ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1; bx
      lr`; the separately linked equal_deref copy at 0x083cf908 follows
      immediately, confirming the 24-byte extent. It aligned-loads both u32
      operands, compares them, and returns normalized 1 or 0 for equality.
      Decoding every ARM B/BL word in osos.dec finds exactly 8 direct callers,
      all unconditional bl: 0x081bd33c, 0x0839bc54, 0x083c8fd0, 0x083c943c,
      0x083c9458, 0x083c94dc, 0x083c95a0, and 0x083db9c4; there are no
      predicated calls, tail branches, or aligned raw-word references. The
      body is byte-identical to the established equal_deref port at
      0x083cf968, so this ledger-only alias deliberately adds no duplicate
      dispatch seam; hook 0x083cf8f0 to equal_deref. Its shared host test
      covers equal values at distinct addresses, unequal values in both
      orders, zero, all-bits-set, and iterator-shaped records whose trailing
      word is not read.
  - address: 0x083cf908
    ghidra: FUN_083cf908
    name: equal_deref_f908_copy
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    renames:
      FUN_083cf908: equal_deref_f908_copy
      param_1: a
      param_2: b
    notes: >-
      Raw osos.dec from 0x083cf908 through bx lr at 0x083cf91c is exactly
      `ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1; bx
      lr`; the next separately linked equal_deref copy begins at 0x083cf920,
      confirming the 24-byte extent. It aligned-loads both u32 operands,
      compares them, and returns normalized 1 or 0 for equality. Decoding
      every ARM B/BL word in osos.dec finds exactly 8 direct callers, all
      unconditional bl: 0x081bea64, 0x0839bca8, 0x083c9a14, 0x083c9e80,
      0x083c9e9c, 0x083c9f20, 0x083c9fe4, and 0x083dba6c; there are no
      predicated calls or tail branches. The body is byte-identical to the
      established equal_deref port at 0x083cf968, so this ledger-only alias
      deliberately adds no duplicate dispatch seam; hook 0x083cf908 to
      equal_deref. Its host test covers equal values at distinct addresses,
      unequal values in both orders, zero, all-bits-set, and iterator-shaped
      records whose trailing word is not read.
  - address: 0x083cf950
    ghidra: FUN_083cf950
    name: equal_deref_f950_copy
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    renames:
      FUN_083cf950: equal_deref_f950_copy
      param_1: a
      param_2: b
    notes: >-
      Raw osos.dec is exactly the six instructions from 0x083cf950 through
      bx lr at 0x083cf964; the next separately linked equal_deref copy starts
      at 0x083cf968. It aligned-loads both u32 operands, compares them, and
      returns the normalized 0/1 equality result. Decoding every ARM B/BL word
      finds exactly 8 direct callers, all unconditional bl: 0x081bdd38,
      0x0839bcfc, 0x083cb914, 0x083cbd80, 0x083cbd9c, 0x083cbe20,
      0x083cbee4, and 0x083dbb14; there are no predicated calls, tail
      branches, or aligned raw-word references. The body is byte-identical to
      the established equal_deref port at 0x083cf968, so this ledger-only
      alias deliberately adds no duplicate dispatch seam; hook 0x083cf950 to
      equal_deref. Its existing host test covers equal values at distinct
      addresses, unequal values in both orders, zero, all-bits-set, and
      iterator-shaped records whose trailing word is not read.

  - address: 0x083cf968
    ghidra: FUN_083cf968
    name: equal_deref
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      `*a == *b` as 0/1 over word-sized operands taken by reference:
      ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1 -
      the EQUALITY twin of not_equal_deref (@ 0x083d6f78, above)
      without that family's redundant final eor. 12 bl call sites at
      this address, all unpredicated, binary-verified: 0x081f04b8,
      0x081f09b4, 0x081f0ae4, 0x081f0c08, 0x081f0f98, 0x081f11d0,
      0x083ccdbc, 0x083cd22c, 0x083cd248, 0x083cd2cc, 0x083cd390,
      0x083dbbbc. Callers are container-iteration loops that
      stack-materialize a cursor iterator and the container's end
      iterator and compare their FIRST words (the current-node
      pointer) - the ADS checked-iterator operator==. Ghidra size 24
      confirmed against the raw bytes (the next copy starts
      immediately at 0x083cf980). This body is one of 41
      BYTE-IDENTICAL copies, one per container instantiation: a
      contiguous 39-copy run 0x083cf650-0x083cf9c8 (every 0x18 bytes)
      plus strays 0x082a1834, 0x082a7884 and 0x083d5e70; 241 direct bl
      plus 2 predicated sites across the family (blne @ 0x083d5d90 ->
      0x083d5e70, blhi @ 0x088a976c -> 0x083cf9c8). Per-copy bl counts
      - 0x083cf848 11, 0x083cf980 9, 0x083cf7b8/0x083cf8a8/0x083cf8f0/
      0x083cf908/0x083cf950 8 each, 0x083cf650/0x083cf7a0/0x083cf8d8/
      0x083cf920 7 each, 0x083cf680/0x083cf698/0x083cf6c8/0x083cf728/
      0x083cf740/0x083cf758/0x083cf788/0x083cf8c0/0x083cf938/
      0x083cf9b0 6 each, 0x083cf668/0x083cf6b0/0x083cf710/0x083cf770/
      0x083cf7d0/0x083cf7e8/0x083cf830/0x083cf860/0x083cf878/
      0x083cf890/0x083cf998/0x083cf9c8 5 each, 0x082a1834 4,
      0x083cf6e0/0x083cf800 4 each, 0x083cf6f8/0x083d5e70 3 each,
      0x082a7884 2, 0x083cf818 1. Two copies are already spoken for
      under other names: 0x082a1834 is ported as fixed16_eq_indirect
      (fp/fp_misc - a Q16.16 comparator that merely shares the
      load-cmp-select shape) and 0x083d5e70 is inlined privately in
      cxx/list_splice as iter_equal; hook every OTHER copy to this
      equal_deref symbol. The export carries a distinct link_section
      (.text.equal_deref) so LLVM's identical-function folding cannot
      merge it onto fixed16_eq_indirect - both labels verified present
      in librustypod.a. match.py structural: same two pointer loads;
      LLVM computes the equality branchlessly as sub r0,r0,r1 / clz
      r0,r0 / lsr r0,r0,#5 (32>>5 = 1 only when the words are equal -
      identical semantics) instead of the movne/moveq pair, and adds
      the usual push {fp,lr}/pop {fp,pc} frame - the same accepted
      deviation as fixed16_eq_indirect.

  - address: 0x083cf980
    ghidra: FUN_083cf980
    name: equal_deref_f980_copy
    size: 24
    sig: "u32 equal_deref(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Byte-identical to equal_deref @ 0x083cf968: `ldr r0,[r0]; ldr
      r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1; bx lr`. The 9
      direct calls are 0x081e1518, 0x081e1568, 0x083cd808, 0x083cdc74,
      0x083cdc90, 0x083cdd14, 0x083cddf4, 0x083cdee4 and 0x083dbcc4;
      all are plain `bl`, with no predicated forms, verified by decoding
      every ARM B/BL word in osos.dec. This is ledger-only: hook this
      physical copy to the established equal_deref export rather than add
      a redundant dispatch seam.

  - address: 0x083cf848
    ghidra: FUN_083cf848
    name: iterator_equal
    size: 24
    sig: "u32 iterator_equal(const u32 *a, const u32 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Iterator equality, `*a == *b`, returning 1 or 0. Raw bytes decode
      ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0; moveq r0,#1;
      next function starts at 0x083cf860, confirming the 24-byte extent.
      11 direct bl sites, all unconditional (zero predicated), verified by
      decoding every ARM B/BL word in osos.dec: 0x08101c60, 0x08101e54,
      0x08101f1c, 0x08101f60, 0x083c4258, 0x083c46c8, 0x083c46e4,
      0x083c4768, 0x083c4848, 0x083c4938, 0x083db5bc. This body is
      byte-identical to equal_deref @ 0x083cf968 but exported separately
      in `.text.iterator_equal` to retain the independently hookable label.

  - address: 0x083d76f8
    ghidra: FUN_083d76f8
    name: vector_size_elem12
    size: 16
    sig: "i32 vector_size_elem12(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::size()` for a 12-byte element - a non-power-of-two
      member of the vector_size family: same `ldm r0,{r0,r1}; sub
      r0,r1,r0` head as the shifts, then `mov r1,#0xc` and a TAIL
      BRANCH into the ADS signed divide @ 0x08031568 (ported as
      __rt_sdiv). 18 bl call sites here, the hottest instantiation of
      the divide half; 62 across all 5 byte-identical copies (the other
      four stay identified - only this one is ported). The port calls
      the ported __rt_sdiv, which is signed and truncating, so a
      reversed vector's negative span truncates toward zero. Verified
      against osos.asm: `ldmia r0,{r0,r1}; sub r0,r1,r0; mov r1,#0xc;
      b 0x08031568`. match.py - same structure as vector_size_elem24;
      the ldm comes out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as
      vector_size_elem4.

  - address: 0x083d7750
    ghidra: FUN_083d7750
    name: vector_size_elem24
    size: 16
    sig: "i32 vector_size_elem24(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::size()` for a 24-byte element - the non-power-of-two
      half of the vector_size family: same `ldm r0,{r0,r1}; sub
      r0,r1,r0` head as the shifts, but then `mov r1,#0x18` and a TAIL
      BRANCH into the ADS signed divide @ 0x08031568 (ported as
      __rt_sdiv; the old blocker is gone). 31 bl call sites here; the
      byte-identical copy 0x083d77dc (14 bl sites) can hook the same
      symbol. The port calls the ported __rt_sdiv, which is signed and
      truncating, so a reversed vector's negative span truncates toward
      zero. match.py - same structure as vector_capacity (sub / mov
      r1,#24 / b __rt_sdiv); the ldm comes out as byte loads from the
      family's read_unaligned host-testing idiom, the same accepted
      deviation as vector_size_elem4.       Other divide instantiations
      (identified, not ported): elem 12 (4 more fns besides the ported
      0x083d76f8; 62 calls across all 5). Elem 40 is now ported
      (0x083d783c, 1 fn, 14 calls; see its entry). Elem 20 is now
      ported (0x083d7640) and elem 28 is
      now ported (0x083d7894); see their entries.

  - address: 0x083d77dc
    ghidra: FUN_083d77dc
    name: vector_size_elem24
    size: 16
    sig: "i32 vector_size_elem24(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical second instantiation of vector_size_elem24
      @ 0x083d7750 (same `ldm r0,{r0,r1}; sub r0,r1,r0; mov r1,#0x18;
      b 0x08031568` - verified against osos.asm). Served by the
      existing cxx/templates port - hook both addresses to the one
      symbol. 14 bl call sites (census from the first instantiation's
      commit; osos.asm text shows 6 `bl` references).

  - address: 0x083d7640
    ghidra: FUN_083d7640
    name: vector_size_elem20
    size: 16
    sig: "i32 vector_size_elem20(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::size()` for a 20-byte element - a non-power-of-two
      member of the vector_size family: same `ldm r0,{r0,r1}; sub
      r0,r1,r0` head as the shifts, then `mov r1,#0x14` and a TAIL
      BRANCH into the ADS signed divide @ 0x08031568 (ported as
      __rt_sdiv). 14 bl call sites here (family census; osos.asm text
      shows 12 `bl` references), 21 across the 2 byte-identical copies;
      the second instantiation 0x083d771c (7 bl sites) is byte-identical
      and can hook the same symbol. Verified against osos.asm: `ldmia
      r0,{r0,r1}; sub r0,r1,r0; mov r1,#0x14; b 0x08031568`. The port
      calls the ported __rt_sdiv, which is signed and truncating, so a
      reversed vector's negative span truncates toward zero. match.py -
      same structure as vector_size_elem24 (sub / mov r1,#20 / b
      __rt_sdiv); the ldm comes out as byte loads from the family's
      read_unaligned host-testing idiom, the same accepted deviation as
      vector_size_elem4.

  - address: 0x083d771c
    ghidra: FUN_083d771c
    name: vector_size_elem20
    size: 16
    sig: "i32 vector_size_elem20(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical second instantiation of vector_size_elem20
      @ 0x083d7640 (same `ldm r0,{r0,r1}; sub r0,r1,r0; mov r1,#0x14;
      b 0x08031568` - verified against osos.asm). Served by the
      existing cxx/templates port - hook this address to the one
      vector_size_elem20 symbol (the vector_size_elem24
      copy-at-0x083d77dc precedent). 7 bl call sites. match.py against
      vector_size_elem20 at this address - same structure (sub / mov
      r1,#20 / b __rt_sdiv); the ldm comes out as byte loads from the
      family's read_unaligned host-testing idiom, the same accepted
      deviation as vector_size_elem4.

  - address: 0x083d772c
    ghidra: FUN_083d772c
    name: vector_size_elem12
    size: 16
    sig: "i32 vector_size_elem12(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem12 @ 0x083d76f8 (same
      `ldm r0,{r0,r1}; sub r0,r1,r0; mov r1,#0xc; b 0x08031568` -
      verified against osos.asm per address; it sits immediately after
      the vector_size_elem20 copy at 0x083d771c and immediately before
      the vector_capacity_elem12 copy at 0x083d773c, exactly like the
      primary sits between vector_size_elem12's own divide siblings).
      Served by the existing cxx/templates port - hook this address to
      the one vector_size_elem12 symbol (the vector_size_elem20
      copy-at-0x083d771c precedent). 3 bl call sites: 0x083e184c,
      0x083e18a0, 0x083e1934. Source C:
      decomp/c/037/083d772c_FUN_083d772c.c - Ghidra inlines the ADS
      signed divide through the tail branch; the real body is the
      4-instruction size/12 head. match.py against vector_size_elem12
      at this address - same structure (sub / mov r1,#12 / b
      __rt_sdiv); the ldm comes out as byte loads from the family's
      read_unaligned host-testing idiom, the same accepted deviation
      as vector_size_elem4.

  - address: 0x083d7774
    ghidra: FUN_083d7774
    name: vector_size_elem12
    size: 16
    sig: "i32 vector_size_elem12(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem12 @ 0x083d76f8 (same
      `ldm r0,{r0,r1}; sub r0,r1,r0; mov r1,#0xc; b 0x08031568` -
      verified against osos.asm per address; per-address verification
      stays the rule. It sits immediately after the vector_capacity
      primary at 0x083d7760 and immediately before a further
      divide-by-12 sibling at 0x083d7784. Served by the existing
      cxx/templates port - hook this address to the one
      vector_size_elem12 symbol (the vector_size_elem12
      copy-at-0x083d772c precedent). 10 bl call sites: 0x08052da8,
      0x08052df0, 0x08052e5c, 0x080665a8, 0x0813cf28, 0x0813dc98,
      0x0813dd98, 0x083e20dc, 0x083e2130, 0x083e21c4. Source C:
      decomp/c/037/083d7774_FUN_083d7774.c - Ghidra inlines the ADS
      signed divide through the tail branch; the real body is the
      4-instruction size/12 head. match.py against vector_size_elem12
      at this address - same structure (sub / mov r1,#12 / b
      __rt_sdiv); the ldm comes out as byte loads from the family's
      read_unaligned host-testing idiom, the same accepted deviation
      as vector_size_elem4.

  - address: 0x083d77bc
    ghidra: FUN_083d77bc
    name: vector_size_elem12
    size: 16
    sig: "i32 vector_size_elem12(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem12 @ 0x083d76f8 (same
      `ldm r0,{r0,r1}; sub r0,r1,r0; mov r1,#0xc; b 0x08031568` -
      verified against osos.asm per address; per-address verification
      stays the rule. The 16 bytes match the primary save the
      tail-branch offset, exactly like the copies at 0x083d772c and
      0x083d7774. It sits immediately after the
      vector_capacity_elem16 primary at 0x083d77a8 and immediately
      before the vector_size_elem4 alias at 0x083d77cc, exactly like
      the other copies sit beside their own capacity siblings. Served
      by the existing cxx/templates port - hook this address to the
      one vector_size_elem12 symbol (the vector_size_elem12
      copy-at-0x083d772c precedent). 11 bl call sites: 0x0820829c,
      0x082083ec, 0x08208500, 0x08208518, 0x083e273c, 0x083e2830,
      0x083e2840, 0x083e28f4, 0x083e2968, 0x083e297c, 0x083e299c.
      Source C: decomp/c/037/083d77bc_FUN_083d77bc.c - Ghidra inlines
      the ADS signed divide through the tail branch; the real body is
      the 4-instruction size/12 head. match.py against
      vector_size_elem12 at this address - same structure (sub / mov
      r1,#12 / b __rt_sdiv); the ldm comes out as byte loads from the
      family's read_unaligned host-testing idiom, the same accepted
      deviation as vector_size_elem4.

  - address: 0x083d7800
    ghidra: FUN_083d7800
    name: vector_size_elem12
    size: 16
    sig: "i32 vector_size_elem12(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem12 @ 0x083d76f8 (same
      `ldm r0,{r0,r1}; sub r0,r1,r0; mov r1,#0xc; b 0x08031568` -
      verified against osos.asm per address; per-address verification
      stays the rule. The 16 bytes match the primary save the
      tail-branch offset, exactly like the copies at 0x083d772c,
      0x083d7774 and 0x083d77bc. It sits immediately after the
      vector_capacity_elem24 copy at 0x083d77ec and immediately
      before the bounds-compare helper at 0x083d7810. Served by the
      existing cxx/templates port - hook this address to the one
      vector_size_elem12 symbol (the vector_size_elem12
      copy-at-0x083d772c precedent). 14 bl call sites: 0x083d7d58,
      0x083d7da4, 0x083e2f94, 0x083e2ff4, 0x083e308c, 0x083e3108,
      0x083e3124, 0x083e3130, 0x083e31b8, 0x083e31dc, 0x083e31e8,
      0x083e3214, 0x083e3234, 0x083e3254. Source C:
      decomp/c/037/083d7800_FUN_083d7800.c - Ghidra inlines the ADS
      signed divide through the tail branch; the real body is the
      4-instruction size/12 head. match.py against
      vector_size_elem12 at this address - same structure (sub / mov
      r1,#12 / b __rt_sdiv); the ldm comes out as byte loads from the
      family's read_unaligned host-testing idiom, the same accepted
      deviation as vector_size_elem4.

  - address: 0x083d7894
    ghidra: FUN_083d7894
    name: vector_size_elem28
    size: 16
    sig: "i32 vector_size_elem28(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::size()` for a 28-byte element - a non-power-of-two
      member of the vector_size family: same `ldm r0,{r0,r1}; sub
      r0,r1,r0` head as the shifts, then `mov r1,#0x1c` and a TAIL
      BRANCH into the ADS signed divide @ 0x08031568 (ported as
      __rt_sdiv). 8 bl call sites; the only copy. Verified against
      osos.asm: `ldmia r0,{r0,r1}; sub r0,r1,r0; mov r1,#0x1c;
      b 0x08031568`. The port calls the ported __rt_sdiv, which is
      signed and truncating, so a reversed vector's negative span
      truncates toward zero. match.py - same structure as
      vector_size_elem24 (sub / mov r1,#28 / b __rt_sdiv); the ldm
      comes out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as
      vector_size_elem4.

  - address: 0x083d783c
    ghidra: FUN_083d783c
    name: vector_size_elem40
    size: 16
    sig: "i32 vector_size_elem40(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::size()` for a 40-byte element - a non-power-of-two
      member of the vector_size family: same `ldm r0,{r0,r1}; sub
      r0,r1,r0` head as the shifts, then `mov r1,#0x28` and a TAIL
      BRANCH into the ADS signed divide @ 0x08031568 (ported as
      __rt_sdiv). 14 bl call sites; the only copy. Verified against
      osos.asm: `ldmia r0,{r0,r1}; sub r0,r1,r0; mov r1,#0x28;
      b 0x08031568`. The port calls the ported __rt_sdiv, which is
      signed and truncating, so a reversed vector's negative span
      truncates toward zero. match.py - same structure as
      vector_size_elem24 (sub / mov r1,#40 / b __rt_sdiv); the ldm
      comes out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as
      vector_size_elem4.

  - address: 0x083d7760
    ghidra: FUN_083d7760
    name: vector_capacity
    size: 20
    sig: "i32 vector_capacity(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for a 24-byte element - the capacity()
      sibling of the vector_size family, reading the end-of-storage word
      at +8 instead of the end word at +4 (`ldr r1,[r0,#8]; ldr
      r0,[r0]; sub r0,r1,r0; mov r1,#0x18`) and TAIL-BRANCHING into the
      ADS signed divide @ 0x08031568 (ported as __rt_sdiv; the old
      blocker is gone). 6 bl call sites. size corrected 16 -> 20
      (functions.csv and the next function at 0x083d7774 agree). The
      port calls the ported __rt_sdiv (now #[inline(never)] to keep the
      tail-branch boundary, the __rt_udiv precedent). match.py - same
      structure (sub / mov r1,#24 / b __rt_sdiv); the ldr pairs come
      out as byte loads from the family's read_unaligned host-testing
      idiom, same deviation as vector_size_elem4. Other instantiations:
      all are now ported - 0x083d7708 (see its entry) turned out to be a
      DIVIDE-by-12 member, not a shift, so element sizes must be read
      from osos.asm per address. 0x083d77a8 (see its entry) really is
      shift-based (asr #4, a 16-byte element). 0x083d77ec (see its
      entry) is another divide-by-24 copy, byte-identical to this one.
      0x083d7828 (see its entry) is a divide-by-12 copy, byte-identical
      to vector_capacity_elem12. 0x083d784c (see its entry) is a
      divide-by-40 member (mov r1,#0x28), a distinct instantiation, not
      a copy. 0x083d7870 (see its entry) is a shift member (asr #3, an
      8-byte element), the second real shift of the family after
      0x083d77a8. 0x083d7954 (see its entry) is a shift member too
      (asr #2, a 4-byte element), the third and last of the family -
      the capacity block is fully ported.

  - address: 0x083d784c
    ghidra: FUN_083d784c
    name: vector_capacity_elem40
    size: 20
    sig: "i32 vector_capacity_elem40(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for a 40-byte element - the DIVIDE member
      of the capacity family for a 40-byte element: same end-of-storage
      head as the 24-byte primary (`ldr r1,[r0,#8]; ldr r0,[r0]; sub
      r0,r1,r0`), then `mov r1,#0x28` and a TAIL BRANCH into the ADS
      signed divide @ 0x08031568 (ported as __rt_sdiv). Verified against
      osos.asm per address - the ledger's old blanket shift-based guess
      for the remaining capacity instantiations is wrong for this one
      too (as it was for 0x083d7708, 0x083d77ec and 0x083d7828); only
      0x083d77a8 is really a shift. The divide is signed and truncating,
      so a reversed span truncates toward zero and a partial element is
      dropped. 4 bl call sites (0x083e32bc, 0x083e33d8, 0x083e3434,
      0x083e34bc). The port calls the ported __rt_sdiv
      (#[inline(never)] keeps the tail-branch boundary, the vector_capacity
      precedent). match.py - same structure (sub / mov r1,#40 / b
      __rt_sdiv); the ldr pairs come out as byte loads from the family's
      read_unaligned host-testing idiom, the same accepted deviation as
      vector_size_elem4, plus LLVM's fp frame.

  - address: 0x083d77a8
    ghidra: FUN_083d77a8
    name: vector_capacity_elem16
    size: 20
    sig: "i32 vector_capacity_elem16(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for a 16-byte element - the SHIFT member
      of the capacity family: same end-of-storage head as the 24-byte
      primary (`ldr r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0`), then `mov
      r0,r0, asr #4` and `bx lr` - 16 is a power of two, so no tail
      branch into the ADS signed divide. Verified against osos.asm; the
      ledger's old blanket shift-based guess is right for THIS address
      only by luck (the 0x083d7708 sibling with the same guess was a
      divide), so per-address verification stays the rule. The shift is
      arithmetic, so a reversed span stays negative (rounding toward
      -inf, unlike the divide members' toward-zero truncation). 4 bl
      call sites. match.py - same sub / asr #4 core; the ldr pairs come
      out as byte loads from the family's read_unaligned host-testing
      idiom, the same accepted deviation as vector_size_elem4.

  - address: 0x083d7870
    ghidra: FUN_083d7870
    name: vector_capacity_elem8
    size: 20
    sig: "i32 vector_capacity_elem8(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for an 8-byte element - the second SHIFT
      member of the capacity family after 0x083d77a8 (elem16): same
      end-of-storage head as the 24-byte primary (`ldr r1,[r0,#8]; ldr
      r0,[r0]; sub r0,r1,r0`), then `mov r0,r0, asr #3` and `bx lr` -
      8 is a power of two, so no tail branch into the ADS signed divide.
      Verified against osos.asm per address; the ledger's old blanket
      shift-based guess is right for THIS address, but that is luck (the
      0x083d7708, 0x083d77ec, 0x083d7828 and 0x083d784c siblings with
      the same guess were all divides), so per-address verification
      stays the rule. The shift is arithmetic, so a reversed span stays
      negative (rounding toward -inf, unlike the divide members'
      toward-zero truncation). 4 bl call sites (0x083e35dc, 0x083e36f4,
      0x083e3748, 0x083e37cc). match.py - same sub / asr #3 core; the
      ldr pairs come out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as
      vector_size_elem4.

  - address: 0x083d7954
    ghidra: FUN_083d7954
    name: vector_capacity_elem4
    size: 20
    sig: "i32 vector_capacity_elem4(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for a 4-byte element - the third and last
      SHIFT member of the capacity family after 0x083d77a8 (elem16) and
      0x083d7870 (elem8): same end-of-storage head as the 24-byte
      primary (`ldr r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0`), then `mov
      r0,r0, asr #2` and `bx lr` - 4 is a power of two, so no tail
      branch into the ADS signed divide. Verified against osos.asm per
      address; the ledger's old blanket shift-based guess is right for
      THIS address, but that is luck (the 0x083d7708, 0x083d77ec,
      0x083d7828 and 0x083d784c siblings with the same guess were all
      divides), so per-address verification stays the rule. The shift
      is arithmetic, so a reversed span stays negative (rounding toward
      -inf, unlike the divide members' toward-zero truncation). 4 bl
      call sites (0x083e5988, 0x083e5a90, 0x083e5bdc, 0x083e5c60).
      match.py - same sub / asr #2 core; the ldr pairs come out as byte
      loads from the family's read_unaligned host-testing idiom, the
      same accepted deviation as vector_size_elem4, plus LLVM's fp
      frame. This was the last identified-not-ported instantiation of
      the capacity family - the block is now fully ported.

  - address: 0x083d7968
    ghidra: FUN_083d7968
    name: vector_size_bool
    size: 76
    sig: "i32 vector_size_bool(const VectorBoolBounds *bits)"
    module: cxx/templates
    status: ported
    vars: {param_1: bits}
    notes: >-
      `std::vector<bool>::size()` - the bit-vector member of the
      vector_size family: the head is TWO bit iterators `{begin_word,
      begin_bit, end_word, end_bit}` and the body is the bit-iterator
      difference `(end_word - begin_word) / 4` whole words of 32 bits
      each, plus end_bit, minus begin_bit (`sub` / `mov r0,r0, asr #2`
      / `add r0,r2,r0, lsl #5` / `sub`). The asr is ARITHMETIC, so a
      reversed head's negative word span floors like the family's shift
      members. The original's spill of all four head words to the stack
      and immediate reload is ADS noise around an inlined iterator
      difference; the port keeps only the computation. Identification:
      the immediate neighbors are the bit-iterator members -
      operator!= @ 0x083d79b4 (compare word pointer, then bit offset),
      the `{word, 1 << bit}` mask-reference ctor @ 0x083d79dc and the
      `*word & mask` bit test @ 0x083d7a20 - and both call sites feed
      the result into `words = (max(0x20, 2*size) + 0x1f) >> 5; alloc
      words * 4`, the vector<bool> storage grow. 2 bl call sites
      (0x083e5dfc, 0x083e5e10), the only copy (the other two
      `add r0,r2,r0, lsl #5` hits in osos.asm, 0x08004c54 and
      0x08008698, are unrelated array indexing). match.py - same
      sub / asr #2 / lsl #5 / sub core; the word loads come out as byte
      loads from the family's read_unaligned host-testing idiom, the
      same accepted deviation as vector_size_elem4, plus LLVM's fp
      frame.

  - address: 0x083d79b4
    ghidra: FUN_083d79b4
    name: vector_bool_iter_not_equal
    size: 40
    sig: "u32 vector_bool_iter_not_equal(const VectorBoolIter *a, const VectorBoolIter *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      `std::vector<bool>` bit-iterator `operator!=`: two `{word, bit}`
      iterators compare unequal when their word pointers differ or,
      the pointers being equal, their bit offsets do. The original is
      a single branchless predicated run (`ldr r2,[r0]; ldr r3,[r1];
      cmp r2,r3; ldreq r0,[r0,#4]; ldreq r1,[r1,#4]; cmpeq r0,r1`) and
      computes the EQUALITY first (`movne r0,#0` / `moveq r0,#1`) before
      inverting it with a final `eor r0,r0,#1` - the same never-folded
      ADS idiom as not_equal_deref. Ghidra's `if (iVar1 == iVar2)`
      rewrite of the predication is noise; the semantics are
      `word_a != word_b || bit_a != bit_b`. Identification: sits in
      the bit-iterator cluster between vector_size_bool @ 0x083d7968
      and the mask-reference ctor @ 0x083d79dc (see the 0x083d7968
      entry for the neighborhood). 2 bl call sites (0x083e5dc4,
      0x083e5f40), both in the vector<bool> storage-grow path; the
      only copy. match.py - same compare-words-then-bits structure;
      the word loads come out as byte loads from the family's
      read_unaligned host-testing idiom, the same accepted deviation
      as vector_size_bool, plus LLVM's fp frame and a subs/movne
      fold of the equality-invert.

  - address: 0x083d79dc
    ghidra: FUN_083d79dc
    name: vector_bool_reference_init
    size: 28
    sig: "VectorBoolReference *vector_bool_reference_init(VectorBoolReference *mask_ref, const VectorBoolIter *iter)"
    module: cxx/templates
    status: ported
    vars: {param_1: mask_ref, param_2: iter}
    notes: >-
      `std::vector<bool>` mask-reference constructor: initializes a
      `{word, mask}` reference proxy from a bit iterator `{word, bit}`
      - `ldr r2,[r1]; ldr r1,[r1,#4]; mov r3,#1; mov r1,r3, lsl r1;
      str r1,[r0,#4]; str r2,[r0]`, i.e. copy the word pointer and
      store the single-bit mask `1 << bit`. r0 (mask_ref) is left
      untouched, so the port returns it like deque_iter_assign. The
      mask shift is an ARM register lsl - only the low byte of the bit
      offset is used and a shift of 32 or more yields zero; the port
      reproduces that with checked_shl (an in-range iterator, bit
      0..32, never hits either edge). Identification: sits in the
      bit-iterator cluster between operator!= @ 0x083d79b4 and the
      `*word & mask` bit test @ 0x083d7a20 (see the 0x083d7968 entry
      for the neighborhood). 8 bl call sites (0x0826a3f4, 0x083e5d90,
      0x083e5da4, 0x083e5dd8, 0x083e5e78, 0x083e5f04, 0x083e5f20,
      0x083e603c), seven of them in the vector<bool> storage-grow
      path; the only copy. match.py - same ldr / lsl / str structure;
      the loads and stores come out as byte accesses from the family's
      read_unaligned/write_unaligned host-testing idiom, the same
      accepted deviation as vector_size_bool, the checked_shl compiles
      to a `cmp #32` + conditional lsl (exactly the ARM register-shift
      zero-above-31 semantics), plus LLVM's fp frame.

  - address: 0x083d79f8
    ghidra: FUN_083d79f8
    name: vector_bool_iter_minus
    size: 40
    sig: "u32 *vector_bool_iter_minus(VectorBoolIter *result, const VectorBoolIter *iter, i32 distance)"
    module: cxx/templates
    status: ported
    vars: {param_1: result, param_2: iter, param_3: distance}
    notes: >-
      `std::vector<bool>` bit-iterator `operator-(iter, n)`: copies
      the `{word, bit}` iterator to a stack temp (`ldmia r1,{r0,r1}` /
      `stmia sp,{r0,r1}`, reusing the pushed r2/r3 slots), advances
      the temp by the NEGATED distance (`rsb r1, r2, #0`, a wrapping
      32-bit negate) through the in-place advance @ 0x083e5f84, and
      stores the result through the hidden sret pointer saved in r4
      (`ldmia sp,{r0,r1}` / `stmia r4,{r0,r1}`). The reload leaves r0
      holding the result's word pointer, NOT the sret pointer, so the
      port returns it; both call sites recompute every pointer they
      need and never consume r0. The direct `bl 0x083e5f84` now calls
      ported `vector_bool_iter_advance`, the `_Vb_iterator::operator+=`
      floor-division fold. Identification: sits between the
      mask-reference ctor @
      0x083d79dc and the `*word & mask` bit test @ 0x083d7a20 in the
      bit-iterator cluster (see the 0x083d7968 entry for the
      neighborhood); the 0x0826a3e8 call site pairs `operator++` @
      0x083e5fc0 with `operator-(iter, 1)` plus the ctor and the
      set/clear-bit store @ 0x083e5fe8 - write-back of the bit just
      stepped past. 2 bl call sites (0x0826a3e8, 0x083e5d6c), the
      latter in the vector<bool> storage-grow path; the only copy.
      match.py - same rsb-negate / direct call / copy-out structure;
      the head loads and stores come out as byte accesses from the
      family's read_unaligned/write_unaligned host-testing idiom (the
      accepted vector_bool_reference_init deviation), plus LLVM's fp
      frame.

  - address: 0x083e5f84
    ghidra: FUN_083e5f84
    name: vector_bool_iter_advance
    size: 60
    sig: "void vector_bool_iter_advance(VectorBoolIter *iter, i32 distance)"
    module: cxx/templates
    status: ported
    vars: {param_1: iter, param_2: distance, iVar1: remainder, iVar2: advanced_word, bVar3: remainder_negative}
    notes: >-
      `std::vector<bool>` bit-iterator `_Vb_iterator::operator+=`:
      adds signed `distance` to `iter.bit` with 32-bit wrapping, then
      folds the total through a sign bias (`asr #31` / `lsr #27`), `asr
      #5`, and `bic #31`. A negative residual triggers `+32` and one
      preceding 4-byte storage word, yielding floor division with a
      0..31 bit remainder. The 60-byte leaf body reads and writes only
      the `{word, bit}` head; r0 remains `iter` incidentally but the
      recovered C return ABI is void. Its direct caller is
      vector_bool_iter_minus @ 0x083d79f8 (`bl` at 0x083d7a10), which
      advances a stack copy by wrapping-negated distance. match.py -
      same sign-bias/floor-fold and conditional `+32` / `-4`
      structure; LLVM expands host-safe unaligned head accesses to byte
      operations and adds a frame.

  - address: 0x083d7a20
    ghidra: FUN_083d7a20
    name: vector_bool_reference_test
    size: 24
    sig: "u32 vector_bool_reference_test(const VectorBoolReference *mask_ref)"
    module: cxx/templates
    status: ported
    vars: {param_1: mask_ref}
    notes: >-
      `std::vector<bool>` mask-reference dereference - the
      `_Vb_reference`-shaped proxy's `operator bool`: `ldr r1,[r0];
      ldr r0,[r0,#4]; ldr r1,[r1]; ands r0,r1,r0; movne r0,#1; bx lr`,
      i.e. read the reference's word pointer and single-bit mask, load
      the storage word, and return whether the masked bit is set as the
      ADS 0/1 word-sized bool (`ands` sets the flags, `movne r0,#1`
      normalizes). Unlike the rest of the bit-iterator cluster this
      member DOES dereference the storage, so `word` must be valid.
      Identification: sits immediately after the mask-reference ctor @
      0x083d79dc in the bit-iterator cluster (see the 0x083d7968 entry
      for the neighborhood), the read half of the `{word, mask}` proxy
      that ctor writes. 3 bl call sites (0x08269edc, 0x083e5dac,
      0x083e5f28), the last two in the vector<bool> storage-grow path;
      the only copy. match.py - same ldr / ldr / ldr / and structure;
      the head loads come out as byte accesses from the family's
      read_unaligned host-testing idiom, the same accepted deviation as
      vector_bool_reference_init, plus LLVM's fp frame.

  - address: 0x083d7708
    ghidra: FUN_083d7708
    name: vector_capacity_elem12
    size: 20
    sig: "i32 vector_capacity_elem12(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for a 12-byte element - the capacity()
      sibling of the vector_size family, reading the end-of-storage word
      at +8 instead of the end word at +4 (`ldr r1,[r0,#8]; ldr
      r0,[r0]; sub r0,r1,r0; mov r1,#0xc`) and TAIL-BRANCHING into the
      ADS signed divide @ 0x08031568 (ported as __rt_sdiv). 4 bl call
      sites. Verified against osos.asm: the old ledger note in
      vector_capacity's entry guessed this instantiation was shift-based
      - wrong, 12 is not a power of two and the function carries `mov
      r1,#0xc; b 0x08031568`, a divide member exactly like the 24-byte
      primary. The port calls the ported __rt_sdiv, which is signed and
      truncating, so a reversed vector's negative span truncates toward
      zero and a partial element is dropped. match.py - same structure
      as vector_capacity (sub / mov r1,#12 / b __rt_sdiv); the ldr
      pairs come out as byte loads from the family's read_unaligned
      host-testing idiom, the same accepted deviation as
      vector_size_elem4.

  - address: 0x083d7650
    ghidra: FUN_083d7650
    name: vector_capacity_elem20
    size: 20
    sig: "i32 vector_capacity_elem20(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      `vector<T>::capacity()` for a 20-byte element - a DIVIDE member
      of the capacity family: same end-of-storage head as the 24-byte
      primary (`ldr r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0`), then `mov
      r1,#0x14` and a TAIL BRANCH into the ADS signed divide @
      0x08031568 (ported as __rt_sdiv), signed and truncating like any
      C `/`, so a reversed span truncates toward zero and a partial
      element is dropped. 2 bl call sites (0x083e0130, 0x083e0260).
      This instantiation was never in the ledger at all - the
      vector_capacity_elem4 entry's "the block is now fully ported"
      claim was premature; it sits immediately before the
      vector_size_elem8-shaped shift at 0x083d7664. Verified against
      osos.asm per address. match.py - same sub / mov r1,#20 / b
      __rt_sdiv core as the other divide members; the ldr pairs come
      out as byte loads from the family's read_unaligned host-testing
      idiom, plus LLVM's fp frame, the same accepted deviation as
      vector_capacity_elem12.

  - address: 0x083d77ec
    ghidra: FUN_083d77ec
    name: vector_capacity_elem24_copy_77ec
    size: 20
    sig: "i32 vector_capacity_elem24_copy_77ec(const VectorStorage *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      A second, BYTE-IDENTICAL instantiation of the 24-byte
      vector_capacity @ 0x083d7760: same end-of-storage head (`ldr
      r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0`), then `mov r1,#0x18` and
      a TAIL BRANCH into the ADS signed divide @ 0x08031568 (ported as
      __rt_sdiv). Verified against osos.asm per address - the ledger's
      blanket shift-based guess for the remaining capacity
      instantiations is wrong for this one too (as it was for
      0x083d7708); only 0x083d77a8 is really a shift. 3 bl call sites
      (0x083e2c1c, 0x083e2d4c, 0x083e2dd0) - the old "4 bl sites" guess
      was stale. The divide is signed and truncating, so a reversed
      vector's negative span truncates toward zero and a partial
      element is dropped. LLVM's MergeFunctions folds the port into an
      alias of vector_capacity's section (the symbol stays exported and
      hookable; objdump prints no separate label), so match.py is run
      against the canonical vector_capacity: same structure (sub / mov
      r1,#24 / b __rt_sdiv); the ldr pairs come out as byte loads from
      the family's read_unaligned host-testing idiom, the same accepted
      deviation as vector_size_elem4, plus LLVM's fp frame.

  - {address: 0x083d7828, ghidra: FUN_083d7828, name: vector_capacity_elem12_alias_7828,
     size: 20, sig: "i32 vector_capacity_elem12_alias_7828(const VectorStorage *vector)",
     module: cxx/templates, status: ported, vars: {param_1: vector},
     notes: "ALIAS - byte-identical to vector_capacity_elem12 @ 0x083d7708 (all 20 bytes: ldr r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0; mov r1,#0xc; b 0x08031568 __rt_sdiv) - a divide-by-12 capacity() copy, so the ledger's old blanket shift-based guess is wrong for this address too; per-address verification stays the rule. 4 bl sites (0x083e2f88, 0x083e30b8, 0x083e3114, 0x083e319c). No separate port exists; hook this address to the single vector_capacity_elem12 symbol (the handle_deref_or_null alias precedent). match.py against vector_capacity_elem12 at this address - same structure (sub / mov r1,#12 / b __rt_sdiv); the ldr pairs come out as byte loads from the family's read_unaligned host-testing idiom, the same accepted deviation as vector_size_elem4."}

  - {address: 0x083d773c, ghidra: FUN_083d773c, name: vector_capacity_elem12_alias_773c,
     size: 20, sig: "i32 vector_capacity_elem12_alias_773c(const VectorStorage *vector)",
     module: cxx/templates, status: ported, vars: {param_1: vector},
     notes: "ALIAS - byte-identical to vector_capacity_elem12 @ 0x083d7708 (all 20 bytes: ldr r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0; mov r1,#0xc; b 0x08031568 __rt_sdiv) - a divide-by-12 capacity() copy verified against osos.asm per address; per-address verification stays the rule. It sits immediately after the vector_size_elem12 copy at 0x083d772c and immediately before the vector_size_elem24 primary at 0x083d7750, exactly like the primary sits beside its own divide siblings. 2 bl sites (0x083e1840, 0x083e1960). Source C: decomp/c/037/083d773c_FUN_083d773c.c - Ghidra inlines the ADS signed divide through the tail branch; the real body is the 5-instruction capacity/12 head. No separate port exists; hook this address to the single vector_capacity_elem12 symbol (the vector_capacity_elem12 alias-at-0x083d7828 precedent). match.py against vector_capacity_elem12 at this address - same structure (sub / mov r1,#12 / b __rt_sdiv); the ldr pairs come out as byte loads from the family's read_unaligned host-testing idiom, the same accepted deviation as vector_size_elem4."}

  - {address: 0x083d7784, ghidra: FUN_083d7784, name: vector_capacity_elem12_alias_7784,
     size: 20, sig: "i32 vector_capacity_elem12_alias_7784(const VectorStorage *vector)",
     module: cxx/templates, status: ported, vars: {param_1: vector},
     notes: "ALIAS - byte-identical to vector_capacity_elem12 @ 0x083d7708 (all 20 bytes: ldr r1,[r0,#8]; ldr r0,[r0]; sub r0,r1,r0; mov r1,#0xc; b 0x08031568 __rt_sdiv) - a divide-by-12 capacity() copy verified against osos.asm per address; per-address verification stays the rule. It sits immediately after the vector_size_elem12 copy at 0x083d7774 and immediately before the vector_size_elem16 shift member at 0x083d7798, exactly like the primary sits beside its own divide siblings. 2 bl sites (0x083e20d0, 0x083e21e8). Source C: decomp/c/037/083d7784_FUN_083d7784.c - Ghidra inlines the ADS signed divide through the tail branch; the real body is the 5-instruction capacity/12 head. No separate port exists; hook this address to the single vector_capacity_elem12 symbol (the vector_capacity_elem12 alias-at-0x083d773c precedent). match.py against vector_capacity_elem12 at this address - same structure (sub / mov r1,#12 / b __rt_sdiv); the ldr pairs come out as byte loads from the family's read_unaligned host-testing idiom, the same accepted deviation as vector_size_elem4."}

  - address: 0x083d7798
    ghidra: FUN_083d7798
    name: vector_size_elem16
    size: 16
    sig: "i32 vector_size_elem16(const VectorBounds *vector)"
    module: cxx/templates
    status: ported
    vars: {param_1: vector}
    notes: >-
      Byte-identical copy of vector_size_elem16 @ 0x083d7884 (same
      `ldmia r0,{r0,r1}; sub r0,r1,r0; mov r0,r0, asr #0x4; bx lr` -
      verified against osos.asm per address; it sits immediately after
      the vector_capacity_elem12 alias at 0x083d7784 and immediately
      before the vector_capacity_elem16 primary at 0x083d77a8, exactly
      like the other elem16 copies sit beside their own capacity
      siblings). Served by the existing cxx/templates port - hook this
      address to the vector_size_elem16 symbol (the vector_size_elem16
      copy-at-0x083d78b4 precedent). 14 bl call sites, no tail
      branches: 0x08197bb8, 0x08197c00, 0x083e227c, 0x083e22e8,
      0x083e236c, 0x083e25bc, 0x083e25d8, 0x083e25e4, 0x083e2668,
      0x083e2684, 0x083e2690, 0x083e26bc, 0x083e26d8, 0x083e26f4.
      Source C: decomp/c/037/083d7798_FUN_083d7798.c - Ghidra's
      `return param_1[1] - *param_1 >> 4`. match.py - the ldm comes
      out as byte loads from the family's read_unaligned host-testing
      idiom, the same accepted deviation as vector_size_elem4; the
      sub/asr#4 structure matches.

  - address: 0x083d5ea0
    ghidra: FUN_083d5ea0
    name: handle_deref_field12
    size: 20
    sig: "u8 *handle_deref_field12(u8 *const *slot)"
    module: cxx/handle
    status: ported
    notes: >-
      `cell = *slot; return cell ? cell[3] : NULL` - handle_deref_or_null
      with the second load at +12 instead of +0. 11 bl call sites; the
      only copy of this offset in the image. The +12 field is
      unidentified; the port addresses it by word index (3) so it is
      byte-exact on target and disjoint on a 64-bit host. match.py -
      LLVM splits the predicated ldrne into a branch, as with the
      primary port.

  - address: 0x083d5eb4
    ghidra: FUN_083d5eb4
    name: three_pointer_select
    size: 36
    sig: "u32 three_pointer_select(const ThreePointers *triple)"
    module: util
    status: ported
    notes: >-
      Reads three pointers from the object (+0, +4, +8) and returns one
      of two dereferences - `if (a < b) return *b; c = [this+8];
      return a <= c ? *a : *c`, all unsigned compares (cc/ls/hi). 14 bl
      sites here (all in the switch body @ 0x080c7b40..0x080c7bdc) plus
      8 at the byte-identical copy 0x083d5ed8, which hooks the same
      symbol. Shape of an iterator clamp or a three-way begin/cur/end
      accessor, but the owning container is not identified, so the
      descriptive name is kept and the object is modeled as a repr(C)
      ThreePointers of three `*const u32`. match.py - same structure
      (ldm r0,{r1,r2} / cmp / early out / ldr +8 / cmp / select /
      deref); LLVM splits the predicated ldrcc/bxcc into a branch and
      folds the ls/hi select through movhi, the usual predication
      deviation.

  - address: 0x083d5ed8
    ghidra: FUN_083d5ed8
    name: three_pointer_select
    size: 36
    sig: "u32 three_pointer_select(const ThreePointers *triple)"
    module: util
    status: ported
    vars: {param_1: triple}
    notes: >-
      Byte-identical second copy of three_pointer_select @ 0x083d5eb4
      (36 bytes verified equal against osos.dec:
      060090e8020051e1000092351eff2f31...). Served by the existing
      util/three_pointer_select port - hook both addresses to the one
      symbol. 8 bl call sites.

  - address: 0x0839bae0
    ghidra: FUN_0839bae0
    name: plane_cursor_init
    size: 120
    sig: "PlaneCursor *plane_cursor_init(PlaneCursor *out, const PlaneDescriptor *plane, u32 mode, u32 index)"
    module: util/plane_cursor
    status: ported
    vars: {param_1: out, param_2: plane, param_3: mode, param_4: index}
    notes: >-
      Initializes a 16-byte sampling cursor over a u32-element plane
      held behind a C++ handle: begin (+4) = element 0, end (+8) =
      element count-1 (count at plane+4; wraps to base-4 for count 0),
      cur (+0) = element index phase-adjusted by mode (0 = even phase
      2*index, 1 = odd phase 2*index+1, anything else = direct index)
      and step (+12) = 2 for the phases, 1 for direct; returns out.
      Store order begin/end/step/cur is the original's. The cursor's
      first three words are the ThreePointers layout consumed by the
      ported clamp-fetch three_pointer_select @ 0x083d5eb4
      (cur/begin/end = first/second/third). 14 bl call sites, all
      unconditional, all in the fixed-point YCbCr converter
      FUN_080c79e8 (an unrolled run @ 0x080c7a2c..0x080c7b30, every
      one mode=2) - verified by decoding every B/BL word in osos.dec;
      no b sites, no predicated forms, no data-word references. A
      byte-similar sibling @ 0x0839bb58 (identical except its 3 bls
      target the other accessor copy 0x083d60f0) starts immediately
      after, confirming the 120-byte extent; it has 8 bl sites of its
      own in the sibling converter @ 0x080c7eb8, also all mode=2, so
      the phase modes are dead in osos but ported faithfully. Ghidra's
      C was accurate here. The three accessor calls go to the ported
      handle_elem_ptr @ 0x083d60dc (both stock copies hook that
      symbol). match.py - same structure (three call/store pairs,
      three-way mode split, return-this); LLVM adds its usual fp
      frame, hoists the slot address into a register, reorders the
      mode compares, and emits orr for 2*index+1.

  - address: 0x0839bb58
    ghidra: FUN_0839bb58
    name: plane_cursor_init_alias_bb58
    size: 120
    sig: "PlaneCursor *plane_cursor_init(PlaneCursor *out, const PlaneDescriptor *plane, u32 mode, u32 index)"
    module: util/plane_cursor
    status: ported
    renames:
      param_1: out
      param_2: plane
      param_3: mode
      param_4: index
    notes: >-
      Byte-similar sibling of plane_cursor_init @ 0x0839bae0: the 30
      ARM instructions and 120-byte extent are identical except all
      three handle-element accessor calls target the byte-identical
      accessor copy @ 0x083d60f0 instead of @ 0x083d60dc. This address
      is therefore served by the existing plane_cursor_init port, whose
      handle_elem_ptr call is the intentional common hook target for
      both accessor copies. Binary-scanning every ARM B/BL word in
      osos.dec found exactly 8 unconditional bl call sites
      (0x080c7f30, 0x080c7f44, 0x080c7f58, 0x080c7f6c, 0x080c7f80,
      0x080c7f94, 0x080c7fa8, 0x080c7fbc); no predicated branch calls
      occur. The sibling fixed-point YCbCr converter supplies mode=2
      at every site, while the port faithfully retains the stock mode
      0/1 phase paths and their wrapping u32 arithmetic. Host tests in
      util/plane_cursor.rs cover direct, both phase, noncanonical mode,
      zero-count, wraparound, NULL-cell, and consumer-clamp behavior.

  - address: 0x0839bbd0
    ghidra: FUN_0839bbd0
    name: signed_key_tree_find_value_copy
    size: 84
    sig: "u32 signed_key_tree_find_value_copy(const SignedKeyTree *tree, i32 key, u32 *out_value)"
    module: cxx/signed_key_tree_find
    status: ported
    renames:
      param_1: tree
      param_2: key
      param_3: out_value
      local_20: candidate
      local_1c: header
    notes: >-
      Raw osos.dec runs from 0x0839bbd0 through the return at 0x0839bc20;
      the separately linked sibling begins at 0x0839bc24. It
      stack-materializes the signed key, calls lower_bound @ 0x083db84c,
      compares the returned node word with tree+0x10 through the ported
      equal_deref copy @ 0x083cf8a8, and only for a non-header node stores
      node+0x14 through out_value before returning 1. A header result leaves
      out_value untouched and returns 0. The lower_bound body calls
      less_signed @ 0x083d7580, establishing signed key ordering, but the
      mapped value type remains opaque. Decoding every ARM B/BL word finds
      exactly 8 direct, unconditional bl callers (0x081be4bc, 0x081be94c,
      0x081bec9c, 0x081bf7e8, 0x081bf9c8, 0x081bfa6c, 0x081bfac0,
      0x081bfba8), no predicated calls, no direct b transfers, and no
      aligned data-word references. The unported lower_bound remains a
      volatile host seam and a direct call to its retailOS address on target;
      equal_deref is inlined as the equivalent candidate != header test.

  - address: 0x0839bccc
    ghidra: FUN_0839bccc
    name: signed_key_tree_find_value
    size: 84
    sig: "u32 signed_key_tree_find_value(const SignedKeyTree *tree, i32 key, u32 *out_value)"
    module: cxx/signed_key_tree_find
    status: ported
    renames:
      param_1: tree
      param_2: key
      param_3: out_value
      local_20: candidate
      local_1c: header
    notes: >-
      Raw osos.dec runs from 0x0839bccc through the return at 0x0839bd1c;
      the separately linked next function begins at 0x0839bd20. It
      stack-materializes the signed key, calls lower_bound @ 0x083dbab4,
      compares the returned node word with tree+0x10 through the ported
      equal_deref copy @ 0x083cf950, and only for a non-header node stores
      node+0x14 through out_value before returning 1. A header result leaves
      out_value untouched and returns 0. The lower_bound body's call to
      less_signed @ 0x083d7580 establishes signed key ordering, but its
      mapped value type remains opaque. Decoding every ARM B/BL word finds
      exactly 8 direct, unconditional bl callers (0x081bca78, 0x081bd1fc,
      0x081bd67c, 0x081bdcf4, 0x081bdf74, 0x081be008, 0x081be0cc,
      0x081be144), no predicated calls, no direct b transfers, and no
      aligned data-word references. The unported lower_bound remains a
      volatile host seam and a direct call to its retailOS address on target;
      equal_deref is inlined as the equivalent candidate != header test.

  # --- scouted 0x08110000-0x0812ffff, not ported (display-layer sweep) ---

  - address: 0x0812c6b0
    ghidra: FUN_0812c6b0
    name: timer_stop
    size: 76
    sig: "void timer_stop(u8 *timer)"
    module: drivers/timer
    status: ported
    notes: >-
      144 call sites (129 bl + 15 tail b) - the busiest unclaimed
      function in 0x08110000-0x0812ffff. Runs a trace/assert helper
      (0x08076954, itself a locked call to 0x0809e620), takes the global
      mutex object @ 0x089cb294, and if the object's state word at +0x20
      equals the FourCC 'expi' (0x65787069, i.e. "expired") cancels the
      pending callback with 0x080a6c0c(obj[+0x28], 0x081216b4, obj);
      then writes the FourCC 'stop' (0x73746f70) to +0x20 and tail
      branches to the unlock. Sibling 0x0812bf4c (32 bytes, 119 sites)
      calls it and then writes 'run ' (0x72756e20) before tail branching
      to 0x0807a228 - i.e. restart. The FourCC state words are what
      identify the class as a timer/alarm. Not ported - needs the global
      mutex object, the cancel helper 0x080a6c0c and the restart
      0x0807a228 first; three dispatch slots for 76 bytes of body, but
      the call count makes it the best next target in this range.

  - address: 0x0812bf84
    ghidra: FUN_0812bf84
    name: timer_stop_then_trace
    size: 24
    sig: "void timer_stop_then_trace(u8 *timer)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
    notes: >-
      26 direct call sites (26 unconditional bl, 0 predicated, 0 tail b),
      verified by decoding every ARM B/BL word in osos.dec. Raw 24-byte
      body saves timer in r4, calls timer_stop @ 0x0812c6b0, restores it to
      r0, and tail-branches to 0x0808747c. That address is a one-instruction
      b veneer to trace/assert helper @ 0x08076954, not a separately
      identified callee. Ghidra incorrectly assigns the helper's
      lock/trace/unlock body to this wrapper. The port reuses the existing
      TimerOps::trace_assert seam rather than adding one. match.py reports
      the expected structural diff: LLVM inlines timer_stop and lowers the
      existing trace dispatch to an indirect call, but preserves the
      stop-before-second-trace order.

  - address: 0x0812bf4c
    ghidra: FUN_0812bf4c
    name: timer_restart
    size: 32
    sig: "void timer_restart(u8 *timer)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
    notes: >-
      119 call sites (72 bl + 47 tail b). timer_stop(timer) @ 0x0812c6b0,
      then stores the FourCC 'run ' (0x72756e20) in the state word +0x20
      (outside the class mutex - the str sits between the stop's return
      and the tail branch), then tail branches to the arm helper
      0x0807a228 with the timer in r0, which re-queues it onto the
      pending list sorted by deadline. The arm helper is now ported as
      timer_arm (its own entry); the TimerOps::arm_timer slot defaults
      to it. Sig corrected from the scouted "u8 *timer_restart(u8
      *timer)" to void: the tail call's r0 is the arm helper's leftover
      (the armed flag on the early-return path, the mutex pointer
      otherwise), not the timer. LLVM inlines timer_stop into the port,
      so match.py shows the full stop sequence plus the 'run ' store and
      the indirect tail call - structurally the original.

  - address: 0x0812c63c
    ghidra: FUN_0812c63c
    name: timer_start_after
    size: 32
    sig: "void timer_start_after(u8 *timer, u32 delay)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
      param_2: delay
    notes: >-
      100 call sites (98 bl + 2 tail b). timer_stop(timer) @ 0x0812c6b0,
      then tail branches to timer_set_delay @ 0x080744c0(timer, delay) -
      the delay survives the stop in r5, which is what marks it as the
      delay/deadline. Unlike timer_restart it never writes 'run ' and
      never arms; it only stops and reprograms the period word at +0x4.
      Sig corrected from the scouted "u8 *timer_start_after(u8 *, u32)"
      to void: the tail call's r0 is timer_set_delay's leftover, not
      timer (the timer_restart precedent). The callee was ported
      first in its own commit. match.py structural - LLVM inlines both
      the stop and the set_delay, so the port shows the full stop
      sequence plus the trace and the str to +4.

  - address: 0x0812c3b0
    ghidra: FUN_0812c3b0
    name: timer_is_running
    size: 60
    sig: "u32 timer_is_running(u8 *timer)"
    module: drivers/timer
    status: ported
    renames:
      param_1: timer
    notes: >-
      6 call sites, all unconditional bl (0x081a6378, 0x081cc550,
      0x081f78cc, 0x8217354, 0x821e5c8, 0x8286778) - 0 predicated, 0
      tail b - verified by decoding every ARM B/BL word in osos.dec.
      Ghidra emits NO C file for this function; the signature is
      recovered from the raw words. Extent: 52 bytes of code plus an
      8-byte literal pool (0x089cb294 and the 'run ' FourCC
      0x72756e20 @ 0x0812c3e4/0x0812c3e8) = 60 bytes true extent,
      0x0812c3b0..0x0812c3ec, where the next function opens `push
      {r4, r5, r6, lr}`. Algorithm: lock the timer-class mutex @
      0x089cb294 (ported mutex_lock @ 0x0807f5c4), read the state
      word at timer+0x20, return 1 iff it equals TIMER_STATE_RUNNING
      ('run ') - 'expi' and 'stop' both answer 0 - then unlock (the
      ported mutex_unlock @ 0x0807f6a0; not a tail branch: the result
      rides in r4 across it) and return. No NULL guard, as stock.
      Ported as the predicate half of class_8c00_rearm_timer_post_0x11
      @ 0x081a6368's first branch. Host tests prove 'run ' answers 1
      with the read bracketed by the class-mutex wait/signal pair and
      nothing else, and that 'stop'/'expi' answer 0 leaving the state
      word untouched. match.py structural: same lock / ldr +0x20 /
      FourCC compare / unlock / 0-or-1 shape; the diff is the crate
      fp prologue, the volatile mutex copy to the stack (the
      timer_stop house pattern), and LLVM's add/clz/lsr equality
      idiom standing in for movne/moveq.

  - address: 0x0811108c
    ghidra: FUN_0811108c
    name: timer_schedule_shim
    size: 20
    sig: "void timer_schedule_shim(u32 config_word, u8 *timer, u32 init_arg, usize callback_handle)"
    module: drivers/timer
    status: ported
    renames:
      param_1: config_word
      param_2: timer
      param_3: init_arg
      param_4: callback_handle
    notes: >-
      64 bl call sites, and nothing but an argument rotation - r0 -> r2,
      r1 -> r0, r2 -> r1 - before `b 0x0812c65c`, the timer constructor
      (runs the object-init helper 0x08076924, writes an initial state
      word to +0x20, stores the first argument at +0x24, and records the
      callback-queue handle at +0x28, substituting the current task's
      queue when the handle is 0). Sig corrected from the scouted
      "u32 timer_schedule_shim(u32 a, u8 *timer, u32 b)": r3 rides
      through untouched and every call site sets it (to 0) before the
      bl, so it is a real fourth argument (the constructor's
      callback-handle parameter); and the tail call's r0 is the
      constructor's leftover, so the return is void (the timer_restart
      precedent). The constructor is not yet ported, so the port
      dispatches through a new TimerOps::construct_timer slot. Exact
      semantics of config_word (+0x24) and init_arg (forwarded to
      0x08076924) are not identified; the names follow the data flow.
      match.py structural: the rotation is verbatim (mov lr, r2;
      mov r2, r0; mov r0, r1; mov r1, lr; tail bx), the constructor
      address becomes a table load. Gotcha recorded in the doc header:
      the whole-table volatile read made LLVM's ARM sibcall lowering
      drop the rotation and branch to the timer pointer - the port
      reads the fn-pointer field directly to sidestep it.

  - address: 0x0810ebbc
    ghidra: FUN_0810ebbc
    name: pair_header_base_construct
    size: 56
    sig: "u32 *pair_header_base_construct(u32 *base)"
    module: cxx/pair_header
    status: ported
    vars: {param_1: base, iVar1: grand_base}
    notes: >-
      Structural name: the concrete 0xb8-byte base embedded at +12 in
      pair_header_construct's otherwise unidentified 200-byte service
      object. Raw ARM stores literal vtable 0x08981630 at +0, calls the
      grand-base ctor 0x0813eee0 at +4, clears +0xac/+0xb0 and the low
      byte at +0xb4, zeroes +4..+0x97 (0x94 bytes through
      0x08037db8), and returns the grand-base result minus four. There
      are no null checks or failure branches. The now-ported grand-base
      wrapper calls the now-ported 0x08185b98 body adaptor and its ported
      0x082ab398 reset dependency. Host tests prove vtable-before-chain
      ordering, post-chain clears, zero-fill, and the fixed grand-base
      reset ABI.
      match.py structural: vtable store, grand-base call, relative field
      stores, memclear, and
      result-minus-one-word return are retained.

  - address: 0x0810ec10
    ghidra: FUN_0810ec10
    name: pair_header_base_destruct
    size: 44
    sig: "u32 *pair_header_base_destruct(u32 *base)"
    module: cxx/pair_header
    status: ported
    vars: {param_1/r4: base, iVar1: body (the grand-base body at base+0x30)}
    notes: >-
      The NON-DELETING destructor of the same 0xb8-byte concrete base
      object pair_header_base_construct @ 0x0810ebbc builds. 56 bl call
      sites and no plain-b tail calls, both verified by decoding every
      branch word in osos.dec. Ghidra's 40-byte extent drops the trailing
      literal-pool word at 0x0810ec38 (0x08981630, the vtable, reached by
      "ldr r0,[pc,#24]" at 0x0810ec18); the next function starts at
      0x0810ec3c, so the true extent is 44 bytes - ten instructions plus
      that word. The vtable literal occurs in exactly seven data words in
      the image (0x0810e9bc, 0x0810ea14, 0x0810ea74, 0x0810eacc,
      0x0810ebb4, 0x0810ebf4, 0x0810ec38), every one of them a literal
      pool belonging to that single class's constructors or to this
      destructor, which is what ties this function to that class. The
      address 0x0810ec10 itself appears in NO data word, so it fills no
      vtable slot - all 56 call sites reach it directly, and the deleting
      form is the separate six-instruction thunk @ 0x0810ebf8
      ("cmp r0,#0; bl 0x0810ec10; b operator_delete").
      Algorithm: store the vtable at +0; call 0x0810e908(this), which
      releases the base's optionally owned 28-byte payload - if the
      ownership byte at +0xb4 is set and the pointer at +0x04 is non-NULL
      it hands that pointer either to the ported heap_free (0x0819d4dc)
      with the pool at +0xb0, or, when there is no pool, to the ported
      free_wrapper (0x080e7970); then call 0x08185bac(this + 0x30) and
      return its result minus 0x30. 0x08185bac is the mirror of the ported
      pair_header_grand_base_body_construct @ 0x08185b98 - it passes
      (this, 4, 0x14, 0x08283a80) to the array adapter 0x082ab3ec, which
      drops the call entirely when the element destructor is NULL and
      otherwise reaches the ported __cpp_finalise (0x080336d8) to destroy
      the four 20-byte elements. Load-bearing asymmetry: construction goes
      base -> grand base at +4 -> body at grandbase+0x2c, but destruction
      SKIPS the middle hop and calls the body teardown at base+0x30
      directly, because the grand base has nothing of its own to unwind.
      The port reproduces that collapsed chain rather than re-introducing
      a wrapper the original does not call. The two unported callees ride
      a PairHeaderBaseDestructOps table whose host defaults panic rather
      than no-op, since both free storage. app/pair_header_destruct's
      PAIR_HEADER_BASE_DESTRUCT seam now defaults to this port on target
      instead of transmuting the stock address. match.py structural: same
      shape throughout (vtable literal load, vtable store, release(this),
      destroy(this + 0x30), sub #0x30, return); the diff is the ops-table
      indirection emitting blx through loaded pointers instead of direct
      bl, the crate's fp frame, and LLVM folding the vtable store and the
      +0x30 pointer bump into one "str r1,[r4],#48".

  - address: 0x0810e6b4
    ghidra: FUN_0810e6b4
    name: pair_header_base_bind_payload
    size: 68
    sig: "u32 pair_header_base_bind_payload(u32 *base, const u32 *descriptor, u32 tag, u32 context)"
    module: cxx/pair_header
    status: ported
    vars: {param_1/r4: base, param_2/r5: descriptor, param_3/r6: tag, param_4/r7: context}
    notes: >-
      The NON-OWNING payload-assignment method of the same 0xb8-byte
      PairHeaderBase object pair_header_base_construct @ 0x0810ebbc
      builds and pair_header_base_destruct @ 0x0810ec10 tears down.
      Ghidra's 68-byte extent is right for once: the last word at
      0x0810e6f4 is "pop {r3, r4, r5, r6, r7, pc}" and the next
      function starts at 0x0810e6f8. 40 bl call sites, zero predicated
      or tail branches, and zero data-word occurrences, all verified by
      decoding every branch word and comparing every word in osos.dec,
      so it is reached only by direct non-virtual calls. Algorithm:
      release the currently owned payload through 0x0810e908(base) - the
      same release the destructor chains - then copy the 16-byte
      descriptor at descriptor+8 into base+0x98..+0xa4 with one
      ldm/stm pair (all four words loaded before any store, which the
      port reproduces with four reads then four writes), store tag at
      base+0xa8, and tail-delegate to the image-payload loader
      0x08094168(base+4, descriptor+8, descriptor, base, context) with
      the fifth argument on the stack. Unlike its owning sibling @
      0x0810e6f8 - which allocates a private copy through the pool at
      +0xb0 and sets the +0xb4 ownership byte - this variant never
      touches +0xb4: the payload it installs stays borrowed. The loader
      0x08094168 (172 bytes; Ghidra's 164 drops the two literal words
      0x00001888 and 0x00000555 at 0x0809420c/10) reads the u16 format
      override at descriptor+0, else maps the s16 depth at descriptor+6
      (1/2/4/8 stay, 16 becomes 0x555, 32 and everything else becomes
      0x1888 - the same pixel-format tags app/image_format uses), picks
      descriptor+28 as pixel data exactly when the flag word at
      descriptor+24 is non-zero, and tail-calls the format decoder @
      0x08094214, which maps the tag to bits per pixel (32/8/16/0).
      Ghidra's void return is a C-level fiction: the original's pop
      leaves the loader's r0 untouched, so the port forwards it even
      though the decompiled call sites ignore it. Both callees are
      unported and ride a PairHeaderBaseBindPayloadOps table whose host
      defaults panic rather than no-op, since a skipped release leaks
      and a skipped load fakes a rebind; on target the release shares
      the destruct table's transmuted 0x0810e908 default and the loader
      transmutes 0x08094168. Host tests prove release-before-rebind
      ordering (the old descriptor word is read back through the
      release), the exact +0x98..+0xa8 store window with nothing else
      written, the five-word loader ABI including the stack context,
      unchanged forwarding of the loader's result (0/8/16/32 and
      0xffffffff), and ldm/stm load-before-store semantics on an
      overlapping descriptor. match.py structural: release-call,
      four-word copy into +0x98, tag store at +0xa8, stack context
      word, loader call with (base+4, descriptor+8, descriptor, base),
      and result forwarding are all retained; the diff is the
      ops-table blx indirection, the crate's fp frame, and LLVM
      splitting the original's single five-register stm into a
      four-word stm plus a separate tag str.

  - address: 0x0813eee0
    ghidra: FUN_0813eee0
    name: pair_header_grand_base_construct
    size: 20
    sig: "u32 *pair_header_grand_base_construct(u32 *base)"
    module: cxx/pair_header
    status: ported
    vars: {param_1: base, iVar1: grand_base_body}
    notes: >-
      Five-instruction wrapper around the now-ported constructor
      0x08185b98. It calls that constructor on the +0x2c embedded subobject
      and returns the callee's pointer minus 0x2c; no null or error path
      exists. Reference C is decomp/c/012/0813eee0_FUN_0813eee0.c. Raw
      retail disassembly is `push {r4,lr}; add r0,#0x2c; bl 0x08185b98;
      sub r0,#0x2c; pop {r4,pc}`. Focused tests prove it receives base+0x2c,
      invokes the ported reset through the body adaptor, and rebases that
      pointer by -0x2c. match.py structural confirms the call and matching
      add/sub pointer dataflow.

  - address: 0x08185b98
    ghidra: FUN_08185b98
    name: pair_header_grand_base_body_construct
    size: 16
    sig: "u32 *pair_header_grand_base_body_construct(u32 *this)"
    module: cxx/pair_header
    status: ported
    vars: {param_1: this, DAT_08185ba8: type_descriptor}
    notes: >-
      Grand-base body constructor adaptor. Raw retail ARM loads literal
      0x08283a74, sets r3=4 and r2=0x14, then tail-branches to the ABI
      adapter at 0x082ab234. That adapter calls the ported 0x082ab398 with
      (this, 4, 0x14, 0x08283a74, 0); its non-allocating helper path leaves
      this in r0. Reference C is decomp/c/015/08185b98_FUN_08185b98.c; its
      apparent direct 0x082ab398 call is reached through this
      compiler-generated five-argument adapter. Focused tests prove exact
      pointer, constants, one-call sequencing, and the enclosing return
      dataflow. match.py structural: literal load and fixed-argument
      tail-call shape are retained.

  - address: 0x082ab398
    ghidra: FUN_082ab398
    name: pair_header_grand_base_reset
    size: 84
    sig: "u32 *pair_header_grand_base_reset(u32 *this, u32 field_count, u32 field_size, u32 element_initializer, u32 initializer_context)"
    module: cxx/pair_header
    status: ported
    vars: {param_1: this, param_2: field_count, param_3: field_size, param_4: element_initializer, param_5: initializer_context}
    notes: >-
      Eleven-word array-helper adapter for the pair-header grand base.
      Reference C is decomp/c/029/082ab398_FUN_082ab398.c. Raw ARM saves
      incoming r1/r2/r3 and incoming stack word 5, then calls 0x082b498c
      as (this, field_count, field_size, 0, 0, element_initializer,
      initializer_context, 0, 0, 0, 0). This proves the original 11-word
      ARM placement: first four words in r0-r3 and the remaining seven at
      successive stack slots. The retail ARM leaves 0x082b498c's pointer
      result in r0 untouched through the pop {r4,r5,r6,pc} - Ghidra's void
      return is a C-level fiction - and the port now forwards it, because
      the only caller family is the four-argument adapter 0x082ab234
      (cpp_array_construct, runtime/cpp_array_construct) whose own callers
      chain on the returned array base. That unported helper is isolated
      in the accurately typed PAIR_HEADER_ELEMENT_ARRAY_OPS::reset seam.
      Focused host tests record every forwarded word, all six literal
      defaults, one-call order, r0 forwarding of a sentinel helper result,
      and 0x08185b98's fixed direct dependency. match.py
      structural: save/stack-argument materialization and one helper call
      match the retail wrapper.

  - address: 0x082ab234
    ghidra: FUN_082ab234
    name: cpp_array_construct
    size: 32
    sig: "u32 *cpp_array_construct(u32 *array, u32 element_ctor, u32 element_size, u32 element_count)"
    module: runtime/cpp_array_construct
    status: ported
    renames:
      param_1: array (base of the object array being constructed)
      param_2: element_ctor (void (*)(void *) constructor, passed as a raw word)
      param_3: element_size
      param_4: element_count
      FUN_082ab398: pair_header_grand_base_reset (ported callee, cxx/pair_header)
    notes: >-
      ARM ADS four-argument array-constructor adapter: 41 bl call sites,
      zero predicated forms, verified by decoding every B/BL word in
      osos.dec; callers never gate the call and the adapter has no NULL
      guard or validation of its own. Ghidra's 32-byte extent is exact:
      eight instructions, and the next function (cpp_finalise_null_guard @
      0x082ab254, ported in heap/veneers) starts immediately after. Raw
      ARM: mov ip,r1; mov r1,r3; push {r3,lr}; mov r3,#0; str r3,[sp];
      mov r3,ip; bl 0x082ab398; pop {ip,pc} - it rotates the ADS (array,
      ctor, size, count) convention into the five-word helper ABI (array,
      count, size, ctor, 0) with the pushed zero as initializer_context,
      and forwards to the ported pair_header_grand_base_reset. Retail r0
      carries the helper's pointer result straight through both wrappers
      (neither clobbers it), and callers depend on it: the 0x081b80b4 site
      stores into ret+0x60 and feeds ret+100 to the next adapter call, the
      0x0816a6e4 site returns ret-4 as the finished object - Ghidra's void
      return type is wrong, so the port returns the pointer. The ctor word
      is treated as data until the helper calls it, matching the callee
      port's u32 typing. Deliberate supporting change: the callee port
      previously discarded the helper result and returned void; it now
      forwards r0 (names.yaml entry 0x082ab398 updated). Host tests prove
      the argument rotation and zero-filled helper words, r0 forwarding of
      a sentinel distinct from array, and verbatim forwarding of extreme
      words (count 0, size 0xffffffff). match.py structural: the rotation
      (mov ip,r1; mov r1,r3; mov r3,ip), the pushed zero stack word, and
      the single helper-writer call are retained; the diff is the crate's
      fp frame and LLVM's argument setup order. No link_section needed:
      the body is not byte-identical to any sibling adapter.

  - address: 0x08124a38
    ghidra: FUN_08124a38
    name: pair_header_construct
    size: 36
    sig: "u32 *pair_header_construct(u32 *this, u32 header_first, u32 header_second)"
    module: cxx/pair_header
    status: ported
    vars: {param_1: this, param_2: header_first, param_3: header_second}
    notes: >-
      90 bl call sites, the only copy. A 200-byte service-object
      constructor: `stmia r0!, {r1, r2}` writes the two argument words
      to +0/+4, skips the word at +8, runs the base-class constructor
      0x0810ebbc (vtable store + grand-base chain 0x0813eee0 + field
      clears through base+0xb4) on the subobject at +12, backs the
      result up by 12 to recover `this`, clears the word at +0xc4 and
      the byte at +8, and returns `this`. Callers allocate exactly 200
      bytes per object (FUN_082aadd4(200) at the 0x081b8c20 cluster).
      The class is unidentified, so the name is STRUCTURAL. The scouted
      notes here originally described the three-way pointer clamp - that
      was a mis-addressed note: the clamp lives at 0x083d5eb4/0x083d5ed8
      and is already ported as util/three_pointer_select. The former
      PairHeaderOps base-constructor seam is now cut over to the direct
      pair_header_base_construct, pair_header_grand_base_construct,
      pair_header_grand_base_body_construct, and
      pair_header_grand_base_reset ports; only the 0x082b498c helper remains
      behind PAIR_HEADER_ELEMENT_ARRAY_OPS.

  - address: 0x08124a5c
    ghidra: FUN_08124a5c
    name: pair_header_destruct
    size: 52
    sig: "u8 *pair_header_destruct(u8 *this)"
    module: app/pair_header_destruct
    status: ported
    vars:
      param_1/r4: this
      FUN_082646ac: trivial_destructor (direct, ported in
        cxx/trivial_destructor - the shared empty dtor body)
      FUN_082aad24: operator_delete (direct, ported in heap/veneers -
        the tag-2 delete)
      FUN_0810ec10: pair_header_base_destruct (PAIR_HEADER_BASE_DESTRUCT
        seam, kept so the host tests can observe the chain; now ported in
        cxx/pair_header, and the target default calls that port instead of
        the stock address) - the base subobject's own destructor chain
    notes: >-
      The NON-DELETING destructor of the 200-byte pair-header service
      object - the exact counterpart of pair_header_construct @
      0x08124a38 (ported, cxx/pair_header; same translation unit, 0x24
      bytes earlier). 52 bytes exactly, 0x08124a5c..0x08124a90,
      thirteen instructions, no literal pool; the timer-area thunk
      opens immediately after. 63 bl call sites in the disassembly
      listing, 0 b (three further words @ 0x081990e0/0xf4/0x08199108
      decode as bl here but sit outside every Ghidra function extent -
      likely data).
      Algorithm: if the word at this+0xc4 (the owned heap sub-object)
      is non-NULL, run trivial_destructor then operator_delete on it
      (the crate's documented destruct-then-free pair); clear the word
      UNCONDITIONALLY (Ghidra's "Subroutine does not return" marking
      on 0x082aad24 distorts this - the raw ARM falls through); then
      chain to the base subobject's teardown 0x0810ec10(this + 0xc)
      and return its result minus 0xc - the multiple-inheritance
      destructor-chain container_of adjustment. The class layout comes
      from the ported constructor: vtable + two header words at
      +0x00/+0x04, the 0x0810ebbc base subobject at +0x0c, the owned
      pointer at +0xc4 (cleared at construction - NULL is its born
      state), 0xc8 bytes total. Every call site is itself a destructor
      deleting an owner-class member (`ldr r0,[r4,#FIELD]; cmp; beq;
      bl 0x08124a5c; bl 0x082aad24` - e.g. the 0x0814219c class's
      +0xec/+0xf0/+0xf4 triple, the 0x0811dce8 class's +0xa4), which
      is what makes this the non-deleting variant: the caller supplies
      the operator delete.
      Deviations: the two ported callees are called directly; only
      0x0810ec10 rides a seam (PAIR_HEADER_BASE_DESTRUCT - the
      event_list.rs pattern, transmuted firmware default on target,
      panicking default on host), so the port is hook-ready on target.
      Host tests: owned word set - destruct+free fire once with the
      +0xc4 pointer (tag 2, observed through the veneers.rs mock
      heap), the word is cleared, the base teardown receives
      this+0xc, and its result returns minus 0xc; owned word clear -
      no free, word still stored, chain still runs; the -0xc
      adjustment is exact across three fixture results. match.py
      structural: same load-test/conditional-delete/clear/chain/
      subtract shape - LLVM folds the empty trivial_destructor into
      the operator_delete blne; the diff is the seam blx and the crate
      fp frame.

  - address: 0x083dc0e0
    ghidra: FUN_083dc0e0
    name: pair_assign_guarded
    size: 24
    sig: "u32 *pair_assign_guarded(u32 *dst, const u32 *src)"
    module: cxx/templates
    status: ported
    vars: {param_1: dst, param_2: src}
    notes: >-
      8-byte (two-word) copy from r1 to r0, skipped entirely when
      `src == dst` - the self-assignment guard of a two-word value type.
      Fully predicated in the original (`cmp` + two ldrne/strne pairs);
      the LLVM build reproduces that body instruction-for-instruction,
      differing only in the frame prologue/epilogue (push/pop vs
      `mov pc, lr`). 14 bl call sites; the only copy.

  - address: 0x083db4c4
    ghidra: FUN_083db4c4
    name: string_map_lookup_or_insert
    size: 76
    sig: "u8 *string_map_lookup_or_insert(StringKeyMap *map, char *const *key)"
    module: cxx/string_map
    status: ported
    vars: {param_1: map, param_2: key}
    notes: >-
      40 bl call sites, the only copy. map<string, u32>::operator[]
      shape: copy-constructs a temporary basic_string from the caller's
      string object into an 8-byte stack pair (real cxx_string_copy_ctor
      @ 0x083d8c30 call - the COW share), zeroes the word after it (the
      default-constructed one-word mapped value), runs the tree
      insert-unique @ 0x083c327c(&result, map, &pair), releases the
      temporary (cxx_string_release @ 0x083d8b04) and returns
      result.node + 0x14 - the mapped value word inside the node (0x10
      header + key string word at +0x10). The string object word
      (r1 on entry) is the whole key; the result's inserted-flag byte
      at +4 is dropped. 0x083c327c is now ported (string_key_tree_insert_unique,
      same module) and is the shipped default of the
      StringKeyMapOps::insert_unique slot the lookup dispatches through
      (PairHeaderOps/ByteKeyMapOps precedent); the pre-port fail-closed
      default (null node + flag 0, so the lookup returned 0x14 - null
      + 0x14, an obviously invalid value pointer, the pre-port
      byte_key_map_find behaviour) is retained test-only. cxx_string_copy_ctor/release are
      reached through #[inline(never)] front-ends
      (pair_string_copy_ctor/pair_string_release) to preserve the
      original's three bl boundaries (the pool_operator_new rationale);
      without them LLVM inlines the whole COW share/release logic into
      the wrapper. match.py structural: the three calls (ctor bl, tree
      op as a table-load blx through the slot, release bl), the value
      word zeroed after the ctor, the node word reloaded across the
      release call and the +0x14 add all correspond; LLVM additionally
      pre-zeroes the pair/result stack slots and uses a wider frame.

  - address: 0x083db69c
    ghidra: FUN_083db69c
    name: string_table_fallback_value_slot
    size: 104
    sig: "char **string_table_fallback_value_slot(u8 *map, char *const *key)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: map
      param_2/r1: key
      DAT_083db700: default_value_data
      FUN_083d8c30: cxx_string_copy_ctor
      FUN_083c4884: map_operation
      FUN_082a8580: string_table_pair_destroy
      FUN_083d8b04: cxx_string_release
    notes: >-
      Raw extent is 104 bytes, not Ghidra's 100: code covers
      0x083db69c..0x083db6fc and the COW default-string data pointer literal
      0x08b31810 at 0x083db700 belongs to this function; the next entry
      pushes at 0x083db704. Decoding every ARM B/BL word and every equal data
      word in osos.dec finds 8 unconditional bl call sites, no predicated
      calls, no b references, and no data-word references. It copy-constructs
      a stack `(key, default_value)` COW-string pair, invokes the real but
      otherwise unported entry 0x083c4884 with `(result, map, pair)`, releases
      pair.value then pair.key (the exact two-release body at 0x082a8580),
      releases the initial default value, and returns result.node + 0x14.
      0x083c4884 has no names.yaml entry, so this port assigns it no
      higher-level identity: STRING_TABLE_MAP_OPS carries only its verified
      result/map/pair ABI, targeting the retail address on device and a host
      recorder in tests. The 0x082a8580 body is a private two-release helper
      calling the established cxx_string_release port rather than a second
      dispatch seam. Host fixtures prove the key COW share is live during the
      map call and restored afterward, the default empty COW value is passed,
      and the raw node+0x14 return address feeds fallback-table hexadecimal
      assignment. match.py reports 32 Rust versus 25 original instructions:
      same two copy calls, map operation, pair destruction, final release,
      result reload and +0x14; the expected differences are LLVM's wider
      frame, private call front-ends, and volatile map-operation blx.

  - address: 0x083c327c
    ghidra: FUN_083c327c
    name: string_key_tree_insert_unique
    size: 396
    sig: "void string_key_tree_insert_unique(StringKeyInsertResult *result, StringKeyMap *map, const StringKeyPair *key_pair)"
    module: cxx/string_map
    status: ported
    vars: {param_1: result, param_2: map, param_3: key_pair, iVar1: went_left, pbVar2: node, pbVar3: parent, "local_2c[0]": position}
    notes: >-
      Ported. libstdc++ _Rb_tree::_M_insert_unique for the string-keyed
      map family - the twin of the ported byte-keyed insert-unique @
      0x083b867c (byte_key_tree_insert_unique in cxx/byte_key_map.rs),
      differing only in the comparator (the string less @ 0x083d74f4,
      the ported cxx_string_less, instead of an inlined byte compare).
      Size corrected from the scouting note's 392 to 396 (the body runs
      0x083c327c..0x083c3408, like the twin). Descend from the root
      (header at map+0x10; header+4 = root) comparing the pair's key
      string against node keys at node+0x10 (less-than -> left at +8,
      else right at +0xc), remembering the last node and direction.
      Uniqueness (multi-insert byte at map+0x18 clear): a last-left
      step tests position == header+8 (begin) for an immediate insert,
      else walks to the in-order predecessor (inline
      _Rb_tree_decrement at 0x083c3368-0x083c33c4: header recognised by
      red color 0 + parent->parent self-loop, else ascend while the
      node is its parent's left child); the candidate's key comparing
      less than the new key links a fresh node via _M_insert @
      0x083c3408 (flag 1), otherwise the existing node returns (flag
      0). Multi-insert set: always links. Result: node word at
      result+0, flag byte at result+4. The port is the shipped default
      of STRING_KEY_MAP_OPS (string_map_lookup_or_insert @ 0x083db4c4's
      slot; the pre-port fail-closed stub is retained test-only) and
      calls the ported _M_insert (string_key_tree_insert_node, same
      module) directly - no insert-node slot, unlike the byte-keyed
      family. The comparator is reached through the inline(never)
      pair_string_less front-end to keep the original's bl boundaries;
      the iterator-equality helper @ 0x083cf818 (a one-word compare) is
      likewise ported (string_key_tree_iter_eq, same module) and called
      for the begin test - the original's bl @ 0x083c3330; the
      byte-keyed twin's 0x083cf740 stays inlined. Deviations: the original's
      not-found insert shares the multimap tail (bne 0x083c32e4 into
      the bl @ 0x083c32f8) - the port expresses all three inserts
      through one link_fresh_node helper (LLVM keeps three call sites);
      _M_insert's fifth stack argument (the key pair) is an ordinary
      fifth parameter in the port; the original's per-descent-step and
      pre-compare dead stack-scratch zeroes are dropped (the twin's
      deviation). Host tests: empty-tree begin insert (root/leftmost/
      rightmost, COW share), existing key flagged 0 with no new share,
      new minimum (begin path) and new maximum (right descent)
      leftmost/rightmost follow-ups, the predecessor path both ways
      (insert between predecessor and position; duplicate of the
      predecessor's key found), the multi-insert byte skipping
      uniqueness, and a 40-key insert-then-find stress in three key
      orders validating flag, node identity, share counts, count and
      the full red-black/BST/extremes contract after every insert.
      match.py structural: 99 vs 106 instructions - same descent loop
      (comparator bl, #8/#0xc child select), multi-insert check, begin
      test, inline predecessor walk, reverse compare, the three result
      epilogues and the _M_insert bl sites with r2=0 and the key pair
      on the stack; the diff is LLVM's fp frame, register scheduling,
      the private comparator front-end's argument promotion (the
      ignored comparator-object argument dropped) and the third _M_insert
      call site (the original tail-shares two) - the expected
      ADS-vs-LLVM noise.

  - address: 0x083c311c
    ghidra: FUN_083c311c
    name: string_key_tree_allocate_node
    size: 184
    sig: "StringKeyTreeNode *string_key_tree_allocate_node(StringKeyMap *map)"
    module: cxx/string_map
    status: ported
    vars: {param_1: map, puVar1: node, piVar3: chunk, iVar4: arena, uVar2: prev_capacity, uVar5: grown, uVar6: capacity}
    notes: >-
      libstdc++ pool allocator for the string-keyed map family's
      0x18-byte nodes - the near-instruction-identical twin of the
      ported byte-keyed allocator @ 0x083b7f40
      (byte_key_tree_allocate_node in cxx/byte_key_map.rs), verified
      against osos.asm: same free-list/bump/growth sequence, differing
      only in the node stride (0x18 vs 0x20 - a mul by 0x18 and an
      add r1,r6,r6,lsl#1 + lsl#3 arena-end where the twin folds 0x20
      into lsl#5 shifts). Pop the free list at map+4 (next threaded
      through node+0xc, the right link), else bump-allocate in the
      map+8..map+0xc range, else grow - capacity max(prev+0x20,
      prev+prev/2+prev/8), 0x20 first - carving a 0xc-byte chunk
      header (prev/capacity/arena, pushed at map+0) and a
      capacity*0x18 arena via two calls to the ported
      operator_new_checked @ 0x08266c70 (its dead r1=0 argument
      dropped; reached through an inline(never) pool_operator_new
      front-end to keep the two bl boundaries). Returned node: color
      byte 0 (red), parent/left/right nulled, pair uninitialised.
      Ghidra types the original void, but it leaves the node in r0 and
      _M_insert @ 0x083c3408 consumes it (mov r5,r0 @ 0x083c3428) -
      the port returns it, like the byte-keyed twin. Called from
      _M_insert (bl @ 0x083c3424) and one sibling site @ 0x081a0b28.
      Pool/chunk state modeled as StringKeyNodePool /
      StringKeyPoolChunk repr(C) structs (32-bit layout asserted);
      StringKeyTree._opaque is now sized off the pool struct (the
      ByteKeyTree precedent) so host fields stay disjoint; node stride
      and chunk size go through size_of (0x18/0xc on target). Exported
      pub with no ops slot - the string-keyed _M_insert @ 0x083c3408
      that will call it is not yet ported, so there is no in-crate
      caller (the rotations precedent; the future _M_insert port will
      introduce the slot, like BYTE_KEY_ALLOC_OPS). No null check on
      the checked-new results, like the original (its new-handler path
      aborts @ 0x08266abc). Host tests: fresh-chunk carve, bump
      exhaustion growth (0x20 -> 0x40), the capacity formula on both
      branches (8 -> 0x28 floor, 0x100 -> 0x1a0), free-list pop
      (next via node+0xc, re-init) - the byte-keyed pool-test set.
      match.py structural: 46 vs 46 instructions - same ldr/str pool
      sequence, growth formula, two bl operator-new boundaries, chunk
      stm and node zeroing; the diff is LLVM's fp frame, register
      scheduling and branch layout (the expected ADS-vs-LLVM noise).

  - address: 0x083c00dc
    ghidra: FUN_083c00dc
    name: word_key_set_allocate_node
    size: 184
    sig: "WordKeySetNode *word_key_set_allocate_node(WordKeySet *set)"
    module: cxx/word_key_set
    status: ported
    vars: {param_1: set, puVar1: node, piVar3: chunk, iVar4: arena, uVar2: prev_capacity, uVar5: grown, uVar6: capacity}
    renames:
      FUN_08266c70: operator_new_checked (ported, heap/new_handler; reached
        through the inline(never) pool_operator_new front-end)
    notes: >-
      libstdc++ pool allocator for the word-keyed set family's 0x14-byte
      tree nodes - the third twin of the ported byte-keyed allocator @
      0x083b7f40 (byte_key_tree_allocate_node, 0x20-byte nodes) and the
      string-keyed one @ 0x083c311c (string_key_tree_allocate_node,
      0x18-byte nodes). Pop the free list at set+4 (next threaded
      through node+0xc, the right link), else bump-allocate in the
      set+8..set+0xc range, else grow - capacity max(prev+0x20,
      prev+prev/2+prev/8), 0x20 for the first chunk - carving a 0xc-byte
      chunk header (prev/capacity/arena, pushed at set+0) and a
      capacity*0x14 arena via two calls to the ported
      operator_new_checked @ 0x08266c70 (its dead r1=0 argument dropped;
      reached through an inline(never) pool_operator_new front-end to
      keep the two bl boundaries). Returned node: color byte 0 (red),
      parent/left/right nulled, the element word at +0x10 deliberately
      untouched. Only the stride differs from the twins, so the original
      multiplies by 0x14 (mov r0,#0x14; mul r0,r6,r0) and forms the
      arena end with add r1,r6,r6,lsl #2 then lsl #2, where the
      byte-keyed twin folds 0x20 into lsl #5.
      EXTENT: 184 bytes, 0x083c00dc..0x083c0194, confirmed from raw
      bytes - 46 instructions from push {r4,r5,r6,r7,r8,lr} to
      pop {r4,r5,r6,r7,r8,pc}, no literal pool (the function makes no
      pc-relative load) and the next function's ldr r2,[r1,#12] starts
      at 0x083c0194. Ghidra's 184 is right for once.
      CALL COUNT: 20 call sites, every one an unconditional bl - zero
      predicated forms and zero plain b tail calls - verified by
      decoding every B/BL word of work/firmware/osos.dec. Nineteen sit
      in the app layer (0x0813ea38, 0x08160580, 0x0817d9c0 and sixteen
      inside the 0x08258ab4-0x0825b800 singleton-constructor cluster,
      which carve this family's self-pointing header node out of the
      same pool) and one is the family's own _M_insert @ 0x083c09d8
      (bl @ 0x083c09f4). The absence of any predicated call is the
      behavioural fact: no caller guards the container pointer, because
      every call site holds a live embedded container.
      FAMILY: the container is shaped exactly like the byte- and
      string-keyed maps - node pool in the first 0x10 bytes, header node
      pointer at +0x10, live count at +0x14, multi-insert flag byte at
      +0x18, comparator at +0x19 - but its element is a single 32-bit
      word. _M_insert @ 0x083c09d8 copies it whole with
      "adds r1,r0,#16; ldrne r2,[r8]; strne r2,[r1]", the key accessor @
      0x083b6a2c is "add r0,r0,#16; bx lr" and the comparator @
      0x083d74c4 is an unsigned word less
      ("ldr r0,[r1]; ldr r1,[r2]; cmp r0,r1; movcs r0,#0; movcc r0,#1"),
      so this is a set of unsigned words - std::set<unsigned> or
      std::set<T *>; nothing in the family separates the two, and the
      port types the element u32. The whole-tree recycler @ 0x083c099c
      pushes nodes back through node+0xc onto set+4, the exact inverse
      of the pop. Not the event_list family: that tree's allocator is
      0x083b9f4c with 0x20-byte nodes.
      Pool/chunk/container state modeled as WordKeySetNodePool /
      WordKeySetPoolChunk / WordKeySet repr(C) structs (32-bit layout
      asserted; WordKeySet._opaque sized off the pool struct so host
      fields stay disjoint); node stride and chunk size go through
      size_of (0x14/0xc on target). Exported pub with no ops slot - this
      family's _M_insert @ 0x083c09d8 is unported, so there is no
      in-crate caller (the string_key_tree_allocate_node precedent).
      Deviations: Ghidra types the original void
      (decomp/c/036/083c00dc_FUN_083c00dc.c), but it leaves the node in
      r0 - only r4-r8/lr are stacked - and _M_insert consumes it
      (adds r1,r0,#16 / mov r5,r0 @ 0x083c09f8), so the port returns it,
      like both twins; the dead r1=0 second argument to 0x08266c70 is
      dropped; the original stacks r8 without ever using it (an
      eight-word alignment pad), not reproduced; no null check on the
      checked-new results, like the original (its new-handler path
      aborts @ 0x08266abc).
      Host tests (5): fresh-chunk carve (0xc header then a 0x20*0x14
      arena, chunk pushed with a null prev, bump range set, node red
      with null links), bump exhaustion across a whole 0x20-node arena
      growing a second chunk at 0x40 with no traffic in between, the
      capacity formula on both branches (8 -> 0x28 floor, 0x100 ->
      0x1a0), free-list pop recycling two nodes with no heap traffic,
      and the element word at +0x10 surviving a pop untouched.
      match.py --lib <worktree archive>: 46 vs 46 instructions - same
      free-list pop, bump test, growth formula, two bl operator-new
      boundaries, chunk stm and node zeroing; the diff is LLVM's fp
      frame, its inverted movhi/movls max and the usual register
      scheduling and branch layout.

  - address: 0x083c31d4
    ghidra: FUN_083c31d4
    name: string_key_tree_rotate_left
    size: 84
    sig: "void string_key_tree_rotate_left(StringKeyTree *tree, StringKeyTreeNode *node)"
    module: cxx/string_map
    status: ported
    vars: {param_1: tree, param_2: node, iVar3: right}
    notes: >-
      Ported, exported pub (the string-keyed _M_insert @ 0x083c3408
      that will call it is not yet ported, so there is no in-crate
      caller; the export keeps the symbol in the staticlib). Address
      correction: the 0x083c327c scouting note cited this rotation as
      0x083c31f4, but that address is mid-function (the ldr
      r0,[r0,#0x10] header reload); the real entry - and the only bl
      target (@ 0x083c35a8 inside _M_insert @ 0x083c3408) - is
      0x083c31d4. Instruction-identical to the byte-keyed rotate_left
      @ 0x083b8090 (byte_key_tree_rotate_left in cxx/byte_key_map.rs),
      verified word for word against osos.asm: libstdc++
      _Rb_tree_rotate_left - the right child takes node's place (root
      pointer at header+4 first, else the matching side of node's
      parent), node adopts its left subtree as its right child
      (re-parented when non-null) and becomes its left child. Node /
      container modeled as StringKeyTreeNode / StringKeyTree repr(C)
      structs (the byte-keyed precedent; 32-bit offsets asserted:
      parent +4, left +8, right +0xc, 8-byte string key pair at +0x10,
      header pointer at tree+0x10 with the first 0x10 bytes the
      not-yet-ported allocator @ 0x083c311c's pool state). Host tests:
      rotate at root (with adopted subtree re-parenting), rotate under
      a left-child parent (null adopted subtree), rotate under a
      right-child parent, and in-order sequence preservation on a
       5-node tree. match.py structural: 21 vs 22 instructions - same
       ldr/str rotation sequence, conditional stores and root/parent
       branch structure; the diff is LLVM's fp frame and register
       scheduling (the expected ADS-vs-LLVM noise).

  - address: 0x083c3228
    ghidra: FUN_083c3228
    name: string_key_tree_rotate_right
    size: 84
    sig: "void string_key_tree_rotate_right(StringKeyTree *tree, StringKeyTreeNode *node)"
    module: cxx/string_map
    status: ported
    vars: {param_1: tree, param_2: node, iVar3: left}
    notes: >-
      Ported, exported pub (the string-keyed _M_insert @ 0x083c3408
      that will call it is not yet ported, so there is no in-crate
      caller; the export keeps the symbol in the staticlib). bl
      targets @ 0x083c3534 and 0x083c3584 (bleq) inside _M_insert.
      Instruction-identical to the byte-keyed rotate_right @
      0x083b80e4 (byte_key_tree_rotate_right in cxx/byte_key_map.rs),
      verified word for word against osos.asm: libstdc++
      _Rb_tree_rotate_right - the mirror of the ported
      string_key_tree_rotate_left @ 0x083c31d4. The left child takes
      node's place (root pointer at header+4 first, else the matching
      side of node's parent - the original tests the parent's right
      link at +0xc first), node adopts its right subtree as its left
      child (re-parented when non-null) and becomes its right child.
      Reuses the StringKeyTreeNode / StringKeyTree repr(C) structs of
      the rotate_left port. Host tests: rotate at root (with adopted
      subtree re-parenting), rotate under a right-child parent (null
      adopted subtree), rotate under a left-child parent, and
      in-order sequence preservation on a 5-node tree. match.py
      structural: 21 vs 22 instructions - same ldr/str rotation
      sequence, conditional stores and root/parent branch structure;
      the diff is LLVM's fp frame, register scheduling and branch
      layout (the expected ADS-vs-LLVM noise).

  - address: 0x083c3408
    ghidra: FUN_083c3408
    name: string_key_tree_insert_node
    size: 476
    sig: "void string_key_tree_insert_node(u8 **result, StringKeyMap *map, u8 *insert_position, u8 *parent, const StringKeyPair *key)"
    module: cxx/string_map
    status: ported
    vars: {param_1: result, param_2: map, param_3: insert_position, param_4: parent, param_5: key, pbVar1: node, pbVar7: node, pbVar4: parent, pbVar5: uncle, uVar6: grandparent}
    notes: >-
      libstdc++ _Rb_tree::_M_insert for the string-keyed map family -
      the node-link + rebalance the (now ported)
      insert-unique @ 0x083c327c reaches by bl @ 0x083c32f8 /
      0x083c3350, passing the
      key pair as a fifth argument on the stack (the port takes an
      ordinary fifth parameter). Mirror of the ported byte-keyed twin
      @ 0x083b8844 (byte_key_tree_insert_node in cxx/byte_key_map.rs,
      480 bytes). Allocates a 0x18-byte node through the ported pool
      allocator @ 0x083c311c (string_key_tree_allocate_node), now
      dispatched through a new StringKeyAllocOps slot (the
      BYTE_KEY_ALLOC_OPS precedent - the allocator's port note
      anticipated the slot; shipped default is the port), copy-
      constructs the 8-byte pair into node+0x10 (the key string via
      cxx_string_copy_ctor @ 0x083d8c30 - the COW share, a real bl
      through the existing pair_string_copy_ctor front-end - plus the
      one mapped-value word) and bumps the node count at map+0x14.
      Link: header parent or nonzero insert_position forces the left
      link (header parent also takes root + rightmost; else leftmost
      follows), else cxx_string_less @ 0x083d74f4 decides left vs
      right (right link: rightmost follows) - a real bl through a new
      #[inline(never)] pair_string_less front-end with the stateless
      comparator object at map+0x19 passed and ignored, like the
      original (LLVM argument-promotes the front-end to the two used
      args; the bl boundary survives). Rebalance is the textbook
      _Rb_tree_rebalance_for_insert: red uncle recolors and ascends,
      black/absent uncle rotates (inner child rotates the parent down
      first) via the ported string_key_tree_rotate_left /
      string_key_tree_rotate_right, root recolored black, fresh node
      written at result+0. Deviations: null check on the allocator
      result for the test-only stub (writes null at result+0 and
      returns; the original cannot fail - operator new throws, abort
      @ 0x08266abc); the placement-new guard node+0x10 != null dropped
      (only a 0xfffffff0 node could fail it, like the twin); the
      original's two dead stack-scratch zeroes before the comparator
      call and its dead r2-r5 rotation arguments dropped. Exported
      pub (no in-crate slot needed - the future insert-unique port
      will introduce that one, like BYTE_KEY_TREE_OPS). StringKeyTree
      gained node_count (+0x14) / multi_insert (+0x18) / comparator
      (+0x19) fields with 32-bit layout asserts (the ByteKeyTree
      precedent). Host tests mirror the twin's set: alloc-stub null,
      first-insert black root with the pair copy verified (COW share
      bumps the refcount, value word copied), left/right link
      leftmost/rightmost follow-ups, insert_position forcing the left
      link, and a 40-insert rebalance stress in three key orders
      validating red-black + BST + count + extremes after every
      insert. match.py structural: 119 vs 123 instructions - same
      allocate/copy-ctor/value-word/count-bump/link/rebalance sequence
      and the same three bl boundaries; the diff is the ops-slot
      table-load blx for the allocator, the documented null check,
      LLVM's fp frame, register scheduling and branch layout (the
      expected ADS-vs-LLVM noise).

  - address: 0x083d71d8
    ghidra: FUN_083d71d8
    name: byte_key_tree_is_empty
    size: 16
    sig: "u32 byte_key_tree_is_empty(const ByteKeyTree *tree)"
    module: cxx/byte_key_map
    status: ported
    renames:
      param_1/r0: tree
      r0_after_ldr: node_count
      r0_after_rsbs: is_empty
    notes: >-
      Byte-keyed red-black-tree empty() leaf. Loads the live node count
      from tree+0x14 and returns 1 exactly when it is zero, else 0;
      `rsbs r0,r0,#1; movcc r0,#0` gives canonical boolean output for
      every unsigned nonzero count. 10 direct bl call sites, all
      unconditional, verified by decoding every ARM B/BL immediate in
      osos.dec; no NULL guard in stock or port. The byte-key map callers
      pass their tree at object+0x48. Host test covers 0, 1, 2, and
      0xffffffff counts. match.py structural: the same count load and
      canonical zero test; LLVM uses clz/lsr where ADS uses rsbs/movcc,
      with a frame prologue/epilogue around the leaf.

  - address: 0x083db038
    ghidra: FUN_083db038
    name: byte_key_map_find
    size: 88
    sig: "u8 *byte_key_map_find(ByteKeyMap *map, const u8 *key)"
    module: cxx/byte_key_map
    status: ported
    vars: {param_1: map, param_2: key}
    notes: >-
      26 bl call sites, the only copy. map<u8, V>::operator[] shape:
      builds a 16-byte pair on the stack (key byte from *key at +0,
      zeroed 12-byte mapped value at +4 - the original zeroes a separate
      12-byte temporary with stmia and block-copies it in), runs the
      red-black-tree insert-unique @ 0x083b867c(&result, map, &pair) and
      returns result.node + 0x14 (node header 0x10: flag/parent/left/
      right, key pair at +0x10, so +0x14 is the mapped value). The
      dependency is now ported (byte_key_tree_insert_unique, same
      module) and is the shipped default of the swappable
      BYTE_KEY_MAP_OPS slot it dispatches through; _M_insert @
      0x083b8844 is also ported now (byte_key_tree_insert_node, same
      module, the shipped BYTE_KEY_TREE_OPS default) with its node
      allocator also ported (byte_key_tree_allocate_node @ 0x083b7f40,
      the shipped BYTE_KEY_ALLOC_OPS default - an inserting find really
      links a pool-carved node). The
      pair's pad bytes are zeroed where the original leaves stack
      garbage (unobservable). match.py structural: pair zeroing, key
      stamp, hook load/blx, node+0x14 all present; frame style (fp) and
      the dispatch blx differ, as expected.

  - address: 0x083b867c
    ghidra: FUN_083b867c
    name: byte_key_tree_insert_unique
    size: 396
    sig: "void byte_key_tree_insert_unique(ByteKeyInsertResult *result, ByteKeyMap *map, const ByteKeyPair *key_pair)"
    module: cxx/byte_key_map
    status: ported
    vars: {param_1: result, param_2: map, param_3: key_pair, iVar1: went_left, pbVar2: node, pbVar4: parent, "local_2c[0]": position}
    notes: >-
      Ported. libstdc++ _Rb_tree::_M_insert_unique for a byte-keyed
      map: descend from the root (header at map+0x10; header+4 = root)
      comparing the pair's key byte against node keys at node+0x10
      (less-than -> left at +8, else right at +0xc), remembering the
      last node and direction. Uniqueness (multi-insert byte at
      map+0x18 clear): a last-left step tests position == header+8
      (begin) for an immediate insert, else walks to the in-order
      predecessor (inline _Rb_tree_decrement at 0x083b8768-0x083b87c4:
      header recognised by red color 0 + parent->parent self-loop, else
      ascend while the node is its parent's left child); the
      candidate's key comparing less than the new key links a fresh
      node via _M_insert @ 0x083b8844 (flag 1), otherwise the existing
      node returns (flag 0). Multi-insert set: always links. Result:
      node word at result+0, flag byte at result+4. The port is the
      shipped default of BYTE_KEY_MAP_OPS (byte_key_map_find @
      0x083db038's slot). The comparator @ 0x083d73bc (unsigned byte
      less-than), iterator-equality @ 0x083cf740 and key accessor @
      0x083b6a44 (node+0x10) are one-/few-instruction leaves, inlined;
      _M_insert @ 0x083b8844 is ported (byte_key_tree_insert_node,
      same module) and is the shipped default of BYTE_KEY_TREE_OPS;
      its pool allocator @ 0x083b7f40 is ported too
      (byte_key_tree_allocate_node, same module), the shipped default
      of BYTE_KEY_ALLOC_OPS.
      Node/container layouts are typed #[repr(C)] structs
      (ByteKeyTreeNode / ByteKeyTree) with 32-bit-asserted offsets, the
      app/node_list.rs precedent. Host tests model trees with typed
      nodes: empty tree, duplicate, new min/max, ascend-loop duplicate,
      predecessor-less insert under the descent parent, unsigned
      compare (0x40 vs 0x80), multi-insert skip, stub default, and an
      end-to-end find through the shipped default with a real mini
      linker. match.py structural: descent loop, begin test, header
      self-loop check, ascend loop, hook blx and flag stores all
      present; LLVM inlines the leaf callees (deviation, documented).

  - address: 0x083b8844
    ghidra: FUN_083b8844
    name: byte_key_tree_insert_node
    size: 480
    sig: "void byte_key_tree_insert_node(u8 **result, ByteKeyMap *map, u8 *insert_position, u8 *parent, const ByteKeyPair *key_pair)"
    module: cxx/byte_key_map
    status: ported
    vars: {param_1: result, param_2: map, param_3: insert_position, param_4: parent, param_5: key_pair, pbVar1: node, pbVar4: parent_link, pbVar5: uncle, pbVar8: node}
    notes: >-
      Ported. libstdc++ _Rb_tree::_M_insert for the byte-keyed map
      family: allocate a 0x20-byte node via the pool allocator @
      0x083b7f40 (dispatched through the BYTE_KEY_ALLOC_OPS slot, now
      ported as byte_key_tree_allocate_node - the shipped default),
      copy-construct the 16-byte pair to node+0x10,
      bump the count at map+0x14, then link: header parent (map+0x10)
      or nonzero insert_position -> left child (+8), a header parent
      also taking root (header+4) and rightmost (header+0xc), else
      leftmost (header+8) following when parent was leftmost;
      otherwise the byte comparator @ 0x083d73bc decides left (same
      leftmost follow-up) vs right (+0xc, rightmost follow-up).
      Rebalance is _Rb_tree_rebalance_for_insert verbatim: while not
      root and parent red (color 0), red uncle recolors parent/uncle
      black (1) and grandparent red (0) and ascends; black/absent
      uncle rotates - inner child rotates the parent down first, then
      parent black, grandparent red, rotate the grandparent (rotations
      @ 0x083b8090 rotate-left / 0x083b80e4 rotate-right ported as
      separate #[inline(never)] helpers keeping the bl boundaries).
      Root recolored black, fresh node written at result+0 (5th
      argument arrives on the stack in the original). Deviations: a
      null allocator result (only the documented stub) writes null at
      result+0 and leaves the tree untouched - the original cannot
      fail (operator new throws); the placement-new guard node+0x10 !=
      0 is folded into that check; the comparator is inlined; dead
      extra rotation arguments dropped. The port is the shipped
      default of BYTE_KEY_TREE_OPS (byte_key_tree_insert_unique @
      0x083b867c's slot). Host tests: arena-backed mock allocator
      honouring the 0x083b7f40 contract (color 0, links null), direct
      _M_insert drives (first-node root/leftmost/rightmost, left/right
      link follow-ups, forced-left insert_position), a full
      red-black/BST/count validator over ascending, descending and
      zigzag key orders (recolor + single/double rotation coverage),
      and an end-to-end find through both shipped defaults with
      duplicate re-finds. match.py structural: 120 instructions on
      both sides; allocator blx (slot), pair ldm/stm copy, count
      increment, link branches with leftmost/rightmost follow-ups,
      rebalance loop with recolor + four rotation bl sites all
      present; the null check and frame style (fp) are the documented
      deviations.

  - address: 0x083b8090
    ghidra: FUN_083b8090
    name: byte_key_tree_rotate_left
    size: 84
    sig: "void byte_key_tree_rotate_left(ByteKeyTree *tree, ByteKeyTreeNode *node)"
    module: cxx/byte_key_map
    status: ported
    vars: {param_1: tree, param_2: node, iVar3: right}
    notes: >-
      Ported as a private #[inline(never)] helper of
      byte_key_tree_insert_node (0x083b8844), preserving the bl
      boundary. libstdc++ _Rb_tree_rotate_left: the right child takes
      node's place (root pointer at header+4 first, else the matching
      side of node's parent), node adopts its left subtree and becomes
      its left child.

  - address: 0x083b80e4
    ghidra: FUN_083b80e4
    name: byte_key_tree_rotate_right
    size: 84
    sig: "void byte_key_tree_rotate_right(ByteKeyTree *tree, ByteKeyTreeNode *node)"
    module: cxx/byte_key_map
    status: ported
    vars: {param_1: tree, param_2: node, iVar3: left}
    notes: >-
      Ported as a private #[inline(never)] helper of
      byte_key_tree_insert_node (0x083b8844), preserving the bl
      boundary. libstdc++ _Rb_tree_rotate_right, the mirror of
      0x083b8090.

  - address: 0x083b7f40
    ghidra: FUN_083b7f40
    name: byte_key_tree_allocate_node
    size: 176
    sig: "ByteKeyTreeNode *byte_key_tree_allocate_node(ByteKeyMap *map)"
    module: cxx/byte_key_map
    status: ported
    vars: {param_1: map, puVar1: node, piVar3: chunk, uVar6: capacity}
    notes: >-
      libstdc++ pool allocator for the byte-keyed map family's
      0x20-byte nodes: pop the free list at map+4 (next threaded
      through node+0xc, the right link), else bump-allocate in the
      map+8..map+0xc range, else grow - capacity max(prev+0x20,
      prev+prev/2+prev/8), 0x20 first - carving a 0xc-byte chunk
      header (prev/capacity/arena, pushed at map+0) and a
      capacity*0x20 arena via two calls to the ported
      operator_new_checked @ 0x08266c70 (its dead r1=0 argument
      dropped; reached through an inline(never) pool_operator_new
      front-end to keep the two bl boundaries). Returned node: color
      byte 0 (red), parent/left/right nulled, pair uninitialised.
      Pool/chunk state modeled as ByteKeyNodePool / ByteKeyPoolChunk
      repr(C) structs (32-bit layout asserted; ByteKeyTree._opaque
      sized off the pool struct so host fields stay disjoint); node
      stride and chunk size go through size_of (0x20/0xc on target).
      Shipped default of the BYTE_KEY_ALLOC_OPS slot the ported
      byte_key_tree_insert_node @ 0x083b8844 dispatches through,
      replacing the missing_allocate_node null stub. No null check on
      the checked-new results, like the original (its new-handler path
      aborts @ 0x08266abc). Host tests: fresh-chunk carve, bump
      exhaustion growth (0x20 -> 0x40), the capacity formula on both
      branches (8 -> 0x28 floor, 0x100 -> 0x1a0), free-list pop
      recycling, and an all-shipped-defaults end-to-end find over a
      mocked heap arena. match.py: 45 vs 44 instructions, instruction-
      parallel apart from the LLVM frame-pointer setup.

  - address: 0x08123600
    ghidra: FUN_08123600
    name: formatted_message_emit
    size: 80
    sig: "void formatted_message_emit(MessageStream *stream, const char *key, int value, uint32 depth)"
    module: printf/formatted_message
    status: ported
    vars: {param_1: stream, param_2: key, param_3: value, param_4: depth}
    notes: >-
      83 bl call sites. Emits one indented plist integer property:
      indent_prepare @ 0x08123a54(stream, depth) fills the scratch at
      +0x215 with depth copies of the stream's indent unit, then the
      ported snprintf @ 0x0802f768 formats the literal @ 0x08123650
      ("%s<key>%s</key>\n%s<integer>%d</integer>\n") into the inline
      512-byte buffer at +0x15 with args (indent, key, indent, value),
      and the stream append @ 0x08123c58(stream, buf) is tail-called.
      The two unported callees are the INDENT_PREPARE / STREAM_APPEND
      dispatch statics (defaults: empty indent, dropped message);
      snprintf is called directly. Sibling 0x0812395c (88 bytes, 58
      sites) is the same function with the value looked up from a table
      @ 0x081239b4 instead of passed in.

  - address: 0x08123834
    ghidra: FUN_08123834
    name: formatted_message_emit_string
    size: 132
    sig: "void formatted_message_emit_string(MessageStream *stream, const char *key, const char *value, uint32 depth)"
    module: printf/formatted_message
    status: ported
    renames:
      param_1: stream
      param_2: key
      param_3: value
      param_4: depth
    notes: >-
      19 verified BL call sites (17 plain `bl`, 2 `blne` at
      0x0814fa48/0x08151278). Raw bytes establish the 132-byte extent:
      code ends at 0x081238b4 and the string format literal starts at
      0x081238b8. Prepares the stream indent at +0x215, calls the
      unported FUN_0809fbc8(value, stack[0x200], 0x200), and formats
      "%s<key>%s</key>\n%s<string>%s</string>\n" into the +0x15
      512-byte buffer before tail-branching to the unported stream append
      @ 0x08123c58. When key is NULL it advances the format address by
      16 bytes, omitting the complete key line and passing only
      (indent, transformed_value). The FUN_0809fbc8 table pointer is
      0x089cb224, but osos.dec contains unrelated resource text there;
      its three trigger/replacement pairs are therefore not recoverable.
      The Rust STRING_ESCAPE dispatch boundary preserves the call shape;
      its temporary bounded identity-copy default is a documented
      deliberate deviation until that callee is ported. Host tests cover
      both key paths, 0x200 transform capacity, exact variadic areas, and
      append handoff.

  - address: 0x081236f8
    ghidra: FUN_081236f8
    name: formatted_message_open_dict
    size: 68
    sig: "void formatted_message_open_dict(MessageStream *stream, const char *key, uint32 depth)"
    module: printf/formatted_message
    status: ported
    vars: {param_1: stream, param_2: key, param_3: depth, param_4: unused}
    notes: >-
      28 bl call sites, binary-scanned: all are unconditional plain bl
      (zero predicated forms), so callers perform no conditional gating.
      Opens an indented plist dict property: indent_prepare @ 0x08123a54
      receives stream and depth (r0/r2; r1 is overwritten from r2), then
      snprintf @ 0x0802f768 writes the literal at 0x0812373c
      ("%s<key>%s</key>\n%s<dict>\n") into the stream's +0x15 512-byte
      buffer with variadic words (indent, key, indent), before a tail b
      to the existing STREAM_APPEND boundary @ 0x08123c58. Raw bytes prove
      the 68-byte extent: the following word begins the literal and sibling
      formatted_message_emit_array @ 0x08123758. Ghidra's four-argument
      prototype is wrong: r3 is never read. Rust calls and returns from
      STREAM_APPEND instead of tail-branching, matching the established
      formatted_message module deviation.
  - address: 0x0812395c
    ghidra: FUN_0812395c
    name: formatted_message_emit_boolean
    size: 88
    sig: "void formatted_message_emit_boolean(MessageStream *stream, const char *key, uint32 index, uint32 depth)"
    module: printf/formatted_message
    status: ported
    vars: {param_1: stream, param_2: key, param_3: index, param_4: depth}
    notes: >-
      58 bl call sites. Sibling of formatted_message_emit @ 0x08123600:
      same indented plist property emitter, but the value is an
      empty-element tag looked up from a table instead of passed in.
      The literal-pool word @ 0x081239b4 holds the table base
      0x089cb210, the tag is table[index] (ldr r3, [r0, r5, lsl #2]),
      and the format literal @ 0x081239b8 ("%s<key>%s</key>\n%s<%s/>\n")
      is formatted with args (indent, key, indent, tag) into the +0x15
      buffer; the stream append @ 0x08123c58 is tail-called. All call
      sites pass index 0/1 with plist boolean semantics ("Stereo" with
      1, "Multichannel" with 0 @ 0x081502b0), i.e. the table is the
      {"false", "true"} tag pair. The osos.dec bytes at VA 0x089cb210
      are an unreferenced resource-string blob, so the runtime table
      contents are not statically readable from the image; the pair is
      pinned by the call sites and plist syntax. The lookup keeps the
      original's double indirection as the swappable BOOLEAN_TAG_TABLE
      static (default: false/true); INDENT_PREPARE / STREAM_APPEND
      dispatch slots are shared with the integer sibling, snprintf is
      called directly (LLVM inlines its bounded-cursor wrapper, same
      codegen shape as the sibling's accepted port).

  - address: 0x081239d4
    ghidra: FUN_081239d4
    name: formatted_message_close_dict
    size: 52
    sig: "void formatted_message_close_dict(MessageStream *stream, uint32 depth)"
    module: printf/formatted_message
    status: ported
    vars: {param_1: stream, param_2: depth}
    notes: >-
      29 bl call sites, binary-scanned (all plain bl, no predicated
      forms - no caller-side gating). The container-closing sibling of
      formatted_message_emit @ 0x08123600: indent_prepare @ 0x08123a54
      (stream, depth) fills the +0x215 scratch (r1 forwarded verbatim;
      the caller's r2/r3 are dead on entry), then the ported snprintf
      @ 0x0802f768 formats the literal @ 0x08123a08 ("%s</dict>\n",
      right after the 52-byte body, reached with add r2, pc, #24) into
      the inline 512-byte buffer at +0x15 with the single argument
      word (indent) - the original's r3, no stack words - and the
      stream append @ 0x08123c58(stream, buf) is tail-called. Call
      sites pass only stream + depth (@ 0x0815032c:
      formatted_message_close_dict(ctx + 0xc, 1) after a track dict's
      property batch). Ghidra's 52-byte extent is exact: the format
      literal sits at +0x34 and the </array> twin @ 0x08123a14 (52
      bytes, same body, literal "%s</array>\n" @ 0x08123a48, NOT
      ported) starts at +0x40. INDENT_PREPARE / STREAM_APPEND dispatch
      slots are shared with the emit siblings; snprintf is called
      directly. match.py structural: same prepare / format / append
      chain - LLVM inlines snprintf's bounded-cursor wrapper (the
      engine blx plus trailing NUL store stand in for the direct bl
      0x0802f768) and the two dispatch slots are ldr/blx pairs instead
      of the direct bl/b - the documented deviations of the sibling
      ports; the crate fp frame accounts for the rest.

  - address: 0x08123a54
    ghidra: FUN_08123a54
    name: indent_prepare
    size: 76
    sig: "void indent_prepare(MessageStream *stream, uint32 depth)"
    module: printf/formatted_message
    status: ported
    vars: {param_1: stream, param_2: depth}
    notes: >-
      14 bl call sites. The indent preparer of the formatted-message
      family: NUL the scratch at +0x215, then strncat @ 0x08031200
      (ported, libc/strcat) the stream's indent unit depth times -
      unit = table[style], table base from the literal-pool word @
      0x08123aa0 (value 0x089cb218, 8 bytes past the boolean-tag table
      base), style byte at +0x14 re-read each iteration (ldrb r0,
      [r5, #0x14]; ldr r1, [r7, r0, lsl #2]), each copy bounded at 0x40
      source bytes with no destination bound. The osos.dec bytes at VA
      0x089cb218 are the same unreferenced resource-string blob as the
      boolean sibling's table, so the runtime unit strings are not
      statically readable from the image; the lookup keeps the
      original's indirection as the swappable INDENT_UNIT_TABLE static
      (default: single-entry {"\t"}, pinned by plist convention). The
      port is the shipped default of the INDENT_PREPARE dispatch slot
      shared with formatted_message_emit / _emit_boolean. match.py
      structural: same algorithm - LLVM inlines the ported strncat
      (strlen scan + copy bounded at 0x40) and early-returns on
      depth == 0 instead of entering the loop; scratch NUL, table
      load, per-iteration style re-read and copy bound all present.

  - address: 0x0811d46c
    ghidra: FUN_0811d46c
    name: vtable_query_4c_kind4
    size: 12
    sig: "u32 vtable_query_4c_kind4(handle: *mut *mut u8, out: *mut u32, unused: usize, forwarded: usize)"
    module: util/vtable_query
    status: ported
    renames:
      param_1: handle
      param_2: out
      param_3: unused
      param_4: forwarded
    notes: >-
      29 bl call sites. `mov r2, r1; mov r1, #4; b 0x0811d7b0` - binds
      the constant 4 as the middle (kind) argument of the generic
      dispatcher FUN_0811d7b0 @ 0x0811d7b0 (28 bytes, 16 sites, NOT
      ported, stays unported), which loads [[*handle] + 0x4c] and calls
      it as method(*handle, kind, arg, &spilled_r3) - a vtable slot
      +0x4c dispatch with an out-parameter. Call sites pass a pointer to
      a local out-word in r1, treat the return as an error code (0 =
      success) and then mask a 0xc0 tag byte off the out word (the
      is_tagged_c0 family) or bit-test it (tst #0x39/#0x43/#0x16):
      kind 4 is a property/attribute query. The caller's r2 is dead on
      entry (first instruction overwrites it); the caller's r3 is
      forwarded verbatim through the dispatcher's stmdb sp!,{r3} spill
      (no call site sets it deliberately). Ported via the documented
      VTABLE_SLOT_4C_DISPATCH slot (the util/inner_state.rs
      INNER_MATERIALIZE_COUNT pattern): the default stub models the
      dispatcher body exactly (double deref + slot +0x4c load + indirect
      call, read_unaligned so the layout stays byte-exact on 64-bit
      hosts), host tests use a recording mock plus an end-to-end fake
      vtable. Neighbour 0x0811d7fc is the identical shape on slot +0x50.
      match.py structural: same mov r2,r1 / mov r1,#4 / tail-dispatch
      shape; the diff is the crate fp frame and the dispatch-slot
      ldr/blx standing in for the direct b 0x0811d7b0 - the documented
      deviation.

  - address: 0x0811d208
    ghidra: FUN_0811d208
    name: is_tagged_c0
    size: 20
    sig: "u32 is_tagged_c0(u32 value)"
    module: util/is_tagged_c0
    status: ported
    notes: >-
      11 bl call sites, a pure leaf - returns 1 when the top byte of the
      argument is exactly 0xc0, else 0. The tag's meaning (0xc0xxxxxx as
      an address range or as a packed type tag) is still not established;
      the port is behavior-only. match.py: LLVM lowers the compare to an
      add/clz/lsr idiom instead of cmp/movne/moveq - same mask-and-test
      structure.

  - address: 0x0811c564
    ghidra: FUN_0811c564
    name: flag_2c_is_clear
    size: 16
    sig: "u32 flag_2c_is_clear(const void *obj)"
    module: ui/flag_2c
    status: ported
    renames:
      param_1: obj
    notes: >-
      16 bl call sites, a pure leaf. `rsbs r0, byte[obj + 0x2c], #1;
      movcc r0, #0` - i.e. it returns 1 when the byte at +0x2c is 0 and 0
      for every other value (the carry-clear predicate kills the negative
      results, so it is exactly `byte == 0`, not `1 - byte`). The owning
      class is unidentified (UI/layout object judging by the callers), so
      the descriptive offset-based name is kept. match.py: LLVM lowers
      `byte == 0` to clz/lsr instead of rsbs/movcc - same load-and-test
      structure.


  - address: 0x0829d3a0
    ghidra: FUN_0829d3a0
    name: flag_bit_2_at_4
    size: 16
    sig: "u32 flag_bit_2_at_4(const void *object)"
    module: ui/flag_bit_2_at_4
    status: ported
    renames:
      param_1/r0: object
      "object+0x4": flags
    notes: >-
      One direct `bl` site (0x0826b858) in the shared collection-navigation
      body calls it after a virtual callback produces an otherwise
      unidentified UI object. The 16-byte sibling entry at 0x0826b784
      tail-branches to that body before inspecting its follow-up display data.
      ABI: r0 is a readable
      Two direct call sites (0x0826b7e4 and 0x0826b878) obtain a UI object
      from a collection-navigation callback and branch on this pure leaf
      before inspecting its follow-up display data. ABI: r0 is a readable
      object pointer and the normalized u32 result returns in r0. Exact
      16-byte sequence: `ldrb r0,[r0,#4]; and r0,r0,#4; lsr r0,r0,#2; bx lr`.
      Thus bit 2 of byte +0x4 is returned as precisely 0 or 1, not as the
      mask 0 or 4. The object family and flag meaning are still unidentified,
      so the descriptive field-and-bit name is deliberate. Reference C:
      decomp/c/028/0829d3a0_FUN_0829d3a0.c; assembly: osos.asm
      0x0829d3a0. Focused host tests exhaust all flag-byte values, vary every
      unrelated object byte, and prove const-pointer aliases leave the object
      byte-identical.

  - address: 0x0811f704
    ghidra: FUN_0811f704
    name: width_0c_minus_inset
    size: 28
    sig: "u32 width_0c_minus_inset(const void *obj, u32 mode)"
    module: ui/width_inset
    status: ported
    vars: {param_1: obj, param_2: mode}
    notes: >-
      13 bl call sites, a pure leaf, all in list/text layout code
      (0x080f6a80, 0x081396xx, 0x0819xxxx, 0x081acxx-0x081afxxx,
      0x081fdxxx). Reads the u16 at obj+0x0c and returns it minus 9 when
      the second argument is 4, minus 8 otherwise, truncated back to 16
      bits (so it wraps rather than clamping). The shape is a width minus
      a mode-dependent inset, but neither the owning class nor the
      meaning of mode 4 is established, so the name describes the
      arithmetic rather than claiming a meaning.

  - address: 0x080e9740
    ghidra: FUN_080e9740
    name: string_object_copy_cstr_to_buffer
    size: 72
    sig: "void string_object_copy_cstr_to_buffer(const StringObject *source, char *destination, u32 *inout_length)"
    module: cxx/string
    status: ported
    renames:
      param_1: source
      param_2: destination
      param_3: inout_length
      iVar1: source_cstr
      uVar2: source_length
    notes: >-
      Source: decomp/c/008/080e9740_FUN_080e9740.c; structure verified
      against the original osos image disassembly. The 72-byte wrapper
      probes string_object_c_str
      @ 0x082a50b0, then when its result is non-NULL calls strncpy
      @ 0x080310d4 with destination, that C string, and the INPUT
      *inout_length. It calls string_object_c_str again, measures that
      untruncated C string through strlen @ 0x08392478, and overwrites
      *inout_length with the full length; when the probe is NULL it skips
      both copy and strlen and writes 0. The current c_str accessor
      substitutes a shared empty string for a NULL payload, so its result
      is normally non-NULL: an empty payload therefore zero-pads the input
      capacity and reports 0. The routine returns void, retains no pointer,
      and has no ownership/release effect. Port reuses the existing
      StringObject/string_object_c_str and strncpy/strlen seams without
      porting callees. match.py structural: expected call-gated
      c_str/c_str/strncpy/c_str/strlen flow and final output store.

  - address: 0x080e9788
    ghidra: FUN_080e9788
    name: iabs
    size: 12
    sig: "i32 iabs(i32 x)"
    module: fp/fp_misc
    status: ported
    renames:
      param_1: x
    notes: >-
      Plain 32-bit integer abs: `cmp r0,#0; rsblt r0,r0,#0; bx lr`.
      The rsb computes 0 - x in 32 bits, so iabs(INT_MIN) wraps back to
      INT_MIN (port uses wrapping_neg). 3 bl call sites (0x08241e4c/
      0x08241e58 in FUN_08241acc, 0x082424cc in FUN_0824039c), all in
      0x0824xxxx graphics-region code comparing two |deltas| and keeping
      the larger. The immediately following functions 0x080e9794 and
      0x080e97a0 are byte-identical duplicate emissions (separate
      assignments, no Rust symbols here). match.py: LLVM emits rsbmi
      (same N-flag test as rsblt) behind the crate's standard frame-
      pointer prologue - structurally the same compare-and-negate leaf.

  - address: 0x080e9794
    ghidra: FUN_080e9794
    name: iabs_alias_9794
    size: 12
    sig: "i32 iabs(i32 x)"
    module: fp/fp_misc
    status: ported
    renames:
      param_1: x
    notes: >-
      ALIAS - byte-identical to iabs @ 0x080e9788 (all 12 bytes
      verified against osos.dec: cmp r0,#0 / rsblt r0,r0,#0 / bx lr),
      a duplicate emission from another translation unit. 6 bl call
      sites (0x08240f18/0x08240f24, 0x0824178c/0x08241798,
      0x082417bc/0x082417c8), all in 0x0824xxxx graphics-region code
      comparing two |deltas| and keeping the larger, same usage as the
      primary. No separate Rust symbol - point any hook at the single
      iabs symbol (the load_be32 alias precedent). match.py against
      iabs at this address: same rsbmi-behind-frame-prologue structure
      as the primary, byte-identical inputs make the diff identical.

  - address: 0x080e97a0
    ghidra: FUN_080e97a0
    name: iabs_alias_97a0
    size: 12
    sig: "i32 iabs(i32 x)"
    module: fp/fp_misc
    status: ported
    renames:
      param_1: x
    notes: >-
      ALIAS - byte-identical to iabs @ 0x080e9788 (all 12 bytes
      verified against osos.dec: cmp r0,#0 / rsblt r0,r0,#0 / bx lr),
      a duplicate emission from another translation unit; third copy
      after 0x080e9794. 12 bl call sites
      (0x082a0a0c/0x082a0a18/0x082a0a30/0x082a0a3c/0x082a0a54/
      0x082a0a60, 0x082a0ce0/0x082a0cec/0x082a0d04/0x082a0d10,
      0x082a0ea0/0x082a0eac), all inside FUN_082a0920 in the
      0x082axxxx region, a fixed-point matrix routine that compares
      |entries| and keeps the largest magnitude - the same
      compare-and-keep-larger usage as the primary and 9794 copies.
       No separate Rust symbol - point any hook at the single iabs
       symbol (the load_be32 alias precedent). match.py against iabs at
       this address: same rsbmi-behind-frame-prologue structure as the
       primary, byte-identical inputs make the diff identical.

  - address: 0x080e988c
    ghidra: FUN_080e988c
    name: fixed16_sin
    size: 40
    sig: "i32 fixed16_sin(i32 angle)"
    module: fp/fp_misc
    status: identified
    renames:
      param_1: angle
      DAT_080e9900: TWO_PI_F32 (0x40c90fdb = 6.2831855f)
      DAT_080e9904: INV_TWO_PI_F32 (0x3e22f983 = 0.15915494f)
      DAT_080e9908: SIN_TABLE (pointer 0x083f10b2)
      FUN_080a67dc: f32_to_fixed16_sat
    notes: >-
      Q16.16 fixed-point sine by quarter-wave table lookup. The Ghidra C
      listing stops after the calls to 0x080a67dc because it falsely marks
      that helper no-return; raw osos.dec disassembly confirms both calls
      return and the body continues through 0x080e98fc. The signed `blt`
      loop adds f32_to_fixed16_sat(2pi) (= 0x6487e) while angle is negative,
      then multiplies by f32_to_fixed16_sat(1/(2pi)) (= 0x28be), retains
      the low 32 product bits, and arithmetic-shifts 20 to form a 4096
      units/turn phase. It mirrors phase bit 0x400 into index 0..0x3ff,
      reads a u16 amplitude, and negates it for phase bit 0x800. This is
      fixed16_sin: the same routine as fixed16_cos @ 0x080e97ac without
      that routine's +0x4000 (pi/2) phase offset. Deferred 2026-08-20:
      direct raw-osOS disassembly confirms the complete 0x080e988c-0x080e98ff
      body and its literal pool, but every output directly depends on
      SIN_TABLE's exact 1024 u16 entries at 0x083f10b2. Reinspection of the
      current osos.dec confirms all 2048 table bytes are zero, inside the
      undecrypted hole (also recovery/patched), and no duplicate table exists.
      Do not synthesize it: unknown entry rounding/clamping changes observable
      Q16.16 outputs. Revisit only after decrypt fills the table, then embed it
      and retain an explicit f32_to_fixed16_sat seam for unported 0x080a67dc.
      Ghidra size 40 is truncated by the false no-return edge; actual code is
      0x080e988c-0x080e98ff plus literals 0x080e9900-0x080e990b.

  - address: 0x082a182c
    ghidra: FUN_082a182c
    name: fixed16_div_indirect
    size: 8
    sig: "i32 fixed16_div_indirect(a: *const i32, b: i32) -> i32"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b, FUN_08037a84: sdiv32}
    notes: >-
      Truncating signed 32-bit division with the DIVIDEND fetched
      through a pointer: the whole body is ldr r0,[r0,#0x0]; b
      0x08037a84 - load *a into r0 and TAIL-CALL the unported ADS
      quotient-only sdiv core @ 0x08037a84 (344 bytes: both operands
      reduced to magnitude with a sign track, a clz difference
      dispatching into an unrolled 16-step shift-subtract loop,
      quotient negated when the signs differed), the divisor passed
      straight through in r1, so the callee's quotient returns directly
      to this function's caller. Heads the indirect fixed16 helper run
      0x082a182c-0x082a18c8 (div, eq, gt, lt, sub, mul, ne, add); the
      eq comparator @ 0x082a1834 sits immediately below it. Unlike
      fixed16_div (@ 0x080e9834), which computes a true Q16.16 quotient
      by reciprocal-multiply, this helper is a PLAIN integer sdiv -
      callers pre-scale: FUN_08273dc8 divides a Q16.16 delta by
      (row_count << 16) for a per-row step, and FUN_0827cf10 forms
      ratios of two Q16.16 values (d / (300.0 - d)), the 2^16 scales
      cancelling. 6 bl call sites, binary-scanned: 0x08273e2c
      (FUN_08273dc8), 0x0827403c (FUN_08274004), 0x08274140
      (FUN_08274108), 0x0827cf54/0x0827cfa4/0x0827cfd0 (FUN_0827cf10).
      The port delegates to a host-swappable sdiv32 seam (the
      fixed16_reciprocal/FIXED16_RECIPROCAL precedent; FIXED16_SDIV32
      cell) since the division core itself is unported. match.py
      structural: same ldr r0,[r0] core; LLVM lowers the
      absolute-address tail call as a literal-pool word (0x08037a84)
      plus bx r2 and adds the usual push {fp,lr}/pop {fp,lr} frame
      instead of the original's direct b - same target, same
      pass-through of the divisor in r1.

  - address: 0x082a1834
    ghidra: FUN_082a1834
    name: fixed16_eq_indirect
    size: 24
    sig: "bool fixed16_eq_indirect(a: *const i32, b: *const i32) -> bool"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 equality comparator with both operands fetched through
      pointers: ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movne r0,#0;
      moveq r0,#1 - returns 1 when the two values are equal, else 0
      (Ghidra: return *param_1 == *param_2). Unlike the signed
      movle/movgt and movge/movlt pairs of fixed16_gt_indirect
      (@ 0x082a184c) and fixed16_lt_indirect (@ 0x082a1864), the EQ/NE
      conditions test raw bit-pattern equality, so this is the exact
      counterpart of fixed16_ne_indirect (@ 0x082a18a8) with the sense
      inverted. 4 bl call sites, all in FUN_0827d380 (0x0827d5f4,
      0x0827d608, 0x0827d61c, 0x0827d630): four consecutive Q16.16
      struct fields (param_1+5..+8) are compared edge-by-edge against
      the stack-materialized 1.0 constant (0x10000) and the results
      ANDed - an identity-scale check that skips further work only when
      every component is exactly 1.0. Heads the indirect fixed16 helper
      run 0x082a1834-0x082a18c8 (eq, gt, lt, sub, mul, ne, add).
      match.py structural: same two pointer loads; LLVM computes the
      equality branchlessly as sub r0,r0,r1 / clz r0,r0 / lsr r0,r0,#5
      (32>>5 = 1 only when the words are equal - identical semantics)
      instead of the movne/moveq pair, and adds the usual push
      {fp,lr}/pop {fp,pc} frame.

  - address: 0x082a184c
    ghidra: FUN_082a184c
    name: fixed16_gt_indirect
    size: 24
    sig: "bool fixed16_gt_indirect(a: *const i32, b: *const i32) -> bool"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 greater-than comparator with both operands fetched through
      pointers: ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movle r0,#0;
      movgt r0,#1 - a SIGNED compare returning 1 when *a > *b, else 0
      (Ghidra: return *param_2 < *param_1). Strict ordering: equal
      values return 0. Used for threshold tests and range folding:
      FUN_082572e4 folds a Q16.16 angle by comparing against 0xb40000
      (180.0) and 0x1680000 (360.0); FUN_080fe5fc tests a struct field
      against the stack-materialized 1.0 constant before clamping;
      FUN_080f780c compares offsets against -100.0 (0xff9c0000) and a
      global bound. 11 bl call sites, binary-scanned (0x080f7884,
      0x080f7938, 0x080fe64c, 0x08152a54, 0x08167a1c, 0x08197788,
      0x08257300, 0x0825732c, 0x08257408, 0x0825749c, 0x0827b6fc).
      Sits immediately before the indirect fixed16 helper cluster
      0x082a1864-0x082a18c8 (lt, sub, mul, ne, add) and is that
      family's greater-than sibling; the eq comparator @ 0x082a1834
      just above it is a separate port. match.py structural: identical
      core (two pointer loads, cmp, conditional result); LLVM adds a
      push {fp,lr}/pop {fp,pc} frame and hoists the movle #0 result
      before the cmp (movgt still overwrites, same semantics).

  - address: 0x082a1864
    ghidra: FUN_082a1864
    name: fixed16_lt_indirect
    size: 24
    sig: "bool fixed16_lt_indirect(a: *const i32, b: *const i32) -> bool"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 less-than comparator with both operands fetched through
      pointers: ldr r0,[r0]; ldr r1,[r1]; cmp r0,r1; movge r0,#0;
      movlt r0,#1 - a SIGNED compare returning 1 when *a < *b, else 0
      (Ghidra: return *param_1 < *param_2). The signed movge/movlt pair
      makes it an ordering comparator, unlike the raw bit-pattern
      fixed16_ne_indirect @ 0x082a18a8. Used for range checks:
      FUN_080fe5fc clamps a freshly multiplied Q16.16 result into
      [0, 1.0] (if (!lt(&v, &ZERO)) { if (ge(&v, &ONE)) v = ONE }); the
      FUN_080f79d8/FUN_081529d8 sites compare locals edge-by-edge
      against live geometry values. 7 bl call sites, binary-scanned
      (0x080f7a68, 0x080f7b10, 0x080fe630, 0x08152a80, 0x08152bdc,
      0x08167a34, 0x081977a0). First member of the indirect fixed16
      helper cluster 0x082a1864-0x082a18c8 (lt, sub, mul, ne, add).
      match.py structural: identical core (two pointer loads, cmp,
      movlt #1); LLVM adds a push {fp,lr}/pop {fp,pc} frame and hoists
      the movge #0 result before the cmp (movlt still overwrites, same
      semantics).

  - address: 0x082a187c
    ghidra: FUN_082a187c
    name: fixed16_sub_indirect
    size: 16
    sig: "i32 fixed16_sub_indirect(a: *const i32, b: *const i32) -> i32"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 subtraction with both operands fetched through pointers:
      ldr r0,[r0]; ldr r1,[r1]; sub r0,r0,r1; bx lr - returns *a - *b
      as a plain 32-bit difference (Ghidra: return *param_1 - *param_2).
      The non-flag-setting sub wraps modulo 2^32 with no saturation, so
      the port uses wrapping_sub. FUN_082a5d54 calls it twice to
      subtract the +0x8 and +0x4 Q16.16 components of two structs
      edge-by-edge (the +0x0 component uses an inline sub on directly
      loaded words), forming a fixed-point vector delta handed to
      FUN_0828018c; the 0x08273e1c site subtracts a stack-materialized
      zero from a local before calling the 0x082a182c helper. 3 bl call
      sites, binary-scanned (0x08273e1c, 0x082a5d6c, 0x082a5d7c).
      Second member of the indirect fixed16 helper cluster
      0x082a1864-0x082a18c8 (lt, sub, mul, ne, add). match.py
      structural: identical core (two pointer loads, sub); LLVM adds a
      push {fp,lr}/pop {fp,pc} frame and swaps the load order.

  - address: 0x082a188c
    ghidra: FUN_082a188c
    name: fixed16_mul_indirect
    size: 28
    sig: "i32 fixed16_mul_indirect(a: *const i32, b: i32) -> i32"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 fixed-point multiply with the multiplicand fetched through a
      pointer: ldr r2,[r0]; smull r0,r1,r2,<b>; then the same
      (hi << 16) | (lo >> 16) funnel as fixed16_mul @ 0x080e9878,
      returning signed product bits [47:16] - arithmetic-shift semantics
      (negative products truncate toward minus infinity) with silent
      32-bit wrap, no clamping. The stmdb sp!,{r0,r1,lr} frame only
      spills both arguments so b can be reloaded after the pointer load
      (ADS register-allocation artifact); no semantic effect. Call sites
      materialize the first operand on the stack and pass sp + N (e.g.
      0x08257354/0x0825735c), which is why the operand is indirect. 57 bl
      call sites, binary-scanned (0x080fxxxx, 0x0816xxxx, and the
      0x0825xxxx/0x0827xxxx geometry code). Result-equivalent to
      fixed16_mul @ 0x080e9878 on the dereferenced value; ported as a
      separate symbol because the hook must replace this distinct
      address. match.py structural: same 7-instruction shape (pointer
      load, smull, lsl/lsr funnel, orr); frame setup and register
      allocation differ (LLVM push {fp,lr} vs ADS arg spill).

  - address: 0x082a18a8
    ghidra: FUN_082a18a8
    name: fixed16_ne_indirect
    size: 20
    sig: "bool fixed16_ne_indirect(a: *const i32, b: *const i32) -> bool"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 not-equal comparator with both operands fetched through
      pointers: ldr r0,[r0]; ldr r1,[r1]; subs r0,r0,r1; movne r0,#1 -
      returns 1 when the values differ, 0 when equal (Ghidra:
      return *param_1 != *param_2). The subs difference is dead; only Z
      feeds the movne, so the port compares the dereferenced values
      directly. Used as a geometry dirty check: FUN_0810da8c compares a
      cached rectangle against the live one edge-by-edge and recomputes
      clipping only when any edge moved (the >> 0x10 shifts there
      confirm the Q16.16 format). 20 bl call sites, binary-scanned
      (0x0810xxxx, 0x0814xxxx, 0x0820xxxx, 0x0827xxxx). Sits in the
      indirect fixed16 helper cluster 0x082a1864-0x082a18c8 (lt, sub,
      mul, ne, add). match.py structural: identical core (two pointer
      loads, subs, movne); LLVM adds a push {fp,lr}/pop {fp,pc} frame
      and swaps the load order.

  - address: 0x082a18bc
    ghidra: FUN_082a18bc
    name: fixed16_add_indirect
    size: 16
    sig: "i32 fixed16_add_indirect(a: *const i32, b: *const i32) -> i32"
    module: fp/fp_misc
    status: ported
    vars: {param_1: a, param_2: b}
    notes: >-
      Q16.16 addition with both operands fetched through pointers:
      ldr r0,[r0]; ldr r1,[r1]; add r0,r0,r1; bx lr - returns *a + *b
      as a plain 32-bit sum (Ghidra: return *param_1 + *param_2). The
      non-flag-setting add wraps modulo 2^32 with no saturation, so
      the port uses wrapping_add. FUN_082a5db0 calls it twice to add
      the +0x8 and +0x4 Q16.16 components of two structs edge-by-edge
      (the +0x0 component uses an inline add on directly loaded
      words), packing the fixed-point vector sum into a 12-byte record
      handed to FUN_0828018c; the two FUN_0827b5a8 sites add
      stack-materialized locals. 4 bl call sites, binary-scanned
      (0x0827b8bc, 0x0827b8d0, 0x082a5dc8, 0x082a5dd8). Last member of
      the indirect fixed16 helper cluster 0x082a1864-0x082a18c8 (lt,
      sub, mul, ne, add). match.py structural: identical core (two
      pointer loads, add); LLVM adds a push {fp,lr}/pop {fp,pc} frame
      and swaps the load order.

  - address: 0x082a18cc
    ghidra: FUN_082a18cc
    name: message_kind_byte
    size: 8
    sig: "u8 message_kind_byte(message: *const u8) -> u8"
    module: fp/fp_misc
    status: ported
    vars: {param_1: message}
    notes: >-
      Kind/discriminant byte getter of the 12-byte UI message
      envelope constructed @ 0x0825790c (vtable word at +0x0, this
      kind byte at +0x4 - the constructor zeroes it with strb - and
      a union word at +0x8 holding either a nested message pointer
      or a plain id). Body: ldrb r0,[r0,#0x4]; bx lr (Ghidra:
      return *(undefined1 *)(param_1 + 4)). Both callers dispatch
      on the tag: 0 takes the nested-message path (the +0x8 word is
      a payload pointer whose own +0x4 message code is compared
      against 0x500/0x501 by FUN_081d71d8), 1 takes the plain-id
      path (FUN_081d6f7c), any other value is ignored. 2 bl call
      sites (0x081d6ad4 in FUN_081d6ac4, 0x081d7e7c in
      FUN_081d7e68). Sits immediately after the indirect fixed16
      helper cluster 0x082a1834-0x082a18c8 and heads a tiny
      accessor run with the +0x8 word getter @ 0x082a18d4 and the
      +0x4 word getter @ 0x082a18dc (separate ports). match.py
      structural: identical ldrb r0,[r0,#4] core; LLVM adds a
      push {fp,lr}/pop {fp,pc} frame, same as the whole fixed16
      cluster.

  - address: 0x082a18d4
    ghidra: FUN_082a18d4
    name: message_payload_word
    size: 8
    sig: "u32 message_payload_word(message: *const u32) -> u32"
    module: fp/fp_misc
    status: ported
    vars: {param_1: message}
    notes: >-
      Union-word getter of the 12-byte UI message envelope
      constructed @ 0x0825790c (vtable word at +0x0, the
      message_kind_byte tag at +0x4, this word at +0x8). Body:
      ldr r0,[r0,#0x8]; bx lr (Ghidra: return *(undefined4
      *)(param_1 + 8)). The word is a tag-selected union: kind 0
      makes it a nested-message payload pointer (whose own +0x4
      message code is compared against 0x500/0x501 by
      FUN_081d71d8), kind 1 a plain id. 1 bl call site (0x081d6f88
      in FUN_081d6f7c), the plain-id path: the returned word is
      handed to FUN_081d70d4 as the id, the resulting index fetches
      an entry from the 8-byte-stride table at param_1+0x158, and
      the entry is vtable-dispatched with code 0x20. Middle member
      of the envelope accessor run 0x082a18cc-0x082a18e0 (kind
      byte, this word, +0x4 word @ 0x082a18dc, a separate port).
      Codegen note: body is byte-identical to ft_stream_pos
      (ldr r0,[r0,#8]; bx lr), so the port carries its own
      link_section to keep LLVM's identical-code folding from
      collapsing the two exports into one section (the
      cxx::clock_source_destroy precedent). match.py structural:
      identical ldr r0,[r0,#8] core; LLVM adds a push {fp,lr}/
      pop {fp,pc} frame, same as the whole fixed16 cluster and
      message_kind_byte.

  - address: 0x082a18dc
    ghidra: FUN_082a18dc
    name: message_code_word
    size: 8
    sig: "u32 message_code_word(message: *const u32) -> u32"
    module: fp/fp_misc
    status: ported
    vars: {param_1: message}
    notes: >-
      Message-code word getter of the UI message object (vtable
      word at +0x0, this code word at +0x4). Body:
      ldr r0,[r0,#0x4]; bx lr (Ghidra: return *(undefined4
      *)(param_1 + 4)). A WORD load, unlike the sibling
      message_kind_byte (@ 0x082a18cc) whose ldrb reads only the
      tag byte of the 12-byte envelope - the two accessors sit on
      different object types: FUN_081d7e68 first unwraps the
      kind-0 envelope with FUN_08257904, then applies THIS getter
      to the nested message it yields. Callers dispatch on the
      code: FUN_081d71d8 compares it against 0x500 (routes the
      message's +0x0 word to FUN_081d6b5c) and 0x501 (routes to
      FUN_081d6760), returning false for any other code;
      FUN_081d7e68 proceeds down its nested-message path only
      when the code is 0. 2 bl call sites (0x081d71f0 in
      FUN_081d71d8, 0x081d7eac in FUN_081d7e68). Last member of
      the envelope/message accessor run 0x082a18cc-0x082a18e0
      (kind byte, +0x8 union word, this +0x4 code word). No
      link_section needed: no other ported export shares the
      ldr r0,[r0,#4]; bx lr body, and match.py confirms the
      symbol heads its own disassembly. match.py structural:
      identical ldr r0,[r0,#4] core; LLVM adds a push {fp,lr}/
      pop {fp,pc} frame, same as the whole fixed16 cluster and
      the two envelope getter siblings.

  - address: 0x082a1918
    ghidra: FUN_082a1918
    name: query_name_to_cxx_string
    size: 156
    sig: "void query_name_to_cxx_string(string: *mut *mut u8)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: string
      uVar1: source
      uVar2: length
      iVar4: rep
      local_68: capacity_floor_slot
      uStack_64: capacity_length_slot
      auStack_60: query
      local_18: name
      DAT_082a19b4: SHARED_EMPTY_REP (0x08b31804, binary-verified)
      DAT_082a19b8: STRING_OBJECT_VTABLE (0x089a6044, binary-verified)
    notes: >-
      No bl call sites in osos.asm and no pointer to 0x082a1918
      anywhere in osos.dec (binary-scanned) - reached indirectly
      (vtable or computed table outside the image body).
      Materializes the DEFAULT query object (the 72-byte
      stack-local class of util/inner_state.rs: constructor
      FUN_0813e474 @ 0x0813e474 with id byte 0 / mode byte 0,
      destructor FUN_0813e5c4 @ 0x0813e5c4), resolves its display
      name into a two-word StringObject (FUN_0813c2ec @
      0x0813c2ec), then builds a COW basic_string<char> from the
      name's C string: string_object_c_str @ 0x082a50b0 (ported),
      strlen @ 0x08392478 (ported), then either the shared empty
      rep 0x08b31804 (length 0, literal DAT_082a19b4) or
      cxx_string_rep_create @ 0x083d8a64 (ported) with capacity
      max(0x20, length) - the original stages 0x20 and the length
      in two stack slots and picks the larger with an
      addhi/movls address select (a direct rep_create call, NOT
      rep_reserve, though reserve(0, len, len) computes the same
      capacity). Stores the data pointer (rep + 0xc) into the
      one-word string object BEFORE the copy, then __rt_memcpy
      (ROM veneer thunk @ 0x08037db0, ported) - unconditional,
      length 0 included. Plants the StringObject vtable
      0x089a6044 (DAT_082a19b8) and runs string_object_release_payload
      @ 0x08275d74 (ported) on the name holder, then destroys
      the query object. The three query-class callees are
      unported; each sits behind a host-swappable usize seam
      (QUERY_OBJECT_CONSTRUCT / QUERY_OBJECT_NAME /
      QUERY_OBJECT_DESTROY, this module's FIXED16_RECIPROCAL
      pattern) - the target build transmutes the fixed firmware
      addresses, host tests install recording mocks. Deviations:
      modeled empty_rep() / STRING_OBJECT_VTABLE statics stand
      in for the ROM/RAM addresses (the cxx/string.rs and
      cxx/string_object.rs simplifications); the two stack
      objects are zero-initialized where the original leaves
      them uninitialized (unobservable - the constructor writes
      every field later steps read); the 0x58-byte frame and
      two-slot capacity staging are ADS artifacts, computed
      directly. match.py structural (exit-1 diff expected):
      same construct -> name -> c_str -> strlen ->
      empty-or-create -> store -> memcpy -> vtable -> release
      -> destroy shape and the same cmp/movls 0x20 capacity
      floor; the diffs are the crate fp frame, the blx seam
      dispatches (0x0813e474/0x0813c2ec visible as literal
      words), an __aeabi_memclr4 for the zero-init, and LLVM
      inlining strlen + rep_create (the documented deviations).

  - address: 0x082a19bc
    ghidra: FUN_082a19bc
    name: context_text_to_cxx_string
    size: 48
    sig: "void context_text_to_cxx_string(string: *mut *mut u8)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: string
      uVar1: string (the default-constructor's r0 return, kept in
        r4 and threaded into the deserializer)
      auStack_20c: counted (the 512-byte counted-u16 staging buffer
        at sp+0x4, a u16 codepoint count plus up to 0xff u16 units)
      auStack_c: DEAD SLOT (sp+0x204; the add r1,sp,#0x204 aiming at
        it is clobbered by the constructor's ldr r1,[0x83d8c2c] -
        ADS scheduling artifact, no reader)
      FUN_083d8c20: cxx_string_default_ctor (ported, cxx/string)
      FUN_080539f0: context_text_to_counted_u16 (unported, behind
        the CONTEXT_TEXT_TO_COUNTED_U16 seam)
      FUN_082596f4: counted_u16_to_cxx_string (unported, behind the
        COUNTED_U16_TO_CXX_STRING seam)
    notes: >-
      No bl call sites in osos.asm and no pointer to 0x082a19bc
      anywhere in osos.dec (binary-scanned) - reached indirectly,
      like its neighbor query_name_to_cxx_string @ 0x082a1918
      (vtable or computed table outside the image body). Hands the
      shared context's text field back as a COW basic_string<char>,
      staged through a counted-u16 (UTF-16) stack buffer:
      cxx_string_default_ctor @ 0x083d8c20 (ported) parks *string
      on the shared empty rep; FUN_080539f0 @ 0x080539f0 (156
      bytes, unported) fetches the shared context (lazy getter
      0x08369bec), builds a StringObject from the C string at
      context+0x98 (first byte 0xff = unset -> count 0), appends
      the lookup name of the u16 code at context+0x92
      (FUN_08068fe8 maps it to short layout tags "ABC"/"B19"/
      "DK"/"CH"/"KH"...), truncates to 0xff codepoints and
      expands UTF-8->UTF-16 into the buffer (decode loop
      0x082767fc); FUN_082596f4 @ 0x082596f4 (72 bytes, unported)
      rebuilds a StringObject from the counted u16 units
      (FUN_082773b4 + FUN_08276db4) and assigns its c_str into
      *string through cxx_string_assign_cstr @ 0x083d8ca0,
      unconditionally (count 0 included). The field is the same
      shared-context text the about/diagnostics record at
      0x08112800 collects (0x080539f0 is one of its four field
      fillers - see object_set_version_text's entry); the
      concrete field is not recovered, but the code-name table
      points at an input/keyboard-layout name. Register flow
      verified against the disassembly: Ghidra's extra auStack_c
      parameter on FUN_083d8c20 is fictitious (r1 dead - the
      constructor clobbers it), and FUN_080539f0's param_2..
      param_4 are the pushed r2/r3 stack-reservation words of
      its stmdb sp!,{r2,r3,r4,r5,r6,lr} frame, not arguments -
      the true arity of all three calls is one argument each.
      The two text-marshaling callees sit behind host-swappable
      usize seams (CONTEXT_TEXT_TO_COUNTED_U16 /
      COUNTED_U16_TO_CXX_STRING, this module's
      FIXED16_RECIPROCAL pattern): the target build transmutes
      the fixed firmware addresses, host tests install recording
      mocks. Deviations: the staging buffer is zero-initialized
      where the original leaves it uninitialized (unobservable -
      the serializer writes the count and every unit the
      deserializer reads); the 4 spare frame bytes at sp+0x204
      and the r4 spill are ADS artifacts. match.py structural
      (exit-1 diff expected): same ctor -> serialize ->
      deserialize three-call shape with the buffer in sp-relative
      r0 and the ctor result threaded r4 -> r1; the diffs are
      the crate fp frame, the blx seam dispatches (0x080539f0/
      0x082596f4 visible as literal words) and an
      __aeabi_memclr4 for the zero-init (the documented
      deviations).

  - address: 0x082a19ec
    ghidra: FUN_082a19ec
    name: pmu_reg87_bit1_clear
    size: 20
    sig: "bool pmu_reg87_bit1_clear(void)"
    module: fp/fp_misc
    status: ported
    vars:
      iVar1: query_status
      thunk_FUN_08086e4c: pmu_reg87_bit1_query (the veneer @
        0x0805381c, `b 0x08086e4c`; target FUN_08086e4c @ 0x08086e4c,
        28 bytes, unported - behind the PMU_REG87_BIT1_QUERY seam)
      FUN_082e55dc: pmu_reg87_bit1_read (68 bytes, unported - the
        mutexed single-register PMU read the query runs)
    notes: >-
      No bl call sites in osos.asm and no pointer to 0x082a19ec
      anywhere in osos.dec (binary-scanned) - reached indirectly,
      like its neighbors query_name_to_cxx_string @ 0x082a1918 and
      context_text_to_cxx_string @ 0x082a19bc (vtable or computed
      table outside the image body); middle member of that
      indirect diagnostics run 0x082a1918-0x082a1a6c. The whole
      body is stmdb sp!,{r4,lr}; bl 0x0805381c; cmp r0,#0; movne
      r0,#1; ldmia sp!,{r4,pc} - booleanizes the PMU
      register-0x87 bit-1 query FUN_08086e4c (reached through the
      4-byte veneer thunk_FUN_08086e4c @ 0x0805381c, binary-
      verified `b 0x08086e4c`), which stages a u32 stack slot,
      runs FUN_082e55dc @ 0x082e55dc (sem 0x11/5 lock pair around
      the raw transfer FUN_0836d3b8: PCF50635 register 0x87, one
      byte, (byte & 2) >> 1 into the slot, transfer status
      returned) and answers 1 when the extracted bit is 0, else 0
      (rsbs r0,r0,#1 / movcc r0,#0: slot 0 -> 1, slot >= 1 -> 0).
      So this function answers whether that flag is clear. The
      concrete meaning of PMU register 0x87 bit 1 is NOT
      recovered: 0x87 lies outside the public PCF5063x register
      map (ends at DCDCPFM 0x84) and Rockbox's ipod6g PMU driver
      never touches it; the query's other consumers FUN_080617c0
      (early-returns when clear, after mapping a charger/
      accessory-kind enum through a byte table) and FUN_081a5940
      (flags bit 0x200 + flag clear -> result code 0xf) point at
      a charger/accessory status flag without pinning it down.
      The query callee is unported firmware behind the
      host-swappable PMU_REG87_BIT1_QUERY usize seam (this
      module's FIXED16_RECIPROCAL pattern): the target build
      transmutes 0x08086e4c (the veneer folds away - a bare b),
      host tests install recording mocks. Deviations: the pushed
      r4 is never used (gratuitous ADS frame spill); the
      cmp/movne pair is the ADS != 0 bool conversion, modeled by
      Rust's bool return (every nonzero query value -> true,
      pinned by host tests over 0/1/2/-1/INT_MIN/INT_MAX).
      match.py structural (exit-1 diff expected): same call ->
      cmp r0,#0 -> movne r0,#1 shape; the diffs are the crate fp
      frame and the blx seam dispatch (0x08086e4c visible as a
      literal word), the documented seam deviations.

  - address: 0x082a1a04
    ghidra: FUN_082a1a04
    name: context_short_text_to_cxx_string
    size: 48
    sig: "void context_short_text_to_cxx_string(string: *mut *mut u8)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: string
      uVar1: string (the default-constructor's r0 return, kept in
        r4 and threaded into the deserializer)
      auStack_20c: counted (the 512-byte counted-u16 staging buffer
        at sp+0x4, a u16 codepoint count plus up to 0xff u16 units)
      auStack_c: DEAD SLOT (sp+0x204; the add r1,sp,#0x204 aiming at
        it is clobbered by the constructor's ldr r1,[0x83d8c2c] -
        ADS scheduling artifact, no reader)
      FUN_083d8c20: cxx_string_default_ctor (ported, cxx/string)
      FUN_08054d78: context_short_text_to_counted_u16 (unported,
        behind the CONTEXT_SHORT_TEXT_TO_COUNTED_U16 seam)
      FUN_082596f4: counted_u16_to_cxx_string (unported, behind the
        COUNTED_U16_TO_CXX_STRING seam - the SAME seam
        context_text_to_cxx_string uses, no duplicate)
    notes: >-
      No bl call sites in osos.asm and no pointer to 0x082a1a04
      anywhere in osos.dec (binary-scanned) - reached indirectly,
      like its neighbors query_name_to_cxx_string @ 0x082a1918,
      context_text_to_cxx_string @ 0x082a19bc and
      pmu_reg87_bit1_clear @ 0x082a19ec (vtable or computed table
      outside the image body); fourth member of that indirect
      diagnostics run 0x082a1918-0x082a1a6c. Instruction-for-
      instruction twin of context_text_to_cxx_string except the
      serializer address: hands the shared context's SHORT text
      field back as a COW basic_string<char>, staged through the
      same counted-u16 (UTF-16) stack buffer:
      cxx_string_default_ctor @ 0x083d8c20 (ported) parks *string
      on the shared empty rep; FUN_08054d78 @ 0x08054d78 (104
      bytes, unported) fetches the shared context (lazy getter
      0x08369bec) and, when the first byte of the C string at
      context+0x18 is 0xff (field unset), stores a zero u16 count
      (moveq r0,#0 / strheq); otherwise it measures the string
      (strlen @ 0x08392478, ported), masks the length to a byte
      and truncates to 0x1f (and r0,r0,#0xff / cmp r0,#0x1f /
      movhi r0,#0x1f), stages the payload byte-counted at sp+0x4
      (count byte, then the bytes via __rt_memcpy through the ROM
      veneer @ 0x08037db0, ported - Ghidra mislabels the copy
      thunk_EXT_FUN_22000020) and expands byte-counted ->
      counted-u16 through FUN_08046a88 @ 0x08046a88 (zero count,
      FUN_08046b44 expansion with the 0xff ceiling, count store);
      FUN_082596f4 @ 0x082596f4 (72 bytes, unported) then rebuilds
      a StringObject from the counted units (FUN_082773b4 +
      FUN_08276db4) and assigns its c_str into *string through
      cxx_string_assign_cstr @ 0x083d8ca0, unconditionally (count
      0 included). The field is the second of the four fillers of
      the about/diagnostics record at 0x08112800 (bl 0x08054d78 @
      0x0811280c; siblings 0x08052228 dotted-triple version from
      context+0x4c, 0x08054ea0, 0x080539f0 - see
      object_set_version_text's entry); the concrete field is not
      recovered - a <=31-byte identifier string at context+0x18.
      The two text-marshaling callees sit behind host-swappable
      usize seams (the new CONTEXT_SHORT_TEXT_TO_COUNTED_U16 and
      the SHARED COUNTED_U16_TO_CXX_STRING, this module's
      FIXED16_RECIPROCAL pattern): the target build transmutes
      the fixed firmware addresses, host tests install recording
      mocks. Deviations: the staging buffer is zero-initialized
      where the original leaves it uninitialized (unobservable -
      the serializer writes the count and every unit the
      deserializer reads); the dead add r1,sp,#0x204, the 4 spare
      frame bytes at sp+0x204 and the r4 spill are ADS artifacts
      (the twin's deviations). match.py structural (exit-1 diff
      expected): same ctor -> serialize -> deserialize three-call
      shape as the twin, buffer in sp-relative r0, ctor result
      threaded r4 -> r1; the diffs are the crate fp frame, the
      blx seam dispatches (0x08054d78/0x082596f4 visible as
      literal words) and an __aeabi_memclr4 for the zero-init
      (the documented deviations).

  - address: 0x082a1a40
    ghidra: FUN_082a1a40
    name: version_info_to_cxx_string
    size: 48
    sig: "void version_info_to_cxx_string(string: *mut *mut u8)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: string
      uVar1: string (the default-constructor's r0 return, kept in
        r4 and threaded into the deserializer)
      auStack_20c: counted (the 512-byte counted-u16 staging buffer
        at sp+0x4, a u16 codepoint count plus up to 0xff u16 units)
      auStack_c: DEAD SLOT (sp+0x204; the add r1,sp,#0x204 aiming at
        it is clobbered by the constructor's ldr r1,[0x83d8c2c] -
        ADS scheduling artifact, no reader)
      FUN_083d8c20: cxx_string_default_ctor (ported, cxx/string)
      FUN_08054ea0: version_info_to_counted_u16 (unported, behind
        the VERSION_INFO_TO_COUNTED_U16 seam)
      FUN_082596f4: counted_u16_to_cxx_string (unported, behind the
        COUNTED_U16_TO_CXX_STRING seam - the SAME seam both
        context-text twins use, no duplicate)
    notes: >-
      No bl call sites in osos.asm and no pointer to 0x082a1a40
      anywhere in osos.dec (binary-scanned) - reached indirectly,
      like every other member of the indirect diagnostics run
      0x082a1918-0x082a1a6c (vtable or computed table outside the
      image body); fifth and last member of that run.
      Instruction-for-instruction twin of
      context_short_text_to_cxx_string @ 0x082a1a04 except the
      serializer address: hands the build VERSION text back as a
      COW basic_string<char>, staged through the same counted-u16
      (UTF-16) stack buffer: cxx_string_default_ctor @ 0x083d8c20
      (ported) parks *string on the shared empty rep; FUN_08054ea0
      @ 0x08054ea0 (104 bytes, unported; 2 bl call sites:
      0x081127ac in the about/diagnostics record builder and
      0x082a1a58 here) constructs a 28-byte stack-local
      version-info object (FUN_08165b84 @ 0x08165b84 with args
      (2, 4, 0x80, 0), internally through FUN_081659cc and
      FUN_082779c4 - the concrete class is not recovered), reads
      FIVE vtable getters (+0x8..+0x18) off it and formats a
      dotted version into a 100-byte stack buffer with the ported
      snprintf @ 0x0802f768: "%d.%d.%d" when the third getter is
      nonzero, "%d.%d" when it is zero, each gaining a "%s%d"
      suffix (suffix word from an 8-byte-stride table whose base
      literal is 0x083e941c, indexed by the fourth getter) unless
      the fourth getter is 5 AND the fifth is 0 - the release
      form, matching this firmware's "2.0.4"; the suffix-table
      base lands INSIDE the __rt_memcpy copy-loop code region, so
      the suffixed forms are believed unreachable on retail units
      and the suffix strings are not recovered. The formatted
      text is staged byte-counted (ported cstr_to_counted_u8 @
      0x08045f14, limit 0xff) and expanded into counted u16 by
      FUN_08046a88 @ 0x08046a88, then the object is destroyed
      (FUN_08165bfc @ 0x08165bfc, a no-op mov r0,#0; bx lr);
      FUN_082596f4 @ 0x082596f4 (72 bytes, unported) then rebuilds
      a StringObject from the counted units (FUN_082773b4 +
      FUN_08276db4) and assigns its c_str into *string through
      cxx_string_assign_cstr @ 0x083d8ca0, unconditionally (count
      0 included). The field is the third of the four fillers of
      the about/diagnostics record (siblings 0x08052228
      dotted-triple version from context+0x4c, 0x08054d78 short
      text, 0x080539f0 context text - see object_set_version_text's
      entry). The two text-marshaling callees sit behind
      host-swappable usize seams (the new
      VERSION_INFO_TO_COUNTED_U16 and the SHARED
      COUNTED_U16_TO_CXX_STRING, this module's FIXED16_RECIPROCAL
      pattern): the target build transmutes the fixed firmware
      addresses, host tests install recording mocks. Deviations:
      the staging buffer is zero-initialized where the original
      leaves it uninitialized (unobservable - the serializer
      writes the count and every unit the deserializer reads); the
      dead add r1,sp,#0x204, the 4 spare frame bytes at sp+0x204
      and the r4 spill are ADS artifacts (both twins'
      deviations). match.py structural (exit-1 diff expected):
      same ctor -> serialize -> deserialize three-call shape as
      the twins, buffer in sp-relative r0, ctor result threaded
      r4 -> r1; the diffs are the crate fp frame, the blx seam
      dispatches (0x08054ea0/0x082596f4 visible as literal words)
      and an __aeabi_memclr4 for the zero-init (the documented
      deviations).

  - address: 0x082a1a70
    ghidra: FUN_082a1a70
    name: board_version_halves
    size: 32
    sig: "void board_version_halves(_this: *mut u8, high: *mut u16, low: *mut u16)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: _this (never read - the original never touches r0; an
        unused leading argument where every sibling in the diagnostics
        run takes exactly what it uses, consistent with an implicit
        this, concrete class not recovered)
      param_2: high
      param_3: low
      uVar1: version (the board_version query's r0 return)
      thunk_FUN_080e624c: board_version (the veneer @ 0x080515e4,
        binary-verified word 0xea025318 = `b 0x080e624c`; target
        FUN_080e624c @ 0x080e624c, 44 bytes, unported - behind the
        BOARD_VERSION seam; the SAME lazily cached system-info word
        cg_timer_wait's entry names board_version)
    notes: >-
      No bl call sites in osos.asm and no pointer to 0x082a1a70
      anywhere in osos.dec (binary-scanned) - reached indirectly,
      like its five neighbors in the indirect diagnostics run
      0x082a1918-0x082a1a6c (vtable or computed table outside the
      image body); sits immediately after that run, the u16-pair
      splitter pmu_reg87_bit1_clear's ledger entry anticipates.
      The whole body is stmdb sp!,{r4,r5,r6,lr}; mov r5,r2; mov
      r4,r1; bl 0x080515e4; mov r1,r0,lsr #0x10; strh r1,[r4,#0x0];
      strh r0,[r5,#0x0]; ldmia sp!,{r4,r5,r6,pc} - queries the
      lazily cached board-version word FUN_080e624c @ 0x080e624c
      (44 bytes, unported: cached word at +0x8 of the struct the
      literal at 0x080e6278 points to, refreshed from context+0x84
      of the 0x08369bec shared-context object when it still holds
      the 0x7fffffff sentinel - cmn r0,#0x80000001 / bne) and
      stores the HIGH halfword into *high, the LOW halfword into
      *low. The same word drives cg_timer_wait's channel pick
      (HIGH halfword 0x10 with LOW halfword >= 6 selects timer
      channel 3). The query callee sits behind the host-swappable
      BOARD_VERSION usize seam (this module's FIXED16_RECIPROCAL
      pattern): the target build transmutes the fixed firmware
      address 0x080e624c (the veneer @ 0x080515e4 folds away - a
      bare b), host tests install recording mocks. Deviations: the
      pushed r6 is never used (gratuitous ADS frame spill); the
      halfword stores are plain strh, modeled by Rust's aligned
      u16 writes, HIGH first. match.py structural (exit-1 diff
      expected): same query -> lsr r1,r0,#16 -> strh high -> strh
      low shape; the diffs are the crate fp frame and the blx seam
      dispatch (0x080e624c visible as a literal word), the
      documented seam deviations.

  - address: 0x082a1b2c
    ghidra: FUN_082a1b2c
    name: current_datetime_to_record
    size: 100
    sig: "void current_datetime_to_record(record: *mut u8)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: record (10-byte compact date/time output)
      iVar1: calendar_valid (the normalized bit-1 status returned by
        FUN_08056524; zero calls heap_panic and never returns)
      auStack_1c: calendar (20-byte query record at sp+0xc; its
        offsets +2..+6 are second/minute/hour/day/month and +8..+9
        are the little-endian year)
      local_28: packed_record (10-byte output staging at sp+0; the
        five calendar bytes and year are assigned individually,
        leaving its three padding bytes uninitialized in retailOS)
      FUN_08056524: current_datetime_query (unported 200-byte clock
        calendar-record builder behind the CURRENT_DATETIME_QUERY seam)
      FUN_08030f44: heap_panic (ported, non-returning)
      thunk_EXT_FUN_22000020: __rt_memcpy (the ported ROM veneer
        thunk @ 0x08037db0)
    notes: >-
      No bl call sites in osos.asm: reached indirectly. Allocates a
      0x20-byte frame, asks FUN_08056524 @ 0x08056524 for a 20-byte
      calendar record at sp+0xc, and checks its 0/1 valid status with
      cmp r0,#0 / bleq heap_panic @ 0x08030f44. On success, it copies
      calendar[2..=6] to packed_record[0..=4] and calendar[8..=9] to
      packed_record[6..=7], then calls __rt_memcpy @ 0x08037db0 for
      all ten output bytes. The same extraction appears at 0x0808ea00
      before FUN_0807ea68's weekday calculation, establishing
      second/minute/hour/day/month/year field meanings. The three
      unassigned output slots are ABI padding: the retail stack bytes
      are indeterminate before memcpy; the Rust port deliberately
      zero-initializes them, following this module's existing staging
      buffer convention. FUN_08056524 remains unported behind a
      host-swappable usize seam (target calls fixed address directly,
      host tests install a recording mock). Host tests verify a single
      query, exact field extraction, zeroed padding, and live results
      on consecutive calls. match.py structural (exit-1 diff expected):
      query -> zero-status fatal gate -> byte/halfword field staging ->
      __rt_memcpy; expected differences are the seam blx dispatch,
      zero-initialization, and Rust frame layout.

  - address: 0x082a1b90
    ghidra: FUN_082a1b90
    name: rtc_context_b1c_probe_one
    size: 20
    sig: "u32 rtc_context_b1c_probe_one(void)"
    module: fp/fp_misc
    status: ported
    vars:
      FUN_080c6348: service_context_get (unported 32-byte asserting
        getter of the system root's +0x30 slot, behind the
        SERVICE_CONTEXT_GET seam)
      FUN_0805129c: rtc_context_b1c_halfword (unported 16-byte getter
        of the u16 at nested-context(+0xf00)+0xb1c, behind the
        RTC_CONTEXT_B1C_HALFWORD seam)
      FUN_0807f254: system_root_get (the asserting root getter
        0x080c6348 wraps; literal pool 0x089ca674)
      FUN_08030f44: heap_panic (both assertions' fatal tail)
    notes: >-
      No bl call sites in osos.asm and no pointer to the address
      anywhere in osos.dec (binary-scanned): reached indirectly, a
      vtable or computed table outside the image body - like the whole
      diagnostics run 0x082a1918-0x082a1b2c above it. Ghidra's 20-byte
      size is exact here (binary-verified): five instructions, `stmdb
      sp!,{r4,lr}; bl 0x080c6348; bl 0x0805129c; mov r0,#1; ldmia
      sp!,{r4,pc}`, the next function starting at 0x082a1ba4.
      Algorithm: fetch the service context through FUN_080c6348 @
      0x080c6348 - the asserting getter of the system root's +0x30 slot
      (root from FUN_0807f254's 0x089ca674 word, both NULLs fatal via
      heap_panic @ 0x08030f44; the +0x30 slot is the service context
      app/scoped_context.rs's ROOT_SERVICE_CONTEXT_SLOT names) - then
      read the u16 at nested-context(+0xf00)+0xb1c through FUN_0805129c
      @ 0x0805129c (time/rtc.rs's RtcContextOwner/RTC-context layout),
      DISCARD the read, and return a constant 1. The discarded read
      survives because ADS cannot elide a call it cannot prove const;
      its only observable effect is walking (and thereby validating)
      the service-context chain. The constant-1 return is consistent
      with a diagnostics availability/entry-count callback paired with
      a sibling formatter in the run, but the table is outside the
      image, so the concrete role is NOT recovered. The +0xb1c
      halfword's meaning is likewise NOT recovered: its companion
      setter FUN_08066cf0 @ 0x08066cf0 mirrors it into the global @
      0x089ca58c+0x4 and runs the change notify @ 0x0805e66c when it
      moves, and the state poller FUN_08172c6c caches it at its +0x52
      and publishes events 0x6097/0x609b/0x60a0 when it changes (the
      same poller publishes 0x60ae for rtc_context_status's +0xb24
      byte). FUN_0805129c's 4 other bl call sites: 0x0812f3b0 (masks
      the value bic #0xc / orr #4 and feeds the setter), 0x08173050 /
      0x08173064 (the poller), 0x0825b71c (the re-publish wrapper @
      0x0825b710, which reads the halfword and tail-calls the setter
      with it). Deviations: both callees are unported firmware behind
      host-swappable usize seams (the module's FIXED16_RECIPROCAL
      pattern) - the target build transmutes 0x080c6348 and 0x0805129c,
      host tests install recording mocks; the pushed r4 is never used
      (an ADS frame artifact), the port keeps no frame. Host tests (3):
      service-context fetch BEFORE the halfword read with the getter's
      exact pointer threaded into the reader (one shared event log),
      the constant-1 return across every halfword boundary shape the
      reader can produce (the read is discarded), and live
      service-context pointers re-threaded on consecutive calls.
      match.py structural (exit-1 diff expected): push; call; call;
      mov r0,#1; pop on both sides - expected differences are the seam
      blx-via-literal-pool dispatch and LLVM's push {fp,lr}/pop {fp,pc}
      frame versus the ADS {r4,lr} spill.

  - address: 0x082a1ba4
    ghidra: FUN_082a1ba4
    name: class6000_ui32_resource_60a4
    size: 24
    sig: "void *class6000_ui32_resource_60a4(void)"
    module: fp/fp_misc
    status: ported
    vars:
      FUN_08172124: instance_of_class_6000 (ported, app/registry)
      FUN_0827216c: resource_chain_find (ported, app/resource_chain)
      DAT_082a1bbc: RESOURCE_ID_60A4 (0x000060a4, the resource id, r2)
      DAT_082a1bc0: RESOURCE_KIND_UI32 (0x55693332, "Ui32" big-endian,
        the resource kind, r1)
    notes: >-
      No bl call sites in osos.asm and no pointer to the address
      anywhere in osos.dec (binary-scanned): reached indirectly, a
      vtable or computed table outside the image body - like the whole
      diagnostics run 0x082a1918-0x082a1ba4 above it. Extent
      binary-verified: 24 bytes of code (functions.csv's size is
      exact), six instructions ending `b 0x0827216c` at 0x082a1bb8,
      followed by the two-word literal pool @ 0x082a1bbc (0x000060a4)
      and 0x082a1bc0 (0x55693332) for a true extent of 32 bytes ending
      where FUN_082a1bc4 begins at 0x082a1bc4.
      Algorithm: `stmdb sp!,{r4,lr}; bl 0x08172124; ldr r2,=0x000060a4;
      ldr r1,=0x55693332; ldmia sp!,{r4,lr}; b 0x0827216c` - resolve
      the registered class-0x6000 singleton through the ported
      instance_of_class_6000 (registry lookup + checked downcast; the
      class registers itself @ 0x08173774 but never names itself to the
      factory, so it is NOT identified) and tail-branch to the ported
      resource_chain_find with that instance as the provider-chain
      head, asking for the resource (kind "Ui32", id 0x60a4). So the
      body is "fetch the Ui32-typed entry 0x60a4 out of the
      class-0x6000 object's resource-provider chain", answering the
      owning provider's raw pointer - NULL when the class is
      unregistered, the cast refuses, or no provider owns the id.
      Ghidra's decompile inlines the tail-called resource_chain_find
      body into this function; the listing pins the real shape. What
      0x60a4 IS is not recovered, but the image gives it a family: the
      same word @ 0x0817360c sits in a table of 0x60xx ids (0x6097,
      0x609b, 0x60a0, 0x60a4, 0x60a6, 0x60a7, 0x60a8, 0x60ac, 0x60ae,
      ...) interleaved with the "BMap"/"Bool" kind literals beside the
      class-0x6000 registration @ 0x08173774 - the same 0x60xx family
      the state poller FUN_08172c6c publishes change events for - and
      the setter-side sibling @ 0x0825b774 runs instance_of_class_6000
      and then the 4-argument chain variant @ 0x08272230 (ported
      alongside as resource_chain_write in app/resource_chain.rs) with
      the SAME
      (kind "Ui32", id 0x60a4) pair (literals @ 0x0825b7ac/
      0x0825b7b0), i.e. 0x60a4 is a mutable Ui32 state entry of the
      class-0x6000 store. Deviations: both callees are ported and
      called directly, no seam (the resource_chain_find_string/_bitmap
      precedent); the "Ui32" ResourceKind constant is kept local to
      fp/fp_misc.rs (the fourcc word appears at ~40 spots in the
      decrypted image, binary-scanned, but this port is its first
      decoded consumer); the pushed r4 is never used (an ADS frame
      artifact) and the port keeps no frame. Host tests (6): the
      registered instance heading a two-node provider chain walked in
      order with the ("Ui32", 0x60a4) pair forwarded verbatim to every
      provider and the second provider's answer returned; the first
      answer stopping the walk with kind and id asserted against the
      raw literals; nobody answering -> NULL; an unregistered class ->
      NULL with no cast and no find; a registered instance whose own
      cast refuses 0x6000 -> NULL with the find slot never dispatched;
      and the kind literal spelling "Ui32" big-endian. match.py
      structural (exit-1 diff expected): push; bl accessor; set up
      (kind, id); pop; tail b on both sides - expected differences are
      LLVM's push {fp,lr}/pop {fp,pc} crate frame versus the ADS
      {r4,lr} spill and LLVM synthesizing the 0x60a4 id with a
      mov/orr pair instead of a literal-pool load (the 0x55693332 kind
      stays a pooled literal on both sides).

  - address: 0x0825c750
    ghidra: FUN_0825c750
    name: plist_node_ctor
    size: 64
    sig: "PlistNode *plist_node_ctor(PlistNode *node, const char *const *tag)"
    module: fp/fp_misc
    status: ported
    renames:
      param_1/r0: node
      param_2/r1: tag
      FUN_083d8c30: cxx_string_copy_ctor
      FUN_083d8c20: cxx_string_default_ctor
    notes: >-
      Raw ARM is exactly 16 instructions from 0x0825c750 through 0x0825c78c;
      the sibling plist_node_destroy starts at 0x0825c790 with no literal
      pool. Full osos.dec ARM B/BL-word decoding finds 15 incoming bl sites,
      all unconditional: no predicated calls and no tail branches. The
      constructor COW-copies tag into +0x00, clears the +0x04 three-word
      attribute vector, default-constructs the +0x10 value string, clears the
      +0x14 three-word child vector, and zeros the +0x20 companion and +0x24
      kind. The COW copy crosses a private noinline front-end so LLVM retains
      the source bl boundary to the established cxx_string_copy_ctor port; the
      default-string callee is also ported. Its return is adjusted back 16
      bytes, so this returns node.
      Host tests verify a non-empty tag COW refcount acquisition, return
      identity, and every poisoned target field; the empty-tag case verifies
      singleton preservation and all zeroed fields. match.py review is
      structural; exact instruction identity is not expected.

  - address: 0x0825c790
    ghidra: FUN_0825c790
    name: plist_node_destroy
    size: 144
    sig: "PlistNode *plist_node_destroy(PlistNode *node)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: node
      iVar2: companion
      local_18: attributes
      local_24: children
    renames:
      FUN_083e3284: plist_node_child_range_destroy
      FUN_083e35a4: cxx_record_range_destroy_8
      FUN_083d8b04: cxx_string_release
      FUN_08266f2c: cxx_array_dealloc
      FUN_082aad24: operator_delete
    notes: >-
      Raw ARM extent is 144 bytes: 36 instructions at 0x0825c790..0x0825c820,
      ending immediately before the distinct sibling FUN_0825c824, with no
      literal pool; Ghidra's 148-byte size is four bytes too large. A full
      osos.dec ARM B/BL-word decode finds 19 incoming bl sites, all
      unconditional (including recursive bl at 0x0825c7a4), plus one
      unconditional tail b at 0x081c8334 and no predicated calls. The
      non-deleting plist/XML-node destructor first recursively destroys and
      operator_delete's the owned +0x20 processing-instruction companion,
      tears down the +0x14 child vector as 40-byte nodes through unported
      FUN_083e3284 then cxx_array_dealloc's its storage, releases the +0x10
      COW value string, tears down/frees the +0x04 8-byte attribute-pair
      vector, releases the +0x00 tag string, and returns node. The sole
      unported direct callee is explicit PLIST_NODE_DESTROY_OPS: target calls
      the verified address 0x083e3284; host tests replace it with a recorder.
      The cxx_array_dealloc count arguments are faithfully computed from the
      capacity-minus-begin spans but dead at its already-ported tail veneer.

  - address: 0x082a0074
    ghidra: FUN_082a0074
    name: rgb8_to_rgb565
    size: 40
    sig: "u32 rgb8_to_rgb565(const u8 *components)"
    module: ui/rgb565_pack
    status: ported
    renames:
      param_1/r0: components (three source bytes in R, G, B order)
    notes: >-
      Raw ARM extent is exactly 40 bytes: ten instructions from
      0x082a0074 through the bx lr at 0x082a0098, immediately before
      the separate RGBA4444 packer at 0x082a009c; no literal pool.
      Full osos.dec ARM B/BL-word decoding finds eight incoming bl
      sites (0x08250bc0, 0x08251054, 0x08251448, 0x082533b4,
      0x08253478, 0x08256748, 0x08256754, and 0x0825ec68), all
      unconditional, with no predicated or tail-branch callers.
      It reads three RGB8 bytes, preserves their upper R5/G6/B5 bits,
      and packs them into bits 15..11, 10..5, and 4..0 respectively.
      No NULL or bounds guard exists. The Rust port keeps the ARM u32
      return-register representation and adds no validation. Host
      tests pin full-intensity primary colors, each field position,
      low-bit discard, and an offset source record. match.py structural
      review is expected to differ from ADS code generation.
  - address: 0x082a009c
    ghidra: FUN_082a009c
    name: rgba8_to_rgba4444
    size: 48
    sig: "u32 rgba8_to_rgba4444(const u8 *components)"
    module: ui/rgba4444_pack
    status: ported
    renames:
      param_1/r0: components (four source bytes in R, G, B, A order)
    notes: >-
      Raw ARM extent is exactly 48 bytes: twelve instructions from
      0x082a009c through the bx lr at 0x082a00c8, immediately before
      the separate RGB555A1 packer at 0x082a00cc; no literal pool.
      Full osos.dec ARM B/BL-word decoding finds eight incoming bl
      sites (0x08250d60, 0x08251174, 0x08251558, 0x08253380,
      0x08253448, 0x0825675c, 0x08256768, and 0x0825ec9c), all
      unconditional, with no predicated or tail-branch callers.
      It reads four RGBA8 bytes, preserves their high nibbles, and
      packs them in descending byte order into the low 16 bits:
      R -> bits 15..12, G -> 11..8, B -> 7..4, A -> 3..0. No NULL
      or bounds guard exists. The Rust port keeps the ARM u32 return
      register representation and adds no validation. Host tests pin
      all four component positions, low-nibble discard, an offset
      record, and the all-set result. match.py structural review is
      expected to differ from ADS code generation.

  - address: 0x082a1bc4
    ghidra: FUN_082a1bc4
    name: plist_node_child_count
    size: 20
    sig: "i32 plist_node_child_count(const u8 *node)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: node (the plist node; embedded child vector at +0x14/+0x18)
      FUN_08031568: __rt_sdiv (ported, runtime/rt_div)
    notes: >-
      12 direct call sites, all in the plist code: bl at 0x0816765c,
      0x081676d8, 0x0829c170, 0x0829c1ac, 0x0829c1cc, 0x0829c22c,
      0x0829c24c, 0x0829c2f8, 0x0829c318, 0x0829c380 and 0x0829c3a4,
      plus the beq tail-call at 0x081676cc; no pointer to the address
      anywhere in osos.dec (binary-scanned). Extent binary-verified:
      20 bytes exactly (functions.csv's size is exact), five
      instructions ending `b 0x08031568` at 0x082a1bd4 with the next
      function starting at 0x082a1bd8 - NO literal pool, unlike the
      class6000_ui32_resource_60a4 literal-pool tail just above.
      Algorithm: `ldr r1,[r0,#0x18]; ldr r0,[r0,#0x14]; sub r0,r1,r0;
      mov r1,#0x28; b 0x08031568` - take the span between the embedded
      child vector's begin (+0x14) and end (+0x18) word pointers and
      signed-divide it by 0x28 = 40 through the ADS signed divide
      (ported as __rt_sdiv), answering the number of 40-byte child
      records. This is vector<T>::size() for a 40-byte element (the
      cxx/templates vector_size_elem* family), except the {begin, end}
      pair sits at +0x14/+0x18 of an enclosing object instead of
      heading it. The enclosing object is a plist tree node: callers
      compare node tags against the string literals "plist" (@
      0x08167698), "dict" (@ 0x081676a0), "key" (@ 0x0829c184) and
      "array" (@ 0x0829c29c), all binary-verified in osos.dec, walk
      children at a 0x28 stride with a C-string pointer at child+0x10,
      and FUN_081676a8 returns the count unchanged for node kind byte
      1 (array) but count>>1 for kind 2 (a dict's interleaved
      key/value children halved to pair count); FUN_08167638
      recognizes the canonical <plist><dict> shape (tag "plist",
      exactly one child, first child's tag "dict"). Ghidra's decompile
      inlines the whole tail-called __rt_sdiv body (sign fixup, clz
      dispatch, shift-subtract loop) into FUN_082a1bc4; the listing
      pins the real 20-byte shape (the class6000_ui32_resource_60a4
      resource_chain_find inlining precedent). Deviations: the ADS
      divide also leaves the remainder in r1, but no caller reads it,
      so the port answers the quotient only (the vector_size_elem*
      precedent); the port reads both fields with read_unaligned (the
      cxx/templates host-testing idiom against a 4-but-not-8-aligned
      node). Host tests (4): exact counts 0..8 for whole children; a
      partial element dropped (span 39 -> 0, 41 -> 1, 79 -> 1, 80 ->
      2); a reversed span truncating toward zero (-40 -> -1, -41 ->
      -1, -39 -> 0); and a poisoned-header test pinning the reads to
      offsets +0x14/+0x18 exactly. match.py structural (exit-1 diff
      expected): identical deviation profile to the accepted
      vector_size_elem40 port - the ldr pair comes out as byte loads
      from the read_unaligned host-testing idiom, with the same sub,
      the same `mov r1,#0x28` and the same tail branch to __rt_sdiv
      (match.py renders the relocated branch as `b 0 <symbol>` for
      both ports).

  - address: 0x082a1bd8
    ghidra: FUN_082a1bd8
    name: plist_node_processing_instruction
    size: 8
    sig: "u8 *plist_node_processing_instruction(const u8 *node)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: node (the plist node; companion-node pointer at +0x20)
    notes: >-
      One bl call site (0x0815aec0, in FUN_0815aeac); no pointer to
      the address anywhere in osos.dec (binary-scanned). Extent
      binary-verified: 8 bytes exactly (functions.csv's size is
      exact), two instructions `ldr r0,[r0,#0x20]; bx lr` ending at
      0x082a1bdc with the next function starting at 0x082a1be0 - no
      literal pool. Algorithm: answer the plist node's +0x20 word, a
      pointer to the node's processing-instruction companion node
      (NULL when none was parsed). The plist node is a 0x28-byte
      record, layout binary-verified from the stack-node constructor
      at 0x0815a810-0x0815a848 (two cxx_string_default_ctor calls
      plus zero stores): +0x00 tag string, +0x04/+0x08/+0x0c
      attribute string-pair vector, +0x10 value string,
      +0x14/+0x18/+0x1c child vector (the plist_node_child_count
      span), +0x20 this companion pointer, +0x24 kind byte. The
      field is filled only by the lazy accessor @ 0x0825c264, which
      operator_new(0x28)'s a second record in the same empty-node
      shape (both strings parked on the shared empty rep data
      0x08b31810 - the cxx_string_default_ctor literal) and stores
      it at node+0x20; that accessor's only caller is the XML
      parser's processing-instruction routine @ 0x0825ceac, which
      parses `<?target attr="..." ?>` (the '?' = 0x3f terminator at
      0x0825cf08; the entity literals "lt"/"gt"/"amp"/"apos"/
      "quot"/"nbsp"/"#xA" @ 0x0825ce84-0x0825ceaa binary-verified)
      into the companion. The node destructor @ 0x0825c790 recurses
      into +0x20 (the companion is owned). The sole consumer
      FUN_0815aeac gates on it: a NULL companion, or wrong values
      for the two attribute keys it probes via cxx_vector_find_equal
      (ported, cxx/templates), rejects the document before the
      node's own tag and attribute map are consulted. Deviations:
      the load is a plain ABI-aligned ldr; the port reads it with
      read_unaligned, the cxx/templates.rs host-testing idiom
      against a 4-but-not-8-aligned node (the
      plist_node_child_count precedent). Host tests (3): the stored
      companion word forwarded untouched (three sentinels incl.
      0xfffffffc); NULL answered for a zeroed field (the no-PI case
      the consumer rejects); and a poisoned-record test pinning the
      read to offset +0x20 exactly (0xa5 at +0x1c and the +0x24 kind
      byte). match.py structural (exit-1 diff expected): identical
      deviation profile to the accepted plist_node_child_count port
      - the ldr comes out as four byte loads merged with orr shifts
      from the read_unaligned host-testing idiom, plus the crate fp
      frame; same straight-line no-branch getter shape as the
      original.

  - address: 0x082a1be0
    ghidra: FUN_082a1be0
    name: nibble_stream_current
    size: 28
    sig: "u8 nibble_stream_current(const u8 *state)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: state (the nibble-stream state; cursor word at +0,
        low-half flag byte at +4)
    notes: >-
      Two bl call sites, both in the pixel-format converter
      FUN_08253158 (0x08253378 and 0x082533ac, the two 4bpp decode
      loops); no pointer to the address anywhere in osos.dec
      (binary-scanned). Extent binary-verified against osos.dec: 28
      bytes exactly (functions.csv's size is exact), seven
      instructions `ldr r1,[r0]; ldrb r0,[r0,#4]; ldrb r1,[r1]; cmp
      r0,#0; moveq r0,r1,lsr#4; andne r0,r1,#0xf; bx lr` ending at
      0x082a1bf8 with the next function starting at 0x082a1bfc - no
      literal pool. Algorithm: read the current nibble of a packed
      4-bit element stream without advancing it. The state is an
      8-byte record { +0 word cursor into the packed bytes, +4
      low-half flag byte }; the body answers *cursor >> 4 when the
      flag is 0, *cursor & 0xf otherwise - high nibble first, then
      the low nibble of the same byte. The paired advance routine @
      0x0825e3e8 (eleven instructions, binary-verified) toggles the
      flag and steps the cursor only after the low nibble was
      consumed, so the pair walks a byte array as a big-endian-nibble
      stream. Both callers sit in FUN_08253158's 4-bit-per-pixel
      decode loops: the state lives on the stack at sp+0x18 (cursor
      seeded from the packed source pointer, flag byte zeroed at
      0x08253358), each iteration feeds the nibble into a
      4-byte-entry palette (`add r0,r6,r0,lsl #2`), converts through
      FUN_082a009c/FUN_082a0074, stores a u16 pixel, then advances
      via 0x0825e3e8. Despite landing between the two plist node
      accessors (0x082a1bc4/0x082a1bd8), this record is NOT a plist
      node - the +0/+4 layout and nibble arithmetic pin it as the
      converter's nibble cursor. Deviations: the cursor is loaded as
      a NATIVE-width pointer and the flag byte addressed right behind
      it (size_of::<*const u8>()), so the record is the byte-exact
      {+0 word, +4 byte} pair on the 32-bit target and stays
      dereferenceable on a 64-bit test host (the cxx/templates.rs
      container_element_at word-index precedent; read_unaligned on
      the cursor keeps a 4-but-not-8-aligned record head safe, the
      plist_node_child_count idiom) - unlike the plist accessors just
      above, this function dereferences the pointer it loads, so the
      strict u32-word test idiom cannot work on host. Ghidra's
      decompile otherwise matches the listing exactly (param_1 + 1 in
      undefined4 * units is byte offset +4). Host tests (3): high
      nibble answered for a clear flag across edge bytes
      (0x00/0xab/0xf0/0x0f/0xff); low nibble answered for a set flag
      incl. 0xff, pinning the `cmp #0`/andne test-against-zero shape;
      and a poisoned-record test (0xa5 past the flag, plus a
      4-byte-offset record head) pinning the read to exactly the
      cursor word and flag byte. match.py structural (exit-1 diff
      expected): identical deviation profile to the accepted
      plist_node_child_count port - the cursor load comes out as four
      byte loads merged with orr shifts from the read_unaligned
      host-testing idiom, plus the crate fp frame; the flag ldrb at
      +4, cmp #0, conditional lsr/and selection and the dereferenced
      packed-byte ldrb all match the original's shape (LLVM computes
      the and unconditionally where ADS predicated it - same value
      semantics).

  - address: 0x082a1bfc
    ghidra: FUN_082a1bfc
    name: cond_wait_attr_clock
    size: 16
    sig: "u32 cond_wait_attr_clock(const u8 *attr, u8 *out)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: attr (the condition-wait attributes record; clock id byte at +1)
      param_2: out (caller byte slot receiving the clock id)
      FUN_08261d58: clock_id_validate (0..3 passthrough, -1 otherwise)
      FUN_082c372c: clock_gettime veneer (dispatch table DAT_082c3788, stride 0x14)
    notes: >-
      Two bl call sites, both in the condition-wait creation path:
      0x082623f4 inside FUN_082623d0 and 0x0826265c inside
      FUN_0826263c; no pointer to the address anywhere in osos.dec
      (binary-scanned). Extent binary-verified against osos.dec: 16
      bytes exactly (functions.csv's size is exact), four
      instructions `ldrb r0,[r0,#0x1]; strb r0,[r1,#0x0]; mov
      r0,#0x0; bx lr` ending at 0x082a1c08 with the next function
      starting at 0x082a1c0c - no literal pool. Algorithm: a byte
      getter over the condition variable's wait-attributes record -
      copy the record's +1 byte into the caller's out pointer and
      answer 0 (success). The record is the two-byte options block
      the wait path threads through FUN_08261fac, default-constructed
      to {0, 0} by the trivial ctor @ 0x08261d44 (two strb zeroes)
      with the empty destructor @ 0x08261d54: { +0 absolute-deadline
      flag, +1 clock id }. The +1 clock byte this getter answers is
      consumed as a clock selector: FUN_0826263c copies it into the
      waiter node at +2, and the timed-wait body @ 0x0826269c feeds
      node+2 through FUN_08262158, which range-checks it against
      0..3 (FUN_08261d58, -1 otherwise) and calls the clock_gettime
      veneer @ 0x082c372c to stamp "now" for a relative timeout.
      The creation gate in FUN_082623d0 calls this getter first and
      refuses a nonzero clock with 0x1a (the posix layer's
      invalid-argument status, the posix_mutex notes), so on this
      ROM only clock 0 (CLOCK_REALTIME) waiters can be created even
      though the wait body is generic. The SIBLING getter @
      0x082a1c0c is the same four-instruction shape reading [r0,#0x0]
      instead (ported as cond_wait_attr_abstime, see its entry): it lands
      at node+1 and selects deadline arithmetic in the wait body - 1
      means the caller's {sec, nsec} pair is an absolute deadline
      copied verbatim into the waiter node at +0x1c/+0x20, 0 means
      relative, added to the freshly read "now" by the timespec add
      @ 0x08261ed8. Despite landing in the same ADS
      template-instantiation run-off as the plist node accessors and
      nibble-stream cursor just above, this record is NOT a plist
      node - the call sites pin it to the posix condition-wait
      machinery (the nibble_stream_current note's area caveat).
      Deviations: none - a byte load and a byte store have no
      alignment hazard on any host, so the port dereferences directly
      and the read_unaligned idiom of the word-loading neighbors does
      not apply. Ghidra's decompile (`*param_2 = *(undefined1
      *)(param_1 + 1); return 0;`) matches the listing exactly; both
      call sites ignore the status return (they re-read the out byte
      off the stack), but the port preserves it. Host tests (3): the
      +1 byte forwarded across clock ids 0..3 and 0xff (the getter
      never validates - the creation gate and the clock_gettime
      veneer do); the status word always 0; and a poisoned-record
      test (0x5a at +0, 0xa5 on both sides) pinning the read to
      offset +1 exactly, the distinguishing feature against the
      0x082a1c0c sibling. match.py structural (exit-1 diff expected):
      identical deviation profile to the accepted
      plist_node_processing_instruction port - the body is
      instruction-for-instruction the original's (ldrb/strb/mov
      r0,#0), differing only by the crate's fp frame (push {fp,lr} /
      mov fp,sp / pop {fp,pc} in place of bx lr).

  - address: 0x082a1c0c
    ghidra: FUN_082a1c0c
    name: cond_wait_attr_abstime
    size: 16
    sig: "u32 cond_wait_attr_abstime(const u8 *attr, u8 *out)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: attr (the condition-wait attributes record; absolute-deadline flag byte at +0)
      param_2: out (caller byte slot receiving the flag)
      FUN_0826263c: cond_waiter_node_init (copies the flag into the waiter node at +1)
      FUN_08261f94: cond_timedwait_body (node+1 selects absolute vs relative deadline)
      FUN_08261ed8: timespec_add (relative timeout, caller delta + freshly read now)
    notes: >-
      The +0 sibling of cond_wait_attr_clock @ 0x082a1bfc - same
      four-instruction shape reading [r0,#0x0] instead of [r0,#0x1].
      Exactly one bl call site, 0x08262670 inside the waiter-node
      initializer FUN_0826263c (the sibling getter is the preceding
      bl at 0x0826265c); no 4-aligned pointer to the address
      anywhere in osos.dec (binary-scanned). Extent binary-verified
      against osos.dec: 16 bytes exactly (functions.csv's size is
      exact), `ldrb r0,[r0,#0x0]; strb r0,[r1,#0x0]; mov r0,#0x0;
      bx lr` ending at 0x082a1c18; the 20 bytes from 0x082a1c1c
      decode as an unnamed tail-call thunk (mov r2,r0; mov r0,r1;
      ldr r1,[r2,#0x8]; ldr r2,[r2,#0x4]; bx r2) before the next
      csv-listed function at 0x082a1c30 - no literal pool.
      Algorithm: a byte getter over the condition variable's
      wait-attributes record - copy the record's +0 byte into the
      caller's out pointer and answer 0 (success). Same two-byte
      options block as the sibling - { +0 absolute-deadline flag,
      +1 clock id } - default-constructed to {0, 0} by the trivial
      ctor @ 0x08261d44. The +0 flag is consumed as the
      absolute/relative deadline selector: FUN_0826263c copies it
      into the waiter node at +1, and the timed-wait body
      FUN_08261f94 compares node+1 against exactly 1 - 1 means the
      caller's {sec, nsec} pair is an absolute deadline copied
      verbatim into the node at +0x1c/+0x20, anything else means
      relative, added to the freshly read "now" by the timespec
      add @ 0x08261ed8. Deviations: none - a byte load and a byte
      store have no alignment hazard on any host, so the port
      dereferences directly as the sibling does. Ghidra's decompile
      (`*param_2 = *param_1; return 0;`) matches the listing
      exactly; the single call site ignores the status return (it
      re-reads the out byte off the stack), but the port preserves
      it. Host tests (3): the +0 byte forwarded across 0/1/2/0xff
      (the getter never validates; the wait body's ==1 compare
      makes the flag effectively boolean); the status word always
      0; and a poisoned-record test (0x5a at +1, 0xa5 on both
      sides) pinning the read to offset +0 exactly, the
      distinguishing feature against the cond_wait_attr_clock
      sibling. match.py structural (exit-1 diff expected): the
      body is instruction-for-instruction the original's
      (ldrb/strb/mov r0,#0), differing only by the crate's fp
      frame (push {fp,lr} / mov fp,sp / pop {fp,pc} in place of
      bx lr).

  - address: 0x082a1c30
    ghidra: FUN_082a1c30
    name: timespec_to_milliseconds
    size: 40
    sig: "u64 timespec_to_milliseconds(const i32 *ts)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: ts (pointer to the { +0x00 sec, +0x04 nsec } pair the clock gettime slot fills)
      DAT_082a1c58: NANOSECONDS_PER_MILLISECOND (literal 0x000f4240 = 1000000, the signed divisor)
      FUN_08031568: __rt_sdiv (ADS signed divide, ported in runtime/rt_div.rs)
      FUN_08262908: clock_source_destroy (ported; the canonical caller's destructor)
    notes: >-
      Timespec-to-milliseconds conversion: sec*1000 +
      nsec/1000000, exactly as the clock_source_destroy entry's
      note anticipated. Extent binary-verified against osos.dec:
      40 bytes of code (functions.csv's size is exact for the
      code) - ten instructions ending `ldmia sp!,{r4,pc}` at
      0x082a1c54 - plus a one-word literal pool at 0x082a1c58 =
      0x000f4240 = 1000000 (the divisor), for a true extent of
      44 bytes ending where the next csv-listed function starts
      at 0x082a1c5c. The 20 bytes immediately above
      (0x082a1c1c-0x082a1c2f) are an unnamed tail-call thunk
      (mov r2,r0; mov r0,r1; ldr r1,[r2,#0x8]; ldr r2,[r2,#0x4];
      bx r2) between cond_wait_attr_abstime @ 0x082a1c0c and
      this function; it belongs to neither and is left
      unported. 30 bl call sites; no pointer to the address
      anywhere in osos.dec (binary-scanned). Algorithm: load
      nsec (+0x4), signed-divide it by 1000000 through the ADS
      divide @ 0x08031568 (ported as __rt_sdiv), then answer
      sec (+0x0) * 1000 + quotient via the 32-bit wrapping mla,
      zero-extended to 64 bits (mov r1,#0). Return type: Ghidra
      decompiles `int`, but the mov r1,#0 and the callers'
      subs/sbc 64-bit elapsed-time subtracts (0x081643bc,
      0x08164f70, 0x081948b8, 0x081d78f8, 0x08201074,
      0x082092b4 - each subtracting a stored 32-bit ms stamp
      and comparing against 500) pin an UNSIGNED 64-bit
      return, never sign-extended: a negative wrap answers
      0x00000000_xxxxxxxx. The input record is the
      { +0x00 sec, +0x04 nsec } pair the clock objects'
      vtable slot +0xc (gettime) fills on the caller's stack;
      the canonical use constructs a clock (ctor @ 0x08262958),
      calls the slot, converts here, and destroys the clock
      (clock_source_destroy @ 0x08262908, ported); the sibling
      @ 0x08262ab8 subtracts two such pairs field by field.
      Deviations: calls the ported __rt_sdiv directly - no seam
      (Ghidra inlines the whole divide body; the listing pins
      the real bl, the plist_node_child_count precedent);
      read_unaligned word loads (the module's host idiom
      against a 4-but-not-8-aligned record head, which LLVM
      expands to ldrb/orr funnels on ARMv5 - the accepted
      plist_node_child_count deviation profile); no frame (the
      pushed r4 is only an ADS spill keeping ts alive across
      the divide call). Host tests (4): whole seconds scaled
      by 1000; nsec divided by a million with sub-milli
      truncation, the {sec=0, nsec=2e9} case pinning +0x04 as
      the divided field against +0x00 the multiplied one;
      negative nsec truncating toward zero (sdiv semantics);
      and the 32-bit mla wrap zero-extended into a zero high
      word (millis(-1,0) = 0x00000000fffffc18). match.py
      structural (exit-1 diff expected): same shape - the
      divide bl, the mla, the mov r1,#0 - differing by the
      crate's fp frame and the read_unaligned ldrb/orr
      funnels, the accepted plist_node_child_count profile.

  - address: 0x082a1c5c
    ghidra: FUN_082a1c5c
    name: timespec_is_nonzero
    size: 28
    sig: "bool timespec_is_nonzero(const i32 *ts)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: ts (pointer to the { +0x00 sec, +0x04 nsec } timespec pair under test)
      DAT_082627fc: NANOSECONDS_PER_SECOND (literal 0x3b9aca00 = 1000000000, the nsec bound the FUN_0826269c caller validates against)
    notes: >-
      Timespec-nonzero predicate: answers 1 when either word of
      the { +0x00 sec, +0x04 nsec } pair differs from 0, 0 only
      when both are zero. Extent binary-verified against
      osos.dec: 28 bytes (functions.csv's size is exact) -
      seven instructions `ldr r1,[r0,#0x0]; cmp r1,#0x0; ldreq
      r0,[r0,#0x4]; cmpeq r0,#0x0; movne r0,#0x1; moveq
      r0,#0x0; bx lr` ending at 0x082a1c74, the next
      csv-listed function starting at 0x082a1c78; no literal
      pool. Two bl call sites, both in the posix
      condition-wait machinery: 0x082624ec inside the waiter
      wake body FUN_08262450 and 0x082626bc inside the
      timed-wait entry FUN_0826269c (Ghidra decompiles the
      latter under decomp/c/025/08261f94_FUN_08261f94.c); no
      pointer to the address anywhere in osos.dec
      (binary-scanned). Both callers use it as the "deadline
      supplied" predicate: FUN_0826269c XORs the result with 1
      (eor r0,r0,#0x1) to mean "no deadline" while validating
      the caller's {sec, nsec} record (nsec bounds-checked
      against the 1000000000 literal @ 0x082627fc,
      binary-verified), then branches on the raw result to
      pick between copying an absolute deadline verbatim
      (abstime flag 1, see cond_wait_attr_abstime @
      0x082a1c0c) and adding the relative pair to the freshly
      read "now" (the timespec add @ 0x08261ed8);
      FUN_08262450 tests the waiter node's +0x14 relative pair
      and, when nonzero, folds it into the +0x1c/+0x20
      deadline through the same add. Sits immediately below
      timespec_to_milliseconds (@ 0x082a1c30, ported just
      above) and heads a run of timespec-pair ordering
      comparators (0x082a1c78 "a >= b", 0x082a1cac "a > b",
      0x082a1ce4 - unsigned sec compare with a signed nsec
      tie-break) that the same wait machinery uses for
      deadline ordering. Deviations: read_unaligned word loads
      (the module's host idiom against a 4-but-not-8-aligned
      record head on a 64-bit host, which LLVM expands to
      ldrb/orr funnels on ARMv5 - the accepted
      plist_node_child_count deviation profile); the +0x04
      word reads ONLY when +0x00 is zero, mirroring the
      original's ldreq/cmpeq conditional chain exactly; the
      bool return models the movne/moveq 1/0 answer (the
      fixed16_eq_indirect precedent). Host tests (3): the {0,
      0} pair as the sole false case against every
      one-word-nonzero combination; both words tested
      independently with sign-bit and MIN/MAX patterns (the
      poisoned 0xa5 padding around the record refuting stray
      reads); and the short-circuit pinned - a nonzero sec
      answers true for every nsec bit pattern including the
      sec + nsec == 0 cancellation a sum-based rewrite would
      miss. match.py structural (exit-1 diff expected): same
      two-word short-circuit shape - first word tested, beq
      to the second-word path, 1/0 answer - differing by the
      crate's fp frame and the read_unaligned ldrb/orr
      funnels, the accepted plist_node_child_count profile.

  - address: 0x080e97ac
    ghidra: FUN_080e97ac
    name: fixed16_cos
    size: 40
    sig: "i32 fixed16_cos(i32 angle)"
    module: fp/fp_misc
    status: identified
    renames:
      param_1: angle
      DAT_080e9828: TWO_PI_F32 (0x40c90fdb = 6.2831855f)
      DAT_080e982c: INV_TWO_PI_F32 (0x3e22f983 = 0.15915494f)
      DAT_080e9830: SIN_TABLE (pointer 0x083f10b2)
      FUN_080a67dc: f32_to_fixed16_sat
      FUN_083ed150: f32_scale_pow2
      FUN_083ec898: fixsfsi
    notes: >-
      Q16.16 fixed-point cosine by quarter-wave table lookup. Fully
      decoded from raw osos.dec bytes, NOT ported - blocked on the same
      undecrypted zero hole as fixed16_rsqrt (0x08076154). The Ghidra
      listing/functions.csv "40 bytes" is truncated: Ghidra believes
      the helper 0x080a67dc does not return ("Subroutine does not
      return") and stops the flow at the second bl; the helper DOES
      return (both saturation exits are ldmia sp!,{r4,pc}; the common
      path tail-calls fixsfsi which returns), and the body actually
      continues at 0x080e97d8. True extent: code 0x080e97ac-0x080e9827
      (31 instructions) + literal pool 0x080e9828-0x080e9833 (TWO_PI_F32,
      INV_TWO_PI_F32, SIN_TABLE pointer). Algorithm (all wrapping
      32-bit): while (i32)angle < 0, angle += f32_to_fixed16_sat(2pi)
      (= 0x6487e, reconverted from the float literal each iteration);
      rev = (angle * f32_to_fixed16_sat(1/(2pi)) = 0x28be) keeping the
      low 32 product bits, then asr #16 -> fractional revolutions in
      Q16.16 mod 1 turn; idx12 = (rev + 0x4000) asr #4 -> 4096
      units/turn, the +0x4000 being exactly a +pi/2 phase shift (this
      is what makes it cos); quadrant mirror: idx = idx12 & 0x3ff, and
      if idx12 & 0x400 then idx = 0x3ff - idx (rsb #0xff + add #0x300);
      entry = SIN_TABLE[idx] (u16, amplitude 65536); if idx12 & 0x800
      return -entry else entry. The smull r1,r2,r3,r1 with r3 = 0x10000
      plus the lsl #16/lsr #16 funnel is an identity (entry * 0x10000
      >> 16) - a fixed16_mul-by-1.0 artifact, no rounding effect.
      0x080a67dc = saturate-to-i32-then-scale-by-2^16 (exponent
      adjust via 0x083ed150, truncate via fixsfsi @ 0x083ec898), i.e.
      f32 -> Q16.16 with clamping. 1 bl call site: 0x08256f70 inside
      FUN_08256eac, which builds a 3x3 Rodrigues rotation matrix from
      an axis (param_3/4/5 through fixed16_mul/fixed16_rsqrt) and this
      cosine (param_1[0] = (1-c)*x*x + c, param_1[4] = (1-c)*x*y -
      s*z, ...), pinning the semantics; the sibling 0x080e988c (NOT
      this entry) is the same body minus the +0x4000 phase shift (asr
      #20 directly) = fixed16_sin. BLOCKER: SIN_TABLE @ 0x083f10b2
      (1024 u16 entries; the pointer is referenced only from this
      function's and fixed16_sin's literal pools, binary-scanned over
      osos.dec) sits inside the 0x442b-byte all-zero run
      0x083ee1c1-0x083f25ec in work/firmware/osos.dec - the same
      still-undecrypted region documented under fixed16_rsqrt. Direct
      byte-for-byte comparison of the exact table interval
      [0x083f10b2, 0x083f18b2) against zeros succeeds in osos.dec,
      osos.recovery, and osos.patched. No duplicate quarter-wave table
      exists anywhere in the decrypted image
      (byte-scanned for both round- and trunc-scaled sin sequences at
      4-byte alignment); aupd.dec does not contain the routine at all
      (searched for the 0x40c90fdb literal). Do NOT guess the table:
      entry rounding (round vs floor vs 65535-clamp of the sin(1023)
      peak) is unverifiable, and every output is a direct table read,
      so any wrong entry is a behavioral divergence, not an ulp
      detail. To port once `make decrypt` fills the region: embed the
      1024 u16 entries as a static table in fp/fp_misc.rs and
      implement the wrapping arithmetic exactly as decoded above
      (wrapping_add for the 2pi loop, wrapping_mul + asr for the
      revolution reduction, mask/mirror/negate for the quadrant
      logic); expected shape for a one-glance check:
      table[i] ~= sin(i*pi/2048) * 65536, table[0] = 0, table[0x3ff]
      ~= 0xffe0.

  - address: 0x080e9834
    ghidra: FUN_080e9834
    name: fixed16_div
    size: 68
    sig: "i32 fixed16_div(i32 numerator, i32 divisor)"
    module: fp/fp_misc
    status: ported
    vars:
      param_1: numerator
      param_2: divisor
      iVar1: reciprocal
      FUN_08076204: fixed16_reciprocal
    notes: >-
      Q16.16 division by a reciprocal helper plus a signed widening
      multiply. The divisor is accepted directly by the reciprocal helper
      when its signed top byte is 0 or -1 (the full 24-bit signed range);
      otherwise the helper receives divisor >> 8 and the numerator is
      arithmetic-shifted by 8 after the call. The function then falls
      straight into the immediately following fixed16_mul body
      (0x080e9878): the result is the signed product bits [47:16]
      assembled as `(product_hi << 16) | (product_lo >> 16)`, with
      negative truncation toward minus infinity and 32-bit wrap.
      FUN_08076204 (16 bytes, an unported leading-zero-count dispatch)
      remains a firmware-address call boundary through
      FIXED16_RECIPROCAL; host tests install a recorder to prove both
      dispatch arms and the exact multiply funnel.

  # --- UI pixel/color (ui/color) ---

  - address: 0x080ed43c
    ghidra: FUN_080ed43c
    name: color_to_rgba8
    size: 652
    sig: "void color_to_rgba8(u32 color, u32 format, u8 *out)"
    module: ui/color
    status: ported
    vars:
      param_1: color
      param_2: format
      param_3: out
      DAT_080ed6c8: PATTERN_FG (pointer @ 0x089cc8d0)
      DAT_080ed6cc: PATTERN_BG (pointer @ 0x089cc8c0)
      DAT_080ed6d0: DITHER_PATTERN_01 (0x55555555)
      DAT_080ed6d4: PATTERN_MID_01 (pointer @ 0x089cc8c4)
      DAT_080ed6d8: DITHER_PATTERN_10 (0xaaaaaaaa)
      DAT_080ed6dc: PATTERN_MID_10 (pointer @ 0x089cc8cc)
      DAT_080ed6e0: GRAY4_DIV15_MAGIC (0x88888889)
      DAT_080ed6e4: NIBBLE_TABLE (pointer @ 0x083f85a8)
    notes: >-
      Unpacks a color from the pixel format selected by format (masked
      with 0xffffdfff - bit 13 ignored) into 4 bytes R,G,B,A. Formats:
      0x1 mono (nonzero -> foreground canned color, zero -> background),
      0x2 2bpp dither (0 -> bg, 0x55555555 -> mid-01, 0xaaaaaaaa ->
      mid-10, else fg), 0x4 gray4 inverted (gray = (0xf-(color&0xf))*255
      umull 0x88888889 >> 35, i.e. exact (15-nibble)*17, RGB=gray A=0xff),
      0x8 gray8 inverted (0xff-(color&0xff) x3, A=0xff), 0x565/0x555
      RGB565/RGB555 shift-or-replication expansion A=0xff, 0x1888
      ARGB8888 byte reorder to RGBA, 0x1444 RGBA4444 via 16-byte nibble
      table, anything else -> foreground. 37 bl call sites, mostly
      text/layout drawing (0x080b2a40, 0x080f1600). Remarkable detail:
      the four canned colors are not the function's own constants - its
      literal pool holds absolute pointers that land inside the localized
      clock-city string table ("\0Clock_String_Katmandu\0...
      Clock_String_Mogadishu\0" @ 0x089cc8c0: bg = 00 43 6c 6f "\0Clo",
      mid-01 = 63 6b 5f 53 "ck_S", mid-10 = 67 5f 4d 6f "g_Mo", fg =
      67 61 64 69 "gadi"), and the RGBA4444 nibble table is 16 bytes
      read out of a (u16 counter, u16 value) pair table @ 0x083f85a8.
      Port captures all of them by value (behavior identical, regions
      are read-only). match.py: LLVM dispatches the small formats
      through a jump table where ADS used a cmp chain; the gray4
      rsb/umull/lsr#3 sequence, both 565/555 expansion blocks, the
      0x1888 byte reorder, the 0x1444 nibble lookups and the 4x
      ldrb/strb canned-color copies all match the original
      instruction-for-instruction in shape.

  # --- UI block-loaded record resource (ui/block_map) ---

  - address: 0x080f8778
    ghidra: FUN_080f8778
    name: block_map_init
    size: 52
    sig: "void block_map_init(BlockMap *ctx, u8 *data, const u8 *desc, const u8 *block_ok)"
    module: ui/block_map
    status: ported
    vars:
      param_1: ctx
      param_2: data
      param_3: desc
      param_4: block_ok
    notes: >-
      Initializes the 0x20-byte BlockMap context the fault-tolerant
      record-resource parsers in the 0x080f8xxx cluster use: data at
      +0x00 and mirrored into the header slot +0x04, desc at +0x08,
      num_blocks at +0x0c = *(u16 *)(desc+0x1c) >> 9 (native-endian
      byte length -> 512-byte block count), block_ok at +0x10, gap
      range +0x14/+0x18 and bad-block flag bytes +0x1c/+0x1d/+0x1e
      zeroed. One call site: FUN_080ed3f8 builds the context on its
      stack (0x24-byte temp) for FUN_080f86c4, which queries the flags
      and dispatches the gap scanner/repairers (FUN_080f83fc,
      FUN_080f8454, FUN_080f84e0) and the big-endian offset-table
      builder FUN_080f824c over a resource read from storage in
      0x200-byte blocks (its caller, unnamed @ 0x081bfdf8, reads the
      blocks via FUN_08136920 and records one status byte per block).
      Struct is repr(C); byte offsets asserted on the 32-bit target
      only. match.py: 13 instructions vs 13 - same store set to the
      same offsets; LLVM folds the three strb zeros into strh+strb and
      derives (u16 >> 9) as ldrb of the high byte + lsr #1, and adds
      the crate-standard frame-pointer prologue.

  - address: 0x08138460
    ghidra: FUN_08138460
    name: refcounted_base_init
    size: 36
    sig: "void refcounted_base_init(FixedValue *this)"
    module: app/fixed_value
    status: ported
    renames:
      param_1: this
      DAT_08138484: REFCOUNTED_BASE_VTABLE
    notes: >-
      Shared base-class constructor for the refcounted UI object family
      (22 bl call sites): first stores the 0x08984ca0 base-vtable word
      at +0x00, then performs ldrb/bic #3/strb at +0x14 and
      ldr/and #3/str at +0x14. Clearing the flag byte before masking the
      word makes the final flags/refcount word zero for every input,
      without writing +0x04..+0x13. Raw ARM proves the function is 36
      instruction bytes: nine instructions through bx lr at
      0x08138480; the separate 4-byte vtable literal is at 0x08138484.
      The reported 16-byte size covers only its first four instructions.
      The port uses volatile byte/word accesses and a u32 vtable field
      so its host layout remains the target's 0x18-byte ARM object
      layout.
  - address: 0x081523b8
    ghidra: FUN_081523b8
    name: fixed_value_init
    size: 32
    sig: "FixedValue *fixed_value_init(FixedValue *this, i32 value_q16)"
    module: app/fixed_value
    status: ported
    renames:
      param_1: this
      param_2: value_q16
      DAT_081523d8: FIXED_VALUE_VTABLE
      FUN_08138460: refcounted_base_init
      DAT_08138484: REFCOUNTED_BASE_VTABLE
    notes: >-
      138 bl call sites - one of the hottest small leaves in the app
      framework. The value constructor of retailOS's UI
      property/animation framework: builds a refcounted Q16.16
      fixed-point scalar object in caller-allocated storage (every
      caller does operator_new(0x18) @ 0x082aadd4, the default ctor's
      array stride is 0x18) and returns it. Runs the refcounted base
      constructor @ 0x08138460 (installs base vtable 0x08984ca0,
      zeroes the flags/refcount word +0x14 via an odd
      byte-clear-then-word-mask sequence that always lands 0), then
      overrides the vtable with 0x08986998, stores the scalar at +0x04
      and zeroes the +0x08 aux word. Callers pass `x << 16` (Q16.16 -
      the util/fixed.rs fixed16 convention; literals 0xff0000 = 255.0,
      0xa00000 = 160.0, 0x1400000 = 320.0) and stash the results as
      view-property slots or hand (from, to) pairs to the animation
      constructor FUN_08166b88, which retains/releases them
      (0x08273a14 / 0x082739e0 - +0x14 word holds flags in bits 0..1
      and the refcount in bits 2.., release dispatches the deleting
      destructor at vtable +0x04) and keeps the maximum of their +0x08
      words, so +0x08 is an aux/rank word a plain scalar leaves 0.
      The base constructor @ 0x08138460 is ported independently as
      refcounted_base_init. Both vtables are address constants: their
      contents in the decrypted image are stale (the app/registry.rs
      0x08989718 caveat - that page does not match what the device runs;
      the slots there point into 16-bit data tables, not code), so only
      the stored pointer value is reproduced. The sibling default ctor
      FUN_081523dc (20 bytes, same base ctor and vtable, stores no
      value) is ported in this module. match.py: same structure - push,
      value saved in r4, bl to the base-ctor function, vtable literal
      (0x08986998) loaded from the pool, stm {vtable, value}, zero
      store to +8, return this; LLVM adds the crate-standard frame-
      pointer prologue and keeps `this` in r5 across the call (the
      original relies on the base constructor preserving r0).
  - address: 0x081384a0
    ghidra: FUN_081384a0
    name: refcounted_base_destroy
    size: 40
    sig: "FixedValue *refcounted_base_destroy(FixedValue *this)"
    module: app/fixed_value
    status: ported
    renames:
      param_1: this
      DAT_081384c8: REFCOUNTED_BASE_VTABLE
      DAT_081384cc: SCHEDULER_SINGLETON_GLOBAL
      FUN_082738e0: timing_wheel_remove
    notes: >-
      The shared refcounted-base destructor body: installs the base vtable
      word 0x08984ca0 at +0x00, loads the live scheduler singleton from
      global 0x089cc7e0, then calls timing_wheel_remove(table, this) and
      returns this. Raw ARM is ten instruction words (40 bytes) at
      0x081384a0..0x081384c4 followed by the two literal-pool words at
      0x081384c8/0x081384cc; the next separately linked function begins
      at 0x081384d0, so the full extent is 48 bytes. Decoding every ARM
      B/BL word finds 15 direct calls, all unconditional bl, plus nine
      unconditional B tail callers; no predicated form or raw DATA word
      targets this address. The target reads the scheduler global directly;
      host tests use animation's shared scheduler-table model. match.py
      (worktree archive): ten original instructions versus thirteen Rust;
      LLVM adds the frame-pointer prologue and emits the vtable store before
      moving this into r1, but retains the literal loads, direct wheel-remove
      bl, and returned this-pointer flow.

  - address: 0x08076370
    ghidra: FUN_08076370
    name: wstr_case_eq
    size: 116
    sig: "i32 wstr_case_eq(const VectorBounds *wide, const u8 *s)"
    module: util/wstr_casecmp
    status: ported
    vars:
      param_1: wide
      param_2: s
      iVar3: len
      iVar6: i
      puVar4: wp
      puVar5: sp
      uVar1: wide_fold
      uVar2: byte_fold
    notes: >-
      94 bl call sites (binary-scanned). Length-bounded case-folded
      equality between a UTF-16-ish wide buffer (passed as a pointer to
      an ADS std::vector<u16> {begin, end} head) and a C string: n from
      the vector, each wide element and string byte run through a
      per-character uppercase fold table, 1 iff all n folded pairs match
      AND strlen(s) == n; empty vector -> 0 even against "". Return is
      a 1/0 bool, not a tri-state. Sampled call sites (0x0811de70
      family) match vector<u16> buffers against command/name C strings
      from object slots - an identifier/verb matcher. Helpers ported in
      place, private (house precedent): wide_vec_len (FUN_0829db9c @
      0x0829db9c, 24 bytes - ADS out-of-line vector<u16>::size, UNSIGNED
      end>begin guard then (end-begin)>>1 asr, inverted -> 0; since
      promoted to the exported vector_size_elem2_clamped in
      cxx/templates, wide_vec_len delegates) and
      fold_upper (FUN_080f4cbc @ 0x080f4cbc, 16 bytes - -1 -> 0xffffffff
      else TABLE[c]). The fold-table literal @ 0x080f4cd0 in the cold
      image holds 0x083ed0dd - one byte into __dscalb (0x083ed0dc, live
      soft-float, ported in fp/fp_scalb.rs) - a pre-locale-init
      placeholder, not a usable map (binary-verified: 'c'/'C' fold
      apart). The image's only real fold maps sit in a full ADS LC_CTYPE
      block: ctype flags @ 0x83f7eb5, lower map @ 0x83f7fb5, upper map @
      0x83f80b5 (Latin-1 toupper: ASCII fold plus 0xe0..=0xfe ->
      0xc0..=0xde, 0xf7/0xff unchanged); no pointer to it exists, the
      address is installed at runtime by the locale init (the ctype.rs
      0x08985f01 precedent). Modeled as the WSTR_FOLD_TABLE dispatch
      slot (FP_TRAP_HANDLER precedent) defaulting to DEFAULT_FOLD_TABLE,
      a byte-exact copy of the upper map @ 0x83f80b5 (verified against
      osos.dec offset 0x3f80b5). Only other user of the same runtime
      table: the in-place narrow-string transformer FUN_0810b498 @
      0x0810b498 (not ported). Faithful quirks pinned by host tests: the
      wide fold is truncated to 16 bits, the byte fold to 8; begin is
      dereferenced only after the n != 0 guard; the strlen == n gate
      runs last, so bytes after the string's first NUL are invisible.
      Wide codes above 0xff index PAST the 256-entry map in the original
      as in the default copy (the slot can be pointed at the live map to
      keep even those reads faithful). The ported unguarded strlen @
      0x08392478 is called directly. match.py structural: same guarded
      size (cmp/bcs -> 0, sub, asr #1, beq -> 0), same two-table-load
      compare loop, same inlined strlen tail; the diff is LLVM folding
      the redundant &0xffff/&0xff (the fold result is already a byte),
      the read_unaligned ldrb chain for the {begin, end} head, the
      clz/lsr equality select for the original's moveq, and LLVM's fp
      frame - the accepted house deviations.

  - address: 0x0810b498
    ghidra: FUN_0810b498
    name: str_fold_upper_inplace
    size: 40
    sig: "void str_fold_upper_inplace(u8 **slot)"
    module: util/str_fold_upper
    status: ported
    vars:
      param_1: slot
      pbVar2: p
      iVar1: table
    notes: >-
      1 bl call site (0x081a3618, binary-scanned). In-place uppercase
      fold of a NUL-terminated narrow string reached through a pointer
      slot: ldr r0,[r0] takes the char* out of the slot, NULL returns
      immediately, every byte up to (not including) the first NUL is
      rewritten through the runtime uppercase fold map (ldrb/cmp #0/
      ldrbne [table,c]/strbne [r0],#1/bne); the NUL is never stored,
      the slot word never written, nothing returned. 40 bytes = 36
      code + the 4-byte fold-table literal @ 0x0810b4c0, which in the
      cold image holds 0x083ed0dd - the same pre-locale-init
      placeholder one byte into __dscalb as wstr_case_eq's pool word
      (binary-verified against osos.dec offset 0x10b4c0); the real ADS
      LC_CTYPE upper map @ 0x83f80b5 is installed at runtime by the
      locale init. Reuses the shared WSTR_FOLD_TABLE dispatch slot /
      DEFAULT_FOLD_TABLE of util/wstr_casecmp.rs rather than
      duplicating the map - this is the only other user of that
      runtime table. Sole caller 0x081a35ec: builds a string object
      from a C string (0x0810b634), folds it to uppercase here, takes
      a 4-byte hash/key (0x08297df8), converts it (0x0810b654) and
      compares against a tag literal (0x0810b444) - a case-normalizing
      tag/key lookup. Deviation: volatile slot/byte/table accesses so
      the fold loop keeps its shape under LLVM's loop-idiom pass.
      Host tests: NULL-slot no-op, ASCII fold in place, unchanged
      uppercase/digits/punctuation, Latin-1 high-half fold with the
      0xf7/0xff exceptions, empty string, interior-NUL early stop
      (bytes after untouched), slot word never written, and a
      200-round swept reference comparison. No WSTR_FOLD_TABLE swap
      test here - the slot is shared with wstr_casecmp's tests, which
      prove the swap mechanism. match.py structural: same slot-deref /
      NULL early-out / literal table load / ldrb-cmp-fold-strb loop
      until NUL; the diff is LLVM's loop rotation (bottom-tested), the
      dispatch-slot indirection for the table pointer, and the crate
      fp frame - the accepted house deviations.

  - address: 0x081523dc
    ghidra: FUN_081523dc
    name: fixed_value_default_init
    size: 20
    sig: "FixedValue *fixed_value_default_init(FixedValue *this)"
    module: app/fixed_value
    status: ported
    renames:
      param_1: this
      DAT_081523f0: FIXED_VALUE_VTABLE
      FUN_08138460: refcounted_base_init
    notes: >-
      The default constructor of the same refcounted Q16.16 scalar
      class as fixed_value_init (FUN_081523b8, ported beside it in
      app/fixed_value.rs): runs the exported refcounted base constructor
      @ 0x08138460 (base vtable 0x08984ca0, flags/refcount word +0x14
      landed at 0 via the byte-clear-then-word-mask sequence), then
      overrides the vtable with 0x08986998 and returns this. Stores NO
      value: the +0x04 scalar and +0x08 aux word keep whatever the
      storage held. Pool literal @ 0x081523f0 binary-verified against
      osos.dec to hold 0x08986998 - the same FIXED_VALUE_VTABLE the
      value ctor's literal @ 0x081523d8 holds. Its callers are array
      builders (FUN_08280fc0, FUN_081eadec, FUN_081a167c, FUN_081b8e74)
      that chain it over consecutive objects at a 0x18 stride
      (operator_new(0x18) per element), so a default-constructed
      element's value/aux words stay dirty. Both vtables are address
      constants: contents stale in the decrypted image (the
      app/registry.rs 0x08989718 caveat); only the stored pointer value
      is reproduced. match.py: same structure - push, bl to the
      exported base constructor, vtable literal (0x08986998) from the
      pool, str to [this], return this; LLVM adds the crate-standard
      frame-pointer prologue and keeps this in r4 across the call (the
      original relies on the base constructor preserving r0) - the
      accepted house deviations.

  - address: 0x08280fc0
    ghidra: FUN_08280fc0
    name: fixed_value_triplet_default_init
    size: 32
    sig: "FixedValue *fixed_value_triplet_default_init(FixedValue *this)"
    module: app/fixed_value
    status: ported
    renames:
      param_1/r0: this
      FUN_081523dc: fixed_value_default_init
    notes: >-
      Raw binary decode establishes the exact 32-byte extent:
      push {r4,lr}; bl fixed_value_default_init(this); add r0,#0x18;
      bl fixed_value_default_init; add r0,#0x18; bl
      fixed_value_default_init; sub r0,#0x30; pop {r4,pc}. It constructs
      three consecutive 0x18-byte FixedValue slots and returns the first.
      Decoding every ARM B/BL word in osos.dec finds eight direct call
      sites (0x08153150/58/60/68 and 0x0816e37c/84/8c/94), all
      unconditional bl; no tail branches, predicated calls, or exact
      DATA-word references target this address. No NULL or bounds guard,
      matching stock. Host test seeds unique dirty payloads in all slots,
      verifies each receives the vtable and zero flags word, and proves
      its value, aux, and opaque fields remain untouched.
      match.py structural review against this worktree's ARM archive:
      the three calls and their 0x18/0x30 slot progression are retained;
      LLVM adds its standard fp frame and preserves the base in r4 rather
      than using stock's final r0 subtract. The prescribed default archive
      resolves to the separate sibling checkout, which lacks this
      worktree's new symbol; --lib selects this worktree's archive.

  # --- 'plst' UI-element counted-string store (ui/plst_counted_string) ---
  - address: 0x08067274
    ghidra: FUN_08067274
    name: plst_element_store_counted_string
    size: 88
    sig: "int plst_element_store_counted_string(u8 *element, const uint16_t *counted)"
    module: ui/plst_counted_string
    status: ported
    renames:
      param_1/r0/r4: element
      param_2/r1/r3: counted
      param_1_plus_0x48: tagged_handler_list
      param_1_plus_0xcc: inline_string_pool
      param_1_plus_0x124: string_pool_entry_id
      DAT_080672cc: POST_STORE_NOTIFY_TAG
      FUN_080613e0: ui_element_is_plst_class
      FUN_080be81c: string_pool_store_counted
      FUN_08066bb8: tagged_list_notify (seam, unported)
    notes: >-
      Raw ARM covers 0x08067274 through the return at 0x080672c8 plus
      literal-pool word 0x63706e6d at 0x080672cc; the next independently
      linked function begins at 0x080672d0, confirming Ghidra's 88-byte
      extent. Decoding every ARM B/BL word in osos.dec finds exactly 10
      direct bl callers, all unconditional (0x080612e0, 0x08063500,
      0x08065c5c, 0x08068a38, 0x080ab4a8, 0x080b4a30, 0x0812cb8c,
      0x0816ed6c, 0x0816ee34, 0x0817c0f4), with zero predicated calls.
      It calls the ported 'plst' class predicate and rejects either a
      non-'plst'/NULL element or NULL counted UTF-16 source with -50
      (mvn r0,#0x31). On success it passes element+0xcc, the counted source,
      and element+0x124 to the ported string_pool_store_counted. A zero store
      status invokes unported tagged-list walker 0x08066bb8 on element+0x48
      with literal key 0x63706e6d, element context, and two zeros; its status
      is discarded while the store status returns. Deliberate deviation:
      0x08066bb8 has no ported ledger entry, so PLST_COUNTED_STRING_OPS
      volatile-dispatches to its verified retail address. The key's semantic
      identity is not recovered or invented. Host tests prove success's exact
      store/notify tuple, status propagation without notification, and all
      class/NULL guard no-call paths.

  - address: 0x08067300
    ghidra: FUN_08067300
    name: set_navigation_mode
    size: 100
    sig: "void set_navigation_mode(u8 *element, u32 mode)"
    module: ui/navigation_mode
    status: ported
    renames:
      param_1/r0/r5: element
      param_2/r1/r4: mode
      local_halfword: previous_mode
      param_1_plus_0x184: navigation_mode
      param_1_plus_0x48: tagged_handler_list
      DAT_08067360: MODE_CHANGED_TAG
      FUN_08054568: query_mode_change
      FUN_0805d31c: apply_mode_change
      FUN_08066bb8: notify_mode_change
    notes: >-
      Raw ARM begins at 0x08067300 and has 96 instruction bytes through
      `pop {r2,r3,r4,r5,r6,pc}` at 0x0806735c, followed by its referenced
      literal-pool word 0x6370726d at 0x08067360; the next independently
      linked function starts at 0x08067364. Thus the true extent is 100
      bytes, despite Ghidra's 96-byte code-only report. Decoding every ARM
      B/BL word in osos.dec finds 11 direct callers, all plain unconditional
      bl and zero predicated forms. The unported 0x08054568 reports whether
      the element may change modes and stores the prior halfword through its
      out-pointer. A permitted request only proceeds when mode is in 0..=2
      and differs from that reported halfword: the port stores mode at +0x184,
      calls unported 0x0805d31c, then invokes unported 0x08066bb8 on the
      handler list at +0x48 with literal tag 0x6370726d, context element,
      old mode in r3, and new mode in the stack word. The tag's semantic
      identity is not recovered. Deliberate deviation: the three unported
      callees dispatch through NAVIGATION_MODE_OPS; target defaults call
      their stock addresses, while host recorders prove denied, out-of-range,
      reported-equal, and accepted-change behavior plus the exact notification
      tuple.

  - address: 0x08067b4c
    ghidra: FUN_08067b4c
    name: tdat_element_set_flag_20_bit_2
    size: 108
    sig: "u32 tdat_element_set_flag_20_bit_2(u8 *element, u32 value)"
    module: ui/tdat_flag_20_bit_2
    status: ported
    vars:
      param_1/r0/r4: element
      param_2/r1/r2: value
      DAT_08067bb4: ENABLE_NOTIFY_TAG
      FUN_0806aa3c: ui_element_is_tdat_class
      FUN_0806aa64: tdat_refcount_release (seam, unported)
      FUN_0806aaac: tdat_refcount_addref (seam, unported)
      FUN_08066bb8: tdat_tagged_list_notify (seam, unported)
    notes: >-
      Setter for bit 2 of the 'tdat' UI-element flag byte at +0x20 with
      refcount coupling. Ghidra's 172-byte extent is wrong: the true
      extent is 108 bytes (0x08067b4c..0x08067bb8, including the
      literal-pool word 0x74646472 @ 0x08067bb4); the next function, a
      two-field setter (str r1,[r0,#0x4c]; str r2,[r0,#0x50]; bx lr),
      starts at 0x08067bb8. Algorithm: run the ported 'tdat' class
      predicate ui_element_is_tdat_class (0x0806aa3c); on success load
      the flag byte at +0x20 and return it unchanged when value exactly
      equals its bit 2 (cmp r2, r1 lsr #31 after lsl r1, r0, #29 - an
      exact u32 compare against 0/1, not a mask test). Otherwise store
      (flags & ~4) | ((value << 2) & 4) back to +0x20 - only bit 0 of
      value reaches the byte, all other bits of the old byte survive.
      value == 0 tail-branches to the refcount release @ 0x0806aa64
      (halfword at element+0x40, fires the virtual handler at +0x4c
      with key 0x7464726c 'tdrl' on reaching zero) and returns its
      result; nonzero value calls the addref @ 0x0806aaac (result
      discarded), then walks the tagged-handler list at element+0x54
      via 0x08066bb8 with event key 0x74646472, the element as context
      and zeroed flags/stack args, returning the walker's result. NO
      NULL guard of its own: when the class check fails (NULL included)
      the flag store still happens with the old byte treated as 0.
      Call count verified by decoding every B/BL word in osos.dec: 14
      bl (12 unconditional, 2 blne @ 0x0805d3c8 and 0x0806ce10) plus 3
      predicated tail branches (bne @ 0x0805d338, bne @ 0x0805d360,
      beq @ 0x0805d390, all passing value 1) - the predicated sites
      gate the call on their own state, consistent with the missing
      NULL guard. Deliberate deviation: the three callees 0x0806aa64,
      0x0806aaac and 0x08066bb8 are unported, so they dispatch through
      the TDAT_FLAG_20_OPS seam (target defaults transmute the stock
      addresses); the ported class predicate is called directly. Host
      tests (8) prove the early-return/no-side-effect paths, bit
      preservation, non-tdat zero-based store, exact-compare semantics
      for value 3 (proceeds, sets bit) and value 2 (proceeds, clears
      bit, takes the addref path), and the exact notify argument tuple
      (element+0x54, 0x74646472, element, 0, 0). match.py --lib
      <worktree archive> --size 108: same class-check/bl, byte
      load/bit-2 extract/compare, mask-store, value==0 split and
      notify-tail structure; deltas are the crate fp frame, the
      seam-static ldrd/blx indirection, and LLVM's and/lsr lowering of
      the bit extract vs ADS lsl #29/lsr #31.

  - address: 0x08067ca4
    ghidra: FUN_08067ca4
    name: inner_set_state
    size: 8
    sig: "void inner_set_state(u8 *inner, u32 state)"
    module: util/inner_state
    status: ported
    vars: {param_1: inner, param_2: state}
    notes: >-
      Two-instruction setter `str r1,[r0,#0xe38]; bx lr` - stores the
      state/mode word at +0xe38 of the inner query object. This is the
      tail target of inner_set_state_4 (FUN_0813b7c4, ported beside
      it); that wrapper's port inlined this store as a documented
      deviation, and this entry gives the setter its own per-address
      symbol. 11 bl call sites, all loading the inner object from the
      query object's +0x40 slot themselves: a switch over query ids @
      0x08111368..0x081113c4 stores states 0..6, and the query-family
      wrappers FUN_0813c02c/FUN_0813c6c0 store 4, FUN_0813c700 stores
      7, FUN_0813d974/FUN_0813deb0/FUN_0813e474 store 5, FUN_0817a238
      stores 5 then a variable. Same word the inline writers @
      0x08053fdc (1), 0x08054038 (3) and 0x08061020 (5) poke and the
      reader @ 0x0808cf4c compares against 1 to pick a result code;
      the byte-setter sibling @ 0x08067c9c (`strb r1,[r0,#0xe3c]`)
      touches a different field. Enum not identified - ported on
      observable behavior. Deviation: none beyond the crate fp frame.
      Host tests: stores the given value, round-trips 0..7 and
      u32::MAX (full-word store), touches only the +0xe38 word
      (whole-buffer byte-compare). match.py structural: identical
      `str r1,[r0,#0xe38]`; the only diff is the crate-standard
      push-fp-lr/pop-fp-pc frame.

  - address: 0x08059644
    ghidra: FUN_08059644
    name: inner_reset_transient_state
    size: 184
    sig: "void inner_reset_transient_state(u8 *inner)"
    module: util/inner_state
    status: ported
    renames:
      param_1: inner
      FUN_0805d070: free_tag4
      FUN_08031568: __rt_sdiv
    notes: >-
      Verified against osos.dec at load address 0x08059644: 45 ARM
      instructions occupy 180 bytes through the pop at 0x080596f8, followed
      by the 0x0000062c literal at 0x080596fc; the next independent function
      starts at 0x08059700, making the extent 184 bytes. Decoding every ARM
      immediate B/BL word finds exactly 11 direct call sites, all plain bl
      (0x08054020, 0x080664ac, 0x080664d8, 0x0806655c, 0x080665f4,
      0x080667a8, 0x080668d4, 0x08066a40, 0x08066b70, 0x08068ec0,
      0x0813d020); none are predicated. It releases nonzero tag-4 transient
      pointers at inner+0xe7c, +0xe84, +0xe80 in that order and clears each,
      clears +0xe88, logically clears the 24-byte-record vector at
      +0xeb0/+0xeb4, stores -1 at +0x18, clears the u16 at +0x62c and bytes
      +0xe8c/+0xef9. The raw vector helper at 0x083e9b68 receives the empty
      source range end..end, so its copy loop has no iterations and returns
      begin; the following no-op iterator walk and ADS signed division yield
      end - ((end - begin) / 24) * 24. The port directly retains that
      arithmetic through the existing __rt_sdiv and calls existing free_tag4;
      it deliberately does not invent a seam or identity for 0x083e9b68.
      Host tests cover three releases and all writes, NULL releases with an
      empty vector, and the raw signed-division result for a partial span.

  - address: 0x08059a98
    ghidra: FUN_08059a98
    name: inner_clear_cached_results
    size: 60
    sig: "void inner_clear_cached_results(u8 *inner)"
    module: util/inner_state
    status: ported
    vars: {param_1: inner, FUN_0805d070: free_tag4}
    notes: >-
      Verified against osos.dec (load base 0x08000000): the 15 instruction
      body starts `push {r4-r6,lr}`, loads the primary cached-result pointer
      at inner+0xeec, and ends at 0x08059ad0; 0x08059ad4 starts the distinct
      next function. A nonzero primary is passed in r0 to free_tag4
      (FUN_0805d070), then cleared. Only in that branch does the function
      inspect, free when nonzero, and clear the auxiliary pointer at +0xef0.
      The result count at +0xef4 is cleared unconditionally. Thus a NULL
      primary deliberately leaves any auxiliary value untouched. Decoding
      every ARM B/BL word in osos.dec finds exactly 17 direct call sites, all
      unconditional `bl` (no predicated calls and no tail `b`). The existing
      free_tag4 port is called directly; its tag-4 heap dispatch is its
      documented existing deviation. The port reads/writes cache fields as
      aligned u32 words, preserving the target's 4-byte pointer slots on
      64-bit hosts rather than using host-width pointer fields. Host tests
      prove primary-only release, ordered primary-plus-auxiliary release, and
      the NULL-primary asymmetric case.

  - address: 0x080be134
    ghidra: FUN_080be134
    name: inner_release_buffer_and_reset_cursor
    size: 144
    sig: "void inner_release_buffer_and_reset_cursor(u8 *inner)"
    module: util/inner_state
    status: ported
    renames:
      param_1: inner
      FUN_0805d070: free_tag4
      DAT_080be1c4: BUFFER_SELECTION
    notes: >-
      Raw osos.dec decoding confirms 36 instructions from 0x080be134 through
      the pop at 0x080be1c0 (144 bytes), followed by the 0x0000082c literal
      at 0x080be1c4; the next separately linked function begins at
      0x080be1c8. Decoding every immediate ARM B/BL word finds exactly ten
      direct calls, all unconditional plain bl (no predicated BL or tail B):
      0x08066428, 0x080664b4, 0x080664e8, 0x08066564, 0x080665fc,
      0x08066780, 0x08066a18, 0x08066b50, 0x08068ec8, 0x0809dc0c. It
      free_tag4-releases and clears inner+0xe90 if nonzero, rewinds the
      16-byte-aligned cursor end at +0xec0 to its +0xebc begin, stores -1 at
      +0x1c, and clears the u16 at +0x82c. Ghidra's apparent record copy is
      dead raw code: r0 and r9 are both loaded from end before the predicated
      ldmne/stmne loop, so it cannot execute. Deliberate deviation: Rust
      omits that provable no-op and the subsequent side-effect-free 16-byte
      walker, directly applying its wrapping rewind. Thus mismatched low four
      cursor-address bits return instead of reproducing the stock walker's
      nontermination; valid target behavior is unchanged.
      Host tests prove the tag-4 release and only the four intended field writes on a
      three-record cursor, plus the NULL-allocation/empty-cursor edge path.


  - address: 0x08059ad4
    ghidra: FUN_08059ad4
    name: inner_dispatch_selected_resource
    size: 76
    sig: "i32 inner_dispatch_selected_resource(void *object)"
    module: util/inner_state
    status: ported
    renames:
      param_1: object
      FUN_08051ce4: resolve_selected_resource
      FUN_08054710: is_resource_selected
      FUN_08067450: set_resource_selected
      FUN_0806b604: resource_callback_dispatch
      DAT_08059b20: SELECTED_RESOURCE_CALLBACK
    notes: >-
      Raw osos.dec decode gives 72 instruction bytes plus the literal-pool
      callback word at 0x08059b20; the separately entered next function
      starts at 0x08059b24. It resolves the active inner resource for object,
      returns zero if resolution fails or that inner's selection bit for
      object+1 is clear, otherwise clears the selection bit through
      set_resource_selected(0, inner, object), then dispatches the inner
      +0x40 callback root with literal 0x080d43c4 and returns that status
      unchanged. Every ARM B/BL word in osos.dec was decoded: 16 direct
      unconditional bl call sites, no predicated calls and no data-word
      occurrence, plus one unconditional tail b at 0x0813d044. The literal
      0x080d43c4 starts in the middle of a larger firmware routine, so no
      callee identity is invented; resource_callback_dispatch preserves its
      recovered r4/r2 callback contract. The unported resolver, selection
      test, and setter retain direct target calls at 0x08051ce4, 0x08054710,
      and 0x08067450 through one volatile operation set; host tests replace
      it. Deliberate deviation: the ARM epilogue tail-branches to the
      dispatcher, while Rust makes the equivalent ordinary operation call.
      Host tests prove null resolution stops before selection, a clear bit
      leaves state untouched, and a set bit clears before dispatching the
      exact root/callback/context and propagates its nonzero status.
  - address: 0x0813b7c4
    ghidra: FUN_0813b7c4
    name: inner_set_state_4
    size: 12
    sig: "void inner_set_state_4(u8 *object)"
    module: util/inner_state
    status: ported
    vars: {param_1: object}
    notes: >-
      Three-instruction forwarding wrapper: `ldr r0,[r0,#0x40]; mov
      r1,#4; b 0x08067ca4` - loads the inner object from the query
      object's +0x40 slot and stores 4 into its state/mode word at
      +0xe38 (the tail target FUN_08067ca4 @ 0x08067ca4 is the 8-byte
      setter `str r1,[r0,#0xe38]; bx lr`, inlined by the port - the
      documented deviation). 1 bl call site: the query runner
      FUN_080feb68 @ 0x080feba4, which stores 4 right before running
      query 0x32 through the sibling wrappers FUN_0813bd10 (same shape,
      constant 0 -> 0x0806673c) and FUN_0813d064. The +0xe38 word is a
      small state/mode enum: other writers store 1 (0x08053fdc), 3
      (0x08054038) and 5 (0x08061020); the reader @ 0x0808cf4c compares
      it against 1 to pick a result code. Enum not identified - ported
      on observable behavior. Sits immediately after the util/berec.rs
      record-reader cluster @ 0x0813b714..0x0813b7b0 but is NOT one of
      them (no record handle, no big-endian decode), so it gets its own
      file. Host tests: stores 4, overwrites a previous value,
      idempotent, touches only the +0x40 slot and the +0xe38 word.
      match.py structural: same ldr/mov pair; the diff is the inlined
      store standing in for the tail branch plus the crate fp frame -
      the documented deviation.

  - address: 0x0813b7d0
    ghidra: FUN_0813b7d0
    name: inner_result_count
    size: 8
    sig: "u32 inner_result_count(u8 *object)"
    module: util/inner_state
    status: ported
    vars: {param_1: object, FUN_080542a0: INNER_MATERIALIZE_COUNT, FUN_08086694: materializer}
    notes: >-
      Two-instruction forwarding wrapper, the read-side sibling of
      inner_set_state_4 (FUN_0813b7c4, ported beside it):
      `ldr r0,[r0,#0x40]; b 0x080542a0` - loads the inner object from
      the query object's +0x40 slot and tail-branches to the
      materialize-and-count getter FUN_080542a0 @ 0x080542a0 (20 bytes,
      3 bl call sites), which calls the lazy materializer FUN_08086694
      @ 0x08086694 (480 bytes: when the cached result-array pointer at
      inner+0xeec is NULL it builds the array under a mutex, caching
      the array at +0xeec, an aux pointer at +0xef0 and the result
      count at +0xef4 - a no-op otherwise) and returns the count word
      at inner+0xef4. 1 bl call site: the query runner FUN_080feb68
      @ 0x080febc8, two instructions after its inner_set_state_4 call,
      which uses the result as the loop bound over the per-index record
      fetch FUN_0813b898 after running query 0x32 - the number of
      records the query produced. Deviation: the materializer is
      unported firmware (allocates, walks a record table, locks a
      mutex, nine further callees), so the whole tail target sits
      behind the INNER_MATERIALIZE_COUNT dispatch slot (the
      app/class_registry.rs pattern); the default stub is the
      materializer's no-op path - reads the cached count word at +0xef4
      without materializing, exact once a query has populated the
      cache, the only state the sole caller ever observes. Host tests:
      mock slot receives exactly the +0x40 slot value and its count is
      returned, exactly one dispatch; default stub reads +0xef4,
      touches nothing else (inner and outer byte-compared), returns 0
      for a fresh zeroed object. match.py structural: same
      ldr-r0-@-+0x40 + tail-branch shape; the diff is the crate fp
      frame and the dispatch-slot ldr/bx standing in for the direct
      `b 0x080542a0` - the documented deviation.

  - address: 0x08259770
    ghidra: FUN_08259770
    name: event_listener_kind_10_get
    size: 48
    sig: "void *event_listener_kind_10_get(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0825979c: event_listener_kind_10_cache (pool literal holding 0x08a09f6c)
      FUN_082aadd4: operator_new
      FUN_0825939c: event_listener_kind_10_construct
    notes: >-
      Raw ARM establishes the 48-byte true extent: 44 instruction bytes from
      0x08259770 through 0x08259798 plus pool word 0x08a09f6c at
      0x0825979c; the separately linked sibling begins at 0x082597a0.
      `push {r4,lr}; ldr r4,[pc,#32]; ldr r0,[r4]; cmp r0,#0; bne;
      mov r0,#0x44; bl operator_new; bl FUN_0825939c; str r0,[r4];
      ldr r0,[r4]; pop {r4,pc}` returns the cached 0x44-byte object,
      allocating and constructing it only when the cache is NULL. The
      post-construction reload is intentional: the constructor's return is
      stored unconditionally, and a NULL return retries on the next call.
      Full-image decoding of every ARM B/BL word found exactly 10 direct
      call sites, all unconditional `bl` (zero predicated forms and zero
      direct plain-B tails): 0x080f5a3c, 0x08104d58, 0x08104d74,
      0x08104f34, 0x08104f94, 0x081e67cc, 0x081e67f0, 0x081e6944,
      0x081e69e0, and 0x08258094. The unported constructor initializes
      an event-listener object then passes its +0x40 interface to
      event_hub_subscribe with kind 10, which establishes the name. No
      aligned image word equals 0x08259770, so it is statically called, not
      vtable-dispatched. Deliberate deviations: its unported constructor
      rides the SINGLETON_CTORS.event_listener_kind_10 dispatch slot with
      the established zeroing default, and the runtime cache word is
      represented by the EVENT_LISTENER_KIND_10 crate static. It is not
      hook-ready until FUN_0825939c is ported. Host tests prove exact
      0x44 allocation, raw-block constructor argument, cache fast path, and
      NULL-return retry behavior.

  - address: 0x082597a0
    ghidra: FUN_082597a0
    name: query_object_create
    size: 32
    sig: "void *query_object_create(u32 mode)"
    module: util/inner_state
    status: ported
    renames:
      param_1: mode
      FUN_082aadd4: operator_new
      FUN_0813e474: query_object_construct
    notes: >-
      Raw-byte extent is exactly 32 bytes (0x082597a0..0x082597bf); the
      next function starts at 0x082597c0. `push {r4,lr}; mov r4,r0; mov
      r0,#0x48; bl operator_new; mov r2,r4; pop {r4,lr}; mov r1,#0; b
      0x0813e474` allocates a 72-byte query object, supplies the original
      mode as constructor r2 and zero as its id r1, then returns the
      constructor result through the tail branch. Decoding every ARM B/BL
      word in osos.dec finds exactly 25 call sites, all unconditional bl
      (zero predicated forms and zero plain-b tails); callers therefore
      always create this object on their selected paths. Allocation has no
      NULL guard: the constructor is reached even when operator_new returns
      NULL. The constructor is unported but already has the shared
      fp_misc QUERY_OBJECT_CONSTRUCT seam: target calls its fixed
      0x0813e474 address and host tests substitute it. Deviation: Rust
      returns normally after that call rather than tail-branching; object,
      id, mode and return semantics are identical. Host tests prove exact
      tag-2 size 0x48 allocation, mode (including u32::MAX), id 0,
      constructor-result return, and unguarded NULL forwarding. match.py
      structural review requested for query_object_create.

  - address: 0x0813b6ec
    ghidra: FUN_0813b6ec
    name: drive_set_last_lba
    size: 8
    sig: "void drive_set_last_lba(u8 *drive, u32 last_lba)"
    module: util/drive
    status: ported
    vars: {param_1: drive, param_2: last_lba, FUN_08283a7c: drive_descriptor_builder, FUN_0813b650: drive_descriptor_ctor, FUN_0813b5d4: drive_set_block_size, FUN_082bcb74: drive_unit_count, DAT_08283c18: DRIVE_VTABLE}
    notes: >-
      Two-instruction field setter `str r1,[r0,#0x58]; bx lr` - stores
      the last-logical-block-address word at +0x58 of the 0x68-byte
      NAND-flash drive descriptor. 1 bl call site: the descriptor
      builder FUN_08283a7c @ 0x08283be8, which operator_new(0x68)s the
      object, tags it with the DRIVE_VTABLE literal and constructs it
      through FUN_0813b650 @ 0x0813b650 (fills "NAND_FLASH_DRIVE" at
      +0x13, serial "12345678901234567890" at +0x43, 0x2000000 at
      +0x60), then sets the geometry pair: FUN_0813b5d4 stores 0x1000
      at +0x64 (block size) and this setter stores FUN_082bcb74's
      result minus 1 at +0x58. FUN_082bcb74 @ 0x082bcb74 computes
      total_sectors / sectors_per_0x1000_unit = capacity in
      0x1000-byte blocks, so the pair is exactly the READ CAPACITY
      response shape (last LBA + block length) - hence the name. The
      +0x58 stores of 4/5 and 1/3/4/5 state comparisons in
      FUN_08283c1c/FUN_08283d20 belong to a different transfer-state
      object class (its accessors FUN_0816607c/FUN_081213dc touch
      +0x30/+0x44, offsets inside this descriptor's serial string), so
      this word has no other writer for the drive class. Sits
      immediately before the util/berec.rs record-reader cluster @
      0x0813b714..0x0813b7b0 and the util/inner_state.rs wrappers @
      0x0813b7c4/0x0813b7d0 but is NOT one of them (no record handle,
      no +0x40 inner forwarding), so it gets its own file - the
      inner_state.rs precedent. Deviation: none beyond the crate fp
      frame; the original leaves r0 = drive but the sole caller
      discards it, so the port is void. Host tests: stores the value,
      touches only the +0x58 word (whole 0x68-byte descriptor
      byte-compared), overwrites, round-trips 0/1/u32::MAX. match.py
      structural: identical `str r1,[r0,#0x58]`; the only diff is the
      crate-standard push-fp-lr/pop-fp-pc frame.

  - address: 0x08079d38
    ghidra: FUN_08079d38
    name: image_pixel_buffer_size_for_kind
    size: 44
    sig: "u32 image_pixel_buffer_size_for_kind(u32 kind)"
    module: app/image_format
    status: ported
    vars: {param_1: kind, local_24: descriptor, local_1c: height_bytes, uVar1: format, FUN_08105a34: image_format_for_kind, FUN_08105b58: image_format_descriptor}
    notes: >-
      The one-step "how many bytes does artwork kind N's pixel buffer
      need" query: `str lr,[sp,#-4]!; sub sp,sp,#0x24; bl
      image_format_for_kind; mov r1,r0; add r0,sp,#4; bl
      image_format_descriptor; ldr r0,[sp,#0xc]; ldr r1,[sp,#4]; add
      sp,sp,#0x24; mul r0,r1,r0; ldr pc,[sp],#4` - resolve the kind to
      a format (PLAIN table, not the kind-4 override: kind 4 here gives
      0x428's 307200, not 0x400's 153600), build the 32-byte descriptor
      on the stack, return desc[0] * desc[8] = width * height_bytes.
      height_bytes is height * bytes-per-pixel (with row padding, e.g.
      kind 8 -> 0x425: 55 wide but height_bytes 0x70 = 56*2), so the
      product is the pixel buffer's byte size. 5 bl call sites, no tail
      b (binary-scanned), all inside FUN_08211898 @ 0x08211898, which
      multiplies the result by an item count for an allocation-size
      computation (FUN_083eb9b4) and sizes a scratch buffer from
      size(9) + size(8) - confirming byte size, not pixel count.
      Deviation (inherited from image_format_descriptor's documented
      zero-fill): kinds without a format (9, 0xb, 0xd, out of range)
      leave desc[0]/desc[8] unstored in the original's default arm, so
      the original's product is stack garbage; the port answers a
      deterministic 0. Every kind with a format returns exactly what
      the original computes. Host tests: every kind 0..=17 vs an
      independently decoded width*height_bytes reference, spot checks
      at 16/32/12bpp and the kind-8 padded row, kinds without a format
      -> 0. match.py structural: same push/sub-sp/bl/bl/ldr/ldr/mul/pop
      shape; the diffs are the crate-standard fp frame and the
      descriptor living at sp+0 instead of sp+4 because of it.

  - address: 0x080a67dc
    ghidra: FUN_080a67dc
    name: f32_to_fixed16_sat
    size: 48
    sig: "i32 f32_to_fixed16_sat(u32 x)"
    module: fp/fp_fixed16
    status: ported
    renames:
      param_1: x
      DAT_080a680c: POS_SAT_THRESHOLD (0x46ffff00 = 32767.5f)
      0xc7000000: NEG_SAT_THRESHOLD (-32768.0f)
      FUN_083ed150: __fscalb
      FUN_083ec898: __f2i
    notes: >-
      f32 (soft-float bit pattern in r0) -> Q16.16 fixed-point s32 with
      clamping. Two saturation compares on the raw bit pattern, then a
      scale-and-truncate tail: signed cmp against literal 0x46ffff00
      (f32 >= 32767.5, +Inf, all positive NaN patterns) ->
      0x7fffffff; unsigned cmp against 0xc7000000 (f32 <= -32768.0,
      -Inf, all negative NaN patterns) -> 0x80000000; common path is
      __f2i(__fscalb(x, 16)) = trunc(x * 65536). Ghidra mislabels the
      function as non-returning (both saturation exits are ldmia
      sp!,{r4,pc}); it returns on every path. Referenced by the
      fixed16_cos/fixed16_sin literal pools. True extent confirmed from
      raw osos.dec: 12 instructions of code 0x080a67dc-0x080a680b (the
      last is a tail `b 0x83ec898`, not a bl) plus the one literal word
      at 0x080a680c; Ghidra's 48-byte size is code-only and correct.
      CORRECTION to the earlier analysis: the literal 0x46ffff00 decodes
      to 32767.5, NOT 32767.984375 (exp 0x8d, mantissa 0x7fff00). The
      thresholds are therefore asymmetric - the negative one is the exact
      -2^31 boundary, the positive one is well inside it, so the highest
      non-saturated output is 0x7fff7f80 and anything above jumps
      straight to i32::MAX. Reproduced faithfully.
      Both callees were already ported (fp/fp_scalb::__fscalb,
      fp/fp_fconv::__f2i), so the port calls them directly - no dispatch
      seam is needed and none was added. Saturation absorbs every
      non-finite input, so the helpers only ever see finite |x| < 32768:
      __fscalb takes its exponent-add fast path (biased exp <= 0x8d) and
      __f2i sees |value| < 2^31, meaning neither helper's error/trap path
      is live from here. __fscalb flushes subnormals to +0.0 instead of
      scaling them, which is unobservable because truncation would yield
      0 anyway. Host tests (9) cover both saturation boundaries and the
      ulp on each side, +/-Inf and both NaN signs, +/-0, subnormals,
      exact powers of two incl. the 2^-16 ulp and 2^-17 underflow,
      trunc-toward-zero on both signs, and a ~2000-case sweep against an
      f64 oracle; the two firmware-witnessed constants from the
      fixed16_sin/cos literal pools are pinned exactly
      (2pi -> 0x6487e, 1/2pi -> 0x28be). match.py structural: same
      two-compares-then-scale-then-truncate shape; LLVM inlined the
      __fscalb/__f2i bodies (12 instructions -> 85) and rewrote both
      compares - signed `> 0x46fffeff` for the high threshold and a
      top-byte `lsr #24; cmp #0xc6; bhi` for the unsigned 0xc7000000
      one - exactly as the earlier pass predicted.

  - address: 0x080e9310
    ghidra: FUN_080e9310
    name: scan_decimal_cursor
    size: 60
    sig: "u32 scan_decimal_cursor(const char **cursor)"
    module: strto/decimal_cursor
    status: ported
    renames:
      param_1: cursor
      pbVar2: input
      iVar1: acc
      DAT_080e934c: DECIMAL_DIGIT_CLASS
    notes: >-
      Cursor-advancing decimal scanner. The byte-class table's 0x10 bit
      admits ASCII '0'..'9'; for every accepted byte, accumulate
      acc = acc*10 + (byte - '0') with 32-bit wrapping arithmetic, then
      store the first non-digit cursor through the input slot. The port
      spells the recovered undefined4 cursor slot as const char ** and
      returns u32 instead of int; both preserve the target's pointer and
      return-word bit patterns. Host tests cover cursor update,
      non-digit termination, and overflow wrapping. match.py structural:
      table-classification loop, shift-add/multiply-by-10 accumulation,
      and final cursor store.
  - address: 0x0808ce8c
    ghidra: FUN_0808ce8c
    name: parse_wide_decimal_counted
    size: 56
    sig: "u32 parse_wide_decimal_counted(const u16 **cursor, i32 count)"
    module: strto/wide_decimal_counted
    status: ported
    renames:
      param_1/r0: cursor
      param_2/r1: count
      r2: input
      r3: value
      r12: code_unit
      lr: radix
    notes: >-
      Raw osos.dec is exactly fourteen ARM words from 0x0808ce8c through
      bx lr at 0x0808cec0; the next separately linked function starts at
      0x0808cec4. Decoding every ARM B/BL word finds exactly nine direct
      callers (0x0807dfd0, 0x0807dfe0, 0x0807dff0, 0x0807e024, 0x0807e034,
      0x0807e044, 0x0820fdc4, 0x0820fde4, 0x0820fe04), all unconditional
      bl and none predicated. For each positive signed count, load a u16
      through the caller-owned cursor, immediately advance the cursor slot,
      then compute value = value * 10 + code_unit - '0' with wrapping
      32-bit arithmetic. There is deliberately no ASCII-digit or Unicode
      validation: every u16 contributes. A count <= 0 touches neither
      pointer and returns zero. The port spells Ghidra's undefined4 cursor
      slot as const u16 ** and its int return as u32, preserving target
      pointer layout and the return-word bit pattern. Host tests cover exact
      cursor advancement, unchecked non-digits, u32 wraparound, and the
      signed no-dereference guard. Volatile cursor-slot accesses retain the
      target's per-iteration load/store rather than allowing LLVM to hoist
      the cursor load. match.py structural review against this worktree's
      archive reports the expected non-exact 19-instruction LLVM form:
      signed early return plus counted u16-load/cursor-store/accumulate loop.
  - address: 0x0804ad80
    ghidra: FUN_0804ad80
    name: scan_stream_allocate_zeroed_scratch
    size: 44
    sig: "*mut u8 scan_stream_allocate_zeroed_scratch(void)"
    module: stdio/scan_stream_setup
    status: ported
    renames:
      puVar1: scratch
      FUN_08043c18: scan_stream_backing_alloc
    notes: >-
      Calls the internal allocator with its three target arguments
      `(16, 0, 0)`, stores zero in all four words of the returned 16-byte
      area in ascending-address order, and returns its base. It deliberately
      has no NULL guard: the first word store preserves the target's fault
      behavior on allocation failure. Host tests record the allocator ABI,
      prove return forwarding, and bound the four initialized words exactly.

  - address: 0x080e9364
    ghidra: FUN_080e9364
    name: scan_stream_setup_scratch
    size: 44
    sig: "bool scan_stream_setup_scratch(ScanStreamState *state)"
    module: stdio/scan_stream_setup
    status: ported
    renames:
      param_1: state
      iVar1: scratch
      FUN_0804ad80: scan_stream_allocate_zeroed_scratch
    notes: >-
      Allocates a 16-byte zeroed scratch area through the ported
      `scan_stream_allocate_zeroed_scratch`. A NULL helper result returns
      false and preserves every state field; success stores the result at
      +0x20, clears the independently recovered words at +0x0c and +0x14,
      and returns true. Host tests cover helper success, NULL failure, and
      preservation of fields not written by the firmware. match.py
      structural: the same helper-call/null-guard/three-store result shape;
      the function-pointer dispatch adds an indirect call and standard LLVM
      frame.


  - address: 0x080e9974
    ghidra: FUN_080e9974
    name: atoi_decimal
    size: 44
    sig: "i32 atoi_decimal(const char *s)"
    module: strto/atoi_decimal
    status: ported
    renames:
      param_1: s
      iVar1: acc
    notes: >-
      Minimal decimal atoi: signed i32 accumulator wraps modulo 2^32 as
      the ARM add/shift arithmetic does: acc = acc*10 + (byte - '0').
      The unsigned byte test accepts exactly '0' through '9' and stops on
      every other byte, including NUL. No whitespace skip, sign parsing,
      end pointer, or overflow check. Six bl call sites, all inside
      FUN_08056d70 @ 0x08056d70, parse a space-separated line of six
      decimal fields into a struct at offsets 0x11c..0x128. This is not
      the ADS strtol-wrapper libc atoi at 0x0802f990. match.py structural
      review: same byte load, unsigned digit-range check, wrapping
      multiply-by-ten accumulation, and repeat loop.

  - address: 0x080e99a0
    ghidra: FUN_080e99a0
    name: clock_pair_subtract
    size: 56
    sig: "void clock_pair_subtract(const ClockPair *left, const ClockPair *right, ClockPair *difference)"
    module: util/clock_pair
    status: ported
    renames:
      param_1: left
      param_2: right
      param_3: difference
      iVar2: left_days
      iVar3: right_days
      iVar1: second_difference
    notes: >-
      Subtracts {signed days, seconds-of-day}; stores both differences,
      then borrows one day only when the signed seconds difference is
      negative. The borrow radix is exactly 0x15180 (86,400).

  - address: 0x0812bf70
    ghidra: FUN_0812bf70
    name: task_message_post_sync
    size: 20
    sig: "u32 task_message_post_sync(usize reply_queue, usize target_queue, const u32 *message, u32 flags)"
    module: kernel/task_message
    status: ported
    renames:
      param_1: reply_queue
      param_2: target_queue
      param_3: message
      param_4: flags
    notes: >-
      2 bl call sites (0x08111050 in FUN_08110fdc, 0x08125228 in
      FUN_081251cc). Pure argument plumbing in front of the message-post
      helper @ 0x0812c088: forwards (reply_queue, target_queue, message,
      flags) with the wait flag (r3) forced to 1, and returns the
      helper's 1-on-success / 0-on-failure result. The pushed r3 slot is
      the helper's fifth (stack) argument, not a saved register. Mirror
      image @ 0x0812c628 (FUN_0812c628) is the identical 20-byte body
      with mov r3, #0x0 - the fire-and-forget flavor. The helper
      allocates a message cell from the global pool (locked alloc
      0x0812bf9c), copies the 3-word tagged message, and posts through
      0x080944b0 when wait != 0 (else 0x080f117c); both bottom out in
      the queue send @ 0x0809eb58, whose wait-for-reply argument blocks
      the sender until the reply - hence _sync. Both call sites build a
      3-word stack message {FourCC tag, arg, arg} and pass queue handles
      read from task context blocks (+0x1c, cf. current_task_ctx_block @
      0x080cb828). Sig corrected from the scouted "void
      FUN_0812bf70(void)": both callers consume the result, so four
      arguments returning u32. Semantics of the fourth argument
      (forwarded verbatim to the send's cell-blocking flag) not yet
      identified; the name follows the data flow. The helper is not yet
      ported, so the port dispatches through a new
      TASK_MESSAGE_OPS.post_message slot (TimerOps pattern; default stub
      returns 0). match.py structural: the str r3,[sp] stack-arg store,
      mov r3,#1 and the call are intact; the bl becomes a table-load blx
      and LLVM uses a frame, same deviation shape as
      timer_schedule_shim.

  - address: 0x0810f9f0
    ghidra: FUN_0810f9f0
    name: object_dispatch_entry_construct
    size: 40
    sig: "ObjectDispatchEntry *object_dispatch_entry_construct(ObjectDispatchEntry *this, void *owner)"
    module: app/object_dispatch_entry
    status: ported
    renames:
      param_1: this
      param_2: owner
      iVar1: drain_state
      FUN_08271cec: construct_drain_state
      FUN_0807f680: construct_condition_variable
    notes: >-
      Constructs the 0x24-byte object-dispatch entry allocated by
      FUN_0810f548 (operator_new(0x24) @ 0x082aadd4), which keys its
      registry records by the owner object pointer. Stores owner at +0,
      delegates the +4..+0x13 embedded drain state to 0x08271cec
      (including its 0x08275bb8 base constructor), clears
      pending_dispatch at +0x14, and initializes the three-word
      condition-variable state at +0x18 through 0x0807f680. The returned
      drain-state base is adjusted by -4 before subsequent stores and the
      final return; this is the literal `sub r4, r0, #4` at 0x0810f9fc,
      not an assumption that the incoming allocation survives the callee.
      Reference C: decomp/c/010/0810f9f0_FUN_0810f9f0.c; dependencies:
      decomp/c/026/08271cec_FUN_08271cec.c and
      decomp/c/005/0807f680_FUN_0807f680.c; assembly: osos.asm
      0x0810f9f0. The two unported dependency calls are explicit
      ObjectDispatchEntryOps injection seams and fail hard until target
      integration installs them. Focused host test records both call bases,
      order, fields, and the adjusted return. match.py structural: stock
      post-index owner store, drain-state call, return-base adjustment,
      zero store, condition-variable call, and return are all retained;
      dependency seams lower to a volatile ops-table load plus blx calls.
      UPDATE: 0x08271cec is now ported (cxx/observable_array,
      observable_array_construct) and is called DIRECTLY; the entry's
      +0x04 subobject is that class, so the local DrainState model was
      replaced by ObservableArray and only the condition-variable seam
      remains.

  - address: 0x080e99d8
    ghidra: FUN_080e99d8
    name: raster_profile_append_edge
    size: 332
    sig: "u64 raster_profile_append_edge(RasterProfileState *state, u32 x, u32 y)"
    module: util/raster_profile
    status: ported
    renames:
      param_1: state
      param_2: x
      param_3: y
      cVar1: profile_direction
      iVar2: callback_error
      uVar4: carried_ordinate
      FUN_08076228: end_profile
      FUN_080767a8: begin_profile
      FUN_080e9b24: emit_profile_segment
    notes: >-
      Raster-edge profile transition state machine. Signed previous_y/y
      comparisons start ascending (1) or descending (2) profiles, and a
      direction change first ends the old profile. Ascending emission carries
      y; descending emission negates y and both limits, then negates the
      active profile's +0x14 accumulator exactly when the pending-profile
      byte changes from nonzero to zero. Callback failure returns low-word 1
      and does not advance previous_x/previous_y; success writes both and
      returns the carried value in the high word. Reference C:
      decomp/c/008/080e99d8_FUN_080e99d8.c; assembly: osos.asm
      0x080e99d8. Unported dependencies 0x08076228, 0x080767a8, and
      0x080e9b24 are volatile RasterProfileOps seams (retail entry-point
      wrappers on ARM, mocks in host tests). match.py structural: direction
      branches, signed comparisons, callback error joins, descending
      negations, and final pair/state stores are retained; ops-table calls
      lower to indirect blx calls.

  - address: 0x080e9b24
    ghidra: FUN_080e9b24
    name: raster_profile_emit_segment
    size: 408
    sig: "u32 raster_profile_emit_segment(RasterProfileState *state, i32 previous_x, i32 previous_y, i32 x, i32 y, i32 lower_limit, i32 upper_limit)"
    module: util/raster_profile
    status: ported
    renames:
      param_1: state
      param_2: previous_x
      param_3: previous_y
      param_4: x
      param_5: y
      param_6: lower_limit
      param_7: upper_limit
      iVar2: first_x
      iVar3: dda_error
      iVar4: delta_x
      iVar5: delta_y
      piVar6: scanline_cursor
      cVar1: reusable_final_scanline
      FUN_0804d1a8: scaled_divide
      FUN_08031568: quotient_remainder
    notes: >-
      Emits a strictly ascending raster edge after inclusive vertical clipping.
      It derives the first/last scanline by arithmetic right shift, removes a
      reusable exact prior endpoint, records an exact final endpoint for the
      following edge, initializes a pending profile's +0x14 accumulator, then
      advances x through a quotient/remainder DDA. If the scanline buffer has
      no room, it stores 0x62 at state+0x30 and returns 1 before any output
      store; all outside/descending edges return 0 without mutation. Reference
      C: decomp/c/008/080e9b24_FUN_080e9b24.c; assembly: osos.asm
      0x080e9b24. The unported signed scaled-divider 0x0804d1a8 and signed
      quotient/remainder helper 0x08031568 are RasterProfileOps seams; ARM
      defaults call retailOS, while host tests use deterministic arithmetic
      mocks. match.py structural: clip/reuse branches, capacity error path,
      pending-profile initialization, and DDA loop are retained.

  - address: 0x0811000c
    ghidra: FUN_0811000c
    name: app_object_init
    size: 52
    status: duplicate
    notes: >-
      Second, independent port of the same 52-byte function already ported as
      node_list_construct (module app/node_list) — same address, same object.
      The "application object" reading was the weaker one: the vtable 0x0898165c
      it installs is the view-list vtable, and the lazy accessor FUN_0810fa30
      both entries cite is node_list_get, whose whole class (enqueue, advance,
      destructor) lives in app/node_list.rs. The duplicate implementation was
      removed; node_list_construct is canonical and is the one its own module
      calls. One observation from this entry is worth keeping and is recorded
      in the canonical port's doc comment: after `bl 0x08271cec` the function
      does `sub r0, r0, #16` and the five trailing zero stores are addressed
      off THAT derived base, not off the incoming pointer. The two differ only
      if the embedded constructor returns something other than this+0x10.

  - address: 0x0811c138
    ghidra: FUN_0811c138
    name: app_string_owner_initialize
    size: 32
    sig: "void *app_string_owner_initialize(void *this, u32 owner_word, void *argument)"
    module: app/string_owner_init
    status: ported
    renames:
      param_1: this
      param_2: owner_word
      param_3: argument
      DAT_0811c158: APP_STRING_OWNER_VTABLE
      FUN_0827735c: initialize_string_object
      iVar2: helper_result
    notes: >-
      28 code bytes plus the 4-byte vtable literal at 0x0811c158
      (= 0x089a7428, binary-verified). Initializer paired with the
      0x0811c15c destructor of an otherwise unidentified application object:
      it plants that outer vtable at +0, stores the caller's owner_word at +4,
      and invokes the unported embedded StringObject constructor
      FUN_0827735c(this + 8, argument). It returns that helper result minus 8
      with wrapping pointer arithmetic, preserving ARM `sub r0, r0, #8` at
      every address. The helper's argument-consuming virtual construction is
      intentionally modeled as a volatile application operation seam rather
      than claimed as a port of FUN_0827735c; its default establishes only the
      independently verified StringObject base layout. Reference C:
      decomp/c/010/0811c138_FUN_0811c138.c; dependency:
      decomp/c/026/0827735c_FUN_0827735c.c; assembly: osos.asm 0x0811c138.
      Focused tests prove the +0/+4 field layout, exact +8 helper and argument
      forwarding, and zero-address wrapping return adjustment. match.py
      structural: outer stores, +8 helper base, argument forwarding, and
      subtract-8 remain; the seam lowers to a volatile ops-table load plus blx.

  - address: 0x0811c15c
    ghidra: FUN_0811c15c
    name: app_string_owner_destroy
    size: 24
    sig: "void *app_string_owner_destroy(void *this)"
    module: app/string_owner
    status: ported
    renames:
      param_1: this
      DAT_0811c174: APP_STRING_OWNER_VTABLE
      FUN_08277484: string_object_destroy
      iVar1: embedded_string
    notes: >-
      20 code bytes plus the 4-byte vtable literal at 0x0811c174
      (= 0x089a7428, binary-verified). Plain non-deleting destructor of an
      unidentified application object: it plants that vtable at +0, leaves
      the opaque +4 word untouched, destroys the embedded StringObject at +8
      through string_object_destroy (0x08277484), then returns the helper
      result minus 8. The port's subtraction uses wrapping pointer arithmetic
      to preserve the ARM `sub r0, r0, #8` result at every address. Reference
      C: decomp/c/010/0811c15c_FUN_0811c15c.c; dependency:
      decomp/c/026/08277484_FUN_08277484.c; assembly: osos.asm 0x0811c15c.
      Deliberate codegen deviation: the already-ported direct helper call is
      exposed through a volatile AppStringOwnerOps seam so host tests can
      prove helper argument/return dataflow and wraparound. Focused tests
      prove the +0 vtable store, untouched +4 word, +8 helper argument,
      helper-derived return, and zero-address wraparound. match.py structural:
      literal load, post-index +8 equivalent, helper call, and subtract-8
      remain; the volatile seam becomes an indirect blx.

  - address: 0x0811c178
    ghidra: FUN_0811c178
    name: object_dispatch_entry_dispatch
    size: 16
    sig: "u32 object_dispatch_entry_dispatch(ObjectDispatchSource *source)"
    module: app/object_dispatch_entry
    status: ported
    renames:
      param_1: source
      iVar1: target_vtable
    notes: >-
      Tail-dispatch veneer: loads the target pointer from source+0x88, loads
      its vtable, then branches to vtable+0x4c with the selected target as r0.
      The target class and slot semantics remain unidentified, so the Rust
      signature preserves the raw returned r0 word. Reference C:
      decomp/c/010/0811c178_FUN_0811c178.c; raw assembly: osos.asm
      0x0811c178. Focused host tests prove +0x4c slot selection, target
      forwarding, and raw return-word propagation. match.py structural:
      source +0x88 load, target-vtable load, +0x4c indirect tail dispatch.

  - address: 0x0811c298
    ghidra: FUN_0811c298
    name: object_dispatch_entry_dispatch_vtable_slot_50
    size: 16
    sig: "void object_dispatch_entry_dispatch_vtable_slot_50(ObjectDispatchSource *source)"
    module: app/object_dispatch_entry
    status: ported
    renames:
      param_1: source
      iVar1: target_vtable
    notes: >-
      Tail-dispatch veneer: loads the target pointer from source+0x88, loads
      its vtable, then branches to the separately verified vtable+0x50 slot
      with the selected target as its sole r0 argument. The target class and
      slot action remain unidentified, so the Rust signature faithfully
      exposes a void tail call without inventing an operation name. Reference
      C: decomp/c/010/0811c298_FUN_0811c298.c; raw assembly: osos.asm
      0x0811c298. Focused host tests prove +0x50 selection (not +0x4c) and
      exact selected-target forwarding. match.py structural: source +0x88
      load, target-vtable load, +0x50 indirect tail dispatch; LLVM adds a
      frame prologue/epilogue around the tail branch.

  - address: 0x0811c284
    ghidra: FUN_0811c284
    name: flag_2c_is_one
    size: 20
    sig: "u32 flag_2c_is_one(const void *obj)"
    module: app/flag_2c_is_one
    status: ported
    renames:
      param_1: obj
    notes: >-
      Pure offset-based predicate on an otherwise unidentified application-side
      object: it loads byte obj+0x2c and returns 1 only when the byte is
      exactly 1; it returns 0 for 0 and for every value 2 through 255. This is
      deliberately not the complement of ui::flag_2c::flag_2c_is_clear
      (0x0811c564), because values above 1 are false for both routines. The
      port makes no object layout or ownership claim beyond this readable byte.
      Reference C: decomp/c/010/0811c284_FUN_0811c284.c; assembly: osos.asm
      0x0811c284 (`ldrb +0x2c; cmp #1; movne/moveq`). Focused host tests cover
      0, 1, 2, and 255 plus an unaligned base. match.py structural output is
      an expected LLVM compare-lowering diff (exit 1).

  - address: 0x0811f720
    ghidra: FUN_0811f720
    name: ui_descriptor_init_static_span
    size: 28
    sig: "void ui_descriptor_init_static_span(void *descriptor)"
    module: ui/static_descriptor
    status: ported
    renames:
      param_1: descriptor
      DAT_0811f73c: UI_DESCRIPTOR_STATIC_BASE
      iVar1: static_base
    notes: >-
      Initializes only three observed fields of a caller-owned UI descriptor:
      pointer 0x08982f40 at +0x00, that base plus 0x20 at +0x04, and u16
      0x0400 at +0x0c. The remaining object layout is unidentified and
      untouched. Reference C: decomp/c/011/0811f720_FUN_0811f720.c; literal
      at 0x0811f73c; assembly: osos.asm 0x0811f720. Three callers overwrite
      additional fields after this common initialization.

  - address: 0x0811f748
    ghidra: FUN_0811f748
    name: ui_descriptor_init_static_span_with_parameter
    size: 32
    sig: "void *ui_descriptor_init_static_span_with_parameter(void *descriptor, u32 parameter)"
    module: ui/static_descriptor
    status: ported
    renames:
      param_1: descriptor
      param_2: parameter
      piVar2: descriptor
      iVar1: static_base
      DAT_0811f768: UI_DESCRIPTOR_STATIC_BASE
    notes: >-
      Calls ui_descriptor_init_static_span (0x0811f720), then writes the
      static base 0x08982f40 at +0x00, its +0x24 endpoint at +0x04, and the
      caller parameter at +0x08; the callee's u16 0x0400 at +0x0c remains.
      r0 reaches the return unchanged from the callee and is the descriptor
      pointer, although the reference C inferred void. Reference C:
      decomp/c/011/0811f748_FUN_0811f748.c; literal at 0x0811f768; assembly:
      osos.asm 0x0811f748. A volatile function-pointer seam prevents LLVM
      from inlining the ported 0x0811f720 callee, so its direct bl becomes an
      indirect blx; focused host tests prove output pointer, field values,
      and callee routing. match.py structural: preserves the callee route,
      static-base literal, +0x24 span, +0x08 parameter store, and descriptor
      return.

  - address: 0x0811f598
    ghidra: FUN_0811f598
    name: ui_dispatch_vtable_slot_20
    size: 16
    sig: "usize ui_dispatch_vtable_slot_20(void *dispatch_record, usize discarded_r1, usize forwarded_r2, usize forwarded_r3)"
    module: ui/vtable_slot_20
    status: ported
    renames:
      param_1: dispatch_record
      r0_after_first_ldr: object
      r1_after_second_ldr: vtable
      r1_after_third_ldr: slot_20_method
      r2: forwarded_r2
      r3: forwarded_r3
    notes: >-
      Four-instruction UI data-provider slot dispatcher: loads object from
      dispatch_record+0x04, loads its vtable at +0x00, loads slot +0x20, and
      tail-branches to it. The raw ARM uses r1 for both vtable and method, so
      it discards incoming r1 and passes the method address as r1; r2/r3 and
      the method's usize return are preserved. UI layout callers use the same
      +0x20 query alongside slots +0x18/+0x24. Reference C:
      decomp/c/011/0811f598_FUN_0811f598.c; raw assembly:
      osos.asm 0x0811f598. No NULL/alignment checks or vtable-target port were
      added. Host tests build byte-accurate ARM-layout records and verify the
      object, overwritten r1, forwarded r2/r3, one slot invocation, and return
      value. match.py structural diff is expected (exit 1).

  - address: 0x0811f5a8
    ghidra: FUN_0811f5a8
    name: ui_dispatch_vtable_slot_24
    size: 16
    sig: "usize ui_dispatch_vtable_slot_24(void *dispatch_record, usize forwarded_r1, usize discarded_r2, usize forwarded_r3)"
    module: ui/vtable_slot_24
    status: ported
    renames:
      param_1: dispatch_record
      r0_after_first_ldr: object
      r2_after_second_ldr: vtable
      r2_after_third_ldr: slot_24_method
      r1: forwarded_r1
      r3: forwarded_r3
    notes: >-
      Four-instruction opaque UI data-provider slot dispatcher: loads object
      from dispatch_record+0x04, loads its vtable at +0x00, loads slot +0x24,
      and tail-branches to it. The raw ARM uses r2 for both vtable and method,
      so it preserves incoming r1, passes the method address as r2, preserves
      r3, and returns the method's usize result. Confirmed from raw ARM at
      0x0811f5a8 and callers 0x08279564 (r1=0) and 0x0827b2a8 (r1=1).
      Reference C: decomp/c/011/0811f5a8_FUN_0811f5a8.c. No NULL/alignment
      checks or vtable-target port were added. Host test builds byte-accurate
      ARM-layout records and verifies slot +0x24, object transfer into r0,
      r1/r3 forwarding, r2 method-address replacement, one invocation, and
      return value. match.py structural diff is expected (exit 1).

  - address: 0x0811f7f4
    ghidra: FUN_0811f7f4
    name: ui_noop_f7f4
    size: 4
    sig: "void ui_noop_f7f4(void)"
    module: ui/noop_f7f4
    status: ported
    renames: {}
    notes: >-
      The complete raw ARM body is `bx lr`, making this a no-argument,
      no-state ABI-visible return. Decompilation lists a void signature.
      Several object-building callers leave an object-derived r0 value live
      across this call, but neither that use nor the empty body establishes an
      object role or a return contract. Reference C:
      decomp/c/011/0811f7f4_FUN_0811f7f4.c; raw ARM at 0x0811f7f4. Focused
      host test proves that the no-argument port returns without changing
      observable state. match.py reports the expected LLVM stack-frame-only
      structural diff (exit 1).

  - address: 0x0811f76c
    ghidra: FUN_0811f76c
    name: ui_resource_release
    size: 72
    sig: "i32 ui_resource_release(void)"
    module: ui/resource_release
    status: ported
    renames:
      iVar1: resource_state
      iVar2: release_result
      FUN_081f5034: lazy_ui_resource_state
      FUN_0838f764: resource_release
    notes: >-
      Gets the lazy 0x18-byte UI resource state, whose only identified fields
      are its resource word at +0x04 and active byte at +0x08. An empty
      resource returns 0 without calling the release helper. Otherwise it
      passes the resource to FUN_0838f764, returns any nonzero helper result
      unchanged, and only on zero clears +0x08 then +0x04. Reference C:
      decomp/c/011/0811f76c_FUN_0811f76c.c; caller shutdown sequence:
      decomp/c/004/08068d68_FUN_08068d68.c. FUN_081f5034 and FUN_0838f764 are
      unported and are exposed through a volatile UI resource lifecycle seam;
      its default empty state safely takes the early-return path. Focused host
      tests cover empty, helper-failure, and success-clear behavior. match.py
      structural output includes the seam's indirect calls and reports its
      expected diff (exit 1).

  - address: 0x0811f7e0
    ghidra: FUN_0811f7e0
    name: ui_store_byte
    size: 8
    sig: "void ui_store_byte(u8 *destination, u8 value)"
    module: ui/byte_store
    status: ported
    renames:
      param_1: destination
      param_2: value
    notes: >-
      Stores only the low byte of r1 to the byte addressed by r0 (`strb r1,
      [r0]`) and returns immediately. The target pointer may be unaligned;
      no validation, adjacent-byte access, or return side effect is present.
      Reference C: decomp/c/011/0811f7e0_FUN_0811f7e0.c; assembly:
      osos.asm 0x0811f7e0. Callers pass stack-local storage onward, but do
      not establish UI-object ownership. Focused host tests prove an
      unaligned destination changes exactly one byte.

  - address: 0x0811f7e8
    ghidra: FUN_0811f7e8
    name: ui_copy_byte
    size: 12
    sig: "void ui_copy_byte(u8 *destination, const u8 *source)"
    module: ui/byte_store
    status: ported
    renames:
      param_1: destination
      param_2: source
    notes: >-
      Loads one byte from source with `ldrb r1, [r1]`, then writes it to
      destination with `strb r1, [r0]`, returning immediately. The source is
      read before the destination write, including when both pointers alias;
      neither pointer is validated or required to be aligned. Reference C:
      decomp/c/011/0811f7e8_FUN_0811f7e8.c; assembly: osos.asm
      0x0811f7e8. Focused host tests cover one-byte-only copying, independent
      source and destination offsets, and aliasing.

  - address: 0x0811ca48
    ghidra: FUN_0811ca48
    name: app_string_resolve
    size: 16
    sig: "void *app_string_resolve(void *resolver, void *context, void *value)"
    module: app/string_resolve
    status: ported
    renames:
      param_1: resolver
      param_2: context
      param_3: value
      FUN_0811ca58: app_string_resolver_resolve
    notes: >-
      The reference C falsely infers a void no-argument wrapper. Raw ARM is
      `push {r3,lr}; mov r3,sp; bl 0x0811ca58; pop {ip,pc}`: it preserves
      r0-r2, reserves one uninitialized stack word, passes its address in r3
      to app_string_resolver_resolve, and leaves the callee return in r0.
      Known callers consume that result as a C string. Reference C:
      decomp/c/010/0811ca48_FUN_0811ca48.c; resolver and callers:
      decomp/c/010/0811ca58_FUN_0811ca58.c,
      decomp/c/028/0829a124_FUN_0829a124.c, and
      decomp/c/028/0829a4dc_FUN_0829a4dc.c; assembly: osos.asm 0x0811ca48.
      Focused host tests prove argument forwarding, an aligned writable
      output slot, discarded secondary output, and return propagation.
  - address: 0x0811ca58
    ghidra: FUN_0811ca58
    name: app_string_resolver_resolve
    size: 172
    sig: "void *app_string_resolver_resolve(void *resolver, void *context, void *value, void **output_slot)"
    module: app/string_resolve
    status: ported
    renames:
      param_1: resolver
      param_2: context
      param_3: value
      param_4: output_slot
      FUN_0812d20c: provider_lookup
      FUN_0815bcd4: primary_fallback_lookup
      local_18: resolved_record_slot
    notes: >-
      A tagged app-string resolver: tag 1 resolves its +0x04 provider source;
      tag 2 first tries its optional +0x18 parent source, then delegates its
      +0x04 source to the range-table fallback. A successful provider lookup
      yields a two-word borrowed record: word 0 is stored through output_slot
      and word 1 is returned. A failed tag-1 lookup clears output_slot; an
      unknown tag returns NULL without writing it. Tag-2 fallback result and
      output are propagated unchanged. Provider and range-table formats are
      deliberately retained behind their original 0x0812d20c/0x0815bcd4 call
      boundaries. Raw ARM is authoritative over the reference C for the
      four-register ABI and control flow. Focused host tests cover provider
      output/return propagation, tag-1 failure, parent precedence, fallback,
      and unknown-tag output preservation. match.py structural report differs
      as expected (exit 1).

  - address: 0x08101cfc
    ghidra: FUN_08101cfc
    name: string_table_set_current_hex
    size: 76
    sig: "void string_table_set_current_hex(u8 *table, char **key, u32 value)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: key
      r2: value
      FUN_0802f724: sprintf
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_08101da0: string_table_assign
      FUN_083d8b04: cxx_string_release
    notes: >-
      Formats value as lowercase unsigned hexadecimal through sprintf("%lx")
      into the 512-byte stack buffer, constructs a temporary COW string,
      assigns it to key in the CURRENT selected string table through
      FUN_08101da0, then releases the temporary. Ghidra drops the r2 value
      parameter and labels the two-argument cxx_string_from_cstr call with a
      stale third argument; raw ARM is authoritative. The raw extent is
      0x08101cfc..0x08101d48: the return is at 0x08101d44, the following
      word is the "%lx" literal, and the distinct decimal-setter entry opens
      at 0x08101d4c, making Ghidra's 76-byte extent exact. Decoding every
      aligned ARM B/BL word in osos.dec finds 11 direct bl call sites, all
      unconditional, plus zero b references and zero data-word references.
      sprintf, cxx_string_from_cstr, and cxx_string_release are ported.
      string_table_assign remains the existing volatile seam whose target
      default transmutates 0x08101da0; the Rust sprintf veneer takes an
      explicit va-list pointer, so the port passes &value rather than the
      original variadic r2 word. Host coverage records table/key assignment
      and verifies 0, 0x2a, 0xdeadbeef, and 0xffffffff formatting. match.py
      structural output differs as expected (exit 1).

  - address: 0x08101d4c
    ghidra: FUN_08101d4c
    name: string_table_set_decimal
    size: 80
    sig: "void string_table_set_decimal(u8 *table, char **key, const i32 *decimal)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: key
      param_3/r2: decimal
      FUN_0802f724: sprintf
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_08101da0: string_table_assign
      FUN_083d8b04: cxx_string_release
    notes: >-
      Formats *decimal as signed decimal through sprintf("%d") into a
      512-byte stack buffer, makes a temporary COW string from that buffer,
      assigns it to key in the current localization table through
      FUN_08101da0, then releases the temporary. Raw extent is
      0x08101d4c..0x08101d9c: the following word is the distinct
      FUN_08101da0 entry at 0x08101da0, so Ghidra's 80-byte size is exact.
      Decoding every ARM B/BL word in osos.dec finds 19 bl call sites, all
      unconditional; no predicated forms. sprintf, cxx_string_from_cstr,
      and cxx_string_release are ported. The unported assignment helper
      remains a volatile seam whose device default transmutates
      0x08101da0; its body selects the current 0x1c-byte table and assigns
      into the key's mapped COW-string slot. The Rust sprintf veneer takes
      an explicit va-list pointer, so the port passes &decimal_value rather
      than the original variadic r2 word. Host test uses a faithful %d
      engine and a heap-backed COW string fixture to cover zero, positive,
      negative, and i32::MIN values; it also proves table and key
      propagation into the assignment seam. match.py structural report
      differs as expected (exit 1).

  - address: 0x08101f94
    ghidra: FUN_08101f94
    name: string_table_set_hex
    size: 84
    sig: "void string_table_set_hex(u8 *table, char **key, u32 value)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: key
      r2: value
      FUN_0802f724: sprintf
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_083db69c: string_table_fallback_value_slot
      FUN_083d8d1c: cxx_string_assign
      FUN_083d8b04: cxx_string_release
    notes: >-
      Formats value through sprintf("%lx") into a 512-byte stack buffer,
      builds a temporary COW string with cxx_string_from_cstr, resolves
      the mapped-value slot for key in the FALLBACK (default-language)
      table at table+0x38 through FUN_083db69c, assigns the temporary
      into that slot with cxx_string_assign, and releases it. value is
      taken BY VALUE in r2 - Ghidra's C drops the third parameter
      entirely, and its three-argument rendering of the two-argument
      cxx_string_from_cstr call is register residue. Extent confirmed
      from raw bytes: code ends with pop {r4,r5,r6,pc} @ 0x08101fe4 plus
      the trailing "%lx" literal word @ 0x08101fe8 (84 bytes total), and
      the next function's push sits at 0x08101fec, so Ghidra's 84 is
      exact. Call census by decoding every ARM B/BL word and every word
      equal to the address in osos.dec: 13 bl call sites, all
      unconditional, zero predicated forms, zero b references, zero
      data-word references. sprintf, cxx_string_from_cstr,
      cxx_string_assign, cxx_string_release, and
      string_table_fallback_value_slot are ported. The lookup copies the key
      and default COW value into a pair, calls the still-unported 0x083c4884
      map operation, releases the pair and default value, then returns
      node+0x14. That remaining operation uses STRING_TABLE_MAP_OPS, whose
      device default calls 0x083c4884 and whose host tests install a recorder.
      This is the sibling of string_table_set_decimal @ 0x08101d4c, which
      takes its number by pointer and routes through the current-table assign
      helper @ 0x08101da0 instead. Sampled callers (0x08238320, 0x08225ea4)
      store UI selection handles under "SelectAlbum"/"SelectArtist" keys. The
      sprintf port takes an explicit va-list pointer, so the port passes
      &value rather than the original variadic r2 word. Host test uses a
      faithful %lx engine and a heap-backed COW string fixture covering 0,
      0x2a, 0xdeadbeef, and 0xffffffff; it also proves table+0x38 fallback
      arithmetic and the now-ported lookup's key/default-value pair. match.py
      structural report differs as expected (exit 1); sprintf/from_cstr/
      lookup/assign/release still retain their call sequence.

  - address: 0x08102048
    ghidra: FUN_08102048
    name: string_table_set_both_decimal
    size: 76
    sig: "void string_table_set_both_decimal(u8 *table, char **key, i32 value)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: key
      r2: value
      FUN_0802f724: sprintf
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_081020ec: string_table_assign_both
      FUN_083d8b04: cxx_string_release
    notes: >-
      Raw ARM confirms Ghidra's 76-byte code extent: the final pop is at
      0x08102090, the trailing "%d" literal is at 0x08102094, and the
      distinct sibling begins with push at 0x08102098. It renders by-value
      r2 as signed decimal into a 512-byte stack buffer, constructs a COW
      string, hands it to FUN_081020ec with table and key, then releases the
      temporary. That unported helper finds-or-inserts the key in the
      current index's 0x1c-byte map and in the other `(index + 1) % 2` map,
      assigning the same value to both. The helper is retained as the
      STRING_TABLE_ASSIGN_BOTH_OPS volatile seam defaulting to 0x081020ec;
      sprintf, cxx_string_from_cstr, and cxx_string_release are ported.
      Decoding every aligned ARM B/BL word in osos.dec finds exactly 9
      inbound direct bl calls, all unconditional, and zero b references or
      aligned data-word references. Callers pass C-string addresses in r2,
      so their addresses are deliberately serialized as signed decimal; the
      port preserves that ABI rather than accepting a string pointer.
      Focused host tests install the helper recorder and real COW-string
      allocation fixture, covering 0, 42, -17, and i32::MIN while proving
      table/key propagation and by-value signed-decimal formatting.

  - address: 0x08101edc
    ghidra: FUN_08101edc
    name: string_table_has_string
    size: 184
    sig: "u32 string_table_has_string(u8 *table, const u32 *key)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: key
      FUN_083db55c: string_map_find
      FUN_083cf848: iter_eq
      FUN_083d6f0c: cxx_string_empty
    notes: >-
      Localized-string table membership test on the singleton callers load
      as DAT_08219384/DAT_081de518: 1 iff key maps to a NON-EMPTY string in
      the current table (index word at this+0x54, inline StringKeyMap array,
      0x1c stride, header-node pointer at map+0x10), else in the fallback
      table at this+0x38 (table[2], the default language). A hit with an
      empty value also falls through to the fallback. Extent confirmed from
      raw bytes: next function's push at 0x08101f94, so Ghidra's 184 is
      exact. Call census by decoding every B/BL word in osos.dec: 38 bl,
      zero predicated, matching the scouting figure. Ghidra's C is WRONG
      about the first callee: FUN_083db55c is not a copy but
      map<string,string>::find (lower_bound via the ported cxx_string_less
      @ 0x083d74f4, equal-range recheck through the node-key accessor
      node+16 @ 0x083b6acc; writes the found node, or the header on a miss,
      through arg 1). FUN_083cf848 is iterator equality (*a == *b, the
      equal twin of the ported not_equal_deref family @ 0x083d6f40).
      FUN_083d6f0c is basic_string::empty (size word at (*string)-4 == 0).
      All three are unported and ride the STRING_TABLE_OPS seam table;
      target defaults transmute the retail addresses 0x083db55c /
      0x083cf848 / 0x083d6f0c, so the port is hook-ready on device.
      Faithful detail: the current-index word is re-read across the first
      find call for the header compare (the original reloads ldr r0,
      [r4, #84]); a host test proves the reload. Deviations: the original
      spills r0-r3 and reuses those slots as the find out-slots, the port
      uses locals; seam dispatch is blx through the ops table instead of
      direct bl. Host tests (u32 slab fixture, faithful-semantics
      eq/empty mocks, scripted find) cover hit/miss x empty/non-empty in
      both tables, index selection for 0..2, the mid-call index reload,
      and the miss short-circuit (no value probe on a miss). match.py
      structural: same rsb+lsl#2 index math, same [#84] reload, same
      header fetch and cmp/bne-cmp/beq skeleton; diff is the ops-table
      blx, LLVM's fp frame, and a clz/lsr booleanize tail (exit 1, the
      expected ADS-vs-LLVM noise).

  - address: 0x08102168
    ghidra: FUN_08102168
    name: string_table_parse_i32
    size: 40
    sig: "u32 string_table_parse_i32(u8 *table, const u32 *key)"
    module: app/string_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: key
      FUN_08101c14: string_table_value
      FUN_0802f92c: sscanf
      local_8: parsed_value
    notes: >-
      Resolves key through the unported string-table lookup FUN_08101c14,
      reads the selected COW string's data-pointer word, scans one signed
      decimal "%d" conversion into a pre-zeroed local, and returns its u32
      bit pattern. Raw extent is 0x08102168..0x08102193: code ends at
      0x0810218c, the literal "%d\0" is the final word at 0x08102190, and
      the sibling function starts at 0x08102194, so Ghidra's 40-byte size is
      exact. Decoding every aligned ARM B/BL word in osos.dec finds 11 direct
      bl call sites, all unconditional, zero predicated forms. FUN_08101c14
      remains a volatile lookup seam whose device default transmutes the
      verified target address; it returns the address of the mapped COW
      string's target-width data-pointer word. The direct Rust sscanf veneer
      cannot consume the original C-varargs output destination in r2, so this
      port inlines the ported scanf integer worker's "%d" semantics: C
      whitespace, optional sign, base-10 digit run, modulo-2^32 accumulation,
      and zero on a matching failure. A dedicated low-u32 host fixture proves
      lookup argument forwarding plus positive, signed, overflow-wrapping,
      prefix-stopping, and nonnumeric inputs. match.py structural diff is
      expected because LLVM emits the inlined scanner and volatile seam
      dispatch instead of the original sscanf bl.

  - address: 0x08003e00
    ghidra: FUN_08003e00
    name: rom_task_start
    size: 28
    sig: "u64 rom_task_start(u32 task_id, u32 input_r1, u32 input_r2, u32 input_r3)"
    module: heap/rom_task_start
    status: ported
    renames:
      param_1: task_id
      param_2: input_r1
      param_3: input_r2
      param_4: input_r3
      local_18: request_selector_or_result_low
      uStack_14: request_word1_or_result_high
      local_10: request_task_id
      uStack_c: request_word3
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for the RTXC task-start gateway (ROM 0x22003e00,
      reached by thunk 0x08037f78). Direct boot callers and the thunk establish
      selector 0x15 as task start with task_id in r0. Assembly saves r0-r4/lr,
      replaces only the stack frame's first word with 0x15, and calls
      0x08003660 with the four words {0x15, incoming r1, task_id, incoming
      r3}; incoming r2 is not submitted. The dispatch may overwrite words 0/1;
      `pop {r0,r1,r2,r3,r4,pc}` returns those two words as the ARM EABI u64
      result (r0 low, r1 high), while r2-r4 are restored. Reference C:
      decomp/c/000/08003e00_FUN_08003e00.c; assembly 0x08003e00. The foreign
      dispatcher is a volatile heap hook seam whose unwired default spins;
      focused host test records the exact frame, proves one delegation, and
      proves the mutated two-word result convention. match.py structural diff
      is expected for the hook indirection.

  - address: 0x08003e1c
    ghidra: FUN_08003e1c
    name: timer_free_gateway
    size: 40
    sig: "void timer_free_gateway(void *timer)"
    module: heap/timer_free_gateway
    status: ported
    renames:
      param_1: timer
      local_1c: request_selector_and_unwritten_words
      local_8: request_timer
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for RTXC KS_free_timer (service selector 0x11), reached
      by thunk 0x08037f10 -> ROM 0x22003e1c. The adjacent selector-0x10
      wrapper (0x08003b8c, KS_alloc_timer), the RTXC service catalogue, and
      timer-lifecycle callers establish the operation and timer argument. A
      zero timer returns without dispatch. For a nonzero timer, assembly
      reserves seven stack words, passes the pointer at its second word, writes
      only selector 0x11 at request word 0 and timer at word 5 (+0x14), and
      deliberately leaves request words 1..4 unwritten. It returns void after
      dispatch. The shared volatile heap gateway seam represents the foreign
      dispatcher; focused host tests prove the zero no-call path and one
      nonzero delegation while observing only initialized words. Reference C:
      decomp/c/000/08003e1c_FUN_08003e1c.c; assembly 0x08003e1c. match.py
      structural diff is expected for the hook indirection.

  - address: 0x080040ac
    ghidra: FUN_080040ac
    name: mailbox_receive_timed_gateway
    size: 80
    sig: "u32 mailbox_receive_timed_gateway(u32 mailbox, u32 task, u32 timeout_ticks, u32 *message_out)"
    module: heap/mailbox_receive_timed_gateway
    status: ported
    renames:
      param_1: mailbox
      param_2: task
      param_3: timeout_ticks
      param_4: message_out
      auStack_50: request_scratch
      local_30: request_selector
      local_2c: request_message_output
      local_24: request_mailbox
      local_20: request_task
      local_18: request_status
      local_14: request_wait_flag
      local_10: request_timeout_ticks
      local_c: request_scratch_pointer
      FUN_08003660: gateway_dispatch
    notes: >-
      Internal mask-ROM gateway ABI for RTXC KS_receivet(MBOX mailbox, TASK
      task, TICKS timeout): selector 5's timed, status-and-out-pointer form.
      Assembly reserves 72 local bytes below saved {r4,lr}: a 32-byte
      uninitialized scratch prefix, then the dispatched ten-word request
      {5, output=0, uninitialized, mailbox, task, uninitialized, status=0, 1,
      timeout, scratch_pointer}. The pointer is sp+0x20 and its last word
      points back to scratch at sp. After exactly one dispatch, it writes
      mutated word 1 through message_out and returns mutated word 6; the
      dispatcher's register return is ignored. The adjacent 0x080040fc
      KS_receive wrapper and RTXC mailbox API establish the mailbox/task/output
      mapping; its extra word 8 is the timed-receive tick count. The shared
      volatile heap gateway seam represents the foreign dispatcher. Focused
      host test proves every initialized request word, scratch geometry,
      delegation, output write, and status return. Reference C:
      decomp/c/000/080040ac_FUN_080040ac.c; assembly 0x080040ac. match.py
      structural diff is expected for the hook indirection.

  - address: 0x0800408c
    ghidra: FUN_0800408c
    name: task_unlock_gateway
    size: 32
    sig: "u64 task_unlock_gateway(u32 task_id)"
    module: heap/task_unlock_gateway
    status: ported
    renames:
      param_1: task_id
      local_18: request_selector_or_result_low
      uStack_14: request_result_high
      local_10: request_task_id
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for the RTXC task-unlock gateway service (ROM
      0x2200408c, reached by thunk 0x08037e50). The thunk catalogue and its
      critical-section callers establish the semantic task_unlock name. Raw
      assembly reserves five stack words, writes only selector 3 at word 0 and
      task_id at word 2, and passes the three-word prefix to 0x08003660; word
      1 is deliberately uninitialized output. The gateway overwrites words
      0/1, and `pop {r0,r1,r2,r3,ip,pc}` returns those words as the ARM EABI
      u64 result (r0 low, r1 high). The shared volatile heap gateway seam
      represents the foreign dispatcher; focused host tests record the two
      initialized request words, prove one delegation, and prove the mutated
      result pair. Reference C: decomp/c/000/0800408c_FUN_0800408c.c;
      assembly 0x0800408c; thunk/caller evidence in kernel/task_lock.rs.

  - address: 0x080040fc
    ghidra: FUN_080040fc
    name: mailbox_receive_gateway
    size: 60
    sig: "u32 mailbox_receive_gateway(u32 mailbox, u32 task)"
    module: heap/mailbox_receive_gateway
    status: ported
    renames:
      param_1: mailbox
      param_2: task
      local_2c: request_selector_and_uninitialized_words
      local_20: request_mailbox
      uStack_1c: request_task
      local_14: request_output
      local_10: request_mode
      local_c: request_zero
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for RTXC KS_receive(MBOX mailbox, TASK task), established
      by service selector 5 and the RTXC service catalogue. Raw assembly
      reserves 44 stack bytes and dispatches its nine-word span at sp+4:
      {5, uninitialized, uninitialized, mailbox, task, uninitialized,
      output=0, 1, 0}. It returns the gateway-mutated output word at request
      word 6; the leading stack word outside that span is also unwritten. The
      shared volatile heap gateway seam represents the foreign dispatcher;
      focused host test records every initialized word, proves one delegation,
      and proves capture/return of the gateway output. Reference C:
      decomp/c/000/080040fc_FUN_080040fc.c; assembly 0x080040fc. match.py
      structural diff is expected for the hook indirection.


  - address: 0x08004154
    ghidra: FUN_08004154
    name: mailbox_send_gateway
    size: 56
    sig: "void mailbox_send_gateway(u32 mailbox, u32 message, u32 priority, u32 semaphore)"
    module: heap/mailbox_send_gateway
    status: ported
    renames:
      param_1: mailbox
      param_2: message
      param_3: priority
      param_4: semaphore
      local_2c: request_selector_and_uninitialized_words
      local_24: request_semaphore
      local_20: request_mailbox
      local_18: request_priority
      local_14: request_message
      local_10: request_output
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for RTXC KS_send(MBOX mailbox, RTXCMSG *message,
      PRIORITY priority, SEMA semaphore), established by service selector 4
      and the RTXC service catalogue. Raw assembly reserves 44 stack bytes
      and dispatches its eight-word span at sp+4: {4, uninitialized,
      semaphore, mailbox, uninitialized, priority, message, output=0}.
      It returns void after exactly one dispatch; the initialized output word
      is not read by this asynchronous-send wrapper. The shared volatile heap
      gateway seam represents the foreign dispatcher; focused host test records
      every initialized word and proves one delegation. Reference C:
      decomp/c/000/08004154_FUN_08004154.c; assembly 0x08004154. match.py
      structural diff is expected for the hook indirection.

  - address: 0x0800427c
    ghidra: FUN_0800427c
    name: task_delete_gateway
    size: 28
    sig: "u64 task_delete_gateway(u32 task_id, u32 input_r1, u32 input_r2, u32 input_r3)"
    module: heap/task_delete_gateway
    status: ported
    renames:
      param_1: task_id
      param_2: input_r1
      param_3: input_r2
      param_4: input_r3
      local_18: request_selector_or_result_low
      uStack_14: request_word1_or_result_high
      local_10: request_task_id
      uStack_c: request_word3
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for the RTXC task-delete gateway (ROM 0x2200427c,
      reached by thunk 0x08037f80 from task_destroy). The thunk and
      task-destroy caller establish selector 0x18 as task delete with task_id
      in r0. Assembly reserves a four-word frame, initializes every word, and
      calls 0x08003660 with {0x18, incoming r1, task_id, incoming r3};
      incoming r2 is saved/restored but is not submitted. The dispatcher may
      overwrite words 0/1; they return as the ARM EABI u64 result (r0 low, r1
      high). The shared volatile heap gateway seam represents the foreign ROM
      dispatcher; focused host test records all initialized request words,
      proves one delegation, and proves the mutated result pair. Reference C:
      decomp/c/000/0800427c_FUN_0800427c.c; assembly 0x0800427c.

  - address: 0x08004298
    ghidra: FUN_08004298
    name: task_unblock_range_gateway
    size: 28
    sig: "void task_unblock_range_gateway(u32 start_task_id, u32 end_task_id)"
    module: heap/task_unblock_range_gateway
    status: ported
    renames:
      param_1: start_task_id
      param_2: end_task_id
      local_10: request_selector
      uStack_c: request_start_task_id
      uStack_8: request_end_task_id
      FUN_08003660: gateway_dispatch
    notes: >-
      Mask-ROM mirror for RTXC gateway service 30, KS_unblock(start, end)
      (ROM 0x22004298, reached by veneer 0x08038250). RTXCbug callers at
      0x082bf120 and 0x082bfa84 send {1, max_task_id} to unblock all
      configured tasks, while 0x08393cfc sends {task_id, task_id} for one;
      together they establish its inclusive task-id range semantics. Assembly
      pushes r1-r3/lr, overwrites the first three stack words with
      {0x1e, incoming r0, incoming r1}, and calls 0x08003660. The pop restores
      only r2/r3/ip/pc, so no gateway result is observed: this is a void
      wrapper. The shared volatile heap gateway seam represents the foreign ROM
      dispatcher; focused host test records all three initialized words, proves
      one delegation, clobbers the request, and proves the wrapper returns
      void. Reference C: decomp/c/000/08004298_FUN_08004298.c; assembly
      0x08004298.

  - address: 0x080043c0
    ghidra: FUN_080043c0
    name: gateway_service1_request
    size: 52
    sig: "u32 gateway_service1_request(u32 input0, u32 input1)"
    module: heap/gateway_service1
    status: ported
    renames:
      param_1: input0
      param_2: input1
      auStack_38: scratch
      local_18: service_selector
      local_14: output
      local_10: input0
      uStack_c: input1
      local_8: scratch_pointer
      FUN_08003660: gateway_dispatch
    notes: >-
      Raw mask-ROM service-1 request. Assembly reserves 52 bytes: the first
      32 are uninitialized scratch, followed by the five words
      {1, 0, input0, input1, scratch_base}. It passes the address of word 0
      (sp + 0x20) to 0x08003660, whose writable request ABI may replace word
      1; the wrapper loads that post-dispatch output word and returns it in r0.
      The scratch-base field points 32 bytes before the dispatch pointer.
      Recovered C has no callers or thunk identifying a higher-level RTXC
      operation, so gateway_service1_request names the established protocol
      rather than guessing one. The foreign dispatcher uses the shared volatile
      heap seam; focused host testing proves all initialized fields, delegation,
      scratch geometry, output initialization, and post-dispatch return.
      Reference C: decomp/c/000/080043c0_FUN_080043c0.c; raw assembly:
      0x080043c0.

  - address: 0x080043f4
    ghidra: FUN_080043f4
    name: task_yield_gateway
    size: 40
    sig: "u32 task_yield_gateway(u32 ignored_r0, u32 ignored_r1, u32 input_r2, u32 input_r3)"
    module: heap/task_yield_gateway
    status: ported
    renames:
      param_1: ignored_r0
      param_2: ignored_r1
      param_3: input_r2
      param_4: input_r3
      local_18: service_selector
      local_14: status
      uStack_10: input_r2
      uStack_c: input_r3
      FUN_08003660: gateway_dispatch
    notes: >-
      RTXC KS_yield gateway wrapper. The retailOS RTXC service catalogue maps
      selector 0x1c to KS_yield (voluntary scheduling yield). There are no
      direct callers or veneer for this otherwise isolated 40-byte mirror;
      raw assembly resolves the complete saved frame. It saves
      {r0, r1, r2, r3, r4, lr}, overwrites its first two words with
      {0x1c, 0x0d}, and dispatches the four-word prefix
      {selector, status, incoming_r2, incoming_r3}. Incoming r0/r1 are
      ignored, r4/lr are preserved by the post-dispatch epilogue, and the
      dispatcher may replace status; that mutated word is returned in r0.
      The shared volatile heap gateway seam represents the foreign ROM
      dispatcher. Focused host testing records request construction, the
      initialized status, one delegation, and the post-dispatch status return.
      Reference C: decomp/c/000/080043f4_FUN_080043f4.c; raw assembly
      0x080043f4; RTXC selector catalogue: freemyipod.org/wiki/RetailOS.

  - address: 0x08046a28
    ghidra: FUN_08046a28
    name: copy_u16_pair
    size: 20
    sig: "void copy_u16_pair(u16 *dst, const u16 *src)"
    module: util/u16_pair_copy
    status: ported
    renames:
      param_1: dst
      param_2: src
    notes: >-
      Two ordered forward halfword transfers: ldrh [src] then strh [dst],
      followed by ldrh [src+2] then strh [dst+2]. The second load occurs
      after the first store, so overlapping pairs intentionally retain this
      forward-copy behavior rather than memcpy or memmove semantics.

  - address: 0x08046a3c
    ghidra: FUN_08046a3c
    name: copy_u8_pair
    size: 20
    sig: "void copy_u8_pair(u8 *dst, const u8 *src)"
    module: util/u8_pair_copy
    status: ported
    renames:
      param_1: dst
      param_2: src
    notes: >-
      Two ordered forward byte transfers: ldrb [src] then strb [dst],
      followed by ldrb [src+1] then strb [dst+1]. The second load occurs
      after the first store, so overlapping pairs intentionally retain this
      forward-copy behavior rather than memcpy or memmove semantics.

  - address: 0x08046a50
    ghidra: FUN_08046a50
    name: pascal_u8_to_cstr_bounded
    size: 56
    sig: "u32 pascal_u8_to_cstr_bounded(const u8 *source, u8 *destination, u32 maximum)"
    module: util/pascal_string
    status: ported
    renames:
      param_1: source
      param_2: destination
      param_3: maximum
      uVar1: offset
      uVar2: copied
    notes: >-
      Reads the unsigned u8 Pascal count at source[0], clamps it against the
      u32 maximum, then performs ordered forward byte loads from source+1 and
      stores to destination. It appends NUL at destination[copied] and returns
      copied; overlapping ranges retain the original interleaved load/store
      behavior rather than memcpy or memmove semantics.
  - address: 0x08046c24
    ghidra: FUN_08046c24
    name: utf8_to_utf16_counted_buffer
    size: 80
    sig: "i32 utf8_to_utf16_counted_buffer(const u8 *source, u32 source_len, u16 *destination, u32 initial_count)"
    module: cxx/string_encoding
    status: ported
    renames:
      param_1: source
      param_2: source_len
      param_3: destination
      param_4: initial_count
      local_c: decoded_count
    notes: >-
      Nine direct unconditional bl call sites, binary-scanned from osos.dec:
      0x08068570, 0x080960a4, 0x0809610c, 0x0809617c, 0x080ab49c,
      0x080c61a4, 0x0811505c, 0x08115eb8, and 0x0812cb34; no predicated
      bl or B entries target it. Raw 80-byte body clears destination[0],
      accepts a NULL source only when source_len is zero, then calls the
      unported 0x08046c74 helper with destination + 2, max 255, and a local
      decoded-count word. That helper constructs a temporary StringObject
      from the byte span, obtains its C string, bounds its UTF-8-to-UTF-16
      conversion, records the count, destroys it, and returns zero. The
      port reaches that remaining retailOS helper directly on target and
      models it with a host-swappable seam only for tests; no already ported
      callee was restubbed.
  - address: 0x082774a8
    ghidra: FUN_082774a8
    name: string_object_assign
    size: 32
    sig: "StringObject *string_object_assign(StringObject *this, const StringObject *source)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: this
      param_2: source
      FUN_08276474: string_object_assign_payload
    notes: >-
      217 bl call sites (binary-scanned) - the most-called function of the
      unidentified two-word string/buffer class whose default ctor is
      string_default_construct @ 0x08277440. The class's copy-assignment
      operator, StringObject &operator=(const StringObject &). Raw ARM:
      push {r4,lr}; cmp r0,r1; mov r4,r0; ldrne r1,[r1,#0x4]; movne r0,r4;
      blne 0x08276474; mov r0,r4; pop {r4,pc}. Two details are load-bearing
      and reproduced exactly. First, the self-assignment guard compares the
      two object ADDRESSES (cmp r0,r1), not payload content, so x = x is a
      complete no-op - not even a redundant copy through the allocator -
      while assigning between distinct objects that happen to share a
      payload pointer still runs the full path. Second, this is returned on
      every path (the mov r0,r4 sits after the conditional call), giving the
      C++ chaining convention even when the guard skipped the work. The
      SOURCE's payload word at +0x4 is forwarded to the already-ported
      string_object_assign_payload @ 0x08276474, which owns replacing and
      releasing this object's prior payload through vtable slots +0x8/+0xc;
      this operator never reads or writes this.payload itself. Neither
      operand is NULL-guarded - the original faults on the ldrne for a NULL
      source, and so does the port. Host tests cover the forwarding path
      (source payload reaches +0x8 with strlen+1), self-assignment as a
      total no-op that still returns this, the address-not-content guard
      (two distinct objects sharing one payload pointer still assign), and a
      NULL source payload reaching only the +0xc clear slot. match.py
      structural: same compare/skip/load-source-payload/call/return-this
      shape; the diff is LLVM inverting the predicated guard into an early
      bxeq lr and adding the crate's fp frame.
  - address: 0x0827216c
    ghidra: FUN_0827216c
    name: resource_chain_find
    size: 84
    sig: "void *resource_chain_find(ResourceProvider *head, ResourceKind kind, u32 id)"
    module: app/resource_chain
    status: ported
    renames:
      param_1: head
      param_2: kind
      param_3: id
      local_8: found
      FUN_0827239c: resource_chain_find_string
      FUN_0827238c: resource_chain_find_bitmap
      FUN_08272360: resource_chain_find_on_current_task
    notes: >-
      377 bl + 11 b call sites (binary-scanned over the whole decrypted
      image by decoding every B/BL word) - the most-called function in
      retailOS that had not been ported. The framework's typed resource
      lookup: walk a chain of resource-provider objects and ask each one,
      through vtable slot +0x64, for the resource named by a
      (four-character kind, numeric id) pair; the first provider that
      answers wins. Raw ARM: push {r3,r4,r5,r6,r7,lr} (the r3 push is the
      one-word out slot); found = NULL stored once at [sp]; enter at the
      loop test; per node - vtable = node[0], ip = vtable[0x64],
      ip(node, kind, id, &found); cmp r0,#0; bne out; node = node[0x14];
      cmp node,#0; bne loop; return [sp]. Two details are load-bearing
      and reproduced exactly. First, the out word is cleared ONCE before
      the loop, never per node, so a provider that writes it and then
      reports "not mine" still decides the result when nobody else
      answers. Second, the provider's r0 is a plain non-zero test
      (cmp #0/bne), not a comparison with 1. A NULL head is the loop's
      own exit test and returns NULL without faulting; a NULL vtable or
      NULL slot faults, and the port keeps that unguarded load.
      Identification - the two constant-binding thunks that follow the
      function are its only shape-revealing callers: 0x0827238c binds
      literal 0x424d6170 ("BMap") and 0x0827239c binds 0x53747220
      ("Str "), both as `mov r2,r1; ldr r1,[pc]; b 0x0827216c`, so r1 is
      a FourCC type tag held big-endian in a word and the caller's second
      argument is the resource id. The "Str " result is consumed as a
      const char *: 0x080c6324 hands it straight to the string-object
      constructor @ 0x0827735c (vtable literal 0x089a6044, the
      cxx/string_object.rs class) and the 0x080d28e0 cluster copies it
      with the bounded UTF-8 copy @ 0x08275d00 into 0x20-byte fields. The
      chain head is task-local - the 17-call-site wrapper @ 0x08272360 is
      `head = task_ctx_field_0x30(); if (head) tail-call this` - i.e.
      field +0x30 of the current task's context block (ported in
      util/context_field.rs) is the head of this provider chain; the next
      link at +0x14 is refcounted by its setter @ 0x082722a0 (release old,
      retain new). The 4-argument sibling @ 0x082721c0 is the same walk
      with an extra bool* "did anyone answer" out parameter, and
      0x08272230 is the slot +0x68 variant that falls back to slot +0x58
      (ported alongside as resource_chain_write); the sibling is not
      ported here. Deviations - the provider object and its
      vtable are modeled as #[repr(C)] structs with real Rust pointers, so
      offsets are the original's on the 32-bit target (vtable +0x00, next
      +0x14, find = the 26th vtable word = +0x64) and self-consistent on
      the 64-bit host, where tests build the graph natively; a compile-time
      offset_of test pins all three. Host tests (12 in the module): empty
      chain asks nobody; first answer stops the walk; three-node walk in
      order with kind and id forwarded verbatim; nobody answers -> NULL
      after asking everyone; a declining provider's write survives to the
      result; an answer without a write yields NULL; every non-zero answer
      (1, 2, 0x80000000, 0xffffffff) stops the walk; a mixed-class chain
      dispatches through each node's OWN vtable; the FourCC literals spell
      "Str " and "BMap" big-endian. match.py structural: identical loop -
      same out-word init before the loop, same ldr [node,#0x64] slot load,
      same blx, same cmp/bne, same ldr [node,#20] link walk; the only diff
      is the crate's fp prologue/epilogue and LLVM's register allocation
      plus a hoisted head-NULL pre-test.

  - address: 0x0814376c
    ghidra: FUN_0814376c
    name: resource_chain_find_with_selector
    size: 184
    sig: "void *resource_chain_find_with_selector(ResourceSelectorProvider *head, ResourceKind kind, u32 id, u32 selector)"
    module: app/resource_chain
    status: ported
    renames:
      param_1: head
      param_2: kind
      param_3: id
      param_4: selector
      local_1c: found
      iVar1: child
      piVar2: child_provider
      iVar3: child_answer
      FUN_0827216c: resource_chain_find
      FUN_08275b9c: object_cast_to_class
    notes: >-
      Selector-aware resource lookup front-end. The head provider is
      asked first through slot +0x80 with `(kind, id, selector, &found)`;
      if that returns non-zero, the written pointer is returned
      immediately. Otherwise the linked children at +0x14 are walked in
      order, each one downcast to class id 6 through
      object_cast_to_class and, when accepted, asked the same slot +0x80
      question. If no provider answers, the function falls back to
      resource_chain_find(head, kind, id) and returns that raw pointer;
      the fallback ignores selector exactly as stock does. The out word
      is cleared once before the search and reused across every
      selector-aware call. Binary scan of osos.dec confirms the 184-byte
      extent and 21 direct BL callers.

  - address: 0x0827239c
    ghidra: FUN_0827239c
    name: resource_chain_find_string
    size: 12
    sig: "const char *resource_chain_find_string(ResourceProvider *head, u32 id)"
    module: app/resource_chain
    status: ported
    renames:
      param_1: head
      param_2: id
      FUN_0827216c: resource_chain_find
    notes: >-
      171 bl + 14 b call sites (binary-scanned). 12 bytes - 8 of code plus
      the 4-byte literal 0x53747220 at 0x082723a8, which is "Str " read
      big-endian. `mov r2,r1; ldr r1,[pc] (=0x53747220); b 0x0827216c`:
      binds the string resource kind as the middle argument of
      resource_chain_find, shifting the caller's id from r2 into place, and
      tail-branches. So it is "look this string resource id up in the
      provider chain", returning a NUL-terminated C string or NULL when no
      provider owns the id. Call-site evidence that the result really is a
      C string: 0x080c6324 passes it to the string-object constructor
      @ 0x0827735c, and 0x080d28e0..0x080d2ca0 feed it to the bounded
      UTF-8 copy @ 0x08275d00 with limits of 0x20 and 0xa bytes. Deviation:
      the port is a call rather than a tail branch (observably identical);
      LLVM still emits `mov r2,r1; ldr r1,=0x53747220; b resource_chain_find`
      around the crate frame, so match.py is instruction-for-instruction on
      the body. Its sibling @ 0x0827238c binds 0x424d6170 ("BMap") the same
      way (46 bl + 2 b call sites) and is now ported alongside it as
      resource_chain_find_bitmap.

  - address: 0x0827238c
    ghidra: FUN_0827238c
    name: resource_chain_find_bitmap
    size: 16
    sig: "void *resource_chain_find_bitmap(ResourceProvider *head, u32 id)"
    module: app/resource_chain
    status: ported
    renames:
      param_1: head
      param_2: id
      FUN_0827216c: resource_chain_find
    notes: >-
      The "BMap" sibling of resource_chain_find_string @ 0x0827239c:
      `mov r2,r1; ldr r1,[pc] (=0x424d6170); b 0x0827216c`, binding the
      bitmap resource kind as the middle argument of resource_chain_find
      and shifting the caller's id from r2 into place, then tail-branching.
      So it is "look this bitmap resource id up in the current provider
      chain", returning the owning provider's raw pointer or NULL.
      Extent - Ghidra says 12 bytes, which drops the trailing literal
      word. The real extent is 16 bytes, 0x0827238c..0x0827239c
      (0x08272398 holds 0x424d6170, and 0x0827239c is the first
      instruction of the "Str " thunk), confirmed by decoding the raw
      words rather than trusting the listing. Call sites - 46 bl (all
      unconditional) plus 2 unconditional b tail calls (0x081eb070,
      0x081ed150), binary-scanned by decoding every B/BL word in
      osos.dec; no DATA word in the image holds 0x0827238c, so it is
      never dispatched virtually. Callers cluster in the UI/resource
      layers (0x08112xxx, 0x08128xxx-0x08129xxx, 0x08133xxx, 0x0815cxxx,
      0x08170xxx-0x08176xxx, 0x08186xxx, 0x081ebxxx-0x081f8xxx),
      consistent with "fetch an artwork bitmap by id". Deviations - the
      port is a call rather than a tail branch (observably identical);
      the return type is the chain walker's raw void* because no call
      site reveals bitmap structure beyond "opaque pointer the drawing
      code consumes". Codegen note - the two thunks differ only in the
      literal they load, so LLVM does NOT fold them; the archive shows
      distinct .text.resource_chain_find_bitmap and
      .text.resource_chain_find_string sections with the correct
      constant in each, and a host test asserts the two bind different
      kinds. Host tests - kind and id forwarded verbatim to the walker,
      NULL on an empty chain, and the two-thunks-differ guard. match.py
      structural: mov r2,r1 / ldr r1,[pc] / b / .word 0x424d6170 match
      instruction-for-instruction; the only diff is the crate-standard
      fp prologue.

  - address: 0x08272360
    ghidra: FUN_08272360
    name: resource_chain_find_on_current_task
    size: 44
    sig: "void *resource_chain_find_on_current_task(ResourceKind kind, u32 id)"
    module: app/resource_chain
    status: ported
    renames:
      param_1: kind
      param_2: id
      FUN_080cb828: current_task_ctx_block
      FUN_0827216c: resource_chain_find
    notes: >-
      17 bl call sites, binary-scanned by decoding every B/BL word in
      osos.dec - all unconditional, no predicated form and no tail b; no
      DATA word in the image holds 0x08272360, so it is never dispatched
      virtually. Ghidra's 44-byte extent is exact (0x08272360..0x0827238c;
      the BMap thunk follows). The task-local front-end of
      resource_chain_find: `push {r4,r5,r6,lr}; mov r5,r1; mov r4,r0;
      bl 0x080cb828; ldr r0,[r0,#0x30]; cmp r0,#0; movne r2,r5; movne
      r1,r4; popne {r4,r5,r6,lr}; bne 0x0827216c; pop {r4,r5,r6,pc}` -
      i.e. head = field +0x30 of the current task's context block; if
      non-NULL, tail-call resource_chain_find(head, kind, id); else
      return NULL (r0 already 0). This establishes what field +0x30 of
      the task context block IS: the head of the task-local
      resource-provider chain (util/context_field.rs ported the getter
      under the field-offset name because its writers were undecoded -
      this wrapper is the decoder). Two NULL-handling details kept: the
      context-block pointer is NOT checked (no current task = data abort
      on the +0x30 load, exactly like the inlined getter; the wrapper
      calls 0x080cb828 directly, not task_ctx_field_0x30 @ 0x0827233c),
      and the chain head IS checked, so a task with no provider chain
      gets NULL without the walk. Signature note: callers pass
      (kind, id) - the call site @ 0x08140cdc shows r1 arriving already
      loaded with the id, which Ghidra's C drops (e.g.
      FUN_08272360(DAT_0826de68) with one visible argument). Deviations:
      the getter sits behind the CURRENT_TASK_CTX_BLOCK dispatch slot
      (same documented deviation as util/context_field.rs; the default
      stub models the callee's known prefix exactly), and the predicated
      tail b is a call in the port (observationally identical). Host
      tests (4 in app/resource_chain.rs): chain walk from the ctx head
      with kind/id forwarded verbatim and exactly one getter call, NULL
      head returns NULL with the walk skipped entirely, NULL when no
      provider answers, and the ctx block read-only byte-compare; the
      chain fixture lives in a below-4-GiB slab (hints::
      RESOURCE_CHAIN_ON_CURRENT_TASK) because the ctx block carries the
      head as a raw u32 word, and slot swaps serialize on testing::
      TASK_CTX_BLOCK_TEST_LOCK, now shared with context_field's tests.
      match.py structural: ldr +0x30 / cmp #0 / forward-and-tail-b shape
      intact; the diff is the crate fp frame, the dispatch-slot ldr/blx
      standing in for bl 0x080cb828, and LLVM materializing an explicit
      mov r0,#0 on the NULL path where the original reused the zeroed
      r0.

  - address: 0x08272230
    ghidra: FUN_08272230
    name: resource_chain_write
    size: 112
    sig: "u32 resource_chain_write(ResourceProvider *head, ResourceKind kind, u32 id, u32 value, u32 flags)"
    module: app/resource_chain
    status: ported
    renames:
      param_1: head
      param_2: kind
      param_3: id
      param_4: value
      "param_5 (stack)": flags
    notes: >-
      28 bl + 2 blne call sites, binary-scanned over the whole decrypted
      image by decoding every B/BL word - Ghidra's "30" is the sum; the
      predicated pair @ 0x0813427c and 0x0816b380 gates the call on a
      caller-side condition, all other sites are unconditional. No DATA
      word in the image holds 0x08272230, so it is never dispatched
      virtually. Extent binary-verified at exactly 112 bytes: the body
      ends `pop {r3,r4,r5,r6,r7,r8,r9,pc}` @ 0x0827229c and the next
      function (the refcounting `next`-link setter @ 0x082722a0) begins
      at 0x082722a0, matching Ghidra's size for once. The write variant
      of resource_chain_find: walk the provider chain and offer each
      node the entry (kind, id) a new value with flags through vtable
      slot +0x68 (27th vtable word, five arguments - the fifth rides
      the stack per the AAPCS, `ldr r8,[sp,#32]; str r8,[sp]`); the
      first provider whose write slot answers non-zero is then asked
      through slot +0x58 (22nd word) for the entry's (kind, id) value
      via a tail `bx r3`, and that answer is the return. Decline
      (`ldreq r4,[r4,#0x14]`) walks the +0x14 next link; a NULL link
      returns 0. Three load-bearing details are reproduced exactly.
      First, unlike the find sibling, entry falls INTO the loop body -
      the NULL test guards only the next link, so a NULL head faults on
      the vtable load; the port keeps the unguarded first dereference
      (and the unguarded slot loads). Second, the accept test is a
      plain non-zero test (cmp r0,#0 / ldreq / beq), not a comparison
      with 1. Third, the read-back slot gets (kind, id) only - value
      and flags are not forwarded - and its result is returned
      verbatim, including 0. Identification - the setter-side caller @
      0x0825b734 (disassembled from raw bytes: `mov r3,#4; str r3,[sp];
      ldr r2,=0x000060a4; ldr r1,=0x55693332; mov r3,r5; bl
      0x08272230`) offers ("Ui32", 0x60a4, the new value, 4) on the
      class-0x6000 singleton's provider chain right after the state
      store - the write-side sibling of the ported
      class6000_ui32_resource_60a4 getter, which reads the same
      (kind, id) pair through resource_chain_find. Other callers (@
      0x081407b0, 0x0816b208, 0x081db908) pass flags 4 or 0 and ignore
      the return. Deviations - the provider object and its vtable are
      modeled as #[repr(C)] structs with real Rust pointers (the same
      model resource_chain_find uses, extended: slots_below split so
      `read` lands on +0x58 and `write` on +0x68; a compile-time
      offset_of test pins all three decoded slots), so offsets are the
      original's on the 32-bit target and self-consistent on the 64-bit
      host; the original tail-branches into slot +0x58, the port calls
      it and returns the result (observably identical, and LLVM in fact
      emits the same tail `bx r3`). Host tests (6 new, 21 in the
      module): the first acceptor is read back and stops the walk with
      all five arguments forwarded verbatim and the read-back seeing
      (kind, id) only; declining providers are walked in order with
      kind/id/value/flags verbatim per node; nobody accepting returns 0
      with the read-back never running; every non-zero accept (1, 2,
      0x80000000, 0xffffffff) stops the walk; a zero read-back is still
      the result (the read ran); a mixed-class chain dispatches both
      slots through each node's OWN vtable. match.py structural:
      identical loop - same slot +0x68 load/blx/cmp, same `ldr
      [node,#20]` link walk, same slot +0x58 tail `bx r3`; the diff is
      the crate fp prologue/epilogue, LLVM's register allocation, and
      an explicit `mov r0,#0` on the nobody-accepted path where the
      original lets the failed call's r0=0 fall through.

  - address: 0x08277304
    ghidra: FUN_08277304
    name: string_object_construct_from_cstr
    size: 40
    sig: "StringObject *string_object_construct_from_cstr(StringObject *this, const char *source)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: this
      param_2: source
      DAT_0827732c: STRING_OBJECT_VTABLE_ADDRESS
      FUN_0827639c: string_object_assign_cstr
    notes: >-
      143 bl call sites (binary-scanned). The converting constructor
      StringObject(const char *) of the unidentified two-word
      string/buffer class whose default ctor is string_default_construct
      @ 0x08277440. Raw ARM: push {r4,lr}; mov r4,r0; ldr r0,[0x0827732c]
      (0x089a6044, the class vtable, binary-verified); str r0,[r4]; mov
      r0,#0; str r0,[r4,#0x4]; mov r0,r4; bl 0x0827639c; mov r0,r4; pop
      {r4,pc}. 40 code bytes plus the 4-byte literal-pool word @
      0x0827732c. Ghidra drops the second argument in
      decomp/c/026/08277304_FUN_08277304.c - r1 is never written between
      entry and the bl, so the caller's C string flows straight into
      string_object_assign_cstr. It is exactly the default ctor followed
      by the ported assign: NULLing the payload word FIRST is what makes
      the shared assignment path safe on raw storage - the +0x8
      allocation slot sees no prior payload to release. source stays
      caller-owned; a NULL or empty source leaves the freshly-constructed
      empty state through the +0xc clear slot, and an allocation failure
      leaves the payload NULL. this is returned unconditionally (the ADS
      constructor convention) and is not NULL-guarded - the original
      faults on the vtable store, and so does the port. Deviation: the
      vtable is the modeled static STRING_OBJECT_VTABLE, not the ROM
      address (the whole module's convention). Host tests cover the
      garbage-storage NULLing plus strlen+1 allocation and copy, the
      NULL/empty source clear-slot path, and allocation failure.
      match.py structural: same save-this / plant-vtable / NULL-payload /
      call-assign / return-this shape; the diff is LLVM reordering the
      two stores and adding the crate's fp frame.
  - address: 0x0827735c
    ghidra: FUN_0827735c
    name: string_owner_embedded_init
    size: 44
    sig: "StringObject *string_owner_embedded_init(StringObject *this, const char *payload)"
    module: app/string_owner_init
    status: ported
    renames:
      param_1: this
      param_2: payload
      DAT_08277384: STRING_OBJECT_VTABLE_ADDRESS
      FUN_08276474: string_object_assign_payload
    notes: >-
      Source: decomp/c/026/0827735c_FUN_0827735c.c. Raw ARM is
      `push {r4,lr}; mov r4,r0; ldr r0,[0x08277384] (0x089a6044);
      str r0,[r4]; mov r0,#0; str r0,[r4,#4]; mov r0,r4; bl
      0x08276474; mov r0,r4; pop {r4,pc}`: 40 code bytes plus the
      vtable literal at 0x08277384. Ghidra omits param_2, but r1 is
      unchanged from entry through the call, so the incoming C-string
      payload is passed directly to the already-ported
      string_object_assign_payload. The vtable store and payload clear
      precede that call, making raw storage safe for the helper's virtual
      replacement path; this is returned regardless of its void result.
      The port uses the cxx module's modeled STRING_OBJECT_VTABLE rather
      than the ROM pointer. Focused tests cover vtable/payload initialization,
      returned receiver, and the existing outer-wrapper forwarding seam.
      match.py structural: save-this, vtable store, payload clear, payload
      helper call, and saved-this return.
  - address: 0x082773e0
    ghidra: FUN_082773e0
    name: string_object_copy_construct
    size: 48
    sig: "StringObject *string_object_copy_construct(StringObject *this, const StringObject *source)"
    module: cxx/string_object
    status: ported
    renames:
      param_1: this
      param_2: source
      DAT_08277410: STRING_OBJECT_VTABLE_ADDRESS
      FUN_08276474: string_object_assign_payload
    notes: >-
      212 bl call sites (binary-scanned) - the copy constructor
      StringObject(const StringObject &) of the same class. Raw ARM: push
      {r4,lr}; mov r4,r0; ldr r0,[0x08277410] (0x089a6044,
      binary-verified); cmp r4,r1; str r0,[r4]; movne r0,#0; strne
      r0,[r4,#0x4]; ldrne r1,[r1,#0x4]; movne r0,r4; blne 0x08276474; mov
      r0,r4; pop {r4,pc}. 48 code bytes plus the 4-byte literal-pool word
      @ 0x08277410. Two details are load-bearing and reproduced exactly.
      First, the vtable store sits OUTSIDE the self-construction guard
      while the payload NULLing sits INSIDE it, so constructing an object
      from itself plants the vtable and leaves the existing payload word
      alone instead of dropping it; the guard is the same
      address-not-content test string_object_assign @ 0x082774a8 uses,
      but here it also decides whether the storage is treated as raw.
      Second, this is returned on every path (the mov r0,r4 follows the
      conditional call). The duplication itself is the already-ported
      string_object_assign_payload @ 0x08276474 over the SOURCE's payload
      word: it sizes the copy with strlen_safe_plus1, asks vtable slot
      +0x8 for the storage and copies through the NUL, or dispatches slot
      +0xc when the source payload is NULL or empty; a failed allocation
      leaves the empty-string state the constructor already established.
      Neither operand is NULL-guarded - the original faults on the vtable
      store for a NULL this and on the ldrne for a NULL source, and so
      does the port. Deviation: the modeled static STRING_OBJECT_VTABLE
      instead of the ROM address. The port also replaces the
      empty-construction stub as the wired default of the
      STRING_OBJECT_COPY_CONSTRUCT dispatch slot that
      string_id_record_construct_from_string_id @ 0x08258c08 and
      string_id_record_copy_construct @ 0x08258c2c chain through (the
      slot stays so host tests can install recording mocks). Host tests
      cover duplication through +0x8, self-construction as vtable-only
      with the payload kept, the address-not-content guard (two distinct
      objects sharing one payload pointer still duplicate), the NULL
      source payload clear-slot path, allocation failure, and the wired
      default. match.py structural: same literal-load / address-compare /
      unconditional-vtable-store / NULL-payload / load-source-payload /
      call / return-this sequence; the diff is LLVM hoisting the
      predicated guard into an early bxeq lr and adding the crate's fp
      frame.

  # --- MPEG video header parser: user-data start-code scan ---
  - address: 0x0807d724
    ghidra: FUN_0807d724
    name: mpeg_skip_user_data_to_start_code
    size: 76
    sig: "void mpeg_skip_user_data_to_start_code(u8 *stream)"
    module: h264/bitstream
    status: ported
    renames:
      param_1: stream
      iVar1: scan_state
    notes: >-
      Recognizes the MPEG user-data start code 0x000001B2 at the current
      two-word cursor {data @ +0x00, bit_pos @ +0x04}. A non-B2 code
      returns without writing the cursor. For B2, it writes bit_pos + 32,
      then calls bitstream_msb_fetch(stream, 24) at every subsequent byte
      until the 24-bit start-code prefix 0x000001 is found. It leaves the
      prefix unread for the caller's start-code parser; each rejected byte
      advances the cursor by eight. The 76-byte retail body is two direct
      calls to bitstream_msb_fetch @ 0x080efa38 (first count 32, loop count
      24), with wrapping ARM-add arithmetic and no bounds check. Rust port
      and host tests: src/h264/bitstream.rs. Host tests cover the non-B2
      no-op and scans across 0..7 payload bytes, verifying the next parser
      can still fetch 0x000001B3. match.py structural: same initial 32-bit
      compare, +32 commit, 24-bit loop check, and +8 non-prefix step; LLVM
      frame/register allocation differs.

  # --- H.264 / MPEG-4 AVC video decoder: RBSP bitstream ---
  - address: 0x082b3258
    ghidra: FUN_082b3258
    name: h264_bitstream_advance
    size: 60
    sig: "bool h264_bitstream_advance(RbspBitReader *reader, i32 bits, u8 *emulation_byte_pending)"
    module: h264/bitstream
    vars: {param_1: reader, param_2: bits, param_3: emulation_byte_pending}
    status: ported
    notes: >-
      The commit half of every H.264 syntax-element read. 60 bytes, all
      code — no literal pool; the body starts with `ldrb r3,[r2]` (no
      push) and ends `bx lr`, a leaf. 114 `bl` call sites, 0 `b`,
      binary-scanned by decoding every B/BL word in osos.dec; they
      cluster in the decoder's syntax parsers at 0x0836xxxx plus the
      Exp-Golomb helpers at 0x082c5dxx.
      The cursor is three words: `data` (+0x00, first byte of the NAL
      payload), `bit_pos` (+0x04, signed bit offset) and `end` (+0x08,
      one past the last readable byte). The read primitives —
      0x082d0630 (read n bits), 0x0809b040 (leading-zero count) — never
      move the cursor; they report through a caller-owned u8 flag that
      they stepped over an emulation-prevention 0x03 byte (detected by
      0x082c319c). This function pays for it: if the flag is set, `bits`
      grows by 8; the flag is then cleared unconditionally, so one flag
      variable serves a whole parse function. `bit_pos += bits` is
      stored back, and the return is the UNSIGNED pointer comparison
      `data + (bit_pos asr 3) < end` (movcc/movcs) — strict, so landing
      exactly on `end` reports exhausted, and arithmetic-shifted, so the
      rewinds parsers do stay correct. The cursor is updated even when
      the result is false.
      Codec identification: emulation-prevention bytes are H.264/HEVC
      specific, and the caller at 0x08365640 is textbook
      dec_ref_pic_marking() — nal_unit_type == 5 reads
      no_output_of_prior_pics_flag and long_term_reference_flag,
      otherwise adaptive_ref_pic_marking_mode_flag then a
      memory_management_control_operation loop pulling an extra ue(v)
      for ops 1/3, 2 and 3/6. Neighbours characterized while porting:
      0x082c5df0 = ue(v), 0x082c5dcc = se(v), 0x082c319c = emulation
      probe.
      Deviations: none. Wrapping arithmetic stands in for the
      original's `add` so a debug build cannot panic on overflow.
      Host tests: flag consumption, the 8-bit surcharge, any nonzero
      flag byte, exact-end exhaustion, empty payload, negative
      positions (arithmetic shift), a 220-case sweep against a
      transcription of the disassembly, and i32 overflow.
      match.py structural: same +0x00/+0x04/+0x08 offsets, same
      `asr #3` bounds test and movcc/movcs verdict; the diff is the
      crate's frame-pointer prologue and LLVM fusing two loads into
      `ldmib`.

  # --- H.264 / MPEG-4 AVC video decoder: RBSP Exp-Golomb prefix count ---
  - address: 0x0809b040
    ghidra: FUN_0809b040
    name: h264_bitstream_count_leading_zeros
    size: 240
    sig: "u32 h264_bitstream_count_leading_zeros(RbspBitReader *reader, u8 *emulation_byte_pending)"
    module: h264/bitstream
    vars: {param_1: reader, param_2: emulation_byte_pending}
    renames:
      uVar3 (first): bit_pos, then window (the first byte's low
        `first` bits, top-aligned)
      pbVar5: p (byte cursor; post-incremented by every ldrb, like
        the original)
      iVar7: first (bits left in the first byte, 1..=8; the
        `iVar7 == 0` arm is dead — 8 - (pos & 7) never yields 0 — and
        is not reproduced)
      iVar6/r4: count (the returned leading-zero count)
      iVar4/r0: i (bit index within the current byte)
      bVar1/r5: skip (pending emulation-byte skip in the whole-byte
        loop)
      bVar2: byte (current whole byte, shifted left as zeros count)
      FUN_082c319c: emulation_prevention_probe (unported elsewhere;
        transcribed as a local helper — exact behavior binary-verified
        from the 60-byte body, so no dispatch seam)
    status: ported
    notes: >-
      The Exp-Golomb prefix count over the shared RBSP cursor {data @
      +0x00, bit_pos @ +0x04, end @ +0x08}: consecutive zero bits from
      the bit position to the next 1, WITHOUT moving the cursor,
      stepping over emulation-prevention 0x03 bytes and storing 1
      through *emulation_byte_pending on every skip (the flag is
      cleared once at entry, before any cursor load). 240 bytes, all
      code, no literal pool; exactly one `bl` call site —
      cg_exp_golomb_ue_read @ 0x082c5df0 — and two direct calls to the
      emulation probe @ 0x082c319c. Its signature matches the
      CG_EXP_GOLOMB_OPS count_leading_zeros slot so the codegen
      reader's seam can later retire to a direct call.
      Algorithm: *flag = 0; p = data + (bit_pos asr 3); first = 8 -
      (bit_pos - (bit_pos & ~7)) — the bic/sub pair, a non-negative
      bit-in-byte index even for rewound positions. An aligned start
      (first == 8) probes p and steps over a 0x03. The first byte's
      low `first` bits are counted top-aligned via
      `(*p << (8 - first)) & 0xff`; then a whole-byte loop probes at
      each boundary, steps over a 0x03 (never re-probing the byte
      after it), and counts the byte's leading zeros. The only bounds
      check anywhere is the probe's UNSIGNED `p + 1 < end`; the byte
      reads themselves are unchecked, so at `end` the probe fails and
      the byte at `end` is read anyway.
      Callee: FUN_082c319c @ 0x082c319c (60 bytes, leaf, 5 `bl` call
      sites, all in the RBSP read primitives) is the
      emulation-prevention probe: true iff `p + 1 < end` (unsigned)
      and p[-2..=0] are `00 00 03` and p[1] <= 3; the two bytes before
      p are read unconditionally once the bounds test passes (in the
      firmware those are the start code's `00 00`). It is unported, so
      the port transcribes it as the local helper
      emulation_prevention_probe — its exact behavior is
      binary-verified, which the house rules allow in place of a seam.
      Deviations: the dead `iVar7 == 0` arm is dropped; `end` is
      reloaded from the cursor before every probe, like the original's
      per-call-site `ldr r1,[r6,#8]`; count wraps where the original's
      add does.
      Host tests: hand-computed prefixes 0..35 across byte boundaries,
      every alignment 0..=7 against an independent bit-walking
      reference, aligned/misaligned emulation skips with flag
      assertions, multiple emulation bytes in one walk, 0x03 as the
      last readable byte and 0x03 followed by a byte above 3 (both
      data), unchecked reads past `end`, flag cleared at entry, cursor
      unmoved, negative positions (asr), and a seeded pseudo-random
      sweep with spliced `00 00 03 0x` sequences (both skip and
      data outcomes).
      match.py structural: same flag-clear/cursor-load/asr#3 prologue,
      both probe bodies, and both counting loops; the diff is the
      crate frame, LLVM folding `bic`/`sub` into `ands #7`, and the
      inlined helper standing in for the two direct `bl 0x082c319c`.

  # --- H.264 / MPEG-4 AVC video decoder: dec_ref_pic_marking parser ---
  - address: 0x08365640
    ghidra: FUN_08365640
    name: dec_ref_pic_marking
    size: 464
    sig: "bool dec_ref_pic_marking(RbspBitReader *reader, DecRefPicMarking *marking, i32 nal_unit_type, i32 bits_out_init)"
    module: h264/dec_ref_pic_marking
    vars: {param_1: reader, param_2: marking, param_3: nal_unit_type, param_4: bits_out_init, local_14: emulation_byte_pending, local_10: bits_out}
    renames:
      param_2[0]: no_output_of_prior_pics_flag (+0x00, IDR u(1))
      param_2[1]: long_term_reference_flag (+0x01, IDR u(1))
      param_2[2]: adaptive_ref_pic_marking_mode_flag (+0x02, non-IDR u(1))
      param_2[3]: memory_management_control_operation (+0x03, ue(v), strb-truncated)
      "*(u32 *)(param_2 + 4)": difference_of_pic_nums_minus1 (MMCO 1/3 operand)
      "*(u32 *)(param_2 + 8)": long_term_pic_num (MMCO 2 operand)
      "*(u32 *)(param_2 + 0xc)": long_term_frame_idx (MMCO 3/6 operand)
      "*(u32 *)(param_2 + 0x10)": max_long_term_frame_idx_plus1 (MMCO 4 operand)
      FUN_082d0630: cg_rbsp_read_bits (u(1) flag reads; ported)
      FUN_082c5df0: cg_exp_golomb_ue_read (ue(v); ported)
      FUN_082b3258: h264_bitstream_advance (read commit; ported)
    status: ported
    notes: >-
      The H.264 dec_ref_pic_marking() syntax parser (spec section
      7.3.3.3), one `bl` call site — the slice-header parser @
      0x08366b8c passes the shared RBSP cursor, slice_header + 0x2c as
      the output record, nal_unit_type in r2 and a STALE r3. 464
      bytes, all code, no literal pool. nal_unit_type == 5 (IDR)
      reads no_output_of_prior_pics_flag and long_term_reference_flag
      (u(1) each); anything else reads
      adaptive_ref_pic_marking_mode_flag and, while the last stored
      mmco byte is nonzero, loops:
      memory_management_control_operation (ue(v), stored with strb —
      values above 255 wrap mod 256), then an extra ue(v) operand for
      mmco 1/3 (difference_of_pic_nums_minus1 @ +0x04), 2
      (long_term_pic_num @ +0x08), 3/6 (long_term_frame_idx @ +0x0c)
      and 4 (max_long_term_frame_idx_plus1 @ +0x10). Every read is
      committed by h264_bitstream_advance @ 0x082b3258; the function
      returns 1 only when every commit kept the cursor inside the NAL
      payload, 0 on the first exhaustion — with the failed element
      already stored (every strb/str precedes its bl 0x082b3258). One
      u8 emulation-prevention flag local serves every read and advance
      (zeroed once at entry); one i32 slot carries each ue(v) bit
      count from reader to advance.
      Callees — all ported, all direct calls, no seams:
      cg_rbsp_read_bits @ 0x082d0630 (u(1) reads),
      cg_exp_golomb_ue_read @ 0x082c5df0 (ue(v) reads),
      h264_bitstream_advance @ 0x082b3258 (commits). The ue reader's
      internal CG_EXP_GOLOMB_OPS seam is unchanged (its own entry).
      Deviations: r3 (bits_out_init) seeds the bits-count slot exactly
      like the original's spilled argument but is dead — the ue reader
      always stores the slot before this function loads it, and the
      call site's r3 is stale. Ghidra's break-then-return-0 on the
      mmco commit failure is the same mov r0,#0 exit every other
      failed commit branches to; the port returns false directly.
      Host tests: synthetic RBSP bitstreams through the REAL ported
      primitives end to end (CG_EXP_GOLOMB_OPS wired to
      h264_bitstream_count_leading_zeros + cg_rbsp_read_bits,
      CG_RBSP_READ_BITS_OPS to a faithful 0x082c319c transcription):
      IDR flag pairs, IDR commit failures with the failed flag stored
      and the other untouched, adaptive flag clear/set, an immediate
      end mmco, a full MMCO loop over commands 0/1/2/3/4/6 with field
      overwrite precedence, a 2^26-1 operand crossing two
      emulation-prevention bytes (prefix scan and suffix read pay one
      each), mid-mmco and mid-operand exhaustion, and raw-cursor
      end-state parity (rbsp bits + 8 per inserted EP byte).
      match.py structural: same cmp #5 split, same +0x00..+0x03 strb
      and +0x04..+0x10 str offsets, same read-then-commit interleave
      and 0/1 exits; the diff is the crate frame, LLVM register
      allocation, and the restructured loop-condition branch.

  # --- MPEG video header parser: bitstream read-and-advance ---
  - address: 0x080ebbe0
    ghidra: FUN_080ebbe0
    name: bitstream_read_advance
    size: 32
    sig: "u32 bitstream_read_advance(u8 *stream, u32 bit_count)"
    module: h264/bitstream
    status: ported
    renames:
      param_1: stream
      param_2: bit_count
    notes: >-
      The read-and-advance primitive of the MPEG-style video header
      parser (the start-code matchers at 0x080ed0e8/0x080f0cec for
      0x000001B5/B3/B2 — see bitstream_stuffing_to_byte_check @
      0x0808554c, the only other function in the cluster ported so
      far). 32 bytes, all code, no literal pool; the complete body is
      eight instructions: `stmdb sp!,{r4,r5,lr}; mov r5,r1; mov r4,r0;
      bl 0x080efa38; ldr r1,[r4,#0x4]; add r1,r1,r5; str r1,[r4,#0x4];
      ldmia sp!,{r4,r5,pc}`. 64 `bl` call sites, all in the video
      header parser cluster 0x0807d7xx..0x080f1xxx, passing counts of
      1, 2, 5, 8 and 0x20. Returns the `bit_count` bits the now-ported
      bitstream_msb_fetch @ 0x080efa38 reads MSB-first from the +0x00
      buffer at the +0x04 bit position (bit i is bit 7-((pos+i)&7) of
      byte (pos+i)>>3), then advances +0x04 by `bit_count` — the
      position word is reloaded AFTER the fetch returns, and the add is
      a plain wrapping `add` with no bounds test (the stream object is
      the two-word {data @ +0x00, bit_pos @ +0x04} pair of
      object_value_set_flags_clear @ 0x08085344; no end word exists
      here). Ghidra types the count `int` and the fetch compares it
      signed, but this body is sign-agnostic, so the port keeps the u32
      of the house BitstreamReadAdvance signature in cxx/object_flags.rs
      — the type the BITSTREAM_READ_ADVANCE seam there expects at target
      integration time.
      The fetch's port retires the BITSTREAM_MSB_FETCH seam: under the
      house convention for a ported callee (for example,
      f32_to_fixed16_sat's direct calls to __fscalb and __f2i), this
      body now calls bitstream_msb_fetch directly, recreating the retail
      `bl` rather than an indirect `blx`. Both stream words use
      pointer-sized word indexing, byte-exact at +0x00/+0x04 on the
      32-bit target and disjoint on a 64-bit host. Host tests now test
      the exported fetch directly and test this function end to end
      against an independent per-bit oracle over 210 position/count
      cases (positions -8..24, counts 0..=32, every in-byte alignment).
      Rust implementation and focused host tests:
      `src/h264/bitstream.rs::bitstream_msb_fetch` and
      `src/h264/bitstream.rs::bitstream_read_advance`.
      match.py structural: the direct branch is followed by the same
      +0x04 load/add/store; only the Rust fp frame and register allocation
      differ from the retail eight-instruction body.

  # --- Direct MSB-first bitstream read-and-advance ---
  - address: 0x080ebc00
    ghidra: FUN_080ebc00
    name: bitstream_msb_read_advance
    size: 116
    sig: "u32 bitstream_msb_read_advance(const u8 *data, u32 *bit_position, u32 bit_count)"
    module: h264/bitstream
    status: ported
    renames:
      param_1: data
      param_2: bit_position
      param_3: bit_count
    notes: >-
      A direct data-pointer and u32 cursor MSB-first reader. It loads the
      tail byte for bit_position + bit_count - 1, shifts its requested tail
      to result bit 0, then walks backward byte by byte ORing each earlier
      byte into place. It masks the result to bit_count bits below 32 and
      returns the full u32 for counts at least 32, then stores the wrapping
      bit_position + bit_count. The 116-byte body is all code with no
      literal pool. Raw decoding of every ARM B/BL word in osos.dec found
      exactly 26 call sites: 0x080a89ac, 0x080a89cc, 0x080aeb34,
      0x080aeb50, 0x080aeb74, 0x080aeb88, 0x080aebc4, 0x080aebf0,
      0x080aec04, 0x080aec30, 0x080aec4c, 0x080aec68, 0x080aec84,
      0x080c8cf4, 0x080c8d1c, 0x080c8d30, 0x080c8d78, 0x080c8d9c,
      0x080c8ed8, 0x080c8ef0, 0x080c8f38, 0x080c8f84, 0x080c8fa0,
      0x080c8fc4, 0x080c8fe8, and 0x080c9008. Every one is an
      unconditional bl; there are no predicated call sites. The retail
      body has no NULL or bounds guard, and even bit_count 0 dereferences
      the preceding byte before returning zero. ARM register-shift behavior
      for wide counts is preserved, including the low-eight-bit count and
      zero result for shifts of 32 or more. Host tests cover all in-byte
      alignments, zero through 32-bit fields, cursor commits, and a
      33-bit ARM-shift case. Rust implementation and tests:
      src/h264/bitstream.rs::bitstream_msb_read_advance.
  # --- MPEG video header parser: MSB-first bit fetch ---
  - address: 0x080efa38
    ghidra: FUN_080efa38
    name: bitstream_msb_fetch
    size: 152
    sig: "u32 bitstream_msb_fetch(u8 *stream, u32 bit_count)"
    module: h264/bitstream
    status: ported
    renames:
      param_1: stream
      param_2: bit_count
    notes: >-
      MSB-first, non-advancing fetch for the two-word MPEG video-header
      cursor {data @ +0x00, bit_pos @ +0x04}. It returns bit_count bits
      where result bit i is bit 7-((pos+i)&7) of byte (pos+i)>>3; the
      caller advances the position separately. The 152-byte body is all
      code with no literal pool or calls: its `str lr,[sp,#-4]!` prologue
      has three `ldr pc,[sp],#4` exits (in-byte fast path, partial tail,
      and a zero-remaining whole-byte-loop exit). Disassembly was checked
      instruction by instruction: `asr #3` makes negative positions
      arithmetic, `bic`/`sub` computes pos&7, the double `rsb` is an ADS
      identity, `cmp`/`movle` and `cmp`/`movge` make both count decisions
      signed, and the slow path appends bytes with `lsl #8`/`orr` before
      taking the next byte's partial MSBs. Ghidra misidentifies the two
      ordinary `ldr pc,[sp],#4` returns at 0x080efac0/0x080efacc as
      unrecovered jump tables and consequently types the function void.
      Four direct `bl` sites consume r0: 0x0807d730 (0x20) and
      0x0807d754 (0x18) in the start-code scan loop @ 0x0807d724,
      0x080ebbec in bitstream_read_advance, and 0x080f0d10 (0x20).
      This port lets bitstream_read_advance retire its former
      BITSTREAM_MSB_FETCH seam and call directly, matching the house
      convention for ported callees. The stream words are indexed by
      pointer-sized word (byte-exact on the 32-bit target; disjoint
      pointer/position slots on a 64-bit host). Deliberate domain
      deviation: Rust shifts differ from ARM register shifts for counts
      outside 0..=32; every retail call site is within that range.
      Host tests compare the export and read-and-advance composition
      against an independent per-bit reference over 210 cases, including
      rewound negative positions, all alignments, fast path, whole-byte
      loop, partial tail, and zero count.
      match.py structural: LLVM folds the duplicated data/position work,
      prefetches and rotates the whole-byte loop, and splits the fast and
      partial-tail exits, while preserving the arithmetic-shift byte
      address, signed fast/loop tests, byte append, and partial-MSB tail.

  # --- retailOS UI: the styled-text view widget ---
  - address: 0x0819b9e0
    ghidra: FUN_0819b9e0
    name: styled_text_view_construct
    size: 108
    sig: "StyledTextView *styled_text_view_construct(StyledTextView *view, ResourceProvider *resources, void *controller, void *parent, const StyledTextSpec *spec)"
    module: ui/styled_text_view
    vars: {param_1: view, param_2: resources, param_3: controller, param_4: parent, param_5: spec}
    status: ported
    notes: >-
      Constructor of a 560-byte screen widget - NOT a heap function
      despite the 0x0819xxxx address (the application heap cluster is
      0x0819cd5c..0x0819d9d8, above this). True extent 108 bytes: 104
      of code, ending `ldmia sp!,{r3,r4,r5,pc}` @ 0x0819ba44, plus the
      4-byte vtable literal @ 0x0819ba48 (= 0x0898a764); Ghidra's 104
      is code only. 109 `bl` call sites and 0 `b`, binary-scanned by
      decoding every B/BL word in osos.dec - 61 in the screen builder
      @ 0x08204714, the rest spread over 0x0821xxxx..0x0826xxxx screen
      builders, plus the `new` wrapper @ 0x0819b614
      (operator_new(0x230) then this, forwarding all four arguments).
      Body: forward all five arguments to the base ctor @ 0x0810b29c
      (the original re-stores the stacked spec into its own outgoing
      slot), plant the vtable, run the 0x0810ebbc subobject ctor on
      +0x168, then clear +0x164 and +0x220 and copy the spec tail
      +0x5c/+0x5d/+0x60/+0x64..0x67 into +0x224/+0x225/+0x228/
      +0x22c..0x22f. Returns `view` (Ghidra says void; the closing
      `sub r0,r0,#0x168` is authoritative - the pool_parent_construct
      precedent).
      Class identified from its siblings rather than its name: the
      draw path @ 0x0819b80c builds the row's string, picks the style
      byte at +0x224 or +0x225 by bit 3 of the view flag word at +0x48,
      applies it with 0x08262d70, passes &view+0x22c to 0x0826319c when
      the style code is 0x21 (a colour), and fits the text with the
      0x0826c574/0x082a2340 ellipsis loop. The lazy resolve @
      0x0819b724 turns the id at +0x228 into the 0x44-byte drawable
      cached at +0x220 (flag +0x164) through resource_chain_find on the
      provider at +0x38, trying 'Draw' then 'BMap' by the view's class
      code at +0x40 (literals @ 0x0819b7d0 = 0x424d6170 'BMap' and
      0x0819b7d4 = 0x44726177 'Draw', binary-verified). The dtor @
      0x0819ba64 deletes +0x220 (0x082646ac then operator delete),
      runs 0x0810ec10 on +0x168 and chains to 0x0810b2f0; the deleting
      variant is the unlisted 0x0819ba4c, which Ghidra drops entirely.
      Layout: vtable +0x00, base subobject to +0x164, graphic_resolved
      +0x164, a second instance of the 0xb8-byte 0x0810ebbc class at
      +0x168 (the base embeds one at +0xa4 too), graphic +0x220,
      text_style +0x224, text_style_alt +0x225, graphic_id +0x228,
      text_color +0x22c - 0x230 total, matching every call site's
      operator_new(0x230). The spec is the generic 104-byte view spec
      the grand-base ctor 0x0826f26c unpacks (+0x04 class code, +0x18
      flags, +0x1c 0x30-byte block); only its +0x5c..+0x68 tail is this
      class's.
      Deviations: the two unported callees (0x0810b29c, and 0x0810ebbc
      which cxx/pair_header.rs already models) go through the new
      STYLED_TEXT_VIEW_OPS dispatch table, both defaults returning
      their argument; `view` is kept in hand rather than recovered from
      the callees' returns by byte arithmetic, which would be wrong on
      a 64-bit host (pool_parent_construct precedent); the vtable is
      the u32 ROM address STYLED_TEXT_VIEW_VTABLE_ADDRESS, since
      nothing here dispatches through it and the image's bytes at
      0x0898a764 are stale RW init data (they read as RTTI name
      strings); and the four-byte colour copy is one array move, which
      LLVM folds into a word load/store - the same bytes, and both
      offsets are word-aligned.
      Host tests: struct offsets against the target layout, the full
      construction with the wired stubs, a zero graphic id, and a
      recording mock proving the five arguments arrive unchanged, the
      subobject ctor gets view+0x168, and the derived tail is written
      after the base ctor runs.
      match.py structural: identical offsets and order (0x164, 0x220,
      0x224, 0x225, 0x228, 0x22c from spec 0x5c/0x5d/0x60/0x64), same
      vtable literal; the diff is ops-table blx for bl, the folded
      colour word, and the crate's frame-pointer prologue.
  # --- Silver UI framework: the grand-base view constructor ---

  - address: 0x0826f26c
    ghidra: FUN_0826f26c
    name: view_base_construct
    size: 188
    sig: "ViewBase *view_base_construct(ViewBase *view, ResourceProvider *resources, void *controller, void *parent, const ViewSpec *spec)"
    module: ui/view_base
    vars:
      param_1: view
      param_2: resources
      param_3: controller
      param_4: parent
      in_stack_00000000: spec
      FUN_08103a4c: construct_linkage_base (VIEW_BASE_OPS slot)
      FUN_0826ee08: initialize (VIEW_BASE_OPS slot)
      thunk_EXT_FUN_22000188: iram_memcpy_veneer (direct, already ported)
      DAT_0826f324: VIEW_BASE_VTABLE_ADDRESS
    status: ported
    notes: >-
      The grand-base view constructor - the root of the retailOS
      screen element hierarchy that ui/string_view.rs and
      ui/styled_text_view.rs derive from. Ghidra's 184-byte extent
      drops the trailing literal again: 0x0826f26c..0x0826f324 code
      ending `ldmia sp!,{r4,r5,r6,r7,r8,pc}`, then the 4-byte vtable
      literal 0x089a59e8 @ 0x0826f324, and the unlisted deleting
      destructor @ 0x0826f328 starts immediately after, so the true
      extent is 188 bytes. 29 bl call sites, all unconditional (no
      predicated or tail form), verified by decoding every B/BL word
      in osos.dec - matching Ghidra's 29. Ghidra's C types it `int
      FUN_0826f26c(void)`; the assembly takes four register arguments
      plus the stacked spec (`ldr r5,[sp,#24]` past the six saved
      registers) and returns `view` (`mov r0,r4` before the pop).
      Body: chain the framework linkage base ctor @ 0x08103a4c with
      (view, parent, spec+0x18 & 1) - itself a 20-byte wrapper that
      runs the already-ported framework_base_construct @ 0x081110d0
      and plants vtable 0x08980854 - then plant this class's own
      vtable 0x089a59e8 over it, store `resources` at view+0x38, clear
      +0x3c, copy the class fourcc spec+0x04 to +0x40, spec+0x08 to
      +0x44, the flag word spec+0x18 to +0x48, spec+0x10 to +0x4c, and
      the 0x30-byte block spec+0x1c to +0x50 through the ROM memcpy
      veneer (bl 0x08037df8 = iram_memcpy_veneer, already ported and
      called directly). Clear +0x80..+0x8c and the bytes +0x94/+0xa0,
      then set +0x90 to spec+0x58 when the reloaded view+0x48 flag
      word covers 0x38000 or 0x1c0000 in full (the original's
      bics/bicsne pair rejects partial masks), else clear it. Finally
      run the initialiser @ 0x0826ee08 with (view, controller, spec)
      and return `view`. 0x0826ee08 inherits the resource provider
      from `controller` when the view's own is NULL and spec+0x0c is
      -1, registers the view with the provider, applies flag-gated
      mode words, dispatches vtable slot +0x9c and fills view
      +0x98/+0x9c. Unlike the derived classes' stale literals, the
      image data at 0x089a59e8 is a LIVE vtable - real code pointers,
      slot +0x9c -> 0x08122124 binary-verified by its prologue - and
      0x0826ee08 dispatches through it while the grand-base vtable is
      still planted. Deviations: the port keeps `view` in hand instead
      of threading r0 out of the linkage ctor (the chain is verified
      to return its argument; wrong on a 64-bit host otherwise, the
      pool_parent_construct precedent); the vtable is the u32 ROM
      address VIEW_BASE_VTABLE_ADDRESS; `resources` is stored as its
      low 32 bits into the target word at +0x38; the two unported
      callees (0x08103a4c, 0x0826ee08) ride the new VIEW_BASE_OPS seam
      (firmware transmute on target, panic on host, the
      ui/string_view.rs pattern). The string_view and styled_text_view
      construct_base slots can now be wired to this port on target.
      Host tests use the unique hints::VIEW_BASE slab (0x2d000000):
      full construction with recording mocks (field-by-field against
      the spec, the +0x98/+0x9c poison proving the ctor never touches
      the initialiser's words, call order linkage-before-initialize,
      the vtable overwrite order via a mock that plants 0x08980854),
      flag bit 0 -> create_link, and the +0x90 mask coverage over 8
      flag words including partial masks. match.py structural: same
      offsets and order (+0x38/+0x3c/+0x40/+0x44/+0x48/+0x4c, the
      0x30-byte copy, +0x80..+0x8c/+0x94/+0xa0 clears, +0x90 from
      spec+0x58), same vtable literal 0x089a59e8; the diff is
      ops-table blx for bl, LLVM's mvn/tst for the bics mask pair, the
      ldreq+unconditional-str fold of the streq/strne pair, and the
      crate's frame-pointer prologue.

  # --- Silver UI framework: the container view constructor ---

  - address: 0x08158778
    ghidra: FUN_08158778
    name: container_view_construct
    size: 124
    sig: "ContainerView *container_view_construct(ContainerView *view, ResourceProvider *resources, void *controller, void *parent, const ViewSpec *spec)"
    module: ui/container_view
    status: ported
    renames:
      param_1: view
      param_2: resources
      param_3: controller
      param_4: parent
      param_5/in_stack_00000000: spec
      FUN_0826f26c: view_base_construct (direct, already ported)
      FUN_0813533c: registry_container_construct_default (direct, already ported)
      FUN_08157f18: refresh_clip_rect (CONTAINER_VIEW_OPS seam, unported)
      DAT_081587f0: CONTAINER_VIEW_VTABLE_ADDRESS
    notes: >-
      The container view constructor - the mid-hierarchy Silver UI
      class between the grand-base view (0x0826f26c) and the concrete
      widgets, owning the 0x28-byte child-view registry at +0xa8 that
      every method in its image block (0x081574c0..0x08158744)
      iterates before dispatching child virtuals: 0x081574c0/
      0x081584f0 forward slot +0xa4, 0x08157fac unions child bounding
      boxes and re-fits the scrollbars, the destructor helper @
      0x08157b18 detaches every child through slot +0x04, and the
      accessor @ 0x081586e0 is a one-liner returning this+0xa8. The
      22 bl call sites are all derived-class constructors or their new
      wrappers chaining into this one; the operator_new(0xe8) wrapper
      @ 0x0815746c fixes the class size at 232 bytes. Extent is 124
      bytes: 120 code ending pop {r3,r4,r5,pc} @ 0x081587ec plus the
      4-byte vtable literal 0x08986f38 @ 0x081587f0 that Ghidra's
      120-byte extent drops; the deleting destructor @ 0x081587f4
      starts immediately after. 22 bl call sites, 0 predicated, 0
      tail b, verified by decoding every B/BL word in osos.dec
      (matches Ghidra's 22); a whole-image word scan finds 0 data
      references to 0x08158778 - never dispatched virtually. Body:
      chain view_base_construct with all five arguments unchanged,
      plant vtable 0x08986f38, copy spec+0x58 to view+0xa4 (the class
      config word; read as a flag word by 0x0815826c, and - unlike
      the grand-base's flag-gated +0x90 copy of the same spec word -
      unconditional), build the capacity-4/growth-4 registry at +0xa8
      via the already-ported registry_container_construct_default,
      clear the clip-rect cache +0xd0..+0xdc (the parent+0xd0 clip
      rect ui/invalidate.rs documents, refreshed from the frame
      +0x80..+0x8c by the callee 0x08157f18) and the word +0xe4, then
      install the content provider at +0xe0: the view itself when the
      controller is NULL (streq), else the controller's vtable slot
      +0x5c query result (ldr/ldr/blx). Finish with 0x08157f18(view)
      and return view. Deviations: the port keeps view in hand
      instead of the original's sub r4,r0,#0xa8 byte arithmetic
      (wrong on a 64-bit host, the styled_text_view precedent); the
      vtable is the u32 ROM address CONTAINER_VIEW_VTABLE_ADDRESS -
      the image words at 0x08986f38 are stale RW init data (they
      decode as data-region pointers, not entry points), same finding
      as styled_text_view; the spec type is reused ViewSpec - this
      class adds no spec tail of its own; the two already-ported
      callees are called directly while the unported clip-rect
      refresh 0x08157f18 (which also intersects the parent's cache
      through the ported rect_intersect 0x0826c1c8 and propagates
      dirty flag 0x80 up the +0x34 ancestor chain; its r0:r1 previous
      -rect return is discarded at this call site) rides the new
      CONTAINER_VIEW_OPS seam (firmware transmute on target, panic on
      host); the controller query is modelled through a usize-slot
      vtable (the app/path_probe.rs idiom) so host tests need no
      sub-4-GiB fixture; the four clip-rect clears are per-word
      stores like the original's r1=0 replay (an array write spilled
      each word through the stack). Host tests: layout against the
      target offsets; the NULL-controller path (self-install, config
      verbatim, registry ctor sees view+0xa8 with (4,4), the
      linkage/initialize/registry/refresh order trace, and the
      flags==0 differential proving the base's +0x90 copy stays gated
      while +0xa4 is not); the controller path (slot result stored,
      controller pointer forwarded unchanged); a zero config word.
      match.py structural: same offsets and order (+0xa4 from
      spec+0x58, +0xd0..+0xdc/+0xe4 clears, +0xe0 store), an
      instruction-identical controller dispatch (ldr r0,[r5]; ldr
      r1,[r0,#0x5c]; mov r0,r5; blx r1), same vtable literal
      0x08986f38; the diff is the ops-table blx for the refresh bl,
      LLVM's branch+mov selection for the streq/beq pair, and the
      crate's frame-pointer prologue.

  - address: 0x081586e0
    ghidra: FUN_081586e0
    name: container_view_children
    size: 8
    sig: "Registry *container_view_children(ContainerView *view)"
    module: ui/container_view
    status: ported
    renames:
      param_1/r0: view
      FUN_081ee17c: cursor_init (direct consumer, already ported)
      FUN_081ee138: cursor_advance (direct consumer, already ported)
      FUN_08155e80: iterator_state_construct (direct consumer, already ported)
    notes: >-
      The container view's child-registry accessor - a one-liner
      returning this+0xa8, the 0x28-byte child-view registry the
      class's constructor (container_view_construct @ 0x08158778,
      ported in the same module) builds there. Extent binary-verified
      at 8 bytes exactly: add r0,r0,#0xa8 ; bx lr, no literal pool;
      the previous method ends pop {r2,r3,r4,pc} immediately before
      and the next method @ 0x081586e8 opens with
      push {r1,r2,r3,r4,r5,lr} immediately after (it re-derives
      this+0xa8 in-line as add r1,r0,#0xa8). Verified counts from
      decoding every B/BL word in osos.dec: 22 bl call sites, 0
      predicated, 0 tail b - matching Ghidra's 22 - and 0
      occurrences of 0x081586e0 as a data word in a whole-image
      scan, so the accessor is never dispatched virtually. The
      all-unconditional call split is a behavioural fact: the callee
      has no NULL guard and the callers never gate the call either,
      so a NULL view returns bare 0xa8. The port reproduces that
      with wrapping byte arithmetic over
      offset_of!(ContainerView, children) rather than a field
      projection. Every recovered caller feeds the result into a
      ported collection iterator (cursor_init/cursor_advance, or
      iterator_state_construct with mask -2) to walk the children,
      or reads the registry's own fields through it (0x081dd30c and
      siblings dereference +4). Host tests: the returned pointer
      lands exactly at view+0xa8 and aliases the children field for
      two distinct fixtures, and NULL wraps to 0xa8 unguarded.
      match.py structural: the body add r0,r0,#0xa8 is
      instruction-identical; the diff is only the crate's
      frame-pointer prologue (push {fp,lr} / pop {fp,pc} for bx lr).

  - address: 0x0826d850
    ghidra: FUN_0826d850
    name: view_base_set_geometry
    size: 44
    sig: "void view_base_set_geometry(ViewBase *view, const void *geometry, u32 redraw)"
    module: ui/set_geometry
    status: ported
    renames:
      param_1/r0/r4: view
      param_2/r1: geometry
      param_3/r2/r5: redraw
      thunk_EXT_FUN_22000188: iram_memcpy_veneer (direct, already ported)
      FUN_0826db38: VIEW_BASE_GEOMETRY_CHANGED seam (unported redraw helper)
    notes: >-
      The geometry setter of the grand-base view (the class
      ui/view_base.rs constructs; +0x50..+0x80 is its 0x30-byte
      `geometry` block). Extent binary-verified: Ghidra's 44 bytes is
      exact - push {r4,r5,r6,lr} @ 0x0826d850 through pop {r4,r5,r6,pc}
      @ 0x0826d878, and the distinct sibling FUN_0826d87c opens with its
      own push {r0-r6,lr} at 0x0826d87c. Decoding every ARM B/BL word
      in osos.dec finds exactly 22 unconditional bl call sites, zero
      predicated forms, zero tail b, and a whole-image word scan finds
      no data references - never dispatched virtually, no NULL guard,
      callers always hold a live view (the style-attribute applier @
      0x08180194 and the screen builders @ 0x08288218.. pass freshly
      decoded blocks with redraw = 1). Body: iram_memcpy_veneer
      (view+0x50, geometry, 0x30) - the bl 0x08037df8 ROM veneer,
      already ported and called directly per the view_base_construct
      precedent - then, when redraw != 0, a tail call (movne r0,r4;
      popne {r4,r5,r6,lr}; bne 0x0826db38) into the unported redraw
      helper FUN_0826db38, which (raw-byte verified) sets flags+0x48 |=
      0x20, invalidates the view's own bounds (+0x80) through the
      unported region form @ 0x0826ec14, runs the parent's vtable
      +0x128 notification when parent +0x34 exists and its byte +0xa0
      is clear, and marks the vtable +0x5c owner's +0x48 word with
      0x40. Deviations: the tail branch becomes a call through the new
      VIEW_BASE_GEOMETRY_CHANGED seam (target builds transmute the
      retail address 0x0826db38, hook-ready on device; host default
      inert; tests install a recording model - the ui/invalidate.rs
      precedent), and the port returns void - the original's exit r0
      (memcpy's return on the redraw==0 path, the helper's leftover on
      the other) is dead everywhere: all 9 sampled call sites
      (0x080966e8/0x0809ef60/0x080a7708/0x080ac0f8/0x081165dc/
      0x081802fc/0x08182888/0x08288218/0x0828865c) overwrite r0 with
      the very next instruction. Host tests cover the verbatim 0x30-byte
      copy with +0x4c/+0x80 canaries, redraw==0 non-dispatch, dispatch
      for redraw 1/2/0x80000000 with the view pointer, full-overwrite
      (not merge) semantics, and the inert host default. match.py
      structural: the add r0,r0,#0x50 / mov r2,#0x30 / bl memcpy /
      cmp-redraw core survives verbatim; the diff is the crate's
      frame-pointer prologue and the seam's ldr/bx tail in place of the
      original's movne/popne/bne predicated fold.
  - address: 0x0826d87c
    ghidra: FUN_0826d87c
    name: view_base_set_x_extent
    size: 184
    sig: "ViewBase *view_base_set_x_extent(ViewBase *view, u32 extent)"
    module: ui/set_x_extent
    status: ported
    renames:
      param_1/r0/r4: view
      param_2/r1/r0: extent
      local_20/local_18: temporary bounds x_start/x_end
      "*(code **)(*param_1 + 0x68)": vtable slot +0x68 (identity unresolved)
      FUN_0826db38: VIEW_BASE_GEOMETRY_CHANGED seam (unported redraw helper)
    notes: >-
      Raw-byte extent is 184 bytes from push {r0-r6,lr} @ 0x0826d87c
      through the final branch @ 0x0826d930; the separately linked next
      function starts push {r4,r5,lr} @ 0x0826d934, so Ghidra's
      100-byte extent truncated this body. A whole-image ARM B/BL decode
      finds eight direct callers: five unconditional bl and three blne
      (0x081bae70, 0x081d9ff0, 0x08287924), zero direct tail branches,
      and zero image-word references. The predicated callers gate the
      update themselves; this body has no NULL guard and its changed path
      always calls the vtable +0x68 slot. It snapshots bounds
      +0x80/+0x84/+0x88/+0x8c, returns view unchanged when x_end-x_start
      equals extent, otherwise handles end rule 0x80000001, absolute
      extent rule 0x80000008, or a generic rule before slot dispatch with
      the temporary bounds and r2=1. The 0x80000001 and absolute end
      rules write geometry +0x70; the matching start rule writes +0x58.
      The generic rule deliberately omits the subsequent redraw helper;
      every other changed path calls it. The grand-base vtable's +0x68
      word is 0x08105d0c, an address inside an instruction stream rather
      than a verified function entry, so the port names the callback only
      by slot and does not invent its identity. Target builds dispatch the
      actual vtable; host builds use a recording seam. Host tests exercise
      bit-0x20-masked modes, all three rule families, modular overflow,
      generic no-redraw, and equal-extent no-dispatch paths. match.py
      structural review is recorded with the port.
  # --- Silver UI framework: the class-0x6800 controller addon ---

  - address: 0x08177e84
    ghidra: FUN_08177e84
    name: class_6800_new
    size: 72
    sig: "Class6800 *class_6800_new(Class6800 *storage)"
    module: app/class_6800
    status: ported
    vars:
      param_1: storage
      DAT_08177ecc: CLASS_6800_VTABLE_ADDRESS
      FUN_081110d0: framework_base_construct
      FUN_081d2204: framework_root_instance (CLASS_6800_OPS slot)
      FUN_081883fc: demo_mode_instance
    notes: >-
      128 bl + 0 b call sites, binary-scanned by decoding every B/BL word
      in osos.dec. True extent is 76 bytes - Ghidra's 72 bytes of code
      plus the literal-pool word @ 0x08177ecc holding the vtable address
      0x0898908c. The constructor of the small Silver-framework object a
      controller hangs off itself, whose runtime class id is 0x6800:
      `base_construct(storage, 0, 1)` @ 0x081110d0, plant the vtable,
      store the framework root instance (0x081d2204 = the +4 slot of the
      global @ 0x089cc858) at +0x14 and the TCDemoMode singleton
      (0x081883fc, already ported, called DIRECTLY) at +0x18, then
      dispatch vtable slot +0x2c with the +0x14 word RE-READ from the
      object, and return `this` - which is the BASE CONSTRUCTOR'S
      RETURN (`mov r4, r0` follows the bl), not the incoming storage.
      Class identification - the cast_to_class operator @ 0x08177e08 is
      `cmp r1,#0x6800; bne 0x08110640; bx lr` (the base @ 0x08110640
      answers ids 4, 3 and 1); the immediate 0x6800 occurs at exactly
      128 sites image-wide, the one cmp plus 127 `mov r1,#0x6800`, one
      per call site, and every call site reads
      `if (object_cast_to_class(ctrl->vtable[0x1c](ctrl), 0x6800) == 0)
      { obj = class_6800_new(operator_new(28)); ctrl->vtable[0x2c](ctrl,
      obj); }`. 125 of the callers are the near-identical Silver
      controller bodies in 0x0839f6a0-0x083b43f4 (the framework prefix is
      the 159 distinct TSilver* mangled names in the image). The CLASS IS
      NOT NAMED anywhere in the image, so the port calls it by its id,
      the same rule app/registry.rs's instance_of_class_6000/_6600
      follow. Object layout - +0x00 vtable, +0x0c the 16-byte node the
      base ctor @ 0x081108b4 allocates, +0x14 default_target, +0x18
      demo_mode, 28 bytes total (`mov r0,#28` before every
      operator_new). Sibling methods corroborate slot +0x2c as "adopt
      this target": 0x08177e14 re-dispatches it with +0x14, 0x08177e24
      dispatches it with a live object when one exists and +0x14
      otherwise. Deviations - the vtable literal 0x0898908c lands inside
      the C++ mangled-name blob in the decrypted image (the same page
      mismatch app/registry.rs records for TCDemoMode's 0x08989718) and
      is the only reference to that word in the image, so the port plants
      the modeled static CLASS_6800_VTABLE and keeps the address as
      CLASS_6800_VTABLE_ADDRESS; the +0x2c dispatch still goes through
      `this->vtable`, so a real vtable is picked up unchanged. The parent
      constructor remains a CLASS_6800_OPS seam, while the base link
      initializer @ 0x081108b4, root getter, and demo lookup are direct
      ports. Its initializer retains only the allocation diagnostic and
      context getters as separate seams. Host tests cover the direct base
      wrapper's call order, return forwarding, NULL storage forwarding
      through a cooperating parent, full derived construction order,
      relocation, NULL root, and both explicit/implicit link-owner paths.
      match.py structural: the outer constructor retains its mov #1 /
      mov #0 / parent call / vtable store / direct initializer call /
      return sequence; the expected diff is the parent ops-table call and
      the crate frame.


  # --- Silver UI framework: root instance getter ---

  - address: 0x081d2204
    ghidra: FUN_081d2204
    name: framework_root_instance
    size: 12
    sig: "void *framework_root_instance(void)"
    module: app/class_6800
    status: ported
    vars:
      DAT_081d2210: FRAMEWORK_ROOT_HOLDER_ADDRESS (0x089cc858)
    notes: >-
      31 bl + 0 predicated call sites, binary-scanned by decoding every
      B/BL word in osos.dec (matches Ghidra's count). True extent is 16
      bytes: Ghidra's 12 bytes of code plus the literal-pool word @
      0x081d2210 holding the holder address 0x089cc858; the next
      function starts at 0x081d2214, confirmed from raw bytes. Raw ARM
      is `ldr r0,[pc,#4]; ldr r0,[r0,#4]; bx lr` - a pure getter that
      returns the +0x04 slot of the runtime global @ 0x089cc858, the
      Silver UI framework root object. Every caller uses the result as
      a C++ object and dispatches a virtual slot on it (offsets +0xf4
      through +0x1ec observed across the 31 sites), or stores it as an
      observer/default-target field. There is no NULL guard: the holder
      address is always loaded, and the instance slot is returned
      verbatim, including the pre-initialization NULL. The holder page
      is runtime-initialized RW data, so the image holds nothing usable
      there; the port models it as the zeroed FRAMEWORK_ROOT_HOLDER
      static and reads the instance slot with read_volatile. Deviations
      - none beyond the modeled global. Host tests (in app/class_6800.rs,
      alongside the class-0x6800 constructor that consumes this getter)
      prove the +0x04 slot is returned verbatim, a pre-init NULL is not
      special-cased, and the +0x00 word is neither read nor written.
      match.py structural: identical load chain (ldr r0,[pc] / ldr
      r0,[r0,#4]); the diff is only the crate's frame-pointer prologue.

  # --- Silver UI framework: linkage parent constructor ---

  - address: 0x08125234
    ghidra: FUN_08125234
    name: framework_linkage_parent_construct
    size: 32
    sig: "Class6800 *framework_linkage_parent_construct(Class6800 *storage)"
    module: app/class_6800
    status: ported
    vars:
      param_1: storage
      puVar1: this
      FUN_08275bb8: framework_object_construct
      DAT_08125254: FRAMEWORK_LINKAGE_PARENT_VTABLE_ADDRESS
    notes: >-
      The unnamed parent of the class-0x6800 linkage base. Raw ARM is
      `push {r4,lr}; bl 0x08275bb8; ldr r1,[pc,#16]; str r1,[r0];
      mov r1,#0; str r1,[r0,#4]; str r1,[r0,#8]; pop {r4,pc}`, with
      vtable literal 0x089830b8 at 0x08125254. The root child returns
      r0 untouched; this constructor uses that result for all three
      stores and returns it unchanged. It has no NULL guard. The parent
      vtable is overwritten immediately by 0x081110d0 before a virtual
      call, so the port models its identity while preserving the direct
      root call and exact store order. Focused host coverage verifies the
      returned storage, vtable replacement, cleared +0x04/+0x08, and
      preservation of every child/derived word.

  # --- Silver UI framework: link-owning base constructor ---

  - address: 0x081110d0
    ghidra: FUN_081110d0
    name: framework_base_construct
    size: 52
    sig: "Class6800 *framework_base_construct(Class6800 *storage, u32 initial_target, u32 create_link)"
    module: app/class_6800
    status: ported
    vars:
      param_1: storage
      param_2: initial_target
      param_3: create_link
      puVar1: this
      FUN_08125234: parent_construct (CLASS_6800_OPS slot)
      DAT_08111104: FRAMEWORK_BASE_VTABLE_ADDRESS
      FUN_081108b4: framework_base_initialize (direct)
    notes: >-
      This unnamed framework linkage base is identified by behavior rather
      than an image class name. Raw ARM is exactly: save r1/r2; bl
      0x08125234; load literal 0x08981958; store it at the returned
      object's +0x00 vtable; restore r1/r2, set r3=0; bl 0x081108b4;
      return the parent result held in r4. The first child itself chains
      to 0x08275bb8 and zeroes +0x04/+0x08. The second child, when
      create_link is nonzero, allocates/zeros a 16-byte node at +0x0c,
      dispatches vtable +0x2c with initial_target, and writes +0x10.
      Thus this wrapper owns only the vtable store and exact child-call
      ordering; the parent is its sole dispatch seam and the second child
      is a direct port. It has no NULL checks: a NULL parent result faults
      at the vtable store exactly as retail ARM does. The parent return is
      forwarded after initialization, even when it differs from storage.
      Host tests prove the vtable is visible to the direct child,
      relocation, and a NULL storage argument forwarded to a cooperating
      parent. match.py structural: parent bl, vtable store, base-init bl,
      and r4 return forwarding; expected diff is the parent seam call.

  # --- Silver UI framework: task-target base constructor ---

  - address: 0x0811113c
    ghidra: FUN_0811113c
    name: framework_base_construct_with_task_target
    size: 56
    sig: "Class6800 *framework_base_construct_with_task_target(Class6800 *storage, u32 create_link)"
    module: app/class_6800
    status: ported
    vars:
      param_1: storage
      param_2: create_link
      puVar1: this
      iVar2: task_context
      FUN_08125234: framework_linkage_parent_construct
      DAT_08111174: FRAMEWORK_BASE_VTABLE_ADDRESS
      FUN_080cb828: current_task_ctx_block
      field_0x24: framework_base_initial_target
      FUN_081108b4: framework_base_initialize
    notes: >-
      Raw ARM is `push {r4,r5,r6,lr}; mov r5,r1; bl 0x08125234; ldr
      r1,[pc,#36]; mov r4,r0; str r1,[r0]; bl 0x080cb828; ldr r1,[r0,#36];
      mov r0,r4; mov r3,#0; mov r2,r5; bl 0x081108b4; mov r0,r4; pop
      {r4,r5,r6,pc}`. The 56-byte code is followed by its vtable literal
      0x08981958 at 0x08111174, so the true image extent is 60 bytes. It
      directly chains the ported linkage parent, replaces the vtable, reads
      the current TaskCtx +0x24 word as framework_base_initial_target, then
      initializes with owner NULL and returns the parent's r0. It has no
      NULL checks after either direct call. Binary scanning every ARM B/BL
      word found 12 call sites, all plain BL (0 predicated): 0x0813ec1c,
      0x0816053c, 0x08167200, 0x081681ac, 0x08184804, 0x081a2348,
      0x081c92fc, 0x081d2834, 0x081fbd6c, 0x08201de8, 0x0820a40c, and
      0x08272478. No dispatch seam is added because all three direct
      callees are already ported. Host coverage installs a synthetic
      running NameNode and verifies one context read, the direct parent
      path, vtable replacement, link allocation, and exact +0x24 target
      forwarding to the base vtable's set-target call.


  # --- Silver UI framework: task-context derived base constructor ---

  - address: 0x08272474
    ghidra: FUN_08272474
    name: framework_task_target_base_construct
    size: 28
    sig: "Class6800 *framework_task_target_base_construct(Class6800 *storage, u32 create_link)"
    module: app/class_6800
    status: ported
    renames:
      param_1/r0: storage
      param_2/r1: create_link
      puVar1/r0: this
      FUN_0811113c: framework_base_construct_with_task_target
      DAT_08272490: FRAMEWORK_TASK_TARGET_BASE_VTABLE_ADDRESS
    notes: >-
      True extent is 32 bytes, 0x08272474..0x08272494: seven code
      instructions followed by the 0x089a5de0 vtable literal at
      0x08272490; raw bytes prove the separately linked null-checking
      destructor begins at 0x08272494. Decoding every ARM B/BL word in
      work/firmware/osos.dec found exactly 8 unconditional BL call sites
      (0x0813042c, 0x081439ac, 0x0819e5cc, 0x081a763c, 0x081b80bc,
      0x081d9ea4, 0x081e93fc, 0x082139c0), with 0 predicated BL forms
      and 0 direct B tail calls. It directly calls the already-ported
      framework_base_construct_with_task_target(storage, create_link),
      overwrites the returned object's +0x00 vtable with the literal,
      clears its +0x14 word, and returns that same r0. There are no NULL
      guards. The vtable's concrete class and slot identities remain
      unestablished, so the name records only the verified task-context
      target construction mechanism; the port preserves the retailOS
      address as an opaque pointer rather than inventing a vtable model.
      Host coverage supplies a live TaskCtx and proves context forwarding,
      direct-parent use, the literal vtable store, the exact +0x14 clear,
      and preservation of the next word. match.py with this worktree's
      archive reports the same call/literal/store/zero/store skeleton;
      the expected differences are LLVM's fp frame and the archive
      relocation rendering the direct callee as the local symbol.
  # --- Silver UI framework: link and owner initializer ---

  - address: 0x081108b4
    ghidra: FUN_081108b4
    name: framework_base_initialize
    size: 188
    sig: "void framework_base_initialize(Class6800 *this, void *initial_target, u32 create_link, FrameworkBaseOwner *owner)"
    module: app/class_6800
    status: ported
    vars:
      param_1: this
      param_2: initial_target
      param_3: create_link
      param_4: owner
      puVar1: link
      iVar2: link_context_or_active_owner
      FUN_080eb67c: malloc_wrapper (direct, 16 bytes, tag 0)
      FUN_081b53e4: report_allocation_failure (FRAMEWORK_BASE_INITIALIZE_OPS slot)
      FUN_080cb828: implicit_link_context (FRAMEWORK_BASE_INITIALIZE_OPS slot)
      FUN_0809444c: active_link_owner (FRAMEWORK_BASE_INITIALIZE_OPS slot)
    notes: >-
      Raw ARM saves r4-r8, branches on r2, and returns void with r0 dead.
      For nonzero create_link it calls malloc_wrapper(16, 0); if r0 is
      NULL it calls FUN_081b53e4(4, "nil") then intentionally falls
      through to store the pointer at this+0x0c and zero node +0x0c,
      +0x08, +0x04, then byte +0x00. It dynamically calls
      this->vtable+0x2c(this, initial_target). For a non-NULL owner it
      reads owner+0x0c and writes context+0x24 only if empty; for NULL it
      gets FUN_080cb828() and overwrites its +0x24. It writes this+0x10
      to the explicit owner, or to FUN_0809444c() when owner is NULL. If
      create_link is zero it clears only +0x0c before the same final owner
      selection. No NULL path is added: every ARM dereference/store after
      allocation, vtable, context, or owner lookup remains unconditional.
      The direct malloc veneer and virtual dispatch are retained, while
      exactly the three unported direct callees use a read-volatile ops
      table. Focused host tests cover no-link explicit/implicit ownership,
      16-byte tag-0 allocation and exact node bytes, dynamic dispatch,
      implicit overwrite, and empty-versus-occupied explicit contexts.

  - address: 0x081109a0
    ghidra: FUN_081109a0
    name: framework_base_transition_link_state
    size: 192
    sig: "void framework_base_transition_link_state(Class6800 *this, void *target)"
    module: app/class_6800
    status: ported
    vars:
      param_1: this
      param_2: target
      puVar1: link
      uVar2: current_target
      uVar3: collection_handle
      local_18: FrameworkBaseLinkIterator
      local_4: FrameworkBaseLinkCollectionEntry
      FUN_08155e80: collection_iterator_begin (FRAMEWORK_BASE_STATE_OPS slot)
      FUN_08155d6c: collection_iterator_next (FRAMEWORK_BASE_STATE_OPS slot)
      FUN_08155ec0: collection_iterator_drop (FRAMEWORK_BASE_STATE_OPS slot)
    notes: >-
      Raw ARM reads the state byte at this+0x0c, returning immediately
      when it is 2. Otherwise it invokes vtable+0x1c twice when the
      first result is non-NULL, recursively transitions that second
      result with the same target, then handles the optional link+0x08
      collection. The collection iterator local is exactly 20 bytes:
      FUN_08155e80(iterator, handle, -2), then FUN_08155d6c(iterator,
      &entry) until zero, clearing the byte through entry+0x0c on every
      yielded element, followed by FUN_08155ec0(iterator). It writes
      state 2 and tail-dispatches vtable+0x4c(this, target). Callers
      treat r0 as dead, so the recovered Rust ABI is void. Collection and
      iterator internals remain opaque local seams; only their recovered
      ABI and the entry +0x0c state-byte pointer are modeled. Focused
      host tests cover the terminal short circuit, repeated +0x1c read,
      recursion, iterator start -2, per-entry byte clears, state writes,
      and final +0x4c dispatch order.

  # --- Silver UI framework: the "SLst" resource-backed item table ---

  - address: 0x081473c0
    ghidra: FUN_081473c0
    name: silver_list_table_item
    size: 24
    sig: "void *silver_list_table_item(SilverListTable *table, u32 item_id)"
    module: app/silver_list_table
    status: ported
    vars:
      param_1: table
      param_2: item_id
      FUN_083db92c: map_value_slot (SILVER_LIST_TABLE_OPS slot)
    notes: >-
      128 bl + 0 b call sites, binary-scanned. 24 bytes, all code - no
      literal-pool word. The by-id accessor of the Silver framework's
      resource-backed item table: `push {r0,r1,r4,lr}` carves a two-word
      scratch frame (it is NOT a register save; the matching `pop
      {r2,r3,r4,pc}` discards it), spilling item_id so the key crosses to
      the map by pointer to a PRIVATE copy, then indexes the map at
      table+0x0c through 0x083db92c and dereferences the returned value
      word. The class - its ctor is the next function, FUN_081473d8
      (table, resource_id, populate): +0x00 vtable 0x08986434, +0x04
      resource_id, +0x0c the map, +0x28 a handle, +0x2c 2. With populate
      nonzero it runs FUN_081472b0, which loads the "SLst" (0x534c7374)
      resource list for resource_id via 0x08184fd4 and, per record,
      allocates 0x5c bytes, constructs the item with FUN_081e0bac(item,
      record[0], record[1]&0xff, record), backlinks the table at
      item+0x58 and stores it as map[record[0]] - so the map is
      `item_id -> 0x5c-byte item` and this is its reader. The ctor then
      resolves ("SCST", resource_id) through the provider-tagged find
      FUN_0811ca58 into the +0x28 handle. "SLst" and "SCST" are both
      kinds in the resource directory @ 0x0840b1c0 (with IMAG, ITEM,
      mTDL, SEVT, SIEM, SLyt, SORC, SSin, "Str ", TEVT), each with 0x95
      records; the S-prefixed kinds are the Silver framework's. CLASS NOT
      NAMED - the vtable literal points at runtime data and no ctor in
      the family names itself to the class-name factory, so the port
      names the table for the resource kind it is built from.
      FUN_083db92c (56 bytes, exactly 2 bl sites - here and the populator
      @ 0x08147328) is `operator[]` on an ordered map: it builds the
      value_type {key, 0} on the stack, calls the red-black insert_unique
      @ 0x083c8aa8 and returns node+20. Node layout {color, parent, left,
      right, key@16, value@20} and container layout {header@0x10,
      flag@0x18, comparator@0x19} both come from 0x083c8aa8's descent. A
      MISS IS NOT FREE - operator[] default-inserts a zero-valued entry,
      so a lookup grows the map and answers NULL; the 125 Silver
      controller call sites in 0x0839f870-0x083b4xxx treat NULL as fatal
      (`movs r6,r0; bleq 0x08030f44`), the three stack-constructed
      callers (0x0816d418, 0x0816d440, 0x08184010) do not. Deviation -
      0x083db92c is unported (its insert_unique pulls in the whole
      red-black tree) so it goes through the SILVER_LIST_TABLE_OPS
      read_volatile dispatch table; the wired default models an EMPTY
      table by handing back a shared always-NULL slot, so the port is NOT
      HOOK-READY until 0x083db92c is ported. Host tests cover a hit, a
      miss (asserting the default insertion), a zero key, the
      private-spill pointer contract and the wired default. match.py
      structural: identical `add r0,r0,#12` / `add r1,sp,#4` / call /
      `ldr r0,[r0]` core; the diff is the explicit spill store ADS folded
      into its push, the ops-table indirection and the crate's fp frame.

  - address: 0x081473d8
    ghidra: FUN_081473d8
    name: silver_list_table_ctor
    size: 224
    sig: "SilverListTable *silver_list_table_ctor(SilverListTable *table, u32 resource_id, i32 populate)"
    module: app/silver_list_table
    status: ported
    renames:
      param_1: table
      param_2: resource_id
      param_3: populate
      DAT_081474ac: vtable_literal (0x08986434, runtime data - class not named)
      DAT_081474b0: empty_string_rep_data (0x08b31810)
      DAT_081474b4: silver_list_name_tag ('SCST', 0x53435354)
      FUN_083c8404: map_header_alloc (SILVER_LIST_TABLE_CTOR_OPS slot; the
        map container's embedded chunk-pool node allocator - initial chunk
        32 nodes, 1.625x growth - NOT a constructor, it returns an
        uninitialized node)
      FUN_081472b0: populate (SILVER_LIST_TABLE_CTOR_OPS slot; the "SLst"
        list loader documented under silver_list_table_item)
      FUN_0819fdb0: registry (SILVER_LIST_TABLE_CTOR_OPS slot; the tagged
        string registry getter - the FUN_0819fe10 provider's +4 object or
        NULL, same getter event_list_populate_from_registry uses)
      FUN_0811ca58: resolve (SILVER_LIST_TABLE_CTOR_OPS slot; ported as
        app_string_resolver_resolve)
      FUN_08030f44: fail (SILVER_LIST_TABLE_CTOR_OPS slot; ported as
        heap_panic, non-returning)
      FUN_083d8bac: cxx_string_from_buffer (direct, ported in cxx/string)
      FUN_083d8d1c: cxx_string_assign (direct, ported in cxx/string)
      FUN_083d8b04: cxx_string_release (direct, ported in cxx/string)
    notes: >-
      132 bl + 0 b call sites, binary-scanned by decoding every B/BL word
      in osos.dec; all 132 are unconditional (no predicated forms - the
      NULL discipline lives inside the callee, not at the sites).
      Ghidra's 212-byte extent drops the owned three-word literal pool:
      true extent 224 bytes, 0x081473d8..0x081474b8 (vtable 0x08986434 @
      0x081474ac, empty-rep data 0x08b31810 @ 0x081474b0, 'SCST'
      0x53435354 @ 0x081474b4), where the next function opens
      push {r4, lr}. The constructor of the Silver framework's
      resource-backed item table whose accessor is silver_list_table_item
      @ 0x081473c0 (the immediately preceding function): plants the
      vtable and resource_id, clears +0x08 and the whole embedded
      red-black map at +0x0c (pool words, header word, flag byte +0x24,
      comparator byte +0x25), pool-allocates the map's header node and
      links it as the empty sentinel (+4 parent = 0, +8 left = self, +12
      right = self - the node layout FUN_083c8aa8 descends), stores 2 at
      +0x2c, parks the +0x28 COW string on the shared empty rep, and only
      when populate is nonzero (cmp r7,#0 / blne - any nonzero value)
      runs the "SLst" populator FUN_081472b0. It then resolves ('SCST',
      resource_id) through the registry getter FUN_0819fdb0 and the
      tagged resolver FUN_0811ca58 - RE-READING resource_id from +0x04
      after the populator ran (ldr r2,[r5,#4] @ 0x08147460), so a
      populator that rewrites the id changes the resolution - and a
      failed lookup is fatal (bleq heap_panic @ 0x08030f44). On success
      the borrowed (byte_length, bytes) record becomes a temporary COW
      string (cxx_string_from_buffer), is assigned to the +0x28 name
      (cxx_string_assign) and the temporary released (cxx_string_release);
      returns this. The +0x28 field was mis-described as a vtable'd
      handle in the accessor's notes: it is an ADS COW basic_string word,
      proven by the assign/release pair and the 0x08b31810 empty-rep
      literal. The three string callees are ported and called directly;
      the map node allocator, populator and registry getter are unported
      and resolve/fail ride the seam beside them anyway (the same
      call-boundary decision app/event_list makes for this exact
      dependency triple), so the port is hook-ready on device without
      wiring APP_STRING_RESOLVE_OPS - the firmware defaults transmute
      0x083c8404 / 0x081472b0 / 0x0819fdb0 / 0x0811ca58 / 0x08030f44.
      Deviations: the empty name parks on the crate's own shared empty
      rep (cxx/string.rs's standing deviation from the firmware word
      0x08b31810); the string temporary is zero-initialized where the
      original passes its saved-resource_id stack word (from_buffer never
      reads the old slot value - unobservable); a swapped-in fail hook
      that returns stops construction at the failed resolution (retailOS
      never returns). Host tests: full word-by-word initialization and
      sentinel self-links, call order alloc -> populate -> registry ->
      resolve, populate=0 skips the load but still resolves, any-nonzero
      populate truthiness, the post-populate resource_id reload, and the
      fatal path (fail fires immediately after the failed resolve, no
      string rep allocated, name stays on the empty rep). match.py
      structural (exit 1, diff expected): same store sequence at
      +0x0c..+0x2c, header self-link triple, conditional populate,
      resource_id reload, 'SCST' literal resolve and NULL gate; the diff
      is the ops-table blx indirection, the crate fp frame, and LLVM
      inlining the from_buffer capacity preamble.

  - address: 0x081481ec
    ghidra: FUN_081481ec
    name: string_record_range
    size: 208
    sig: "void string_record_range(u32 *out, u8 *record, u32 track)"
    module: cxx/string_record
    status: ported
    renames:
      param_1: out
      param_2: record
      param_3: track
      DAT_081482bc: ACTIVE_CHARACTER_SET_WORD (literal 0x089d00b4)
      FUN_08147f7c: materialize (STRING_RECORD_OPS slot; sole call site in
        the image - lazily decodes the record's string into the u16 range
        words at +0x54/+0x58 behind the done byte at +0x64)
      FUN_080f020c: string_from_range (STRING_RECORD_OPS slot; UTF-16 range
        to UTF-8 StringObject converter with quote stripping and backslash
        unescaping)
      FUN_082770e0: utf8_codepoint_count_safe (direct, ported)
      FUN_082a50b0: string_object_c_str (direct, ported)
      FUN_08276214: utf8_next_codepoint (direct, ported)
      FUN_082a4ef8: bit_set_test (STRING_RECORD_OPS slot; the BitSet test
        with PRE-SPLIT word/bit index - now ported in cxx/bit_set, and the
        slot defaults to the port on both target and host)
      FUN_08277484: string_object_destroy (direct, ported)
      FUN_081bb6a4: range copy tail (inlined as two word stores - the
        shared 8-byte copy *out = *src, out[1] = src[1], decoded from its
        own bytes)
    notes: >-
      50 bl + 0 b + 0 predicated call sites, binary-verified by decoding
      every B/BL word in osos.dec. Ghidra's 208-byte extent is exact for
      code: 0x081481ec..0x081482bc, plus the owned 4-byte literal pool
      word 0x089d00b4 @ 0x081482bc; the next function opens push
      {r4, r5, r6, lr} @ 0x081482c0. The string-range accessor of a
      record object whose UTF-16 string is materialized lazily at
      +0x54/+0x58 (begin/end u16 pointers). Besides copying that range
      to out, it is one of the two feeders of the system-wide active
      character set: the global word @ 0x089d00b4 holds a BitSet* (the
      16-byte set of cxx/bit_set.rs) installed by the text-system object
      constructor @ 0x081b2a38 (str r0,[r1] with r0 = this+0xb4) and
      consumed by the same inlined insert loop @ 0x08094034. When track
      is nonzero and the set is installed, every codepoint of the
      string's UTF-8 transcription is added: test (word = cp >> 5, bit
      = cp & 31), and only a clear bit bumps set->cardinality and ORs
      the mask - the set path of the BitSet write @ 0x082746f4 with
      value 1, inlined. The loop runs exactly utf8_codepoint_count_safe
      iterations (no early stop on a zero decode, unlike
      bit_set_insert_utf8) and RELOADS the global word every iteration
      (ldr r4,[sl] in the loop), so a set swapped mid-walk receives the
      remaining codepoints. Sampled site clusters 0x08135e50..0x081360bc,
      0x081dc718..0x081dca64, 0x0820fbf8..0x0820fce4 all pass r2 = 1 and
      forward the range to a sibling consumer. Deviations - the
      0x081bb6a4 tail copy is inlined; materialize/string_from_range ride
      seams (target transmutes the retail addresses, host defaults
      panic); the 0x089d00b4 global is a modeled static on host.
      match.py structural (exit 1, diff expected): same predicated
      ldrne/cmpne guard, same global reload inside the loop, same
      inlined cardinality++/OR block at [r4,#4] / [r1,r5,lsl#2], same
      0x089d00b4 literal, same +0x54/+0x58 range copy; the diff is the
      ops-table blx indirection, the crate fp frame, and a count-down
      loop counter. Host tests: track==0 and NULL-set skips, duplicate
      codepoint not recounted, multibyte U+00E9 marking, preset bit
      untouched, mid-walk set swap (the reload), empty string; fixtures
      in testing::hints::STRING_RECORD (new).

  - address: 0x081346e4
    ghidra: FUN_081346e4
    name: template_binding_name_or_default
    size: 48
    sig: "const char *template_binding_name_or_default(void *this)"
    module: app/template_binding
    status: ported
    vars: {param_1: this}
    notes: >-
      114 bl + 114 tail b call sites (every B/BL word in osos.dec decoded
      and its target computed). 48 bytes of code plus the two-word
      literal pool it owns at 0x08134714..0x0813471b (56 total); the
      next function, a lone `bx lr`, starts at 0x0813471c. Reads the
      embedded two-word StringObject at this+0x28 through the ported
      string_object_c_str @ 0x082a50b0 and returns that C string, except
      that when it equals a ROM sentinel the function returns a ROM
      default instead - `bl 0x082a50b0; ldr r1,[0x08134714]; bl strcmp;
      cmp r0,#0; addne r0,r4,#0x28; popne; bne 0x082a50b0; ldreq
      r0,[0x08134718]`. Both callees (string_object_c_str, strcmp @
      0x08391e38) are ported and called DIRECTLY - no dispatch seam -
      and the port keeps the original's two separate c_str calls, one
      per branch. It is the base of a 114-member override family: every
      bl site is followed by the identical 44-byte wrapper `bl
      0x081346e4 / adr r1,"CntrlHistoryFn" / bl strcmp / cmp r0,#0 /
      movne r0,this / popne / bne 0x081346e4 / ldreq r0,[pool] / pop`,
      each derived class substituting its own ROM string when the
      inherited name is the framework's "CntrlHistoryFn" marker (158
      copies of that literal in the image; the app-layer literal pools
      carry it in per-translation-unit triples with "TSilverCntlr" and
      the unit's controller names - "TCNotesDispatcher", "TCClock",
      "TCVoiceMemos", "TDiskModeCntlr" ... @ 0x083f5cbc onward), which
      places the family in the TSilverCntlr controller framework. 108 of
      those 114 wrappers have no reference anywhere in the image
      (neither data word nor branch), i.e. they are emitted-but-unreached
      overrides. Deviation: the two literal-pool words hold 0x083e267c
      and 0x083e266c, which are CODE addresses - this toolchain resolves
      short string literals to any matching byte run in the image, the
      same phenomenon string_object_c_str's 0x083e2e3a empty string
      documents - so the port models their CONTENT (sentinel = the two
      bytes 0x12 0x00, default = the empty string) and keeps the ROM
      addresses as NAME_SENTINEL_CSTR_ADDRESS /
      NAME_DEFAULT_CSTR_ADDRESS. The class behind this is inferred, not
      proven, so the object stays opaque bytes and the port claims only
      the offset the instruction spells out. Host tests: an ordinary
      name returns the payload pointer itself, a name equal to the
      sentinel returns the modeled default, a name that merely STARTS
      with the sentinel byte survives (strcmp, not memcmp), the empty
      name survives, and a NULL payload yields string_object_c_str's
      shared empty string rather than the default. match.py structural:
      same add-this+0x28 / c_str / compare / tail-call-c_str vs
      load-constant split; the diff is LLVM inlining the ported strcmp
      byte loop and the crate's fp frame.

  - address: 0x081346c8
    ghidra: FUN_081346c8
    name: template_binding_map_assign
    size: 28
    sig: "u32 template_binding_map_assign(void *this, const void *source)"
    module: app/template_binding
    status: ported
    vars: {param_1: this, param_2: source}
    notes: >-
      116 bl call sites and no tail b (binary-scanned over osos.dec).
      28 bytes, no literal pool - it ends at 0x081346e0 and
      0x081346e4 begins immediately, so the two are back-to-back members
      of the same ROM block. Copy-assigns the container member at
      this+0x94 from `source` (`bl 0x083c2130`, a red-black-tree
      operator= that self-assignment-guards on dst==src and keeps its
      header word at member+0x10) and then TAIL-BRANCHES to 0x08134938
      with `this`, returning its verdict; the assignment's own return is
      discarded by the original's `mov r0,r4`. 0x08134938 is the
      validation walk over that same container - it opens with `ldr
      r0,[this+0xa4]`, which IS member+0x10 - iterating the tree
      (the classic [+0x4]/[+0x8]/[+0xc] rb-tree increment at
      0x081349ac), calling 0x08134ad4(this, name) per node value and
      0x08134c70(this, name) per 0x10-byte record in the node's
      sub-array, ANDing the results into r6 and returning it. 0x08134c70
      is a name whitelist: empty name passes, otherwise it strcmps
      against a NULL-terminated table (pool word @ 0x08134cc0), so the
      member is a NAME-KEYED BINDING MAP and the return value means
      "every bound name is known". 0x08134ad4 additionally dispatches
      vtable slot +0x114 (with the literal 0xdeadbeef in r2). The 116
      call sites are a run of near-identical ~0x29c-byte functions in
      0x0839f88c..0x083b44e4, a code-only region of UI view classes.
      Deviation: neither callee is ported, so both go through the
      TEMPLATE_BINDING_OPS read_volatile seam (house pattern - see
      cxx/string_object's STRING_OBJECT_ASSIGN_CSTR_OPS); the default
      assign returns its destination unchanged and the default validate
      returns 0, the original's "some bound name did not validate"
      answer. The class behind this is inferred, so the object stays
      opaque bytes addressed by the one offset the instruction spells
      out. Host tests: the assignment is handed exactly
      this+BINDING_MAP_OFFSET and the source pointer, validation is
      handed the unadjusted this and runs strictly after the
      assignment, its verdict is returned verbatim for both 1 and 0, and
      the wired default reports an unvalidated object. match.py
      structural: same mov r4,r0 / add r0,r0,#0x94 / call / mov r0,r4 /
      pop / tail-branch shape; the diff is the ops-table indirection and
      the crate's fp frame.
  # --- view event source: lazily-built event list (matched pair) ---
  - address: 0x081e0280
    ghidra: FUN_081e0280
    name: event_list_populate_from_registry
    size: 548
    sig: "void event_list_populate_from_registry(void *source)"
    module: app/event_list
    status: ported
    vars: {param_1: source, uVar1: registry, piVar4: declaration, iVar2: resolved_bytes, uVar3: string}
    notes: >-
      Clears source+0x38's transient red-black event tree, then walks the
      pointer vector [source+0x10,source+0x14), resolving each declaration's
      +0x08 value under native-endian 'TEVT' (0x54455654). It then resolves
      source+0x08 under 'SEVT' (0x53455654) and, if source+0x58 is non-NULL,
      that object's +0x04 value under 'CEVT' (0x43455654). The registry getter
      is 0x0819fdb0 and the tagged resolver is 0x0811ca58: successful results
      are {byte_length, bytes}, copied into a temporary owned descriptor before
      it is appended to the event tree. A failed resolution calls 0x08030f44,
      which does not return; no later declaration is considered. The port keeps
      the registry, resolver, descriptor insertion, and fatal boundaries
      explicit in EVENT_LIST_BUILD_OPS; firmware defaults call their original
      addresses while host tests cover empty, multiple, and failure traversal.
      match.py structural: the same clear-first sequence, pointer-vector walk,
      TEVT/SEVT/CEVT lookup order, and immediate failed-lookup branch; the
      Rust port compacts the repeated descriptor construction sequence behind
      its insertion boundary.

  - address: 0x081e04b0
    ghidra: FUN_081e04b0
    name: event_list_acquire
    size: 44
    sig: "void *event_list_acquire(void *source)"
    module: app/event_list
    status: ported
    vars: {param_1: source}
    notes: >-
      125 bl call sites (binary-scanned across osos.dec by decoding every
      B/BL word; no plain-`b` tail calls), exactly the count of its paired
      release half 0x081e054c, and every site invokes the two as brackets.
      11 instructions: push {r4,lr}; mov r4,r0; ldrb r0,[r0,#0x54]; cmp
      r0,#0; bne +3; mov r0,r4; bl 0x081e0280; mov r0,#1; strb
      r0,[r4,#0x54]; add r0,r4,#0x38; pop {r4,pc}. The source object embeds
      an ADS C++ std::list at +0x38 (28 bytes) and a "list is built" flag
      byte at +0x54; the accessor builds the list on first use through
      0x081e0280, raises the flag, and returns the list either way. The
      flag test is `cmp #0`, so any non-zero byte counts as built. The
      canonical call site @ 0x0839f838 shows the whole protocol: source =
      0x081473c0(view+0xbc, key); list = 0x081e04b0(source); 0x081346c8(view,
      list) - whose callee 0x083c2130 is list::operator= - then
      0x081e054c(source). So acquire / copy-out / release, not a lock.
      The build @ 0x081e0280 walks the source's collection at +0x10 and
      resolves each element through the registry from 0x0819fdb0 keyed by
      the ADS multi-character literals in its pool - 'TEVT' (0x54455654 @
      0x081e04a4) for the collection elements, 'CEVT' (0x43455654 @
      0x081e04ac) for the object at the source's +0x58 - appending each
      resolved 12-byte descriptor to the very list at +0x38 this returns;
      hence "event list". Deviation - 0x081e0280 is unported and has no
      names.yaml entry, so it goes through the EVENT_LIST_OPS.build
      read_volatile dispatch seam (house pattern, cxx/string_object.rs's
      STRING_OBJECT_ASSIGN_CSTR_OPS); its wired default is a no-op, which
      still exercises the once-only flag protocol. The list stays opaque
      (*mut u8), as the original leaves it to list::operator=. `source` is
      dereferenced unchecked, as in the original. match.py structural: same
      flag load / zero test / build call / flag store / add r0,r4,#0x38 /
      return; the diff is LLVM inverting the branch into an early-return
      block and the crate's fp frame.

  - address: 0x081e054c
    ghidra: FUN_081e054c
    name: event_list_release
    size: 80
    sig: "void event_list_release(void *source)"
    module: app/event_list
    status: ported
    vars: {param_1: source}
    notes: >-
      125 bl call sites (binary-scanned, same method), identical to its
      paired acquire half 0x081e04b0 and paired with it at every site. 20
      instructions. Empties the source's event list with erase(begin(),
      end()) and clears the built flag at +0x54, so the next acquire
      rebuilds against fresh registry state. end() is the list's circular
      sentinel node at list+0x10 - the same word the source reaches as
      +0x48, which is what pins the embedded list to +0x38 - and begin() is
      that node's next pointer at +0x8. Both are read before the erase and
      passed to it by address (r2 = &begin, r3 = &end) with an sret slot in
      r0; the returned iterator is discarded. The callee 0x083c1c3c is
      std::list::erase(first, last): it stores list->sentinel through the
      sret slot, then compares *first against sentinel->next and *last
      against the sentinel via the iterator operator== @ 0x083cf7d0 before
      unlinking. It is identified by 0x083c2130 (list::operator=), whose
      own first act is byte-for-byte this same marshalling on the
      destination list - i.e. this half is a plain clear(). Nothing gates
      the erase, so releasing a never-acquired source still runs it over an
      already-empty (self-linked sentinel) list and stores the already-clear
      flag again; preserved. Deviations - 0x083c1c3c is unported and
      unnamed, so it goes through the EVENT_LIST_OPS.erase_range
      read_volatile dispatch seam; the original's 24-byte frame holds TWO
      copies of each iterator (sp+4/sp+16 = end(), sp+8/sp+20 = begin(),
      ADS materializing the temporaries twice) where the port keeps one
      slot each, and the original loads the sentinel word twice (ldr
      r0,[r0,#0x48] then ldr r0,[r1,#0x10]) where the port loads it once -
      same address, no intervening store. The list/node fields are 32-bit
      target pointers read with aligned u32 loads, so the host tests need a
      sub-4-GiB fixture (crate::testing::try_map_u32_slab, hint
      0x0d000000) and skip on arm64 macOS. match.py structural: 21
      instructions vs 20, same ldr r0,[r0,#0x48] / ldr r1,[r0,#8] / two
      stack iterator slots / add r1,r4,#0x38 / erase call / strb 0 to
      +0x54; the diff is the seam's literal-pool indirect call and the
      crate's fp frame.
  - address: 0x083c1c3c
    ghidra: FUN_083c1c3c
    name: event_list_tree_erase_range
    size: 224
    sig: "u32 *event_list_tree_erase_range(u32 *out, void *tree, u32 *first, u32 *last)"
    module: app/event_list
    status: ported
    vars: {param_1: out, param_2: tree, param_3: first, param_4: last, iVar1: first_is_begin, iVar2: last_is_end, piVar3: current_node, local_24: successor}
    notes: >-
      libstdc++ _Rb_tree::erase(first,last) specialization behind the event
      source's historical "event list" name, not std::list::erase. The header
      pointer is tree+0x10; root/leftmost/rightmost are header+4/+8/+0xc and
      count is tree+0x14. Writes header through the sret out pointer before
      every path and returns out. Whole nonempty ranges (first == leftmost,
      last == header) call subtree destruction @ 0x083c1f10 on root, then
      restore root=0, leftmost=rightmost=header, and count=0. Other ranges
      save each node, advance its iterator @ 0x083b5bb0 before invalidating
      it, and call single-node erase @ 0x083c17d8, forwarding its successor
      to out. Those three lower runtime calls remain EVENT_LIST_OPS seams;
      their allocator/value cleanup belongs to 0x083c1648. Host tests cover
      empty, partial single, partial multiple, and whole-tree releases plus
      successor, destruction ordering, and header bookkeeping. match.py:
      original 56 instructions vs Rust 55; same initial header sret,
      full-range predicate/destruction/reset, and advance-then-erase loop.

  - address: 0x083c2130
    ghidra: FUN_083c2130
    name: event_list_tree_assign
    size: 164
    sig: "void *event_list_tree_assign(void *destination, const void *source)"
    module: app/event_list
    status: ported
    renames:
      param_1/r0/r4: destination
      param_2/r1/r5: source
      param_1_plus_0x10: destination_header
      param_2_plus_0x10: source_header
      iVar1/r0: copied_root
    notes: >-
      Binary-verified extent is 0x083c2130 through 0x083c21d0; the next
      function begins at 0x083c21d4. Every ARM B/BL word in osos.dec yields
      exactly 11 incoming calls, all unconditional bl instructions (no
      predicated call sites). This libstdc++ red-black tree operator= returns
      destination unchanged for self-assignment; otherwise it erases
      destination's complete [begin,end) range through the ported
      event_list_tree_erase_range, clones source_header->root beneath the
      destination header, resets root/leftmost/rightmost as appropriate, and
      copies source's +0x14 node count. The recursive clone at 0x083c1e9c
      is still unported: ARM builds call its retailOS entry directly, while
      host tests use EVENT_LIST_OPS.copy_subtree as a volatile seam receiving
      destination, source root, and destination header. The original calls
      0x083b6a80 and 0x083b6a6c to find leftmost and rightmost; their unchecked
      loops are inlined in the
      port. Host tests cover self-assignment, nonempty replacement and
      leftmost/rightmost reconstruction, and an empty source. match.py
      structural review is expected to differ at the retail clone call and
      the Rust frame.
  - address: 0x082769d4
    ghidra: FUN_082769d4
    name: string_object_format
    size: 68
    sig: "int string_object_format(StringObject *this, const char *format, ...)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: format, in_stack_ffffffe8: varargs, iVar1: length, auStack_200: scratch}
    notes: >-
      Decoded from raw bytes (osos.dec 0x082769d4..0x08276a14, 68 bytes
      all code - no literal-pool word; next function starts at
      0x08276a18). 117 bl call sites, binary-scanned. The polymorphic
      string class's printf-style assignment, StringObject::format(const
      char *fmt, ...). The ADS variadic prologue `push {r0,r1,r2,r3}`
      spills the four register arguments, `sub sp,sp,#516` reserves a
      512-byte scratch at sp+4 (plus a 4-byte alignment pad), then
      `ldr r2,[sp,#532]` re-reads the spilled format, `add r3,sp,#536`
      points at the spilled third argument word - the va_list - and
      `bl 0x08074ba0` runs the bounded formatter as
      vsnprintf(scratch, 512, format, va). Its count is parked in r4
      while `bl 0x08276474` hands the scratch to the already-ported
      string_object_assign_payload (which sizes the copy with
      strlen_safe_plus1, requests storage from vtable slot +0x8, or
      dispatches slot +0xc when the formatted text came out empty), and
      `mov r0,r4` returns the FORMATTER's character count - not this and
      not the stored length. The epilogue `ldr pc,[sp],#20` drops the
      variadic spill. No NULL guard on this (the assignment dereferences
      it for the vtable). Call sites pin the shape: 0x08176a4c passes
      "%s, %s" with two string arguments, 0x0816ff3c passes "%d".
      Deviations: (a) the `...` becomes an explicit VaList (*const u32)
      parameter - stable Rust cannot define C-variadic functions, and
      args IS the pointer the original's spill builds (house convention,
      printf/printf_api.rs); (b) the bounded formatter @ 0x08074ba0 is
      NOT ported (it is a second vsnprintf, distinct from 0x08032f94:
      its literal-pool word @ 0x08074bdc holds the sink descriptor
      0x0807ca58 and it drives the conversion core @ 0x08077c94), so it
      goes through the STRING_OBJECT_FORMAT_VSNPRINTF dispatch slot
      whose wired default is the ported vsnprintf @ 0x08032f94 - same
      (buf, size, format, args) contract and NUL termination, differing
      only in the conversion engine; (c) the scratch is MaybeUninit,
      matching the original's unwritten stack frame. Host tests: the
      formatter receives size 512 with format and va_list verbatim, the
      formatted text sizes the +0x8 allocation (strlen + NUL) and is
      copied into it, empty output dispatches only +0xc, allocation
      failure still returns the count and never falls back to +0xc, the
      formatter result is returned verbatim including a would-be length
      past the scratch, and the slot's wired default is the ported
      vsnprintf. match.py structural: same 512-byte scratch reservation,
      argument shuffle into r1/r2/r3, formatter call, count parked in a
      callee-saved register, assign_payload(this, scratch) and
      count-in-r0 return; the diff is the crate's fp frame, the absent
      variadic spill (the explicit VaList) and the blx-through-volatile-
      slot dispatch standing in for the direct bl 0x08074ba0.

  - address: 0x08276a18
    ghidra: FUN_08276a18
    name: string_object_insert_cstr
    size: 68
    sig: "void string_object_insert_cstr(StringObject *this, int index, const char *source)"
    module: cxx/string_object
    status: ported
    renames:
      FUN_08275e20: strlen_safe_plus1
      param_1: this
      param_2: index
      param_3: source
    notes: >-
      The guard wrapper of the StringObject insert operation, `void
      StringObject::insert(int index, const char *source)`. 68 bytes,
      all code (0x08276a18..0x08276a5c; no literal-pool word; the next
      function is the insert-buffer sibling @ 0x08276a5c). 61 bl call
      sites, binary-scanned. Ghidra's decompile
      (decomp/c/026/08276a18_FUN_08276a18.c) INLINES the tail-branch
      target 0x08275f48 into the body - the vtable grow, codepoint-walk
      position resolve (0x082a50c4 over utf8_next_codepoint @
      0x08276214), memmove/memcpy splice and final NUL all belong to
      that callee, NOT to this function. The raw 17 instructions:
      `movs r4,r2 / ldrbne r0,[r4] / cmpne r0,#0 / popeq` (return when
      source == NULL or source[0] == 0), `cmp r5,#0 / poplt` (return
      when index < 0), `bl 0x08275e20` (strlen_safe_plus1, ported),
      `sub r3,r0,#1`, then tail `b 0x08275f48` with (this, index,
      source, strlen(source)). `index` is a UTF-8 CHARACTER position
      (the callee walks codepoints to resolve it; a past-the-end index
      appends) while the length argument is a plain BYTE count. Sampled
      call sites pass `mvn r1,#0x80000000` (0x7fffffff, INT_MAX) as the
      append-at-end idiom (0x08053a34, 0x08074258). The insertion body
      now calls string_object_insert_bytes directly; its preserving virtual
      allocator remains the existing STRING_OBJECT_ASSIGN_CSTR_OPS boundary.
      The byte-length helper retains its volatile function-pointer call.
      Host tests exercise actual insertion, including multibyte character
      indices, byte-counted source lengths, allocation failure and guards.
      Codegen retains the guard set, length call, source length minus one
      and direct branch to the ported body.


  - address: 0x082770e0
    ghidra: FUN_082770e0
    name: utf8_codepoint_count_safe
    size: 48
    sig: "size_t utf8_codepoint_count_safe(const char *text)"
    module: cxx/string_object
    status: ported
    vars: {param_1: text, local_c: cursor, iVar1: count, uVar2: codepoint}
    notes: >-
      Decoded from raw bytes (osos.dec 0x082770e0..0x0827710c, 48 bytes
      all code - no literal-pool word; next function starts at
      0x08277110). 102 bl call sites, binary-scanned. The UTF-8
      counterpart of the NULL-guarded byte strlen_safe @ 0x082770bc that
      sits immediately before it in the image: this one counts
      CODEPOINTS. `push {r0,r4,lr}` spills the argument and that spill
      slot IS the cursor cell - `ldr r0,[sp]; cmp r0,#0; beq` returns 0
      for NULL without decoding, then the loop is `mov r0,sp;
      bl 0x08276214; cmp r0,#0; addne r4,r4,#1; bne` - the already-ported
      utf8_next_codepoint advances the spilled cursor and the counter is
      bumped for every nonzero codepoint. Termination is whatever the
      decoder calls zero: the NUL byte, but also any four-byte or
      otherwise malformed lead (those consume three bytes and stop the
      count), the same terminator semantics utf8_strcmp_safe @ 0x08276d64
      relies on. Continuation bytes, overlong forms and surrogates are
      not validated. The 48-byte sibling at 0x08277110 is the bounded
      variant (same loop with an `s + len` end pointer, 4 bl call sites)
      and is NOT ported here. Deviations: none - utf8_next_codepoint is
      ported and called directly, and the NULL guard runs once before the
      loop as in the original; a non-NULL unreadable string faults in
      both. Host tests: NULL and empty are 0, pure ASCII agrees with
      strlen_safe, a two-byte + three-byte + ASCII string is 3 codepoints
      over 6 bytes, a four-byte lead and a lone continuation byte each
      end the count early, and a malformed two-byte sequence still counts
      as one. match.py structural: same spill-as-cursor cell, NULL guard,
      decoder call with &cursor, nonzero-codepoint count and count-in-r0
      return; the diff is the crate's fp frame and LLVM rotating the loop
      (counter seeded at -1 with mvn and incremented at the loop head
      instead of the original's predicated addne).
  # --- IRAM veneers for the ADS block-memory routines ---
  # 0x22000000 is S5L8702 *internal SRAM*, not mask ROM as the header of
  # kernel/thunks.rs says (mask ROM is at 0x20000000). The relocator @
  # 0x080046e0 copies a 0xaed8-byte blob to 0x22000000, zero-fills the IRAM
  # BSS at 0x2200aed8 for 0x589c bytes and jumps to 0x22000000 — so the
  # mirrored code region is exactly 0x22000000..0x2200aed8, matching osos
  # 0x08000000..0x0800aed8. Binary confirmation of that extent: of 190027
  # decoded B/BL words in osos.dec, 1467 stay wholly below 0x0800aed8 and
  # 188560 wholly above it; the 7 apparent crossings are literal-pool words
  # that happen to decode as branches. The low block is reached from the rest
  # of the image only through the veneer table.

  - address: 0x08037df8
    ghidra: thunk_EXT_FUN_22000188
    name: iram_memcpy_veneer
    size: 8
    sig: "void *iram_memcpy_veneer(void *dst, const void *src, size_t len)"
    module: libc/iram_veneers
    status: ported
    notes: >-
      ldr pc,[pc,#-4] -> 0x22000188 = the IRAM copy of memcpy @ 0x08000188
      (ADS __rt_memcpy's word-aligned fast path). Ghidra reports 4 bytes;
      the stub is 8 (0xe51ff004 + the target word). THE most-called
      veneer-only address in the image: 477 bl (465 unconditional, 12
      predicated) + 11 tail b, all from above 0x0800aed8 — binary-verified
      by decoding every B/BL word, not from Ghidra xrefs. Body recoverable
      and already ported, so this is a real forwarding call, not a stub.
      Deviation: the ARM body returns dst+len; the port returns dst. Safe —
      sampling 24 call sites across the image, the instruction after every
      bl either overwrites r0 or ignores it; none reads it. The port loads
      its callee through read_volatile, otherwise LLVM re-lowers the call to
      __aeabi_memcpy and the veneer stops reaching the port. match.py
      structural: compiles to ldr/ldr/bx through a literal — the original's
      indirect-jump-through-a-literal shape plus one indirection and the
      crate's fp frame, r0-r2 passed through untouched.

  - address: 0x08037f08
    ghidra: thunk_EXT_FUN_22004230
    name: gateway_service19_request
    size: 8
    sig: "u32 gateway_service19_request(u32 input)"
    module: kernel/gateway_service19
    status: ported
    renames:
      thunk_EXT_FUN_22004230: gateway_service19_request
      EXT_FUN_22004230: gateway_service19_request
      DAT_08037f0c: gateway_service19_rom_target
    notes: >-
      Raw osos.dec proves the Ghidra 4-byte function is an 8-byte ADS literal
      veneer: e51ff004 (ldr pc,[pc,#-4]) at 0x08037f08 and target word
      0x22004230 at 0x08037f0c; the sibling veneer opens at 0x08037f10.
      The relocator at 0x080046e0 copies 0xaed8 bytes from 0x08000000 to
      0x22000000, so the target mirrors the 48-byte body at 0x08004230
      (0x08004230..0x08004260). It stores r0 at sp+24, zero at sp+8, and
      selector 19 at sp+4, passes sp+4 to the foreign 0x08003660 gateway
      dispatcher, then returns the dispatcher-written word at sp+8: the
      verified protocol is {19, 0, _, _, _, input}. Decoding every ARM B/BL
      word in osos.dec finds exactly nine direct calls, all unconditional bl
      at 0x080845ac, 0x08084c28, 0x080c9c70, 0x080e184c, 0x082d965c,
      0x08393580, 0x083935b0, 0x08393850, and 0x08393864; no predicated
      forms or tail branches. No data word holds the thunk or mirror address;
      the target literal above is the sole 0x22004230 word. Callers pass
      owned words before freeing them, but selector 19's RTXC operation is
      not otherwise identified, so the port calls the existing
      message_dispatch_veneer seam and names only the established wire
      protocol. Host tests prove store positions, dispatcher-written output,
      and unguarded full-width inputs. match.py is structurally expected to
      differ because Rust calls the dispatcher seam rather than tail-loading
      PC from the veneer literal.
  - address: 0x08037f58
    ghidra: thunk_EXT_FUN_220060e0
    name: lazy_singleton_106dc_acquire
    size: 8
    sig: "void *lazy_singleton_106dc_acquire(void)"
    module: kernel/thunks
    status: ported
    renames: {thunk_EXT_FUN_220060e0: lazy_singleton_106dc_acquire}
    notes: >-
      One stub of the osos -> IRAM thunk table (ROM_THUNKS in
      kernel/thunks.rs): ldr pc,[pc,#-4] + literal word 0x220060e0.
      Ghidra reports 4 bytes; the stub is 8 (next stub starts at
      0x08037f60). Call count binary-verified by decoding every B/BL word
      in osos.dec: 32 bl, all unconditional, zero predicated forms, zero
      tail b - callers never NULL-check the result (the accessor always
      returns the fixed singleton address). Sites cluster at
      0x080a56c8-0x080a61dc (29) and 0x080c8be8-0x080c8ca8 (3), the
      FreeType glyph rendering region; no DATA word holds the thunk
      address, so it is not virtually dispatched. Target identified via
      the relocator mirror (0x080046e0 copies 0xaed8 bytes from
      0x08000000 to 0x22000000, literals verified), so IRAM 0x220060e0 is
      byte-identical to FUN_080060e0 @ 0x080060e0 (88 bytes of code +
      16-byte literal pool; next function opens at 0x08006148): lazy
      accessor for the C++ singleton object at 0x220106dc. Under
      once-guard bit 0 of the word at state+4 (state block 0x22008cdc) it
      runs construct-and-register: glue veneer 0x080036e0 ->
      0x082a0444(state+4); on success ctor 0x08006988(0x220106dc) (plants
      vtable 0x22008a10, zeroes fields, allocates a 0x1000-byte buffer),
      glue veneer 0x080036e8 -> 0x082a02f0(result, dtor 0x22006ab4,
      __dso_handle 0x089ca09c), glue veneer 0x080036f0 ->
      0x082a0460(state+4). A second once-flag (byte at state+0) is then
      set to 1 with NO companion init call - unlike ui_manager_acquire's
      target FUN_08005018, which runs FUN_08005448 under its flag.
      Returns 0x220106dc unchanged. Class identity unrecovered: every
      vtable slot (0x22006ab4/0x22006928/0x220068e0/0x220065dc/
      0x220065a8/0x220060cc/0x220068b8/0x22006180) is unnamed; callers
      read flag bytes at +0x47/+0x94 and an 8-valued mode byte at +0x58
      that selects per-mode pixel-geometry tables. Port keeps the
      verbatim veneer on ARM via global_asm (ldr pc preserves LR, target
      returns straight to the caller); host builds dispatch through the
      volatile LAZY_SINGLETON_106DC_ACQUIRE_OPS seam, default NULL.
      match.py structural: instruction stream identical
      (ldr pc,[pc,#-4]); only diff line is the literal word shown as
      .word vs data-decoded-andcs.

  - address: 0x08037fd8
    ghidra: thunk_EXT_FUN_22006e88
    name: iram_stream_buffer_initializer_veneer
    size: 8
    sig: "void *iram_stream_buffer_initializer_veneer(void)"
    module: kernel/stream_buffer_initializer
    status: ported
    renames: {thunk_EXT_FUN_22006e88: iram_stream_buffer_initializer_veneer}
    notes: >-
      Raw bytes are `ldr pc,[pc,#-4]` (0xe51ff004) plus target literal
      0x22006e88; Ghidra's 4-byte extent omits that literal and the next
      independent veneer begins at 0x08037fe0, so the true extent is 8 bytes.
      The relocator at 0x080046e0 mirrors osos 0x08000000..0x0800aed8 into
      IRAM 0x22000000..0x2200aed8, making the target byte-identical to
      FUN_08006e88 @ 0x08006e88, already ported as initialize_stream_buffer.
      Decoding every ARM B/BL word in osos.dec verified 16 direct bl callers,
      all unconditional, zero predicated; zero tail b and no DATA word stores
      the veneer address, so it is not a virtual dispatch target. The veneer
      transfers the no-argument lazy initializer's fixed stream-buffer result
      directly to its caller. The Rust port volatile-loads and invokes the
      existing initializer instead of duplicating it; this deliberately uses
      a normal call/return rather than the literal tail jump, but the body
      consumes no arguments and exposes only the returned pointer. Host test
      proves the cold veneer call performs both construction and independent
      page initialization through the existing port. match.py structural
      review is expected to differ by the Rust indirect-call frame.
  - address: 0x08037f88
    ghidra: thunk_EXT_FUN_22005018
    name: ui_manager_acquire
    size: 8
    sig: "void *ui_manager_acquire(void)"
    module: kernel/thunks
    status: ported
    renames: {thunk_EXT_FUN_22005018: ui_manager_acquire}
    notes: >-
      One stub of the osos -> IRAM thunk table (ROM_THUNKS in
      kernel/thunks.rs): ldr pc,[pc,#-4] + literal word 0x22005018.
      Ghidra reports 4 bytes; the stub is 8 (next stub starts at
      0x08037f90). Call count binary-verified by decoding every B/BL word
      in osos.dec: 44 bl, all unconditional, zero predicated forms, zero
      tail b — callers rely on the returned pointer with no NULL guard.
      Target identified via the relocator mirror (0x080046e0 copies 0xaed8
      bytes from 0x08000000 to 0x22000000, literals verified), so IRAM
      0x22005018 is byte-identical to FUN_08005018 @ 0x08005018 (100 bytes
      + 16-byte pool): lazy accessor for the shared UI manager object at
      0x220104e8. Under once-guard bit 0 of the word at state+8 (state
      block 0x22008c94) it runs construct-and-register: glue veneer
      0x080036e0 -> 0x082a0444(state+8); on success 0x08005cb8(0x220104e8),
      glue veneer 0x080036e8 -> 0x082a02f0(result, 0x22005cf4,
      __dso_handle 0x089ca09c), glue veneer 0x080036f0 ->
      0x082a0460(state+8). Under a second once-flag (byte at state+1) it
      runs FUN_08005448(0x220104e8) which zeroes the manager fields and
      constructs sub-objects at +0x10/+0x28, then stores 1 to the flag.
      Returns 0x220104e8 unchanged; callers feed it as first argument of
      sibling manager-op thunks (0x22005114, 0x2200509c, 0x2200521c,
      0x22004ee4, 0x2200530c). UI attribution from ported neighbours:
      the 0x2200509c mirror tail-calls ui_dispatch_pending_cleanup
      (0x08005d28) and the init path calls ui_operation_unavailable
      (0x08005f10). Port keeps the verbatim veneer on ARM via global_asm
      (ldr pc preserves LR, target returns straight to the caller); host
      builds dispatch through the volatile UI_MANAGER_ACQUIRE_OPS seam,
      default NULL. match.py structural: instruction stream identical
      (ldr pc,[pc,#-4]); only diff line is the literal word shown as
      .word vs data-decoded-andcs.

  # --- shared UI manager lazy initialization ---
  - address: 0x08005018
    ghidra: FUN_08005018
    name: ui_manager_instance
    size: 100
    sig: "void *ui_manager_instance(void)"
    module: ui/manager
    status: ported
    renames:
      FUN_08005018: ui_manager_instance
      DAT_0800507c: ui_manager_state
      DAT_08005080: ui_manager
      DAT_08005084: dso_handle
      DAT_08005088: ui_manager_destructor
    notes: >-
      Raw bytes verify 100 bytes of instructions from 0x08005018 through
      0x0800507b, followed by this function's four-word literal pool at
      0x0800507c..0x0800508b; the independent veneer at 0x0800508c proves
      the true extent is 116 bytes. Decoding every ARM B/BL word in osos.dec
      found 9 direct bl callers (0x08002fcc, 0x08002fdc, 0x08003040,
      0x080047cc, 0x08005640, 0x0800565c, 0x08005668, 0x080063e4, and
      0x08007268), all unconditional and zero predicated, plus no direct tail
      b. The function tests bit 0 of the state+8 C++ guard at 0x22008c9c;
      if acquired, it calls the unported 0x08005cb8 with fixed manager
      0x220104e8, registers that call's return through cxa_atexit with literal
      destructor 0x22005cf4 and __dso_handle 0x089ca09c, then releases the
      guard. Separately, a zero state+1 byte calls unported 0x08005448 with
      that manager and becomes 1. It always returns 0x220104e8. Rust directly
      uses the existing C++ guard and shutdown-chain ports; the two unported
      calls are volatile replaceable seams (direct retailOS calls on target,
      inert callbacks on host), because their class-level behavior remains
      unported. Host tests cover cold initialization, the bit-0-clear nonzero
      guard, independent initialized flag, registration's constructor-return
      argument, and repeat calls. match.py structural review is expected to
      differ at the callback seams and Rust frame.

  - address: 0x08038060
    ghidra: thunk_EXT_FUN_22007470
    name: iram_event_handler_source_veneer
    size: 8
    sig: "void *iram_event_handler_source_veneer(void)"
    module: kernel/thunks
    status: ported
    renames:
      thunk_EXT_FUN_22007470: iram_event_handler_source_veneer
    notes: >-
      One stub of the osos -> IRAM thunk table (ROM_THUNKS in
      kernel/thunks.rs): ldr pc,[pc,#-4] + literal word 0x22007470.
      Ghidra reports 4 bytes; the stub is 8 (next stub starts at
      0x08038068). Call count binary-verified by decoding every B/BL word
      in osos.dec: 30 bl, all unconditional, zero predicated forms, zero
      tail b. The boot relocator at 0x080046e0 copies 0xaed8 bytes from
      0x08000000 to 0x22000000, so the literal target is the mirror of
      event_handler_source @ 0x08007470. The Rust veneer makes a volatile
      indirect call to that port and returns its source pointer, rather than
      loading PC from the literal; this preserves its no-argument,
      source-pointer ABI while keeping the call on the ported path.

  - address: 0x08037dc8
    ghidra: thunk_EXT_FUN_220002d4
    name: iram_memzero_veneer
    size: 8
    sig: "void *iram_memzero_veneer(void *dst, size_t len)"
    module: libc/iram_veneers
    status: ported
    notes: >-
      ldr pc,[pc,#-4] -> 0x220002d4 = the IRAM copy of memzero @ 0x080002d4
      (zero-fill with the len<4 / misalignment prologue). 106 bl (96
      unconditional, 10 predicated) + 7 tail b, all from above 0x0800aed8,
      binary-verified the same way. Sibling of the memzero_aligned veneer @
      0x08037db8. Same deviations and the same read_volatile callee load as
      iram_memcpy_veneer; match.py structural: ldr/ldr/bx through a literal.

  - address: 0x08165520
    ghidra: FUN_08165520
    name: service_manager_instance
    size: 24
    sig: "void *service_manager_instance(void)"
    module: app/service_manager
    renames:
      DAT_08165534: service_manager_holder (global @ 0x089ca948; instance in its +4 slot)
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Asserting accessor for the service-manager singleton:
      `ldr r0,[0x08165534]; ldr r0,[r0,#4]; cmp r0,#0; bxne lr;
      bl 0x08030f44`. Never returns NULL - a NULL instance falls into
      heap_panic (already ported, called directly). 24 bytes, NOT the 20
      Ghidra reports: the fifth instruction is followed by the holder
      literal at 0x08165534, and the next function starts at 0x08165538
      (`push {r4,lr}`) - verified from raw bytes. 17 direct `bl` sites and
      exactly one plain `b`, which is the veneer 0x081391ec (213 more
      `bl`), so 230 call sites in total; binary-scanned by decoding every
      B/BL word in osos.dec, not from Ghidra xrefs. The object is a 0xE8
      C++ singleton built by the unported lazy getter 0x081655e0
      (operator_new(0xe8) + ctor 0x0816566c); its ctor picks up to
      thirteen subsystem handlers by a per-hardware-model capability mask
      (default 0x1fbf @ 0x0816592c) into the slot table at `this + 4`, and
      callers dispatch to a handler through vtable +0x14. No class name
      survives in the image, so the symbol names the role only.
      Deviations: the holder's +4 slot is the crate static
      SERVICE_MANAGER_INSTANCE, since 0x089ca948 is a runtime-initialized
      RW page (the image holds stale UI strings there, same as the
      app/singletons.rs caches); the fatal path is not exercised on host
      (heap_panic is `-> !`), as in cxx/list_splice.rs. NOT HOOK-READY
      until something ports 0x081655e0 and publishes an instance - today
      every call site would reach heap_panic. match.py structural: same
      global load / `cmp r0,#0` / `bxne lr` / fatal-call shape, the port
      adding only the cold-path frame push.

  - address: 0x081391ec
    ghidra: thunk_FUN_08165520
    name: service_manager_instance_veneer
    size: 4
    sig: "void *service_manager_instance_veneer(void)"
    module: app/service_manager
    renames:
      FUN_08165520: service_manager_instance
    status: ported
    notes: >-
      Long-branch veneer to service_manager_instance @ 0x08165520 - one
      word, `b 0x08165520` (0xea00b0cb). Genuinely 4 bytes, not the 8 of
      the `ldr pc,[pc,#-4]` + target-word form: this is a direct B and the
      following word (0x081391f0, `add r1,r0,#0x18`) is the entry of an
      unrelated function. 213 `bl` call sites, binary-scanned by decoding
      every B/BL word in osos.dec - one of the most-called veneers left,
      and the only `b` that reaches 0x08165520. Ported as its own
      `#[inline(never)]` symbol rather than an alias so a hook here lands
      on a real forwarding branch; the callee is called directly (not
      through read_volatile - unlike the libc/iram_veneers, nothing here
      resembles a compiler builtin, so LLVM cannot re-lower it).
      match.py structural: `push {fp,lr}; mov fp,sp; pop {fp,lr};
      b service_manager_instance` - the original's tail branch plus the
      target's mandatory non-leaf frame pointer.

  - address: 0x08138b84
    ghidra: FUN_08138b84
    name: service_handler_lifecycle_state
    size: 36
    sig: "i32 service_handler_lifecycle_state(void *manager, i32 selector)"
    module: app/service_handler_availability
    renames:
      param_1/r0: manager (ignored)
      param_2/r1: selector
      DAT_08138ba4: service_handler_lifecycle_records (0x08ad0f34; 0x114-byte records)
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Raw extent is 36 bytes, 0x08138b84..0x08138ba8: eight ARM
      instructions plus literal 0x08ad0f34 at 0x08138ba4. Ghidra's
      reported 32 bytes omits that literal; 0x08138ba8 (`sub r2,r1,#1`)
      begins the distinct next function. The signed selector comparison
      admits negative selectors and terminates selectors >=3 via heap_panic;
      the normal path ignores r0 and returns the sign-extended state byte at
      table + selector*0x114. Decoding every ARM B/BL word in osos.dec finds
      12 direct unconditional bl call sites, no predicated bl and no tail
      branches. The port shares service_handler_state_is_ready's five-record
      host fixture centred on selector zero, so tests safely exercise selector
      -1; firmware reads 0x08ad0f34 directly. match.py structural review:
      LLVM uses `cmp r1,#2` plus a conditional normal return and out-of-line
      heap_panic path, equivalent to the retail `cmp r1,#3; blge heap_panic`
      path.

  - address: 0x08138c30
    ghidra: FUN_08138c30
    name: service_handler_lifecycle_select_default_descriptor
    size: 80
    sig: "u32 service_handler_lifecycle_select_default_descriptor(void *manager, i32 selector)"
    module: app/service_handler_availability
    renames:
      param_1/r0: manager
      param_2/r1: selector
      DAT_08138c7c: service_handler_lifecycle_records (0x08ad0f34; 0x114-byte records)
      FUN_08138bd0: registry_find_for_slot
    status: ported
    notes: >-
      Raw extent is 80 bytes, 0x08138c30..0x08138c80: nineteen ARM
      instructions plus literal 0x08ad0f34 at 0x08138c7c. Ghidra's reported
      76 bytes omits that literal; 0x08138c80 begins the distinct next
      function. Selector 0 and signed selectors >=3 return 9 without writes.
      Every other signed selector calls registry_find_for_slot(manager,
      selector, 0), stores its record pointer at lifecycle +4, clears +8 and
      +0xc, and returns 0. Negative selectors are deliberately admitted:
      their ARM register shift produces no slot bit, so the lookup returns
      NULL but the pre-table lifecycle record is still cleared. Decoding every
      ARM B/BL word in osos.dec finds 9 direct unconditional bl call sites,
      no predicated bl, and no tail branches. The port shares the adjacent
      five-record host fixture centered on selector zero; its descriptor field
      remains u32 so host pointers truncate to the target representation.

  - address: 0x08138d8c
    ghidra: FUN_08138d8c
    name: service_handler_state_is_ready
    size: 52
    sig: "u32 service_handler_state_is_ready(void *manager, i32 selector)"
    module: app/service_handler_availability
    renames:
      param_1/r0: manager (ignored on the normal path)
      param_2/r1: selector
      DAT_08138dbc: service_handler_lifecycle_records (0x08ad0f34; 0x114-byte records)
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Raw extent is 52 bytes, 0x08138d8c..0x08138dc0: twelve ARM
      instructions plus literal 0x08ad0f34 at 0x08138dbc. Ghidra's
      reported 48 bytes omits that literal; 0x08138dc0 (`sub r2,r1,#1`)
      begins the distinct next function. The signed selector comparison
      admits negative selectors and terminates selectors >=3 via heap_panic;
      the normal path ignores r0, reads the signed byte at
      table + selector*0x114, then returns the unsigned test
      `(state - 4) <= 2`, so only states 4..=6 are ready. Decoding every ARM
      B/BL word in osos.dec finds 12 direct unconditional bl call sites, no
      predicated bl and no tail branches. Host builds use a five-record
      fixture centred on selector zero so tests can safely exercise selector
      -1; firmware reads 0x08ad0f34 directly.

  - address: 0x0818e624
    ghidra: FUN_0818e624
    name: service_handler_is_available
    size: 100
    sig: "u32 service_handler_is_available(u32 selector)"
    module: app/service_handler_availability
    renames:
      param_1/r0: selector
      thunk_FUN_08165520: service_manager_instance_veneer
      DAT_0818e684: service_handler_availability_globals (0x089ca8d0; +0x30 is the gate)
      FUN_08194080: service_handler_at
      FUN_08138d8c: service_handler_state_is_ready
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Raw extent is 100 bytes, 0x0818e624..0x0818e688: 24 ARM instructions
      plus literal 0x089ca8d0 at 0x0818e684. Ghidra's reported 96 omits the
      literal; 0x0818e688 (`ldr r0,[pc,#4]`) starts the distinct next
      function. Decoding every ARM B/BL in osos.dec finds exactly 32 direct
      unconditional bl callers and no predicated calls. The predicate obtains
      the asserting service-manager singleton, requires the +0x30 word in
      the otherwise unidentified global 0x089ca8d0 and selector 0..2, then
      calls service_handler_at(manager+4, selector) and
      service_handler_state_is_ready(manager, selector). It reloads the
      global gate after both calls and returns one only if that reload, the
      handler word, and the state predicate are all nonzero. A zero gate or
      selector >=3 falls through to heap_panic. The two unported callees use
      a volatile host dispatch seam; target defaults call their verified stock
      addresses 0x08194080 and 0x08138d8c. Host tests cover selectors 0 and
      2, the absent-handler result while still calling the state predicate,
      and the observable final gate reload.

  - address: 0x081d71c0
    ghidra: FUN_081d71c0
    name: iap_incoming_process_thread_instance
    size: 24
    sig: "void *iap_incoming_process_thread_instance(void)"
    module: app/iap_incoming_process_thread
    status: ported
    vars:
      DAT_081d71d4: iap_incoming_process_thread_holder (global @
        0x089cca0c; the instance is its +4 slot)
      FUN_08030f44: heap_panic (direct, ported in heap/veneers)
    notes: >-
      Asserting accessor for the CIapIncomingProcessThread singleton -
      the iAP (iPod Accessory Protocol) incoming-packet worker.
      `ldr r0,[0x081d71d4]; ldr r0,[r0,#4]; cmp r0,#0; bxne lr;
      bl 0x08030f44`. Never returns NULL - a NULL instance falls into
      heap_panic (already ported, called directly). Structurally the
      twin of service_manager_instance @ 0x08165520.
      EXTENT - 24 bytes, 0x081d71c0..0x081d71d8, NOT the 20 an
      instruction-only scan reports: the fifth instruction is followed
      by the holder literal 0x089cca0c at 0x081d71d4, which the first
      instruction reaches with `ldr r0,[pc,#12]`, and the next function
      opens at 0x081d71d8 with `push {r4,r5,r6,lr}` - read off the raw
      bytes, not Ghidra.
      8 direct `bl` sites and exactly one plain `b`, which is the
      veneer 0x08139210 (65 more `bl`), so 73 call sites in total;
      binary-scanned by decoding every B/BL word in osos.dec and
      resolving its target, not from Ghidra xrefs.
      CLASS NAMED - unlike its service_manager twin, the class name
      survives: the lazy creator @ 0x081d68f0 forms a string address
      with `add r9,pc,#388` @ 0x081d6914, which resolves to 0x081d6aa0,
      and the bytes there are "CIapIncomingProcessThread". That creator
      measures the literal, builds a std::string from it,
      operator_new(0x44)s the object (`mov r0,#0x44` @ 0x081d6970),
      initializes the sub-object at +0x0c and starts it with
      FUN_081d7240(object, argument); a non-zero result is failure - the
      object is destroyed through vtable slot +0x08 and NULL is
      published, so the holder slot legitimately stays NULL when
      accessory processing never came up. Only three words in the image
      name 0x089cca0c (the pool entries 0x081d6a9c, 0x081d6db0,
      0x081d71d4), all in this one compilation unit, so the holder is
      private to it.
      Deviations - the holder's +4 slot is the crate static
      IAP_INCOMING_PROCESS_THREAD_INSTANCE, since 0x089cca0c is a
      runtime-initialized RW page (the image holds stale UI data there,
      the app/singletons.rs 0x089cxxxx cache deviation); the fatal path
      is not exercised on host (heap_panic is `-> !`), as in
      app/service_manager and cxx/list_splice. NOT HOOK-READY until
      something ports the creator 0x081d68f0 and publishes an instance -
      today all 73 call sites would reach heap_panic.
      Host tests - the slot starts NULL, the accessor hands back exactly
      what was published, it re-reads the holder on every call rather
      than caching, and a misaligned pointer passes through unmasked.
      match.py structural (exit 1, diff expected) - same global load /
      `cmp r0,#0` / `bxne lr` / fatal-call shape, the port adding only
      the cold-path frame push and using `[r0]` instead of `[r0,#4]`
      for the crate static.

  - address: 0x08139210
    ghidra: thunk_FUN_08139210
    name: iap_incoming_process_thread_instance_veneer
    size: 4
    sig: "void *iap_incoming_process_thread_instance_veneer(void)"
    module: app/iap_incoming_process_thread
    renames:
      FUN_081d71c0: iap_incoming_process_thread_instance
    status: ported
    notes: >-
      Long-branch veneer to iap_incoming_process_thread_instance @
      0x081d71c0 - one word, `b 0x081d71c0` (0xea0277ea). Genuinely 4
      bytes, not the 8 of the `ldr pc,[pc,#-4]` + target-word form and
      not an empty `bx lr` destructor: the word was decoded from raw
      bytes, and the following word (0x08139214, `mov r0,#1`) is part
      of an unrelated function. Sits in the same long-branch veneer
      region as service_manager_instance_veneer @ 0x081391ec, nine
      words above it.
      65 `bl` call sites, 0x08164134..0x08201278 - the bulk in
      0x081fxxxx (25), 0x0819xxxx (13) and 0x081axxxx (11) - against
      only 8 that reach the accessor directly; binary-scanned by
      decoding every B/BL word in osos.dec. The sites share one shape:
      take the returned pointer as `this`, pull a request word out of
      the caller's own object and dispatch (`bl veneer; ldr r1,[r6,#8];
      bl ...` @ 0x08164134).
      Ported as its own `#[inline(never)]` symbol rather than an alias
      so a hook here lands on a real forwarding branch; the callee is
      called directly, not through read_volatile - unlike the
      libc/iram_veneers, nothing here resembles a compiler builtin, so
      LLVM cannot re-lower it. Same NOT-HOOK-READY caveat as its
      target.
      Host tests - the veneer reaches the same accessor and agrees with
      it, and the two symbols are distinct addresses. match.py
      structural (exit 1, diff expected) - `push {fp,lr}; mov fp,sp;
      pop {fp,lr}; b iap_incoming_process_thread_instance`, the
      original's tail branch plus the target's mandatory non-leaf frame
      pointer.

  - address: 0x081d6e38
    ghidra: FUN_081d6e38
    name: iap_incoming_process_thread_register_client
    size: 48
    sig: "i32 iap_incoming_process_thread_register_client(u8 *thread, i32 timeout_millis, u8 *client, u32 unread_seed)"
    module: app/iap_incoming_process_thread
    status: ported
    renames:
      param_1/r0/r4: thread (the 0x240-byte CIapIncomingProcessThread context)
      param_2/r1: timeout_millis (signed relative timeout converted to {seconds,nanos})
      param_3/r2/r5: client (registered at the slot's +4 context word)
      param_4/r3/r6: unread_seed (carried to the registry body but never read there)
      FUN_08261e94: milliseconds_to_timespec (unported eight-byte stack-object helper;
        reproduced inline with ported __rt_sdivmod)
      FUN_081d6dbc: iap_incoming_process_thread_register_client_body (unported;
        target default through IAP_THREAD_REGISTER_CLIENT_OPS)
    notes: >-
      Raw ARM confirms Ghidra's 48-byte extent exactly: the code starts with
      push {r2,r3,r4,r5,r6,lr} at 0x081d6e38 and returns with
      pop {r2,r3,r4,r5,r6,pc} at 0x081d6e64; the distinct sibling starts at
      0x081d6e68 with push {r4,r5,r6,r7,r8,lr}, no literal pool. It preserves
      thread, client, and r3 across a local eight-byte deadline, calls
      0x08261e94(deadline, timeout_millis), then calls
      0x081d6dbc(thread, deadline, client, unread_seed) and returns the
      registry body's slot index or -1 unchanged. The conversion helper calls
      the ported ADS signed quotient/remainder core __rt_sdiv with divisor
      1000, stores quotient at +0 and remainder*1000000 at +4: signed
      {seconds,nanos} with truncation toward zero. The port reproduces that
      helper inline through __rt_sdivmod; the unported registry body remains
      behind IAP_THREAD_REGISTER_CLIENT_OPS (target default transmuting
      0x081d6dbc, host recorder) rather than inventing its allocation and
      lock behavior. A whole-image ARM B/BL-immediate decode finds exactly 11
      direct inbound bl sites, all unconditional (0x08139e54, 0x0816428c,
      0x081649e0, 0x08193d40, 0x08196f7c, 0x081af2cc, 0x081f2654,
      0x081f2684, 0x081f3920, 0x08200e54, 0x08201268), zero predicated bl,
      zero tail b, and zero aligned data-word references. The host tests in
      app::iap_incoming_client_base prove the normal 1ms deadline becomes
      {0,1000000}, client/seed forwarding, slot propagation, and the
      signed -1001ms edge becomes {-1,-1000000}. match.py structural
      review is expected to differ at the intentionally inlined helper and
      the registry-body seam.
  - address: 0x081d7270
    ghidra: FUN_081d7270
    name: iap_incoming_process_thread_slot_poll
    size: 68
    sig: "u32 iap_incoming_process_thread_slot_poll(u8 *this, u32 index)"
    module: app/iap_incoming_process_thread
    status: ported
    renames:
      param_1: this (the 0x240-byte iAP incoming-process context, NOT
        the 0x44-byte CIapIncomingProcessThread wrapper - see notes)
      param_2: index (registration slot, 0..28)
      FUN_08030f44: heap_panic (direct, ported in heap/veneers)
      FUN_08257cb4: slot_object_condvar_poll (unported; zero-timeout
        condition wait on the slot object's condvar at +0x10, behind
        the IAP_THREAD_SLOT_POLL_OPS seam)
    notes: >-
      68 bytes, 0x081d7270..0x081d72b4, all code, no literal pool;
      Ghidra's 68 is exact (the next function opens at 0x081d72b4
      with `mov r1,#0`). Byte-decoded from osos.dec. Call count
      binary-verified by decoding every B/BL word in osos.dec: 36
      unconditional `bl` plus exactly one plain tail `b` @ 0x081f2210,
      zero predicated forms, and no DATA word anywhere references the
      address - only ever direct-called, never virtual.
      Ghidra's reference C inlines the posix_mutex_unlock body at the
      tail (the veneer 0x08261e24 is a bare `b`), which is also what
      proves the return value IS the unlock's status.
      Algorithm: under the context's registry mutex at this+0x114
      (veneers 0x08261e20/0x08261e24, resolved to the canonical
      posix_mutex_lock/unlock per the names.yaml alias resolution):
      refuse index >= 29 and a NULL slot-object word at
      this+0x154+index*8 - both fatal via heap_panic WITH THE MUTEX
      HELD (it never returns, there is no unlock on that path) -
      then call 0x08257cb4(slot_object) and tail-branch to the
      unlock. The callee is a 4-instruction veneer (`add r0,r0,#16;
      b 0x8261f78`) running a ZERO-TIMEOUT condition wait on the
      registration object's condvar: 0x8261f78 stacks a zeroed
      {sec,nsec} and chains 0x8261f28 -> 0x8261f94 -> the posix
      timed-wait body @ 0x0826269c (the cond_wait_attr_clock notes).
      A pending signal is drained, an empty condvar times out
      immediately; the wait status is discarded. Callers hold their
      own session mutex around `iap_incoming_process_thread_instance();
      slot_poll(thread, session->slot_index)` (e.g. @ 0x081f1ca8,
      index from [r5,#0x30]).
      OBJECT IDENTITY - `this` is the 0x240-byte context object the
      ported accessor hands out, and the existing module doc's
      0x44-byte story needs the correction the module header now
      carries: the creator @ 0x081d68f0 publishes thread->+0x00 (`ldr
      r4,[r4]` @ 0x081d69fc, `str r4,[r6,#4]` @ 0x081d6a38), which the
      spawn helper 0x0839e874 fills with the factory-0x081d7058
      (`operator new(0x240)`, ctor 0x081d72c8, vtable 0x0898e05c)
      product; FUN_081d7240 is that context's locked +0x150 setter
      (it touches this+0x114/+0x150), and the failed-start destroy @
      0x081d6a24 runs the context's vtable slot +0x08. Layout from
      the ctor: vtable +0x000, ctor arg +0x110, mutex wrapper (0x1c)
      +0x114, second wrapper +0x130, byte +0x14c, object ptr +0x150,
      29 slots of 8 bytes {object, context} at +0x154, count 30 at
      +0x23c. The sibling @ 0x081d6e68 has the IDENTICAL guard shape
      but calls the two-argument setter 0x08257ca0; the register path
      @ 0x081d6dbc news the 0x24-byte slot object (ctor 0x08257cc8,
      owner=this, condvar at its +0x10) into a free slot found by the
      29-bound linear search @ 0x081d70d4.
      Deviations - 0x08257cb4 is unported and rides the
      IAP_THREAD_SLOT_POLL_OPS seam: target default transmutes the
      ROM address (hook-ready), host default is inert and tests
      install a recording model; the fatal paths are not host-tested
      (heap_panic is `-> !`, the pending_event_take precedent).
      Host tests (4, app::iap_incoming_process_thread::slot_poll_tests,
      low-slab fixture at hints::IAP_THREAD_SLOT_POLL): the poll runs
      under the held mutex with the slot's +0 pointer verbatim and the
      unlock status 0 returned; edge indices 0 and 28 both resolve;
      the index strides by 8 and the slot's +4 context word is never
      read (poison-pinned); the unlock status is forwarded as the
      return value (mock drops the owner word, the function's own
      unlock then reports 0x05 non-owner, surfaced verbatim).
      match.py structural (exit 1, diff expected): same lock /
      bounds-check / lsl-#3 + 0x154 slot load / NULL-check / poll /
      mov r0,r4 / tail-unlock skeleton; deltas are the crate fp
      frame, `cmp #28/bhi` for `cmp #29/bcs`, a predicated `bleq`
      heap_panic, and the seam's literal-load + blx for the unported
      poll callee.

  - address: 0x082792fc
    ghidra: thunk_FUN_082792fc
    name: string_object_destroy_veneer
    size: 4
    sig: "StringObject *string_object_destroy_veneer(StringObject *this)"
    module: cxx/string_object
    renames:
      FUN_08277484: string_object_destroy
    status: ported
    notes: >-
      Long-branch veneer to string_object_destroy @ 0x08277484 - one
      word, `b 0x08277484` (0xeafff860). Genuinely 4 bytes, not the 8 of
      the `ldr pc,[pc,#-4]` + target-word form: this is a direct B and
      the following word (0x08279300, `push {r4,lr}`) is the entry of an
      unrelated function. It sits in a veneer block - the word before it,
      0x082792f8, is `b 0x082aad24` (operator delete). 98 `bl` call sites
      and 0 `b`, binary-scanned by decoding every B/BL word in osos.dec.
      The target is already ported, so the veneer calls it directly (no
      ops slot, no read_volatile - nothing here resembles a compiler
      builtin). Kept as its own `#[inline(never)]` symbol rather than an
      alias so a hook at 0x082792fc lands on a real forwarding branch.
      match.py structural: `push {fp,lr}; mov fp,sp; pop {fp,lr};
      b string_object_destroy` - the original's tail branch plus the
      target's mandatory non-leaf frame pointer.

  # --- the framework's scoped context token (vtable 0x089a5b30) ---

  - address: 0x0826fda0
    ghidra: FUN_0826fda0
    name: capture_context_fields
    size: 52
    sig: "void capture_context_fields(ScopedContext *this, void *owner)"
    module: app/scoped_context
    renames:
      param_1: this
      param_2: owner
      iVar1: service_context
      uVar2: registry_token
      DAT_0826fdd4: APP_ROOT_OBJECT
    status: ported
    notes: >-
      Stores owner unconditionally at this+0x08. If owner is NULL, the
      conditional store clears this+0x0c and the common tail clears
      this+0x10 before the system-root literal is loaded. Otherwise it
      follows the runtime-initialized RW root word @ 0x089ca674, loads
      root+0x30 into this+0x0c, then stores either NULL or
      (service_context+0xf60)+0x18 at this+0x10. The root and service
      context are intentionally unguarded, matching the ARM fault
      behavior. Raw ARM confirms the two direct BL callers at
      0x0827037c and 0x082703bc; the scoped constructor also has the
      tail-B caller at 0x0816ef78. The public CAPTURE_CONTEXT_FIELDS
      dispatch slot defaults to this port and remains replaceable only
      for constructor-test isolation. Host tests directly prove the
      owner-NULL path makes zero root accesses, captures all three
      non-NULL fields, and yields NULL for a NULL registry.

  - address: 0x08270394
    ghidra: FUN_08270394
    name: scoped_context_construct
    size: 60
    sig: "ScopedContext *scoped_context_construct(ScopedContext *this, void *owner, u8 mode)"
    module: app/scoped_context
    vars: {param_1: this, param_2: owner, param_3: mode, DAT_082703cc: SCOPED_CONTEXT_VTABLE_ADDRESS, FUN_0826fda0: capture_context_fields}
    status: ported
    notes: >-
      109 bl + 2 b call sites (the tail-b sites are 0x08159a6c and
      0x0816ef78), binary-scanned by decoding every B/BL word in
      osos.dec - not from Ghidra xrefs. Ghidra's 56-byte extent drops
      the trailing literal-pool word @ 0x082703cc (= 0x089a5b30, the
      class vtable), so the true extent is 60. Constructor of a
      0x18-byte polymorphic stack token: `str vtable,[this]; mov r0,#0;
      stmib this,{r0,r1}` (+0x04 = 0, +0x08 = owner), zero +0x0c/+0x10
      and the +0x14 byte, `bl 0x0826fda0` with r1 still the owner, then
      `strb r2,[this,#0x14]` and return this. Two orderings are
      load-bearing and reproduced: the mode byte is zeroed BEFORE the
      callee and written AFTER it, and the owner is stored twice (once
      by the stmib, once by the callee). Sibling members of the same
      class, all planting 0x089a5b30: 0x08270414 (trivial dtor, ported
      below), 0x08270418 (field copy +0x04..+0x14, vtable untouched, 15
      bl), 0x082703d0 (adopts another token's owner through 0x0826fd24,
      7 bl), 0x08270320 (2 bl). Field map: +0x00 vtable, +0x04
      owner_valid (word; only the low byte is ever read, by
      0x0826fd24), +0x08 owner, +0x0c service_context, +0x10
      registry_token, +0x14 mode byte. The class name does not survive
      in the image - no constructor hands a literal to the class-name
      factory, and all fifteen vtable slots at 0x089a5b30..0x089a5b68
      point into undecoded code - so the port names it for what it
      holds (the cxx/string_object.rs precedent). Deviations: repr(C)
      structs with real Rust pointers, so no literal byte offset
      appears (app/resource_chain.rs precedent); the vtable is the
      modeled static SCOPED_CONTEXT_VTABLE carrying the fifteen ROM
      words, not the ROM address; capture_context_fields @ 0x0826fda0
      is separately ported above. Its public CAPTURE_CONTEXT_FIELDS
      dispatch slot still wraps the real port so constructor tests can
      install a recording callee without changing its ABI. The unguarded
      root walk remains, so a NULL root with a non-NULL owner faults
      exactly where the original would. What the derived words are: the
      object at root+0x30 owns a registry at +0xf60 that 0x0805e36c
      interrogates by FourCC (0x66696c65 = "file" at 0x0803bf48, one of
      this ctor's own callers), and +0x18 of that registry is the cached
      word. Host tests cover every constructor field and this returned;
      the capture callee receives the owner and zero mode byte; mode
      0/1/0x7f/0x80/0xff round-trips. match.py structural: same vtable
      literal, same six field initializations, same callee call, same
      `strb mode,[this,#0x14]` tail, same `mov r0,this` return; the diff
      is the crate fp frame, LLVM's reordering of independent stores, and
      the dispatch-slot ldr/blx standing in for the direct
      `bl 0x0826fda0` documented deviation.

  - address: 0x08270414
    ghidra: FUN_08270414
    name: scoped_context_destroy
    size: 4
    sig: "void scoped_context_destroy(ScopedContext *this)"
    module: app/scoped_context
    vars: {param_1: this}
    status: ported
    notes: >-
      175 bl call sites, 0 b - binary-scanned by decoding every B/BL
      word in osos.dec. Ghidra's 4 bytes are right and the function is
      NOT a veneer: the whole body is `bx lr` (0xe12fff1e), the word
      before it @ 0x08270410 is the sibling ctor's vtable literal
      0x089a5b30 and the word after it starts the separate field-copy
      member @ 0x08270418, so there is no target word to follow. The
      trivial destructor of the scoped context token constructed by
      0x08270394 above: the class owns nothing (its two derived words
      are borrowed views onto the system root, its owner is not
      retained), so destruction has nothing to release. Identified as
      this class's destructor rather than a generic shared no-op by
      correlating call sites: 166 of the 175 sit within 0x300 bytes of
      a call to 0x08270394 / 0x082703d0 / 0x08270320 / 0x08270418, or
      to a function that itself calls one - 0x0813b898 is the archetype
      (runs 0x08270394 on this, copies a second token in with
      0x08270418), and its caller at 0x080fec78 destroys the result at
      0x080feca8. Deviation: the recovered C signature is `void(void)`
      (the body consumes nothing), but the port takes `this` because
      every call site passes the token in r0 and an unused AAPCS
      argument is ABI-identical - the ui/noop_f7f4.rs precedent argued
      the other way for a function with no destructor role. Host tests:
      destroying a fully constructed token leaves all six fields
      byte-identical; a NULL token is accepted, since the original
      never dereferences this. match.py: the port compiles to the same
      three-instruction frame as runtime/cxa_guard.rs
      cxa_guard_release, so the linker folds them and emits
      scoped_context_destroy as a second global on
      .text.cxa_guard_release; objdump prints only the first label, so
      `match.py 0x08270414 scoped_context_destroy` reports the symbol
      missing. Diffed through the folded section instead
      (`match.py 0x08270414 cxa_guard_release --size 4`): `push
      {fp,lr}; mov fp,sp; pop {fp,pc}` - the crate frame around the
      original's bare `bx lr`, identical to ui_noop_f7f4.

  - address: 0x08270418
    ghidra: FUN_08270418
    name: scoped_context_copy_fields
    size: 44
    sig: "void scoped_context_copy_fields(ScopedContext *destination, const ScopedContext *source)"
    module: app/scoped_context
    renames:
      param_1/r0: destination
      param_2/r1: source
    status: ported
    notes: >-
      Raw ARM spans exactly 0x08270418..0x08270443: five ordered loads
      from source then stores to destination copy +0x04 owner_valid,
      +0x08 owner, +0x0c service_context, +0x10 registry_token, and
      +0x14 mode; it never reads or writes either vtable at +0x00.
      The next word, 0x08270444, starts a separate six-halfword
      initializer, so Ghidra's 44-byte extent is exact. Binary decoding
      of every ARM B/BL word in osos.dec finds 15 direct BL callers
      (0x08113c10, 0x0811515c, 0x0812f740, 0x0812fce0, 0x08130388,
      0x0813b8f4, 0x0813cfe4, 0x0813e468, 0x08179390, 0x08179c38,
      0x08218f00, 0x0821980c, 0x08219afc, 0x082a5f70, 0x082a5ff8);
      all are unconditional, with no predicated BL or direct tail-B
      sites. The Rust port copies named #[repr(C)] fields, retaining
      32-bit target offsets and host consistency rather than literal byte
      offsets. Its recovered void signature intentionally does not
      surface the raw function's incidental r0 pass-through. Host tests
      prove that each payload field, including a NULL pointer and mode
      0xff, copies while the destination vtable remains unchanged, and
      that source == destination is safe.

  - address: 0x0812c75c
    ghidra: FUN_0812c75c
    name: copy_service_context_selection
    size: 104
    sig: "void copy_service_context_selection(ServiceContextProvider *provider, u32 selection, ScopedContext *destination)"
    module: app/scoped_context
    renames:
      param_1/r0: provider
      param_2/r1: selection
      param_3/r2: destination
      FUN_082a5ee4: service_context_selection_construct
    status: ported
    notes: >-
      Raw ARM proves this is one logical 104-byte member split across two
      separately linked regions: entry 0x0812c75c..0x0812c768 is `ldr
      r0,[r0,#0x30]; cmp r0,#0; bne 0x0816ec30; bx lr`, while the 88-byte
      body is 0x0816ec30..0x0816ec84 (the next entries are 0x0812c76c and
      0x0816ec88). A NULL provider+0x30 context returns without touching
      destination. A non-NULL one forms context+0x14, calls the unported
      FUN_082a5ee4 with that source, selection, and zero mode, copies the
      temporary ScopedContext payload words +0x04..+0x14 to destination
      while preserving its vtable, then calls the trivial
      scoped_context_destroy @ 0x08270414. Decoding every ARM B/BL word in
      osos.dec finds exactly 10 direct call sites, all unconditional plain
      BL (0x081758b0, 0x08176878, 0x081768d0, 0x08225ddc, 0x08225eec,
      0x08225fc0, 0x08226380, 0x082266c8, 0x082269a0, 0x08226bb8), with no
      predicated forms or direct tail B. 0x082a5ee4 has no port ledger
      entry, so the Rust port uses a volatile dispatch seam whose target
      default calls the stock address; host tests replace it. Provider and
      context layouts use named repr(C) fields backed by u32 word arrays,
      retaining target addresses without host pointer-width overlap. Host
      tests prove the NULL branch does not call the seam or change the
      destination, and the non-NULL branch forwards source/selection/mode
      zero then copies every payload field while leaving destination vtable
      intact.

  - address: 0x08270f84
    ghidra: FUN_08270f84
    name: descriptor_field_map
    size: 160
    sig: "void descriptor_field_map(const u32 *source, u32 *destination)"
    module: app/descriptor_field
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: destination
      r3: field_tag
      DAT_08271024: tag_0_prefix
      DAT_08271028: tag_5_prefix
      DAT_0827102c: tag_6_prefix
      DAT_08271030: tag_7_field
      DAT_08271034: tag_8_field
      DAT_08271038: tag_9_field
      DAT_0827103c: tag_10_field
      FUN_08030f44: heap_panic
    notes: >-
      Raw ARM extent is exactly 0x08270f84..0x08271040: code through bx lr
      at 0x08271020 plus the seven literal-pool words at
      0x08271024..0x0827103c; the next separately linked function starts
      at 0x08271040. Decoding every ARM B/BL word in osos.dec finds 10
      direct BL callers, all unconditional plain BL, and one unconditional
      plain-B tail caller at 0x082a472c; there are no predicated forms.
      The low byte at source+8 selects an opaque three-word descriptor-field
      transformation. Tags 0, 5, and 6 select a two-word literal prefix and
      preserve source word zero as destination word two. Tags 1..4 place
      source word one, 0x80000010+(tag<<8), and source word zero. Tags 7..10
      select literal three-word fields. Other tag values call heap_panic
      (0x08030f44), which does not return. The source/destination format has
      no recoverable higher-level identity, so the port names its observed
      transformation rather than inventing one. The Rust port directly calls
      the existing heap_panic port on the fatal path; this is the only
      code-generation deviation from the original direct BL. Host tests cover
      every valid tag with high tag-word bits set and source/destination
      aliasing; descriptor_layout_initialize now calls this port directly,
      replacing its former fixed-address mapper seam.
  - address: 0x082a3dcc
    ghidra: FUN_082a3dcc
    name: scoped_context_is_valid
    size: 72
    sig: "u32 scoped_context_is_valid(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    renames:
      param_1/r0: this
      FUN_0806b410: scoped_context_owner_validity
      iVar1: owner_validity
      iVar2: registry
    notes: >-
      Raw extent is exactly 72 bytes, 0x082a3dcc through 0x082a3e10:
      the following word, 0x082a3e14, begins a separately linked
      push {r4,r5,lr} function, so there is no literal pool. Decoding
      every ARM B/BL word in osos.dec finds ten direct calls, all plain
      unconditional BL (0x080aa544, 0x0813d794, 0x0817c224,
      0x0817dfc4, 0x081b5df0, 0x081fd158, 0x081fd1cc, 0x08208b3c,
      0x08208bb4, and 0x08226bc0), with zero predicated forms and no
      direct tail B sites. It always calls the unported 0x0806b410
      owner predicate first. When that returns nonzero, it reads the
      ScopedContext service_context +0xf60 registry slot and, if that
      registry exists, returns 1 immediately when its +0x18 word equals
      this->registry_token. Every other path returns whether the full
      owner_valid word is nonzero. The Rust port keeps the existing
      repr(C) ScopedContext model and aligned word-indexed service and
      registry accesses, avoiding host pointer-width overlap. The
      0x0806b410 callee has no existing ledger entry, so its target
      default dispatches to the ROM address while host tests install a
      recording seam; no callee identity is invented. The helper's raw
      code rejects NULL, then checks a dereferenced object's +0x04 word
      against the literal tdat tag, but it remains unported. Host tests
      prove the helper runs before every fallback, matching live registry
      token succeeds, and absent or mismatched registry tokens fail when
      owner_valid is zero.

  - address: 0x082a3fc4
    ghidra: FUN_082a3fc4
    name: scoped_context_owner_flags_bit_3
    size: 52
    sig: "u32 scoped_context_owner_flags_bit_3(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    vars: {param_1: this}
    notes: >-
      16 bl call sites, all unconditional with zero predicated forms and
      zero tail b sites, binary-scanned by decoding every B/BL word in
      osos.dec - no caller NULL-guards the token. Ghidra's 52-byte
      extent is exact: the thirteen instructions end at 0x082a3ff4 and
      the next function begins with its own push {r4,lr} at 0x082a3ff8,
      so there is no trailing literal pool. Validity-gated owner-flags
      predicate over the ScopedContext token (app/scoped_context.rs):
      dispatches the token's vtable slot +0x08 and, only when it returns
      nonzero, reads the owner's flags word at +0xbc and returns bit 3
      as 0 or 1 via and #8 / lsr #3. A failing slot answers 0 without
      dereferencing owner, so a NULL owner is safe only on that path.
      This belongs to the capability-predicate family with
      0x082a40c8 (mask 0x8062), 0x082a4574 (owner byte +0x8f bit 0),
      and 0x082a45a4 (bit 21); the context-menu builder at 0x08222eec
      ANDs their answers to decide whether to suppress an item. What
      bit 3 means does not survive in the image, so the numeric name
      records only the observed bit. The function itself is not
      virtual-dispatched: no data word in the image holds 0x082a3fc4.
      Deviations: repr(C) ScopedContext and ScopedContextVtable models
      replace literal offsets, and the serialized vtable slot word is
      transmuted to its call ABI at the dispatch point. LLVM may branch
      where ADS predicates its owner loads and extraction; the
      validity-gated dereference and 0/1 result remain the same. Host
      tests prove slot-zero short-circuit with a NULL owner and isolate
      bit 3 against immediate neighbours, sibling masks, bit 3 with
      strays, and all bits set; all nonzero slot results reach the
      owner read. match.py structural: same vtable load, slot +0x08 blx
      dispatch, validity cmp, gated owner +0x08 and flags +0xbc loads,
      bit-3 mask/extraction, and 0/1 result; the diff is the crate fp
      frame and predicate-versus-branch code generation.

  - address: 0x082a40c8
    ghidra: FUN_082a40c8
    name: scoped_context_owner_flags_any_8062
    size: 60
    sig: "u32 scoped_context_owner_flags_any_8062(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    vars: {param_1: this, DAT_082a4100: OWNER_FLAGS_MASK_8062}
    notes: >-
      42 bl call sites, binary-scanned by decoding every B/BL word in
      osos.dec - every one an unconditional bl, no predicated forms, so
      no caller NULL-guards the token (the callee itself faults on a
      NULL this, exactly like the port). Ghidra's 56-byte extent drops
      the trailing literal-pool word @ 0x082a4100 (= 0x00008062, the
      mask), so the true extent is 60. Validity-gated owner-flags
      predicate over the ScopedContext token (app/scoped_context.rs):
      dispatches the token's vtable slot +0x08 and, only when it
      returns nonzero, reads the owner's flags word at +0xbc and
      returns 1 when any of bits 1, 5, 6, 15 is set, else 0. A failing
      slot short-circuits, so a NULL-owner token answers 0 without a
      dereference. One of a family of same-shaped predicates over the
      same owner word - 0x082a3fc4 tests bit 3, 0x082a45a4 tests bit
      21, 0x082a4574 tests owner byte +0x8f bit 0 - that callers AND
      together: the context-menu builder 0x08222eec (the
      "HandleAddToOTG"/"HandleViewAlbum"/"HandleCancel" literals)
      suppresses an item only when all four answer 0, and the
      media-player event dispatcher 0x0817c468 compares the predicate
      across two track tokens. 39 of the 42 call sites build their
      token with scoped_context_construct within the same function
      (binary-scanned correlation); the rest receive it from their own
      caller. The token argument is NOT virtual-dispatched itself: no
      data word in the image holds 0x082a40c8. Base-vtable anomaly,
      recorded not invented: the serialized slot +0x08 word 0x0820ca2c
      lands 0x40 bytes INSIDE FUN_0820c9ec (a 96-byte retry loop @
      0x0820c9ec..0x0820ca4b per Ghidra's own boundary) where the first
      instruction is a flags-dependent ble, so the base-class default
      is effectively undispatchable and live tokens must carry an
      overriding derived vtable planted by the filling call (callers'
      vtable+0x170/vtable+0x22c methods). The port dispatches whatever
      vtable the token carries, so the anomaly costs it nothing.
      Deviations: the repr(C) ScopedContext/ScopedContextVtable models
      stand in for literal offsets; LLVM narrows the +0xbc read to an
      ldrh because 0x8062 fits in 16 bits (identical on plain RAM);
      LLVM branches (beq) where ADS predicated (ldrne/tstne/moveq) and
      synthesizes the mask with mov/orr instead of a literal pool.
      Host tests: slot-0 short-circuit with a NULL owner (proves no
      dereference), zero and non-mask bits answer 0, each of bits
      1/5/6/15 singly answers 1, mask+stray mix answers 1, any nonzero
      slot result is truthy. match.py structural: same vtable load,
      same slot +0x08 blx dispatch, same gated owner +0xbc read, same
      0x8062 mask, same 0/1 result; the diff is the crate fp frame and
      the three deviations above.

  - address: 0x082a4574
    ghidra: FUN_082a4574
    name: scoped_context_owner_byte_8f_bit_0
    size: 48
    sig: "u32 scoped_context_owner_byte_8f_bit_0(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    vars: {param_1: this, bVar1: owner_byte}
    notes: >-
      18 bl call sites, all unconditional with zero predicated forms,
      binary-scanned by decoding every B/BL word in osos.dec; the only
      additional direct caller is an unconditional tail b at
      0x0811343c. Ghidra's 48-byte extent is exact: the twelve
      instructions end at 0x082a45a0 and the next function begins with
      push {r4,lr} at 0x082a45a4. Validity-gated predicate over the
      ScopedContext token (app/scoped_context.rs): dispatches the
      vtable slot +0x08, then when it returns nonzero loads owner
      (+0x08) byte +0x8f and returns its bit 0 as 0 or 1. A zero slot
      result answers 0 without reading owner, so a NULL owner is safe
      only on that path. The field's semantic name does not survive;
      byte_8f_bit_0 records only what raw code establishes. The function
      itself is not virtual-dispatched: no data word in the image holds
      0x082a4574. It is one of the scoped-context capability-predicate
      family with 0x082a3fc4, 0x082a40c8, and 0x082a45a4; the menu
      builder at 0x08222eec ANDs their results. Deviation: repr(C)
      ScopedContext and ScopedContextVtable models replace original
      field offsets, while the owner byte remains an aligned-base u8
      load at +0x8f. Host tests prove slot-zero short-circuit with a
      NULL owner and each relevant byte pattern: values 0x00, 0x02,
      0x80 answer 0; 0x01, 0x81, 0xff answer 1. match.py structural:
      same vtable load, slot +0x08 blx, validity cmp, gated owner +0x08
      and byte +0x8f loads, bit-0 mask, and 0/1 result. LLVM places
      moveq r0,#0 before the predicated nonzero path and adds the crate
      fp frame; both preserve the original's result and dereference
      gate.

  - address: 0x082a45a4
    ghidra: FUN_082a45a4
    name: scoped_context_owner_flags_bit_21
    size: 52
    sig: "u32 scoped_context_owner_flags_bit_21(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    vars: {param_1: this}
    notes: >-
      17 bl call sites, all unconditional with zero predicated forms
      and zero tail b sites, binary-scanned by decoding every B/BL
      word in osos.dec - no caller NULL-guards the token. Ghidra's
      52-byte extent is exact: the thirteen instructions end at
      0x082a45d4 and the next function begins with its own push
      {r4,lr} at 0x082a45d8 (a same-shaped sibling that reads owner
      word +0x90 instead), so there is no trailing literal pool - the
      0x200000 mask is a rotated immediate inside the andne, unlike
      the 0x8062 sibling's literal-pool word. Validity-gated
      owner-flags predicate over the ScopedContext token
      (app/scoped_context.rs): dispatches the token's vtable slot
      +0x08 (the same virtual the whole 0x082a4 family gates on) and,
      only when it returns nonzero, reads the owner's flags word at
      +0xbc and returns its bit 21 as 0 or 1 via and 0x200000 / lsr
      #21. A failing slot answers 0 without dereferencing owner, so a
      NULL-owner token is safe. One of the capability-predicate
      family with 0x082a3fc4 (bit 3), 0x082a40c8 (mask 0x8062,
      ported), and 0x082a4574 (byte +0x8f bit 0, ported); the
      context-menu builder 0x08222eec ANDs their answers to decide
      whether to suppress an item. What bit 21 means does not survive
      in the image, so the mask keeps its numeric name, the
      OWNER_FLAGS_MASK_8062 precedent. The function itself is not
      virtual-dispatched: no data word in the image holds 0x082a45a4
      (binary-scanned). Deviations: repr(C) ScopedContext and
      ScopedContextVtable models stand in for literal offsets, and
      the slot word is transmuted to the call ABI at the dispatch
      point - the module's standing deviations. Host tests: slot-0
      short-circuit with a NULL owner (proves no dereference), bit-21
      isolation across 8 flag patterns (0, mask alone, all-ones,
      complement, the 0x8062 sibling's bits, both immediate
      neighbours, mask-plus-strays), and any-nonzero-slot truthiness.
      match.py structural: same vtable load, same slot +0x08 blx
      dispatch, same validity cmp, same gated owner +0x08 and flags
      +0xbc loads, same bit-21 extraction and 0/1 result; the diff is
      the crate fp frame (push {r4,sl,fp,lr} / add fp,sp,#8) and LLVM
      synthesizing movne r1,#1 / andne r0,r1,r0,lsr #21 where ADS
      used andne/lsrne/moveq - identical results on both exits. NOTE:
      in this worktree match.py needs --lib pointing at the
      worktree's librustypod.a; the default path resolves the main
      checkout's archive.

  - address: 0x082a368c
    ghidra: FUN_082a368c
    name: scoped_context_owner_u64_110
    size: 52
    sig: "u64 scoped_context_owner_u64_110(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    vars: {param_1: this}
    notes: >-
      33 bl call sites, 0 predicated forms, binary-scanned by decoding
      every B/BL word in osos.dec - no caller NULL-guards the token.
      Ghidra's 52-byte extent is exact for once: the thirteen
      instructions end at 0x082a36bc and the next function starts at
      0x082a36c0 (its own push {r4,r5,lr}), so there is no trailing
      literal pool to check. Validity-gated 64-bit owner getter over
      the ScopedContext token (app/scoped_context.rs), the same shape
      as scoped_context_owner_flags_any_8062 @ 0x082a40c8: dispatch the
      token's vtable slot +0x08 and, only when it returns nonzero,
      load the owner (+0x08) and return its adjacent word pair
      +0x110 (r0, low) / +0x114 (r1, high) as a u64; an invalid token
      answers 0 in both halves (moveq r0/r1) without touching the
      owner, so a NULL-owner token returns 0 instead of faulting. What
      the pair means does not survive in the image - callers treat it
      as an opaque identity (0x081688e4's `if (value != 0)` gate,
      0x08179990 discards it) - so the port keeps its offset name, the
      OWNER_FLAGS_MASK_8062 precedent. GHIDRA BUG: the recovered C
      returns only `undefined4` - the decompiler drops the r1 half of
      the pair; the raw `ldrne r1, [r1, #0x114]` / `moveq r1, #0` are
      unambiguous that both halves are live on every exit. The
      function itself is not virtual-dispatched: no data word in the
      image holds 0x082a368c (binary-scanned). Deviations: the token
      and vtable are the module's repr(C) models, so the vtable load
      and slot index are struct field accesses (the module's standing
      deviation); the slot word is transmuted to the call ABI at the
      dispatch point, the ui/element_reference.rs idiom; the pair is
      read as two aligned u32 words rather than one u64 read because
      the original needs only 4-byte alignment (two plain ldr), which
      an ldrd's 8-byte requirement would exceed. Host tests: slot-0
      short-circuit with a NULL owner (proves no dereference), AAPCS
      lane order (+0x110 low / +0x114 high) with distinctive halves,
      each half independently, a zero pair, and any nonzero slot
      result treated as true. match.py structural: same vtable load,
      same slot +0x08 blx dispatch, same cmp gate, same owner load
      ldr r1,[r4,#8], same +0x110/+0x114 reads, same zero-both-halves
      invalid path; the diff is the crate fp frame and LLVM branching
      (beq) where ADS predicated (ldrne/moveq).
  # --- Indexed source tagged-value construction (cxx/tagged_value) ---
  - address: 0x0813da28
    ghidra: FUN_0813da28
    name: tagged_value_from_indexed_source
    size: 48
    sig: "TaggedValue *tagged_value_from_indexed_source(TaggedValue *this, void *source, u32 index)"
    module: cxx/tagged_value
    status: ported
    renames:
      param_1/r0/r4: this
      param_2/r1: source
      param_3/r2: index
      r3: source_write_indexed_record
      "source->vtable+0x27c": write_indexed_record
      FUN_0828407c: tagged_value_from_optional_word4
    notes: >-
      Raw osos.dec confirms Ghidra's 48-byte extent: the twelve instructions
      at 0x0813da28..0x0813da54 allocate a 20-byte temporary at sp+4, invoke
      source->vtable slot +0x27c as (temporary, source, index), and convert
      the temporary through the already ported tagged_value_from_optional_word4.
      The next separately linked function begins with push at 0x0813da58;
      there is no literal pool. Decoding every ARM B/BL immediate in osos.dec
      finds exactly nine inbound calls, all unconditional bl: 0x0810c92c,
      0x08179f00, 0x08179f18, 0x08179f5c, 0x08179f78, 0x0817a09c,
      0x0817a0b4, 0x0817a0f8, and 0x0817a114. No predicated bl or direct tail
      b reaches this function. The virtual source class and slot target remain
      unrecovered, so the port dispatches the raw slot rather than inventing a
      fixed callee or seam. The temporary's word +0x04 is the only word the
      following helper observes. Deliberate host deviation: the fixture vtable
      holds a native-width function pointer at the structurally equivalent
      slot, avoiding 64-bit function-pointer truncation. Host coverage proves
      the virtual source and both index edge values (0 and u32::MAX) reach the
      slot, and that its +0x04 output becomes the tagged payload.


  # --- Tagged value construction (cxx/tagged_value) ---
  - address: 0x0828407c
    ghidra: FUN_0828407c
    name: tagged_value_from_optional_word4
    size: 108
    sig: "TaggedValue *tagged_value_from_optional_word4(TaggedValue *this, const u32 *source_words)"
    module: cxx/tagged_value
    status: ported
    renames:
      param_1/r0/r4: this
      param_2/r1: source_words (class identity unrecovered; word +0x04 only)
      DAT_082840dc: tagged_value_guard_base (0x089ca608)
      DAT_082840e0: default_tagged_value_guard (0x089ca620)
      DAT_082840e4: default_tagged_value (0x08a126ac)
      FUN_08258d00: tagged_value_copy
      FUN_08258d28: tagged_value_construct_kind1
      FUN_08258d48: tagged_value_default_construct
      FUN_082ab31c: cxa_guard_acquire (ported, runtime/cxa_guard.rs)
      FUN_082ab338: cxa_guard_release (ported, runtime/cxa_guard.rs)
    notes: >-
      EXTENT CORRECTED: Ghidra's 96 bytes are the code at
      0x0828407c..0x082840d8; three literal-pool words at
      0x082840dc..0x082840e4 make the true extent 108 bytes, and
      0x082840e8 is the next function. Decoding every ARM B/BL word in
      osos.dec found 18 direct call sites: 18 unconditional bl, zero
      predicated bl, zero tail b. The opaque source contributes only its
      aligned word at +0x04: nonzero tail-constructs the 16-byte result
      as {vtable 0x089a76fc, kind byte 1, source word, 0}; zero lazily
      initializes the BSS default at 0x08a126ac with the ADS guard at
      0x089ca620, then copies its vtable, kind byte, and +0x08/+0x0c
      words. The source class and the target of the vtable literal are
      not recovered, so no identity or dispatch seam is invented.
      Deliberate deviation: fixed firmware RAM addresses become host
      statics; the vtable remains an opaque u32 identity. Host tests
      cover nonzero construction, zero/default initialization, padding
      preservation, and the already-initialized path.


  # --- Tagged value default construction (cxx/tagged_value) ---
  - address: 0x08258d48
    ghidra: FUN_08258d48
    name: tagged_value_default_construct
    size: 24
    sig: "TaggedValue *tagged_value_default_construct(TaggedValue *this)"
    module: cxx/tagged_value
    status: ported
    renames:
      param_1/r0: this
      r1: vtable
      DAT_08258d5c: tagged_value_vtable (0x089a76fc)
    notes: >-
      EXTENT CORRECTED: Ghidra reports 20 instruction bytes at
      0x08258d48..0x08258d58, but the ldr at entry consumes its literal-pool
      word at 0x08258d5c; the true 24-byte extent ends there and
      0x08258d60 starts the separate assignment helper. Decoding every ARM
      B/BL word in osos.dec found exactly nine direct callers: nine
      unconditional bl, zero predicated bl, and zero tail b. No aligned
      osos.dec word equals 0x08258d48, so no stored function-pointer
      dispatch was found. The constructor stores the opaque vtable literal
      0x089a76fc at +0x00 and zero at kind byte +0x04, preserving the three
      padding bytes and the payload words at +0x08/+0x0c; r0 still holds and
      returns this. The vtable target's class identity is unrecovered, so no
      identity or dispatch seam is invented. Deliberate deviation: volatile
      stores preserve the observed vtable-then-kind sequence. Host test covers
      return-this and preservation of all unmodified bytes.

  # --- Tagged value comparison (cxx/tagged_value) ---
  - address: 0x08258d7c
    ghidra: FUN_08258d7c
    name: tagged_value_payload_pair_equal
    size: 36
    sig: "bool tagged_value_payload_pair_equal(const TaggedValue *left, const TaggedValue *right)"
    module: cxx/tagged_value
    status: ported
    renames:
      param_1/r0: left
      param_2/r1: right
      r2: left_payload
      r3: right_payload
    notes: >-
      Raw bytes establish a 36-byte, nine-instruction extent at
      0x08258d7c..0x08258d9c; 0x08258da0 begins the separately linked next
      function. Decoding every ARM B/BL word in osos.dec finds exactly nine
      incoming direct calls: nine unconditional bl, zero predicated bl, and
      zero direct tail b. It loads the aligned +0x08 word from each
      16-byte TaggedValue, returning false on mismatch; equality gates a
      comparison of the aligned +0x0c words. Vtable, kind, and padding are
      ignored, and neither pointer is NULL-checked. No deliberate deviations.

  # --- Application context scope (app/context_scope) ---
  - address: 0x082840e8
    ghidra: FUN_082840e8
    name: context_scope_init
    size: 48
    sig: "void *context_scope_init(void *scope, void *subject, u8 flag)"
    module: app/context_scope
    status: ported
    vars: {param_1: scope, param_2: subject, param_3: flag}
    notes: >-
      Constructor of the 20-byte stack-local application "context scope"
      record. Plants the class descriptor 0x089a6600 at +0x00, zeroes
      +0x04, calls context_scope_capture @ 0x08283f3c (r1 flows through
      untouched — Ghidra's C drops that argument), stores the low byte of
      r2 at +0x10 and returns this. SIZE: Ghidra says 44; the true extent
      is 48 — the eleven instructions end at 0x08284110 and the word at
      0x08284114 is the descriptor literal `ldr r0, [pc, #32]` loads.
      Ghidra dropped the trailing literal word. 110 bl call sites plus one
      tail b, binary-scanned by decoding every B/BL word in osos.dec (not
      from Ghidra xrefs). Call-site shapes confirm the RAII reading — 99
      of the 110 set `mov r2, #0` and 71 set `mov r1, #0` within four
      instructions, and 87 have a context_scope_drop site within the
      following 0x800 bytes. The class has no surviving name (nothing
      reaches the class-name factory @ 0x0820b230); its siblings are the
      handle-taking constructor @ 0x08284118 (12 bl), the copy constructor
      @ 0x0828414c (7 bl), the assignment operator @ 0x0828418c (37 bl)
      and the deleting destructor @ 0x0828417c. Deviation — the root
      object global @ 0x089ca674 is the crate static APP_ROOT_OBJECT (the
      block_mgr.rs / app/context.rs precedent: that RW page is
      runtime-initialized and the image holds stale UI strings there), so
      the non-NULL-subject path is not hook-ready until integration
      assigns it; the 71 default-constructing sites never touch it.
      match.py structural: same store order (descriptor, +0x04 zero, bl,
      strb, return this) with LLVM keeping the flag in r4 and the crate's
      frame-pointer prologue.

  - address: 0x08284118
    ghidra: FUN_08284118
    name: context_scope_init_from_handle
    size: 52
    sig: "void *context_scope_init_from_handle(void *scope, const void *handle, u8 flag)"
    module: app/context_scope
    status: ported
    vars: {param_1: scope, param_2: handle, param_3: flag}
    notes: >-
      Handle-taking constructor for the 20-byte application context-scope
      record. The 48 code bytes plant descriptor 0x089a6600 at +0x00, clear
      +0x04, call 0x08283e80 with handle still in r1, store r2's low byte
      at +0x10, and return scope. The callee is exactly five instructions:
      ldrb handle+4; cmp zero; ldrne handle+8; moveq zero; tail-b
      context_scope_capture @ 0x08283f3c. Thus a zero tag selects NULL and
      does not read handle+8 or APP_ROOT_OBJECT; every nonzero tag selects
      the 32-bit subject word. Ghidra's 48 omits the descriptor literal at
      0x08284148, so the true extent is 52 bytes. The name distinguishes
      this constructor's handle-to-subject adaptation from the direct-subject
      context_scope_init. Direct callsites pass 12-byte stack handles
      produced by vtable slot +0x1c0 and scope records to subsequent scoped
      calls. match.py structural: the port preserves descriptor, +0x04,
      selected capture, flag, and return ordering while folding the
      state-free unexported tail adapter at 0x08283e80 into this one port.

  - address: 0x08283f3c
    ghidra: FUN_08283f3c
    name: context_scope_capture
    size: 56
    sig: "void context_scope_capture(void *scope, void *subject)"
    module: app/context_scope
    status: ported
    vars: {param_1: scope, param_2: subject}
    notes: >-
      The one callee of context_scope_init: stores subject at +0x04, and
      with a NULL subject zeroes +0x08/+0x0c without reading any global
      (the `streq`/`beq` pair). Otherwise walks *0x089ca674 -> +0x30
      (stored at +0x08) -> +0xf60, storing that record's +0x18 at +0x0c or
      0 when it is NULL. Ghidra's 52 bytes covers the code
      (0x08283f3c..0x08283f6c) but drops the trailing literal at
      0x08283f70 (the 0x089ca674 global), so the true extent is 56; its
      one-argument signature is also wrong, r1 is a real parameter that
      the constructor passes through untouched. 1 bl call site plus the tail b from the sibling
      constructor's handle adapter @ 0x08283e80. Same APP_ROOT_OBJECT
      deviation as context_scope_init. match.py structural: identical walk
      and identical NULL-owner select; LLVM splits the NULL-subject branch
      into its own tail instead of predicating the two stores.

  - address: 0x08284188
    ghidra: FUN_08284188
    name: context_scope_drop
    size: 4
    sig: "void *context_scope_drop(void *scope)"
    module: app/context_scope
    status: ported
    vars: {param_1: scope}
    notes: >-
      The context-scope class's non-deleting destructor, and it is empty.
      NOT A VENEER: the single word at 0x08284188 is 0xe12fff1e (`bx lr`);
      a veneer would be 0xe51ff004 (`ldr pc,[pc,#-4]`) plus a target word,
      and a dispatch stub would be a `b`. There is nothing to follow — the
      record owns nothing that needs releasing, so ADS emitted a bare
      return and shared it across all 120 call sites. Ghidra's 4-byte
      extent is exact on both sides: 0x08284184 is the closing `bx lr` of
      the deleting destructor @ 0x0828417c and 0x0828418c is the first
      instruction of the assignment operator (37 bl sites of its own).
      120 bl call sites, no b sites, binary-scanned by decoding every B/BL
      word in osos.dec; 87 of the 110 context_scope_init sites have one
      within the following 0x800 bytes, the pairing the RAII reading
      r0 is untouched, so the ADS `this` return is preserved and
      the port returns its argument. match.py structural: the body is
      empty on both sides; LLVM adds the crate-wide frame-pointer
      prologue (push {fp,lr} / mov fp,sp / pop {fp,pc}) that every small
      port in this crate carries, e.g. cxx::handle::handle_deref_or_null.

  - address: 0x0828418c
    ghidra: FUN_0828418c
    name: context_scope_assign
    size: 36
    sig: "void *context_scope_assign(void *scope, const void *source)"
    module: app/context_scope
    status: ported
    vars: {param_1: scope, param_2: source}
    notes: >-
      The context-scope class's assignment operator: a plain memberwise
      copy of the four payload fields in register order — +0x04 subject,
      +0x08 context, +0x0c owner id (ldr/str word pairs) and the +0x10
      flag byte (ldrb/strb) — then bx lr. The +0x00 descriptor is NOT
      copied (ADS assignment never touches the constructor-planted class
      word), and +0x08/+0x0c travel verbatim rather than being re-derived
      through context_scope_capture, so unlike the constructors this
      function reads no global and APP_ROOT_OBJECT is irrelevant to it.
      EXTENT: Ghidra's 36 bytes are exact this time — nothing is
      PC-relative so there is no trailing literal, and 0x082841b0 is the
      first instruction of the separately listed sibling equality
      predicate (ldr r0,[r0,#4]; ldr r1,[r1,#4]; cmp; movne r0,#0;
      moveq r0,#1; bx lr). 37 bl call sites, no b and no predicated
      forms, binary-scanned by decoding every B/BL word in osos.dec
      (matches the count in the assignment brief). Call-site shapes
      confirm the memberwise reading: 0x081b600c copies between scope
      records embedded in a larger object (r0 = owner+0x58,
      r1 = owner+0x6c). r0 is untouched, so the ADS `this` return is
      preserved and the port returns its first argument. No NULL guard
      anywhere and none needed — with no predicated sites, every caller
      passes live records. Host tests: payload copy preserves the
      descriptor and the source record, the flag copy is one byte wide
      (+0x11 onward untouched), zero/all-ones/pointer-like field values
      copy verbatim, self-assignment is an identity. match.py structural:
      the nine-instruction body is instruction-identical; the only diff
      is the crate-wide frame-pointer prologue (push {fp,lr} / mov fp,sp
      / pop {fp,pc}), the same shape context_scope_drop carries.

  - address: 0x08283168
    ghidra: FUN_08283168
    name: parse_result_init
    size: 16
    sig: "u8 *parse_result_init(u8 *out, u8 status, u8 code, u16 detail)"
    module: app/parse_result
    status: ported
    vars:
      param_1: out
      param_2: status
      param_3: code
      param_4: detail
    notes: >-
      The 4-byte result-record constructor of the fault-tolerant
      record-resource parsers (the 0x080f8xxx-0x080fexxx cluster,
      ui/block_map's "record-resource parsers"; 52 of the 53 bl sites
      sit inside it, the outlier is 0x081d5e48). Three stores and out:
      `strb r1,[r0]; strb r2,[r0,#1]; strh r3,[r0,#2]; bx lr` -
      status at +0x00, code at +0x01, detail as a little-endian u16 at
      +0x02. r0 is never written and callers consume it (`mov r7, r0`
      &c.), so the port returns the out pointer like the original.
      The record's class occupies one translation unit at
      0x08283120-0x08283178, which proves the layout from the reader
      side: status() 0x08283120 (ldrb +0), detail() 0x08283124 (ldrh
      +2), code() 0x0828312c (ldrb +1), an IDENTICAL init twin
      0x08283134, is_ok() 0x08283144 (status == 1), clear() 0x08283154
      (zeroes all four bytes) and a trivial destructor 0x08283178 (the
      context_scope_drop phenomenon, a lone `bx lr`). Status values
      recovered from the sites: 0 = cleared (the canonical call shape
      zero-initializes, e.g. 0x080f8ef4/0x080f8f08), 1 = ok (is_ok()
      gates the consumer, e.g. `bl 0x08283144; cmp; beq <bail>` @
      0x080f8d74), 2 = error with code 5 (FUN_080fc74c writes {2, 5,
      0x2000}; FUN_080f8ba4's error path writes {2, 5, <pool
      constant>}). The adjacency to context_scope (0x08283f3c+) is ADS
      translation-unit packaging only - no context_scope function
      references the record. Extent exact: 16 bytes, 0x08283168..
      0x08283178, no literal pool. Deviation: the three stores are
      volatile so LLVM cannot merge them into one 32-bit str - the
      original emits three distinct stores, and a merged word store
      would fault where the original's byte/half stores do not if out
      were ever unaligned (the strh carries the original's halfword
      alignment contract). Host tests: field layout (byte, byte, LE
      u16) with a guard byte past the record, all-zero (the canonical
      clear shape), all-ones values, and the {2, 5, 0x2000} error
      shape. match.py structural: the strb/strb/strh triple matches
      instruction-for-instruction; the diff is only the crate-standard
      fp prologue.

  - address: 0x0828312c
    ghidra: FUN_0828312c
    name: parse_result_code
    size: 8
    sig: "u8 parse_result_code(const u8 *record)"
    module: cxx/parse_result_code
    status: ported
    vars:
      param_1: record
    notes: >-
      The code() accessor of the four-byte C++ parser-result value type:
      `ldrb r0,[r0,#1]; bx lr`, so it returns the unsigned byte at +0x01
      and neither writes memory nor null-checks its argument. The sibling
      constructor FUN_08283168 establishes the record layout as {status
      u8 @ +0x00, code u8 @ +0x01, detail u16 @ +0x02}; the adjacent
      readers are status() FUN_08283120 and detail() FUN_08283124. Its sole
      recovered direct caller, FUN_080fb68c @ 0x080fb68c, passes the
      embedded record at param_2 + 0x5c and emits the returned byte into
      output byte +0x02, confirming it is serialized as the result code.
      Eight-byte extent is exact from functions.csv (0x0828312c..0x08283133);
      no literal pool. Host tests cover every u8 return value, guard bytes
      around the record, and aliasing/nonmutation. match.py structural
      review compares the ldrb/bx-lr leaf against the target.

  - address: 0x08283134
    ghidra: FUN_08283134
    name: parse_result_init_alias_3134
    size: 16
    sig: "u8 *parse_result_init_alias_3134(u8 *out, u8 status, u8 code, u16 detail)"
    module: app/parse_result
    status: ported
    vars:
      param_1: out
      param_2: status
      param_3: code
      param_4: detail
    notes: >-
      Byte-identical twin of parse_result_init @ 0x08283168 - the same
      `strb r1,[r0]; strb r2,[r0,#1]; strh r3,[r0,#2]; bx lr` body
      writing the same 4-byte parser result record (status +0x00, code
      +0x01, detail LE u16 +0x02) and returning out in the preserved
      r0. 42 call sites binary-scanned by decoding every B/BL word in
      osos.dec: 37 bl (7 conditional - 6 blne, 1 bleq) plus 5 b (3
      unconditional, beq @ 0x080f8830, bne @ 0x080f8be0). The
      call-site evidence shows NO semantic split from 0x08283168: the
      same record-resource parser cluster (0x080f8xxx-0x080fdxxx)
      calls this symbol where siblings call the canonical one,
      including the signature error rewrites ({2, 2, 0x3a00} via the
      beq tail @ 0x080f8830, {2, 5, <pool constant>} via the bne tail
      @ 0x080f8be0) and the zero-initializations; the one
      out-of-cluster site tail-writes an error record {2, 3, detail}
      from a flags decode @ 0x080a8a74. ADS simply emitted the
      constructor twice inside the class's translation unit
      (0x08283120-0x08283178). Ported as an independent exported body
      (the handle_deref_or_null_alias_64f4 naming precedent) rather
      than a delegation - the original is a second copy, not a branch.
      Release-build note: LLVM MergeFunctions folds the two identical
      bodies into the shared .text.parse_result_init section;
      parse_result_init_alias_3134 stays in the archive symbol table
      at the merged address, so hooks resolve it normally - match.py
      is therefore run as `match.py 0x08283134 parse_result_init`,
      which shows the store triple matching instruction-for-instruction
      (diff: the crate-standard fp prologue). Host tests: same layout
      coverage as the canonical - byte/byte/LE-u16 offsets with a
      guard byte, all-zero, all-ones, and the cluster's {2, 5, 0x3a00}
      error shape.

  - address: 0x08283144
    ghidra: FUN_08283144
    name: parse_result_is_ok
    size: 16
    sig: "u32 parse_result_is_ok(const u8 *record)"
    module: app/parse_result
    status: ported
    renames:
      param_1: record
    notes: >-
      Exact 16-byte extent from 0x08283144 through 0x08283153, bounded
      by the alias constructor's bx lr at 0x08283140 and clear() at
      0x08283154; no literal pool. Raw ARM:
      `ldrb r0,[r0]; rsbs r0,r0,#1; movcc r0,#0; bx lr`. It returns one
      exactly when the parser-result status byte is one, zero for status
      zero and every value >= two, and neither writes the record nor
      NULL-checks it. Every B/BL word in osos.dec was decoded: 25 direct
      `bl` sites, all unconditional, and 0 tail `b` sites. Each caller
      gates the success consumer with `bl; cmp r0,#0; beq <bail>` (for
      example, 0x080f8d74); no predicated calls means the status gate
      itself, not call conditionality, determines the path. The record
      layout is established by sibling parse_result_init: status is u8
      +0x00, code u8 +0x01, detail u16 +0x02. Deviation: none; Rust's
      equality comparison represents the ARM carry-flag sequence and
      returns its 0/1 result as u32. Host tests cover cleared, successful,
      observed error (2), high-byte error (0xff), and nonmutation.

  - address: 0x08283178
    ghidra: FUN_08283178
    name: parse_result_destroy
    size: 4
    sig: "u8 *parse_result_destroy(u8 *record)"
    module: app/parse_result
    status: ported
    renames:
      param_1: record
    notes: >-
      The trivial destructor of the 4-byte parser result record whose
      constructors are parse_result_init @ 0x08283168 and its twin @
      0x08283134 - the last function of that class's translation unit
      (0x08283120-0x08283178). The whole function is one instruction,
      0xe12fff1e = `bx lr`, decoded from the raw word. NOT a veneer: a
      veneer is 0xe51ff004 (`ldr pc,[pc,#-4]`) plus a target word, or a
      plain `b <target>`, and neither is present. The record holds two
      u8s and a u16 and owns nothing, so scope exit has nothing to
      release, but ADS still emits and calls the destructor - hence 46
      real call sites. Extent exact on both sides, 4 bytes: 0x08283174
      is the closing `bx lr` of parse_result_init and 0x0828317c is the
      `push {r4,r5,r6,r7,r8,lr}` of the next function; no literal pool.
      Call sites - 46 bl, ALL unconditional, and 0 b, binary-scanned by
      decoding every B/BL word in osos.dec. No DATA word in the image
      holds 0x08283178, so it is never dispatched virtually - every
      caller binds it statically, which is what a compiler does for a
      known-type destructor. 44 of the 46 sit in the record-resource
      parser cluster 0x080f8xxx-0x080fdxxx in the canonical scope-exit
      shape `add r0, sp, #N; bl 0x08283178`, destroying a record the
      parser built on its own frame (e.g. 0x080f8b60, 0x080f8d68,
      0x080fd9a4); the other two are 0x08160a58 (same stack shape) and
      0x081d5e5c.
      r0 passes through, and one caller depends on it - `bx lr` leaves
      r0 untouched, so the function returns its argument. The outer
      destructor @ 0x081d5e54 proves that is load-bearing rather than
      cosmetic: `push {r4,lr}; add r0,r0,#0x34; bl 0x08283178;
      sub r0,r0,#0x34; pop {r4,pc}` - it destroys an embedded record at
      member offset +0x34 and rebases r0 to return its own `this`,
      which only works if r0 survives the call. A void port would
      compile to the same `bx lr` today but document the wrong
      contract, so the signature returns record - the same reading
      cxx/trivial_destructor records for 0x082646ac.
      Deviations - none behaviorally; the port is the identity
      function. It reads and writes nothing, so record may be NULL,
      unaligned or dangling, and the original's missing NULL guard is
      not a latent fault.
      Release-build note - the body is byte-identical to the image's
      other empty destructors, so LLVM MergeFunctions folds it into the
      shared .text.context_scope_drop section together with
      trivial_destructor; parse_result_destroy stays in the archive
      symbol table at the merged address, so hooks resolve it normally.
      match.py is therefore run as `match.py 0x08283178
      context_scope_drop` (the parse_result_init_alias_3134 precedent),
      which shows the body empty on both sides - the only diff is the
      crate-standard fp prologue/epilogue (push {fp,lr} / mov fp,sp /
      pop {fp,pc}) that every small port in this crate carries.
      Host tests (3) - destroying a filled record leaves every byte
      untouched, the +0x34 embedded-member rebase from 0x081d5e54
      round-trips through the returned pointer, and NULL and an
      unaligned record both pass straight through.

  - address: 0x08264214
    ghidra: FUN_08264214
    name: draw_state_move_to
    size: 8
    sig: "void draw_state_move_to(u8 *draw_state, i32 x, i32 y)"
    module: cxx/draw_state_line
    status: ported
    renames:
      param_1/r0: draw_state (the 0x44-byte scoped draw-state record)
      param_2/r1: x (local current-point horizontal coordinate)
      param_3/r2: y (local current-point vertical coordinate)
    notes: >-
      Whole function binary-verified: 0x08264214 begins `stm r0,{r1,r2}`
      and the next function begins at 0x0826421c with `ldm r1,{r2,r3}`;
      Ghidra's 8-byte extent has no swallowed literal pool or sibling.
      Raw ARM is then `bx lr`: it replaces the scoped draw-state record's
      consecutive current-point words +0/+4 with local x/y, without
      drawing, coordinate translation, or validation. This is the
      `move_to` sibling of draw_state_line_to @ 0x082641a4: the latter
      reads these words as its starting point, draws, then overwrites them
      with its endpoint. Every direct B/BL word in osos.dec was decoded:
      12 direct bl call sites, all unconditional, zero predicated forms,
      and zero tail b calls (sites 0x080906fc, 0x081285c8, 0x08128608,
      0x08140f6c, 0x08143238, 0x08148c48, 0x081b3278, 0x081ca6b8,
      0x0820de20, 0x08285f30, 0x08285f64, 0x08290c18). No deliberate
      deviations. Host tests prove signed-extreme full-width writes, both
      coordinates overwritten across consecutive calls, and all remaining
      record and guard bytes untouched. match.py structural review is run
      against draw_state_move_to; the expected diff is the crate frame
      around the two stores and return.
  - address: 0x0826412c
    ghidra: FUN_0826412c
    name: draw_state_line
    size: 120
    sig: "void draw_state_line(u8 *draw_state, i32 x1, i32 y1, i32 x2, i32 y2)"
    module: cxx/draw_state_line
    status: ported
    renames:
      param_1/r0: draw_state (the 0x44-byte scoped draw-state record)
      param_2/r1: x1 (first local endpoint x)
      param_3/r2: y1 (first local endpoint y)
      param_4/r3: x2 (second local endpoint x)
      param_5/[sp]: y2 (second local endpoint y)
      FUN_080e7870: draw_line_engine (unported dispatcher)
    notes: >-
      Whole 120-byte function binary-verified: 0x0826412c begins
      `push {r4,r5,r6,r7,lr}` and its post-call `stm r4,{r5,r6}` /
      `pop {r4,r5,r6,r7,pc}` ends at 0x082641a4, the distinct next
      function's prologue. 39 unconditional bl call sites, 0 predicated
      bl and 0 b, verified by decoding every ARM B/BL word in osos.dec;
      no DATA word holds 0x0826412c, so it is not virtually dispatched.
      The line-drawing member of the scoped 0x44-byte draw-state record:
      it translates both local endpoints by origin words +0x2c/+0x30,
      then calls the unported dispatcher 0x080e7870 with surface body
      `*(this+0x1c)+4`, translated x1/y1/x2/y2, thickness 1, foreground
      colour +0x11, style byte +0x10, embedded clip rect +0x34 and
      scaled selector 0. It finally stores the raw second local endpoint
      to current-point words +0/+4 AFTER that call. The sibling at
      0x082641a4 uses current point +0/+4 as its starting endpoint; this
      explicit-four-coordinate form and 0x0826343c's four-call rectangle
      outline establish the `draw_state_line` name. The callee is a
      binary-decoded dispatcher: because this caller's selector is zero,
      it always selects its integer Bresenham engine 0x080f2efc, never
      the 16.16 path 0x080ea434. Deviation: 0x080e7870 is unported and
      rides DRAW_STATE_LINE_OPS; the device default calls that exact
      address and host tests install a recorder. The surface is read as
      the record's fixed 32-bit word, rather than a host pointer-width
      field, so the host does not corrupt or read through the embedded
      member at +0x20. Host tests (5) prove all ten engine operands,
      old current-point visibility during the call, post-call endpoint
      update, wrapping coordinate addition, degenerate endpoints, and
      untouched record/guard bytes. match.py structural review is run
      against this symbol; it should differ only in the crate frame and
      the volatile dispatch seam.

  - address: 0x082643c0
    ghidra: FUN_082643c0
    name: draw_state_fill_rect_background
    size: 112
    sig: "void draw_state_fill_rect_background(u8 *this, const Rect *rect)"
    module: cxx/draw_state_fill
    status: ported
    renames:
      param_1/r0: this (the 0x44-byte scoped draw-state record)
      param_2/r1: rect (record-local bounds, QuickDraw top/left/bottom/right)
      local_14/local_10/local_c/local_8: adjusted (the origin-translated stack rect)
      FUN_08074898: fill_rect_engine (unported; IRAM-mirror resident)
    notes: >-
      Whole 112-byte function binary-verified: 0x082643c0 begins `mov
      r2,r0; push {lr}` and 0x08264430 begins the distinct sibling's
      `push {lr}`; no literal pool lies between. 22 unconditional bl
      call sites, 0 predicated bl and 0 b, verified by decoding every
      ARM B/BL word in osos.dec; no DATA word in the image holds
      0x082643c0, so it is never dispatched virtually. The
      background-fill member of the scoped 0x44-byte draw-state record
      (cxx/draw_state.rs): copies the caller's four-word rect onto its
      stack translated by the record's origin (top/bottom += +0x30
      origin_y, left/right += +0x2c origin_x - the QuickDraw field
      order of ui/rect.rs is what fixes which origin word is which,
      matching cxx/draw_state_line.rs and the identical mapping in the
      sibling @ 0x08264430 and the bitmap draw helper @ 0x08262bdc),
      then calls the unported rect-fill engine @ 0x08074898 with
      surface body `*(this+0x1c)+4`, the translated rect, the
      BACKGROUND colour pointer this+0x15 (opaque white by default -
      cxx/draw_state_color.rs's three-way evidence), a hard-zero style
      word (`mov r3,#0` - solid, never the engine's 0x22 alpha-blend
      path) and the embedded clip rect this+0x34 as the stack-carried
      fifth argument. It is the erase-before-draw primitive: the +0x15
      background setter FUN_08263194 has exactly the same 22-site call
      count and sampled sites (0x0815c2b0, 0x08186730) set the
      background and immediately fill. The engine @ 0x08074898 lives
      below 0x0800aed8, so its body is readable through the 0x22000000
      IRAM mirror: it converts the colour to the surface's pixel
      format (helper 0x08079c0c keyed on the surface +0x10 halfword),
      intersects with the clip (0x8075e90) and fills. Return: the
      epilogue `pop {r0,r1,r2,r3,ip,pc}` leaves r0 = this+0x34 and r1
      = adjusted.top, but every sampled call site (0x0808da10,
      0x0816b3cc, 0x082917d0) clobbers or reloads r0 with its next
      instruction, so the port is void (the string_id_record.rs
      precedent). Ghidra's `undefined8` return and its
      CONCAT44(local_14, param_1+0x34) are that dead register pair.
      Deviations: the engine rides the new DRAW_STATE_FILL_OPS
      dispatch slot (the draw_state_line.rs seam pattern); the target
      default transmutes 0x08074898, the host default panics until
      the engine is independently ported, so this is NOT hook-ready
      with the host default. The +0x1c surface word is read as a u32,
      not a host pointer-width field, so a 64-bit host does not spill
      into the +0x20/+0x24 embedded pair. All record and rect offsets
      are word-aligned, so the port uses plain aligned loads. Host
      tests (5) prove the translated rect and all five engine operands
      (surface word + 4, background pointer this+0x15, style 0, clip
      pointer this+0x34), the vertical/horizontal origin split with
      all-distinct edges, wrapping ARM-add translation, a degenerate
      empty rect forwarded unchecked, and that the record, the caller's
      rect and guard bytes all survive the call unwritten. match.py
      structural review (exit 1, diff expected): 28 instructions on
      both sides with identical dataflow - four ldr/add offset pairs,
      surface +4, clip pointer pushed as the stack argument, colour
      this+0x15, style 0 - the diff is the crate fp frame and the
      blx-through-volatile-slot dispatch standing in for the direct
      bl.

  - address: 0x082644a8
    ghidra: FUN_082644a8
    name: draw_state_fill_rect_foreground
    size: 112
    sig: "void draw_state_fill_rect_foreground(u8 *this, const Rect *rect)"
    module: cxx/draw_state_fill
    status: ported
    renames:
      param_1/r0: this (the 0x44-byte scoped draw-state record)
      param_2/r1: rect (record-local bounds, QuickDraw top/left/bottom/right)
      local_14/local_10/local_c/local_8: adjusted (the origin-translated stack rect)
      FUN_08074898: fill_rect_engine (unported; IRAM-mirror resident)
    notes: >-
      Whole 112-byte function binary-verified: 0x082644a8 begins `mov
      r2,r0; push {lr}` and 0x08264518 begins the distinct sibling's
      `push {r0,r1,r2,r3,r4,r5,r6,lr}`; no literal pool lies between.
      18 unconditional bl call sites, 0 predicated bl and 0 b, verified
      by decoding every ARM B/BL word in osos.dec; no DATA word in the
      image holds 0x082644a8, so it is never dispatched virtually. The
      foreground-fill member of the scoped 0x44-byte draw-state record
      translates the caller's four-word QuickDraw rect on its stack:
      top/bottom += origin_y at +0x30 and left/right += origin_x at
      +0x2c, with the native wrapping add semantics. It calls the
      unported rect-fill engine @ 0x08074898 with surface body
      `*(this+0x1c)+4`, adjusted rect, foreground colour pointer
      this+0x11, zero-extended style byte this+0x10 (`ldrb r3,[r2,#16]`)
      and embedded clip rect this+0x34 as the fifth stack argument.
      This differs from the 0x082643c0 background sibling only in
      colour source and its dynamic style rather than a hard-zero solid
      style; a 0x22 style can therefore select the engine alpha-blend
      path. Ghidra's undefined8 return is dead: `pop
      {r0,r1,r2,r3,ip,pc}` leaves incidental registers, and sampled
      callers immediately clobber or reload r0, so the Rust port is
      void. Deviation: it reuses the existing volatile
      DRAW_STATE_FILL_OPS dispatch seam because the engine is unported;
      target builds call 0x08074898 and host tests install a recorder.
      Its fixed 32-bit surface field is read as u32, never host usize,
      preserving the +0x20 following field. Host tests (3) prove
      foreground/style operands including 0xff zero extension and 0x22
      blend selection, all-distinct translated edges and ARM wrapping,
      and empty-rect plus record/input non-mutation.

  - address: 0x08264518
    ghidra: FUN_08264518
    name: surface_attach
    size: 56
    sig: "void surface_attach(u8 *record, const u8 *surface)"
    module: cxx/draw_state_surface
    status: ported
    renames:
      param_1/r0: record (the 0x44-byte scoped draw-state record)
      param_2/r1: surface (the draw-target surface descriptor)
      FUN_0826c2e8: rect_move_to_origin (already ported in ui/rect)
    notes: >-
      Whole 56-byte function binary-verified: 0x08264518 begins `push
      {r0,r1,r2,r3,r4,r5,r6,lr}` and 0x08264550 begins the distinct
      sibling setter's `str r1,[r0,#28]!`; no literal pool lies
      between. 16 unconditional bl call sites, 0 predicated bl and 2
      tail b (0x081b82ec, 0x081b83ac), verified by decoding every ARM
      B/BL word in osos.dec; no DATA word in the image holds
      0x08264518, so it is never dispatched virtually. The
      surface-attaching member of the 0x44-byte draw-state record:
      copies the surface descriptor's +0x98 bounds rect (ldm of four
      words) onto a 16-byte stack scratch formed by the push's
      argument spill, origin-moves THAT COPY with the already-ported
      rect_move_to_origin @ 0x0826c2e8 (bl; the extra r1=surface
      argument is dead - the callee reads only r0), stores the surface
      identity as one raw 32-bit word at +0x1c, then stores the
      origin-moved copy into the record's clip rect at +0x34..+0x40.
      The surface's own +0x98 bounds are never mutated (the sibling
      setter 0x08264550, by contrast, stores a caller-computed rect
      verbatim with no origin move). No NULL guard on either argument,
      matching the all-unconditional call sites. Ghidra's undefined8
      return is dead: `pop {r0,r1,r2,r3,...,pc}` restores the argument
      spill, so r0 only incidentally holds `record`; sampled callers
      reload or clobber it - the port is void. Deviations: void
      return; aligned Rect loads/stores (+0x98 and +0x34 are
      word-aligned offsets), not read_unaligned. Host tests (4) prove
      the +0x1c identity store and origin-moved +0x34 clip with
      all-distinct edges, source-bounds non-mutation, ARM wrapping
      subtraction on inverted bounds, and verbatim copy of an
      already-origin rect. match.py structural: identical dataflow -
      four-word copy from surface+0x98 to stack, bl
      rect_move_to_origin, str at +0x1c, stm at +0x34; the diff is the
      crate fp frame and per-word ldr in place of the original's ldm.

  - address: 0x08264570
    ghidra: FUN_08264570
    name: draw_state_construct_with_surface
    size: 48
    sig: "u8 *draw_state_construct_with_surface(u8 *this, usize surface)"
    module: cxx/draw_state
    status: ported
    renames:
      param_1/r0: this (the 0x44-byte scoped draw-state record)
      param_2/r1: surface (caller-selected draw-target descriptor identity)
      iVar1/r4: this (member constructor return minus 0x20)
      FUN_081598a4: embedded_pair_construct
      FUN_082630f0: body_init
      FUN_08264518: surface_attach
    notes: >-
      Whole 48-byte function binary-verified: 0x08264570 begins `push
      {r4,r5,r6,lr}` and 0x082645a0 begins the distinct sibling's `push`;
      no literal pool lies between. It has 35 unconditional bl call sites,
      0 predicated bl and 0 b, verified by decoding every ARM B/BL word in
      osos.dec; no DATA word in the image holds 0x08264570, so every caller
      binds it statically rather than through a virtual dispatch. This is
      the caller-selected-surface constructor for the scoped 0x44-byte
      draw-state record: it saves r1, constructs the embedded pair at
      this+0x20, derives this from that constructor's r0 return minus
      0x20, initializes the body, then calls surface_attach with the saved
      surface and returns the derived this. It differs from
      draw_state_construct @ 0x0826467c only in taking the surface from r1
      instead of its 0x08a77c3c literal-pool default. The shared
      DRAW_STATE_CONSTRUCT_OPS seam already exists for its three unported
      callees: the pair constructor default is faithful, while body_init
      and surface_attach are documented partial defaults. This wrapper
      deliberately shares that seam rather than adding a duplicate; it is
      not hook-ready until those callees are ported. Host tests (3) prove
      initializer order with the supplied surface, r0-return-derived base
      propagation, and the current default stubs' exact record and guard
      bytes.

  - address: 0x082645e8
    ghidra: FUN_082645e8
    name: draw_state_copy_construct
    size: 148
    sig: "u8 *draw_state_copy_construct(u8 *this, const u8 *source)"
    module: cxx/draw_state
    status: ported
    renames:
      param_1/r0: this (destination scoped draw-state record)
      param_2/r1: source (source scoped draw-state record)
      r4: this (embedded pair constructor return minus 0x20)
      r5: source (preserved across member and colour copy calls)
      FUN_081598a4: embedded_pair_construct
      FUN_082720e8: color_copy
      FUN_082724b4: unnamed two-word copy leaf
    notes: >-
      Whole 148-byte function binary-verified: 0x082645e8 begins `push
      {r4,r5,r6,lr}` and 0x0826467c begins the distinct default-surface
      constructor; no literal pool lies between. Decoding every ARM B/BL
      word in osos.dec found exactly 11 unconditional bl call sites
      (0x081416c4, 0x0814170c, 0x081418cc, 0x08141930, 0x08141984,
      0x081419e0, 0x08141a24, 0x08143104, 0x0820dd8c, 0x0820fa18,
      0x08213508), zero predicated bl and zero b. No aligned DATA word in
      the image holds 0x082645e8, so callers bind it statically rather than
      dispatching it virtually. This copy constructor first constructs the
      embedded two-word member at this+0x20, derives this from its r0 return
      minus 0x20, then copies the scalar words, two two-word members, style
      byte, two unaligned RGBA colours, and four-word clip rectangle from
      source. The clip's four source words are all loaded before any clip
      destination store, exactly matching `ldm` then `stm`. The 12-byte
      unported helper at 0x082724b4 is only a raw-decoded two-word copy
      leaf, so it is expanded inline without assigning it an identity; the
      independently ported color_copy preserves the unaligned byte-copy
      calls. Deliberate deviation: the existing DRAW_STATE_CONSTRUCT_OPS
      embedded-pair seam is shared instead of duplicated. Its device
      default is faithful; the host tests install a u32-word faithful
      fixture to avoid the existing constructor stub's pointer-width model.
      Host tests (2) prove exact field copies with both padding ranges and
      a post-record guard preserved, source non-mutation, and propagation
      of a shifted member-constructor return into the copy destination.
  - address: 0x0826467c
    ghidra: FUN_0826467c
    name: draw_state_construct
    size: 44
    sig: "u8 *draw_state_construct(u8 *this)"
    module: cxx/draw_state
    status: ported
    renames:
      param_1: this
      iVar1: this (recovered from the member ctor's return minus 0x20)
      FUN_081598a4: embedded_pair_construct
      FUN_082630f0: body_init
      FUN_08264518: surface_attach
      DAT_082646a8: DRAW_STATE_DEFAULT_SURFACE_ADDRESS (0x08a77c3c, binary-verified)
    notes: >-
      The constructor of the scoped 0x44-byte draw-state record every
      drawing call builds on its stack, fills, hands to a draw routine,
      and destroys at scope exit through trivial_destructor @ 0x082646ac
      (the module sibling; call-site pair 0x0810245c..0x081024ec). 50 bl
      call sites, 0 b, binary-scanned. Class unidentified; name is
      evidence-based. The consumers establish the layout: 0x0826e250 /
      0x0826eb5c store the render context's +0x5c bitmap surface at
      +0x1c and a clip rect at +0x34 (setter 0x08264550), 0x08262d70
      writes the style byte at +0x10, and the draw call 0x08262bdc
      offsets two rects by the record's +0x2c/+0x30 origin and invokes
      the text/layout draw engine 0x080f1600 with the surface pointers
      *(this+0x1c)+4, the two colors at +0x11/+0x15, the style byte and
      the clip rect. The 44 bytes: `add r0,r0,#0x20; bl 0x081598a4`
      (the embedded two-word member ctor at +0x20 - 12 bytes, fully
      decoded: zero two words, return r0), `sub r4,r0,#0x20` (recover
      this from the callee's RETURN, not the entry argument - the
      string_id_record.rs return-derivation contract, host-tested with
      a shifted canned return), `bl 0x082630f0` (body init: +0/+4 = 0,
      +8/+0xc = 1, style byte and first color 0, five 0xff bytes
      +0x14..+0x18, +0x20/+0x24 re-copied from a global pair,
      +0x28..+0x43 = 0), `ldr r1,[0x82646a8]` (the default draw-target
      surface descriptor 0x08a77c3c, an RW-region address identity
      carrying the default clip rect at its own +0x98), `bl 0x08264518`
      (stores the surface at +0x1c and copies its +0x98 rect,
      origin-moved by the ported rect_move_to_origin @ 0x0826c2e8, into
      +0x34..+0x40), `mov r0,r4` return. Deviations (the settings.rs
      SETTINGS_CTOR pattern): the three callees ride the
      DRAW_STATE_CONSTRUCT_OPS dispatch slots; the
      embedded_pair_construct default is faithful (the callee is fully
      decoded), the body_init default is a documented zeroing stub, the
      surface_attach default stores only the surface identity at +0x1c
      (pointer-sized on host, the pfr_face_done face-word model) and
      skips the rect copy a host cannot reproduce - NOT HOOK-READY
      until the two larger callees are ported and wired in. Host tests:
      chain order (pair at this+0x20, body, surface with the default
      surface address), the return-minus-0x20 recovery under a shifted
      member-ctor return, and the exact final bytes under the default
      stubs (zeroed record, surface identity at +0x1c, guard bytes
      intact). match.py structural: identical dataflow - add #0x20,
      call, sub #0x20, call, literal 0x08a77c3c in the Rust pool, call,
      mov r0,r4; the diff is the crate fp frame and the three
      blx-through-volatile-slot dispatches standing in for the direct
      bl's.

  - address: 0x082646ac
    ghidra: FUN_082646ac
    name: trivial_destructor
    size: 4
    sig: "void *trivial_destructor(void *this)"
    module: cxx/trivial_destructor
    status: ported
    notes: >-
      A statically bound shared EMPTY destructor body - one instruction,
      `bx lr`. Extent binary-verified: 0x082646a8 is the literal pool of
      the ctor @ 0x0826467c and 0x082646b0 starts the next function, so
      the 4-byte size is right and there is no trailing literal word. Not
      a veneer - no `ldr pc,[pc,#-4]`, no `b`.
      112 `bl` call sites and 0 `b`, binary-scanned by decoding every
      B/BL word in osos.dec (range 0x08090754..0x0829869c), plus ZERO
      occurrences of the word 0x082646ac anywhere in the image, so it is
      never reached through a vtable: every caller binds it statically,
      as a compiler does for a known-type destructor. The sites are two
      destructor shapes - 79 are `add r0, sp, #N` immediately before the
      `bl` (a stack temporary going out of scope) and 24 are immediately
      followed by `bl 0x082aad24` (operator_delete), the destroy-then-free
      pair that names.yaml already records from the other side at the view
      destructor 0x0819ba64 ("0x082646ac then operator delete").
      Load-bearing detail: `bx lr` leaves r0 untouched, and the
      destroy-then-free sites rely on it - e.g. 0x081023d0 does
      `ldr r0,[r0,#0xa8]; cmp r0,#0; beq +; bl 0x082646ac; bl 0x082aad24`,
      so operator_delete consumes the pointer this function passed
      through. The port therefore returns `this` rather than being void.
      Reads and writes nothing, so a NULL, unaligned or dangling `this`
      is fine. Deviations: none behaviorally. match.py structural: the
      body is empty in both; the port adds only the crate's fp frame
      (push {fp,lr}/mov fp,sp/pop {fp,pc}) around the same no-op, with r0
      untouched on every path.

  - address: 0x08262d70
    ghidra: FUN_08262d70
    name: draw_state_set_style
    size: 8
    sig: "void draw_state_set_style(u8 *record, u8 style)"
    module: cxx/draw_state_style
    status: ported
    notes: >-
      The style-byte setter of the 0x44-byte draw-state record
      (cxx/draw_state.rs): one instruction, `strb r1, [r0, #16]`, then
      `bx lr`. Whole function decoded from the raw words (1010 c0e5 /
      1eff 2fe1). Not a veneer (no `ldr pc,[pc,#-4]` + target word) and
      not an empty destructor (a real store, not a lone `bx lr`).
      Ghidra OMITS the function from its C listing entirely - only its
      callers appear - so the raw bytes are the sole authority. The next
      function starts at 0x08262d78 (a sibling two-word setter,
      `str r1,[r0,#8]; str r2,[r0,#12]; bx lr`, unported), confirming
      the 8-byte extent with no trailing literal word. Call count
      binary-verified by decoding every B/BL word in osos.dec: 36 `bl`
      + 2 `blne` = 38, exactly Ghidra's count. No DATA word in the
      image holds 0x08262d70, so it is never dispatched virtually.
      The record packs the style byte at +0x10 immediately ahead of the
      two unaligned 4-byte colours at +0x11/+0x15
      (cxx/draw_state_color.rs); the draw call @ 0x08262bdc passes the
      byte to the text/layout draw engine @ 0x080f1600 with both
      colours and the clip rect. The 2 `blne` sites (0x0826cd10 /
      0x0826cd3c) guard on a source byte != 0xff before storing 0x22 or
      a computed code - 0xff is a CALLER-side "no style override"
      sentinel; the callee itself has no sentinel check and no NULL
      guard, and stores whatever byte it is handed. Observed style
      codes: 0x21 ("a colour", the list-row path @ 0x0819b80c then also
      calls the foreground setter) and 0x22. The original never writes
      r0, so no caller can depend on a return value and the port is
      void. Deviations: none behaviorally. match.py structural: the
      core `strb r1,[r0,#16]` matches; the port adds only the crate's
      usual fp frame (push {fp,lr}/mov fp,sp/pop {fp,pc}), same as the
      three_word_clear leaf setters. Host tests (4): the exact byte at
      +0x10 with the rest of the record proven untouched (both colours
      included), all byte values incl. 0x00/0x21/0x22/0xff,
      base-alignment agnosticism, and a seeded-record preservation
      check.

  - address: 0x0827196c
    ghidra: FUN_0827196c
    name: observable_array_append
    size: 144
    sig: "u32 observable_array_append(ObservableArray *this, void *element)"
    module: cxx/observable_array
    status: ported
    renames:
      param_1/r0: this (the concrete observable array)
      param_2/r1: element (opaque value representation forwarded to the concrete writer)
      iVar1/r5: index (the pre-growth element count, returned to the caller)
      FUN_082a4ca0: observable_array_notify_append (unported observer broadcast)
    notes: >-
      Extent is binary-verified: 0x0827196c begins `push {r4,r5,r6,lr}`,
      0x082719f8 is its `pop {r4,r5,r6,pc}`, and the distinct next
      function is the `bx lr` at 0x082719fc, so Ghidra's 144 bytes is
      correct with no literal pool. Decoding every ARM B/BL word in
      osos.dec finds exactly 19 direct call sites: 17 unconditional `bl`
      and 2 caller-guarded `bleq` (0x0810aab8 and 0x0810ffe4); no
      word-aligned DATA occurrence holds 0x0827196c, so it is never a
      virtual target. This appends one opaque element to a concrete
      observable array. Slot +0x60 decides whether append is deferred; its
      nonzero branch tail-dispatches slot +0x20 with r1=0x7fffffff and
      returns that result. Otherwise it snapshots +0x04 length, calls
      +0xbc with one, calls +0xa8 with the old index and element, calls
      0x082a4ca0 to notify every observer of that index, calls +0x88 with
      zero, and returns the old index. The slot targets are runtime
      concrete-vtable data, so none is assigned an invented callee
      identity. The direct helper is separately decoded as a 40-byte
      observer-list walk calling 0x08155cc8, but is intentionally not
      ported by this one-function change: the target seam reaches its
      retailOS address; host tests install a recorder. The host vtable
      fixture uses native function pointers rather than the target's u32
      vtable word; it proves both branches, including the deferred
      0x7fffffff sentinel, `u32::MAX` old-index wrap edge, exact call
      order, return values, and no wrapper-owned length mutation.
  - address: 0x08271c84
    ghidra: FUN_08271c84
    name: observable_array_clear
    size: 20
    sig: "void observable_array_clear(ObservableArray *this)"
    module: cxx/observable_array
    vars: {param_1: this}
    status: ported
    notes: >-
      Clears a concrete observable array by tail-dispatching its signed
      element-removal vtable slot with the wrapping negative of its current
      length. EXTENT - raw osos.dec words decode as `ldr r2,[r0]` /
      `ldr r1,[r0,#4]` / `ldr r2,[r2,#0xbc]` / `rsb r1,r1,#0` / `bx r2`;
      the next independent function starts at 0x08271c98, so the true extent
      is exactly 20 bytes (0x08271c84..0x08271c94) with no literal pool.
      Ghidra's 20-byte extent is right, though its C loses the tail-call
      property. CALL SITES - 19 `bl` plus 57 direct `b`, 76 total, all
      unconditional, binary-verified by decoding every ARM B/BL word in
      osos.dec; there are no word-aligned DATA occurrences of 0x08271c84.
      The 19 `bl` matches Ghidra's reported direct-call count. The wrapper
      has no NULL guard: callers must supply a readable object, vtable, and
      `+0xbc` slot. The default observable-array vtable 0x089a5d0c has a
      zero word at +0xbc in the static decrypted image, consistently with
      the abstract base documented by cxx/observable_array; the actual
      dispatch target is runtime concrete-vtable data and is intentionally
      not assigned an invented identity. `match.py` against the worktree ARM
      archive is an instruction-for-instruction exact match. Host tests
      model the wider host vtable pointer structurally and prove one call
      with `(this, -len)` for zero, one, INT_MAX, INT_MIN, and UINT_MAX
      lengths; INT_MIN uses ARM's wrapping negation. Deviations: host-only
      structural vtable model, required because firmware vtable words are
      32-bit while host function pointers are wider.

  - address: 0x08271c98
    ghidra: FUN_08271c98
    name: observable_array_copy_construct
    size: 84
    sig: "ObservableArray *observable_array_copy_construct(ObservableArray *destination, const ObservableArray *source)"
    module: cxx/observable_array
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: source
      puVar1/r4: constructed_destination
      iVar2/r1: source_element_count
      DAT_08271ce8: OBSERVABLE_ARRAY_VTABLE
      FUN_082718a4: observable_array_grow (unported direct target)
      thunk_EXT_FUN_220000d4: rom_memmove
    notes: >-
      True extent is 84 bytes, not Ghidra's 80: 80 instruction bytes from
      0x08271c98 through the `pop {r4,r5,r6,pc}` at 0x08271ce4, then the
      shared 4-byte observable-array vtable literal 0x089a5d0c at
      0x08271ce8; the separate sibling constructor begins at 0x08271cec.
      Decoding every word in osos.dec as ARM B/BL finds 15 direct inbound
      branches: 13 unconditional `bl` (0x080fe8e4, 0x0810b398, 0x0813ef58,
      0x08156014, 0x081d5efc, 0x081d5f08, 0x081fa664, 0x081fa758,
      0x081fa834, 0x081faa00, 0x081fab6c, 0x0820bd18, 0x0827dff8) and two
      unconditional tail `b` (0x0819e460, 0x0820bd28); no calls are
      predicated. It default-constructs the destination directly through
      framework_object_construct, grows it by source +0x04 using the
      unported direct helper 0x082718a4, reloads source +0x04 into
      destination +0x04, then copies `count << 2` bytes from source +0x08
      to destination +0x08 through the 0x08037e00 ROM memmove veneer. Ghidra
      incorrectly drops the destination parameter: raw r0 reaches the base
      constructor and every destination store. The seam for 0x082718a4 is
      wired to its verified target address on device and a host allocation
      model in tests; its observed virtual-growth role is documented without
      inventing a callee identity. Host tests prove zero, one, and five
      element copies, exact storage byte span, source preservation, guard
      integrity, result r0, and the unconditional growth call. match.py
      structural review of the worktree archive found the same constructor /
      grow / reload-count / memmove control flow; LLVM adds an fp frame and
      indirect seam load, and resolves memmove through __aeabi_memmove.

  - address: 0x08271cec
    ghidra: FUN_08271cec
    name: observable_array_construct
    size: 36
    sig: "ObservableArray *observable_array_construct(ObservableArray *this)"
    module: cxx/observable_array
    status: ported
    vars: {puVar1: this, DAT_08271d10: OBSERVABLE_ARRAY_VTABLE, FUN_08275bb8: framework_object_construct}
    notes: >-
      The default constructor of retailOS's OBSERVABLE ARRAY - a
      polymorphic, growable array of 32-bit elements that broadcasts
      changes to a list of attached observers. Extent binary-verified as
      32 bytes of code (0x08271cec..0x08271d0c) plus the 4-byte vtable
      literal 0x089a5d0c @ 0x08271d10, so Ghidra's 36 is right and does
      include the literal. 82 `bl` call sites and 0 `b`, binary-scanned by
      decoding every B/BL word in osos.dec (0x0807f7d8..0x083d0e64).
      Body - `bl 0x08275bb8` (the root base ctor), then plant 0x089a5d0c
      at +0x00 and zero +0x04/+0x08/+0x0c, returning this in r0.
      Class identified from its three siblings, all binding the same
      vtable literal (binary-verified at 0x08271ce8, 0x08271d10,
      0x08271d84) - the copy ctor ending at 0x08271ce4 grows by the
      source's +0x04 through 0x082718a4 then `lsl r2,r1,#2` /
      `ldr r1,[src,#8]` / `ldr r0,[this,#8]` / `bl 0x08037e00`
      (rom_memmove), so +0x04 is a count of 4-byte elements and +0x08
      their storage; the dtor @ 0x08271d2c broadcasts via 0x082a4ccc,
      drains the +0x0c list with 0x08271724, hands +0x08 to `free`
      (0x0802edc8, ported), zeroes +0x04/+0x08 and tail-branches to the
      root dtor 0x08275bc8, so +0x08 is owned heap storage; and
      0x082a4ccc is `for (n = this[+0xc]; n; n = n[+0x10])
      0x08155d30(n, arg)`, a broadcast over the observer list rooted at
      +0x0c - hence "observable". The growth helper 0x082718a4 rounds up
      to the granularity from vtable slot +0x70, compares with the
      capacity from +0xa0 and adjusts through +0xc0, all three NULL in
      0x089a5d0c, so the class is abstract and every concrete array is
      one of the 82 derived ctors' classes.
      Load-bearing - the four stores are addressed off the BASE CTOR'S
      RETURN (r0), and this function returns it; names.yaml already
      recorded the same from 0x0810f9f0's `sub r4, r0, #4`. The port
      returns `this` rather than being void.
      Earlier repo entries called this object the "drain state" after the
      one caller whose use of it is a drain (0x0811000c, 0x0810f9f0);
      same class. Both of those ports now call this one DIRECTLY instead
      of open-coding it (node_list) or failing through an injection seam
      (object_dispatch_entry, whose local DrainState model is replaced by
      ObservableArray).
      Deviations - none; same words, same order, same return register,
      and the vtable is a plain u32 constant since nothing in the crate
      dereferences it. match.py structural - identical
      bl/ldr-literal/str/mov#0/three-str body, differing only by the
      crate's fp frame and the literal being emitted after the body.

  - address: 0x08271d2c
    ghidra: FUN_08271d2c
    name: observable_array_destruct
    size: 92
    sig: "ObservableArray *observable_array_destruct(ObservableArray *this)"
    module: cxx/observable_array
    vars: {param_1: this, DAT_08271d84: OBSERVABLE_ARRAY_VTABLE, FUN_082a4ccc: OBSERVABLE_ARRAY_NOTIFY, FUN_08271724: OBSERVABLE_ARRAY_DETACH_OBSERVER, FUN_0802edc8: free}
    status: ported
    notes: >-
      The destructor of cxx/observable_array's class, the mirror of
      observable_array_construct @ 0x08271cec.
      EXTENT - Ghidra's 88 is WRONG, it drops the trailing literal-pool
      word. Binary-verified from osos.dec - 88 bytes of code
      (0x08271d2c..0x08271d80) plus the 4-byte vtable literal 0x089a5d0c
      @ 0x08271d84, and the next function starts at 0x08271d88
      (`push {r4, r5, r6, lr}`), so the true extent is 92.
      CALL SITES - 60 `bl` and 42 `b`, 102 total, binary-scanned by
      decoding every B/BL word in osos.dec. The 42 tail-branches are
      derived-class destructors chaining into their base.
      BODY - re-plant vtable 0x089a5d0c at +0x00; notify(this, 0) via
      0x082a4ccc over the still-intact observer list; drain
      `while ((head = this[+0x0c])) detach(this, head)` via 0x08271724;
      `if (this[+0x08]) free(this[+0x08])` via 0x0802edc8; zero +0x04 and
      +0x08; return this. The +0x0c word is never explicitly zeroed - the
      loop only exits once it is already 0 - and the vtable word is left
      planted.
      GHIDRA IS WRONG TWICE MORE, both load-bearing.
      decomp/c/026/08271d2c_FUN_08271d2c.c renders the free as
      `FUN_0802edc8()` with NO argument (the real `ldr r0,[r4,#8]; cmp
      r0,#0; blne 0x0802edc8` passes the element buffer), and renders the
      drain as `FUN_08271724(param_1)` with ONE argument when r1 is live
      across the loop - the `ldr r1,[r4,#0xc]` that tests the loop
      condition IS the next iteration's second argument, so the call is
      detach(this, head).
      The tail `b 0x08275bc8` into the root destructor is binary-verified
      a bare `bx lr` (already recorded from framework_object_construct's
      side), i.e. the identity on r0, so the port models it as the plain
      `return this` it is with no call. Callers depend on that return -
      the 42 `b` sites are derived destructors tail-chaining, and several
      `bl` sites feed r0 straight into operator_delete.
      SEAMS - 0x082a4ccc (broadcast, its own callee 0x08155d30 unported)
      rides OBSERVABLE_ARRAY_NOTIFY with a no-op default; 0x0802edc8 rides
      OBSERVABLE_ARRAY_FREE wired to the PORTED free in runtime/malloc_rt
      (indirected only so host tests can watch the pointer without routing
      a fixture address into the firmware heap - the SHUTDOWN_FREE /
      STDIO_FREE precedent). 0x08271724 rides OBSERVABLE_ARRAY_DETACH_OBSERVER
      whose default is NOT a no-op and must not be - this call site always
      passes the current head, so the only branch of 0x08271724 it can
      reach is the head match `owner[+0x0c] = target[+0x10]`, and that
      store is what makes the drain terminate; a no-op default would hang
      on any non-empty list. NOTE - this names.yaml records 0x08271724 as
      `iterator_state_release`, status ported, module app/vtable_set, but
      NO such function exists in the tree at origin/master (49a7a00); only
      app/vtable_set's no-op ITERATOR_STATE_RELEASE seam. The entry is
      ahead of the code.
      TESTS - 5 host tests, trace-based so the ORDER is proved, not just
      the effects - empty array (broadcast sees the vtable already
      re-planted, reason 0, nothing freed, guards intact), nonzero storage
      handed to free then cleared, NULL storage skipping the call
      entirely, a three-node chain drained head-first between the
      broadcast and the free, and the WIRED default draining a one-node
      list without hanging. Observer nodes live in a sub-4-GiB slab
      (testing::hints::OBSERVABLE_ARRAY, new).
      Deviations - none behaviorally. match.py structural - same
      register allocation (r4 = this), same `ldr r1,[r4,#0xc]; cmp r1,#0`
      drain, same `ldr r0,[r4,#8]; cmp r0,#0` free guard, same
      `mov r0,#0` / two stores / `mov r0,r4` epilogue, same vtable
      literal; differing only by the crate's fp frame and by the three
      seam calls being `blx` through a static instead of direct `bl`.

  - address: 0x08275bb8
    ghidra: FUN_08275bb8
    name: framework_object_construct
    size: 16
    sig: "FrameworkObject *framework_object_construct(FrameworkObject *this)"
    module: cxx/observable_array
    status: ported
    vars: {param_1: this, DAT_08275bc4: FRAMEWORK_OBJECT_VTABLE}
    notes: >-
      The trivial constructor of the abstract ROOT of the class hierarchy
      cxx/observable_array's class descends from - `ldr r1,[pc,#4];
      str r1,[r0]; bx lr` plus the 4-byte vtable literal 0x089a5fdc @
      0x08275bc4: 12 bytes of code, true extent 16. Its matching
      destructor @ 0x08275bc8 is a bare `bx lr`.
      9 `bl` call sites and 0 `b`, binary-scanned - 0x08125238,
      0x081433ac, 0x081b1270, 0x08266a50, 0x0826bad0, 0x0826bffc,
      0x08271ca0, 0x08271cf0, 0x08275cd8. All nine follow the call
      immediately with their own `ldr r1,[pc,#N]; str r1,[r0]`, i.e. all
      nine are derived-class constructors overwriting the root vtable -
      which is what makes this the shared root rather than one class's
      ctor.
      r0 passes through untouched and every caller depends on it to
      address its own vtable store, so the port returns `this`. Ported
      alongside its only in-crate caller because a dispatch seam for
      three instructions would be pure ceremony. Deviations - none.
      match.py structural - same ldr-literal/str body plus the crate's fp
      frame, with the literal after the body.

  - address: 0x08275bc8
    ghidra: FUN_08275bc8
    name: framework_object_destruct
    size: 4
    sig: "FrameworkObject *framework_object_destruct(FrameworkObject *this)"
    module: cxx/observable_array
    status: ported
    renames:
      param_1/r0: this
    notes: >-
      A single `bx lr` at 0x08275bc8. The next function begins immediately
      at 0x08275bcc, proving the 4-byte extent Ghidra reported. Decoding
      every ARM B/BL word in osos.dec found 12 direct bl callers
      (0x081433d0, 0x0816cca0, 0x081862c0, 0x081b12c4, 0x081b96ec,
      0x081b9734, 0x081cd818, 0x08212a30, 0x08266a70, 0x08275b80,
      0x08289074, 0x0829206c), all unconditional; there are no predicated
      BL forms. Ten unconditional tail branches also target it. A scan
      found no word-aligned DATA reference to 0x08275bc8, so it is not a
      vtable-dispatched entry. It reads and writes no memory and passes r0
      through unchanged. That return is load-bearing: 0x08275b80 runs bl
      then tail-branches into its operator-delete path with r0 untouched.
      The port returns this rather than Ghidra's void; no other deviation.

  # --- C++ layer bit set / character set (0x082746f4..0x082748d8) ---
  #
  # Six consecutive unnamed functions describe one 16-byte class: +0x00 a
  # bit capacity, +0x04 a running cardinality, +0x08 heap storage of
  # ((bits + 31) >> 5) u32 words, +0x0c an allocator tag byte.
  #   0x08274834  ctor(this, bit_capacity, tag) - sizes the buffer at
  #               ((bits + 31) >> 5) * 4, mallocs via 0x080eb67c with the
  #               tag, stores +0x08/+0x00/+0x0c, then clears
  #   0x082747b8  clear-all - memzero (0x08037db8) the same span, +0x04 = 0
  #   0x082747e4  copy ctor - copies +0x00/+0x04/+0x0c, fresh buffer,
  #               memcpy (0x08037df8) the span
  #   0x08274874  dtor - `if (words && tag != 0x3a) free_wrapper(words, 0)`,
  #               returns this; 0x3a marks borrowed storage
  #   0x082a4ef8  test(this, word_index, bit_index) - index arrives
  #               PRE-SPLIT; `ldr [words, word << 2]; ands #1 << bit`
  #   0x082746f4  write(this, bit, value) - splits bit, early-outs when the
  #               bit already matches, else steps +0x04 by +-1 and ORs/XORs
  # Indexing it by codepoint is what makes it a character set: the ported
  # 0x0827489c below is the keyboards' allowed-character-set builder.

  - address: 0x082746f4
    ghidra: FUN_082746f4
    name: bit_set_write
    size: 128
    sig: "void bit_set_write(BitSet *set, u32 bit, u32 value)"
    module: cxx/bit_set
    status: ported
    renames:
      param_1/r0: set
      param_2/r1: bit
      param_3/r2: value
      uVar1/r5: word_index
      uVar3/r6: bit_mask
      FUN_082a4ef8: bit_set_test
    notes: >-
      Writes one BitSet bit and preserves the object-wide cardinality:
      `word_index = bit >> 5`, `mask = 1 << (bit & 31)`, then
      bit_set_test(set, word_index, bit & 31). `movs r0,r2; beq` makes
      every nonzero value a SET request. A matching current state returns
      with no store; a transition to set increments set+4 and ORs mask,
      while a transition to clear decrements set+4 and XORs the known-set
      mask. The clear XOR is safe precisely because the preceding test
      establishes that bit is set.
      EXTENT - 128 bytes, 0x082746f4..0x08274770, binary-verified from
      osos.dec: `pop {r4, r5, r6, pc}` is the final word at 0x08274770;
      the next function starts with `push {r4, r5, r6, lr}` at
      0x08274774, and no literal pool intervenes. Ghidra's 128-byte extent
      is exact here.
      CALL SITES - 19 plain `bl`, 0 predicated `bl`, and 0 `b`,
      binary-scanned by decoding every B/BL word in osos.dec. Every direct
      caller therefore invokes it unconditionally; the function itself
      owns the state-match early-outs.
      CALLEE - bit_set_test @ 0x082a4ef8 is already ported in cxx/bit_set
      and is called directly; no dispatch seam remains.
      TESTS - host tests cover clear and set no-ops, bit positions
      0/31/32/63, cardinality transitions, and a non-1 nonzero set value.
      The separate UTF-8 insert tests now exercise this function directly.
      Deviations - none. match.py structural review is expected to retain
      the split index/mask, test call, two early returns, wrapping
      cardinality update, and OR/XOR mutation, while differing in register
      allocation and LLVM's frame.
  - address: 0x08274774
    ghidra: FUN_08274774
    name: bit_set_clear
    size: 68
    sig: "void bit_set_clear(BitSet *set, u32 bit)"
    module: cxx/bit_set
    status: ported
    renames:
      param_1/r0: set
      param_2/r1: bit
      uVar1/r5: word_index
      uVar2/r6: bit_index
      FUN_082a4ef8: bit_set_test
    notes: >-
      Splits bit into word_index = bit >> 5 and bit_index = bit & 31,
      then calls the already ported bit_set_test. An already-clear bit
      returns without any store. A set bit decrements BitSet +0x04 with
      ARM wrapping-subtraction semantics, then BIC-clears its mask from
      words[word_index]. EXTENT - raw ARM is 68 bytes,
      0x08274774..0x082747b4: `pop {r4, r5, r6, pc}` is final at
      0x082747b4; the next function opens `push {r4, lr}` at 0x082747b8,
      without a literal pool. CALL SITES - every ARM B/BL immediate in
      osos.dec was decoded: 9 unconditional `bl`
      (0x08131af4, 0x08131c14, 0x08131d3c, 0x0813291c, 0x08132a34,
      0x08132b80, 0x081cc6a0, 0x081cd138, 0x081cd3a4), 0 predicated
      `bl`, and 0 `b`; callers invoke unconditionally, so the callee owns
      its already-clear early return. CALLEE - bit_set_test @ 0x082a4ef8
      is ported in cxx/bit_set and called directly; no dispatch seam was
      added. TESTS - host tests cover an already-clear no-op, bit positions
      0/31/32/63 across two words, and cardinality underflow wrapping.
      Deviations - none.
  - address: 0x0827489c
    ghidra: FUN_0827489c
    name: bit_set_insert_utf8
    size: 64
    sig: "BitSet *bit_set_insert_utf8(BitSet *set, const char *text)"
    module: cxx/bit_set
    vars: {param_1: set, param_2: text, local_c: cursor, iVar1: codepoint, FUN_08276214: utf8_next_codepoint, FUN_082746f4: bit_set_write}
    status: ported
    notes: >-
      Walks `text` as UTF-8 and sets the bit named by each codepoint,
      i.e. builds a CHARACTER SET out of a string.
      EXTENT - 64 bytes, 0x0827489c..0x082748d8, binary-verified from
      osos.dec; the next function starts at 0x082748dc
      (`push {r4, r5, r6, r7, r8, lr}`) and there is no trailing literal
      pool, so Ghidra's size is right for once.
      CALL SITES - 53 `bl` and 0 `b`, binary-scanned by decoding every
      B/BL word in osos.dec.
      BODY - `push {r0, r1, r4, lr}` is the ADS "give this argument an
      address" idiom - it spills `text` so `add r0, sp, #4` can hand the
      decoder a char** cursor to advance in place; the matching
      `pop {r2, r3, r4, pc}` throws both slots away (r2/r3 are
      call-clobbered, so those are stack adjustment, not values). Then
      `if (text == 0) return this;` and
      `while ((cp = utf8_next_codepoint(&cursor))) write(set, cp, 1);`,
      returning this via `mov r0, r4`. The write's third argument is
      always `mov r2, #1`, so this can only ADD characters to a set.
      IDENTIFICATION - the class comes from the five siblings listed in
      the block comment above, not from this function. The call sites
      confirm the role - of the 53, the two whose argument is a string
      constant pass "0123456789" (0x08297808 from 0x08297754 and
      0x08299b1c from 0x08299840, both text-input UI), and 0x081f99e8
      feeds it the result of string_object_c_str @ 0x082a50b0. These are
      the on-screen keyboards' allowed-character sets.
      CALLEES - utf8_next_codepoint @ 0x08276214 and bit_set_write @
      0x082746f4 are both ported in cxx/ and called DIRECTLY.
      INHERITED EDGES, both from the decoder - a 0xf0..0xff or invalid
      lead byte makes utf8_next_codepoint consume three bytes and return
      0, which this loop cannot distinguish from the terminator, so an
      astral-plane character truncates the insert; and codepoint 0 can
      never be inserted.
      TESTS - 6 host tests directly exercise the real BitSet: NULL text,
      empty string, the "0123456789" literal from the real call sites, mixed
      1/2/3-byte sequences decoded as single codepoints, a 4-byte lead
      truncating the walk, and repeated-character cardinality deduplication.
      Deviations - none. match.py structural - same `mov r4, r0`, NULL
      guard, `add r0, sp, #4` cursor, decoder call, `cmp r0, #0`, the
      `mov r1, r0; mov r0, r4; mov r2, #1` direct write, and the `mov r0,
      r4` return; differing by the crate's fp frame and the loop rotated so
      the first decode is peeled.

  - address: 0x082a4ef8
    ghidra: FUN_082a4ef8
    name: bit_set_test
    size: 24
    sig: "u32 bit_set_test(BitSet *set, u32 word_index, u32 bit_index)"
    module: cxx/bit_set
    vars: {param_1: set, param_2: word_index, param_3: bit_index}
    status: ported
    notes: >-
      The BitSet test, one bit in, exactly 0 or 1 out.
      EXTENT - 24 bytes, 0x082a4ef8..0x082a4f10, binary-verified from
      osos.dec: the next function opens `push {r4, lr}` @ 0x082a4f10 and
      there is no trailing literal pool, so Ghidra's size is exact.
      CALL SITES - 22 `bl`, 0 `b`, 0 predicated, binary-scanned by
      decoding every B/BL word in osos.dec (Ghidra's 22 agrees).
      BODY - `ldr r0, [r0, #8]` (set->words), `ldr r0, [r0, r1, lsl #2]`
      (words[word_index]), `ands r0, r0, #1 lsl r2`, `movne r0, #1`,
      `bx lr`: the raw mask never escapes, the result is normalized.
      The index arrives PRE-SPLIT - callers compute `word = bit >> 5` /
      `bit = bit & 31` themselves (the write @ 0x082746f4 does so in two
      instructions before the `bl`; string_record_range shifts each
      codepoint down by 5). Nothing masks either index, so the port
      keeps the contract: bit_index MUST be in 0..=31. Ported as
      `(words[word_index] >> bit_index) & 1`, the exact ands/movne
      semantics for every in-contract input.
      SEAM CUTOVER - string_record.rs's STRING_RECORD_OPS.bit_set_test
      slot previously transmuted this address on target and ran a
      faithful model on host; both defaults are now the port itself,
      the slot staying seamed only so host tests can intercept it.
      TESTS - 4 host tests over a u32-mapped words slab
      (testing::hints::BIT_SET_TEST): every bit position of a word
      holding only bit 17, mask normalization (0x8000_0401 returns
      exactly 1 for bits 0/10/31, 0 for the gap), pre-split indexing
      across four words with the bit/word indices proven uncrossed, and
      an all-zero set reading 0 everywhere.
      Deviations - none. match.py structural - same chained
      `ldr [r0, #8]` / `ldr [r0, r1, lsl #2]` then a shift-and
      normalize; LLVM emits the shift as `lsr` with the amount masked
      by 31 (in-contract no-op) and adds the crate's fp frame.

  # --- C++ layer owner-tracked mutex (0x082e8390 / 0x082e83d8) ---
  #
  # NOT the RTXC mutex of kernel/sync_mutex (0x080744a4 & co.): this pair
  # carries an owner thread, a recursion count and a kind, over a counting
  # semaphore held inline at mutex+0x14. Object layout (24 bytes, all
  # scalar): +0x00 magic, +0x04 owner thread, +0x08 reserved, +0x0c attr
  # word (kind = bits 4..5 of the halfword at +0x0e: 0 normal,
  # 1 error-checking, 2 recursive), +0x12 recursion count (u16),
  # +0x14 ROM semaphore handle cell.
  # Status codes: 0x1a invalid object, 0x0f would-deadlock, 0x27 recursion
  # overflow, 0x05 not owner.

  - address: 0x082e8390
    ghidra: FUN_082e8390
    name: posix_mutex_lock
    size: 72
    sig: "u32 posix_mutex_lock(PosixMutex *mutex)"
    module: kernel/posix_mutex
    status: ported
    vars: {param_1: mutex}
    notes: >-
      NULL -> 0x1a; an object still carrying the static-init magic
      0x4d545853 (literal @ 0x082e83d4) is run through the lazy
      initializer @ 0x082e82f8 with a NULL attr and its error propagated;
      then tail b 0x080cdd6c with r1 = 0 (no deadline).
      SIZE IS 72 BYTES (0x082e8390..0x082e83d8: 68 code + the 4-byte
      literal), NOT the 320 Ghidra reports — its extent swallows the
      unlock and two siblings. Byte-decoded from osos.dec.
      240 call sites, binary-verified by decoding every B/BL word: 2
      direct bl (0x080c9848, 0x080c9884) + the 4-byte alias veneer
      `b` @ 0x08261e20 (156 bl + 1 tail b) + the veneer @ 0x082621a8
      (78 bl + 3 tail b).
      Deviation: the mask-ROM-backed callees (initializer 0x082e82f8,
      running-thread query 0x080a3e68, semaphore helpers 0x080a3c7c /
      0x080a3ca4 / 0x080a3d30, clock read 0x082c372c) dispatch through
      POSIX_MUTEX_OPS; the defaults model the pre-kernel machine and
      provide no mutual exclusion. This port is the shipped default of
      block_region.rs's REGION_MUTEX_OPS.lock, replacing the no-op stub.
      match.py structural: same NULL check, same magic compare against the
      same literal, same init call with r1=0, same propagate-nonzero, same
      tail branch with r1=0.

  - address: 0x080cdd6c
    ghidra: (inlined into FUN_082e8390)
    name: posix_mutex_lock_deadline
    size: 256
    sig: "u32 posix_mutex_lock_deadline(PosixMutex *mutex, const TimeSpec *deadline)"
    module: kernel/posix_mutex
    status: ported
    vars: {param_1: mutex, param_2: deadline}
    notes: >-
      The acquire core, a real separately linked function (0x080cdd6c..
      0x080cde6c, including the 1e9 literal @ 0x080cde68) that Ghidra
      inlined into FUN_082e8390 and then mis-sized. EXACTLY ONE reference
      in the image — the lock's tail b @ 0x082e83d0 — so the deadline half
      of the interface is unreachable on this ROM; ported anyway because
      it IS the lock's body.
      Reads the kind (ldrh +0xe, lsl #26, lsr #30), asks who is running
      (0x080a3e68) and compares the owner at +0x4. Already ours: a
      recursive mutex bumps the u16 at +0x12 unless it has reached 0xffff
      (the subs/subscs carry chain against 0xff00 then 0xff) -> 0x27; an
      error-checking mutex -> 0x0f; a normal mutex falls through and
      blocks on itself. Otherwise waits: 0x080a3c7c with no deadline, or
      0x080a3ca4 after converting the absolute deadline to a relative one
      (clock 0 via 0x082c372c — its error returns raw, past the result
      register — field-wise subtract, then borrow whole seconds while the
      nanosecond field is negative). A successful wait sets owner and
      count = 1.
      match.py structural: same kind decode, owner compare, recursion
      limit, clock/subtract/normalize/timed-wait chain and claim stores;
      LLVM turns the three-way kind test into a jump table and reshapes
      the borrow loop into a min/max form.

  - address: 0x082e83d8
    ghidra: FUN_082e83d8
    name: posix_mutex_unlock
    size: 156
    sig: "u32 posix_mutex_unlock(PosixMutex *mutex)"
    module: kernel/posix_mutex
    status: ported
    vars: {param_1: mutex}
    notes: >-
      NULL -> 0x1a; same lazy-init prologue as the lock (magic literal
      @ 0x082e8470, same word). A non-owner -> 0x05. A recursive mutex
      decrements the u16 at +0x12 and stops while it stays nonzero;
      otherwise count and owner are cleared and the semaphore is signalled
      (0x080a3d30), whose status is the return value.
      SIZE IS 156 BYTES (0x082e83d8..0x082e8474): Ghidra's 152 drops the
      trailing literal.
      266 call sites, binary-verified by decoding every B/BL word: 1
      direct bl (0x080c97ac) + the alias veneer `b` @ 0x08261e24
      (153 bl + 12 tail b) + the veneer @ 0x082621ac (76 bl + 24 tail b).
      Shipped default of REGION_MUTEX_OPS.unlock.
      match.py structural: same prologue, same owner compare, same
      predicated recursion decrement and same clear/signal tail; LLVM
      hoists the status constants and returns through bx r2 on the
      indirect release.

  - address: 0x08261e20
    ghidra: (veneer)
    name: posix_mutex_lock_veneer_a
    size: 4
    sig: "u32 posix_mutex_lock_veneer_a(PosixMutex *mutex)"
    module: kernel/posix_mutex
    status: ported
    notes: >-
      ALIAS - bare `b 0x082e8390` tail veneer onto posix_mutex_lock
      (word 0xea02195a in osos.dec, binary-verified). Nothing to port —
      the veneer address IS the lock for every caller that reaches it.
      156 bl + 1 tail b (0x0818af78), binary-verified by decoding every
      B/BL word. No separate Rust symbol exists; hook this address to
      the single posix_mutex_lock symbol (precedent: the iabs aliases
      in fp/fp_misc, atomic_add_irqsafe_alias_f041c in kernel/csem,
      rom_svc_22001cbc_alias_67fc in kernel/task_lock — all single-
      instruction veneers resolved by pointing hooks at the canonical
      port). match.py against posix_mutex_lock at this address confirms
      the original is exactly the 1-instruction branch; the body diff is
      the lock itself, which is the resolution.

  - address: 0x08261e24
    ghidra: (veneer)
    name: posix_mutex_unlock_veneer_a
    size: 4
    sig: "u32 posix_mutex_unlock_veneer_a(PosixMutex *mutex)"
    module: kernel/posix_mutex
    status: ported
    notes: >-
      ALIAS - bare `b 0x082e83d8` tail veneer onto posix_mutex_unlock
      (word 0xea02196b in osos.dec, binary-verified). Nothing to port —
      the veneer address IS the unlock for every caller that reaches it.
      153 bl + 12 tail b, binary-verified by decoding every B/BL word.
      No separate Rust symbol exists; hook this address to the single
      posix_mutex_unlock symbol (precedent: the iabs aliases
      in fp/fp_misc, atomic_add_irqsafe_alias_f041c in kernel/csem,
      rom_svc_22001cbc_alias_67fc in kernel/task_lock — all single-
      instruction veneers resolved by pointing hooks at the canonical
      port). match.py against posix_mutex_unlock at this address confirms
      is exactly the 1-instruction branch; the body diff is
      the unlock itself, which is the resolution.

  - address: 0x08261e28
    ghidra: FUN_08261e28
    name: cxx_mutex_construct
    size: 44
    sig: "u8 *cxx_mutex_construct(u8 *this, usize param_2, usize scope_word0, usize scope_word1)"
    module: cxx/mutex
    status: ported
    renames:
      param_1: this
      param_2: (unread - the original never touches r1)
      param_3: scope_word0 (pushed as the scope's first word)
      param_4: scope_word1 (pushed as the scope's second word)
      uStack_10/uStack_c: scope (the two-word stack attr object)
      FUN_08261d1c: attr_init wrapper (pthread_mutexattr_init @ 0x082e84a4)
      FUN_08261de8: mutex_init (settype NORMAL @ 0x082e84dc + init @ 0x082e82f8, status to this+0x18)
      FUN_08261d30: attr_destroy wrapper (pthread_mutexattr_destroy @ 0x082e8474)
    notes: >-
      The constructor of the C++ layer's mutex wrapper class: a
      0x1c-byte object embedding a PosixMutex (kernel/posix_mutex.rs,
      24 bytes) at +0x00 plus the init status word at +0x18
      (CXX_MUTEX_STATUS_OFFSET). 44 bytes, all code, no literal pool
      (0x08261e28..0x08261e54). 52 bl call sites, binary-scanned.
      Despite its lock-guard SHAPE (scope init / work / scope
      teardown) it is a constructor, not a guard: the scope is an
      8-byte pthread_mutexattr built on the stack frame - the `push
      {r2,r3}` IS the allocation, so the scope's words start as the
      incoming r2/r3 register values (call sites pass no meaningful
      seeds; the ADS uninitialized-frame idiom) - and the work is the
      embedded mutex's one-time init as a NORMAL mutex. The three
      callees, identified from their bodies and binary-verified
      literals: 0x08261d1c wraps pthread_mutexattr_init @ 0x082e84a4
      (plants attr magic 0x4d545841 "MTXA" - the literal @ 0x082e84d8,
      the same word the destroy/settype literals @ 0x082e84a0/
      0x082e852c hold, and the attribute twin of the mutex object's
      0x4d545853 "MTXS" static-init magic - at scope+0, default
      halfword 0xffc2 at +4, RMW bits 0..5+3 of the +6 halfword);
      0x08261de8 runs pthread_mutexattr_settype @ 0x082e84dc with
      kind 0 (NORMAL - bits 4..5 of the attr +6 halfword, the field
      the PosixMutex layout keeps at mutex+0x0e), stores the status at
      this+0x18, and only on success calls the mutex initializer @
      0x082e82f8 (the lazy initializer posix_mutex_lock documents)
      with (this, &scope) and stores ITS status instead; 0x08261d30
      wraps pthread_mutexattr_destroy @ 0x082e8474 (magic check, clear
      scope+0). Returns this (mov r0,r4). Call sites pin the class:
      0x08153348 constructs the wrapper member at this+0x04 and the
      next member at return+0x1c (the wrapper size), and the same
      object's lock/unlock route the member through the
      posix_mutex_lock/unlock veneers @ 0x08261e20/0x08261e24
      (0x0815331c). Deviations (the settings.rs SETTINGS_CTOR
      pattern): the three callees ride the CXX_MUTEX_CONSTRUCT_OPS
      dispatch slots with no-op defaults - NOT HOOK-READY until the
      wrappers are ported (with the defaults the mutex is never
      initialized and the status word never written); the scope is
      modeled as two pointer-sized words (the pfr_face_done face-word
      model). Host tests: exact call order (attr_init -> mutex_init ->
      attr_destroy), the scope's initial words are the incoming
      param_3/param_4 registers and the SAME mutable scope object flows
      through all three callees (an attr_init mock's write is visible
      to the later mocks), this passthrough, and the default stubs
      leaving the wrapper untouched. match.py structural: the Rust
      even emits `stm sp,{r2,r3}` for the scope seeding - same scope
      on stack, same three calls with (scope)/(this,scope)/(scope),
      same mov r0,r4 return; the diff is the crate fp frame with an
      explicit sub sp,#8 and the three blx-through-volatile-slot
      dispatches standing in for the direct bl's.

  - address: 0x08261e54
    ghidra: FUN_08261e54
    name: cxx_mutex_destroy
    size: 20
    sig: "void *cxx_mutex_destroy(void *this)"
    module: cxx/mutex_destroy
    status: ported
    renames:
      param_1: this
      FUN_082e82a4: pthread_mutex_destroy (unported)
    notes: >-
      20 bytes of code, no literal pool (0x08261e54..0x08261e68; next
      function starts at 0x08261e68), decoded from osos.dec as push
      {r4,lr}; mov r4,r0; bl 0x082e82a4; mov r0,r4; pop {r4,pc}.
      The C++ mutex-wrapper destructor calls pthread_mutex_destroy on
      the embedded mutex at this+0, discards its status, and returns
      this unchanged. Its only direct callee is the real entry
      0x082e82a4 (raw body validates mutex magic 0x4d555458, rejects
      NULL or invalid magic with EINVAL 26, rejects a live owner or
      semaphore with EBUSY 20, otherwise calls the real entry
      0x080a3cf0 with this+0x14 and clears the magic).
      Binary-decoding every B/BL word in osos.dec finds 19 unconditional
      bl sites, zero predicated calls, and 4 tail-b sites
      (0x08261c44, 0x08261cd8, 0x082628e8, 0x08262a98); no data word
      equals 0x08261e54, so no virtual dispatch uses this address.
      Deliberate deviation: unported pthread_mutex_destroy crosses the
      CXX_MUTEX_DESTROY_OPS volatile dispatch slot, whose default is an
      inert success-returning stub. Thus this port is not hook-ready
      until 0x082e82a4 is ported. Host tests prove one-call forwarding,
      ignored callee status, this passthrough, no direct writes, and
      NULL forwarding. match.py structural review records the expected
      blx-through-dispatch difference.

  - address: 0x082621a8
    ghidra: (veneer)
    name: posix_mutex_lock_veneer_b
    size: 4
    sig: "u32 posix_mutex_lock_veneer_b(PosixMutex *mutex)"
    module: kernel/posix_mutex
    status: ported
    notes: >-
      ALIAS - duplicate of the 0x08261e20 veneer (the linker emitted a
      second block so far callers stay in bl range): bare
      `b 0x082e8390` tail veneer onto posix_mutex_lock (word
      0xea021878 in osos.dec, binary-verified). Nothing to port — the
      veneer address IS the lock for every caller that reaches it.
      78 bl + 3 tail b, binary-verified. This is the thunk every heap
      client in src/heap/ names. No separate Rust symbol exists; hook
      this address to the single posix_mutex_lock symbol (precedent:
      posix_mutex_lock_veneer_a @ 0x08261e20, commit a0f27c1, and the
      alias resolutions it cites — iabs aliases in fp/fp_misc,
      atomic_add_irqsafe_alias_f041c in kernel/csem,
      rom_svc_22001cbc_alias_67fc in kernel/task_lock). match.py
      against posix_mutex_lock at this address confirms the original
      is exactly the 1-instruction branch; the body diff is the lock
      itself, which is the resolution.

  - address: 0x082621ac
    ghidra: (veneer)
    name: posix_mutex_unlock_veneer_b
    size: 4
    sig: "u32 posix_mutex_unlock_veneer_b(PosixMutex *mutex)"
    module: kernel/posix_mutex
    status: ported
    notes: >-
      ALIAS - duplicate of the 0x08261e24 veneer (the linker emitted a
      second block so far callers stay in bl range): bare
      `b 0x082e83d8` tail veneer onto posix_mutex_unlock (word
      0xea021889 in osos.dec, binary-verified). Nothing to port — the
      veneer address IS the unlock for every caller that reaches it.
      76 bl + 23 tail b, binary-verified by decoding every B/BL word
      in osos.dec (the earlier identified note said 24 tail b —
      recount lists all 23 tail sites; the sibling 0x082621a8 lock
      veneer is likewise 78 bl + 2 tail b, not 3). This is the
      unlock thunk every heap client in src/heap/ names, twin of the
      lock thunk 0x082621a8. No separate Rust symbol exists; hook
      this address to the single posix_mutex_unlock symbol
      (precedent: posix_mutex_lock_veneer_b @ 0x082621a8, commit
      7cbc65c; posix_mutex_unlock_veneer_a @ 0x08261e24; and the
      alias resolutions they cite — posix_mutex_lock_veneer_a @
      0x08261e20 commit a0f27c1, iabs aliases in fp/fp_misc,
      atomic_add_irqsafe_alias_f041c in kernel/csem,
      rom_svc_22001cbc_alias_67fc in kernel/task_lock). match.py
      against posix_mutex_unlock at this address confirms the
      original is exactly the 1-instruction branch; the body diff is
      the unlock itself, which is the resolution.

  - address: 0x08262138
    ghidra: FUN_08262138
    name: resource_handle_release_if_owned
    size: 32
    sig: "ResourceHandle *resource_handle_release_if_owned(ResourceHandle *handle)"
    module: cxx/resource_handle_release
    status: ported
    renames:
      param_1: handle
      FUN_082620d8: unported immediate resource-handle release helper
    notes: >-
      Raw extent is exactly 32 bytes (0x08262138..0x08262158): push
      {r4,lr}; save this; load byte this+8; compare zero; only when zero
      move this into r0 and bleq 0x082620d8; restore this into r0; pop.
      The next distinct function begins at 0x08262158, and there is no
      literal pool. Binary decoding every B/BL word in osos.dec finds 14
      direct call sites, all unconditional plain bl at 0x08165330,
      0x081654c0, 0x081939d8, 0x081d7788, 0x081d77f8, 0x081e6c10,
      0x082011a0, 0x082628b0, 0x0839e938, 0x0839e958, 0x0839eaa4,
      0x0839eac4, 0x0839f480, and 0x0839f4a0; no predicated BL forms
      and no word-aligned DATA references. The gate byte marks a handle
      whose resource release is skipped; the helper's status is discarded
      and this is always returned. 0x082620d8 is absent from names.yaml:
      target builds call that fixed retailOS address, while host tests use
      a volatile recording seam solely to prove zero/nonzero gating and
      handle passthrough. match.py structural review: the Rust early return
      is the inverse branch of the retail predicated call, followed by a
      literal-loaded blx to the same unported helper address.

  # --- retailOS per-kind mask-table union fold ---
  - address: 0x08055db8
    ghidra: FUN_08055db8
    name: object_kind_mask_union
    size: 72
    sig: "u64 object_kind_mask_union(const void *object, u32 kind)"
    module: ui/object_state
    status: ported
    renames:
      FUN_080e4e64: object_kind_table
      uVar2: union_lo
      uVar3: union_hi
      puVar1: entry
    notes: >-
      OR-fold of a per-kind mask table into a 64-bit union returned in
      r0:r1: push {r4,r5,r6,lr}; zero r5/r6; bl 0x080e4e64; null table
      returns 0:0; otherwise ldrd-walk the table OR-ing each entry's low
      word into r5 and high word into r6, advancing 8 bytes until a zero
      doubleword terminator. Ghidra's `FUN_08055db8(void)` with a
      zero-argument `FUN_080e4e64()` call is an artifact — the bl has no
      argument setup, so the original forwards its own r0/r1 untouched;
      the single `bl 0x08055db8` call site (grep -c on decomp/osos.asm),
      at 0x08067968 in FUN_0806786c, passes the record owner in r0 and
      the word at record+0x14 in r1, then ORs the result into the 64-bit
      field at owner+0x1d0 (alongside a sibling fold through 0x08054b0c),
      so the function aggregates the table's entries into a combined
      mask; the masks' concrete meaning is not recovered. The getter
      0x080e4e64 stays in retailOS behind the OBJECT_KIND_TABLE boundary:
      kind==1 with bit 0 of the byte at object+0x18c set returns the
      override table at literal 0x080e4e84, otherwise the kind switch
      0x080d8948 returns one of the per-kind tables 0x080d8b68..0x080d8c10
      (null for kind 0, flagless kind 1, and kinds above 0x2c). The
      redundant in-loop `ldrdne` reload is an ADS artifact not
      reproduced. match.py (structural, exit 1 expected) shows the same
      null guard, two-accumulator OR fold, 8-byte stride and
      zero-doubleword termination; diffs are the boundary blx and LLVM's
      ldm word pairs in place of ldrd. Rust implementation and focused
      host tests: `src/ui/object_state.rs::object_kind_mask_union`.

  # --- retailOS UI object state-to-kind mapping ---
  - address: 0x08055e00
    ghidra: FUN_08055e00
    name: object_state_kind
    size: 128
    sig: "u32 object_state_kind(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
    notes: >-
      Ten-entry `addls pc,pc,r0,lsl #2` branch table over the state word
      at object+0xe38 (the word object_state_word @ 0x08055e80 exposes):
      state 0 -> 9, 2 -> 0x18, 3 -> 6, 4 -> 5, 5 -> 1, 6 -> 0x24,
      7 -> 0x28, 9 -> 0x2a; states 1 and 8 and every unsigned state word
      above 9 fall through to the default code 7. Kind-code semantics
      remain unidentified. Nine `bl 0x08055e00` call sites (grep -c on
      decomp/osos.asm); callers forward the code to other UI helpers
      (e.g. 0x08086710 stores it and special-cases 5; 0x08065c68 passes
      it as r2 to 0x08046e50). match.py diff is expected: LLVM lowers
      the match to a constant 10-word lookup table guarded by the same
      unsigned `<= 9` check instead of a branch table — same table-driven
      structure and results. Rust implementation and focused host tests:
      `src/ui/object_state.rs::object_state_kind`.

  # --- retailOS UI object state accessor ---
  - address: 0x08055e80
    ghidra: FUN_08055e80
    name: object_state_word
    size: 12
    sig: "u32 object_state_word(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
      uVar1: state_word
    notes: >-
      Pure leaf accessor: returns the aligned 32-bit little-endian state
      word at object+0xe38. The owning UI object remains unidentified;
      no null or alignment guard is present in the original.

  # --- retailOS scaled-field multiply-accumulate ---
  - address: 0x08064980
    ghidra: FUN_08064980
    name: scaled_field_total
    size: 148
    sig: "i64 scaled_field_total(const void *fields)"
    module: ui/object_state
    status: ported
    renames:
      param_1: fields
      r4: fields
      sp_10: base
      sp_8: count_or_scale
      sp_0: count_for_multiply
    notes: >-
      Assembly evidence at 0x08064980..0x08064a10: null fields return
      zero; otherwise four 8-byte copies load base, count, scale, and count
      again. A zero count branches directly to the copied base. The nonzero
      path performs the unsigned 64-bit product using umull/mla/mla, then
      adds base with adds/adc, so the i64 result is the wrapping bit pattern
      of base + count * scale. Ghidra did not emit a standalone
      functions.csv row at this internal boundary; the next indexed function
      starts at 0x08064a14, establishing the 148-byte extent. Tail callers
      object_scaled_total @ 0x08055f24 and object_scaled_total_base @
      0x08055f3c, plus their reference C, corroborate the three field
      offsets and arithmetic. Rust implementation and focused host tests:
      `src/ui/object_state.rs::scaled_field_total`.

  # --- retailOS null-guarded scaled-total thunk ---
  - address: 0x08055f24
    ghidra: FUN_08055f24
    name: object_scaled_total
    size: 24
    sig: "i64 object_scaled_total(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
      thunk_EXT_FUN_22000020: memcpy_8
      FUN_08064980: scaled_field_total
    notes: >-
      Null-guarded thunk (cmp r0,#0; addne r0,r0,#0x18; bne 0x08064980;
      moveq r0,#0; moveq r1,#0; bx lr): a null object returns 0 in r0:r1,
      otherwise it tail-calls retailOS helper 0x08064980 with object+0x18,
      which folds the three doublewords at object offsets 0x18/0x20/0x28
      into base + count * scale (plain base when count is zero); the
      degenerate object+0x18 == 0 wraparound also returns 0. Ghidra
      decompiles the thunk with the callee body inlined, hence the full
      arithmetic in the reference C; the callee stays in retailOS behind
      the SCALED_FIELD_TOTAL boundary, same pattern as
      indexed_object_offset's 0x080aa828. The adjacent 0x08055f3c is the
      same thunk without the +0x18 offset and is a separate function. One
      `bl 0x08055f24` call site (grep -c on decomp/osos.asm), at
      0x080aaf44: it applies the thunk to a 0x44-byte descriptor copied by
      0x08054d28 and forwards the result together with the sibling
      0x08055f3c's to 0x08045174; the field triple's concrete meaning is
      not recovered. match.py (structural, exit 1 expected) shows the same
      guard/add/conditional-branch/zero-return shape; diffs are LLVM's
      push {fp,lr} frame, flag-setting addsne for addne, and the indirect
      call through the boundary static instead of the direct b 0x08064980.

  # --- retailOS null-guarded base scaled-total thunk ---
  - address: 0x08055f3c
    ghidra: FUN_08055f3c
    name: object_scaled_total_base
    size: 20
    sig: "i64 object_scaled_total_base(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
      thunk_EXT_FUN_22000020: memcpy_8
      FUN_08064980: scaled_field_total
    notes: >-
      Null-guarded thunk (cmp r0,#0; bne 0x08064980; moveq r0,#0; moveq
      r1,#0; bx lr): a null object returns 0 in r0:r1, otherwise it
      tail-calls the same retailOS helper 0x08064980 as sibling
      object_scaled_total @ 0x08055f24, but with object itself (no +0x18
      offset), folding the three doublewords at object offsets 0/8/0x10
      into base + count * scale (plain base when count is zero). The real
      extent is 20 bytes (bx lr at 0x08055f4c); functions.csv's 168 is a
      Ghidra fall-through merge of the following thunks. Ghidra decompiles
      the thunk with the callee body inlined, hence the full arithmetic in
      the reference C; the callee stays in retailOS behind the
      SCALED_FIELD_TOTAL boundary shared with object_scaled_total. One
      `bl 0x08055f3c` call site (grep -c on decomp/osos.asm), at
      0x080aaf54: it applies the thunk to the same 0x44-byte descriptor
      copied by 0x08054d28 as object_scaled_total and forwards both
      results together to 0x08045174; the field triple's concrete meaning
      is not recovered. match.py (structural, exit 1 expected) shows the
      same guard/conditional-branch/zero-return shape; diffs are LLVM's
      push {fp,lr} frame and the indirect call through the boundary
      static instead of the direct b 0x08064980. Rust implementation and
      focused host tests:
      `src/ui/object_state.rs::object_scaled_total_base`.


  # --- retailOS lazy shared-context getter (ui/object_state) ---
  - address: 0x08369bec
    ghidra: FUN_08369bec
    name: shared_context
    size: 28
    sig: "u8 *shared_context(void)"
    module: ui/object_state
    status: ported
    renames:
      DAT_08369c08: shared_context_slot
      DAT_08369c0c: default_shared_context
    notes: >-
      Raw ARM is ldr r0,[pc,#0x14]; ldr r1,[r0]; cmp r1,#0; ldreq
      r1,[pc,#0xc]; streq r1,[r0]; ldr r0,[r0]; bx lr. It lazily publishes
      default context base 0x08a11cd8 into the mutable pointer slot at
      0x089ca574 only when that slot is zero, then reads and returns the
      slot. The instruction body is 28 bytes; its two trailing literal-pool
      words extend the true occupied range through 0x08369c0f, where the
      next function begins at 0x08369c10. Binary decoding finds 27 direct
      bl callers, all unconditional: no predicated call sites. The port uses
      volatile pointer loads/stores so the fixed RAM slot remains observable;
      host backing storage exercises zero-slot publication and preservation
      of an existing pointer. Port deviation: none on target. match.py
      structural review is expected to differ in address materialization and
      volatile accesses, while retaining the load/zero-test/conditional-store/
      final-load sequence.

  # --- retailOS lazily cached board-version word (sysinfo) ---
  - address: 0x080e624c
    ghidra: FUN_080e624c
    name: board_version
    size: 48
    sig: "u32 board_version(void)"
    module: sysinfo
    status: ported
    renames:
      FUN_08369bec: shared_context (the lazy shared-context getter, already
        ported in ui/object_state; the refresh source)
      DAT_089caaac: system_info_base (the literal-pool word at 0x080e6278,
        binary-verified bytes ac aa 9c 08; its field +0x8 is the lazily
        cached board-version word; binary scan finds the base address only
        here and in the literal pools at 0x082bc7c0 / 0x082bc9e8 of the
        0x082bcxxx diagnostics cluster)
      thunk_FUN_080e624c: board_version_veneer (the 4-byte bare
        `b 0x080e624c` @ 0x080515e4, word 0xea025318, that the ported
        board_version_halves @ 0x082a1a70 calls through)
    notes: >-
      Ghidra reports 44 bytes; the true occupied range is 48: 11 code words
      0x080e624c-0x080e6277 plus the literal-pool word at 0x080e6278
      (0x089caaac) that Ghidra's extent excludes - the next function's
      stmdb sp!,{r0-r11,lr} prologue starts at 0x080e627c. Binary decoding
      verifies 13 direct bl callers, ALL unconditional (cond=e; no
      predicated forms), plus the b-veneer @ 0x080515e4; no word in
      osos.dec holds 0x080e624c, so it is never dispatched virtually.
      Body: stmdb sp!,{r4,lr}; ldr r4,[0x080e6278]; ldr r0,[r4,#0x8]; cmn
      r0,#0x80000001; bne out; bl 0x08369bec; cmp r0,#0; ldrne
      r0,[r0,#0x84]; strne r0,[r4,#0x8]; out: ldr r0,[r4,#0x8]; ldmia
      sp!,{r4,pc}. Answers the lazily cached board/firmware revision word
      at system_info+0x8: while it holds the 0x7fffffff sentinel (the cmn
      spelling of a 0x7fffffff compare) the authoritative word is fetched
      from +0x84 of the shared_context object and republished into the
      cache, but only when the getter returned non-NULL (the cmp/ldrne/
      strne guard; a NULL context leaves the sentinel in place and it is
      returned as-is). Once cached the context is never consulted again;
      the final ldr reloads the cache rather than keeping a register copy,
      which the port reproduces with volatile accesses. The word's HIGH
      halfword 0x10 with LOW halfword >= 6 selects timer channel 3 in
      cg_timer_wait @ 0x082bc4fc. Deviations: the {r4,lr} frame (keeps
      the struct base alive across the bl) is an ADS artifact - the port
      rematerializes the address; the refresh callee is the already-ported
      shared_context, called directly (no seam - on target the link
      resolves it to the Rust port, verified R_ARM_CALL shared_context in
      the archive); host builds substitute a static for the fixed RAM
      field, with object_state's new pub(crate) SHARED_CONTEXT_TEST_LOCK
      and host_install_shared_context hook serializing the cross-module
      fixture (the fp_misc CURRENT_DATETIME_QUERY_TEST_LOCK pattern).
      board_version_halves (fp_misc) still reaches this function through
      its pre-existing BOARD_VERSION usize seam - untouched, the seam's
      target-side transmute of 0x080e624c now lands on this port once
      hooked. match.py structural (exit-1 diff expected): identical
      ldr-literal/ldr-cache/cmn-sentinel/bne/bl/cmp/ldrne/strne/reload
      sequence; the diffs are the crate push {r4,sl,fp,lr} frame, LLVM
      folding base+8 into one literal 0x089caab4, and the in-archive
      shared_context relocation. Host tests (sysinfo::tests): cached word
      returned without a context, sentinel kept on NULL context, one-shot
      refresh from context+0x84 that then sticks, and a zero context word
      cached as a valid value (not mistaken for the sentinel).
  # --- retailOS object counted-text install from the shared context's dotted-triple word ---
  - address: 0x08055f50
    ghidra: FUN_08055f50
    name: object_set_version_text
    size: 64
    sig: "void object_set_version_text(void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
      iVar1: context
      auStack_208: counted
      auStack_108: formatted
      FUN_08369bec: shared_context
      FUN_0806e0a4: format_dotted_triple
      FUN_08045f14: cstr_to_counted_u8
      FUN_08046a88: object_counted_text
    notes: >-
      Fetches the process-wide shared context through the lazy getter
      0x08369bec, reads the word at context+0x48, and formats it through
      stock helper 0x0806e0a4 — sprintf(dst, "%d.%d.%d", word >> 24,
      (word >> 16) & 0xff, word & 0xffff) over the format string at
      0x0806e0d4 — into the upper of two uninitialized 256-byte stack
      buffers (sub sp,sp,#0x200). Ported cstr_to_counted_u8 (0x08045f14)
      converts the text into a byte-counted string in the lower buffer
      with limit 0xff, and stock setter 0x08046a88 installs it on the
      object (halfword count at object+0, expanded halfword payload at
      object+2 via 0x08046b44). The context word's identity as a version
      is inferred from the dotted-triple format and the byte-identical
      sibling 0x08052228, which reads context+0x4c and is a separate
      function. One `bl 0x08055f50` call site (grep -c on
      decomp/osos.asm), at 0x08112800: a dispatch chain of field setters
      (0x08052228, 0x08054d78, 0x08054ea0, 0x080539f0) filling a stack
      object at sp+0x5c before 0x081131a8 merges it into the record at
      r5+0xf8 — an about/diagnostics-style text field; the concrete
      field is not recovered. Ghidra declares the original void; its r0
      residue is 0x08046a88's return, which the call site ignores. The
      ported shared_context getter supplies context directly; the formatter
      and setter remain behind FORMAT_DOTTED_TRIPLE and OBJECT_COUNTED_TEXT
      boundaries. match.py (structural, exit 1 expected) shows the same
      four-call sequence with the same +0x48 load, 0xff limit and 0x200
      frame; diffs are LLVM's larger push {r4-r6,sl,fp,lr} frame, the
      formatter/setter boundary calls, swapped stack-buffer order, and the
      in-crate calls to shared_context and cstr_to_counted_u8. Rust
      implementation and focused host tests:
      `src/ui/object_state.rs::object_set_version_text`.

  # --- retailOS nested UI-object flag accessor ---
  - address: 0x08055f90
    ghidra: FUN_08055f90
    name: object_nested_flag
    size: 12
    sig: "u8 object_nested_flag(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
      puVar1: nested_object
      uVar1: nested_flag
    notes: >-
      Pure leaf accessor: follows the aligned pointer at object+0xf00 and
      returns the byte at nested_object+0xb88. The complete body is ldr;
      ldrb; bx lr, so the result is zero-extended in r0 and neither pointer
      receives a null guard. The UI object and flag semantics remain
      unidentified. Rust implementation and focused host tests:
      `src/ui/object_state.rs::object_nested_flag`.

  # --- retailOS nested UI-object property accessor ---
  - address: 0x08055f9c
    ghidra: FUN_08055f9c
    name: object_nested_property
    size: 12
    sig: "u8 object_nested_property(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
    notes: >-
      Pure leaf accessor: follows the aligned pointer at object+0xf00 (the
      same nested object object_nested_flag @ 0x08055f90 dereferences) and
      returns the byte at nested_object+0xb51. The complete body is ldr;
      ldrb; bx lr, so the result is zero-extended in r0 and neither pointer
      receives a null guard. Two `bl 0x08055f9c` call sites (grep -c on
      decomp/osos.asm), both in the change-notification routine at
      0x081732b0/0x081732c4: they cache the byte at r4+0x86 and fire a
      virtual callback when it changes, so it behaves as a polled nested
      property; its concrete meaning remains unidentified. The adjacent
      0x08055fa8 (12 bytes) is the same ldr/ldrb/bx lr shape but reads
      offset 0xb66, not byte-identical, so it stays a separate function.
      match.py diff is only LLVM's push {fp,lr}/pop {fp,pc} frame versus
      the ADS `bx lr` leaf; the two loads are instruction-identical. Rust
      implementation and focused host tests:
      `src/ui/object_state.rs::object_nested_property`.

  # --- retailOS nested UI-object attribute accessor ---
  - address: 0x08055fa8
    ghidra: FUN_08055fa8
    name: object_nested_attribute
    size: 12
    sig: "u8 object_nested_attribute(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
    notes: >-
      Pure leaf accessor: follows the aligned pointer at object+0xf00 (the
      same nested object object_nested_flag @ 0x08055f90 and
      object_nested_property @ 0x08055f9c dereference) and returns the byte
      at nested_object+0xb66. The complete body is ldr; ldrb; bx lr, so the
      result is zero-extended in r0 and neither pointer receives a null
      guard. Two `bl 0x08055fa8` call sites (grep -c on decomp/osos.asm),
      at 0x081a3890 and 0x081a4d60: both cache the byte into caller state
      at r4+0xa1, and the second gates a virtual dispatch on the adjacent
      byte at r4+0xa0, so it behaves as a cached nested attribute; its
      concrete meaning remains unidentified. Distinct from the adjacent
      object_nested_property (offset 0xb51), not byte-identical, hence its
      own port. match.py diff is only LLVM's push {fp,lr}/pop {fp,pc}
      frame versus the ADS `bx lr` leaf; the two loads are
      instruction-identical. Rust implementation and focused host tests:
      `src/ui/object_state.rs::object_nested_attribute`.

  # --- retailOS fixed-point timestamp accessor ---
  - address: 0x08055fb4
    ghidra: FUN_08055fb4
    name: scaled_timestamp_now
    size: 28
    sig: "i64 scaled_timestamp_now(void)"
    module: ui/object_state
    status: ported
    renames:
      lVar1: sample
      FUN_0809b60c: clock_sample
    notes: >-
      Calls retailOS sampled-clock getter 0x0809b60c with interface index
      0 (`mov r0,#0x0`) and returns its 64-bit sample shifted left by 10
      (`lsl r1,#10; orr r1,r1,r0,lsr #22; lsl r0,#10`) — a fixed-point
      timestamp 2^10 finer than the raw getter's unit; the concrete unit
      is not recovered. Three `bl 0x08055fb4` call sites (grep -c on
      decomp/osos.asm), at 0x08141640, 0x081417a8, and 0x081eb42c: each
      subtracts a lazily snapshotted baseline from the result (0x08055ff8
      reads the doubleword at global 0x089ca5e0 + 0x20, first populated by
      0x08055fd0, which stores the identically scaled sample of sibling
      getter 0x08090b88) and converts the 64-bit delta to double, so the
      function behaves as the "now" half of an elapsed-time measurement.
      The 0x0809b60c dependency stays in retailOS behind a replaceable
      function-pointer boundary (same pattern as indexed_object_offset's
      0x080aa828 seam). match.py confirms the structural shape: `mov
      r0,#0` and the three shift instructions are instruction-identical;
      the diff is only LLVM's push {fp,lr}/pop {fp,pc} frame and the
      indirect `blx` through the boundary static versus the ADS direct
      `bl`. Rust implementation and focused host tests:
      `src/ui/object_state.rs::scaled_timestamp_now`.

  # --- retailOS elapsed-time baseline snapshot ---
  - address: 0x08055fd0
    ghidra: FUN_08055fd0
    name: snapshot_timestamp_baseline
    size: 36
    sig: "i64 snapshot_timestamp_baseline(void)"
    module: ui/object_state
    status: ported
    renames:
      lVar1: scaled_sample
      FUN_08090b88: baseline_clock_sample
      DAT_08055ff4: TIMESTAMP_BASELINE
    notes: >-
      Calls retailOS baseline-clock getter 0x08090b88 with interface index
      0 (`mov r0,#0x0`), shifts its 64-bit sample left by 10 (the same
      fixed-point scale scaled_timestamp_now @ 0x08055fb4 applies to
      sibling getter 0x0809b60c; 0x08090b88 reads through accessor
      0x08296ea4 instead of 0x08296f18 and zero-extends a 32-bit result),
      and stores the result as the doubleword at global 0x089ca5e0 + 0x20
      (`strd r0,r1,[r2,#0x20]`; both the 0x08055ff4 and 0x08056024 literals
      hold 0x089ca5e0, verified in osos.dec). This is the "snapshot" half
      of the firmware's elapsed-time measurement. Three `bl 0x08055fd0`
      call sites (grep -c on decomp/osos.asm): 0x08056014 inside the lazy
      baseline getter 0x08055ff8 (populates a zero baseline), 0x08068638,
      and 0x081eb420, which moves the r0:r1 residue into r6/r8 as the
      subtrahend for the following scaled_timestamp_now delta. Ghidra
      declares the original void, but it leaves the scaled sample in r0:r1
      and both non-trivial call sites rely on that residue, so the port
      returns the stored value; the global slot is modeled by the
      TIMESTAMP_BASELINE static and 0x08090b88 stays in retailOS behind a
      replaceable function-pointer boundary (same pattern as
      scaled_timestamp_now's 0x0809b60c seam). match.py confirms the
      structural shape: `mov r0,#0`, the three shift instructions, and the
      doubleword store; the diff is only LLVM's push {fp,lr}/pop {fp,pc}
      frame, the indirect `blx` through the boundary static versus the ADS
      direct `bl`, and `strd` against the modeled static (offset 0) versus
      the 0x089ca5e0 literal at offset 0x20. Rust implementation and
      focused host tests:
      `src/ui/object_state.rs::snapshot_timestamp_baseline`.

  # --- retailOS elapsed-time lazy baseline getter ---
  - address: 0x08055ff8
    ghidra: FUN_08055ff8
    name: timestamp_baseline
    size: 44
    sig: "i64 timestamp_baseline(void)"
    module: ui/object_state
    status: ported
    renames:
      iVar1: baseline_slot
      uVar2: scaled_sample
      DAT_08056024: TIMESTAMP_BASELINE
      FUN_08055fd0: snapshot_timestamp_baseline
    notes: >-
      Lazily initializes and returns the elapsed-time baseline doubleword
      at global 0x089ca5e0 + 0x20 (the 0x08056024 literal holds
      0x089ca5e0, same as snapshot_timestamp_baseline's 0x08055ff4
      literal): loads the slot (`ldrd r0,r1,[r4,#0x20]`), treats a zero
      doubleword as "not yet snapshotted" (`cmp r1,#0; cmpeq r0,r2`),
      calls snapshot_timestamp_baseline @ 0x08055fd0 only then, stores
      its r0:r1 result back (`strd r0,r1,[r4,#0x20]`) and returns the
      slot. A nonzero slot short-circuits, so the baseline is sampled
      exactly once over uptime. Three `bl 0x08055ff8` call sites (grep -c
      on decomp/osos.asm): 0x0814164c, 0x081417b4, and 0x081eb418 — each
      subtracts the result from a following scaled_timestamp_now sample
      and converts the 64-bit delta to double, so this is the "zero
      point" half of the firmware's elapsed-time measurement. The port
      calls the ported snapshot_timestamp_baseline rather than
      re-implementing it; the baseline-clock dependency behind it stays
      in retailOS behind the same replaceable function-pointer boundary
      as its siblings, and the slot is modeled by the TIMESTAMP_BASELINE
      static. match.py confirms the structural shape — ldrd, zero
      doubleword test, conditional bl, strd, final ldrd — 13 versus 11
      instructions; the diff is only LLVM's push {r4,sl,fp,lr} frame, an
      `orrs` fusion of the cmp/cmpeq pair, the direct in-crate `bl` to
      the ported snapshot versus the ADS inter-function `bl`, and
      strd/ldrd against the modeled static (offset 0) versus the
      0x089ca5e0 literal at offset 0x20. Rust implementation and focused
      host tests: `src/ui/object_state.rs::timestamp_baseline`.

  # --- shared-context mode-flag low-bit accessor ---
  - address: 0x08056028
    ghidra: FUN_08056028
    name: context_mode_flag
    size: 20
    sig: "u32 context_mode_flag(void)"
    module: ui/object_state
    status: ported
    renames:
      iVar1: context
      FUN_08369bec: shared_context
    notes: >-
      Fetches the process-wide shared context through the retailOS lazy
      getter 0x08369bec (the same SHARED_CONTEXT boundary
      object_set_version_text @ 0x08055f50 uses), loads the word at
      context+0x94 (`ldr r0,[r0,#0x94]`), and returns its low bit
      (`and r0,r0,#0x1`) — a boolean flag of the shared context. One
      `bl 0x08056028` call site (grep -c on decomp/osos.asm), at
      0x081602b0: lazily caches a record size selected by the flag into
      a global slot — 0x100 when the bit is clear, 0xd8 when set
      (`moveq r0,#0x100; movne r0,#0xd8`) — so the bit behaves as a
      two-state layout/capacity mode selector; the concrete mode is not
      recovered. The context getter stays in retailOS behind the
      replaceable SHARED_CONTEXT function-pointer boundary. The
      original's `push {r4,lr}` never uses r4 — an ADS frame artifact
      not reproduced. match.py confirms the structural shape — call the
      getter, `ldr [r0,#0x94]`, `and #1` — 9 versus 5 instructions; the
      diff is only LLVM's push {fp,lr} frame and the indirect `blx`
      through the boundary static versus the ADS direct `bl`. Rust
      implementation and focused host tests:
      `src/ui/object_state.rs::context_mode_flag`.

  # --- nested-object companion-word accessor ---
  - address: 0x0805603c
    ghidra: FUN_0805603c
    name: object_nested_companion_word
    size: 12
    sig: "u32 object_nested_companion_word(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
    notes: >-
      Leaf read-only accessor: ldr r0,[r0,#0xf00]; ldr r0,[r0,#0xb54];
      bx lr. Follows the nested-object pointer at object+0xf00 (the same
      nested object object_nested_flag/property/attribute @
      0x08055f90/0x08055f9c/0x08055fa8 dereference) and returns the full
      32-bit word at +0xb54 — a word ldr, unlike the zero-extending ldrb
      of those byte accessors. One `bl 0x0805603c` call site (grep -c on
      decomp/osos.asm), at 0x08160278 in the tail-dispatch setup
      FUN_08160254: obtains the owner through FUN_08289690()->+0x28->+0x30,
      stores the sibling rtc_context_handle @ 0x08056124 (the nested
      object's +0x0c handle word) result at its +0x54, then passes this
      +0xb54 companion word as r1 to its vtable +0x84 method; the word's
      concrete meaning is not recovered. ABI: r0 is the sole pointer
      argument and return register; no stack access, writes, or null
      check. match.py confirms the structural shape — both ldr loads are
      instruction-identical (offsets 0xf00 and 0xb54); the diff is only
      LLVM's push {fp,lr}/pop {fp,pc} frame versus the ADS bx lr leaf.
      Rust implementation and focused host tests:
      `src/ui/object_state.rs::object_nested_companion_word`.

  # --- record-field text installer (query kind 2) ---
  - address: 0x08056048
    ghidra: FUN_08056048
    name: object_install_field_text
    size: 220
    sig: "i32 object_install_field_text(void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1: object
      iVar1/r6: status
      uVar2/uVar3: text_len (clamped)
      local_24c: query_word
      local_40: result.flags
      local_3e: result.text_len
      local_38: result.text
      local_1f8: result.field_word
      local_23c: result.tagged_word
      auStack_248: result (FieldQueryResult block, 0x238 bytes)
      FUN_08051600: field_query
      FUN_08042cbc: bcopy (ported; called directly)
      FUN_08049398: heap_free
      thunk_EXT_FUN_2200027c: iram_memzero_aligned_veneer
    notes: >-
      Fetches field kind 2 of the object's record through the retailOS
      query helper 0x08051600 and installs the returned text on the
      object. Zeroes the 0x2c-byte tail of a 0x238-byte stack result
      block (flags/length/pointer head at +0x208, via IRAM memzero
      veneer 0x08037db8) and a separate query word, then calls
      (object, 2, 0, 0, 0, &block, &word) — Ghidra's 12-byte
      auStack_248 is an artifact: the helper's own decompilation writes
      the block out to param_6 + 0x214, so argument six is the whole
      0x238-byte block. On a zero status it clamps the block's length
      halfword (+0x20a) to 0xff, bcopy-port-copies that many bytes from
      the block's text pointer (+0x210) into the object's inline buffer
      at +0x1c, NUL-terminates at +0x1c+len, stores the block's +0x50
      word into object+0x188, and — only when the object's tag halfword
      at +2 is 0x482b (the same tag the helper tests to pick its larger
      inline capacity) — stores the block's +0x0c word into object+4.
      Then, regardless of status, if bit 0 of the block's flags
      halfword is set it frees the text pointer through the retailOS
      heap free 0x08049398; the helper's status is returned in r0. One
      `bl 0x08056048` call site (grep -c on decomp/osos.asm), at
      0x08161fb8: applies it to the sub-record at r4+0x208 after
      building three sibling records and running 0x08058150, and
      discards the result — an init-time text/field population step;
      the fetched field's concrete meaning is not recovered.
      Deviations: the port zero-initializes the entire result block
      instead of memset-ing only its +0x208..+0x234 tail (the head
      fields are only read after the helper writes them), and skips the
      original's dead post-free cleanup (clearing the flag bit and
      nulling the pointer in the dying stack frame) plus the redundant
      strh re-zeroing the flags right after the memset. Query helper
      and heap free stay in retailOS behind replaceable FIELD_QUERY /
      HEAP_FREE function-pointer boundaries; the copy runs the ported
      bcopy @ 0x08042cbc directly. match.py confirms the structural
      shape — zero block, query call, length clamp (LLVM movcs vs ADS
      movhi: clamping 255 to 255 is a no-op), bcopy, NUL store, the
      +0x188 and conditional +4 word stores, the `tst #1` free gate —
      52 versus 55 instructions; the diff is LLVM's frame, the
      __aeabi_memclr4/__aeabi_memmove lowerings, and the indirect blx
      through the boundary statics versus the ADS direct bls. Rust
      implementation and focused host tests:
      `src/ui/object_state.rs::object_install_field_text`.

  # --- C++ object-header low-flag predicate ---
  - address: 0x0808539c
    ghidra: FUN_0808539c
    name: object_low_flags_clear
    size: 20
    sig: "u32 object_low_flags_clear(const void *object)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: object
      uVar1: flags
    notes: >-
      Pure leaf predicate: loads the aligned 32-bit object-header word at
      +0x04, tests mask 0x7, and returns 1 exactly when all three low bits
      are clear (otherwise 0). The complete retail body is 20 bytes:
      ldr; tst #7; moveq #1; movne #0; bx lr. The owning object type and
      individual flag meanings remain unidentified.

  # --- C++ object-header value store + flag reset ---
  - address: 0x08085344
    ghidra: FUN_08085344
    name: object_value_set_flags_clear
    size: 16
    sig: "void object_value_set_flags_clear(void *object, u32 value)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: object
      param_2: value
    notes: >-
      Pure leaf initializer for the two-word object header: stores `value`
      into the aligned 32-bit word at +0x00 and clears the entire 32-bit
      flag word at +0x04 (the same flag word object_low_flags_clear @
      0x0808539c tests). The complete retail body is 16 bytes:
      str r1,[r0]; mov r1,#0; str r1,[r0,#4]; bx lr. Three `bl
      0x08085344` call sites (grep -c on decomp/osos.asm), at 0x081b0594,
      0x081b05c4, and 0x081b06f4: all apply it to the sub-header at
      object+0xb8 with a value advanced through a data cursor (caller
      r6+0x34, bumped by consumed bytes), so the header reads as {
      position-or-pointer, flags }; the concrete object type remains
      unidentified, hence the field-level name. match.py confirms the
      structural shape: both stores and the `mov r1,#0` are
      instruction-identical; the diff is only LLVM's push {fp,lr}/pop
      {fp,pc} frame versus the ADS `bx lr` leaf. Rust implementation and
      focused host tests:
      `src/cxx/object_flags.rs::object_value_set_flags_clear`.

  # --- C++ singleton flag-word fetch-and-increment ---
  - address: 0x08085354
    ghidra: FUN_08085354
    name: object_flags_fetch_increment
    size: 68
    sig: "u32 object_flags_fetch_increment(void)"
    module: cxx/object_flags
    status: ported
    renames:
      iVar1: previous
      DAT_08085398: SINGLETON_OBJECT
    notes: >-
      Fetch-and-increment of the fixed singleton's +0x04 word (the same
      header offset object_low_flags_clear @ 0x0808539c tests and
      object_value_set_flags_clear @ 0x08085344 clear on their
      caller-passed objects) under the retailOS lock dispatcher
      FUN_08043b94: acquire (9,2,0,0) via the `bl` @ 0x08085368, read the
      word, store the value plus one, release (10,2,0,0) via the `bl` @
      0x0808538c, return the pre-increment value. The op 9/10 pair is the
      acquire/release bracket used throughout retailOS (59 `bl
      0x08043b94` sites); FUN_08043b94's own negative-argument path
      performs a timeout-queue wait, confirming the roles. SIZE IS 68
      BYTES: 64 of code (0x08085354..0x08085394) plus the 4-byte literal
      @ 0x08085398 holding the singleton address 0x08a0e9e0 — the lock
      machinery's own singleton (FUN_08075914 lazily points its +0x00
      word at the embedded vtable @ 0x08a0e9ec; FUN_080439e0 /
      FUN_08043e04 dispatch through its vtable slots +0x14 / +0x0c). The
      word's role (plain counter versus sequence number) is unverified,
      hence the field-level name. 0 `bl 0x08085354` call sites (grep -c
      on decomp/osos.asm) and no pointer reference anywhere in osos.dec —
      the function appears unreferenced on this ROM. Deviations: the
      unported dispatcher FUN_08043b94 (no names.yaml entry) rides the
      OBJECT_FLAGS_FETCH_INCREMENT_LOCK seam (house pattern —
      app/node_list.rs's NODE_LIST_ENQUEUE_OPS) instead of a direct `bl`,
      and host builds substitute test storage for the firmware singleton
      @ 0x08a0e9e0. match.py confirms the structural shape: both
      dispatch calls carry (9|10, 2, 0, 0), the ldr/add/str of the +0x04
      word and the pre-increment return in r4 are instruction-identical;
      the diff is only LLVM's push {r4,r5,fp,lr} frame, the indirect
      `blx` through the seam versus the ADS direct `bl`, and LLVM folding
      the +0x04 into the 0x08a0e9e4 literal. Rust implementation and
      focused host tests:
      `src/cxx/object_flags.rs::object_flags_fetch_increment`.

  # --- FreeType buffered-stream bounded reader wrapper ---
  - address: 0x08073e10
    ghidra: FUN_08073e10
    name: ft_stream_read_bounded
    size: 44
    sig: "i32 ft_stream_read_bounded(void *owner, void *buffer, u32 len, u32 extra)"
    module: ft/stream_read_bounded
    status: ported
    renames:
      param_1: owner
      param_1_plus_0x40000: stream_handle
      param_2: buffer
      param_3: len
      param_4: extra
    notes: >-
      Length-gated wrapper around the buffered-stream reader at 0x08042fd4.
      Requests above 0x40000 return !0xcf immediately; otherwise the wrapper
      loads the stream handle from owner+0x40000, spills the requested byte
      count into a one-word stack slot, and forwards stream, count-slot,
      buffer, and the live r3 word unchanged. The callee remains behind a
      host-mockable seam in src/ft/stream_read_bounded.rs: target builds
      transmute the retailOS 0x08042fd4 body directly, while host tests
      install a recorder that proves the guard and forwarding shape.
  # --- FreeType stream-skip owner wrapper ---
  - address: 0x08073e3c
    ghidra: FUN_08073e3c
    name: ft_stream_skip_from_owner
    size: 12
    sig: "i32 ft_stream_skip_from_owner(void *owner, i32 distance)"
    module: ft/stream_skip
    status: ported
    renames:
      param_1: owner
      param_1_plus_0x40000: stream
      param_2: distance
    notes: >-
      Raw ARM decoding confirms a 12-byte wrapper: add r0, r0, #0x40000;
      ldr r0, [r0]; b 0x0809d81c. It loads the owner's aligned stream word,
      preserves signed distance in r1, and tail-enters the shared FreeType
      stream-skip helper. Decoding every ARM B/BL word in osos.dec found 19
      direct call sites, all unconditional bl: there are no predicated or
      plain-b callers. The helper at 0x0809d81c is not ported; target builds
      use a literal veneer because the payload cannot retain the stock
      PC-relative branch, while host tests install a volatile callback seam
      that proves zero, positive, and i32::MIN distances pass unchanged.
  # --- FreeType size-capped stream read wrapper ---
  - address: 0x080764e4
    ghidra: FUN_080764e4
    name: ft_stream_read_capped
    size: 44
    sig: "i32 ft_stream_read_capped(void *owner, void *buffer, u32 len)"
    module: ft/stream_read_capped
    status: ported
    renames:
      param_1: owner
      param_1_plus_0x0: stream
      param_1_plus_0x8: limit
      param_2: buffer
      param_3: len
      FUN_08042fd4: buffered_stream_read
    notes: >-
      Size-gated wrapper around the buffered-stream reader at 0x08042fd4.
      The owner object carries the stream handle in its word at +0x00 and a
      size limit in its word at +0x08. A request longer than the limit
      (unsigned compare) returns !0xcf (-208) without touching the stream;
      otherwise the wrapper loads the stream handle from +0x00, seeds a
      one-word count slot with the requested length, and forwards stream,
      count slot, buffer, and the limit word itself as the live r3 fourth
      argument. Raw ARM confirms the exact 44-byte extent: the next
      separately linked function (resource_fork_probe) starts at 0x08076510.
      Decoding every ARM B/BL word in osos.dec found exactly 13 inbound
      branches, all unconditional bl at 0x08063f74, 0x0806405c, 0x0807f900,
      0x0807f958, 0x0807fa24, 0x0807fb2c, 0x0807fc0c, 0x0807fcd0,
      0x080963e0, 0x0809ed08, 0x0809edf4, 0x080be5f0, and 0x080be618; no
      predicated BL, no plain B callers, and no data-word references (not
      virtually dispatched). Ghidra's C drops the return value; the pop
      {r2,r3,ip,pc} epilogue returns the callee's r0 unchanged. Deviation:
      the 0x08042fd4 reader rides a host-mockable seam (same house pattern
      as the sibling ft_stream_read_bounded @ 0x08073e10); target builds
      transmute the retailOS entry. match.py structural review: same
      guard-load/compare/mvncc shape and same forwarding sequence; the diff
      is LLVM's push {fp,lr} frame and the indirect blx through the seam
      versus the ADS direct bl. Rust implementation and host tests:
      `src/ft/stream_read_capped.rs::ft_stream_read_capped`.
  # --- FreeType MacBinary resource-fork probe ---
  - address: 0x08076510
    ghidra: FUN_08076510
    name: resource_fork_probe
    size: 220
    sig: "u32 resource_fork_probe(void *library, FT_Stream stream, i32 face_index, void *face_out)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: library
      param_2: stream
      param_3: face_index
      param_4: face_out
      iVar1: error
      uVar2: data_fork_length
      local_94: macbinary_header
      local_93: filename_length
      bVar3: header_byte_valid
      bVar4: header_valid
    notes: >-
      FreeType's IsMacBinary probe, immediately called by the FT_Open_Face
      dfont fallback at 0x080853bc. It seeks stream to zero, reads the
      128-byte MacBinary header, and propagates either stream error
      unchanged. Bytes 0, 74, 82, and 63 must be zero; the filename length
      at byte 1 must be 1..33 and byte 2+length must be zero. Invalid input
      returns class 2 (unknown file format). It builds the big-endian
      data-fork length from bytes 83..86 and delegates
      128 + ((length + 127) & ~127) to FUN_0807f478, returning its result
      unchanged. The 220-byte ARM is `seek(0); read(128); header checks;
      ldrb 0x53..0x56; add #127; bic #127; add #128; bl 0x0807f478`.
      The unported dfont opener retains resource-map validation and rides
      DFONT_FALLBACK_OPS.open_dfont; resource_fork_probe is now the default
      probe seam. Focused tests cover malformed headers, read-error
      propagation, offset derivation, delegated arguments, and result
      propagation in src/cxx/object_flags.rs.

  # --- FreeType flattened resource-fork dfont opener ---
  - address: 0x0807f478
    ghidra: FUN_0807f478
    name: dfont_open
    size: 228
    sig: "u32 dfont_open(void *library, FT_Stream stream, u32 offset, i32 face_index, void *face_out)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: library
      param_2: stream
      param_3: resource_fork_offset
      param_4: face_index
      param_5: face_out
      uVar2: memory
      iVar1: result
      local_24: resource_map_offset
      local_28: resource_data_offset
      local_2c: resource_offsets
      local_30: resource_count
      DAT_0807f55c: RESOURCE_TAG_POST
      DAT_0807f560: RESOURCE_TAG_SFNT
    notes: >-
      FreeType's dfont opener, invoked by resource_fork_probe after its
      MacBinary-derived offset and directly by the FT_Open_Face fallback
      with offset zero. First calls FT_Raccess_Get_HeaderInfo
      (FUN_0804e38c) to validate the resource fork and recover absolute
      map/data offsets. It looks up `POST` (0x504f5354) using
      FT_Raccess_Get_DataOffsets (FUN_0804e17c); when found, calls the
      PostScript resource driver FUN_080c63cc and returns that result.
      Only a POST lookup failure tries `sfnt` (0x73666e74), then dispatches
      to FUN_080c6634. Header and second lookup failures propagate unchanged;
      a POST driver error never falls through to sfnt. Whichever driver was
      selected is followed by ft_mem_free(library->memory, offsets), even on
      driver error. ARM sequence: header; lookup POST; POST driver or lookup
      sfnt; sfnt driver; free. The 228-byte body is 0x0807f478..0x0807f55b;
      the two adjacent tag literals are at 0x0807f55c/0x0807f560. The four
      still-unported dependencies use the DFONT_OPEN_OPS seam; ft_mem_free
      @ 0x082cfae8 is ported and direct. Focused tests cover both tags,
      resource-map/data offset forwarding, driver and setup error
      propagation, and the exact offset-array release in
      src/cxx/object_flags.rs::tests.

  # --- FreeType FT_Open_Face dfont / fallback-rule retry stage ---
  - address: 0x080853b0
    ghidra: FUN_080853b0
    name: ft_open_face_dfont_fallback
    size: 200
    sig: "u32 ft_open_face_dfont_fallback(void *library, void *stream, i32 face_index, void *face_out, const u32 *open_args)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: library
      param_2: stream
      param_3: face_index
      param_4: face_out
      param_5: open_args
      uVar2: result
      iVar1: trace_levels
      pcVar3: outcome
      DAT_08085478: TRACE_LEVELS
      s_Try_as_dfont___s_____0808547c: TRY_AS_DFONT_FORMAT
      s_successful_08085494: OUTCOME_SUCCESSFUL
      s_failed_080854a0: OUTCOME_FAILED
      DAT_080854a8: OUTCOME_FORMAT
    notes: >-
      The format-fallback stage of FT_Open_Face (FUN_0804d6b8), reached
      from its single call site @ 0x0804d844 (1 `bl 0x080853b0` site,
      grep -c on decomp/osos.asm) after every registered font driver
      rejected the stream. Re-probes the stream as a Mac resource fork
      (FUN_08076510); on low-byte status class 2 (unknown format) it
      traces "Try as dfont: %s ..." (string @ 0x0808547c, arg open_args[3]
      = pathname), retries as a data-fork resource file
      (FUN_0807f478(library, stream, 0, face_index, face_out) — the
      `mov r2, #0` @ 0x080853f8), and traces "successful"/"failed"
      (strings @ 0x08085494/0x080854a0 under the "%s\n" format @
      0x080854a8) by whether the retry returned 0. Both traces fire only
      when the signed level at 0x08b209dc+0x34 (literal @ 0x08085478)
      exceeds 2. If the surviving class is then 2 or 0x55 and
      open_args[0] (flags) has bit 0x4 set, a final attempt runs through
      the fallback-rule chain FUN_080db8ac ("Try rule %d: %s offset %d")
      and its status is returned; every other status passes through
      unchanged. SIZE IS 200 BYTES: 196 of code (0x080853b0..0x08085474)
      plus the 4-byte literal @ 0x08085478 holding 0x08b209dc. The fifth
      argument arrives on the stack (ldr r5, [sp, #0x28] @ 0x080853b4).
      Deviations: the three unported callees ride the DFONT_FALLBACK_OPS
      seam (house pattern — app/node_list.rs's NODE_LIST_ENQUEUE_OPS)
      instead of direct `bl`s; the already-ported ft_error_trace @
      0x0804d17c takes the two trace calls directly (its unused r2/r3
      slots, garbage in retail, are passed as 0); host builds substitute
      test storage for the firmware trace-level block. match.py confirms
      the structural shape (probe; and #0xff; cmp #2; signed cmp #3 on
      the +0x34 level; outcome select; cmp #0x55; tst #4 on open_args[0];
      three calls, fifth argument homed via [sp]); the diff is LLVM's
      push {r4-r8,sl,fp,lr} frame, the indirect `blx`s through the seam,
      and the direct call into the ported ft_error_trace. Rust
      implementation and focused host tests:
      `src/cxx/object_flags.rs::ft_open_face_dfont_fallback`.

  # --- FreeType embedded resource-fork fallback-rule chain ---
  - address: 0x080db8ac
    ghidra: FUN_080db8ac
    name: fallback_rule_chain
    size: 360
    sig: "u32 fallback_rule_chain(void *library, FT_Stream stream, i32 face_index, void *face_out, const u32 *open_args)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: library
      param_2: stream
      param_3: face_index
      param_4: face_out
      param_5: open_args
      uVar5: memory
      iVar2: rule_index
      iVar3: trace_levels
      iVar4: result
      pcVar1: outcome
      local_54: new_names
      auStack_74: offsets
      local_94: errors
      local_b4: stream_open_args
      local_b8: candidate_stream
      local_a8: pathname
      DAT_080dba14: TRACE_LEVELS
    notes: >-
      FreeType FT_Raccess_Guess fallback after the dfont/MacBinary attempts.
      FT_Raccess_Guess (FUN_0804e53c) fills eight parallel pathname, offset,
      and error slots; each zero-error slot reopens its guessed pathname (or
      the original pathname when the guess is null) via FT_Stream_New
      (FUN_0804f250), attempts dfont_open at the corresponding offset, and
      calls FT_Stream_Free (FUN_0804ed9c) regardless of that attempt's
      outcome. The first successful dfont open exits the loop. Every
      non-null guessed pathname is released with ft_mem_free(library->memory,
      name), including skipped and post-success entries. Failure of every
      rule, stream open, or dfont attempt returns class 2; only a dfont
      success returns 0. ARM 0x080db8ac..0x080dba0f derives the arrays,
      traverses eight slots, then performs the independent cleanup loop;
      0x080dba14 is the trace-level literal. The unported guess/open/close
      helpers use FALLBACK_RULE_OPS; dfont_open, ft_mem_free, and
      ft_error_trace are direct. Focused object_flags host tests cover
      matching and failed multi-rule traversal, stream-open failure,
      pathname forwarding, and final status behavior.

  # --- FreeType PFR physical-font finalizer ---
  - address: 0x080a3554
    ghidra: FUN_080a3554
    name: pfr_phy_font_done
    size: 208
    sig: "void pfr_phy_font_done(void **phys, FT_Memory memory)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: phys
      param_2: memory
      piVar1: kern_item
      iVar2: next_kern_item
    notes: >-
      Upstream FreeType pfr_phy_font_done (pfrload.c), called once by
      pfr_face_done at 0x0808561c with the embedded PFR_PhyFontRec at
      face+0x120 and FT_FACE_MEMORY (face+0x64). The 208-byte ARM body
      FT_FREEs, in order, font_id (+0x50), family_name (+0x54), style_name
      (+0x58), vertical.stem_snaps (+0x4c), strikes (+0x64), chars (+0x80),
      blue_values (+0x6c), then every +0x88 kern_items list node after
      loading its +0x00 next link. It zeroes each freed slot and precisely
      the associated counts/aliases: vertical count (+0x48), horizontal
      stem pointer/count (+0x40/+0x3c), strike count/capacity (+0x5c/+0x60),
      character count/offset (+0x78/+0x7c), blue-value count (+0x68), and
      kern list head/tail/pair count (+0x88/+0x8c/+0x84). Horizontal snaps
      is an interior alias into the vertical allocation and is therefore
      cleared without being freed. There is no entry null guard; null owned
      slots are valid because ft_mem_free @ 0x082cfae8 suppresses its
      callback. Raw ARM, the Ghidra C, and the upstream PFR_PhyFontRec
      layout agree on every field. match.py confirms the ordered loads,
      free calls, stores, and next-before-free list traversal; the expected
      LLVM diff is ft_mem_free inlining (null cmp / callback blx) and frame
      allocation. Rust implementation and focused host tests:
      `src/cxx/object_flags.rs::pfr_phy_font_done`.

  # --- FreeType PFR driver face finalizer ---
  - address: 0x080855f8
    ghidra: FUN_080855f8
    name: pfr_face_done
    size: 60
    sig: "void pfr_face_done(void **face)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: face
      uVar1: memory
    notes: >-
      Upstream FreeType pfr_face_done (pfrdrivr.c), the PFR driver's
      FT_Done_Face. Loads the allocator from the face's driver
      (FT_FaceRec.driver @ +0x60 -> FT_ModuleRec.memory @ +0x08,
      upstream pfrface->driver->root.memory), NULLs family_name (+0x14)
      and style_name (+0x18) so no dangling pointers outlive the face,
      tears down the embedded physical-font record at +0x120 with the
      face's own memory (+0x64) through pfr_phy_font_done
      (FUN_080a3554 — 1 `bl 0x080a3554` site, @ 0x0808561c,
      here), then FT_FREEs available_sizes (+0x20) with the driver
      memory: ft_mem_free(memory, available_sizes) plus the macro's own
      NULL store. The complete retail body is 60 bytes: push
      {r4,r5,r6,lr}; ldr r0,[r0,#0x60]; ldr r6,[r0,#0x8]; str #0 @
      +0x14/+0x18; bl 0x080a3554(face+0x120, [face,#0x64]); bl
      0x082cfae8(r6, [face,#0x20]); str #0 @ +0x20; pop {r4,r5,r6,pc}.
      0 `bl 0x080855f8` call sites (grep -c on decomp/osos.asm) and no
      data pointer anywhere in osos.dec (binary-scanned): the function
      is reached through the PFR driver class's done_face slot, whose
      record does not survive in the decrypted image — upstream stores
      done_face adjacent to init_face = FUN_08085634, likewise absent,
      so the class record presumably sits in one of the image's
      undecrypted zero holes. The PFR pin is FUN_08085634 itself
      (pfr_face_init: checks the PFR header signature at PFR_Face
      +0x84, the header right after the 0x84-byte FT_FaceRec) plus
      FUN_080a3624 = pfr_phy_font_load, whose trace string reads
      "pfr_phy_font_load: invalid physical font" and which
      FT_LIST-initializes the same +0x88 extra-items list that
      FUN_080a3554 walks and frees. The FT_FaceRec field offsets are
      the upstream layout: FT_Open_Face's face object is 0x84 bytes
      here (pfr_face_init finds the PFR header at face+0x84), and the
      statement-for-statement match with upstream pfr_face_done —
      driver@+0x60, family/style@+0x14/+0x18, available_sizes@+0x20,
      memory@+0x64, phy_font@+0x120 — pins each field. The port now calls
      pfr_phy_font_done directly. Deviations: the already-ported
      ft_mem_free @ 0x082cfae8 takes the FT_FREE half directly (LLVM
      inlines its null-guarded `blx [memory,#8]` body — the same inlining
      deviation ft_mem_alloc's entry records); and the face fields are
      addressed by word index (byte-exact +0x14..+0x120 on the 32-bit
      target, disjoint words on a 64-bit host —
      cxx/handle.rs's handle_deref_field12 model). match.py confirms the
      structural shape: the ldr-@+0x60 / ldr-@+8 allocator chain, both
      NULL stores, the +0x64 load and face+0x120 add, direct pfr_phy_font_done
      call, and +0x20 load/store bracket around the free. The diff is
      LLVM's push {r4-r6,sl,fp,lr} frame and inlined ft_mem_free
      (cmp/beq guard plus `blx [memory,#8]`) replacing the
      `bl 0x082cfae8`.
      Rust implementation and focused host tests:
      `src/cxx/object_flags.rs::pfr_face_done`.


  # --- String-registry fallback name hash ---
  - address: 0x082d7e54
    ghidra: FUN_082d7e54
    name: registry_default_name_hash
    size: 88
    sig: "u32 registry_default_name_hash(const u8 *name)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: name
      param_2: unused
      param_3: first_byte
      uVar1: rotation
      pbVar2: next_byte
      uVar3: hash
      uVar4: salt
      uVar5: salted_byte
    notes: >-
      Default NUL-terminated name hash called by registry_key_hash's
      fallback branch at 0x080855e4. The raw ARM body is 88 bytes:
      it returns 0 for a null pointer or an initial NUL; otherwise it
      begins with hash=0 and salt=0x100, and for each unsigned input byte
      b forms c = b | salt, rotates hash left by
      (c ^ (c >> 2)) & 0xf (the `rsb #0; ror` pair), XORs the
      low 32 bits of c*c, and advances salt by 0x100 modulo 2^32.
      It stops at the first subsequent NUL and returns hash ^ (hash >> 16).
      Ghidra's param_2/param_3 are not ABI inputs: the raw code overwrites
      r1 with the zero accumulator and r2 with the first byte/salt. No
      deviations. match.py (expected exit 1) confirms the null/empty
      guard, byte loop, xor/lsr-and rotation count, negated ROR, multiply,
      next-byte termination check, salt increment, and final 16-bit fold.
      Focused host tests cover null, NUL-only/terminated input, non-ASCII
      bytes, and distinct byte-position salts in
      `src/cxx/object_flags.rs::registry_default_name_hash`.

  # --- String-registry key hash (namespace index XOR name hash) ---
  - address: 0x080855a0
    ghidra: FUN_080855a0
    name: registry_key_hash
    size: 84
    sig: "u32 registry_key_hash(const usize *key)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: key
      iVar1: registry_singleton
      iVar2: count
      puVar3: provider
      uVar4: name_hash
      DAT_080855f4: REGISTRY_SINGLETON
    notes: >-
      The key hash of the retailOS string registry rooted at the fixed
      singleton @ 0x08a0ea6c (the 4-byte literal @ 0x080855f4, just past
      the 84 code bytes 0x080855a0..0x080855f3). A key is three words —
      namespace index (+0x00), unused (+0x04), name string pointer
      (+0x08) — and the hash combines the index with a hash of the
      name: when the singleton's +0x08 providers array exists and its
      signed entry count exceeds the key's index (`ble` @ 0x080855c4
      falls back), the provider object at table[index] supplies the
      name hash through its vtable slot +0x00 (the `blx r1` @
      0x080855d8); otherwise the registry's default name hash
      FUN_082d7e54 computes it (unported: 88 bytes of per-byte salted
      rotate-xor-multiply — c = b | 0x100*(i+1); h = rol(h, (c ^
      c>>2) & 0xf) ^ c*c — folded as h ^ (h>>16); null/empty name
      hashes to 0). The return is the key's index word, reloaded after
      the hash call (`ldr r1,[r4,#0x0]` @ 0x080855e8), XORed with the
      name hash. The two tiny accessor callees are FUN_08369780 (16
      bytes: signed count at the providers object's +0x00 word, or -1
      for null) and FUN_08369864 (20 bytes: table[index] of the +0x04
      table, or null); the +0x08 providers word is reloaded before the
      table index (`ldr r0,[r5,#0x8]` @ 0x080855ac and again @
      0x080855c8). 0 `bl 0x080855a0` call sites (grep -c on
      decomp/osos.asm) and no data pointer anywhere in osos.dec
      (binary-scanned) — like pfr_face_done @ 0x080855f8 next door,
      the record referencing it does not survive the decrypted image.
      The registry is pinned by the singleton's other users (literal
      pools @ 0x0805e8a4 / 0x0805e938 / 0x0805e984 / 0x0807d174):
      FUN_0805e7dc inserts {index, flag, name, value} nodes into the
      singleton's +0x00 hash map (lazily created by FUN_0805e93c
      through FUN_082d7d08, a 16-bucket table with pluggable hash/eq)
      and then notifies the same providers array's entry through
      vtable slot +0x08; FUN_0805e8a8 looks nodes up by {index, name},
      chasing +0x0c value links up to ten deep while the 0x8000 flag
      bit is clear. The subsystem's own name is unlocated, so
      "registry" names only this observed insert/lookup/notify
      cluster. Deviations: the three unported callees ride the
      REGISTRY_KEY_HASH_OPS seam (house pattern — DFONT_FALLBACK_OPS)
      instead of direct `bl`s; host builds substitute test storage for
      the firmware singleton @ 0x08a0ea6c; the key and singleton words
      are addressed by pointer-sized word index (byte-exact on the
      32-bit target, disjoint slots on a 64-bit host — the
      pfr_face_done face-word model). match.py confirms the structural
      shape: the null check, the signed cmp/ble gate on the count, the
      providers-word reload, the vtable-slot-0 load, the key-word
      reloads and the final eor are instruction-identical; the diff is
      LLVM's push {r4-r8,sl,fp,lr} frame, the indirect `blx`s through
      the seam, the constant-folded 0x08a0ea74 providers-word literal,
      and LLVM tail-merging the default-hash and vtable-hash calls
      into a single `blx r5` (r5 preloaded with the seam's
      default_name_hash, overwritten by the vtable slot on the
      provider path). Rust implementation and focused host tests:
      `src/cxx/object_flags.rs::registry_key_hash`.

  # --- Bitstream byte-alignment stuffing check (0 bit then 1 bits) ---
  - address: 0x0808554c
    ghidra: FUN_0808554c
    name: bitstream_stuffing_to_byte_check
    size: 80
    sig: "u32 bitstream_stuffing_to_byte_check(u8 *stream)"
    module: cxx/object_flags
    status: ported
    renames:
      param_1: stream
      iVar1: read_result
    notes: >-
      Validates and consumes one byte-alignment stuffing sequence of
      the {data-pointer @ +0x00, bit-position @ +0x04} stream object
      whose initializer is object_value_set_flags_clear @ 0x08085344
      ({value, 0} into +0x00/+0x04) and whose predicate
      object_low_flags_clear @ 0x0808539c reports "+0x04 low three
      bits clear" — the bit position sitting on a byte boundary.
      Reads one bit, which must be 0, then keeps reading bits, each
      of which must be 1, until the position is byte-aligned again;
      returns 0 once aligned and 1 the moment any bit breaks the
      pattern. Every read goes through the unported read-and-advance
      FUN_080ebbe0(stream, 1) (the `mov r1, #0x1` @ 0x08085554 and
      @ 0x08085568) — 28 bytes wrapping FUN_080efa38's MSB-first
      fetch (bit i is bit 7-(i&7) of byte i>>3 of the +0x00 buffer
      at the +0x04 bit position) with a +0x04 += bit_count advance
      (64 `bl 0x080ebbe0` sites, all in the video header parser
      cluster 0x0807d7xx..0x080f1xxx) — so each call nudges the +0x04
      position by one. The complete retail body is 80 bytes (20
      instructions, no literal pool): push {r4,lr}; mov r4,r0;
      mov r1,#1; bl 0x080ebbe0; cmp #0; bne-ret1; loop: mov r0,r4;
      bl 0x0808539c; cmp #0; beq-read; mov r0,#0; pop; read:
      mov r1,#1; mov r0,r4; bl 0x080ebbe0; cmp #1; bne-ret1;
      b-loop. The only 2 `bl 0x0808554c` sites (@ 0x080ed0e8 and
      @ 0x080f0cec, grep -c on decomp/osos.asm) sit in the video
      header parser that matches 32-bit start codes 0x000001B5 /
      0x000001B3 (`sub r12,r0,#0x100; subs r12,r12,#0xb5/#0xb3` @
      0x080ecfcc/0x080f0d14), a 24-bit == 1 prefix with an 8-bit
      code < 0x20 (@ 0x080ed104-0x080ed120), MPEG's
      video_signal_type field pattern (1-bit flag gating 3-bit
      format, 1-bit range, and a 1-bit colour-description flag
      gating three 8-bit tables @ 0x080ed074-0x080ed0e0), and
      FUN_0807d724's scan to the next 0x000001xx start code (which
      both callers run only when this function returns 0). The
      0-bit-then-1-bits-to-the-byte-boundary shape is the
      byte-alignment stuffing an MPEG-style stream writes before a
      start code, and the return value gates the scan for that
      start code; the parser's own name is unlocated, so the name
      claims only the verified bitstream behavior. Deviations: the
      unported read-and-advance rides the BITSTREAM_READ_ADVANCE
      seam (house pattern — OBJECT_FLAGS_FETCH_INCREMENT_LOCK)
      instead of a direct `bl`; the ported object_low_flags_clear
      takes the byte-alignment test directly. match.py confirms the
      structural shape: the first read with cmp #0 branching to the
      return-1 block, the loop's bl into the ported
      object_low_flags_clear with cmp #0, the second read with
      cmp #1 looping back, and the two 0/1 return blocks are all
      present; the diff is LLVM's push {r4-r6,sl,fp,lr} frame, the
      `blx` through the seam (with the seam word reloaded per call
      site) versus the ADS direct `bl`, and LLVM's loop rotation.
      Rust implementation and focused host tests:
      `src/cxx/object_flags.rs::bitstream_stuffing_to_byte_check`.

  - address: 0x083d73bc
    ghidra: FUN_083d73bc
    name: less_unsigned_byte
    size: 24
    sig: "u32 less_unsigned_byte(void *this, const u8 *a, const u8 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: a, param_3: b}
    notes: >-
      `std::less<unsigned char>::operator()(const u8 &, const u8 &)` —
      the byte-width member of the less_signed @ 0x083d7580 /
      less_unsigned @ 0x083d7598 comparator family: ldrb both operand
      references, unsigned cmp, return 1/0 via `movcs #0` / `movcc #1`.
      `this` arrives in r0 and is immediately overwritten, as with the
      word-sized functors. 10 `bl 0x083d73bc` sites (grep -c on
      decomp/osos.asm); callers in the byte key tree
      (byte_key_tree_insert_node @ 0x083b8844) use it as the
      key-ordering predicate deciding left vs right insertion. The
      byte-identical twin @ 0x083d73d4 (3 bl sites) is deliberately
      NOT ported here — a separate future commit. Deviations: none
      semantic. match.py (exit 1, diff expected): same
      ldrb/ldrb/cmp + predicated 0/1 return shape; the diff is LLVM's
      push {fp,lr} frame and instruction scheduling. Host tests prove
      the equal/less/greater truth table with 0 and 255 edges plus an
      exhaustive 256x256 sweep against a reference `u8 < u8`:
      src/cxx/templates.rs::less_unsigned_byte_edges /
      less_unsigned_byte_matches_reference_exhaustively.

  - address: 0x083d73d4
    ghidra: FUN_083d73d4
    name: less_unsigned_byte_alias_73d4
    size: 24
    sig: "u32 less_unsigned_byte_alias_73d4(void *this, const u8 *a, const u8 *b)"
    module: cxx/templates
    status: ported
    vars: {param_1: this, param_2: a, param_3: b}
    notes: >-
      ALIAS — byte-identical second instantiation of less_unsigned_byte
      @ 0x083d73bc (all 24 bytes: `ldrb r0,[r1]; ldrb r1,[r2]; cmp
      r0,r1; movcs r0,#0; movcc r0,#1; bx lr`), the same
      `std::less<unsigned char>::operator()(const u8 &, const u8 &)`
      functor. 3 `bl 0x083d73d4` sites (grep on decomp/osos.asm):
      0x083b9bec and 0x083b9d10 in FUN_083b9bac, 0x083b9de4 in
      FUN_083b9d38 — both callers sit in the byte-key-tree cluster
      beside byte_key_tree_insert_node @ 0x083b8844 and use it as the
      key-ordering predicate (walk `ldreq right(+0xc)` / `ldrne
      left(+0x8)`; insert child pick `streq`/`strne`). Reference C:
      decomp/c/037/083d73d4_FUN_083d73d4.c. Ported as its own exported
      symbol with an identical body under a distinct link_section (the
      vector_size_elem4_alias_76c8 precedent, NOT the ledger-only
      not_equal_deref aliases) so LLVM's identical-function folding
      keeps both labels hookable. match.py (exit 1, diff expected):
      same ldrb/ldrb/cmp + predicated 0/1 return shape as the primary;
      byte-identical inputs make the diff identical (LLVM frame +
      scheduling). Host tests: truth-table spot checks plus an
      exhaustive 256x256 equivalence sweep against the primary —
      src/cxx/templates.rs::less_unsigned_byte_alias_73d4_truth_table /
      less_unsigned_byte_alias_73d4_matches_primary_exhaustively.

  - address: 0x0811d68c
    ghidra: FUN_0811d68c
    name: vtable_set_50_kind4
    size: 64
    sig: "u32 vtable_set_50_kind4(handle: *mut *mut u8, selector: u32, value: *const u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: selector
      param_3: value
    notes: >-
      21 bl call sites (grep on decomp/osos.asm): 5-site cluster
      0x0811c60c..0x0811c710, 0x0811e8c8, 3-site cluster
      0x081b0094..0x081b0120, 0x081bc700, 6-site cluster
      0x081d1408..0x081d14e4, 5-site cluster 0x082713e0..0x08271448.
      Three-stage guarded property-write dispatcher of the kind-4
      vtable message family (slot +0x50 twin of util/vtable_query.rs's
      vtable_query_4c_kind4 @ 0x0811d46c, which uses slot +0x4c):
      open = FUN_0811d458 (dispatch(handle, 4, &bare_selector)); if
      nonzero return it; write = FUN_0811d56c (reuses its push {r2,r3}
      spill slots as the message: dispatch(handle, 4, &4) then, only
      on 0, dispatch(handle, 4, &{*value, 4})); if nonzero return it;
      else tail-call commit = FUN_0811d340 (dispatch(handle, 4,
      &(selector | 0x80000000))). Arg routing: r5 = arg2 (selector)
      feeds stages 1 and 3, r6 = arg3 (pointer to the caller's value
      word) feeds only stage 2. Tag-bit family: neighbours 0x0811d6ec /
      0x0811d6cc are the same one-dispatch thunk with 0x40000000 /
      0xc0000000 tags (0x0811d6ec is the callers' "supported?" probe);
      sibling 0x0811d64c is the identical three-stage shape with the
      u16/kind-2 write stage 0x0811d52c (ldrh) - kind encodes the value
      width. All stages bottom out in the unported slot +0x50
      dispatcher FUN_0811d7fc (stmdb sp!,{r3,lr}; ldr r0,[r0]; ldr
      r3,[r0]; ldr r12,[r3,#0x50]; mov r3,sp; blx r12). Deviations:
      the three callees sit behind the VTABLE_SET_50_KIND4_OPS seam
      (class_6800.rs CLASS_6800_OPS pattern) whose defaults model each
      callee's exact body; 0x0811d7fc sits behind the
      VTABLE_SLOT_50_DISPATCH seam (vtable_query.rs
      VTABLE_SLOT_4C_DISPATCH pattern, read_unaligned slot load); the
      caller's r3 is forwarded verbatim to every stage, modeled as a
      4th parameter `forwarded` (vtable_query_4c_kind4 precedent).
      Reference C decomp/c/010/0811d68c_FUN_0811d68c.c inlines the
      commit thunk as FUN_0811d7fc(param_1, 4, &stack) (hiding the
      0x80000000 tag) and drops stage 1's selector + the r3 forwarding
      - the port follows the disassembly. match.py (exit 1, diff
      expected): same three-call/two-guard/tail-call structure; the
      diff is the crate fp frame and the ops-table ldr/blx standing in
      for the direct bl 0x0811d458 / 0x0811d56c / b 0x0811d340 - the
      documented seams. Host tests: open/write short-circuits, full
      order + r5/r6 arg routing, default stage bodies' message
      sequence (bare selector, kind word, {value, kind}, tagged
      selector) and a fake-vtable end-to-end through the default
      dispatcher body.

  - address: 0x082bc4fc
    ghidra: FUN_082bc4fc
    name: cg_timer_wait
    size: 96
    sig: "u32 cg_timer_wait(u32 ticks, void *event)"
    module: codegen/timer_wait
    status: ported
    vars: {param_1: ticks, param_2: event, uVar1: version, uVar2: channel, FUN_080e624c: board_version, FUN_0836dc14: timer_channel_arm, FUN_0836d854: notify_wait_enter, FUN_0836dd90: timer_channel_start, thunk_EXT_FUN_22001f78: event_wait, FUN_0836ddbc: timer_channel_stop, FUN_0836d82c: notify_wait_exit}
    notes: >-
      3 bl call sites, all from heap_alloc_core (FUN_0819d048 @
      0x0819d048, now ported in heap/alloc_core — the port deliberately
      omits this OOM low-memory trace as a documented deviation) @
      0x0819d278/0x0819d284/0x0819d290 (tick counts 0x4b0, a global,
      0x960 against one event handle). A timed wait on an event handle
      built on an S5L8702 timer channel (block at 0x3c700000, 0x20-byte
      stride): channel = 3 when board_version()'s HIGH halfword == 0x10
      and LOW halfword >= 6 (cmp r1,r0,lsr#16 / movcs r4,#3), else 0;
      then the fixed sequence timer_channel_arm(channel, ticks) ->
      notify_wait_enter() -> timer_channel_start(channel) ->
      event_wait(event) -> timer_channel_stop(channel), tail-calling
      notify_wait_exit(), whose result (always 0) is the return value.
      Callees: 0x080e624c board_version (lazily cached sysinfo word,
      +0x84 of 0x08369bec's object behind a 0x7fffffff sentinel);
      0x0836dc14 timer_channel_arm (smallest prescaler n<=0x400 with
      CLK/(ticks*n)<=0x10000, count -> ch+0x08, n-1 -> ch+0x10, 0x40 ->
      ch+0x00, cmd word ch+0x04 |= 2, left stopped); 0x0836dd90 /
      0x0836ddbc timer_channel_start/stop (cmd word ch+0x04 bit 0
      set/clear); 0x0836d854 / 0x0836d82c notify_wait_enter/exit
      (mailbox pairs (6,3,0),(7,5,0) / (6,1,0),(7,1,0) through the
      command writer @ 0x0836b5b0); 0x08037ef0 event_wait (ROM veneer
      to 0x22001f78, mirror of osos 0x08001f78: poll an event service
      with the handle until ready). Deviation: all seven callees are
      unported and sit behind the CG_TIMER_WAIT_OPS read_volatile seam
      (heap.rs CG_HEAP_OPS pattern) with missing_* spin-loop defaults
      (node_list.rs convention); the tail b is a returned seam call.
      match.py (exit 1, diff expected): identical call sequence and
      two-halfword channel-select condition; LLVM even emits the final
      seam call as a real tail `bx r0`, matching the original's
      `b 0x0836d82c`; the diff is the ops-table ldr/blx pairs and the
      crate fp frame. Host tests: channel select boundaries (high !=
      0x10, low < 6, low == 6, low > 6, 0xffff), fixed seven-call
      order, verbatim ticks/event/channel forwarding, and tail-call
      result propagation.

  - address: 0x0811d6cc
    ghidra: FUN_0811d6cc
    name: vtable_set_50_commit_probe_kind4
    size: 32
    sig: "u32 vtable_set_50_commit_probe_kind4(handle: *mut *mut u8, selector: u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: selector
    notes: >-
      14 bl call sites (grep on decomp/osos.asm): 0x0810aa30, 3-site
      cluster 0x0811c724..0x0811c788, 3-site cluster
      0x08136640..0x08136694, 0x081b0134, 0x081b015c, 0x081bc714,
      0x081d0e80, 2-site cluster 0x081d1504..0x081d1518, 0x08285864.
      One-dispatch commit+probe thunk of the kind-4 vtable message
      family (slot +0x50; sibling of vtable_set_50_kind4 @ 0x0811d68c):
      stmdb sp!,{r0,r1,r4,lr}; ldr r1,[sp,#4]; add r2,sp,#4; orr
      r1,r1,#0xc0000000; str r1,[sp,#4]; mov r1,#4; bl 0x0811d7fc;
      ldmia sp!,{r2,r3,r4,pc} - handle passes through in r0 untouched,
      kind 4 in r1, r2 points at the stack slot holding
      selector | 0xc0000000 (COMMIT_PROBE_TAG: the 0x80000000 commit
      tag of the sibling's third stage 0x0811d340 plus the 0x40000000
      "supported?" probe tag of the unported neighbour 0x0811d6ec;
      exact protocol meaning not established), and the dispatcher's r0
      returns unbranched. Callers issue it after successful writes
      (e.g. probe 0x0811c6f0 -> vtable_set_50_kind4 0x0811c710 ->
      this call 0x0811c724), always branching on the error code.
      Deviations: callee 0x0811d7fc is unported and sits behind the
      existing VTABLE_SLOT_50_DISPATCH seam (this body IS the
      0x0811d340 stage shape with a wider tag, so no new seam); the
      caller's r3 is forwarded verbatim, modeled as a 3rd parameter
      `forwarded` (vtable_query_4c_kind4 / vtable_set_50_kind4
      precedent). Reference C decomp/c/010/0811d6cc_FUN_0811d6cc.c
      invents a phantom 5th argument (FUN_0811d7fc(param_1, 4,
      &local_c, param_4, param_1)) - the dispatcher consumes r0..r3
      only; the port follows the disassembly. match.py (exit 1, diff
      expected): same orr 0xc0000000 / mov r1,#4 / tagged-stack-slot /
      single-dispatch structure; the diff is the crate fp frame and the
      seam's ldr/blx standing in for bl 0x0811d7fc. Host tests:
      0xc0000000 tagging + argument routing (handle verbatim, kind 4,
      stack-slot indirection, r3 forwarding), orr-not-replace on
      selectors already carrying each tag bit, and verbatim return
      forwarding -
      src/app/vtable_set.rs::commit_probe_tags_selector_and_routes_arguments /
      commit_probe_ors_instead_of_replacing_the_high_bits /
      commit_probe_forwards_the_dispatch_return_verbatim.
  - address: 0x0811d6ec
    ghidra: FUN_0811d6ec
    name: vtable_set_50_probe_kind4
    size: 32
    sig: "u32 vtable_set_50_probe_kind4(handle: *mut *mut u8, selector: u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: selector
    notes: >-
      16 bl call sites (grep on decomp/osos.asm): 0x0810a9c4, 3-site
      cluster 0x0811c5ec..0x0811c6f0, 0x0811e8b0, 3-site cluster
      0x081365b0..0x08136654, 2-site cluster 0x081b004c..0x081b0070,
      0x081bc6b0, 0x081d0e34, 2-site cluster 0x081d13f0..0x081d149c,
      0x082713c8, 0x08285804. The "supported?" probe thunk of the
      kind-4 vtable message family (slot +0x50; sibling of
      vtable_set_50_kind4 @ 0x0811d68c and
      vtable_set_50_commit_probe_kind4 @ 0x0811d6cc, whose body sits
      one instruction earlier and differs only in the orr immediate):
      stmdb sp!,{r0,r1,r4,lr}; ldr r1,[sp,#4]; add r2,sp,#4; orr
      r1,r1,#0x40000000; str r1,[sp,#4]; mov r1,#4; bl 0x0811d7fc;
      ldmia sp!,{r2,r3,r4,pc} - handle passes through in r0 untouched,
      kind 4 in r1, r2 points at the stack slot holding
      selector | 0x40000000 (PROBE_TAG; exact protocol meaning not
      established, see the module header), and the dispatcher's r0
      returns unbranched. Callers issue it as a probe before the
      guarded write (e.g. this call 0x0811c6f0 -> vtable_set_50_kind4
      0x0811c710 -> commit-probe 0x0811c724), always branching on the
      error code. Deviations: callee 0x0811d7fc is unported and sits
      behind the existing VTABLE_SLOT_50_DISPATCH seam (the body is
      the sibling's shape with a narrower tag, so no new seam); the
      caller's r3 is forwarded verbatim, modeled as a 3rd parameter
      `forwarded` (vtable_query_4c_kind4 / vtable_set_50_kind4 /
      vtable_set_50_commit_probe_kind4 precedent). Reference C
      decomp/c/010/0811d6ec_FUN_0811d6ec.c invents a phantom 5th
      argument (FUN_0811d7fc(param_1, 4, &local_c, param_4, param_1))
      - the dispatcher consumes r0..r3 only; the port follows the
      disassembly. match.py: same orr 0x40000000 / mov r1,#4 /
      tagged-stack-slot / single-dispatch structure; the diff is the
      crate fp frame and the seam's ldr/blx standing in for bl
      0x0811d7fc. Host tests: 0x40000000 tagging + argument routing
      (handle verbatim, kind 4, stack-slot indirection, r3
      forwarding), orr-not-replace on selectors already carrying each
      tag bit, and verbatim return forwarding -
      src/app/vtable_set.rs::probe_tags_selector_and_routes_arguments /
      probe_ors_instead_of_replacing_the_high_bits /
      probe_forwards_the_dispatch_return_verbatim.

  - address: 0x0811d7fc
    ghidra: FUN_0811d7fc
    name: vtable_slot_50_dispatch
    size: 28
    sig: "u32 vtable_slot_50_dispatch(handle: *mut *mut u8, kind: u32, data: usize, extra: *const usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: kind
      param_3: data
      param_4: extra
    notes: >-
      15 bl call sites (grep on decomp/osos.asm), all inside the slot
      +0x50 message-family thunk cluster: the commit stage 0x0811d340
      (site 0x0811d358), the multi-message routine 0x0811d360 (eight
      sites 0x0811d390..0x0811d440), the open stage 0x0811d458
      (0x0811d464), the kind-2 write stage 0x0811d52c (0x0811d550),
      the kind-4 write stage 0x0811d56c (0x0811d590), the tag thunks
      vtable_set_50_commit_probe_kind4 0x0811d6cc (0x0811d6e4) and
      vtable_set_50_probe_kind4 0x0811d6ec (0x0811d704), and the
      routine at 0x0811d880 (0x0811d890). The shared vtable
      dispatcher of the slot +0x50 family - the slot +0x50 twin of
      the still-unported FUN_0811d7b0 @ 0x0811d7b0
      (util/vtable_query.rs), differing only in the slot offset:
      stmdb sp!,{r3,lr}; ldr r0,[r0]; ldr r3,[r0]; ldr r12,[r3,#0x50];
      mov r3,sp; blx r12; ldmia sp!,{r12,pc} - double dereference
      handle -> object -> vtable, the method at slot +0x50 invoked as
      method(object, kind, data, &spilled_r3), its error code
      (0 = success) returned verbatim; the spilled r3 is whatever the
      caller carried (no call site sets it deliberately - every
      caller is a message thunk forwarding its own caller's r3).
      Deviations: the r3 spill is collapsed into the 4th parameter -
      the original receives r3 by value and spills it itself
      (stmdb/mov r3,sp), but the port's callers pre-spill the
      forwarded word and pass its address (vtable_query.rs
      VTABLE_SLOT_4C_DISPATCH precedent), so `extra` reaches the
      method verbatim; the export is therefore NOT ABI-hookable at
      0x0811d7fc, where r3 arrives by value (no hook targets it). The
      blx targets are firmware vtable methods, so the ported body
      stays the wired default of the VTABLE_SLOT_50_DISPATCH seam and
      the three ported siblings (vtable_set_50_kind4 0x0811d68c,
      vtable_set_50_commit_probe_kind4 0x0811d6cc,
      vtable_set_50_probe_kind4 0x0811d6ec) keep routing through it.
      REWIRING OPPORTUNITY (follow-up, one function per commit): the
      app/class_6800.rs precedent calls ported callees directly, so
      the siblings' read_volatile seam reads can become direct calls
      to vtable_slot_50_dispatch in separate commits. read_unaligned
      slot load keeps the layout byte-exact on a 64-bit host (0x50 is
      4-aligned but not 8-aligned). match.py (exit 1, diff expected):
      same double-deref / slot-load / indirect-call shape; LLVM
      lowers read_unaligned on ARMv5 to 4x ldrb + orr assembly
      instead of ldr r12,[r3,#0x50], tail-calls (pop then bx) instead
      of blx/ldmia-pc, and needs no mov r3,sp because `extra`
      already arrives in r3 - the documented collapsed-spill
      deviation; the crate fp frame accounts for the rest. Host tests
      (direct calls on a fake vtable): double-deref chain traversal +
      slot +0x50 selection against +0x48/+0x58 decoys, verbatim
      r3-spill pointer forwarding, verbatim return forwarding -
      src/app/vtable_set.rs::dispatch_double_dereferences_and_loads_slot_50_exactly
      / dispatch_forwards_the_spilled_r3_pointer_verbatim /
      dispatch_returns_the_methods_error_code_verbatim.

  - address: 0x082c23d4
    ghidra: FUN_082c23d4
    name: cg_buffer_current_offset
    size: 8
    sig: "u32 cg_buffer_current_offset(cg_codegen_buffer_t *output)"
    module: codegen/ir
    status: ported
    vars: {param_1: output}
    notes: >-
      8 bl call sites, all passing codegen->output (+0x10 of the
      caller's cg_codegen_t): the ported cg_label_add_fixup @
      0x082c17ac plus unported 0x082c0e28, 0x082c1320 (x3),
      0x0824a43c (x2), 0x082493c0. A pure leaf getter - the whole
      body is `ldr r0,[r0,#0x804]; bx lr` - returning
      output->current_offset, the running byte write position inside
      the emitted-code buffer. Buffer shape from its siblings: a
      lazily allocated table of 0x1000-byte code pages
      (FUN_082b7edc allocates page[offset >> 12] on demand), aligned
      by FUN_082c2290, stored through and post-incremented by the
      word emitters (0x082c231c, 0x082beb4c, 0x082c0f4c, 0x082c6430,
      0x082cb824, 0x082cc0c8, 0x0836816c, 0x083685d4), and mapped to
      a pointer by FUN_082c5b1c as page[offset >> 12] + (offset &
      0xfff). Field rename: the +0x804 word is
      cg_codegen_buffer_t::current_offset (CG_CODEGEN_OUTPUT_OFFSET,
      word index 0x201). The port returns usize (a word) and the one
      ported caller, cg_label_add_fixup, now issues a real bl to it
      instead of inlining the load. match.py (exit 1, diff expected):
      the single ldr r0,[r0,#0x804] is verbatim; the diff is the
      crate fp frame push/pop. Host test:
      src/codegen/ir.rs::buffer_current_offset_returns_the_word_at_0x804
      - NULL/0 and a live pointer-sized word at +0x804 come back
      exactly, with poisoned neighbours proving no adjacent word is
      read.

  - address: 0x082c0d2c
    ghidra: FUN_082c0d2c
    name: cg_block_create
    size: 64
    sig: "cg_block_t *cg_block_create(cg_proc_t *proc, cg_block_kind_t kind)"
    module: codegen/block
    status: ported
    vars:
      param_1: proc
      param_2: kind
      iVar1: block
      bVar2: has_blocks
    notes: >-
      66 bl call sites. Allocates exactly 0x30 bytes through
      proc->module->heap (ldr r0,[r0,#4]; ldr r0,[r0,#0]; mov r1,#0x30;
      bl cg_heap_alloc), then appends the new cg_block_t to the procedure's
      intrusive FIFO list: proc+0x08 is its head, proc+0x0c its tail, and
      block+0x00 its next link. For a nonempty list it first stores the new
      block through old_tail->next; every append makes it the tail, and the
      first append also assigns the head. It stores proc at block+0x04 and
      the caller's block-kind enum at block+0x2c. The function returns the
      block despite Ghidra's void signature: callers retain r0, pass it to
      instruction factories, and back-patch cg_block_ref_create cells with
      it for forward branches (e.g. 0x082401cc: `*cell = block`). Observed
      kind arguments include 1, 2, 3, 4, 5, and 9. The port relies on
      cg_heap_alloc's matching zero-fill for the fresh next link. Host
      tests: src/codegen/block.rs::empty_append_sets_head_tail_owner_and_kind
      and nonempty_append_preserves_head_links_old_tail_and_advances_tail.

  - address: 0x082c0d6c
    ghidra: FUN_082c0d6c
    name: cg_block_ref_create
    size: 16
    sig: "cg_block_t **cg_block_ref_create(cg_proc_t *proc)"
    module: codegen/block_ref
    status: ported
    vars: {param_1: proc}
    notes: >-
      43 bl call sites. The whole body is four instructions:
      `ldr r0,[r0,#4]` (proc->module), `mov r1,#4`, `ldr r0,[r0,#0]`
      (module->heap), `b 0x082c1a08` - a typed-allocation wrapper that
      resolves the arena through the same proc->module->heap chain as
      cg_virtual_reg_create and tail-calls cg_heap_alloc(heap, 4).
      The 4-byte cell is an INDIRECT basic-block reference: branch
      instructions (cg_create_inst_branch_label @ 0x082c1878 kind 7,
      cg_create_inst_branch_cond @ 0x082c1850 kind 8) store the CELL
      pointer in their target field (+0xc), not a block pointer, so a
      branch can be emitted before its destination block exists.
      Call-site evidence: all 43 callers are pipeline generators
      (0x0823fa7c, 0x082439b0, 0x08248728, 0x082498d4, 0x0824a478,
      0x0824af94); the pattern is `cell = FUN_082c0d6c(proc); emit
      branch(..., cell); ...; *cell = cg_block_create(...)` (e.g.
      082439b0 lines 174-177: `local_c0 = FUN_082c0d6c(iVar1);
      FUN_082c1878(block,0x25,local_c0); *puVar4 = uVar2;`), and the
      emitter FUN_082c6430 line 133 resolves it as
      `*(cg_block_t **)target` then block->label (+0x8) for
      cg_label_add_fixup. The port calls the already-ported
      cg_heap_alloc (0x082c1a08, codegen/heap.rs) directly - no seam -
      and returns its result verbatim, matching the original's tail
      branch (R_ARM_JUMP24 cg_heap_alloc). The arena rounds the
      4-byte request to 8 and zero-fills, so a fresh cell reads as a
      NULL block pointer until back-patched. match.py (exit 1, diff
      expected): both ldrs and `mov r1,#4` verbatim; the diff is the
      crate fp frame push/pop. Host tests:
      src/codegen/block_ref.rs::resolves_heap_through_proc_module_and_returns_the_carve_verbatim
      (chain proc+0x04 -> module+0x00 -> heap, verbatim carve return,
      decoy arena untouched),
      requests_one_word_rounded_to_the_arena_granularity (consecutive
      cells 8 bytes apart, cursor advances by exactly the rounded
      word), cell_is_zero_filled_and_back_patchable (fresh cell NULL
      despite poisoned allocator, writable back-patch).

  - address: 0x0811d340
    ghidra: FUN_0811d340
    name: vtable_set_50_commit_kind4
    size: 32
    sig: "u32 vtable_set_50_commit_kind4(handle: *mut *mut u8, selector: u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: selector
    notes: >-
      4 conditional-branch sites, no bl callers (grep on
      decomp/osos.asm) - every reach is a tail beq/bleq: 0x0811d338
      from FUN_0811d2f8's third stage, 0x0811d450 (bleq) from the
      multi-message routine FUN_0811d360, 0x0811d684 from the kind-2
      three-stage sibling FUN_0811d64c, and 0x0811d6c4, the tail call
      of vtable_set_50_kind4 (0x0811d68c). The commit thunk of the
      kind-4 vtable message family (slot +0x50) - the exact shape of
      vtable_set_50_commit_probe_kind4 @ 0x0811d6cc and
      vtable_set_50_probe_kind4 @ 0x0811d6ec with the lone top commit
      bit: stmdb sp!,{r0,r1,r4,lr}; ldr r1,[sp,#4]; add r2,sp,#4;
      orr r1,r1,#0x80000000; str r1,[sp,#4]; mov r1,#4; bl 0x0811d7fc;
      ldmia sp!,{r2,r3,r4,pc} - handle passes through in r0 untouched,
      kind 4 in r1, r2 points at the stack slot holding
      selector | 0x80000000 (COMMIT_TAG; exact protocol meaning not
      established, see the module header), and the dispatcher's r0
      returns unbranched. This is the third and final stage of the
      guarded write: vtable_set_50_kind4 tail-calls it after a
      successful open and write. Deviations: callee 0x0811d7fc is
      ported in-module as vtable_slot_50_dispatch; like the two
      ported tag-thunk siblings the call still routes through the
      VTABLE_SLOT_50_DISPATCH read_volatile seam (retained for
      hookability - rewiring to a direct call is a follow-up), and the
      VTABLE_SET_50_KIND4_OPS commit default stays the private
      identical-body model vtable_set_commit for now (pointing the
      table at this export is a deliberate follow-up; one function
      per commit). The caller's r3 is forwarded verbatim, modeled as
      a 3rd parameter `forwarded` (vtable_set_50_commit_probe_kind4
      precedent). Reference C decomp/c/010/0811d340_FUN_0811d340.c
      invents a phantom 5th argument (FUN_0811d7fc(param_1, 4,
      &local_c, param_4, param_1)) - the dispatcher consumes r0..r3
      only; the port follows the disassembly. match.py (exit 1, diff
      expected): same orr 0x80000000 / mov r1,#4 / tagged-stack-slot /
      single-dispatch structure; the diff is the crate fp frame and
      the seam's ldr/blx standing in for bl 0x0811d7fc. Host tests:
      0x80000000 tagging + argument routing (handle verbatim, kind 4,
      stack-slot indirection, r3 forwarding), orr-not-replace on
      selectors already carrying each tag bit, and verbatim return
      forwarding -
      src/app/vtable_set.rs::commit_tags_selector_and_routes_arguments /
      commit_ors_instead_of_replacing_the_high_bits /
      commit_forwards_the_dispatch_return_verbatim.
  - address: 0x0811d360
    ghidra: FUN_0811d360
    name: vtable_set_50_write_eight_byte_record
    size: 248
    sig: "u32 vtable_set_50_write_eight_byte_record(handle: *mut *mut u8, record: *const u8, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: record
      param_4: forwarded
    notes: >-
      3 bl callers (grep on decomp/osos.asm): 0x0813662c, 0x0813666c,
      and 0x081d147c. The selector-0x12 batch serializer opens through
      FUN_0811d458 (ported vtable_set_50_open_kind4), then makes eight
      slot-0x50 dispatches: kind 4 &8 at 0x0811d390; kind-1 byte
      fields record+0 through record+4 at 0x0811d3a8..0x0811d408;
      kind-2 u16(record+6) at 0x0811d428; and kind-1 record+8 at
      0x0811d440. It commits selector 0x12 via ported
      vtable_set_50_commit_kind4 only after all eight return zero.
      Every nonzero status returns verbatim and short-circuits the
      remaining messages. The 0x0813662c / 0x0813666c callers pass
      records ten bytes apart, confirming padding at offsets 5 and 9.
      The port calls the open/commit exports directly and observes the
      eight dispatches through the existing VTABLE_SLOT_50_DISPATCH
      seam. r3 reaches open as `forwarded`, open's epilogue reloads
      r3 with selector 0x12 for the length dispatch, and later r3 is
      method-clobbered (modeled as zero). Reference C
      decomp/c/010/0811d360_FUN_0811d360.c incorrectly declares void
      and treats entry r2/r3 spills as locals; callers branch on r0,
      and r2 is overwritten before use. Host tests:
      src/app/vtable_set.rs::eight_byte_record_serializes_every_field_in_protocol_order
      / eight_byte_record_propagates_field_error_and_skips_later_messages.

  - address: 0x0811d458
    ghidra: FUN_0811d458
    name: vtable_set_50_open_kind4
    size: 20
    sig: "u32 vtable_set_50_open_kind4(handle: *mut *mut u8, selector: u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: selector
    notes: >-
      4 bl call sites (grep on decomp/osos.asm): 0x0811d30c, the first
      stage of the three-stage routine FUN_0811d2f8; 0x0811d370, from
      the multi-message routine FUN_0811d360; 0x0811d65c, the first
      stage of the kind-2 three-stage sibling FUN_0811d64c; and
      0x0811d69c, the first stage of vtable_set_50_kind4 (0x0811d68c).
      The open thunk of the kind-4 vtable message family (slot +0x50)
      - the smallest family member: the exact shape of
      vtable_set_50_commit_kind4 @ 0x0811d340 and the two tag thunks
      0x0811d6cc / 0x0811d6ec minus their ldr/orr/str tag sequence, so
      the selector goes out BARE: stmdb sp!,{r0,r1,r4,lr}; add
      r2,sp,#4; mov r1,#4; bl 0x0811d7fc; ldmia sp!,{r2,r3,r4,pc} -
      handle passes through in r0 untouched, kind 4 in r1, r2 points
      at the stack slot holding the untagged selector (see the module
      header for the tag-bit family convention), and the dispatcher's
      r0 returns unbranched. First stage of the guarded write:
      vtable_set_50_kind4 calls it before the write stage and
      propagates its error code (cmp r0,#0; popne). Deviations:
      callee 0x0811d7fc is ported in-module as
      vtable_slot_50_dispatch; like the ported tag-thunk siblings the
      call still routes through the VTABLE_SLOT_50_DISPATCH
      read_volatile seam (retained for hookability - rewiring to a
      direct call is a follow-up; this body is their shape minus the
      tag sequence, so no new seam), and the VTABLE_SET_50_KIND4_OPS
      open default stays the private identical-body model
      vtable_set_open for now (pointing the table at this export is a
      deliberate follow-up; one function per commit). The caller's r3
      is forwarded verbatim, modeled as a 3rd parameter `forwarded`
      (vtable_set_50_commit_kind4 precedent). Reference C
      decomp/c/010/0811d458_FUN_0811d458.c invents a phantom 5th
      argument (FUN_0811d7fc(param_1, 4, &uStack_c, param_4,
      param_1)) - the dispatcher consumes r0..r3 only; the port
      follows the disassembly. match.py (exit 1, diff expected): same
      spill-pair / add r2,sp,#4 / mov r1,#4 / single-dispatch
      structure; the diff is the crate fp frame and the seam's ldr/blx
      standing in for bl 0x0811d7fc. Host tests: bare selector (no
      tag bits) + argument routing (handle verbatim, kind 4,
      stack-slot indirection, r3 forwarding), verbatim pass-through
      of selectors already carrying each sibling's tag bit, and
      verbatim return forwarding -
      src/app/vtable_set.rs::open_sends_the_bare_selector_and_routes_arguments
      / open_passes_even_tagged_selectors_through_untouched /
      open_forwards_the_dispatch_return_verbatim.

  - address: 0x0811d56c
    ghidra: FUN_0811d56c
    name: vtable_set_50_write_kind4
    size: 64
    sig: "u32 vtable_set_50_write_kind4(handle: *mut *mut u8, value: *const u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: value
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0811d6b0, the
      second stage of vtable_set_50_kind4 (0x0811d68c) - its nonzero
      result short-circuits the commit tail call (cmp r0,#0 at
      0x0811d6b4). The 0x0811d590 from the cluster ledger is the
      bl 0x0811d7fc INSIDE this body (decomp/functions.csv lists no
      function there), not a separate entry point - not ported
      separately. The write thunk of the kind-4 vtable message
      family (slot +0x50) - the only two-dispatch member: stmdb
      sp!,{r2,r3,r4,lr} reuses the {r2,r3} spill slots as a two-word
      message; str #4 -> [sp,#4]; ldr r0,[r1] (*value) -> [sp,#0];
      then dispatch(handle, 4, sp+4) sends the kind word alone and,
      only when it returns 0, dispatch(handle, 4, sp) sends the
      two-word {*value, 4} message; the last result returns
      (ldmia sp!,{r2,r3,r4,pc}). The value arrives by pointer in r1
      and is loaded once, before the first dispatch. Deviations:
      callee 0x0811d7fc is ported in-module as
      vtable_slot_50_dispatch; both calls still route through the
      VTABLE_SLOT_50_DISPATCH read_volatile seam (retained for
      hookability - rewiring to a direct call is a follow-up), and
      the VTABLE_SET_50_KIND4_OPS write default stays the private
      identical-body model vtable_set_write for now (pointing the
      table at this export is a deliberate follow-up; one function
      per commit). The caller's r3 is forwarded verbatim, modeled as
      a 3rd parameter `forwarded` (vtable_set_50_open_kind4
      precedent). Reference C
      decomp/c/010/0811d56c_FUN_0811d56c.c invents phantom 4th/5th
      arguments on the first call (FUN_0811d7fc(param_1, 4,
      &local_c, param_4, *param_2)) and drops the second call's
      message pointer entirely (FUN_0811d7fc(param_1, 4)) - the
      dispatcher consumes r0..r3 only; the port follows the
      disassembly. match.py: same [sp]=*value / [sp,#4]=4 message
      layout, kind 4 in r1, and cmp-gated double dispatch; the diff
      is the crate fp frame and the seam's ldr/blx standing in for
      bl 0x0811d7fc. Host tests: two-dispatch count + argument
      routing (handle verbatim, kind 4, first message = the kind
      word, second message = {*value, 4}, r3 forwarding), first-error
      short-circuit skipping the value message, and verbatim
      forwarding of the second dispatch's return -
      src/app/vtable_set.rs::write_sends_kind_word_then_value_message_and_routes_arguments
      / write_short_circuits_the_value_message_on_a_first_error /
      write_forwards_the_second_dispatch_return_verbatim.

  - address: 0x0811d52c
    ghidra: FUN_0811d52c
    name: vtable_set_50_write_kind2
    size: 64
    sig: "u32 vtable_set_50_write_kind2(handle: *mut *mut u8, value: *const u16, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: value
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0811d670, the
      second stage of the unported kind-2 three-stage sibling
      FUN_0811d64c (open 0x0811d458 -> this call -> tail commit
      0x0811d340, the vtable_set_50_kind4 shape with this u16 write
      stage in place of the u32 0x0811d56c) - its nonzero result
      short-circuits the commit tail call (cmp r0,#0 at 0x0811d674).
      The 0x0811d550 from the cluster ledger is the bl 0x0811d7fc
      INSIDE this body (decomp/functions.csv lists no function
      there), not a separate entry point - not ported separately. The
      write thunk of the kind-2 (u16) vtable message family (slot
      +0x50) - the exact 64-byte shape of vtable_set_50_write_kind4
      (0x0811d56c, one instruction later) with three deltas: mov
      r0,#0x2 (message word, was #0x4), ldrh r0,[r1] (u16 load, was
      ldr) and moveq r1,#0x2 on the second dispatch (was #0x4). The
      FIRST dispatch's r1 stays kind 4 in both siblings (mov r1,#0x4)
      - the kind-4 body cannot distinguish "first kind = 4" from
      "first kind = the message word", but this body's #0x4 beside
      the #0x2 message word proves the first dispatch binds a fixed
      command kind 4 while the width kind travels in the payload; the
      value is loaded once as a halfword and zero-extended into the
      32-bit message word 0. Message layout identical otherwise: stmdb
      sp!,{r2,r3,r4,lr} spill slots become the two-word message;
      dispatch(handle, 4, sp+4) sends the kind word alone and, only
      on 0, dispatch(handle, 2, sp) sends {*value, 2}; nonzero first
      status returns verbatim, else the second dispatch's status.
      Deviations: both dispatches route through the retained
      VTABLE_SLOT_50_DISPATCH read_volatile seam (vtable_slot_50_dispatch
      is ported; rewiring is a follow-up); the caller's r3 is
      forwarded verbatim, modeled as a 3rd parameter `forwarded`
      (vtable_set_50_write_kind4 precedent). Reference C
      decomp/c/010/0811d52c_FUN_0811d52c.c invents phantom 4th/5th
      arguments on the first call (FUN_0811d7fc(param_1, 4,
      &local_c, param_4, *param_2)) and drops the second call's
      message pointer entirely (FUN_0811d7fc(param_1, 2)) - the
      dispatcher consumes r0..r3 only (its undefined2 *param_2 does
      catch the u16 value pointer); the port follows the disassembly.
      Host tests: two-dispatch count + argument routing (handle
      verbatim, first kind 4, first message = the kind-2 word, second
      kind 2, second message = {*value zero-extended, 2}, r3
      forwarding), ldrh halfword semantics (no high-half junk),
      first-error short-circuit skipping the value message, and
      verbatim forwarding of the second dispatch's return -
      src/app/vtable_set.rs::write_kind2_sends_kind_word_then_value_message_and_routes_arguments
      / write_kind2_loads_a_halfword_not_a_word /
      write_kind2_short_circuits_the_value_message_on_a_first_error /
      write_kind2_forwards_the_second_dispatch_return_verbatim.

  - address: 0x082c17ec
    ghidra: FUN_082c17ec
    name: cg_codegen_output
    size: 8
    sig: "cg_codegen_buffer_t *cg_codegen_output(cg_codegen_t *codegen)"
    module: codegen/ir
    status: ported
    vars: {param_1: codegen}
    notes: >-
      1 bl call site: 0x0824320c in FUN_08243138, the JIT's
      compile-and-patch driver. A pure leaf getter - the whole body is
      `ldr r0,[r0,#0x10]; bx lr` - returning codegen->output, the
      emitted-code buffer at +0x10 of cg_codegen_t
      (CG_CODEGEN_OUTPUT, word index 4). Identification: the
      constructor FUN_082c0d7c allocates the 0x220-byte cg_codegen_t
      and fills +0x10 with the buffer FUN_082c22b0 builds; the caller
      invokes the getter back-to-back with the label-fixup resolver
      FUN_082c16e0 (which itself reads codegen+0x10 as the buffer it
      patches words into) on the same object in r6, then feeds the
      returned pointer straight to cg_buffer_current_offset @
      0x082c23d4 - so +0x10 is the output buffer, not a label's bound
      offset. Sits immediately after cg_label_add_fixup @ 0x082c17ac
      in the image. match.py (exit 1, diff expected): the single
      `ldr r0,[r0,#16]` is verbatim; the diff is the crate fp frame
      push/pop. Host test:
      src/codegen/ir.rs::codegen_output_returns_the_word_at_0x10 -
      NULL/0 and a live pointer-sized word at +0x10 come back
      exactly, with poisoned neighbours proving no adjacent word is
      read.

  - address: 0x082c22b0
    ghidra: FUN_082c22b0
    name: cg_codegen_buffer_create
    size: 52
    sig: "CgCodegenBuffer *cg_codegen_buffer_create(const char *name)"
    module: codegen/ir
    status: ported
    vars: {param_1/r5: name, r4: buffer}
    notes: >-
      1 bl call site: 0x082c0db4 in FUN_082c0d7c (the cg_codegen_t
      constructor, which stores the result at codegen->output, +0x10).
      The emitted-code buffer constructor: mallocs the 0x808-byte
      record (literal pool @ 0x082c22e4 = 0x00000808, verified in
      osos.dec: 4 name + 0x800 page table + 4 offset), zeroes the
      0x800-byte code-page table at +0x04, sets current_offset
      (+0x804) = 0 and stores the name pointer at +0x00.
      CORRECTION (made by the 0x082c0d7c cg_codegen_create port):
      +0x00 is NOT an owner back-pointer - the constructor's bl at
      0x082c0db4 passes r0 = adr 0x82c0de4, the eight-byte datum
      "CSEG" plus 4 NULs sitting in the gap before cg_label_create @
      0x082c0dec (verified in osos.dec at file offset 0x2c0de4:
      43 53 45 47 00 00 00 00). It is a write-only segment name - no
      function reads buffer+0x00 (the destructor walks only the page
      table; the emitters/page accessor 0x082c2290/0x082c231c/
      0x082c2350/0x082b7edc touch only pages and +0x804). The port's
      parameter was renamed owner -> name and retyped *const u8.
      Callees: 0x0802edac = malloc (ported, runtime/malloc_rt);
      0x08037db8 = iram_memzero_aligned_veneer -> IRAM copy 0x2200027c
      of memzero_aligned @ 0x0800027c (ported, libc/memzero) - the
      port calls memzero_aligned directly. Layout pinned by the
      destructor @ 0x082c22e8 (walks +0x04+i*4 freeing each non-NULL
      page) and by FUN_082c5b1c (pages[offset >> 12] +
      (offset & 0xfff)). Deviations: allocation goes through the
      swappable CG_BUFFER_ALLOC slot (default the ported malloc; same
      scheme as stdio's STDIO_ALLOC) and a failed allocation returns
      NULL where the original has NO null check and would zero 0x800
      bytes at address 4. match.py (exit 1, diff expected): the diff
      is the hook-slot ldr/blx in place of bl malloc, the NULL guard,
      and LLVM folding the current_offset zero-store into an
      __aeabi_memclr4 of 0x804 bytes (memclr idiom merge); the record
      init structure is otherwise identical. Host tests:
      src/codegen/ir.rs::codegen_buffer_create_allocates_zeroes_and_stamps_the_record
      and ::codegen_buffer_create_returns_null_when_allocation_fails.

  - address: 0x082c0d7c
    ghidra: FUN_082c0d7c
    name: cg_codegen_create
    size: 104
    sig: "CgCodegen *cg_codegen_create(CgHeap *heap, const usize *helpers, usize *status)"
    module: codegen/ir
    status: ported
    vars: {param_1/r0->r5: heap, param_2/r1->r6: helpers, param_3/r2->r7: status, r4: codegen, r0 (loop): reg_no}
    notes: >-
      7 bl call sites, all JIT compile-and-patch drivers: 0x08243138
      and its six clones at 0x08246580/0x0824854c/0x082493c0/
      0x0824a43c/0x0824af58/0x0824bd40, each calling
      create(*proc, &local_helper_table, &zeroed_status_cell). The
      cg_codegen_t constructor: cg_heap_alloc(heap, 0x220) (direct
      call to the ported 0x082c1a08), zero-fill through the IRAM
      veneer 0x08037db8 -> memzero_aligned @ 0x2200027c (ported,
      libc/memzero, called directly - same as
      cg_codegen_buffer_create), then the field stores: +0x08 = heap
      (pinned by cg_label_create reading codegen+0x08 as its
      allocation arena), +0x0c = NULL labels head (explicit store,
      redundant with the zero-fill), stmia r4,{r6,r7} -> +0x00 =
      helpers, +0x04 = status, +0x10 = the fresh output buffer from
      cg_codegen_buffer_create (pinned by cg_codegen_output and the
      fixup resolver FUN_082c16e0). +0x00 helpers is the driver's
      0x28-byte ten-word table of code pointers (0x08025f88,
      0x080de95c, 0x0806b32c, 0x0806b27c, 0x080e2468, 0x0809b904 -
      all mid-function entries into osos text, dumped from osos.dec);
      the emitters copy words OUT of it INTO the generated code
      (FUN_082c0f4c reads helpers[6] for kind 0x17 and hands it to
      the word emitter 0x082beb4c), so it is a runtime-helper
      address table, not a vtable the codegen calls. +0x04 status:
      every driver passes &zeroed_stack_word; no reader recovered in
      the compile/fixup path. THE adr ANOMALY: the bl
      cg_codegen_buffer_create at 0x082c0db4 passes r0 = adr
      0x82c0de4, NOT the new record - the datum there is the
      NUL-padded string "CSEG" (osos.dec offset 0x2c0de4), the
      emitted-code buffer's write-only name; see the corrected
      0x082c22b0 entry. The port passes a static with identical
      contents. +0x20: 16-entry hardware-register descriptor table,
      28 bytes per entry (rsb r1,r0,r0,lsl #3 / add r1,r4,r1,lsl #2
      = i*28), one per ARM register r0-r15; the loop stamps
      strb i,[codegen+0x20+i*28] - the register NUMBER, not zero
      (the Ghidra reference C confirms: *(char*)(puVar1+iVar3*7+8) =
      (char)iVar3). Field evidence for the table from its walkers
      FUN_082cea24/FUN_082d7800/FUN_082d7870: each entry is a
      descriptor anchor at codegen+0x14+i*0x1c with an intrusive
      binding-list head at anchor+0x10 and a flags word at
      anchor+0x18 (bit 0x100); the stamped byte is anchor+0x0c.
      +0x1e0 = 0: the id byte of the codegen's own embedded anchor
      at +0x1d4 (list head at +0x1e4 per FUN_082d7870 and the
      fixup-path store at 0x082c1660), another explicit
      zero-redundant store. Deviation: none beyond the inherited
      buffer_create NULL-output one. match.py (exit 1, diff
      expected): LLVM unrolls the 16-iteration loop into 16 strb at
      the identical offsets (0x20..0x1c4, stride 0x1c), merges the
      record zero-fill into an __aeabi_memclr4 of 0x210 bytes from
      +0x10 (the four stm-words hoisted out; same memclr idiom merge
      as buffer_create), and the two bl show as local symbol calls
      in the relocatable - field stores, table stride, +0x1e0 store
      and r0 return all line up. Host tests:
      src/codegen/ir.rs::codegen_create_allocates_zeroes_and_wires_the_record
      (arena carve + return value, poison-proven zero-fill, all five
      field stores, 16 register-number bytes at the 28-byte stride,
      +0x1e0 zero, the real buffer_create's CSEG-named 0x808 buffer,
      and cg_codegen_output/cg_label_create layout consistency) and
      ::codegen_create_stores_a_null_output_when_the_buffer_alloc_fails.

  - address: 0x08243138
    ghidra: FUN_08243138
    name: cg_compile_and_patch
    size: 348
    sig: "void cg_compile_and_patch(CgCompileDriver *driver, u8 *code_space, usize extra)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r4: driver (vtable at +0x00, module at +0x04)
      param_2/r5: code_space (destination cursor object, forwarded to reserve/flush)
      param_3/r9: extra (forwarded verbatim to the reserve stage's r2)
      r6: codegen
      r7: output
      r8: size, then dst
      sp+0x04..sp+0x28: helper_table (ten-word CG_HELPER_TABLE)
      sp+0x00: status (zero-initialized cell)
    notes: >-
      0 bl callers; 6 tail `b 0x08243138` sites - the clones at
      0x08246580/0x0824854c/0x082493c0/0x0824a43c/0x0824af58/0x0824bd40
      (each builds a driver object, calls its own lowering driver such
      as FUN_082439b0, then tail-branches here; porting the six clones
      is future work). The JIT's compile-and-patch driver, top of the
      codegen stack: (1) eight whole-module analysis passes over
      driver->module, each bl'd with the module as sole arg, in this
      exact order - FUN_082c2008 (link defining insts), FUN_082c1d78
      (canonicalize), FUN_082c1f04 (mark-and-sweep DCE), FUN_082c21b8
      (phi resolution), FUN_082c1d4c (register allocation; calls
      FUN_082c21b8 again internally), FUN_082c2098 (link use insts),
      FUN_082c1ddc (branch-edge linking), FUN_082c214c (register
      indexing); (2) build the ten-word helper table (literal pool
      0x08243294-0x082432a8: 0x08025f88, 0x080de95c, 0x0806b32c,
      0x0806b27c, 0x080e2468, then 0x080de95c/0x0806b32c/0x0806b27c/
      0x080e2468 duplicated, 0x0809b904 - osos.dec offset 0x243294)
      plus a zeroed status cell; (3) cg_codegen_create(*module,
      &table, &status) - ported, direct; (4) FUN_082c12c4
      lower/emit (seam slot; default models the exact body: two
      cg_label_create per proc at +0x2c/+0x30, then per-proc
      FUN_082c1320 behind the nested CG_PROC_EMIT seam); (5)
      cg_codegen_resolve_label_fixups - ported, direct; (6)
      cg_codegen_output + first of three cg_buffer_current_offset
      reads; (7) the one indirect call: driver->vtable->+0x0c(driver);
      (8) FUN_082438f0 reserve(code_space, base, extra, size) - seam
      slot, no-op NULL default (needs the unported state allocator
      FUN_08243844 with grow path 0x082433b4); (9) second offset read
      + cg_buffer_copy_out(output, 0, dst, size) - ported, direct;
      (10) third offset read + FUN_08243768 flush(code_space, dst,
      size) - seam slot, no-op default (CP15 D-cache clean+invalidate
      via IRAM veneer 0x08037d0c, tail into IRAM 0x220031b8 via
      0x08038238; the original loads r0-r2 and never reads them);
      (11) FUN_082c0e20 codegen_destroy - seam slot; default models
      the exact body flattened with its tail target, the buffer
      destructor @ 0x082c22e8 (free 512 non-NULL pages in index
      order, then the buffer), routing frees through the new
      CG_BUFFER_FREE seam (default: the ported free @ 0x0802edc8,
      the CG_BUFFER_ALLOC mirror) and adding a NULL-output guard;
      (12) cg_heap_destroy(*module) - ported, direct: the whole arena
      (module, procs, labels, codegen record) dies wholesale.
      Deviations: helper table is a shared static (the original's
      0x28-byte stack memzero is dead - all ten words stored before
      any read); the eight passes and the emitter/reserve/flush
      stages default to no-ops as documented on CgCompileAndPatchOps
      (each original identified with address/size/recovered behavior
      on its slot; benign default chain - nothing emitted means the
      NULL dst is never dereferenced). Host tests:
      src/codegen/ir.rs::compile_and_patch_runs_the_pipeline_in_order_through_the_seams
      (stage order, per-stage argument routing, helper table + status
      wiring, resolve-before-copy_out proven by the patched branch
      word in the copied image),
      ::compile_and_patch_default_ops_run_the_benign_empty_pipeline,
      ::lower_and_emit_default_creates_two_labels_per_proc_then_emits_in_list_order,
      ::codegen_destroy_default_frees_pages_in_order_then_the_buffer
      and ::compile_and_patch_seams_stay_wired_to_the_defaults.

  - address: 0x082c0e28
    ghidra: FUN_082c0e28
    name: cg_block_emit
    size: 292
    sig: "void cg_block_emit(CgCodegen *codegen, CgBlock *block)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0->r5: codegen
      param_2/r1->r8: block
      r4: block->label (prologue), then num_registers, then the used-reg cursor
      r7: tails (the malloc'd per-register tail-pointer table)
      r6: inst (both list walks)
      r9: collector end cursor
      aiStack_120: used (64-entry on-stack used-register buffer, sp..sp+0x100)
      FUN_082c23d4: cg_buffer_current_offset (ported, direct)
      FUN_0802edac: malloc (the CG_BLOCK_EMIT_OPS.alloc_use_tails slot)
      FUN_082c1bfc: cg_inst_collect_used_regs (ported, direct)
      FUN_082c1a08: cg_heap_alloc (ported, direct)
      FUN_0802edc8: free (the CG_BLOCK_EMIT_OPS.free_use_tails slot)
      FUN_082b3b5c: bind_registers slot (unported; no-op default)
      FUN_082c0f4c: emit_inst slot (unported; no-op default)
      FUN_082cea24: release_hw_reg_bindings slot (unported; no-op default)
      FUN_082ccae4: flush_pending_bindings slot (unported; no-op default)
    notes: >-
      1 bl call site: 0x082c13ac, the per-procedure emitter
      FUN_082c1320's block loop (its r2 - 0 for the proc's first
      block, 1 for the rest - is NEVER read by the callee: r2's first
      use in the body is a clobber, ldr r2,[r7,...] @ 0x082c0ec0; the
      ported signature drops it). The JIT's per-block code generator.
      (1) Publishes the block at codegen +0x208 (CG_CODEGEN_CURRENT_BLOCK,
      read by FUN_082b3b5c/FUN_082d7870/FUN_082c5c58). (2) Binds the
      block's label (block +0x08 = CG_BLOCK_LABEL, created per block
      by FUN_082c1320's label loop): label->offset (+0x08) =
      cg_buffer_current_offset(codegen->output). (3) mallocs a
      num_registers-entry tail table (num from block->proc +0x20),
      then per register tails[r] = &heads[r], heads[r] = NULL, heads
      = codegen +0x21c (CG_CODEGEN_REG_USES, the per-proc array
      FUN_082c1320 carves from the codegen heap). (4) Walks the
      block's instructions; per instruction collects USED registers
      into the 64-slot stack frame (sub sp,sp,#0x100; bounded by
      add r2,sp,#0x100) through the ported cg_inst_collect_used_regs
      and appends one 8-byte arena record {next, inst} per use
      through the tail pointer, exact store order *tail = record,
      record->inst = inst, tail = record (tail's next stays NULL by
      the arena zero-fill). Result: heads[r] = the in-order list of
      the block's instructions using register r - the use lists the
      emitters drain use-by-use (FUN_082beb4c unlinks each record as
      the using instruction is emitted and releases the register's
      binding when its list runs empty; binary-verified at
      0x082bec18-0x082becc8). (5) free(tails), then the block-entry
      register binder FUN_082b3b5c (216 bytes, unported). (6)
      Emission: skips LEADING phi instructions (kind byte 9), then
      dispatches EVERY remaining instruction - including any phi
      later in the list - to the per-instruction emitter FUN_082c0f4c
      (1428 bytes, unported; a 25-way dispatch on the OPCODE byte
      inst+0x09). (7) Epilogue: FUN_082cea24(codegen, block +0x20
      live-out bitset record, 152 bytes, unported), FUN_082ccae4
      (codegen) (204 bytes, unported), then codegen +0x208 = NULL.
      Deviations: the four unported callees and the malloc/free
      boundary route through the swappable CG_BLOCK_EMIT_OPS table
      (the CgCompileAndPatchOps precedent; alloc/free default to the
      real ports so host tests avoid racing malloc's global HEAP_OPS,
      the CG_BUFFER_ALLOC precedent; the four callee slots default to
      no work, the documented default_cg_graph_pass deviation); every
      ported callee is called directly. match.py (exit 1, diff
      expected): field offsets (0x208 store first, +0x10/+0x08 label
      bind, +0x20 num_registers, 0x21c heads, +0x10 reg_no, the
      three-store append, +0x08 kind byte cmp #9, +0x20 live-out,
      0x208 clear) and both loop structures line up; the diff is the
      crate fp prologue, the ops-table ldrd/blx dispatch in place of
      the direct bls, and LLVM's pointer-incrementing loop
      re-expressions. Host tests:
      src/codegen/ir.rs::block_emit_binds_label_publishes_block_and_runs_the_empty_pipeline_in_order
      (label binding, publish-before-binder, alloc(0)/free identity,
      epilogue order, zero-register heads never touched),
      ::block_emit_builds_in_order_use_lists_per_register (per-register
      chains in instruction order, single- and multi-use registers,
      alloc size, full stage order),
      ::block_emit_skips_leading_phis_emits_later_ones_and_collects_every_use
      (leading-phi skip vs later-phi dispatch, skipped phis still
      contribute uses),
      ::block_emit_all_phi_block_emits_nothing_but_still_builds_use_lists
      (no dispatch, NULL head for an unused register, unpublished on
      exit) and ::block_emit_seams_stay_wired_to_the_defaults.

  - address: 0x082430c4
    ghidra: FUN_082430c4
    name: cg_cell_table_create
    size: 72
    sig: "CgCellTable *cg_cell_table_create(CgCellTable *record)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0/r4: record (arrives in r0, held in r4 across the cell
        init calls, moved back to r0 for the return)
      r3: index (0..=0xc, the `cmp r3,#0xc; bls` loop counter)
    notes: >-
      1 bl call site: the video engine's lazy slot-table getter
      FUN_083b4830 @ 0x083b4830 (call at 0x083b4914), whose
      operator_new(0x110) result is still in r0 when it bls here; the
      returned pointer goes into the freshly claimed slot. 0 tail b
      sites. Binary-adjacent to cg_compile_and_patch (0x08243138) but
      NOT called by it (verified: no `bl 0x082430c4` in the driver's
      body or its clones). Constructor for a 0x110-byte record:
      stamps the five config bytes at +0x104..+0x108 as {1, 1, 0, 1,
      1} in that store order, then initializes thirteen 0x14-byte
      cells in index order (addressing `add r0,r3,r3,lsl #0x2; add
      r0,r4,r0,lsl #0x2` = record + index*0x14) through the cell init
      cg_cell_table_cell_init @ 0x08256c10 (24 bytes, leaf: word +0x00
      = 0, byte +0x0c = 0xff, word +0x10 = 0), and returns the record
      - the C++ constructor convention. Each cell is an
      optionally-owned heap buffer: the mirror destructor FUN_0824310c
      @ 0x0824310c (44 bytes, unported) runs the same thirteen-cell
      loop over the cell destroy FUN_08256c28, which frees cell +0x10
      when cell +0x00 is non-zero. Record size 0x110 from the
      caller's operator_new argument (the constructor touches 0x109
      bytes; the tail is allocator padding). Deviation: the per-cell
      init routes through the CG_CELL_TABLE_CELL_INIT seam (the
      CG_PROC_EMIT precedent), whose default is the ported
      cg_cell_table_cell_init; the two cell words are genuine 32-bit
      stores at fixed byte offsets (record ABI pinned by the unported
      caller/destructor), not the module's word-index convention. Host
      tests: src/codegen/ir.rs::cell_table_create_stamps_flags_and_every_cell_through_the_ported_init
      (return-this, exact flag bytes, per-cell writes, poison on
      untouched bytes), ::cell_table_create_routes_thirteen_cells_in_index_order_through_the_seam,
      and ::cell_table_cell_init_seam_stays_wired_to_the_ported_function.

  - address: 0x0824310c
    ghidra: FUN_0824310c
    name: cg_cell_table_destroy
    size: 44
    sig: "CgCellTable *cg_cell_table_destroy(CgCellTable *record)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0/r5: record (arrives in r0, held in r5 across all cell
        destroys, then returned in r0)
      r4: index (0..=0xc, the `cmp r4,#0xc; bls` loop counter)
    notes: >-
      2 bl call sites, at 0x08251848 and 0x08256518; both callers
      immediately call operator_delete @ 0x082aad24 after this routine
      returns, proving this is a destructor rather than a deleting
      destructor. It walks all thirteen cells at record + index*0x14
      in ascending order through ported sibling cell helper
      cg_cell_table_cell_destroy (FUN_08256c28, 40 bytes). That helper
      loads +0x00 into r1, skips NULL, loads +0x10 into r0, calls
      allocator-aware FUN_0807f234(r0 = allocator, r1 = buffer), then
      clears +0x00. Thus +0x00 is the owned buffer pointer and +0x10
      the allocator handle (NULL selects FUN_0807f234's default heap);
      no operator_delete occurs in this body. The
      CG_CELL_TABLE_CELL_DESTROY seam defaults to the ported helper.
      On target its allocator-registry call stays at the stock helper
      address; host behavior models every non-NULL buffer with
      free_wrapper(ptr, 0x38) because the registry is unported. The
      fixed ABI words use u32 loads/stores at byte offsets, not host
      pointer-width slots. Host tests:
      src/codegen/ir.rs::cell_table_cell_destroy_skips_a_null_buffer_without_mutating_the_cell,
      ::cell_table_cell_destroy_releases_then_clears_only_the_owned_buffer_word,
      ::cell_table_destroy_frees_all_owned_cells_in_order_and_pairs_with_create,
      ::cell_table_destroy_routes_all_cells_in_index_order_through_the_seam,
      and ::cell_table_destroy_seam_stays_wired_to_the_ported_helper.

  - address: 0x08256c10
    ghidra: FUN_08256c10
    name: cg_cell_table_cell_init
    size: 24
    sig: "void cg_cell_table_cell_init(u8 *cell)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0: cell (4-byte-aligned 0x14-byte cell)
      r1: zero (shared value for the two 32-bit stores)
      r2: tag_value (0xff, stored at cell +0x0c)
    notes: >-
      Leaf helper called once for each cell by cg_cell_table_create.
      Raw ARM at 0x08256c10 is `mov r1,#0; mov r2,#0xff; str
      r1,[r0]; strb r2,[r0,#0xc]; str r1,[r0,#0x10]; bx lr`; it takes
      cell in r0, returns void, clears its 32-bit buffer and allocator
      fields at +0x00 and +0x10, and stamps byte +0x0c to 0xff in that
      store order, leaving every other byte untouched. Source:
      ipod-decomp/decomp/osos.asm. No deviations. Host test:
      src/codegen/ir.rs::cell_init_writes_its_three_abi_fields_and_returns_void
      verifies the void return and all 20 cell bytes.

  - address: 0x08256c28
    ghidra: FUN_08256c28
    name: cg_cell_table_cell_destroy
    size: 40
    sig: "void cg_cell_table_cell_destroy(u8 *cell)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0/r4: cell (4-byte-aligned 0x14-byte cell; retained in r4
        across the allocator release)
      r1: buffer (cell +0x00, NULL-checked before the call)
      r0: allocator (cell +0x10, first argument to FUN_0807f234)
    notes: >-
      Direct destruction seam for one CgCellTable cell. Raw ARM is `push
      {r4,lr}; ldr r1,[r0]; mov r4,r0; cmp r1,#0; popeq {r4,pc}; ldr
      r0,[r4,#0x10]; bl 0x0807f234; mov r0,#0; str r0,[r4]; pop
      {r4,pc}`. A NULL buffer returns without changing any cell byte.
      Otherwise it invokes allocator-registry release with r0=the
      target-width allocator handle and r1=the target-width buffer,
      then clears only buffer word +0x00 after return. The callee's
      NULL allocator path initializes/uses the process default allocator;
      its non-NULL registry path clears a matched live-entry bit or frees
      through the registered heap, while a zero registry heap returns
      without freeing. The cell helper still clears +0x00 in all such
      non-NULL-buffer cases. Source:
      ipod-decomp/decomp/osos.asm and
      decomp/c/024/08256c28_FUN_08256c28.c. Target calls the unported
      FUN_0807f234 directly with its recovered two-register ABI. Host
      deviation: no allocator-registry representation exists, so its
      default model releases every non-NULL buffer via
      free_wrapper(buffer, 0x38); a non-default host registry requires
      replacing CG_CELL_TABLE_CELL_DESTROY. Host tests:
      src/codegen/ir.rs::cell_table_cell_destroy_skips_a_null_buffer_without_mutating_the_cell
      and ::cell_table_cell_destroy_releases_then_clears_only_the_owned_buffer_word.

  - address: 0x0811d5ac
    ghidra: (absent from functions.csv)
    name: vtable_query_4c_read_scalar_body
    size: 80
    sig: "u32 vtable_query_4c_read_scalar_body(handle: *mut *mut u8, kind: u32, out: *mut u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: handle
      r1/r5: kind (the calling thunk's bound width, 2 or 4)
      param_2/r6: out
      r3: forwarded
    notes: >-
      GHIDRA-MISSED: absent from decomp/functions.csv - its 80 bytes
      (0x0811d5ac-0x0811d5fb) are folded into the FUN_0811d718 csv
      row (92 bytes = this body + the 12-byte kind-4 entry thunk) and
      decompiled inline under both decomp/c/010/0811d70c_FUN_0811d70c.c
      and 0811d718_FUN_0811d718.c. 0 bl call sites; exactly 2 tail
      `b 0x0811d5ac` sites (grep on decomp/osos.asm): 0x0811d714 from
      vtable_query_4c_read_scalar_kind2 (0x0811d70c, binds kind 2) and
      0x0811d720 from vtable_query_4c_read_scalar_kind4 (0x0811d718,
      binds kind 4) - the kind-encodes-value-width convention of the
      write stages. The shared scalar-read body of the slot +0x4c
      message family: entry stmdb sp!,{r2,r3,r4,r5,r6,lr} spills arg2
      (out) and arg4 (r3) into a two-word stack pair; the PROBE sends
      kind 4 (hardcoded mov r1,#0x4) with &pair[1] as a scratch
      out-slot whose word is never read back - status 5 =
      "unsupported" and any other nonzero = hard error, both
      returning verbatim (cmp r0,#0x5; beq / cmp r0,#0; bne). The
      READ re-dispatches through vtable_slot_4c_dispatch (0x0811d7b0)
      with the thunk-bound kind (mov r1,r5) and the caller's out
      pointer as data. Only on a zero read status does the FINISH
      bleq 0x0811d46c fire - the ported kind-4 query thunk
      util/vtable_query.rs vtable_query_4c_kind4, re-dispatching
      kind 4 with the one-word {out} pair; its error code is the
      return value. Deviations: both callees are ported and route
      through the VTABLE_QUERY_4C_SCALAR_DISPATCH /
      VTABLE_QUERY_4C_SCALAR_FINISH seams (the vtable_slot_50_dispatch
      precedent - seams retained, wired to the ported
      implementations); both entry thunks now tail-call this port.
      arg3 (r3) is modeled as forwarded per family convention (no
      call site sets r3 deliberately) - it doubles as the probe
      out-slot's initial word and the probe dispatcher's stmdb r3
      spill. r3 is DEAD for the read dispatch and the finish call
      (the 0x0811d874 dead-slot refinement): a zero stack word and 0
      stand in for those unobservable arguments (r2 likewise dead).
      The return type is u32 - r0 carries the failing dispatch's
      status (or the finish's) back through the tail-calling thunk.
      Host tests (the scalar-read recording mocks through the two
      shared seams, SLOT_TEST_LOCK serialized):
      src/app/vtable_set.rs::scalar_body_probe_unsupported_bails_before_the_read
      / ::scalar_body_probe_args_and_initial_out_slot /
      ::scalar_body_read_dispatch_routes_the_thunk_bound_kind /
      ::scalar_body_read_error_skips_the_finish /
      ::scalar_body_finish_routing_and_final_return /
      ::scalar_body_end_to_end_kind4_through_the_thunk /
      ::scalar_body_end_to_end_kind2_through_the_thunk.

  - address: 0x0811d5fc
    ghidra: FUN_0811d5fc
    name: vtable_query_4c_read_buffer_size_out
    size: 80
    sig: "u32 vtable_query_4c_read_buffer_size_out(handle: *mut *mut u8, _capacity: u32, buffer: *mut u8, size_out: *mut u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r5: handle
      param_2/r1: _capacity (DEAD - mov r1,#0x4 kills it before any use)
      param_3/r6: buffer
      param_4/r3/r4: size_out
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0811afeb8 in
      FUN_081afdb8, which passes the handle (param_1 + 0xd), the
      buffer capacity 0x100 in r1, a 0x100-byte stack buffer in r2
      and &local_140 in r3, then NUL-terminates auStack_13c[local_140]
      - the probed word IS a byte count. Reference C
      decomp/c/010/0811d5fc_FUN_0811d5fc.c is accurate. The
      unclamped, size-reporting sibling of vtable_query_4c_kind4_read
      (0x0811d818): the same probe-read-finish shape as the shared
      scalar body vtable_query_4c_read_scalar_body (0x0811d5ac)
      through vtable_slot_4c_dispatch (0x0811d7b0), but the probe's
      out-slot is the CALLER's size_out pointer (mov r2,r3 - r3 is a
      REAL argument here, breaking the family's no-r3 convention),
      and the probed size feeds the read UNCLAMPED (ldr r1,[r4] -
      no strhi) with the caller's buffer as data. Probe sends the
      hardcoded kind 4; status 5 = unsupported and any other nonzero
      = hard error, both returning verbatim. The finish bleq
      0x0811d46c (ported util/vtable_query.rs vtable_query_4c_kind4)
      fires only on a zero read status, with the one-word {size_out}
      entry spill (moveq r1,sp) as the message; its error code is
      the return value. Deviations: both callees are ported and
      route through the new VTABLE_QUERY_4C_BUFFER_DISPATCH /
      VTABLE_QUERY_4C_BUFFER_FINISH seams (the
      VTABLE_QUERY_4C_SCALAR_DISPATCH/FINISH precedent - seams
      retained, wired to the ported implementations). r1 (capacity)
      is DEAD - mov r1,#0x4 overwrites it before anything reads it;
      the sole call site's 0x100 is never consulted (unlike the
      clamping 0x0811d818 sibling), modeled as _capacity. r3 is DEAD
      for the read dispatch and the finish call (the 0x0811d874
      dead-slot refinement): a zero stack word and 0 stand in for
      those unobservable arguments (r2 likewise dead). Host tests
      (the buffer recording mocks through the two new seams,
      SLOT_TEST_LOCK serialized):
      src/app/vtable_set.rs::buffer_read_probe_unsupported_bails_before_the_read
      / ::buffer_read_probe_error_bails_before_the_read /
      ::buffer_read_probe_args_route_size_out_and_ignore_capacity /
      ::buffer_read_read_dispatch_routes_the_probed_size_unclamped /
      ::buffer_read_read_error_skips_the_finish /
      ::buffer_read_finish_routing_and_final_return /
      ::buffer_read_end_to_end_reports_size_and_delivers_bytes.

  - address: 0x0811d70c
    ghidra: FUN_0811d70c
    name: vtable_query_4c_read_scalar_kind2
    size: 12
    sig: "u32 vtable_query_4c_read_scalar_kind2(handle: *mut *mut u8, out: *mut u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: handle
      param_2/r6: out
      param_4/r3: forwarded
      r5: kind (entry-thunk binding, 2 here)
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x08136810 in
      FUN_08136520 - it passes the handle in r0 (add r0, sp, #0x48)
      and a pointer to the caller's out word in r1 (add r1, r5,
      #0x18), does not set r3, branches on a zero status (movs r4,
      r0; beq) and maps the unsupported status 5 to success (cmp r4,
      #0x5; moveq r4, #0x0). The 12 bytes (decomp/functions.csv)
      are the kind-2 entry thunk only (mov r2,r1; mov r1,#2; b
      0x0811d5ac); the shared 80-byte body at 0x0811d5ac it
      tail-calls belongs to the kind-4 sibling's csv row
      (vtable_query_4c_read_scalar_kind4, 0x0811d718, 92 bytes) and
      is ported separately as vtable_query_4c_read_scalar_body
      (0x0811d5ac) - this thunk binds kind 2 and tail-calls it, the
      body routing through the existing
      VTABLE_QUERY_4C_SCALAR_DISPATCH / VTABLE_QUERY_4C_SCALAR_FINISH
      seams. The u16-width
      sibling of the kind-4 scalar read: the
      kind-encodes-value-width convention of the write stages
      (vtable_set_50_write_kind4 / vtable_set_50_write_kind2) applied
      to the scalar (single-word) property read of the slot +0x4c
      message family. The shared body is identical for both entries:
      the probe's mov r1,#0x4 is hardcoded there, so only the READ
      dispatch carries the thunk-bound width (kind 2 here, telling
      the method to deliver a u16 property through the caller's out
      word); probe status 5 = "unsupported" and any other nonzero a
      hard error (both returning verbatim), and the closing bleq
      0x0811d46c finish (the ported kind-4 query thunk
      util/vtable_query.rs vtable_query_4c_kind4) fires only on a
      zero read status. Deviations: arg3 (r3) is modeled as
      forwarded (the family convention - the single call site does
      not set r3 deliberately), doubling as the probe out-slot's
      initial word and the probe dispatcher's stmdb sp!,{r3} spill;
      r3 is DEAD for the read dispatch and the finish call (the
      0x0811d874 dead-slot refinement), so a zero stack word and 0
      stand in for those unobservable arguments (r2 likewise dead -
      discarded by the thunk's mov r2,r1). The return type is u32,
      not the reference C's void - the call site branches on r0.
      Reference C (decomp/c/010/0811d70c_FUN_0811d70c.c) gets the
      shape right (scratch probe out-slot, hardcoded kind-4 probe,
      kind-2 read) but types param_2 as undefined4 (the disassembly
      and the call site show a pointer) and hides the entry-thunk /
      shared-body split - the port follows the disassembly. Host
      tests (the kind-4 sibling's recording mocks through the two
      shared seams, SLOT_TEST_LOCK serialized):
      src/app/vtable_set.rs::scalar_kind2_read_unsupported_status_bails_before_the_read
      / ::scalar_kind2_read_probe_error_bails_before_the_read /
      ::scalar_kind2_read_probe_args_and_initial_out_slot /
      ::scalar_kind2_read_read_args_and_value_delivery /
      ::scalar_kind2_read_read_error_skips_the_finish /
      ::scalar_kind2_read_finish_args_and_final_return.

  - address: 0x0811d718
    ghidra: FUN_0811d718
    name: vtable_query_4c_read_scalar_kind4
    size: 92
    sig: "u32 vtable_query_4c_read_scalar_kind4(handle: *mut *mut u8, out: *mut u32, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: handle
      param_2/r6: out
      param_4/r3: forwarded
      r5: kind (entry-thunk binding, 4 here)
    notes: >-
      15 bl call sites (grep on decomp/osos.asm): the 5-site cluster
      0x0811b008..0x0811b1c0, 0x0811e914, the 3-site cluster
      0x081afe70..0x081aff7c, 0x081bc790, 0x081d10d8, 0x081d1584 and
      the 3-site cluster 0x08271528..0x08271560 - every site passes
      the handle in r0 and a pointer to the caller's out word in r1
      (&local_34, local_20 + 8 ...), none sets r3; e.g. FUN_081afdb8
      reads local_34 as the delivered value after a zero status. The
      92 bytes (decomp/functions.csv) are the 12-byte kind-4 entry
      thunk at 0x0811d718 (mov r2,r1; mov r1,#4; b 0x0811d5ac) plus
      the shared 80-byte body at 0x0811d5ac it tail-calls (ported
      separately as vtable_query_4c_read_scalar_body) - the ported
      12-byte sibling thunk vtable_query_4c_read_scalar_kind2 @
      0x0811d70c binds kind 2 into the same body (the write stages'
      kind-encodes-value-width convention). The scalar
      (single-word) property read of the slot +0x4c message family -
      the little sibling of vtable_query_4c_kind4_read (0x0811d818,
      the sized-buffer read): the same probe -> dispatch ->
      finish-thunk shape through vtable_slot_4c_dispatch (0x0811d7b0,
      ported in this module) minus the size clamp. The body's entry
      stmdb sp!,{r2,r3,r4,r5,r6,lr} spills arg2 (out) and arg4 (r3)
      into a two-word stack pair: the PROBE sends kind 4 (hardcoded
      mov r1,#0x4 in the shared body - only the read carries the
      thunk-bound width) with &pair[1] as a scratch out-slot whose
      word is never read back; status 5 = "unsupported" and any other
      nonzero = hard error, both returning verbatim (cmp r0,#0x5;
      beq / cmp r0,#0; bne). The READ re-dispatches with the
      thunk-bound kind (mov r1,r5) and the caller's out pointer as
      data; the method stores the property word through it. Only on
      a zero read status does the FINISH bleq fire: 0x0811d46c is
      the PORTED kind-4 query thunk util/vtable_query.rs
      vtable_query_4c_kind4, re-dispatching kind 4 with the one-word
      {out} pair as the message; its error code becomes the return
      value. Deviations: both callees are ported and route through
      the new VTABLE_QUERY_4C_SCALAR_DISPATCH /
      VTABLE_QUERY_4C_SCALAR_FINISH seams (the
      VTABLE_QUERY_4C_READ_DISPATCH / _FINISH pattern - hookability
      plus host-test interception without racing the sibling's or
      util's own parallel tests). arg3 is modeled as forwarded per
      family convention - it doubles as the probe out-slot's INITIAL
      word (the entry spill) and as the probe dispatcher's stmdb r3
      spill. r3 is DEAD for the read dispatch and the finish call
      (the 0x0811d874 refinement): method-clobbered across the
      probe, never reloaded, and its spill slot is the probe's
      method-written out-slot - the port passes a pointer to a zero
      stack word for the read's extra and 0 for the thunk's
      forwarded, plus 0 for the thunk's _unused (r2 likewise dead,
      discarded by the thunk's mov r2,r1). The return type is u32,
      not the reference C's void - every call site branches on r0.
      Reference C (decomp/c/010/0811d718_FUN_0811d718.c) gets the
      shape right (scratch probe out-slot, kind-4 constants) but
      types param_2 as undefined4 (the disassembly and every call
      site show a pointer) and hides the entry-thunk / shared-body
      split - the port follows the disassembly. Host tests
      (recording mocks through the two seams, SLOT_TEST_LOCK
      serialized):
      src/app/vtable_set.rs::scalar_read_unsupported_status_bails_before_the_read
      / scalar_read_probe_error_bails_before_the_read /
      scalar_read_probe_args_and_initial_out_slot /
      scalar_read_read_args_and_value_delivery /
      scalar_read_read_error_skips_the_finish /
      scalar_read_finish_args_and_final_return.

  - address: 0x0811d724
    ghidra: FUN_0811d724
    name: vtable_file_open
    size: 140
    sig: "u32 vtable_file_open(record: *mut u8, path: *const u8, write_mode: u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: record
      param_2/r7: path
      param_3/r5: write_mode
      r6: read_only (mode bit for the ctor; later the zero source)
      r7 (second use): status
    notes: >-
      9 bl call sites (grep on decomp/osos.asm): 0x0810a99c,
      0x0811af90, 0x0811c5cc, 0x08136580, 0x0815e8b8, 0x081afe0c,
      0x081b0028, 0x081bc680 and 0x082857c0 - every site passes a
      six-byte record (a feature-state field param_1 + 0x6c / + 0x84 /
      + 0x54 ... or a stack record) in r0, a 0x08279284-built path
      (iPod_Control/Device/radio_..., PlayCounts, Users) in r1 and a
      write-mode flag (0 or 1) in r2; none sets r3 deliberately, and
      every site branches on the returned status. The file-open entry
      of the vtable message family - creates the file-backed
      property-store object the family's query/set thunks then drive
      through the SAME record pointer (FUN_0811af5c opens param_1 +
      0x6c, then queries param_1 + 0x6c). Write mode first calls the
      pre-open remove 0x08084d58(path, 0) - a mutex-guarded
      (0x08206e40/0x08206e6c) vtable-slot-+0x5c call on a facade from
      0x0818a0bc, result discarded; delete-before-recreate pattern,
      exact operation not established - and inverts the mode bit
      (read_only = write_mode == 0). Then operator_new(0x34) (the
      ported heap::veneers::operator_new, called directly) and the
      store-object ctor 0x08149cec(this, path, read_only, 0, 0x8000):
      base ctor 0x0816bfe4 with the flags word, derived vtable store,
      inner operator_new(0x54) file object opened via 0x08278e8c and
      stashed at +0x30. The result is stored into the record
      UNCONDITIONALLY (never null-checked), the inner object's status
      word at +0x1c decides: zero keeps the object, nonzero disposes
      it through vtable slot +0x04 (the exact vtable_slot_04_dispose
      sequence, inlined in the original; the port calls the ported
      export directly) and NULLs the record. Either way record+0x04
      is cleared, record+0x05 = (write_mode != 0), and the status
      returns verbatim. Deviations: the two unported callees sit
      behind the VTABLE_FILE_OPEN_REMOVE (no-op default) and
      VTABLE_FILE_OPEN_CTOR (fail-closed default: stand-in object
      with a hard-failure status word - the ft/system.rs
      FT_PLATFORM_FILE_CTOR policy; NOT exact-body models, the
      callees are a filesystem facade and a C++ file-class
      subsystem). arg4 (r3) is DEAD - no forwarded parameter, the
      entry spill slot is overwritten with 0x8000 before any read
      (the epilogue restores r3 as 0x8000). The record's first word
      is stored/read pointer-sized (32-bit str/ldr), so on a 64-bit
      host the trailing byte stores overwrite the pointer's high
      half - the vtable_query_4c_kind4_read host-representation
      note; on the failure path the word is NULLed first, so no live
      pointer is truncated. Reference C
      (decomp/c/010/0811d724_FUN_0811d724.c) is largely accurate
      (catches the mode inversion and the trailing bool) but passes
      a phantom param_4 into FUN_08084d58 and types param_1 as int*,
      hiding the trailing mode bytes - the port follows the
      disassembly. Host tests (recording mocks through the two
      seams, SLOT_TEST_LOCK serialized):
      src/app/vtable_set.rs::file_open_write_mode_calls_the_preopen_remove_with_a_zeroed_r1
      / file_open_read_mode_skips_the_preopen_remove /
      file_open_ctor_argument_routing_and_mode_inversion /
      file_open_success_stores_the_object_and_mode_bytes /
      file_open_failure_disposes_nulls_and_returns_the_status_verbatim
      / file_open_mode_byte_mirrors_write_mode_on_both_paths /
      file_open_default_remove_stub_is_a_noop /
      file_open_default_ctor_stub_fails_the_open_closed.

  - address: 0x0811d7b0
    ghidra: FUN_0811d7b0
    name: vtable_slot_4c_dispatch
    size: 28
    sig: "u32 vtable_slot_4c_dispatch(handle: *mut *mut u8, kind: u32, data: usize, extra: *const usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
      param_2: kind
      param_3: data
      param_4: extra
    notes: >-
      16 call sites (grep on decomp/osos.asm): 15 bl - the 8-site
      cluster 0x0811d230..0x0811d2e0, 0x0811d4a0, the 4-site cluster
      0x0811d5c4..0x0811d634, and the two sites 0x0811d830 /
      0x0811d85c in the unported query-clamp-write routine at
      0x0811d818 (calls it with kind 4 and a stack out-slot, treats
      5 as "unsupported - bail silently" via cmp r0,#0x5; beq, other
      nonzero as hard error) - plus the tail b at 0x0811d474 from
      the ported util/vtable_query.rs vtable_query_4c_kind4 thunk
      0x0811d46c. The shared vtable dispatcher of the slot +0x4c
      message family - the slot +0x4c twin of this module's
      vtable_slot_50_dispatch (FUN_0811d7fc @ 0x0811d7fc), differing
      only in the slot offset: stmdb sp!,{r3,lr}; ldr r0,[r0];
      ldr r3,[r0]; ldr r12,[r3,#0x4c]; mov r3,sp; blx r12;
      ldmia sp!,{r12,pc} - double dereference handle -> object ->
      vtable, the method at slot +0x4c invoked as method(object,
      kind, data, &spilled_r3), its error code (0 = success)
      returned verbatim; kind (r1) passes through untouched (the
      kind-4 binding lives in the callers, unlike the +0x50 family),
      and the spilled r3 is whatever the caller carried (no call
      site sets it deliberately). Deviations: the r3 spill is
      collapsed into the 4th parameter (the vtable_slot_50_dispatch
      deviation verbatim - the original receives r3 by value and
      spills it itself; the port's callers pre-spill and pass the
      address), so the export is NOT ABI-hookable at 0x0811d7b0
      where r3 arrives by value (no hook targets it). NO new seam:
      the one ported caller vtable_query_4c_kind4 routes through
      util/vtable_query.rs's VTABLE_SLOT_4C_DISPATCH static, still
      wired to its private stub of this same body - pointing that
      seam at this export is a deliberate follow-up (one function
      per commit). read_unaligned slot load keeps the layout
      byte-exact on a 64-bit host (0x4c is 4-aligned but not
      8-aligned). Reference C
      (decomp/c/010/0811d7b0_FUN_0811d7b0.c) mis-decompiles: drops
      all four call arguments, showing a void call of a no-arg
      method; the port follows the disassembly. match.py (exit 1,
      diff expected): same double-deref / slot-load / indirect-call
      shape as the +0x50 twin; LLVM lowers read_unaligned on ARMv5
      to 4x ldrb + orr assembly instead of ldr r12,[r3,#0x4c],
      tail-calls (pop then bx) instead of blx/ldmia-pc, and needs
      no mov r3,sp because `extra` already arrives in r3 - the
      documented collapsed-spill deviation; the crate fp frame
      accounts for the rest. Host tests (direct calls on a fake
      vtable): double-deref chain traversal + slot +0x4c selection
      against +0x44/+0x54 decoys with a non-4 kind (r1 genericity),
      verbatim r3-spill pointer forwarding, verbatim status
      forwarding across 0 / 5 (the 0x0811d818 caller's
      "unsupported") / generic error -
      src/app/vtable_set.rs::dispatch_4c_double_dereferences_and_loads_slot_4c_exactly
      /
      dispatch_4c_forwards_the_spilled_r3_pointer_verbatim /
      dispatch_4c_returns_the_methods_status_verbatim.

  - address: 0x0811d7cc
    ghidra: FUN_0811d7cc
    name: vtable_slot_04_dispose
    size: 48
    sig: "u32 vtable_slot_04_dispose(handle: *mut *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: handle
    notes: >-
      14 bl call sites (grep on decomp/osos.asm): 0x0810ab00,
      0x0811b278, 0x0811c798, 0x0811d8c8 (inside the unported wrapper
      thunk 0x0811d8c0, which disposes and then returns the handle),
      0x08136840, 0x0815e904, 0x081affb4, 0x081b0174, 0x081bc82c and
      the 5-site cluster 0x08285738..0x08285938 - several sit in the
      same routines that drive the kind-4 probe/set thunks, the
      message family's teardown counterpart. The NULL-guarded dispose
      thunk sitting between the two ported dispatchers
      vtable_slot_4c_dispatch (0x0811d7b0) and vtable_slot_50_dispatch
      (0x0811d7fc) - NOT a third dispatch variant (it binds no kind
      and sends no message): stmdb sp!,{r4,lr}; mov r4,r0; ldr
      r0,[r0]; cmp r0,#0; beq out; ldr r1,[r0]; ldr r1,[r1,#0x4];
      blx r1; mov r0,#0; str r0,[r4]; out: mov r0,#0; ldmia
      sp!,{r4,pc} - if *handle is non-NULL, double dereference
      handle -> object -> vtable, invoke the method at vtable slot
      +0x4 with the object in r0, NULL the handle AFTER the call
      (str r0,[r4] follows blx r1), return 0; a NULL *handle skips
      both the call and the store and still returns 0 (mov r0,#0 on
      both paths; every observed call site ignores the return).
      Deviations: the slot +0x4 method is modeled as a
      single-argument unit-returning call - at the blx only r0 is
      meaningful (r1 holds the method pointer itself, r2/r3 are the
      caller's leftovers; the frame spills {r4,lr} only, so there is
      no forwarded parameter, unlike the dispatcher siblings) and
      any method return value is discarded (r0 reloaded with 0);
      read_unaligned slot load keeps the layout byte-exact on a
      64-bit host (0x4 is 4-aligned but not 8-aligned); no seam - no
      ported caller exists, host tests call the body directly on a
      fake vtable (the dispatcher-test precedent). Reference C
      (decomp/c/010/0811d7cc_FUN_0811d7cc.c) mis-decompiles the blx:
      drops the object argument ((**(code **)(*(int *)*param_1 +
      4))()), showing a no-arg call; the disassembly loads r0 =
      *param_1 before blx r1 - the port follows the disassembly.
      Host tests (direct calls on a fake vtable): double-deref chain
      traversal + slot +0x4 selection against a +0xc decoy (a 0x0
      decoy would overlap the 8-byte host pointer at 0x4), handle
      NULLed after (not before) the method, NULL-handle skip path -
      src/app/vtable_set.rs::dispose_double_dereferences_and_loads_slot_04_exactly
      / dispose_nulls_the_handle_after_the_call /
      dispose_skips_the_call_on_a_null_handle_and_returns_zero.

  - address: 0x0811d818
    ghidra: FUN_0811d818
    name: vtable_query_4c_kind4_read
    size: 92
    sig: "u32 vtable_query_4c_kind4_read(handle: *mut *mut u8, capacity: u32, buffer: *mut u8, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r5: handle
      param_2/r4: capacity
      param_3/r6: buffer
      param_4/r3: forwarded
    notes: >-
      3 bl call sites (grep on decomp/osos.asm): 0x08136784,
      0x081bc7f0, 0x08271504 - all pass a capacity in r1 (0x40,
      0x40, 0x100), a caller stack-buffer pointer in r2 (add r2,
      sp, #0x4) and the handle in r0; none sets r3. NO reference C
      exists (decomp/c/010/ has no 0811d818 file); the port follows
      the disassembly. The query-size-then-read routine of the slot
      +0x4c message family - the only caller that drives
      vtable_slot_4c_dispatch (0x0811d7b0, ported in this module)
      twice, and the mirror of the unported slot +0x50 sibling at
      0x0811d874. The entry stmdb sp!,{r2,r3,r4,r5,r6,lr} spills
      arg3 (buffer) and arg4 (r3) into a two-word stack pair: the
      first dispatch sends kind 4 with &pair[1] as out-slot and the
      method answers with the available size; the size is clamped
      to capacity with an UNSIGNED compare (strhi); the second
      dispatch performs the read with the clamped size as the
      generic middle argument and the buffer pointer as data; only
      on a zero read status does the closing bleq fire - and
      0x0811d46c is the PORTED kind-4 query thunk
      util/vtable_query.rs vtable_query_4c_kind4 (mov r2,r1;
      mov r1,#4; b 0x0811d7b0), NOT a block-copy helper: it
      re-dispatches kind 4 with the {buffer, clamped_size} pair as
      the message and its error code becomes the return value.
      Every bail path returns the last call's status verbatim
      (5 = "unsupported", behaviorally identical to a hard error
      here). Deviations: both callees are ported and route through
      the new VTABLE_QUERY_4C_READ_DISPATCH /
      VTABLE_QUERY_4C_READ_FINISH seams (the VTABLE_SLOT_50_DISPATCH
      pattern; role-specific names because util/vtable_query.rs's
      glob-re-exported VTABLE_SLOT_4C_DISPATCH already claims the
      family name at the crate root, and a module-local finish seam
      avoids racing util's parallel tests). arg4 is modeled as
      `forwarded` per family convention - it doubles as the size
      out-slot's INITIAL word (the entry spill) and as each
      dispatcher's stmdb r3 spill. r2 into the closing call is DEAD
      (method-clobbered across the second bl; the thunk's mov r2,r1
      discards it) - the "arg3 forwarded to the copy-out" reading
      does NOT hold; the port passes 0 for the thunk's _unused.
      pair[1] is only ever consumed as a 32-bit word (ARM ldr), so
      the port truncates to u32 on every read, keeping the stack
      pair exact on a 64-bit host. match.py (exit 1, diff
      expected): the two dispatches and the finish call are ldr/blx
      through the seams in place of bl; LLVM merges the cmp #5;beq
      / cmp #0;bne pair into one status test (both branch to the
      same return-status epilogue); the clamp survives as
      cmp/movhi/strhi (unsigned HI); the collapsed-spill convention
      puts the extra pointer in r3 directly; the crate fp frame
      accounts for the rest. Host tests:
      src/app/vtable_set.rs::query_read_unsupported_status_bails_before_the_read
      / query_read_query_error_bails_before_the_read /
      query_read_first_dispatch_args_and_initial_out_slot /
      query_read_clamps_the_size_unsigned_and_routes_the_read /
      query_read_read_error_skips_the_finish /
      query_read_finish_args_and_final_return.

  - address: 0x0811d874
    ghidra: FUN_0811d874
    name: vtable_set_50_write_indirect_kind4
    size: 56
    sig: "u32 vtable_set_50_write_indirect_kind4(handle: *mut *mut u8, selector: u32, data: *const u8, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r5: handle
      param_2/r4: selector
      param_3/r6: data
      param_4/r3: forwarded
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0811d324, the
      middle stage of the unported three-stage routine FUN_0811d2f8
      (open 0x0811d458 -> this call -> tail commit 0x0811d340 - the
      vtable_set_50_kind4 shape with this routine in place of the
      by-value write stage 0x0811d56c; FUN_0811d2f8 passes its own
      arg4 as this function's arg2 and its own arg3 - a pointer from
      an accessor, e.g. 0x082a50b0/0x082770e0 at its call sites
      0x08136600/0x081b00fc - as arg3). NO reference C exists
      (decomp/c/010/ has no 0811d874 file); the port follows the
      disassembly. True extent verified against the disassembly: the
      next function starts at 0x0811d8ac, so 0x38 = 56 bytes, matching
      functions.csv. The indirect (by-pointer) write stage of the
      kind-4 slot +0x50 message family - the second two-dispatch
      member after vtable_set_50_write_kind4 and the slot +0x50 mirror
      of vtable_query_4c_kind4_read (0x0811d818). The entry stmdb
      sp!,{r3,r4,r5,r6,r7,lr} spills arg4, but str r1,[sp,#0x0]
      OVERWRITES that spill slot with the selector and mov r2,sp makes
      it the first message's data word: dispatch(handle, 4,
      &selector) - the open stage 0x0811d458's exact message. Only on
      a zero status does the bleq fire the second, GENERIC dispatch:
      the selector itself is the middle (kind) argument (moveq r1,r4)
      and the caller's arg3 pointer is the data (moveq r2,r6) - the
      shape of query_read's read dispatch, whose middle argument
      carries the clamped size. A nonzero first status short-circuits
      and returns verbatim; otherwise the second dispatch's status
      returns. Deviations: the callee 0x0811d7fc is ported
      (vtable_slot_50_dispatch) and both calls route through the
      VTABLE_SLOT_50_DISPATCH read_volatile seam, as the siblings do.
      arg4 reaches ONLY the first dispatch (nothing before the first
      bl touches r3), modeled as `forwarded` per family convention;
      for the SECOND dispatch r3 is DEAD - the first method clobbers
      r0-r3, the dispatcher's ldmia {r12,pc} epilogue does not restore
      r3, and the entry spill slot was overwritten with the selector -
      so the port passes a pointer to a zero stack word for that
      unobservable argument (the query_read _unused precedent). arg3
      is typed *const u8 (the query_read buffer mirror, direction
      reversed: the second method reads the payload); it is only ever
      consumed as an address. match.py (exit 0): the two dispatches
      are ldr/blx through the seam in place of bl; LLVM turns the
      conditional bleq into a beq over the epilogue (same structure);
      the collapsed-spill convention pre-spills the forwarded word and
      passes its address; the crate fp frame accounts for the rest.
      Host tests:
      src/app/vtable_set.rs::indirect_write_first_dispatch_error_skips_the_second
      / indirect_write_success_redispatches_with_exact_args /
      indirect_write_arg4_spill_reaches_only_the_first_dispatch /
      indirect_write_forwards_the_second_status_verbatim.

  - address: 0x082c2290
    ghidra: FUN_082c2290
    name: cg_buffer_align_offset
    size: 32
    sig: "u32 cg_buffer_align_offset(cg_codegen_buffer_t *output, u32 align)"
    module: codegen/ir
    status: ported
    vars: {param_1: output, param_2: align}
    notes: >-
      2 bl call sites (grep on decomp/osos.asm), both passing align = 4
      via `mov r1, #0x4`: 0x082c13e4 inside the label-fixup resolver
      FUN_082c1360 (aligning the output before the fixup words are
      emitted) and 0x082c232c inside the word emitter @ 0x082c231c
      (aligning before the 32-bit store, then post-incrementing by 4).
      The whole body is the classic power-of-two round-up idiom on the
      emitted-code buffer's current_offset (+0x804):
      `ldr r2,[r0,#0x804]; add r2,r2,r1; sub r2,r2,#0x1; sub r1,r1,#0x1;
      bic r1,r2,r1; str r1,[r0,#0x804]; mov r0,r1; bx lr`, i.e.
      current_offset = (current_offset + align - 1) & ~(align - 1),
      stored back and returned. This is the "page accessor"/alignment
      helper referenced by the cg_codegen_buffer_t notes at
      0x082c23d4/0x082c22b0. Edge semantics kept for parity, not
      sanitized: align is ASSUMED a power of two (a non-power-of-two
      align clears more low bits than it rounds to); align == 1 is the
      identity; align == 0 is pathological - align - 1 wraps to
      all-ones, the bic yields 0 and the buffer rewinds to its start,
      unguarded in the original and in the port (which uses wrapping
      arithmetic, as ARM has no overflow traps). match.py (exit 1, diff
      expected): the ldr/sub/str/mov sequence is verbatim; LLVM emits
      `add r2, r1, r2; rsb r1, r1, #0; and r1, r2, r1` for the bic
      (identical, since ~(a-1) == -a in two's complement), plus the
      crate fp frame push/pop. Host tests:
      src/codegen/ir.rs::buffer_align_offset_rounds_up_stores_back_and_leaves_neighbors
      (already-aligned identity, +1 rounds up, store-back visible
      through cg_buffer_current_offset, poisoned neighbours proving no
      adjacent word is written),
      buffer_align_offset_sweeps_power_of_two_alignments (align
      1/2/4/8/0x10 over offsets 0..64, return == stored),
      buffer_align_offset_keeps_align_zero_parity (align 0 rewinds to
      0, matching the unguarded original).

  - address: 0x082c231c
    ghidra: FUN_082c231c
    name: cg_buffer_emit_word
    size: 52
    sig: "void cg_buffer_emit_word(cg_codegen_buffer_t *output, u32 value)"
    module: codegen/ir
    status: ported
    vars: {param_1: output, param_2: value}
    notes: >-
      42 bl + 15 tail b call sites (grep on decomp/osos.asm; sampled bl
      sites 0x082b48a4/0x082b4954/0x082beb60/0x082becac/0x082c111c,
      sampled tail sites 0x082bcf30/0x082bebc8/0x082c1240) - the JIT's
      canonical 32-bit word emitter, one of the eight emitter bodies
      listed in the cg_codegen_buffer_t notes at 0x082c23d4. Body:
      align current_offset (+0x804) up to 4 through cg_buffer_align_offset
      @ 0x082c2290 (ported, called directly), RE-READ the aligned offset
      (the original ignores the aligner's r0 and reloads the field),
      resolve it to a write pointer through the page accessor
      cg_buffer_page_pointer @ 0x082c5b1c (ported; the call still
      routes through the CG_BUFFER_PAGE_POINTER seam, which stays wired
      to the ported accessor - the vtable_slot_50_dispatch precedent,
      direct-call rewiring a deliberate follow-up), store value
      verbatim (str r5, [r0]), then
      RE-READ current_offset again and post-increment it by 4 from the
      ALIGNED value. Both field reloads are kept verbatim: through the
      seam a replacement could legally move the offset, and only the
      reloads reproduce the original's behavior in that case. No NULL
      check on the accessor's result, matching the original. match.py
      (exit 1, diff expected): the align bl, offset ldr, word str and
      the ldr/add #4/str post-increment are verbatim; the accessor is
      ldr/blx through the CG_BUFFER_PAGE_POINTER static in place of
      bl 0x082c5b1c (the documented seam scheme, same as CG_BUFFER_ALLOC);
      the align call shows as a same-section reloc; the crate fp frame
      accounts for the rest. Host tests:
      src/codegen/ir.rs::buffer_emit_word_aligns_stores_verbatim_and_advances_from_the_aligned_offset
      (misaligned 9 rounds to 12, accessor receives (buffer, 12), word
      bit-exact at the returned pointer, offset becomes 16 = aligned +
      4, poisoned neighbours),
      buffer_emit_word_end_to_end_through_a_cooperating_accessor (three
      emits over a flat-arena fake: verbatim stores, align gap skipped
      untouched, final offset 16),
      buffer_page_pointer_lazy_allocates_zeroes_and_reuses_the_page
      (accessor through the page-slot seam's wired default: lazy
      0x1000-byte allocation, zero-fill over the allocator poison,
      slot store-back, resident-page reuse, distinct pages distinct),
      buffer_page_pointer_returns_null_when_the_page_allocation_fails
      (the deviation: NULL, slot never stored).

  - address: 0x082c23b0
    ghidra: FUN_082c23b0
    name: cg_buffer_read_word
    size: 16
    sig: "u32 cg_buffer_read_word(cg_codegen_buffer_t *output, u32 offset)"
    module: codegen/ir
    status: ported
    vars: {param_1: output, param_2: offset}
    notes: >-
      2 bl call sites (grep on decomp/osos.asm): 0x082c1720 and
      0x082c174c, both inside the label-fixup resolver FUN_082c16e0 -
      reading back the previously emitted instruction word at a fixup
      position so the resolver can splice the resolved branch offset
      into it while preserving the opcode/condition bits. The
      read-side mirror of cg_buffer_emit_word @ 0x082c231c: the whole
      body is `bl 0x082c5b1c` (resolve offset to a pointer through the
      code-page accessor cg_buffer_page_pointer - ported, call routed
      through the CG_BUFFER_PAGE_POINTER seam wired to that port, the
      documented seam scheme same as the emitter) followed by
      `ldr r0, [r0, #0x0]` (load the word). The saved r4 in the frame
      (`stmdb sp!, {r4, lr}` / `ldmia sp!, {r4, pc}`) is never
      written - the pair only keeps the stack 8-byte aligned across
      the bl. Unlike the emitter the offset is NOT aligned first (no
      bl 0x082c2290): the resolver's fixup positions are word-aligned
      by construction. No NULL check on the accessor's result, same
      failure semantics as cg_buffer_emit_word. Host tests:
      src/codegen/ir.rs::buffer_read_word_loads_through_the_page_pointer_seam_with_the_offset_verbatim
      (recording fake: word bit-exact, accessor receives (buffer,
      offset) verbatim - offset 6 NOT aligned up, unlike the emitter,
      cell untouched, current_offset never read),
      buffer_read_word_reads_back_words_emitted_across_a_page_boundary
      (end-to-end through the wired defaults: 0x404 words emitted via
      cg_buffer_emit_word across the 0x1000-byte page boundary, every
      word reads back bit-exact, offset unmoved).

  - address: 0x082c23c0
    ghidra: FUN_082c23c0
    name: cg_buffer_write_word
    size: 20
    sig: "void cg_buffer_write_word(cg_codegen_buffer_t *output, u32 offset, u32 value)"
    module: codegen/ir
    status: ported
    vars: {param_1: output, param_2: offset, param_3: value}
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x082c1788, inside the
      label-fixup resolver FUN_082c16e0 - storing the patched
      instruction word back into the emitted-code buffer after the
      resolver spliced the resolved branch offset into the word it
      read with cg_buffer_read_word @ 0x082c23b0 (FUN_082c23c0's
      param_3 is uVar2 | uVar1 in the resolver: the original word's
      opcode/condition bits OR the resolved offset). The write-side
      mirror of cg_buffer_read_word: the whole body is `mov r4, r2`
      (keep value across the call - unlike the reader, the saved r4
      IS used here), `bl 0x082c5b1c` (resolve offset to a pointer
      through the code-page accessor cg_buffer_page_pointer - ported,
      call routed through the CG_BUFFER_PAGE_POINTER seam wired to
      that port, the documented seam scheme same as the emitter and
      the reader), `str r4, [r0, #0x0]` (store the word). Like the
      reader - and unlike the emitter - the offset is NOT aligned
      first (no bl 0x082c2290) and current_offset (+0x804) is never
      touched: the resolver's fixup positions are word-aligned by
      construction. No NULL check on the accessor's result, same
      failure semantics as cg_buffer_emit_word. Host tests:
      src/codegen/ir.rs::buffer_write_word_stores_through_the_page_pointer_seam_with_the_offset_verbatim
      (recording fake: word stored bit-exact, accessor receives
      (buffer, offset) verbatim - offset 6 NOT aligned up, unlike the
      emitter, current_offset never touched),
      buffer_write_word_patches_words_emitted_across_a_page_boundary
      (end-to-end through the wired defaults: 0x404 words emitted via
      cg_buffer_emit_word, then the resolver's read-modify-write at
      fixup positions on both sides of the 0x1000-byte page boundary,
      every word reads back bit-exact, offset unmoved).

  - address: 0x082c2350
    ghidra: FUN_082c2350
    name: cg_buffer_copy_out
    size: 96
    sig: "void cg_buffer_copy_out(cg_codegen_buffer_t *output, u32 offset, u8 *dst, u32 len)"
    module: codegen/ir
    status: ported
    vars: {param_1: output, param_2: offset, param_3: dst, param_4: len}
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0824325c inside the
      compile-and-patch driver FUN_08243138, called as
      copy_out(buffer, 0, dst, cg_buffer_current_offset(buffer)) -
      flattening the freshly emitted code into its final linear
      destination. The emitted-code buffer's bulk READER, mirror of
      the word emitters: loops while len != 0 (b-to-tail-test entry,
      so len == 0 copies nothing), chunk = 0x1000 - (offset & 0xfff)
      clamped to len (`cmp r4,r6; movls r6,r4`, unsigned), resolves
      the page BASE through the lazy page-slot accessor FUN_082b7edc
      (the CG_BUFFER_PAGE_SLOT seam, same bl target
      cg_buffer_page_pointer routes through), copies chunk bytes from
      the page base to the dst cursor through ROM thunk 0x08037db0
      (-> 0x22000020, __rt_memcpy - the ported
      libc::rt_memcpy::__rt_memcpy, called directly per house
      pattern), then advances offset/dst/len by chunk. QUIRK kept for
      parity, not sanitized: the memcpy source is the page BASE - the
      original never adds offset & 0xfff to it (no add between the
      accessor's return and the bl 0x08037db0), so for a misaligned
      offset the first chunk reads from the page's START while being
      sized 0x1000 - (offset & 0xfff), i.e. the copy comes out
      shifted early by offset & 0xfff bytes; the sole call site passes
      offset 0 (mov r1,#0x0 @ 0x08243258), so the quirk is latent and
      the port reproduces it verbatim. No NULL check on the page-slot
      result, same failure semantics as cg_buffer_emit_word. match.py
      (exit 1, diff expected): the lsl/lsr mask shows as an `and`
      against a precomputed 0xfff register, the movls clamp shows as
      movcc (equal-valued when len == page_left, so identical), the
      page-slot accessor is ldr/blx through the CG_BUFFER_PAGE_SLOT
      static in place of bl 0x082b7edc (the documented seam scheme,
      same as emit_word's CG_BUFFER_PAGE_POINTER), __rt_memcpy shows
      as a same-section reloc, LLVM hoists the len == 0 test to the
      entry in place of the original's b-to-tail-test, and the crate
      fp frame accounts for the rest. Host tests:
      src/codegen/ir.rs::buffer_copy_out_copies_a_sub_page_run_in_one_chunk
      (single page-slot call, bit-exact bytes, nothing past len),
      buffer_copy_out_chunks_a_copy_spanning_two_pages (0x1000 + 0x10:
      one chunk per page in offset order, full page then remainder),
      buffer_copy_out_exact_page_multiple_stops_without_a_third_chunk
      (len 0x2000: exactly two calls, no index-2 lookup),
      buffer_copy_out_with_zero_length_copies_nothing (len 0: no
      page-slot call, dst untouched),
      buffer_copy_out_keeps_the_page_base_quirk_for_misaligned_offsets
      (offset 0xff0, len 0x20: chunk sized 0x10 but read from page0
      BASE, not base + 0xff0; second chunk from page1 base),
      buffer_copy_out_flattens_words_emitted_across_a_page_boundary
      (end-to-end through the wired defaults: 0x404 words emitted via
      cg_buffer_emit_word, flattened back bit-exact across the
      0x1000-byte page boundary).

  - address: 0x082c5b1c
    ghidra: FUN_082c5b1c
    name: cg_buffer_page_pointer
    size: 32
    sig: "u8 *cg_buffer_page_pointer(cg_codegen_buffer_t *output, u32 offset)"
    module: codegen/ir
    status: ported
    vars: {param_1: output, param_2: offset, r4: intra}
    notes: >-
      3 bl call sites (0x082c2338 in cg_buffer_emit_word plus two more;
      grep on decomp/osos.asm). The emitted-code buffer's page accessor:
      returns pages[offset >> 12] + (offset & 0xfff) - the body is
      `mov r4, r2, lsl #0x14; mov r4, r4, lsr #0x14` (intra-page byte),
      `mov r1, r1, lsr #0xc` (page index), `bl 0x082b7edc` (the lazy
      page-slot accessor), `add r0, r0, r4` (base + intra). PORTED in
      src/codegen/ir.rs. SEAM DECISION (the app/vtable_set.rs
      vtable_slot_50_dispatch precedent): the port stays the wired
      default of the CG_BUFFER_PAGE_POINTER seam and cg_buffer_emit_word
      keeps routing through the seam - retained for hookability and
      host-test interception; rewiring the emitter to a direct call is a
      deliberate follow-up (one function per commit). The bl target
      0x082b7edc stays identified behind the new CG_BUFFER_PAGE_SLOT
      seam, whose wired default (default_cg_buffer_page_slot) models its
      exact body - the lazy-alloc/memzero/store-back logic formerly
      inlined in default_cg_buffer_page_pointer. DEVIATION: the original
      has NO NULL guard on the accessor's result (verified in osos.asm:
      the `add r0, r0, r4` @ 0x082c5b34 is unconditional, and an
      allocation failure has already crashed one callee earlier, on the
      wild 0x1000-byte memzero at address 0 inside 0x082b7edc); the port
      propagates the page-slot seam's NULL instead, carrying forward the
      previous seam default's documented deviation, so emit_word's
      store-through-NULL failure semantics are unchanged. match.py
      (exit 1, diff expected): same shape - offset split (lsr #12 +
      and 0xfff), call through the CG_BUFFER_PAGE_SLOT static (ldr/blx
      in place of bl 0x082b7edc, the documented seam scheme), base +
      intra add; the cmp/addne pair is the documented NULL-propagation
      deviation, the crate fp frame accounts for the rest. Host tests:
      src/codegen/ir.rs::page_pointer_seam_stays_wired_to_the_ported_accessor
      (both seams' wiring),
      buffer_page_pointer_splits_offsets_straddling_page_boundaries
      (0xfff/0x1000/0x1001/0x2000: exact idx 0/1/1/2 to the slot seam,
      result = base + intra),
      buffer_page_pointer_propagates_null_from_the_page_slot_seam (the
      deviation: NULL out, exact idx still passed),
      buffer_page_pointer_lazy_allocates_zeroes_and_reuses_the_page
      (through the slot seam's wired default: lazy 0x1000-byte
      allocation, zero-fill over the allocator poison, slot store-back,
      resident-page reuse, distinct pages distinct),
      buffer_page_pointer_returns_null_when_the_page_allocation_fails
      (the deviation end to end: NULL, slot never stored).

  - address: 0x082b7edc
    ghidra: FUN_082b7edc
    name: cg_buffer_page_slot
    size: 52
    sig: "u8 *cg_buffer_page_slot(cg_codegen_buffer_t *output, u32 index)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0: output
      param_2/r1: index
      r5: page_slot (output + index*4, the pages[index] slot address)
      r4: page
    notes: >-
      2 bl call sites: 0x082c2384 inside cg_buffer_copy_out @ 0x082c2350
      (ported) and 0x082c5b30 inside cg_buffer_page_pointer @ 0x082c5b1c
      (ported). The lazy page-slot accessor of the emitted-code buffer:
      reads buffer->pages[index] at +0x04+index*4; when NULL, mallocs
      one 0x1000-byte page (bl 0x0802edac = malloc, ported), zero-fills
      it through the IRAM veneer 0x08037dc8 (-> 0x220002d4, the
      relocated copy of memzero @ 0x080002d4, ported in libc/memzero),
      stores it into the slot and returns it; a resident slot comes back
      unchanged. No NULL check after malloc (a failure memzeroes 0x1000
      bytes at address 0) and no bounds check on the 512-slot table.
      PORTED in src/codegen/ir.rs. The body is exactly the former wired
      default of the CG_BUFFER_PAGE_SLOT seam, promoted to the export
      and kept as that seam's wired default (the app/vtable_set.rs
      vtable_slot_50_dispatch precedent): both callers keep routing
      through the seam, retained for hookability; rewiring to direct
      calls is a deliberate follow-up (one function per commit).
      DEVIATIONS (carried forward from the seam default, documented in
      the module header): the page allocation goes through the
      swappable CG_BUFFER_ALLOC slot (default: the ported malloc) where
      the original bls malloc directly, and a failed allocation returns
      NULL where the original would memzero 0x1000 bytes at address 0.
      match.py (exit 1, diff expected): the slot read
      (add r7,r0,r1,lsl #2 / ldr r0,[r7,#4]), NULL check, malloc
      through the CG_BUFFER_ALLOC static (ldr/blx in place of
      bl 0x0802edac, the documented seam scheme), zero-fill, store-back
      (str r0,[r7,#4]) and return structure all remain; LLVM inlined
      the ported memzero and idiom-lowered the fixed 0x1000-byte
      zero-fill to __aeabi_memclr/__aeabi_memclr4 calls, and the crate
      fp frame accounts for the rest. Host tests:
      src/codegen/ir.rs::buffer_page_slot_returns_a_resident_page_unchanged
      (hit path: returned unchanged, no store-back, no zero-fill),
      buffer_page_slot_lazy_allocates_zeroes_and_stores_back (miss:
      0x1000-byte allocation, zero-fill over the allocator poison,
      slot store-back, resident reuse, index-511 slot arithmetic,
      untouched slots stay NULL),
      buffer_page_slot_returns_null_and_never_stores_when_allocation_fails
      (the deviation end to end: NULL, slot never stored), plus the
      updated page_pointer_seam_stays_wired_to_the_ported_accessor
      (the port is the CG_BUFFER_PAGE_SLOT seam's wired default).

  - address: 0x0811d2f8
    ghidra: FUN_0811d2f8
    name: vtable_set_50_indirect_kind4
    size: 72
    sig: "u32 vtable_set_50_indirect_kind4(handle: *mut *mut u8, selector: u32, data: *const u8, write_selector: u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: handle
      param_2/r5: selector
      param_3/r6: data
      param_4/r7: write_selector
    notes: >-
      4 bl call sites (0x08136600, 0x081b00fc, 0x081bc6e4,
      0x08271478), all checking the returned r0 status. The
      indirect-write sibling of vtable_set_50_kind4 @ 0x0811d68c:
      calls the ported vtable_set_50_open_kind4 @ 0x0811d458 directly,
      bails with its nonzero status, calls ported
      vtable_set_50_write_indirect_kind4 @ 0x0811d874 directly with
      (handle, arg4, arg3), bails with its nonzero status, then
      tail-calls ported vtable_set_50_commit_kind4 @ 0x0811d340 with
      (handle, selector). arg2 is the property selector used by open
      and commit; arg3 is the data pointer consumed only by the
      indirect write; arg4 is the indirect write's selector/kind
      word. Unlike the older vtable_set_50_kind4 port, no
      VTABLE_SET_50_KIND4_OPS stage seam is used because all three
      callees are now ported; their retained VTABLE_SLOT_50_DISPATCH
      seam keeps the firmware vtable-method boundary hookable. The
      r3 spill each stage exposes is modeled exactly: open sees arg4,
      its ldmia {r2,r3,r4,pc} epilogue makes the write see selector,
      and the write's str r1,[sp] overwrites its r3 spill so its
      ldmia {r3,...} makes commit see arg4. Reference C
      decomp/c/010/0811d2f8_FUN_0811d2f8.c mis-decompiles this as void,
      drops open's arguments, and inlines commit as the raw dispatcher;
      the port follows osos.asm. match.py (exit 1, diff expected): the
      same two error branches and direct three-stage call/tail-call
      structure remain; crate frame, Rust ABI argument reshuffling, and
      same-section relocations account for the diff. Host tests:
      src/app/vtable_set.rs::indirect_pipeline_open_error_short_circuits_before_write_and_commit /
      indirect_pipeline_write_error_skips_commit /
      indirect_pipeline_runs_in_order_with_exact_argument_routing.

  - address: 0x0811d64c
    ghidra: FUN_0811d64c
    name: vtable_set_50_kind2
    size: 64
    sig: "u32 vtable_set_50_kind2(handle: *mut *mut u8, selector: u32, value: *const u16, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: handle
      param_2/r5: selector
      param_3/r6: value
    notes: >-
      2 bl call sites (grep on decomp/osos.asm): 0x081365cc and
      0x081366d4 - both set r1 to a small property selector (0x34 /
      0x38), r2 to a pointer into a live structure (object + 0x1c /
      r5 + 0x18), r0 to a stack handle, and both test the returned
      r0 status (movs r4, r0, then bne at the first site, strbeq +
      beq at the second). The kind-2 (u16) twin of
      vtable_set_50_kind4 @ 0x0811d68c (one instruction later): the
      identical three-stage guarded property-write pipeline with the
      u16 write stage vtable_set_50_write_kind2 @ 0x0811d52c (ldrh)
      in place of the u32 0x0811d56c - open = FUN_0811d458
      (dispatch(handle, 4, &bare_selector)); if nonzero return it;
      write = FUN_0811d52c (dispatch(handle, 4, &2) then, only on 0,
      dispatch(handle, 2, &{zero-extended *value, 2})); if nonzero
      return it; else tail-call commit = FUN_0811d340
      (dispatch(handle, 4, &(selector | 0x80000000))). Arg routing:
      r5 = arg2 (selector) feeds stages 1 and 3, r6 = arg3 (pointer
      to the caller's u16 value) feeds only stage 2; kind encodes the
      value width. Deviations: all three callees are ported in this
      module and called DIRECTLY (vtable_set_50_indirect_kind4
      precedent - no VTABLE_SET_50_KIND4_OPS-style stage seam; the
      stages still route through the retained VTABLE_SLOT_50_DISPATCH
      seam); each stage's forwarded r3 is modeled exactly
      (vtable_set_50_indirect_kind4 refinement) - open sees the
      caller's r3 (the `forwarded` parameter), open's ldmia
      {r2,r3,r4,pc} epilogue makes the write see the selector, and
      the write's str r0,[sp,#4] kind-word store makes the commit
      see 2 (MESSAGE_KIND_2); return type is u32, not the reference
      C's void (both call sites branch on r0). Reference C
      decomp/c/010/0811d64c_FUN_0811d64c.c drops the open call's
      arguments and inlines the commit tail call as
      FUN_0811d7fc(param_1, 4, &stack0xfffffff4), hiding the
      0x0811d340 thunk and its 0x80000000 tag - the port follows
      osos.asm. Host tests:
      src/app/vtable_set.rs::kind2_open_error_short_circuits_before_write_and_commit /
      kind2_write_error_skips_commit_and_routes_the_value_pointer /
      kind2_loads_the_value_as_a_halfword /
      kind2_runs_in_order_with_exact_argument_routing.

  - address: 0x0811d478
    ghidra: FUN_0811d478
    name: vtable_query_4c_walk_alloc
    size: 180
    sig: "u32 vtable_query_4c_walk_alloc(handle: *mut *mut u8, selector: u32, scratch: usize, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r5: handle
      param_2/r1: selector (tag in the top byte, 24-bit selector under it)
      param_3/r2: scratch (only the query out-slot pair[0]'s initial content)
      param_4/r3: forwarded (reaches only the bare-branch size query and the FIRST walk query)
      r4: "bare_selector (bic r1, #0xff000000)"
      local_18 (sp+0): pair[0] (walk-query out-slot)
      local_14 (sp+4): pair[1] (size-query out-slot, zeroed at entry)
    notes: >-
      11 bl call sites (grep on decomp/osos.asm): 0x0810aac4,
      0x0811e954, 0x08136824, 0x081affa0, 0x081bc810, 0x081d0f10,
      0x081d110c, 0x081d15e4, 0x08271590, 0x0828595c - plus the
      recursive self-call at 0x0811d4f4 inside its own body. NOT
      called from the multi-message routine 0x0811d360 (that
      cluster's eight 0x0811d390..0x0811d440 sites all target the
      slot +0x50 dispatcher 0x0811d7fc). The stream-walking callers
      share one pattern: query a journal word via the kind-4 thunk,
      return 0 when its tag byte is 0xc0000000 (0x0811e930..,
      0x081d15cc.., 0x0828592c..), otherwise call THIS function with
      the word in r1 and branch on the status. The recursive
      tagged-word walker of the slot +0x4c vtable message family -
      the largest family member, between vtable_query_4c_kind4
      (0x0811d46c) and vtable_set_50_write_kind2 (0x0811d52c). The
      selector word is split into a top-byte tag (ands #0xff000000)
      and a 24-bit selector (bic); the tag picks the branch. Tag 0
      (bare): one kind-4 size query through the slot +0x4c
      dispatcher 0x0811d7b0 with &pair[1] as the out-slot, then on a
      zero status the object's vtable slot +0x54 method is invoked
      DIRECTLY as method(object, size + 4, 1) - the only family
      member that calls a vtable method without the dispatcher; any
      nonzero query status returns verbatim (status 5 gets NO
      special case, unlike the read siblings). Tag 0x40000000
      (PROBE_TAG): a walk loop queries the next journal word through
      the ported kind-4 thunk 0x0811d46c with &pair[0] as the
      out-slot (nonzero status returns verbatim); a word tagged
      0xc0000000 (COMMIT_PROBE_TAG) whose 24-bit selector matches
      closes the walk with 0, every other word is RECURSED into with
      the full tagged word as the new selector, its error
      propagating. Any other tag: return 0 with no message sent.
      Deviations: the ported callees route through the new
      role-specific seams VTABLE_QUERY_4C_WALK_DISPATCH (wired to
      the ported vtable_slot_4c_dispatch) and
      VTABLE_QUERY_4C_WALK_QUERY (wired to the ported util
      vtable_query_4c_kind4) - the VTABLE_QUERY_4C_SCALAR_DISPATCH /
      VTABLE_QUERY_4C_SCALAR_FINISH precedent; the slot +0x54 method
      has no seam (a firmware vtable method reached by blx; host
      tests install it on a fake vtable; read_unaligned slot load -
      0x54 is 4-aligned, not 8-aligned). arg3 (r2) is only the
      pair[0] out-slot's initial content, never read back - modeled
      as scratch. arg4 (r3) reaches only the bare-branch size-query
      dispatch and the FIRST walk query verbatim - modeled as
      forwarded; its spill slot is zeroed at entry (pair[1] starts
      at 0), and for later iterations / the recursion r3 is dead
      (method-clobbered), so 0 stands in (the
      vtable_set_50_write_indirect_kind4 dead-slot precedent); r2
      into the recursion is likewise dead, so scratch = 0 there.
      The pair is modeled as two u32 words (both slots only ever
      consumed as 32-bit ldr/str). The recursion is a direct
      self-call. Reference C decomp/c/010/0811d478_FUN_0811d478.c
      is accurate (tag split, size + 4 bump, slot +0x54 call, walk
      loop, recursion) - it only hides the r2/r3 register routing.
      Host tests (recording mocks through the two seams plus a fake
      vtable slot +0x54 method, SLOT_TEST_LOCK serialized):
      src/app/vtable_set.rs::walk_alloc_bare_selector_queries_size_and_allocs_via_slot_54
      / walk_alloc_bare_size_query_error_skips_the_alloc /
      walk_alloc_bare_status_5_gets_no_special_case /
      walk_alloc_other_tags_return_zero_without_any_message /
      walk_alloc_probe_walks_recurses_and_stops_at_the_matching_marker
      / walk_alloc_probe_recurses_into_foreign_commit_probe_markers /
      walk_alloc_probe_query_error_returns_verbatim /
      walk_alloc_probe_recursion_error_propagates /
      walk_alloc_default_seams_are_wired_to_the_ported_callees /
      walk_alloc_default_dispatch_seam_runs_the_ported_dispatcher.

  - address: 0x0811d8ac
    ghidra: FUN_0811d8ac
    name: vtable_file_record_init
    size: 20
    sig: "*mut u8 vtable_file_record_init(record: *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r0: record
    notes: >-
      7 bl call sites (grep on decomp/osos.asm): 0x0810a94c,
      0x0811c874, 0x0813653c, 0x0815e968, 0x081b0268, 0x081bc63c and
      0x082859d8 - every site clears a six-byte record (a
      feature-state field like param_1 + 0x6c / + 0x54 ... or a stack
      record) immediately before handing the SAME pointer to
      vtable_file_open (0x0811d724), e.g. FUN_08136520 does
      init(auStack_28); ...; vtable_file_open(auStack_28, path, mode),
      and FUN_081b0240 / FUN_0811c7fc / FUN_0815e934 / FUN_08285988
      init a record field inside a freshly built object. No b (tail)
      sites. The leaf initializer of the six-byte file-open record:
      mov r1,#0x0; str r1,[r0,#0x0]; strb r1,[r0,#0x4]; strb
      r1,[r0,#0x5]; bx lr - record.object = NULL, record.flags = 0,
      record.write = 0. Deviations: the return type is *mut u8, not
      the reference C's void - the body never writes r0, so the
      argument falls through, and callers rely on it (iVar2 =
      FUN_0811d8ac(puVar1 + 2) at 0x081b0268, iVar3 =
      FUN_0811d8ac(iVar3 + 0x10) at 0x082859d8, ...);
      decomp/c/011/0811d8ac_FUN_0811d8ac.c is otherwise exact. The
      first store is a 32-bit str modeled as a u32 write, byte-exact
      on a 64-bit host (the inverse of vtable_file_open's
      host-representation note). The adjacent sibling FUN_0811d8c0 @
      0x0811d8c0 (20 bytes) is a SEPARATE routine, not one body split
      by Ghidra: this function ends in a real bx lr at 0x0811d8bc; the
      sibling is the dispose-and-return-handle wrapper over
      vtable_slot_04_dispose (0x0811d7cc), ported in the same module
      as vtable_file_record_dispose.
      Host tests:
      src/app/vtable_set.rs::file_record_init_zeroes_the_record_and_returns_the_pointer
      / file_record_init_clears_a_field_inside_a_larger_object.

  - address: 0x0811d8c0
    ghidra: FUN_0811d8c0
    name: vtable_file_record_dispose
    size: 20
    sig: "*mut *mut u8 vtable_file_record_dispose(handle: *mut *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r0: handle
    notes: >-
      8 bl call sites (grep on decomp/osos.asm): 0x0810a96c,
      0x0810ab08, 0x0811c9cc, 0x08136558, 0x08136848, 0x0815eb58,
      0x081bc658 and 0x081bc834 - the teardown counterpart of
      vtable_file_record_init: 0x08136558 / 0x08136848 sit in
      FUN_08136520, which inits the SAME auStack_28 record at
      0x0813653c and opens it through vtable_file_open; 0x081bc658 /
      0x081bc834 do the same in FUN_081bc620 (init at 0x081bc63c);
      0x0810a96c / 0x0810ab08 (FUN_081010d0) and 0x081bc658 run the
      raw vtable_slot_04_dispose on the record FIRST and then this
      wrapper, so real callers exercise the NULL-handle path; the
      remaining sites pass interior record pointers (FUN_0811c98c:
      iVar2 - 0x20, FUN_0815eb20: param_1 + 0x21) and consume the
      RETURNED pointer for further container-of arithmetic
      (FUN_0839ca74(iVar2 - 0x3c), FUN_081d0f58(iVar3 - 0x58)). No b
      (tail) sites. A SEPARATE routine from its predecessor
      vtable_file_record_init (0x0811d8ac), which ends in a real bx
      lr at 0x0811d8bc. The dispose-and-return-handle wrapper over
      the ported vtable_slot_04_dispose: push {r4, lr}; mov r4, r0;
      bl 0x0811d7cc; mov r0, r4; pop {r4, pc} - the record's first
      word IS the handle word (vtable_file_open stores the object at
      record +0x0), the dispose's always-0 status is discarded and
      the handle/record pointer returns. Deviations: the reference C
      (decomp/c/011/0811d8c0_FUN_0811d8c0.c) is exact (FUN_0811d7cc();
      return param_1;), only the untyped undefined4 gets the concrete
      handle-pointer type; the ported vtable_slot_04_dispose is called
      directly, reproducing the original bl 0x0811d7cc exactly (the
      app/class_6800.rs ported-callees-called-directly precedent), no
      seam. Host tests:
      src/app/vtable_set.rs::file_record_dispose_calls_through_and_returns_the_handle
      / file_record_dispose_null_handle_is_a_noop_and_still_returns_it
      / file_record_dispose_discards_the_dispose_status.

  - address: 0x0811d148
    ghidra: FUN_0811d148
    name: vtable_file_record_construct_kind1
    size: 64
    sig: "*mut u8 vtable_file_record_construct_kind1(record: *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: record
      r5: zero (the NULL immediate reused by every trailing store)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_0812d2fc: registry-construct thunk (bare b 0x0810e64c -> class_registry_construct, VTABLE_FILE_RECORD_KIND1_CTOR slot)
      thunk_FUN_0810e64c: class_registry_construct (the thunk's target, ported in app/class_registry)
      FUN_080edb74: checked-construct guard (VTABLE_FILE_RECORD_KIND1_GUARD slot)
    notes: >-
      2 bl call sites (grep on decomp/osos.asm): 0x0815a808 and
      0x081a0b70 - both allocate the 0x1c-byte record with
      operator_new(0x1c) immediately before the call and consume the
      returned record pointer (str r0,[r4,#0xcc] / stmib r4,{r0,r5}).
      The kind-1 constructor of the tagged file-record family (the
      kind-2 sibling constructor FUN_0811d104 @ 0x0811d104, 68 bytes,
      and the tag-dispatching dispose FUN_0811d188 @ 0x0811d188, 128
      bytes, are unported; NOT the six-byte file-open record of
      vtable_file_record_init / vtable_file_open): tag byte 1 at +0x00
      (strb), NULL word at +0x18, operator_new(0x28) whose block passes
      straight through the 0x0812d2fc thunk's tail branch in r0 as the
      registry constructor's this - the argument the reference C drops
      (it shows FUN_082aadd4(0x28) discarded and thunk_FUN_0810e64c()
      argument-less) - the construct result stored at +0x04 (32-bit
      str), checked by the unported guard 0x080edb74 (cmp r0,#0; on
      NULL a tail call to report_allocation_failure(4, lit@0x080edb88)
      - 8 bl sites in the constructor cluster, its r0 dead here since
      the continuation overwrites r0 with the record pointer), then
      NULL words at +0x08/+0x10, a zero halfword at +0x14 (strh), and
      the record pointer returns (mov r0,r4). +0x0c is NEVER written
      (a gap in the 0x1c layout). Deviations: operator_new called
      directly (vtable_file_open precedent); the construct routes
      through the new VTABLE_FILE_RECORD_KIND1_CTOR seam wired to the
      exact original chain (host-test interception - the real
      class_registry_construct ends in raw firmware-vtable dispatches
      through literal vtables 0x089910ac / 0x08984770 that cannot run
      on a 64-bit host, and swapping CLASS_REGISTRY_OPS from this
      module's tests would race class_registry.rs's own parallel
      tests; the VTABLE_SET_50_KIND4_OPS pattern); the guard sits
      behind the new VTABLE_FILE_RECORD_KIND1_GUARD seam with a no-op
      default (the non-NULL path is a bare bx lr and the NULL path's
      only effect is the unported diagnostic - the class_6800.rs
      unported_report_allocation_failure no-op precedent); every store
      keeps the original's width (strb/str/strh; the registry pointer
      truncates to its low 32 bits on a 64-bit host - the
      vtable_file_record_init byte-exact precedent); no forwarded
      parameter (the entry stmdb spills no argument registers and
      r1..r3 are never read). match.py: same store/alloc/construct/
      guard/zero/return structure; the diff is the crate fp frame, the
      two seam ldr/blx pairs standing in for the direct bl 0x0812d2fc
      / bl 0x080edb74 (the documented seams), and LLVM's reordering of
      the independent trailing zero stores. Host tests:
      src/app/vtable_set.rs::file_record_kind1_initializes_the_record_and_returns_it
      / file_record_kind1_allocates_the_registry_and_feeds_it_to_the_ctor
      / file_record_kind1_default_guard_is_a_noop.

  - address: 0x0811d104
    ghidra: FUN_0811d104
    name: vtable_file_record_construct_kind2
    size: 68
    sig: "*mut u8 vtable_file_record_construct_kind2(record: *mut u8, descriptor: *const u32, extra: u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: record
      param_2/r5: descriptor (the block 0x0815bdbc reads as u32 words)
      param_3/r6: extra (the verbatim word 0x0815bdbc stores at block +0x04)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_0815bdbc: vtable_file_record_construct_kind2_block (ported directly in app/vtable_set)
      FUN_080edb74: checked-construct guard (unported; VTABLE_FILE_RECORD_KIND2_GUARD seam, same no-op default as the kind-1 sibling's guard)
    notes: >-
      21 bl call sites (grep on decomp/osos.asm), ALL inside the
      0x081a0454..0x081a0a80 cluster (0x081a0488, 0x081a04cc,
      0x081a050c, 0x081a054c, 0x081a058c, 0x081a05cc, 0x081a060c,
      0x081a064c, 0x081a068c, 0x081a06cc, 0x081a070c, 0x081a074c,
      0x081a078c, 0x081a07cc, 0x081a080c, 0x081a0940, 0x081a0980,
      0x081a09c0, 0x081a0a00, 0x081a0a40, 0x081a0a80): every site
      allocates the 0x1c-byte record with operator_new(0x1c)
      immediately before the call, loads arg2/arg3 from a per-site
      literal pair (ldr r2,[lit]; ldr r1,[lit]) and consumes the
      returned record pointer (mov r5,r0). The kind-2 constructor of
      the tagged file-record family (the kind-1 twin
      vtable_file_record_construct_kind1 @ 0x0811d148 is ported; the
      tag-dispatching dispose FUN_0811d188 @ 0x0811d188 is unported -
      its kind-2 branch operator_deletes the +0x04 block via
      0x082aad24 and disposes a non-NULL +0x18 through 0x0812d300),
      operating on the SAME 0x1c-byte record layout: tag byte 2 at
      +0x00 (strb), NULL word at +0x18, operator_new(0x14) whose block
      feeds 0x0815bdbc in r0 alongside the descriptor (r1->r5) and
      the extra word (r2->r6), the construct result stored at +0x04
      (32-bit str), checked by the unported guard 0x080edb74 (its r0
      dead here - the continuation overwrites r0 with the record
      pointer), then the record pointer returns (mov r0,r4). UNLIKE
      kind 1 there are NO trailing field stores: +0x08..+0x17 are
      never written. The callee 0x0815bdbc (72 bytes; 1 bl site at
      0x0811d134) is ported directly as
      vtable_file_record_construct_kind2_block: it fills the five-word
      block {descriptor, extra, descriptor + descriptor[1],
      descriptor[2], descriptor + 0xc}, with a descriptor[0] == 3
      check whose mismatch diagnostic (0x081b53e4) is the target
      function's narrow seam. Deviations: operator_new and the block
      constructor are called directly (the
      vtable_file_record_construct_kind1 precedent); the guard remains
      behind VTABLE_FILE_RECORD_KIND2_GUARD with the same
      construct_guard_unported no-op default as the kind-1 sibling
      (the VTABLE_QUERY_4C_READ_DISPATCH /
      VTABLE_QUERY_4C_SCALAR_DISPATCH role-specific-name precedent);
      every store keeps the original's width (strb/str; the block
      pointer truncates to its low 32 bits on a 64-bit host -
      the vtable_file_record_init byte-exact precedent); arg2 typed
      *const u32 (the callee dereferences it as words), arg3 a
      verbatim u32 (only stored, stmia r0,{r1,r2}); no forwarded
      parameter (the entry stmdb spills no argument registers and r3
      is never read). Reference C
      (decomp/c/010/0811d104_FUN_0811d104.c) is accurate - it catches
      the allocation feeding FUN_0815bdbc(alloc, param_2, param_3),
      the +0x04 store, the guard and the return; only the untyped
      undefined4 parameters get concrete types. The directly called
      block constructor's focused tests are recorded in its entry below.

  - address: 0x0815bdbc
    ghidra: FUN_0815bdbc
    name: vtable_file_record_construct_kind2_block
    size: 72
    sig: "*mut u8 vtable_file_record_construct_kind2_block(block: *mut u8, descriptor: *const u32, extra: u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: block (the caller's fresh five-word allocation)
      param_2/r5: descriptor (kind, byte offset, and count words)
      param_3/r2: extra (verbatim word stored at block +0x04)
      r0: descriptor_kind / descriptor_relative_address / count / return block
      DAT_0815be04: diagnostic_message (literal pointer 0x088f8c4c)
      FUN_081b53e4: report_diagnostic (unported; VTABLE_FILE_RECORD_KIND2_BLOCK_DIAGNOSTIC seam)
    notes: >-
      The kind-2 file-record descriptor-block constructor, called only
      from vtable_file_record_construct_kind2 at 0x0811d134 after
      operator_new(0x14). Reference C
      decomp/c/014/0815bdbc_FUN_0815bdbc.c and the 72-byte ARM body
      agree: stmia first stores {descriptor, extra}; descriptor[0] is
      compared with 3; only a mismatch calls FUN_081b53e4(0,
      0x088f8c4c), then both paths fill {descriptor + descriptor[1],
      descriptor[2], descriptor + 0xc} and return block. The diagnostic
      is the sole unported dependency and is isolated behind a
      role-specific seam; focused host tests verify all five word stores
      without a diagnostic and the mismatch diagnostic plus fall-through
      construction:
      src/app/vtable_set.rs::kind2_block_constructs_five_words_without_a_diagnostic
      / kind2_block_reports_bad_descriptor_and_still_constructs.

  - address: 0x08155d6c
    ghidra: FUN_08155d6c
    name: iterator_state_next
    size: 44
    sig: "u32 iterator_state_next(state: *mut u32, out: *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: state
      param_2/r5: out
      FUN_08155bac: iterator_state_refresh (ITERATOR_STATE_REFRESH
        seam) - rederives next +0x0c / prev +0x04 from the position
        word and the owner's count
      FUN_08155e30: iterator_state_fetch (ITERATOR_STATE_FETCH seam) -
        the shared guarded fetch (valid-iterator check 0x0829baa8,
        0 <= pos < count guard, vtable+0x3c item_at(owner, pos, out))
    notes: >-
      The advance half of the collection iterator's next, same state
      class as iterator_state_construct @ 0x08155e80 /
      iterator_state_cleanup @ 0x08155ec0. 44 bytes exactly,
      0x08155d6c..0x08155d98, eleven instructions, no literal pool;
      the previous-index sibling opens immediately after. 55 bl call
      sites binary-scanned (54 unconditional + 1 conditional), 0 b.
      Algorithm: adopt the buffered next index as the position (ldr
      [r0,#0xc]; str [r4,#8]), refresh the prev/next bookkeeping
      (0x08155bac), then TAIL into the shared guarded fetch
      0x08155e30 (mov r1, out; mov r0, state; b). GHIDRA ILLUSION:
      the decompile inlines the fetch body, making 0x08155d6c look
      like it owns the valid-index guard and the vtable+0x3c dispatch;
      the raw ARM shows both live in 0x08155e30, a separately called
      function (bl @ 0x08213728, plus the seek's b @ 0x08155dc0).
      This port is exactly the 44-byte advance; both callees ride new
      ITERATOR_STATE_REFRESH / ITERATOR_STATE_FETCH seams (no-op /
      0-returning defaults - the family precedent; an unswapped table
      advances the position and reports an empty traversal). The tail
      branch is a plain call in the source (Rust has no guaranteed
      tail calls); release LLVM happens to emit a real pop+bx r2 tail
      call, matching the original's shape. class_6800's local seam
      knows this function as collection_iterator_next;
      vtable_file_record_teardown's inner step (@ 0x081dddf8) is one
      of the 55 sites. Host tests: next-at-+0x0c adopted as the
      position at +0x08, refresh strictly before fetch with state
      routed to both, out forwarded verbatim, the fetch's verdict
      returned for both 1 and 0, and a faithful scripted fetch pinning
      the seam-boundary contract end to end (index guard boundaries
      -2/-1/0/2/3 against count 3, vtable slot +0x3c read, item_at
      args) over a sub-4-GiB mapped fixture (new testing.rs hint
      VTABLE_SET_ITERATOR). match.py structural: identical
      load/store/refresh/tail-fetch sequence - LLVM even emitted the
      pop+bx tail call; the diff is the two seam blx indirections and
      the crate fp frame.

  - address: 0x081ddde8
    ghidra: FUN_081ddde8
    name: file_record_iterator_next
    size: 48
    sig: "u32 file_record_iterator_next(iter: *mut u32, key_out: *mut u32, node_out: *mut u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r0: iter (six-word file-record iterator)
      param_2/r4: key_out
      param_3/r5: node_out
      auStack_8/sp: pair (private yielded key/node words)
      FUN_08155d6c: iterator_state_next (direct, ported in app/vtable_set)
    notes: >-
      File-record iterator step. Raw ARM is exactly 48 bytes,
      0x081ddde8..0x081dde18: a twelve-instruction body with no literal
      pool; the next sibling starts immediately at 0x081dde18. Decoding
      every ARM B/BL word in osos.dec finds 10 bl call sites, all
      unconditional; 0 predicated bl and 0 b sites. Algorithm: call
      iterator_state_next(iter + 4, pair), then only if its status is
      nonzero copy pair[0] to key_out and pair[1] to node_out, returning
      that status unchanged. Ghidra incorrectly invents a fourth
      parameter and treats r2 as the first local; raw ARM establishes
      three arguments and an 8-byte stack pair. Deliberate deviation:
      Rust zero-initializes the private pair where ARM leaves it
      uninitialized; zero status does not read either pair word, so this
      is unobservable. Host tests prove a high-bit nonzero status copies
      both words, while zero leaves both output sentinels untouched.
      match.py structural review: same state-step, zero guard, and
      conditional two-word copy; LLVM differs only in frame setup,
      zeroing the unobservable pair, and coalescing its successful pair
      load into ldmne.

  - address: 0x08155e80
    ghidra: FUN_08155e80
    name: iterator_state_construct
    size: 64
    sig: "*mut u32 iterator_state_construct(state: *mut u32, owner: *mut u8, start: i32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: state
      param_2/r0-then-r1: owner
      param_3/r5: start
      FUN_08271710: iterator_state_link (ITERATOR_STATE_LINK seam) -
        pushes state onto owner+0x0c's observer list via the +0x10 next
        link (the exact inverse of the 0x08271724 unlink)
      FUN_08155dc4: iterator_state_seek (ITERATOR_STATE_SEEK seam) -
        normalizes start (-2 before-first / -3 end / clamp) into the
        +0x08 position word and derives prev +0x04 / next +0x0c
    notes: >-
      Constructs the 20-byte collection-iterator state object (the
      class iterator_state_cleanup @ 0x08155ec0 tears down and
      iterator_state_release @ 0x08271724 unlinks) and positions it.
      64 bytes exactly, 0x08155e80..0x08155ec0, 16 instructions, no
      literal pool; 54 bl call sites, 0 b, binary-scanned by decoding
      every B/BL word in osos.dec. Store/call order verified against
      osos.asm: owner to +0x00, ZERO the +0x10 link word, link(owner,
      state), position -2 to +0x08 (mvn r0, #1), seek(state, start),
      return state (mov r0, r4). The link word's zero lands before the
      link call so the callee observes it cleared; the -2 store sits
      between link and seek. Both callees unported - they ride new
      ITERATOR_STATE_LINK / ITERATOR_STATE_SEEK seams with no-op
      defaults (the ITERATOR_STATE_RELEASE precedent), so an unswapped
      table leaves the state object field-initialized but unlinked and
      unpositioned, matching every sibling seam's opaque-collection
      contract. class_6800's FRAMEWORK_BASE_STATE_OPS knows this
      function as collection_iterator_begin (call shape
      (iterator, handle, -2) on a 20-byte frame local, then 0x08155d6c
      steps and the 0x08155ec0 drop); that local seam keeps its
      modeling default, as it did after iterator_state_cleanup landed.
      Host tests: field stores at +0x00/+0x08/+0x10, link-before-seek
      invocation order with (owner, state) / (state, start) argument
      routing, the link word observed cleared inside the link callee,
      start forwarded verbatim for -3/0/7, and the state returned.
      match.py structural: identical store/store/link/store/seek/
      return sequence; the diff is the two seam blx indirections and
      the crate fp frame.

  - address: 0x08155ec0
    ghidra: FUN_08155ec0
    name: iterator_state_cleanup
    size: 48
    sig: "*mut u32 iterator_state_cleanup(state: *mut u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: state
      FUN_08271724: iterator_state_release (ported direct observer-list unlink)
      param_1[0]: owner (release arg1)
      param_1[2]: release_state (state word at +0x08)
    notes: >-
      The 48-byte body at 0x08155ec0 was verified against the assembly:
      `ldr r0,[r0,#8]; cmn r0,#5; beq` makes -5 the no-release sentinel;
      otherwise `ldr r0,[r4]; mov r1,r4; bl 0x08271724` invokes the
      ported observer-list unlink as release(*state, state), then `mvn
      r0,#0; str r0,[r4,#8]` poisons the state word with -1 and `mov
      r0,r4` returns the original state pointer. Reference C
      decomp/c/013/08155ec0_FUN_08155ec0.c agrees. The existing
      file-record teardown seam defaults to this direct port, and the two
      container-teardown defaults call it directly. Focused host evidence:
      iterator_state_cleanup_sentinel_skips_release_and_returns_the_state /
      iterator_state_cleanup_unlinks_its_state_then_poisons_it_and_returns_it /
      iterator_state_cleanup_is_the_wired_teardown_and_container_default.

  # --- H.264 / MPEG-4 AVC video decoder: emulation-prevention probe ---
  - address: 0x082c319c
    ghidra: FUN_082c319c
    name: cg_rbsp_emulation_probe
    size: 64
    sig: "u32 cg_rbsp_emulation_probe(const u8 *p, const u8 *end)"
    module: codegen/rbsp_read_bits
    vars: {param_1: p, param_2: end}
    status: ported
    notes: >-
      The H.264 emulation-prevention probe: returns 1 iff the byte at
      `p` is the 0x03 of a `00 00 03 xx` escape sequence the RBSP
      reader must skip, 0 otherwise. 64 bytes, 16 instructions, all
      code — no literal pool, no stack traffic, a leaf ending `bx lr`;
      5 `bl` call sites, all in the RBSP read primitives: two in
      h264_bitstream_count_leading_zeros @ 0x0809b040, three in
      cg_rbsp_read_bits @ 0x082d0630. It lives in the Vincent JIT's
      0x082cxxxx block, so it is ported under the codegen/ roof with
      the cg_ prefix, beside its caller.
      Algorithm: if p + 1 >= end (UNSIGNED pointer comparison —
      `add r2,r0,#1; cmp r2,r1; bcs` — the byte after p must exist,
      so a 00 00 03 whose 0x03 is the last readable byte is data, not
      an escape) return 0; else return p[-2] == 0 && p[-1] == 0 &&
      p[0] == 3 && p[1] <= 3 (the trailing gate is `cmp r0,#3; movls`
      — unsigned <= 3, exactly the values H.264 permits after an
      emulation-prevention byte). The lookback reads p[-2]
      unconditionally once the bounds test passes (`ldrb r1,[r0,#-2]`),
      p[-1] under predicate; in the firmware that is always sound
      because the NAL payload follows the 00 00 01 start code in flat
      DRAM, so callers must guarantee two readable bytes before
      `data`.
      Deviations: the && chain short-circuits the p[-1]/p[0]/p[1]
      loads where the original predicates them; only p[-2] is
      unconditional in both. Verified against osos.asm and the Ghidra
      C (decomp/c/030/082c319c_FUN_082c319c.c): exact boundary
      conditions (p + 1 < end unsigned, follower <= 3 unsigned) match.
      FOLLOW-UPS (deliberately NOT done in this commit, one function
      per commit): the CG_RBSP_READ_BITS_OPS.emulation_probe /
      CG_EXP_GOLOMB_OPS.read_bits seam stubs and the private
      emulation_prevention_probe helper in src/h264/bitstream.rs
      (binary-verified transcription of this same body) can now be
      retired/rewired to direct calls of this export.
      Rust port and host tests: src/codegen/rbsp_read_bits.rs —
      00 00 03 firing at payload offsets 0/1/7 and at end - 2 with
      every legal trailer 0..=3, 0x03 without two preceding zeros
      (incl. a lone 0x03 at the payload start), the unconditional
      two-byte lookback across the data boundary (start-code layout:
      fires with 00 00 before `data`, fails with 01 00), the
      p + 1 >= end bound (0x03 as the last byte, p == end, p past
      end with a matching sequence in reach), non-0x03 current bytes,
      trailers above 3 (incl. 0x80/0xff proving the gate unsigned),
      and a seeded xorshift sweep with spliced 00 00 03 0x sequences
      checked per-index against the independent index-space
      predicate.
      match.py structural: same +1/cmp/bcs bounds test, same [-2]/
      [-1]/[0] lookback loads, same cmp #3 / follower-gate verdict
      shape; the diff is the crate fp frame and LLVM branching where
      ADS predicated (ldrbEQ/cmpeq/movls), with the follower compare
      as `cmp #4; movcc` instead of `cmp #3; movls`.

  # --- H.264 / MPEG-4 AVC video decoder: Exp-Golomb ue(v) reader ---
  - address: 0x082c5df0
    ghidra: FUN_082c5df0
    name: cg_exp_golomb_ue_read
    size: 88
    sig: "u32 cg_exp_golomb_ue_read(RbspBitReader *reader, i32 *bits_out, u8 *emulation_byte_pending)"
    module: codegen/exp_golomb
    vars: {param_1: reader, param_2: bits_out, param_3: emulation_byte_pending}
    status: ported
    notes: >-
      The H.264 ue(v) unsigned Exp-Golomb reader over the shared RBSP
      cursor {data @ +0x00, bit_pos @ +0x04, end @ +0x08} (see
      h264/bitstream.rs). 88 bytes, 22 instructions, no literal pool;
      50 `bl` call sites counted in osos.asm — every syntax parser at
      0x0836xxxx (dec_ref_pic_marking @ 0x08365640 and siblings) plus
      the se(v) wrapper FUN_082c5dcc @ 0x082c5dcc. It lives inside the
      Vincent JIT's 0x082cxxxx address block, so it is ported under
      the codegen/ roof with the cg_ prefix, but it is pure decoder
      machinery.
      Algorithm: n = count_leading_zeros @ 0x0809b040 (cursor unmoved);
      h264_bitstream_advance(reader, n, flag) commits exactly the zero
      prefix; *bits_out = n + 1; suffix = read_bits @ 0x082d0630
      (n + 1 bits, cursor unmoved); return
      (1 << n) + (suffix & ~(1 << n)) - 1 — the textbook
      2^n - 1 + suffix with the mandatory leading 1 masked off rather
      than skipped. The caller advances the stored n + 1 afterwards;
      that is the FUN_082b3258 trailing every call site. The
      emulation-prevention flag passes verbatim through all three
      primitives; the port consumes nothing. Observable ordering: the
      prefix advance precedes the *bits_out store, which precedes the
      suffix read. The 1 << n shift is the ARM register form
      (mov r2, r2, lsl r4): amount is n & 0xff, amounts 32..=255 yield
      0, so a degenerate 32-zero prefix returns 0xffffffff (read_bits
      refuses counts > 32 and yields 0). All additions wrap like the
      original's add/sub.
      Deviations: count_leading_zeros @ 0x0809b040 and read_bits
      @ 0x082d0630 are unported and ride the CG_EXP_GOLOMB_OPS
      read_volatile seam (missing_* spin-loop defaults, the
      codegen/timer_wait.rs convention); h264_bitstream_advance
      @ 0x082b3258 is ported and takes a direct call, retiring its
      seam. Rust port and host tests: src/codegen/exp_golomb.rs —
      controlled-mock sweeps of the fold over prefix lengths
      0..255 (incl. the ARM-shift degenerate cases) and suffix bit
      patterns, store-ordering and flag-passthrough probes, and
      end-to-end decodes over faithful transcriptions of the three
      unported primitives: byte-aligned and misaligned known code
      words, and streams with emulation-prevention bytes inserted per
      the H.264 rule, checked against a naive RBSP-space reference.
      match.py: same push / lz-call / advance / store / read / combine
      structure; the diff is the crate fp frame, blx seam dispatch in
      place of the two direct bls, and LLVM folding
      top + (suffix & ~top) into an orr with a cmp/#31 guard for the
      32..=255 shift case (value-identical: a single-bit addend turns
      add into or).

  # --- H.264 / MPEG-4 AVC video decoder: Exp-Golomb se(v) wrapper ---
  - address: 0x082c5dcc
    ghidra: FUN_082c5dcc
    name: cg_exp_golomb_se_read
    size: 36
    sig: "i32 cg_exp_golomb_se_read(RbspBitReader *reader, i32 *bits_out, u8 *emulation_byte_pending)"
    module: codegen/exp_golomb
    vars: {param_1: reader, param_2: bits_out, param_3: emulation_byte_pending}
    status: ported
    notes: >-
      The H.264 se(v) signed Exp-Golomb wrapper over
      cg_exp_golomb_ue_read @ 0x082c5df0 (see its entry for the shared
      RbspBitReader cursor layout and the CG_EXP_GOLOMB_OPS seam
      conventions). 36 bytes, 9 instructions, no literal pool. Ghidra
      shows FUN_082c5dcc(void) — wrong: the body is push {lr};
      bl 0x082c5df0; cmp r0,#0; popeq {pc}; tst r0,#1;
      mov r0, r0, asr #1; rsbeq r0, r0, #0; addne r0, r0, #1;
      pop {pc}, so r0/r1/r2 flow into the callee untouched and the
      wrapper forwards reader/bits_out/emulation_byte_pending
      verbatim, verified against the raw words in osos.dec.
      Algorithm: code_num = ue_read(...); if code_num == 0 return 0;
      half = code_num asr 1; return -half when code_num is even,
      half + 1 when odd — the standard se(v) mapping 0→0, 1→1, 2→−1,
      3→2, 4→−2, … Every bitstream side effect (cursor commit, the
      stored n + 1, flag passthrough) is the callee's, including on
      the code_num == 0 early return.
      Deviations: none. The ue callee is ported, so it takes a direct
      call and no new seam is introduced; plain (non-wrapping)
      negation/addition since no overflow is reachable (extreme folds:
      0xfffffffe → 1, 0xffffffff → 0). Rust port and host tests:
      src/codegen/exp_golomb.rs — mock-driven sweep of code_nums
      0..=64 against the textbook mapping plus the four degenerate
      sign-bit folds scripted at the seam, argument-forwarding and
      bits_out/cursor parity probes with the ue path, and an
      end-to-end decode of known se words over the faithful primitive
      transcriptions with emulation-prevention bytes inserted, checked
      against a naive RBSP-space reference. match.py: same
      push/bl/cmp/tst/asr/rsbeq/addne structure; the diff is the crate
      fp frame and LLVM turning the popeq early return into a branch
      to a shared mov r0,#0 / pop epilogue.

  # --- H.264 / MPEG-4 AVC video decoder: u(n) RBSP bit reader ---
  - address: 0x082d0630
    ghidra: FUN_082d0630
    name: cg_rbsp_read_bits
    size: 292
    sig: "u32 cg_rbsp_read_bits(RbspBitReader *reader, i32 count, u8 *emulation_byte_pending)"
    module: codegen/rbsp_read_bits
    vars: {param_1: reader, param_2: count, param_3: emulation_byte_pending}
    status: ported
    notes: >-
      The H.264 u(n) fixed-width bit reader over the shared RBSP
      cursor {data @ +0x00, bit_pos @ +0x04, end @ +0x08} (see
      h264/bitstream.rs and the cg_exp_golomb_ue_read entry for the
      flag protocol). 292 bytes, 73 instructions, no literal pool;
      55 `bl` call sites counted in osos.asm — the syntax parsers at
      0x0836xxxx and the Exp-Golomb reader @ 0x082c5df0. It lives
      inside the Vincent JIT's 0x082dxxxx address block, so it is
      ported under the codegen/ roof with the cg_ prefix, but it is
      pure decoder machinery.
      Algorithm: *flag = 0 FIRST (even a refused count clears it);
      counts outside 1..=32 return 0. p = data + (bit_pos >> 3)
      (arithmetic shift); first = 8 - (bit_pos & 7). A byte-ALIGNED
      entry probes p with the emulation-prevention probe @ 0x082c319c
      and skips the 00 00 03 byte (setting the flag) before reading;
      a mid-byte entry never probes the first byte. count <= first is
      the single-byte path: the original issues a 32-bit unaligned
      `ldr` at p and reduces it with lsl (8-first) / and #0xff /
      lsr (8-count) — only *p survives the mask. Longer reads take
      the low `first` bits, then a whole-byte loop (probe the byte
      AFTER each consumed byte; defer the verdict in a pending-skip
      register that makes the NEXT fetch step over the EP byte;
      report every probe hit through the flag — including a hit on
      the byte after the last consumed byte, whose pending skip is
      never consumed), then a 1..7-bit tail that consumes the pending
      skip before taking the top bits of the last byte.
      Contract pinned from the asm: the cursor is NEVER advanced (the
      caller commits n bits plus an 8-bit surcharge per flagged EP
      byte via h264_bitstream_advance @ 0x082b3258), so bit_pos
      indexes the RAW (EBSP) stream — EP bytes occupy bit positions,
      and a mid-byte start inside one reads its bits as data. Byte
      fetches are not bounds-checked against `end`; only the probe
      is (p + 1 < end), so a trailing 00 00 03 with the 0x03 as the
      last byte is not an EP sequence and is read as data.
      Deviations: the single-byte path reads one byte instead of the
      original's unaligned word load (the extra bytes are masked off
      in the original — value-identical, strictly narrower). The
      probe @ 0x082c319c is unported and rides the new
      CG_RBSP_READ_BITS_OPS read_volatile seam (missing_* spin-loop
      default, the codegen/timer_wait.rs convention); host tests
      install a faithful transcription. NOTE: with read_bits now
      ported, the read_bits slot of CG_EXP_GOLOMB_OPS in
      codegen/exp_golomb.rs can be retired in favour of a direct call
      (per the ported-callee convention) — deliberately NOT rewired
      here (one function per commit). Rust port and host tests:
      src/codegen/rbsp_read_bits.rs — width sweep 1..=32 x bit_pos
      alignments 0..7 against a naive MSB-first reference, refused
      counts (0, 33, negatives, i32 extremes) with flag-clearing,
      single-byte isolation, aligned/mid-read/tail EP skips against
      an RBSP-space reference over prevention-inserted streams, exact
      flag-fire pinning (incl. the trailing-edge probe hit), the
      EP-at-end and follower >= 4 non-sequences, unchecked over-end
      reads, and the cursor-unmoved contract on every call.
      match.py: same flag-store / count-gate / aligned-probe /
      single-byte / whole-byte-loop / tail structure; the diff is the
      crate fp frame, blx seam dispatch in place of the direct bls,
      LLVM folding the two bound checks into one sub/cmn/bcc, an ldm
      for the cursor words, and reassociated shift/mask sequences
      (value-identical).

  - address: 0x082d81e4
    ghidra: FUN_082d81e4
    name: word_list_init
    size: 28
    sig: "WordList *word_list_init(WordList *this)"
    module: util/word_list
    status: ported
    renames:
      param_1: this
    notes: >-
      Constructor for the inline-storage WordList, the small counted
      u32 list consumed by word_list_copy @ 0x082d27a8 (same module).
      Body: count = 0 (strh [r0]), capacity = 6 (strh [r0,#2]),
      entries = this + 8 (str [r0,#4]), bx lr with r0 never clobbered
      so `this` is returned. 7 words; extent self-verified: the next
      function's prologue (push {r4-r6, lr}) starts exactly at
      0x082d8200, so Ghidra's 28 bytes is right. 29 bl call sites, ALL
      unconditional (0 predicated), verified by decoding every ARM
      B/BL word in osos.dec, not from Ghidra xrefs. No data word in
      osos.dec holds 0x082d81e4, so the constructor is not dispatched
      virtually. Callers build the list on their own stack frames
      (e.g. FUN_082cb18c @ 0x082cb18c constructs four), matching the
      8-byte header + 6 inline words = 32-byte stack slots. The port
      returns this to match the original's preserved r0. Host tests
      pin count/capacity/entries == this+8, the this return, and that
      nothing past the header is scribbled (inline words stay dirty).
      match.py: LLVM merges the two strh stores into one stm of
      0x00060000 plus entries (value-identical); the diff is the
      crate fp frame and the stm merge.

  - address: 0x082d27a8
    ghidra: FUN_082d27a8
    name: word_list_copy
    size: 44
    sig: "void word_list_copy(const WordList *src, WordList *dst)"
    module: util/word_list
    status: ported
    renames:
      param_1: src
      param_2: dst
    notes: >-
      Copies the CONTENTS of a small counted u32 list: ldrh src->count,
      memcpy(dst->entries, src->entries, count*4) via the ROM memcpy
      veneer @ 0x08037df8, then a SECOND ldrh of src->count stored to
      dst->count. The container is {u16 count @ +0x00, u16 capacity @
      +0x02, u32 *entries @ +0x04} with inline storage at +0x08, laid
      out by constructor FUN_082d81e4 @ 0x082d81e4 (count 0, capacity 6,
      entries = self+8); dst keeps its own capacity and buffer pointer.
      Callers snapshot/restore operand lists (e.g. FUN_082cb18c @
      0x082cb18c). Companion FUN_082d27d4 @ 0x082d27d4 trims trailing
      zero entries. Extent self-verified: next function starts exactly
      at 0x082d27d4, so Ghidra's 44 bytes is right. 41 bl call sites,
      ALL unconditional (0 predicated), verified by decoding every ARM
      B/BL word in osos.dec. The post-copy count reload is reproduced:
      if dst->entries overlaps src's header the clobbered count is what
      gets stored (host-tested). Port calls memcpy_forward_words (the
      veneer's target body) directly instead of the veneer. match.py:
      same load/lsl #2/bl memcpy/reload-count/store structure; the diff
      is the crate fp frame and register allocation.
  - address: 0x082cf7bc
    ghidra: FUN_082cf7bc
    name: word_list_modular_square_assign
    size: 72
    sig: "void word_list_modular_square_assign(WordList *accumulator, const ModularReductionContext *modulus)"
    module: util/word_list_modular_square
    status: ported
    renames:
      param_1: accumulator
      param_2: modulus
    notes: >-
      Modular squaring assignment for the small counted-u32 WordList type:
      construct an empty capacity-10 temporary list on the stack, square
      accumulator into it through FUN_082d24a4, reduce it through
      FUN_082cdb04 with r2 = 0, then word_list_copy the reduced contents
      back to accumulator. Raw extent verified: the next distinct function
      starts exactly at 0x082cf804 with push {r4,r5,r6,lr}, so Ghidra's
      18-instruction / 72-byte extent is correct. FUN_082d24a4 is a
      schoolbook squaring core: it emits each limb square then doubled
      cross-products with carry propagation, and trims a high zero word.
      All 12 decoded B/BL references are plain unconditional BLs
      (0 predicated); no osos.dec data word holds 0x082cf7bc, so this is
      not a virtual dispatch target. FUN_082d24a4 and FUN_082cdb04 have
      no ported names.yaml entries, so the Rust port uses a volatile
      operation-table seam that transmutes their exact retail addresses
      on target and test-installed reference models on host. The
      capacity-10 constructor FUN_082d81c8 is reproduced inline rather
      than introduced as another exported port. Host tests prove square ->
      mode-zero reduce ordering, the empty capacity-10 temporary contract,
      retention of high square bits until reduction, two-word input
      handling, zero-count output, and preserving the destination buffer
      and capacity.
      match.py: same square/reduce/copy call order and capacity-ten temporary;
      the diff is the crate fp frame, volatile operation-table load with blx
      seams for the two unported cores, merged count/capacity initialization,
      and a direct word_list_copy call whose archive relocation disassembles
      as address zero.
  - address: 0x082cf804
    ghidra: FUN_082cf804
    name: word_list_modular_subtract_assign
    size: 76
    sig: "void word_list_modular_subtract_assign(const WordList *subtrahend, WordList *minuend, const ModularReductionContext *modulus)"
    module: util/word_list_modular_subtract
    status: ported
    renames:
      param_1: subtrahend
      param_2: minuend
      param_3: modulus
    notes: >-
      Modular subtraction assignment: construct an empty capacity-six inline
      WordList temporary, copy subtrahend into it, negate the temporary
      through FUN_082ceb0c, then add it into minuend through the already
      ported word_list_modular_add_assign. The result is therefore
      minuend - subtrahend modulo modulus and remains in minuend's existing
      buffer. Raw extent verified: the next separately linked sibling starts
      at 0x082cf850 with mov r0,r1; b 0x082d3ca4, so the 19-instruction /
      76-byte Ghidra extent is correct. Decoding every ARM B/BL immediate in
      osos.dec finds 9 direct call sites, all unconditional bl (0 predicated:
      0x0807c484, 0x0807c4d8, 0x080ebad4, 0x080ebb28, 0x080eef0c,
      0x080ef034, 0x083692ac, 0x083692f4, 0x08369404). No data word in
      osos.dec holds 0x082cf804, so it is not dispatched virtually.
      FUN_082ceb0c has no ported names.yaml entry, so the port reaches its
      exact retail address through a volatile operation-table seam on target
      and uses a host reference model in tests. The target invokes the
      ported word_list_init directly; only on 64-bit hosts the temporary's
      entry pointer is repaired after construction because WordList's native
      pointer field makes its host header larger than the target's 8 bytes.
      Tests prove copied-subtrahend negation, capacity-six temporary setup,
      negate -> add -> mode-zero-reduce order, modular borrow wrap, zero
      result count, source preservation, and minuend buffer/capacity
      retention.
  - address: 0x082ceabc
    ghidra: FUN_082ceabc
    name: word_list_modular_multiply_assign
    size: 80
    sig: "void word_list_modular_multiply_assign(const WordList *multiplicand, WordList *accumulator, const ModularReductionContext *modulus)"
    module: util/word_list_modular_multiply
    status: ported
    renames:
      param_1: multiplicand
      param_2: accumulator
      param_3: modulus
    notes: >-
      Modular multiplication assignment for the small counted-u32 WordList
      type: construct an empty capacity-10 temporary list on the stack,
      multiply multiplicand x accumulator into it through FUN_082d9c34,
      reduce it through FUN_082cdb04 with r2 = 0, then word_list_copy the
      reduced contents back to accumulator. Raw extent verified: the
      next distinct function begins at 0x082ceb0c with push {r4-r6,lr}, so
      the 20 instructions / 80-byte Ghidra extent is correct. The first
      call is the distinct 20-byte constructor FUN_082d81c8 (not the
      ported capacity-6 FUN_082d81e4): it stores count 0, capacity 10,
      and entries = this+8. All 27 decoded B/BL references are plain
      unconditional BLs (0 predicated). FUN_082d9c34 and FUN_082cdb04
      have no ported names.yaml entries, so the Rust port uses a volatile
      operation-table seam that transmutes their exact retail addresses
      on target and test-installed reference models on host. The capacity-10
      constructor is inlined rather than introduced as another exported
      port. Host tests prove multiply -> mode-zero reduce ordering, the
      capacity-10 empty temporary contract, preservation of a high product
      limb until reduction, and that word_list_copy preserves accumulator
      buffer/capacity while assigning only the reduced count/limbs.
  - address: 0x082cdac8
    ghidra: FUN_082cdac8
    name: word_list_modular_add_assign
    size: 36
    sig: "void word_list_modular_add_assign(const WordList *addend, WordList *accumulator, const ModularReductionContext *modulus)"
    module: util/word_list_modular_add
    status: ported
    renames:
      param_1: addend
      param_2: accumulator
      param_3: modulus
    notes: >-
      Modular addition assignment for the small counted-u32 WordList type:
      add addend into accumulator through FUN_082daec8, then tail-call
      FUN_082cdb04 with r2 = 0 to reduce accumulator in place. Raw extent
      verified: the next distinct function starts exactly at 0x082cdaec
      with cmp r0, #0, so Ghidra's 9-instruction / 36-byte extent is
      correct. All 11 decoded B/BL references are plain unconditional BLs
      (0 predicated); the tail branch to FUN_082cdb04 is internal control
      flow, not an incoming call. FUN_082daec8 and FUN_082cdb04 have no
      ported names.yaml entries, so the Rust port uses a volatile
      operation-table seam that transmutes their exact retail addresses on
      target and test-installed reference models on host. Host tests prove
      addition -> mode-zero reduction ordering, carry into a high limb
      before reduction, zero-count output, and retention of accumulator
      buffer/capacity.
      match.py: same add/reduce call order and mode-zero tail dispatch; the
      diff is the crate frame, separate volatile operation-table slot loads,
      and indirect blx/bx seams for the two unported cores.
  - address: 0x080f4aa8
    ghidra: FUN_080f4aa8
    name: path_exists
    size: 48
    sig: "u32 path_exists(const char *path, u32 flags)"
    module: app/path_exists
    status: ported
    renames:
      param_1/r0: path (NUL-terminated path C string)
      param_2/r1: flags (every call site passes 0)
      param_3/param_4 (r2/r3): DEAD - their spill slots ARE the 8-byte
        path-object guard on sp, overwritten by the constructor before
        any read; Ghidra's uStack_10/uStack_c in
        decomp/c/008/080f4aa8_FUN_080f4aa8.c
      FUN_08279284: "PATH_OBJECT_CTOR seam (path-object ctor veneer:
        string_object_construct_from_cstr @ 0x08277304 + derived
        path-class vtable 0x089a60d8 store, literal @ 0x08279298
        binary-verified)"
      FUN_080f4ad8: "PATH_EXISTS_WORKER seam (the exists query:
        mutex-guarded 0x08206e40/0x08206e6c filesystem facade from
        0x0818a0bc, vtable slot +0x50 with the path object)"
      thunk_FUN_082792fc: "string_object_destroy_veneer (ported, called
        directly)"
      r4: saved flags, then saved query status
    notes: >-
      66 bl call sites (grep on decomp/osos.asm); every site passes a
      path C string in r0 and 0 in r1 and branches on the returned
      status being nonzero (FUN_08164bf4 gates reading
      "iPod_Control/Device/1da_..." on it; FUN_08264c94 enumerates
      consecutively-indexed paths with `while (path_exists(...) != 0)`;
      FUN_0811b254 probes "iPod_Control/Device/radio_..."), hence the
      exists name - the exact facade slot-+0x50 operation beyond that
      is not established. Scoped-guard wrapper:
      path_object_construct(&guard, path), then
      worker(ctor_return, flags), then
      string_object_destroy_veneer(&guard), returning the worker's
      status verbatim. Faithful: the worker's first argument is the
      CONSTRUCTOR'S RETURN (r0 flows straight through), while the
      destructor is handed sp (the guard STORAGE) unconditionally -
      pinned by tests with a constructor mock returning a distinct
      sentinel. The guard object is the two-word StringObject; the
      r2/r3 spill slots are its whole storage. Deviations: the
      unported ctor veneer 0x08279284 rides the PATH_OBJECT_CTOR seam
      with a FAITHFUL default (both pieces known: the ported base ctor
      plus the derived vtable identity store); the unported worker
      0x080f4ad8 rides the PATH_EXISTS_WORKER seam with a FAIL-CLOSED
      default returning 0 ("does not exist") - the symbol is NOT
      hook-ready until the filesystem-facade worker is ported. Host
      tests (recording mocks on both seams plus the
      STRING_OBJECT_OPS.release_payload slot to observe the ported
      destructor):
      src/app/path_exists.rs::tests::call_order_is_construct_query_destroy
      / ctor_receives_the_storage_and_the_path_verbatim /
      worker_receives_the_ctor_return_not_the_storage /
      dtor_receives_the_storage_even_when_the_ctor_returns_elsewhere /
      flags_flow_to_the_worker_verbatim /
      the_query_status_survives_the_destructor_and_returns_verbatim /
      default_ctor_builds_the_derived_path_object /
      default_worker_fails_closed_as_does_not_exist. match.py (exit 1,
      diff expected): same spill/save-flags/ctor/worker/dtor/return
      structure; the diff is the crate fp frame and the two seams' blx
      dispatch in place of the direct bls.

  - address: 0x080f4ad8
    ghidra: FUN_080f4ad8
    name: path_probe_via_facade
    size: 68
    sig: "u32 path_probe_via_facade(StringObject *path_object, u32 flags)"
    module: app/path_probe
    status: ported
    renames:
      param_1/r0: path_object (a 0x08279284-family path object; the
        path_exists wrapper passes the constructor's RETURN)
      param_2/r1: flags (DEAD - spilled into the guard frame at entry,
        never reloaded; the vtable call receives only
        (facade, path_object))
      param_3/param_4 (r2/r3): DEAD - their spill slots are the top
        half of the 16-byte guard frame (Ghidra's uStack_10/uStack_c
        in decomp/c/008/080f4ad8_FUN_080f4ad8.c)
      FUN_08206e40: "PATH_PROBE_GUARD_CTOR seam (interface-guard ctor:
        guard-class vtable store over the 0x0818a0c4 base, lock
        acquire 0x0818a144 on the +0x0c word)"
      FUN_0818a0bc: "PATH_PROBE_FACADE_FETCH seam (facade accessor
        veneer: ldr r0,[r0,#4]; b 0x08296ec0 - waits out a NULL
        interface->field_8 and returns that field, the facade)"
      FUN_08206e6c: "PATH_PROBE_GUARD_DTOR seam (interface-guard dtor:
        vtable re-plant, unlock 0x0818a164 on +0x0c, base teardown
        tail 0x0818a0fc)"
      r4: saved path_object, then saved query status
    notes: >-
      18 bl call sites (grep on decomp/osos.asm): 0x08072b24,
      0x08097b6c, 0x080ac088, 0x080f4ac0 (the path_exists wrapper),
      0x080ff940, 0x08100a20, 0x08117938, 0x0811796c, 0x081186e0,
      0x081ee798, 0x081eeeb0, 0x0827ef64, 0x0827ef80, 0x0827f798,
      0x0827f7b4, 0x0827fb4c, 0x0827fb98 and 0x08399474; every site
      passes a path object in r0 and a small flag (0 or a
      sign-extended byte) in r1 and branches on the returned status
      being nonzero - the exists semantics the wrapper's name
      carries. Scoped-lock facade query: construct the 16-byte
      interface guard over the r0-r3 spill frame (0x08206e40: vtable
      +0x00, interface +0x04, flag bytes +0x08/+0x09, lock token
      +0x0c), fetch the facade through it with selector 1
      (0x0818a0bc), indirect-call the facade vtable slot +0x50 with
      (facade, path_object), destroy the guard (0x08206e6c), return
      the query status verbatim (saved across the dtor in r4; the
      dtor's own return is discarded). The probe runs inside the
      guard's critical section. The facade class owning slot +0x50 is
      unidentified (the accessor returns interface->field_8 of an
      object fetched from a registry via 0x0814a130); the sibling
      pre-open remove @ 0x08084d58 is slot +0x5c on the same facade.
      Deviations: the three callees remain in retailOS, so the
      PATH_PROBE_GUARD_CTOR / PATH_PROBE_FACADE_FETCH /
      PATH_PROBE_GUARD_DTOR seams' wired defaults call their fixed
      firmware addresses on target_os = "none" (the
      ui/object_state.rs firmware_clock_sample boundary precedent) -
      the symbol IS hook-ready - and fail closed on host (no-op guard
      boundaries; a stand-in facade whose slot +0x50 answers 0, the
      vtable_set.rs store_ctor_unported policy). With this port the
      path_exists PATH_EXISTS_WORKER seam's wired default was
      switched from its fail-closed stub to this symbol (same
      commit), making path_exists hook-ready; path_exists's own
      entry above still documents the stub era. Host tests
      (recording mocks on all three seams plus a mock facade whose
      non-+0x50 slots are wrong-slot traps):
      src/app/path_probe.rs::tests::call_order_is_guard_fetch_query_unguard
      / the_same_guard_frame_flows_through_ctor_fetch_and_dtor /
      fetch_receives_selector_one_and_flags_is_dead /
      query_dispatches_through_slot_50_with_the_fetched_facade_and_path_verbatim
      / the_query_status_survives_the_guard_dtor_and_returns_verbatim
      / default_chain_fails_closed_on_host, plus the rewired
      src/app/path_exists.rs::tests::default_worker_is_the_ported_probe_and_fails_closed_on_host
      integration. match.py (exit 1, diff expected): same
      guard-ctor / fetch-selector-1 / slot-+0x50-indirect-call /
      save-status / guard-dtor / return-verbatim structure; the diff
      is the crate fp frame and the three seams' blx dispatch in
      place of the direct bls.

  - address: 0x08271724
    ghidra: FUN_08271724
    name: iterator_state_release
    size: 64
    sig: "void iterator_state_release(owner: *mut u8, target: *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1: owner
      param_2: target
      iVar1: current
      iVar2: candidate
      iVar3: predecessor
      param_1 + 0x0c: observer_head
      param_2 + 0x10: next_observer
    notes: >-
      The ARM ABI takes owner and target in r0/r1. The 64-byte body reads
      only 32-bit words: owner +0x0c is the observer-list head and each
      node +0x10 is its next link. It walks from the head, returns without
      stores if target is absent, and otherwise copies target +0x10 to the
      owner head for a head match or predecessor +0x10 for any later match.
      Reference C decomp/c/026/08271724_FUN_08271724.c and the
      functions.csv 64-byte entry agree on the control-flow bounds. 2 bl
      call sites (grep on decomp/osos.asm): 0x08155edc inside
      iterator_state_cleanup (0x08155ec0, ported in the same module) and
      0x08271d50 inside the cxx/observable_array destructor 0x08271d2c,
      which drains its observer list by repeatedly releasing the current
      head. Ported as app/vtable_set::iterator_state_release; the list
      words are modeled as u32 target pointers (zero-extended on read,
      target compared as its low 32 bits - the cxx/list_splice.rs idiom).
      The direct caller iterator_state_cleanup now calls this export
      DIRECTLY - the no-op ITERATOR_STATE_RELEASE seam is removed (the
      app/class_6800.rs ported-callees-called-directly precedent), and
      the cxx/observable_array OBSERVABLE_ARRAY_DETACH_OBSERVER seam's
      head-match default is now replaceable by this export (its drain
      site can only reach the head-match branch). Host evidence uses a
      sub-4-GiB fixture slab (crate::testing::hints::VTABLE_SET_ITERATOR_RELEASE)
      so raw u32 pointers round-trip:
      iterator_state_release_leaves_an_empty_list_unchanged /
      iterator_state_release_leaves_an_absent_target_unchanged /
      iterator_state_release_replaces_the_head_with_its_successor /
      iterator_state_release_splices_an_interior_target_from_its_predecessor /
      iterator_state_release_unlinks_a_tail_target /
      iterator_state_release_copies_the_target_next_word_without_reencoding,
      plus the reworked iterator_state_cleanup tests that now drive the
      real unlink. match.py (exit 1, diff expected): same
      walk-compare / advance-through-+0x10 / head-vs-predecessor
      conditional-store structure; the diff is the crate fp frame and
      block scheduling. HISTORY - this entry previously claimed `ported`
      with no such function in the tree (commit ce10e27 introduced it as
      a side effect of an unrelated port) and was demoted to
      `identified`; the port above is the real one.


  - address: 0x0811d008
    ghidra: FUN_0811d008
    name: vtable_file_record_teardown
    size: 176
    sig: "void vtable_file_record_teardown(record: *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: record
      local_c (sp+0x3c): key (the outer iterator's yielded key out-slot)
      local_2c (sp+0x1c): node (the inner iterator's yielded node out-slot)
      auStack_24 (sp+0x24): outer iterator scratch, six words (state object at +4)
      auStack_44 (sp+0x04): inner iterator scratch, six words (state object at +4)
      auStack_28 (sp+0x20): inner_key (dead - nothing reads the inner key back)
      thunk_FUN_081dde18 (0x08212a5c): outer-iterator begin thunk (bare b 0x081dde18; unported; VTABLE_FILE_RECORD_TEARDOWN_OUTER_BEGIN seam)
      FUN_08212a4c: outer-iterator step (spill-slot wrapper over FUN_081ddde8 with a dead node out-slot; unported; VTABLE_FILE_RECORD_TEARDOWN_OUTER_NEXT seam)
      FUN_0821c4c8: inner-iterator begin (key lookup 0x0812d160 + tail FUN_081dde18; unported; VTABLE_FILE_RECORD_TEARDOWN_INNER_BEGIN seam)
      FUN_081ddde8: file_record_iterator_next (direct, ported in this module)
      FUN_08155ec0: iterator-state cleanup (ported; VTABLE_FILE_RECORD_TEARDOWN_ITER_CLEANUP seam)
      FUN_080e7970: free_wrapper (direct, ported in heap/veneers; caller tag 0x19)
      FUN_0812d300: registry dispose (unported; VTABLE_FILE_RECORD_TEARDOWN_REGISTRY_DISPOSE seam)
      FUN_082aad24: operator_delete (direct, ported in heap/veneers)
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0811d200, the tail
      of the kind-1 branch of the tag-dispatching dispose FUN_0811d188
      @ 0x0811d188 (128 bytes, unported), which first deletes a
      non-NULL +0x08 (0x08212a60 + operator_delete) and a non-NULL
      +0x10 (0x0821c4ec + operator_delete), then calls THIS function
      and returns the record. The teardown/destructor of the tagged
      0x1c-byte file-record family's registry half - the counterpart
      of vtable_file_record_construct_kind1 @ 0x0811d148 and
      vtable_file_record_construct_kind2 @ 0x0811d104 (both ported in
      the same module), operating on the SAME record layout (tag at
      +0x00, registry at +0x04). A nested walk of the registry: the
      OUTER iterator (begin thunk 0x08212a5c -> FUN_081dde18, 5 bl
      sites; step FUN_08212a4c, 1 bl site, wrapping the shared step
      FUN_081ddde8 with the node out-slot bound to its dead r3 spill)
      yields each key at sp+0x3c; for every key the INNER iterator
      (begin FUN_0821c4c8, 3 bl sites = key lookup 0x0812d160 + tail
      FUN_081dde18; step FUN_081ddde8 directly, 10 bl sites) walks
      that key's bucket, and every node is freed TWICE through
      free_wrapper with caller tag 0x19 - first the node's +0x04
      payload word (ldr r0,[sp,#0x1c]; ldr r0,[r0,#0x4]), then the
      node itself. Each iterator is released by the cleanup
      FUN_08155ec0 (69 bl sites) on its state object (iter + 4:
      sp+0x08 inner / sp+0x28 outer). Finally, if record +0x04 is
      non-NULL (cmp r0,#0; beq), the registry dispose FUN_0812d300 (2
      bl sites - this one and 0x0811d1c4 in the caller's kind-2
      branch; it drains remaining entries via 0x0812d294 and tail-
      branches to the object teardown 0x0810e6b0 -> b 0x08135380)
      runs and the object is operator_deleted; record +0x04 (32-bit
      str) and the tag byte (strb) are then zeroed on every path.
      Deviations: the four unported callees sit behind
      VTABLE_FILE_RECORD_TEARDOWN_OUTER_BEGIN / _OUTER_NEXT /
      _INNER_BEGIN / _REGISTRY_DISPOSE read_volatile seams whose wired
      defaults do NOT model the exact bodies - the callees are an
      unported registry-iterator (red-black-tree) subsystem, so the
      defaults yield an EMPTY traversal (no-op begins/dispose,
      0-returning steps) and an unswapped table runs straight to the
      guard/delete/zero tail (the store_remove_unported /
      construct_guard_unported no-op precedent, unlike
      VTABLE_SET_50_KIND4_OPS's exact-body defaults); free_wrapper and
      operator_delete are called directly (both ported in
      heap/veneers - the app/class_6800.rs
      ported-callees-called-directly precedent), and host tests observe
      their frees through the HEAP_OPS.free slot. The node out-slot is a
      target-width u32, cast to *mut u8 only for the immediate
      dereference; its host fixtures are mapped below 4 GiB.
      The record's +0x04 registry field stays a 32-bit word (the kind-1/
      kind-2 constructors' byte-exact precedent - the exact inverse
      of their u32 store); the return type is void - the epilogue's
      r0 is the zero immediate of the clearing stores and the sole
      caller overwrites r0 with the record pointer (mov r0,r4 at
      0x0811d1fc) before the call; no forwarded parameter (the entry
      stmdb spills no argument registers and r1..r3 are never read).
      Reference C (decomp/c/010/0811d008_FUN_0811d008.c) gets the
      loop shape and the double free right, but its "Subroutine does
      not return" on FUN_0812d300 is a Ghidra mis-analysis - the
      callee's tail chain returns (0x08135380 ends in a returning
      tail b 0x08271d2c) and the operator delete after it is live
      code here and at the sibling site 0x0811d1c8 - and its stack
      naming hides that the outer step's node out-slot is the
      0x08212a4c wrapper's dead r3 spill. match.py: same
      begin/next/cmp loop structure, the payload-then-node double
      free with tag 0x19, the guard/delete/zero tail; the diff is the
      crate fp frame, the five seam ldr/blx pairs standing in for the
      direct bls (the documented seams) and LLVM's inlining of the
      ported free_wrapper/operator_delete bodies (their documented
      HEAP_OPS dispatch). Host tests:
      src/app/vtable_set.rs::file_record_teardown_empty_registry_runs_straight_to_the_dispose_delete_tail
      / file_record_teardown_populated_registry_walks_keys_and_frees_nodes_in_order
      / file_record_teardown_null_registry_skips_the_dispose_and_delete
      / file_record_teardown_zeroes_only_the_registry_word_and_the_tag
      / file_record_teardown_default_seams_yield_an_empty_traversal.

  - address: 0x0811d188
    ghidra: FUN_0811d188
    name: vtable_file_record_destruct
    size: 128
    sig: "*mut u8 vtable_file_record_destruct(record: *mut u8)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: record
      FUN_08212a60: kind-1 +0x08 container teardown (16-byte iterator-state-cleanup wrapper, 1 bl site - this function's; unported; VTABLE_FILE_RECORD_DESTRUCT_KIND1_CONTAINER08_TEARDOWN seam)
      FUN_0821c4ec: kind-1 +0x10 container teardown (the byte-identical twin of FUN_08212a60, 2 bl sites - 0x0811cd44 and this function's; unported; VTABLE_FILE_RECORD_DESTRUCT_KIND1_CONTAINER10_TEARDOWN seam)
      FUN_082aad24: operator_delete (direct, ported in heap/veneers)
      FUN_0812d300: kind-2 +0x18 registry dispose (2 bl sites - this function's and vtable_file_record_teardown's; unported; VTABLE_FILE_RECORD_DESTRUCT_KIND2_DISPOSE seam, sharing the teardown sibling's no-op default)
      FUN_0811d008: vtable_file_record_teardown (direct, ported in the same module)
    notes: >-
      2 bl call sites (grep on decomp/osos.asm): 0x0815aa74 and
      0x0819fdf8 - both are the destruct-then-free pair `bl 0x0811d188;
      bl 0x082aad24` on a NULL-guarded record pointer, consuming the
      returned record pointer as operator_delete's argument. The
      tag-dispatched destructor of the tagged 0x1c-byte file-record
      family - the counterpart of vtable_file_record_construct_kind1 @
      0x0811d148 and vtable_file_record_construct_kind2 @ 0x0811d104
      (both ported in the same module), dispatching on the tag byte
      they write at +0x00 (ldrb). Tag 0 and any unknown tag (cmp/bne)
      are a straight no-op returning the record. Kind 1 tears the two
      iterator containers down in field order - +0x08 first, then
      +0x10 - each only when non-NULL, each teardown (FUN_08212a60 /
      FUN_0821c4ec: add r0,r0,#0x4; bl FUN_08155ec0; sub r0,r0,#0x4 -
      the same iterator-state cleanup vtable_file_record_teardown
      uses, then return the container) immediately followed by
      operator_delete on the teardown's RETURN value, then runs the
      shared registry-half teardown FUN_0811d008 (ported as
      vtable_file_record_teardown, commit 30604e8) on the whole
      record. Kind 2 deletes the +0x04 block directly (blne 0x082aad24
      - NO dispose: the block is a plain five-word operator_new(0x14)
      allocation from the kind-2 constructor, not an object) and, when
      +0x18 is non-NULL, disposes it through FUN_0812d300 before
      deleting it. This function writes NOTHING back to the record -
      the tag and +0x04 clearing on the kind-1 path is the teardown's
      own tail. Deviations: the three unported callees sit behind the
      new VTABLE_FILE_RECORD_DESTRUCT_KIND1_CONTAINER08_TEARDOWN /
      _CONTAINER10_TEARDOWN / _KIND2_DISPOSE read_volatile seams; the
      container seams share one wired default modeling the callees'
      exact 16-byte bodies minus the unported FUN_08155ec0 cleanup
      (the no-op teardown_iter_cleanup_unported stands in - its
      rationale lives on VTABLE_FILE_RECORD_TEARDOWN_ITER_CLEANUP),
      returning the container so the following operator_delete frees
      the right block (the KIND1_GUARD/KIND2_GUARD two-seams-one-default
      precedent), and the kind-2 dispose seam is wired to the SAME
      no-op default as VTABLE_FILE_RECORD_TEARDOWN_REGISTRY_DISPOSE
      (role-specific names keep host tests from racing the sibling's
      parallel tests); vtable_file_record_teardown and operator_delete
      are called directly (both ported - the app/class_6800.rs
      ported-callees-called-directly precedent), host tests observe
      the deletes through the HEAP_OPS.free slot; the kind-1 deletes
      consume the teardown seams' return value (both callees provably
      return their argument - `sub r0, r0, #0x4`), while the kind-2
      +0x18 delete gets the guarded word (the
      vtable_file_record_teardown convention for the same bl 0x0812d300;
      bl 0x082aad24 pair - 0x0812d300 reloads r0 with its argument
      before tail-branching into the object-teardown chain, whose
      return is not established); every field load keeps the
      original's width (ldrb tag, 32-bit ldr pointer words - the
      vtable_file_record_init byte-exact precedent); no forwarded
      parameter (the entry stmdb spills no argument registers and
      r1..r3 are never read). Reference C
      (decomp/c/010/0811d188_FUN_0811d188.c) marks all three
      container/registry teardowns "Subroutine does not return" - the
      disassembly shows real returns (FUN_08212a60 / FUN_0821c4ec end
      in ldmia sp!,{r4,pc}; FUN_0812d300's tail chain returns) and the
      operator_delete after each is live code - and drops every call
      argument; the port follows the disassembly. match.py: same tag
      dispatch, the guarded teardown-then-delete pairs, the bare
      blne delete of the kind-2 +0x04 block, the trailing teardown
      call and the record return; the diff is the crate fp frame, the
      three seam ldr/blx pairs standing in for the direct bls (the
      documented seams), LLVM keeping the +0x18 registry in r5 and its
      block ordering (kind-1 branch laid out ahead of kind-2's).
      Host tests:
      src/app/vtable_set.rs::file_record_destruct_tag0_is_a_noop_and_returns_the_record
      / file_record_destruct_unknown_tag_is_a_noop_and_returns_the_record
      / file_record_destruct_kind1_null_containers_run_straight_to_the_teardown
      / file_record_destruct_kind1_tears_down_and_deletes_both_containers_in_order
      / file_record_destruct_kind2_deletes_the_block_then_disposes_and_deletes_the_registry
      / file_record_destruct_kind2_null_members_is_a_noop.

  - address: 0x0811d0b8
    ghidra: FUN_0811d0b8
    name: vtable_file_record_insert
    size: 160
    sig: "void vtable_file_record_insert(record: *mut u8, key: u32, inner_key: u32, data: u32, payload: u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r0/r5: record
      param_2/r1/r7: key (the OUTER registry key)
      param_3/r2/r8: inner_key (the bucket key)
      param_4/r3/r6: data (stored at node +0x04; the sole call site passes a fresh memcpy'd block)
      param_5/stack-arg/r9: payload (stored at node +0x00; the sole call site passes the data block's byte count)
      FUN_080eb67c: malloc_wrapper (direct, ported in heap/veneers) - the checked wrapper, called (8, 0x19)
      FUN_080edb74: checked-alloc guard (unported; the EXISTING VTABLE_FILE_RECORD_KIND1_GUARD seam - same callee the kind-1 constructor seamed, no duplicate seam)
      FUN_0812d1b8: the shared 84-byte tail body (lookup-or-create-bucket then insert node), INLINED - functions.csv's 160-byte row lumps it into this function and Ghidra decompiles the pair as one C function; its only other reach is the tag-0x1a sibling FUN_0811cefc's `b 0x0812d1b8` at 0x0811cf68 (that sibling reads the registry at +0x18 and stays unported)
      FUN_0812d160: vtable_file_record_lookup (24-byte spill-slot wrapper over the ported registry_lookup 0x0810e4c8, 6 bl sites; ported in app/vtable_set; the wired VTABLE_FILE_RECORD_INSERT_LOOKUP default, callers still route through the seam)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers) - the miss path's 0x28 bucket allocation
      FUN_0810e64c: class_registry_construct (the EXISTING VTABLE_FILE_RECORD_KIND1_CTOR seam - same callee the kind-1 constructor seamed; note the original here bls 0x0810e64c DIRECTLY, not through the kind-1's 0x0812d2fc thunk, so the seam's wired default models this chain exactly)
      FUN_0810e4ac: registry_insert (direct, ported in app/registry) - the vtable +0x1c dispatch building the {key, value} pair on its stack (stmia sp, {r1, r2}); called twice on a miss (outer keyed bucket insert, then the node insert tail), once on a hit
    notes: >-
      1 bl call site (grep on decomp/osos.asm): 0x0811ca40 in
      FUN_0811c9f0, which malloc_wrappers a byte-count-sized block,
      memcpys (0x08037db0) its arg4 into it and hands this function
      the block as arg4 and the byte count as the stacked arg5 - arg4
      is the node PAYLOAD pointer, arg5 its length. The insert/append
      operation of the tagged 0x1c-byte file-record family - the
      counterpart of vtable_file_record_construct_kind1 @ 0x0811d148 /
      vtable_file_record_construct_kind2 @ 0x0811d104 /
      vtable_file_record_teardown @ 0x0811d008 (which walks exactly
      the two-level registry this builds) /
      vtable_file_record_destruct @ 0x0811d188 (all ported in the
      same module). An 8-byte node is allocated through
      malloc_wrapper(8, 0x19) and handed to the checked-alloc guard
      0x080edb74, then filled (arg4 at node +0x04, arg5 at node +0x00,
      both 32-bit str); the registry at record +0x04 is looked up for
      the arg2 key through the thunk 0x0812d160; on a miss a fresh
      0x28 registry is operator_new'd, constructed (0x0810e64c) and
      itself inserted into the outer registry keyed by arg2
      (0x0810e4ac); either way the node is finally inserted into the
      bucket keyed by arg3 through the same registry_insert tail.
      Deviations: the port INLINES the shared tail body 0x0812d1b8
      (see its renames entry); malloc_wrapper, operator_new and
      registry_insert are called DIRECTLY (all ported - the
      app/class_6800.rs ported-callees-called-directly precedent;
      host tests observe the allocations through HEAP_OPS.alloc and
      drive the +0x1c dispatch with fake registry vtables); the guard
      and the registry construct REUSE the existing
      VTABLE_FILE_RECORD_KIND1_GUARD / VTABLE_FILE_RECORD_KIND1_CTOR
      read_volatile seams (same callees the kind-1 constructor seamed
      - no duplicate seam under a second name); the ported lookup thunk
      0x0812d160 is vtable_file_record_lookup, the wired
      VTABLE_FILE_RECORD_INSERT_LOOKUP default (the cell_size_ptr /
      get_varint promotion precedent — callers still route through
      the seam for hookability); it directly calls the already-ported
      registry_lookup 0x0810e4c8, its dead r2 parameter is dropped,
      and its r3 spill only seeds an unobservable out-slot (the
      search writes every nonzero-status result before the thunk's
      conditional load); the record's +0x04 registry field
      is read POINTER-SIZED, a host-representation deviation from the
      32-bit `ldr r0, [r5, #0x4]` (the miss path dereferences the
      registry through registry_insert's vtable read, so a truncated
      host pointer would fault - the vtable_file_record_teardown node
      out-slot precedent; byte-identical on the 32-bit target); the
      node field stores keep the 32-bit str widths (the
      vtable_file_record_init byte-exact precedent); the return type
      is () - the tail chain leaves registry_insert's result in r0
      but the sole call site discards it; no forwarded parameter (the
      entry stmdb spills no argument registers and the stacked fifth
      argument is REAL). Reference C
      (decomp/c/010/0811d0b8_FUN_0811d0b8.c) gets the node shape
      right but drops every argument of the inlined tail -
      FUN_0812d160() takes (registry, arg2), FUN_0810e64c() takes the
      fresh operator_new block, FUN_0810e4ac(uVar2, param_2, piVar3)
      is the OUTER keyed insert of the new bucket - and its closing
      `(**(code **)(*piVar3 + 0x1c))(piVar3, &stack0xfffffff0)` hides
      that there are TWO registry_insert calls and that the final
      one's stack pair is {arg3, node} built inside 0x0810e4ac
      itself; the port follows the disassembly. match.py: same chain
      - malloc (the ported veneer's inlined lazy-init body standing
      in for bl 0x080eb67c), guard blx (the KIND1_GUARD seam), the
      two node stores fused into one stm, the record +0x04 load, the
      lookup blx (the INSERT_LOOKUP seam), the cmp/bne hit branch,
      the miss path's mov r0,#0x28 -> operator_new -> ctor blx (the
      KIND1_CTOR seam) -> keyed insert, and the tail b to the final
      insert; the diff is the crate fp frame, the three seam ldr/blx
      pairs and the direct ported calls' unresolved bl relocations
      (match.py prints them as bl 0). Host tests:
      src/app/vtable_set.rs::file_record_insert_hit_path_reuses_the_existing_bucket
      / file_record_insert_miss_path_allocates_constructs_and_keyed_inserts_the_bucket
      / file_record_insert_stores_arg4_and_arg5_into_the_node
      / file_record_insert_dispatches_vtable_slot_1c_with_the_stack_pair
      / file_record_insert_runs_the_checked_alloc_guard_on_the_node
      / file_record_lookup_hit_returns_the_slot_value
      / file_record_lookup_miss_returns_null
      / file_record_lookup_routes_registry_and_key_verbatim_to_the_search.


  - address: 0x0812d160
    ghidra: FUN_0812d160
    name: vtable_file_record_lookup
    size: 24
    sig: "*mut u8 vtable_file_record_lookup(registry: *mut u8, key: u32)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r0: registry
      param_2/r1: key
      param_3/r2: dead (overwritten with the stack out-slot pointer by `mov r2, sp` before the search; the reference C's unused param_3)
      param_4/r3: the initial content of the stack out-slot (spilled by `stmdb sp!, {r3, lr}`; unobservable because registry_lookup writes every nonzero-status result before `ldrne` reads the slot, while a zero status returns 0 without loading it)
      FUN_0810e4c8: registry_lookup (direct, ported in app/registry; `registry, key, &out_slot`)
    notes: >-
      6 bl call sites (grep on decomp/osos.asm): 0x0812d184 in the
      dispose-bucket wrapper FUN_0812d178, 0x0812d1cc in the shared
      insert tail inlined by vtable_file_record_insert, 0x0812d218 /
      0x0812d240 / 0x0812d264 in the rest of the registry-facade
      thunk cluster 0x0812d178..0x0812d254, and 0x0821c4d8 in the
      unported inner-iterator begin FUN_0821c4c8. A keyed search
      wrapper: it spills r3 and lr, overwrites dead r2 with sp, calls
      registry_lookup(registry, key, &slot), and returns slot on its
      nonzero found status or 0 on a miss. The r3 spill is arg4's
      initial slot value (vtable_file_record_insert's inlined tail
      happens to pass the node), not an argument forwarded to the
      search; it is unobservable because registry_lookup writes every
      hit before the conditional load. The reference C
      decomp/c/011/0812d160_FUN_0812d160.c accurately shows this
      shape, including `local_8[0] = param_4`; its param_3 is dead.
      Promotion: this export replaced the private insert_lookup_default
      model as the wired VTABLE_FILE_RECORD_INSERT_LOOKUP seam default
      (the cell_size_ptr / get_varint precedent); vtable_file_record_insert
      deliberately remains routed through that seam for hookability,
      with no caller rewiring. FUN_0810e4c8 is already ported as
      app/registry.rs registry_lookup and is called directly; its
      remaining firmware vtable methods stay beneath that port's own
      dispatches. match.py: 12 Rust instructions versus the original
      6; same registry_lookup-call -> status-test -> conditional-slot-
      return structure, with the port's explicit zero initialization
      and frame accounting for the extra instructions (the direct
      relocation is printed as `bl 0`). Host tests:
      src/app/vtable_set.rs::file_record_lookup_hit_returns_the_slot_value
      / file_record_lookup_miss_returns_null
      / file_record_lookup_routes_registry_and_key_verbatim_to_the_search.
  - address: 0x0811d21c
    ghidra: FUN_0811d21c
    name: vtable_query_4c_read_eight_byte_record
    size: 220
    sig: "u32 vtable_query_4c_read_eight_byte_record(handle: *mut *mut u8, record: *mut u8, scratch: usize, forwarded: usize)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r0/r5: handle
      param_2/r1/r4: record
      param_3/r2: scratch (the entry spill into triple[1], the finish out-slot's initial content - never read back)
      param_4/r3: forwarded (the entry spill into triple[2], the kind-4 out-slot's initial content - never read back - and the word the first dispatcher's stmdb sp!,{r3} spill exposes verbatim)
      local_18: triple[0] (the record spill slot, sp+0 - doubles as the kind-2 out-slot; only its low half is consumed, ldrh)
      uStack_14: triple[1] (the arg3 spill slot, sp+4 - the finish thunk's out-slot)
      uStack_10: triple[2] (the arg4 spill slot, sp+8 - the kind-4 scratch out-slot)
      FUN_0811d7b0: vtable_slot_4c_dispatch (through the new VTABLE_QUERY_4C_RECORD_DISPATCH seam, wired to the ported implementation in the same module)
      FUN_0811d46c: vtable_query_4c_kind4 (through the new VTABLE_QUERY_4C_RECORD_FINISH seam, wired to the ported implementation in util/vtable_query.rs)
    notes: >-
      2 bl call sites (grep on decomp/osos.asm): 0x081367e4 and
      0x081d15ac - both pass the handle in r0 and a record pointer in
      r1 (param + 0x8 / + 0x12 at the first site, r4 + 0x24 at the
      second), neither sets r2/r3 deliberately, and both branch on the
      returned status (movs r4, r0; bne / cmp r0, #0; beq). The
      multi-message record READ of the slot +0x4c message family - the
      exact slot +0x4c mirror of the ported slot +0x50
      vtable_set_50_write_eight_byte_record @ 0x0811d360, direction
      reversed. Eight guarded dispatches through vtable_slot_4c_dispatch,
      every stage short-circuiting on the first nonzero status
      (cmp r0, #0; bne - NO status-5 special case, unlike the
      probe/read siblings): kind 4 with &triple[2] (the arg4 spill,
      sp+8 - a scratch out-slot whose word is never read back, site
      0x0811d230), kind 1 over record+0..+4 (five sequential
      dispatches, 0x0811d248..0x0811d2a8), kind 2 with &triple[0] (the
      RECORD spill slot itself, sp+0 - the pointer is dead, r4 saved
      it at entry; site 0x0811d2c0), then kind 1 at record+8
      (0x0811d2e0); on a zero status the tail bleq 0x0811d46c (the
      ported kind-4 query thunk vtable_query_4c_kind4) fires with
      &triple[1] (the arg3 spill, sp+4) as the message and its error
      code becomes the return value. The two subtle orderings: the u16
      store at record+6 runs BEFORE the status branch (ldrh r1,[sp];
      cmp r0,#0; strh r1,[r4,#6]; bne - even a failing kind-2 dispatch
      leaves its out-slot's low half at record+6), and the kind-2
      dispatch reads its result into the SAME stack word the u16 store
      then consumes (Ghidra's local_18 aliasing is CORRECT here,
      unlike its mangled siblings: local_18/uStack_14/uStack_10 match
      sp+0/sp+4/sp+8 exactly). Deviations: both callees are ported and
      route through the new role-specific VTABLE_QUERY_4C_RECORD_DISPATCH
      / VTABLE_QUERY_4C_RECORD_FINISH read_volatile seams (the
      VTABLE_QUERY_4C_SCALAR_DISPATCH/_FINISH precedent - hookability
      plus host-test interception without racing the siblings' or
      util's own parallel tests); the {r1, r2, r3} entry spill is
      modeled as a three-word usize stack triple (the
      vtable_query_4c_kind4_read / vtable_query_4c_walk_alloc stack-pair
      convention widened by one slot), triple[0] doubling as the
      kind-2 out-slot - only its low 16 bits are consumed, so the port
      reads triple[0] as u16, byte-exact on a 64-bit host where the
      method's 32-bit answer store lands in the word's low half; arg3
      is scratch (the walk_alloc precedent - only the finish
      out-slot's initial content), arg4 is forwarded (verbatim into
      the first dispatch's extra - nothing touches r3 before the first
      bl; DEAD for every later dispatch and the finish call, so a zero
      stack word stands in for those extras and 0 for the thunk's
      forwarded/_unused - the 0x0811d360 dead_slot precedent; the
      thunk discards r2 with mov r2, r1); the return type is u32, not
      the reference C's void (both call sites branch on r0; bail paths
      return the failing dispatch's status verbatim, success returns
      the finish thunk's code). match.py (exit 1, diff expected): same
      guarded-dispatch chain, kind sequence 4/1x5/2/1, unconditional
      u16 store before the kind-2 status branch, conditional finish
      tail; the diff is the crate fp frame, the two seam ldr/blx pairs
      standing in for the direct bl 0x0811d7b0 / bleq 0x0811d46c (the
      documented seams), and LLVM lowering the unaligned u16
      load/store to a full-word ldr + two strb byte stores (ARMv5
      unaligned-access splitting, semantically identical to
      ldrh/strh). Host tests:
      src/app/vtable_set.rs::record_read_full_chain_dispatches_all_fields_in_protocol_order
      / record_read_short_circuits_at_every_failing_stage
      / record_read_finish_result_is_the_return_value.

  - address: 0x082cf274
    ghidra: FUN_082cf274
    name: cg_operand_stack_offset
    size: 84
    sig: "i32 cg_operand_stack_offset(cg_codegen_t *codegen, cg_stack_operand_t *operand)"
    module: stdio/cg_stack_slot
    status: ported
    vars: {param_1/r0: codegen, param_2/r1: operand, "[codegen + 0x208]": stack_slot_context, "[operand + 0x04]": canonical_parent, "[operand + 0x14]": cached_stack_offset, "[context + 0x04]": stack_slot_allocator, "[allocator + 0x28]": stack_slot_cursor}
    notes: >-
      JIT operand stack-slot resolver. A signed cached frame offset at
      operand+0x14 is returned directly once bound. Otherwise the
      canonical parent (operand+0x04) owns the cache; when that too is
      -1, the stack-slot allocator through codegen+0x208 advances its
      +0x28 cursor by four and the parent receives -(new_cursor + 0x28).
      The child copies that parent value, so every operand in one
      canonical set shares one lazily allocated four-byte frame slot.
      Raw ARM, Ghidra C, and both emitters (0x082ccbb0 / 0x083685f0)
      agree on the loads, stores, sentinel, and signed result. Host tests
      cover existing-parent reuse, first allocation and sibling sharing,
      poisoned adjacent target-word slots, and 32-bit cursor wraparound.
      match.py (exit 1, diff expected): same 21-instruction control/data
      shape and every recovered target offset; LLVM emits an early
      `bxne lr` for the already-bound path and materializes
      `-(cursor + 0x28)` as `mvn #43; sub` rather than `add #40; rsb`.

  - address: 0x08260558
    ghidra: FUN_08260558
    name: align_up
    size: 20
    sig: "u32 align_up(u32 value, u32 align)"
    module: util/align
    status: ported
    renames:
      param_1: value
      param_2: align
    notes: >-
      76 bl call sites, a pure leaf: the classic power-of-two round-up
      (value + align - 1) & ~(align - 1), all wrapping mod 2^32
      (add/sub/sub/bic/bx). Port preserves the exact original arithmetic
      including the align == 0 (always 0) and value+align-1 overflow
      (wraps) degenerate cases. match.py (exit 1, diff expected): LLVM
      folds ~(align-1) into -align via rsb/and (algebraically identical)
      and adds the crate's fp frame; core add/sub/mask structure matches.

  # --- opaque application-object dispatch-target word accessor ---
  - address: 0x0829919c
    ghidra: FUN_0829919c
    name: object_dispatch_target
    size: 8
    sig: "u32 object_dispatch_target(const void *object)"
    module: app/object_dispatch_target
    status: ported
    renames:
      param_1: object
      r0: dispatch_target_word
    notes: >-
      8-byte leaf: `ldr r0,[r0,#0x18]; bx lr` - returns the complete raw
      u32 word at object+0x18 with no NULL guard and no writes. Extent
      verified against the next function's entry @ 0x082991a4
      (bitmap_draw_in_rect). Call-site census (binary-scanned over every
      B/BL word in osos.dec): 30 plain bl sites, zero predicated forms,
      plus one tail b from the already-ported singleton_state_get wrapper
      @ 0x08086df0 (app/singleton_state), which fetches the singleton
      base and tail-branches here for the same +0x18 load. No data-word
      references to 0x0829919c, so the accessor is never itself
      dispatched virtually. Every sampled caller treats the returned word
      as a vtable-bearing object pointer: the settings dispatcher @
      0x0816f8ac and the command cluster @ 0x08203a44 invoke its vtable
      slots +0x88/+0x8c with staged three-word operation records, and
      singleton_state_get's callers do the same through slot +0x84. The
      producing objects are heterogeneous (registry class-0x6180
      instances via instance_of_class_6180 @ 0x08284e2c, dispatcher this
      pointers, the singleton base), so +0x18 is a shared dispatch-target
      member of this controller family [INFERENCE from the uniform
      vtable-dispatch usage]; the concrete target class is not recovered.
      No deviations: the port returns the raw word and adds no NULL
      check; +0x18 is word-aligned in every recovered owner, so the port
      uses an aligned load, not read_unaligned. match.py (exit 1, diff
      expected): the single ldr matches; the crate's fp frame accounts
      for the extra push/pop. Host tests prove the exact word at +0x18 is
      returned for extreme values, the rest of the object is untouched,
      and distinct objects yield their own word (no caching).

  - address: 0x082991a4
    ghidra: FUN_082991a4
    name: bitmap_draw_in_rect
    size: 76
    sig: "void bitmap_draw_in_rect(u8 *wrapper, u8 *ctx, const u32 *rect_a, const u32 *rect_b, u32 alpha)"
    module: drivers/bitmap
    status: ported
    renames:
      param_1: wrapper
      param_2: ctx
      param_3: rect_a
      param_4: rect_b
      param_5: alpha (stack-passed; every sampled caller passes 0xff)
      FUN_082993b4: bitmap_ensure_loaded (ported direct lazy BMap loader; parser alone uses BITMAP_HOOKS.parse)
      FUN_08262bdc: bitmap_draw_helper (rect-offsetting draw helper; BITMAP_HOOKS.draw seam, no-op default)
    notes: >-
      42 bl call sites, all plain bl (no predicated forms), no tail
      branches, no data-word references (binary-scanned). Extent
      verified against the next function's entry @ 0x082991f0. The
      bitmap resource wrapper's draw-in-rect accessor, sibling of
      bitmap_query_bounds @ 0x08299368 running the same
      loader-then-flag-check shape over the lazy "BMap" handle cluster
      (wrapper layout: +0x00 provider chain head, +0x04 resource id,
      +0x08 loaded flag, +0xc4 parsed bitmap object). Algorithm: call
      the lazy loader FUN_082993b4(wrapper) FIRST and unconditionally;
      if the +0x08 flag is still clear, return without drawing;
      otherwise dispatch FUN_08262bdc(ctx, *(wrapper+0xc4), rect_a,
      rect_b, alpha, 0) - the wrapper itself (r0) is consumed and NOT
      forwarded; the helper's sixth word is the original's hard zero
      (mov r3,#0; str r3,[sp,#4]). The helper @ 0x08262bdc (224
      bytes, 60 bl sites) re-bases both four-word rects and the
      object's by the draw context's +0x2c/+0x30 origin and invokes
      the text/layout draw engine @ 0x080f1600 with the surface
      pointers *(ctx+0x1c)+4 / *(object+0x1c)+4, the ctx +0x11/+0x15
      colours, the +0x10 style byte and the +0x34 clip rect. Every
      sampled call site (0x08141f7c, 0x081989fc, 0x082130a8,
      0x0829930c, 0x081eab20) passes 0xff for the stack argument, so
      it is the draw opacity/alpha [INFERENCE from the uniform
      constant]; 0x08141f7c passes the same bounds block
      bitmap_query_bounds just filled as BOTH rect arguments. Return:
      the epilogue (pop {r2,r3,...pc}) leaves r0 holding the helper's
      return on the draw path and the zero flag byte on the skip path,
      but every sampled call site overwrites r0 with its next
      instruction, so the port is void (the string_id_record.rs
      precedent). Deviations - the draw helper alone rides the
      BITMAP_HOOKS seam (documented no-op default, NOT hook-ready - the
      stock helper and the engine beneath it must be ported before the
      draw path runs on target). +0xc4 is read as a native-width pointer;
      0xc4 is not eight-aligned, so on 64-bit hosts the field is parked
      at 0xc8 in the wrapper's unaccounted tail (the module header's
      parked-pointer rule; on target it is exactly the 4-byte field).
      match.py (exit 1, diff expected): blx-indirect dispatch through
      the hooks struct and the crate's fp frame are the seam pattern;
      the loader-first / flag-check / conditional-draw structure, the
      +0xc4 object load, the stm {alpha, 0} stack pair and the void
      epilogue all match.

  - address: 0x082991f0
    ghidra: FUN_082991f0
    name: bitmap_get_loaded_object
    size: 28
    sig: "u8 * bitmap_get_loaded_object(u8 *wrapper)"
    module: drivers/bitmap
    status: ported
    renames:
      param_1: wrapper
      FUN_082993b4: bitmap_ensure_loaded (ported direct lazy BMap loader)
    notes: >-
      Extent binary-verified against the next function entry at
      0x0829920c: push {r4,lr}; preserve wrapper; call the lazy loader;
      read wrapper+0x08; conditionally load wrapper+0xc4; return. The
      binary scan over every ARM B/BL word in osos.dec finds exactly nine
      direct call sites (0x08291408, 0x0829141c, 0x08291430, 0x08291444,
      0x08291484, 0x082914d0, 0x0829151c, 0x08291568, 0x0829176c), all
      plain unconditional bl, with no predicated forms or tail branches.
      Every direct caller tests r0 for zero before using it. A single
      data-word reference at 0x089b0b20 occurs in a contiguous table of
      0x0829xxxx function pointers, so this accessor is also vtable
      dispatched. Algorithm: call FUN_082993b4(wrapper) first and
      unconditionally; return null when its +0x08 loaded flag remains
      clear, otherwise return the parsed bitmap pointer at +0xc4.
      The loader is now called directly; only its unported bitmap parser
      FUN_082645a0 uses the BITMAP_HOOKS.parse seam. Deliberate host-only
      deviation: the target's 4-byte +0xc4 pointer naturally lands at +0xc8
      in the 64-bit #[repr(C)] fixture; ARM reads the literal +0xc4 field.
      Host tests cover unloaded null return and a non-1 nonzero loaded flag.
  - address: 0x08299368
    ghidra: FUN_08299368
    name: bitmap_query_bounds
    size: 76
    sig: "u64 bitmap_query_bounds(u32 *out, u8 *wrapper, u32 arg3, u32 arg4)"
    module: drivers/bitmap
    status: ported
    renames:
      param_1: out
      param_2: wrapper
      param_3: arg3
      param_4: arg4
      local_20: bounds (the r0..r3 spill slots doubling as the query block)
      FUN_082993b4: bitmap_ensure_loaded (ported direct lazy BMap loader; parser uses BITMAP_HOOKS.parse)
      FUN_082a1dbc: bitmap_object_read_bounds (parsed object's direct bounds getter)
      DAT_08299400: 'the "BMap" kind literal (0x424d6170 big-endian, ResourceKind::BITMAP in app/resource_chain)'
    notes: >-
      59 bl call sites, no tail branches (binary-scanned). The bitmap
      resource wrapper's bounding-rectangle query, part of the lazy
      "BMap" handle cluster at 0x08299xxx: wrapper layout +0x00
      provider chain head, +0x04 resource id, +0x08 loaded flag, +0x0c
      inline parser argument block, +0xc4 parsed bitmap object.
      Algorithm: call the lazy loader FUN_082993b4(wrapper) FIRST and
      unconditionally (it resolves the ("BMap", id) pair through
      resource_chain_find @ 0x0827216c, parses into an
      operator_new(0x44) object via FUN_082645a0, stores it at +0xc4
      and raises the +0x08 flag; a failed lookup leaves the flag
      clear); if the flag is still clear, zero all four words of out
      (four streq word stores); otherwise dispatch FUN_082a1dbc
      (*(wrapper+0xc4), &block) - which copies the four words at
      +0x98..+0xa4 of the inner object *(object+0x1c) - and copy the
      block into out (ldmia/stmia). The stack block IS the entry's
      r0..r3 spill (stmdb sp!,{r0,r1,r2,r3,...}), so the epilogue's
      pop returns the first two words in r0/r1: the bounds words on
      the query path, the incoming (out, wrapper) pointers on the zero
      path - a quirk the port keeps verbatim (u64 return, low half =
      r0). Callers read the result as a rectangle of four signed words
      (the layout code @ 0x08141e30 subtracts word[1] from word[3] for
      a height and hands the block to the draw-in-rect helper
      FUN_082991a4 - edges, not origin/size [INFERENCE from that
      caller's arithmetic]). Sibling accessors: FUN_082991a4 (draw in
      rect) and FUN_08299404 run the same loader-then-flag-check shape.
      Deviations - arg3/arg4 exist only because the original spills all
      four argument registers into the block; FUN_082a1dbc overwrites
      four words, making them unobservable. Only the bitmap parser
      FUN_082645a0 remains on the BITMAP_HOOKS.parse seam (documented
      pass-through default, NOT hook-ready for a real bitmap resource).
      +0xc4 is read
      as a native-width pointer; 0xc4 is not eight-aligned, so on
      64-bit hosts the field is parked at 0xc8 in the wrapper's
      unaccounted tail (the drivers/display_layer.rs parked-pointer
      rule); on target every offset is literal. match.py (exit 1, diff
      expected): same spill-block / loader-call / flag-test /
      zero-or-query structure; the crate fp frame and LLVM outlining
      the zero path account for the diff.
  - address: 0x082993b4
    ghidra: FUN_082993b4
    name: bitmap_ensure_loaded
    size: 80
    sig: "void bitmap_ensure_loaded(u8 *wrapper)"
    module: drivers/bitmap
    status: ported
    renames:
      param_1/r0: wrapper
      wrapper_plus_0x00: resource_head
      wrapper_plus_0x04: resource_id
      wrapper_plus_0x08: loaded
      wrapper_plus_0x0c: parser_args
      wrapper_plus_0xc4: parsed_bitmap
      DAT_08299400: '"BMap" resource kind (0x424d6170 big-endian)'
      FUN_0827216c: resource_chain_find
      FUN_082aadd4: operator_new
      FUN_082645a0: bitmap_resource_parse (BITMAP_HOOKS.parse seam)
    notes: >-
      Raw ARM disassembly confirms a true 80-byte extent: 76 bytes of code
      from 0x082993b4 through 0x082993fc, followed by the 0x424d6170
      ("BMap") literal at 0x08299400; the next distinct function starts
      with push {r4,r5,r6,lr} at 0x08299404. Decoding every ARM B/BL word
      in osos.dec finds exactly eight direct incoming calls, all
      unconditional bl (0x082991bc, 0x082991f8, 0x08299378, 0x08299418,
      0x082994b8, 0x0829952c, 0x082995b4, 0x0829963c), with zero
      predicated calls and no direct tail branch; no aligned raw data word
      references this address. If wrapper.loaded is nonzero, return. Else
      look up (resource_head, "BMap", resource_id); a null result leaves
      loaded clear. On success allocate 0x44 bytes via operator_new, call
      FUN_082645a0(allocation, wrapper+0x0c, resource) without an
      allocation-null check, store its return at +0xc4, then store 1 at
      +0x08. The resource lookup and allocator call their existing direct
      ports. Deliberate deviation: the unported parser remains a volatile
      BITMAP_HOOKS.parse dispatch whose default passes the allocation through;
      it preserves this loader's store and flag flow but cannot initialize a
      real bitmap until FUN_082645a0 is ported. #[repr(C)] named fields retain
      the target layout and avoid host pointer/u32 overlap. Host tests cover
      failed lookup, a successful lookup's BMap kind/id/allocation/parser
      arguments, and the already-loaded no-op path.
  # --- Tagged payload signed-field sum ---
  - address: 0x082a1d7c
    ghidra: FUN_082a1d7c
    name: tagged_payload_signed_field_sum
    size: 44
    sig: "i32 tagged_payload_signed_field_sum(const u32 *object)"
    module: util/tagged_payload_signed_field_sum
    status: ported
    renames:
      param_1/r0: object
      param_1_plus_0x20: tagged_payload
      uVar2/r0: untagged_payload
      sVar1/r0: signed_field_sum
    notes: >-
      Raw ARM extent is exactly 44 bytes, from 0x082a1d7c through bx lr at
      0x082a1da4; the next separately linked function begins at 0x082a1da8.
      A binary scan of every ARM B/BL word in osos.dec found 10 direct call
      sites, all unconditional bl (zero predicated forms): 0x08140d00,
      0x081a1458, 0x081a1474, 0x081ea9a4, 0x081ea9c0, 0x08262e0c,
      0x08263be4, 0x08263eac, 0x08291158, and 0x0829133c. The callee has no
      guard for object itself: it loads the target-width word at object+0x20,
      clears its low tag bit, and returns zero if that produces NULL (so raw
      words 0 and 1 both return zero). Otherwise it sign-extends and sums the
      three i16 fields at payload+0x52, +0x54, and +0x56, then truncates the
      result back to signed i16 via lsl/asr #16. The concrete object and
      payload types are unrecovered, so the name records only this observed
      operation. The Rust port deliberately models object pointer fields as
      u32 target words, retaining the exact +0x20 layout on 64-bit hosts;
      host tests use a dedicated low-4-GiB fixture and cover tag clearing,
      signed reads, zero/one payload words, and i16 truncation.
  - address: 0x082a1dbc
    ghidra: FUN_082a1dbc
    name: bitmap_object_read_bounds
    size: 24
    sig: "void bitmap_object_read_bounds(u8 *object, u32 *out)"
    module: drivers/bitmap
    status: ported
    renames:
      param_1: object
      param_2: out
      r0: inner_object
      r2: bounds
    notes: >-
      24-byte leaf; extent verified against the distinct next entry
      @ 0x082a1dd4. Binary scan over every ARM B/BL word in osos.dec
      found 25 plain bl call sites, no predicated forms, no tail
      branches, and no data-word references to this address. The target
      has no NULL guard: `ldr r0,[r0,#0x1c]` dereferences object
      immediately. Algorithm: load the inner object
      pointer at object+0x1c; use its +0x98 bounds block as the source;
      move out into r0; then ldmia four words into r1-r3/ip and stmia
      them to out. All source words load before any destination store,
      so exact source/destination aliasing is preserved. Deliberate
      host-only deviation: +0x1c is a 4-byte pointer on target but is
      parked at +0x20 in aligned 64-bit fixtures; the ARM build retains
      the literal +0x1c offset. Host tests use the stock-sized 0x44-byte
      parsed object and a full inner fixture through +0xa4, proving
      ordered copying and exact-alias behavior. The prescribed match.py
      invocation cannot inspect this worktree: it hardcodes the sibling
      ../rustypod archive and reported this new symbol absent there.
      Local archive disassembly instead confirmed the inner-pointer load,
      +0x98 source offset, four loads before four stores, and leaf return;
      volatile accesses and the crate frame account for instruction
      differences.

  - address: 0x0818a0bc
    ghidra: FUN_0818a0bc
    name: facade_for_selector
    size: 8
    sig: "FacadeObject *facade_for_selector(InterfaceGuard *guard, u32 selector)"
    module: app/facade_for_selector
    status: ported
    renames:
      param_1/r0: guard (the 16-byte scoped interface guard; only its
        +0x04 interface word is read - ldr r0,[r0,#4])
      param_2/r1: selector (untouched by the veneer, flows to the walk
        in r1; 0 = accept any published facade, nonzero = accept only a
        state-0/kind-1 match)
      FUN_08296ec0: "FACADE_REGISTRY_WALK seam (the facade registry
        walk the veneer tail-branches to; modeled on host)"
    notes: >-
      8-byte veneer: ldr r0,[r0,#4] (the guard's interface word, planted
      by guard ctor 0x08206e40 via 0x0818a06c/0x0814a130); b 0x08296ec0
      (a true tail branch - lr never saved, the walk's r0 return reaches
      the caller directly). Ghidra's decomp/c/016/0818a0bc_FUN_0818a0bc.c
      shows a LOOP because it inlined the tail callee; the 8-byte
      function itself is only the veneer.
      34 bl call sites (grep on decomp/osos.asm): 0x08084d70, 0x0808911c,
      0x080891ac, 0x08089234, 0x08089284, 0x080892e0, 0x08090c18,
      0x0809b6c0, 0x080a8ef8, 0x080b6b70, 0x080f4af0
      (path_probe_via_facade), 0x080f4b8c, 0x081ef64c, 0x081ef688,
      0x081ef804, 0x081efa00, 0x08277a60, 0x08277b1c, 0x08277c48,
      0x08277d9c, 0x08277df4, 0x0827843c, 0x08278488, 0x08278588,
      0x082786bc, 0x082788dc, 0x08278908, 0x08278974, 0x08278a58,
      0x08278ad8, 0x08278c20, 0x08278d98, 0x08278e50 and 0x08278f10.
      Observed selectors: 33 sites pass 1 (mov r1,#0x1 immediately ahead
      of the bl); the lone selector-0 site is 0x082788dc (facade vtable
      slot +0x18 dispatch, immediately followed by a selector-1 fetch at
      0x08278908 for slot +0x30 on the same facade). Every site
      indirect-calls a facade vtable slot with the result - slots +0x18,
      +0x1c, +0x30, +0x34, +0x38, +0x3c, +0x40, +0x44, +0x50, +0x54,
      +0x5c, +0x60, +0x64, +0x68, +0x6c, +0x70, +0x78, +0x7c, +0x80 and
      +0x9c observed.
      The registry the walk @ 0x08296ec0 reads: node->field_0x08 is the
      published facade, node->byte_0x19 the interface state byte,
      facade->byte_0x08 the kind byte; a nonzero selector matches only
      state==0 AND kind==1 (32-bit residue: kind 0 wraps to 0xffffffff
      and never matches). On a miss the walk calls FUN_0814a08c (trace
      static accessor - never reads its argument, the residue in r0 is
      dead) and retries against the registry's DEFAULT interface root,
      the fixed object @ 0x08a778f4 handed out by the ADS local-static
      accessor FUN_0814a030 (guard page 0x089cb1ec, its word at +0x08;
      ctor 0x082973bc, dtor 0x0828c580), with the selector relaxed to 0.
      The default root's ctor publishes field +0x08 (str r0,[r4,#0x8] @
      0x08297430) BEFORE the guard release - born published, which is
      what makes the stock spin terminate; the host model keeps that
      (publication is instantaneous), so the modeled chain is total.
      The walk rides the FACADE_REGISTRY_WALK seam (wired default calls
      0x08296ec0 on target_os=none - hook-ready; host default is the
      modeled walk over house statics). Verified by
      src/app/facade_for_selector.rs::tests (guard+4 reload and selector
      passthrough via a recording mock; modeled-walk accept/miss/
      fallback for selectors 0 and 1, kind 0 wrap, busy state, NULL
      facade). match.py (exit 1, diff expected): same ldr r0,[r0,#4] +
      tail-branch structure; the diff is the crate fp frame and the
      seam's ldr/bx static dispatch in place of the direct b 0x08296ec0.
  - address: 0x08296ec0
    ghidra: FUN_08296ec0
    name: facade_registry_walk
    size: 60
    sig: "FacadeObject *facade_registry_walk(InterfaceNode *interface, u32 selector)"
    module: app/facade_registry_walk
    status: ported
    vars: {param_1: interface, param_2: selector, r0: node, r1: selector}
    notes: >-
      60 bytes. The facade registry walk the 8-byte facade_for_selector
      veneer @ 0x0818a0bc tail-branches to (Ghidra inlines it there,
      which is why 0818a0bc's decomp shows a loop). node = interface;
      facade = node->field_0x08; if facade != NULL and (selector == 0 or
      (node->state_0x19 | (facade->kind_0x08 - 1)) == 0) return facade;
      on a miss call trace-static accessor FUN_0814a08c (dead argument,
      result discarded) and node = FUN_0814a030() (fixed default root
      0x08a778f4, born published), selector = 0, loop. Kind 0 wraps its
      subtraction to 0xffff_ffff, so only kind 1 + state 0 accepts a
      nonzero selector. 9 direct bl call sites (0x080a28cc, 0x0813a34c,
      0x0813a3ec, 0x0813a6d4, 0x0813a9d8, 0x08296f50, 0x082971a8,
      0x08297200, 0x08297288) plus the 0x0818a0c0 tail branch. The two
      ADS local-static accessors are fixed-address target calls; host
      builds use a no-op trace accessor and a born-published
      house-static default root with test seams replacing both.
      Verified by src/app/facade_registry_walk.rs::tests (accept with
      selector 0, kind-1/state-0 match, kind-0 wrap rejection, busy
      state rejection, NULL facade fallback, trace accessor invoked per
      miss, loop terminates at the default root).
  - address: 0x08386454
    ghidra: FUN_08386454
    name: sqlite_vm_printf
    size: 100
    sig: "char *sqlite_vm_printf(sqlite3 *db, const char *format, va_list ap)"
    module: sqlite/vm_printf
    status: ported
    vars: {param_1: db, param_2: format, param_3: ap, uVar1: max_length, auStack_188: accum, local_174: "accum.mallocFailed shadow (sp+0x14)", auStack_170: base, r4: db, r5: ap, r6: format}
    notes: >-
      SQLite 3.5.9's sqlite3VMPrintf (util.c) - the va_list printf every
      formatted diagnostic funnels through. 5 bl call sites
      (binary-scanned): 0x08376754 (sqlite_error), 0x083767cc
      (sqlite_error_msg), 0x0837d368 (sqlite_set_string_formatted),
      0x0837d804 (FUN_0837d7dc = sqlite3NestedParse, which latches
      db->mallocFailed itself on a NULL result and runs the text through
      the parser @ 0x08382580), and 0x082c2474 (FUN_082c2438, a
      refcounted log-string appender - the only caller passing db ==
      NULL, exercising the literal-default limit). Algorithm: build a
      stack StrAccum (sp+0) over a 350-byte uninitialized stack base
      buffer (sp+0x18; size literal 0x15e pooled @ 0x083864bc) via
      sqlite3StrAccumInit @ 0x08384e84 (ported, sqlite::vdbe_op), with
      mxAlloc = db->aLimit[SQLITE_LIMIT_LENGTH] (+0x50) or, for a NULL
      db, SQLITE_MAX_LENGTH = 1,000,000,000 (literal 0x3b9aca00 pooled
      @ 0x083864b8); run the conversion engine sqlite3VXPrintf @
      0x0839788c (UNPORTED, 3324 bytes - SQLITE_VXPRINTF dispatch seam
      with a documented no-op default, house pattern) with useMalloc =
      1; return sqlite3StrAccumFinish @ 0x08384e14 (ported,
      sqlite::str_accum) verbatim - a heap-owned NUL-terminated copy or
      NULL; then, only when accum.mallocFailed (sp+0x14, reloaded AFTER
      the finish since the finish itself can set it) and db != NULL,
      store 1 into db->mallocFailed (+0x1e) - the sticky SQLITE_NOMEM
      latch. A NULL db is never dereferenced (the ldreq/ldrne pair skips
      the limit load too). Deviations: the C va_list is the house
      explicit VaList pointer (exactly the &spilled-arg the callers
      build); StrAccum + base buffer live in the Rust frame; init and
      finish are called directly per the porting rules. The port is the
      natural wired default for the shared SQLITE_VM_PRINTF seam in
      sqlite/error_msg.rs; that swap is deliberately not in this commit
      (the seam file is untouched). Verified by
      src/sqlite/vm_printf.rs::tests (limit selection for db and NULL
      db, argument/capacity forwarding to the engine, heap-copy return
      with n_char+1 allocation, allocator-failure NULL + latch,
      latch-never-cleared, formatter-reported failure with and without a
      db, default-engine empty-string and down-allocator shapes).
      match.py (exit 1, diff expected): same 376-byte frame, same
      cmp/ldrne [r4,#0x50]/ldreq limit select, same init -> engine ->
      finish -> strbne [r4,#0x1e] tail; the diff is the crate fp frame
      and the seam's ldr/blx static dispatch in place of the direct bl
      0x0839788c.

  - address: 0x08261d1c
    ghidra: FUN_08261d1c
    name: cxx_mutexattr_init
    size: 20
    sig: "usize *cxx_mutexattr_init(usize *attr)"
    module: cxx/mutex_attr_init
    status: ported
    renames:
      param_1: attr (saved in r4 and returned)
      FUN_082e84a4: pthread_mutexattr_init (the wrapped initializer)
    notes: >-
      20 bytes, all code, no literal pool (0x08261d1c..0x08261d30; the
      pthread_mutexattr_destroy wrapper @ 0x08261d30 follows
      immediately). 2 bl call sites, binary-scanned: 0x08261e34 in
      cxx_mutex_construct (cxx/mutex.rs) and 0x082621bc in the twin
      constructor FUN_082621b0. Five-instruction C++-layer veneer over
      pthread_mutexattr_init @ 0x082e84a4: save attr in r4, bl the
      initializer, return the SAVED pointer (mov r0,r4) - the
      initializer's status (0 or 0x1a) is discarded; no NULL guard of
      its own. The wrapped initializer (0x082e84a4..0x082e84d4, still
      unported): NULL attr -> 0x1a with no stores; otherwise plants the
      attr magic 0x4d545841 "MTXA" (literal @ 0x082e84d8,
      binary-verified in osos.dec; the destroy/settype literals @
      0x082e84a0/0x082e852c hold the same word) at attr+0x00, the
      default halfword 0xffc2 at +0x04 (mvn r1,#0x3d via strh), RMWs
      the +0x06 halfword (bic #0x3f clears bits 0..5 - including the
      type field bits 4..5 -> NORMAL, the field pthread_mutexattr_
      settype @ 0x082e84dc rewrites and the PosixMutex layout records
      at mutex+0x0e - then orr #0x8 sets bit 3, the process-scope
      default; the mutex initializer @ 0x082e82f8 copies the whole
      attr+0x04 word into mutex+0x0c), and returns 0. Deviations: the
      callee rides the CXX_MUTEXATTR_INIT_OPS dispatch slot (the
      facade_registry_walk fixed-address pattern) - the wired default
      transmutes 0x082e84a4 on target (hook-ready), the host default is
      a behavioral model of the decoded stores so tests can observe the
      planted fields; attr is passed as *mut usize matching
      cxx/mutex.rs's two-word stack scope model (byte-exact on the
      32-bit target). The port is wired as cxx/mutex.rs's
      CXX_MUTEX_CONSTRUCT_OPS.attr_init default (that module's
      documented "a later port replaces its default" pattern; the
      pointer return is discarded there, matching the original call
      site whose next instruction overwrites r0). Host tests: pointer
      forwarding and identity return with the callee status discarded
      (a recording mock returns 0x1a and the veneer still returns the
      pointer), NULL forwarded unguarded and returned, the default
      model's planted fields compared byte-exact against an independent
      byte-level reference across +0x06 halfword seeds (0x0000, 0xffff,
      0x003f, 0xffc0, 0xaa95 - bits 0..5 cleared, bit 3 set, upper bits
      preserved), bytes past the 8-byte attr untouched, and the host
      model's 0/0x1a statuses. match.py (exit 1, diff expected): same
      save-r0-in-r4 -> call -> mov r0,r4 -> pop structure; the diff is
      the crate fp frame and the seam's ldr/blx static dispatch in
      place of the direct bl 0x082e84a4.

  - address: 0x082621b0
    ghidra: FUN_082621b0
    name: cxx_recursive_mutex_construct
    size: 44
    sig: "u8 *cxx_recursive_mutex_construct(u8 *this, usize param_2, usize scope_word0, usize scope_word1)"
    module: cxx/recursive_mutex
    status: ported
    renames:
      param_1/r0: this (saved in r4 and returned)
      param_2/r1: (unread - overwritten by the attr pointer before the second bl)
      param_3/r2: scope_word0 (pushed as the scope's first word)
      param_4/r3: scope_word1 (pushed as the scope's second word)
      uStack_10/uStack_c: scope (the two-word stack pthread_mutexattr)
      FUN_08261d1c: cxx_mutexattr_init (ported)
      FUN_08262170: recursive settype-and-mutex-init helper (unported)
      FUN_08261d30: cxx_mutexattr_destroy (unported)
    notes: >-
      44 bytes, all code and no literal pool (0x082621b0..0x082621dc;
      the C++ mutex destructor thunk starts at 0x082621dc), verified
      from osos.dec rather than Ghidra. Decoding every ARM B/BL word in
      osos.dec found exactly 14 direct call sites, all unconditional bl
      and zero predicated calls: 0x0817da48, 0x0818b3ac, 0x081a8b9c,
      0x081f0068, 0x081fc908, 0x08280474, 0x082804d4, 0x0828df14,
      0x0828e0a4, 0x0828e2a4, 0x0828e4b8, 0x082acc6c, 0x082adf94,
      and 0x082af5e0. No tail b reaches this address. The raw body is a
      C++ recursive-mutex-wrapper constructor: push r2/r3 as its
      two-word mutexattr stack scope; call cxx_mutexattr_init @
      0x08261d1c; call 0x08262170, which passes kind 2 (RECURSIVE) to
      pthread_mutexattr_settype @ 0x082e84dc, stores that status at
      this+0x18, and only on success initializes the mutex through
      0x082e82f8 before replacing the status; then call
      cxx_mutexattr_destroy @ 0x08261d30 and return the saved this
      pointer. The scope's incoming contents are the uninitialized
      caller r2/r3 values, overwritten by attr init before use.
      Deliberate deviations: 0x08262170 and 0x08261d30 remain unported,
      so volatile dispatch slots call their retailOS entries on target;
      host defaults model their decoded stores, including type bits
      4..5 = 2, initialized mutex fields, and valid-attr magic clear.
      Host tests prove attr-init -> recursive-init -> teardown order,
      mutable scope identity, this return, ignored r1, and host-default
      recursive mutex layout. match.py structural review is expected to
      differ at the dispatch blx calls.

  - address: 0x08279284
    ghidra: FUN_08279284
    name: path_object_construct
    size: 20
    sig: "StringObject *path_object_construct(StringObject *this, const char *path)"
    module: app/path_object_construct
    status: ported
    renames:
      param_1/r0: this (raw two-word StringObject storage, forwarded
        verbatim into the base ctor)
      param_2/r1: path (caller-owned C string, forwarded verbatim - no
        register is written between entry and the bl)
      FUN_08277304: string_object_construct_from_cstr (ported, called
        directly - the transition_addon.rs ported-callees precedent)
      DAT_08279298: "PATH_OBJECT_VTABLE_ADDRESS (0x089a60d8, the
        StringObject-derived path class's vtable; binary-verified
        against osos.dec)"
      r4: DEAD - pushed and popped, never written (non-leaf frame
        etiquette)
    notes: >-
      ADS C++ converting constructor PathObject(const char *path) over
      caller storage: bl the ported base ctor @ 0x08277304 with both
      arguments forwarded untouched in r0/r1, then ldr the
      literal-pool word @ 0x08279298 (0x089a60d8, the derived
      path-class vtable) and str it over the base vtable at +0x00,
      returning the base ctor's r0 result verbatim (the ADS this
      convention). The overwrite is unconditional: empty/NULL sources
      and failed payload allocations still carry the derived vtable
      over a NULL payload. 24 bl call sites (grep on
      decomp/osos.asm): 0x0806859c / 0x080685bc / 0x080685dc (the
      FUN_08068504 database-path trio: Photo Database, ArtworkDB,
      iTunesDB), 0x08084d38 (the remove wrapper's guard), 0x080890c4,
      0x08089168, 0x080891f0, 0x08090bd4, 0x0809b688, 0x080a8ec0,
      0x080f4ab8 (the path_exists wrapper), 0x080f4b2c / 0x080f4b3c
      (the FUN_080f4b1c pair - Ghidra shows no r1 write for the
      first, a stale-register empty-path construction), 0x08100980,
      0x08117914, 0x0811af80, 0x0811c5bc, 0x08136570, 0x081a29b0,
      0x081a2c54, 0x081afdfc, 0x081b0018, 0x081bc670, 0x081bda2c;
      every site passes raw two-word storage in r0 and a path C
      string in r1, and most destroy the object through the ported
      string_object_destroy_veneer @ 0x082792fc on scope exit.
      Deviations: the planted vtable is the ROM identity constant
      (the StringObjectVtable ROM-identity precedent; nothing ported
      dispatches through it); with this port the path_exists
      PATH_OBJECT_CTOR seam's wired default switched from its
      faithful local default to this symbol (same commit - the
      path_probe_via_facade rewiring precedent), so the symbol IS
      hook-ready. Host tests:
      src/app/path_object_construct.rs::tests::returns_the_base_ctors_result_verbatim
      / plants_the_derived_vtable_over_the_base /
      base_construction_defines_both_words_from_garbage /
      empty_and_null_sources_still_carry_the_derived_vtable /
      fail_closed_assign_leaves_a_null_payload_for_nonempty_sources /
      matches_the_reference_model_across_sources, plus the rewired
      src/app/path_exists.rs::tests::default_ctor_builds_the_derived_path_object
      integration. match.py (exit 1, diff expected): identical
      push / bl base-ctor / ldr-literal / str [r0] / pop structure
      with the same 0x089a60d8 literal-pool word; the diff is the
      crate fp frame.

  - address: 0x0827929c
    ghidra: FUN_0827929c
    name: path_object_construct_from_string_object
    size: 20
    sig: "StringObject *path_object_construct_from_string_object(StringObject *this, const StringObject *source)"
    module: app/path_object_construct
    status: ported
    renames:
      param_1/r0: this (raw two-word PathObject storage, forwarded
        unchanged to the base StringObject copy constructor)
      param_2/r1: source (already-constructed StringObject, forwarded
        unchanged to the base copy constructor)
      FUN_082773e0: string_object_copy_construct (ported, called
        directly)
      DAT_082792b0: "PATH_OBJECT_VTABLE_ADDRESS (0x089a60d8, the
        derived StringObject path-class vtable)"
      r4: DEAD - pushed and popped but never written (non-leaf frame
        etiquette)
    notes: >-
      ADS C++ StringObject-to-PathObject constructor: raw ARM at
      0x0827929c..0x082792b0 is push {r4,lr}; bl 0x082773e0; ldr
      r1,[pc,#4]; str r1,[r0]; pop {r4,pc}, followed by the literal-pool
      word @ 0x082792b0 = 0x089a60d8. The next separately linked,
      byte-identical copy-constructor sibling starts at 0x082792b4, so
      Ghidra's 20-byte extent excludes the literal and the true extent is
      24 bytes. The base StringObject copy constructor receives r0/r1
      unchanged, then its returned r0 has +0x00 overwritten
      unconditionally with the derived vtable identity and is returned
      verbatim. 25 plain bl call sites, 0 tail b, 0 predicated calls, and
      zero data-word references, binary-scanned by decoding every B/BL
      word in osos.dec; this constructor is never virtually dispatched.
      At 0x080474d4, for example, r1 is the StringObject returned by
      string_owner_embedded_init, confirming the StringObject source
      boundary. No NULL guard exists in the veneer or its callers;
      invalid this/source faults in the base constructor. Deliberate
      deviations: the port calls the existing base port directly and
      plants the ROM vtable address as an identity constant rather than a
      host-callable pointer. It has a distinct link section so LLVM cannot
      fold this export into byte-identical path_object_copy_construct.
      Host tests cover null, empty, one-byte, and nonempty source payloads,
      unchanged source storage, base-result return identity, derived vtable
      replacement, and the this == source path that preserves the old
      payload word. match.py (exit 1, diff expected): same non-leaf call /
      literal load / vtable store / return structure; crate frame and
      modern LLVM scheduling differ from ADS.
  - address: 0x082792b4
    ghidra: FUN_082792b4
    name: path_object_copy_construct
    size: 20
    sig: "StringObject *path_object_copy_construct(StringObject *this, const StringObject *source)"
    module: app/path_object_construct
    status: ported
    renames:
      param_1/r0: this (raw two-word StringObject storage, forwarded
        verbatim into the base copy ctor)
      param_2/r1: source (caller-owned StringObject, forwarded verbatim
        - no register is written between entry and the bl)
      FUN_082773e0: string_object_copy_construct (ported, called
        directly - the transition_addon.rs ported-callees precedent)
      DAT_082792c8: "PATH_OBJECT_VTABLE_ADDRESS (0x089a60d8, the
        StringObject-derived path class's vtable; binary-verified
        against osos.dec)"
      r4: DEAD - pushed and popped, never written (non-leaf frame
        etiquette)
    notes: >-
      ADS C++ copy constructor PathObject(const StringObject &source)
      of the StringObject-derived path class, sitting between the
      converting (0x08279284) and default (0x082792cc) siblings:
      push {r4,lr} / bl the ported base copy ctor @ 0x082773e0 with
      both arguments forwarded untouched in r0/r1 / ldr r1,[pc,#4]
      (literal @ 0x082792c8 = 0x089a60d8, the derived path-class
      vtable) / str r1,[r0] over the base vtable at +0x00 / pop
      {r4,pc}, returning the base ctor's r0 result verbatim (the ADS
      this convention). 20 bytes of code + the 4-byte literal-pool
      word, so 24 bytes of true extent; Ghidra's 20-byte extent drops
      the literal, and the next function (path_object_default_construct)
      starts at 0x082792cc. The overwrite is unconditional and runs
      after the copy: a NULL-payload source, a failed payload
      allocation and the base's address-not-content self-construction
      guard path (this == source keeps the existing payload word) all
      still leave the derived vtable at +0x00. Neither argument is
      NULL-guarded - the original faults inside the base ctor for a
      NULL this/source, and so does the port. Call-site census,
      binary-scanned by decoding every B/BL word in osos.dec: 28 bl
      (27 plain + ONE predicated blne @ 0x083de170 under cmp r0,#0,
      where the caller NULL-guards the storage pointer itself), 0
      tail b, and ZERO data-word references to 0x082792b4, so the ctor
      is never dispatched virtually. Plain sites: 0x0812bbb4 /
      0x0812bbe0, 0x081a2ae0, the 0x081bc9d8 - 0x081bde78 cluster of
      thirteen, 0x081ef788 / 0x081ef9a4, 0x0826c970, 0x08278418,
      0x08278eb8 (inside silver_controller_transition_addon_construct,
      building the embedded string member at this+0x0c) and the
      0x082a55f4 - 0x082a563c cluster of four. Deviations: the planted
      vtable is the ROM identity constant PATH_OBJECT_VTABLE_ADDRESS
      shared with both siblings (the StringObjectVtable ROM-identity
      precedent; nothing ported dispatches through it). The
      transition_addon.rs TransitionAddonConstructOps
      string_member_construct seam keeps its faithful local default
      (derived vtable + NULL payload, source ignored) - its own tests
      pin that routing, so rewiring it to this symbol is left for the
      transition-addon porter. Host tests:
      src/app/path_object_construct.rs::tests::copy_construct_returns_the_base_ctors_result_verbatim
      / copy_construct_plants_the_derived_vtable_over_the_base /
      copy_construct_from_a_null_payload_source_stays_empty /
      copy_construct_fail_closed_assign_leaves_a_null_payload /
      copy_construct_from_self_keeps_the_payload (the guard pin: a
      missing self-guard would fault on the garbage payload) /
      copy_construct_matches_the_reference_model_across_sources.
      match.py (exit 1, diff expected): identical push / bl base-ctor
      / ldr-literal / str [r0] / pop structure with the same
      0x089a60d8 literal-pool word; the diff is the crate fp frame.

  - address: 0x082792cc
    ghidra: FUN_082792cc
    name: path_object_default_construct
    size: 20
    sig: "StringObject *path_object_default_construct(StringObject *this)"
    module: app/path_object_construct
    status: ported
    renames:
      param_1/r0: "this (raw two-word StringObject storage; never written
        to a register, so r0 passes through as the return - the ADS ctor
        convention Ghidra drops in decomp/c/026/082792cc_FUN_082792cc.c,
        which shows a void return)"
      DAT_082792e0: "PATH_OBJECT_VTABLE_ADDRESS (0x089a60d8, the
        StringObject-derived path class's vtable; binary-verified
        against osos.dec)"
      r1: scratch (the NULL, then the vtable literal)
    notes: >-
      ADS C++ default constructor PathObject() of the same
      StringObject-derived path class as path_object_construct @
      0x08279284: mov r1,#0 / str r1,[r0,#4] (payload = NULL) / ldr
      r1,[pc,#4] (literal @ 0x082792e0 = 0x089a60d8) / str r1,[r0]
      (vtable) / bx lr. 20 bytes of code + the 4-byte literal, so 24
      bytes of true extent; the next function starts at 0x082792e4 (the
      cmp r0,#0/push NULL-guarded delete helper). A LEAF: unlike the
      converting veneer it does NOT chain the base default ctor @
      0x08277440 (ported string_default_construct) - the base's vtable
      store would be dead one instruction later, so base+derived fold
      into the two stores, payload NULL written FIRST (the base ctor's
      own order is vtable-then-payload; unobservable either way,
      reproduced in the port). 42 bl / 0 b / 0 predicated call sites
      (binary-scanned by decoding every B/BL word in osos.dec); ZERO
      data-word references to 0x082792cc in the image, so the ctor is
      never dispatched through any vtable. Sampled sites build
      stack-resident path-object guards in pairs: 0x0805aa08/
      0x0805aa10 (add r0,sp,#12 / add r0,sp,#4, both destroyed through
      the ported string_object_destroy_veneer @ 0x082792fc on the error
      path) and 0x082736ec/0x082736f4 (add r0,sp,#16 / add r0,sp,#8).
      this is never NULL-guarded (the original faults on the payload
      store); both words are written UNCONDITIONALLY, so constructing
      over a live object discards vtable AND payload pointer (the
      original leaks the old payload; so does the port). The planted
      vtable is the ROM identity constant PATH_OBJECT_VTABLE_ADDRESS
      shared with path_object_construct (nothing ported dispatches
      through it). Host tests:
      src/app/path_object_construct.rs::tests::default_construct_returns_this_verbatim
      / default_construct_defines_both_words_from_garbage /
      default_construct_over_an_existing_object_discards_both_words /
      default_construct_matches_the_byte_reference. match.py (exit 1,
      diff expected): same two-stores-and-return structure with the
      same 0x089a60d8 literal-pool word; LLVM folds the two strs into
      one stm r0,{r1,r2} (value-identical: vtable at +0x00, NULL at
      +0x04) and adds the crate fp frame.

  - address: 0x08279314
    ghidra: FUN_08279314
    name: path_object_assign_from_string_object
    size: 36
    sig: "StringObject *path_object_assign_from_string_object(StringObject *this, const StringObject *source)"
    module: app/path_object_construct
    status: ported
    renames:
      param_1/r0: this (PathObject's two-word StringObject base; saved in r4 and returned)
      param_2/r1: source (readable StringObject supplying the NULL-safe C string)
      r4: this (preserved across both direct callees)
      FUN_082a50b0: string_object_c_str (ported direct callee)
      FUN_08276474: string_object_assign_payload (ported direct callee)
    notes: >-
      PathObject assignment from a StringObject source. Raw ARM at
      0x08279314..0x08279334 is push {r4,lr}; mov r4,r0; mov r0,r1; bl
      string_object_c_str @ 0x082a50b0; mov r1,r0; mov r0,r4; bl
      string_object_assign_payload @ 0x08276474; mov r0,r4; pop {r4,pc}.
      The next separately linked function starts at 0x08279338, making
      the true extent 36 bytes, all code. The NULL-safe accessor turns a
      NULL source payload into its shared empty string, so the assignment
      helper's existing empty-source path dispatches vtable slot +0xc;
      nonempty sources use its +0x8 allocation/copy path. Unlike the base
      StringObject assignment operator @ 0x082774a8, this body has no
      this == source guard and always invokes both callees. Full-image ARM
      B/BL decode found exactly 19 direct BL callers, all unconditional
      (0 predicated): 0x0805aa5c, 0x08074224, 0x0807a5b8, 0x08100a04,
      0x08103ecc, 0x081045bc, 0x081bcfb8, 0x081bd51c, 0x081bf2dc,
      0x081bf37c, 0x081ef6b4, 0x081ef748, 0x081ef7bc, 0x081ef7e0,
      0x081ef854, 0x0820216c, 0x082582b8, 0x0828fabc, 0x08298964.
      At 0x08074224 it assigns the caller's output PathObject from a
      stack StringObject before appending '/'. No pointer guard exists;
      invalid this/source faults while loading source.payload or in the
      assignment helper. No deliberate deviations: both callees are
      ported and called directly. Host tests cover nonempty copy and
      return identity, the NULL-payload canonical-empty clear path,
      vtable preservation, and the intentionally unguarded self-assignment
      allocation. match.py (exit 1, diff expected) retains the two-call
      save-this / return-this structure; the crate frame differs from ADS.

  - address: 0x081655e0
    ghidra: FUN_081655e0
    name: service_manager_get
    size: 52
    sig: "void *service_manager_get(void)"
    module: util/service_manager_get
    status: ported
    renames:
      DAT_08165614: SERVICE_MANAGER_HOLDER (pool literal holding &holder @ 0x089ca948; instance in its +4 slot)
      iVar1: holder
      iVar2: constructed (the ctor's return)
      FUN_082aadd4: operator_new (ported in heap/veneers, called directly)
      FUN_0816566c: service_manager_ctor (unported; SERVICE_MANAGER_CTOR dispatch slot, documented zeroing default)
      FUN_08030f44: heap_panic (ported in heap/veneers, called directly)
    notes: >-
      Lazy constructor-getter for the service-manager singleton - the
      publisher half of the app/service_manager.rs pair (its asserting
      accessor service_manager_instance @ 0x08165520 is already ported
      and names this address as the only thing that fills the slot).
      1 bl call site: 0x08258030 inside the boot-time singleton checker
      FUN_08258028, which calls this getter first and then nine sibling
      getters (0x08259740, 0x08257f34, 0x082598f8, 0x08259564,
      0x08257f04, 0x08259534, 0x0825a16c, 0x08259770) and falls into
      heap_panic unless every one returns non-NULL. Algorithm: load the
      holder global @ 0x089ca948, return its +4 instance word if
      non-NULL; otherwise operator_new(0xe8) (mov r0,#0xe8; bl
      0x082aadd4), construct via the unported ctor @ 0x0816566c, cache
      the ctor's return into holder+4 UNCONDITIONALLY, and - unlike the
      ten app/singletons.rs getters - fall into heap_panic @ 0x08030f44
      (bleq, non-returning) when the ctor returned NULL, so the getter
      never hands out NULL; the live path RELOADS holder+4 before
      returning (observable because the ctor itself stores the instance
      into holder+4 and sets the holder's +0/+1 flag bytes). The
      0xE8-byte object owns retailOS's per-hardware subsystem handlers:
      its ctor reads the hardware model id through the
      settings/platform-info singleton (FUN_08259928, vtable slot
      +0x34), indexes a 26-entry capability table by it (default mask
      0x1fbf @ 0x0816592c) and builds up to thirteen handler objects
      into the slot table at this+4 (see app/service_manager.rs for the
      full object write-up; no class name survives in the image).
      Deviations (the app/singletons.rs / cxx/settings.rs contract):
      the cache slot is the crate static SERVICE_MANAGER_INSTANCE owned
      by app/service_manager.rs rather than the word @ 0x089ca94c (the
      0x089caxxx RW pages are runtime-initialized; the image holds
      stale UI strings there); the ctor rides the SERVICE_MANAGER_CTOR
      dispatch slot with a documented zeroing stub default. The fatal
      NULL-ctor path is not host-tested (heap_panic is -> !), the
      cxx/list_splice.rs precedent. NOT HOOK-READY until FUN_0816566c
      is ported - the default hands out a zeroed block with no vtable
      and no handler slot table. Host tests: first call allocates 0xe8
      exactly once, constructs with the raw operator_new block, caches
      and returns the ctor result; second/third calls hit the cache
      with no realloc; a pre-populated cache takes the bne fast path
      with no allocation; the return is the reloaded slot, not the raw
      block; the zeroing default ctor clears the whole 0xe8 block.
      match.py: same push/ldr-pool/ldr/cmp/branch/mov-0xe8/bl/ctor/
      cmp/str/bleq-panic skeleton; the diff is the crate fp frame, the
      intra-archive bl relocations for operator_new/heap_panic, the
      blx-through-volatile-slot ctor dispatch standing in for the
      direct bl 0x0816566c (the documented deviation), and LLVM folding
      the post-store slot reload into the just-stored r0.

  - address: 0x08261de8
    ghidra: FUN_08261de8
    name: cxx_mutex_settype_init
    size: 56
    sig: "void cxx_mutex_settype_init(u8 *this, usize *attr)"
    module: cxx/mutex_settype_init
    status: ported
    renames:
      param_1/r0: this (the 0x1c-byte C++ mutex wrapper - 24-byte
        PosixMutex at +0x00 plus the status word at +0x18; saved in r4)
      param_2/r1: attr (the two-word stack scope attr object built by
        cxx_mutex_construct; saved in r5)
      iVar1: settype_status (stored at this+0x18 before the branch)
      uVar2: init_status (stored at this+0x18 on the success path)
      FUN_082e84dc: pthread_mutexattr_settype
      FUN_082e82f8: posix_mutex_init (the lazy initializer
        src/kernel/posix_mutex.rs documents)
    notes: >-
      56 bytes, all code, no literal pool (0x08261de8..0x08261e20; the
      posix_mutex_lock/unlock veneers @ 0x08261e20/0x08261e24 follow
      immediately). 1 bl call site, binary-scanned: 0x08261e40 in
      cxx_mutex_construct (cxx/mutex.rs); the twin constructor
      FUN_082621b0 inlines the same settype/init pair itself at
      0x08262184/0x0826219c instead of calling this veneer. The middle
      operation of the C++ mutex wrapper's scoped-attribute
      construction: pthread_mutexattr_settype(attr, 0=NORMAL), status
      stored at this+0x18 BEFORE the branch on it (cmp sets flags, str
      does not clobber them), bail when nonzero; on 0 the mutex
      initializer @ 0x082e82f8 runs with (this, attr) and ITS status
      overwrites the recorded 0. The wrapped callees (both still
      unported, decoded from the raw ARM, literals binary-verified in
      osos.dec): settype @ 0x082e84dc (80 bytes) rejects a NULL attr, a
      bad magic at attr+0x00 (literal @ 0x082e852c = 0x4d545841 "MTXA",
      the word pthread_mutexattr_init plants) and kinds outside {0,1,2}
      with 0x1a, else rewrites bits 4..5 of the attr+0x06 halfword with
      0x30 & (kind << 4) and returns 0 - kind 0 CLEARS the type field
      (recorded at mutex+0x0e by the PosixMutex layout). The
      initializer @ 0x082e82f8 (140 bytes) returns 0x1a for a NULL
      mutex or a bad attr magic (literal @ 0x082e8384 = "MTXA"); a NULL
      attr selects the process-wide default attr object @ 0x089cfcbc
      (literal @ 0x082e8388; its static image bytes are "act\0", so it
      is lazily run through pthread_mutexattr_init first); it allocates
      the inline semaphore via the kernel-object allocator @ 0x0808b1c0
      (object type 3; handle to mutex+0x14, failure -> 0x27), copies
      the whole attr+0x04 word to mutex+0x0c, zeroes mutex+0x04/+0x08
      and the +0x12 halfword, and plants the live-object magic
      0x4d555458 "MUTX" (literal @ 0x082e838c - distinct from the
      0x4d545853 "MTXS" static-init magic the lock/unlock pair @
      0x082e8390/0x082e83d8 match for lazy init, literals @
      0x082e83d4/0x082e8470) at mutex+0x00. Deviations: both callees
      ride the CXX_MUTEX_SETTYPE_INIT_OPS dispatch slots (the
      facade_registry_walk fixed-address pattern) - the wired defaults
      transmute 0x082e84dc/0x082e82f8 on target (hook-ready); the host
      defaults are behavioral models of the decoded bodies with one
      simplification: the mask-ROM kernel allocator is replaced by a
      fixed nonzero sentinel handle at mutex+0x14. attr is *mut usize,
      matching cxx/mutex.rs's two-word stack scope model. The port is
      wired as cxx/mutex.rs's CXX_MUTEX_CONSTRUCT_OPS.mutex_init
      default (that module's documented "a later port replaces its
      default" pattern; its defaults test now asserts the end-to-end
      initialized wrapper). Host tests: settype receives (attr,
      0=NORMAL); a settype failure is recorded at this+0x18 with the
      mutex bytes untouched and init never called; on success init
      receives (this, attr), observes the already-planted 0 status, and
      its status (tested with 0x27 and 0) overwrites it; end-to-end
      with the default models from a reference-initialized attr
      pre-set to kind 2 - the type bits return to NORMAL, the attr+0x04
      word lands at mutex+0x0c, MUTX/zeroed fields/sentinel handle
      planted, status 0, bytes past the 8-byte attr and the +0x10
      halfword untouched; NULL attr records 0x1a with no stores; the
      default settype model matches an independent byte-level reference
      across kinds 0..=2 and +0x06 seeds (0x0000, 0xffff, 0xffcf,
      0x0008, 0xaa95) and rejects kind 3/bad magic/NULL with 0x1a and
      no stores; the default init model rejects NULL mutex/bad magic
      with 0x1a and lazily initializes the default attr object for a
      NULL attr. match.py (exit 1, diff expected): same
      save-this-in-r4/attr-in-r5 -> settype(attr, #0) -> cmp/str
      [r4,#0x18] -> popne -> init(this, attr) -> str [r4,#0x18] -> pop
      structure; the diff is the crate fp frame and the seam's ldr/blx
      static dispatch in place of the direct bl 0x082e84dc/0x082e82f8.

  - address: 0x083866c0
    ghidra: FUN_083866c0
    name: sqlite_value_new
    size: 44
    sig: "Mem *sqlite_value_new(sqlite3 *db)"
    module: sqlite/value_new
    status: ported
    vars: {param_1: db, iVar1: value, r4: db, FUN_08374998: db_malloc_zero}
    notes: >-
      SQLite 3.5.9's sqlite3ValueNew (vdbemem.c) - the constructor for a
      fresh NULL Mem/sqlite3_value. 44 bytes, no literal pool
      (0x083866c0..0x083866ec; the sqlite3ValueSetStr wrapper @
      0x083866ec follows immediately). 5 bl call sites, binary-scanned:
      0x08376718 in sqlite_error (ported, sqlite/error.rs - lazily
      creates the connection's cached pErr); 0x08386578 and 0x08386640
      in FUN_08386524 (408 bytes - formats a message into a fresh value,
      installs it with sqlite3ValueSetStr @ 0x083866ec, and on a NULL
      here latches db->mallocFailed itself, frees through
      sqlite3ValueFree @ 0x08386504, returns SQLITE_NOMEM=7);
      0x082bea64 in FUN_082be9e8 (232 bytes - a user-function result
      bridge: with the callback word at +0xc0 set it builds a value,
      fills it with sqlite3ValueSetStr, converts with sqlite3ValueText @
      0x08386718, hands the text to the callback, frees the value); and
      0x0838fe98 in FUN_0838fe88 (120 bytes - the only caller passing
      db == NULL). Algorithm: allocate a zeroed 0x28-byte Mem on the
      connection with sqlite3DbMallocZero @ 0x08374998 (PORTED,
      sqlite::mem::db_malloc_zero, called directly - the only callee)
      and return NULL verbatim when it fails; the sticky
      db->mallocFailed latch is the allocator's job, so the whole OOM
      contract is the NULL return. On success stamp the NULL datum:
      flags = MEM_Null (1) at +0x1c (strhne), type = SQLITE_NULL (5) at
      +0x1e (strbne), owning connection at +0x10 (strne) - every other
      field stays zero from the allocator's zero-fill. A NULL db is
      legal: db_malloc_zero goes straight to the heap with nothing to
      latch, and the back-pointer store lands NULL. Deviations: no
      dispatch seam in this file (the allocator is ported and called
      directly). The port is the shipped default of the
      SQLITE_VALUE_NEW slot in sqlite/error.rs (that module's documented
      "the real port should replace this default when it lands" pattern,
      same swap expr_new made when expr_delete landed); the old
      OOM-shaped stub missing_value_new is retained for host tests.
      Verified by src/sqlite/value_new.rs::tests (success shape across
      buffer seeds 0x00/0xa5/0xff/0x5a compared byte-for-byte against an
      independent zero-fill+stamp reference model, one 0x28-byte
      allocation logged, mallocFailed clear; OOM returns NULL with the
      latch set by the ported allocator; an already-failed connection
      short-circuits without touching the heap; db == NULL allocates
      with a NULL back-pointer; db == NULL with a down heap returns NULL
      with the heap still tried, no flag to latch) and
      src/sqlite/error.rs::tests::the_shipped_value_new_port_preserves_the_oom_effect
      (end-to-end: shipped default is the port, forced allocator failure
      leaves pErr NULL and errCode untouched with mallocFailed latched).
      match.py not run (host-only verification this round).

  - address: 0x083866ec
    ghidra: FUN_083866ec
    name: sqlite_value_set_str
    size: 44
    sig: "void sqlite_value_set_str(Mem *value, int n, const void *z, u8 enc, void *x_del)"
    module: sqlite/value_set_str
    status: ported
    vars: {param_1: value, param_2: n, param_3: z, param_4: enc, param_5: x_del, r12: n, FUN_0838c158: sqlite3VdbeMemSetStr}
    notes: >-
      SQLite 3.5.9's sqlite3ValueSetStr (vdbemem.c) - a pure
      register-shuffle wrapper with a NULL guard, 44 bytes
      (0x083866ec..0x08386718; sqlite3ValueText @ 0x08386718 follows
      immediately). The outgoing registers are rebuilt
      (mov r12,r1 / mov r1,r2 / mov r2,r3), the fifth argument xDel is
      pulled from the caller's stack into the outgoing slot
      (ldr r3,[sp,#0x8] / strne r3,[sp,#0x0]), and only when value is
      non-NULL does control reach sqlite3VdbeMemSetStr @ 0x0838c158
      (blne) with (value, z, n, enc, xDel) - the wrapper's n and z
      swapped into the callee's order. A NULL value skips the call
      entirely; nothing else is validated (a NULL z with a live value
      is forwarded - the callee turns that into MemSetNull @
      0x0838c13c). No epilogue write to r0, so the callee's
      SQLITE_OK/SQLITE_NOMEM leaks out (the NULL path leaks the 0 left
      by cmp r0,#0, which is SQLITE_OK anyway); no caller reads it and
      the port is void like the decompile. The callee is now ported as
      sqlite/vdbe_mem_set_str and is the shipped default of the shared
      SQLITE_VDBE_MEM_SET_STR dispatcher. Its semantics are: base
      flags MEM_Str (0x2), or MEM_Blob (0x10) when enc == 0; n < 0
      measures z in place (NUL scan for UTF-8, double-NUL in two-byte
      steps otherwise) and sets MEM_Term (0x20); xDel == (void*)-1
      (SQLITE_TRANSIENT) grows the Mem (0x0838bdb0) and copies,
      anything else releases old content (0x0838c04c) and marks
      MEM_Static (0x80, xDel NULL) or MEM_Dyn (0x40) with xDel at +0x20
      and z at +0x14; n lands at +0x18, a zero enc becomes 1,
      flags/enc/type (SQLITE_TEXT=3 / SQLITE_BLOB=4) at
      +0x1c/+0x1f/+0x1e, non-UTF-8 recoded through 0x0838be98. 7 bl
      call sites, binary-scanned: 0x082bea7c in FUN_082be9e8
      (user-function result bridge: value from sqlite3ValueNew,
      n=param_3, z=param_2, enc=1, xDel=0 = SQLITE_STATIC); 0x0837676c
      in sqlite_error (PORTED, sqlite/error.rs); 0x083865a8 in
      FUN_08386524 (n=-1, enc=1, xDel = pool literal 0x0838581c);
      0x0838cb68 and 0x0838cbc8 in FUN_0838cb28 (n=-1, enc=1, same
      0x0838581c destructor literal - both pool words read from
      osos.dec); 0x0838fec4 in FUN_0838fe88 (n=-1, caller-chosen enc,
      xDel=0); 0x08390318 in FUN_08390294 (n=-1, enc=1, xDel=0). The
      port is the shipped default of the SQLITE_VALUE_SET_STR slot in
      sqlite/error.rs (that module's documented "the real port should
      replace this default when it lands" pattern, the same swap
      sqlite_value_new made); the old no-op stub missing_value_set_str
      is retained for host tests. Verified by
      src/sqlite/value_set_str.rs::tests (NULL value never reaches the
      callee across n/enc/xDel shapes; non-NULL forwards exactly one
      call with n/z swapped against an independent reference model -
      length -1 vs explicit, STATIC/TRANSIENT/pool-literal xDel, NULL
      z, blob and UTF-16 encodings; callee result discarded for both
      SQLITE_OK and SQLITE_NOMEM; shipped error.rs slot default is the
      port while the callee seam ships the SQLITE_NOMEM stub).
      match.py not run (host-only verification this round).

  - address: 0x08386718
    ghidra: FUN_08386718
    name: sqlite_value_text
    size: 160
    sig: "const void *sqlite_value_text(Mem *value, u8 enc)"
    module: sqlite/value_text
    status: ported
    vars: {param_1: value, param_2: enc, uVar1: flags, uVar2: z, iVar3: make_writeable_rc, bVar4: aligned, FUN_0838bbb4: sqlite3VdbeMemExpandBlob, FUN_083869f4: sqlite3VdbeChangeEncoding, FUN_0838bb30: sqlite3VdbeMemMakeWriteable, FUN_0838bfb0: sqlite3VdbeMemNulTerminate, FUN_0838c32c: sqlite3VdbeMemStringify}
    notes: >-
      SQLite 3.5.9's sqlite3ValueText (vdbemem.c) - the text extractor
      that renders a Mem/sqlite3_value as a NUL-terminated string in a
      requested encoding and returns its z pointer. 160 bytes
      (0x08386718..0x083867b8; follows sqlite3ValueSetStr @ 0x083866ec
      immediately). Verified line-for-line against the public 3.5.9
      source, including the final pVal->enc == (enc &
      ~SQLITE_UTF16_ALIGNED) guard, which IS upstream (only the
      asserts are compiled out of the firmware). Algorithm: NULL value
      or MEM_Null (0x1) yields NULL; otherwise (MEM_Blob >> 3) ==
      MEM_Str promotes a blob's flag into MEM_Str (0x2) at +0x1c so
      blobs take the recode path; MEM_Zero (0x800, zero-tail blob) is
      materialized through the inlined expandBlob(P) macro guard
      (sqlite3VdbeMemExpandBlob @ 0x0838bbb4); with MEM_Str set
      (re-read after expansion) the string is recoded to enc & ~8 by
      sqlite3VdbeChangeEncoding @ 0x083869f4, an SQLITE_UTF16_ALIGNED
      (8) request with an odd z at +0x14 must survive
      sqlite3VdbeMemMakeWriteable @ 0x0838bb30 (the only callee whose
      return code is checked) or the call fails to NULL, then
      sqlite3VdbeMemNulTerminate @ 0x0838bfb0 guarantees the double
      NUL; without MEM_Str (int/real) sqlite3VdbeMemStringify @
      0x0838c32c renders with the FULL enc (aligned bit not masked,
      mov r1,r5). The text is returned only when the enc byte at +0x1f
      matches enc & ~8 afterwards - a recode that could not land (OOM
      in a callee) leaves the byte stale and NULL comes out instead of
      a mis-encoded buffer. Callee identities cross-checked against
      3.5.9 and their decompiles: 0x0838bbb4 grows via 0x0838bdb0,
      zero-fills the tail, n += i, clears MEM_Zero|MEM_Term (& 0xf7df);
      0x0838bb30 expands zero-blobs then copies MEM_Str|MEM_Blob (0x12)
      content off zMalloc (+0x24); 0x0838bfb0 grows n+2 and appends the
      double NUL when MEM_Str without MEM_Term; 0x0838c32c grows 0x20,
      prints, sets enc=1 and MEM_Str|MEM_Term (0x22), recodes through
      0x083869f4. 8 bl call sites, binary-scanned: 0x082bea9c in
      FUN_082be9e8 (user-function result bridge - converts the value
      filled by sqlite3ValueSetStr before the callback); 0x0837d5e4,
      0x0837d5fc, 0x0837d630, 0x0837d63c in sqlite3MemCompare @
      0x0837d47c (both operands to pColl->enc at +0x04 and back);
      0x083864d4 in sqlite3ValueBytes @ 0x083864c0; 0x0838a718 in
      sqlite3VdbeExec @ 0x08386ecc (enc = SQLITE_UTF8 = 1); 0x0838fed4
      in FUN_0838fe88. Deviations: all five callees are unported and
      dispatched through statics whose defaults are documented stubs
      reproducing the original's failure/no-op end states (the
      sqlite/value_set_str.rs seam pattern); enc is typed u8 like
      upstream - the firmware's full-width bic/cmp on r1 are identical
      for the zero-extended arguments every observed call site passes.
      Verified by src/sqlite/value_text.rs::tests (NULL value short
      circuit; a 10-flag-shape x 6-encoding x 2-initial-enc x 2-z-parity
      x 5-callee-effect matrix - 2400 cases - compared against an
      independent reference model for result pointer, exact callee
      sequence, and final flags/enc; shipped stub defaults reproduce
      the documented no-recode/recode-OOM/stringify-OOM/make-writeable-OOM
      end states; slot defaults are the documented stubs). match.py
      (exit 1, diff expected): same NULL guard -> MEM_Null test ->
      and/orr/strh blob promotion -> MEM_Zero test + call -> flags
      reload -> MEM_Str path split with bic-masked change-encoding and
      full-enc stringify -> ldrb/cmp/ldreq final check structure; the
      diff is the crate fp frame and the seams' ldr/blx static dispatch
      in place of the direct bls.

  - address: 0x08003d44
    ghidra: FUN_08003d44
    name: task_delay
    size: 44
    sig: "void task_delay(u32 task, u32 ticks)"
    module: kernel/task_delay
    status: ported
    vars: {param_1: task, param_2: ticks, local_14: request_selector, local_10: request_task, uStack_c: request_ticks, local_8: scratch_pointer, auStack_34: scratch, FUN_08003660: gateway_dispatch}
    notes: >-
      RTXC timed task-delay gateway stub, service 0x14 (20): builds a
      0x34-byte frame — pushed-lr slot, uninitialized 32-byte scratch at
      sp+4..sp+0x24, then the 4-word request {0x14, task (r0), ticks (r1),
      scratch_pointer = sp+4} — and hands &request to the foreign gateway
      dispatcher 0x08003660, returning void (nothing read back). osos
      mirror of mask ROM 0x22003d44, aliased by thunk 0x08037e88
      (kernel/task_lock.rs rom_task_delay: 20 osos bl callers — 18 plain
      and two blne — plus task_sleep's bne tail; observed r0 = 0 = current
      task and r1 = small tick count 1/0x64) and standing
      behind kernel/task.rs task_sleep's nonzero-ticks path. The mirror's
      only direct bl caller is the ROM tick-housekeeping path @ 0x08002dc4
      (delays the current task 100 ticks). Reference C:
      decomp/c/000/08003d44_FUN_08003d44.c; assembly 0x08003d44..0x08003d70.
      Deviations (heap/gateway_service1.rs precedent): indirect dispatch
      through the shared volatile heap::rom_task_start::ROM_GATEWAY_OPS
      hook (unwired default spins); scratch and pushed-lr slot modeled with
      MaybeUninit, never written. Host tests prove one dispatch, the exact
      request words, scratch geometry (word 3 = &request - 0x20), edge
      argument forwarding (task u32::MAX, ticks 0/u32::MAX), and void
      survival of a full frame clobber. match.py structural diff expected
      (hook indirection). Rewiring opportunity (deliberately NOT done here
      — src/runtime is another porter's territory): runtime/i2c0_idle.rs
      and runtime/i2c0_transfer.rs reach this routine as task_delay(0, 1)
      through their target-default seams (ROM_TASK_DELAY = 0x08003d44);
      those defaults can now bind the ported kernel::task_delay symbol
      instead of the raw ROM address.

  - address: 0x082b3b5c
    ghidra: FUN_082b3b5c
    name: cg_block_bind_registers
    size: 216
    sig: "void cg_block_bind_registers(CgCodegen *codegen)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0->r7: codegen
      iVar5/r6: block (codegen->current_block, codegen +0x208
        CG_CODEGEN_CURRENT_BLOCK, published by cg_block_emit)
      puVar4/r4: cell (register-copy list cursor, proc +0x1c
        CG_PROC_REG_COPIES; cells are {next, reg}, the
        CG_VREG_LIST_NEXT/CG_VREG_LIST_REG layout)
      r8: constant 1 (the bit-shift base, mov r8,#0x1)
      iVar2/r0: reg (the cell's register), then binding
      uVar3/r1: set word index (reg_no >> 5), then the new flags value
      uVar1/r2: set bit (1 << (reg_no & 0x1f)), then the rebind call's
        reg argument (reloaded from the cell, ldr r2,[r4,#0x4])
      r3: live-in (block +0x1c CG_BLOCK_LIVE_IN) / live-out (block
        +0x20 CG_BLOCK_LIVE_OUT) bitset record, then the bitmap word
      r5: binding (reg +0x08 CG_VREG_BINDING)
      FUN_083673b0: binding_unlink slot (CG_BLOCK_BIND_OPS; 64 bytes,
        8 bl sites - the runtime's intrusive doubly-linked-list REMOVE)
      FUN_082b5234: binding_rebind slot (CG_BLOCK_BIND_OPS; 100 bytes,
        13 bl sites, all in the register-binding machine)
      FUN_08367358: binding_push slot (CG_BLOCK_BIND_OPS; 56 bytes,
        8 bl + 2 tail b sites - the runtime's list PUSH-HEAD)
    notes: >-
      1 bl call site: 0x082c0ef0 inside cg_block_emit (PORTED,
      codegen/ir), called right after the use-list tail table is freed
      - the block-entry register binder, the bind_registers slot of
      CG_BLOCK_EMIT_OPS. The JIT's block-entry binder: re-seats the
      bindings of every register copy whose register crosses the
      current block's boundary. Walks the procedure's register-copy
      list (proc +0x1c); per cell tests the register number (vreg
      +0x10 CG_VREG_NO) against the block's live-IN bitset (block
      +0x1c) and, only when that fails, the live-OUT bitset (block
      +0x20); both bitset records have word 0 = capacity and the
      bitmap from +0x04 (CG_BITSET_BITS; word reg_no >> 5, bit
      1 << (reg_no & 0x1f)). Live-IN: the register's binding (vreg
      +0x08) is unlinked from its current anchor (binding +0x08
      CG_BINDING_ANCHOR) through FUN_083673b0, re-pointed at the
      register through FUN_082b5234(codegen, binding, reg), pushed
      onto the codegen's pending-bindings anchor at +0x200
      (CG_CODEGEN_PENDING_BINDINGS; {head +0x200, tail +0x204},
      drained from the tail by the block-exit flush FUN_082ccae4)
      through FUN_08367358, and its flags word (binding +0x18
      CG_BINDING_FLAGS) gains bit 0x200 CG_BINDING_FLAG_BLOCK_ENTRY
      (orr). Live-OUT only: the same unlink/rebind/push sequence (the
      original's second full copy at 0x082b3bec-0x082b3c20) but the
      flag is CLEARED (bic). In neither set: the cell is skipped
      entirely (beq 0x082b3c24 straight to the list advance). The
      flags word is re-read AFTER the three calls in both branches
      (ldr r0,[r5,#0x18] @ 0x082b3bcc/0x082b3c18) - FUN_082b5234
      itself rewrites flags bits 0x100/0x200 - and the register is
      reloaded from the cell for the rebind argument; both reloads are
      kept verbatim (through the ops seam a replacement could legally
      rewrite the cell/flags). Deviations: the three callees route
      through the swappable CG_BLOCK_BIND_OPS table with no-op
      defaults (the CgBlockEmitOps/default_cg_graph_pass deviation),
      so with the defaults wired the walk, bit tests and flag updates
      run exactly as the original's but no list re-linking happens;
      porting a callee replaces its slot. Ghidra folds the two call
      sequences into one shared LAB_082b3c20 and drops the second
      register reload; the raw disassembly's two copies are
      authoritative and the port mirrors them. SEAM DECISION /
      REWIRING OPPORTUNITY: CG_BLOCK_EMIT_OPS.bind_registers KEEPS
      its documented no-op default - wiring this port in would turn
      cg_block_emit's default pipeline into a half-running binder
      (walk and flag stores live, the three callees no-ops), worse
      than no binder. Rewire once FUN_083673b0 / FUN_082b5234 /
      FUN_08367358 are ported and wired as the CG_BLOCK_BIND_OPS
      defaults. match.py (exit 1, diff expected): the walk
      (codegen +0x208, block +0x04, proc +0x1c, cell +0x04, vreg
      +0x10, the >>5/&0x1f bit math, bitmap reads at record +0x04),
      both full unlink/rebind/push copies, the +0x200 push anchor and
      the post-call flags orr/bic #0x200 at binding +0x18 all line up;
      the diff is the crate fp prologue, the ops-table ldrd/blx
      dispatch in place of the direct bls (the documented scheme),
      LLVM merging the list advance into the live-in path, and its
      lsl/lsr re-expressions. Host tests:
      src/codegen/ir.rs::block_bind_registers_reseats_boundary_copies_in_list_order
      (live-in precedence when a register is in BOTH sets, live-out-
      only clearing, neither-set skip, unlink/rebind/push order per
      copy, all pushes on the +0x200 anchor, flags updated from the
      POST-rebind value via a flags-rewriting rebind fake),
      ::block_bind_registers_with_defaults_walks_and_updates_flags_only
      (no-op defaults: flags still orr/bic'd, anchors never re-linked,
      walk terminates),
      ::block_bind_registers_empty_copy_list_is_a_no_op (NULL copy
      list: no calls, pending anchor and published block untouched)
      and ::block_bind_registers_seams_stay_wired_to_the_defaults.

  - address: 0x082b3c34
    ghidra: FUN_082b3c34
    name: cg_binding_acquire
    size: 204
    sig: "CgBinding *cg_binding_acquire(CgCodegen *codegen, CgVirtualReg *reg, u32 mask)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r5: codegen (binding-pool owner)
      param_2/r1->r3: reg, then its phi-web parent
      param_3/r2: mask (zero expands to all resource bits)
      r4: binding candidate
      r1: binding resource index, then candidate's bound register
      FUN_08367390: binding_promote seam (refreshes a reused binding in its anchor)
      FUN_082c5c58: cg_binding_release (ported; releases an eligible active binding)
      FUN_083673b0: cg_binding_unlink (ported direct default)
      FUN_08367358: binding_push seam (inserts the selected binding at active-head)
    notes: >-
      Verified call count: 11 plain bl sites, 0 predicated bl sites:
      0x082c10a8, 0x082c1148, 0x082c11f0, 0x082c1620, 0x082c1678,
      0x082cb754, 0x082cbc88, 0x082cc8c0, 0x082cc8e8, 0x082d78e0,
      0x082d814c. No aligned osos.dec data word references 0x082b3c34,
      so there is no discovered indirect dispatch. Selects a binding with
      mask bit (binding +0x0c) set. Zero mask becomes 0xffffffff. First
      accepts and promotes the requested register's phi-web parent's
      binding only when its bound register points back to that same parent.
      Otherwise walks the free anchor {head +0x1f0, tail +0x1f4} from tail
      to head, then walks the active anchor {head +0x1f8, tail +0x1fc};
      an active match is released before the normal free-unlink then
      active-push transfer. The raw body is exactly 204 bytes:
      0x082b3c34..0x082b3cfc, followed by distinct FUN_082b3d00.
      Deliberate deviation: unported FUN_08367390/FUN_08367358 use the
      CG_BINDING_ACQUIRE_OPS no-op defaults; ported FUN_082c5c58 is now
      the release default but retains its documented no-op direct-callee
      FUN_083685f0 and list-push FUN_08367358 seams. Ported
      FUN_083673b0 remains the unlink default. Host tests cover a
      zero-mask bit-31 parent reuse, an out-of-range bit-32 free-tail
      skip before bit-31 selection, active-list release fallback, call
      order and seam defaults. match.py exit 1 diff dispatch replaces
      stock direct bl calls, while LLVM may merge the two tail-to-head
      filter loops.

  - address: 0x082c5c58
    ghidra: FUN_082c5c58
    name: cg_binding_release
    size: 196
    sig: "void cg_binding_release(CgCodegen *codegen, CgBinding *binding)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r5: codegen (binding-pool owner; current block +0x208,
        free anchor +0x1f0, active anchor +0x1f8, use-head table +0x21c)
      param_2/r4: binding (binding +0x10 bound register, +0x18 flags)
      r2: bound register (binding +0x10), then its virtual-register number
        (+0x10 CG_VREG_NO)
      r0/r1: current block live-OUT bitset and its bitmap word, then the
        queued-use head and final flags word
      FUN_083685f0: unported direct callee; identity deliberately unrecovered
        (called with codegen, binding, bound register)
      FUN_083673b0: cg_binding_unlink (ported active-list removal)
      FUN_08367358: unported binding_push (free-list insertion)
    notes: >-
      Verified call count: 9 bl sites — 7 plain bl at 0x082b3cf8,
      0x082c1518, 0x082cb808, 0x082ccaa8, 0x082ccab4, 0x082ccb18 and
      0x082d783c; two are predicated bleq at 0x082becc8 and 0x082c1588.
      No aligned osos.dec data word references 0x082c5c58. Raw extent is
      exactly 196 bytes, 0x082c5c58..0x082c5d18, followed by distinct
      FUN_082c5d1c. The predicated callers already prove their candidate
      binding must be releasable before entry; the function itself has no
      NULL guard. It verifies binding +0x10 against the bound register's
      +0x08 back-pointer. For a consistent pair it invokes FUN_083685f0
      only when binding flags bit 0x100 is set and the virtual register is
      live-OUT in the published block's +0x20 bitset or has a non-NULL
      codegen +0x21c use-list head. It then unconditionally clears binding
      +0x10; an active-anchor binding is unlinked from +0x1f8 and pushed
      onto +0x1f0; and it clears flags 0x100 and 0x200. The raw
      bic-0x200/and-0x100-(flags>>1)/bic-0x100 sequence makes the apparent
      0x200-to-0x100 shift dead, so the exact final flags are flags & ~0x300;
      Ghidra drops the first clear. Deliberate deviation: FUN_083685f0 and
      FUN_08367358 dispatch through CG_BINDING_ACQUIRE_OPS inert defaults
      until their own ports; their identities are not invented. Host tests
      prove queued-use release and active-to-free order, bit-31 live-OUT
      release without dereferencing the use table, stale back-pointer
      short-circuiting before liveness reads, unconditional detachment, and
      the raw 0x300 flag clear. match.py exit 1 diff expected: stock direct
      bl/blne calls become ops-table dispatch; same back-pointer guard,
      live-OUT/use-head split, active-anchor transfer, and terminal flag
      mask remain.


  - address: 0x082d817c
    ghidra: FUN_082d817c
    name: cg_consume_register_use
    size: 76
    sig: "CgBinding *cg_consume_register_use(CgCodegen *codegen, CgVirtualReg *reg, u32 resource, CgInst *inst)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0: codegen (owner of the per-register use-list heads at +0x21c)
      param_2/r1: reg (virtual register; +0x10 is its use-list index)
      param_3/r2: resource (hardware-resource index, converted to 1 << resource)
      param_4/r3: inst (instruction record to remove from the use list)
      r12: link (address of the head slot, then each preceding record's next slot)
      lr: use record (the `{next, inst}` cursor)
      r4: candidate instruction (use record +0x04)
      FUN_082d8140: unported tail helper (binding acquisition/rebind and materialization)
    notes: >-
      Verified call count: 10 plain bl sites, 0 predicated bl sites:
      0x082c0fa0, 0x082c0fb4, 0x082c1018, 0x082c1048, 0x082c1078,
      0x082c10ec, 0x082c1100, 0x082c1194, 0x082c11a8, 0x082cbc28.
      Raw extent is exactly 76 bytes, 0x082d817c..0x082d81c4, followed
      by distinct FUN_082d81c8. It computes the ARM-register-shift mask
      `1 << resource` (zero when resource >= 32), walks codegen +0x21c
      at index reg +0x10, and bypasses only the FIRST `{next, inst}`
      record whose inst word equals r3. The detached record is not
      cleared; a missing record leaves the chain unchanged. It always
      tail-branches to FUN_082d8140 with (codegen, reg, mask). The raw
      tail's observed operations are acquisition, rebind and required
      materialization, but its identity is not invented. Deliberate
      deviation: that unported tail dispatches through
      CG_CONSUME_REGISTER_USE_OPS with an inert default after the exact
      unlink. Host tests
      cg_consume_register_use_unlinks_the_first_matching_record_then_binds,
      cg_consume_register_use_preserves_a_missing_record_and_zeroes_large_shift_masks,
      and cg_consume_register_use_seam_keeps_its_unported_tail_inert
      prove middle-chain bypass, first-match behavior, missing-record
      preservation, ARM large-shift semantics, forwarded tail result and
      the documented default.
  - address: 0x082b5234
    ghidra: FUN_082b5234
    name: cg_binding_rebind
    size: 100
    sig: "void cg_binding_rebind(CgCodegen *codegen, CgBinding *binding, CgVirtualReg *reg)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0: codegen (never read - the register-binding machine's
        uniform (codegen, binding, reg) call shape; kept for the
        signature)
      param_2/r1: binding
      param_3/r2: reg
      iVar1/r0: old (the binding's current register, binding +0x10
        CG_BINDING_REG), then the old register's phi-web parent, the
        flags word, reg->binding and the rival binding's reg
      r3: the new register's phi-web parent (reg +0x04 CG_VREG_PARENT),
        then the 0x200 mask of the dead shift-insert
      binding +0x18: flags (CG_BINDING_FLAGS; bit 0x100 =
        CG_BINDING_FLAG_BOUND, bit 0x200 = CG_BINDING_FLAG_BLOCK_ENTRY)
    notes: >-
      13 bl call sites, all inside the register-binding machine:
      0x082b3bbc/0x082b3c08 in cg_block_bind_registers (PORTED,
      codegen/ir) plus 0x082c10bc, 0x082c115c, 0x082c1204, 0x082c1634,
      0x082c1688, 0x082cb768, 0x082cbc9c, 0x082cc8d4, 0x082cc8fc,
      0x082d78f4, 0x082d8160 in the hardware-register allocators. The
      binding machine's binding<->register re-point, a leaf. Step 1:
      if the binding's register (binding +0x10) is not reg, install it
      (str r2,[r1,#0x10] ahead of the old-NULL test), and when the old
      register is NULL or the two registers' phi-web parents (+0x04,
      CG_VREG_PARENT) differ, rewrite the flags word (binding +0x18):
      clear 0x100, then clear 0x200 and OR in 0x200 & (flags << 1)
      computed from the ALREADY-cleared word - the bic #0x100 at
      0x082b5264 precedes the and r3,r3,r0,lsl #1 at 0x082b5268, so
      the bit-move is provably dead and the net effect is flags &=
      ~0x300 (a latent firmware quirk: the intended 0x100 -> 0x200
      move never happens; Ghidra folds it to & 0xfffffcff). The port
      mirrors the instruction sequence verbatim; LLVM folds the dead
      insert to a single bic #0x300. Step 2: back-point reg +0x08
      (CG_VREG_BINDING) at the binding UNLESS the register already
      carries a self-consistent binding (reg +0x08 non-NULL whose
      +0x10 points back at the register), left in place via bxeq lr.
      The port is NOT wired: CG_BLOCK_BIND_OPS::binding_rebind keeps
      its documented no-op default (cg_block_bind_registers' seam
      decision). REWIRING OPPORTUNITY (deliberately NOT done here):
      this slot can now default to the ported cg_binding_rebind once
      siblings FUN_083673b0 (binding_unlink) and FUN_08367358
      (binding_push) are also ported - and only then should
      CG_BLOCK_EMIT_OPS::bind_registers bind cg_block_bind_registers.
      match.py (exit 1, diff expected): the repoint guard/store, the
      NULL/parent-compare flags condition, the +0x18 flags rewrite,
      the back-pointer guard (reg +0x08, rival +0x10) and final store
      all line up; the diff is the crate fp prologue, LLVM folding the
      dead shift-insert into bic #0x300 (the documented quirk), a
      swapped ldr order in the parent compare and a strne merge of the
      guarded store. Host tests:
      src/codegen/ir.rs::binding_rebind_web_crossing_repoints_and_clears_flags
      (NULL codegen proves the argument unread; install, flags 0x700
      -> 0x400, back-pointer on a NULL slot),
      ::binding_rebind_same_web_keeps_flags (matching parents skip the
      flags rewrite only),
      ::binding_rebind_first_bind_clears_flags (NULL old register
      forces the clear with no parent compare),
      ::binding_rebind_same_register_skips_the_repoint (old == new
      short-circuits step 1, step 2 still runs) and
      ::binding_rebind_respects_a_consistent_back_pointer
      (self-consistent rival kept via the bxeq guard, stale rival
      replaced).

  - address: 0x082b5298
    ghidra: FUN_082b5298
    name: atoi_dead_sign
    size: 56
    sig: "i32 atoi_dead_sign(const u8 *s)"
    module: strto/atoi_dead_sign
    status: ported
    renames:
      param_1/r0: s
      iVar1/r1: "acc (wrapping decimal accumulator, acc = acc*10 + digit
        via the add-r1-r1-r1-lsl-2 / add-r1-r2-r1-lsl-1 / sub-0x30
        shift-add sequence)"
      iVar2/ip: "sign (1, mvneq #0 sets -1 when *s == '-')"
      r2: "byte (loaded twice from r0: once for the sign test, once
        for the digit loop - the pointer is never advanced past '-')"
    notes: >-
      Verified call count: 29 bl sites, 0 predicated bl, plus one beq
      tail-branch @ 0x082d0984 (30 control-flow transfers total); no
      data word in osos references the address, so it is never
      dispatched indirectly. Decimal atoi with a DEAD sign flag: the
      '-' compare sets sign = -1 but the pointer is not advanced, so
      a leading '-' fails the (byte - '0') <= 9u digit test
      immediately, acc stays 0 and 0 * -1 == 0. The function can
      never return a negative value from a '-' string (only from
      unsigned wrap of long digit runs, e.g. "4294967295" -> -1).
      Callers such as FUN_0837f25c defensively negate a negative
      return that a sign can never cause. The port keeps the flag and
      the final multiply verbatim. Ghidra's decompile of this
      function is accurate (size 60 includes the first word of the
      next function FUN_082b52d4; the true extent is 56 bytes, ending
      at bx lr @ 0x082b52d0). Distinct from libc atoi (ADS strtol
      wrapper @ 0x0802f990) and from atoi_decimal @ 0x080e9974 (no
      sign flag at all). match.py (exit 1, diff expected): same byte
      load, unsigned digit-range test, shift-add x10 accumulation and
      loop back-branch; LLVM emits a preheader check plus do-while
      loop and folds the mvneq+mul sign into cmp #0x2d / rsbeq
      (negate-when-'-'), semantically identical, plus the crate fp
      prologue. Host tests: leading_minus_is_dead ("-42" -> 0,
      "42-99" -> 42, result never negative from '-'),
      every_byte_uses_the_unsigned_decimal_range, overflow_wraps
      (mod 2^32), digit_boundaries_and_long_runs, digits_then_junk,
      random_vs_reference.

  - address: 0x083673b0
    ghidra: FUN_083673b0
    name: cg_binding_unlink
    size: 64
    sig: "void cg_binding_unlink(u8 *anchor, CgBinding *node)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0: anchor (the {head +0x00, tail +0x04} list anchor the
        node hangs from - a hardware-register descriptor anchor's list
        head at anchor +0x10, or one of the codegen's embedded anchors
        at +0x1f0/+0x1f8/+0x200 CG_CODEGEN_PENDING_BINDINGS)
      param_2/r1: node (the cg_binding_t list node - next +0x00
        CG_BINDING_NEXT toward the head/push end, prev +0x04
        CG_BINDING_PREV toward the tail/drain end, anchor back-pointer
        +0x08 CG_BINDING_ANCHOR)
      r3: the node's next link (ldr r3,[r1,#0x0])
      r2: the node's prev link (ldr r2,[r1,#0x4]), reloaded for the
        tail-side fixup (ldr r2,[r1,#0x4]), then the reloaded next
        (ldreq r2,[r1,#0x0])
      r0 (tail-side, non-NULL prev): the reloaded next link
        (ldrne r0,[r1,#0x0]), then constant 0 for the node clears
    notes: >-
      8 bl call sites: 0x082b3bac/0x082b3bf8/0x082b3cac in
      cg_block_bind_registers (PORTED, codegen/ir), 0x082c5cec,
      0x082ccb28/0x082ccb6c in the block-exit flush FUN_082ccae4,
      0x082d7848 in the spill path FUN_082d7870 and 0x0836739c in the
      unlink-then-push move FUN_08367390. Leaf. The runtime's
      intrusive doubly-linked-list REMOVE: unlinks node from its
      {head, tail} anchor and clears the node's next/prev/anchor
      words. Head-side fixup: when the node's next (+0x00, the link
      toward the anchor's head end - the end sibling push
      FUN_08367358 inserts at) is NULL the node sits at the head and
      the anchor's head word takes the node's prev (streq
      r2,[r0,#0x0]); otherwise the next node's prev takes it (strne
      r2,[r3,#0x4]). Tail-side fixup: the node's prev (+0x04) is
      RELOADED (ldr r2,[r1,#0x4]); when NULL the node sits at the
      tail and the anchor's tail word takes the reloaded next (ldreq
      r2,[r1,#0x0] / streq r2,[r0,#0x4]); otherwise the prev node's
      next takes it (ldrne r0,[r1,#0x0] / strne r0,[r2,#0x0]). Then
      prev, next and the anchor back-pointer (+0x08) are cleared in
      that store order (str r0,[r1,#0x4] / str r0,[r1,#0x0] / str
      r0,[r1,#0x8]). Both link reloads are kept verbatim (only a
      anchor, like the rest of the cluster. Now that sibling
      FUN_08367358 (cg_binding_push) is ported, all three block-entry
      helpers are wired as CG_BLOCK_BIND_OPS defaults and
      CG_BLOCK_EMIT_OPS::bind_registers defaults to
      cg_block_bind_registers.
      match.py (exit 1, diff expected): 16 instructions on both
      sides; the head/tail fixup conditions, the link reloads, both
      anchor-word stores and the three node clears all line up; the
      diff is the crate fp prologue, LLVM folding the tail-side
      reload pair into one ldm r1,{r2,r3} (semantically identical:
      prev compared, next stored), a swapped streq/strne order in the
      head-side fixup and the next/prev clear stores exchanged. Host
      tests:
      src/codegen/ir.rs::binding_unlink_unlinks_a_middle_node
      (neighbor re-link in both directions, anchor words untouched,
      node words cleared),
      ::binding_unlink_unlinks_the_head_node (NULL next seats the
      node's prev as the new head, new head's next takes the removed
      node's NULL),
      ::binding_unlink_unlinks_the_tail_node (NULL prev seats the
      node's next as the new tail, mirror fixup),
      ::binding_unlink_unlinks_the_only_node (both anchor words
      drain to NULL) - every test also proves no write lands past
      the +0x08 anchor back-pointer via per-node sentinel words.

  - address: 0x08367358
    ghidra: FUN_08367358
    name: cg_binding_push
    size: 56
    sig: "void cg_binding_push(u8 *anchor, CgBinding *node)"
    module: codegen/ir
    status: ported
    renames:
      param_1/r0: anchor (the intrusive {head +0x00, tail +0x04} list anchor)
      param_2/r1: "node (the CgBinding pushed at anchor's head: next +0x00,
        prev +0x04, anchor back-pointer +0x08)"
      r3: prior tail (anchor +0x04, tests the empty-list path)
      r2: zero for empty-node links, then prior head for non-empty node prev
    notes: >-
      Verified raw extent: 56 bytes, 0x08367358..0x0836738c, followed by
      distinct FUN_08367390 at 0x08367390. Branch census decoding every
      ARM B/BL word in osos.dec found 8 plain bl sites, 0 predicated bl
      sites, and 2 tail b sites: bl at 0x082b3bc8, 0x082b3c14,
      0x082b3cb8, 0x082b6da8, 0x082c5cf8, 0x082ccb34, 0x082ccb78 and
      0x082d7854; tail b at 0x082d66c4 and 0x083673ac. No aligned
      osos.dec data word equals 0x08367358, so no indirect dispatch was
      discovered. Leaf intrusive doubly-linked-list PUSH-HEAD: it reads
      anchor tail +0x04; for an empty list stores node into both anchor
      words and clears node prev +0x04, then in every path clears node
      next +0x00 and stores anchor at node +0x08. For a non-empty list,
      node prev takes the old head, old-head next takes node, and anchor
      head takes node. No NULL guards, matching raw ARM. The port is wired
      as the binding_push default for CG_BLOCK_BIND_OPS and
      CG_BINDING_ACQUIRE_OPS; because all three block-entry helper defaults
      are now ported, CG_BLOCK_EMIT_OPS::bind_registers is also wired to
      cg_block_bind_registers. Host tests cover empty and multi-node head
      insertion, preserved tail, reciprocal links, back-pointers and
      sentinels past node +0x08; the binder default test proves end-to-end
      rebind/unlink/push integration. match.py exit 1 diff is expected:
      LLVM expresses predicated ARM stores as branches and the Rust export
      carries the crate frame.

  # --- C++ null-pointer status ---
  - address: 0x08027940
    ghidra: FUN_08027940
    name: cxx_null_pointer_status
    size: 16
    sig: "u32 cxx_null_pointer_status(const void *pointer)"
    module: cxx/null_pointer_status
    status: ported
    renames:
      param_1/r0: pointer
      uVar1/r0: status
    notes: >-
      Leaf null-status helper. The raw ARM only compares r0 to zero, returning
      4 for null and 0 otherwise, without dereferencing the pointer. Seven
      recovered callers in decoder-related switch cases feed this result into
      status-derived byte arithmetic; the pointer's pointee type therefore
      remains intentionally opaque. Host tests cover both pointer states;
      match.py diff reviewed after the ARM release build.

  # --- C++ u32 slot reset ---
  - address: 0x08027c7c
    ghidra: FUN_08027c7c
    name: reset_u32_slot
    size: 12
    sig: "void reset_u32_slot(u32 *slot)"
    module: cxx/slot_reset
    status: ported
    renames:
      param_1/r0: slot
      r1: zero
    notes: >-
      Leaf slot reset helper. The 12-byte ARM body is mov r1,#0; str r1,[r0];
      bx lr, so it clears one aligned u32 and accesses nothing else. Its sole
      recovered caller invokes it in a batch that clears independent state
      slots. Host test proves the target word becomes zero while adjacent words
      retain their sentinels; match.py diff reviewed after the ARM release
      build.
  - address: 0x08027c88
    ghidra: FUN_08027c88
    name: reset_u32_slot_alias_08027c88
    size: 12
    sig: "void reset_u32_slot_alias_08027c88(u32 *slot)"
    module: cxx/slot_reset
    status: ported
    renames:
      param_1/r0: slot
      r1: zero
    notes: >-
      Byte-identical alias of reset_u32_slot at 0x08027c7c. Its 12-byte ARM
      body is mov r1,#0; str r1,[r0]; bx lr, so it clears one aligned u32 and
      accesses nothing else. The independent exported Rust symbol preserves
      this address-specific hook target; its focused host test verifies the
      target becomes zero while adjacent words retain their sentinels.
  - address: 0x08027c94
    ghidra: FUN_08027c94
    name: reset_u32_slot_alias_08027c94
    size: 12
    sig: "void reset_u32_slot_alias_08027c94(u32 *slot)"
    module: cxx/slot_reset
    status: ported
    renames:
      param_1/r0: slot
      r1: zero
    notes: >-
      Byte-identical alias of reset_u32_slot at 0x08027c7c. Its 12-byte ARM
      body is mov r1,#0; str r1,[r0]; bx lr, so it clears one aligned u32 and
      accesses nothing else. The independent exported Rust symbol preserves
      this address-specific hook target; its focused host test verifies the
      target becomes zero while adjacent words retain their sentinels; match.py
      structural diff reviewed after the ARM release build.


  # --- H.264 CBC decode forwarding adapter ---
  - address: 0x0802ac10
    ghidra: FUN_0802ac10
    name: h264_decode_forwarder
    size: 40
    sig: "void h264_decode_forwarder(u32 arg1, u32 arg2, u32 arg3, u32 arg4, u32 arg5, u32 arg6)"
    module: app/h264_decode_forwarder
    status: ported
    renames:
      param_1/r0: arg1
      param_2/r1: arg2
      param_3/r2: arg3
      param_4/r3: arg4
      param_5/stack: arg5
      param_6/stack: arg6
      FUN_0802ab90: h264_decode_body
    notes: >-
      Six-word adapter for the CBC decode body at 0x0802ab90. Raw ARM saves
      r2/r3 and both incoming stack words, restores them as its callee's r2/r3
      and stack arguments, calls once, and discards the body status because
      this wrapper is void. The body remains a retailOS target seam; the host
      test records every word and verifies their order and the single call.

  # --- H.264 aligned-buffer status ---
  - address: 0x080276c8
    ghidra: FUN_080276c8
    name: h264_aligned_buffer_status
    size: 40
    sig: "u32 h264_aligned_buffer_status(const u8 *buffer, i32 aligned_value)"
    module: h264/aligned_buffer_status
    status: ported
    renames:
      param_1/r0: buffer
      param_2/r1: aligned_value
      r0: status
    notes: >-
      Leaf validation helper with no direct recovered bl caller in the
      indexed code graph. Raw ARM returns 2 immediately for a null buffer.
      Otherwise it evaluates r1 as signed (`cmp r1,#0; ble`), returning 0
      only for a positive multiple of 16 (`tst r1,#15`), and 3 for zero,
      negative, or unaligned values. Host tests cover each status boundary;
      match.py diff reviewed after the ARM release build.

  # --- stream-buffer request flags ---
  - address: 0x08037fe8
    ghidra: thunk_EXT_FUN_22004450
    name: stream_buffer_request_flags_update
    size: 8
    sig: "void stream_buffer_request_flags_update(u32 enable, u32 mask)"
    module: app/buffer_refill_request
    status: ported
    renames:
      param_1/r0: enable
      param_2/r1: mask
      DAT_08037fec: stream_buffer_request_flags_update_body (target RAM @ 0x22004450)
      DAT_08004480: stream_buffer_request_controller_flags (target RAM @ 0x2200aebc)
      FUN_08001e70: cpsr_disable_irq_fiq
      FUN_08001e84: cpsr_restore_irq_fiq
    notes: >-
      Ghidra's 4-byte size omits the veneer literal: raw osos.dec has
      e51ff004 at 0x08037fe8 followed by target word 0x22004450 at
      0x08037fec. The relocator's 0x08000000 to 0x22000000 IRAM mirror
      resolves that target to 0x08004450. Its 52-byte body saves IRQ/FIQ
      state, volatile-loads request-controller flags at 0x2200aebc, BICs
      mask when enable is zero or ORRs it otherwise, stores, and restores
      the saved state. Decoding every ARM B/BL word found 30 direct calls:
      28 unconditional bl plus blhi at 0x080f5720 and blls at 0x080f57dc;
      no tail B caller. Host tests cover the zero and arbitrary-nonzero
      predicate cases through a target-RAM replacement.

  # --- stream-buffer refill request ---
  - address: 0x08004484
    ghidra: FUN_08004484
    name: stream_buffer_refill_level_advance
    size: 32
    sig: "u32 stream_buffer_refill_level_advance(void)"
    module: app/buffer_refill_request
    status: ported
    renames:
      DAT_080044a4: stream_buffer_refill_level (target RAM @ 0x2200ad14)
      DAT_080044a8: stream_buffer_request_controller (target RAM @ 0x2200aebc)
      iVar5/r0: requested_level
      FUN_080029ac: stream_buffer_transition_controller
    notes: >-
      Sole recovered caller is the stream-buffer consumer
      FUN_08006b64 at 0x08006de4, when readable distance exceeds half
      capacity (its low-water sibling 0x080044ac uses the complementary
      bit-derived level). Raw ARM reads the refill-request level, holds
      exactly level 3, otherwise adds one with ARM 32-bit wrapping
      arithmetic, stores the result to request-controller +0x10, then
      tail-branches to the shared transition controller 0x080029ac.
      ARM port preserves the two RAM literals and tail transfer through a
      linker-safe literal veneer. Host builds expose the target RAM word and
      controller edge as test seams. Host tests cover ordinary advance,
      the held level, controller result/once-only forwarding, and wrapping
      0xffffffff. match.py diff reviewed; structural shape is
      literals -> compare -> conditional add -> store -> tail controller.

  # --- stream-buffer transition controller ---
  - address: 0x080380f0
    ghidra: thunk_EXT_FUN_220029ac
    name: stream_buffer_transition_controller
    size: 8
    sig: "u32 stream_buffer_transition_controller(void)"
    module: app/buffer_transition_controller
    status: ported
    renames:
      DAT_080380f4: stream_buffer_transition_controller_body (target RAM @ 0x220029ac, osos body @ 0x080029ac, 172 bytes)
      DAT_08002d48: stream_buffer_request_controller (target RAM @ 0x2200aebc; +0x00 flags, +0x0c inhibit, +0x10 requested level, +0x14 base level)
      DAT_08002d4c: stream_transition_inhibit_byte (RAM @ 0x089caf4a)
      thunk_FUN_080518a0: stream_transition_gate (role-based; true identity unverified)
      thunk_FUN_0804afb4: stream_transition_dispatch (role-based; true identity unverified)
      thunk_FUN_081561f8: controller_history_fn (veneer targets the "CntrlHistoryFn" string head; real entry 0x0815620c)
      FUN_08002948: stream_buffer_level_wait_equal
      FUN_080044c8: stream_buffer_level_wait_zero
    notes: >-
      Ghidra's 4-byte size omits the veneer literal: raw osos.dec has
      e51ff004 at 0x080380f0 followed by target word 0x220029ac at
      0x080380f4; the relocator's 0x08000000 to 0x22000000 IRAM mirror
      resolves that to the osos body at 0x080029ac. The body's true raw
      extent is 172 bytes (0x080029ac..0x08002a57), ending in the tail
      branch b 0x08002948; 0x08002a58 opens the distinct
      iPodPowerProfile.txt function. Ghidra's 348-byte FUN_080029ac lumps
      the body with that sibling and both wait functions. Raw decoding of
      every ARM B/BL word found 24 unconditional bl call sites plus 3
      tail b transfers (0x081a5d9c, 0x081a6794, 0x081a684c); no predicated
      bl forms, so all policy checks live inside the controller. Behavior:
      suppress to the drain wait (0x080044c8, spin until level word
      0x2200ad14 is 0) when (flags & 0x46) | controller+0x0c is nonzero,
      the 0x089caf4a byte is nonzero, the storage gate 0x080518a0 returns
      nonzero, or the history check returns nonzero after the dispatch
      0x0804afb4 runs; otherwise reload flags/base level post-call and
      compute the target: bit0 clamps to max(min(base, requested),
      bit7 ? 0 : 1), bit3 overrides to bit4 ? 0 : 1, bit5 (without bit3)
      forces 0; then tail-enter 0x08002948(level), spinning until the
      level word equals the target. Both waits fall through to the
      stream-consumer status block 0x080042b4(22), whose result (or the
      already-met level on the early-exit path) is the return value.
      Anomaly: the history callee's veneer at 0x08003548 (sole caller
      0x080029e4) targets 0x081561f8, the head of the "CntrlHistoryFn"
      name string, 0x14 bytes before the real entry 0x0815620c; the five
      intervening words decode as VS/EQ-predicated instructions that no-op
      only when entering with V=0 and Z=0. The port preserves the
      0x081561f8 transfer verbatim through a literal veneer. ARM callees
      are reached via retail_* literal veneers replacing stock PC-relative
      branches; host builds seam target RAM and all five callees. Host
      tests cover each inhibit source, gate/dispatch/history short-circuit
      order, base passthrough, the min/max clamp including the bit7 floor
      removal, both override values, force-zero precedence, result
      forwarding, and the post-call flags/base reload. match.py diff
      reviewed (--lib worktree archive): 44 vs 43 instructions,
      structurally isomorphic; LLVM selects equivalent idioms (movcc/movls
      min, bic-computed override, movne force-zero) and tail-branches to
      the retail wait veneers.

  # --- eight-slot byte identity-table initialization ---
  - address: 0x08028460
    ghidra: FUN_08028460
    name: initialize_eight_byte_identity_slots
    size: 36
    sig: "void initialize_eight_byte_identity_slots(u8 *base)"
    module: drivers/eight_slot_identity_init
    status: ported
    renames:
      param_1/r0: base
      iVar1/r4: slot_index
      FUN_0802b9ec: initialize_byte_identity_slot
    notes: >-
      Calls the unported byte identity-table initializer at 0x0802b9ec exactly
      eight times in ascending order, passing base + slot_index * 0x100 for
      slot_index 0 through 7. The target build dispatches directly to the
      retail helper and host builds provide a volatile recorder seam. The
      direct host test proves every passed offset and the complete call order;
      match.py diff reviewed after the ARM release build.

  # --- address-distinct eight-slot byte identity-table initialization alias ---
  - address: 0x08028490
    ghidra: FUN_08028490
    name: initialize_eight_byte_identity_slots_alias
    size: 36
    sig: "void initialize_eight_byte_identity_slots_alias(u8 *base)"
    module: drivers/eight_slot_identity_init_alias
    status: ported
    renames:
      param_1/r0: base
      iVar1/r4: slot_index
      FUN_0802b9ec: initialize_byte_identity_slot
    notes: >-
      Address-distinct, byte-identical alias of
      initialize_eight_byte_identity_slots at 0x08028460; it remains its own
      hookable Rust export because retail callers retain the 0x08028490 load
      address. Calls the recovered initialize_byte_identity_slot helper at
      0x0802b9ec exactly eight times in ascending order, passing base +
      slot_index * 0x100 for slot_index 0 through 7. The direct host behavior
      test verifies every initialized byte across all eight slots and confirms
      both adjacent guard bytes remain unchanged; match.py diff reviewed after
      the ARM release build.

  # --- UI checked-byte-block forwarding basic block ---
  - address: 0x0802dd88
    ghidra: FUN_0802dd88
    name: ui_checked_byte_block_forwarder
    size: 32
    sig: "u32 ui_checked_byte_block_forwarder(u32 mode, u8 **input_cursor, u8 **input_cursor_mirror, u8 **output_cursor, u8 **output_cursor_mirror, i32 row_count, i32 bytes_per_row)"
    module: ui/checked_byte_block_forwarder
    status: ported
    renames:
      r5: mode
      stack+0x30: input_cursor
      stack+0x34: input_cursor_mirror
      stack+0x38: output_cursor
      stack+0x2c: output_cursor_mirror
      r2/outgoing_stack+0x04: row_count
      r3/outgoing_stack+0x08: bytes_per_row
      FUN_0802b6a8: checked_byte_block_reader
    notes: >-
      This 32-byte sequence is a basic block in the parser at 0x0802d9c4,
      reached through the 4-byte thunk at 0x08003480 after the parser has
      preserved mode in r5. Raw ARM, not Ghidra's stack artifacts, shows it
      loading the four cursor-slot addresses from the containing frame, then
      placing live r2 and r3 at outgoing sp+4/sp+8 before calling
      0x0802b6a8. Thus r2/r3 are the reader's sixth/seventh arguments:
      row_count and bytes_per_row (normally 0x10 and 0x100), not cursor
      pointers. The port makes the otherwise implicit frame inputs explicit
      and forwards all seven reader words to the ported
      checked_byte_block_reader, returning its 0/4 status unchanged. Focused
      host coverage drives the reader through this forwarding entry.

  # --- FreeType conditional optional-offset adjustment ---
  - address: 0x0802de50
    ghidra: FUN_0802de50
    name: ft_adjust_offset_if_nonzero
    size: 40
    sig: "u32 ft_adjust_offset_if_nonzero(u32 bias_selector, u32 offset, u32 *adjusted_offset)"
    module: ft/conditional_offset
    status: ported
    renames:
      param_1/r0: bias_selector
      param_2/r1: offset
      param_3/r2: adjusted_offset
      uVar1/r0: adjusted_value
    notes: >-
      40-byte FreeType-area leaf helper (0x0802de50..0x0802de78), ten ARM
      instructions. A zero offset takes the early return, yielding 0 without
      reading or writing adjusted_offset. Otherwise the selector chooses the
      fixed adjustment: zero adds 4, nonzero adds 0x1004 (spelled as two ADDs
      on the latter ARM path); the wrapping u32 result is stored through
      adjusted_offset and the function returns 1. No deviations. Focused host
      tests cover both selector arms, wrapping addition, and the zero-offset
      no-write path including a null output pointer; match.py reviewed after
      the ARM release build.

  # --- proprietary STANDARD cipher first lookup table ---
  - address: 0x0802de78
    ghidra: FUN_0802de78
    name: standard_cipher_table_one_lookup
    size: 12
    sig: "u8 standard_cipher_table_one_lookup(u32 index)"
    module: crypto/standard_cipher_table_one
    status: ported
    renames:
      param_1/r0: index
      DAT_0802e0d0: STANDARD_CIPHER_TABLE_ONE
    notes: >-
      Twelve-byte leaf at 0x0802de78..0x0802de84, immediately followed by
      sibling lookup 0x0802de84. Raw ARM loads the table base 0x0891f04c from
      literal 0x0802e0d0 then returns its byte at unguarded r0 offset. A
      complete decoded ARM B/BL-word scan finds ten direct callers, all plain
      unconditional bl instructions (0x08027d08, 0x08027d38, 0x08027d64,
      0x08027d8c, 0x08027dbc, 0x08027f48, 0x08027f78, 0x08027fa4,
      0x08027fcc, 0x08027ffc); each supplies a byte-range table index during
      proprietary STANDARD cipher transforms. No deviations: unchecked Rust
      indexing preserves the retailOS valid-byte caller contract. Host tests
      cover table boundaries and representative interior values; match.py
      reviewed after the ARM release build.

  # --- sixteen-slot byte identity-table initialization ---
  - address: 0x0802dda8
    ghidra: FUN_0802dda8
    name: initialize_sixteen_byte_identity_slots
    size: 36
    sig: "void initialize_sixteen_byte_identity_slots(u8 *base)"
    module: drivers/sixteen_slot_identity_init
    status: ported
    renames:
      param_1/r0: base
      iVar1/r4: slot_index
      FUN_0802b9ec: initialize_byte_identity_slot
    notes: >-
      Calls the recovered initialize_byte_identity_slot helper at 0x0802b9ec
      exactly sixteen times in ascending order, passing base + slot_index *
      0x100 for slot_index 0 through 15. It preserves the r0 pointer ABI and
      void return. The direct host test verifies each identity entry in all
      sixteen 0x100-byte slots and both adjacent guard bytes; match.py
      structural diff reviewed after the ARM release build.

  # --- byte identity-table slot initialization ---
  - address: 0x0802b9ec
    ghidra: FUN_0802b9ec
    name: initialize_byte_identity_slot
    size: 24
    sig: "void initialize_byte_identity_slot(u8 *slot)"
    module: drivers/byte_identity_slot_init
    status: ported
    renames:
      param_1/r0: slot
      iVar1/r1: byte_index
    notes: >-
      24-byte leaf helper (0x0802b9ec..0x0802ba04), six ARM instructions.
      It initializes exactly one 0x100-byte identity table, writing byte i at
      slot + i for i from 0 through 255, inclusive. The eight-slot driver
      helpers at 0x08028460 and 0x08028490 are its recovered callers. Rust
      preserves the r0 pointer ABI and void return; volatile byte stores
      prevent LLVM from replacing the fixed-width loop with an unavailable
      libc helper. Focused host coverage verifies the endpoints, every mapping
      entry, and both adjacent guard bytes; match.py reviewed after the ARM
      release build.

  # --- selector-controlled word endian transform ---
  - address: 0x0802bad0
    ghidra: FUN_0802bad0
    name: transform_word_endianness
    size: 40
    sig: "u32 transform_word_endianness(u32 selector, u32 word)"
    module: heap/endian_selector
    status: ported
    renames:
      param_1/r0: selector
      param_2/r1: word
      uVar1/r0: transformed_word
    notes: >-
      40-byte leaf helper (0x0802bad0..0x0802baf8), ten ARM instructions.
      It returns word unchanged for selector 0, rotates the word right by
      8 for selector 1, right by 16 for selector 2, and right by 24 for
      every other selector. The non-identity arms are direct ARM ror
      instructions, represented by u32 rotations so shifted-out bits wrap
      exactly. No deviations. Focused host tests cover each selector class
      and representative patterns including high/low-bit wrap; match.py
      reviewed after the ARM release build.

  # --- H.264 parse-cursor advance with old-position output ---
  - address: 0x0802b924
    ghidra: FUN_0802b924
    name: h264_cursor_advance_with_start
    size: 24
    sig: "void h264_cursor_advance_with_start(i32 *cursor, i32 *cursor_mirror, i32 *start, i32 delta)"
    module: h264/cursor_advance_with_start
    status: ported
    renames:
      param_1/r0: cursor
      param_2/r1: cursor_mirror
      param_3/r2: start
      param_4/r3: delta
      iVar1/r12: old_cursor
    notes: >-
      24-byte leaf helper (0x0802b924..0x0802b93c), six ARM instructions.
      The H.264 syntax-header callers at 0x08028838 and 0x08028b08 initialize
      paired local parse cursors from the input position then call this helper
      when a flag selects a direct displacement. It reads cursor, applies
      ARM's wrapping signed 32-bit addition with delta, writes the result to
      cursor then cursor_mirror, and finally writes the original cursor to
      start. The ordered raw-pointer stores intentionally preserve the
      observable result when outputs alias. No deviations. Focused host tests
      cover ordinary and wrapping displacement plus all meaningful output
      alias combinations; match.py reviewed after the ARM release build.

  # --- FreeType word-cursor advance with range-start output ---
  - address: 0x0802b93c
    ghidra: FUN_0802b93c
    name: ft_advance_word_cursor
    size: 24
    sig: "void ft_advance_word_cursor(u32 *next_cursor, u32 *cursor, u32 *range_start, u32 word_count)"
    module: ft/word_cursor
    status: ported
    renames:
      param_1/r0: next_cursor
      param_2/r1: cursor
      param_3/r2: range_start
      param_4/r3: word_count
      iVar1/r12: old_cursor
      iVar2/r3: advanced_cursor
    notes: >-
      24-byte FreeType parser-layout leaf helper
      (0x0802b93c..0x0802b954), six ARM instructions. The compound parser
      builders at 0x08028548, 0x080286c0, 0x08028838, and 0x08028b08 use it
      to reserve word ranges: it reads cursor, computes cursor + word_count *
      4 with ARM's wrapping shifted-add arithmetic, writes the advanced cursor
      to next_cursor and cursor, then writes the pre-advance cursor to
      range_start. Ordered raw-pointer stores preserve observable output
      aliasing. No deviations. Focused host tests cover normal reservation,
      shift/add overflow, and output aliasing; match.py reviewed after the ARM
      release build.

  # --- strided parser cursor advance with range-start output ---
  - address: 0x0802b954
    ghidra: FUN_0802b954
    name: advance_strided_cursor_with_start
    size: 32
    sig: "void advance_strided_cursor_with_start(u32 *cursor, u32 *cursor_mirror, u32 *range_start, u32 stride, u32 count)"
    module: util/strided_cursor_advance
    status: ported
    renames:
      param_1/r0: cursor
      param_2/r1: cursor_mirror
      param_3/r2: range_start
      param_4/r3: stride
      param_5/[sp,#4]: count
      iVar2/r12: old_cursor
      iVar1/r3: advanced_cursor
    notes: >-
      32-byte parser-layout leaf helper (0x0802b954..0x0802b974), eight ARM
      instructions. The compound parser builders at 0x08028548, 0x080286c0,
      0x08028838, and 0x08028b08 use it to reserve count entries of stride
      bytes: it reads cursor, computes cursor + count * stride with ARM's
      wrapping multiply-accumulate arithmetic, writes the advanced cursor to
      cursor then cursor_mirror, and finally writes the pre-advance cursor to
      range_start. Ordered raw-pointer stores preserve observable output
      aliasing. No deviations. Focused host tests cover normal reservation,
      multiply/add overflow, and all meaningful output alias combinations;
      match.py reviewed after the ARM release build.

  # --- FreeType scaled word-cursor advance with range-start output ---
  - address: 0x0802b974
    ghidra: FUN_0802b974
    name: ft_advance_scaled_word_cursor
    size: 36
    sig: "void ft_advance_scaled_word_cursor(u32 *next_cursor, u32 *cursor_mirror, u32 *range_start, u32 stride, u32 count)"
    module: cxx/scaled_cursor
    status: ported
    renames:
      param_1/r0: next_cursor
      param_2/r1: cursor_mirror
      param_3/r2: range_start
      param_4/r3: stride
      param_5/[sp,#4]: count
      iVar2/r12: old_cursor
      iVar1/r3: advanced_cursor
    notes: >-
      36-byte FreeType parser-layout leaf helper (0x0802b974..0x0802b998),
      nine ARM instructions. The compound parser builders at 0x08028548,
      0x080286c0, 0x08028838, and 0x08028b08 call it for the distinct scaled
      field form: it reads cursor_mirror, computes old_cursor + (count *
      stride << 2) with ARM's wrapping multiply and shifted-add arithmetic,
      writes the advanced cursor to next_cursor then cursor_mirror, and
      finally writes old_cursor to range_start. Ordered raw-pointer stores
      preserve observable output aliasing. No deviations. Focused host tests
      cover normal advancement, multiply/shift/add overflow, and every
      meaningful output-alias combination; match.py reviewed after the ARM
      release build.

  # --- Application triple-scaled cursor advance with range-start output ---
  - address: 0x0802b998
    ghidra: FUN_0802b998
    name: advance_triple_scaled_cursor_with_start
    size: 40
    sig: "void advance_triple_scaled_cursor_with_start(u32 *cursor, u32 *cursor_mirror, u32 *range_start, u32 factor_1, u32 factor_2, u32 factor_3)"
    module: app/triple_scaled_cursor_advance
    status: ported
    renames:
      param_1/r0: cursor
      param_2/r1: cursor_mirror
      param_3/r2: range_start
      param_4/r3: factor_1
      param_5/[sp,#4]: factor_2
      param_6/[sp,#8]: factor_3
      iVar2/r12: old_cursor
      iVar1/r3: advanced_cursor
    notes: >-
      40-byte application parser-layout leaf helper
      (0x0802b998..0x0802b9c0), ten ARM instructions. It reads cursor,
      computes old_cursor + factor_3 * factor_2 * factor_1 with ARM's wrapping
      32-bit multiply and multiply-accumulate arithmetic, writes the advanced
      cursor to cursor then cursor_mirror, and finally writes old_cursor to
      range_start. Ordered raw-pointer stores preserve observable output
      aliasing. No deviations. Focused host tests cover ordinary advancement,
      multiply/add overflow, and valid output aliases; match.py reviewed after
      the ARM release build.

  # --- Triple-scaled word cursor advance with range-start output ---
  - address: 0x0802b9c0
    ghidra: FUN_0802b9c0
    name: advance_triple_scaled_word_cursor_with_start
    size: 44
    sig: "void advance_triple_scaled_word_cursor_with_start(u32 *cursor, u32 *cursor_mirror, u32 *range_start, u32 stride, u32 factor_2, u32 factor_3)"
    module: app/triple_scaled_word_cursor_advance
    status: ported
    renames:
      param_1/r0: cursor
      param_2/r1: cursor_mirror
      param_3/r2: range_start
      param_4/r3: stride
      param_5/[sp,#4]/lr: factor_2
      param_6/[sp,#8]/r4: factor_3
      iVar2/r12: old_cursor
      iVar1/r3: advanced_cursor
    notes: >-
      44-byte parser-layout leaf helper (0x0802b9c0..0x0802b9ec), eleven ARM
      instructions. It reads cursor, computes old_cursor + ((factor_3 *
      factor_2 * stride) << 2) with ARM's wrapping multiply, shift, and add
      arithmetic, writes the advanced cursor to cursor then cursor_mirror, and
      finally writes old_cursor to range_start. Decoding every ARM B/BL word
      in osos.dec found exactly ten incoming direct calls, all unconditional
      bl (0x080285fc, 0x08028694, 0x08028774, 0x0802880c, 0x0802897c,
      0x080289f0, 0x08028adc, 0x08028c04, 0x08028ce4, 0x08028db4); no
      predicated bl or direct b reaches it. Ordered raw-pointer stores
      preserve observable output aliasing. No deviations. Focused host tests
      cover normal advancement, each arithmetic wrap point, and every
      meaningful output-alias combination; match.py reviewed after the ARM
      release build.

  # --- byte-block checksum continuation literal veneer ---
  - address: 0x08003510
    ghidra: thunk_FUN_0802c03c
    name: tail_dispatch_byte_block_checksum_continuation
    size: 8
    sig: "void tail_dispatch_byte_block_checksum_continuation(void) __attribute__((noreturn))"
    module: runtime/byte_block_checksum_veneer
    status: ported
    renames:
      DAT_08003514: byte_block_checksum_continuation_target
      FUN_0802c03c: byte_block_checksum_reader_continuation
    notes: >-
      Raw ARM is the eight-byte literal veneer `ldr pc, [pc, #-4]` /
      `.word 0x0802c03c`; Ghidra reports only the first four-byte instruction.
      The next veneer begins at 0x08003518. Decoding every ARM B/BL word in
      osos.dec found exactly 18 incoming direct calls, all unconditional `bl`
      (0x0800214c, 0x080021f8, 0x08002270, 0x080022e0, 0x08002404,
      0x08002414, 0x08002498, 0x08002b30, 0x08002b4c, 0x08002b60,
      0x08002b74, 0x08002ba0, 0x080062c4, 0x0800641c, 0x08007cdc,
      0x08007ce4, 0x08007d0c, 0x08007d1c); no predicated `bl` or direct `b`
      reaches it. `ldr pc` preserves every register and LR, so execution
      enters the stack-sensitive 0x0802c03c continuation directly. That
      continuation uses r7 plus its surrounding parser frame, removes 0x34
      bytes of stack, then pops `{r4-r7, pc}`; it does not have a normal C
      return edge. ARM builds reproduce the original words in global assembly
      rather than calling the separately ported continuation as a Rust
      function. Deliberate host deviation: the target/frame are unavailable,
      therefore the host stand-in terminates rather than fabricating a return;
      its focused host test proves that contract. match.py reviewed after the
      ARM release build.

  # --- byte-block checksum continuation forwarder ---
  - address: 0x0802c03c
    ghidra: FUN_0802c03c
    name: forward_byte_block_checksum_reader
    size: 28
    sig: "void forward_byte_block_checksum_reader(void)"
    module: runtime/byte_block_checksum_forwarder
    status: ported
    renames:
      FUN_0802b6a8: checked_byte_block_reader
      r0: inherited_parser_mode
      r1: inherited_input_cursor
      r2: inherited_output_cursor
      r7: inherited_parser_context
    notes: >-
      28-byte continuation helper (0x0802c03c..0x0802c058), seven ARM
      instructions. Its caller has already established parser state in
      r0-r2 (r0 receives the surrounding frame's r7; r1 and r2 name local
      input/output cursor slots), then this entry calls the ported
      checked-byte-block reader at 0x0802b6a8. It compares and discards that
      reader's status, unwinds its 0x34-byte caller frame, and returns. The
      recovered C entry has no explicit parameters, so this port preserves
      the no-argument void ABI and the forwarding/return behavior only.
      Deviation: target builds retain the direct target call; host tests
      install a recording seam because the inherited register/frame state
      remains the caller's responsibility until the surrounding parser is
      ported.

  # --- transposed four-by-four byte-block XOR ---
  - address: 0x0802e54c
    ghidra: FUN_0802e54c
    name: xor_transposed_4x4
    size: 32
    sig: "void xor_transposed_4x4(u8 *dst, const u8 *src)"
    module: util/xor_transposed_block
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: src
      iVar2/r3: row
      iVar1/r2: column
      iVar3/r12: destination_row
      iVar4/r4: source_offset
    notes: >-
      32-byte leaf helper (0x0802e54c..0x0802e56c), eight ARM instructions.
      For each row i and column j in row-major order, it immediately stores
      dst[4*i+j] XOR src[i+4*j]. The source layout is transposed rather than
      linear. Source and destination ranges may overlap: each source load,
      destination load, and store remains ordered exactly as the ARM ldrb,
      ldrb, eor, strb sequence. No deviations. Focused host tests use
      distinguishable matrix values to prove every mapping, guard the exact
      16-byte destination range, and model in-place ordering; match.py
      structural diff reviewed after the ARM release build.

  # --- proprietary AES-like cipher: name gate ---
  - address: 0x0802ddcc
    ghidra: FUN_0802ddcc
    name: cipher_name_is_standard
    size: 72
    sig: "u64 cipher_name_is_standard(const u8 *name)"
    module: crypto/cipher_name
    status: ported
    renames:
      param_1/r0: name
      iVar1: name_length
      uVar2: status
      local_18/acStack_14/uStack_10: standard_string
      s_STANDARD_0802e0c4: STANDARD_STRING
      ram0x0802e0cc: STANDARD_STRING_pad_word
      FUN_08392478: strlen_unguarded
      FUN_08030f64: memcmp
    notes: >-
      72-byte validator (0x0802ddcc..0x0802de14, two bl call sites:
      0x0802de28, 0x0802e928) gating the proprietary AES-like cipher
      cluster (SubBytes 0x0802e118, ShiftRows-family 0x0802e190+,
      xor_transposed_4x4 0x0802e54c). Copies the 12 bytes at s_STANDARD
      @ 0x0802e0c4 ("STANDARD\0" + zero word @ 0x0802e0cc) onto its stack
      frame (ldmia/stmia of three words), takes unguarded strlen of name,
      fails unless exactly 8, then memcmps 8 bytes against the stack copy.
      Returns CONCAT44(local_18, status): low word r0 is 0 on success / 1
      on failure; high word r1 is always "STAN" (0x4E414154 LE) because
      the stmia overwrites the saved-r1 slot its pop epilogue reloads —
      both callers test only r0. Deviations: strlen callee is a private
      read_volatile byte loop with identical semantics instead of a call
      into crate::libc::strlen (that module concurrently in flight by
      another porter); memcmp goes through an inline(never) seam so the
      ARM build keeps the original's call boundary; the packed return is
      an explicit u64 rather than save-slot aliasing. Host tests cover
      exact/wrong/short/long names, post-NUL bytes ignored, the
      unterminated-8-byte-buffer edge (accepted only when the following
      byte is NUL), return packing, and a reference-model sweep;
      match.py structural diff reviewed after the ARM release build.

  # --- proprietary STANDARD cipher: second byte lookup table ---
  - address: 0x0802de84
    ghidra: FUN_0802de84
    name: standard_cipher_table_two_lookup
    size: 12
    sig: "u8 standard_cipher_table_two_lookup(u32 index)"
    module: crypto/standard_cipher_table_two
    status: ported
    renames:
      param_1/r0: index
      DAT_0802e0d4: STANDARD_CIPHER_TABLE_TWO
      DAT_0891f14c: STANDARD_CIPHER_TABLE_TWO
    notes: >-
      Raw ARM is exactly ldr r1,[pc,#0x248]; ldrb r0,[r1,r0]; bx lr, loading
      the literal 0x0891f14c from 0x0802e0d4 and returning the byte at its raw
      r0 offset. The next function starts at 0x0802de90, confirming the
      12-byte extent. A full decoded scan of every B/BL word in osos.dec finds
      10 callers, all unconditional bl (zero predicated calls): 0x08027d1c,
      0x08027d2c, 0x08027d54, 0x08027d78, 0x08027dd0, 0x08027f5c,
      0x08027f6c, 0x08027f94, 0x08027fb8, and 0x08028010. The callers pass
      byte-range inputs while constructing the proprietary STANDARD cipher's
      table-driven transforms. No deviations: unchecked Rust indexing retains
      the original's unguarded valid-byte caller contract.
  # --- proprietary STANDARD cipher: third byte lookup table ---
  - address: 0x0802de90
    ghidra: FUN_0802de90
    name: standard_cipher_table_three_lookup
    size: 12
    sig: "u8 standard_cipher_table_three_lookup(u32 index)"
    module: crypto/standard_cipher_table_three
    status: ported
    renames:
      param_1/r0: index
      DAT_0802e0d8: STANDARD_CIPHER_TABLE_THREE
      DAT_0891f24c: STANDARD_CIPHER_TABLE_THREE
    notes: >-
      Raw ARM is exactly ldr r1,[pc,#0x240]; ldrb r0,[r1,r0]; bx lr, loading
      the literal 0x0891f24c from 0x0802e0d8 and returning the byte at its raw
      r0 offset. The next function starts at 0x0802de9c, confirming the
      12-byte extent. A full decoded scan of every B/BL word in osos.dec finds
      14 callers, all unconditional bl (zero predicated calls): 0x08027e0c,
      0x08027e4c, 0x08027e90, 0x08027ea0, 0x08027f00, 0x0802804c,
      0x0802808c, 0x080280d0, 0x080280e0, 0x08028140, 0x0802ae60,
      0x0802ae70, 0x0802aeb4, and 0x0802aef4. The callers pass byte-range
      inputs while constructing or applying the proprietary STANDARD cipher's
      table-driven transforms. No deviations: unchecked Rust indexing retains
      the original's unguarded valid-byte caller contract.
  # --- proprietary STANDARD cipher: fourth byte lookup table ---
  - address: 0x0802de9c
    ghidra: FUN_0802de9c
    name: standard_cipher_table_four_lookup
    size: 12
    sig: "u8 standard_cipher_table_four_lookup(u32 index)"
    module: crypto/standard_cipher_table_four
    status: ported
    renames:
      param_1/r0: index
      DAT_0802e0dc: STANDARD_CIPHER_TABLE_FOUR
      DAT_0891f34c: STANDARD_CIPHER_TABLE_FOUR
    notes: >-
      Raw ARM is exactly ldr r1,[pc,#0x238]; ldrb r0,[r1,r0]; bx lr, loading
      the literal 0x0891f34c from 0x0802e0dc and returning the byte at its raw
      r0 offset. The next function starts at 0x0802dea8, confirming the
      12-byte extent. A full decoded scan of osos.dec finds 14 calls, all
      unconditional bl (zero predicated calls): 0x08027e24, 0x08027e34,
      0x08027e78, 0x08027eb8, 0x08027f18, 0x08028064, 0x08028074,
      0x080280b8, 0x080280f8, 0x08028158, 0x0802ae48, 0x0802ae88,
      0x0802aecc, and 0x0802aedc. The callers pass byte-range inputs while
      constructing or applying the proprietary STANDARD cipher's
      table-driven transforms. No deviations: unchecked Rust indexing retains
      the original's unguarded valid-byte caller contract.
  # --- proprietary STANDARD cipher: fifth byte lookup table ---
  - address: 0x0802dea8
    ghidra: FUN_0802dea8
    name: standard_cipher_table_five_lookup
    size: 12
    sig: "u8 standard_cipher_table_five_lookup(u32 index)"
    module: crypto/standard_cipher_table_five
    status: ported
    renames:
      param_1/r0: index
      DAT_0802e0e0: STANDARD_CIPHER_TABLE_FIVE
      DAT_0891f44c: STANDARD_CIPHER_TABLE_FIVE
    notes: >-
      12-byte leaf (0x0802dea8..0x0802deb4): ldr r1,[pc,#0x230];
      ldrb r0,[r1,r0]; bx lr. The literal pool word at 0x0802e0e0 is
      0x0891f44c, the fifth 256-byte table of the proprietary STANDARD
      cipher. It reads that table at raw r0 without a bounds or NULL guard.
      Every decoded ARM B/BL instruction in osos.dec finds exactly 14
      callers, all unconditional bl: 0x08027e18, 0x08027e58, 0x08027e6c,
      0x08027eac, 0x08027f0c, 0x08028058, 0x08028098, 0x080280ac,
      0x080280ec, 0x0802814c, 0x0802ae54, 0x0802ae94, 0x0802aea8, and
      0x0802aee8. Their index values are bytes. No deviations: the
      unchecked Rust array access retains the valid-byte caller contract.

  # --- proprietary STANDARD cipher: sixth byte lookup table ---
  - address: 0x0802deb4
    ghidra: FUN_0802deb4
    name: standard_cipher_table_six_lookup
    size: 12
    sig: "u8 standard_cipher_table_six_lookup(u32 index)"
    module: crypto/standard_cipher_table_six
    status: ported
    renames:
      param_1/r0: index
      DAT_0802e0e4: STANDARD_CIPHER_TABLE_SIX
      DAT_0891f54c: STANDARD_CIPHER_TABLE_SIX
    notes: >-
      Raw ARM is exactly ldr r1,[pc,#0x228]; ldrb r0,[r1,r0]; bx lr, loading
      the 256-byte table at 0x0891f54c and returning its byte at the raw r0
      offset with no bounds check. The next function begins at 0x0802dec0,
      confirming the 12-byte extent. A full decoded scan of osos.dec finds
      14 calls, all unconditional bl (zero predicated calls): 0x08027e00,
      0x08027e40, 0x08027e84, 0x08027ec4, 0x08027ef4, 0x08028040,
      0x08028080, 0x080280c4, 0x08028104, 0x08028134, 0x0802ae3c,
      0x0802ae7c, 0x0802aec0, and 0x0802af00. All pass byte-range inputs
      while constructing or applying the proprietary STANDARD cipher's
      table-driven transforms. No deviations: the Rust port retains the
      original unguarded valid-byte caller contract with unchecked indexing.

  # --- unsigned zero predicate ---
  - address: 0x08027afc
    ghidra: FUN_08027afc
    name: is_zero
    size: 12
    sig: "u32 is_zero(u32 value)"
    module: util/value_predicate
    status: ported
    renames:
      param_1/r0: value
      iVar1/r0: zero_predicate
    notes: >-
      12-byte ARM leaf: `rsbs r0,r0,#1; movcc r0,#0; bx lr`. The unsigned
      borrow condition retains 1 only for input zero; zero, one, and every
      higher u32 input therefore produce 1, 0, and 0 respectively. The
      u32 argument/result ABI uses r0, matching the C `uint` input and its
      0-or-1 int result. No deviations; focused host tests cover 0, 1, 2,
      and both high unsigned boundaries.
  # --- zero-inclusive single-bit predicate ---
  - address: 0x0825ebb4
    ghidra: FUN_0825ebb4
    name: is_zero_or_power_of_two
    size: 24
    sig: "u32 is_zero_or_power_of_two(u32 value)"
    module: util/value_predicate
    status: ported
    renames:
      param_1/r0: value
      r1: negated_value
      r0: predicate_result
    notes: >-
      24-byte ARM leaf: `mvn r1,r0; add r1,r1,#1; bics r0,r0,r1; movne
      r0,#0; moveq r0,#1; bx lr`. It clears value's least-significant set
      bit and returns one when the result is zero, so zero and every
      single-bit u32 value are true. The u32 input/result ABI both use r0;
      Ghidra's `bool` return is the normalized 0-or-1 word result. All three
      recovered callers (0x0824de50, 0x08251630, and 0x08253158) first
      require their signed dimension-like inputs to be nonnegative, then
      accept this predicate's nonzero result; because they do not separately
      reject zero, zero is an intentional accepted input. No deviations;
      host tests exhaust the dense low range, every bit position, and
      high-bit boundary values against the recovered C expression.

  # --- 32-bit word equality predicate ---
  - address: 0x082a7884
    ghidra: FUN_082a7884
    name: u32_value_eq
    size: 24
    sig: "u32 u32_value_eq(const u32 *left, const u32 *right)"
    module: util/value_predicate
    status: ported
    renames:
      param_1/r0: left
      param_2/r1: right
      "*param_1": left_value
      "*param_2": right_value
      r0 return: equality_result
    notes: >-
      24-byte, six-instruction ARM leaf: load one aligned 32-bit word from
      each input pointer, compare them, and materialize the condition into
      the normalized 0-or-1 C-ABI result in r0. Ghidra assigns `int *` to
      both inputs, but the sole recovered caller at 0x083d820c passes
      unsigned state words and compares against the 0xffffffff sentinel;
      signedness cannot affect equality, so the Rust ABI uses `const u32 *`.
      Neither input is written. No deviations; focused host tests prove
      equality, inequality, identical-pointer aliasing, both u32 boundaries,
      and preservation of both input values. match.py structural diff
      reviewed after the ARM release build.


  # --- ARM second-argument return forwarder ---
  - address: 0x080275f0
    ghidra: FUN_080275f0
    name: return_second_argument
    size: 8
    sig: "u32 return_second_argument(u32 first_argument, u32 second_argument)"
    module: cxx/return_forwarder
    status: ported
    renames:
      param_1/r0: first_argument
      param_2/r1: second_argument
      r0: returned_second_argument
    notes: >-
      8-byte ARM leaf: `mov r0,r1; bx lr`. It returns the raw second 32-bit
      ABI argument unchanged, never reads the first argument, and performs no
      memory access. No direct callers were recovered by the codegraph, so
      return_second_argument is the narrowest semantic name supported by the
      exact ABI behavior. No deviations; focused host tests cover independence
      from the first argument and zero/all-one second-argument bit patterns.

  # --- big-endian wire-format packer cluster (src/util/beload.rs) ---
  - address: 0x0826161c
    ghidra: FUN_0826161c
    name: pack_be16
    size: 28
    sig: "void pack_be16(u8 *dst, u16 value)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: value
      uVar1/r2: reversed
    notes: >-
      Writes value as two big-endian bytes at an unaligned dst - the first
      member of the pack/unpack overload set. 28 bytes, 7 instructions,
      0x0826161c..0x08261634 ending in bx lr; the next function (pack_be32)
      starts at 0x08261638, extent decoded from the raw words rather than
      taken from Ghidra. 46 bl call sites, counted by decoding every B/BL
      word in osos.dec. Algorithm - same reverse-then-spill two-step shape
      as pack_be32: byte-reverse the halfword in registers (lsl #8, orr of
      value & 0xff00 shifted right 8), then spill the reversed halfword's
      two bytes least-significant first to ascending addresses, so dst[0]
      receives value >> 8. The original ignores the argument register's top
      half, which the u16 parameter states directly. Kept in its own text
      section so LLVM cannot fold it onto another packer. Deviation - LLVM
      collapses the reverse into two direct strb and emits them in
      descending address order (dst[1] before dst[0]); the original writes
      dst[0] first. Both are plain writes to distinct addresses, so no
      caller can observe the difference. Host tests cover every single-byte
      lane at misalignments 0..5, guard the untouched neighbouring bytes,
      and pin the byte order against a little-endian store. match.py
      structural diff reviewed - two strb with the most significant byte at
      the lowest address, plus the crate-wide push {fp, lr} frame.

  - address: 0x08261638
    ghidra: FUN_08261638
    name: pack_be32
    size: 56
    sig: "void pack_be32(u8 *dst, u32 value)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: value
      uVar1/r2: reversed
      uVar2/r3: lane_mask
    notes: >-
      Writes value as four big-endian bytes at an unaligned dst. 56 bytes,
      14 instructions, 0x08261638..0x0826166c; the next function starts at
      0x08261670 (push {r4, lr}), so the Ghidra extent is correct here. 49
      bl call sites, counted by decoding every B/BL word in osos.dec.
      Algorithm - byte-reverse the word in registers first (lsl #24, then
      orr of value & 0xff00 shifted left 8, orr of value & 0xff0000 shifted
      right 8, orr of value >> 24), then spill the reversed word's four
      bytes least-significant first to ascending addresses, walking dst with
      pre-indexed strb. Net effect is a big-endian store, identical in
      observable behavior to store_be32 @ 0x083816cc and store_u32_be_bytes
      @ 0x08046b1c, but a separate firmware function with a distinct
      implementation, so it is kept distinct via its own text section. Third
      member of the overload set at 0x0826161c (16-bit pack), 0x08261638
      (32-bit pack), 0x08261670 (64-bit pack), 0x08261750 / 0x08261770 /
      0x08261790 (the matching unpacks). Callers build big-endian protocol
      packets - FUN_081ac6f0 fills a six-byte reply body at 0x081ac7bc and
      hands it to the transport at 0x080f6efc; FUN_081fffac appends four
      bytes at packet + packet[6] + 7 then bumps packet[6] by 4. No
      deviations; the port keeps the reverse-then-spill two-step shape.
      Host tests cover every single-byte lane at misalignments 0..7,
      guard the untouched neighbouring bytes, and cross-check against
      store_be32 and load_be32. match.py structural diff reviewed - LLVM
      picks the ror idiom over the mask/orr chain, and emits the same
      crate-wide push {fp, lr} frame the accepted store_be32 port has.

  - address: 0x08261670
    ghidra: FUN_08261670
    name: pack_be64
    size: 100
    sig: "void pack_be64(u8 *dst, u64 value)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: dst
      param_2/r2: value_lo
      param_3/r3: value_hi
      uVar1/r0+r1: reversed
      uVar2/r12: lane
      uVar3/r2: cursor
    notes: >-
      Writes value as eight big-endian bytes at an unaligned dst - the
      widest member of the pack overload set. 100 bytes, 25 instructions,
      0x08261670..0x082616d0 from push {r4, lr} to pop {r4, pc}; the next
      function (reverse_bytes32) starts at 0x082616d4, extent decoded from
      the raw words rather than taken from Ghidra. 5 bl call sites, counted
      by decoding every B/BL word in osos.dec. ABI - dst in r0, the
      doubleword in the r2/r3 pair, exactly what AAPCS gives
      pack_be64(u8*, u64). Algorithm - same two-step shape as pack_be32,
      but the doubleword reverse is too large to inline, so it moves the
      value into r0/r1 and calls reverse_bytes64 @ 0x082616f0 with a real
      bl, then spills the reversed doubleword's eight bytes
      least-significant first to ascending addresses (dst[0] receives
      value >> 56). The port keeps that call rather than folding the
      reverse in, which is why reverse_bytes64 carries #[inline(never)].
      Deviation - the spill is unrolled so LLVM's loop-idiom pass cannot
      rewrite it into a memcpy that does not exist on the target, and LLVM
      addresses dst with constant offsets instead of the original's
      pre-indexed strb walk. Kept in its own text section so LLVM cannot
      fold it onto another packer. Host tests cover the eight boundary
      values and all eight single-byte lanes at misalignments 0..7, guard
      the untouched neighbouring bytes, and assert the high word lands at
      the low addresses (the transposition a half-swapped doubleword would
      produce). match.py structural diff reviewed - save dst, move the
      value into the argument pair, bl the reverser, eight strb; the frame
      is the crate-wide push, widened to {r4, sl, fp, lr}.

  - address: 0x082616d4
    ghidra: FUN_082616d4
    name: reverse_bytes32
    size: 28
    sig: "u32 reverse_bytes32(u32 value)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: value
      uVar1/r1: reversed
      uVar2/r2: lane_mask
    notes: >-
      Register-only byte reverse of a 32-bit value - the private reverser
      the wire-format cluster shares. 28 bytes, 7 instructions,
      0x082616d4..0x082616ec ending in bx lr; the next function
      (reverse_bytes64) starts at 0x082616f0, extent decoded from the raw
      words rather than taken from Ghidra. 2 bl call sites plus the tail b
      from unpack_be32 @ 0x0826178c, counted by decoding every B/BL word in
      osos.dec, so it is a genuine firmware entry point and not an inlining
      artefact. Algorithm - lsl #24, orr of value & 0xff00 shifted left 8,
      orr of value & 0xff0000 shifted right 8, orr of value >> 24. Same
      observable result as bswap32 @ 0x0805dc24, which instead round-trips
      the word through a stack slot, so the two stay separate symbols and
      this one keeps its own text section (LLVM folds byte-identical bodies
      otherwise). No deviations. Host tests mirror every single-byte lane,
      prove the transform is its own inverse, cross-check against bswap32,
      and confirm it is exactly what turns unpack_be32's little-endian
      gather into a big-endian read. match.py structural diff reviewed -
      LLVM picks the ror idiom over the mask/orr chain and adds the
      crate-wide push {fp, lr} frame.

  - address: 0x082616f0
    ghidra: FUN_082616f0
    name: reverse_bytes64
    size: 96
    sig: "u64 reverse_bytes64(u64 value)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: value_lo
      param_2/r1: value_hi
      uVar1/r2: saved_hi
      uVar2/r3: reversed_hi
      uVar3/r12: lane
    notes: >-
      Register-only byte reverse of a 64-bit value - the 64-bit twin of
      reverse_bytes32 @ 0x082616d4 and the only reverser the 64-bit
      pack/unpack pair uses. 96 bytes, 24 instructions,
      0x082616f0..0x0826174c ending in bx lr; the next function
      (unpack_be16) starts at 0x08261750, extent decoded from the raw words
      rather than taken from Ghidra. 1 bl call site (pack_be64 @
      0x08261680) plus the tail b from unpack_be64 @ 0x082617e8, counted by
      decoding every B/BL word in osos.dec. ABI - doubleword in r0 (low
      word) / r1 (high word), returned the same way. Algorithm - returns
      (reverse_bytes32(hi), reverse_bytes32(lo)): each half byte-reversed
      and the two halves swapped. Deviation - the original open-codes the
      swap as a mask/shift chain over the whole 64-bit value and so emits
      four provably-zero lane terms ((lo & 0xff0000) << 24, (lo &
      0xff000000) << 8, (hi & 0xff) >> 8, (hi & 0xff00) >> 24); those are
      dead in the original and are not written in the port, which is why it
      is 12 instructions to the original's 24. Kept in its own text section
      so LLVM cannot fold it onto another member of the family. Host tests
      mirror every one of the eight single-byte lanes (a port that reversed
      only within each half fails there while still passing the all-ones
      cases), prove the transform is its own inverse, and cross-check the
      half-swap decomposition against reverse_bytes32. match.py structural
      diff reviewed - LLVM picks the ror idiom, drops the dead lanes, and
      adds the crate-wide push {fp, lr} frame.

  - address: 0x08261750
    ghidra: FUN_08261750
    name: unpack_be16
    size: 32
    sig: "u16 unpack_be16(const u8 *src)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: src
      uVar1/r1: gathered
    notes: >-
      Reads two big-endian bytes at an unaligned src and returns them as a
      u16 - the read twin of pack_be16 @ 0x0826161c. 32 bytes, 8
      instructions, 0x08261750..0x0826176c ending in bx lr; the next
      function (unpack_be32) starts at 0x08261770, extent decoded from the
      raw words rather than taken from Ghidra. 35 bl call sites, counted by
      decoding every B/BL word in osos.dec. Algorithm - gather src[0] and
      src[1] into a little-endian halfword, then reverse it inline (lsl #24
      then lsr #16 for the low lane, orr of gathered & 0xff00 shifted right
      8) instead of branching to reverse_bytes32 the way the 32- and 64-bit
      unpacks do; the result is zero-extended into the return register.
      Kept in its own text section so LLVM cannot fold it onto another
      unpacker. Deviation - LLVM collapses gather-then-reverse into the
      single orr r0, r0, r1, lsl #8 over the two ldrb, which is bit-exactly
      the same zero-extended result. Host tests cover misalignments 0..8,
      the 0 / 1 / 0x7fff / 0x8000 / 0xffff boundaries, and round trips
      through pack_be16 at every offset. match.py structural diff reviewed
      - two ldrb with the low address supplying the most significant byte,
      plus the crate-wide push {fp, lr} frame.

  - address: 0x08261770
    ghidra: FUN_08261770
    name: unpack_be32
    size: 32
    sig: "u32 unpack_be32(const u8 *src)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: src
      uVar1/r1: gathered
      uVar2/r2: lane_byte
    notes: >-
      Reads four big-endian bytes at an unaligned src and returns them as a
      u32. TRUE EXTENT IS 32 BYTES, NOT THE 60 GHIDRA REPORTS - eight words
      at 0x08261770..0x0826178c, the last of them a tail branch (ea ff ff
      d0, an unconditional B, not a BL) to the private register-only byte
      reverse at 0x082616d4. Ghidra's 60 runs 0x2c bytes past that branch
      and swallows the separately-linked 64-bit unpack that starts at
      0x08261790. 60 bl call sites, counted by decoding every B/BL word in
      osos.dec. Algorithm - gather bytes 0..3 into a little-endian word,
      walking src with pre-indexed ldrb, then tail-branch to the 32-bit byte
      reverse; net effect is a big-endian load, identical in observable
      behavior to load_be32 @ 0x081f3b30 / 0x0837a158, but a separate
      firmware function with a distinct implementation, so it is kept
      distinct via its own text section. It is the read twin of pack_be32 @
      0x08261638 and the third member of the unpack overload set at
      0x08261750 (16-bit), 0x08261770 (32-bit), 0x08261790 (64-bit, which
      reaches the 64-bit reverse at 0x082616f0 the same way). Deviation -
      the port folds the tail-branched byte reverse into the body, since
      LLVM inlines an equal-bodied leaf call regardless and 0x082616d4 is
      behaviorally the already-ported bswap32; nothing else differs. Host
      tests cover every single-byte lane at misalignments 0..11, prove the
      most significant byte sits at the lowest address, and cross-check
      round trips against pack_be32 and load_be32. match.py structural diff
      reviewed against the true 32-byte extent - the four ldrb plus the
      shift-8/16/24 orr chain line up, with the byte reverse inlined in
      place of the b and the crate-wide push {fp, lr} frame added.

  - address: 0x08261790
    ghidra: FUN_08261790
    name: unpack_be64
    size: 92
    sig: "u64 unpack_be64(const u8 *src)"
    module: util/beload
    status: ported
    renames:
      param_1/r0: src
      uVar1/r1: gathered_lo
      uVar2/r2: gathered_hi
      uVar3/r3: lane_byte
    notes: >-
      Reads eight big-endian bytes at an unaligned src and returns them as
      a u64 - the read twin of pack_be64 @ 0x08261670 and the widest member
      of the unpack overload set. 92 bytes, 23 instructions,
      0x08261790..0x082617e8, the last of them a tail branch (ea ff ff c0,
      an unconditional B, not a BL) to reverse_bytes64 @ 0x082616f0; the
      next function starts at 0x082617ec with an unrelated push {r4, r5,
      r6, lr}. Ghidra does not list this address at all - it folds these 92
      separately-linked bytes into the preceding unpack_be32, so the extent
      is decoded from the raw words. 3 bl call sites, counted by decoding
      every B/BL word in osos.dec. Algorithm - gather bytes 0..7 into a
      little-endian doubleword, walking src with pre-indexed ldrb, then
      tail-branch to the 64-bit reverse. The port keeps that call rather
      than folding it in, matching pack_be64; LLVM reproduces the tail
      branch. Deviation - the original's gather open-codes a 64-bit shift
      over register pairs and so emits several provably-zero terms (asr #31
      of a byte just loaded with ldrb, and lsr #24/#16/#8 of single-byte
      values); those are dead there and are not written in the port, and
      LLVM addresses src with constant offsets instead of the pre-indexed
      walk. Kept in its own text section so LLVM cannot fold it onto
      another unpacker. Host tests cover misalignments 0..15, the 0 / 1 /
      0x7fffffffffffffff / 0x8000000000000000 / 0xffffffffffffffff
      boundaries, a lone 0x80 byte in each of the eight lanes (which is
      what a stray sign-extension of a gathered byte would corrupt), round
      trips through pack_be64 at every offset, and the split into two
      unpack_be32 reads with the high word at the low addresses. match.py
      structural diff reviewed - eight ldrb feeding two orr chains, one per
      half, then the tail b to the reverser, plus the crate-wide push
      {fp, lr} frame.

  # --- retailOS event source object (see also 0x081e04b0 / 0x081e054c) ---
  - address: 0x081e0ba4
    ghidra: FUN_081e0ba4
    name: event_source_kind
    size: 8
    sig: "u8 event_source_kind(const void *source)"
    module: app/event_source
    status: ported
    renames:
      param_1/r0: source
    notes: >-
      52 bl call sites, no plain-b tail calls; both counts verified by
      decoding every branch word in osos.dec. The entire function is
      "ldrb r0,[r0,#0xc]; bx lr" - an accessor for the byte at +0x0c of the
      92-byte event source object that 0x081e04b0 / 0x081e054c also operate
      on, not a veneer (a veneer would be 0xe51ff004 plus a target word).
      The byte is constructor argument 2 of 0x081e0bac, which the factory
      @ 0x081472b0 takes from declaration[1] & 0xff. Call sites treat the
      result as a small enumeration - the view transition state machine
      @ 0x0817f7c0 compares two sources' kinds against 1, 5, 6, 9 and 10
      to decide whether the change animates. No deviations; match.py shows
      the identical ldrb/return pair inside the crate's fp frame.

  - address: 0x081e0bac
    ghidra: FUN_081e0bac
    name: event_source_construct
    size: 136
    sig: "void *event_source_construct(void *source, int id, u8 kind, void *declaration)"
    module: app/event_source
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: id
      param_3/r2: kind
      param_4/r3: declaration
    notes: >-
      Constructor of the 92-byte event source object. 128 bl call sites, no
      plain-b tail calls; both counts verified by decoding every branch word
      in osos.dec. Ghidra's 132-byte extent drops the trailing literal-pool
      word at 0x081e0c30 (0x0898ecb4, the vtable, reached by
      "ldr r1,[pc,#116]" at 0x081e0bb4); the next function starts at
      0x081e0c34, so the true extent is 136 bytes - 33 instructions plus
      that word. Body - install the vtable with "str r1,[r0],#16", then
      default-construct three sub-objects in ascending order, chaining each
      constructor's returned this into the next address - the 3-word
      declaration vector at +0x10 (0x083e4a7c), the 28-byte child
      collection at +0x1c (0x083db10c, node allocator 0x083b9f4c) and the
      event tree at +0x38 (0x083db2d4, node allocator 0x083c14c8, the very
      tree 0x081e04b0 / 0x081e054c manage). Then zero the owning-registry
      word at +0x58 and store id at +0x08, declaration at +0x04 and the
      kind byte at +0x0c. Finally, if id is positive, load the declaration
      vector from the registry through 0x081e059c and clear the built flag
      at +0x54. Two details are load-bearing - the id test is
      "cmp r6,#0; ble", signed, so a negative id skips the load exactly
      like zero; and +0x54 is written only on that path, so a non-positive
      id leaves it at whatever the storage held. The stack-temporary sites
      @ 0x0817ead0 and @ 0x08183638 both null-check the id first, so the
      original never observes the stale flag, and the port adds no store
      the original does not make. Layout confirmed by the factory
      @ 0x081472b0 - operator new(92) @ 0x082aadd4, then
      ctor(obj, declaration[0], declaration[1] & 0xff, declaration),
      "str r6,[r0,#88]" for the owning registry, and insertion into that
      registry's map at +12 keyed by the same id. Deviation - the original
      passes uninitialized stack slots (sp+20, sp+12) as the ignored
      allocator arguments; the port passes zeroed ones. The comparator
      bytes at sp+16 and sp+8, which the collection constructors do read,
      are zero in both. match.py - same store/offset skeleton throughout
      (str r1,[r0],#16 / add #12 / add #28 / str [r0,#32] /
      strb [r0,#-44] / str [r7,#-52] / str [r7,#-48] / cmp+ble /
      strb [r7,#28]); the diff is the ops-table indirection emitting blx
      through loaded pointers instead of direct bl, plus the crate's fp
      frame.

  - address: 0x081e0c4c
    ghidra: FUN_081e0c4c
    name: event_source_destruct
    size: 160
    sig: "void *event_source_destruct(void *source)"
    module: app/event_source
    status: ported
    renames:
      param_1/r0: source
      r4: children (the collection at source+0x1c)
      r5: chunk (the node-pool chunk record being freed)
      param_2/param_3/param_4: >-
        not arguments - Ghidra invents them from the
        "push {r1,r2,r3,r4,r5,lr}" prologue, whose three scratch words are
        the erase call's iterator slots. The function takes only this.
    notes: >-
      Non-deleting destructor of the 92-byte event source object, paired
      one-for-one with event_source_construct @ 0x081e0bac - 128 bl call
      sites and no plain-b tail calls, exactly the constructor's 128, both
      counts verified by decoding every branch word in osos.dec. Ghidra's
      156-byte extent drops the trailing literal-pool word at 0x081e0ce8
      (0x0898ecb4, the vtable, reached by "ldr r1,[pc,#144]" at
      0x081e0c50); the next function starts at 0x081e0cec, so the true
      extent is 160 bytes - 39 instructions plus that word. The deleting
      form is the separate six-instruction thunk @ 0x081e0c34
      ("cmp r0,#0; bl 0x081e0c4c; b operator_delete"). Body - reinstall the
      vtable with "str r1,[r0],#56", then unwind the three sub-objects in
      reverse construction order. First the event tree at +0x38 through
      0x082a7fd8. Then the child collection at +0x1c, whose teardown is
      inlined here rather than called - 0x082a7fd8 is byte-for-byte this
      same sequence bound to the other node allocator family (0x083c1c3c /
      0x083c1648 instead of 0x083ba534 / 0x083b9ffc). That inlined half
      runs only when the collection's header word at +0x10 is non-zero and
      does three things - erase the whole range with
      "erase(&iter, collection, &begin, &end)" where begin is header->left
      (+0x8) and end is the header itself (0x083ba534); re-read the header
      word and recycle that node with destroy_value 0, because the sentinel
      carries no value (0x083b9ffc); then drain the collection's embedded
      node pool by walking the chunk-record list at collection+0x00,
      unlinking one record at a time and calling the ported
      cxx_array_dealloc first on the record's block (+0x8, with the record's
      capacity at +0x4 as the dead count argument) and then on the 12-byte
      record itself. That pool layout is pinned by the node allocator
      0x083b9f4c - +0x00 chunk list, +0x04 recycled-node list threaded
      through node+0x0c, +0x08/+0x0c bump cursor and limit, 32-byte nodes,
      12-byte records of "next, capacity, block". Finally the declaration
      vector at +0x10 through 0x083e4b2c. Load-bearing detail - every
      sub-object address is formed from the previous callee's returned
      pointer ("sub r4,r0,#28" then "sub r0,r4,#12" then "sub r0,r0,#16"),
      never from source, which is what lets the function work on a source
      reached through a base-class pointer; the port keeps that chain.
      Deviation - the original reads the header word twice in a row before
      the erase with no intervening call, to fill both iterator slots; the
      port reads it once. The four unported firmware callees ride an
      EventSourceDestructOps table whose host defaults panic rather than
      no-op, since each of them frees storage and a silent stand-in would
      let a test claim a teardown that never happened. match.py - same
      skeleton throughout (str r1,[r0],#56 / ldr [r0,#-12] / cmp+beq to the
      tail / three stack iterator slots / erase / re-read + recycle with
      r2=0 / the two-dealloc chunk loop with its unlink store / sub #40 /
      sub #16); the diff is the ops-table indirection emitting blx through
      loaded pointers instead of direct bl, the crate's fp frame, and one
      dropped "ldr r1,[chunk,#4]" - LLVM sees that cxx_array_dealloc
      ignores its count argument and elides the load.

  # --- fixed-block pool (class-specific operator new) ---
  - address: 0x0826c0d8
    ghidra: FUN_0826c0d8
    name: fixed_block_pool_alloc
    size: 92
    sig: "void *fixed_block_pool_alloc(FixedBlockPool *pool, size_t size)"
    module: heap/fixed_block_pool
    status: ported
    renames:
      param_1/r0: pool
      param_2/r1: size
      r4: pool (saved)
      r5: block (the popped free-list head)
      r6: size (saved across the tail call)
      FUN_082aadd4: operator_new (already ported, heap/veneers.rs)
      FUN_0807f5c4: mutex_lock (already ported, kernel/sync_mutex.rs)
      FUN_0807f6a0: mutex_unlock (already ported, kernel/sync_mutex.rs)
    notes: >-
      The allocation entry point of retailOS's fixed-block pool - a small
      C++ object owning one slab pre-carved into block_count blocks of
      exactly block_size bytes, chained through a mutex-protected singly
      linked free list. It backs the class-specific operator new a couple
      of dozen framework classes install; every call site is
      `bl <pool accessor>; mov r1,#sizeof(class); bl 0x0826c0d8`, and the
      thinnest of them (0x08103550, one of the two tail-b sites) is
      literally `void *operator new(size_t n) { return alloc(pool(), n); }`.
      Extent 0x0826c0d8..0x0826c134 (23 words, Ghidra's 92 confirmed - the
      next function starts at 0x0826c134 with its own push). 70 bl + 2 tail
      b call sites, binary-scanned by decoding every B/BL word in osos.dec.
      No DATA word holds the address, so it is never dispatched virtually.
      Algorithm - read pool->block_size (+8) BEFORE locking and compare for
      exact equality (not "fits") against size; a mismatch tail-branches to
      the global tag-2 operator_new. On a match, mutex_lock(pool) - the
      mutex is the object's FIRST member, so `this` is the mutex address -
      load free_head (+0x18), and if NULL unlock and take the same
      operator_new fallback (an exhausted pool is not an error, it silently
      degrades). Otherwise pop - free_head = block->next, the link living
      at offset 0 of the free block itself - unlock and return the block.
      Layout recovered from the pool's own ctor/dtor rather than from any
      symbol - ctor FUN_0826c134 (116 bytes) stores block_size/count at
      +8/+0xc, zeroes +0x10/+0x14/+0x18, runs mutex_create @ 0x080744a4 on
      `this` unadjusted, sets +0x10 = block_size * count, allocates the slab
      with operator_new into +0x14, links block[i].next = block[i+1] with a
      predicated hi loop terminated by a NULL store, and points free_head at
      the slab. The dtor FUN_0826c1a8 is operator_delete(+0x14),
      mutex_delete(this), return this - and is 32 bytes, not the 16 Ghidra
      reports (0x0826c1a8..0x0826c1c8, the usual dropped `mov r0,r4;
      pop {r4,pc}` tail).
      Deviations - field widths follow the host/target pointer width via a
      #[repr(C)] FixedBlockPool so the port never spells a literal byte
      offset (on target the layout is exactly +0/+8/+0xc/+0x10/+0x14/+0x18).
      The three callees ride FIXED_BLOCK_POOL_OPS (heap/pool_client.rs
      precedent) purely so host tests can observe the lock/pop/unlock
      bracketing - all three defaults ARE the real ports, so with no mocks
      installed the original call graph runs end to end (proven by the
      no-mock test; mutex_lock/unlock no-op on host through their own
      NULL-cell guard). No NULL guard on pool, matching the original.
      Host tests (5) - the head pop with the free head snapshotted from
      inside the lock/unlock recorders, draining a 3-block pool then the
      exhausted lock-look-unlock-fallback, the exact-equality boundary
      (0, 1, block_size-1, block_size+1, usize::MAX all bypass the lock
      entirely and leave the list untouched), NULL propagation from a
      failing global allocator, and the wired-defaults end-to-end run.
      match.py structural - same dataflow and the same literal offsets
      (`ldr r2,[r0,#8]`, `ldr r5,[r0,#24]`, `str r1,[r0,#24]`), same
      early-mismatch branch, same pop-inside-the-lock ordering, same
      block-in-r5 return; the diff is the crate fp frame and the three
      blx/bx-through-volatile-slot dispatches standing in for the direct
      bl / tail b.

  # --- RGBA component to packed pixel conversion ---
  - address: 0x08272018
    ghidra: FUN_08272018
    name: color_pack_for_format
    size: 120
    sig: "u32 color_pack_for_format(u32 format, u32 red, u32 green, u32 blue, u32 alpha)"
    module: cxx/color_pack
    status: ported
    renames:
      param_1/r0: format
      param_2/r1: red
      param_3/r2: green
      param_4/r3: blue
      param_5/[sp,#12]: alpha
    notes: >-
      Packs the RGBA8 components into a 16-bit pixel according to the exact
      full-width format tag. Raw ARM decoding fixes the extent at
      0x08272018..0x08272090 (30 words, 120 bytes): push {r4,r5,lr} starts
      it, pop {r4,r5,pc} at 0x0827208c ends it, and the next word at
      0x08272090 begins the distinct bare RGB565 packer. There is no literal
      pool. The three subtract/subs comparisons select 0x0565 and 0x2565 for
      RGB565 ((R&0xf8)<<8 | (G&0xfc)<<3 | (B&0xf8)>>3), 0x1444 for RGBA4444
      ((A&0xf0)<<8 | (R&0xf0)<<4 | (G&0xf0) | (B&0xf0)>>4), and RGB555
      ((R&0xf8)<<7 | (G&0xf8)<<2 | (B&0xf8)>>3) for every other tag,
      including 0x0555. The tag is not masked: e.g. 0x00010565 takes the
      RGB555 fallback. Binary-scanning every aligned ARM B/BL word in
      osos.dec finds exactly 10 unconditional bl call sites
      (0x08079cc4, 0x0807e458, 0x0807e4c8, 0x0807e6e8, 0x0807e744,
      0x080f1ea4, 0x080f1f08, 0x080f1f6c, 0x080f250c, 0x080f2700), with no
      predicated or tail branches. The port keeps the u32 ARM return
      register representation and adds no range or format validation.

  # --- packed RGBA4444 cursor reader ---
  - address: 0x0825ecc8
    ghidra: FUN_0825ecc8
    name: rgba4444_cursor_read_rgba8
    size: 20
    sig: "void rgba4444_cursor_read_rgba8(u8 *destination, u32 unused, const u16 **source_cursor)"
    module: cxx/color_unpack
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: unused (overwritten by the pixel load)
      param_3/r2: source_cursor
      "*source_cursor": packed_pixel_pointer
      uVar1/r1: packed_rgba4444
    notes: >-
      Raw ARM decoding confirms the complete 20-byte wrapper:
      `ldr r1,[r2]`, `add r3,r1,#2`, `str r3,[r2]`, `ldrh r1,[r1]`, then
      `b 0x0824bed8`. The preceding sibling ends at 0x0825ecc4 with
      `pop {r4,pc}`; 0x0825ecdc starts the next distinct cursor reader with
      `ldr r1,[r2]`; no literal pool is present. The target leaf starts at
      0x0824bed8, immediately after a predecessor's `bx lr`, and expands
      packed bits 15..12, 11..8, 7..4, and 3..0 to R/G/B/A bytes by
      duplicating each nibble. The port preserves the cursor-before-halfword
      read and RGBA output order while inlining that unported decoded leaf
      rather than adding a dispatch seam. Binary-scanning every aligned ARM
      B/BL word in osos.dec finds exactly nine direct unconditional bl call
      sites (0x0825f348, 0x0825f3fc, 0x0825f4b4, 0x0825f568, 0x0825f61c,
      0x0825f6d0, 0x082603b8, 0x083b4de0, 0x083b4eb0), with no predicated or
      direct-tail callers. The original has no NULL, alignment, or bounds
      guard. Host tests exhaust all 65,536 packed values, retain guards around
      the four-byte destination, and prove cursor advancement.

  # --- packed RGB555A1 cursor reader ---
  - address: 0x0825ecdc
    ghidra: FUN_0825ecdc
    name: rgb555a1_cursor_read_rgba8
    size: 20
    sig: "void rgb555a1_cursor_read_rgba8(u8 *destination, u32 unused, const u16 **source_cursor)"
    module: cxx/color_unpack
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: unused (overwritten by the pixel load)
      param_3/r2: source_cursor
      "*source_cursor": packed_pixel_pointer
      uVar1/r1: packed_rgb555a1
    notes: >-
      Raw ARM decoding confirms the complete 20-byte wrapper: `ldr r1,[r2]`,
      `add r3,r1,#2`, `str r3,[r2]`, `ldrh r1,[r1]`, then `b 0x0824bf18`.
      The preceding word, 0x0825ecd8, is a sibling's tail branch; the next
      function begins at 0x0825ecf0 with `cmn r0,#1`; no literal pool is
      present. The target leaf starts immediately after its predecessor's
      `bx lr` at 0x0824bf14 and expands bits 15..11, 10..6, and 5..1 into
      R/G/B bytes by repeating their top three bits; bit 0 selects alpha 0
      or 0xff. The port preserves the cursor-before-halfword-load order and
      RGBA output order while inlining that unported, decoded leaf instead of
      creating a dispatch seam. Binary-scanning every aligned ARM B/BL word
      in osos.dec finds exactly nine direct unconditional bl call sites
      (0x0825f840, 0x0825f8f4, 0x0825f9ac, 0x0825fa60, 0x0825fb14,
      0x0825fc28, 0x082603f0, 0x083b4f7c, 0x083b504c), with no predicated or
      direct-tail callers. The original has no NULL, alignment, or bounds
      guard. Focused host tests exhaust all 65,536 packed values, retain
      guards around the four-byte destination, and prove successive calls
      advance one halfword each.

  # --- the second 4-byte colour value copy ---
  - address: 0x082720ac
    ghidra: FUN_082720ac
    name: color_copy_unaligned
    size: 36
    sig: "void color_copy_unaligned(u8 *dst, const u8 *src)"
    module: cxx/color_copy
    status: ported
    renames:
      param_1/r0: dst (the destination colour)
      param_2/r1: src (the source colour)
    notes: >-
      The second ADS-emitted copy helper for the 4-byte RGBA colour value:
      four forward ldrb/strb pairs then bx lr, decoded directly from raw
      words (ldrb r2,[r1] / strb r2,[r0]; +1; +2; then ldrb r1,[r1,#3] /
      strb r1,[r0,#3]). The advertised 36-byte extent is exact: the prior
      word at 0x082720a8 is bx lr ending the RGB565 packer, and the next
      word at 0x082720d0 starts the four-component setter. There is no
      literal pool. Binary-scanning every aligned ARM B/BL word in osos.dec
      finds 20 unconditional bl and 2 unconditional b sites, no predicated
      calls. The b sites are the tail calls at 0x082a1d68 and 0x082a1d70.
      No aligned DATA word in the image equals 0x082720ac, so this helper
      is not virtually dispatched. It is byte-identical to color_copy at
      0x082720e8, but is separately ported because hooks target addresses,
      not source equivalence. The volatile ascending byte copies preserve
      the original's unaligned access and forward overlap propagation; no
      NULL or alignment guard was added. Its private ARM link section keeps
      LLVM MergeFunctions from folding this distinct hook target into
      color_copy. Host tests exercise opaque NUL/0xff bytes at all 16
      source/destination alignments with an end guard, and every overlapping
      pair against an independent forward-copy model.

  # --- the 4-byte colour value's copy ---
  - address: 0x082720e8
    ghidra: FUN_082720e8
    name: color_copy
    size: 36
    sig: "void color_copy(u8 *dst, const u8 *src)"
    module: cxx/color_copy
    status: ported
    renames:
      param_1/r0: dst (the destination colour)
      param_2/r1: src (the source colour)
    notes: >-
      The copy member of retailOS's 4-byte RGBA colour value class: four
      forward ldrb/strb pairs then bx lr, decoded from the raw words
      (ldrb r2,[r1] / strb r2,[r0]; +1; +2; then ldrb r1,[r1,#3] /
      strb r1,[r0,#3]). Extent - Ghidra's 36 bytes is exact on both
      sides for once: 0x082720e4 is the closing bx lr of the
      four-component setter @ 0x082720d0 and 0x0827210c is the push {lr}
      of the 28-byte record copy that follows; no literal pool. Call
      sites - 46 bl (44 unconditional plus 2 blne, at 0x0829190c and
      0x0829193c) and 2 unconditional b, binary-scanned by decoding
      every B/BL word in osos.dec. The two b sites are 0x08263198 and
      0x082631a0, the tails of the background and foreground draw-state
      colour setters. No DATA word in the image holds 0x082720e8, so it
      is never dispatched virtually.
      Why the class is a colour - the whole translation unit
      0x08272018-0x08272108 is one small value type - a pixel-format
      packer @ 0x08272018 masking its four components with 0xf8/0xfc/
      0xf0 for RGB565, RGBA4444 and RGB555, the bare RGB565 case @
      0x08272090 ((R & 0xf8) << 8 | (G & 0xfc) << 3 | (B & 0xf8) >> 3),
      a byte-identical twin at 0x082720ac (20 bl + 2 b sites), now ported
      as color_copy_unaligned, a four-component setter @
      0x082720d0 (strb r1,[r0]; strb r2,[r0,#1]; strb r3,[r0,#2];
      strb [sp],[r0,#3]) and this copy. So the record is four bytes
      wide, laid out {R,G,B,A}. Two call-site readings confirm alpha is
      last - 0x080749a8 and 0x080b31f0 build a colour with mov r3,#0xff
      before the constructor @ 0x08271de4 and copy it here, and
      cxx/draw_state's body initializer @ 0x082630f0 defaults the
      foreground to {0,0,0,0xff} and the background to {0xff x 4}.
      Why byte-at-a-time - draw-state colours are stored unaligned
      (a style byte at +0x10 sits immediately ahead of colours at +0x11
      and +0x15), so a 32-bit ldr/str pair would fault where the
      original does not. That is the entire reason this helper exists
      instead of a word assignment, and why the two setters reach it by
      add r0,r0,#0x11 / #0x15 and a tail b.
      Deviations - the byte moves are read_volatile/write_volatile
      (PORTING.md's defence against LLVM's loop-idiom pass rewriting a
      small copy into a memcpy call); this also pins the forward,
      overlap-propagating order, so an overlapping copy observes each
      store before the next load exactly as the instruction pairs do.
      Returns nothing - bx lr leaves r0 = dst, but none of the 46 bl
      sites reads it; every one overwrites r0 within an instruction or
      two, and the two that pop {..., pc} straight after the call
      (0x081ea170, 0x081ea2ec) are void functions whose own callers
      ignore the value. No NULL or alignment guard on either pointer.
      The byte-identical twin @ 0x082720ac is separately ported as
      color_copy_unaligned in the same module; its private ARM text section
      prevents LLVM MergeFunctions from folding its distinct hook target.
      Host tests (5) - a plain four-byte copy with a guard byte past the
      end, every-byte-value coverage including NUL and 0xff, all 16
      source/destination alignment combinations, every overlap of two
      colours inside one 8-byte buffer against an independent forward
      model, and a self-copy. cxx/draw_state_color's own 5 tests now
      exercise this port through the setter. match.py structural - the
      four ldrb/strb pairs match instruction-for-instruction and in the
      original's exact register order (r2 for the first three bytes,
      r1 reused for the fourth); the only diff is the crate-standard fp
      prologue/epilogue in place of the bare bx lr.

  # --- draw-state record: the foreground colour setter ---
  - address: 0x0826319c
    ghidra: FUN_0826319c
    name: draw_state_set_foreground_color
    size: 8
    sig: "void draw_state_set_foreground_color(u8 *record, const u8 *color)"
    module: cxx/draw_state_color
    status: ported
    renames:
      param_1/r0: record (the 0x44-byte draw-state record, cxx/draw_state)
      param_2/r1: color (four colour bytes)
      FUN_082720e8: color_copy (the shared unaligned 4-byte colour copy)
    notes: >-
      The foreground-colour setter of the 0x44-byte draw-state record
      (cxx/draw_state.rs). Whole function, decoded from the raw words -
      `add r0,r0,#0x11` then `b 0x082720e8`. NOT a veneer (no
      `ldr pc,[pc,#-4]` plus target word) and not an empty destructor - a
      two-instruction member function that binds one field offset and
      tail-branches into a shared copy helper. 68 bl call sites and 0 b,
      binary-scanned by decoding every B/BL word in osos.dec. No DATA word
      holds the address, so it is never dispatched virtually. Its immediate
      neighbour FUN_08263194 @ 0x08263194 is the identical shape with
      `#0x15` (22 bl call sites) - the background colour, not ported here.
      The record packs a style byte at +0x10 immediately ahead of two 4-byte
      colours, so both colours are UNALIGNED (+0x11..+0x14 and
      +0x15..+0x18) - which is exactly why the shared helper @ 0x082720e8
      (36 bytes, 46 bl + 2 b call sites) is four ldrb/strb pairs and a
      `bx lr` rather than a word move.
      Which colour is which - three independent readings agree that +0x11 is
      the foreground. (1) The record's body initializer @ 0x082630f0 writes
      +0x10..+0x13 = 0 and +0x14..+0x18 = 0xff, so the +0x11 colour defaults
      to {0,0,0,0xff} (opaque black) and the +0x15 colour to {0xff x 4}
      (opaque white) - text on paper. (2) The draw call @ 0x08262bdc hands
      the engine @ 0x080f1600 both colours as `add r2,r12,#0x11;
      add r3,r12,#0x15; strd r2,[sp]`, so the +0x11 colour is the FIRST of
      the pair. (3) The list-row draw path @ 0x0819b80c calls this setter
      with &view+0x22c, the row's text colour, and only when the row's style
      code says "a colour".
      Deviations - the tail-called helper @ 0x082720e8 is now ported under
      its own name as cxx/color_copy.rs's color_copy, and this setter calls
      it; the module's former private copy_color_bytes duplicate is gone.
      The byte moves, their forward overlap-propagating order and their
      read_volatile/write_volatile-ness all live in that module now. The
      original returns nothing - r0 survives the tail call as record+0x11,
      but no bl site reads it, so the port is void. No NULL guard on either
      pointer.
      Host tests (5) - the exact 4-byte write at +0x11 with the whole rest of
      the record proven untouched (style byte at +0x10 and background at
      +0x15 included), all 16 source/destination alignment combinations,
      every-byte-value coverage including NUL and 0xff, the forward
      overlap-propagation the byte-at-a-time order implies, and a seeded
      default-shaped record where repainting the text colour must not smear
      into the paper. match.py structural - `add r0, r0, #17` then the tail
      `b` to the helper; the diff is the crate fp frame alone.

  # --- draw-state record: the background colour setter ---
  - address: 0x08263194
    ghidra: FUN_08263194
    name: draw_state_set_background_color
    size: 8
    sig: "void draw_state_set_background_color(u8 *record, const u8 *color)"
    module: cxx/draw_state_color
    status: ported
    renames:
      param_1/r0: record (the 0x44-byte draw-state record, cxx/draw_state)
      param_2/r1: color (four colour bytes)
      FUN_082720e8: color_copy (the shared unaligned 4-byte colour copy)
    notes: >-
      The background-colour setter of the 0x44-byte draw-state record
      (cxx/draw_state.rs) - the byte-identical twin shape of the foreground
      setter @ 0x0826319c, binding the second colour slot. Whole function,
      decoded from the raw words: `add r0,r0,#0x15` then `b 0x082720e8`.
      NOT a veneer (no `ldr pc,[pc,#-4]` plus target word) and not an empty
      destructor. Extent is exactly 8 bytes - the very next word is the
      foreground setter's own `add r0,r0,#0x11`, so no trailing literal
      pool and no swallowed sibling; Ghidra's 8 is right. Call sites - 22
      bl and 0 b, binary-scanned by decoding every B/BL word in osos.dec:
      20 unconditional plus TWO blne, at 0x081286ec and 0x082917c4. Both
      predicated sites gate on a caller-side FLAG BIT, not a NULL check:
      0x081286ec is `ldrh r0,[r4,#20]; tst r0,#8; ... blne` (set the
      background only when style flag 0x8 says there is one, mirroring the
      foreground's flag 0x4 at 0x081286d8) and 0x082917c4 is
      `tst r1,#0x8000; ldrne r1,[pc,#36]; blne` (a flag-gated default
      colour from the literal pool). The setter itself checks nothing.
      No DATA word in the image holds 0x08263194, so it is never
      dispatched virtually.
      What "background" means - the body initializer @ 0x082630f0 defaults
      this slot to {0xff,0xff,0xff,0xff} (opaque white, the "paper" the
      opaque-black foreground prints on; cxx/draw_state_color.rs's
      three-way evidence), and the fill helper @ 0x08262b88 erases with
      exactly this colour pointer (this+0x15); sampled site 0x0815c2b0
      calls this setter and immediately fills - the set-then-erase idiom.
      Deviations - the tail-called helper @ 0x082720e8 is ported as
      cxx/color_copy.rs's color_copy, and this setter calls it; the byte
      moves, their forward overlap-propagating order and their volatility
      all live there. The original returns nothing - r0 survives the tail
      call as record+0x15, but no bl site reads it (both blne sites
      reload r0 within two instructions), so the port is void. No NULL
      guard on either pointer, matching the original.
      Host tests (5) - the exact 4-byte write at +0x15 with the whole rest
      of the record proven untouched (style byte at +0x10 and foreground
      at +0x11 included), all 16 source/destination alignment
      combinations, every-byte-value coverage including NUL and 0xff, the
      forward overlap-propagation the byte-at-a-time order implies, and a
      seeded default-shaped record where repainting the paper must not
      smear into the text colour. match.py structural - `add r0, r0, #21`
      then the tail `b` to the helper; the diff is the crate fp frame
      alone, identical to the accepted foreground twin's diff.

  # --- ARM constant-zero return ---
  - address: 0x080275f8
    ghidra: FUN_080275f8
    name: return_zero
    size: 8
    sig: "u32 return_zero()"
    module: runtime/return_zero
    status: ported
    renames:
      r0: zero_result
    notes: >-
      8-byte ARM leaf: `mov r0,#0; bx lr`. It unconditionally produces the
      zero-valued u32 result in r0 and reads neither arguments nor memory.
      The recovered caller at 0x082fc020 carries six apparent values into
      this target, but the two-instruction body discards their argument
      registers; the no-argument ABI and return_zero name therefore express
      the entire observed behavior. No deviations; focused host tests assert
      the zero word and its all-clear bit representation.

  # --- JPEG decoder: the source byte reader ---
  # The decoder is one global @ 0x08a0a79c with no instance handle: the
  # marker scan (0x0807a7b0), the entropy bit reader (0x0807ec24), DHT
  # (0x080e9eb4) and the unnamed SOF/SOS/DQT blocks in
  # 0x080ea000-0x080ea400 / 0x080ed100-0x080ed320 all reach it through a
  # literal-pool word. Identified as JPEG by 0x0807ec24's `0xff 0x00`
  # unstuffing, 0x080e9eb4's Tc/Th nibble split into separate DC and AC
  # table arrays, and the "jpeg"/"tiff"/"pict" format-name table
  # @ 0x08117cec. Source bytes are produced by 0x08086ed0.
  - address: 0x080e7ed4
    ghidra: FUN_080e7ed4
    name: jpeg_source_read_byte
    size: 96
    sig: "u32 jpeg_source_read_byte()"
    module: jpeg/source
    renames:
      DAT_080e7f30: JPEG_DECODER_STATE
      uVar2: position
      uVar4: queue
      iVar3: bank
    status: ported
    notes: >-
      The JPEG decoder's "next source byte" primitive, and the consumer
      end of its 2 KiB circular double buffer. Body decoded from the raw
      image, not from Ghidra - true extent is 96 bytes (92 of code
      0x080e7ed4-0x080e7f2c plus the state-pointer literal 0x08a0a79c
      @ 0x080e7f30); functions.csv reports 92 and drops the pool word,
      and the next function starts at 0x080e7f34 (`mov r2,r0; mov r0,r1;
      ldr r1,[r2,#12]; b 0x08065054`). Algorithm - `pos = state+0x84`;
      when `pos & 3 == 0` reload `state+0x88` with the word at
      `(pos & 0x400 ? state+0x68 : state+0x64) + (pos & 0x3ff)`; then
      return `queue & 0xff`, store `pos+1` and `queue >> 8`. So
      `state+0x88` is a four-byte little-endian byte queue and
      `state+0x84` a free-running byte counter that is never masked in
      place - only the buffer index and the bank-select bit are derived
      from it, which is what lets DHT @ 0x080e9eb4 push a marker back
      with `queue = queue << 8 | 0xff; pos -= 1`. The bank select is
      `lsls r2,r1,#21` reading the sign bit, i.e. bit 10 of the position,
      not a comparison against the fill level. The producer @ 0x08086ed0
      mirrors it exactly - it memcpys 0x400-byte chunks alternately into
      `+0x64` and `+0x68`, tracks its own fill position at `+0x8c`,
      selects the destination bank with the same bit-10 test, and stalls
      while `fill - read > 0x3ff`. There is no bounds, NULL or
      end-of-data check anywhere in the body; call sites counted by
      decoding every ARM B/BL word in osos.dec are 60 plain `bl` with
      ZERO predicated forms and ZERO `b`, so no caller guards it either -
      exhaustion is detected by the decoder's marker logic instead.
      Faithful detail - the original reloads `state+0x84` a second time on
      the refill path (`add r0,r3,#0; ldr r0,[r0,#0x84]` re-reads what r1
      already holds), a redundant ADS reload folded to one read.
      Deviations - the state lives at 0x08a0a79c on device and in host
      test storage otherwise (cxx/object_flags.rs precedent), and only the
      four words this function touches are modeled, as a #[repr(C)]
      window struct with named fields so the target offsets 0x64/0x68/
      0x84/0x88 are asserted rather than hand-written per access.
      Host tests (10) - byte order within the reloaded word, a mid-word
      start consuming the stale queue before the next refill, the bit-10
      bank switch, the 2 KiB alias, two unrelated bank pointers, the
      DHT push-back sequence, exact one-per-call position advance, u32
      position wrap, and all 256 byte values returned zero-extended.
      match.py structural - `tst r1,#3` refill test, `tst r1,#0x400` with
      predicated bank selection, masked index, word load, then the
      `+1` / `>> 8` / `& 0xff` tail, one-to-one with the original;
      LLVM folds the index mask to `and r3,r1,#0x3fc` (equivalent, since
      the refill only runs on a word boundary) and adds the crate frame.
      #[inline(never)] for the 60 `bl` targets.

  # --- OpenSSL object database: the NID lookup ---
  # Apple vendored OpenSSL; its object-identifier database sits in
  # 0x0805e000-0x08064000 (OBJ_obj2nid @ 0x0805f074, OBJ_obj2txt @
  # 0x0805f110, OBJ_ln2nid @ 0x0805edc4, OBJ_sn2nid @ 0x0805f2c4, all
  # over the generic OBJ_bsearch @ 0x0805eb04) with the lhash machinery
  # in 0x082d7xxx (lh_retrieve @ 0x082d7e0c, getrn @ 0x080e82cc).
  # Identifying strings, binary-verified: "ssl2-md5"/"ssl3-md5"/
  # "ssl3-sha1"/"RSA-SHA1" @ 0x0805f574 and "signature has problems,
  # re-make with post SSLeay045" @ 0x08063268; Apple's own arc is
  # "1.3.6.1.4.1.63.42" / "iPod Serial Number" @ 0x080782bc.
  - address: 0x082d7c48
    ghidra: FUN_082d7c48
    name: lh_insert
    size: 192
    sig: "void *lh_insert(LHASH *table, void *data)"
    module: crypto/obj_dat
    status: ported
    renames:
      param_1/r0/r4: table
      param_2/r1/r6: data
      uVar1/r0: load_ratio
      piVar2/r0/r5: bucket
      puVar4/r0: node
      FUN_08036f14: __rt_udiv
      FUN_080e8ee4: lh_expand
      FUN_080e82cc: getrn
      FUN_08043c18: traced_alloc
    notes: >-
      OpenSSL lhash.c lh_insert. Raw ARM extent is exactly 192 bytes:
      0x082d7c48 through 0x082d7d04, and the separately linked successor
      starts at 0x082d7d08. It clears error (+0x5c), expands with
      lh_expand @ 0x080e8ee4 when num_items(+0x24) << 8 divided by
      num_nodes(+0x0c) reaches up_load(+0x1c), then asks getrn @
      0x080e82cc for the bucket word. A hit replaces node->data and
      increments num_replace(+0x44), returning the prior data. A miss
      allocates 12 bytes through traced_alloc @ 0x08043c18, initializes
      {data, NULL}, installs it in the bucket, and increments num_insert
      (+0x40) and num_items; failure increments error and returns NULL.
      Decoding every ARM B/BL word in osos.dec confirms eight unconditional
      BL callers and one BLNE @ 0x080efb34, plus a predicated BVC tail
      branch @ 0x089390b8. Deliberate deviation: unported lh_expand and
      getrn are volatile seams; traced_alloc and __rt_udiv are existing
      direct ports. Host test covers replacement and counter wrap, failure
      after error clearing, exact 12-byte allocator request and tags, new
      node initialization, and equality-triggered expansion.
  - address: 0x082d7e0c
    ghidra: FUN_082d7e0c
    name: lh_retrieve
    size: 72
    sig: "void *lh_retrieve(LHASH *table, const void *key)"
    module: crypto/obj_dat
    status: ported
    renames:
      param_1/r0/r4: table
      param_2/r1: key
      uStack_10/sp: hash
      piVar1/r0: bucket
      "*piVar1": node
    notes: >-
      OpenSSL lhash.c lh_retrieve. Raw ARM extent is exactly 72 bytes
      (0x082d7e0c-0x082d7e54); the separately linked successor begins at
      0x082d7e54. Clears table->error (+0x5c), calls getrn @ 0x080e82cc
      with a stack hash output, reads the resulting bucket's node, returns
      node->data and increments num_retrieve (+0x50), or returns NULL and
      increments num_retrieve_miss (+0x54) when the node is NULL. Every
      ARM B/BL word in osos.dec was decoded: 13 unconditional BL callers,
      zero predicated forms and zero tail branches. getrn has no existing
      ledger entry, so the port reaches that verified unported target through
      the volatile LHASH_GETRN seam; the target default calls it in place and
      host tests install bucket resolvers. Tests cover error clearing, hit,
      miss, a non-NULL node with NULL data (still a hit), counter updates and
      the existing object-database callers' key/result integration. match.py
      review: both implementations clear error, call the resolver, load its
      bucket, branch on node presence, then increment/return by outcome;
      LLVM adds its volatile seam load and frame, producing 27 instructions
      versus the original 18.
  - address: 0x0805ef18
    ghidra: FUN_0805ef18
    name: obj_nid2obj
    size: 172
    sig: "ASN1_OBJECT *obj_nid2obj(int nid)"
    module: crypto/obj_dat
    vars: {param_1: nid}
    renames:
      DAT_0805efbc: NID_OBJS
      DAT_0805efc0: ADDED_SLOT
      local_1c: key_nid
      local_c: key_kind
      local_8: key_object
    status: ported
    notes: >-
      OpenSSL's OBJ_nid2obj from crypto/objects/obj_dat.c. Raw ARM extent is
      172 bytes: code 0x0805ef18-0x0805efb8 followed by literal-pool words
      0x0805efbc (NID_OBJS = 0x08906668) and 0x0805efc0 (ADDED_SLOT =
      0x08a0c334); Ghidra's 164-byte extent omits both words, and the
      separately linked next function starts at 0x0805efc4. For unsigned
      nid < NUM_NID (650), it computes NID_OBJS + nid * sizeof(ASN1_OBJECT)
      and returns that entry if nid == NID_undef or entry->nid != NID_undef.
      An unresolved in-range entry records error (8, 0x67, 0x65, 0, 0) and
      returns NULL without probing added. Out-of-range, including negative,
      nids probe lh_retrieve(added, { ADDED_NID = 3, &nid }) when added is
      non-NULL; a hit returns adp->obj, while a miss emits the same error and
      returns NULL. Identification is corroborated by the adjacent
      OBJ_obj2nid @ 0x0805f074, shared ASN1_OBJECT nid field at +0x08, shared
      added slot, and lh_retrieve @ 0x082d7e0c documented in obj_dat.rs.
      Call sites binary-verified by decoding every ARM B/BL word in
      osos.dec: exactly 18 plain unconditional bl, ZERO predicated forms and
      ZERO plain b, so callers rely on this function's internal failure
      result. Deliberate deviation: the raw body stores nid at sp+0xc but
      hands the ADDED_NID key an object pointer to uninitialized sp+4; sibling
      FUN_0805ee68 repeats this sequence. Rust passes &nid per OpenSSL source
      rather than reproduce undefined stack contents. Device NID_OBJS remains
      the original absolute array; host tests install an inline fixture.

  - address: 0x0805f074
    ghidra: FUN_0805f074
    name: obj_obj2nid
    size: 156
    sig: "int obj_obj2nid(const ASN1_OBJECT *object)"
    module: crypto/obj_dat
    vars: {param_1: object}
    renames:
      DAT_0805f100: ADDED_SLOT
      DAT_0805f104: obj_cmp
      DAT_0805f108: NUM_OBJ
      DAT_0805f10c: OBJ_OBJS
      local_14: key_kind
      local_10: key_object
      piVar2: slot
    status: ported
    notes: >-
      OpenSSL's `OBJ_obj2nid` from crypto/objects/obj_dat.c, verbatim -
      `if (a == NULL) return NID_undef; if (a->nid != 0) return a->nid;
      if (added != NULL) { ad.type = ADDED_DATA; ad.obj = a; adp =
      lh_retrieve(added, &ad); if (adp) return adp->obj->nid; } op =
      OBJ_bsearch(&a, obj_objs, NUM_OBJ, 4, obj_cmp); return op ?
      (*op)->nid : NID_undef;`. Body decoded from the raw image - true
      extent is 156 bytes (140 of code 0x0805f074-0x0805f0fc plus the
      four literal-pool words 0x0805f100-0x0805f10c); functions.csv
      reports 140 and drops the pool, and the next function
      (OBJ_obj2txt) starts at 0x0805f110. Evidence for the
      identification - the layout the body walks is ASN1_OBJECT's, with
      `nid` @ +0x08 here and `length` @ +0x0c / `data` @ +0x10 read off
      the same pointer by OBJ_obj2txt @ 0x0805f110, which then decodes
      the DER OID (`b & 0x7f` accumulated with `<< 7` per continuation
      byte, first arc split by 40 through __rt_sdiv @ 0x08036f14,
      formatted with "%d.%lu" @ 0x0805f2b4); 0x082d7e0c is lh_retrieve
      (clears lh->error @ +0x5c, calls getrn @ 0x080e82cc, bumps
      num_retrieve_miss @ +0x54 or num_retrieve @ +0x50); 0x0805eb04 is
      OBJ_bsearch (lo/hi halving, `mla` for base + size*mid, `blx`
      through the stacked fifth argument); and the siblings share the
      machinery with a different ADDED_OBJ tag and table - OBJ_sn2nid
      @ 0x0805f2c4 (tag 1, sn_objs @ 0x08a0c33c, 643 entries) and
      OBJ_ln2nid @ 0x0805edc4 (tag 2, ln_objs @ 0x08a0cd48, 643
      entries). NUM_OBJ = 617 with NUM_SN = NUM_LN = 643 dates the
      vendored copy to the 0.9.6/0.9.7 era, where obj_objs is an array
      of `ASN1_OBJECT *` rather than the `unsigned int` index array
      0.9.8 switched to - hence the two-load hit path `(*op)->nid` and
      hence the table living in RW data @ 0x08a0d754 filled by the ADS
      runtime initializers. Note the shared tail - the added-hash hit
      and the table hit both land on `ldr r0,[r0,#8]`, one dereference
      apart. Call sites counted by decoding every ARM B/BL word in
      osos.dec - 57 plain `bl`, ZERO predicated forms and ZERO `b`
      (callers pass possibly-NULL objects and rely on the internal
      guard). UNRESOLVED ANOMALY, recorded rather than guessed at - the
      comparator in the literal pool is 0x080e1168, and that address
      does NOT decode as a function entry in our decrypted image: it
      lands mid-body in the 0x080df818-0x080e1318 block, on a
      `bl 0x0804d17c` trace call. The same holds for every comparator
      this bsearch family passes (0x080de770 for sn_objs, 0x080de478
      for ln_objs, 0x080e1158 and 0x080e0cf0 for two other tables), and
      an image-wide scan finds no cluster of comparator-shaped leaf
      functions near any of them. Since the target cannot be read, the
      port implements upstream's obj_cmp - encoded length first, then
      memcmp over `data` - which the +0x0c/+0x10 layout recovered from
      OBJ_obj2txt corroborates. Other deviations - `added` and
      `obj_objs` live at 0x08a0c334+4 and 0x08a0d754 on device and in
      host test storage otherwise (cxx/object_flags.rs precedent), with
      the host table starting empty so an uninstalled fixture misses
      instead of dereferencing a null base; the unported lh_retrieve
      rides the OBJ_ADDED_RETRIEVE seam (house pattern); and
      OBJ_bsearch plus its comparator are inlined rather than called
      out, since the generic bsearch is shared with OBJ_sn2nid /
      OBJ_ln2nid and belongs with them, and the key indirection (`&a`
      plus the comparator's `*(ASN1_OBJECT * const *)`) folds away to
      passing the object directly. Host tests (12) - NULL, an
      already-resolved nid (positive and negative, since the early-out
      tests `!= 0`), every entry of a sorted table found by its encoded
      OID, four flavours of miss, the empty table, a full 617-entry
      table exercising every search path, added-hash hit shadowing the
      static table with the key's ADDED_DATA tag asserted, added-hash
      miss falling through, the NULL-`added` short circuit (proven by
      the spinning seam never being reached), length-before-bytes
      ordering, and a one-entry table. match.py structural - the NULL
      and `nid != 0` early-outs correspond one-to-one; the remainder is
      the inlined added-hash probe (`NUM_OBJ` materialized as
      `mov r8,#105; orr r8,r8,#512`) and the inlined halving search.
      #[inline(never)] for the 57 `bl` targets.

  # --- OpenSSL BIO layer: the formatted-write front end ---
  # The bio_lib.c/b_print.c cluster sits in 0x0803d294-0x0803da74:
  # BIO_ctrl @ 0x0803d294 (callback code 6 = BIO_CB_CTRL), BIO_push @
  # 0x0803d69c (links next_bio +0x24 / prev_bio +0x28, notifies with
  # BIO_ctrl(b, BIO_CTRL_PUSH=6, 0, 0)), BIO_puts' variadic twin @
  # 0x0803d954, BIO_vprintf @ 0x0803d970 (doapr engine @ 0x080e7a68
  # into a 2048-byte stack buffer, growing into a traced_free'd
  # allocation, "doapr()" literal @ 0x0803da1c), BIO_write @
  # 0x0803da74 (callback code 3 = BIO_CB_WRITE, method bwrite at
  # vtable+0x08, num_write byte counter at bio+0x34; failures log
  # through diag_ring_record with facility 32 = ERR_LIB_BIO and
  # bio_lib.c line numbers 113/120/121).
  - address: 0x0803d270
    ghidra: FUN_0803d270
    name: bio_copy_next_retry
    size: 36
    sig: "void bio_copy_next_retry(BIO *bio)"
    module: crypto/bio_copy_next_retry
    status: ported
    renames:
      param_1/r0: bio
      r1: bio->next_bio
      r2: bio->flags
      r3: bio->next_bio->flags
    notes: >-
      OpenSSL BIO retry propagation helper. Raw ARM confirms the exact
      36-byte extent (0x0803d270..0x0803d294): the next instruction is the
      separately linked BIO_ctrl at 0x0803d294, with no literal pool. Binary
      decoding every B/BL word in osos.dec finds exactly 12 direct call
      sites, all unconditional bl, no predicated calls, no tail branches, and
      no raw image data word holding the address. The unguarded body loads
      bio->next_bio, ORs its flags & 0x0f into bio->flags without clearing
      existing bits, and copies its retry_reason word to bio->retry_reason.
      The port preserves those unguarded reads and all-word behavior; host
      tests map a two-BIO raw-u32 chain below 4 GiB to preserve target pointer
      layout.
  - address: 0x0803d294
    ghidra: FUN_0803d294
    name: bio_ctrl
    size: 196
    sig: "int bio_ctrl(BIO *bio, int cmd, long larg, void *parg)"
    module: crypto/bio_ctrl
    status: ported
    renames:
      param_1/r0/r5: bio
      param_2/r1/r6: cmd
      param_3/r2/r7: larg
      param_4/r3/r8: parg
      r4: callback
      "*(bio->method + 0x18)": method_ctrl
    notes: >-
      OpenSSL BIO_ctrl: method control dispatch with BIO callback wrapping
      (BIO_CB_CTRL = 6, BIO_CB_RETURN = 0x80). Raw ARM confirms the
      196-byte extent exactly (0x0803d294..0x0803d358): its successor is
      the separately linked 8-byte veneer at 0x0803d358, not a trailing
      literal pool. Binary decoding every B/BL word in osos.dec finds
      exactly 18 direct call sites, all unconditional bl, no predicated
      calls, no tail branches, and no raw image data word holding the
      address. Null bio returns 0; missing bio->method or method->ctrl logs
      diag_ring_record(32, 0x67, 0x79, 0, 0) and returns -2. A callback
      preflight receives (bio, 6, parg, cmd, larg, 1) and non-positive
      results bypass method->ctrl; otherwise the callback's postflight
      (bio, 0x86, parg, cmd, larg, ctrl_result) replaces the ctrl result.
      Host tests use a typed host dispatch model because the retail layout
      deliberately keeps the callback and ctrl entries as 32-bit words;
      target builds transmute and invoke those original words directly.
  - address: 0x0803d558
    ghidra: FUN_0803d558
    name: bio_new
    size: 104
    sig: "BIO *bio_new(BIO_METHOD *method)"
    module: crypto/bio_new
    status: ported
    renames:
      param_1/r0/r5: method
      r4: bio
      FUN_08043c18: traced_alloc
      FUN_0803d8c0: bio_set
      FUN_08043994: traced_free
    notes: >-
      OpenSSL BIO_new. Raw ARM extent is exactly 0x0803d558..0x0803d5c0:
      the separately linked successor starts at 0x0803d5c0, so Ghidra's
      104-byte extent is correct. Binary decoding every ARM B/BL word in
      osos.dec finds exactly 9 direct inbound calls, all unconditional bl
      (0x0803d634, 0x0805ffdc, 0x0806006c, 0x080602d0, 0x0806030c,
      0x082728e8, 0x082728f8, 0x082d44b8, 0x082d45b0); no predicated calls,
      tail branches, or raw-image data words hold this address. Allocates
      64 bytes via traced_alloc(0x40, 0, 0), logs (0x20, 0x6c, 0x41, 0, 0)
      and returns NULL on allocation failure, then invokes BIO_set
      @ 0x0803d8c0. A zero initializer result frees the exact block through
      traced_free and returns NULL; any nonzero result returns the block.
      BIO_set remains unported: target builds invoke its verified original
      entry directly, while host tests replace the call boundary.
  - address: 0x0803d680
    ghidra: FUN_0803d680
    name: bio_printf
    size: 28
    sig: "int bio_printf(BIO *bio, const char *format, ... /* args: VaList */)"
    module: crypto/bio_printf
    status: ported
    renames:
      param_1/r0: bio
      param_2/r1: format
      "r2/r3 + stack": args (the va_list the spill frame builds)
    notes: >-
      OpenSSL's `BIO_printf` from crypto/bio/b_print.c - the variadic
      front end of the vendored BIO layer's formatted write (identity
      evidence: "doapr()" literal in the worker, callback codes 3 =
      BIO_CB_WRITE / 6 = BIO_CB_CTRL and BIO_CTRL_PUSH = 6 across the
      sibling cluster, ERR_LIB_BIO = 32 diag records with bio_lib.c
      line numbers, X.509v3 GeneralName callers). Body decoded from the
      raw image - true extent is 28 bytes (0x0803d680..0x0803d69c), no
      literal pool; BIO_push starts at 0x0803d69c with its own `push`,
      so Ghidra's 28 is right. Textbook AAPCS variadic prologue:
      `push {r0-r3}; push {r4,lr}; ldr r1,[sp,#12]` (the format);
      `add r2,sp,#16` (&spilled r2 = the va_list); `bl 0x0803d970`;
      `pop {r4}; ldr pc,[sp],#20` - forwards (bio, format, ap) to
      BIO_vprintf and returns its result (the BIO_write byte count, or
      <= 0 on failure) untouched; nothing is validated. Call count
      binary-verified by decoding every B/BL word in osos.dec: exactly
      25 sites, ALL unconditional `bl` (no predicated forms, no tail
      branches) - no caller NULL-guards or flag-gates the call. No DATA
      word in the image holds the address, so it is never dispatched
      virtually. Callers are the X.509v3 display code ("email: %s" /
      "DNS: %s" / "URI: %s" / "DirName: " @ 0x08051098, certificate
      time "%s %2d %02d:%02d:%02d %d%s" @ 0x08039cac, which treats a
      result < 1 as failure). Deviations: the `...` becomes an explicit
      `args: VaList` (*const u32, the house convention - exactly the
      pointer the spill frame manufactures; a variadic C caller needs
      the capture trampoline printf/printf_api.rs documents);
      BIO_vprintf @ 0x0803d970 is unported, so it rides the BIO_VPRINTF
      seam (target default calls 0x0803d970 in place; host panics until
      a test installs a mock). Host tests: bio/format/args forwarded
      untouched with the worker's count returned, a -1 write failure
      propagated unchanged, NULL bio + NULL format passed straight
      through, and the slot re-read on every call. match.py structural
      - the Rust veneer is a volatile slot load plus a tail `bx r3`
      instead of the spill frame (by design: the explicit VaList IS the
      spill's product); 7 instructions either way.
      #[inline(never)] for the 25 `bl` targets.

  - address: 0x0803d954
    ghidra: FUN_0803d954
    name: bio_snprintf
    size: 28
    sig: "int bio_snprintf(char *buf, size_t size, const char *format, ... /* args: VaList */)"
    module: crypto/bio_snprintf
    status: ported
    renames:
      param_1/r0: buf
      param_2/r1: size
      param_3/r2: format
      "r3 + stack": args (the va_list the spill frame builds)
      FUN_0803da24: bio_vsnprintf
    notes: >-
      OpenSSL's BIO_snprintf from crypto/bio/b_print.c, the bounded,
      caller-buffer variadic front end of the BIO formatter (identity
      evidence: it is the four-argument sibling of BIO_printf at
      0x0803d680 and calls the b_print.c BIO_vsnprintf worker at
      0x0803da24, whose next sibling is BIO_write). Raw ARM confirms
      Ghidra's 28-byte extent: 0x0803d954..0x0803d970 is seven
      instructions with no literal pool, followed by separately linked
      BIO_vprintf. `push {r0-r3}; push {r4,lr}; ldr r2,[sp,#16]`
      reloads format; `add r3,sp,#20` forms the va_list from spilled r3;
      `bl 0x0803da24` forwards (buf, size, format, args); `pop {r4};
      ldr pc,[sp],#20` returns that result untouched. Ghidra incorrectly
      declares it void. Call count binary-verified by decoding every ARM
      B/BL word in osos.dec: exactly 16 direct BL calls, 14 unconditional
      bl and two bleq at 0x08049750 / 0x08049768, no tail branches. Those
      BLEQ sites synthesize fallback text only if their preceding helper
      returned NULL; the veneer itself validates nothing. No image DATA
      word equals 0x0803d954, so it is not virtually dispatched.
      Deviation: Rust receives explicit `args: VaList` (*const u32), the
      exact product of the retail spill frame; a real variadic caller needs
      the printf_api capture trampoline. The unported BIO_vsnprintf worker
      rides a volatile BIO_VSNPRINTF seam: target default calls 0x0803da24
      in place; host default panics until a test installs it. Host tests
      prove forwarding without buffer access, return-value propagation,
      NULL plus zero-size forwarding, and volatile slot reload across calls.
      #[inline(never)] retains a concrete target for all 16 BL call sites.

  # --- OpenSSL bignum: the bit-length query ---
  # The bn_lib.c cluster sits in 0x0803d800..0x08041000 on the
  # { BN_ULONG *d; int top; int dmax; int neg; int flags } BIGNUM
  # (the multiplier @ 0x080400b0 EORs the neg words @ +0x0c, tests
  # flags @ +0x10 against 2 = BN_FLG_STATIC_DATA). BN_num_bits_word
  # @ 0x080404dc is the limb bit-length helper (most-significant-
  # nonzero-byte select into a 256-byte bits[] table); BN_num_bytes
  # @ 0x08063040 is (BN_num_bits + 7) / 8; the X.509 chain walk
  # FUN_082d48ac @ 0x082d48ac requires BN_num_bits(key) == 0x400,
  # the RSA-1024 modulus check.
  - address: 0x080404a8
    ghidra: FUN_080404a8
    name: bn_num_bits
    size: 52
    sig: "int bn_num_bits(const BigNum *a)"
    module: crypto/bn_num_bits
    status: ported
    renames:
      param_1/r0: a
      FUN_080404dc: bn_num_bits_word (the callee; BN_num_bits_word from bn_lib.c)
    notes: >-
      OpenSSL's `BN_num_bits` from crypto/bn/bn_lib.c, verbatim minus
      the compiled-out bn_check_top assert - part of the SSLeay-era
      libcrypto Apple vendored (identity evidence: the BIGNUM layout
      the surrounding 0x0803d800..0x08041000 cluster operates on, the
      BN_num_bits_word byte-table helper @ 0x080404dc, the
      (bits + 7) / 8 BN_num_bytes caller @ 0x08063040, and the
      RSA-1024 `== 0x400` check in the X.509 chain walk @ 0x082d48ac).
      Body decoded from the raw image: 13 words, no literal pool of
      its own, and the next entry is the callee BN_num_bits_word
      itself (a separately linked function with 4 other callers), so
      Ghidra's 52-byte extent is exactly right. Algorithm: top == 0
      (BN_is_zero) returns 0 via moveq/popeq BEFORE d is loaded;
      otherwise return (top - 1) * 32 + BN_num_bits_word(d[top - 1]).
      Call count binary-verified by decoding every B/BL word in
      osos.dec: exactly 22 sites, ALL unconditional `bl` (no
      predicated forms, no tail branches) - no caller NULL-guards or
      flag-gates the query, and none is needed (no NULL check exists).
      No DATA word in the image holds 0x080404a8, so it is never
      dispatched virtually. ANOMALY, documented not explained: the
      helper's bits[] table literal @ 0x08040514 is 0x08906520, which
      at rest lands mid-string in the Italian UI heap ("tata." inside
      "...non e' piu' supportata."); no instruction writes a table
      there and no canonical {0,1,2,2,3,3,3,3,...} byte run exists
      anywhere in osos.dec, so the runtime content must be installed
      by a loader pass this port does not model. The port therefore
      reaches the helper through the BN_NUM_BITS_WORD slot whose
      target default calls the stock 0x080404dc in place - device
      behavior is bit-exact whatever lives at 0x08906520. Upstream
      bits[b] = floor(log2(b)) + 1 with bits[0] = 0, so a zero top
      limb contributes nothing and a 32-limb value with bit 1023 set
      measures 31*32 + 32 = 0x400, matching the caller. Deviations:
      the `bl 0x080404dc` becomes a volatile slot load + `blx` (the
      documented seam shape, same as crypto/bio_printf.rs); the
      top-limb index arithmetic is wrapping_* so a bogus top < 0
      cannot trip host overflow checks (same single sub/lsl on
      target). Host tests: top == 0 returns 0 without touching the
      worker or the limb pointer (NULL d), the most significant limb
      alone reaches the worker, the (top - 1) << 5 scaling, the slot
      re-read on every call, and end-to-end OpenSSL semantics with the
      upstream table's behavior installed (RSA-1024 modulus == 0x400,
      full top limb 0x400, one bit less 0x3ff, value 1 == 1 bit, zero
      top limb contributes 0). match.py structural - the early-out,
      limb select, and i*32 add correspond one-to-one; the diff is
      the seam's indirect call and LLVM's shrink-wrapped frame.
      #[inline(never)] for the 22 `bl` targets.
  - address: 0x08040ddc
    ghidra: FUN_08040ddc
    name: bn_ucmp
    size: 88
    sig: "int bn_ucmp(const BigNum *a, const BigNum *b)"
    module: crypto/bn_ucmp
    status: ported
    renames:
      param_1/r0: a
      param_2/r1: b
    notes: >-
      OpenSSL's `BN_ucmp` from crypto/bn/bn_lib.c, verbatim minus the
      compiled-out bn_check_top asserts - same SSLeay-era libcrypto
      bignum cluster as bn_num_bits (0x0803d800..0x08041000, BIGNUM
      d/top/dmax/neg/flags layout). Body decoded from the raw image:
      22 words, no literal pool, and the next entry @ 0x08040e34 is a
      separately linked function with its own push {r3-r9, lr}
      prologue and callers, so Ghidra's 88-byte extent is exactly
      right. Algorithm: if a->top - b->top != 0 return the RAW signed
      difference (not a normalized sign - e.g. 5 or -3); otherwise
      scan limbs i = top - 1 down to 0, and at the first wa != wb
      return +1/-1 by UNSIGNED comparison (mvnls/movhi), else 0.
      Magnitude only: the neg word @ +0x0c is never read (that is the
      `u` in BN_ucmp; BN_cmp @ 0x08040d68 handles signs first). The
      most-significant-first scan means the highest differing limb
      decides and lower limbs are never read; equal tops <= 0 return
      0 without dereferencing d. Call count binary-verified by
      decoding every B/BL word in osos.dec: exactly 15 sites, ALL
      unconditional `bl` (cond 0xe) - no caller NULL-guards or
      flag-gates the compare, and none is needed (no NULL check
      exists). No DATA word in the image holds 0x08040ddc, so it is
      never dispatched virtually. Deviations: none beyond wrapping_*
      index arithmetic (same single sub on target; a bogus negative
      top cannot trip host overflow checks). Host tests: raw signed
      top difference both directions, both-zero-top equality with
      NULL limb buffers, most-significant-difference precedence,
      least-significant-limb scan to index 0, unsigned limb semantics
      (0x80000000 > 0x7fffffff), identical bignums equal, and 500
      deterministic xorshift cases against a textbook reference.
      match.py structural - the early-out, descending scan and
      mvn/movhi -1/+1 correspond one-to-one; the diff is LLVM's
      shrink-wrapped frame and rotated loop. #[inline(never)] for the
      15 `bl` targets.
  # --- display object: the six-layer compositor root ---
  - address: 0x081d892c
    ghidra: FUN_081d892c
    name: display_set_layer_enabled
    size: 124
    sig: "void display_set_layer_enabled(Display *display, u32 layer_index, u32 enabled)"
    module: drivers/display
    status: ported
    renames:
      param_1/r3: display
      param_2/r1: layer_index
      param_3/r2: enabled
    notes: >-
      Sets one secondary-display layer's requested-enable byte. EXTENT
      CORRECTED - Ghidra reports 164 bytes but raw disassembly puts this
      body at 0x081d892c..0x081d89a8 (31 instructions, 124 bytes); the
      sibling 0x081d89a8 opens with `push {r4,r5,r6,lr}`. Call count was
      verified by decoding every ARM B/BL word in osos.dec: 20 `bl` callers
      (19 plain plus one `blne` at 0x0811faac), and no tail `b` callers.
      That lone predicated site is layer_render's gated notification; this
      callee has no NULL guard. Algorithm - return unless display+0x94
      (panel driver) is non-NULL and display+0x90 == 1. Store enabled != 0
      as 1/0 at display+0x18+layer_index. An enabled request tail-calls
      0x081d914c iff +0x98 is clear. A disabled request returns while +0x50
      is non-NULL; otherwise 0x081d8e04 scans all six +0x18 bytes, and when
      they are all zero it tail-calls 0x081d9270 iff +0x98 is set.
      New Display fields name +0x50 layer_activity_blocker and +0x98
      layers_active; repr(C) preserves target offsets while keeping the
      pointer field native-width on hosts. Deliberate deviations: the
      44-byte scan helper 0x081d8e04 is inlined, while the two unported
      activity routines dispatch through narrow volatile DisplayHooks slots;
      their defaults preserve +0x98 but omit panel-driver and layer work.
      Host tests cover NULL-driver and non-secondary gates, nonzero enable
      normalization and idempotent start, last-disable stop only after all
      six bytes clear, and blocker-deferred teardown. match.py structural
      review used the raw --size 124 and this worktree archive: the
      driver/id gates, +0x18 byte store, +0x98 and +0x50 tests, six-byte
      scan, and tail hook dispatches correspond; LLVM adds its fp frame and
      the volatile hook loads.

  - address: 0x081d9064
    ghidra: FUN_081d9064
    name: display_get_layer
    size: 96
    sig: "u8 *display_get_layer(Display *display, u32 index)"
    module: drivers/display
    status: ported
    renames:
      param_1/r4: display
      param_2/r5: index
      cVar1: display_id
      uVar2: layer
    notes: >-
      The display object's lazy per-layer accessor - returns layer `index`
      of `display`, constructing it on first request under the display's
      own mutex. EXTENT - 96 bytes, 0x081d9064..0x081d90c4, 24 instructions
      with no literal pool; the next function opens at 0x081d90c4 with
      `push {r4, lr}` and Ghidra's function table misses it entirely, so
      the 96-byte extent is right here only by luck. 63 `bl` and 0 `b` call
      sites, counted by decoding every branch word in osos.dec (the third
      largest consumer group is 0x0819a70c..0x0819a818, ten sites in one
      function).
      Algorithm - mutex_lock(display + 0x28); if the slot at
      display[index] (word-indexed, `ldr r0,[r4,r5,lsl #2]`) is NULL,
      allocate operator_new(0x1d8) and hand it to the layer constructor @
      0x08120b10 with (display, display+0x94 driver, index, display_id ==
      1), store the result back in the slot; mutex_unlock; return the slot
      re-loaded. The slot is genuinely loaded three times - test, store,
      post-unlock return - so the port keeps volatile accesses instead of
      caching across the constructor.
      SUBSYSTEM - the address sits in the app/Silver band but the class is
      pure display driver, so the port lives in drivers/display.rs beside
      drivers/display_layer.rs, whose header already names this function as
      the way a layer is obtained. The constructor @ 0x081d92a4 is
      decisive - mutex_create(display+0x28), a `i < 6` loop clearing six
      layer slots at +0x00..+0x17 and six bytes at +0x18..+0x1d,
      `display+0x90 = param_2` (the display id) and `display+0x94` = the
      panel driver from 0x0814ece0 (id 0) or 0x08169018 (id 1). The
      singleton getter 0x081d8870 hands out exactly two displays, id 0
      (internal LCD) and id 1 (secondary output).
      Field map recovered - +0x00 six layer slots, +0x18 six per-layer
      bytes, +0x28 Mutex, +0x90 u8 display id, +0x94 panel driver. Modeled
      as a repr(C) Display with named fields, so the header offsets hold on
      the 32-bit target while a 64-bit host test simply gets a larger,
      self-consistent object. The index is addressed unchecked
      (addr_of_mut + add), matching the original's unbounded
      `ldr r0,[r4,r5,lsl #2]`; slice indexing would add a panic path the
      firmware has not got.
      NEW FACT for drivers/display_layer.rs - the layer byte at +0x09 that
      module could only call "(opaque)" is this constructor's fifth
      argument, i.e. "this layer is on the secondary display".
      Deviations - mutex_lock @ 0x0807f5c4, mutex_unlock @ 0x0807f6a0 and
      operator_new @ 0x082aadd4 are all ported and called directly; the
      layer constructor 0x08120b10 is not ported and dispatches through
      DISPLAY_HOOKS with an identity default (0x08120b10's body leaves r0
      alone on the path the accessor observes, so the NULL-block case still
      behaves). operator_new's result reaches the constructor unchecked and
      the constructor's result is stored unchecked - the original has no
      NULL test anywhere.
      Host tests (5) - first-request construction with every constructor
      argument checked and the tag-2 operator_new(0x1d8) logged, caching on
      the second request, the six slots kept independent, the secondary
      flag proven to be `display_id == 1` and nothing else (ids 0, 1, 2,
      0xff), a NULL constructor result stored and retried, and a failed
      allocation still reaching the constructor with the NULL block.
      match.py structural - the slot addressing (`ldr r0,[r5,r4,lsl #2]`),
      the +0x90 ldrb, `mov r0,#0x1d8`, the +0x94 load and the slot store
      are all present in order; LLVM inlines the eight-byte ported
      mutex_lock/mutex_unlock (so the ROM_KERNEL volatile table read shows
      up as an ldrd chain) and lowers `display_id == 1` to sub/clz/lsr
      instead of cmp/moveq.

  - address: 0x081d9138
    ghidra: FUN_081d9138
    name: display_set_pending_command
    size: 20
    sig: "void display_set_pending_command(Display *display, u8 command, u8 parameter)"
    module: drivers/display
    status: ported
    renames:
      param_1/r0: display
      param_2/r1: command
      param_3/r2: parameter
    notes: >-
      Stores the caller's two-byte pending command at display+0x54/+0x55,
      then sets its +0x24 dirty byte to 1. Raw ARM is exactly five
      instructions, 0x081d9138..0x081d914c: `strb r1,[r0,#0x54]; mov r1,#1;
      strb r2,[r0,#0x55]; strb r1,[r0,#0x24]; bx lr`. The separately linked
      sibling begins at 0x081d914c with `push {r4,r5,r6,lr}`. Decoding every
      ARM B/BL immediate in osos.dec found 11 inbound direct branches: 9
      unconditional bl (0x0811e794, 0x08146488, 0x081465ec, 0x08158ac8,
      0x081f5b80, 0x081f5cc0, 0x081f5f38, 0x081f6054, 0x08296410) and two
      unconditional tail b (0x08145500, 0x0828d0c0); no predicated sites.
      No aligned data word equals this address, so it is not directly
      vtable-dispatched. The state-completion routine at 0x081d90c4 copies
      a queued entry's bytes into the same +0x54/+0x55 pair, then sets +0x24
      after decrementing its +0x5c count; this setter is the direct
      caller-facing path. New Display fields preserve the 32-bit target
      layout while native host pointers can widen. Deliberate deviations:
      none. Host tests cover all caller immediate pairs plus 0x00/0xff
      boundaries, replacement of a previously pending command, exact dirty
      flag value, and adjacent-byte preservation. match.py structural review
      expects three ordered byte stores and the mov-immediate.

  - address: 0x081d8cfc
    ghidra: FUN_081d8cfc
    name: display_set_clear_color
    size: 16
    sig: "void display_set_clear_color(Display *display, u32 color)"
    module: drivers/display
    status: ported
    renames:
      param_1/r0: display
      param_2/r1: color
    notes: >-
      Arms the display's panel clear - stores `color` into the display's
      +0x20 word and sets the +0x1e pending byte, in that order
      (`mov r2,#1; strb r2,[r0,#0x1e]; str r1,[r0,#0x20]; bx lr`).
      EXTENT - exactly 16 bytes, 0x081d8cfc..0x081d8d0c; the next function
      opens at 0x081d8d0c with `cmp r2,#0`. Ghidra's 16 bytes and its
      28-site count are both right - verified by decoding every branch
      word in osos.dec: 28 `bl`, 0 predicated. NO GUARD on `display` and
      r0 passes through unmodified (declared void; no caller reads a
      return).
      SEMANTICS - the consumer is the flush loop @ 0x081d8b3c: when +0x1e
      is set it calls the panel driver (+0x94) through vtable slot +0x10
      as driver->vtable[0x10](driver, display->clear_color) (0x081d8bdc..
      0x081d8bec) and then clears the pending byte (0x081d8bf0). The
      constructor @ 0x081d92a4 zeroes +0x1e (0x081d92b4), so a fresh
      display starts with no clear armed. Call sites pass RGB colors
      (0x00ffffff, 0x00ff0000, 0xffffffff, 0) straight from display_get's
      return, so this is the "clear the panel to this color on the next
      flush" request; the sibling setter @ 0x081d8d0c arms the second
      pending byte at +0x24 instead.
      NEW FIELDS for drivers/display.rs - reserved_18 split into
      per_layer_bytes (+0x18..+0x1d), clear_pending (+0x1e), clear_color
      (+0x20), pending_command_dirty (+0x24), and adjacent reserved bytes;
      32-bit offset asserts updated to match.
      Host tests (2) - every call-site immediate (0, 0xffffff, 0xff0000,
      0xffffffff) arms +0x1e and stores the color verbatim while the
      neighbouring bytes stay untouched, and a re-arm over an already
      pending clear leaves the flag set with the last color winning.
      match.py structural - both stores present in the original's order
      with the same immediates; the only diff is LLVM's push {fp,lr} /
      pop {fp,pc} frame around the body.

  # --- iAP (iPod Accessory Protocol): the packet class ---
  - address: 0x080f6c58
    ghidra: FUN_080f6c58
    name: iap_packet_init_with_compact_payload
    size: 220
    sig: "void iap_packet_init_with_compact_payload(u8 *packet, u8 *owner, u8 *context, u32 lingo_word, u32 command_word, u32 header_kind, u32 header_value, u32 header_data, u8 extra_byte)"
    module: app/iap_packet
    status: ported
    renames:
      param_1/r0: packet
      param_2/r1: owner
      param_3/r2: context
      param_4/r3: lingo_word
      param_5: command_word
      param_6: header_kind
      param_7: header_value
      param_8: header_data
      param_9: extra_byte
      local_20: payload[0]
      puVar2/r3: payload_end
    notes: >-
      Builds a compact iAP reply payload on the stack, then calls the
      unported packet initializer at 0x080f73a0. EXTENT - 220 bytes,
      0x080f6c58..0x080f6d34, 55 instructions and no literal pool. Ghidra's
      220-byte extent is exact: 0x080f6d30 is this function's `pop
      {r4-r8,pc}`, and the next sibling opens at 0x080f6d34 with `push
      {r2,r3,r4,lr}`. Decoding every branch word in osos.dec found 12 `bl`
      and 0 `b` call sites, all unconditional (zero predicated forms):
      0x080f6f7c, 0x0813a254, 0x0818e5c8, 0x08192cc0, 0x08193aa8,
      0x08195cf0, 0x081a9330, 0x081d6d2c, 0x081f1df8, 0x081f1eac,
      0x081f2d70, and 0x081fddd0. No image word equals 0x080f6c58, so it
      is not a direct virtual-dispatch target.
      ALGORITHM - store header_kind's low byte first. For lingo_word == 4,
      append header_value's low u16 as big-endian; otherwise append its low byte,
      with extra_byte appended only when lingo_word == 12. If header_kind
      equals the full word 6, append header_data as big-endian u32. Forward
      the packet/owner/context unchanged; lingo_word and command_word become
      the initializer's u8/u16 lingo/command. The full-word selector tests
      happen before truncation, exactly as the ARM `cmp` instructions do.
      DEVIATION - 0x080f73a0 remains unported; the port uses the existing
      volatile IAP_PACKET_OPS.init seam. Its return is deliberately
      discarded, matching the original's void wrapper. Host tests cover the
      lingo-4 big-endian u16 plus kind-6 big-endian u32 case, the lingo-12
      extra-byte order, and high-word selector values that must not select
      the truncated-byte layout. match.py structural review (`--lib` worktree archive) - 53 Rust instructions versus 55 original; both select the same lingo-4/lingo-12 layouts, conditionally append four big-endian data bytes, marshal the three trailing initializer arguments on the stack, and call once. LLVM zeroes the seven-byte local buffer and dispatches through the volatile ops table with `blx`, rather than the original's direct `bl 0x080f73a0`; its different frame/register allocation yields no common textual prefix.
  - address: 0x080f6da0
    ghidra: FUN_080f6da0
    name: iap_packet_create
    size: 104
    sig: "u8 *iap_packet_create(u8 *owner, u8 *context, u8 lingo, u16 command, const u8 *payload, u32 payload_len)"
    module: app/iap_packet
    status: ported
    renames:
      param_1/r5: owner
      param_2/r6: context
      param_3/r7: lingo
      param_4/r8: command
      param_5/r9: payload
      param_6/sl: payload_len
      iVar1/r4: packet
    notes: >-
      The iAP packet factory - allocate, construct and initialize one iPod
      Accessory Protocol message object, or return NULL.
      EXTENT - 104 bytes, 0x080f6da0..0x080f6e08, 26 instructions with no
      literal pool. Ghidra's functions.csv says 100 and is one word short -
      the word at 0x080f6e04 is this function's own
      `pop {r1,r2,r3,r4-r11,pc}`, and the next function opens at 0x080f6e08
      with `mov r2,r0; push {lr}`. Use `match.py --size 0x68`.
      59 `bl` and 0 `b` call sites, counted by decoding every branch word
      in osos.dec.
      Algorithm - packet = construct(operator_new(0x24)); if NULL return
      NULL; if init(packet, owner, context, lingo, command, payload,
      payload_len) is nonzero return packet; else
      operator_delete(destruct(packet)) and return NULL. Two details the
      port keeps rather than tidies - the NULL test is on the constructor's
      result, taken only after the constructor has already run over the
      possibly-NULL block (`bl 0x082aadd4; bl 0x080f745c; movs r4,r0`); and
      the delete takes the DESTRUCTOR's return value, not the saved r4, the
      two coinciding only because 0x080f74ac ends in `mov r0,r4`.
      SUBSYSTEM - iAP, proven three ways. (1) The header parser feeding
      this factory, 0x080f6d34, uses a 3-byte header with a big-endian u16
      command when the first byte is 4 and a 2-byte header with a u8
      command otherwise - the iAP rule that lingo 0x04 (Extended Interface)
      alone carries 16-bit command ids. (2) Decoding the third argument at
      all 59 call sites yields the immediates 0..10 and nothing else, the
      allocated lingo id range. (3) The default constructor 0x080f745c arms
      +0x0e = 0xff and +0x10 = 0xffff, the "no lingo"/"no command"
      sentinels. The wire-length helper 0x080f6e08 closes it - it returns
      3 + payload_len + (lingo == 4 ? 2 : 1) + (payload_len > 0xfd ? 3 : 1)
      + ..., i.e. sync + length field (one byte, or the 0x00 escape plus a
      16-bit length) + lingo + command + payload + checksum. Filed under
      app/ beside app/iap_incoming_process_thread.rs (the consumer), not
      under drivers/, despite the 0x080fxxxx address band.
      Packet object (0x24 = 36 bytes), from 0x080f745c / 0x080f73a0 /
      0x080f7420 - +0x00 owner (the lone field the sibling factory
      0x080f6f8c sets), +0x04 a second opaque word that is NULL at all 59
      call sites, +0x08 payload buffer (tag-3 operator_new @ 0x082aad74,
      released through 0x082aad14), +0x0c u16 payload length, +0x0e u8
      lingo, +0x10 u16 command, +0x14 a second buffer, +0x18 its u16
      length, +0x1a flag, +0x1c and +0x20 zeroed by the initializer. The
      constructor and destructor bump a live-packet counter at the global
      word 0x089ccc24. The port never dereferences the packet, so it stays
      an opaque *mut u8 and no host/target layout question arises.
      Argument mapping verified against the initializer's own prologue -
      0x080f73a0 does `stm r4,{r7,r8}` (owner, context), `strb r9,[r4,#14]`
      (lingo), `ldrd sl,[sp,#48]` and `strh sl,[r4,#16]` (command), with
      the payload pointer in fp and the length in r5; the factory's
      `stm sp,{r8,r9,sl}` lands its own r3/stack args in exactly those
      three slots. lingo/command are typed u8/u16 because the initializer
      truncates them with strb/strh - nothing observable is lost.
      Deviations - operator_new @ 0x082aadd4 and operator_delete @
      0x082aad24 are ported and called directly. The destructor
      (0x080f74ac) is now ported too (iap_packet_destruct) and called
      directly - only its payload-release helper @ 0x080f7420 still
      dispatches, through IAP_PACKET_OPS.release (default no-op). The
      constructor (0x080f745c) and initializer (0x080f73a0) remain
      unported and dispatch through IAP_PACKET_OPS with documented
      defaults (identity, return 1). The initializer's ARM return
      is a 64-bit pair (r0 = success flag, r1 = the owner argument passing
      back out); the factory reads only r0, so the hook returns just that.
      Host tests (6) - every argument threaded into the initializer in
      order with the tag-2 operator_new(0x24) logged, all eleven observed
      lingo ids plus the 0xff sentinel crossed with five command values,
      a zero-length body treated as success, the failure path proven to
      destruct then tag-2 operator_delete and return NULL, the delete
      proven to take the destructor's result rather than the saved
      pointer, and a NULL constructed packet short-circuiting before the
      initializer.
      match.py structural (`--size 0x68`) - `mov r0,#0x24`, the
      operator_new call, the construct dispatch, `cmp r0,#0` NULL exit, the
      three stack-marshalled arguments, the init dispatch and its zero
      test, and the destruct-then-delete teardown all appear in the
      original's order; the differences are the crate fp frame, the
      indirect `blx` hook dispatch in place of three direct `bl`s, and
      LLVM keeping the NULL result in r6 instead of reusing r4.
  - address: 0x080f6efc
    ghidra: FUN_080f6efc
    name: iap_packet_reinit
    size: 48
    sig: "u32 iap_packet_reinit(u8 *packet, u8 *owner, u8 *context, u8 lingo, u16 command, const u8 *payload, u32 payload_len)"
    module: app/iap_packet
    status: ported
    renames:
      param_1/r0: packet
      param_2/r1: owner
      param_3/r2: context
      param_4/r3: lingo
      param_5: command
      param_6: payload
      param_7: payload_len
    notes: >-
      The reinitialize-in-place entry of the iAP packet class - a pure
      identity forwarder into the file-local packet initializer @
      0x080f73a0 (the same init hook iap_packet_create uses).
      EXTENT - 48 bytes, 0x080f6efc..0x080f6f2c, 12 instructions, no
      literal pool. Ghidra's 48 is exact: the next function opens at
      0x080f6f2c with `push {r4-r11,lr}; sub sp,sp,#0x14`.
      43 `bl` and 0 `b` call sites, ALL unconditional (cond=e, zero
      predicated forms), counted by decoding every branch word in
      osos.dec; 0x080f6efc appears in no data word, so it is never
      dispatched virtually.
      Algorithm - push {r1-r5,lr} (an 0x18-byte frame), ldm/stm the
      caller's three stack arguments down into the bottom of it, ride
      r0-r3 through a three-register shuffle (r4/lr/ip) untouched, bl
      0x080f73a0, pop and return. Nothing is tested, added or masked;
      the wrapper exists because the initializer is file-local and the
      lingo handlers link against this out-of-line copy. There is no
      NULL guard in the wrapper and none at any call site.
      Semantics - the 43 call sites are reply paths: each passes its
      incoming request packet as r0, reloads the packet's own owner
      field (+0x00) into r1 and passes the reply's
      lingo/command/payload/length; the Extended Interface handler @
      0x08139974 reads the lingo back out of the packet's own +0x0e.
      Since the initializer's first act is releasing any payload the
      packet already holds (release helper @ 0x080f7420), this call is
      how a request packet is turned into its reply.
      Deviations - the initializer @ 0x080f73a0 is not ported; the
      forward dispatches through IAP_PACKET_OPS.init, same seam
      iap_packet_create uses. lingo/command are typed u8/u16 because
      the initializer truncates with strb/strh - nothing observable is
      lost. The ARM return is the initializer's 64-bit pair (r0 =
      success flag, r1 = owner passing back out); the wrapper leaves
      both live, but all 43 call sites discard them, so the port
      returns just r0.
      LLVM sibcall hazard - left alone, LLVM lowers this body to a
      sibling call that materializes the volatile ops read with
      `ldrd r0,[ip]` and the branch target in r1, clobbering the
      packet/owner arguments the tail call must forward (verified in
      the release archive). An empty asm barrier after the call keeps
      the bl-and-return shape - also what the original does.
      Host tests (3) - all seven arguments forwarded to the init hook
      in order with the initializer's success flag returned verbatim,
      an init failure (0) propagated with no teardown of the wrapper's
      own, and NULL packet/owner/context passing through unguarded
      (matching the 43 unconditional call sites).
      match.py structural - the frame, the three stack-argument bounces
      ([fp,#8/12/16] -> [sp,#0/4/8]) in the original's order, and the
      init dispatch followed by a plain return are all present; the
      differences are the crate fp frame and the indirect `blx r7`
      hook dispatch in place of the direct `bl 0x080f73a0`.
  - address: 0x080f6b3c
    ghidra: FUN_080f6b3c
    name: iap_packet_owner_mode
    size: 24
    sig: "u32 iap_packet_owner_mode(const u8 *packet)"
    module: app/iap_packet
    status: ported
    renames:
      param_1/r0: packet
      r1: owner
    notes: >-
      The iAP packet class's owner-mode getter - `owner = packet->owner`
      (+0x00); return `owner != NULL ? *(u32 *)(owner + 8) : 0`.
      EXTENT - 24 bytes, 0x080f6b3c..0x080f6b54, 6 instructions, no
      literal pool. Ghidra's 24 is exact: the next function opens at
      0x080f6b54 with `mov r1,r0; ldrh r2,[r1,#0x18]` (a sibling getter
      over the packet's +0x14/+0x18 second-buffer pair). Body verified
      from osos.dec word by word: `mov r1,r0; ldr r1,[r1]; mov r0,#0;
      cmp r1,#0; ldrne r0,[r1,#8]; bx lr`.
      CALL SITES - 43 `bl` and 0 `b`, ALL unconditional (cond=e, zero
      predicated forms), counted by decoding every branch word in
      osos.dec; the address appears in no data word anywhere in the
      image, so it is never dispatched virtually. The NULL guard is the
      callee's own - no caller pre-checks the owner.
      SEMANTICS from the call sites - the returned word is the owner
      (transport) object's framing mode. The packet serializer @
      0x080f6b70 prepends the 0xff sync byte ahead of the fixed 0x55
      exactly when this returns 1 (`cmp r0,#1; moveq r0,#0xff;
      strbeq`), and the wire-length helper @ 0x080f6e08 budgets that
      same extra byte; the mode remap @ 0x08192124 translates {1->0,
      2->1, 4->3, else 0xff}, so the meaningful range is 0..=4; the
      reply path @ 0x081fd688 splits on `< 3` vs `>= 3`. The owner
      object's class is NOT identified - only its +0x08 word is
      observed here, read as a raw u32 and returned verbatim.
      Deviations - none. Both loads are aligned word reads, as in the
      original, and there is no NULL guard on `packet` itself because
      the original has none.
      Host tests (5, app::iap_packet::tests::owner_mode, low-slab
      fixture at hints::IAP_PACKET_OWNER_MODE) - a NULL owner
      short-circuits to 0, mode 1 (the sync-framing case) passes
      through, a present owner with mode 0 returns 0 through the load
      path rather than the guard, the boundary modes 2/3/4 pass
      through, and 0xffffffff round-trips at full width.
      match.py structural - the owner load, the NULL test, and the
      guarded +0x08 load are all present; the differences are LLVM
      laying the two exits out as separate blocks (an early
      `moveq r0,#0; bxeq lr` and a framed `ldr r0,[r0,#8]; pop` tail)
      instead of the original's single branchless predicated block -
      semantically identical, expected under the crate's fp frame.
  - address: 0x080f74ac
    ghidra: FUN_080f74ac
    name: iap_packet_destruct
    size: 36
    sig: "u8 *iap_packet_destruct(u8 *packet)"
    module: app/iap_packet
    status: ported
    renames:
      param_1/r0: packet
      r4: packet_saved
    notes: >-
      The iAP packet class destructor - release the payload buffers,
      decrement the live-packet counter, return `this`.
      EXTENT - 36 bytes, 0x080f74ac..0x080f74d0, 9 instructions, followed
      by its one literal-pool word 0x089ccc24 @ 0x080f74d0. Ghidra's 36
      is exact: the next function opens at 0x080f74d4 with
      `ldr r0,[r0]; b 0x080f74f8`. Whole body, verified word by word
      against osos.dec: `push {r4,lr}; mov r4,r0; bl 0x080f7420;
      ldr r0,[pc,#16]; ldr r1,[r0]; sub r1,r1,#1; str r1,[r0];
      mov r0,r4; pop {r4,pc}`.
      CALL SITES - 24 `bl` and 0 `b`, ALL unconditional (zero predicated
      forms), counted by decoding every branch word in osos.dec; the
      address appears in no data word, so it is never dispatched
      virtually. The earlier ledger note on the IAP_PACKET_OPS seam
      claiming 1 call site was WRONG - only the factory's failure path
      was known then; the other 23 are delete paths in the lingo
      handlers (0x081055dc, 0x0814e460, 0x08164070, ...), each
      immediately followed by `bl 0x082aad24` (tag-2 operator_delete),
      the destroy-then-free idiom. Ghidra's C drops the argument at
      several of those sites (`FUN_080f74ac()`); the raw bytes keep r0
      live into the call.
      Algorithm - r4 = packet; call the payload-release helper @
      0x080f7420; load the counter word @ 0x089ccc24, subtract 1 (a
      plain `sub` - wraps through zero with no guard), store it back;
      return r4. The return-this is what every caller feeds to
      operator_delete, and what the factory's failure path relies on
      (the delete takes the destructor's r0, never the saved pointer).
      No NULL guard here and none at any of the 24 call sites - a NULL
      packet faults inside the release helper's first field load.
      The release helper @ 0x080f7420 itself: runs the conditional
      second-buffer release @ 0x080f6af8 (tag-2 operator_delete of the
      +0x14 buffer, only when the +0x18 length word holds the 0xffff
      ownership sentinel), then NULL-guarded tag-3 operator_delete @
      0x082aad14 of +0x08 and +0x14, zeroing both pointer/length pairs.
      Deviations - the release helper @ 0x080f7420 is not ported; the
      call dispatches through IAP_PACKET_OPS.release (default no-op).
      The counter is the REAL word @ 0x089ccc24 on target (a fixed
      address, not a crate static) because the stock constructor @
      0x080f745c increments that word - a hooked destructor decrementing
      anything else would break the pairing; host builds decrement a
      stand-in static (the buffer_refill_request.rs pattern).
      Host tests (3, app::iap_packet::tests) - release runs BEFORE the
      decrement (the mock observes the un-decremented counter) and the
      packet passes to it and back out verbatim; the counter wraps
      0 -> 0xffffffff; a NULL packet passes through unguarded and
      returns NULL. The factory's failure-path tests now exercise the
      real destructor: release called once with the packet, counter
      decremented, tag-2 operator_delete fed the destructor's return.
      match.py structural - `mov r4,r0`, the release call, the literal
      load of 0x089ccc24, the ldr/sub/str decrement, `mov r0,r4` and the
      return all appear in the original's order; the differences are
      the crate fp frame and the indirect `blx` ops dispatch in place of
      the direct `bl 0x080f7420`, both documented deviations.
  # --- IRAM veneer for the microsecond timer read ---
  - address: 0x08037e20
    ghidra: thunk_EXT_FUN_08037e20
    name: iram_usec_timer_read_veneer
    size: 8
    sig: "u32 iram_usec_timer_read_veneer(void)"
    module: drivers/timer
    status: ported
    notes: >-
      ldr pc,[pc,#-4] -> 0x22001edc = the IRAM copy of usec_timer_read @
      0x08001edc (the three-word Timer E TECNT read). Ghidra reports 4
      bytes; the stub is 8 (0xe51ff004 at 0x08037e20 + the target word
      0x22001edc at 0x08037e24). 47 bl call sites, all unconditional, and
      0 tail b - binary-verified by decoding every B/BL word in osos.dec
      for every condition code, not from Ghidra xrefs. The mirror
      0x2200XXXX = osos 0x0800XXXX holds because the relocator @
      0x080046e0 memmoves 0xaed8 bytes from 0x08000000 to 0x22000000
      (S5L8702 internal SRAM, not mask ROM), so the body is recoverable
      and is ALREADY PORTED - this is a real forwarding call, not a stub.
      No arguments, no stack, lr untouched: a pure tail transfer returning
      the counter word in r0. The port loads its callee through
      read_volatile and carries its own link_section; without either,
      LLVM inlines the three-instruction body and its identical-function
      folding collapses the veneer onto usec_timer_read, destroying the
      separate 0x08037e20 hook seam. Note the mirror region holds three
      byte-identical 12-byte bodies at 0x08001ec4/0x08001ed0/0x08001edc;
      the veneer names the third. match.py structural: ldr/ldr/bx through
      a literal - the original's indirect-jump-through-a-literal shape
      plus one indirection and the crate's fp frame.
  # --- IRAM veneer for the microsecond deadline predicate ---
  - address: 0x08037eb8
    ghidra: thunk_EXT_FUN_22001ee8
    name: iram_usec_timer_elapsed_veneer
    size: 8
    sig: "bool iram_usec_timer_elapsed_veneer(u32 start, u32 interval)"
    module: drivers/timer
    status: ported
    notes: >-
      ldr pc,[pc,#-4] -> 0x22001ee8 = the IRAM copy of usec_timer_elapsed @
      0x08001ee8 (the 28-byte Timer E deadline predicate (TECNT - start) >=
      interval -> 0/1). Ghidra reports 4 bytes; the stub is 8 (0xe51ff004
      at 0x08037eb8 + the target word 0x22001ee8 at 0x08037ebc). 42 bl
      call sites, ALL unconditional (zero predicated forms), and 0 tail b -
      binary-verified by decoding every B/BL word in osos.dec for every
      condition code, not from Ghidra xrefs. The mirror 0x2200XXXX = osos
      0x0800XXXX holds because the relocator @ 0x080046e0 memmoves 0xaed8
      bytes from 0x08000000 to 0x22000000 (S5L8702 internal SRAM, not mask
      ROM), so the body is recoverable and is ALREADY PORTED - this is a
      real forwarding call, not a stub. Correction: kernel/task_lock.rs's
      THUNK_CATALOG labels this target "tick_elapsed" with
      (kernel_ticks() - start) >= span - stale; the mirror body reads the
      Timer E microsecond counter at 0x3c70_00b4 via the 0x080023d0
      literal, exactly like usec_timer_elapsed (the kernel/thunks.rs
      ROM_THUNKS name slot is left None per the 0x08037e20 precedent).
      r0/r1 pass through untouched, no stack, lr untouched: a pure tail
      transfer returning the predicate in r0. The port loads its callee
      through read_volatile and carries its own link_section; without
      either, LLVM inlines the small predicate body and its
      identical-function folding can collapse the veneer, destroying the
      separate 0x08037eb8 hook seam. match.py structural: ldr/ldr/bx
      through a literal - the original's indirect-jump-through-a-literal
      shape plus one indirection and the crate's fp frame, r0/r1 passed
      through untouched.
  # --- IRAM veneer for the microsecond busy-wait delay ---
  - address: 0x08037ef0
    ghidra: thunk_EXT_FUN_22001f78
    name: iram_usec_delay_veneer
    size: 8
    sig: "u32 iram_usec_delay_veneer(u32 interval)"
    module: drivers/timer
    status: ported
    notes: >-
      ldr pc,[pc,#-4] -> 0x22001f78 = the IRAM copy of usec_delay @
      0x08001f78 (the 44-byte Timer E microsecond busy-wait: capture
      TECNT once, poll usec_timer_elapsed(start, interval) until true,
      return 0). Ghidra reports 4 bytes; the stub is 8 (0xe51ff004 at
      0x08037ef0 + the target word 0x22001f78 at 0x08037ef4). 25 bl call
      sites, ALL unconditional (zero predicated forms), plus 2 tail b @
      0x080b1b4c and 0x080bb658 - binary-verified by decoding every B/BL
      word in osos.dec for every condition code, not from Ghidra xrefs;
      no data word anywhere holds 0x08037ef0, so the veneer is never
      dispatched indirectly. The mirror 0x2200XXXX = osos 0x0800XXXX
      holds because the relocator @ 0x080046e0 memmoves 0xaed8 bytes
      from 0x08000000 to 0x22000000 (S5L8702 internal SRAM, not mask
      ROM), so the body is recoverable and is ALREADY PORTED - this is a
      real forwarding call, not a stub. Corrections: kernel/task_lock.rs's
      THUNK_CATALOG labels this target "tick_delay" in kernel ticks -
      stale, as with its "tick_elapsed" sibling; the mirror body reads
      the Timer E microsecond counter at 0x3c70_00b4, so interval is
      microseconds (observed bl arguments: 1, 10, 100, 800, 1000).
      codegen/timer_wait.rs's "event_wait(handle)" reading of the same
      veneer is likewise stale: +0xb4 is the counter itself, not an
      event slot, and r0 is a duration, not a handle (the
      kernel/thunks.rs ROM_THUNKS name slot is left None per the
      0x08037e20 precedent). r0 passes through untouched, no stack, lr
      untouched: a pure tail transfer returning the body's r0 (always
      0). The port loads its callee through read_volatile and carries
      its own link_section; without either, LLVM inlines the poll loop
      and its identical-function folding can collapse the veneer,
      destroying the separate 0x08037ef0 hook seam. match.py structural:
      ldr pc through a literal vs the port's ldr/bx-indirect tail
      transfer - the original's indirect-jump-through-a-literal shape
      plus one indirection and the crate's fp frame, r0 passed through
      untouched.

  # --- StringObject: bounded UTF-16 assignment ---
  - address: 0x08277164
    ghidra: FUN_08277164
    name: utf16_code_unit_count_safe
    size: 36
    sig: "int utf16_code_unit_count_safe(const unsigned short *source)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "source"
      iVar1: "count"
    notes: >-
      Four unconditional BL references. NULL returns zero; otherwise count nonzero halfwords until NUL. Surrogates remain separate units; signed count wraps at 32 bits. No deviations. ARM review: 17 LLVM instructions versus 9 ADS; equivalent halfword loop with a peeled first iteration.

  - address: 0x08276aa0
    ghidra: FUN_08276aa0
    name: string_object_insert_utf16_cstr
    size: 196
    sig: "void string_object_insert_utf16_cstr(StringObject *this, int index, const unsigned short *source)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "index"
      param_3: "source"
      uVar2: "count"
      iVar3: "old_inclusive_len"
      iVar6: "new_inclusive_len"
    notes: >-
      One unconditional BL reference. NULL/first-unit-zero source and negative index return before object access. Count source units first, then measure old inclusive byte length and bounded encoded size. Grow to a 32-byte-rounded capacity with preserve flag 1, resolve the character index against the updated payload, shift its tail, transcode exactly the nonzero unit count and write the new final NUL. Unlike bounded insertion with a count including NUL, this excludes the source terminator and preserves the shifted tail. Shares inlined insertion mechanics with the byte and bounded UTF-16 variants. Only the existing unported virtual allocator remains a boundary. ARM review: 62 versus 49 instructions; inlined length scan and shared gap logic, expected sizing/decoder/transcoder relocations, and LLVM __aeabi_memmove lowering.

  - address: 0x08276b64
    ghidra: FUN_08276b64
    name: string_object_insert_utf16
    size: 184
    sig: "void string_object_insert_utf16(StringObject *this, int index, const unsigned short *source, int count)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "index"
      param_3: "source"
      param_4: "count"
      iVar1: "old_inclusive_len"
      iVar4: "new_inclusive_len"
    notes: >-
      17 unconditional BL references. NULL source, signed nonpositive unit count and negative index return before object access. Measure old inclusive byte length, compute encoded byte length through bounded sizing, round new capacity to 32 with preserve flag 1, reload payload, shift the tail and transcode count units. An encountered source NUL is actually written and overwrites the first shifted tail byte, even when added byte length is zero; the final NUL is also stored at the sized end. Retains this truncation quirk, 32-bit wrapping and allocation-failure behavior. Shared insertion mechanics are inlined; existing virtual allocator remains unported. ARM review: 59 versus 46 instructions; inlined length scan and shared gap logic, expected helper relocations, and LLVM __aeabi_memmove lowering.

  - address: 0x08276900
    ghidra: FUN_08276900
    name: string_object_erase
    size: 212
    sig: "void string_object_erase(StringObject *this, int index, int count)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "index"
      param_3: "count"
      local_1c: "start"
      local_20: "end"
      iVar2: "old_inclusive_len"
    notes: >-
      15 unconditional BL references and one conditional BLGT. Read payload before checking index/count; NULL payload, negative index or nonpositive count returns. Walk start/end by decoder steps guarded only by literal NUL, move remaining bytes including NUL over the removed region, then request exact reduced inclusive size with preserve flag 1. Even a past-end index still requests resizing. Allocation return is discarded: failure leaves the already-erased bytes in place. Uses the ported character-pointer helper for the first walk and preserves its malformed-sequence behavior. Existing virtual allocator remains unported; no new boundary. ARM review: 49 versus 53 instructions; LLVM moves guards ahead of the nonvolatile payload load, inlines length, and lowers memmove to __aeabi_memmove. Valid-object behavior and mutation-before-allocation order are preserved.

  - address: 0x082768dc
    ghidra: FUN_082768dc
    name: string_object_append_cstr
    size: 12
    sig: "void string_object_append_cstr(StringObject *this, const unsigned char *source)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "source"
    notes: >-
      Nine unconditional BL and one tail B references. Three-instruction argument shuffle to string_object_insert_cstr with index INT_MAX. NULL/empty input stays a no-op, and the real byte insertion path handles growth. No new deviations beyond the existing virtual allocator. ARM review: 6 versus 3 instructions; LLVM frame setup/teardown surrounds the same argument shuffle and tail branch to insert_cstr.

  - address: 0x082768e8
    ghidra: FUN_082768e8
    name: string_object_append_code_unit
    size: 24
    sig: "void string_object_append_code_unit(StringObject *this, unsigned int value)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "value"
      uStack_c: "unit"
    notes: >-
      Five unconditional BL and nine conditional BLNE references. Original spills r1 and passes its address to bounded UTF-16 insertion with count 1 and index INT_MAX. On little-endian ARM only the low halfword is read; the Rust u16 local preserves that truncation on all hosts. Zero still requests preserving allocation, unlike empty append_cstr. Existing virtual allocator remains unported; no new seam. ARM review: 10 versus 6 instructions; halfword stack store and call relocation confirm truncation and bounded insertion.

  - address: 0x08275f48
    ghidra: FUN_08275f48
    name: string_object_insert_bytes
    size: 164
    sig: "void string_object_insert_bytes(StringObject *this, int index, const unsigned char *source, unsigned int source_len)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "index"
      param_3: "source"
      param_4: "source_len"
      iVar1: "old_inclusive_len"
      iVar2: "insertion"
      iVar3: "payload"
    notes: >-
      One unconditional BL and two unconditional tail B references. NULL source, zero length and negative index exit before object access. Add old inclusive byte length and source_len with 32-bit wrapping, round up to 32, and allocate via virtual slot +0x8 with preserve flag 1. On success resolve the character index against the updated payload, memmove the tail including NUL, copy source_len raw bytes with __rt_memcpy and store a final NUL. Allocation return is only a non-NULL flag; the object payload is authoritative. Replaces the no-op STRING_OBJECT_INSERT_CORE seam; only the existing unported virtual allocator remains. Caller must keep source valid across allocation and satisfy the ADS copy padding/nonoverlap contract.
      Codegen review: 41 original versus 54 Rust instructions. LLVM inlines the inclusive byte-length loop; rounded allocation, payload reload, character-index call, memmove/copy calls and final NUL store remain. LLVM lowers memmove to __aeabi_memmove (compiler_builtins); __rt_memcpy stays a direct ADS-port call. Capacity arithmetic stays 32-bit.

  - address: 0x082773b4
    ghidra: FUN_082773b4
    name: string_object_construct_from_utf16
    size: 44
    sig: "StringObject *string_object_construct_from_utf16(StringObject *this, const unsigned short *source, int max_code_units)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      r1: "source"
      r2: "max_code_units"
    notes: >-
      Six unconditional BL references. 40 code bytes plus literal 0x089a6044 at 0x082773dc; Ghidra excludes the literal and loses both forwarded arguments. Initialize the vtable and NULL payload, call the ported bounded UTF-16 assignment and return this on every path. Uses the modeled vtable and the existing virtual allocation/clear boundary inherited from assignment; no new seam.
      Codegen review: initialization, unchanged source/count forwarding and saved-this return retained; LLVM adds a frame and reorders the two independent initialization stores.

  - address: 0x08277414
    ghidra: FUN_08277414
    name: string_object_construct_from_codepoint
    size: 44
    sig: "StringObject *string_object_construct_from_codepoint(StringObject *this, unsigned int codepoint)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      r1: "codepoint"
    notes: >-
      Three unconditional BL references. 40 code bytes plus literal 0x089a6044 at 0x0827743c; Ghidra excludes the literal and drops r1. Plant modeled vtable and NULL payload, assign the unsigned codepoint through its Rust port and return this, including on zero/clear and allocation failure. Retains the existing virtual-method boundary; no new seam.
      Codegen review: initialization, unchanged unsigned argument and saved-this return retained; modeled vtable literal replaces the ROM literal.

  - address: 0x082774c8
    ghidra: FUN_082774c8
    name: string_object_append
    size: 40
    sig: "StringObject *string_object_append(StringObject *this, const StringObject *other)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "other"
      uVar1: "source"
    notes: >-
      18 unconditional BL references. Resolve other through string_object_c_str, then string_object_insert_cstr with index INT_MAX (append sentinel), and return this regardless of success. No self-append guard or source snapshot. Both callees are direct Rust ports; the insertion allocator remains an unported virtual boundary. Source must remain valid across any allocation.
      Codegen review: 10 original versus 11 Rust instructions; both direct calls, INT_MAX index and this return preserved.

  - address: 0x082a5394
    ghidra: FUN_082a5394
    name: string_object_codepoint_count
    size: 8
    sig: "unsigned int string_object_codepoint_count(const StringObject *this)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      iStack_c: "payload"
    notes: >-
      Two unconditional BL references. Load payload at +4 and tail-branch to utf8_codepoint_count_safe; NULL payload is zero and malformed/overlong zero-decoding sequences end the count. Ghidra inlines the entire decoder loop but raw extent is only two instructions. No deviations.
      Codegen review: payload load and direct count tail call preserved; LLVM adds and immediately restores its frame.

  - address: 0x08275ecc
    ghidra: FUN_08275ecc
    name: utf8_write_codepoint
    size: 124
    sig: "void utf8_write_codepoint(unsigned char **cursor, unsigned int codepoint)"
    module: cxx/string_encoding
    status: ported
    vars:
      param_1: "cursor"
      param_2: "codepoint"
      pbVar3: "out"
      bVar1: "low_byte"
    notes: >-
      Four unconditional BL references and one unconditional tail B. Cursor-based encoder uses unsigned 0x80/0x800 thresholds and advances the pointer cell before each byte store. All values >=0x800 emit exactly three bytes, including out-of-range u32 inputs; the first byte truncates (codepoint >> 12) | 0xe0. No trailing NUL beyond an encoded zero itself. Ghidra misreads the final pop pc as an indirect call; raw bytes show a normal return. No deviations.
      Codegen: 31 original versus 30 Rust instructions. Same unsigned thresholds, cursor-cell stores before byte stores, reloads and three-byte truncation; helper inlined.

  - address: 0x0827654c
    ghidra: FUN_0827654c
    name: string_object_assign_utf16_cstr
    size: 92
    sig: "void string_object_assign_utf16_cstr(StringObject *this, const unsigned short *source)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "source"
      uVar3: "requested_size"
      pbVar4: "destination"
    notes: >-
      Three unconditional BL references. True entry body is 92 bytes ending at 0x082765a8; Ghidra reports 192 by including the separately ported 100-byte encoder at 0x082766f8 reached by tail BNE at 0x082765a0. NULL or first-unit zero invokes virtual clear; otherwise inclusive UTF-8 sizing, allocation with flags zero, and direct utf16_to_utf8 conversion on success. Empty strings clear instead of allocating as the bounded sibling does. Unported virtual allocate/clear methods use the existing STRING_OBJECT_ASSIGN_CSTR_OPS boundary; not hook-ready until those methods are wired. No new stub or indirection.
      Codegen: 23 original versus 27 Rust instructions. Same NULL/first-unit guard, inclusive sizing call, allocation failure exit and tail call to utf16_to_utf8; virtual methods retain the existing ops indirection.

  - address: 0x08276620
    ghidra: FUN_08276620
    name: string_object_assign_codepoint
    size: 108
    sig: "void string_object_assign_codepoint(StringObject *this, unsigned int codepoint)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "codepoint"
      puStack_10: "cursor"
      iVar1: "byte_width"
    notes: >-
      Two unconditional BL references: the codepoint constructor at 0x08277414 and a caller at 0x0827ee6c. Ghidra invents extra r2/r3 arguments; raw ARM reads only this and codepoint. Zero invokes virtual clear, otherwise allocate utf8_codepoint_byte_width(value)+1 with flags zero, call utf8_write_codepoint on the spilled cursor, and store NUL at the advanced pointer. All u32 values follow the unsigned one/two/three-byte encoding. Allocation failure performs no write or clear. Reuses the existing unported virtual-method boundary; not hook-ready until the virtual methods are wired.
      Codegen: 27 original versus 30 Rust instructions. Same zero clear branch, width-plus-one allocation, spilled cursor encoding and trailing NUL store.

  - address: 0x082a50c4
    ghidra: FUN_082a50c4
    name: string_object_codepoint_ptr
    size: 84
    sig: "const unsigned char *string_object_codepoint_ptr(const StringObject *this, int index)"
    module: cxx/string_object
    status: ported
    vars:
      param_1: "this"
      param_2: "index"
      local_10: "cursor"
      iVar1: "walked"
    notes: >-
      Seven unconditional BL references. Negative index returns NULL before touching this; NULL payload returns NULL. Walk at most index decoder steps, stopping on a literal NUL byte, and return the byte pointer. Past-end indices clamp to the terminator. Decoder results are ignored, so unsupported leads and overlong NUL encodings still advance instead of ending the walk. No deviations; utf8_next_codepoint is a direct Rust call.
      Codegen: 21 original versus 26 Rust instructions. Negative-index exit precedes object access; payload NULL guard, literal-byte NUL test, decoder call and clamped pointer retained.

  - address: 0x082a52c8
    ghidra: FUN_082a52c8
    name: string_object_codepoint_at
    size: 32
    sig: "unsigned int string_object_codepoint_at(const StringObject *this, int index)"
    module: cxx/string_object
    status: ported
    vars:
      local_8: "cursor"
      r0: "codepoint"
    notes: >-
      17 unconditional BL references. Resolve the byte pointer with string_object_codepoint_ptr, returning zero if NULL, otherwise decode that position. Ghidra incorrectly claims void; callers such as 0x0826bd10 and 0x0826bee0 compare r0 with path delimiters. Negative and past-end indices, NULL payloads, overlong NUL and unsupported leads return zero. No deviations; both callees are real Rust ports.
      Codegen: 8 original versus 11 Rust instructions. Pointer accessor call and NULL-conditioned decoder call preserve the return value, with an LLVM frame.

  - address: 0x082766a0
    ghidra: FUN_082766a0
    name: utf8_copy_codepoints
    size: 88
    sig: "int utf8_copy_codepoints(unsigned char *out, const unsigned char *source, int max_codepoints)"
    module: cxx/string_encoding
    status: ported
    vars:
      param_1: "out"
      param_2: "source"
      param_3: "max_codepoints"
      local_18: "cursor"
      iVar2: "count"
    notes: >-
      One unconditional BL at 0x08046e20. Decode each sequence using the existing utf8_next_codepoint port, copy its original bytes forward, and then stop on decoded zero. Returns nonzero characters, not bytes. Signed nonpositive bounds touch nothing; exhausting the bound adds no terminator. Overlong encodings remain verbatim; unsupported leads copy three bytes and stop without adding a NUL. Forward overlap preserves the original sequential byte writes.
      Codegen review: 22 original versus 30 Rust instructions; decoder call, sequential forward byte loop, zero exit and signed limit retained. LLVM uses an increasing count rather than a decreasing remaining bound.

  - address: 0x082766f8
    name: utf16_to_utf8
    size: 100
    sig: "void utf16_to_utf8(unsigned char *out, const unsigned short *source)"
    module: cxx/string_encoding
    status: ported
    vars:
      r0: "destination / code_unit"
      r1: "source"
      r2: "out"
    notes: >-
      Recovered from raw osos.dec: 25 instructions, no literal pool, ending immediately before 0x0827675c. Ghidra omits this function entry and inlines the body into FUN_0827654c. Its only direct branch reference is the bne tail call at 0x082765a0. Encode NUL-terminated 16-bit units with thresholds 0x80 and 0x800, then store one zero byte. Surrogates are encoded independently. No deviations. Use match.py --size 100 because functions.csv has no entry.
      Codegen review: 25 original versus 31 Rust instructions; the encoder remains a leaf with identical 0x80/0x800 thresholds, byte masks and NUL stop. LLVM adds a frame and reorders stores within a decoded unit.

  - address: 0x0827675c
    ghidra: FUN_0827675c
    name: utf16_to_utf8_bounded
    size: 124
    sig: "int utf16_to_utf8_bounded(unsigned char *out, const unsigned short *source, int max_code_units)"
    module: cxx/string_encoding
    status: ported
    vars:
      param_1: "out"
      param_2: "source"
      param_3: "max_code_units"
      iVar2: "count"
      uVar1: "unit"
    notes: >-
      Five unconditional BL references. Encode up to the signed unit bound with 0x80/0x800 thresholds; do not combine surrogate pairs. Write an encountered source NUL but exclude it from the returned unit count. Exhausting the bound appends no NUL; nonpositive bounds access neither pointer. Called directly by string_object_assign_utf16, replacing its previous no-op transcoder seam. No deviations.
      Codegen review: 31 original versus 40 Rust instructions; signed nonpositive early exit, encoding branches, NUL store before return and unit count preserved. The shared unit writer is inlined.

  - address: 0x082767d8
    ghidra: FUN_082767d8
    name: utf8_to_utf16
    size: 36
    sig: "void utf8_to_utf16(unsigned short *out, const unsigned char *source)"
    module: cxx/string_encoding
    status: ported
    vars:
      param_1: "out"
      param_2: "source"
      local_c: "cursor"
      iVar1: "codepoint"
    notes: >-
      Five unconditional BL references. Repeatedly call the ported utf8_next_codepoint and store each result as a halfword, including decoded zero. Permissive decoder semantics are retained: malformed continuation payload bits are masked, overlong forms accepted, and unsupported leads terminate conversion. No deviations.
      Codegen review: 9 original versus 13 Rust instructions; direct utf8_next_codepoint relocation, halfword store and nonzero loop retained with an LLVM frame.

  - address: 0x082767fc
    ghidra: FUN_082767fc
    name: utf8_to_utf16_bounded
    size: 68
    sig: "int utf8_to_utf16_bounded(unsigned short *out, const unsigned char *source, int max_codepoints)"
    module: cxx/string_encoding
    status: ported
    vars:
      param_1: "out"
      param_2: "source"
      param_3: "max_codepoints"
      local_14: "cursor"
      iVar2: "count"
    notes: >-
      Six unconditional BL references. Decode and store up to the signed codepoint bound using utf8_next_codepoint. A decoded zero is written but not counted; exhausting the bound appends no terminator. Nonpositive bounds access neither pointer. No deviations.
      Codegen review: 17 original versus 25 Rust instructions; signed bound, direct decoder relocation, halfword store before zero exit and nonzero count retained.

  - address: 0x082765a8
    ghidra: FUN_082765a8
    name: string_object_assign_utf16
    size: 120
    sig: "void string_object_assign_utf16(StringObject *this, const unsigned short *utf16, int max_code_units)"
    module: cxx/string_object
    status: ported
    vars: {param_1: this, param_2: utf16, param_3: max_code_units,
      iVar3: requested_size, iVar4: destination}
    notes: >-
      The UTF-16 sibling of string_object_assign_payload @ 0x08276474 in the
      same two-word polymorphic string class (vtable 0x089a6044): it
      transcodes a bounded UTF-16 range into this object's payload. 120
      bytes, all code, no literal-pool word - the next function's
      `push {r3,r4,r5,lr}` starts at 0x08276620. 48 bl call sites (47
      unconditional, 1 predicated) and 0 tail b, binary-verified by decoding
      every B/BL word in osos.dec for every condition code, not from Ghidra
      xrefs. Algorithm: `subs r5,r1,#0; cmpne r6,#0` guards on utf16 == NULL
      or max_code_units <= 0 (signed LE) and tail-dispatches vtable slot
      +0xc with `this`; otherwise the ported bounded counter
      utf16_utf8_byte_len_bounded_plus1 @ 0x08276338 gives the inclusive
      UTF-8 byte length, vtable slot +0x8 is asked for exactly that many
      bytes as (this, byte_len, 0), a NULL result returns immediately with
      no copy and no +0xc fallback, the transcoder @ 0x0827675c writes the
      bytes, and THIS function stores the terminator at
      destination[byte_len - 1] from the sizing helper's count - the
      transcoder's return value is discarded. Note the guard tests the COUNT,
      not the first code unit as the C-string siblings test source[0]: a
      range whose first code unit is a UTF-16 NUL still allocates one byte.
      Deviations: vtable slots +0x8/+0xc stay modeled by
      STRING_OBJECT_ASSIGN_CSTR_OPS, because
      STRING_OBJECT_VTABLE holds ROM identities rather than host-callable
      pointers; the transcoder @ 0x0827675c calls utf16_to_utf8_bounded
      directly. The byte length stays i32 and the terminator is addressed
      with a signed offset, preserving the counter's documented 32-bit
      wrapping. match.py structural: same order and same shape - guard,
      sizing bl, allocating blx, NULL test, direct transcode call, and
      terminator store. Only the virtual allocation/clear boundary remains
      indirect through an ops table.

  # --- time: the packed calendar record ---
  - address: 0x08093c38
    ghidra: FUN_08093c38
    name: datetime_to_unix_seconds
    size: 104
    sig: "i32 datetime_to_unix_seconds(DateTime *dt)"
    module: time/datetime
    status: ported
    vars: {param_1/r0/r4: dt, FUN_08093af8: datetime_compare, FUN_0807ea68: datetime_day_number,
      DAT_08093c94: DATETIME_FLOOR (0x083e2e3e), DAT_08093c98: -719163, DAT_08093c9c: 86400}
    notes: >-
      Converts retailOS's packed ten-byte calendar record to a Unix
      timestamp. This is a second, entirely separate time stack from the ADS
      struct tm one in time/mktime.rs and time/localtime.rs - the record is
      byte-wide fields with a 16-bit year, and it is the shape every
      0x0807exxx / 0x08093xxx calendar helper takes.
      46 call sites - 45 bl plus one predicated b (condition CC) - counted by
      decoding every ARM B/BL word in work/firmware/osos.dec and resolving
      each target, not a Ghidra xref count. The lone b is a predicated tail
      call, not a veneer.
      Extent 104 bytes, 0x08093c38..0x08093ca0, not the 92 functions.csv
      reports. The 92 covers the 23 instructions and stops at the pop @
      0x08093c90; it drops the three literal-pool words this function's own
      pc-relative loads reach - 0x08093c94 = 0x083e2e3e (the floor record's
      address), 0x08093c98 = 0xfff506c5 = -719163, 0x08093c9c = 0x00015180 =
      86400. The next function opens at 0x08093ca0 with stmdb sp!,{r4,lr}.
      Algorithm - if datetime_compare(dt, FLOOR) is negative, return 0;
      otherwise (datetime_day_number(dt) - 719163) * 86400 + hour * 3600 +
      minute * 60 + second. 719163 is the day number of 1970-01-01 under
      0x0807ea68's proleptic-Gregorian Rata Die numbering (0001-01-01 = day
      1), so the add of the negative literal turns a Rata Die day count into
      days since the Unix epoch. The original strength-reduces both remaining
      multiplies - hour * 3600 is mov r2,#0xe1 / smulbb r1,r1,r2 then
      add r0,r0,r1 lsl #4, and minute * 60 is rsb r1,r1,r1 lsl #4 (times 15)
      then add r0,r0,r1 lsl #2. Every add is flagless ARM arithmetic, so the
      port wraps rather than panicking on overflow. The day-number call is
      side-effecting - it rewrites dt->weekday - which is why the argument is
      a mutable pointer, as in the original, which passes its incoming r0
      straight through.
      Record layout, recovered from the three functions that touch it (this
      converter, the comparator 0x08093af8 and the day-number helper
      0x0807ea68) - +0x00 second, +0x01 minute, +0x02 hour, +0x03 day of
      month, +0x04 month (1 = January), +0x05 padding with no reader
      anywhere, +0x06 u16 full year (ldrh), +0x08 weekday as an OUT field
      written by 0x0807ea68, +0x09 padding.
      The floor record is unreadable, so it stays a pointer. The comparator's
      second argument is the fixed record at runtime 0x083e2e3e; under this
      image's scatterload skew (image address = runtime address + 0xaed8, the
      region survey in this file) its bytes sit at image 0x083f1d16, inside
      the still-undecrypted zero run 0x083ee1c1..0x083f25ec. They read as
      zeros because that region was never decrypted, not because they are
      zero. Everything about the function says the record is 1970-01-01
      00:00:00 - it is the value below which the converter clamps to 0, and 0
      is exactly that instant's timestamp - but the port does not write the
      guess down as data. DATETIME_FLOOR holds the address, which is what the
      original's literal holds, and the comparison goes through the
      DATETIME_OPS dispatch; on target with the stock comparator wired in
      that is byte-for-byte the original whatever the record says.
      Deviations - datetime_compare @ 0x08093af8 (8 bl sites) is not
      ported and dispatches through DATETIME_OPS, the house pattern
      (app/iap_packet.rs, heap/alloc_core.rs). The wired default comparator
      returns 0, "at or after the floor", so the arithmetic path stays live
      with no hooks installed and nothing dereferences the unmapped host
      address in DATETIME_FLOOR. datetime_day_number @ 0x0807ea68 (25 bl
      sites) has since been ported in time/day_number.rs and is now the
      wired DATETIME_OPS day_number default, so the default path computes
      the real Rata Die value and weekday.
      Host tests (7) - the ten-byte record layout with year halfword-aligned
      at +6, the epoch converting to 0 through the arithmetic path rather
      than the clamp, ten known instants including a leap day, the 2100
      century non-leap year and two i32-wrapping post-2038 dates, records
      below the floor clamping to 0 without running the day-number helper (so
      the weekday side effect must not fire), the clamp being strict so an
      equal record takes the arithmetic path, out-of-range time-of-day bytes
      added verbatim with no validation, and the wired defaults producing the
      documented result without touching the floor address. The comparator
      and day-number stand-ins used by the tests are independent
      re-implementations of the two originals.

  # --- time: unsigned Unix seconds -> packed calendar record ---
  - address: 0x080964cc
    ghidra: FUN_080964cc
    name: unix_seconds_to_datetime
    size: 128
    sig: "void unix_seconds_to_datetime(u32 unix_seconds, DateTime *out)"
    module: time/unix_to_datetime
    status: ported
    renames:
      param_1/r0/r6: unix_seconds
      param_2/r1/r4: out
      iVar3/r5: whole_days
      FUN_08036f14: __rt_udiv
      FUN_0807eafc: day_number_to_datetime
      DAT_08096540: SECONDS_PER_DAY
      DAT_08096544: UNIX_EPOCH_DAY_NUMBER
      DAT_08096548: NEGATIVE_DAY_SCALE
    notes: >-
      Raw ARM confirms a 116-byte executable body
      (0x080964cc..0x08096540) and three literal-pool words through
      0x0809654c; the next push begins at 0x0809654c, so the complete extent
      is 128 bytes rather than the reported body-only 116. Decoding every
      B/BL word in osos.dec finds 28 unconditional BL callers, no predicated
      callers, and two unconditional B tail callers (0x080aa9a4,
      0x08271398). It treats unix_seconds as unsigned, computes
      whole_days = unix_seconds / 86400, passes whole_days + 719163 to the
      unported day-number-to-record converter 0x0807eafc, then writes hour,
      minute, and second from the residual seconds of the day after that
      callee returns. The port retains 0x0807eafc behind a volatile
      target/host seam: the target default calls its verified retailOS load
      address, while host tests install a recorder that proves day-number
      input, time boundary values including u32::MAX, call count, date-field
      preservation, and that the caller overwrites the callee's time fields.

  # --- Vincent JIT: pipeline emission sugar ---
  - address: 0x082605f0
    ghidra: FUN_082605f0
    name: cg_emit_load_word_at_offset
    size: 136
    sig: "cg_virtual_reg_t *cg_emit_load_word_at_offset(cg_block_t *block, cg_virtual_reg_t *base, U32 offset)"
    module: codegen/pipeline_emit
    status: ported
    vars: {param_1: block, param_2: base, param_3: offset}
    notes: >-
      136 bytes, 34 instruction words, no literal pool - the next
      function's own `stmdb sp!,{r3,r4,r5,r6,r7,r8,r9,lr}` starts at
      0x08260678, so the extent is exact. 44 call sites, ALL
      unconditional bl (0 predicated, 0 tail b), binary-scanned by
      decoding every B/BL word in osos.dec for every condition code, not
      from Ghidra xrefs. Callers are the Vincent 3D JIT's pipeline
      generators in three runs - 0x08243a28-0x082447e8,
      0x08245680-0x0824575c, 0x08245830-0x08245cc8 - passing offsets like
      0x1c/0x20/0x24/0x28/0x2c and `n * 4 + 0x6c`, i.e. word-strided
      structure fields. Algorithm: create three general-purpose virtual
      registers up front through cg_virtual_reg_create(block->proc, 0),
      then emit LDI offset_reg, offset / ADD address_reg, base,
      offset_reg / LDW value_reg, [address_reg] and return value_reg.
      Registers are therefore numbered in creation order, not use order.
      Opcode identification: an exhaustive census of the r1 immediate at
      every call site of each instruction factory gives load_immed = 40
      unanimously (201/201 sites), load in {38, 39, 41}, store in
      {42, 43, 44} and binary dominated by 1 (91 sites) - one contiguous
      alphabetical run ldb/ldh/ldi/ldw/stb/sth/stw at 38..44 anchored by
      ldi = 40, which fixes ldw = 41, plus add = 1 as the first opcode
      after nop = 0. The same census pins reg type 0 as the
      general-purpose class (792 of 835 cg_virtual_reg_create sites).
      Constants CG_INST_OPCODE_LDI/LDW/ADD and CG_REG_TYPE_GENERAL added
      to codegen/ir alongside the existing CG_INST_OPCODE_PHI.
      Deviation: the original reloads block->proc from block+4 before
      each of the three register creations; the port reads it once (the
      field is not written in between). match.py structural: identical
      call order, identical opcode/argument registers and the same
      `str r7,[sp]` fifth argument to cg_create_inst_binary; the diff is
      LLVM's frame-pointer prologue and that single hoisted load.

  - address: 0x0823a830
    ghidra: FUN_0823a830
    name: cg_emit_load_word_at_offset
    size: 136
    sig: "cg_virtual_reg_t *cg_emit_load_word_at_offset(cg_block_t *block, cg_virtual_reg_t *base, U32 offset)"
    module: codegen/pipeline_emit
    status: ported
    renames:
      param_1/r0/r4: block
      param_2/r1/r5: base
      param_3/r2/r6: offset
      uVar1/r7: offset_reg
      uVar2/r8: address_reg
      uVar3/r9/r0: value_reg
      FUN_082c23dc: cg_virtual_reg_create
      FUN_082c18ec: cg_create_inst_load_immed
      FUN_082c17f4: cg_create_inst_binary
      FUN_082c18c4: cg_create_inst_load
    notes: >-
      Raw ARM confirms the 136-byte, 34-word extent from the `push
      {r3,r4,r5,r6,r7,r8,r9,lr}` at 0x0823a830 through the `pop
      {r3,r4,r5,r6,r7,r8,r9,pc}` at 0x0823a8b4; the next sibling starts
      at 0x0823a8b8, and there is no literal pool. Decoding every ARM B/BL
      word in osos.dec finds nine direct callers, ALL unconditional bl
      (zero predicated, zero tail b): 0x0823d180, 0x0823d194, 0x0823d244,
      0x0823d2b0, 0x0823d308, 0x0823d7dc, 0x0823db10, 0x0823eb3c, and
      0x0823fcac. The body creates three general-purpose virtual registers
      from block->proc, then emits LDI offset_reg, offset / ADD address_reg,
      base, offset_reg / LDW value_reg, [address_reg], returning value_reg.
      A raw word-by-word comparison with the already ported
      cg_emit_load_word_at_offset @ 0x082605f0 finds exactly six differing
      words, all relative bl encodings to the same three register creations
      and three instruction factories. Deliberate deviation: no duplicate
      Rust export exists; the established codegen/pipeline_emit seam is
      behaviorally identical and serves this hook address. Its host tests
      prove zero and usize::MAX offsets, NULL bases, exact instruction
      wiring and append order, and virtual-register creation order.

  - address: 0x08260678
    ghidra: FUN_08260678
    name: cg_emit_store_word_at_offset
    size: 124
    sig: "cg_inst_t *cg_emit_store_word_at_offset(cg_block_t *block, cg_virtual_reg_t *base, U32 offset, cg_virtual_reg_t *value)"
    module: codegen/pipeline_emit
    status: ported
    vars: {param_1: block, param_2: base, param_3: offset, param_4: value}
    notes: >-
      124 bytes, 31 instruction words 0x08260678-0x082606f0, no literal
      pool - the last word is a tail `b` to cg_create_inst_store @
      0x082c196c and the next function's own `stmdb
      sp!,{r3,r4,r5,r6,r7,r8,r9,lr}` starts at 0x082606f4, so Ghidra's
      124-byte extent is exact. 17 call sites, binary-scanned by
      decoding every branch word in osos.dec: 16 unconditional bl plus
      one tail b at 0x08246fa8, zero predicated forms. They sit in one
      run inside the pipeline generators 0x08246f2c-0x08248538 - the
      same generator group that calls cg_emit_load_matrix4x4_word. The
      store twin of cg_emit_load_word_at_offset @ 0x082605f0: create
      two general-purpose virtual registers up front through
      cg_virtual_reg_create(block->proc, 0), then emit LDI offset_reg,
      offset / ADD address_reg, base, offset_reg / STW [address_reg],
      value and return the store instruction (the tail call hands the
      factory's return value straight to the caller). Only TWO registers
      are created - the value arrives as the fourth argument rather
      than being loaded, so there is no value_reg. Opcode 44 = stw by
      the alphabetical memory-opcode run census (see the 0x082605f0
      entry); CG_INST_OPCODE_STW added to codegen/ir next to LDI/LDW.
      Deviation: the original reloads block->proc from block+4 before
      each register creation; the port reads it once (the field is not
      written in between). match.py structural: identical call order,
      identical opcodes (40/1/44) and the same `str r6,[sp]` fifth
      argument to cg_create_inst_binary; the diff is LLVM's
      frame-pointer prologue and that single hoisted load.

  - address: 0x082606f4
    ghidra: FUN_082606f4
    name: cg_emit_load_word_at_offset
    size: 136
    sig: "cg_virtual_reg_t *cg_emit_load_word_at_offset(cg_block_t *block, cg_virtual_reg_t *base, U32 offset)"
    module: codegen/pipeline_emit
    status: ported
    renames:
      param_1/r0/r4: block
      param_2/r1/r5: base
      param_3/r2/r6: offset
      uVar1/r7: offset_reg
      uVar2/r8: address_reg
      uVar3/r9/r0: value_reg
      FUN_082c23dc: cg_virtual_reg_create
      FUN_082c18ec: cg_create_inst_load_immed
      FUN_082c17f4: cg_create_inst_binary
      FUN_082c18c4: cg_create_inst_load
    notes: >-
      136 bytes, 34 instruction words, no literal pool: 0x082606f4 through
      0x08260778. The next function begins with its own `stmdb
      sp!,{r3,r4,r5,r6,r7,r8,r9,lr}` at 0x0826077c, so the Ghidra extent is
      exact. Binary-scanning every ARM B/BL word in osos.dec finds 12 call
      sites, all unconditional bl (no predicated forms and no tail b):
      0x08246624, 0x08246638, 0x08246688, 0x082466e8, 0x08246734,
      0x08246748, 0x08246798, 0x082467fc, 0x08246810, 0x082468c4,
      0x082468dc, and 0x08246944, all in FUN_082465bc. Algorithm: create
      three general virtual registers from block->proc, then append LDI
      offset_reg, offset / ADD address_reg, base, offset_reg / LDW
      value_reg, [address_reg], returning value_reg. A word-by-word raw
      comparison with cg_emit_load_word_at_offset @ 0x082605f0 finds six
      differing words, exclusively the relative encodings of those same six
      calls; the two bodies are otherwise identical. Deliberate deviation:
      no duplicate Rust body or export exists. The established
      cg_emit_load_word_at_offset seam is behaviorally identical and serves
      a hook at this address; its host tests cover zero and usize::MAX
      offsets, NULL bases, instruction wiring, append order, and virtual
      register creation order.

  - address: 0x0826b198
    ghidra: FUN_0826b198
    name: cg_emit_load_word_at_offset
    size: 136
    sig: "cg_virtual_reg_t *cg_emit_load_word_at_offset(cg_block_t *block, cg_virtual_reg_t *base, U32 offset)"
    module: codegen/pipeline_emit
    status: ported
    renames:
      param_1/r0/r4: block
      param_2/r1/r5: base
      param_3/r2/r6: offset
      uVar1/r7: offset_reg
      uVar2/r8: address_reg
      uVar3/r9/r0: value_reg
      FUN_082c23dc: cg_virtual_reg_create
      FUN_082c18ec: cg_create_inst_load_immed
      FUN_082c17f4: cg_create_inst_binary
      FUN_082c18c4: cg_create_inst_load
    notes: >-
      136 bytes, 34 instruction words, no literal pool: `push
      {r3,r4,r5,r6,r7,r8,r9,lr}` at 0x0826b198 through `pop
      {r3,r4,r5,r6,r7,r8,r9,pc}` at 0x0826b21c. The next function begins
      at 0x0826b220 with its own `push {r3,r4,r5,r6,r7,r8,r9,lr}`, so
      Ghidra's extent is exact. Decoding every ARM B/BL word in osos.dec
      finds 10 call sites, all unconditional `bl` (zero predicated, zero
      tail `b`): 0x08249b04, 0x08249b18, 0x08249b2c, 0x08249b50,
      0x08249d04, 0x08249d1c, 0x0824a250, 0x0824a264, 0x0824a278 and
      0x0824a28c, all in FUN_082498d4. Its incoming r0/r1/r2 are block,
      base and offset. The body creates three general virtual registers
      from block->proc, then appends LDI offset_reg, offset / ADD
      address_reg, base, offset_reg / LDW value_reg, [address_reg], and
      returns value_reg. A word-by-word raw comparison with
      cg_emit_load_word_at_offset @ 0x082605f0 finds exactly six differing
      words: the relative `bl` encodings to the same three
      cg_virtual_reg_create calls and the load-immediate, binary and load
      factories. Deliberate deviation: no duplicate Rust body or export
      exists; the established cg_emit_load_word_at_offset seam is
      behaviorally identical and serves a hook at this address. Its host
      tests cover zero and usize::MAX offsets, NULL bases, instruction
      wiring, append order and virtual-register creation order.

  - address: 0x0826077c
    ghidra: FUN_0826077c
    ghidra: FUN_0826077c
    name: cg_emit_load_word_at_offset (second emission)
    size: 136
    status: duplicate
    notes: >-
      BYTE-IDENTICAL duplicate emission of cg_emit_load_word_at_offset @
      0x082605f0 (ported, module codegen/pipeline_emit): a word-by-word
      compare of both 136-byte bodies found exactly six differing words,
      all `bl` encodings to the SAME six targets
      (cg_virtual_reg_create x3, cg_create_inst_load_immed,
      cg_create_inst_binary, cg_create_inst_load) differing only in the
      displacement field. Same signature
      (block, base, offset) -> value_reg, same LDI/ADD/LDW idiom, same
      up-front creation of three general virtual registers. Ghidra's
      136-byte extent is exact here: the next function starts at
      0x08260804 with `ldr r0,[r0,#4]; mov r1,r2; cmp r0,#0; ldrne
      r2,[r0]; ldrne r2,[r2,#0x98]; bxne r2; bx lr` (a virtual-call
      trampoline on vtable slot 0x98). 17 call sites, ALL unconditional
      bl (0 predicated, 0 tail b), verified by decoding every branch
      word in osos.dec - matches Ghidra's count of 17. The callers sit
      in one run inside a second set of pipeline generators,
      0x08248790-0x08249254, passing the same word-strided field offsets
      as the primary's callers (0x1c, 0x20, 0x24, 0x2c, n*4+0x6c,
      0x70/0x74/0x78). The compiler emitted the helper twice for two
      generator groups (primary's callers: 0x08243a28-0x08245cc8).
      Following the rect_set / FUN_0826c5c0 precedent, no separate Rust
      symbol exists: a hook at 0x0826077c can target
      cg_emit_load_word_at_offset directly - the bodies are
      instruction-identical.

  - address: 0x0823c0dc
    ghidra: FUN_0823c0dc
    name: cg_emit_negated_sum
    size: 144
    sig: "cg_virtual_reg_t *cg_emit_negated_sum(void *ctx, cg_block_t *block, cg_virtual_reg_t *lhs, cg_virtual_reg_t *rhs)"
    module: codegen/pipeline_emit
    status: ported
    renames:
      param_1/r0: ctx (dead)
      param_2/r1/r4: block
      param_3/r2/r9: lhs
      param_4/r3/r8: rhs
      uVar1/r6: sum_reg
      uVar2/r7: mask_reg
      uVar3/r5/r0: result
      FUN_082c23dc: cg_virtual_reg_create
      FUN_082c18ec: cg_create_inst_load_immed
      FUN_082c17f4: cg_create_inst_binary
    notes: >-
      Raw ARM confirms 36 instruction words from 0x0823c0dc through
      0x0823c168: `push {r3-r9,lr}` to `pop {r3-r9,pc}`. The next
      function begins at 0x0823c16c with its own push, so Ghidra's
      144-byte extent is exact. Decoding every ARM B/BL word in osos.dec
      finds 10 callers, all unconditional BL (zero predicated, zero tail
      B): 0x0823e6d0, 0x0823e6e8, 0x0823e700, 0x0823e864, 0x0823e87c,
      0x0823e894, 0x0823f4dc, 0x0823f4f4, 0x0823f50c and 0x0823f524.
      Creates sum_reg and mask_reg through cg_virtual_reg_create, emits
      LDI mask_reg,0xff, creates result, then emits ADD sum_reg,lhs,rhs
      and opcode-15 RSB result,sum_reg,mask_reg before returning result.
      The backend's opcode-15 path emits `rsb result,sum_reg,#0`, so it
      ignores the binary record's source1 mask_reg even though this helper
      deliberately creates and records it. ctx is dead: the first
      non-prologue instruction overwrites r0 from block+4. No dispatch seam:
      all three direct callees are already ported.
  - address: 0x0824035c
    ghidra: FUN_0824035c
    name: cg_emit_subtract
    size: 60
    sig: "cg_virtual_reg_t *cg_emit_subtract(void *ctx, cg_block_t *block, cg_virtual_reg_t *lhs, cg_virtual_reg_t *rhs)"
    module: codegen/pipeline_emit
    status: ported
    renames:
      param_1/r0: ctx (dead)
      param_2/r1/r4: block
      param_3/r2/r7: lhs
      param_4/r3/r6: rhs
      uVar1/r5: dest
      FUN_082c23dc: cg_virtual_reg_create
      FUN_082c17f4: cg_create_inst_binary
    notes: >-
      Raw ARM confirms 15 instruction words from 0x0824035c through
      0x082403598. Ghidra's 64-byte extent is wrong: 0x0824039c starts
      the next function with its own push. Decoding every B/BL word in
      osos.dec found 20 callers, all unconditional BL (zero predicated,
      zero tail B): 0x0823de74, 0x0823dee0, 0x0823df50, 0x0823df6c,
      0x0823df88, 0x0823e04c, 0x0823e1d8, 0x0823e1ec, 0x0823e204,
      0x0823f064, 0x0823f090, 0x0823f0bc, 0x0823f0e8, 0x0823f208,
      0x0823f294, 0x0823f404, 0x0823f430, 0x0823f45c, 0x0823f488 and
      0x0823f634. Algorithm: create a general virtual register through
      cg_virtual_reg_create(block->proc, 0), then append a binary
      instruction with opcode 13, destination dest, source0 lhs and
      source1 rhs, returning dest. The JIT's opcode dispatch at
      0x082c6430 maps IR opcode 13 to ARM data-processing opcode 2
      (`sub`), so this is a subtraction emitter. Incoming r0 is dead:
      `ldr r0,[r1,#4]` overwrites it before any read; the port retains
      its ctx parameter for ABI parity but never dereferences it. Host
      coverage proves a garbage ctx, NULL lhs and opaque rhs pass through
      safely, stamps a general register numbered zero and appends exactly
      one opcode-13 binary instruction with the returned destination.

  - address: 0x08240738
    ghidra: FUN_08240738
    name: cg_emit_lerp_u8
    size: 112
    sig: "cg_virtual_reg_t *cg_emit_lerp_u8(void *ctx, cg_block_t *block, cg_virtual_reg_t *start, cg_virtual_reg_t *end, cg_virtual_reg_t *factor)"
    module: codegen/pipeline_emit
    status: ported
    renames:
      param_1/r0/r6: ctx (dead)
      param_2/r1/r4: block
      param_3/r2/r7: start
      param_4/r3/r8: end
      param_5/stack0/r9: factor
      uVar1/r5: delta (end - start)
      FUN_08240658: unported normalized signed multiply helper
      FUN_0824031c: unported add emitter
      FUN_082c23dc: cg_virtual_reg_create
      FUN_082c17f4: cg_create_inst_binary
      FUN_082c18ec: cg_create_inst_load_immed
    notes: >-
      Raw ARM confirms 28 instruction words from 0x08240738 through
      0x082407a4: `push {r3-r9,lr}` to the tail `b 0x0824031c`; the next
      function starts at 0x082407a8 with its own push. Decoding every ARM
      B/BL word in osos.dec found 11 callers, all unconditional BL (zero
      predicated, zero tail B): 0x0823e038, 0x0823e180, 0x0823e1a0,
      0x0823e1c0, 0x0823e79c, 0x0823e7bc, 0x0823e7dc, 0x0823e968,
      0x0823e984, 0x0823e9a0 and the wrapper call at 0x082407f8.
      Algorithm: emit delta = end - start (opcode 13); pass it with factor
      to 0x08240658, which emits product = delta * factor (opcode 9), then
      `(product + (product ASR 8)) ASR 8` through opcode-3 arithmetic
      shifts, the signed divide-by-255 idiom; tail-emit start + scaled
      (opcode 1) and return that virtual register. The leading ctx is saved
      in r6 but never read. Deviation: 0x08240658 and 0x0824031c are
      unported, so the Rust function emits their exact register creations
      and IR records inline rather than adding a dispatch seam; register
      and instruction creation order remains delta, product, shift,
      product_shifted, rounded_product, scaled, result. Host coverage
      proves opaque input registers and garbage ctx pass through without
      dereference, the seven precise IR records and their operands/opcodes,
      the factor-8 immediate, the returned final add register, and all seven
      general-register sequence numbers.

  - address: 0x0826087c
    ghidra: FUN_0826087c
    name: view_event_apply_localized_flags
    size: 4
    sig: "u32 view_event_apply_localized_flags(u8 *this, void *event)"
    module: app/view_event
    status: ported
    renames:
      param_1/r0/r6: this
      param_2/r1/r9: event
      DAT_08219380: localized flag-key configuration
      DAT_08219384: localization string-table singleton
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_08101edc: string_table_has_string
      FUN_08102168: string_table_parse_i32
      FUN_083d8b04: cxx_string_release
      FUN_0810de48: view_event_apply_mapped_staged_flags
    notes: >-
      Ghidra's four-byte extent is mechanically right but semantically
      incomplete: 0x0826087c is `b 0x082192a0`; the reached body runs from
      0x082192a0 through its tail `b 0x0810de48` at 0x0821937c, followed
      by literal words at 0x08219380/0x08219384. Decoding every aligned ARM
      B/BL word in osos.dec finds 11 direct callers, all unconditional bl
      (zero predicated), at 0x08222858, 0x08225538, 0x082291e0,
      0x08229b00, 0x0822a4c0, 0x08230db8, 0x082324f4, 0x08234430,
      0x08236ddc, 0x08239ec8, and 0x0823a224. A data word at 0x089b06cc
      also names the veneer, proving this is a virtual-method entry.
      When event is non-NULL, it builds temporary COW strings from the
      configuration keys at +0x0c and +0x10; each present localization-table
      key is fetched/parsed through FUN_08102168 (`"%d"`) and its low byte
      is written at this+0xb0 or this+0xb1. Both temporaries are released,
      then the function tail-branches to the ported mapped-staged-flag
      wrapper and returns its handled verdict. r2/r3 are only stack
      scratch/register residue at the two-argument string constructor.
      The unported integer getter is a volatile target seam at 0x08102168;
      target global-pointer reads are callbacks so host tests never
      reinterpret 32-bit firmware fields as 64-bit host pointers. Host tests
      prove the NULL-event skip, present-first/missing-second sequence,
      low-byte store, releases through real COW ports, and final delegation.

  # --- Silver framework: the kind-tagged mid-hierarchy controller ---
  - address: 0x08260a50
    ghidra: FUN_08260a50
    name: kinded_controller_construct
    size: 132
    sig: "void *kinded_controller_construct(void *this, RefcountedBody **body_slot, u8 kind, u8 flag, u8 extra_byte, u32 extra_word)"
    module: app/kinded_controller
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: body_slot (caller's handle slot, only READ - `ldr
        r1,[r1]`)
      param_3/r2: kind (per-leaf-class tag byte -> +0xc7)
      param_4/r3: flag (0/1 byte -> +0xc8)
      param_5/stack0: extra_byte (byte -> +0xc9)
      param_6/stack1: extra_word (word -> +0xcc)
      FUN_0839cd5c: body_acquire (KINDED_CONTROLLER_OPS slot - the
        60-byte refcounted_body_acquire counterpart of the ported
        refcounted_body_release @ 0x0839cd98, unported)
      FUN_0821a180: construct_base (KINDED_CONTROLLER_OPS slot - the
        direct base constructor, unported)
      FUN_0839cd98: refcounted_body_release (direct, ported in
        cxx/handle)
      FUN_08277440: string_default_construct (direct, ported in
        cxx/string_object)
      DAT_08260acc: empty-name literal (a zero word; the base threads it
        to silver_controller_construct as the name C string)
      DAT_08260ad0: VTABLE_ADDRESS (0x089a35e4)
    notes: >-
      Constructor of the mid-hierarchy Silver-framework controller class
      that 24 leaf classes in the 0x0822xxxx-0x0823xxxx block derive
      from, each chaining in with a distinct small-integer kind byte in
      r2 (0, 1, 3..11, 13..18, 20..23 observed at the 23 sites). TRUE
      EXTENT 132 BYTES, not Ghidra's 124: the code ends `pop
      {r3-r9,pc}` @ 0x08260ac8 and the function owns BOTH trailing
      literal-pool words - 0x00000000 @ 0x08260acc (reached by `add r2,
      pc, #76` @ 0x08260a78, the empty-name argument) and 0x089a35e4 @
      0x08260ad0 (reached by `ldr r0, [pc, #56]` @ 0x08260a90, the
      vtable). The next function @ 0x08260ad4 is the class's
      NULL-guarded deleting-dtor wrapper (`bl 0x08260aec`; tail `b
      operator_delete`), and the plain dtor @ 0x08260aec re-plants the
      SAME vtable literal 0x089a35e4 before destroying the StringObject
      at +0xbc and tail-branching to the base dtor @ 0x0821a31c - the
      cross-check that pins the vtable and the member layout to this
      class. 23 bl call sites, ALL unconditional (0 predicated, 0 tail
      b, 0 data-word references - never virtually dispatched), verified
      by decoding every ARM B/BL word in osos.dec; matches Ghidra's 23.
      Hierarchy: 0x08134db4 silver_controller_construct (ported) <-
      0x0821a180 (vtable 0x08993c90; refcounted UI-proxy body handle at
      +0xb4, word +0xb8 = -1, flag bytes +0xb0/+0xb1/+0xb2; chains
      silver_controller_construct with the THIRD argument as the name)
      <- THIS CLASS <- 24 leaves that plant their own vtable plus a
      secondary vtable on the embedded interface sub-object at +0xd0
      (via the 12-byte helper @ 0x0821599c, literal 0x08993258).
      Algorithm: body = *body_slot; acquire the body into a stack-local
      handle (0x0839cd5c); object = construct_base(this, &local,
      empty_name) (0x0821a180); release the local (0x0839cd98, net
      refcount-neutral around the base's own acquire into +0xb4); plant
      vtable 0x089a35e4 at object+0; default-construct the StringObject
      at +0xbc; store bytes +0xc4 = 0, +0xc5 = 0, +0xc6 = 1, +0xc7 =
      kind, +0xc8 = flag, +0xc9 = extra_byte and the word +0xcc =
      extra_word; return object - the BASE CONSTRUCTOR'S RETURN (`mov
      r4, r0` @ 0x08260a84), not the incoming this, and Ghidra's `void`
      return is wrong (the epilogue's `sub r0, r0, #188` recovers the
      object into r0). The stack arguments arrive via `ldrd r8, [sp,
      #32]` past the eight saved registers; the local handle slot IS
      the pushed-r3 scratch word, overwritten by the acquire's first
      store before any read. What kind/flag/extra_* MEAN is unrecovered
      (nothing in the ctor/dtor reads them; no symbolic name survives).
      Deviations - the vtable is stored as the u32 ROM address
      0x089a35e4 and the port claims nothing about its contents: the
      decrypted osos.dec holds an app/image_format jump table there
      (first words decode as `b 0x08105da0` & co., code addresses, not
      function entries), the stale-RW-page caveat app/silver_controller
      records for 0x08984570; the name is a crate static of four NUL
      bytes where the original passes its own zero literal word - both
      are empty C strings, all the base reads; the two unported callees
      ride the KINDED_CONTROLLER_OPS read_volatile seam (firmware
      transmutes of 0x0839cd5c / 0x0821a180 on target - hook-ready - a
      faithful host model of the acquire and a return-this base stub on
      host); on a 64-bit host the ported string_default_construct
      writes two host-width pointers at +0xbc, so its payload store
      covers +0xc4..+0xcc and is overwritten by the flag-byte stores
      that follow it in BOTH original and port (the ui/string_view
      precedent) - final state identical on target, deterministic on
      host. Host tests (5, slab hints::KINDED_CONTROLLER 0x3a000000,
      object at slab+4 for 8-alignment of the host StringObject):
      field-by-field state with argument propagation (kind 0x17, flag
      1, 0xab, 0xdeadbeef), call order acquire-before-base with the
      body visible in the slot, stores-and-return following a
      REDIRECTED base return (incoming this untouched), the NULL-body
      path (acquire/release no-ops, object still initialized), the
      wired host defaults keeping the refcount exactly neutral, and the
      zero-argument constant tail (0, 0, 1 not argument-driven).
      match.py --lib <worktree archive> structural: same sequence -
      acquire (blx seam), base (blx seam), release (bl), vtable literal
      0x089a35e4 store, string ctor bl, then the +0xcc word and
      +0xc9/+0xc6/+0xc8/+0xc7 byte stores; deltas are LLVM's fp frame,
      the two stack arguments reloaded from fp+8/fp+12 instead of the
      entry ldrd (same values), the +0xc4/+0xc5 zero pair merged into
      one strh (both constants zero), and the tail stores issued in a
      different but observationally equivalent order.

  - address: 0x082469b8
    ghidra: FUN_082469b8
    name: cg_emit_load_matrix4x4_word
    size: 144
    sig: "cg_virtual_reg_t *cg_emit_load_matrix4x4_word(void *ctx, cg_block_t *block, cg_virtual_reg_t *base, U32 col, U32 row)"
    module: codegen/pipeline_emit
    status: ported
    vars: {param_1: ctx, param_2: block, param_3: base, param_4: col, param_5: row}
    notes: >-
      144 bytes, 36 instruction words 0x082469b8-0x08246a44, no literal
      pool - the next function's own
      `stmdb sp!,{r0,r1,r2,r3,r4,r5,r6,r7,r8,r9,sl,fp,lr}` starts at
      0x08246a48, so Ghidra's 144-byte extent is exact for once.
      36 call sites, ALL unconditional bl (0 predicated, 0 tail b),
      verified by decoding every B/BL word in osos.dec for every
      condition code; they sit in one run inside the pipeline
      generators 0x082470d4-0x08248494, called as
      `FUN_082469b8(ctx, block, base, col, row)` with both indices in
      0..=3 (FUN_08247870 and FUN_082473c4 sweep the full 4x4) - the
      fragment pipeline's 4x4 matrix state. Algorithm: the
      computed-offset sibling of cg_emit_load_word_at_offset @
      0x082605f0 - create three general-purpose virtual registers up
      front (three back-to-back cg_virtual_reg_create(block->proc, 0)
      calls into r6/r7/r8), then emit LDI offset_reg, 4*(col + 4*row)
      (opcode 40) / ADD address_reg, base, offset_reg (opcode 1, the
      offset register passed on the stack as source1) / LDW value_reg,
      [address_reg] (opcode 41), and return value_reg. All four
      callees were already ported (cg_virtual_reg_create @ 0x082c23dc,
      cg_create_inst_load_immed @ 0x082c18ec, cg_create_inst_binary @
      0x082c17f4, cg_create_inst_load @ 0x082c18c4). Deviations: (1)
      the leading r0 argument is dead on arrival - the first
      instruction after the prologue overwrites r0 with the stacked
      fifth argument (`ldr r0,[sp,#0x20]`) and the incoming value is
      never read; the port keeps the parameter for ABI parity and
      never touches it. (2) The original reloads block->proc from
      block+4 before each register creation; the port reads it once
      (same deviation as the sibling). match.py structural: identical
      call order, opcodes and argument wiring including the
      `str r5,[sp]` fifth argument to cg_create_inst_binary; LLVM
      recomputes the offset as row*16 + col*4 (`lsl r0,#4; add r3,r0,
      r9,lsl #2`, arithmetically identical), adds its frame-pointer
      prologue, and hoists the single block->proc load.


  # --- C++ framework: debug/stats dump string writer ---
  - address: 0x082647c0
    ghidra: FUN_082647c0
    name: stream_write_cstr
    size: 48
    sig: "U32 stream_write_cstr(void *owner, Stream *stream, const char *text)"
    module: cxx/stream_write_cstr
    status: ported
    vars: {param_1: owner, param_2: stream, param_3: text, param_4: (phantom - the pushed r3 slot), local_10: written}
    notes: >-
      48 bytes, twelve instruction words, no literal pool: the body runs
      0x082647c0..0x082647ec inclusive, `stmdb sp!,{r3,r4,r5,lr}` through
      `ldmia sp!,{r3,r4,r5,pc}`, and the next function starts at
      0x082647f0 with `ldrb r1,[r0,#0x19]; strb r1,[r0,#0x18]; bx lr`.
      This CORRECTS an earlier survey of the big-endian neighbourhood
      that recorded it as 44 bytes - one word short. That survey's other
      conclusions hold: it opens push {r3,r4,r5,lr}, calls 0x08392478
      then 0x0827899c with sp as an out-parameter, returns [sp], and is
      unrelated to the pack/unpack family @ 0x0826161c-0x082617ec.
      53 call sites, ALL unconditional bl (0 predicated, 0 tail b),
      binary-scanned by decoding every B/BL word in osos.dec. They form
      five runs in 0x08265010-0x08266418, all inside the framework's
      debug/stats dump methods shaped Dump(this, stream), which build a
      scratch StringObject (ctor 0x08277440 or 0x08276a18, formatter
      string_object_format 0x082769d4), take its c_str
      (string_object_c_str 0x082a50b0) and hand it here - the literals
      are things like "Max Events In Queue: %d", "\t\t-\t" and "\n".
      Algorithm: length = strlen(text) through the ported unguarded
      retailOS strlen 0x08392478, then 0x0827899c(stream, length, text,
      &written), return written. The NUL is measured but not written.
      r0 is never read - it is the dump method's `this`, present because
      the helper is one of its member functions; kept in the signature so
      call sites transcribe unchanged. Ghidra's phantom fourth parameter
      and `local_10 = param_4` are only `push {r3,...}` spilling the
      scratch register whose slot becomes the out-parameter; that value
      is dead because 0x0827899c's core @ 0x082789b8 stores 0 through the
      out-pointer (`mov r6,#0; str r6,[r5]`) before any branch.
      Deviations: 0x0827899c is unported (it zeroes a fifth, stack-passed
      argument and tail-calls the buffered-stream write core @ 0x082789b8,
      which takes the lock guard at +0x00, appends length bytes and
      returns 0x15 when the stream's error byte at +0x14 is set), so it
      is reached through the new STREAM_WRITE_OPS table rather than a
      guessed implementation; and the out-parameter word is initialized
      to 0 in the port so an unwired or partially-writing hook cannot
      return a stale value. match.py structural: same shape - measure,
      call the writer with a stack out-slot, reload and return it -
      inflated because LLVM inlined the crate's five-instruction strlen
      instead of emitting the original's `bl 0x8392478`.

  # --- C++ framework: buffered-stream write front end (mode 0) ---
  - address: 0x0827899c
    ghidra: FUN_0827899c
    name: stream_write
    size: 28
    sig: "u32 stream_write(void *stream, u32 length, const u8 *data, u32 *written)"
    module: cxx/stream_write
    status: ported
    vars: {param_1: stream, param_2: length, param_3: data, param_4: written}
    renames:
      FUN_082789b8: "stream_write_core seam (unported, reached through
        STREAM_WRITE_CORE_OPS)"
    notes: >-
      28 bytes, seven words, no literal pool: mov ip,r3 / push {r3,lr} /
      mov r3,#0 / str r3,[sp] / mov r3,ip / bl 0x082789b8 / pop {ip,pc}.
      Ghidra's 28-byte extent is CORRECT here - it did not swallow the
      sibling: the next word @ 0x082789b8 opens a full push
      {r4-r11,lr} and is the separately linked shared buffered-stream
      write core (876 bytes, 0x082789b8..0x08278d24, next function's
      push {r3,r4,r5,lr} @ 0x08278d24), with its own second caller, the
      mode-6 wrapper @ 0x08277ef4 (byte-pattern-identical to this
      function except it stores 6 into the stacked slot). Verified by
      decoding every B/BL word in osos.dec: 25 branch sites - 21
      unconditional bl, 2 predicated bleq (@ 0x0827f010, @ 0x0827f068,
      callers gate the write on a zero test), 2 tail b (@ 0x08161ab0,
      bgt @ 0x082c7274). The attributed 23 bl count = the 21 bl plus the
      2 bleq. Algorithm: this is the mode-0 front end - push {r3,lr}
      reserves the first stack-argument slot (the pushed r3 is dead), 0
      is stored into it as a FIFTH argument the core reads at [sp,#64],
      r3 is restored through ip, and the core runs (stream, length,
      data, written, 0); the core's r0 status (0 ok, 5 short final
      flush, 0x15 error byte at +0x14 already set) returns verbatim
      through pop {ip,pc}. The fifth word is forwarded by the core as
      the third stacked argument of the facade vtable +0x20 write call;
      its meaning is not established (0 here, 6 in the sibling) so it is
      named mode and documented, not invented. The two predicated bleq
      callers check a condition themselves; the wrapper has no guards of
      its own (length 0 and NULL data reach the core untouched).
      Deviation: the core @ 0x082789b8 is unported, reached through the
      new STREAM_WRITE_CORE_OPS table (volatile dispatch, like
      stream_write_cstr's STREAM_WRITE_OPS); the unwired default mimics
      the core's own error-byte-set no-progress outcome - stores 0
      through written (the core's mov r6,#0 / str r6,[r5] before its
      first branch) and returns 0x15. The original pushes the dead r3
      and overwrites its slot with 0; the port passes the literal 0.
      stream_write_cstr keeps its own STREAM_WRITE_OPS seam unchanged.
      match.py structural: LLVM frame-pointer prologue, then ldr of the
      ops word, str of the zeroed fifth stack arg, blx through the seam
      in place of the direct bl 0x082789b8 - same shape, inflated by
      the volatile dispatch.

  # --- Stream-owner exact-write status adapter ---
  - address: 0x081f53e0
    ghidra: FUN_081f53e0
    name: stream_write_exact
    size: 76
    sig: "u32 stream_write_exact(StreamWriteOwner *owner, const u8 *data, u32 *length)"
    module: cxx/stream_write_exact
    status: ported
    renames:
      param_1: owner
      param_2: data
      param_3: length
      local_10: completed
      FUN_0827899c: stream_write
    notes: >-
      Raw osos.dec decoding confirms nineteen instruction words at
      0x081f53e0..0x081f5428, no literal pool; the next word at
      0x081f542c starts the separately linked stream-owner constructor.
      Binary decoding of every ARM B/BL immediate finds twelve callers,
      all unconditional bl (0x081615b4, 0x081615f4, 0x08161624,
      0x08161650, 0x08161684, 0x081616c4, 0x081616fc, 0x08161728,
      0x08161760, 0x0816180c, 0x08161854, 0x081619b0); no predicated or
      tail-branch caller. Algorithm: load requested from length and the
      stream pointer at owner+8, call ported stream_write with a stack
      completed out-word, replace length with completed, then return 0
      when requested equals completed, otherwise 1 for stream_write
      status 0x11 or 3 for any other status. The ARM prologue pushes its
      ABI-dead incoming r3 as the out-word; retailOS stream_write's core
      overwrites it before return. Deviation: the Rust stack word starts
      as zero, preventing an incomplete replacement core from exposing
      scratch-register contents; a conforming core has identical output.

  - address: 0x08134db4
    ghidra: FUN_08134db4
    name: silver_controller_construct
    size: 204
    sig: "void *silver_controller_construct(void *this, const char *name)"
    module: app/silver_controller
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: name
    notes: >-
      Constructor of the retailOS controller object whose two accessors
      app/template_binding already ports - same class, proven by the
      members it writes: its base call reaches
      string_object_construct_from_cstr @ 0x08277304 on this+0x28
      (template_binding NAME_OFFSET) and its second inline container
      construction targets this+0x94 (template_binding
      BINDING_MAP_OFFSET, whose header word at +0x10 is the this+0xa4
      the validation walk @ 0x08134938 loads). 92 bl call sites and 1
      plain-b tail call (a derived constructor chaining in), no
      predicated branches, and 0 occurrences of 0x08134db4 as a data
      word, so it is never reached through a vtable - all four counts
      from decoding every B/BL word in osos.dec, not from Ghidra xrefs.
      Extent 0x08134db4..0x08134e80 = 204 bytes, 50 instructions plus
      the single literal-pool word 0x08984570 at 0x08134e7c that
      "ldr r1,[pc,#184]" @ 0x08134dbc reaches; the previous function
      ends with "b 0x0810e2a0" at 0x08134db0 and the next starts with
      "cmp r0,#0" at 0x08134e80. TWO arguments, not one - nothing writes
      r1 before "bl 0x0810e2e4", so the caller's r1 flows into the base
      constructor, which opens "mov r6,r1" and hands r6 to the ported
      string constructor for the name at +0x28. r2/r3 are not arguments:
      "push {r2,r3,...}" is the ADS idiom for eight bytes of stack
      scratch and both pushed words are overwritten before any read.
      Algorithm - base ctor 0x0810e2e4(this, name); overwrite the vtable
      word at +0 with 0x08984570; inline-construct the auxiliary
      container at +0x78 (node allocator 0x083c4d30) and then the
      binding map at +0x94 (node allocator 0x083c14c8, the very
      allocator app/event_source names for its own event tree); finally
      0x0810de38(this, demo_mode_instance()), four instructions that
      store the singleton into this+0x44 and tail-dispatch vtable slot
      +0x2c. Returns the base constructor's return value, ADS ctor
      convention. Each container is 28 bytes with an inline node pool -
      chunk list +0, free list +4, cursor +8, limit +0xc, header node
      +0x10, count +0x14, two flag bytes +0x18/+0x19 - the same shape
      app/event_list documents; the 28-byte stride is confirmed by the
      sibling constructor @ 0x081cdab4, which builds the identical pair
      at +0x0c and +0x28 through the same two allocators. The header
      node is closed into an empty circle: parent(+4)=NULL,
      left(+8)=right(+0xc)=header. Deviations - the pool word 0x08984570
      is a vtable address in the LOADED image, and the decrypted
      osos.dec holds unrelated table bytes there, exactly the caveat
      app/registry records for demo_mode_instance's own vtable
      0x08989718, so the port stores it as a named constant and claims
      nothing about its contents; the four unported callees
      (0x0810e2e4, 0x083c4d30, 0x083c14c8, 0x0810de38) go through the
      SILVER_CONTROLLER_OPS read_volatile seam (house pattern) while
      demo_mode_instance @ 0x081883fc is ported and called directly;
      the original materializes the two zero flag bytes through its
      stack scratch, and the port stores zero directly with no scratch
      frame. NOT HOOK-READY - the default allocator boundary returns
      NULL and neither the original nor the port guards the store
      through it, so branching stock code here before 0x083c4d30 /
      0x083c14c8 are ported would fault. match.py structural - same
      skeleton throughout (base call, vtable store, the eight-store
      container clear, allocator call, str [+0x10], str [node+4],
      reload, str [node+8], str [node+0xc], twice; then the singleton
      fetch, the install and "mov r0,this"); the diff is the four
      ops-table blx indirections replacing direct bl, LLVM merging the
      two adjacent flag strb into one strh and CSE-ing away the second
      of the original's two header-slot reloads, and the crate's fp
      frame.
  - address: 0x08262908
    ghidra: FUN_08262908
    name: clock_source_destroy
    size: 4
    sig: "void *clock_source_destroy(void *this)"
    module: cxx/clock_source_destroy
    status: ported
    vars: {param_1: this}
    notes: >-
      One instruction, "bx lr" (the word 0xe12fff1e) - an empty C++
      destructor, NOT a veneer. Binary-verified from osos.dec: a veneer
      would be 0xe51ff004 plus a target word or a plain b, and neither
      is here. The preceding function ends "bx lr" at 0x08262900 with
      its own literal pool word 0x089a80b0 at 0x08262904, and the next
      function is the real veneer "b 0x082e8070" at 0x0826290c, so the
      extent is exactly four bytes with no trailing literal. 45 bl call
      sites, 0 plain b, 0 predicated forms and 0 occurrences of
      0x08262908 as a data word - never reached through a vtable, so
      every caller binds it statically, which is what a compiler does
      for a known-type destructor. All four counts from decoding every
      B/BL word in osos.dec. The pairing is the proof - 18 sites are
      literally "add r0,sp,#N / bl 0x08262958" ... "add r0,sp,#N /
      bl 0x08262908" on the SAME stack slot, and 2 more pair the same
      way with the sibling constructor 0x08262a9c; the rest are the
      same shape with the object pointer materialized a few
      instructions earlier. No site is followed by operator delete:
      all 45 destroy a stack temporary. Those two constructors are
      siblings over the base 0x082628f4, which is the whole object -
      "str <vtable>,[r0]; strb r1,[r0,#4]" - so the class is
      { vtable, u8 kind }, 0x08262958 passes kind=1 and 0x08262a9c
      kind=0, each installing its own vtable, and both share this one
      empty destructor. The object is a clock: the canonical use (e.g.
      @ 0x081944b4) constructs it on the stack, calls vtable slot +0xc
      with an out pointer to a {sec, nsec} pair, converts that through
      0x082a1c30 - which is sec*1000 + nsec/1000000, dividing by the
      literal 1000000 @ 0x082a1c58 - and destroys it here; 0x08262ab8
      subtracts two such pairs field by field and 0x081643cc compares
      the elapsed result against 500. r0 passing through is the
      load-bearing detail: bx lr leaves it untouched, the ADS
      destructor convention, so the signature returns this rather than
      void even though no caller of this one chains into operator
      delete. Deviations - none, the port is the identity function. It
      carries its own link_section because it compiles to bytes
      identical to cxx::trivial_destructor @ 0x082646ac and LLVM's
      identical-code folding would otherwise collapse the two exports
      into one, leaving the two stock addresses sharing a single
      hookable symbol. match.py structural - one instruction vs three,
      the difference being only the crate's fp frame
      (push {fp,lr} / mov fp,sp / pop {fp,pc}) around an empty body
      that returns r0 unchanged.
  - address: 0x0826291c
    ghidra: FUN_0826291c
    name: initialize_opaque_context
    size: 40
    sig: "u32 *initialize_opaque_context(u32 *context)"
    module: cxx/opaque_context_initialize
    status: ported
    renames:
      param_1/r0/r4: context
      "context+0x18": initialization_status
      FUN_082e7e54: opaque_context_initialize (unported host/target seam)
      r1: null_selector
      uVar1/r0: initialization_status
    notes: >-
      Raw osos.dec establishes the exact 40-byte instruction extent
      0x0826291c..0x08262940: push {r4,lr}; mov r4,r0; mov r0,#0;
      str r0,[r4,#0x18]; mov r0,r4; mov r1,#0; bl 0x082e7e54;
      str r0,[r4,#0x18]; mov r0,r4; pop {r4,pc}. The following
      separately linked prologue at 0x08262944 pins the end; no literal
      pool is present. Whole-image decoding of every ARM B/BL immediate
      finds exactly 9 direct unconditional bl callers at 0x0818a298,
      0x0818b3c4, 0x081d6990, 0x081d7d34, 0x08261b64, 0x08261b80,
      0x08262868, 0x08262a54, and 0x08262a70: 0 predicated calls and
      0 tail b calls. It clears the sixth u32 word, invokes
      FUN_082e7e54 with a NULL second argument, stores that returned
      status at the same word, and returns the original r0 preserved in
      r4. The opaque context type and the callee's semantic identity are
      unrecovered and intentionally not invented. Deviation: the
      unported callee is an indirect fixed-address call on target and a
      test seam on host, preserving argument order and the two stores
      around it. Host tests verify the clear-before-call observation,
      NULL selector, returned status including zero, unchanged neighbour
      words, and original-pointer return.
      match.py against this worktree archive preserves the move/clear,
      call, second store, and return sequence; LLVM adds its fp frame,
      loads the fixed retail address from a literal, and uses blx for the
      deliberate unported-callee boundary.


  - address: 0x08262958
    ghidra: FUN_08262958
    name: clock_source_construct
    size: 28
    sig: "void *clock_source_construct(void *this)"
    module: cxx/clock_source_construct
    status: ported
    vars: {param_1: this}
    notes: >-
      The kind-1 clock constructor, sibling of clock_source_destroy @
      0x08262908 (ported) and of the kind-0 constructor @ 0x08262a9c
      (identified, not ported). Ghidra's 24 bytes dropped the trailing
      literal pool word again: the body is push {r4,lr} / mov r1,#1 /
      bl 0x082628f4 / ldr r1,[pc,#4] / str r1,[r0] / pop {r4,pc} plus
      the literal 0x089a80d0 @ 0x08262970, and the next function's
      prologue @ 0x08262974 pins the extent at 28 bytes. Call count
      from decoding every B/BL word in osos.dec: 41 bl call sites, 0
      plain b, 0 predicated forms, and 0 occurrences of 0x08262958 as
      a data word - always bound statically, never through a vtable,
      as a compiler calls a known-type constructor. Algorithm: the
      canonical derived-class constructor over the base @ 0x082628f4
      (identified, not ported; its own 16-byte body is binary-verified
      as "str 0x089a80b0,[r0]; strb r1,[r0,#4]; bx lr" with its
      literal @ 0x08262904), so the object is { u32 vtable, u8 kind }
      and this constructor builds it with kind=1, then replaces the
      base vtable with the derived one 0x089a80d0. r0 is live across
      the bl, so the vtable store and the return both follow the
      base's r0, not the argument - the port keeps that contract and a
      test pins it with a redirecting base. The object is a clock (the
      clock_source_destroy analysis, independently re-verified): the
      canonical use @ 0x081944b4 constructs it on the stack, calls
      vtable +0xc into a { sec, nsec } out pair, converts through
      0x082a1c30 (sec*1000 + nsec/1000000) and destroys it; the only
      other reference to 0x089a80d0 in the image is the literal pool @
      0x08190a34 of a function inlining this constructor, whose next
      pool word is 0x000f4240 = 1000000. ANOMALY, documented not
      invented: the installed vtable 0x089a80d0 does not fully decode
      - slots +0x04/+0x08/+0x0c (0x081c0030, 0x081c0038, 0x081c0028)
      are the middle of one conditional path inside FUN_081bff64
      (fallen into from beq @ 0x081c0024, never an entry), +0x14 and
      +0x18 are inside FUN_0810301c / FUN_08102f44, only +0x00
      (0x083a5a54) and +0x10 (0x0810301c) decode as entries, and the
      kind-0 vtable @ 0x089a80f0 has literal zeros at +0x00, +0x08,
      +0x0c, +0x10, +0x14 - yet call sites blx the +0x0c slot. A
      whole-image scan finds no data word or code reference that could
      patch the slots at runtime, and the region is not a decryption
      artifact (the surrounding code's internal branches are
      self-consistent). The resolution is unknown; the port encodes
      nothing about it - the constructor's contract is only to install
      the pointer, which it does verbatim. Deviations: the unported
      base goes through the CLOCK_SOURCE_OPS read_volatile seam (house
      pattern), whose default is NOT a stub but a binary-verified
      reproduction of the base's four instructions, so with the
      default wired the port is behaviorally identical and hook-ready;
      a future port of 0x082628f4 replaces the default without
      touching this caller. Host tests (4): default-ops construction
      yields { vtable = 0x089a80d0, kind = 1 }, returns this, and
      touches no other byte; a recording base proves the base runs
      exactly once with kind=1 and the derived store lands after it;
      a redirecting base proves the store and the return follow the
      base's r0; the default base reproduces the stock four
      instructions for an arbitrary kind byte. match.py structural
      (exit-1 diff expected): same skeleton - the base call, mov
      r1,#1, the 0x089a80d0 literal, str r1,[r0] - differing by the
      ops-table blx indirection replacing the direct bl and the
      crate's fp frame, the accepted silver_controller profile.

  # --- display: the two-panel singleton table ---
  - address: 0x081d8870
    ghidra: FUN_081d8870
    name: display_get
    size: 188
    sig: "Display *display_get(u32 display_id)"
    module: drivers/display
    status: ported
    vars: {param_1/r4: display_id,
      DAT_081d8914: guard-base-pool-word (0x089ca8b0 = SECONDARY_DISPLAY_GUARD),
      DAT_081d8918: object-pool-word (0x08a1b6cc = SECONDARY_DISPLAY),
      DAT_081d891c: DSO_HANDLE (0x089ca09c = __dso_handle),
      DAT_081d8920: dtor-pool-word (0x081ce4d4),
      DAT_081d8924: guard-pool-word (0x089ca8b4 = INTERNAL_DISPLAY_GUARD),
      DAT_081d8928: object-pool-word (0x08a1b624 = INTERNAL_DISPLAY)}
    notes: >-
      The image's whole display table, and the other half of
      display_get_layer @ 0x081d9064 (same module) - display_get(0) is the
      internal LCD, display_get(1) the secondary output, every other id is
      NULL. EXTENT CORRECTED - Ghidra says 164 bytes, which is the code
      only; the six pool words @ 0x081d8914..0x081d8928 belong to the
      function and the next one opens at 0x081d892c (mov r3,r0; push
      {r4,lr}), so the true extent is 0x081d8870..0x081d892c = 188 bytes.
      123 call sites - 118 bl and 5 bleq, zero plain-b - verified by
      decoding every branch word in osos.dec for every condition code.
      Shape - two ADS function-local statics written out in full and
      selected by the argument, the media_command_facade_get idiom done
      twice. movs r4,r0 / beq picks the id-0 arm; cmp r4,#1 / movne r0,#0 /
      popne is the NULL exit for everything else. Each arm - tst guard,#1
      fast path; cxa_guard_acquire (0x082ab31c); ctor 0x081d92a4(object,
      r4) returning this in r0, the id coming from r4 and not an
      immediate; cxa_atexit(this, 0x081ce4d4, __dso_handle 0x089ca09c)
      (0x082ab1c8); cxa_guard_release (0x082ab338); reload the OBJECT pool
      word - never the ctor's return - and return it. Pure ADS pool
      artifact worth knowing - the id-1 fast path reads its guard as
      [0x089ca8b0], the id-0 fast path as [0x089ca8b0 + 4], while the id-0
      slow path loads 0x089ca8b4 as its own literal; one word, two names.
      THE 5 PREDICATED SITES ARE A REAL BEHAVIOURAL FACT. All five
      (0x0828c528, 0x0828c5f4, 0x0828cc2c, 0x0828d0b0, 0x0828d714) sit in
      one cluster and all five read `cmp r0,#0; moveq r0,#1; bleq
      0x081d8870` - "if the caller was handed a NULL display, default to
      display 1". display_get has no NULL-related guard of its own; the
      callers do the test, and every predicated site asks for the
      secondary output.
      OBJECT SIZE 0xa8, two independent witnesses - the constructor's last
      store is strb r5,[r4,#0xa5], and the two static objects are
      0x08a1b6cc - 0x08a1b624 = 0xa8 apart. Display therefore gains a
      reserved_98 [u8; 0x10] tail and a size_of assert on the 32-bit
      target; the constructor's block there is zeroes except 1 at +0x9a
      and 3 at +0xa3.
      Deviations - the guard pair and cxa_atexit are ported and called
      directly; the constructor 0x081d92a4 is not, so it rides the new
      DisplayHooks.display_construct slot whose documented zeroing default
      installs neither the mutex nor the panel driver nor the id, which is
      why this symbol is NOT hook-ready. Guards and objects are crate
      statics rather than the stock .bss words (the media_command_facade
      deviation - the 0x089cxxxx / 0x08a1xxxx pages are runtime
      initialized and the decrypted image holds UI strings at those
      offsets); zero is the exact pre-init state either way. The
      registered destructor 0x081ce4d4 is NOT a function entry - it is a
      `mov r0,r7` in the middle of a large function feeding a
      bl 0x08391e38 string compare, so as a shutdown handler it would run
      on registers belonging to nobody and return through a frame it never
      pushed; the registration could never fire (and retailOS never runs
      exit's chain), so it is modeled as a no-op, same as
      media_command_facade_get's 0x0817f190 and node_list_get's
      0x0810516c.
      Host tests (10) - both arms constructing their own object with their
      own id, the two guards proven independent, ids 2/3/0xff/0x80000000/
      0xffffffff returning NULL and constructing nothing, the bit-0 fast
      path preserving a post-construction mutation, a bit-0-set guard
      short circuiting everything, a nonzero bit-0-clear guard still
      refused by acquire, the reloaded object literal winning over the
      ctor's return while the cxa_atexit node carries the ctor's return,
      the chain running the no-op destructor, and the zeroing default
      covering exactly 0xa8 bytes.
      match.py structural - both arms present with guard test, inlined
      acquire (cmp/str), the hooked ctor via blx, inlined cxa_atexit
      (node alloc + stm of the four fields) and the reloaded object
      literal; LLVM tests id == 1 before id == 0 and inlines the ported
      guard/atexit callees, the accepted intra-crate noise.

  # --- app: mutex service adapters ---
  - address: 0x0822834c
    ghidra: FUN_0822834c
    name: lock_service_create
    size: 20
    sig: "int lock_service_create(void *service, Mutex *mutex)"
    module: app/lock_service
    status: ported
    renames: {param_1/r0: service, param_2/r1: mutex}
    notes: >-
      Member function of the Silver-era mutex-service class that adapts void
      mutex_create @ 0x080744a4 (ported in kernel/sync_mutex.rs) to the int
      method(this, Mutex *) convention. Raw osos.dec words at
      0x0822834c..0x08228360 are exactly `stmdb sp!,{r4,lr}; mov r0,r1; bl
      0x080744a4; mov r0,#0; ldmia sp!,{r4,pc}`; 0x08228360 opens the
      separately linked lock sibling, confirming Ghidra's 20-byte extent.
      r0 is dead: every direct caller supplies its unrelated service this
      pointer in r0, while r1 reaches mutex_create. The unconditional
      `mov r0,#0` makes success observable after the call.
      Decoding every ARM B/BL word in osos.dec found exactly 9 callers, all
      unconditional `bl` with no predicated forms and no tail branches:
      0x081c89b4, 0x081d9b18, 0x081d9b4c, 0x081e5d34, 0x081e5d44,
      0x081e5da0, 0x08206c70, 0x08206c98, and 0x0820cec0.
      Deviations - none: mutex_create is called directly and marked
      inline(never) so this adapter retains its original direct-call shape.
      Host tests prove a garbage service pointer is ignored, the mutex from
      r1 reaches mutex_create, creation allocates and defines a 4-byte cell
      with initial count 1, the cell receives the ROM handle, and the
      Mutex padding word is reset to zero.

  - address: 0x08228360
    ghidra: FUN_08228360
    name: lock_service_lock
    size: 20
    sig: "int lock_service_lock(void *service, Mutex *mutex)"
    module: app/lock_service
    status: ported
    vars: {param_1/r0: service, param_2/r1: mutex}
    notes: >-
      Member function of a Silver-era service class that adapts the void
      kernel primitive mutex_lock @ 0x0807f5c4 (ported, kernel/sync_mutex.rs)
      to the int method(this, Mutex *) shape the manager classes call. Raw
      body at 0x08228360 is exactly five instructions: stmdb sp!,{r4,lr} /
      mov r0,r1 / bl 0x0807f5c4 / mov r0,#0 / ldmia sp!,{r4,pc}. The next
      entry opens at 0x08228374, confirming the 20-byte
      0x08228360..0x08228374 extent.
      r0 is dead: the adapter overwrites its this argument before it can be
      read, and supplies its Mutex argument from r1 to mutex_lock. The
      unconditional zero result is observable: after calls at 0x0820ca68
      and 0x081e4998 the callers compare r0 with zero and branch on failure.
      Binary scanning every ARM B/BL word found 39 callers: all are
      unconditional bl, with no predicated forms and no tail branches.
      Deviations - none. mutex_lock is called directly and is marked
      inline(never) so this adapter retains its original direct-call shape.
      Host tests (7) prove a garbage or NULL this is ignored, a live mutex
      reaches the ROM wait through r1, NULL-cell and zero-handle mutexes
      are no-ops that return success, no mutex word is modified, and
      repeated waits plus a lock/unlock pair preserve the same handle.

  - address: 0x08228374
    ghidra: FUN_08228374
    name: lock_service_delete
    size: 20
    sig: "int lock_service_delete(void *service, Mutex *mutex)"
    module: app/lock_service
    status: ported
    renames: {param_1/r0: service, param_2/r1: mutex}
    notes: >-
      Member function of the Silver-era mutex-service class that adapts void
      mutex_delete @ 0x0807f650 (ported in kernel/sync_mutex.rs) to the int
      method(this, Mutex *) convention. Raw osos.dec words at
      0x08228374..0x08228388 are exactly `stmdb sp!,{r4,lr}; mov r0,r1; bl
      0x0807f650; mov r0,#0; ldmia sp!,{r4,pc}`; 0x08228388 opens the
      separately linked unlock sibling, confirming Ghidra's 20-byte extent.
      r0 is dead: each direct caller supplies the mutex in r1, while r0 is
      its unrelated service this pointer. The unconditional `mov r0,#0`
      makes status success observable after the call.
      Decoding every ARM B/BL word in osos.dec found exactly 9 callers, all
      unconditional `bl` with no predicated forms and no tail branches:
      0x081c89d8, 0x081d9b74, 0x081d9b8c, 0x081e5f28, 0x081e5f5c,
      0x081e5f6c, 0x08206e00, 0x08206e1c, and 0x0820cedc.
      Deviations - none: the direct Rust call retains the already-ported
      primitive, and both exports are inline(never) to preserve an ARM call
      target. Host tests prove invalid and NULL service pointers are ignored,
      a live mutex reaches the delete primitive through r1, an uncreated
      mutex remains a successful no-op, and repeated deletion destroys its
      cell only once.

  - address: 0x08228388
    ghidra: FUN_08228388
    name: lock_service_unlock
    size: 20
    sig: "int lock_service_unlock(void *service, Mutex *mutex)"
    module: app/lock_service
    status: ported
    vars: {param_1/r0: service, param_2/r1: mutex}
    notes: >-
      Member function of a Silver-era service class that adapts the void
      kernel primitive mutex_unlock @ 0x0807f6a0 (ported, kernel/sync_mutex.rs)
      to the int method(this, Mutex *) shape the manager classes call. Body is
      five instructions, binary-verified against osos.asm at 0x08228388:
      stmdb sp!,{r4,lr} / mov r0,r1 / bl 0x0807f6a0 / mov r0,#0 /
      ldmia sp!,{r4,pc}. The push of r4 is ADS keeping the stack
      eight-byte aligned; r4 is never read or written.
      One of four byte-identical-shaped adapters sitting back to back at
      0x0822834c (create -> 0x080744a4), 0x08228360 (lock -> 0x0807f5c4),
      0x08228374 (delete -> 0x0807f650) and 0x08228388 (unlock -> 0x0807f6a0).
      All four adapters are ported. The next function opens at 0x0822839c
      with the same prologue over a different callee, so Ghidra's 20-byte
      extent is correct.
      The first argument is the caller's this and is dead - the mutex arrives
      in r1 and r0 is overwritten before it is used. Call sites pass three
      unrelated kinds of value in it - a loaded field (ldr r0,[r4,#0x28c] @
      0x081d9ae8, in a 32-iteration constructor loop that only assigns that
      field AFTER the loop, so the adapter is handed an uninitialized word),
      an interior pointer (add r0,r4,#0xccc @ 0x081c89b0) and the word next to
      the mutex (ldr r0,[r4,#0xa08] with r1 = r4 + 0xa00 @ 0x0820c9cc).
      The zero return is load-bearing, not epilogue noise - 0x0820c9d4 does
      bl / cmp r0,#0 / moveq r0,r6, and 0x081d9980 tail-branches into the
      adapter so its zero becomes the caller's return value. The status is
      unconditional; the kernel primitives are void and nothing can fail.
      46 call sites total, counted in osos.asm - 45 bl plus the one tail b at
      0x081d9980 - spread over six manager classes at 0x081c8494, the
      0x081d95xx..0x081d99xx slot table (32 twenty-byte slots each with an
      embedded mutex at +0x0c), 0x081e36xx..0x081e5cxx, 0x08206xxx and
      0x0820cxxx.
      Deviations - none; mutex_unlock is ported and called directly.
      Host tests (6) - the mutex comes from the second argument while a
      garbage this is ignored, a NULL this is equally fine, an uncreated
      mutex (NULL sem cell) and a created-but-undefined cell are both no-ops
      that still report success, the Mutex object is left byte-for-byte
      untouched, and repeated unlocks signal every time.

  # --- sqlite: VDBE result-column names ---
  - address: 0x0838d004
    ghidra: FUN_0838d004
    name: vdbe_set_col_name
    size: 140
    sig: "int vdbe_set_col_name(Vdbe *p, int idx, int var, char *z_name, int n)"
    module: sqlite/vdbe_set_col_name
    status: ported
    vars: {param_1: p, param_2/r12: idx, param_3/r2: var, param_4/r1: z_name, param_5/r5: n, FUN_0838c158: sqlite3VdbeMemSetStr}
    notes: >-
      Upstream SQLite's sqlite3VdbeSetColName - records the name (var 0) or
      the declared type (var 1) the prepared statement will report for result
      column idx. 45 bl call sites, all unconditional; no predicated or tail-b
      entries.
      Extent 0x0838d004..0x0838d090 confirmed from the listing, not just
      Ghidra - 0x0838d08c is ldmia sp!,{r3,r4,r5,pc} and 0x0838d090 is the
      stmdb entry of sqlite3VdbeSetNumCols, with no literal pool between.
      Ghidra types this void and drops the early return; the raw words are
      movne r0,#7 / bne 0x0838d08c, an SQLITE_NOMEM exit, and every other
      path leaves the callee's r0 alone, so it really does return a result
      code.
      Algorithm - refuse up front if p->db->mallocFailed (+0x1e) is any
      nonzero byte. Otherwise the element for (idx, var) is
      aColName[idx + var * nResColumn], computed with the no-multiplier
      sequence mla r0,r2,r0,r12 / add r0,r0,r0 lsl #2 / add r4,r3,r0 lsl #3 -
      index times five times eight, a 40-byte Mem stride. Upstream's two
      asserts on idx and var are compiled out, so nothing bounds-checks.
      n is both a length and a tag, tested with cmn r5,#1 / cmnne r5,#2 -
      P4_DYNAMIC (-1) and P4_STATIC (-2) install the name as NUL-terminated
      (length -1) and borrowed (xDel NULL, SQLITE_STATIC); any other value is
      a real byte count installed with xDel = (void *)-1, SQLITE_TRANSIENT,
      so the Mem copies. The encoding is always SQLITE_UTF8 (mov r3,#1).
      Then the P4_DYNAMIC twist - that tag means the caller handed over an
      sqlite3_malloc buffer, but the install above marked it MEM_Static, so
      nobody would free it. On rc == SQLITE_OK && n == -1 only (the guard is
      cmp r0,#0 / cmneq r5,#1) the original clears MEM_Static from flags
      (+0x1c, bic #0x80) and copies z (+0x14) into zMalloc (+0x24) - the Mem
      takes ownership with no copy. P4_STATIC deliberately skips this.
      Struct evidence, all with two witnesses. Vdbe gains aColName at +0x28
      and nResColumn at +0xec (both read here, both written by
      sqlite3VdbeSetNumCols @ 0x0838d090). Mem is 40 bytes - SetNumCols
      allocates mov r0,#0x28 / mul r1,r0,r5 per element, and this function
      strides by 40 - with z +0x14, n +0x18, flags +0x1c, type +0x1e,
      enc +0x1f, xDel +0x20, zMalloc +0x24, matching
      sqlite3VdbeMemSetStr @ 0x0838c158 (sqlite/value_set_str.rs); +0x00..
      +0x14 is upstream's union u / double r / sqlite3 *db triple.
      COLNAME_N is 2, not upstream's 5 - SetNumCols sizes the array
      nResColumn << 1 (mov r1,r0 lsl #1), the name/decltype configuration.
      MEM_Static is 0x80 in this build's numbering, from
      sqlite3VdbeMemSetStr's own constants, not upstream's 0x0800.
      Deviations - sqlite3VdbeMemSetStr @ 0x0838c158 is now ported
      (sqlite/vdbe_mem_set_str) and is the shipped default of the
      existing SQLITE_VDBE_MEM_SET_STR dispatcher; that slot remains
      solely for host recording mocks. Vdbe and Mem are
      repr(C) structs with named fields rather than byte offsets, so pointer
      fields stay disjoint on a 64-bit host; the original offsets are
      statically asserted on 32-bit targets in sqlite/vdbe.rs. p->db is read
      unconditionally, exactly like the original.
      Host tests (9) - a failed connection refused before the array is
      touched, every nonzero mallocFailed byte pattern refusing, the
      idx + var*nResColumn element swept over both planes and four columns,
      the -1/-2 borrow vs -3/0/positive/i32 extremes copy boundary against an
      independent reference model, the P4_DYNAMIC retake clearing only
      MEM_Static and pointing zMalloc at z, P4_STATIC and the plain lengths
      never retaking, a failing installer returning its code with no retake,
      a NULL name forwarded untouched, and the shared callee seam still
      holding its documented stub.

  - address: 0x0838d090
    ghidra: FUN_0838d090
    name: vdbe_set_num_cols
    size: 112
    sig: "void vdbe_set_num_cols(Vdbe *p, int n_res_column)"
    module: sqlite/vdbe_set_num_cols
    status: ported
    vars: {param_1/r4: p, param_2/r6: n_res_column, iVar3/r5: n, iVar2/r0: col, FUN_083675c0: releaseMemArray, FUN_083906f4: sqlite3_free, FUN_08374998: sqlite3DbMallocZero}
    notes: >-
      Upstream SQLite's sqlite3VdbeSetNumCols - resizes the prepared
      statement's column-name/decltype array (aColName, +0x28) to
      n_res_column columns. 20 bl call sites, all unconditional,
      binary-scanned from osos.dec; no predicated or tail-b entries.
      Immediately follows sqlite3VdbeSetColName @ 0x0838d004 (ported);
      extent 0x0838d090..0x0838d100 from functions.csv and the listing -
      0x0838d0fc is ldmia sp!,{r4,r5,r6,pc} and 0x0838d100 is the next
      function.
      Algorithm - releaseMemArray @ 0x083675c0 releases the old array's
      Mem guts with the OLD count shifted left by one (mov r1,r0 lsl #1 -
      nResColumn * COLNAME_N, the two name/decltype planes) and
      freebuffer = 1 (mov r2,#0x1, the per-Mem sqlite3VdbeMemRelease
      mode); the array block itself is then freed unconditionally through
      sqlite3_free @ 0x083906f4 (the ported heap::tracked::tracked_free,
      called directly - its NULL guard covers a fresh statement, exactly
      like the original's). The NEW count is stored into nResColumn
      (+0xec) BEFORE the allocation (str r6,[r4,#0xec] ahead of the bl),
      so a failed resize still leaves the statement claiming the new
      column count with a NULL array. The replacement is
      db_malloc_zero(p->db, n * 0x28) where n = nResColumn << 1 and
      0x28 = sizeof(Mem) (mov r0,#0x28 / mul r1,r0,r5), stored into
      aColName even when NULL (ldmiaeq early-out). Each fresh Mem is then
      stamped flags = MEM_Null (1, mov r1,#0x1 / strhgt at +0x1c) and
      db = p->db (ldrgt/strgt at +0x10, re-loaded every pass), advancing
      0x28 - a signed subs/bgt while (n-- > 0) loop, so a zero or
      negative count stamps nothing. Ghidra's decompile has the shape
      right but reads the store order loosely; the listing pins it.
      Deviations - releaseMemArray @ 0x083675c0 has since been ported
      (sqlite/release_mem_array) and is the shipped default of the
      SQLITE_MEM_ARRAY_RELEASE dispatch static; the slot is kept so
      host tests can install recording mocks (the
      missing_mem_array_release no-op stub is retained for them).
      sqlite3_free and sqlite3DbMallocZero ARE ported
      and called directly. Vdbe and Mem are repr(C) structs with named
      fields rather than byte offsets; the original's offsets are
      statically asserted on 32-bit targets in sqlite/vdbe.rs.
      Host tests (6) - old array released (old count * two planes, guts
      mode) then freed then the new block allocated, in that order, via
      one event log across the three seams; the new count stored even
      when the allocation fails (with the sticky mallocFailed byte
      latched by the real allocator); a failed connection still releasing
      and freeing the old array before the short-circuited allocator is
      consulted; every fresh Mem stamped MEM_Null with the connection
      back-pointer across 1/2/3/6 columns; a zero-column resize still
      sending the zero-byte request to the heap and stamping nothing;
      and the release seam shipping the ported releaseMemArray as its
      default.

  # --- sqlite: VDBE b-tree use tracking ---
  - address: 0x0838d100
    ghidra: FUN_0838d100
    name: vdbe_uses_btree
    size: 192
    sig: "void vdbe_uses_btree(Vdbe *p, int database_index)"
    module: sqlite/vdbe_uses_btree
    status: ported
    vars: {param_1: p, param_2: database_index, uVar1/r2: database_bit, iVar3/r1: btree, iVar5/r0: insert_index, piVar4/r2: mutex_array}
    notes: >-
      Upstream SQLite's sqlite3VdbeUsesBtree. Extent 0x0838d100..0x0838d1c0
      verified from raw osos.dec: 0x0838d1bc is ldr pc,[sp],#4 and
      0x0838d1c0 starts the next stmdb function. 10 bl call sites, all
      unconditional, decoded from every ARM B/BL word in osos.dec:
      0x0836fd24, 0x08378d6c, 0x08378ebc, 0x0837913c, 0x0837d72c,
      0x0837f374, 0x0837f938, 0x08381058, 0x083848bc, 0x0838f378; no
      predicated calls or tail branches.
      Algorithm - form 1 << (database_index & 0xff) with ARM's zero result
      for shifts >= 32; return when that bit is already set in Vdbe.btreeMask
      (+0x110), otherwise set it. Load db->aDb[database_index].pBt (Db stride
      0x18); a NULL or non-sharable (+0x09 zero) Btree stops there. For a
      sharable handle, insert the Btree into Vdbe.aMutex after every existing
      entry whose BtShared pointer (+0x04) is <= the candidate's, shifting
      higher entries right, then increment nMutex (+0x114). The list is
      therefore ascending by BtShared pointer and equal keys preserve insertion
      order. No callee is reached.
      Deviations - VdbeBtreeUse is a repr(C) target-layout facade with named
      fields and ARM-only offset assertions, rather than guessing the shared
      Vdbe's unmodeled intermediate fields; 64-bit host fixtures consequently
      retain distinct pointer fields. Host tests (4) cover the already-marked
      no-dereference return, NULL and non-sharable handles, sorted insertion
      with stable equal keys, and ARM's shift-32 zero-bit path.

  # --- Packed-record month length (time/month_length) ---
  - address: 0x08076074
    ghidra: FUN_08076074
    name: datetime_days_in_month
    size: 52
    sig: "u8 datetime_days_in_month(const DateTime *dt)"
    module: time/month_length
    status: ported
    vars: {param_1: dt, cVar2: days, iVar1: leap, DAT_080760a8: DAYS_IN_MONTH,
      FUN_080aabac: is_leap_year (ported, time/leap_year)}
    notes: >-
      Length of the packed calendar record's month in its year, over the
      ten-byte DateTime record of time/datetime.rs (reads +0x04 month,
      1=January, and +0x06 u16 year). The literal pool word @ 0x080760a8
      holds 0x083e2e48, the RUNTIME address of the packed-calendar stack's
      days-in-month table; under the scatterload skew (runtime = image -
      0xaed8, see the region survey) its bytes sit at image 0x083edd20 and
      are decrypted: {0, 31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31},
      index 0 an unused pad and FEBRUARY STORED AS 29 - the function
      subtracts one (subeq + andeq #0xff, a wrapping u8 decrement) when the
      year is common. The same table literal is shared by three more
      calendar helpers (literal pools @ 0x080d6d58 / 0x080ff8c8 /
      0x08158c40). The runtime address overlaps dead C++ vector code in the
      stored image (mid-FUN_083e2dac); the scatterloader copies the RW block
      over it at boot. Callee FUN_080aabac @ 0x080aabac (52 bytes, ported in
      time/leap_year.rs) is the proleptic-Gregorian leap-year test: year % 4
      == 0 and year % 400 not in {100, 200, 300} (the % 400 through the
      ported __rt_udiv @ 0x08036f14, reached via its __rt_udivmod wrapper in
      runtime/rt_div.rs), returning 1 leap / 0 common; it dispatches through
      the IS_LEAP_YEAR seam whose wired default is now the real port. 6 bl
      call sites: @ 0x081409b0 / 0x081409c0 in FUN_081408a8 (month step
      rollover clamp), @ 0x081e90c0 / 0x081e9140 in FUN_081e8f78, @
      0x082712f0 / 0x08271308 in FUN_082710e0. The table index is the raw
      month byte with no bounds check (bare ldrb [r2, r1]), mirrored by a
      pointer read.
      Host tests (7) - every table index 0..=12, non-February months (incl.
      0) never consulting the callee, February leap -> 29, February common
      -> 28, the year handed through zero-extended, the decrement wrapping
      0 -> 0xff under a temporarily-zeroed table entry, and the wired
      default exercising the real port (leap February -> 29, common -> 28).
      match.py structural: same shape -
      table literal load, ldrb table[month], cmp #2 / bne, ldrh year, cmp
      #0 / subeq / and #0xff; differences are the crate fp frame, the
      indirect blx seam dispatch in place of the direct bl, and LLVM
      applying the byte mask unconditionally (a no-op on byte values).


  # --- Packed-record Rata Die day number + weekday (time/day_number) ---
  - address: 0x0807ea68
    ghidra: FUN_0807ea68
    name: datetime_day_number
    size: 148
    sig: "i32 datetime_day_number(DateTime *dt)  // also writes dt->weekday"
    module: time/day_number
    status: ported
    vars: {param_1/r0/r4: dt, r7: year, r6: month, r8: day, r5/r9: days,
      FUN_08036f14: __rt_udiv (ported, runtime/rt_div), FUN_08086e00: leap_month_correction,
      DAT_0807eaf4: 365, DAT_0807eaf8: 367}
    notes: >-
      Proleptic-Gregorian Rata Die day number (0001-01-01 = day 1) of the
      packed DateTime record's year/month/day, with the day number mod 7
      stored to dt->weekday (+0x08; 1 = Monday, 0 = Sunday, so 1970-01-01
      = 719163 lands on weekday 4, Thursday). Raw extent is 148 bytes
      (0x0807ea68..0x0807eafc): Ghidra's 140 stops at the pop @ 0x0807eaf0
      and drops the two literal-pool words (365 @ 0x0807eaf4, 367 @
      0x0807eaf8); the next function's push begins at 0x0807eafc. Formula
      from the raw words: 365*(y-1) + (y-1)/4 [mla + lsr #2] -
      (y-1)/100 + (y-1)/400 + (367*m - 256 - 106)/12 [smulbb, split subs]
      + FUN_08086e00(y, m) + d, with y = year - 1 wrapping as u32. All
      four divides are UNSIGNED __rt_udiv @ 0x08036f14 (ported): the
      /100, /400, /12 take the quotient, and the final /7's r1 remainder
      becomes the weekday store while mov r0, r5 returns the FULL day
      number, not the quotient. 25 bl call sites, 0 predicated bl, 0 b,
      verified by decoding every ARM B/BL word in osos.dec. Callee
      FUN_08086e00 @ 0x08086e00 (28 bytes, unported) is the February
      correction: cmp r1,#2 / movls r0,#0 for January/February, else
      is_leap_year @ 0x080aabac (ported) and mvneq r0,#1 (common, -2) /
      mvnne r0,#0 (leap, -1). It dispatches through the
      LEAP_MONTH_CORRECTION seam: target default transmutes the verified
      load address; the host default reproduces the fully decoded body on
      the ported is_leap_year (month_length precedent). The port is wired
      as the day_number default of time/datetime.rs's DATETIME_OPS now
      that it exists. Fields are raw bytes/halfwords with no validation:
      year=0 wraps y to 0xffffffff through the unsigned divides (host
      tests pin 0x3e147975 for 0000-01-01 and 0x5369ceab for
      0/0/0), month=0 feeds 0xfffffe96 to the /12. Host tests (5):
      full-year sweeps of 14 ordinary/century/leap/epoch years against an
      independent Hinnant days_from_civil oracle (day number and weekday,
      plus one correction-seam call per record), Monday/Thursday/Saturday
      anchors, the day-number-not-quotient return, (year, month) register
      order at the seam, and the out-of-range wrapping cases. match.py
      structural: same shape - field loads, four __rt_udiv/udivmod bl
      boundaries, the blx seam dispatch, strb weekday, r5 returned;
      differences are the crate fp frame, LLVM fusing 367*m-362 into one
      smlabb accumulate operand, and reordered accumulation.

  # --- Current calendar query -> normalized packed record (time/current_datetime) ---
  - address: 0x0808e9e0
    ghidra: FUN_0808e9e0
    name: current_datetime_to_normalized_record
    size: 108
    sig: "u32 current_datetime_to_normalized_record(DateTime *out)"
    module: time/current_datetime
    status: ported
    renames:
      param_1/r0/r4: out
      auStack_1c: calendar (20-byte current-calendar query record)
      FUN_08056524: current_datetime_query (unported, shared fp/fp_misc seam)
      FUN_0807ea68: datetime_day_number (ported, time/day_number)
      FUN_0807eafc: day_number_to_datetime (unported, shared time/unix_to_datetime seam)
    notes: >-
      Raw extent is exactly 108 bytes, 0x0808e9e0..0x0808ea4c: the next
      separately linked function opens at 0x0808ea4c with push {r3,lr}.
      Decoding every ARM B/BL word in osos.dec (load base 0x08000000) finds
      20 unconditional bl callers and no predicated bl or b tail caller,
      matching the requested count. The raw body clears its 20-byte stack
      calendar record through the ported IRAM memzero veneer 0x08037db8,
      calls current_datetime_query @ 0x08056524, and deliberately ignores
      that query's 0/1 return. It transfers calendar bytes +2..+6 into
      out.second/minute/hour/day/month and the aligned little-endian
      halfword +8 into out.year. datetime_day_number then returns the Rata
      Die day number while storing out.weekday; day_number_to_datetime
      receives that day number and out, normalizing the date fields. It
      returns 1 unconditionally. The port calls the two pre-existing
      volatile seams rather than adding duplicate dispatch state; on the
      target each reaches its verified firmware address, and host mocks
      verify one query and one conversion, the ignored false query status,
      exact calendar extraction, preserved unassigned padding, weekday 4
      for 1970-01-01, Rata Die 719163, and the converter's final write.


  # --- Macintosh epoch timestamp to local packed calendar record ---
  - address: 0x080aa95c
    ghidra: FUN_080aa95c
    name: mac_epoch_seconds_to_datetime
    size: 80
    sig: "void mac_epoch_seconds_to_datetime(u32 mac_epoch_seconds, DateTime *out, i32 apply_utc_offset)"
    module: time/mac_epoch_to_datetime
    status: ported
    renames:
      param_1/r0/r4: mac_epoch_seconds
      param_2/r1/r5: out
      param_3/r2: apply_utc_offset
      FUN_080ab558: current_utc_offset_query
      FUN_080964cc: unix_seconds_to_datetime
      DAT_080aa9a8: mac_to_unix_epoch_seconds
    notes: >-
      Raw ARM has a 76-byte executable body from 0x080aa95c through the
      tail branch at 0x080aa9a4, then its 0x83da4f80 literal at 0x080aa9a8;
      the next separately linked function begins at 0x080aa9ac, making the
      complete extent 80 bytes. Decoding every ARM B/BL word in osos.dec
      finds 12 unconditional plain BL callers, no predicated BL callers, and
      no B tail callers. When r2 is nonzero, it queries the ported
      current_utc_offset_query with stack i16/u8 outputs. Only a nonzero
      result adds (signed i16 base offset plus signed i8 daylight adjustment)
      * 60 to the input; all additions wrap as ARM u32 arithmetic. It then
      adds the negative Macintosh-to-Unix epoch delta 0x83da4f80 and
      tail-calls the ported unix_seconds_to_datetime. The target calls both
      existing Rust ports directly. A private host-test seam only replaces
      current_utc_offset_query to exercise successful, failed, and skipped
      queries; it creates no target dispatch seam.

  # --- Proleptic-Gregorian leap-year test (time/leap_year) ---
  - address: 0x080aabac
    ghidra: FUN_080aabac
    name: is_leap_year
    size: 52
    sig: "i32 is_leap_year(u32 year)"
    module: time/leap_year
    status: ported
    vars: {param_1: year, extraout_r1: residue, r4: year}
    notes: >-
      Proleptic-Gregorian leap-year predicate. Calls the ported
      __rt_udiv @ 0x08036f14 with (year, 400) unconditionally; its r1
      remainder result is represented by __rt_udivmod's residue out-pointer
      in Rust. Returns 1 iff year & 3 is zero and residue is not 100, 200,
      or 300; otherwise returns 0. Thus 400-year centuries (including year
      0, 1600, 2000) are leap while the other century residues are common.
      The ARM input is a u32 register, so sign-bit-set values are treated as
      large unsigned years; the packed DateTime caller zero-extends its u16
      year field before the IS_LEAP_YEAR seam. Nine direct bl call sites:
      0x08076090 in datetime_days_in_month, 0x0807ebb4 in FUN_0807eafc,
      0x08086e10 in FUN_08086e00, 0x080d6d40 in FUN_080d6d1c, three in
      FUN_080ff4e4 (0x080ff55c / 0x080ff5e8 / 0x080ff788), 0x08158bec in
      FUN_08158ba0, and 0x08271338 in FUN_082710e0. Host tests cover the
      div-by-4 gate, all three excluded century residues, 400-year leaps,
      zero and sign-bit-set inputs, and exhaustive DateTime-range comparison
      with the independent textbook Gregorian predicate.

  # --- Raw object-state byte predicate (cxx/object_state) ---
  - address: 0x080395d8
    ghidra: FUN_080395d8
    name: object_byte2_is_nonzero
    size: 16
    sig: "u32 object_byte2_is_nonzero(const u8 *object)"
    module: cxx/object_state
    status: ported
    vars: {param_1/r0: object, object_plus_0x02: byte2, r0: boolean_result}
    notes: >-
      Raw ARM is `ldrb r0,[r0,#2]; cmp r0,#0; movne r0,#1; bx lr`: it
      returns the exact u32 ABI values 0 for byte2 == 0 and 1 for every
      nonzero byte, with no null guard and no writes. The sole recovered
      direct call is 0x080a55ec in FUN_080a5420: r0 receives the 16-byte
      input-record cursor r5 and the result is stored as one byte at
      output-record +0x14; the caller loads r1 from its own stack just
      before the call, but this leaf does not read it. The object type and
      byte's meaning remain unlocated, so the name records only the verified
      raw layout behavior. Host tests cover zero, several nonzero byte
      values, the exact three-byte minimum object, a nonzero base offset,
      and preservation of all surrounding bytes.


  # --- Raw object byte accessor (cxx/object_state) ---
  - address: 0x0829d0b4
    ghidra: FUN_0829d0b4
    name: object_byte_at_8
    size: 8
    sig: "u8 object_byte_at_8(const u8 *object)"
    module: cxx/object_state
    status: ported
    renames:
      param_1/r0: object
      object_plus_0x08: byte_at_8
      r0: byte_result
    notes: >-
      Raw ARM is exactly `ldrb r0,[r0,#8]; bx lr`, an unguarded,
      non-mutating byte load. The true extent is 8 bytes at
      0x0829d0b4..0x0829d0bc, where the sibling accessor
      FUN_0829d0bc begins. Decoding every ARM B/BL word in osos.dec found
      exactly 11 inbound branches, all unconditional `bl` (condition 0xe)
      at 0x081ef5fc, 0x08202774, 0x08277a0c, 0x08277ac8, 0x08277c10,
      0x0827826c, 0x0827850c, 0x082787e0, 0x08278d40, 0x082a53b8, and
      0x082a5434; there are no predicated calls, tail branches, or DATA-word
      references. The callers use the result as a state/error gate after
      counted-mutex acquisition, but do not establish the object's concrete
      type or the byte's domain meaning, so this deliberately remains an
      offset-based accessor. No deliberate deviation. Host tests exhaust all
      256 return values, prove the load does not mutate the object, and vary
      every unrelated byte in a minimally sized nine-byte object.

  # --- Raw object byte accessor (cxx/object_state) ---
  - address: 0x0829d0bc
    ghidra: FUN_0829d0bc
    name: object_byte_at_4
    size: 8
    sig: "u8 object_byte_at_4(const u8 *object)"
    module: cxx/object_state
    status: ported
    renames:
      param_1/r0: object
      object_plus_0x04: byte_at_4
      r0: byte_result
    notes: >-
      Raw ARM is exactly `ldrb r0,[r0,#4]; bx lr`, an unguarded,
      non-mutating byte load. The true extent is 8 bytes at
      0x0829d0bc..0x0829d0c4, where the next separately linked function
      FUN_0829d0c4 begins. Decoding every ARM B/BL word in osos.dec found
      exactly 10 inbound branches, all unconditional `bl` (condition 0xe)
      at 0x0814bd50, 0x08167ca0, 0x0818b220, 0x081a7be0, 0x081ba274,
      0x081efd8c, 0x082140f0, 0x082235d8, 0x08235e8c, and 0x0828b324;
      there are no predicated calls, tail branches, or DATA-word references.
      Callers use the result to select object-dispatch paths but do not
      establish the concrete object type or field domain, so this deliberately
      remains an offset-based accessor. No deliberate deviation. Host tests
      exhaust all 256 return values and verify neighboring bytes and object
      storage remain unchanged.

  # --- Raw object dispatch-code accessor (cxx/object_state) ---
  - address: 0x0829f184
    ghidra: FUN_0829f184
    name: object_dispatch_code_at_8
    size: 12
    sig: "u32 object_dispatch_code_at_8(const u8 *object)"
    module: cxx/object_state
    status: ported
    renames:
      param_1/r0: object
      object_plus_0x08: dispatch_word
      r0: dispatch_code
    notes: >-
      Raw ARM is exactly `ldr r0,[r0,#8]; and r0,r0,#0xff; bx lr`: an
      unguarded, non-mutating aligned word load followed by low-byte masking.
      The true extent is 12 bytes at 0x0829f184..0x0829f190, where the
      byte-identical sibling FUN_0829f190 begins. Decoding every ARM B/BL
      word in osos.dec found exactly 10 inbound branches, all unconditional
      `bl` (condition 0xe) at 0x0814bddc, 0x08167cd0, 0x0818aa00,
      0x081a7c18, 0x081ba2a4, 0x081efdc8, 0x0821411c, 0x08223604,
      0x08235ebc, and 0x0828b354; there are no predicated calls or direct
      tail branches. Callers compare the result to dispatch/action codes
      6 through 10 but do not establish a concrete object type, so the name
      preserves only that verified role and offset. No deliberate deviations.
      Host tests cover zero, all-low-byte-set, observed dispatch codes with
      unrelated high bits, the aligned +8 word position, and no mutation.
  # --- Resource callback cache refresh/getter (app/resource/cache) ---
  - address: 0x08047020
    ghidra: FUN_08047020
    name: resource_cache_refresh_get
    size: 60
    sig: "u32 resource_cache_refresh_get(void *object, u32 force_refresh)"
    module: app/resource/cache
    status: ported
    vars: {param_1/r0: object, param_2/r1: force_refresh, object_plus_0x40: resource_callback_root, object_plus_0x618: cache_dirty, object_plus_0x61c: cache_auxiliary, object_plus_0x620/r0: cached_value, DAT_0804705c: resource_cache_refresh_callback}
    notes: >-
      Raw ARM is `ldrb object+0x618; orrs r0,r0,force_refresh; beq return`;
      either nonzero input clears the byte and both following words before
      dispatching FUN_0806b604(resource_callback_root, 0x080db63c, object).
      Its dispatcher status is ignored and the post-dispatch +0x620 word is
      returned. The literal 0x080db63c enters a mid-function allocation
      handler tail, not a normal callable function: the dispatcher holds the
      root in r4 and leaves object in r2, which that tail stores into the
      handler before calling FUN_080e1cc8. It is consequently modeled by the
      RESOURCE_CACHE_CALLBACK_DISPATCH seam rather than guessed as a Rust
      callback. Recovered forced callers are 0x080687a0 and 0x0806893c in
      FUN_08068504; the virtual wrapper FUN_082a6040 exercises the same
      unforced dirty-cache path. Host tests cover clean/no-dispatch, dirty
      refresh with all cleared fields and exact dispatcher arguments, forced
      refresh with a high-bit force value, callback-written returned values,
      and ignored dispatcher status.

  # --- Resource callback dispatcher (app/resource/cache) ---
  - address: 0x0806b604
    ghidra: FUN_0806b604
    name: resource_callback_dispatch
    size: 136
    sig: "i32 resource_callback_dispatch(void *resource_callback_root, ResourceCallback callback, void *context)"
    module: app/resource/cache
    status: ported
    vars: {param_1/r0: resource_callback_root, param_2/r1: callback, param_3/r2: context, iVar1/r0: callback_status, iVar2/r0: child_status, param_1_plus_0x08: next_sibling, param_1_plus_0x1d: node_flags, param_1_plus_0x20: first_child}
    notes: >-
      Rejects a null root or callback with -0x32. Otherwise calls the root
      first with (node, context); a nonzero status returns immediately. Only
      a root whose +0x1d bit 0 is set exposes the +0x20 child chain. Each
      chain node comes from +0x08: unmarked nodes call the callback directly,
      while marked nodes recurse before their parent chain advances, producing
      a depth-first traversal. Every child or recursive nonzero status returns
      unchanged and prevents later siblings. Firmware's recovered call state
      also preserves the active node in r4 and context in r2 for the
      0x080db63c allocation-handler-tail callback used by
      resource_cache_refresh_get; ordinary callbacks use r0/r1. Host tests
      cover null rejection, marked depth-first traversal, root/direct/nested
      failure propagation, skipped siblings, and context forwarding.

  # --- Current UTC offset applied to seconds timestamp (time/utc_adjust) ---
  - address: 0x0806ba5c
    ghidra: FUN_0806ba5c
    name: apply_current_utc_offset_seconds
    size: 32
    sig: "u32 apply_current_utc_offset_seconds(u32 seconds)"
    module: time/utc_adjust
    status: ported
    renames:
      param_1/r0/r4: seconds
      FUN_08054fcc: current_utc_offset_minutes_negated (unported)
      FUN_080ab558: current_utc_offset_query (ported, time/utc_offset)
    notes: >-
      Raw extent is exactly 32 bytes, 0x0806ba5c..0x0806ba7c: the next
      separately linked function begins at 0x0806ba7c with cmp r1,#0.
      Decoding every ARM B/BL word in osos.dec (load base 0x08000000) finds
      exactly 9 direct unconditional plain BL callers at 0x0806c61c,
      0x0806c628, 0x0806c634, 0x0806c6a0, 0x0806c6b0, 0x0806c6c0,
      0x080b44ec, 0x080bdc0c, and 0x080e8510; no predicated BL callers.
      The zero input branch returns without calling the 48-byte unported
      helper FUN_08054fcc. Otherwise that helper calls the ported
      current_utc_offset_query, returns zero when its validity result is
      zero, and returns the negated signed (i16 base + i8 daylight) minute
      sum otherwise. The main sub r0,r0,r0,lsl#4 / add r4,r4,r0,lsl#2
      arithmetic therefore adds that signed sum times 60 seconds, modulo
      2^32. The port directly calls current_utc_offset_query instead of
      adding a target seam for the fully decoded unported adapter; a private
      host-only volatile seam provides valid, invalid, signed-daylight, and
      zero-input test coverage without changing device dispatch.

  # --- Selected paired-buffer capacity predicate (runtime/buffer_state) ---
  - address: 0x0807595c
    ghidra: FUN_0807595c
    name: buffer_state_is_full
    size: 36
    sig: "u32 buffer_state_is_full(u32 selector)"
    module: runtime/buffer_state
    status: ported
    vars: {param_1/r0: selector, DAT_08075980: paired_buffer_states, iVar1/r1: selected_buffer_state,
      selected_buffer_state_plus_0x40/r0: item_count, selected_buffer_state_plus_0x44/r1: capacity,
      r0: is_full}
    notes: >-
      Raw ARM loads the fixed paired-state BSS object 0x08b1c858 through the
      literal at 0x08075980, applies `addeq #0x48` only when selector == 1,
      then compares the selected record's +0x40 and +0x44 words and returns
      exact u32 0/1 with `movne`/`moveq`. The shared-state users
      FUN_08090c48, FUN_08092230, FUN_0809ae44, FUN_080b0fbc, and
      FUN_080b8228 establish two 0x48-byte records whose +0x40 word is an
      item count and +0x44 is capacity (initialized to 16); equality is
      therefore the conservative full predicate, not an empty-count test.
      The only direct ARM `bl` is 0x081c0eb8 in FUN_081c0b18 and passes
      selector 0. Host tests cover exact selector one, representative
      non-one u32 values, and equal/unequal words including zero and
      u32::MAX.

  # --- Opaque global-state table getter (util/global_state) ---
  - address: 0x080781b0
    ghidra: FUN_080781b0
    name: global_state_get
    size: 16
    sig: "void *global_state_get(const u8 *global_name, const void *global_state_table)"
    module: util/global_state
    status: ported
    renames:
      param_1/r0: global_name
      param_2/r1: global_state_table
      FUN_08077ff0: global_state_slot_find
      puVar1/r0: global_state_record
    notes: >-
      Raw ARM is exactly `push {r4,lr}; bl 0x08077ff0; ldr r0,[r0];
      pop {r4,pc}`. The 140-byte helper at 0x08077ff0 hashes the NUL-ended
      name, searches an opaque table supplied in r1, and returns its matching
      slot; this wrapper forwards both words unchanged and loads only that
      slot's first word, with no null check or validation. Six direct calls
      were recovered at 0x0807b2fc, 0x0809fc9c, 0x0809fd78, 0x0809fda8,
      0x080ae028, and 0x080bfbb4. The 0x0807b294 and 0x0809fc74 callers use
      their returned record as global state; table and record ownership remain
      opaque. The unported helper is a target-ROM/host-mock seam; host tests
      prove exact two-word forwarding and first-word result loading.

  # --- Stream transfer-record cursor copy (drivers/stream_transfer) ---
  - address: 0x08088ed0
    ghidra: FUN_08088ed0
    name: stream_transfer_record_to_cursor
    size: 56
    sig: "u32 stream_transfer_record_to_cursor(const StreamTransferRecord *record, u8 **cursor)"
    module: drivers/stream_transfer
    status: ported
    vars: {param_1/r4: record, param_2/r5: cursor, record_plus_0x00/r1: byte_count,
      record_plus_0x08/r1: source, cursor_deref/r0: destination,
      FUN_08037db0: __rt_memcpy}
    notes: >-
      Raw ARM is `movs r5,r1; mov r4,r0; beq return; ldr r1,[r4,#8];
      ldr r2,[r4]; ldr r0,[r5]; bl 0x08037db0; ldr r0,[r5]; ldr
      r1,[r4]; add r0,r0,r1; str r0,[r5]; ldr r0,[r4]; return`.
      Thus record+0 is a byte count, record+8 the copy source, and the
      nullable second argument is a destination-cursor slot: the copy ABI
      is exactly (cursor_deref, source, byte_count), and cursor advancement
      occurs only after the call, with 32-bit wrapping addition. Veneer
      0x08037db0 dispatches to IRAM 0x22000020, the established
      __rt_memcpy mirror at osos 0x08000020; the port uses the existing
      runtime-copy seam and an exact-u32 ABI recorder in host tests. A raw
      decode of every ARM B/BL instruction in osos.dec found no direct branch
      to this untracked leaf. The same decode finds 788 branches to veneer
      0x08037db0 (761 bl, 27 tail b), corroborating its recovered runtime
      memcpy role. Host tests cover the null-cursor no-call return, exact
      destination/source/count forwarding and old-cursor observation during
      the call, post-call cursor advancement, and the default runtime copy's
      exact written span.

  # --- Singleton state base accessor (app/singleton_state) ---
  - address: 0x08127954
    ghidra: FUN_08127954
    name: singleton_state_base_get
    size: 12
    sig: "void *singleton_state_base_get(void)"
    module: app/singleton_state
    status: ported
    vars:
      DAT_08127960: singleton_state_globals
      DAT_089cfda0: singleton_state_base_slot
      iVar1/r0: singleton_state_base
    notes: >-
      Raw ARM is `ldr r0,[0x08127960]; ldr r0,[r0,#0x4]; bx lr`; the
      pool word at 0x08127960 is 0x089cfda0, and the accessor returns the
      untyped pointer held at that globals block's +0x04 slot. It does not
      construct, cache, or validate the object. The ARM port reads that
      exact 32-bit firmware slot; host tests model the +0x04 slot directly
      with a native-width pointer, so two successive stores prove no result
      is cached.

  # --- Opaque singleton state-word accessor (app/singleton_state) ---
  - address: 0x08086df0
    ghidra: FUN_08086df0
    name: singleton_state_get
    size: 16
    sig: "u32 singleton_state_get(void)"
    module: app/singleton_state
    status: ported
    vars:
      FUN_08127954: singleton_state_base_get
      iVar1/r0: singleton_object
      singleton_object_plus_0x18/r0: singleton_state_word
    notes: >-
      Raw ARM is `push {r4,lr}; bl 0x08127954; ldr r0,[r0,#0x18];
      pop {r4,pc}`: it calls the 12-byte singleton-state base accessor
      before loading the object's untyped +0x18 word, with no null check.
      The accessor reads the +0x04 word of the globals block at 0x089cfda0,
      so it is neither a constructor nor a lazy path. The two direct callers
      (0x08140464 and 0x081404b4) use the returned word as an object pointer,
      invoking its vtable +0x84 after staging their three-word operation
      records. The state object's concrete class is otherwise unknown, so
      the port uses the conservative singleton_state_get name and preserves
      a raw u32 result. Host tests prove the base accessor reads its current
      modeled global slot and that the wrapper returns the complete raw
      +0x18 word.

# --- timed transition wheel node ---
  - address: 0x081607b0
    ghidra: FUN_081607b0
    name: timed_transition_init
    size: 120
    sig: "void timed_transition_init(TimedTransition *transition, u32 target_value, u32 duration_ms, u32 start_value, u32 aux_value, u32 owner_or_self)"
    module: app/timed_transition
    status: ported
    vars:
      param_1: transition
      param_2: target_value
      param_3: duration_ms
      param_4: start_value
      param_5: aux_value
      param_6: owner_or_self
      param_1_plus_0x08: wheel_rank
      param_1_plus_0x0c: wheel_prev
      param_1_plus_0x10: wheel_next
      param_1_plus_0x18: start_value
      param_1_plus_0x1c: target_value
      param_1_plus_0x20: duration_ms
      param_1_plus_0x24: aux_value
      param_1_plus_0x28: owner_or_self
      param_1_plus_0x2c: armed
      param_1_plus_0x30: inverse_duration_q16
    notes: >-
      Wheel node initializer used by the 0x0816082c allocator wrapper and by
      the geometry/scroll callers recovered around 0x081529d8 and 0x081ea3b8.
      The raw ARM is a remove/initialize/insert sequence: unlink from the
      timing-wheel bucket table at 0x089cc7e0, clear +0x2c, force rank 1,
      store the four raw inputs, compute a Q16.16 reciprocal from
      `duration_ms * 65`, and store either the explicit owner or `this` at
      +0x28. 21 `bl` call sites were verified by scanning every B/BL word in
      osos.dec; all are unconditional `bl`.

  # --- refcounted timed-transition constructor ---
  - address: 0x0816082c
    ghidra: FUN_0816082c
    name: timed_transition_construct
    size: 64
    sig: "TimedTransition *timed_transition_construct(TimedTransition *this, u32 target_value, u32 duration_ms, u32 start_value, u32 aux_value, u32 owner_or_self)"
    module: app/timed_transition
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: target_value
      param_3/r2: duration_ms
      param_4/r3: start_value
      stack_arg_1: aux_value
      stack_arg_2: owner_or_self
      DAT_0816086c: TIMED_TRANSITION_VTABLE
      FUN_08138460: refcounted_base_init
      FUN_081607b0: timed_transition_init
    notes: >-
      Caller-allocated 0x34-byte refcounted timed-transition constructor.
      Raw extent is 64 bytes from 0x0816082c through the separately loaded
      vtable literal at 0x0816086c; the following cmp at 0x08160870 begins
      a distinct sibling function. It preserves r1-r3 and both stack
      arguments, calls refcounted_base_init(this), stores vtable 0x08987d60
      at +0x00, then calls timed_transition_init(this, target_value,
      duration_ms, start_value, aux_value, owner_or_self) and returns this.
      Ghidra omitted the receiver passed in r0. All 19 branch targets were
      decoded by scanning every B/BL word in osos.dec: every caller uses an
      unconditional bl, with no predicated or tail-branch call sites.
  # --- opaque object flag low-nibble accessor ---
  - address: 0x08160cfc
    ghidra: FUN_08160cfc
    name: object_flag_low_nibble
    size: 12
    sig: "u8 object_flag_low_nibble(const u8 *object)"
    module: app/object_flag_nibble
    status: ported
    renames:
      param_1/r0: object
      param_1_plus_0x0d: flag_byte
      r0: flag_low_nibble
    notes: >-
      12-byte leaf: `ldrb r0,[r0,#0xd]; and r0,r0,#0xf; bx lr`. It reads
      only byte +0x0d of an otherwise opaque application object and returns
      its low four bits. The direct recovered caller 0x08160888 uses this
      value as an index into its table at +0x18; that does not establish a
      broader object layout or a meaning for the high nibble. No deviations;
      host tests exhaust all 256 high/low-nibble combinations and prove the
      byte load leaves the complete surrounding object untouched.

  # --- three-category table-domain predicate ---
  - address: 0x08161258
    ghidra: FUN_08161258
    name: category_is_below_three
    size: 24
    sig: "u32 category_is_below_three(u32 context, u32 category)"
    module: app/category_is_below_three
    status: ported
    renames:
      param_1/r0: context_ignored
      param_2/r1: category
      r0: is_below_three
    notes: >-
      24-byte leaf predicate. The original compares r1 independently with 0,
      1, and 2, then returns one only for those three values; all other u32
      values return zero. It never reads r0. Direct callers at 0x08161068 and
      0x08161208 pass an owning context in r0 and the category in r1; each
      subsequently indexes that context's +0x04 table with the category.
      This establishes table-domain category values but not a more specific
      category meaning, so the port retains the conservative category name.
      Reference C: decomp/c/014/08161258_FUN_08161258.c; assembly: osos.asm
      0x08161258 (`cmp r1,#0; cmpne #1; cmpne #2; moveq/movne; bx lr`).
      Focused host tests cover categories 0, 1, 2, 3, and u32::MAX and prove
      that arbitrary first-argument values do not affect the result.

  # --- opaque object +0x04 word accessor ---
  - address: 0x080fe4c4
    ghidra: FUN_080fe4c4
    name: object_word_at_4
    size: 8
    sig: "u32 object_word_at_4(const u32 *object)"
    module: runtime/object_word
    status: ported
    renames:
      param_1/r0: object
      param_1_plus_0x04: word_at_0x04
      r0: word_at_0x04
    notes: >-
      Eight-byte leaf: `ldr r0,[r0,#4]; bx lr`. It returns the complete
      untyped word at byte offset +0x04 from an opaque, word-aligned object,
      with no null check or mutation. All seven direct callers first obtain
      the object pointer from FUN_083d602c and preserve it in r0 for this
      load: 0x0812f9c0, 0x081425cc, 0x081b5860, 0x081b5d2c, 0x081f8a70,
      0x081f8b50, and 0x081f8f9c. Their consumers use the result as an
      opaque pointer/value, so neither the object type nor this field's
      semantic meaning is recoverable. No deviations. Host tests cover
      arbitrary words, zero/all-ones extremes, pointer aliasing, and the
      absence of mutation.

  # --- opaque object +0x08 word accessor ---
  - address: 0x0811e82c
    ghidra: FUN_0811e82c
    name: object_word_at_8
    size: 8
    sig: "u32 object_word_at_8(const u32 *object)"
    module: runtime/object_word
    status: ported
    renames:
      param_1/r0: object
      param_1_plus_0x08: word_at_0x08
      r0: word_at_0x08
    notes: >-
      Eight-byte leaf: `ldr r0,[r0,#8]; bx lr`. It returns the complete
      untyped word at byte offset +0x08 from an opaque, word-aligned object,
      with no null check or mutation. The two direct callers are
      FUN_0816f75c @ 0x0816f75c and FUN_08171ae4 @ 0x08171ae4. The former
      narrows the value to signed 16 bits before passing it onward; the latter
      compares it to a signed 16-bit value. Neither use establishes a stronger
      object or field type, so the port retains the offset-based name. No
      deviations. Host tests cover arbitrary words, zero/all-ones boundaries,
      pointer aliasing, and the absence of mutation.

  # --- opaque object +0x0d equality-to-one predicate ---
  - address: 0x0813af44
    ghidra: FUN_0813af44
    name: byte_flag_at_d_is_one
    size: 20
    sig: "u32 byte_flag_at_d_is_one(const void *object)"
    module: app/byte_flag_at_d_is_one
    status: ported
    renames:
      param_1/r0: object
      param_1_plus_0x0d: flag_byte
      r0: is_one
    notes: >-
      20-byte leaf predicate: `ldrb r0,[r0,#0xd]; cmp r0,#1; movne/moveq;
      bx lr`. It reads only byte +0x0d of an otherwise opaque application
      object and returns 1 only when that byte is exactly 1; values 0 and 2
      through 255 return 0. The two direct callers, FUN_080f87ac @ 0x080f87ac
      and FUN_080fca98 @ 0x080fca98, poll it on subobjects at their respective
      +0x08 and +0x08 word offsets; that does not establish a concrete object
      type or a semantic name for the byte. No deviations. Focused host tests
      cover 0, 1, 2, and 0xff; neighboring-byte guards; aliased access; and
      no mutation. Reference C: decomp/c/012/0813af44_FUN_0813af44.c;
      assembly: osos.asm 0x0813af44.

  # --- opaque object +0x05 byte accessor ---
  - address: 0x0813b0a4
    ghidra: FUN_0813b0a4
    name: object_byte_at_5
    size: 8
    sig: "u8 object_byte_at_5(const u8 *object)"
    module: app/object_byte_at_5
    status: ported
    renames:
      param_1/r0: object
      param_1_plus_0x05: byte_at_0x05
      r0: byte_at_0x05
    notes: >-
      Eight-byte leaf: `ldrb r0,[r0,#5]; bx lr`. It returns the raw unsigned
      byte at +0x05 of an otherwise opaque application object, with no null
      check or mutation. The recovered direct callers FUN_081c08b8 @
      0x081c08b8 and FUN_081ee314 @ 0x081ee314 respectively distinguish zero
      from nonzero and compare this value to one; neither use establishes a
      concrete object type or a domain meaning for the field. No deviations.
      Host tests cover the full 0..=255 byte range, neighboring-byte guards,
      pointer aliasing, and absence of mutation. Reference C:
      decomp/c/012/0813b0a4_FUN_0813b0a4.c; assembly: osos.asm 0x0813b0a4.

  # --- Class8900 queued-work constructor ---
  - address: 0x0813b05c
    ghidra: FUN_0813b05c
    name: class_8900_queued_work_construct
    size: 60
    sig: "Class8900QueuedWork *class_8900_queued_work_construct(Class8900QueuedWork *this, void *request_context)"
    module: app/class_8900_queued_work_construct
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: request_context
      param_1_plus_0x04: primary_name
      param_1_plus_0x0c: state
      param_1_plus_0x0d: flags
      param_1_plus_0x0e: option
      param_1_plus_0x10: secondary_name
      param_1_plus_0x18: next
      param_1_plus_0x1c: trailing
      FUN_08277440: string_default_construct
    notes: >-
      Raw ARM is exactly 60 code bytes from 0x0813b05c through the
      `pop {r4,pc}` at 0x0813b094; the separate sibling starts `ldr r2,[r0]`
      at 0x0813b098. Decoding every ARM B/BL immediate in osos.dec finds
      nine direct calls, all plain unconditional bl at 0x081ee574, 0x081ee624,
      0x081ee97c, 0x081eeb6c, 0x081eec24, 0x081eecc0, 0x081eed68,
      0x081eee44, and 0x081eef24; there are no predicated BL forms, direct
      tail branches, or aligned data words containing this address. It stores
      the opaque request-context pointer, default-constructs embedded
      StringObjects at +0x04 and +0x10, writes state = 0xff and flags/option
      = 0, then clears the +0x18 FIFO link and +0x1c trailing word. Byte
      +0x0f is deliberately untouched. All nine callers allocate a 0x20-byte
      record and populate command-specific fields before handing it to
      class_8900_work_queue_enqueue. No deviations: both nested calls use the
      already ported string_default_construct directly, so no dispatch seam
      was added. Host tests prove constructor return identity, non-NULL and
      NULL context propagation, string reset, the 0xff sentinel, zeroed
      owned fields, repeat construction, and preserved padding.

  # --- unsigned stride-20 index arithmetic ---
  - address: 0x083e0298
    ghidra: FUN_083e0298
    name: array_index_stride20
    size: 16
    sig: "u32 array_index_stride20(const u32 *base_word, u32 index)"
    module: cxx/array_index_stride20
    status: ported
    renames:
      param_1/r0: base_word
      param_2/r1: index
      uVar1/r0: result
    notes: >-
      Loads the unsigned 32-bit base word and returns base_word + index * 20,
      with multiplication and addition wrapping modulo 2^32. It reads only
      the initial word, so neither a containing array type nor a stronger
      semantic meaning for its stride can be recovered. No deviations.
      Focused host tests cover distinct base words, zero index, and both
      index-product and result-addition wraparound. Reference C:
      decomp/c/038/083e0298_FUN_083e0298.c; assembly: osos.asm 0x083e0298.

  # --- signed stride-24 index arithmetic ---
  - address: 0x083e20b0
    ghidra: FUN_083e20b0
    name: array_index_stride24
    size: 16
    sig: "i32 array_index_stride24(const i32 *base_word, i32 index)"
    module: cxx/array_index_stride24
    status: ported
    renames:
      param_1/r0: base_word
      param_2/r1: index
      iVar1/r0: result
    notes: >-
      Loads the signed 32-bit base word and returns base_word + index * 24,
      with multiplication and addition wrapping modulo 2^32. It reads only
      the initial word, so neither a containing array type nor a stronger
      semantic meaning for its stride can be recovered. No deviations.
      Focused host tests cover zero index, positive and negative inputs, and
      both signed-boundary wrap directions. Reference C:
      decomp/c/038/083e20b0_FUN_083e20b0.c; assembly: osos.asm 0x083e20b0.

  # --- three consecutive word clear ---
  - address: 0x083e0aa4
    ghidra: FUN_083e0aa4
    name: three_word_clear
    size: 20
    sig: "void three_word_clear(u32 *target)"
    module: cxx/three_word_clear
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Unconditionally stores zero to exactly the three consecutive u32 words
      at target +0x00, +0x04, and +0x08. It has no NULL guard, no target
      reads, and no other stores. No deviations. Focused host test uses
      nonzero guard and target fixtures to prove only those three words
      change. Reference C: decomp/c/038/083e0aa4_FUN_083e0aa4.c; assembly:
      osos.asm 0x083e0aa4. match.py: structural match reviewed.

  # --- byte-identical alternate three-word clear ---
  - address: 0x083e0e68
    ghidra: FUN_083e0e68
    name: three_word_clear_alt
    size: 20
    sig: "u32 *three_word_clear_alt(u32 *target)"
    module: cxx/three_word_clear_alt
    status: ported
    renames:
      param_1/r0: target
      return/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical alternate of 0x083e0aa4: unconditionally stores zero to
      exactly the three consecutive u32 words at target +0x00, +0x04, and
      +0x08. It has no NULL guard, no target reads, and no other stores.
      Though the standalone C decompile declares void, its two direct
      decompiled callers, 0x0811f050 and 0x0811f4f4, consume the preserved r0
      destination after the call; the Rust signature returns target to preserve
      that observed ABI behavior. Focused host test verifies the returned
      destination and nonzero guard words. Reference C:
      decomp/c/038/083e0e68_FUN_083e0e68.c; caller evidence:
      decomp/c/011/0811f050_FUN_0811f050.c and
      decomp/c/011/0811f4f4_FUN_0811f4f4.c; assembly: osos.asm 0x083e0e68.
      match.py: structural match reviewed.

  # --- byte-identical third three-word clear ---
  - address: 0x083e143c
    ghidra: FUN_083e143c
    name: three_word_clear_third
    size: 20
    sig: "void three_word_clear_third(u32 *target)"
    module: cxx/three_word_clear_third
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical third instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. A dedicated
      link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      uses nonzero guard and target fixtures to prove only those three words
      change. Reference C:
      decomp/c/038/083e143c_FUN_083e143c.c; disassembly: osos.dec
      0x083e143c (mov r1, #0; str r1, [r0, #0/#4/#8]). match.py:
      structural match reviewed.

  # --- byte-identical fourth three-word clear ---
  - address: 0x083e1ad0
    ghidra: FUN_083e1ad0
    name: three_word_clear_fourth
    size: 20
    sig: "void three_word_clear_fourth(u32 *target)"
    module: cxx/three_word_clear_fourth
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical fourth instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. Two
      decompiled callers (0x0813a500 and 0x0813abf8) pass the start of
      stack-local three-word ranges; neither consumes a return value. A
      dedicated link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      uses nonzero guard and target fixtures to prove only those three words
      change. Reference C: decomp/c/038/083e1ad0_FUN_083e1ad0.c;
      disassembly: osos.dec 0x083e1ad0 (mov r1, #0; str r1,
      [r0, #0/#4/#8]). match.py: structural match reviewed.


  # --- byte-identical sixth three-word clear ---
  - address: 0x083e41fc
    ghidra: FUN_083e41fc
    name: three_word_clear_sixth
    size: 20
    sig: "void three_word_clear_sixth(u32 *target)"
    module: cxx/three_word_clear_sixth
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical sixth instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. A dedicated
      link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      proves the +0/+4/+8 words are cleared while the surrounding bytes remain
      unchanged. Reference C: decomp/c/038/083e41fc_FUN_083e41fc.c;
      disassembly: osos.dec 0x083e41fc (mov r1, #0; str r1,
      [r0, #0/#4/#8]). match.py: structural match reviewed.

  # --- byte-identical seventh three-word clear ---
  - address: 0x083e472c
    ghidra: FUN_083e472c
    name: three_word_clear_seventh
    size: 20
    sig: "void three_word_clear_seventh(u32 *target)"
    module: cxx/three_word_clear_seventh
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical seventh instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. A dedicated
      link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      proves the +0/+4/+8 words are cleared while the surrounding bytes remain
      unchanged. Reference C: decomp/c/038/083e472c_FUN_083e472c.c;
      disassembly: osos.dec 0x083e472c (mov r1, #0; str r1,
      [r0, #0/#4/#8]). match.py: structural match reviewed.

  # --- byte-identical eighth three-word clear ---
  - address: 0x083e4a7c
    ghidra: FUN_083e4a7c
    name: three_word_clear_eighth
    size: 20
    sig: "void three_word_clear_eighth(u32 *target)"
    module: cxx/three_word_clear_eighth
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical eighth instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. A dedicated
      link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      proves the +0/+4/+8 words are cleared while the surrounding bytes remain
      unchanged. Reference C: decomp/c/038/083e4a7c_FUN_083e4a7c.c;
      disassembly: osos.dec 0x083e4a7c (mov r1, #0; str r1,
      [r0, #0/#4/#8]). match.py: structural match reviewed.

  # --- byte-identical ninth three-word clear ---
  - address: 0x083e4dd4
    ghidra: FUN_083e4dd4
    name: three_word_clear_ninth
    size: 20
    sig: "void three_word_clear_ninth(u32 *target)"
    module: cxx/three_word_clear_ninth
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical ninth instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. A dedicated
      link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      proves the +0/+4/+8 words are cleared while the surrounding bytes remain
      unchanged. Reference C: decomp/c/038/083e4dd4_FUN_083e4dd4.c;
      disassembly: osos.dec 0x083e4dd4 (mov r1, #0; str r1,
      [r0, #0/#4/#8]). match.py: structural match reviewed.

  # --- byte-identical tenth three-word clear ---
  - address: 0x083e5194
    ghidra: FUN_083e5194
    name: three_word_clear_tenth
    size: 20
    sig: "void three_word_clear_tenth(u32 *target)"
    module: cxx/three_word_clear_tenth
    status: ported
    renames:
      param_1/r0: target
      param_1_plus_0x00: target_word_0
      param_1_plus_0x04: target_word_1
      param_1_plus_0x08: target_word_2
    notes: >-
      Byte-identical tenth instance of the 20-byte three-word-clear helper:
      moves zero into r1, then stores it at target +0x00, +0x04, and +0x08.
      It has no NULL guard, no target reads, and no other stores. A dedicated
      link section keeps this separately hookable export from LLVM's
      identical-code folding; no behavioral deviations. The focused host test
      proves the +0/+4/+8 words are cleared while the surrounding bytes remain
      unchanged. Reference C: decomp/c/038/083e5194_FUN_083e5194.c;
      disassembly: osos.dec 0x083e5194 (mov r1, #0; str r1,
      [r0, #0/#4/#8]). match.py: structural match reviewed.
  # --- trivial eight-byte vector destructor ---
  - address: 0x083e1ae4
    ghidra: FUN_083e1ae4
    name: trivial_vector8_destruct
    size: 64
    sig: "u8 ** trivial_vector8_destruct(u8 **vector)"
    module: cxx/trivial_vector8_destruct
    status: ported
    renames:
      param_1/r0: vector
      param_1_plus_0x00: begin
      param_1_plus_0x04: end
      param_1_plus_0x08: capacity
      iVar1/r2: begin
      iVar2/r0: current
      return/r0: vector
    notes: >-
      Walks the half-open [begin, end) range in eight-byte increments; the
      element destructor is trivial, so the walk has no body. It calculates
      (capacity - begin) >> 3, clears r2, then calls FUN_08266f2c to release
      begin and returns vector. The recovered C signature for FUN_08266f2c
      uses only r0, so the port retains the descriptor reads and calculation
      while routing its sole live cleanup argument through the existing free
      seam. The descriptor is never written; the focused host test uses guard
      words and a recording cleanup seam to prove that exact write footprint
      and released pointer. Four direct ARM callers at 0x0813a638,
      0x0813a65c, 0x0813acc8, and 0x0813adcc pass stack-local descriptors.
      functions.csv correctly records 64 bytes; the 20-byte task metadata was
      stale. Reference C: decomp/c/038/083e1ae4_FUN_083e1ae4.c; disassembly:
      osos.dec 0x083e1ae4. match.py: structural match reviewed.
      absence of mutation.

  - address: 0x083e717c
    ghidra: FUN_083e717c
    name: trivial_vector8_destruct
    size: 64
    sig: "u8 ** trivial_vector8_destruct(u8 **vector)"
    module: cxx/trivial_vector8_destruct
    status: ported
    renames:
      param_1/r0: vector
      param_1_plus_0x00: begin
      param_1_plus_0x04: end
      param_1_plus_0x08: capacity
      iVar1/r2: begin
      iVar2/r0: current
      return/r0: vector
    notes: >-
      Raw extent is exactly 0x083e717c..0x083e71bc; 0x083e71bc begins the
      separate 24-byte FUN_083e71bc. The body is structurally identical to
      the canonical trivial_vector8_destruct @ 0x083e1ae4: it walks
      [begin,end) in eight-byte increments with an optimized-away trivial
      element destructor, calculates (capacity - begin) >> 3, clears r2,
      calls cxx_array_dealloc (FUN_08266f2c), and returns vector without
      writing the descriptor. The relative BL encoding naturally differs
      because this copy is at a different address, but both targets are
      0x08266f2c and all other instruction words agree. Decoding every ARM
      B/BL word in osos.dec verifies ten direct, unconditional bl call sites,
      no predicated bl forms, and no tail b forms: 0x08269c74, 0x08269d68,
      0x08269e34, 0x0826a1d8, 0x0826a2b0, 0x0826a76c, 0x0826a7a8,
      0x0826a7cc, 0x082caafc, and 0x082cab08. The canonical Rust export
      serves this semantically indistinguishable copy instead of adding a
      redundant body. Deliberate deviation: it reaches the existing free seam
      with cxx_array_dealloc's sole live argument, while retaining the raw
      descriptor reads and capacity calculation. Host tests cover non-empty
      and empty descriptors, cleanup pointer, return value, and a no-write
      guard footprint. Reference C: decomp/c/038/083e717c_FUN_083e717c.c;
      raw disassembly: osos.dec 0x083e717c. match.py structural review is
      recorded with this port.

  # --- COW string vector destructor ---
  - address: 0x083e5b88
    ghidra: FUN_083e5b88
    name: cxx_string_vector_destruct
    size: 48
    sig: "CxxStringVector * cxx_string_vector_destruct(CxxStringVector *vector)"
    module: cxx/string_vector_destruct
    status: ported
    renames:
      param_1/r0/r4: vector
      param_1_plus_0x00/r1/r0: begin
      param_1_plus_0x04/r2: end
      param_1_plus_0x08/r1: capacity
      FUN_083e5950: cxx_string_range_destroy
      FUN_083d8b04: cxx_string_release
      FUN_08266f2c: cxx_array_dealloc
      return/r0: vector
    notes: >-
      Raw ARM extent is exactly 0x083e5b88..0x083e5bb8: 12 instruction words,
      with the separately linked next function beginning at 0x083e5bb8. Loads
      `{begin,end}`, calls the unported range walker 0x083e5950 (which walks
      four-byte COW string slots and calls cxx_string_release @ 0x083d8b04),
      computes `(capacity - begin) >> 2`, clears r2, calls cxx_array_dealloc
      @ 0x08266f2c, and returns vector without descriptor writes. The port
      deliberately inlines the verified behavior of the unported range walker
      to the already ported cxx_string_release seam; its separate call boundary
      is absent. Decoding every ARM B/BL word in osos.dec verifies exactly ten
      direct, unconditional bl callers, no predicated bl calls, and no direct
      tail b calls: 0x08197b74, 0x082aae7c, 0x082aaf74, 0x082ab064,
      0x082ab074, 0x082ab08c, 0x083c4e00, 0x083db3b8, 0x083e2244, and
      0x083e256c. Focused host tests prove ordered releases for a three-element
      vector, empty-range behavior, deallocation arguments, return value, and
      an unchanged descriptor/guard footprint. Raw disassembly:
      osos.dec 0x083e5b88; match.py structural review recorded.

  # --- pending-event queue take ---
  - address: 0x08138f14
    ghidra: FUN_08138f14
    name: pending_event_take
    size: 188
    sig: "u32 pending_event_take(u8 *this, u32 key, u16 *tag_a, u16 *tag_b, u32 *payload_out)"
    module: app/pending_event_take
    status: ported
    renames:
      param_1/r0/r9: this
      param_2/r1/sl: key
      param_3/r2/r4: tag_a
      param_4/r3/r5: tag_b
      param_5/sp+0x20/r8: payload_out
      r6: mutex_ptr (this + 0x2ac)
      r7/iVar3: status
      piVar2/r0: link (address of the link word)
      star_piVar2/r1: node
      DAT_08138fcc: WILDCARD_TAG (0xffff)
    notes: >-
      Take-and-cancel of the pending-event queue embedded at the start of
      the playback session object. True extent 0x08138f14..0x08138fd0 =
      188 bytes: 184 bytes of code plus the trailing literal-pool word
      0x0000ffff @ 0x08138fcc; Ghidra's 184 drops that word (next function
      0x08138fd0). Call count binary-verified by decoding every B/BL word
      in osos.dec: exactly 45 sites, all unconditional bl (no predicated
      forms), spread over the playback/IAP cluster 0x08139cb0..0x082091cc;
      no DATA word references the address, so it is only ever direct-called.
      Under the PosixMutex at this+0x2ac (veneers 0x08261e20/0x08261e24):
      find the first live entry whose key matches exactly and whose two tag
      halfwords match equal-or-0xffff (search FUN_08139190 returns the
      ADDRESS OF THE LINK WORD, &prev->next); fill each wildcarded tag from
      the node (+0x0c/+0x0e), copy the payload word (+0x10) out when the
      caller passed a non-null slot, unlink the node back onto the free
      list at +0x18 (release FUN_0813908c re-searches by the node's own
      fields and heap_panics on a corrupt queue), then re-arm the deadline
      timer from the new head (rearm FUN_0813957c, always returns 0).
      Returns 0 = taken, 0x52 = no matching entry (out-params untouched);
      firmware release/rearm can only produce 0 but the port keeps the
      original's general status chaining. Node layout (20 bytes) recovered
      from insert FUN_08138fd0 next/key/deadline_ms/tag_a/tag_b/payload;
      insert stores deadline = now_ms + delta and pops free nodes via
      FUN_081391f0, so entries are scheduled events serviced by the IAP
      incoming-process thread's timer queue (thread+0x114 mutex; handle at
      session+0x2c8). Deviations: the three callees stay unported behind
      PENDING_EVENT_TAKE_OPS (target transmutes 0x08139190/0x0813908c/
      0x0813957c, inert host stubs, tests install recording reference
      models); lock/unlock call the canonical ported posix_mutex symbols
      directly per the names.yaml veneer-alias resolution; the original's
      three node reloads from the link word collapse to one load (C
      aliasing artifact, unobservable for non-aliased stack outs); the
      defensive node==NULL branch calls ported heap_panic and is not
      host-tested (unreachable through find-link's contract, terminate
      spins). Host tests: wildcard fill both/one side, exact tags kept,
      payload copy and NULL slot, miss path leaves outs untouched and
      returns 0x52, release-status propagation skips rearm, rearm-status
      chaining, raw input tags seen by the search, mid-chain unlink plus
      free-list push, mutex owner held inside the critical section and
      cleared after every path. match.py --size 184 structural review:
      identical branch skeleton and predicated ldrh/strh field pairs;
      deltas are the seam blx indirection, a frame pointer for the spilled
      ops slot, and 0xffff built from mov/orr instead of a pool load.
  # --- pending-event queue insert ---
  - address: 0x08138fd0
    ghidra: FUN_08138fd0
    name: pending_event_insert
    size: 188
    sig: "u32 pending_event_insert(u8 *this, u32 key, u16 tag_a, u16 tag_b, u32 payload, u32 delta_ms)"
    module: app/pending_event_insert
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: key
      param_3/r2/r8: tag_a
      param_4/r3/r9: tag_b
      param_5/sp+0x48/sl: payload
      param_6/sp+0x4c/fp: delta_ms
      r4: node (the popped free node)
      r5: status (0x0c = no free node)
      r6: mutex_ptr (this + 0x2ac)
      r7: zero scratch for the node->next store
      sp+0x0c: clock (the { vtable, u8 kind } kind-1 clock object)
      sp+0x04: timespec (the { sec, nsec } out pair)
    notes: >-
      Schedule-half of the pending-event queue embedded at the start of
      the playback session object; sibling of pending_event_take @
      0x08138f14 (ported, same worktree family). True extent
      0x08138fd0..0x813908c = 188 bytes, pure code, no literal pool:
      the body ends pop {.., pc} @ 0x08139088 and the next function is
      the sorted-insert helper 0x081390ec this one calls, so Ghidra's
      188 is right for once (byte-decoded from osos.dec anyway). Call
      count binary-verified by decoding every B/BL word in osos.dec:
      exactly 22 sites, all unconditional bl (0 predicated forms, 0
      tail b), and 0 occurrences of 0x08138fd0 as a data word - direct
      calls only, never virtual. Under the PosixMutex at this+0x2ac
      (veneers 0x08261e20/0x08261e24, resolved to the canonical ported
      posix_mutex symbols per the names.yaml alias entries): pop a
      free node (FUN_081391f0, free-list head at +0x18; the returned
      node's next still holds the old free-link and this function
      clears it), fill {next=0, key, tag_a, tag_b, payload} in that
      register-order write sequence, construct the kind-1 clock on the
      stack (clock_source_construct @ 0x08262958, ported), read it
      through the vtable +0x0c blx into a { sec, nsec } pair, convert
      (timespec_to_milliseconds @ 0x082a1c30, ported; only the low 32
      bits are used), stamp deadline_ms = now_ms + delta_ms with a
      wrapping add, link the node (sorted insert FUN_081390ec, returns
      0 or panics), destroy the clock (clock_source_destroy @
      0x08262908, ported identity), and ALWAYS re-arm the deadline
      timer (FUN_0813957c, returns 0) - even when the pool was empty
      or the insert failed - keeping the rearm's status only when the
      status so far is 0 (cmp r5,#0 / moveq r5,r0). Default status
      0x0c (mov r5,#12) reports pool exhaustion; the take sibling
      uses 0x52 for its miss, so these are module-level codes, not
      errno values. Observable contract on firmware: 0 = scheduled,
      0x0c = no free node. Deviations: the four unported callees
      (0x081391f0 pop, 0x081390ec insert, 0x0813957c rearm, and the
      vtable +0x0c clock read) dispatch through
      PENDING_EVENT_INSERT_OPS; target defaults transmute the ROM
      addresses, the clock-read default re-reads the vtable word from
      the constructed object and calls slot +0x0c - reproducing the
      original blx verbatim, whose slot value is the documented
      mid-function anomaly from the clock_source_construct entry -
      and host defaults are inert (NULL / 0 / zeroed timespec) with
      recording reference models installed by the tests. LLVM inlines
      timespec_to_milliseconds (the sec*1000 + nsec/1000000 arithmetic
      survives, fused with the delta add, wrap preserved) and elides
      the clock_source_destroy call entirely (the callee is an empty
      identity and its result is unused); both are semantically
      invisible. Host tests (6): happy-path field stamps and free-list
      pop with vtable-pinning call order, empty pool returns 0x0c and
      still rearms with zero writes to the pool, deadline u32 wrap
      (0xffffff00 + 0x200 = 0x100, no panic), insert-failure status
      propagation discarding the rearm status while still rearming,
      rearm status chaining through success, halfword tag stores
      staying inside the 20-byte node (guard bytes), and the mutex
      owner asserted held inside every seam callee and released after
      every path. match.py --lib structural review (exit-1 diff
      expected): identical branch skeleton (null check beq to the
      rearm path), the strh tag pair and the cmp/moveq status select;
      deltas are the four seam blx indirections, the crate's fp frame,
      the inlined conversion and the elided identity destroy above.
  - address: 0x080495d0
    ghidra: FUN_080495d0
    name: diag_ring_attach_strings
    size: 192
    sig: "void diag_ring_attach_strings(i32 count, ...)"
    module: kernel/diag_ring_strings
    status: ported
    renames:
      param_1/r0/sp+0x20: count
      r4: total_len
      r5: capacity
      r6: string_index
      r7: string_cursor
      r8: owned_string
      r9: current_string
      FUN_08043c18: traced_alloc
      FUN_08043994: traced_free
      FUN_08392478: strlen
      FUN_08043f3c: traced_realloc
      FUN_0804228c: strlcat
      FUN_08049bbc: diag_ring_attach_owned_string
    notes: >-
      Raw extent is 0x080495d0..0x08049690; the next BL-targeted function
      starts at 0x08049690, so Ghidra's 192-byte extent is right. Every ARM
      B/BL word in osos.dec was decoded: 14 direct calls, all unconditional
      bl (zero blne/bleq). Callers use 2, 4, and 6 string arguments, proving
      this is a true ARM varargs function, not a three-string helper. It
      allocates 81 bytes through traced_alloc, joins exactly count non-NULL
      C strings, and grows only when the signed accumulated strlen exceeds
      the signed capacity (new capacity = length + 20). Realloc failure frees
      the old buffer and returns; success attaches the owned result with flag
      3 to the current diagnostic-ring record. The target export is a small
      ARM wrapper that spills r1-r3 directly beside caller-stack arguments,
      preserving the original varargs sequence before entering typed Rust.
      Ported traced_alloc, traced_free, and strlen are direct calls; unported
      traced_realloc and record attachment dispatch through volatile
      DIAG_RING_STRING_OPS defaults (NULL realloc / inert attach). The
      unported strlcat's bounded byte behavior is reproduced locally because
      its return value is discarded. Host tests cover NULL strings across a
      fourth spilled argument, the strict 80-byte growth boundary, realloc
      failure release, negative count without varargs dereference, and initial
      allocation failure. match.py structural review is necessarily split
      between the ABI wrapper and typed body.
  - address: 0x08049a84
    ghidra: FUN_08049a84
    name: diag_ring_record
    size: 212
    sig: "void diag_ring_record(u32 facility, u32 subsystem, u32 code, u32 data0, u32 data1)"
    module: kernel/diag_ring_record
    status: ported
    renames:
      param_1/r0/r5: facility
      param_2/r1/r6: subsystem
      param_3/r2/r7: code
      param_4/r3/r8: data0
      param_5/sp+0x20/r9: data1
      r4: block (per-task DiagEventRing returned by 0x080498f8)
    notes: >-
      Extent confirmed from raw bytes: 0x08049a84..0x08049b58, no literal
      pool, next function at 0x08049b58 - Ghidra's 212 is right. Call
      count binary-verified by decoding every BL word in osos.dec: exactly
      197 sites, ALL unconditional bl (no predicated forms) - no caller
      NULL-guards or gates it, consistent with a logger that is always
      safe to call. The fifth argument rides the stack (ldr r9,[sp,#32]).
      Appends one event to the calling task's diagnostic ring: head =
      (head+1) % 16 in signed C modulo (branchless asr/bic/sub idiom,
      reproduced instruction-exact); if head lands on tail the ring was
      full and tail follows, dropping the oldest record; packs
      facility<<24 | (subsystem & ~0xff000)<<12 | code&0xfff into
      tags[head] (the bic strips bits 12..19 BEFORE the shift; bits 20+
      fall off the shift), stores data0/data1 verbatim in parallel arrays,
      then if pointers[head] != 0 and flags[head] bit 0 is set releases
      the STALE occupant's block through traced_free @ 0x08043994 and
      zeroes the pointer, then clears flags[head]. The freed pointer is
      the record 16 writes ago; the new tag/data are already stored when
      the free runs (order pinned by a test). Quirk reproduced: head
      starts at 0 and advances before use, so the first record lands in
      slot 1 and slot 0 stays empty until the first full wraparound.
      Block layout (332 bytes, allocated by 0x080498f8) decoded from this
      function plus reset 0x08049694 and formatter 0x080496f0: owner task
      id +0x000, five parallel 16-word arrays tags/pointers/flags/data0/
      data1 at +0x004/+0x044/+0x084/+0x0c4/+0x104 (field bases 0x40 apart,
      column-major - NOT an array of structs), head +0x144, tail +0x148;
      reset pre-fills data1 with -1. Deviations: block getter 0x080498f8
      is unported and sits behind DIAG_RING_BLOCK_GETTER (default None =
      documented no-op; the real getter never returns NULL, so there is no
      faithful NULL behavior to inherit; hooks.yaml wires it once ported);
      traced_free IS ported and called directly as in the original
      (Ghidra's C drops its argument - r0 still holds the stale word from
      [r1,#0x44]); slot addressing is raw pointer arithmetic like the
      original rather than bounds-checked indexing, whose abort path a
      corrupt head could reach but the firmware cannot; LLVM reorders the
      three independent slot stores (tag last) and narrows the flags load
      to a byte - both unobservable. Callers of note: the traced
      allocator's failure path records (0x22,0x6f,0x41) here via
      sqlite/blob_to_hex, whose HEX_BLOB_HOOKS seam can now be rewired to
      diag_ring_record. Host tests: layout offsets, first-record-in-slot-1,
      packing masks (incl. subsystem bit-12 strip and shift truncation),
      verbatim data words, wraparound + oldest-drop at full, flagged stale
      pointer freed and cleared, unflagged/bit-1-only/NULL-pointer slots
      not freed but flags cleared, store-before-free order, no-getter and
      null-block no-ops. match.py --lib <worktree archive>: same branch
      skeleton - identical head-mod idiom, predicated ptr/flag guard, bl
      to traced_free, double head reload after the free; deltas are the
      seam checks, an fp prologue, an equivalent alternate mod-16 sequence
      for the tail, and the store reorder above.

  - address: 0x0825b680
    ghidra: FUN_0825b680
    name: lazy_singleton_0x40
    size: 44
    sig: "void *lazy_singleton_0x40(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0825b6ac: SINGLETON_0X40_CACHE_POOL (the trailing pool word @
        0x0825b6ac holding 0x089cc94c; the cache word itself is the crate
        static SINGLETON_0X40, the block_mgr.rs RW-page deviation)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_0825bd20: singleton_0x40_ctor (SINGLETON_CTORS.singleton_0x40 slot)
    notes: >-
      The twelfth member of the app/singletons family, idiomatically
      identical to the other eleven - `if (cache == 0) cache =
      ctor(operator_new(0x40)); return cache;` over the cache word
      0x089cc94c, the next word after the settings store's cache
      0x089cc948 in the 0x089cc9xx globals page. EXTENT: functions.csv's
      44 bytes is the code only; the function owns one trailing
      literal-pool word (0x089cc94c @ 0x0825b6ac, reached by `ldr r4,
      [pc, #32]`), so the true extent is 48 bytes,
      0x0825b680..0x0825b6b0, where the next function opens `push {r4,
      lr}`. CALL COUNT: 43 unconditional bl call sites, binary-verified
      by decoding every B/BL word in work/firmware/osos.dec - 0
      predicated and 0 plain b. Several sites NULL-check the RESULT
      (0x08216574 `cmp r0,#0; beq`) because a NULL-returning ctor
      propagates and leaves no failure memory (reproduced and tested),
      but none of the calls themselves are predicated.
      OBJECT: the ctor 0x0825bd20 plants a vtable literal 0x089a7ffc at
      +0, builds an embedded fixed-block pool at +4 through FUN_083c0ba8
      (20-byte records, growth n+n/2+n/8, free-list head chained through
      each node's +12) parking that pool's descriptor pointer at +0x14
      with the descriptor's +8/+12 self-pointing (circular sentinel),
      constructs a mutex at +32 via the ported cxx_mutex_construct
      0x08261e28 and zeroes byte +62 last, which is what makes 0x40 the
      allocation size. The 43 call sites use it as a hub: registration
      of sub-objects through vtable slot +8 (service_manager_ctor
      0x0816566c hands it this+0xd0; the class-8c00 ctor 0x081a71fc
      hands it this+0x1c) and boolean polls through slots +0x18/+0x48/
      +0x4c (stopwatch retry loop 0x08216568 gates on +0x4c). CLASS NOT
      NAMED: no name-factory call or name literal anywhere; worse, the
      vtable page 0x089axxxx is runtime-initialized and the image holds a
      stale copy whose slot words land mid-flow of plausible-looking
      target functions, so the vtable cannot be read for a name either -
      the symbol names the size only, per the lazy_singleton_0x3c /
      lazy_singleton_0x58 precedent. Deviations: operator_new called
      directly; the ctor rides the SINGLETON_CTORS dispatch table with a
      documented zeroing default (NOT HOOK-READY, the family contract);
      the cache is the crate static SINGLETON_0X40 rather than the word
      @ 0x089cc94c. match.py --lib <worktree archive>: same skeleton -
      cache-ptr load, ldr/cmp/bne test, `mov r0,#0x40`, bl to
      operator_new, ctor call, store, reload, pop; deltas are the seam
      indirection (`ldr r5,[r0,#0x2c]; blx r5` - slot offset 0x2c = the
      twelfth SingletonCtors field), an fp prologue and the inverted
      branch LLVM chose for the cold path.

  - address: 0x0825a6e8
    ghidra: FUN_0825a6e8
    name: lazy_singleton_0x44
    size: 44
    sig: "void *lazy_singleton_0x44(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0825a714: SINGLETON_0X44_CACHE_POOL (the trailing pool word @
        0x0825a714 holding 0x08a09f64; the cache word itself is the crate
        static SINGLETON_0X44, the block_mgr.rs RW-page deviation)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_0825aa58: singleton_0x44_ctor (SINGLETON_CTORS.singleton_0x44 slot)
    notes: >-
      A lazy singleton getter: `if (cache == 0) cache =
      ctor(operator_new(0x44)); return cache;` over cache word 0x08a09f64.
      EXTENT: functions.csv's 44 bytes covers code only. The trailing pool
      word 0x08a09f64 at 0x0825a714 is loaded by `ldr r4,[pc,#32]`, so the
      true extent is 48 bytes, 0x0825a6e8..0x0825a718. Raw bytes show the
      next separately linked entry at 0x0825a718; Ghidra omits that entry
      and this function's pool word. CALL COUNT: exactly 14 direct,
      unconditional bl sites (0x08139f6c, 0x08139f7c, 0x08139fb4,
      0x08139fc4, 0x08139ff4, 0x0813a008, 0x0813a03c, 0x0813a054,
      0x0813a078, 0x0813a150, 0x0813a174, 0x0813a1a4, 0x0813a1b4,
      0x0813a1cc), binary-verified by decoding every ARM B/BL word in
      work/firmware/osos.dec - 0 predicated and 0 plain b. They all belong
      to the 0x08139fxx command-processing cluster and call virtual slots
      +0x20..+0x40 of the returned object. OBJECT: ctor 0x0825aa58 first
      calls 0x08037db8(this, 0x3d), plants the shared interface vtable
      0x089a75c8 at +0, initializes an embedded map header at +4 (descriptor
      allocated by unported FUN_083c00dc, self-linked at +8/+12), constructs
      a mutex at +0x20 via cxx_mutex_construct, sets byte +0x3c = 2, plants
      primary vtable 0x089a7d30 at +0 and secondary vtable 0x089a7d80 at
      +0x40, then subscribes that secondary interface to event hub kind 11
      through event_hub_instance_get / event_hub_subscribe. No class name,
      registry id, or other identity survives, so the Rust name states only
      the recovered lazy-singleton role and allocation extent. Deviations:
      operator_new is direct; unported ctor dispatches through the
      SINGLETON_CTORS singleton_0x44 slot (zeroing default); cache is crate
      static SINGLETON_0X44 rather than runtime word 0x08a09f64. NOT
      HOOK-READY until 0x0825aa58 is ported. Host tests cover allocation
      extent, raw allocation handoff, constructor-return caching, cached
      second call, NULL-return retry, and zeroing default bounds. match.py
      structural review confirms the cache-test / allocate / construct /
      store / reload skeleton; expected differences are the crate frame and
      dispatch-table indirect constructor call.
  - address: 0x08259534
    ghidra: FUN_08259534
    name: lazy_singleton_0xbc
    size: 44
    sig: "void *lazy_singleton_0xbc(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_08259560: SINGLETON_0XBC_CACHE_POOL (the trailing pool word @
        0x08259560 holding 0x089cc960; the cache word itself is the crate
        static SINGLETON_0XBC, the block_mgr.rs RW-page deviation)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_08258a70: singleton_0xbc_ctor (SINGLETON_CTORS.singleton_0xbc slot)
    notes: >-
      The eighteenth member of the app/singletons family, idiomatically
      identical to the other seventeen - `if (cache == 0) cache =
      ctor(operator_new(0xbc)); return cache;` over the cache word
      0x089cc960 (the pool literal points straight at the cache; no +N
      slot). One of the ten getters the boot-time singleton checker
      FUN_08258028 calls before heap_panic-ing on any NULL (its site is
      0x0825807c). EXTENT: functions.csv's 44 bytes is the code only;
      the function owns one trailing literal-pool word (0x089cc960 @
      0x08259560, reached by `ldr r4, [pc, #32]`), so the true extent is
      48 bytes, 0x08259534..0x08259564, where the sibling 0x4c-byte
      getter opens `push {r4, lr}`. CALL COUNT: 22 unconditional bl call
      sites, binary-verified by decoding every B/BL word in
      work/firmware/osos.dec - 0 predicated and 0 plain b. Ghidra's C
      (decomp/c/025/08259534_FUN_08259534.c) drops the `this` argument
      to the ctor, the family-wide ADS decomp artifact. OBJECT: an
      event-listener controller. Its ctor 0x08258a70 first plants the
      shared interface base 0x089a75c8 at +0 (the same first store
      settings_ctor makes), zeroes +0x04..+0x1d, builds an embedded map
      head whose descriptor node at +0x14 is allocated by the unported
      FUN_083c00dc (self-pointing +8/+12 circular sentinel, the
      lazy_singleton_0x40 pool shape), constructs a mutex at +0x20 via
      the ported cxx_mutex_construct 0x08261e28, sets byte +0x3c = 2,
      then plants the real primary vtable 0x089a7604 at +0 and the
      secondary vtable 0x089a76e0 (+0xdc off the same literal) at +0x40,
      builds a second embedded map head at +0x48 whose header node
      (+0x58) comes from the ported byte_key_tree_allocate_node
      0x083b7f40, default-constructs two embedded PathObjects at
      +0x64/+0x6c (ported path_object_default_construct 0x082792cc) and
      two embedded StringObjects at +0x7c/+0x94 (ported
      string_default_construct 0x08277440), zeroes bytes +0x74/+0x75/
      +0x9e, constructs a second mutex at +0xa0, and finally subscribes
      this+0x40 - the secondary-vtable sub-object - to the process-wide
      event hub's kind-1 listener list through event_hub_instance_get @
      0x082579a0 and the subscribe push_back @ 0x08257aec (the
      app/event_hub.rs pair). The 22 call sites dispatch the object
      virtually: fourteen inside the registry-class-0x8780 method
      cluster 0x081a3xxx-0x081a5xxx (slots +0x54/+0x6c observed), four
      in the command handler FUN_081c9228, which maps command ids
      0x10001-0x10005 onto vtable slots +0x20/+0x28/+0x30/+0x34, three
      more at 0x08114ee4/0x08114f60/0x0815fef4, and the boot-checker
      site. CLASS NOT NAMED: the ctor reaches neither a name factory nor
      the by-id registry, so - per the lazy_singleton_0x3c /
      lazy_singleton_0x58 / lazy_singleton_0x40 precedent - the symbol
      names the size only. Deviations: operator_new called directly; the
      ctor rides the SINGLETON_CTORS dispatch table with a documented
      zeroing default (NOT HOOK-READY, the family contract); the cache
      is the crate static SINGLETON_0XBC rather than the word @
      0x089cc960. Host tests: exact 0xbc allocation size, constructed
      once on the raw block, cached and reloaded; the bne fast path
      (pre-seeded cache never allocates); a NULL-returning ctor caches
      NULL and re-allocates every call (no failure memory); the store
      lands AFTER the ctor runs (a cache-poking ctor is overwritten by
      the store of its own return, the original's `str r0, [r4]` order);
      the zeroing stub clears exactly 0xbc bytes and is NULL-safe; cache
      independence from the neighbouring 0x089cc9xx-page statics.
      match.py --lib <worktree archive>: same skeleton - cache-ptr pool
      load, ldr/cmp/branch test, `mov r0,#0xbc`, bl to operator_new,
      ctor call, store, reload, pop; deltas are the seam indirection
      (`ldr r5,[r0,#0x44]; blx r5` - slot offset 0x44 = the eighteenth
      SingletonCtors field), an fp prologue and the inverted branch
      layout LLVM chose for the cold path.

  - address: 0x082577bc
    ghidra: FUN_082577bc
    name: f32_to_fixed16_trunc
    size: 88
    sig: "*mut i32 f32_to_fixed16_trunc(out: *mut i32, bits: u32) -> *mut i32  // *out = trunc(f32(bits) * 65536), wrapping"
    module: util/fixed
    status: ported
    renames:
      param_1: out
      param_2: bits
    notes: >-
      IEEE-754 binary32 -> Q16.16 fixed point, truncating toward zero, NO
      saturation - overflowing results wrap modulo 2^32. Pure bit arithmetic,
      no soft-float calls: exponent = (bits << 1) >> 24, mantissa =
      (bits & 0x00ffffff) | 0x00800000 (implicit bit forced on even for
      denormals); exponent < 134 (= 127 + 16) shifts the mantissa right by
      134 - exponent (arithmetic, >= 32 gives 0), exponent >= 134 shifts left
      by exponent - 134 (>= 32 gives 0), then the sign bit negates. Equals
      trunc(f * 65536) for every finite input whose scaled value fits; since
      the magnitude is truncated and the sign reapplied, negatives truncate
      toward zero (-0.9999 -> -65535, never -65536). Denormals land on 0;
      infinities and NaNs (exponent 255, shift 121) land on 0 too - unlike
      the saturating sibling f32_to_fixed16_sat @ 0x080a67dc (fp/fp_fixed16),
      which clamps them to the i32 range for the cos/sin tables; this one's
      overflows are real outputs (40000.0 -> 0x9c400000 negative, +inf ->
      0). Extent verified from raw osos.dec: exactly 0x082577bc..0x08257814,
      no literal pool; the next symbol @ 0x08257814 is a separate 4-byte
      empty destructor, so Ghidra's 88 bytes are correct here. 43 call
      sites, ALL plain unconditional bl, none predicated - there is no NULL
      guard and callers never gate the call (binary-scanned): 0x081528c8 /
      0x08152d84 (audio gain literals 0.995 / 1.33 from pools 0x081529d4 /
      0x08152e28), thirty-six in the Q16.16 rasterizer run
      0x08273a64..0x0827419c (line walkers stepping pixel coordinates by
      >> 16, e.g. FUN_08274004 converts the literal 0.5 then compares and
      steps with it), five in font-metric code 0x082ac214..0x082ac41c
      (points-per-inch literal 72.5 @ pool 0x082ac504). ABI quirk preserved:
      r0 passes through untouched, so the function "returns" its out pointer
      and callers use that (`piVar4 = FUN_082577bc(&local_3c, 0x3f000000);
      *piVar4 ...` in FUN_08274004) - the port returns `out` explicitly.
      Deviations: the original stores the mantissa to *out before scaling
      and reloads through the pointer for the sign negation; nothing can
      observe that intermediate store between the two stores, so the port
      computes in registers and writes once. Own link_section
      .text.f32_to_fixed16_trunc as fold insurance; symbol resolves
      separately in the archive (no fold). match.py --lib <worktree
      archive>: same structure - mantissa mask/orr, exponent extract, two-
      way branch on 134, shift with zero beyond 32 (LLVM inverts both
      conditions instead of materializing the >= 32 case), sign negate via
      cmp/rsbmi, single str through r0; deltas are LLVM extracting the
      exponent as (bits >> 23) & 0xff (same value), using lsr where ADS used
      asr (identical: the mantissa's top bit is clear), an fp frame instead
      of ADS's push {r0,r1,lr} arg-spill, and a trailing literal word for
      the 0x007fffff mask.

  - address: 0x0811b2c0
    ghidra: FUN_0811b2c0
    name: singleton_class_6280
    size: 44
    sig: "void *singleton_class_6280(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0811b2ec: CLASS_6280_CACHE_POOL (the trailing pool word @
        0x0811b2ec holding 0x089cc30c; the cache word itself is the crate
        static CLASS_6280_INSTANCE, the block_mgr.rs RW-page deviation)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_0811c7fc: class_6280_ctor (SINGLETON_CTORS.class_6280 slot)
    notes: >-
      The thirteenth member of the app/singletons family, idiomatically
      identical to the other twelve - `if (cache == 0) cache =
      ctor(operator_new(0xa0)); return cache;` over the cache word
      0x089cc30c. EXTENT: functions.csv's 44 bytes is the code only; the
      function owns one trailing literal-pool word (0x089cc30c @
      0x0811b2ec, reached by `ldr r4, [pc, #32]`), so the true extent is
      48 bytes, 0x0811b2c0..0x0811b2f0, where the next function opens
      `push {r4-r8, lr}`. CALL COUNT: Ghidra's 43 confirmed -
      binary-verified by decoding every B/BL word in
      work/firmware/osos.dec: exactly 43 unconditional bl call sites, 0
      predicated and 0 plain b; callers fetch the object then hand it
      straight to worker functions (0x0811c564 / 0x0811c010 / 0x0811c284
      at the 0x0822f97c cluster) or park it in a field (+0xbc of the
      object built @ 0x0811aecc). OBJECT IDENTIFIED BY REGISTRY ID: the
      ctor 0x0811c7fc plants vtable literal 0x08981d2c at +0 and a
      secondary vtable 0x08982934 on an embedded sub-object, allocates a
      0x34-byte sub-block through operator_new built by
      FUN_081bb450(. , 6, 1, 0x15e) plus an 8-byte pair initialized by
      FUN_0810dddc, and registers the object in the by-id class registry
      under id 0x6280 - pool literal @ 0x0811c968 loaded @ 0x0811c884
      (`ldr r1, [pc, #220]`) immediately before `bl 0x081d23f8` @
      0x0811c88c. The registered pointer is `this` itself: the compiler
      forms `(FUN_0811d8ac(this + 0x6c) + 0x20) - 0x8c` through two
      opaque calls whose arithmetic folds back to this (FUN_0811d8ac
      returns its argument). Unlike lazy_singleton_0x3c / _0x58 / _0x40
      the class IS identified - by its own registry id, per the
      singleton_class_8900 precedent; no name literal survives, so the
      symbol carries the id only. The image word at the cache address is
      stale UI string data ("ls_T"), confirming the RW-page deviation.
      Deviations: operator_new called directly; the ctor rides the
      SINGLETON_CTORS dispatch table with a documented zeroing default
      (NOT HOOK-READY, the family contract); the cache is the crate
      static CLASS_6280_INSTANCE rather than the word @ 0x089cc30c.
      match.py --lib <worktree archive>: same skeleton - cache-ptr load,
      ldr/cmp test, `mov r0,#0xa0`, bl to operator_new (R_ARM_CALL
      relocation verified), ctor call, store, reload, pop; deltas are
      the seam indirection (`ldr r5,[r0,#0x30]; blx r5` - slot offset
      0x30 = the thirteenth SingletonCtors field), an fp prologue and
      the inverted branch LLVM chose for the cold path.

  # --- Stage progress increment ---
  - address: 0x081fa36c
    ghidra: FUN_081fa36c
    name: stage_progress_increment
    size: 12
    sig: "void stage_progress_increment(StageProgressTracker *tracker, uint stage)"
    module: app/stage_progress
    status: ported
    renames:
      param_1: tracker
      param_2: stage
      iVar1: next_progress
      FUN_081fa378: stage_progress_set
      thunk_FUN_0815940c: set_running_deadline
    notes: >-
      EXTENT: the assigned 12 bytes are exact for this entry
      (0x081fa36c..0x081fa378): `ldr r2,[r0,#4]; add r2,r2,#1; nop`.
      The nop falls through deliberately into separately linked sibling
      FUN_081fa378, which performs the stage/budget checks and deadline
      update; the Rust port includes that complete fall-through path. CALL
      COUNT: exactly 11 unconditional bl call sites, 0 predicated forms and
      0 plain b, verified by decoding every ARM B/BL word in
      work/firmware/osos.dec; Ghidra's 11 confirmed. ALGORITHM: compute
      next_progress = tracker+0x04 + 1; return unless byte tracker+0x00
      equals stage, stage's u32 budget at tracker+0x10+stage*4 is at least
      next_progress, and the unsigned increment did not wrap; then store
      next_progress and call veneer 0x081b9154 with tracker+0x08 +
      next_progress. The veneer reaches unported FUN_0815940c, which writes
      the running-deadline word only when its global deadline keeper exists.
      All callers use an unconditional bl, so the port deliberately retains
      no NULL guard for tracker. Deviation: the unported setter is called at
      its retail veneer on target; host tests use the StageProgressOps seam.

  # --- Stage progress tracker lazy singleton ---
  - address: 0x081fa3b0
    ghidra: FUN_081fa3b0
    name: stage_progress_tracker_get
    size: 56
    sig: "void *stage_progress_tracker_get(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_081fa3e8: TRACKER_GLOBALS_POOL (the trailing pool word @
        0x081fa3e8 holding 0x089ca58c; the cache is that global's +0x48
        slot 0x089ca5d4, the crate static STAGE_PROGRESS_TRACKER, the
        block_mgr.rs RW-page deviation)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_081fa440: stage_progress_tracker_ctor
        (SINGLETON_CTORS.stage_progress_tracker slot)
      FUN_08030f44: heap_panic (direct, ported in heap/veneers)
    notes: >-
      The fifteenth app/singletons member and the only one that breaks
      the idiom. EXTENT: Ghidra's 56 bytes is exact for the code
      (0x081fa3b0..0x081fa3e8); the function owns one trailing
      literal-pool word (0x089ca58c @ 0x081fa3e8, reached by `ldr r4,
      [pc, #44]`), so the true extent is 60 bytes, ending where the
      sibling stage wait FUN_081fa3ec opens `push {r4, r5, r6, lr}`.
      CALL COUNT: exactly 30 unconditional bl call sites, 0 predicated
      and 0 plain b, binary-verified by decoding every B/BL word in
      work/firmware/osos.dec; Ghidra's count of 30 confirmed. STRUCTURE
      (verified against the raw words): cache test on the +0x48 slot of
      the global 0x089ca58c; on NULL, `mov r0,#0x2c; bl operator_new`,
      the raw allocation kept in r5 across the ctor `bl 0x081fa440`,
      then `cmp r5,#0; str r5,[r4,#72]; bleq heap_panic`, then reload
      and return the slot. TWO BREAKS FROM THE FAMILY: (1) the RAW
      allocation is cached, not the ctor's return - and it must be,
      because the ctor 0x081fa440 returns `[this+0xc]` (the
      just-zeroed total-budget word it hands to the deadline keeper via
      veneer 0x081b9150 -> 0x081593f4), NOT `this`; (2) OOM is fatal:
      the NULL is stored first, then heap_panic 0x08030f44 runs and
      never returns (the other fourteen cache a NULL ctor result and
      retry). The ctor is invoked on the NULL block BEFORE the panic
      (unpredicated bl); reproduced, unreachable in practice behind the
      ported heap_panic. OBJECT: a 0x2c-byte stage progress tracker for
      the long database-commit/copy paths - byte +0x00 current stage
      (0..7), word +0x04 progress counter, word +0x08 accumulated base,
      word +0x0c total budget, seven per-stage budget words
      +0x10..+0x2b. The ctor zeroes the object and resets the global
      deadline keeper 0x089cc79c (running deadline keeper+0x24 = 0 via
      veneer 0x081b9154 -> 0x0815940c; total keeper+0x2c = 0 via
      0x081593f4). Siblings: FUN_081fa2e8 advances the stage (waiting
      out the in-flight one via FUN_081fa3ec, which polls in 10-tick
      yields through the wait_or_yield veneer 0x080e9eb0), FUN_081fa36c
      bumps progress and falls into FUN_081fa378 (pushes a fresh running
      deadline base+progress to the keeper when inside the stage
      budget), FUN_081fa33c commits the seven-budget total to
      keeper+0x2c. CALLERS: 22 sites in the database-commit path
      FUN_08068504 (stage budgets set from the Photo Database /
      ArtworkDB / iTunesDB file sizes in KiB via FUN_080ac06c), five in
      the copy loops FUN_081b9878 / FUN_081b979c (byte-count-derived
      progress into stages 5 and 6), plus one each at 0x0807fca0,
      0x0808781c, 0x080ac0b4. CLASS NOT NAMED: no name factory or
      registry id reachable from the ctor, so the symbol names the
      proven role. Deviations: operator_new and heap_panic called
      directly (both ported); the ctor rides the SINGLETON_CTORS
      dispatch table with a documented zeroing default (the zeroing IS
      faithful to the ctor's object writes but skips the keeper reset -
      NOT HOOK-READY, the family contract); the cache is the crate
      static STAGE_PROGRESS_TRACKER rather than 0x089ca5d4; the pushed
      r6 is an unused ADS frame artifact. match.py --lib <worktree
      archive> (exit-1 diff expected): same skeleton - cache-ptr load,
      ldr/cmp/bne test, `mov r0,#0x2c`, bl operator_new (R_ARM_CALL
      verified), ctor call, cmp/str, panic gate, reload, pop; deltas
      are the seam indirection (`ldr r1,[r0,#0x38]; blx r1` - slot
      offset 0x38 = the fifteenth SingletonCtors field), an fp
      prologue, and LLVM splitting the predicated `bleq heap_panic`
      into `beq` + a tail bl after the pop.

  # --- Registry-class-0x9300 lazy singleton ---
  - address: 0x081303a8
    ghidra: FUN_081303a8
    name: singleton_class_9300
    size: 44
    sig: "void *singleton_class_9300(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_081303d4: CLASS_9300_GLOBALS_POOL (the trailing pool word @
        0x081303d4 holding 0x089cc2b8; the cache is that global's +4 slot
        0x089cc2bc, the crate static CLASS_9300_INSTANCE, the block_mgr.rs
        RW-page deviation)
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_08130424: class_9300_ctor (SINGLETON_CTORS.class_9300 slot)
    notes: >-
      The sixteenth member of the app/singletons family, idiomatically
      identical to the other fifteen - `if (cache == 0) cache =
      ctor(operator_new(0x14c)); return cache;` over the +4 slot
      0x089cc2bc of the global 0x089cc2b8. EXTENT: functions.csv's 44
      bytes is the code only (10 instructions); the function owns one
      trailing literal-pool word (0x089cc2b8 @ 0x081303d4, reached by
      `ldr r4, [pc, #32]`), so the true extent is 48 bytes,
      0x081303a8..0x081303d8, where the next function opens
      `push {r4, r5, r6, lr}`. Ghidra also emits NO C file for this
      getter - its listing is silently incomplete here. CALL COUNT:
      binary-verified by decoding every B/BL word in
      work/firmware/osos.dec: exactly 28 unconditional bl call sites, 0
      predicated, 0 plain b; callers fetch the object and hand it to
      its own method cluster sitting immediately around the getter
      (FUN_0812f754 / FUN_0812fda0 / FUN_0812fb64 / FUN_0812ff28 /
      FUN_08130134), e.g. the menu handler @ 0x081ca278 which polls
      FUN_0812f754, runs FUN_08130134 when it answers non-zero, and
      gates a command-dispatch pair on FUN_0812fb64(obj, 1). OBJECT
      IDENTIFIED BY REGISTRY ID: the ctor 0x08130424 (192 bytes,
      0x08130424..0x081304e4, followed by the NULL-guarded deleting
      destructor @ 0x081304f0) runs base ctor FUN_08272474(this, 0),
      plants primary vtable 0x089841e4 at +0, builds the embedded cxx
      observable_array at +0x18 (0x08271cec, ported), plants secondary
      vtable 0x089a4b48 at +0x18, sets byte +0x28=1 and word +0x2c=0,
      zeroes byte +0x70 and word +0x74, runs FUN_0839ebc4 at +0x78, the
      ported scoped_context_construct 0x08270394 at +0x7c and the
      ported pair_header_base_construct 0x0810ebbc at +0x94, then
      registers `this` under id 0x9300 (`mov r1,#0x9300; bl 0x081d23f8`
      @ 0x0813048c, the ported registry_register). It finishes by
      wiring an embedded timer: timer_schedule_shim 0x0811108c(this,
      this+0x44, 0, 0) then timer_start_after 0x0812c63c(this+0x44,
      2000) arms a 2000 ms timer at +0x44 (both ported), followed by
      FUN_0812f6ec(this) and a stack-temp copy into +0x78. Class NOT
      named: no name factory is reachable from the ctor, so the symbol
      carries the registry id only, per the singleton_class_8900
      precedent. Deviations: operator_new called directly; the ctor
      rides the SINGLETON_CTORS dispatch table with a documented
      zeroing default (NOT HOOK-READY, the family contract); the cache
      is the crate static CLASS_9300_INSTANCE rather than the word @
      0x089cc2bc. match.py --lib <worktree archive> (exit-1 diff
      expected): same skeleton - cache-ptr load, ldr/cmp test,
      `mov r0,#0x14c`, bl to operator_new (R_ARM_CALL relocation
      verified), ctor call, store, reload, pop; deltas are the seam
      indirection (`ldr r5,[r0,#0x3c]; blx r5` - slot offset 0x3c = the
      sixteenth SingletonCtors field), an fp prologue and the inverted
      branch LLVM chose for the cold path. Host tests: exact 0x14c
      allocation + single construction, cache hit on repeat calls,
      pre-seeded cache never allocates, NULL-returning ctor caches NULL
      and re-allocates every call, ctor-poked slot is overwritten by
      the ctor-return store, zeroing stub clears exactly 0x14c bytes
      and is NULL-safe, cache independence from the neighbouring
      singletons.

  # --- UI animation three-value rebinder ---
  - address: 0x08166a40
    ghidra: FUN_08166a40
    name: animation_set_values
    size: 172
    sig: "void animation_set_values(Animation *this, FixedValue *current_value, FixedValue *from, FixedValue *to)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: current_value
      param_3/r2: from
      param_4/r3: to
      DAT_08166aec: SCHEDULER_SINGLETON_GLOBAL (pool word 0x089cc7e0)
      FUN_082738e0: timing_wheel_remove (ported in app/animation)
      FUN_08273a14: retain_value (ported in app/refcounted_value)
      FUN_082739e0: release_refcounted_value (ported in app/refcounted_value)
      FUN_08273898: wheel_insert (unported, existing ANIMATION_INIT_OPS seam)
    notes: >-
      The true code extent is the 43 instruction words
      0x08166a40..0x08166ae8 (172 bytes); its separately linked pool word
      at 0x08166aec holds 0x089cc7e0, and the next real function begins at
      0x08166af0. Decoding every ARM B/BL word in osos.dec finds exactly 18
      direct call sites, all plain unconditional bl: no predicated BL forms
      or raw DATA-word references. Three additional unconditional tail B
      callers target it at 0x081675a4, 0x081b836c, and 0x081b8c1c. This
      rebinder removes this from the timing wheel, retains current/from/to,
      conditionally releases the old +0x20/+0x18/+0x1c values in that
      order, stores from/to/current with stm at +0x18, sets rank +0x08 to
      max(from->aux, to->aux, current->aux) + 1 with unsigned comparisons
      and wrapping addition, then reloads the scheduler global and
      tail-branches to wheel_insert. FixedValue arguments have no NULL
      guard: each +0x08 load is unconditional. timing_wheel_remove is now a
      direct port; wheel_insert remains the volatile ANIMATION_INIT_OPS seam,
      and retain and release are direct ports.

  # --- UI animation current-value setter ---
  - address: 0x08166b2c
    ghidra: FUN_08166b2c
    name: animation_set_current_value
    size: 88
    sig: "void animation_set_current_value(Animation *this, FixedValue *value)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: value
      DAT_08166b84: SCHEDULER_SINGLETON_GLOBAL (pool word 0x089cc7e0)
      FUN_082738e0: timing_wheel_remove (ported in app/animation)
      FUN_08273a14: retain_value (ported in app/refcounted_value)
      FUN_082739e0: release_refcounted_value (ported in app/refcounted_value)
      FUN_08273898: wheel_insert (unported, ANIMATION_INIT_OPS seam)
    notes: >-
      True extent is the 22 instruction words 0x08166b2c..0x08166b80
      (88 bytes); the separately linked 4-byte literal pool at
      0x08166b84 holds 0x089cc7e0, and the next function starts at
      0x08166b88. Decoding every ARM B/BL word in osos.dec finds exactly
      30 call sites, all plain unconditional bl: no predicated caller
      forms, B tail branches, or raw DATA-word references. The setter
      removes this from the timing wheel, retains value, conditionally
      releases the previous +0x20 current-value slot, stores value, and
      updates +0x08 only if its unsigned rank is below value->aux + 1
      (the ARM add wraps), then reloads the scheduler global and
      tail-branches to wheel_insert. value has no NULL guard: the stock
      ldr [value, #8] is unconditional. timing_wheel_remove, retain, and
      release are direct ports; wheel_insert remains the ANIMATION_INIT_OPS
      seam. Remove and insert each reload the scheduler-global word,
      matching the two original ldr instructions.

  # --- UI animation constructor over three refcounted scalars ---
  - address: 0x08166b88
    ghidra: FUN_08166b88
    name: animation_init
    size: 212
    sig: "Animation *animation_init(Animation *this, FixedValue *current_value, FixedValue *from_value, FixedValue *to_value)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: current_value
      param_3/r2: from_value
      param_4/r3: to_value
      DAT_08166c54: ANIMATION_VTABLE (pool word 0x08987f00)
      DAT_08166c58: SCHEDULER_SINGLETON_GLOBAL (word 0x089cc7e0)
      FUN_08138460: refcounted_base_init (direct, ported in app/fixed_value)
      FUN_08273a14: retain_value (ported in app/refcounted_value)
      FUN_082739e0: release_refcounted_value (ported in app/refcounted_value)
      FUN_082738e0: timing_wheel_remove (ported in app/animation)
      FUN_08273898: wheel_insert (unported, ops-seam default)
    notes: >-
      Full constructor of the UI animation object that binds three
      refcounted Q16.16 scalars (animated value + from/to endpoints) and
      enqueues itself in the firmware timing wheel. Callers build it as
      animation_init(operator_new(0x24) @ 0x082aadd4, current, from, to);
      e.g. 0x08180a54 feeds two fresh fixed_value_init(x << 16) scalars
      plus the view's stored property slot at +0x30. EXTENT: true size is
      212 bytes, not Ghidra's 204 - fifty instruction words run
      0x08166b88..0x08166c50 and the trailing pool holds TWO words (the
      animation vtable 0x08987f00 @ 0x08166c54 and the scheduler pointer
      word 0x089cc7e0 @ 0x08166c58); Ghidra dropped the last pool word,
      and the next real function opens push {lr} at 0x08166c5c (the
      sibling default ctor, same vtable, never touches the wheel). The
      preceding setter 0x08166b2c swaps slot +0x20 and raises-but-never-
      lowers this->rank, confirming the field map. CALL COUNT:
      binary-verified by decoding every B/BL word in osos.dec - exactly
      44 call sites, all plain unconditional bl, no predicated forms, no
      b tail branches, no DATA references; callers invoke the ctor
      directly on fresh storage with no NULL guard. ALGORITHM:
      refcounted_base_init(this) @ 0x08138460, override vtable with
      0x08987f00, zero slots +0x18/+0x1c/+0x20, defensive
      wheel_remove([0x089cc7e0], this), retain(current)/retain(from)/
      retain(to) in that order, then the ADS memberwise-assign releases
      of the old slot values checked +0x20/+0x18/+0x1c order - all three
      statically dead because the slots were just cleared, reproduced for
      structure parity and pinned by a test; store stm {from,to,current}
      at +0x18..+0x20, rank = max(from.aux, to.aux, current.aux) + 1
      stored at +0x08 (unsigned selects movls/movhi, wraps on aux
      0xffffffff; plain scalars carry aux 0 so such an animation lands at
      rank 1 = bucket 0), wheel_insert(table, this), return this. THE
      WHEEL: insert 0x08273898 buckets a node at table[rank - 1], threads
      the doubly-linked list through +0x0c/+0x10 and sets linked flag bit
      0 of byte +0x14; remove 0x082738e0 unlinks only when that flag is
      set (hence the defensive no-op here); both take the table word
      stored at global 0x089cc7e0, which the lazy allocator 0x082739a0
      fills with operator_new(0x34) whose first 48 bytes are 12 bucket
      heads. retain 0x08273a14 is flags-bit-1-gated += 4; release
      0x082739e0 is flags-bit-1-gated -= 4 on the +0x14 word, dispatching
      the deleting destructor through vtable +0x04 when the count drains.
      timing_wheel_remove is now a direct port. The remaining wheel_insert
      callee uses the ANIMATION_INIT_OPS read_volatile seam
      (pending_event_take pattern): target default transmutes the ROM
      address; host default is inert; the scheduler word is loaded once
      instead of twice (nothing between the two stock loads can change it).
      The stale 0x0898xxxx page caveat applies to the vtable (address
      constant only); LLVM emits an fp prologue, direct call for removal,
      inverted beq+blx forms for the three dead releases and an indirect
      insertion seam call. match.py --lib <worktree>: identical skeleton,
      63 instructions vs 51.

  # --- UI animation default constructor ---
  - address: 0x08166c5c
    ghidra: FUN_08166c5c
    name: animation_default_init
    size: 40
    sig: "Animation *animation_default_init(Animation *this)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: this
      DAT_08166c80: ANIMATION_VTABLE (pool word 0x08987f00)
      FUN_08138460: refcounted_base_init (direct, ported in app/fixed_value)
    notes: >-
      Ghidra's 36-byte body is nine instruction words from
      0x08166c5c..0x08166c7c; its separately linked pool word at
      0x08166c80 stores the animation vtable 0x08987f00, yielding a true
      40-byte extent through 0x08166c83. The next real function begins at
      0x08166c84. Decoding every ARM B/BL word in osos.dec finds exactly
      ten direct callers, all plain unconditional bl: six at
      0x08153198..0x081531c0 and four at 0x0816e3bc..0x0816e3d4. There
      are no predicated BL forms, B tail callers, or raw DATA-word
      references. Each caller uses the returned pointer as its next array
      cursor, correcting Ghidra's void signature. The constructor calls
      refcounted_base_init, replaces the base vtable with ANIMATION_VTABLE,
      and clears only the from/to/current value slots at +0x18/+0x1c/+0x20;
      rank, wheel links, and opaque +0x04 remain untouched. The base call
      and all subsequent stores preserve r0, so it returns this. No
      deliberate deviations; refcounted_base_init is a direct port.

  # --- UI animation non-deleting destructor ---
  - address: 0x08166c9c
    ghidra: FUN_08166c9c
    name: animation_destroy
    size: 68
    sig: "Animation *animation_destroy(Animation *this)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: this
      DAT_08166cdc: ANIMATION_VTABLE (pool word 0x08987f00)
      FUN_082739e0: release_refcounted_value (direct, ported in app/refcounted_value)
      FUN_081384a0: refcounted_base_destroy (tail branch, ported in app/fixed_value)
    notes: >-
      The non-deleting destructor of the UI animation object, the exact
      inverse of animation_init @ 0x08166b88. EXTENT: Ghidra's 64 bytes
      are the sixteen instruction words 0x08166c9c..0x08166cdb; the
      separately linked literal pool holds ONE word (the animation vtable
      0x08987f00 @ 0x08166cdc), true extent 68 bytes. The next function
      opens bx lr at 0x08166ce0 (a lone-instruction empty body, probably
      a trivial destructor), then a new function at 0x08166ce4. GHIDRA
      ERRORS: the osos.asm listing shows stale bytes for this address
      (a phantom stmdb/cmp body that matches nothing in osos.dec - raw
      bytes verified instead), and its C decompile runs THROUGH the tail
      branch into refcounted_base_destroy, mislabeling that callee
      FUN_082738e0 and inventing a *DAT_081384cc first argument; the
      real epilogue is mov r0, r4 ; pop {r4, lr} ; b 0x081384a0.
      CALL COUNT: binary-verified by decoding every B/BL word in
      osos.dec - exactly 14 call sites, all plain unconditional bl, no
      predicated forms, no b tail callers, no DATA-word references (the
      class vtable instead points at the deleting destructor @
      0x08166c84, which NULL-guards this, calls this body, then
      operator-deletes @ 0x082aad24; it shares the vtable literal @
      0x08166c80). Ten of the 14 sit in two array-destructor walks with
      sub r0, r0, #0x24 between calls (0x08153260..0x08153288, six
      calls; 0x0816760c..0x08167624, four calls) - 0x24 is
      sizeof(Animation); the callers at 0x081a17e0 and 0x081eb018 feed
      the returned this pointer onward as their walk cursor, so the
      return value is load bearing (the port returns this, never void).
      ALGORITHM: reinstall the derived vtable 0x08987f00 at +0x00 (the
      classic two-phase C++ teardown store), release the three retained
      endpoint values in slot order current (+0x20), from (+0x18), to
      (+0x1c) - each behind its own NULL check, the ADS blne idiom
      matching release_refcounted_value's guard-less callee - then
      tail-branch into the ported refcounted_base_destroy, which
      installs the base vtable 0x08984ca0, unlinks the node from the
      timing wheel, and returns this. The slots keep their (now
      released) pointer values; the body never clears them. Both
      callees are direct ports, no seam. Deviations: the intermediate
      derived-vtable store is dead (the base destructor overwrites it
      and nothing can observe the word in between), so like
      animation_init's statically dead releases the compiler stays free
      to keep or drop it. match.py --lib <worktree> --size 64:
      identical skeleton, 16 original instructions vs 20 Rust - LLVM
      adds the fp prologue and emits inverted beq+bl for the first two
      guarded releases; the 0x08987f00 pool word is emitted and the
      symbol resolves standalone in the archive (no folding).

  # --- UI two-value wheel-node constructor (driver + step scalar) ---
  - address: 0x08167a6c
    ghidra: FUN_08167a6c
    name: timer_step_value_init
    size: 152
    sig: "TimerStepValue *timer_step_value_init(TimerStepValue *this, FixedValue *driver, FixedValue *step)"
    module: app/timer_step_value
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: driver
      param_3/r2: step
      DAT_08167afc: TIMER_STEP_VALUE_VTABLE (pool word 0x0898806c)
      DAT_08167b00: SCHEDULER_SINGLETON_GLOBAL (word 0x089cc7e0)
      FUN_08138460: refcounted_base_init (direct, ported in app/fixed_value)
      FUN_08273a14: retain_value (ported in app/refcounted_value)
      FUN_082739e0: release_refcounted_value (ported in app/refcounted_value)
      FUN_08273a40: value_aux_max (direct, ported in app/fixed_value)
      FUN_082738e0: timing_wheel_remove (direct, ported in app/animation)
      FUN_08273898: wheel_insert (unported, TIMER_STEP_VALUE_OPS seam)
    notes: >-
      Constructor of the two-value timing-wheel node that couples a
      retained driver value with a retained step scalar and is then used
      as the animated current value of a subsequent animation_init.
      Every observed caller builds it as
      timer_step_value_init(operator_new(0x20) @ 0x082aadd4, driver,
      step); the driver argument is timer-like in all 14 callers (the
      view's TimedTransition at +0x11c - the same pointer handed to
      timed_transition_init - or the 0x34-byte periodic timer built at
      0x08224a9c), the step a fresh fixed_value_init(x << 16) scalar
      (usually 1.0). EXTENT: Ghidra's 144 bytes cover only the
      instruction words 0x08167a6c..0x08167afc (pop {r4-r8,pc}); the
      trailing pool holds TWO words (class vtable 0x0898806c @
      0x08167afc, scheduler pointer word 0x089cc7e0 @ 0x08167b00), true
      extent 152 bytes; the next real function opens cmp r0,#0 at
      0x08167b04 (the class's deleting destructor, sharing the vtable
      literal; its non-deleting sibling 0x08167b1c releases +0x1c then
      +0x18 and tail-branches to refcounted_base_destroy). CALL COUNT:
      binary-verified by decoding every B/BL word in osos.dec - exactly
      14 call sites (0x08156b68, 0x08156c44, 0x08156d48, 0x08156df8,
      0x08199da8, 0x08199e04, 0x08199efc, 0x08199fa4, 0x0819a0fc,
      0x0819a168, 0x0819a26c, 0x0819a330, 0x08224b08, 0x08224b38), all
      plain unconditional bl, no predicated forms, no B tail branches,
      no DATA references. ALGORITHM: refcounted_base_init(this),
      override vtable with 0x0898806c, clear slots +0x18/+0x1c,
      defensive timing_wheel_remove([0x089cc7e0], this), retain(driver)
      then retain(step), the ADS memberwise-assign releases of the old
      slot values checked +0x1c then +0x18 - both statically dead
      because the slots were just cleared, reproduced for structure
      parity and pinned by a test - store driver -> +0x1c and step ->
      +0x18, rank = value_aux_max(step, driver) + 1 (unsigned max of the
      two +0x08 aux words, wrapping add) at +0x08, wheel_insert(table,
      this), return this. FAMILY: byte-identical sibling constructors
      differ only in the vtable literal - 0x081977c4 (vtable
      0x08989b48) is word-for-word the same body; 0x081679d8 /
      0x8181060 / 0x8197744 share the aux-max leaf. The vtable lives on
      the stale 0x0898xxxx page (app/registry.rs caveat; its bytes there
      are RTTI strings), so it is an address constant only. Deviations:
      wheel insert dispatches through the TIMER_STEP_VALUE_OPS
      read_volatile seam (target transmutes 0x08273898, host default
      inert); the scheduler word is loaded once instead of twice
      (nothing between the two stock loads can change it). match.py
      --lib (worktree archive): identical skeleton, 45 instructions vs
      36 - LLVM adds the crate fp prologue, loads the ops slot
      volatiles early, inverts the first dead release into beq+bl, and
      calls the insert seam with blx.

  - address: 0x0826d64c
    ghidra: FUN_0826d64c
    name: task_registry_register
    size: 156
    sig: "void task_registry_register(u32 class_id, u8 *instance)"
    module: app/task_registry
    status: ported
    vars: {param_1: class_id, param_2: instance, piVar3: registry, iVar1: query_result, uVar2: observer}
    notes: >-
      The per-task class-registry registering writer: registers the
      (class_id, instance) pair in the current task's ctx+0x38 map,
      lazily constructing that registry on first use. Extent verified
      against raw bytes (pop {r0..r6,pc} @ 0x0826d6e4, the sibling
      FUN_0826d6e8 prologue starts at 0x0826d6e8); Ghidra's 156 is
      exact. 43 bl call sites binary-scanned over osos.dec, 0
      predicated forms. Raw ARM: ctx = current_task_ctx_block()
      (0x080cb828, ported); registry = ctx->+0x38 (the TaskCtx field
      this port names); if NULL, registry = registry_container_construct
      (operator_new(0x28), 8, 4) (0x08135308, ported), observer =
      FUN_081f6b14(operator_new(8)), attach via registry vtable slot
      +0x54, then ctx RE-FETCHED and the result stored at +0x38 (a task
      switch mid-construction installs on the current task). Then entry
      = the spilled r0/r1 pair itself (add r1, sp, #8 addresses the push
      slots), dispatched to vtable slot +0x48 with the literal operand 4
      (mov r2, #4); a -1 result (cmn r0, #1) gates slot +0x1c insert of
      {entry.class_id RE-READ through the query-writable pointer
      (ldr r0, [sp, #8]), instance from the register copy r5}. Slot
      roles +0x48 (query)/+0x54 (attach_observer) are inferred - the
      cold image's container vtable page 0x08984770 is 0x55-fill (the
      registry.rs caveat); +0x1c is the known insert. No NULL guard on
      the ctx pointer or the allocations; the vtable is reloaded per
      dispatch. Ghidra's undefined8 return is the restored spilled pair,
      not a result - the function returns void. The observer ctor
      FUN_081f6b14 @ 0x081f6b14 (24 bytes incl. its literal word
      0x0899073c @ 0x081f6b28; exactly 1 bl call site - this function)
      is NOT ported; it sits behind the TASK_REGISTRY_OPS seam
      (CLASS_REGISTRY_OPS pattern) with a faithful default built on the
      ported registry_observer_base_construct (0x0810dddc) plus the
      derived vtable store. current_task_ctx_block and
      registry_container_construct are the ported wired defaults;
      operator_new (0x082aadd4, ported) is called directly. match.py
      --lib <worktree>: identical skeleton, 51 instructions vs 39; the
      deltas are the ops-table ldrd/blx indirection, the crate fp
      prologue, and archive bl relocations.
  # --- vector<T>::push_back, 12-byte element ---
  - address: 0x0816caa4
    ghidra: FUN_0816caa4
    name: vector_push_back_elem12
    size: 64
    sig: "void vector_push_back_elem12(VectorStorage *vector, u32 word0, u32 word1, u32 word2)"
    module: cxx/templates
    status: ported
    renames:
      param_1/r0: vector
      param_2/r1: word0
      param_3/r2: word1
      param_4/r3: word2
      FUN_083e12dc: insert_aux (unported, VECTOR_PUSH_BACK_ELEM12_OPS seam)
    notes: >-
      std::vector<T>::push_back for a 12-byte trivially-copyable element
      on the {begin, end, end_of_storage} VectorStorage head; the
      element arrives BY VALUE in r1/r2/r3. EXTENT: exactly 64 bytes -
      Ghidra's size is right this time; the next function opens push
      {r4, r5, r6, lr} at 0x0816cae4, and a byte-pattern scan of
      osos.dec finds no second instantiation (unique copy). CALL COUNT:
      verified by decoding every B/BL word in osos.dec - exactly 43
      sites, 38 plain bl plus 5 predicated forms (4 bleq @ 0x0821ec80,
      0x08222f98, 0x08226c18, 0x08239524 and 1 blne @ 0x0821ec58), each
      immediately after a `cmp r0, #0`: the CALLER skips the push when
      its preceding lookup returned NULL, the callee carries no such
      guard. ALGORITHM: push {r1, r2, r3, lr} spills the 12-byte element
      (the strb r3, [sp, #8] is redundant - sp+8 already holds FULL r3,
      so the third element word is the whole word2, not a byte); ldmib
      r0, {r1, r2} loads end (+4) and end_of_storage (+8); if equal
      (full) r2 = sp and bl 0x083e12dc (insert_aux(vector, end,
      &element), the grow-and-insert helper, UNPORTED - its own
      size/capacity/allocate callees vector_size_elem12 0x083d76f8,
      vector_capacity_elem12 0x083d7708 and operator_new_checked
      0x08266c70 are all ported); otherwise end += 12 stored back
      UNCONDITIONALLY (str before the guard), then subs r0, r1, #12
      skips the 12-byte element store when the OLD end is NULL (the ADS
      placement-new null check), leaving end = 12. Ghidra's C matches
      this decode; its puVar1 + 3 is the +12-byte advance in 4-byte
      units. OBSERVED USE: binds button handlers - callers push
      {command_id, handler_name, flag} records (e.g. FUN_082342b4 pushes
      {id, "HandleAddToOTG" + 1, 1}) into a stack vector handed to
      FUN_08134720; push_back interprets none of the fields. Deviations:
      insert_aux dispatches through the VECTOR_PUSH_BACK_ELEM12_OPS
      read_volatile seam (animation.rs pattern), target default
      transmutes 0x083e12dc, host default inert; head loads/stores are
      plain aligned word accesses (the original's ldmib/str - the
      family's read_unaligned host-fixture idiom would only pessimize
      ARMv5TE codegen into ldrb quads here, and every call site heads a
      word-aligned vector). match.py --lib <worktree archive>: same
      skeleton - ldmib head load, cmp/beq, +12 advance, str [r0,#4],
      predicated 3-word element store (LLVM emits strne + stmibne direct
      from registers instead of the original's ldmne/stmne stack
      round-trip), slow path spills the element and blx through the ops
      slot; deltas are the fp prologue and the seam indirection, the
      accepted animation.rs deviation classes. 24 instructions vs 16.

  # --- handler-list constructor ---
  - address: 0x0816cbfc
    ghidra: FUN_0816cbfc
    name: handler_list_construct
    size: 32
    sig: "HandlerList *handler_list_construct(HandlerList *list, u32 mode)"
    module: cxx/handler_list_construct
    status: ported
    renames:
      param_1/r0: list
      param_2/r1: mode
      param_1_plus_0x00: handlers.begin
      param_1_plus_0x04: handlers.end
      param_1_plus_0x08: handlers.end_of_storage
      param_1_plus_0x0c: mode
      param_1_plus_0x10: state
      param_1_plus_0x14: state_flag
      FUN_083e143c: three_word_clear_third
    notes: >-
      Raw ARM extent is exactly 32 bytes: push {r3,r4,r5,lr} at 0x0816cbfc
      through pop {r3,r4,r5,pc} at 0x0816cc1c; the independent vector
      destructor begins at 0x0816cc20, so Ghidra's reported 36-byte extent
      consumes its first word. Decoding every ARM B/BL immediate in osos.dec
      finds exactly 15 direct callers, all unconditional plain bl and zero
      predicated forms or plain-B tail calls. The function calls the already
      ported three_word_clear_third at 0x083e143c to clear the vector head
      (+0/+4/+8), stores the low mode byte at +12, and clears the state word
      and flag at +16/+20. The helper preserves r0 and the constructor returns
      list unchanged despite Ghidra's void declaration; caller 0x081e8f14
      consumes that r0 result. Its other callers build command-handler records
      with vector_push_back_elem12 before handing the list to 0x08134720.
      No NULL guard or device-code deviations; host builds clear the three
      typed vector pointers separately because they are 8-byte pointers there.
      Source: decomp/c/015/0816cbfc_FUN_0816cbfc.c; raw disassembly: osos.dec
      0x0816cbfc; match.py --size 32 --lib <worktree archive> structural
      review: the helper call and all field writes remain in order; LLVM adds
      its frame/r5 preservation and passes the helper its actual one argument.

  # --- trivial twelve-byte vector destructor ---
  - address: 0x0816cc20
    ghidra: FUN_0816cc20
    name: trivial_vector12_destruct
    size: 72
    sig: "TrivialVector12 *trivial_vector12_destruct(TrivialVector12 *vector)"
    module: cxx/trivial_vector12_destruct
    status: ported
    renames:
      param_1/r0: vector
      param_1_plus_0x00: begin
      param_1_plus_0x04: end
      param_1_plus_0x08: capacity
      iVar1/r5: begin
      iVar2/r0: current
      uVar3/r0: capacity_element_count
      return/r0: vector
    notes: >-
      Raw ARM is exactly 72 bytes: its last instruction is pop {r4, r5, r6,
      pc} at 0x0816cc64, immediately before the next independent code. A
      complete decode of every ARM B/BL immediate in osos.dec finds exactly
      15 direct callers, all unconditional bl and zero predicated forms. It
      walks the half-open [begin, end) range by 12 bytes with an empty body
      because the element destructor is trivial; then it computes the signed
      quotient (capacity - begin) / 12 through __rt_sdiv, passes begin,
      quotient, and zero to cxx_array_dealloc (0x08266f2c), and returns the
      descriptor unchanged. cxx_array_dealloc is already ported and tail
      branches to operator_delete, so its count and element arguments are
      dead but retained by this port. Host tests cover empty, one-, two-, and
      three-element walks with spare capacity; a recording deallocator proves
      the count/zero argument tuple and guard words prove no descriptor write.
      Source: decomp/c/015/0816cc20_FUN_0816cc20.c; raw disassembly:
      osos.dec 0x0816cc20. Deliberate codegen-only deviation: black_box keeps
      the original signed divide live although its result is dead in the
      ported deallocator; LLVM may lower that retained arithmetic differently.

  - address: 0x082579a0
    ghidra: FUN_082579a0
    name: event_hub_instance_get
    size: 48
    sig: "void *event_hub_instance_get(void)"
    module: app/event_hub
    status: ported
    renames:
      DAT_082579cc: EVENT_HUB_SINGLETON_CACHE (literal-pool word holding
        0x089cca00)
      DAT_089cca00: event_hub singleton pointer cache
      FUN_082aadd4: operator_new (direct, ported in heap/veneers)
      FUN_08257b4c: event_hub listener-list construction adapter (unported
        local tail adapter to cpp_array_construct)
      FUN_082ab234: cpp_array_construct (direct, ported in
        runtime/cpp_array_construct)
      DAT_0802df58: raw element-constructor callback word (not a verified
        function entry)
    notes: >-
      Raw ARM is 48 bytes: 44 instruction bytes from 0x082579a0 through the
      pop {r4, pc} at 0x082579c8, followed by the 0x089cca00 literal-pool
      word at 0x082579cc; the next independent function starts at
      0x082579d0. A complete decode of every ARM B/BL immediate in osos.dec
      finds exactly 11 incoming calls, all unconditional bl, with zero
      predicated bl forms and zero tail branches. It loads the cache at
      0x089cca00; if NULL, it calls operator_new(0x9c), calls the local
      adapter 0x08257b4c, stores that call's return to the cache, then
      reloads and returns it. The local adapter tail-branches to the ported
      cpp_array_construct with raw arguments (storage, 0x0802df58, 12, 13).
      Raw decoding proves 0x0802df58 lies within code rather than at a
      verified function entry, so this port carries it as data and does not
      invent a callee identity. Deliberate deviation: it invokes the already
      ported cpp_array_construct directly rather than reproducing the local
      adapter's argument shuffles and tail branch. Host tests prove the
      allocation size/tag, all eleven generic-array-helper arguments, cached
      constructor return, and the no-NULL-guard path.

  - address: 0x08257aec
    ghidra: FUN_08257aec
    name: event_hub_subscribe
    size: 96
    sig: "u32 event_hub_subscribe(EventHub *hub, void *listener, u32 kind)"
    module: app/event_hub
    status: ported
    renames:
      param_1/r0: hub
      param_2/r1: listener
      param_3/r2: kind (listener-list selector; no range check)
      puVar4/r4: hub.listener_lists[kind] ({begin, end, end_of_storage})
      local_14/[sp,#4]: listener_slot (address passed to insertion)
      thunk_FUN_083ea6dc: pointer-range equality scan veneer (tail-branches
        to FUN_083ea6dc)
      FUN_083e52dc: unported vector pointer insertion/reallocation helper
    notes: >-
      Raw ARM is exactly 96 bytes: push {r0,r1,r2,r4,r5,lr} at 0x08257aec
      through pop {r1,r2,r3,r4,r5,pc} at 0x08257b48; the independent
      16-byte array-construction adapter starts at 0x08257b4c. A complete
      decode of every ARM B/BL immediate in work/firmware/osos.dec finds
      exactly 10 direct callers, all unconditional plain bl, with zero
      predicated bl forms and zero plain-B tail callers. It indexes thirteen
      12-byte listener vectors by kind, calls the 4-byte veneer at
      0x083ea6d4 to scan [begin,end) for the exact listener pointer, and
      returns 0 if present. On absence it passes the vector, its original
      end pointer, and a stack copy of listener to FUN_083e52dc, then returns
      1. It has no NULL, kind-range, or capacity guard before its raw reads.
      Deliberate deviation: the port inlines the verified pointer-equality
      scan instead of calling the unported veneer; its target build retains
      the real insertion/reallocation call at 0x083e52dc through a volatile
      seam, while host tests install a concrete insertion model. Host tests
      prove duplicate suppression, selected-list/end-pointer forwarding,
      append placement, return values, and NULL-listener forwarding from an
      empty list. match.py structural review is expected to differ in the
      inlined scan and indirect seam call.

  - address: 0x08257b60
    ghidra: FUN_08257b60
    name: event_hub_broadcast
    size: 100
    sig: "void event_hub_broadcast(u32 kind, u32 arg, usize payload, u32 payload_len)"
    module: app/event_hub
    status: ported
    renames:
      param_1: kind (listener-list selector 0..12, also forwarded to each
        listener callback)
      param_2: arg
      param_3: payload (NOT always a pointer - call sites pass small
        immediates, e.g. (8, 0, 0xd, 4); typed usize, never dereferenced)
      param_4: payload_len
      DAT_08257bc0: EVENT_HUB_STATIC_POOL (the trailing pool word holding
        0x089cca00; the guard word 0x089cca04 is the crate static
        EVENT_HUB_GUARD and the instance cache 0x089cca08 is
        EVENT_HUB_INSTANCE, the media_command_facade.rs deviation)
      FUN_082ab31c: cxa_guard_acquire (direct, ported in runtime/cxa_guard)
      FUN_082ab338: cxa_guard_release (direct, ported in runtime/cxa_guard)
      FUN_082579a0: event_hub_instance_get (direct, ported in app/event_hub)
      FUN_082579d0: event_hub_dispatch (EVENT_HUB_OPS.dispatch seam - not
        yet ported)
    notes: >-
      The single public entry into retailOS's process-wide event hub:
      thirteen listener lists, one per event kind. An ADS function-local
      static over a CACHED POINTER: guard @ 0x089cca04, instance copy @
      0x089cca08, while the getter 0x082579a0 keeps its own cache @
      0x089cca00 (it allocates 0x9c = 13*12 bytes - thirteen
      begin/end/capacity vector triplets - via the ported operator_new and
      its local adapter 0x08257b4c, which tail-branches to the ported
      cpp_array_construct with raw callback word 0x0802df58).
      cxa_guard_acquire; instance = getter(); store; cxa_guard_release;
      then ALWAYS reload the cache (ldr r0,[r4,#8] @ 0x08257ba8, never
      the init return) and tail-call the dispatch 0x082579d0(hub, kind,
      arg, payload, payload_len) with the 5th argument on the stack (str
      r8,[sp]). The dispatch walks list `kind` invoking vtable slot +0x04
      of each listener, and the sibling 0x08257aec is the matching
      vector-push_back subscribe. EXTENT CORRECTED: Ghidra's 96 bytes is
      the code only; the function owns one trailing literal-pool word
      (0x089cca00 @ 0x08257bc0) and the next function opens at
      0x08257bc4 (an 8-byte `ldr r0,[r0,#12]; bx lr` getter) - true
      extent 0x08257b60..0x08257bc4, 100 bytes. 42 bl call sites,
      binary-verified by decoding every B/BL word in osos.dec: 38 plain,
      4 predicated (blne @ 0x0818f1f8 / 0x08192d58, bleq @ 0x08192420 /
      0x081931ac); the predicated sites gate on the CALLER's own
      precondition (a range check, NULL checks), never on hub state - the
      callee has no NULL guard on the hub and forwards a NULL instance to
      the dispatch untouched. Ghidra decorates two call sites with
      phantom 5th/6th arguments; the binary proves only r0-r3 are
      consumed. Deviations: both callees are unported and ride the
      EVENT_HUB_OPS seam with inert defaults (NULL getter, no-op
      dispatch) - NOT hook-ready until they are ported; guard/cache are
      crate statics rather than the .bss words. Host tests pin the
      acquire-before-getter ordering, the always-reload (a re-entrant
      broadcast from inside the getter dispatches the not-yet-stored
      NULL, exactly like the original), the refused-acquire path (a
      nonzero guard with bit 0 clear skips init AND release and rides the
      stale cache), and verbatim four-word forwarding including
      immediate-valued payloads. match.py --lib <worktree archive>: same
      skeleton - guard load/test, cold init block (getter blx, store),
      cache reload, arg shuffle, `str r7,[sp]` stack arg, dispatch blx;
      deltas are LLVM inlining the ported guard pair (acquire's
      whole-word test subsumes the tst #1 fast path - net behavior
      identical, the guard==2 case still skips init), an fp prologue,
      `ldrd` pair-loads of the two-field ops table, and the seam
      indirection. 29 instructions vs 24.
  - address: 0x080377d8
    ghidra: FUN_080377d8
    name: highest_set_bit
    size: 12
    sig: "int highest_set_bit(u32 value)"
    module: util/highest_set_bit
    status: ported
    renames:
      param_1/r0: value
    notes: >-
      31 - clz(x): the 0-based index of the most-significant set bit, or -1
      when no bit is set (clz(0) = 32) — integer floor(log2) for nonzero
      inputs with a defined -1 for zero. Instruction-faithful: leading_zeros
      lowers to ARM clz, matching the original `clz r0,r0; rsb r0,r0,#0x1f;
      bx lr`. 5 bl call sites.

  - address: 0x08078400
    ghidra: FUN_08078400
    name: q15_mul
    size: 16
    sig: "int q15_mul(int a, int b)"
    module: util/q15_mul
    status: ported
    renames:
      param_1/r0: a
      param_2/r1: b
    notes: >-
      Signed Q15 fixed-point multiply `(a * b * 2) >> 16` from the audio
      interpolation path (callers weight a sample against a Q15 fraction and
      its 0x8000 complement). Evaluated with 32-bit wrapping: the doubling of
      the product overflows at the (-0x8000)^2 corner and arithmetic-shifts
      to -32768 rather than +32768, matching the original `mul r0,r1,r0;
      mov r0,r0,lsl #1; mov r0,r0,asr #0x10; bx lr`. 12 bl call sites.
  - address: 0x080789a8
    ghidra: FUN_080789a8
    name: skip_ascii_whitespace_and_comments
    size: 100
    sig: "void skip_ascii_whitespace_and_comments(u8 **cursor, const u8 *end)"
    module: util/skip_ascii_whitespace_and_comments
    status: ported
    renames:
      param_1/r0: cursor
      param_2/r1: end
      pcVar2/r2: current
      cVar1/r3: byte
    notes: >-
      Advances *cursor past ASCII space, CR, LF, tab, form feed, NUL, and
      percent-introduced comment lines. The comment scan stops at CR or LF,
      whose following outer-loop increment discards the terminator. A comment
      without either terminator deliberately leaves the cursor one byte beyond
      end: the decoded ARM executes add r2,r2,#1 after its inner end comparison.
      The function has no NULL guard. Every B/BL word in osos.dec was decoded:
      12 direct bl call sites, all unconditional (0x0809149c, 0x0809c370,
      0x080a938c, 0x080a9494, 0x080a94cc, 0x080a95a0, 0x080b8050,
      0x080cdab8, 0x080cdb48, 0x080d46d4, 0x080d472c, and 0x080d47b4);
      no predicated forms.
  - address: 0x083913b4
    ghidra: FUN_083913b4
    name: sqlite_snprintf
    size: 84
    sig: "char *sqlite_snprintf(int n, char *z_buf, const char *format, VaList args)"
    module: sqlite/snprintf
    status: ported
    renames:
      param_1/r0: n
      param_2/r1: z_buf
      param_3/r2: format
      param_4/r3: args
      FUN_08384e84: str_accum_init
      FUN_0839788c: sqlite3VXPrintf
      FUN_08384e14: str_accum_finish
    notes: >-
      SQLite 3.5.9's sqlite3_snprintf (util.c) - the bounded,
      never-allocating printf that renders into a caller buffer. Extent
      0x083913b4..0x08391408 confirmed from raw words: 21 instructions,
      no literal pool, sibling at 0x08391408 opens with its own
      push {r4,r5,r6,lr}. 41 bl call sites, binary-scanned: 40 plain
      bl plus 1 blne @ 0x0838f234 whose caller gates the whole call on
      its own flag (the callee carries no NULL guard and needs none).
      Algorithm - n <= 0 returns zBuf untouched (movle r0,r1; ble end;
      nothing written, engine never runs); otherwise the caller buffer
      IS the accumulator: sqlite3StrAccumInit @ 0x08384e84 (ported,
      sqlite::vdbe_op) plants zBuf as base+text with capacity n and
      mxAlloc 0, then an explicit strb at acc+0x15 forces useMalloc
      back to 0 (the init had set it), so the render truncates in
      place; sqlite3VXPrintf @ 0x0839788c (UNPORTED, 3324 bytes - the
      shared SQLITE_VXPRINTF seam in sqlite/vm_printf.rs, documented
      no-op default) runs with useMalloc = 0 over zFormat (reloaded
      from the spilled r2) and the variadic words at the spilled r3
      slot; sqlite3StrAccumFinish @ 0x08384e14 (ported,
      sqlite::str_accum) takes its no-transfer branch and returns zBuf
      itself. The va_list is the house explicit VaList pointer, exactly
      the &spilled-r3 the original builds with push {r0-r3}. Host tests
      (3) - n in {0, -1, i32::MIN} returns zBuf untouched with the
      engine never invoked; the engine observes (use_malloc=0, format,
      ap) and an accumulator with n_alloc=n / mx_alloc=0 /
      use_malloc=0, the finish returns zBuf verbatim with the text
      terminated in place and zero allocator traffic; the default
      no-op engine terminates zBuf[0] and writes nothing else.
      match.py (exit 1, diff expected): same shape - n<=0 early return
      (cmp #1/blt vs cmp #0/ble), StrAccum in frame, init with
      (acc, zBuf, n, 0), strb use_malloc=0 at acc+0x15, seam ldr/blx
      engine dispatch in place of the direct bl 0x0839788c, finish
      tail; deltas are the crate fp frame and register shuffling. 29
      instructions vs 21.

  # --- UTF-16 tokenizer: next-token step ---
  - address: 0x081613d8
    ghidra: FUN_081613d8
    name: tokenizer_next
    size: 120
    sig: "u32 *tokenizer_next(u32 *out, Tokenizer *state)"
    module: cxx/tokenizer
    status: ported
    renames:
      param_1: out
      param_2: state
      FUN_08161364: scan_token_end (ported as a private helper - its only
        call site in the image is the bl @ 0x0816141c inside this function)
      FUN_081bb6b8: empty-pair zero (inlined as two zero word stores)
      FUN_081bb6a4: range copy tail (inlined as two word stores - the
        shared 8-byte copy *out = *src, out[1] = src[1], decoded from its
        own bytes)
      FUN_08161464: tokenizer_init (constructor, ported, 17 bl sites)
      FUN_08161494: tokenizer_init_empty (constructor, unported, 1 bl site)
    notes: >-
      41 bl + 0 b + 0 predicated call sites, binary-verified by decoding
      every B/BL word in osos.dec. Ghidra's 120-byte extent is exact: the
      body ends in pop {r0-r3, r4, r5, r6, pc} @ 0x0816144c and the next
      function, a standalone 20-byte three-word copy reached only by a
      tail b from 0x081e8764, opens at 0x08161450 with ldm r1,{r2,r3};
      no trailing literal pool. The next-token step of retailOS's UTF-16
      string tokenizer over a 21-byte cursor object (begin/end u16 range
      at +0x00/+0x04, delimiter u16 at +0x08, cursor at +0x0c, signed
      countdown at +0x10, allow_quotes byte at +0x14) built by constructors
      @ 0x08161464 (ported tokenizer_init) / 0x08161494 (unported empty).
      NULL or cursor >= end (unsigned) writes the empty pair {0, 0} and
      leaves the object untouched; otherwise, when remaining < 1 (signed
      movlt/blt against 1) the token is {cursor, end} with NO scan and NO
      decrement; otherwise scan_token_end finds the first delimiter code
      unit that is neither inside a double-quoted region (only when the
      allow_quotes byte, re-read every non-quoted character, is nonzero)
      nor backslash-escaped, remaining is decremented (scan path only),
      and the token is {cursor, found}. Either way the cursor advances to
      stop + 2 and the pair is written through out; the pop {r0, ...}
      epilogue restores r0 to out, which the port returns. Inside quotes
      a backslash is INERT (only '"' toggles the region), and a scan that
      finds nothing returns end, so a trailing delimiter leaves the
      cursor sitting ON end and the next call reports exhaustion - no
      zero-width tail token is ever produced. remaining is a count of
      delimited scans, so a constructor remaining of N yields up to N+1
      tokens, the last absorbing the rest; the canonical caller
      0x081ba9ac passes remaining = 0x7fffffff and allow_quotes = 1 to
      split HH:MM:SS on ':' for the number parser @ 0x080f01c4. Ghidra's
      C drops the scan helper's arguments (FUN_08161364(param_2, uVar1))
      - the asm passes (state, cursor, end, delimiter) in r0-r3.
      Deviations: the two shared 8-byte helpers (0x081bb6b8 zero,
      0x081bb6a4 copy) are inlined per precedent; scan_token_end is a
      private fn (no seam - sole call site); pointer fields are u32
      firmware addresses in a #[repr(C)] Tokenizer with offset asserts.
      match.py structural (exit 1, diff expected): same NULL/end guards,
      same signed countdown (cmp #0/ble vs cmp #1/movlt), the scan loop
      inlined with its 0x22/0x5c/delimiter compares and quote/escape
      flags, decrement only on the scan path, cursor = stop + 2 only on
      the token path, {0, 0} empty pair skipping the cursor store;
      deltas are the crate fp frame and LLVM's clz/tst boolean
      arithmetic. Host tests (13): NULL and >= end cursors, the
      HH:MM:SS split idiom, countdown semantics (remaining = 0 and 2),
      quoted delimiters on/off, escaped delimiter, double backslash,
      inert backslash inside quotes, unterminated quote, trailing
      delimiter exhaustion, and the returned out pointer; fixtures in
      testing::hints::TOKENIZER (new), one process-wide slab shared via
      LazyLock because the mapper never unmaps.

  # --- UTF-16 tokenizer constructor (cxx/tokenizer) ---
  - address: 0x08161464
    ghidra: FUN_08161464
    name: tokenizer_init
    size: 48
    sig: "Tokenizer *tokenizer_init(Tokenizer *state, const u32 *range, u16 delimiter, i32 remaining, u8 allow_quotes)"
    module: cxx/tokenizer
    status: ported
    renames:
      param_1/r0/r0_return: state
      param_2/r1: range
      param_3/r2: delimiter
      param_4/r3: remaining
      param_5/[sp]: allow_quotes
      FUN_081bb6a4: pair_copy (inlined)
    notes: >-
      Raw ARM decoding confirms a 48-byte extent,
      0x08161464..0x08161494: push {r4,r5,r6,r7,r8,lr} opens the body and
      pop {r4,r5,r6,r7,r8,pc} ends at 0x08161490; the separately linked
      empty-range constructor starts at 0x08161494. 17 call sites verified
      by decoding every B/BL word in osos.dec: all are unconditional bl,
      with zero plain b or predicated forms. Algorithm: invoke the shared
      two-word copy at 0x081bb6a4 with (state, range), write the delimiter
      halfword at +0x08, seed cursor +0x0c from range.begin, then write the
      signed remaining count (+0x10) and quote flag byte (+0x14). Ghidra
      incorrectly gives a void signature: pair_copy leaves r0 as state and
      the pop epilogue returns that pointer. Deliberate deviation: the
      pair_copy leaf is inlined with volatile ordered loads/stores, pinning
      its destination-alias behavior without a seam for an already inlined
      helper. Host tests verify copied fields, returned state pointer,
      delimiter/tail padding, and the second source load after an aliased
      first destination store.
      match.py --lib worktree archive (exit 1, diff expected): same ordered
      pair copy, halfword delimiter, cursor reload, remaining word, and
      flag-byte stores; LLVM substitutes an fp frame and inlines the pair
      helper (13 instructions versus 12).

  # --- Managed two-word entry release (app/managed_entry) ---
  - address: 0x080645b8
    ghidra: FUN_080645b8
    name: managed_entry_release
    size: 76
    sig: "i32 managed_entry_release(ManagedEntryManager *manager, ManagedEntry *entry)"
    module: app/managed_entry
    status: ported
    renames:
      param_1/r0/r4: manager
      param_2/r1/r5: entry
      param_2_deref: entry_record
      param_1_plus_0x04/r0: release_context
      param_1_plus_0x40/r3: release_callback
      param_1_plus_0x50: release_count
      uVar1/r0: status
    notes: >-
      Verified 76 bytes (19 instructions) ending at 0x08064604, where the
      next function begins; Ghidra's extent and C agree with the raw bytes
      exactly. 40 call sites verified by decoding every B/BL word in
      osos.dec: all 40 are unconditional `bl`, zero predicated forms, so
      callers never gate the call on a flag themselves. Algorithm: if
      entry->record (+0x00) is non-NULL, invoke the manager's object-carried
      release callback (+0x40) as (manager->+0x04 context, entry, flags=0),
      bump the manager's +0x50 release counter, and keep the callback
      status; then unconditionally zero both entry words and return the
      status (0 for an already-empty slot). The trailing clear wins over
      anything the callback wrote into the slot mid-call. Sibling
      FUN_0806ce40 is the flagged variant: same callback with flags | 2,
      its own counter at +0x54, and it KEEPS the slot filled when the
      callback fails; 0x080645b8 is the unconditional error-path cleanup
      (callers such as 0x08044f20, 0x0805921c, 0x08068288 invoke it on
      failure edges). Records recovered from callers carry two big-endian
      u32 ids at +0x00/+0x04 (BE word helpers 0x08031140/0x08031160), a
      state byte at +0x08 (-1 special), a kind byte at +0x09, and a u16
      count at +0x0a; the owning subsystem is not recovered, so the name
      describes the mechanism only. The callback is an object-carried code
      word, not a fixed firmware address, so no dispatch seam was needed
      or added. Deviations: manager and entry are #[repr(C)] models with
      named fields replacing raw byte offsets (the crate's standing idiom
      for object-carried callbacks); every touched field is a single
      4-byte word, so the target layout is exact. match.py structural
      (exit 1, diff expected): identical shape — same +0x04/+0x40/+0x50
      offsets, empty-slot branch skipping callback and count, blx through
      the callback field, flags=0, unconditional two-word clear; deltas
      are LLVM register allocation and its sl/fp frame. Host tests (5):
      empty slot skips callback and counter yet clears both words, exact
      (context, entry, flags=0) forwarding with the slot observed still
      occupied inside the callback, error status propagating while the
      slot still clears and counts, callback writes overwritten by the
      trailing clear, and u32 counter wrap.

  - address: 0x0806ce40
    ghidra: FUN_0806ce40
    name: managed_entry_release_flagged
    size: 84
    sig: "i32 managed_entry_release_flagged(ManagedEntryManager *manager, ManagedEntry *entry, u32 unused, u32 flags)"
    module: app/managed_entry
    status: ported
    renames:
      param_1/r0/r4: manager
      param_2/r1/r5: entry
      param_2_deref: entry_record
      param_3/r2: unused
      param_4/r3/r0: flags
      param_1_plus_0x04/r0: release_context
      param_1_plus_0x40/r3: release_callback
      param_1_plus_0x54/r0: flagged_release_count
      iVar1/r0: status
    notes: >-
      Verified 84 bytes (21 instructions) ending at 0x0806ce94, where the
      next function begins; Ghidra's extent and C agree with the raw bytes
      exactly. 21 call sites verified by decoding every ARM B/BL word in
      osos.dec: all 21 are unconditional `bl`, with zero predicated forms
      and zero plain B branches. Algorithm: an empty entry is normalized to
      two zero words and returns 0 without a callback. For an occupied
      entry, call the manager's object-carried +0x40 release callback as
      (manager->+0x04 context, entry, flags | 2). A nonzero callback status
      returns immediately, preserving both entry words and the +0x54 count.
      On status 0, increment the +0x54 flagged release count with ARM u32
      wrapping semantics, clear both entry words, and return 0. The r2
      parameter is ignored; raw code copies r3 to r0 before ORRing bit 1.
      The object-carried callback is not a fixed firmware function, so no
      dispatch seam was needed or added. Deliberate deviation: named fields
      in the shared #[repr(C)] manager and entry replace raw offsets; every
      target field here is one 4-byte word. match.py structural (exit 1,
      diff expected): the same empty guard, callback `blx`, nonzero early
      return, +0x54 increment, and two-word clear; LLVM adds a frame
      pointer and emits a second zero move before the second store. Host
      tests (4) cover empty-slot normalization, successful flags | 2
      forwarding/count/clear, error preservation without a count, and count
      wrapping.

  # --- UI font handle query wrapper (ui/font_handle) ---
  - address: 0x082756fc
    ghidra: FUN_082756fc
    name: ui_font_handle_for_name
    size: 44
    sig: "void ui_font_handle_for_name(u64 *out, const u8 *name, u32 size, u32 style)"
    module: ui/font_handle
    status: ported
    renames:
      param_1/r0: out
      param_2/r1: name
      r2: size
      r3: style
      FUN_08275044: font_handle_resolver
    notes: >-
      Extent verified from raw bytes: 44 bytes, 0x082756fc..0x08275728, the
      sibling at 0x08275728 opening with `push {r3,r4,r5,r6,r7,lr}` - no
      swallowed trailing words. Decoding every ARM B/BL word in osos.dec
      found exactly 38 direct call sites, all unconditional `bl`, zero
      predicated forms and zero plain B branches (Ghidra's count of 38
      confirmed). Algorithm: zero an 8-byte stack local, and only when the
      font name in r1 is non-NULL call the resolver at 0x08275044 as
      (&local, name, size, style) - the `movne r0, sp; blne` pair leaves
      r2/r3 untouched, so the caller's point size (0xc / 0x14 / 6) and
      style selector (0 or 2) reach the resolver even though Ghidra drops
      them from the call; then store the local's two words to out
      unconditionally (`ldrd r0, [sp]; stm r4, {r0, r1}`), so a NULL name
      yields a zero handle. No NULL guard on out; the stm contract means
      out needs only 4-byte alignment. Identity: the 8-byte result is the
      current-font pair of the draw state - the setter at 0x0826421c
      copies it verbatim into the draw-state record at +0x20/+0x24 (the
      member zero-initialized by embedded_pair_construct) and derives the
      style byte at +0x28 from the first word; call-site names are fonts
      ("Helvetica" @ 0x08186a38, "MonoHope LCD" / "MonoHope TV" @
      0x081cde5c). The resolver at 0x08275044 (its own 108-byte function:
      hash, registry lookup via 0x0829bea4, register-on-miss via
      0x08275490, one-time init flag at 0x089cc8d4) remains unported
      behind a volatile typed boundary (FONT_HANDLE_RESOLVER); its r0:r1
      register return is discarded exactly as the original discards it in
      favor of the stack local. Deviation: blx through the seam instead of
      the stock `blne 0x08275044`. match.py cannot run from a sibling
      worktree (it hardcodes ../rustypod), so its exact steps were
      reproduced manually against the worktree archive: structurally
      identical - same zero-init pair, conditional call with r0 = sp,
      unconditional two-word stm to out; deltas are the crate sl/fp frame,
      blx-through-seam, and ldm/stm in place of ldrd/stm. Host tests (4):
      NULL name stores zero without calling the resolver, named font
      forwards (name, size, style) verbatim and stores the resolver's
      canned handle, empty string still reaches the resolver, and a
      4-byte-aligned (not 8) out is stored without touching guard words.

  # --- Platform C++ file read wrapper (fs/file_read) ---
  - address: 0x082784b8
    ghidra: FUN_082784b8
    name: retail_file_read
    size: 28
    sig: "i32 retail_file_read(void *handle, u32 count, u8 *buffer, u32 *transferred)"
    module: fs/file_read
    status: ported
    renames:
      param_1/r0: handle
      param_2/r1: count
      param_3/r2: buffer
      param_4/r3: transferred
      param_5/stack: control
    notes: >-
      Verified as exactly 28 bytes (seven ARM words) by raw disassembly:
      0x082784d4 begins a distinct 740-byte function immediately after the
      wrapper. Decoding every ARM B/BL word in osos.dec found 38 direct
      callsites: all 38 are unconditional bl, with zero predicated forms and
      zero plain B branches. Algorithm: preserve transferred from r3 in ip,
      push its stack slot, overwrite that fifth callee argument with zero,
      restore transferred to r3, bl 0x082784d4, and return the body status
      unchanged. The fifth word's meaning and 0x082784d4's deeper identity are
      not recovered or invented. Deliberate deviation: the Rust port calls the
      resident target through a volatile typed boundary so host tests can
      record it; target builds dispatch to its fixed 0x082784d4 address.

  # --- Exact resource-reader transfer (fs/resource_reader_read_exact) ---
  - address: 0x082a6aa4
    ghidra: FUN_082a6aa4
    name: resource_reader_read_exact
    size: 52
    sig: "u32 resource_reader_read_exact(const ResourceReader *reader, u8 *buffer, u32 count, u32 transferred_initial)"
    module: fs/resource_reader_read_exact
    status: ported
    renames:
      param_1/r0: reader
      param_2/r1: buffer
      param_3/r2: count
      param_4/r3: transferred_initial
      param_1_plus_0x8c: file_handle
      local_stack_word: transferred
    notes: >-
      Raw ARM fixes the extent at exactly 52 bytes,
      0x082a6aa4..0x082a6ad8: the next separately linked function opens at
      0x082a6ad8. It loads reader+0x8c, preserves count in r4, pushes the
      incoming r3 as the initial transferred word, and calls
      0x08277c74(handle, count, buffer, &transferred). That 28-byte wrapper
      calls the unrecovered 0x082784d4 body with fifth argument 2. Status
      zero then transferred == count returns 1; every other combination
      returns 0. Decoding every ARM B/BL word in osos.dec found exactly 15
      inbound direct calls, all unconditional bl (0x0828fc24, 0x0828fcac,
      0x0828fd68, 0x0828fdf8, 0x0828ff4c, 0x0829001c, 0x082900a0,
      0x082900ec, 0x08290120, 0x082901c8, 0x08290244, 0x082a6d28,
      0x082a6f40, 0x082a6f90, 0x082a6ff0); zero predicated forms and no
      plain B branches. The callers leave r3 as live state, so the port
      exposes it as transferred_initial rather than inventing a fourth
      semantic input. Deliberate deviation: it reuses fs/file_read's existing
      volatile typed 0x082784d4 body seam with control word 2 instead of
      creating a duplicate seam for the thin 0x08277c74 wrapper. Host tests
      prove exact-success, short-success, and complete-error outcomes and
      verify every forwarded body argument.

  # --- Absolute resource-reader file seek (fs/resource_reader_seek_absolute) ---
  - address: 0x082a6ad8
    ghidra: FUN_082a6ad8
    name: resource_reader_seek_absolute
    size: 44
    sig: "u32 resource_reader_seek_absolute(const ResourceReader *reader, u32 offset)"
    module: fs/resource_reader_seek_absolute
    status: ported
    renames:
      param_1/r0: reader
      param_2/r1: offset
      param_1_plus_0x8c: file_handle
      callee_param_2/r1: duplicate_offset
      callee_param_3/r2: offset_low
      callee_param_4/r3: offset_high
      callee_param_5/stack: origin
    notes: >-
      Raw ARM fixes the extent at exactly 44 bytes,
      0x082a6ad8..0x082a6b04: the next separately linked function opens at
      0x082a6b04. It copies offset from r1 to r2, supplies zero for the
      otherwise unused duplicate-offset r1, high offset r3, and stacked
      origin, then loads reader+0x8c and calls 0x082787b8. Status zero
      becomes 1; every nonzero status becomes 0. Decoding every ARM B/BL
      word in osos.dec found exactly 12 inbound direct calls, all
      unconditional bl (0x0828fd4c, 0x0828fddc, 0x0828ff30, 0x08290000,
      0x08290084, 0x082900d0, 0x082901ac, 0x08290228, 0x082a6d10,
      0x082a6f28, 0x082a6f78, 0x082a6fd8); zero predicated forms and no
      plain B branches. The port directly calls the existing
      ft_platform_file_seek port rather than duplicating a dispatch seam.
  # --- Platform C++ file seek (ft/system) ---
  - address: 0x082787b8
    ghidra: FUN_082787b8
    name: ft_platform_file_seek
    size: 220
    sig: "i32 ft_platform_file_seek(void *handle, u32 duplicate_offset, u32 offset_low, u32 offset_high, u32 origin)"
    module: ft/system
    status: ported
    renames:
      param_1/r0: handle
      param_2/r1: duplicate_offset
      param_3/r2: offset_low
      param_4/r3: offset_high
      param_5/stack: origin
      param_1_plus_0x08: state
      param_1_plus_0x18: directory_entry_index
      param_1_plus_0x1c: open_status
      param_1_plus_0x20: cached_entry_length
      param_1_plus_0x34: cursor
    notes: >-
      Raw bytes prove the reported 220-byte extent exactly: 0x08278894 is
      the next function. Decoding every ARM B/BL word in osos.dec found 21
      direct callsites, all unconditional bl; there are no predicated direct
      calls or plain B tail branches. It constructs the counted-lock guard
      from handle+4+0x44, rejects nonzero state with 2, and returns the
      open-status word unchanged when the directory-entry sentinel is -1.
      For an existing entry, origins 0/1/2 select absolute/current/end
      unsigned 64-bit positions; unsupported origins return 6. Candidates
      beyond the u32 cached length return 5; otherwise the low word is stored
      at +0x34 and 0 is returned. r1 is overwritten before use, but remains
      in the Rust ABI because ft_platform_stream_read passes the offset in
      both r1 and r2. No deliberate deviations: target field layout is
      asserted and the host model uses native synchronization-owner pointers.

  # --- RTXC message-dispatch veneer (runtime/message_dispatch_veneer) ---
  - address: 0x08003660
    ghidra: FUN_08003660
    name: message_dispatch_veneer
    size: 8
    sig: "void message_dispatch_veneer(u32 *request)"
    module: runtime/message_dispatch_veneer
    status: ported
    renames:
      DAT_08003664: rom_service_dispatch_target
    notes: >-
      Literal veneer: ldr pc, [pc, #-4] at 0x08003660 tail-branches through
      the target word 0x0802dca8 at 0x08003664. Ghidra reports 4 bytes; the
      true extent is 8 (instruction + literal) since the next veneer opens at
      0x08003668. Binary-verified call count: 37 sites (36 plain bl plus one
      blne at 0x08003e38, a caller-side NULL guard on its own r0 before
      dispatching selector 0x11; the veneer itself has no guard). Zero plain
      B and zero data-word references: never tail-branched to from osos, never
      dispatched virtually. Every caller builds a stack record with an RTXC
      selector in word 0 (observed 0x0e..0x1e range), passes r0 = &record,
      and reloads result words from the record afterwards; all 37 sites
      discard the dispatcher's r0, so the port's observable ABI is a void
      call on one writable record pointer. The target 0x0802dca8 is unported:
      it decodes as add r1, sp, #0x30; mov r0, r5; bl 0x0802b56c, one entry
      in the multi-entry RTXC service-stub family at 0x0802d9c4..0x0802ddb0
      (Ghidra lumps it into FUN_0802d9c4/FUN_0802dcd8; nine sibling veneers
      0x08003428..0x080034a0 jump to neighbouring entries). No call site
      writes r5 near its bl (checked all 37), so r5 is a firmware-wide
      reserved context register, not a per-call input. Deliberate deviation:
      the unported target sits behind an installable volatile dispatch seam
      (house foreign-service pattern) whose target default transmutes the
      retail literal; Rust's call adds a return edge the bare tail branch
      lacks. Reference C: decomp/c/000/08003660_FUN_08003660.c.
  - address: 0x080d5170
    ghidra: FUN_080d5170
    name: codec_update_bits
    size: 56
    sig: "void codec_update_bits(u32 reg, u32 mask, u32 value)"
    module: drivers/codec
    status: ported
    vars: {param_1: reg, param_2: mask, param_3: value}
    notes: >-
      Read-modify-write of one byte-wide register on the I2C device at
      slave address 0x4a, the 6G audio codec: callers are the codec
      init/power-mode sequencer FUN_080aa0a0 @ 0x080aa0a0 (register 0
      reset strobed 0x99 -> 0, 0x2e..0x3f register dump, 75 ms delay,
      four output channels powered via the bit-7 power-down of regs
      0x1a..0x1d), the route/mute flag decoder FUN_0809393c @ 0x0809393c
      (mute bits at regs 0x10/0x11 bit 7), and the sibling dispatcher
      FUN_080d51a8 immediately below. Body: read reg via FUN_080aa060
      (I2C write of the register index to slave 0x4a, one byte read back,
      stored with strh), merged = (old & ~mask) | (value & mask) in full
      32-bit arithmetic over the zero-extended ldrh result, write merged
      back via FUN_080b2894 (two-byte {reg, value} push to slave 0x4a;
      keeps only the low byte of r1 itself). The reader remains unported
      behind the CODEC_READ_REG volatile slot (retail address 0x080aa060);
      codec_write_reg @ 0x080b2894 is now called directly. Both I2C
      transactions retain the RTXC semaphore pair
      (0x0806a4a0/0x080645a8); the raw hardware transfer 0x0836bb84 remains
      behind each writer module's typed target/host-test boundary. Verified
      call count: 36 plain bl, 0 predicated, by decoding every B/BL word
      in osos.dec (6 in FUN_0809393c, 28 in FUN_080aa0a0, 2 in
      FUN_080d51a8). Ghidra's 56-byte extent is correct: the next
      function opens at 0x080d51a8 with push {r4,r5,r6,lr}. Ghidra's C
      drops the third argument at several call sites
      (FUN_080d5170(3,0xff) is really codec_update_bits(3, 0xff, r2)
      with r2 live); the ARM always reads r2. Deliberate deviations: the
      dead str r1,[sp] before the write call is dropped (the writer
      takes the value in r1 and never reads the slot); the port
      zero-initializes the reader's stack slot (inert). match.py structural:
      same op sequence (read seam call -> ldrh -> and/bic/orr -> direct
      writer call); LLVM adds an fp frame. Reference C:
      decomp/c/007/080d5170_FUN_080d5170.c.
  - address: 0x080b2894
    ghidra: FUN_080b2894
    name: codec_write_reg
    size: 40
    sig: "usize codec_write_reg(u32 reg, u32 value)"
    module: drivers/codec
    status: ported
    renames:
      param_1/r0: reg
      param_2/r1: value
      local_8/sp: bytes
      FUN_0806a4a0: kernel_sem5_wait
      FUN_0836bb84: i2c_write
      FUN_080645a8: kernel_sem5_signal
    notes: >-
      Audio-codec two-byte I2C write. Raw ARM extent is
      0x080b2894..0x080b28bc: push {r3,lr}; strb r0,[sp];
      strb r1,[sp,#1]; kernel_sem5_wait(); i2c_write(0x4a,2,sp);
      kernel_sem5_signal(); pop {ip,pc}. The caller-visible return is the
      semaphore-release r0 word; raw I2C status is ignored. The two input
      words are independently truncated to their low bytes, so the wire
      bytes are {reg as u8, value as u8}. Decoding every ARM B/BL word in
      osos.dec finds 19 direct call sites, all plain unconditional bl and
      zero predicated forms: one in FUN_0809393c, 16 in FUN_080aa0a0, one at
      raw address 0x080b2820, and one in FUN_080d5170. The next function
      begins at 0x080b28bc, confirming Ghidra's 40-byte extent. No deliberate
      semantic deviation: kernel semaphore shims are existing ports and
      unported raw I2C write 0x0836bb84 uses a typed fixed-address target
      call / volatile host seam (an indirect blx instead of retail's bl).

  - address: 0x0833dd48
    ghidra: FUN_0833dd48
    name: xor_c6_in_place
    size: 48
    sig: "void xor_c6_in_place(u8 *bytes, i32 len)"
    module: util/xor_c6_in_place
    status: ported
    renames:
      param_1/r0: bytes
      param_2/r1: len
      iVar1/r2: offset
    notes: >-
      XORs each byte in the signed range [0, len) with 0xc6. Raw ARM confirms
      Ghidra's 48-byte extent: the next sibling opens at 0x0833dd78. The
      byte-at-a-time ldrb/eor/strb loop has no NULL guard and skips all memory
      accesses when len <= 0. Decoding every B/BL word in osos.dec finds 33
      direct call sites: all are plain unconditional bl instructions, with no
      predicated bl or direct B sites. Deliberate deviations: none. Reference
      C: decomp/c/032/0833dd48_FUN_0833dd48.c.

  - address: 0x080570cc
    ghidra: FUN_080570cc
    name: stream_seek_tagged_entry
    size: 152
    sig: "i32 stream_seek_tagged_entry(u32 stream, u32 entry_tag, u32 *out_entry_value)"
    module: util/stream_seek_tagged_entry
    status: ported
    renames:
      param_1/r0: stream
      param_2/r1: entry_tag
      param_3/r2: out_entry_value
      local_1c/[sp+0xc]: entry_count
      local_20/[sp+0x8]: tag
      local_24/[sp+0x4]: offset
      FUN_0805e79c: stream_seek
      FUN_080575cc: stream_read_be32_or_zero
      FUN_08057874: stream_read_be32
    notes: >-
      Raw ARM verifies the 152-byte extent 0x080570cc..0x08057160: the
      following standalone bx lr at 0x08057164 begins a sibling. It first
      seeks to absolute offset 8, reads a signed big-endian entry count and
      an opaque second header word, then scans count entries of three
      big-endian words: tag, absolute seek offset, and an opaque third value.
      It reads the third value directly through stream_read_be32 into the
      caller output on every iteration; matching tag seeks to that entry
      offset and returns 0, while exhausting the signed count returns -24.
      Every read and seek status is deliberately ignored. Decoding every ARM
      B/BL word in osos.dec finds exactly 10 inbound direct calls, all plain
      unconditional BL and no predicated forms. The second header word and
      third entry word lack a verified format identity, so their names remain
      opaque rather than inventing a media-database meaning. Host tests cover
      a second-entry match, miss, negative signed count, ignored seek errors,
      and direct-reader failure preserving the caller output. Reference C:
      decomp/c/004/080570cc_FUN_080570cc.c.

  - address: 0x08057874
    ghidra: FUN_08057874
    name: stream_read_be32
    size: 80
    sig: "i32 stream_read_be32(StreamObject **stream, u32 *out)"
    module: util/stream_read_be32
    status: ported
    renames:
      param_1/r0: ctx
      param_2/r1: out
      FUN_0805e754: stream_read_core
    notes: >-
      Reads four bytes from a stream handle via stream_read_core @
      0x0805e754 and stores them big-endian into *out; returns 1 on success,
      0 on core failure, leaving *out untouched on failure. Ghidra's 80-byte
      extent is verified from raw words: the u16 twin FUN_080578c4 (identical
      shape, len=2, strh) opens immediately after the final pop. Decoding
      every B/BL word in osos.dec finds exactly 34 call sites, all plain
      unconditional bl. The core double-dereferences the stream handle and
      dispatches vtable slot +0x10 with (object, buf, len, mode=2), returning
      0 on success or -3 on a short nonzero-length read. Deliberate
      deviation: the port assembles the word with u32::from_be_bytes instead
      of four discrete ldrb + lsl/orr steps; LLVM emits a single word load
      plus rotate-based reverse with the same observable result. match.py
      structural: same shape (core call -> status branch -> BE assembly ->
      str -> 0/1 return). Reference C:
      decomp/c/004/08057874_FUN_08057874.c.

  - address: 0x08057c2c
    ghidra: FUN_08057c2c
    name: image_library_is_liti_class
    size: 40
    sig: "u32 image_library_is_liti_class(const u8 *target)"
    module: app/liti_class_check
    status: ported
    renames:
      param_1/r0: target
      DAT_08057c50: LITI_CLASS_TAG
    notes: >-
      NULL-guarded 'liti' ImageLibrary-database class-tag predicate:
      returns 1 iff target is non-NULL and the word at target+0x0 equals
      the tag 0x6974696c (in-memory bytes "liti"), held in the literal
      pool at 0x08057c50. Ghidra's 36-byte extent drops that literal
      word; the true extent is 40 (the next function's push {r3,lr}
      prologue starts at 0x08057c54). Call count verified by decoding
      every B/BL word in osos.dec: 15 unconditional bl, zero predicated
      - the internal NULL guard is what all callers rely on. The only
      stamper is the ImageLibrary database constructor at 0x0805e148
      (allocates 0x9e0 bytes via calloc_tag4 0x0805d1dc, stores the tag
      at object+0x0); its grandcaller 0x08058230 builds the object over
      the "iPod_Control/Artwork" and "ArtworkDB" paths, so a
      'liti'-tagged object is the ImageLibrary (artwork) database.
      Callers pass the object directly (0x08063e9c opens the database
      after the check) or a pointer pulled from another object's field
      (wrapper 0x08057bb4 checks *(obj+4), 0x08057bdc checks *(obj+8),
      0x081070a0 checks *(*ref+4)). Same predicate shape as the 'plst'
      check ui_element_is_plst_class (0x080613e0), but reads the tag at
      +0x0 instead of +0x4; own link_section keeps the hook seam
      unfolded. match.py: crate fp frame plus LLVM clz equality lowering
      vs ADS cmp/moveq; the core cmp r0,#0 / beq / ldr r0,[r0] / ldr
      r1,[pc] sequence matches verbatim. Reference C:
      decomp/c/004/08057c2c_FUN_08057c2c.c.

  - address: 0x0805e6d0
    ghidra: FUN_0805e6d0
    name: stream_tell
    size: 28
    sig: "i32 stream_tell(StreamObject **stream)"
    module: util/stream_tell
    status: ported
    renames:
      param_1/r0: stream
      uVar1/r0: position
      pcVar2/r1: tell
    notes: >-
      Stream-position wrapper over a double-dereferenced handle. Raw ARM
      confirms Ghidra's 28-byte extent: the next sibling begins with
      cmp r0,#0 at 0x0805e6ec immediately after this wrapper's bx lr.
      Algorithm: return -5 only when stream is NULL; otherwise load
      object = *stream, load its vtable, and tail-dispatch vtable slot +0x1c
      as tell(object), preserving the slot result unchanged. A non-NULL
      handle with a NULL object or vtable faults because the initial cmp
      condition still guards only the handle; this is deliberately preserved.
      Decoding every B/BL word in osos.dec finds exactly 10 direct call
      sites: 10 unconditional bl, zero predicated bl, and no plain B sites.
      All callers therefore rely on this wrapper's internal NULL guard.
      The slot target is runtime vtable data, not a statically identifiable
      function entry, so no dispatch seam is introduced. Deliberate
      deviation: named #[repr(C)] vtable fields preserve the 32-bit slot
      layout and widen on 64-bit hosts. Reference C:
      decomp/c/004/0805e6d0_FUN_0805e6d0.c. match.py structural review:
      Rust adds an fp frame and splits the predicated retail bxne into a NULL
      branch, frame pop, and tail bx, while retaining the +0x1c dispatch.

  - address: 0x0805e754
    ghidra: FUN_0805e754
    name: stream_read_core
    size: 72
    sig: "i32 stream_read_core(StreamObject **stream, u8 *buf, u32 len, i32 *result_out)"
    module: util/stream_read_core
    status: ported
    renames:
      param_1/r0: stream
      param_2/r1: buf
      param_3/r2: len
      param_4/r3: result_out
      iVar1/r0: slot_result
      pcVar2/ip: read_10
    notes: >-
      Raw ARM verifies the 72-byte extent 0x0805e754..0x0805e79c: the next
      standalone sibling starts with cmp r0,#0 at 0x0805e79c after this
      function's pop {r4, r5, r6, pc}. It has no NULL guard: it loads
      object = *stream, vtable = *object, then blx vtable slot +0x10 as
      read_10(object, buf, len, 2). It stores the raw slot result through
      result_out only when that pointer is non-NULL. A zero slot result with
      nonzero len returns -3; a nonzero result, or zero result with zero len,
      returns 0. Decoding every B/BL word in osos.dec finds exactly 9 inbound
      direct calls, all unconditional bl and no predicated forms. Host tests
      cover raw-result output, the nonzero-length short-read error, and the
      zero-length exception. Deliberate deviations: Rust uses direct
      conditionals rather than predicated ARM instructions, and #[repr(C)]
      named vtable fields widen on 64-bit hosts while preserving target slots.
      match.py structural review: expected LLVM frame and branch differences;
      it retains the double dereference, +0x10 dispatch, optional output
      store, and -3 normalization. Reference C:
      decomp/c/004/0805e754_FUN_0805e754.c.

  - address: 0x0805e79c
    ghidra: FUN_0805e79c
    name: stream_seek
    size: 64
    sig: "i32 stream_seek(StreamObject **stream, i32 offset)"
    module: util/stream_seek
    status: ported
    renames:
      param_1/r0: stream
      param_2/r1: offset
      iVar1/r0: status
      pcVar2/r1: seek
    notes: >-
      Absolute seek wrapper over the same double-dereferenced stream handle
      family as the ported stream_read_be32 @ 0x08057874: the read core @
      0x0805e754 dispatches vtable slot +0x10, this wrapper dispatches slot
      +0x14, and the tell wrapper @ 0x0805e6d0 dispatches slot +0x1c (all
      return -5 on a NULL handle). Algorithm: if the handle is NULL return
      -5; else load object = *handle, sign-extend the 32-bit offset into
      r2:r3 (asr #31), and blx vtable slot +0x14 with (object, position);
      return 0 when the slot reports 0, else -5 (mvn r0, #4) -- the
      underlying status is discarded. The r1 word at the blx is leftover
      scratch (the loaded slot pointer), not a real argument; the s64
      position rides in r2:r3 per AAPCS even-pair rules. Raw extent
      verified: the next function opens with push {r4-r8, lr} at
      0x0805e7dc, exactly after this function's pop {ip, pc}, so Ghidra's
      64-byte size is correct; the mov r3,#0 / str r3,[sp] pair is a dead
      store whose slot is only re-popped into ip. Decoding every B/BL word
      in osos.dec finds exactly 15 direct call sites, all plain
      unconditional bl and zero predicated forms -- the wrapper's own NULL
      guard is the only guard, so callers never check first. Callers in
      the 0x080570cc-0x08057874 record-walking cluster seek with
      tell(stream) + delta idioms; FUN_080a69cc rewinds with offset 0.
      Deliberate deviations: the dead store is not reproduced; the host
      vtable model widens structurally on 64-bit hosts while the filler
      keeps slot +0x14 at its target byte offset on device; the slot
      target is runtime vtable data dispatched through the object's own
      vtable, so no dispatch seam is needed (app/class_8900.rs precedent).
      match.py (exit 1, diff expected): same NULL check, double deref,
      sign extension, slot +0x14 load, blx, and 0/-5 normalization; LLVM
      folds the branches into predicated mvneq/bxeq/mvnne and adds the
      crate-wide fp frame. Reference C:
      decomp/c/004/0805e79c_FUN_0805e79c.c.

  - address: 0x083864c0
    ghidra: FUN_083864c0
    name: sqlite_value_bytes
    size: 68
    sig: "i32 sqlite_value_bytes(sqlite3_value *value, u8 enc)"
    module: sqlite/value_bytes
    status: ported
    renames:
      param_1/r0/r4: value
      param_2/r1: enc
      r2: flags
    notes: >-
      Internal sqlite3ValueBytes extractor. Raw extent is exactly
      0x083864c0..0x08386504: push {r4,lr}; ldrh flags,[r0,#0x1c];
      tst flags,#0x10; conditionally bl sqlite3ValueText @ 0x08386718;
      reload flags; return n at +0x18 or (i64 at +0x00)+n's low word
      when MEM_Zero (#0x800) is set; next function begins at
      0x08386504 with push {r4,lr}. Two unconditional tail B entries
      (0x08391754 and 0x0839176c), no direct BL callers, binary-scanned.
      RetailOS deliberately gates conversion on MEM_Blob (#0x10) clear,
      unlike upstream SQLite 3.5.9's MEM_Str test: blobs return their n
      directly, while zero-blobs add the i64 zero-tail counter at +0x00.
      The post-call flags reload preserves a conversion's MEM_Zero change.
      sqlite_value_text is already ported and called directly. Host tests
      prove blob/no-z no-coercion, MEM_Zero signed-add wrapping and the
      non-blob NULL-text zero result. No implementation deviation.

  - address: 0x08391750
    ghidra: FUN_08391750
    name: sqlite3_value_bytes
    size: 8
    sig: "i32 sqlite3_value_bytes(sqlite3_value *value)"
    module: sqlite/value_bytes
    status: ported
    renames:
      param_1/r0: value
      r1: SQLITE_UTF8
    notes: >-
      SQLite public API wrapper. Raw words e3a01001 eaffd359 are
      mov r1,#1 then b 0x083864c0, forcing SQLITE_UTF8 and tail-branching
      into sqlite_value_bytes with r0 untouched. Exact 8-byte extent:
      sqlite3_value_bytes16 begins at 0x08391758. Decoding every ARM B/BL
      word in osos.dec finds exactly 14 direct call sites, all plain
      unconditional BL and zero predicated forms or tail B references.
      Therefore no caller-side NULL guard exists; the target's opening
      flags dereference is retained. Host tests prove forwarding on a blob
      and the non-blob NULL-text result. No deliberate deviation: LLVM adds
      its normal frame around the forced UTF-8 forwarding call.

  - address: 0x0839179c
    ghidra: FUN_0839179c
    name: sqlite3_value_text
    size: 8
    sig: "const void *sqlite3_value_text(sqlite3_value *value)"
    module: sqlite/value_text
    status: ported
    renames:
      param_1/r0: value
    notes: >-
      SQLite 3.5.9's public API wrapper sqlite3_value_text (vdbemem.c):
      a pure 8-byte tail veneer, mov r1,#1; b 0x08386718, forcing
      enc = SQLITE_UTF8 and branching into the already-ported extractor
      sqlite_value_text (FUN_08386718) with the r0 value pointer
      untouched. Raw bytes decoded: e3a01001 eaffd3dc; Ghidra's 8-byte
      extent is exact - the next function (the sqlite3_value_text16
      sibling) opens at 0x083917a4. The sibling family confirms the
      identity: 0x083917c8 is sqlite3_value_text16le (mov r1,#2),
      0x083917c0 sqlite3_value_text16be (mov r1,#3), 0x083917a4
      sqlite3_value_text16 (r1 = 2 or 3 by the byte-order flag byte at
      0x088fa948), each tail-branching to the same extractor. Decoding
      every B/BL word in osos.dec finds exactly 32 call sites, all
      plain unconditional bl, zero predicated forms and zero plain B:
      no caller NULL-guards the value first, the extractor's own
      NULL/MEM_Null short-circuit is the only guard (verified by test).
      Deliberate deviations: none - the port is a direct forwarding
      call; LLVM emits an fp frame around the mov r1,#1 + b
      sqlite_value_text (R_ARM_JUMP24 reloc), behaviorally identical to
      the bare veneer. Verified by
      src/sqlite/value_text.rs::tests::the_api_wrapper_forwards_utf8_unchanged_value_and_result
      (recode path receives exactly ChangeEncoding(1)+NulTerminate and
      returns z, stringify path receives Stringify(1), NULL value
      returns NULL with no callee reached). match.py (exit 1, diff
      expected): same mov r1,#1 + branch-to-extractor core; the extra
      push/pop fp frame is the crate-wide LLVM frame-pointer
      boilerplate.

  - address: 0x0827288c
    ghidra: FUN_0827288c
    name: bitfield_test
    size: 28
    sig: "u32 bitfield_test(const u32 *const *word_handle, u32 bit)"
    module: util/bitfield
    status: ported
    vars: {param_1: word_handle, param_2: bit}
    notes: >-
      (**word_handle & (1 << bit)) ? 1 : 0 - a single-bit test of a
      word reached through a handle (callers pass &local where local
      holds the word address; two ldr derefs), same convention as its
      sibling bitfield_extract. Raw bytes decoded: e5900000 e3a02001
      e5900000 e1a01112 e0110000 13a00001 e12fff1e - 28 bytes exact,
      bx lr at +0x18, sibling FUN_082728a8 opens at 0x082728a8 (extent
      confirmed by adjacency, not Ghidra). Call sites verified by
      decoding every B/BL word in osos.dec: exactly 17, all plain
      unconditional bl, zero predicated, all from the packed-config
      decoder FUN_08188b8c @ 0x08188b8c (0x08188d90-0x08189008), which
      stores each 0/1 result byte into the same struct bitfield_extract
      fills - the decoder uses bitfield_test for one-bit fields and
      bitfield_extract for multi-bit ones. The mask shift is by
      register (mov r1, r2, lsl r1), so ARM register-shift rules apply
      to the bit index (amount = bottom byte, 32..=255 yields mask 0,
      256 wraps to bit 0), replicated via berec::arm_lsl. The ands /
      movne pair normalises the result to exactly 0 or 1, so returning
      u32::from(bool) is exact. Deliberate deviations: none. match.py
      (exit 1, diff expected): same double-ldr + mask-build + ands +
      movne core; LLVM renders the shift-amount clamp explicitly
      (and #255, cmp #31, movhi #0) exactly as it does for the sibling,
      plus the crate-wide fp frame.

  - address: 0x082728a8
    ghidra: FUN_082728a8
    name: bitfield_extract
    size: 36
    sig: "u32 bitfield_extract(const u32 *const *word_handle, u32 hi, u32 lo)"
    module: util/bitfield
    status: ported
    vars: {param_1: word_handle, param_2: hi, param_3: lo}
    notes: >-
      (word >> lo) & ((1 << (hi - lo + 1)) - 1) - a bit-field pulled
      out of a word reached through a handle (callers pass &local where
      local holds the word address; two ldr derefs). Raw bytes decoded:
      e0411002 e5900000 e2811001 e3a03001 e5900000 e1a01113 e2411001
      e0010230 e12fff1e - 36 bytes exact, bx lr at +0x20, next
      function's push opens at 0x082728cc. Call sites verified by
      decoding every B/BL word in osos.dec: exactly 32, all plain
      unconditional bl, zero predicated, all from the packed-config
      decoder FUN_08188b8c @ 0x08188b8c (TCDemoMode area), which pulls
      32 two-bit fields out of the words at 0x38400044/0x38400048 into
      a byte struct. Both shifts are by register, so ARM register-shift
      rules apply (amount = bottom byte, 32..=255 yields 0), replicated
      via berec::arm_lsl/arm_lsr: width 32 makes the mask all-ones,
      lo >= 32 returns 0. Sibling FUN_0827288c @ 0x0827288c (28 bytes)
      is the same handle convention as a single-bit test, ported as
      bitfield_test in this same module. Deliberate deviations: none. match.py (exit 1, diff
      expected): same double-ldr + lsr + mask-build + and core; LLVM
      renders the >=32 clamps as cmp/bhi and bicls, exactly as it does
      for the already-ported berec_extract_bits.

  - address: 0x0819c9d0
    ghidra: FUN_0819c9d0
    name: set_global_flag_byte
    size: 16
    sig: "void set_global_flag_byte(u32 index, u8 value)"
    module: app/global_flag_table
    status: ported
    renames:
      param_1/r0: index
      param_2/r1: value
      DAT_0819c9dc: global flag-byte table at 0x08a77a8f
    notes: >-
      Stores value at the unguarded global flag-byte-table offset index.
      Raw ARM is e59f2004 (ldr r2,[pc,#4]), e7c21000 (strb r1,[r2,r0]),
      e12fff1e (bx lr), followed by its 0x08a77a8f literal-pool word at
      0x0819c9dc. The next function opens at 0x0819c9e0, so Ghidra's
      12-byte extent omits the trailing literal and the actual extent is
      16 bytes. Decoding every ARM B/BL word in osos.dec finds 32 direct
      call sites: all plain unconditional bl, with zero predicated forms
      and zero direct B sites. The callers include staged view-event flag
      commits and explicit UI writes at indices 0x1d, 0x1e, 0x59, 0x5a,
      and 0x6f; the existing view_event port identifies this as its global
      flag table. Deliberate deviation: host builds use a local 0x78-byte
      table for observable tests; target builds retain the literal address
      and unguarded byte write. Reference C:
      decomp/c/016/0819c9d0_FUN_0819c9d0.c.

  # --- two-word runtime-data record name accessor ---
  - address: 0x082a7774
    ghidra: FUN_082a7774
    name: tracker_record_name
    size: 8
    sig: "u32 tracker_record_name(const u32 *record)"
    module: app/tracker_record
    status: ported
    renames:
      param_1/r0: record
      param_1_plus_0x04: name
      r0: name
    notes: >-
      Eight-byte leaf: `ldr r0,[r0,#4]; mov pc,lr`. Returns the full word
      at byte offset +0x04 of a two-word runtime-data record - the record's
      name pointer; word +0x0 is never read. Ghidra's 8-byte extent is
      exact: the sibling identity comparator FUN_082a777c opens with
      `push {r4,r5,r6,lr}` at 0x082a777c and calls this accessor twice.
      Call count verified by decoding every B/BL word in osos.dec: exactly
      30 unconditional bl sites (matching Ghidra's 30), zero predicated
      forms, zero tail b references, and no data word in the image holds
      0x082a7774 - never dispatched virtually, and no caller NULL-gates
      the call, so records are always live. Established semantics: the
      nine element_arrayN_construct instantiations (0x083d3c28 ..
      0x083d56b8, ported in app/element_table, which inlines this
      accessor as its arrayN_tracker_name helpers) call it on
      runtime-data record literals (0x0897baac, 0x0897bbc4, 0x0897bab8,
      0x0897b904, 0x0897cc8c, 0x0897cbec, 0x0897cba0, 0x0897cbac,
      0x0897bc00) and immediately strlen the result as the container's
      tracker/fTable name; the comparator at 0x082a777c and the
      keyed-insert path at 0x0807d93c compare two records' returned words
      for identity. The word is written at runtime (the image's record
      slots hold garbage), so no static name string is recoverable. No
      deviations: one aligned word load like the original. The port
      carries its own link_section because runtime/object_word.rs's
      object_word_at_4 is a byte-identical body and LLVM identical-code
      folding must not collapse the two hook seams; both symbols verified
      present in librustypod.a. match.py (exit 1, diff expected): the
      `ldr r0,[r0,#4]` survives verbatim; LLVM wraps it in the crate's
      fp frame (push {fp,lr}/pop {fp,pc}) instead of the original's bare
      mov pc,lr. Host tests cover the +0x4 word read, independence from
      word +0x0, zero/all-ones extremes, no mutation, and a guard-page
      fixture proving no byte past +0x8 is read.
  - address: 0x08281000
    ghidra: FUN_08281000
    name: fixed3_assign
    size: 48
    sig: "FixedValue *fixed3_assign(FixedValue *dst, const FixedValue *src)"
    module: app/fixed3_assign
    status: ported
    renames:
      param_1: dst
      param_2: src
      FUN_08152410: fixed_value_assign
    notes: >-
      Copy-assignment operator of retailOS's three-FixedValue record
      (0x48 bytes): three sequential bl calls to the member operator
      FUN_08152410 @ 0x08152410 at strides 0/0x18/0x30, then returns
      dst. FixedValue is the refcounted Q16.16 scalar of
      app/fixed_value (ported there). 30 bl call sites, 0 predicated
      forms, 0 plain b, and 0 word-aligned occurrences of 0x08281000
      as a data word (all binary-scanned) - never dispatched
      virtually, statically bound everywhere, as a compiler binds a
      known-type operator=. Ghidra's 48-byte extent is exact:
      0x08281030 opens the next function (push {r4,r5,r6,lr}), no
      literal pool. The member operator (104 bytes, exactly 3 bl
      sites - the three here, 0 data words) is FixedValue::operator=:
      copies +0x04/+0x08/+0x0c/+0x10 word-wise, then rebuilds the
      +0x14 flags/refcount word from the source's via bit-0 and bit-1
      byte merges followed by (dst & 3) | (src & ~3) - where dst & 3
      already holds the source's two bits, so the net effect is a
      plain word copy of +0x14 (ADS bitfield-assignment codegen for
      {flags: 2, refcount: 30}); only the +0x00 vtable word is
      preserved, exactly what a C++ copy-assignment must do. Ported in
      place as the private helper fixed_value_assign (wstr_case_eq
      house precedent, no separate ledger entry); the byte-partial
      intermediate states the merge skips are observable to no one.
      Callers copy from static template records into view-object slots
      at a 0x18 stride: FUN_08152728's pools @ 0x081528ac/0x081528b0
      hold 0x08a79840/0x08a79918, and it reads a template's +0x04 word
      as a Q16.16 scalar (adding 0x730000 = 115.0) right after the
      copy; FUN_08167498 fills four slots from DAT_081675ac/b4/b8/bc
      templates. Template addresses sit above the decrypted body's end
      (0x08a1c3e8), so their contents are not binary-verifiable. No
      NULL guard on either pointer, matching the original. match.py
      (exit 1, diff expected): identical structure - push, three bls
      at +0x18/+0x30 strides, return dst; the diff is the crate's fp
      frame and LLVM swapping the r4/r5 saves, the accepted house
      deviations. Host tests: all three members' payload words copied,
      each dst vtable preserved, flags word copied wholesale including
      refcount bits (dst's old low bits do not survive), source
      untouched, dst returned, self-assignment a no-op.

  - address: 0x082e06f4
    ghidra: FUN_082e06f4
    name: drive_slot_lookup
    size: 68
    sig: "u8 *drive_slot_lookup(u32 index)"
    module: fs/drive_slot
    status: ported
    renames:
      param_1/r0: index
      DAT_082e0738: drive_slot_table_ptr_literal (BSS word 0x08a0a724)
      FUN_082d7924: cache_lock_wait (ported, fs/cache_lock)
      FUN_082d7944: cache_lock_signal (ported, fs/cache_lock)
      FUN_082e4b3c: drive_slot_index_is_valid (8-byte `index < 4` validator,
        deliberately inlined)
    notes: >-
      Four-slot filesystem drive-table accessor. Raw extent verified:
      0x082e06f4..0x082e0734 contains 68 code bytes, followed by the
      0x08a0a724 BSS-address literal at 0x082e0738; the next separately
      entered function opens push {r4,r5,r6,lr} at 0x082e073c. It acquires
      the fs/disk cache lock, validates index as unsigned index < 4 through
      FUN_082e4b3c, then computes base + index*0x1fc via
      rsb r0,r5,r5,lsl #7 (index*127) and add r0,r1,r0,lsl #2. The pointer
      is returned only when its first word is nonzero; both the table-base
      and slot-word loads are deliberately unguarded, and the cache-lock
      release runs on every path. All 12 raw branch references are
      unconditional bl (0x081bd38c, 0x081bdb0c, 0x082c3040, 0x082e0628,
      0x082e0748, 0x082e14a0, 0x082e15f8, 0x082e1760, 0x082e1ce8,
      0x082e23bc, 0x082e2470, 0x082e24a8); zero predicated forms and zero
      tail b forms, so callers never flag-gate this lookup. No aligned data
      word in osos equals 0x082e06f4, so it is not dispatched virtually.
      The Rust port inlines the trivial validator rather than introducing a
      new dispatch seam, and calls the already ported cache lock pair.
      Host tests map four full 508-byte slots below 4 GiB, prove every valid
      live slot's exact returned address, reject zero first words and both
      out-of-range boundaries, and prove acquire/release brackets every
      outcome. cargo test --target x86_64-unknown-linux-gnu passed
      (6136 tests). match.py against the worktree archive (exit 1, diff
      expected) reports 17 original and 17 Rust instructions: both retain
      the cache-lock bracket, unsigned range test, index*508 address
      calculation, first-word liveness check, and conditional NULL result;
      LLVM makes the condition branch-based instead of predicated and adds
      its frame setup.
  - address: 0x082c3000
    ghidra: FUN_082c3000
    name: resolve_path_drive_index
    size: 116
    sig: "i32 resolve_path_drive_index(const u8 *path)"
    module: fs/path_drive_index
    status: ported
    renames:
      param_1/r0: path
      local_10/[sp]: drive_index
      FUN_083690a8: ata_report_error
      FUN_082e377c: drive_prefix_parse_resident
      FUN_082d7934: ata_semaphore_wait
      FUN_082c3168: drive_ready_check_resident_wrapper
      FUN_082e06f4: drive_slot_lookup
      FUN_082d7954: ata_semaphore_signal
    notes: >-
      Raw extent is exact: 0x082c3000..0x082c3070 is 116 bytes and the
      next separately entered body opens push {r4,r5,r6,r7,r8,lr} at
      0x082c3074. It clears the ATA error record with error 0, asks the
      resident parser at 0x082e377c to store a drive index, then only on
      parse success acquires that index's ATA semaphore. While held, it
      invokes 0x082c3168 (a 12-byte argument-setting wrapper which branches
      into the separately entered body at 0x082c3074) and the ported
      drive_slot_lookup. It always signals after a successful parse; a null
      parser result, failed ready check, or null slot reports ATA error 31
      and returns -1. All 12 decoded branch callers are unconditional bl
      (0x082e0600, 0x082e173c, 0x082e1d40, 0x082e22a4, 0x082e30f8,
      0x082e3458, 0x082e4094, 0x082e458c, 0x082e462c, 0x082e46d4,
      0x082e47b4, 0x082e6338); zero predicated forms and zero tail b forms.
      The parser and ready checker remain resident boundaries rather than
      invented ports. Target builds call them at verified addresses; host
      tests install volatile recorders and prove success, parser failure,
      failed readiness, missing-slot failure, lock bracketing, error order,
      and forwarding a null path unchanged. Deviation: the original's direct
      bl calls become typed resident boundary calls so host tests can observe
      the unrecovered entries.
  - address: 0x082e0e1c
    ghidra: FUN_082e0e1c
    name: volume_table_lookup
    size: 108
    sig: "u8 *volume_table_lookup(i32 index, u32 flags)"
    module: fs/volume_table
    status: ported
    renames:
      param_1/r0: index
      param_2/r1: flags
      DAT_082e0e88: volume_table_ptr_literal (BSS word 0x08a0a72c)
      FUN_082d7924: cache_lock_wait (ported, fs/cache_lock)
      FUN_082d7944: cache_lock_signal (ported, fs/cache_lock)
      FUN_082e3774: volume_table_capacity (8-byte `return 64` getter,
        ported as the constant VOLUME_TABLE_CAPACITY)
    notes: >-
      Mounted-volume table slot accessor. The filesystem keeps a fixed
      table of 64 volume descriptors (36-byte stride: `add r0, r4, r4,
      lsl #3` then `lsl #2` = index*9*4) whose base pointer lives in the
      BSS word at 0x08a0a72c; only four literal references to that word
      exist in osos (the slot-destroy scan @ 0x082e0094, the disk-cache
      code @ 0x082b1710, the descriptor family @ 0x082db600, and this
      lookup's literal @ 0x082e0e88). Table identity: the immediately
      following sibling @ 0x082e377c parses "X:" drive-letter prefixes
      through toupper @ 0x082e0180 and accepts only letters mapping to
      slots 0..3 (the `index < 4` check @ 0x082e4b3c). Under the fs/disk
      cache lock (ported cache_lock_wait/cache_lock_signal pair) the
      function returns base + index*36 when 0 <= index <= 64, the slot
      word at +0x18 is clear (nonzero marks a slot mid-teardown - the
      destroy scan @ 0x082e0094 memsets exactly such slots), and either
      flags == 0 or the slot's u16 flags at +0x04 share a bit with the
      mask (`ldrh` zero-extends, so mask bits above bit 15 can never
      match); every rejection path still runs the lock release. Bounds
      quirk ported faithfully: the fail condition is `capacity < index`,
      so index == 64 is ACCEPTED even though both table iterators (the
      destroy scan and walker FUN_082e0b34 @ 0x082e0b34) loop strictly
      below 64 - the original admits one slot past the table. Extent
      byte-verified: 108 code bytes plus the 4-byte BSS-address literal
      at 0x082e0e88; the next separately entered function opens push
      {r4,r5,r6,r7,r8,lr} at 0x082e0e8c, so Ghidra's 108 is exact. 12
      call sites, verified by decoding every ARM B/BL word in osos.dec
      for every condition code: all 12 are unconditional bl
      (0x081bd694, 0x082b194c, 0x082b1c2c, 0x082e0b54, 0x082e193c,
      0x082e19fc, 0x082e5de0, 0x082e5e90, 0x082e6240, 0x082e62a4,
      0x082e636c, 0x082e660c), zero predicated forms, zero tail b -
      callers never flag-gate the lookup. No data word in osos
      references 0x082e0e1c - never dispatched virtually. Deviations:
      the capacity getter FUN_082e3774 (`mov r0, #64; bx lr`) is the
      constant VOLUME_TABLE_CAPACITY instead of a bl; the lock pair
      dispatches through the ported fs/cache_lock functions; host builds
      read the table base from a private model word so tests exercise
      the lookup without mapping the firmware BSS address. match.py
      structural (exit 1, diff expected): same literal 0x08a0a72c, same
      index*36 stride computation, same +0x18 word and +0x04 ldrh mask
      checks, lock calls bracketing; LLVM folds the two signed compares
      into one unsigned `cmp index, #65; bcs` (equivalent over i32) and
      reorders the pure flag/word loads. Host tests cover: plain slot
      pointer return across the table, lock bracket on every path
      (accepted, negative, past-capacity, torn-down), index == capacity
      acceptance, +0x18-set rejection, mask shared-bit filtering
      including unreachable high mask bits and the zero mask, and the
      NULL base + index 0 case.

  - address: 0x082e1098
    ghidra: FUN_082e1098
    name: find_zero_cache_position
    size: 84
    sig: "u32 find_zero_cache_position(void *cache, u32 start, u32 end)"
    module: fs/zero_cache_position
    status: ported
    renames:
      param_1/r0: cache
      param_2/r1: start
      param_3/r2: end
      local_18/[sp]: value
      FUN_082e0cac: read_cache_position_value (unported resident boundary)
    notes: >-
      Raw extent is exactly 84 code bytes from 0x082e1098 through the
      `pop {r3,r4,r5,r6,r7,pc}` at 0x082e10e8; the next separately entered
      function opens `push {r4,r5,r6,r7,r8,r9,sl,fp,lr}` at 0x082e10ec.
      The unsigned `cmp start,end; bcc` loop calls unported
      FUN_082e0cac(cache, start, &value). A zero call result aborts with
      zero; a successful zero value returns start; every other successful
      value increments start. Reaching the exclusive end returns zero.
      Therefore result zero intentionally conflates failure/not-found with a
      successful match at position zero. Decoding every ARM B/BL word in
      osos.dec finds exactly eight direct calls, all unconditional plain bl
      (0x082dff24, 0x082dff4c, 0x082dff78, 0x082dff90, 0x082dffa8,
      0x082e0294, 0x082e02ac, 0x082e02c4), with zero predicated forms; no
      caller flag-gates the search. FUN_082e0cac has no names.yaml entry, so
      it remains an explicit resident boundary: target builds call its
      verified address 0x082e0cac and host tests install a recorder rather
      than inventing its identity. The original leaves its stack output word
      uninitialized before the call, but the decoded helper writes it on
      every successful path; Rust initializes that local solely to make the
      same successful contract defined. Host tests prove first-match
      selection, reader argument progression and cache forwarding,
      reader-failure short-circuiting, end exhaustion, empty/reversed
      unsigned ranges, and the zero-position ambiguity.

  - address: 0x082e3774
    ghidra: FUN_082e3774
    name: volume_table_capacity
    size: 8
    sig: "i32 volume_table_capacity(void)"
    module: fs/volume_table
    status: identified
    notes: >-
      Constant getter for the mounted-volume table's slot count: `mov
      r0, #64; bx lr` (8 bytes; the next function opens push
      {r4,r5,r6,lr} at 0x082e377c, so the extent is exact). Used as the
      loop bound by the slot-destroy scan @ 0x082e0094 and walker
      FUN_082e0b34, and as the bounds value in volume_table_lookup @
      0x082e0e1c. Not ported as its own symbol: the single consumer
      among the ported functions (volume_table_lookup) uses it as the
      constant VOLUME_TABLE_CAPACITY, documented in that entry.

  - address: 0x082e18bc
    ghidra: FUN_082e18bc
    name: cache_entry_release
    size: 60
    sig: "void cache_entry_release(u32 *entry, u32 detach_owner)"
    module: fs/cache_entry
    status: ported
    renames:
      param_1: entry
      param_2: detach_owner
    notes: >-
      Releases one reference held on the five-word cache descriptor
      returned by FUN_082dfdf0: it decrements word +0x10 only if nonzero,
      and clears the source/owner word +0x08 when detach_owner is
      nonzero. Both mutations occur between calls to the cache lock
      acquire/release thunks 0x082d7924 (cache_lock_wait) and 0x082d7944
      (cache_lock_signal), both ported since. The port calls those
      retailOS addresses directly,
      while host tests use recording boundaries only to prove both calls
      bracket every path, including NULL. Size byte-verified: the next
      separately entered function begins at 0x082e18f8, so Ghidra's 60
      bytes is exact. 29 call sites, verified by decoding every B/BL word
      in osos.dec: all 29 are plain bl, with no predicated forms.

  - address: 0x082e1960
    ghidra: FUN_082e1960
    name: shared_data_release
    size: 104
    sig: "u32 shared_data_release(u32 *data)"
    module: fs/shared_data
    status: ported
    renames:
      param_1: data
      iVar2: remaining_references
      piVar1: shared_data_list_head
      DAT_082e19c8: shared_data_list_head_literal
      FUN_082d7924: cache_lock_wait
      FUN_082d7944: cache_lock_signal
      FUN_082e2f74: shared_data_pool_recycle
    notes: >-
      NULL-guarded release of the path-resolution cluster's shared 0x54-byte
      data block. Under the cache lock, decrements nonzero refcount word +0x24;
      on a zero transition, or an already-zero count, unlinks +0x38/+0x3c
      next/previous links from the list headed at 0x08a0a720, then recycles
      through separately entered shared_data_pool_recycle @ 0x082e2f74. Raw
      extent is 100 code bytes at 0x082e1960..0x082e19c4 plus the 4-byte
      list-head literal at 0x082e19c8; next function path_node_release starts
      @ 0x082e19cc, so Ghidra's 208-byte extent swallowed its sibling. Every
      B/BL word in osos.dec was decoded: 9 unconditional bl call sites
      (0x082e126c, 0x082e1288, 0x082e1840, 0x082e19dc, 0x082e1fc8,
      0x082e21ac, 0x082e220c, 0x082e2368, 0x082e241c), no predicated calls,
      tail branches, or data-word references. The port calls existing
      cache_lock_wait/cache_lock_signal ports; the unported pool recycle stays
      a fixed-address firmware call and a host-test recording boundary.

  - address: 0x082e1378
    ghidra: FUN_082e1378
    name: fat_dirent_start_cluster
    size: 24
    sig: "u32 fat_dirent_start_cluster(const FatVolume *volume, const FatDirEntry *entry)"
    module: fs/fat_dirent
    status: ported
    renames:
      param_1/r0: volume
      param_2/r1: entry
      uVar1/r0: start_cluster
    notes: >-
      FAT short-directory-entry start-cluster reader. It returns the low
      first-cluster word at entry +0x1a for every format except exactly 8;
      format 8 combines entry +0x14 as the upper word with the low word,
      unmasked, into the FAT32 cluster number. Raw extent is byte-verified:
      this six-instruction body ends at 0x082e138c and the separate next
      entry starts at 0x082e1390. Every B/BL word in osos.dec was decoded:
      all 16 call sites are plain bl, with no predicated calls or tail
      branches. No NULL guards or deliberate deviations.

  - address: 0x082e3d30
    ghidra: FUN_082e3d30
    name: fat_dirent_set_start_cluster
    size: 24
    sig: "void fat_dirent_set_start_cluster(const FatVolume *volume, FatDirEntry *entry, u32 start_cluster)"
    module: fs/fat_dirent
    status: ported
    renames:
      param_1/r0: volume
      param_2/r1: entry
      param_3/r2: start_cluster
    notes: >-
      FAT short-directory-entry start-cluster writer. It first truncates
      start_cluster into entry +0x1a, then, only for format 8 (FAT32), stores
      its upper halfword at entry +0x14. Raw extent is exactly 24 bytes:
      0x082e3d30..0x082e3d44 ends in bx lr and the separate next function
      begins with push {r4,r5,r6,lr} at 0x082e3d48. Every ARM B/BL word in
      osos.dec was decoded: 8 direct call sites, 7 plain bl at 0x082e3634,
      0x082e3668, 0x082e4270, 0x082e618c, 0x082e6484, 0x082e64cc, and
      0x082e6728, plus one caller-gated bleq at 0x082b1d7c; no tail branches.
      The sole predicated call means that caller elides this otherwise
      unguarded writer when its preceding equality check fails. No deliberate
      deviations. Host tests prove low-word truncation, high-word preservation
      for every non-FAT32 format edge, and full FAT32 splitting.

  - address: 0x082e2a48
    ghidra: FUN_082e2a48
    name: fat_dirent_is_directory
    size: 32
    sig: "u32 fat_dirent_is_directory(const FatDirentHandle *handle)"
    module: fs/fat_dirent
    status: ported
    renames:
      param_1/r0: handle
      bVar1: flag_clear
    notes: >-
      FAT lookup-handle directory predicate. It answers 1 when the
      handle's cached directory flag at +0x14 is nonzero, without
      dereferencing anything else; otherwise it reads the attribute byte
      at +0x0b of the FatDirEntry pointed at by handle +0x04 and tests
      ATTR_DIRECTORY (0x10). Handle word +0x00 is the FatVolume and +0x04
      the dirent, confirmed by callers FUN_082e20d0/FUN_082e1448 which
      feed the same pair to fat_dirent_start_cluster; the opendir paths
      (FUN_082e20d0, FUN_082e3080) require a nonzero answer while the
      unlink path FUN_082e47a0 rejects on one. The byte at dirent +0x0b
      is the FAT attribute byte: sibling FUN_082e2a68 tests its 0x08
      (volume label) bit, and FUN_082e47a0 masks it with 0x19
      (read-only | volume | directory). Raw extent byte-verified: the bx
      lr at 0x082e2a64 ends the 32-byte body and the separately entered
      sibling starts at 0x082e2a68, so Ghidra's 32 bytes is exact. Every
      B/BL word in osos.dec was decoded: all 12 call sites are plain bl,
      with no predicated calls or tail branches, so the missing NULL
      guard matters to no caller. FatDirentHandle keeps native pointers
      with named fields; 32-bit-only offset asserts pin entry at +0x04
      and directory_flag at +0x14. match.py (exit 1, diff expected):
      same field offsets and short-circuit; LLVM emits and r0, r1, r0,
      lsr #4 for the bit test and adds the crate's fp frame, both
      accepted house deviations. Host tests cover flag short-circuit
      with a dangling entry pointer, attribute bits 0x10/0x30/0x1f
      answering 1, and 0x00/0x08/0x20/0x0f answering 0.

  - address: 0x0836f91c
    ghidra: FUN_0836f91c
    name: sqlite_auth_check
    size: 156
    sig: "int sqlite_auth_check(Parse *parse, int code, const char *z_arg1, const char *z_arg2, const char *z_arg3)"
    module: sqlite/auth_check
    status: ported
    renames:
      param_1: parse
      param_2: code
      param_3: z_arg1
      param_4: z_arg2
      param_5: z_arg3
    notes: >-
      SQLite 3.5.9's sqlite3AuthCheck (src/auth.c), matched
      statement-for-statement against the upstream source: the parser's
      authorization gate, 28 bl call sites verified by decoding every
      B/BL word in osos.dec (all 28 plain bl, zero predicated forms,
      zero tail b). Size byte-verified: the literal-pool string "not
      authorized" follows immediately at 0x0836f9b8 and the next
      separately entered function begins at 0x0836f9c8, so Ghidra's 156
      bytes is exact. Skips the check (returns SQLITE_OK) when
      db->init.busy (db +0x80), pParse->declareVtab (parse +0x198) or no
      xAuth (db +0xe0); otherwise calls xAuth(pAuthArg (db +0xe4), code,
      z_arg1, z_arg2, z_arg3, pParse->zAuthContext (parse +0x18c)) and
      maps the verdict: SQLITE_DENY (1) reports "not authorized" through
      the ported sqlite_error_msg @ 0x083767a0 and latches parse->rc =
      SQLITE_AUTH (23); anything other than 0/1/2 is an authorizer
      protocol violation handled by the unported sqliteAuthBadReturnCode
      @ 0x083918f4 (32 bytes, sole caller is this function's blne), the
      SQLITE_AUTH_BAD_RETURN_CODE seam - default stub latches
      parse->rc = SQLITE_ERROR (the original's unconditional final
      store) and skips the report. That wrapper's identity is confirmed
      by its literal-pool format 0x088fd968, whose datum under the
      documented +0xaed8 image/runtime skew (sqlite/mod.rs) is
      0x08908840: "illegal return value (%d) from the authorization
      function - should be SQLITE_OK, SQLITE_IGNORE, or SQLITE_DENY".
      Deviations: z_arg3 arrives on the stack in the original (plain
      fifth parameter in the Rust ABI), and the error_msg call passes a
      NULL VaList where the original passes dead r2/r3 - the format has
      no conversions, so the formatter never reads ap. match.py (exit 1,
      diff expected): same skeleton (skip tests, blx ip six-arg call,
      deny branch with mov #23 store, bad-return tail); the diff is the
      crate's fp frame, the seam's volatile slot load, and LLVM folding
      the !=0 && !=2 test into mvn r1,#2 / tst. Host tests: busy /
      declareVtab / no-authorizer skips, arg pass-through incl.
      zAuthContext as sixth arg, OK and IGNORE pass-through, deny
      reporting (n_err bumped, SQLITE_AUTH latched, unconditional
      overwrite of a preset rc), positive and negative garbage verdicts
      through both the default stub and a recording seam mock.

  - address: 0x083817ec
    ghidra: FUN_083817ec
    name: sqlite_read_schema
    size: 64
    sig: "int sqlite_read_schema(Parse *parse)"
    module: sqlite/read_schema
    status: ported
    renames:
      param_1: parse
      iVar1: rc
      FUN_0837b394: sqlite3Init (unported, SQLITE_SCHEMA_INIT seam)
    notes: >-
      SQLite 3.5.x's sqlite3ReadSchema (src/build.c), matched
      statement-for-statement against the upstream source: the gate every
      statement that needs a loaded catalogue passes through. Extent
      byte-verified - the sixteen instruction words
      0x083817ec..0x08381828 inclusive, and the next separately entered
      function (stmdb sp!,{r4,r5,r6,r7,r8,r9,sl,fp,lr}) begins at
      0x0838182c, so Ghidra's 64 bytes is exact with no trailing literal
      pool. 20 call sites, verified by decoding every B/BL word in
      osos.dec - all 20 are plain unconditional bl, zero predicated
      forms, zero tail b - and the address occurs in no data word, so it
      is never dispatched through a vtable. Algorithm - db = parse->db
      (parse +0x00); if db->init.busy (byte at db +0x80) is non-zero
      return SQLITE_OK without doing anything, because the loader opens
      with the same test; otherwise call sqlite3Init(db,
      &parse->zErrMsg) (the add r1,r4,#8 that precedes the bl) and, when
      it fails, store the code into parse->rc (+0x04) and then bump
      parse->nErr (+0x40) - in that register order - before returning
      it. A successful load leaves the parse context completely
      untouched. Behavioural fact worth pinning - the failure path
      OVERWRITES an already-latched parse->rc (strne r1,[r4,#4] with no
      compare of the old value), unlike sqlite_error_msg @ 0x083767a0
      where the first error wins; every one of the 20 call sites tests
      the return against zero and bails immediately, so a stale rc is
      never the interesting one (e.g. 0x0837d264 - bl; cmp r0,#0;
      movne r0,#0; bne out). Deviations - sqlite3Init @ 0x0837b394 (252
      bytes) is not ported - it walks db->aDb calling sqlite3InitOne @
      0x0837b5f0 per attached database, resets the internal schema @
      0x0838209c on failure, and folds in sqlite3CommitInternalChanges
      (clearing SQLITE_InternChanges, bit 4 of db->flags at +0x0c, which
      is what the entry-time bic r7,#1,r1,lsr #4 latches) - so it is the
      SQLITE_SCHEMA_INIT dispatch seam (house pattern, see
      sqlite/error_msg.rs); its default stub returns SQLITE_OK, the state
      the original reaches when every attached database already carries
      DB_SchemaLoaded, i.e. no work and no diagnostic. The Parse view is
      the existing sqlite::error_msg::Parse (db +0x00, rc +0x04,
      z_err_msg +0x08, n_err +0x40 - exactly the four fields this gate
      touches) and the Db view is the existing sqlite::auth_check::Db
      (init_busy +0x80); no new struct was needed and both already assert
      the original offsets on the 32-bit target. match.py (exit 1, diff
      expected) - identical skeleton and identical field arithmetic
      (ldrb +0x80 test, add r1,#8, predicated rc store then +0x40
      increment); the diff is the crate's fp frame, the seam's volatile
      slot load plus blx in place of the direct bl, and LLVM keeping rc
      in r4 rather than r1. Host tests (9) - busy skip for busy bytes
      1/2/0x80/0xff with the loader never entered, the (db,
      &parse->zErrMsg) argument pair, success leaving rc and nErr alone,
      failure recording the code and bumping the counter, the
      unconditional overwrite of a preset rc, the loader's diagnostic
      landing in z_err_msg, nErr wrapping i32::MAX -> i32::MIN like the
      plain ARM add, the default stub, and poison checks proving nothing
      outside rc/nErr/z_err_msg is written.

  - address: 0x082738e0
    ghidra: FUN_082738e0
    name: timing_wheel_remove
    size: 96
    sig: "void timing_wheel_remove(u32 *table, u32 *node)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: node
      uVar2/r2: bucket_index_or_link_word
      puVar3/r3: bucket_head
    notes: >-
      Raw ARM is exactly the 24 instruction words
      0x082738e0..0x0827393c (96 bytes); the next separately linked function
      begins at 0x08273940. Ghidra's reported size is exact. Decoding every
      ARM B/BL word in osos.dec finds 15 direct call sites, all plain
      unconditional bl; there are no predicated BL forms. One unconditional
      B tail caller at 0x08273a38 loads the scheduler singleton then branches
      here, and no DATA word references this address. The function has no
      NULL guard or rank bounds check: only when node->flags bit 0 is set, it
      selects table[node->rank - 1], replaces a matching bucket head with
      node->next (including zero), otherwise patches node->prev->next when
      present, patches node->next->prev when present, then clears just bit 0
      while retaining every other flags/refcount bit and both node link words.
      The port uses word indexes for the shared 32-bit wheel prefix
      (+0x08/+0x0c/+0x10/+0x14), so Animation and TimedTransition callers
      now direct-call it; wheel insertion remains their existing volatile
      seam. Host tests cover the unlinked no-op, a head with successor, a
      terminal head that must clear its bucket word, and an interior splice.
      There are no deliberate deviations.

  - address: 0x082739e0
    ghidra: FUN_082739e0
    name: release_refcounted_value
    size: 52
    sig: "void release_refcounted_value(void *object)"
    module: app/refcounted_value
    status: ported
    renames:
      param_1/r0: object
      iVar1/r1: flags_refcount
      FUN_08273a14: retain_value
    notes: >-
      The true extent is the thirteen instruction words
      0x082739e0..0x08273a10 (52 bytes); the next separately linked
      function, retain_value, begins at 0x08273a14. Ghidra's reported
      size is exact. Decoding every ARM B/BL word in osos.dec finds 28
      direct call sites, all blne; there are no plain bl or other
      predicated BL forms, tail branches, or DATA-word references.
      Every caller guards the object pointer, matching this function's
      deliberate absence of a NULL guard. The low byte of object +0x14
      tests flag bit 1. When set, ARM wrapping arithmetic subtracts four
      from the whole aligned +0x14 flags/refcount word. If all count bits
      (bits 2..) then clear, the function dispatches the deleting
      destructor at vtable +0x04 with object in r0. The port retains
      volatile target-word accesses, wrapping subtraction, and exact
      target vtable-slot dispatch. Deliberate host-only deviation: a
      test callback observes draining destructor dispatch because an ARM
      32-bit code address cannot be stored in a host function pointer
      word; target builds have no callback or seam. Existing animation
      callers now use both direct ports; only the timing-wheel functions
      remain in their animation seam.

  - address: 0x08273a14
    ghidra: FUN_08273a14
    name: retain_value
    size: 24
    sig: "void retain_value(void *object)"
    module: app/refcounted_value
    status: ported
    renames:
      param_1/r0: object
      iVar1/r1: flags_refcount
    notes: >-
      Raw ARM is exactly the six instruction words 0x08273a14..0x08273a28;
      the next separately linked function begins at 0x08273a2c. Ghidra's
      reported 24-byte extent is exact. Decoding every ARM B/BL word in
      osos.dec finds 19 direct call sites, all plain unconditional bl; there
      are no predicated BL forms, tail branches, or DATA-word references.
      The callee deliberately has no NULL guard. It reads the low byte of
      object +0x14 and, only when flag bit 1 is set, adds four with ARM
      wrapping arithmetic to the aligned flags/refcount word. The port keeps
      that volatile byte predicate and aligned volatile word update exactly;
      there are no deliberate deviations. Existing animation callers now use
      this direct port rather than a retain dispatch seam.

  - address: 0x08273a2c
    ghidra: FUN_08273a2c
    name: timing_wheel_remove_global
    size: 20
    sig: "void timing_wheel_remove_global(u32 *node)"
    module: app/animation
    status: ported
    renames:
      param_1/r0: node
      DAT_08273a3c: SCHEDULER_SINGLETON_GLOBAL (pool word 0x089cc7e0)
      FUN_082738e0: timing_wheel_remove
    notes: >-
      Raw ARM has four instruction words at 0x08273a2c..0x08273a38 followed
      by its separately linked literal-pool word 0x089cc7e0 at 0x08273a3c,
      for a true extent of 20 bytes; the next real function starts at
      0x08273a40. Ghidra reports only the 16 instruction bytes and attributes
      timing_wheel_remove's body to this wrapper. Decoding every ARM B/BL word
      in osos.dec finds exactly eight direct call sites (0x081446d8,
      0x081446e0, 0x081446e8, 0x081448d4, 0x081448dc, 0x081448e4,
      0x08145e40, and 0x081463b4), all plain unconditional bl; there are no
      predicated forms, tail callers, or raw DATA-word references. The wrapper
      copies node from r0 to r1, loads the live timing-wheel table pointer
      from global 0x089cc7e0 into r0, then unconditionally tail-branches to
      timing_wheel_remove(table, node). The port calls the existing direct
      timing-wheel port through scheduler_table; its established host model
      substitutes a house-static table. No other deliberate deviations.

  - address: 0x08273a40
    ghidra: FUN_08273a40
    name: value_aux_max
    size: 20
    sig: "u32 value_aux_max(FixedValue *a, FixedValue *b)"
    module: app/fixed_value
    status: ported
    renames:
      param_1/r0: a
      param_2/r1: b
    notes: >-
      The two-value constructors' rank-key leaf: returns the larger of
      the two arguments' +0x08 aux words under unsigned comparison. Raw
      ARM is exactly the five instruction words 0x08273a40..0x08273a50
      (ldr r0,[r0,#8]; ldr r1,[r1,#8]; cmp; movls r0,r1; bx lr); the
      next separately linked function begins at 0x08273a54, so Ghidra's
      extent is exact and there is no literal pool. Decoding every ARM
      B/BL word in osos.dec finds 5 direct call sites (0x081679d8,
      0x08167adc, 0x8181060, 0x8197744, 0x8197834), all plain
      unconditional bl; no predicated forms, tail branches, or DATA-word
      references. Every caller is one of the byte-identical two-value
      wheel-node constructors (timer_step_value_init @ 0x08167a6c and
      its vtable siblings), which feed the result + 1 into the node's
      wheel rank; a plain scalar carries aux 0, so such nodes land at
      rank 1. No NULL guard, matching its callers. The port reads the
      aligned aux words directly; there are no deliberate deviations.
      match.py --lib (worktree archive): 8 instructions vs 5 - the
      crate fp prologue plus LLVM selecting movhi+mov where the original
      uses movls; the two +0x08 loads and the unsigned compare/select
      are identical.

  # --- guarded opaque counter increment ---
  - address: 0x0808e16c
    ghidra: FUN_0808e16c
    name: tagged_counter_try_increment
    size: 40
    sig: "int tagged_counter_try_increment(TaggedCounter *counter)"
    module: util/tagged_counter
    status: ported
    renames:
      param_1/r0: counter
      DAT_080a7738: crts_tag
    notes: >-
      Raw ARM extent is exactly 0x0808e16c..0x0808e190; the next separately
      entered function starts at 0x0808e194. Ghidra's 40-byte size is exact.
      The unported 0x080a7714 validates a non-NULL counter's first word
      against 0x73747263 (in-memory bytes "crts"). Valid counters increment
      their signed word at +0x30 with ARM wrapping arithmetic and return zero;
      invalid tags and NULL return -50 unchanged. Decoding every ARM B/BL
      word in osos.dec finds 27 direct call sites: 26 plain bl and one bleq
      at 0x0805c1dc, reached only when that caller's preceding increment made
      its owner count equal one; there are no tail branches. Deliberate
      deviation: the verified four-instruction guard is inlined because
      0x080a7714 has no port; no callee identity is invented. Host tests cover
      NULL, invalid tags, negative values, and signed wrap at i32::MAX.

  # --- guarded opaque counter decrement ---
  - address: 0x0809f744
    ghidra: FUN_0809f744
    name: tagged_counter_try_decrement
    size: 44
    sig: "int tagged_counter_try_decrement(TaggedCounter *counter)"
    module: util/tagged_counter
    status: ported
    renames:
      param_1/r0: counter
      DAT_080a7738: crts_tag
    notes: >-
      Raw ARM extent is exactly 0x0809f744..0x0809f76c; the next separately
      entered function starts at 0x0809f770. The call to 0x080a7714 is a
      NULL-safe first-word equality guard against 0x73747263 (in-memory bytes
      "crts"); its semantic identity is not established. On a valid object,
      the signed word at +0x30 decrements only when strictly positive,
      returning 0; invalid tag, NULL, zero, and negative all return -50
      unchanged. Decoding every ARM B/BL word in osos.dec finds 28 direct call
      sites: 27 plain bl and one bleq at 0x0806c074, with no tail branches.
      The port calls crts_has_tag directly without a dispatch seam. Host tests
      cover NULL, wrong tag, zero, negative, one, and i32::MAX counters.

  # --- linked 'plst' item-count accessor ---
  - address: 0x08054214
    ghidra: FUN_08054214
    name: ui_plst_linked_item_count
    size: 24
    sig: "u32 ui_plst_linked_item_count(const u8 *owner)"
    module: ui/plst_linked_item_count
    status: ported
    renames:
      param_1/r0: owner
      "owner+0xf50": plst_element
      "plst_element+0x40": collection_header
      "collection_header+0x2e": item_count
    notes: >-
      Raw ARM extent is exactly 0x08054214..0x0805422c: ldr
      r0,[r0,#0xf50]; cmp r0,#0; ldrne r0,[r0,#0x40]; ldrhne
      r0,[r0,#0x2e]; moveq r0,#0; bx lr. The next separately linked
      function begins with push {r3,r4,r5,lr} at 0x0805422c, confirming
      Ghidra's 24-byte extent. The owner points at a linked 'plst' element
      because the same owner+0xf50 word goes to ui_plst_slot_item_at
      (0x08052728) in callers 0x08053170, 0x0806465c, and 0x0806705c;
      that already ported function reads its header at element+0x40 and
      range-checks its slots against the u16 at header+0x2e. This leaf
      returns that count zero-extended, or zero when owner+0xf50 is NULL.
      It deliberately has neither an owner NULL guard nor a header NULL
      guard. Decoding every ARM B/BL word in osos.dec finds 11 direct
      call sites, all plain unconditional bl and no predicated forms:
      0x0805318c, 0x08064670, 0x08067078, 0x081a330c, 0x081a33b8,
      0x081a3490, 0x081a3f7c, 0x081a4000, 0x081a4238, 0x081a4ad4,
      and 0x081a4c74. No deliberate deviations; Rust retains aligned
      u32/u16 loads and the exact one-link NULL gate. Host tests cover
      the NULL link plus zero, representative, and u16::MAX counts.

  # --- retailOS flag-gated opaque payload accessor ---
  - address: 0x08054454
    ghidra: FUN_08054454
    name: object_payload_if_available
    size: 20
    sig: "u32 object_payload_if_available(const void *object)"
    module: ui/object_payload
    status: ported
    renames:
      param_1/r0: object
      r1: availability_flags
      uVar1/r0: payload_address
    notes: >-
      Raw ARM extent is exactly 0x08054454..0x08054464: ldrb r1,
      [r0,#0x1d]; tst r1,#1; ldreq r0,[r0,#0x20]; movne r0,#0; bx lr.
      The next separately linked function begins at 0x08054468. Decoding
      every ARM B/BL word in osos.dec finds 28 direct call sites, all plain
      bl and no predicated forms; no DATA word contains this address. The
      leaf reads availability bit 0 at object+0x1d: when clear it returns
      the aligned 32-bit payload address at object+0x20, otherwise zero.
      It deliberately has no NULL or alignment guard. The owner and payload
      semantics remain unidentified, so the name describes only this
      verified behavior. The Rust ABI represents the stored target pointer
      as u32 to preserve the four-byte firmware layout on 64-bit hosts;
      focused host tests cover every flag byte, a zero payload, and payload
      suppression when bit 0 is set.

  # --- Timer E microsecond counter to millisecond tick ---
  - address: 0x081bb384
    ghidra: FUN_081bb384
    name: tick_millis
    size: 28
    sig: "u32 tick_millis(void)"
    module: drivers/timer
    status: ported
    renames:
      stack0/sp: counter_usec
      FUN_08086e38: read_usec_timer_into_2
      FUN_08036f14: __rt_udiv
    notes: >-
      Raw ARM extent is exactly 28 bytes, 0x081bb384..0x081bb3a0: `push
      {r3,lr}; mov r0,sp; bl 0x08086e38; ldr r0,[sp]; pop {ip,lr}; mov
      r1,#1000; b 0x08036f14`. The separately linked next function starts
      at 0x081bb3a0 with `push {r4,lr}` and there is no literal pool. Decoding
      every ARM B/BL word in osos.dec finds nine direct call sites, all plain
      unconditional bl, with zero predicated forms and zero tail b sites;
      aligned-word scanning finds no data reference to this address. It takes
      exactly one Timer E microsecond sample through read_usec_timer_into_2
      and returns its unsigned truncating division by 1000, i.e. elapsed
      milliseconds modulo 2^32. Ghidra's C falsely absorbs the tail branch
      into a 64-bit division body; the raw 28-byte boundary establishes the
      actual u32 result. Rust preserves the out-pointer helper and the ported
      ADS divider call with no deviation. Host tests cover exact truncation
      boundaries, u32::MAX, and the one-sample invariant.

  # --- adaptive tick accumulator step ---
  - address: 0x081bb3a0
    ghidra: FUN_081bb3a0
    name: tick_accumulator_step
    size: 48
    sig: "u32 tick_accumulator_step(TickAccumulator *accumulator, u32 input, u32 measured_rate)"
    module: app/tick_accumulator
    status: ported
    renames:
      param_1/r0: accumulator
      r1: input
      r2: measured_rate
      iVar1/r0: update_result
      FUN_081bb2a0: tick_accumulator_update
      FUN_081bb384: tick_millis
    notes: >-
      EXTENT: exactly 48 bytes, 12 instructions, 0x081bb3a0..0x081bb3d0;
      the next separately linked function (FUN_081bb3d0) begins
      immediately at 0x081bb3d0 and there is no trailing literal pool.
      CALL COUNT: decoding every ARM B/BL word in work/firmware/osos.dec
      finds exactly 23 direct call sites, all unconditional plain bl and
      no predicated BL forms, matching the assignment figure. SIGNATURE:
      Ghidra reports one parameter, but the bl at 0x081bb3b8 to the
      update sibling 0x081bb2a0 sets only r0; r1/r2 pass through from
      this function's own caller, and call sites verifiably load both
      argument registers first (mov r1,r4 / mov r2,#0 at
      0x08113dfc..0x08113e04; ldr r1,[r5,#0xc] / mov r2,r6 at
      0x0811a278..0x0811a284), so the true signature is three-argument.
      ALGORITHM: if last_tick_ms (+0x00) is nonzero, call the still
      unported update body 0x081bb2a0 (its sole call site) with all
      three arguments and take its r0 (the __rt_sdiv output-tick
      quotient; Ghidra mistypes the sibling void), else result = 0;
      store result to update_result (+0x20), stamp last_tick_ms from
      the unported tick_millis helper 0x081bb384, return update_result.
      A zero last_tick_ms (fresh or externally cleared accumulator)
      skips the update and zeroes the stored result. No NULL guard on
      the accumulator, matching the unconditional ldr [r0]. DEVIATION:
      the two unported callees ride the module's existing operation
      table (direct retail entry calls on target, fixtures on host),
      the same seam pattern as tick_accumulator_construct; match.py
      shows the expected blx-through-constant indirection, with
      0x081bb2a0 as a literal word and tick_millis derived as +0xe4.
      Host tests cover the seeded path (argument forwarding, result
      store and re-stamp), the zero-tick skip (update never runs,
      update_result zeroed), and zero-result propagation.

  # --- adaptive tick accumulator constructor ---
  - address: 0x081bb450
    ghidra: FUN_081bb450
    name: tick_accumulator_construct
    size: 176
    sig: "TickAccumulator *tick_accumulator_construct(TickAccumulator *accumulator, u32 input_divisor, u8 mode_enabled, u32 backoff_interval_ms)"
    module: app/tick_accumulator
    status: ported
    renames:
      param_1/r0: accumulator
      param_2/r1: input_divisor
      param_3/r2: mode_enabled
      param_4/r3: backoff_interval_ms
      FUN_081bb384: tick_millis
      FUN_080562ec: system_mode_enabled
      FUN_080b43e8: observer_getter
      DAT_081bb500: disabled_lower_input_bound
      DAT_081bb504: enabled_lower_input_bound
      DAT_081bb508: disabled_upper_input_bound
      DAT_081bb50c: enabled_upper_input_bound
    notes: >-
      EXTENT: Ghidra's 176 bytes describe code at
      0x081bb450..0x081bb500; the four trailing literal-pool words belong
      to this function, so raw bytes establish the true 192-byte extent
      0x081bb450..0x081bb510, with the next separately linked function
      opening at 0x081bb510. CALL COUNT: decoding every ARM B/BL word in
      work/firmware/osos.dec finds exactly 28 direct call sites, all
      unconditional plain bl and no predicated BL forms. The constructor
      samples the unported millisecond helper three times, initializes the
      0x34-byte accumulator's deadlines, scale factor, input fields and
      flags, then selects 10000/15000 or 17000/20000 input bounds through
      two unported system-mode queries. It stores the fourth argument as
      the backoff interval and passes a pointer to the local this pointer
      to vtable slot +0x1c of the singleton returned by unported
      observer_getter. The concrete observer identity is not established;
      the Rust name records only this verified registration role. Deliberate
      target-compatible deviation: the three unported callees are expressed
      as an operation table on hosts and as direct retail entry calls on
      target, so the freestanding link has no undefined externals. Host
      tests cover enabled and disabled bounds, exact field layout and
      observer registration, including ARM-wrapping tick-deadline addition.

  # --- global observer unregister ---
  - address: 0x081bb510
    ghidra: FUN_081bb510
    name: global_observer_unregister
    size: 39
    sig: "u8 *global_observer_unregister(u8 *object)"
    module: app/global_observer_unregister
    status: ported
    renames:
      param_1/r0: object
      FUN_080b43e8: global_observer_getter
      vtable[0x28]: unregister
    notes: >-
      EXTENT: raw ARM establishes 40 code bytes from 0x081bb510 through
      0x081bb538, despite Ghidra's 39-byte size; the next separately linked
      function begins at 0x081bb538. CALL COUNT: decoding every ARM B/BL word
      in work/firmware/osos.dec finds exactly 20 direct call sites, all
      unconditional plain bl and no predicated BL forms. The function saves
      object, passes the address of a stack-local object word to vtable slot
      +0x28 of the singleton supplied by the unported 0x080b43e8 getter, and
      returns the saved original object pointer even if the callback changes
      the local word. ROLE: the +0x28 role is inferred as unregister because
      the adjacent 0x081bb450 constructor registers the same indirect pointer
      form through the same observer's +0x1c slot; the observer's concrete
      identity remains unknown. DEVIATION: target calls the retail getter at
      its fixed address while host tests use a getter seam.

  # --- field-query result block scope-exit cleanup ---
  - address: 0x081bfb50
    ghidra: FUN_081bfb50
    name: field_query_result_release
    size: 60
    sig: "FieldQueryResult *field_query_result_release(FieldQueryResult *result)"
    module: util/field_query_result_release
    status: ported
    renames:
      param_1/r0/r4: result
      r5: result + 0x200 (flags halfword base)
      FUN_08049398: heap_free (retailOS boundary, unported)
    notes: >-
      EXTENT: raw ARM confirms exactly 60 bytes 0x081bfb50..0x081bfb8c; the
      separately linked sibling FUN_081bfb8c begins immediately afterward
      (push {r0-r8, lr}), so Ghidra's 60-byte size is right for once. CALL
      COUNT: decoding every ARM B/BL word in work/firmware/osos.dec finds
      exactly 16 direct call sites, all unconditional plain bl (zero
      predicated forms, zero B tails), clustered at 0x081be7f0..0x081bf7bc
      immediately before the function itself. BEHAVIOUR: scope-exit cleanup
      for the 0x238-byte record field-query result block (the same layout
      object_install_field_text @ 0x08056048 builds on its stack): if bit 0
      of the flags halfword at +0x208 is set, the text pointer at +0x210 is
      a heap allocation — it is released through the retailOS heap free
      0x08049398, the flag bit is cleared (every other flag preserved), and
      the pointer nulled. The block pointer is returned in r0
      (destructor-style this passthrough); every caller runs it on a
      stack-local block right after a query through 0x08051a48 and
      immediately overwrites r0, so the return is never consumed. The
      original re-reads the flags halfword after the free (second ldrh),
      which the port reproduces via an in-place &= ~1. DEVIATIONS: heap
      free stays in retailOS behind a HEAP_FREE static boundary (fixed
      address 0x08049398 on target, recording stub on host), so the call is
      a blx through the static rather than a direct bl; LLVM also emits the
      pointer-nulling store before the flags-clearing strh (the original
      clears flags first) — both stores are inside the same conditional to
      disjoint fields and are unobservable. match.py: structural diff only
      (seam blx, register allocation), exit 1 as expected. Host tests prove
      flag-clear no-op (no free call, block untouched, returns block), free
      path (one free with the text pointer, bit cleared, pointer nulled,
      returns block), and preservation of all other flag bits (0xffff ->
      0xfffe).

  # --- growable target-word buffer ---
  - address: 0x082b8174
    ghidra: FUN_082b8174
    name: word_buffer_reserve
    size: 76
    sig: "WordBuffer *word_buffer_reserve(WordBuffer *buffer, u32 requested_capacity)"
    module: heap/word_buffer
    status: ported
    renames:
      param_1/r0: buffer
      param_2/r1: requested_capacity
      iVar1/r0: new_data
    notes: >-
      Raw extent is 0x082b8174..0x082b81c0; the following multiply helper
      begins immediately at 0x082b81c0. Decoding every B/BL in osos.dec
      found 27 direct calls, all unconditional BL. When capacity (+0x08) is
      below requested_capacity it calls the unported 0x080a7f2c, which
      allocates requested_capacity + 1 words, copies len (+0x04) words and
      zeroes the new remainder. NULL leaves the header unchanged and returns
      NULL; success conditionally releases old data (+0x00) through ported
      traced_free 0x08043994, then stores new data and requested capacity.
      Deviation: 0x080a7f2c remains a volatile hook (stock target address on
      firmware, NULL on host) because it is not ported.

  - address: 0x0804082c
    ghidra: FUN_0804082c
    name: word_buffer_reset_optional_singleton
    size: 88
    sig: "u32 word_buffer_reset_optional_singleton(MarkedWordBuffer *buffer, u32 value)"
    module: heap/word_buffer
    status: ported
    renames:
      param_1/r0: buffer
      param_2/r1: value
      puVar1/r0: reserved_buffer
    notes: >-
      Raw extent is 0x0804082c..0x08040884; the following independent
      function starts at 0x08040884. Decoding every ARM B/BL word in
      osos.dec found 19 direct callers: 18 unconditional BL and one BLNE at
      0x0803e9e8. The BLNE is gated by its caller's NE condition; this
      function has no NULL guard. It reserves two words only when capacity
      (+0x08) is zero, returns zero on failed reserve without mutation, and
      otherwise clears marker (+0x0c), resets length (+0x04), writes value
      through data (+0x00), and sets length to one iff value is nonzero.

  - address: 0x0803e6e8
    ghidra: FUN_0803e6e8
    name: marked_word_buffer_assign
    size: 212
    sig: "MarkedWordBuffer *marked_word_buffer_assign(MarkedWordBuffer *target, const MarkedWordBuffer *source)"
    module: heap/word_buffer
    status: ported
    renames:
      param_1/r0: target
      param_2/r1: source
      piVar1/r0: reserved_target
      puVar2/r0: target_words
      puVar4/r1: source_words
      iVar3/r2: remaining_blocks
      uVar5/r2: trailing_words
      FUN_082b8174: word_buffer_reserve
    notes: >-
      Raw extent is exactly 0x0803e6e8..0x0803e7bc (53 ARM instructions);
      the separately linked parsing function at 0x0803e7bc follows
      immediately. Decoding every ARM B/BL immediate in osos.dec found 12
      direct callers: 10 unconditional BL plus BLNE at 0x08040330 and
      0x08040a18. Both BLNE callers gate the call on NE; aside from the
      pointer-equality fast path the body has no NULL guard. It returns target
      unchanged for self-assignment; otherwise it reserves source len (+0x04)
      through ported word_buffer_reserve when target capacity (+0x08) is too
      small, returns NULL unchanged on reserve failure, forward-copies len
      u32 data words in four-word blocks plus a tail, retains target capacity,
      stores len, zeroes target data[0] iff len is zero and data is non-NULL,
      then copies marker (+0x0c). Deliberate deviations: none.

  # --- tagged word buffer pool nesting pop ---
  - address: 0x0803dd84
    ghidra: FUN_0803dd84
    name: tagged_word_buffer_pool_pop
    size: 68
    sig: "void tagged_word_buffer_pool_pop(TaggedWordBufferPool *pool)"
    module: heap/tagged_word_buffer_pool
    status: ported
    renames:
      param_1/r0: pool
      iVar1/r0: nesting_depth
      param_1_plus_0x0: slot_count
      param_1_plus_0x288: nesting_depth
      param_1_plus_0x28c: saved_slot_counts
      FUN_0803dec8: tagged_word_buffer_pool_push
    notes: >-
      Raw ARM confirms the 68-byte extent 0x0803dd84..0x0803ddc8, followed
      by separately linked three_buffer_owner_release at 0x0803ddc8.
      Decoding every ARM B/BL immediate in osos.dec found 11 direct callers,
      all unconditional BL. It returns immediately for NULL. Otherwise, when
      nesting_depth is zero it calls the ported tagged_word_buffer_pool_push,
      clears overflow_reported, decrements nesting_depth with u32 wraparound,
      and restores saved_slot_counts[depth] only when that decremented depth is
      signed less than 12. This preserves the ARM signed condition and its
      unchecked raw pointer arithmetic for bit-31-set depths. Deliberate
      deviations: none.

  # --- tagged word buffer pool nesting push ---
  - address: 0x0803dec8
    ghidra: FUN_0803dec8
    name: tagged_word_buffer_pool_push
    size: 36
    sig: "void tagged_word_buffer_pool_push(TaggedWordBufferPool *pool)"
    module: heap/tagged_word_buffer_pool
    status: ported
    renames:
      param_1/r0: pool
      iVar1/r1: nesting_depth
      iVar2/r2: slot_count
      param_1_plus_0x0: slot_count
      param_1_plus_0x288: nesting_depth
      param_1_plus_0x28c: saved_slot_counts
    notes: >-
      Raw ARM confirms the 36-byte extent 0x0803dec8..0x0803deec, followed
      by the separately linked three_buffer_owner_release at 0x0803deec.
      Decoding every ARM B/BL word in osos.dec found 11 direct calls: ten
      unconditional BL plus BLEQ at 0x0803dd9c, whose caller gates it on an
      equal comparison before this no-NULL-guard body. It saves slot_count
      into saved_slot_counts[nesting_depth] when nesting_depth is signed less
      than 12, then increments nesting_depth with u32 wraparound. The signed
      condition deliberately permits bit-31-set depths and the resulting
      unchecked raw pointer arithmetic. Deliberate deviations: none.

  # --- tagged word buffer pool slot allocator ---
  - address: 0x0803de0c
    ghidra: FUN_0803de0c
    name: tagged_word_buffer_pool_take
    size: 100
    sig: "TaggedWordBuffer *tagged_word_buffer_pool_take(TaggedWordBufferPool *pool)"
    module: heap/tagged_word_buffer_pool
    status: ported
    renames:
      param_1/r0: pool
      iVar1: slot_index
      iVar2: nesting_depth
      piVar3: slot
      param_1_plus_0x0: slot_count
      param_1_plus_0x4: slots
      param_1_plus_0x284: flags
      param_1_plus_0x288: nesting_depth
      param_1_plus_0x28c: saved_slot_counts
      param_1_plus_0x2bc: overflow_reported
    notes: >-
      The object is a 0x2c0-byte pool of 32 embedded TaggedWordBuffer slots.
      When nesting_depth is below 13 the function uses slot_count as the
      selected slot index; otherwise it uses nesting_depth directly. If the
      chosen index is below 32, it stores index + 1 back to slot_count and
      returns the embedded slot. On overflow it records
      diag_ring_record(3, 0x74, 0x6d, 0, 0) once, latches overflow_reported,
      and returns NULL. The ported tagged_word_buffer_pool_push and
      tagged_word_buffer_pool_pop helpers maintain nesting_depth and
      saved_slot_counts on the same object; this port keeps the same unchecked
      raw pointer arithmetic and a direct call to the already-ported diagnostic
      logger.
  # --- three-buffer owner destructor ---
  - address: 0x0803deec
    ghidra: FUN_0803deec
    name: three_buffer_owner_release
    size: 60
    sig: "void three_buffer_owner_release(ThreeBufferOwner *this)"
    module: heap/three_buffer_owner
    status: ported
    renames:
      param_1/r0: this
      param_1_plus_0x04: first
      param_1_plus_0x18: second
      param_1_plus_0x2c: third
      param_1_plus_0x44: flags
      FUN_0803eee4: releasable_buffer_release
      FUN_08043994: traced_free
    notes: >-
      Raw extent is exactly 0x0803deec..0x0803df28 (15 ARM instructions);
      the independently linked sibling at 0x0803df28 follows immediately,
      with no trailing literal pool. Decoding every ARM B/BL immediate in
      osos.dec finds exactly 11 direct callers: eight unconditional BL
      (0x0803f940, 0x0808e6b8, 0x0808e6f0, 0x0808e748, 0x080c6eac,
      0x080c6f04, 0x080c7184, and 0x080c71dc) and three BLNE
      (0x0808777c, 0x08087788, and 0x08087794). The three BLNE call sites
      independently gate destruction on owner words, while this function
      still returns on NULL. It calls ported releasable_buffer_release in
      order for embedded buffers at +0x04, +0x18, and +0x2c, then tests
      outer flags +0x44 bit 0 and frees this through ported traced_free when
      set. Deliberate deviation: the retail final tail branch to traced_free
      is an ordinary returning Rust call; traced_free returns, so behavior is
      unchanged.
  # --- heap debug-fill helper ---
  - address: 0x0805f440
    ghidra: FUN_0805f440
    name: heap_poison
    size: 92
    sig: "void heap_poison(void *dst, u32 len)"
    module: heap/heap_poison
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: len
      pcVar2/r2: cursor
      iVar3/r3: remaining
      DAT_0805f49c: large_alloc_tag
      FUN_08031180: memchr
    notes: >-
      Raw extent is exactly 0x0805f440..0x0805f49c (92 bytes); the next word
      at 0x0805f49c is the literal pointer 0x08a0ea04, and the adjacent
      thunk FUN_080568e8 begins at 0x0805f4a0. A complete ARM B/BL immediate
      scan of osos.dec found 19 inbound direct calls, all unconditional bl:
      0x0803b54c, 0x0803e604, 0x0803e628, 0x08044098, 0x08049c4c,
      0x0804ac90, 0x0806027c, 0x0806219c, 0x0806302c, 0x08063244,
      0x08064fa4, 0x080b1b8c, 0x080b1ba8, 0x080c6fb4, 0x080c7288,
      0x080cbaf4, 0x080cbe38, 0x080e8d24, and 0x080ef1fc; no direct b
      tail calls or predicated direct calls. For each byte, store the current
      tag then set tag = tag + ((next_dst & 15) + 17), wrapping to u8. It
      calls ported memchr(dst, tag, len) and adds 63 to the tag on a match.
      Deviation: retailOS address 0x08a0ea04 is represented by the existing
      shared LARGE_ALLOC_TAG crate static rather than an absolute host address.
  # --- releasable buffer destructor ---
  - address: 0x0803eee4
    ghidra: FUN_0803eee4
    name: releasable_buffer_release
    size: 68
    sig: "void releasable_buffer_release(ReleasableBuffer *this)"
    module: heap/releasable_buffer
    status: ported
    renames:
      param_1/r0: this
      param_1_plus_0x0: data
      param_1_plus_0x10: flags
      FUN_08043994: traced_free
    notes: >-
      Raw extent is exactly 0x0803eee4..0x0803ef28 (17 ARM instructions);
      FUN_0803ef28 starts the next independently linked function, with no
      trailing literal pool. Decoding every ARM B/BL immediate in osos.dec
      finds exactly 17 direct call sites, matching Ghidra's count: 10
      unconditional BL, three BLNE (0x0803dbbc, 0x0803dbc8, 0x0803e5c8),
      and four BLEQ (0x0803e940, 0x0803eed8, 0x0803f344, 0x0803ff68).
      The predicated call sites are caller-side NULL/flag gates; the body
      still returns on NULL. It reads data (+0x00) and flags (+0x10), frees
      non-NULL data through ported traced_free when flags bit 1 is clear,
      reloads flags after that free, sets bit 15, and if reloaded bit 0 was
      set releases this through traced_free. Data is deliberately left
      dangling, so repeated release of an owned buffer double-frees just as
      retailOS does. Deliberate deviation: the final retail tail branch to
      traced_free is an ordinary returning Rust call; traced_free returns, so
      behavior is unchanged.

  # --- tagged word buffer destructor ---
  - address: 0x0803e5e4
    ghidra: FUN_0803e5e4
    name: tagged_word_buffer_destroy
    size: 92
    sig: "void tagged_word_buffer_destroy(TaggedWordBuffer *this)"
    module: heap/tagged_word_buffer
    status: ported
    renames:
      param_1/r0: this
      FUN_0805f440: heap_poison
      FUN_08043994: traced_free
    notes: >-
      Raw extent is exactly 0x0803e5e4..0x0803e640 (23 instructions, no
      trailing literal pool): the sibling key comparator FUN_0803e640
      begins immediately at 0x0803e640. Decoding every B/BL word in
      osos.dec found 22 direct call sites, matching Ghidra: 14
      unconditional bl plus 8 blne (all at 0x080623cc..0x08062420, each
      immediately preceded by cmp r0,#0 - those callers NULL-check even
      though the destructor has its own movs/popeq guard); no b tail
      calls. The object is five target words {data +0x00, len +0x04,
      capacity +0x08, tag +0x0c, flags +0x10}: the first three match
      WordBuffer (heap/word_buffer.rs), and the comparator @ 0x0803e640
      pins the layout - it orders by tag (zero tag sorts above nonzero),
      then signed len, then an unsigned lexicographic walk of the data
      u32 elements. Algorithm: NULL this returns; if data != NULL, poison
      FUN_0805f440(data, capacity << 2) then traced_free(data) unless
      flags bit 1 (buffer borrowed); flags is RELOADED from +0x10 after
      the buffer release (0x0803e618 - a flag stored by either call is
      honored, pinned by a host test), bit 0 latched, the object poisoned
      with a literal 20 (FUN_0805f440(this, 20)), and if the latched bit
      was set the original tail-branches bne traced_free(this) - the
      scalar deleting destructor bit. The now-ported heap_poison is called
      directly, sharing LARGE_ALLOC_TAG with traced_alloc; its byte-fill and
      state evolution are covered independently. The tail branch remains an
      ordinary returning call, as documented by traced_free.

  # --- service-manager handler access ---
  - address: 0x08194080
    ghidra: FUN_08194080
    name: service_handler_at
    size: 16
    sig: "u32 service_handler_at(const u32 *slot_table, i32 selector)"
    module: app/service_manager
    renames:
      param_1/r0: slot_table
      param_2/r1: selector
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Raw extent is exactly 0x08194080..0x08194090: `cmp r1,#3; blge
      0x08030f44; ldr r0,[r0,r1,lsl #5]; bx lr`; 0x08194090 (`mov ip,r0`)
      starts the next separately linked function. It returns word zero from
      one of three 8-word primary handler records. The signed `blge` admits
      negative selectors, which address unchecked before the table exactly as
      retailOS does; selectors >=3 terminate through ported non-returning
      heap_panic. Decoding every ARM B/BL word in osos.dec found exactly 20
      direct call sites: all unconditional plain BL, with no predicated forms
      or tail branches. The concrete handler identities are not established,
      so the name records only the verified table role. Deliberate deviations:
      none. Host coverage proves both table edges, NULL-word propagation, and
      the uncached reload.

  - address: 0x08193e50
    ghidra: FUN_08193e50
    name: service_manager_secondary_handler_state_flags_get
    size: 20
    sig: "u32 service_manager_secondary_handler_state_flags_get(const u32 *slot_table, i32 slot)"
    module: app/service_manager
    status: ported
    renames:
      param_1/r0: slot_table
      param_2/r1: slot
      param_1_plus_slot_times_0x20_plus_0x10: state_flags
      FUN_08030f44: heap_panic
    notes: >-
      Raw extent is exactly 0x08193e50..0x08193e64: `cmp r1,#3; blge
      0x08030f44; add r0,r0,r1,lsl #5; ldr r0,[r0,#16]; bx lr`; the next
      `cmp r1,#3` at 0x08193e64 begins the separately linked byte-constrained
      setter. It reads the full word at +0x10 from one of three 8-word
      secondary handler records. The signed `blge` admits negative slots,
      which address unchecked before the table exactly as retailOS does; slots
      >=3 terminate through ported non-returning heap_panic. The paired setter
      only accepts values <=0xff, while callers mask this word as a bitfield,
      so state_flags records the observed field behavior without inventing a
      concrete identity. Decoding every ARM B/BL word in osos.dec found
      exactly 10 inbound direct call sites — 0x0818e330, 0x08190710,
      0x081932a0, 0x08193ae4, 0x08194aac, 0x081a96fc, 0x081d6d54,
      0x081d76e4, 0x082011fc, and 0x08209310 — all unconditional plain BL;
      no predicated forms or tail branches. Deliberate deviations: none. Host
      coverage proves all valid record offsets, full-word propagation,
      uncached reload, and the signed negative-slot path.

  - address: 0x08193e84
    ghidra: FUN_08193e84
    name: service_manager_secondary_handler_code_get
    size: 20
    sig: "u16 service_manager_secondary_handler_code_get(const u32 *slot_table, i32 slot)"
    module: app/service_manager
    status: ported
    renames:
      param_1/r0: slot_table
      param_2/r1: slot
      param_1_plus_slot_times_0x20_plus_0x1c: handler_code
      FUN_08030f44: heap_panic
    notes: >-
      Raw extent is exactly 0x08193e84..0x08193e98: `cmp r1,#3; blge
      0x08030f44; add r0,r0,r1,lsl #5; ldrh r0,[r0,#28]; bx lr`; the next
      `cmp r1,#3` at 0x08193e98 begins the separately linked setter. It
      returns the low u16 at +0x1c in one of three 8-word secondary handler
      records. The signed `blge` admits negative slots, which address
      unchecked before the table exactly as retailOS does; slots >=3 terminate
      through ported non-returning heap_panic. Decoding every ARM B/BL word in
      osos.dec found exactly 17 inbound direct call sites, all unconditional
      plain BL (0x08138734, 0x08138ce4, 0x08139940, 0x08163b60, 0x08163dc8,
      0x0818e448, 0x0818e87c, 0x0818ef90, 0x0818f338, 0x08190464,
      0x08190650, 0x081917f8, 0x08192074, 0x0819279c, 0x08192c54,
      0x08193984, and 0x08195714); no predicated forms or tail branches.
      Callers pass the result to a u16-keyed lookup table, so it is named a
      handler code without asserting a concrete code identity. Deliberate
      deviations: none. Host coverage proves all valid record offsets,
      halfword truncation, uncached reload, and the signed negative-slot
      path using an allocation that makes the retail address valid.
  - address: 0x08193ee8
    ghidra: FUN_08193ee8
    name: service_manager_slot_handler_get
    size: 20
    sig: "void *service_manager_slot_handler_get(const u32 *slot_table, i32 slot)"
    module: app/service_manager
    status: ported
    renames:
      param_1/r0: slot_table
      param_2/r1: slot
      param_1_plus_slot_times_8_plus_0x64: handler
      FUN_08030f44: heap_panic
    notes: >-
      Raw extent is exactly 0x08193ee8..0x08193efc: `cmp r1,#13; blge
      0x08030f44; add r0,r0,r1,lsl #3; ldr r0,[r0,#100]; bx lr`; the next
      `cmp r1,#3` at 0x08193efc begins a separately linked function. It
      returns word one from one of thirteen 8-byte slot records starting at
      slot_table+0x60; word zero is the flags bitmask. The signed `blge`
      admits negative slots, which then address unchecked before the bank
      exactly as retailOS does; slots >=13 terminate through ported
      non-returning heap_panic. Decoding every ARM B/BL word in osos.dec
      found exactly 14 inbound direct call sites — 0x08163b7c, 0x08164938,
      0x0816494c, 0x081652bc, 0x081653b4, 0x0818e170, 0x0818f844,
      0x0818fa8c, 0x08190ccc, 0x08191088, 0x08192bdc, 0x081d6bec,
      0x081d6cd8, 0x081d6d70 — all unconditional plain BL; no predicated
      forms or tail branches. The pointer's concrete handler identity is not
      established, so the name records only its verified slot-table role.
      Deliberate deviations: none. Host coverage proves all bank edges, NULL
      propagation, an uncached reload, and the signed negative-slot path
      using a low u32-addressable fixture.
  - address: 0x08193ec0
    ghidra: FUN_08193ec0
    name: service_manager_secondary_handler_get
    size: 20
    sig: "void *service_manager_secondary_handler_get(const u32 *slot_table, i32 slot)"
    module: app/service_manager
    renames:
      param_1/r0: slot_table
      param_2/r1: slot
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Raw extent is exactly 0x08193ec0..0x08193ed4: five ARM instructions;
      the following `cmp r1,#13` opens the separately linked 0x08193ed4
      setter. Raw ARM is `cmp r1,#3; blge 0x08030f44; add r0,r0,r1,lsl #5;
      ldr r0,[r0,#8]; bx lr`. It returns word +2 of one of three 8-word
      secondary handler records. The signed `blge` admits negative slots,
      which then address unchecked exactly as the retailOS code does; slots
      >=3 terminate through the ported non-returning heap_panic. Decoding
      every ARM B/BL word in osos.dec found exactly 27 direct call sites:
      all are unconditional plain BL, with no predicated forms or tail
      branches. The secondary table's concrete handler identities are not
      established, so the name records only its verified role. Deliberate
      deviations: none. Host coverage proves all three valid slot offsets,
      NULL propagation, and the uncached reload.

  - address: 0x0819420c
    ghidra: FUN_0819420c
    name: service_manager_slot_flags_or
    size: 28
    sig: "void service_manager_slot_flags_or(u32 *slot_table, i32 slot, u32 flags)"
    module: app/service_manager
    renames:
      param_1/r0: slot_table
      param_2/r1: slot
      param_3/r2: flags
      param_1_plus_slot_times_8_plus_0x60: slot_flags
      FUN_08030f44: heap_panic
    status: ported
    notes: >-
      Raw extent is exactly 0x0819420c..0x08194228, matching Ghidra's 28
      bytes: `cmp r1,#13; blge 0x08030f44; add r0,r0,r1,lsl #3; ldr
      r1,[r0,#96]; orr r1,r1,r2; str r1,[r0,#96]; bx lr`; the next
      function starts at 0x08194228 (`push {r4,lr}`, the slot-table
      constructor). It OR-accumulates a bitmask into word 0 of one of
      thirteen 8-byte slot records at slot_table+0x60; word 1 is the
      slot's handler object pointer (stored by the setter @ 0x08193ed4,
      read back @ 0x08193ee8) and the whole 0x00..0xc8 span is zeroed by
      the constructor @ 0x08194228. The signed `blge` admits negative
      slots, which address unchecked before the bank exactly as retailOS
      does; slots >= 13 terminate through ported non-returning heap_panic.
      Observed callers accumulate multi-bit masks (0x2c into slot 7 from
      0x0819535c's caller, 0x2d into slot 0xc), so this is always an
      accumulate, never a replace; the flags word's concrete bit semantics
      are not established, and the name records only the verified
      mechanism. Decoding every ARM B/BL word in osos.dec found exactly 16
      direct call sites, all unconditional plain BL (0x0813a308,
      0x08165120, 0x08193d00, 0x0819535c, 0x081953c0, 0x08196f68,
      0x081af32c, 0x081d7c7c, 0x081e3100, 0x081f26b4, 0x081f2720,
      0x081f394c, 0x08200e88, 0x08201254, 0x0820956c, 0x082095cc); no
      predicated forms, no tail branches, and no aligned data word holds
      the address, so it is not vtable-dispatched. Deliberate deviations:
      none. LLVM if-converts the guard (cmp #12 + predicated add/ldr/orr/
      str/bxle falling through to a relocated bl heap_panic); the port
      carries its own link_section .text.service_manager_slot_flags_or and
      the symbol resolves in librustypod.a. Host coverage proves the OR
      accumulate at table edges (slots 0 and 12), preservation of existing
      flags, the untouched handler-pointer neighbor word, a zero-mask
      no-op, and the signed negative-slot path.

  - address: 0x081946ec
    ghidra: FUN_081946ec
    name: active_service_handler_is_ready
    size: 76
    sig: "u32 active_service_handler_is_ready(void)"
    module: app/active_service_handler_readiness
    renames:
      DAT_08194734: active_service_handler_context_slot (0x089ccb5c)
      FUN_081391ec: service_manager_instance_veneer
      FUN_08138d8c: service_handler_state_is_ready
    status: ported
    notes: >-
      EXTENT CORRECTED: Ghidra's reported 72 bytes covers the eighteen ARM
      instructions at 0x081946ec..0x08194734, but the `ldr r5,[pc,#60]`
      consumes the following literal 0x089ccb5c at 0x08194734. The next
      separately linked function starts at 0x08194738 (`push {r4-r7,r8,lr}`),
      so the raw extent is 76 bytes. Decoding every ARM B/BL word in
      osos.dec finds exactly 14 direct callers, all unconditional plain BL
      (0x08194274, 0x0819434c, 0x081948f4, 0x0819496c, 0x081949e8,
      0x08194a84, 0x08194b74, 0x08194bfc, 0x08194e88, 0x08194f04,
      0x08194f80, 0x08195010, 0x081950a8, 0x08195124); no predicated BL
      forms or tail branches target it. It reads the active context pointer
      from 0x089ccb5c and returns zero unless its handler word +0x2d0 is
      nonzero and its selector word +0x2d4 compares below three as signed.
      The passing path obtains the service-manager singleton through the
      already ported veneer, then calls the unported 0x08138d8c lifecycle
      predicate and booleanizes the result. The context type and handler
      identity are unrecovered, so the names state only this verified role.
      Deliberate deviation: the runtime-initialized global slot is a host
      fixture; 0x08138d8c shares the established volatile dispatch seam in
      app/service_handler_availability rather than acquiring a second stub.
      A selector word with the sign bit set passes the signed ARM `bge`
      guard and is deliberately forwarded unchanged, as host coverage proves.

  - address: 0x08195214
    ghidra: FUN_08195214
    name: schedule_iap_packet_event
    size: 196
    sig: "u32 schedule_iap_packet_event(void *unused_context, IapPacket *packet, u32 completion_state, u32 delay_ms)"
    module: app/iap_packet_event_schedule
    status: ported
    renames:
      param_1/r0: unused_context
      param_2/r1: packet
      param_3/r2: completion_state
      param_4/r3: delay_ms
      DAT_081952d0: active_service_handler_context_slot (0x089ccb5c)
      DAT_081952d4: active_context_pending_event_sequence_offset (0x2fa)
      param_2_plus_0x10: packet_command_tag
      active_context_plus_0x2f4: pending_event_timestamp_ms
      active_context_plus_0x2f8: pending_event_active
      active_context_plus_0x2fa: pending_event_sequence
    notes: >-
      Raw extent is 196 bytes, not Ghidra's code-only 188: the 47
      instructions end with pop {r4-r8,pc} at 0x081952cc, followed by
      literal words 0x089ccb5c and 0x000002fa at 0x081952d0/0x081952d4;
      0x081952d8 begins the next separate function. Decoding every ARM B/BL
      word in osos.dec found exactly 13 inbound direct calls, all
      unconditional plain BL (0x081942e8, 0x08194948, 0x081949c0,
      0x08194a50, 0x08194b2c, 0x08194bd4, 0x08194c50, 0x08194edc,
      0x08194f58, 0x08194fe4, 0x0819507c, 0x081950fc, 0x08195178);
      no predicated BL targets it. It obtains packet owner mode, snapshots
      the packet command halfword, then calls unported 0x081d7f14 with the
      packet and completion state. Only when that returns zero does it insert
      a zero-payload pending event into the active context: key=owner mode,
      tag_a=the pre-increment context +0x2fa halfword, tag_b=packet command,
      delay=param_4. A successful insert alone constructs a kind-1 clock,
      reads it through vtable +0x0c, writes its milliseconds to +0x2f4,
      sets +0x2f8 to 1, and increments the sequence with u16 wrapping.
      The first ABI word is stacked then overwritten with zero before the
      clock output path and is otherwise unused. The unported 0x081d7f14
      is left as a volatile target/host seam: its decoded body changes packet
      +0x1a and submits a worker request, but that worker protocol is not
      recovered, so no stronger callee identity is invented. The existing
      pending-event clock-read seam also models this function's runtime
      vtable dispatch. Host tests prove completion-error short-circuiting,
      successful event tags/clock/state including sequence wrap, and
      queue-exhaustion preservation of the timestamp, active byte and
      sequence.


  # --- AppMotor fixed singleton accessor ---
  - address: 0x08295be0
    ghidra: FUN_08295be0
    name: app_motor_get
    size: 88
    sig: "void *app_motor_get(void)"
    module: app/app_motor
    status: ported
    renames:
      DAT_08295c28: APP_MOTOR_GUARD (0x089ca8b8)
      DAT_08295c2c: APP_MOTOR (0x08a1b774)
      DAT_08295c30: DSO_HANDLE (0x089ca09c = __dso_handle)
      DAT_08295c34: dtor-pool-word (0x0828bf04, not a function entry)
      FUN_08296cc8: APP_MOTOR_CTOR seam (unported AppMotor constructor)
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab1c8: cxa_atexit
      FUN_082ab338: cxa_guard_release
    notes: >-
      EXTENT CORRECTED - Ghidra's 72 bytes are only the instructions at
      0x08295be0..0x08295c28. Its four literal-pool words
      0x08295c28..0x08295c38 belong to the accessor; the next separately
      linked function begins with `push {r4-r8,lr}` at 0x08295c38. The
      raw extent is therefore 88 bytes. CALL COUNT: decoding every ARM
      B/BL word in work/firmware/osos.dec finds exactly 27 direct call
      sites, all unconditional plain bl: no predicated BL forms and no
      plain-b tail sites.
      This is the ADS function-local-static accessor for the fixed
      AppMotor object at 0x08a1b774. It tests guard word 0x089ca8b8 bit
      0; when clear, cxa_guard_acquire decides whether to call
      FUN_08296cc8(object), registers the constructor's r0 with
      cxa_atexit(r0, 0x0828bf04, __dso_handle), then calls the no-op
      cxa_guard_release. It always reloads and returns the object
      literal, not the constructor's result. The raw constructor has the
      `eAppMotor` literal and writes its last word at object+0x2b4, so the
      object is 0x2b8 bytes.
      Deliberate deviations: guard and fixed object are crate statics;
      the unported constructor is APP_MOTOR_CTOR with a host-safe zeroing
      default, so this accessor is not hook-ready until that constructor
      is ported or installed. The destructor pool word 0x0828bf04 is NOT
      a function entry: raw disassembly places it inside FUN_0828bed0 on
      its conditional `beq 0x0828bf10`. The port records this anomaly
      rather than inventing a callee and registers a shutdown-safe no-op,
      matching the observable retail path where this chain is not run.

  # --- current calendar UTC-offset query ---
  - address: 0x080ab558
    ghidra: FUN_080ab558
    name: current_utc_offset_query
    size: 68
    sig: "i32 current_utc_offset_query(i16 *base_utc_offset_minutes, u8 *daylight_saving_minutes)"
    module: time/utc_offset
    status: ported
    renames:
      param_1/r0: base_utc_offset_minutes
      param_2/r1: daylight_saving_minutes
      auStack_20: calendar_query_record
      FUN_08037db8: iram_memzero_aligned_veneer
      FUN_08056524: current_datetime_query
    notes: >-
      Raw extent is exactly 0x080ab558..0x080ab59c: the next separately
      linked function starts at 0x080ab59c. Decoding every ARM B/BL word in
      osos.dec finds 27 direct BL call sites, all unconditional plain BL:
      no predicated BL forms. It also has two unconditional plain-B tail
      sites at 0x080a6b00 and 0x08259be8. The retail body zeroes a 20-byte
      aligned stack record through the ported 0x08037db8 veneer, calls
      unported current_datetime_query (0x08056524), then independently writes
      its signed little-endian base UTC offset in minutes from record+10 and
      its daylight-saving adjustment in minutes from record+12 when each
      output pointer is non-NULL. It returns the provider's 0/1 validity
      result unchanged; Ghidra's void signature loses this r0 pass-through.
      The Rust target calls the unported provider's fixed load address; host
      tests install a deterministic provider. Deliberate code-generation
      deviation: LLVM emits five aligned word stores for the 20-byte stack
      clear rather than calling the IRAM memzero veneer; the initialized
      record and observable behavior are unchanged.

  # --- character-class flag-mask predicate ---
  - address: 0x082a7258
    ghidra: FUN_082a7258
    name: character_class_matches_mask
    size: 24
    sig: "u32 character_class_matches_mask(const CharacterClass *class, u16 mask, u32 character)"
    module: cxx/character_class
    status: ported
    renames:
      param_1/r0: class
      param_2/r1: mask
      param_3/r2: character
      "[class+0x14]": flags
      "[flags+character*2]": character_flags
      r0 return: matches_mask
    notes: >-
      Raw extent is exactly 0x082a7258..0x082a7270: the next separately
      linked function starts at 0x082a7270. Decoding every ARM B/BL word in
      osos.dec finds exactly 26 direct call sites, all unconditional plain
      BL; there are no predicated BL forms and no plain-B tail sites. The
      six-instruction leaf loads the aligned u16 table pointer at class+0x14,
      indexes it by character*2, masks the selected flag word, and
      normalizes the result to 0 or 1. Callers use masks 0x8 and 0x10 while
      scanning and case-folding text, so the recovered table is named a
      character-class flag table; its unread 20-byte object prefix remains
      explicitly unrecovered. The function has no NULL or bounds guards and
      performs no writes. No deviations; host tests cover zero and absent
      masks, a matching bit, a high u16-table index, normalized return
      values, and input preservation. match.py structural diff reviewed
      after the ARM release build.

  # --- command descriptor layout baseline ---
  - address: 0x082a4680
    ghidra: FUN_082a4680
    name: descriptor_layout_initialize
    size: 180
    sig: "void descriptor_layout_initialize(const u32 *source, u32 *layout)"
    module: app/descriptor_layout
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: layout
      DAT_082a4730: NONE_TOKEN (0x4e6f6e65)
      FUN_08270f84: descriptor_field_map (ported direct call)
    notes: >-
      Raw extent is 0x082a4680..0x082a4734: Ghidra's reported 176 bytes
      covers the 44 ARM instructions but drops its final literal-pool word
      0x4e6f6e65 at 0x082a4730; the next separately linked function starts
      at 0x082a4734. Decoding every ARM B/BL word in osos.dec finds exactly
      25 direct call sites, all unconditional plain BL; there are no
      predicated BL or plain-B tail callers. The body initializes a
      command-builder descriptor layout with the None token, its source
      current-value word twice, a -1 sentinel, fixed 0x200 and source-bit
      derived flags, then optional source metadata and state. It maps four
      source triples through descriptor_field_map (0x08270f84) in source
      order 1, 0, 2, 3; the retail fourth transfer is a tail branch. The
      nested triple format remains opaque, so the field mapper is named for
      its observed transformation rather than an invented type. Deliberate
      deviation: the Rust port now calls descriptor_field_map directly, while
      the former version dispatched through a fixed-address mapper seam for
      host tests. Host tests cover zero/nonzero optional metadata, all three
      source flag bits, output word-zero preservation, and each mapped field's
      expected retail triple. match.py structural diff reviewed after the ARM
      release build.

  # --- matched opaque entry selection ---
  - address: 0x0826fb4c
    ghidra: FUN_0826fb4c
    name: matched_entry_select_nth
    size: 200
    sig: "void matched_entry_select_nth(RefcountedBody **out, const EntryMatchSource *source, u32 match_key, u32 ensure_ready, i32 occurrence)"
    module: app/matched_entry
    status: ported
    renames:
      param_1/r0: out
      param_2/r1: source
      param_3/r2: match_key
      param_4/r3: ensure_ready
      param_5/[sp+40]: occurrence
      r4: matched_entry
      r5: found_count
      FUN_08053b14: entry_match_next (unported target-default seam)
      FUN_080fe500: entry_result_construct (unported target-default seam)
      FUN_080fe1c4: entry_result_ensure_ready (unported target-default seam)
      FUN_0839ebc4: refcounted_handle_construct
      FUN_083d6180: handle_deref_or_null alias
      FUN_0839ec2c: refcounted_body_attach equivalent
      FUN_0816ccb0: refcounted_body_release_dtor wrapper
    notes: >-
      Raw ARM establishes the exact 200-byte extent
      0x0826fb4c..0x0826fc14; the next separately linked function begins at
      0x0826fc14. Decoding every ARM B/BL word in osos.dec finds exactly ten
      incoming direct BL calls, all unconditional plain BL: no predicated BL
      forms or plain-B tail callers. No aligned image word equals this address,
      so it is not statically dispatched through a data pointer. The body
      reads source+4 without a source NULL guard, rejects a NULL collection or
      signed-negative occurrence, and otherwise calls the matcher from NULL
      then from each prior match until it has the requested zero-based match.
      It allocates 28 bytes through ported operator_new, constructs a result
      around that match and source+8 byte, wraps it in a ported refcounted
      handle, optionally makes the result ready with mode 0, assigns the
      temporary body to out, then drops the temporary reference. Its no-result
      path constructs out from NULL without releasing its prior body. The
      matcher, result constructor, and readiness helper are unported and use
      target-default retail-address seams with host recording replacements;
      all other helpers use their existing canonical ports. Host tests cover
      selecting the second match, exact matcher successor chaining, 28-byte
      allocation and constructor arguments, optional readiness, signed
      negative occurrence, missing collection, and exhausted matches.
  # --- tagged record nested liti class check ---
  - address: 0x0826fc14
    ghidra: FUN_0826fc14
    name: tagged_record_payload_is_liti_class
    size: 24
    sig: "u32 tagged_record_payload_is_liti_class(const TaggedRecord *record)"
    module: cxx/tagged_record
    status: ported
    renames:
      param_1/r0: record
      record_payload/r0: payload
      FUN_08057bdc: nested_liti_class_check (unported target-default seam)
    notes: >-
      Raw ARM establishes the exact 24-byte extent 0x0826fc14..0x0826fc2c;
      the next separately linked function, tagged_record_init, starts at
      0x0826fc2c. Decoding every ARM B/BL word in osos.dec finds exactly nine
      direct inbound calls, all unconditional plain BL, with zero predicated
      BL forms and no plain-B tail callers. The wrapper loads the target-width
      payload word at record+4, calls the unported 0x08057bdc nested 'liti'
      class check, then canonicalizes its result to strict 0 or 1. It has no
      record NULL guard and deliberately still calls the nested check when the
      payload is NULL; that callee itself guards NULL and follows its +8 field
      to ported image_library_is_liti_class. The enclosing record and nested
      object identities beyond these observed fields remain opaque, so the
      port names only the verified relationship. Rust represents the
      unported direct call with a target-default replaceable seam at
      0x08057bdc, enabling host recording tests; target code consequently
      makes an indirect call rather than the retail direct BL. Host tests
      cover forwarding a nonzero target-width payload, strict normalization of
      a nonzero callee result, and the zero-payload call-through. Reference C:
      decomp/c/026/0826fc14_FUN_0826fc14.c.
  # --- opaque tagged record initializer ---
  - address: 0x0826fc2c
    ghidra: FUN_0826fc2c
    name: tagged_record_init
    size: 20
    sig: "TaggedRecord *tagged_record_init(TaggedRecord *this, u32 payload, u32 flag)"
    module: cxx/tagged_record
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: payload
      param_3/r2: flag
      DAT_0826fc40: tagged_record_descriptor
      r0 return: this
    notes: >-
      Raw extent is 0x0826fc2c..0x0826fc44: four ARM instruction words and
      the literal-pool word at 0x0826fc40; the next separately linked function
      starts at 0x0826fc44. Decoding every ARM B/BL word in osos.dec finds
      exactly 20 direct call sites, all unconditional plain BL, with no
      predicated BL forms or plain-B tail callers. The body stores r1 at +0x04,
      installs literal 0x089a5b04 at +0x00, stores only the low byte of r2 at
      +0x08, and returns this unchanged in r0. The literal does not decode as
      a function entry in the static image: 0x089a5b04 is a branch-table case
      label inside a larger body. Since this function only stores that opaque
      target-width word and never dispatches through it, the port adds no
      speculative callee identity or dispatch seam. No deliberate behavioral
      deviations. Host tests cover descriptor/payload stores, flag truncation,
      preserved trailing bytes, zero inputs, and pointer return.

  # --- statically bound empty C++ destructor ---
  - address: 0x0826fc64
    ghidra: FUN_0826fc64
    name: empty_destructor
    size: 4
    sig: "void *empty_destructor(void *this)"
    module: cxx/empty_destructor
    status: ported
    renames:
      param_1/r0: this
      r0 return: this
    notes: >-
      Raw extent is exactly 0x0826fc64..0x0826fc68: 0x0826fc60 is the
      preceding function's literal-pool word and 0x0826fc68 starts the next
      separately linked function. The sole instruction is `bx lr`
      (0xe12fff1e), so this is an empty C++ destructor rather than an
      `ldr pc,[pc,#-4]` veneer or a plain branch. Decoding every ARM B/BL word
      in osos.dec finds exactly 25 direct call sites: all are unconditional
      plain BL, with no predicated BL forms and no plain-B tail calls. No image
      data word contains 0x0826fc64, so it is statically bound, not dispatched
      through a vtable. The load-bearing r0 pass-through is used by the
      destroy-then-free sequence at 0x083d4a70..0x083d4a74, where the next BL
      is operator_delete at 0x082aad24. The port returns this rather than
      Ghidra's void signature, performs no reads or writes, and permits NULL,
      unaligned, or dangling input exactly as the raw body does. No deliberate
      behavioral deviations. Its own .text.empty_destructor section prevents
      LLVM identical-code folding with other empty destructor exports; host
      tests cover pointer preservation and no object writes. match.py
      structural diff reviewed after the ARM release build.
  - address: 0x08166a3c
    ghidra: FUN_08166a3c
    name: empty_destructor_166a3c
    size: 4
    sig: "void *empty_destructor_166a3c(void *this)"
    module: cxx/empty_destructor_166a3c
    status: ported
    renames:
      param_1/r0: this
      r0 return: this
    notes: >-
      Raw ARM is a bare `bx lr`, so this is an empty C++ destructor body with
      21 unconditional BL callers and no predicated or tail-call forms. The
      calls pass an object pointer in r0 and discard the result, which matches
      a destructor that owns nothing. This is the second empty-destructor
      export in cxx/: it is behaviorally identical to 0x0826fc64
      `empty_destructor`, but its own text section keeps the symbols distinct.
      Host tests verify pointer preservation and that the body performs no
      stores. match.py structural diff reviewed after the ARM release build.

  # --- screen layout-name setter ---
  - address: 0x08174300
    ghidra: FUN_08174300
    name: app_screen_set_layout_from_resource
    size: 76
    sig: "void app_screen_set_layout_from_resource(void *screen, u32 provider_class_id, u32 string_id)"
    module: app/screen_layout
    status: ported
    renames:
      param_1/r0: screen
      param_2/r1: provider_class_id
      param_3/r2: string_id
      FUN_081d2184: registry_lookup_by_id
      FUN_08275b9c: object_cast_to_class
      FUN_0827239c: resource_chain_find_string
      FUN_0827735c: string_owner_embedded_init
      FUN_0817434c: app_screen_set_layout
      FUN_08277484: string_object_destroy
    notes: >-
      Raw extent is exactly 0x08174300..0x0817434c; the next separately
      linked function is app_screen_set_layout @ 0x0817434c. Decoding every
      ARM B/BL word in osos.dec finds exactly 25 direct call sites, all
      unconditional plain BL (zero predicated forms, zero tail branches); no
      DATA word contains the address, so it is never dispatched virtually.
      The resource front-end of app_screen_set_layout: resolves the provider
      registered under provider_class_id, downcasts it to class id 5 (the
      resource-provider interface id; five other sites - 0x08172b1c,
      0x081805ac, 0x081851c8, 0x0819ee98, 0x081dd590 - cast to 5 immediately
      around resource-chain calls), asks it for the "Str " resource
      string_id, wraps the C string in a temporary two-word StringObject on
      the stack (the pushed r2/r3), assigns it as the screen's layout name,
      and destroys the temporary. Callers pass app_screen_get's result with
      class ids 0x80 (@ 0x082257ac) and 0x7e00 (@ 0x081e106c). No NULL guard
      anywhere: a missing registration propagates NULL through the
      NULL-tolerant cast and the NULL-headed chain, clearing the layout via
      the StringObject NULL-payload path. Deliberate deviation: the return
      word is dead in stock (r0 = string_object_destroy's `this`, a pointer
      into the collapsed frame; both sampled callers overwrite r0 on the
      next instruction), so the port declares void and lets r0 fall out of
      the last call identically. Host tests drive the full pipeline with a
      mock registry vtable and a combined provider vtable (slot +0x14 cast,
      slot +0x64 find) plus heap-mocked StringObject slots +0x8/+0xc.
      match.py: same six-call sequence; the diff is the crate's fp frame,
      register allocation, and `mov r1,#5` materialized after the lookup
      instead of hoisted in r4.

  - address: 0x0817434c
    ghidra: FUN_0817434c
    name: app_screen_set_layout
    size: 28
    sig: "void *app_screen_set_layout(void *screen, const StringObject *layout)"
    module: app/screen_layout
    status: ported
    renames:
      param_1/r0: screen
      param_2/r1: layout
      FUN_082774a8: string_object_assign
      FUN_081779c8: layout-change notification (unported framework callback)
    notes: >-
      Raw extent is exactly 0x0817434c..0x08174368: `push {r4,lr}` saves
      screen; `add r0,r0,#0x1c; bl 0x082774a8` assigns its embedded
      StringObject from layout; then `mov r0,r4; pop {r4,lr}; b
      0x081779c8` tail-enters the layout-change notification and preserves
      its return. The next separately linked function begins at 0x08174368.
      Decoding every ARM B/BL word in osos.dec finds exactly 25 direct call
      sites, all unconditional plain BL (zero predicated forms); no data word
      contains this address. The raw assignment and notification sequence
      confirms this sets the app screen's layout name, but 0x081779c8's
      framework identity remains unrecovered: it dispatches vtable slot +0x58
      around 0x08177424. Deliberate deviation: ARM calls its fixed retail
      entry directly; host tests use the injectable SCREEN_LAYOUT_ASSIGN_OPS
      seam because the ROM callback cannot execute natively. The Rust normal
      call represents the stock tail branch and returns the callback result.
  # --- UI element registry: the two-dispatch add wrapper ---
  - address: 0x0816e020
    ghidra: FUN_0816e020
    name: element_registry_add
    size: 56
    sig: "void element_registry_add(ElementRegistry *registry, RegistryElement *element)"
    module: app/element_registry
    status: ported
    renames:
      param_1/r0: registry (the lazy_singleton_0x3c object)
      param_2/r1: element
      param_3/param_4: phantom (r2/r3 dead on entry; Ghidra invented them)
    notes: >-
      The add/registration wrapper of the 0x3c-byte UI element registry
      handed out by lazy_singleton_0x3c @ 0x0816df60 (ported): callers
      build a view, call this, then name the element by id through
      FUN_0816e220. Body is exactly two virtual dispatches - first
      element->vtable[+0x08](element), a pre-insert lifecycle hook
      (retain/activate in shape; the removal sibling @ 0x0816e058 pairs
      slot +0x0c with a slot +0x10 teardown on the same object), then
      registry->vtable[+0x1c](registry, &slot) with slot a stack word
      holding the element pointer, passed by reference so the callee may
      replace it. Both returns are dead at every call site, so the ABI
      is void; no NULL guards anywhere. Extent verified at exactly 56
      bytes - the next function opens at 0x0816e058 (push {r4-r7,lr};
      sub sp,#28). Call count verified by decoding every B/BL word in
      work/firmware/osos.dec: 25 bl, 0 predicated forms, 0 plain b, and
      0 data-word occurrences of 0x0816e020 - the wrapper itself is
      never vtable-dispatched. Ghidra's C is wrong about the prototype
      (four params; r2/r3 are dead on entry) and its "local_10 =
      param_4" is a phantom - the slot is written exactly once, with the
      element pointer. UNRESOLVED CALLEES, documented not invented: the
      registry ctor @ 0x0816e2ac installs vtable 0x089a4c20 whose +0x1c
      word reads 0x08102f44 - a mid-function address (the containing
      prologue is at 0x08102f30) with zeroed sibling slots - and the
      element class (FUN_0817e3b0, vtable 0x08989468) has +0x08 reading
      0x6c695354 = ASCII "TSil"; both are the known runtime-initialized
      vtable page mismatch (same as clock_source_construct @ 0x08262958
      and class_6800), so the port dispatches through whatever vtable
      the objects carry at runtime. Key ordering fact: the registry
      vtable is loaded only AFTER the first blx returns, so a hook that
      swaps registry->vtable redirects the insert dispatch - pinned by a
      host test. No seams and no deviations beyond the structural usize
      vtable model (host pointers wider than the 32-bit target words).
      Host tests (3): dispatch order plus forwarded arguments and the
      by-reference slot contents, callee replacement of the slot
      tolerated, and the post-call vtable load order. match.py
      structural diff (exit-1 expected): identical core - mov r5,r0 /
      ldr r0,[r1] / mov r4,r1 / ldr r1,[r0,#8] / blx / store slot / ldr
      r0,[r5] / ldr r2,[r0,#28] / blx - differing only by the crate's
      fp frame and slot offset, the accepted house profile.
  - address: 0x0816e220
    ghidra: FUN_0816e220
    name: element_registry_set_name_for_id
    size: 96
    sig: "void element_registry_set_name_for_id(ElementRegistry *registry, u32 element_id, u32 name)"
    module: app/element_registry
    status: ported
    renames:
      param_1/r0: registry
      param_2/r1: element_id
      param_3/r2: name
      local_24/sp+4: yielded RegistryElement pointer
      auStack_20/sp+8: 20-byte collection iterator state
      FUN_08155e80: iterator_state_construct (ported)
      FUN_08155d6c: iterator_state_next (ported)
      FUN_08155ec0: iterator_state_cleanup (ported)
    notes: >-
      Exact raw extent is 0x0816e220..0x0816e280: 24 instructions, no
      literal pool, with the next separately linked function starting at
      0x0816e280. It constructs the 20-byte iterator as
      iterator_state_construct(state, registry, -2), repeatedly invokes
      iterator_state_next(state, &element), compares element+0x04 with
      element_id, and on the first equality stores name at element+0x08
      before branching to cleanup. The cleanup runs both after a match and
      after exhaustion. No NULL guard protects registry or a yielded
      element. Raw decoding of every ARM B/BL word in osos.dec found exactly
      20 direct call sites: 20 unconditional BL, zero predicated forms,
      zero plain B, and zero data-word occurrences of 0x0816e220. The three
      direct callees are already ported, so the Rust port calls them directly
      and adds no dispatch seam. Deliberate deviation: none. Host tests
      script the existing iterator fetch seam only to provide native
      traversal fixtures; they prove a non-match is untouched, the first
      matching element alone receives the name, iteration stops immediately
      after that match, and an absent id exhausts without a write.
  - address: 0x0832774c
    ghidra: FUN_0832774c
    name: inline_encoded_word_block_test_bit
    size: 88
    sig: "u32 inline_encoded_word_block_test_bit(const InlineEncodedWordBlock *block, i32 bit_index)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: block
      param_2/r1: bit_index
      r2: preserved block pointer
      r3: decoded word count then selected decoded word
      r0: magnitude word index then result
      DAT_083277a4: INLINE_ENCODED_COUNT_MULTIPLIER (0x4b6143ff)
      DAT_083277a8: INLINE_ENCODED_WORD_COMPARE_MULTIPLIER (0x3399e27f)
    notes: >-
      Raw ARM confirms the 88-byte full extent 0x0832774c..0x083277ac:
      20 instruction words through 0x083277a0, followed by the count and
      word decode literal-pool words at 0x083277a4/0x083277a8; the next
      separately linked function begins with stmdb sp! at 0x083277ac.
      Decoding every ARM B/BL word in osos.dec finds exactly nine direct
      inbound call sites, all unconditional bl, with zero predicated forms,
      tail branches, or aligned DATA-word references to this entry. It takes
      the wrapping magnitude of bit_index, rejects a magnitude word index
      outside the wrapping absolute decoded inline-word count before loading
      any payload, then multiplies that word by 0x3399e27f and returns the
      selected bit. Negative indices select their positive magnitude
      counterparts; i32::MIN retains ARM's wrapping-negation behavior and
      therefore has word index 0x04000000. The same word multiplier is used
      by inline_encoded_word_block_compare. Deliberate deviation: none.
  - address: 0x08309be0
    ghidra: FUN_08309be0
    name: copy_inline_encoded_word_block
    size: 84
    sig: "void copy_inline_encoded_word_block(InlineEncodedWordBlock *destination, const InlineEncodedWordBlock *source)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: source
      iVar1/r3: encoded_count then decoded_count
      DAT_08309c34: INLINE_ENCODED_COUNT_MULTIPLIER (0x4b6143ff)
    notes: >-
      Unbounded copy for the INLINE-storage encoded-count word block
      family: single-word header (encoded count) with the u32 word payload
      inline at offset +4, NOT behind the out-of-line words pointer of the
      sibling EncodedWordBlock family (copy_encoded_word_block @
      0x0835443c, copy_encoded_word_block_from @ 0x08322bcc). Alias check
      first (subs/movne/cmp/bxeq lr): identical source/destination returns
      with zero writes and no decode. Otherwise the header word is stored
      verbatim, the decoded count is the signed wrapping absolute value of
      encoded_count * 0x4b6143ff mod 2^32 (muls + rsbmi), and words
      src[1..=count] are copied to dst[1..=count] in descending-index
      order. No bound check and no return value, unlike the bounded
      siblings. The header encoding is confirmed by the producer
      FUN_0833dbd0 @ 0x0833dbd0, which stores count * 0xda8ebbff - the
      multiplicative inverse of 0x4b6143ff mod 2^32, verified against both
      literal pools (copy literal @ 0x08309c34, producer literal @
      0x0833dd0c). Callers (e.g. FUN_083232d4 @ 0x083232d4) shuffle
      0x10-byte bignums produced by FUN_0833dbd0 between stack slots
      inside the firmware's modular-arithmetic signature math. Extent
      self-verified: 21 instructions = 84 bytes, trailing literal-pool
      multiplier word at 0x08309c34, next function's stmdb sp! prologue at
      0x08309c38; Ghidra's 84 bytes is the instruction body only. Call
      count verified by decoding every ARM B/BL word in osos.dec: exactly
      24 direct call sites, all unconditional bl, 0 predicated forms; the
      address occurs in no data word, so it is not dispatched virtually.
      Deliberate deviation: none - i32::MIN retains the ARM
      wrapping-negation behavior and a distinct destination would attempt
      a 2^31-word copy. Host tests (5): header plus inline words for
      decoded counts 0/1/3/5 with trailing sentinels untouched, the rsbmi
      negative-product path via encoded count for -3, zero count copies
      the header only, an aliased i32::MIN header is a no-op proving the
      alias check precedes any decode, and an overlapping block copies in
      descending-index order. match.py structural diff (exit-1 expected):
      same predicated alias check, verbatim header store, mul + rsbmi
      decode, and descending ldr/str [rN, r2, lsl #2] loop with the
      0x4b6143ff literal pool word; differs only by the crate fp frame
      and register allocation, the accepted house profile.

  - address: 0x082f5364
    ghidra: FUN_082f5364
    name: inline_encoded_word_block_compare
    size: 204
    sig: "int inline_encoded_word_block_compare(const InlineEncodedWordBlock *left, const InlineEncodedWordBlock *right)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: left
      param_2/r1: right
      iVar1/r0: left_count then remaining_count
      iVar2/r1: right_count then compare_sign
      uVar3/r1: right_product
      uVar4/r2: left_product
      DAT_082f5430: INLINE_ENCODED_COUNT_MULTIPLIER (0x4b6143ff)
      DAT_082f5434: INLINE_ENCODED_WORD_COMPARE_MULTIPLIER (0x3399e27f)
    notes: >-
      Signed-magnitude comparator for the INLINE-storage encoded-count word
      block family. The decoded headers are compared first; if they differ,
      the sign of that signed comparison is returned immediately. When the
      decoded headers match, the header sign selects the result polarity and
      payload words 1..count are compared from high to low after multiplying
      each word by 0x3399e27f. Unsigned ordering on those products decides
      whether the result is sign, -sign, or zero when every payload word
      matches. Raw ARM at 0x082f5364..0x082f5434 (Ghidra size 204 bytes,
      literals at 0x082f5430/0x082f5434; next function starts at 0x082f5438).
      Call count verified by decoding the osos.asm branch words at this
      address: 21 direct call sites, all unconditional bl, 0 predicated
      forms. Host tests cover header precedence, descending payload compare,
      the negative-sign flip, and zero-count early return.

  - address: 0x082fc770
    ghidra: FUN_082fc770
    name: encoded_word_block_is_zero
    size: 84
    sig: "u32 encoded_word_block_is_zero(const EncodedWordBlock *block)"
    module: util/encoded_word_block
    status: ported
    renames:
      param_1/r0: block
      iVar1/r0: decoded_count then remaining_count
      DAT_082fc7c4: ENCODED_COUNT_MULTIPLIER (0x0a7e377f)
      DAT_082fc7c8: ENCODED_WORD_DECODE_MULTIPLIER (0x76b4197f)
    notes: >-
      Raw ARM has an 84-byte instruction body at 0x082fc770..0x082fc7c4
      followed by the two multiplier literal-pool words at
      0x082fc7c4/0x082fc7c8; the next separately linked function begins at
      0x082fc7cc, so the full extent is 92 bytes. Every ARM B/BL word in
      osos.dec was decoded: exactly 11 inbound direct calls, all
      unconditional bl and zero predicated forms; no direct B or aligned
      DATA-word reference targets this address. It multiplies the encoded
      count by 0x0a7e377f, takes a wrapping absolute value, then scans the
      separate payload pointer from index count-1 to 0. A word is nonzero
      when its product with 0x76b4197f is nonzero; the odd multiplier is
      invertible modulo 2^32, so this is exactly a raw nonzero test. It
      returns 0 at the first nonzero word and 1 when all examined words are
      zero. Zero and i32::MIN decoded counts return 1 before reading the
      words pointer, while other negative decoded counts scan their absolute
      count. Deliberate deviations: none. Host tests cover null pointers on
      both no-read cases, positive and negative counts, every payload
      position, all-zero blocks, and an ignored trailing nonzero word.

  - address: 0x080dadfc
    ghidra: FUN_080dadfc
    name: string_object_initial_sort_key
    size: 108
    sig: "u32 string_object_initial_sort_key(const StringObject *value)"
    module: cxx/string_object_initial_sort_key
    status: ported
    renames:
      param_1/r0: value
      iVar4/r5: codepoint_index
      uVar1/r4: first_codepoint
      uVar2/r0: byte_map
      uVar3/r0: mapped_key
      DAT_080dae68: INITIAL_SORT_KEY_CHARACTER_FLAGS (0x083ecfdd)
      FUN_0820b234: retail_byte_map_getter (identity unrecovered)
      FUN_0829f1f4: signed_range_byte_map_lookup
    notes: >-
      Raw ARM extent is 0x080dadfc..0x080dae6c: 104 instruction bytes plus
      the 4-byte character-flag literal at 0x080dae68; the next separately
      linked function starts at 0x080dae6c. Decoding every ARM B/BL word in
      osos.dec found exactly 10 inbound direct calls, all unconditional plain
      bl (0 predicated forms and 0 direct B tails); no data-word reference
      targets the entry. It repeatedly obtains StringObject codepoints until a
      non-space, gets an unidentified three-word byte map through
      FUN_0820b234, and applies FUN_0829f1f4's signed inclusive range lookup.
      A zero mapping returns 0x21 for NUL or codepoints >= 0x2e80 and 0x23
      otherwise; nonzero mappings whose 0x083ecfdd flag has bit 0x10 return
      0x23, otherwise returning the mapped byte. Deliberate deviation: the
      unported getter remains a typed fixed-address target call and a host
      seam, while its independently decoded 20-byte lookup leaf is inlined.
  - address: 0x080da15c
    ghidra: FUN_080da15c
    name: wmcodec_write_reg
    size: 72
    sig: "void wmcodec_write_reg(uint reg, uint value)"
    module: drivers/wmcodec
    status: ported
    renames:
      param_1/r0: reg
      param_2/r1: value
      DAT_080da1a4: WMCODEC_SHADOW_ADDRESS (0x08ad9de4)
      FUN_0806a4a0: kernel_sem5_wait
      FUN_0836bb84: i2c_write
      FUN_080645a8: kernel_sem5_signal
    notes: >-
      Wolfson two-wire codec register write. Raw ARM extent is
      0x080da15c..0x080da1a8 (68 instruction bytes plus the 4-byte
      shadow-table literal at 0x080da1a4); the next function starts at
      0x080da1a8. Ghidra's four-argument prototype is wrong: the raw body
      reads only r0/r1. It makes the two-byte stack control word
      { (reg << 1) | ((value & 0x100) >> 8), value & 0xff }, waits on
      semaphore 5, calls FUN_0836bb84(0x1a, 2, buf), signals semaphore 5
      without checking the transfer status, then strh-stores value into the
      unchecked 68-entry u16 shadow at 0x08ad9de4 + reg*2. Thus the wire
      retains only value bits 8..0 while the shadow retains value bits
      15..0. Call count verified by decoding every ARM B/BL word in
      osos.dec: 21 direct unconditional bl call sites, 0 predicated forms;
      its only non-call reach is wmcodec_update_bits's tail b at
      0x080e3330, and no data word references this address. Deliberate
      deviations: the unported raw I2C routine is invoked through a typed
      fixed-address function pointer on target (blx instead of bl) and a
      recording host boundary; the existing semaphore ports retain their
      documented ROM-hook behavior. Host tests verify byte packing across
      bit 8, full 16-bit cache truncation, semaphore/transfer ordering, and
      the ignored transfer-failure status.

  - address: 0x080e3318
    ghidra: FUN_080e3318
    name: wmcodec_update_bits
    size: 28
    sig: "void wmcodec_update_bits(uint reg, uint mask, uint value)"
    module: drivers/wmcodec
    status: ported
    renames:
      param_1/r0: reg
      param_2/r1: mask
      param_3/r2: value
      DAT_080e3334: WMCODEC_SHADOW_ADDRESS (0x08ad9de4, same word as DAT_080da1a4)
    notes: >-
      Shadow-cached read-modify-write of one 9-bit register on the I2C
      device at slave 0x1a - a Wolfson-format audio codec (the writer's
      2-wire control word "(reg << 1) | (value >> 8) & 1, value & 0xff"
      is the Wolfson two-wire interface; distinct from the byte-register
      codec at slave 0x4a in drivers/codec.rs). Body: ldrh the shadow
      halfword at 0x08ad9de4 + reg*2 (zero-extended, no bounds check),
      merged = (old & ~mask) | (value & mask) in 32 bits, then tail branch
      to wmcodec_write_reg @ 0x080da15c. That writer brackets the
      FUN_0836bb84 I2C write to slave 0x1a with the
      kernel_sem5_wait/rom_sem_signal(5) pair and strh-stores merged back
      into shadow[reg]. The shadow table is 68 u16 entries (count from
      "cmp r0, #0x44" @ 0x080a9834) in osos BSS past the end of osos.dec,
      zero-filled at boot. Callers are all audio routing/power config
      (FUN_0808f894 volume latch regs 0/1, route regs 0x18/0x19/0x1a;
      FUN_080b5248 reset writes 0x17 / 0x1c = 0 dB). Extent verified:
      7 instructions = 28 bytes, literal-pool table word at 0x080e3334,
      next function's "push {r4,r5,r6,lr}" at 0x080e3338; Ghidra's 28
      bytes is correct. Call count verified by decoding every ARM B/BL word
      in osos.dec: exactly 24 direct call sites, all unconditional bl, 0
      predicated forms; the address occurs in no data word, so it is not
      dispatched virtually. Deliberate deviations: the retail tail branch
      becomes a normal Rust call to the ported writer, and the shadow is
      addressed through a cfg-split accessor (retail BSS on target,
      module-owned 68-entry replica on host). Host tests cover the merge
      formula, packet encoding, and final cached halfword.

  # --- "crts"-tagged object in-place destructor ---
  - address: 0x0809e160
    ghidra: FUN_0809e160
    name: crts_object_destroy
    size: 92
    sig: "int crts_object_destroy(CrtsObject *this)"
    module: util/crts_object
    status: ported
    renames:
      param_1/r0: this
      FUN_080a7714: crts_has_tag
      FUN_0805d028: MEMH_HANDLE_DESTROY seam
      FUN_080d86b0: CRTS_TABLE_TEARDOWN seam
      FUN_0805cfb4: bzero (ported, via CRTS_OBJECT_ZERO slot)
    notes: >-
      In-place destructor body for the 0x58-byte "crts"-tagged object
      family: reject NULL and foreign tags with -50 (mvn r0, #0x31),
      destroy the three "MemH" managed-buffer handles at +0x08/+0x0c/+0x10
      in offset order (each guarded blne; the 0x0805d028 callee is itself
      NULL-safe), run the table teardown 0x080d86b0(this, force=1) with
      its status discarded, then bzero(this, 0x58) and return 0. Extent
      self-verified: the next separately entered function starts at
      0x0809e1bc, so Ghidra's 92 bytes is exact. Call census verified by
      decoding every ARM B/BL word in osos.dec: exactly 24 direct call
      sites, all unconditional bl (three in the 0x080490xx array
      destructor loops, 21 in the 0x080da928 table clear); no predicated
      forms, no tail branches, and the address occurs in no data word, so
      it is never dispatched virtually. Caller context: 0x080490b8
      destroys embedded sub-objects at +0x34, +0x8c and an array of stride
      0x58 at +0xe4; 0x083b467c sets a vtable, runs this destructor on a
      heap object, then tag-2 operator_delete - the classic split
      destructor shape. Deliberate deviations: the verified
      four-instruction tag guard of 0x080a7714 (returns 1 iff non-NULL
      and first word is 0x73747263, the in-memory bytes "crts"; semantic
      identity not established) is inlined per the util/tagged_counter.rs
      precedent; the two unported non-trivial callees dispatch through
      the volatile seams MEMH_HANDLE_DESTROY and CRTS_TABLE_TEARDOWN
      whose target defaults transmute the retail addresses 0x0805d028 and
      0x080d86b0 (hook-ready on device; host defaults panic); the ported
      bzero is reached through the volatile CRTS_OBJECT_ZERO slot so LLVM
      cannot inline the 76-byte fill and erase the stock bl boundary.
      Handle fields are u32 words, never native pointers - this function
      only compares and forwards them - keeping 4-byte spacing on target
      and host without slab mapping. match.py structural diff (exit-1
      expected): identical shape - inlined guard with hoisted mvn
      r0, #0x31, three guarded handle loads and calls in order, teardown
      (this, 1), zero-fill (this, 0x58), return 0; differs only by the
      crate fp frame and blx-via-slot dispatch in place of the three bl,
      the documented seams. Host tests (6): layout is exactly 0x58 bytes
      and 4-aligned; NULL returns -50 with no seam calls; bad tag returns
      -50 leaving the object byte-identical; valid object destroys handles
      in offset order, teardowns once with force=1, zeroes all 88 bytes,
      returns 0; absent handles skip destroy but teardown still runs;
      teardown status is discarded.

  - address: 0x082f0a1c
    ghidra: FUN_082f0a1c
    name: digest_init
    size: 136
    sig: "u32 digest_init(DigestCtx *ctx, u32 algorithm)"
    module: crypto/digest_init
    status: ported
    renames:
      param_1/r0: ctx
      param_2/r1: algorithm
      DAT_082f0a90: STATE4_IV (0xc658de10, kind-2 5th state word)
      DAT_082f0a94..DAT_082f0aa0: STATE_IVS (0xfcd4a0ff, 0x7dbc3877, 0x14dd9b02, 0x93f6038a)
    notes: >-
      Init half of Apple's proprietary MBA-obfuscated message digest in
      the 0x082f0xxx..0x0835xxxx FairPlay/DRM cluster (NOT the vendored
      OpenSSL copy; the sqlite/hash_init @ 0x0837ade8 is unrelated).
      Siblings: digest_update @ 0x08338384 (data, ctx, len), digest_final
      @ 0x082fbbfc (out, ctx) which dispatches on (algorithm & 0xff) == 2
      and separately tests flag bit 0x100 of the stored selector, and the
      textbook HMAC key schedule @ 0x0835ab54 (64-byte block, 0x36 ipad /
      0x5c opad hidden behind the MBA identity (x * -0x4b + 0x24) * -99
      - 0x14, which is x mod 256). Behavior: ctx->algorithm @ +0x64 =
      algorithm & 0x1ff (8 kind bits plus the 0x100 flag); length counter
      @ +0x40/+0x44 and state_len @ +0x60 zeroed; kind 1 writes 4 state
      words @ +0x48..+0x54 and sets state_len 16, kind 2 additionally
      writes the 5th word 0xc658de10 @ +0x58 and ends with state_len 20;
      any other kind leaves the state words untouched and returns 0 after
      only the counter/selector writes. Always returns 0. The IVs match
      neither MD5/SHA-1 nor any byte swap of them and occur nowhere else
      in the image - custom, identity unconfirmed. SIZE IS 136 BYTES
      (0x082f0a1c..0x082f0aa4): Ghidra's 116 drops the five trailing
      literal-pool words @ 0x082f0a90..0x082f0aa4; the next function
      starts at 0x082f0aa4. Call count verified by binary scan over every
      branch word in osos.dec: 23 sites, all unconditional bl, zero
      predicated forms, zero plain b; address occurs in no data word, so
      no virtual dispatch. Wrapper @ 0x0834f42c re-inits a used ctx with
      digest_init(ctx, ctx->algorithm), pinning the +0x64 round-trip.
      match.py structural diff (exit-1 expected): same field offsets,
      same kind-1/kind-2/unknown dispatch, same five literals, same
      return-0; LLVM adds the crate fp frame, folds state_len += 16 into
      the immediates 16/20, and emits the kind-2 5th word through the
      same literal pool. Host tests (6): kind 1 writes exactly 4 state
      words + len 16 leaving state[4] dirtied; kind 2 writes all 5 + len
      20; kinds 0/3/0xfe/0xff touch only counters and selector; 0x102
      stores the 0x100 flag yet dispatches kind 2; 0x202/0xffff_fe01 mask
      to 0x1ff on the store but dispatch on the low byte; re-init with
      the stored selector restores a dirtied context to pristine; guard
      words before/after the context and +0x00..+0x40/+0x5c stay
      untouched.

  - address: 0x08297e34
    ghidra: FUN_08297e34
    name: heap_string_data
    size: 8
    sig: "char *heap_string_data(HeapString *this)"
    module: cxx/heap_string
    status: ported
    vars: {param_1: this}
    notes: >-
      Data accessor of retailOS's one-word heap string holder (the object
      is a single word: the heap char buffer pointer, NULL when empty;
      freed with caller tag 0x14 by the destructor @ 0x0810b3e4). Whole
      body, verified against the raw image (osos.dec, load base
      0x08000000, words e5900000 e12fff1e): `ldr r0,[r0]; bx lr` -
      returns the payload word unchanged, INCLUDING NULL for an empty
      holder (no shared-empty substitution; that NULL-replacing behavior
      belongs to the different two-word StringObject class, whose
      string_object_c_str @ 0x082a50b0 is ported separately). Extent is
      exact: the next function (a `mov r0,#1; bx lr`) starts at
      0x08297e3c. Call count verified by binary scan over every branch
      word in osos.dec: 23 sites, all unconditional bl, zero predicated
      forms; the address occurs in no data word, so no virtual dispatch.
      No NULL guard on `this` - the original faults on NULL, and so does
      the port; the all-unconditional call sites are consistent with
      callers holding live stack instances. Sampled call sites:
      FUN_081502b0 builds stack holders via the printf-assign @
      0x0810b5cc for "ImageSpecifications" device-info strings and feeds
      the accessor's result to the XML writer @ 0x0814f924; 0x08113358
      feeds it to a substring search then dups it via 0x080e7904. The
      sibling class methods (destructor 0x0810b3e4, assign-from-cstr
      0x0810b514, assign-from-buffer 0x0810b568, printf-assign
      0x0810b5cc, suffix-assign 0x08297df8) are NOT ported - one
      function per commit. Port has its own link_section because the
      exact `ldr r0,[r0]; bx lr` body occurs 22 times in osos.dec
      (binary-counted), keeping the hook seam distinct under LLVM
      identical-code folding. match.py structural diff (exit-1
      expected): identical load-and-return; the only diff is the crate's
      fp frame prologue/epilogue, the accepted house deviation. Host
      tests (3): payload pointer returned verbatim, NULL payload passes
      through as NULL, arbitrary (even unaligned) word returned verbatim
      with the holder word never written.
  - address: 0x0810b3e4
    ghidra: FUN_0810b3e4
    name: heap_string_destroy
    size: 40
    sig: "void heap_string_destroy(HeapString *this)"
    module: cxx/heap_string
    status: ported
    vars: {param_1: this, iVar1: payload}
    renames:
      param_1/r0/r4: this
      iVar1/r0: payload
      FUN_080e7970: free_wrapper
    notes: >-
      Raw osos.dec words at 0x0810b3e4..0x0810b408 are
      e92d4010 e1a04000 e5900000 e3500000 08bd8010 e3a01014 ebff715b
      e3a00000 e5840000 e8bd8010. The next sibling begins with cmp r0,#0
      at 0x0810b40c, so the extent is exactly 40 bytes. It saves this in
      r4, loads this->data, and returns immediately if that word is NULL;
      otherwise it calls free_wrapper(payload, 0x14), writes NULL to
      this->data, and returns void. Whole-image ARM branch decoding found
      exactly 19 direct calls, all unconditional bl, zero predicated bl;
      its only other branch references are predicated tail b forms beq at
      0x0810b530 and ble at 0x0810b584. No word in the image equals
      0x0810b3e4, so there is no data/virtual dispatch seam. The absence
      of a this NULL guard is deliberate: the initial ldr faults just as
      stock does. Rust's if models the predicated early return; the already
      ported free_wrapper's HEAP_OPS dispatch is the established heap-path
      deviation. Host tests (2) prove a non-NULL sentinel is forwarded once
      with tag 0x14 and then cleared, while NULL makes no heap call and
      remains NULL.
  - address: 0x0810b5cc
    ghidra: FUN_0810b5cc
    name: heap_string_format
    size: 68
    sig: "i32 heap_string_format(HeapString *this, const char *format, VaList args)"
    module: cxx/heap_string
    status: ported
    vars: {param_1: this, param_2: format, in_stack_ffffffe8: args, auStack_200: scratch, uVar1: length}
    renames:
      param_1/r0/r5: this
      param_2/r1: format
      in_stack_ffffffe8/r3: args
      auStack_21c: scratch
      uVar1/r4/r0: length
      FUN_08074ba0: retail_vsnprintf
      FUN_0810b514: heap_string_assign_from_cstr (stock; intentionally unported)
    notes: >-
      Raw osos.dec at 0x0810b5cc..0x0810b60c is exactly 68 bytes; the
      next function begins at 0x0810b610 with cmp r0,#0. It spills r0-r3
      for the ADS variadic ABI, reserves 516 bytes, formats into the
      aligned 512-byte scratch at sp+4 by calling retail_vsnprintf
      (0x08074ba0) with size 512 and the pointer to the spilled varargs,
      then retains that i32 result in r4 while it calls the stock
      HeapString::assign_from_cstr @ 0x0810b514. The result is returned
      unchanged after the assignment, including a negative conversion
      result. Whole-image ARM branch decoding finds exactly 20 direct
      calls: all are unconditional bl, with zero predicated bl, zero
      plain b, and no data word equal to 0x0810b5cc. The repeated pairs
      at 0x081508cc..0x0815103c are device-info XML construction in
      FUN_081502b0, consistent with its live stack holders and supplied
      format strings. Rust models the variadic spill as explicit VaList,
      the crate convention. The assignment sibling is not re-stubbed:
      target builds invoke its verified stock entry directly, while host
      tests install a lock-guarded recorder. match.py structural review
      (exit 1 expected): both bodies reserve a 512-byte scratch, preserve
      the formatter result across assignment, and return it; LLVM's frame,
      explicit VaList register shuffle, and literal-loaded blx to the stock
      callee differ from the retail variadic spill and direct bl. Host tests
      (5 module tests, 2 new) verify the 511-byte payload bound plus NUL in
      the 512-byte scratch, verbatim negative result propagation, argument
      forwarding, one assignment call, and empty output.
  # --- track-metadata fetch framework: serialized-record scalar reader ---
  - address: 0x0826863c
    ghidra: FUN_0826863c
    name: metadata_record_read_u32
    size: 68
    sig: "u32 metadata_record_read_u32(MetadataRecordReader *reader, const u8 *record, u32 *cursor)"
    module: app/metadata_record
    status: ported
    vars: {param_1: reader, param_2: record, param_3: cursor}
    notes: >-
      Reads the u32 at record + *cursor into a stack local, advances
      *cursor by 4, calls the reader's vtable slot +0x20 as
      transform(reader->engine, &value, 4, 1), and returns the
      POST-transform value. The reader object is {vtable @ +0x00,
      engine @ +0x04}; slot +0x20 is the framework's in-place
      element-wise converter (elem_size/count signature: this helper
      passes 4/1, the 8-byte sibling @ 0x08268680 passes 8/1, and the
      vector path @ 0x0826850c-0x08268524 passes 8/N) - almost
      certainly the byte-order/normalization hook of the track-metadata
      fetch decoders (diagnostic strings "fetch metadata: track id =
      %llu" and "metadata_size not being set by metadata_for_global_id
      callback" sit in the same 0x08268xxx block). The concrete engine
      behind the slot is NOT identified and none is invented; the call
      is genuine virtual dispatch. Extent verified against osos.dec:
      68 bytes exactly, the sibling's push prologue opens at
      0x08268680. Call count verified by decoding every B/BL word in
      osos.dec: exactly 23 sites, all unconditional bl (0x08268a90,
      0x08268ab0, 0x08268c58, 0x08268d08, 0x08268d1c, 0x08268d78,
      0x08269054, 0x08269068, 0x0826907c, 0x08269090, 0x082693e0,
      0x082693f4, 0x08269408, 0x0826941c, 0x0826953c, 0x08269550,
      0x08269564, 0x08269578, 0x08269658, 0x0826966c, 0x08269780,
      0x08269794, 0x082697c8), zero predicated forms, zero tail b
      references, and the address occurs in no data word - never
      dispatched virtually, and no caller NULL-gates the reader, so the
      port keeps the original's no-guard fault-on-NULL behavior. The
      cursor advances BEFORE the transform runs. The record word load
      is a plain aligned read (cursors only advance by 4/8 from zero
      over word-aligned record buffers); read_unaligned would emit four
      ldrb per word on ARMv5TE. Reader/vtable modeled as repr(C)
      structs with native pointer fields (vtable slot +0x20 = index 8,
      slots +0x00..+0x1f reserved), so no u32 fixture slab is needed
      and host tests drive real native mock vtables. match.py
      structural diff (exit-1 expected): same shape - cursor load,
      indexed word load, cursor +4 store, vtable/engine loads, slot
      +0x20 blx with (buf, 4, 1), post-call reload; only diffs are the
      crate fp frame prologue/epilogue (accepted house deviation) and
      LLVM coalescing the original's redundant second ldr of *cursor.
      Host tests (4): word at cursor returned post-transform with
      cursor advanced and (engine, 4, 1) args observed; identity
      transform returns the raw word; successive reads walk the record
      (8 -> 12 -> 16); an unconditionally overwriting transform proves
      the return is the post-transform value. Siblings 0x08268680
      (u64 reader), 0x082686cc (+0x18 veneer), 0x08268734 (vector
      apply), 0x08268750 (flag test) are NOT ported - one function per
      commit.

  - address: 0x081066a8
    ghidra: FUN_081066a8
    name: surface_new
    size: 64
    sig: "u8 *surface_new(u32 format, u32 width, u32 stride, u32 height, u32 flags, u32 plane0, u32 plane1, u32 plane2, u32 plane3)"
    module: drivers/surface_new
    status: ported
    renames:
      param_1: format (pixel format byte, ctor stores at +0x08; 0 planar YUV 4:2:0, 1 two-plane, 2/3/4 packed)
      param_2: width (ctor word +0x0c; multiplied with height into +0x1c)
      param_3: stride (ctor word +0x10; forwarded to the plane allocator as its 5th arg)
      param_4: height (ctor word +0x14)
      param_5: flags (ctor byte +0x18; forwarded to the plane allocator)
      param_6..param_9: plane0..plane3 (caller-supplied external plane bases; all zero => ctor allocates)
      FUN_082aadd4: operator_new (ported in heap/veneers, called directly)
      FUN_08106a8c: surface_ctor (unported; SURFACE_CTOR dispatch slot, documented zeroing default)
    notes: >-
      Factory for the display surface object (the class whose plane
      accessor surface_plane_address @ 0x082978bc is already ported in
      drivers/display_layer.rs - the layout match is the class
      identification: format byte +0x08, plane words +0x24/+0x28/+0x2c,
      0x54-byte object with an RTXC mutex @ +0x40 and condvar @ +0x48).
      Ghidra's 64-byte extent is exact (next function @ 0x081066e8
      opens with push {r4, lr}) but its C return type void is wrong:
      the ctor's `this` return survives in r0 through the epilogue and
      every caller stores the result, so the port returns *mut u8.
      Algorithm: operator_new(0x54) @ 0x082aadd4 (ported, called
      directly), then forward the raw block plus all nine arguments to
      the unported constructor @ 0x08106a8c - 4 in registers, arg4 plus
      the five stack args re-stored at [sp]..[sp+20] - returning the
      ctor's result. No NULL check between allocation and ctor (the
      original faults in the ctor's first store on OOM); the port
      reproduces that. Call count verified by decoding every BL word
      in osos.dec: exactly 23 sites, all plain bl (0x0811e77c,
      0x081201bc, 0x0812021c, 0x08120260, 0x081202a4, 0x081202dc,
      0x08144bfc, 0x08144ca0, 0x08156570, 0x081565b4, 0x08158ab4,
      0x08199b48, 0x08199b98, 0x081f5af8, 0x081f5ebc, 0x082057b8,
      0x08224ca0, 0x08256848, 0x0825687c, 0x08256a44, 0x08256a80,
      0x08256ab4, 0x0828d4d4), zero predicated forms - no caller
      NULL-gates or flag-gates the factory. Callers pass the Classic 6G
      panel geometry, e.g. surface_new(3, 0xf0, 0x140, 0x140, 2, plane,
      0, 0, 0) from FUN_0815643c. Anomaly documented, not invented: the
      ctor's vtable word 0x08980b70 does not decode as a table of code
      pointers in the image (first word 0x80c00010, DRAM past the osos
      extent) - presumably runtime-initialized; the zeroing default
      stub installs no vtable, so the port is NOT hook-ready until the
      ctor is ported. match.py structural diff (exit-1 expected): same
      shape - mov r0,#0x54, alloc bl, nine-arg forward with arg4 at
      [sp] and the five stack args at [sp+4..20]; the ctor call is a
      blx through the SURFACE_CTOR dispatch slot instead of the
      original's direct bl (the service_manager_get.rs contract), plus
      the crate fp frame. Host tests (3): allocation size 0x54 with the
      block plus all nine args forwarded in order and the ctor result
      returned; distinct values 11..99 prove no argument shuffling;
      default stub zeroes the whole 0x54-byte block and returns it.

  # --- tag -> handler registry: set-if-unset (src/app/tag_handler_registry.rs) ---
  - address: 0x081e11a8
    ghidra: FUN_081e11a8
    name: tag_handler_register_default
    size: 52
    sig: "void tag_handler_register_default(u32 tag, u32 handler)"
    module: app/tag_handler_registry
    status: ported
    renames:
      param_1/r0: tag
      param_2/r1: handler
      DAT_081e11dc: TAG_HANDLER_MAP_ADDRESS (BSS map global 0x08ad7d74)
      FUN_083dbd9c: TAG_MAP_VALUE_SLOT (unported; dispatch slot, ROM
        transmute on target, inert shared-word stub on host)
    notes: >-
      True extent 56 bytes (Ghidra's 52 drops the trailing literal-pool
      word @ 0x081e11dc = 0x08ad7d74, past the image end 0x08a1b5e8, so
      runtime-zeroed BSS); next function (the registrar FUN_081e11e0)
      opens push {r4, lr} @ 0x081e11e0. Call count verified by decoding
      every B/BL word in osos.dec: exactly 22 sites, all plain bl, 0
      predicated, and ALL 22 sit inside FUN_081e11e0 @ 0x081e11e0
      (itself called once from 0x08143ac8) - the registrar maps eleven
      big-endian fourcc tags ("albm" "aclk" "bmdg" "calv" "cpct" "capt"
      "sbtl" "date" "imag" "imgc" "imgg", pool words @
      0x081e12fc..0x081e1350) to handler entry points. Algorithm:
      find-or-create the node for `tag` in the global std::map<u32,u32>
      rooted @ BSS 0x08ad7d74 via FUN_083dbd9c @ 0x083dbd9c (tree walk @
      0x083ce874, plain u32 less-than comparator @ 0x083d7598 on both
      dereferenced key words, returns node+0x14 = the mapped-value
      slot); store `handler` only when the slot reads 0 - set-if-unset,
      a pre-existing registration wins. The callee is called TWICE on
      the store path (never reusing the slot across a possible
      rebalancing insert); the port reproduces both calls. Sibling
      getter FUN_081e13b4 @ 0x081e13b4 (same map, same callee, one bl
      site @ 0x081e9b5c) reads the slot and blx's the handler with the
      tag owner object in r3, so the stored word is a raw entry-point
      address. Not hooked anywhere yet; on target the seam default
      transmutes 0x083dbd9c so a hooked build is faithful. match.py
      structural diff vs worktree --lib archive: same shape - key
      spilled to a stack slot, two calls with (map, &key), the
      conditional store; differences are the dispatch-slot blx
      indirection with the slot loaded through read_volatile (the
      UPDATE_DISPATCH_OPS house pattern) and the crate fp frame; the
      pool word 0x08ad7d74 is emitted. Host tests (4): unset tag stores
      with exactly two lookups and (map, key) args verified; occupied
      tag keeps the existing handler with a single lookup; a zero
      handler is still stored (the branch tests the current value, not
      the new one); distinct tags receive distinct slots. No
      try_map_u32_slab fixture needed - the test model is a
      static-array find-or-insert map under a parking_lot lock.

  - address: 0x08076920
    ghidra: FUN_08076920
    name: state_object_destroy
    size: 4
    sig: "void *state_object_destroy(void *this)"
    module: cxx/state_object_destroy
    status: ported
    renames:
      param_1/r0: this
      r0 return: this
    notes: >-
      A lone `bx lr` (the word 0xe12fff1e) - an empty C++ destructor,
      not a veneer: no `ldr pc,[pc,#-4]`, no branch target, no trailing
      literal word. The next separately linked function (the object-init
      helper @ 0x08076924) starts at +4, so Ghidra's 4-byte extent is
      exact. This is the destructor of the 0x20-byte state-machine base
      subobject the init helper builds: link fields at +0x00/+0x04/+0x08
      zeroed, class-descriptor word at +0x0c, init argument at +0x10,
      zero at +0x14, state FourCC 'stop' (0x73746f70) at +0x18, zero at
      +0x1c. The timer class embeds it at offset 0 (its constructor @
      0x0812c65c calls the init helper with the class descriptor from
      0x089cb26c, then writes its own state word at +0x20). Binary
      scan of every B/BL word in osos.dec: exactly 22 bl call sites, 0
      predicated forms, 0 plain-b tail calls, matching Ghidra's count;
      the address appears in no image data word, so binding is static,
      never virtual. Every site is member cleanup inside a base-object
      destructor: `add r0, r<base>, #offset` (offsets 0x8, 0x14, 0x1c,
      0x28, 0x48, 0x4c, 0x60, 0x64, 0x98, 0x114, 0x238) then the bl,
      then either the next member's add or `mov r0, r4/r5` - r0 is dead
      after the call at all 22 sites and no caller chains into
      operator_delete (0x082aad24) with it live. The port still returns
      this, matching both the instruction and the crate's other empty
      destructor ports. Own link_section .text.state_object_destroy so
      LLVM MergeFunctions cannot fold it into empty_destructor
      (0x0826fc64) or trivial_destructor (0x082646ac), which share this
      body. match.py structural diff vs worktree --lib archive: LLVM
      emits the crate's usual fp frame (push {fp, lr}; mov fp, sp;
      pop {fp, pc}) instead of the bare bx lr - the identical shape
      empty_destructor compiles to, semantically a no-op return with r0
      preserved. Host tests (2): r0 pass-through across NULL, unaligned
      and sentinel pointers; a 0x2c-byte fixture byte-compared before
      and after to prove the body stores nothing.

  - address: 0x082e9b40
    ghidra: FUN_082e9b40
    name: negate_i32_in_place
    size: 20
    sig: "u32 negate_i32_in_place(i32 *value)"
    module: app/negate_i32_in_place
    status: ported
    renames:
      param_1: value
      r0 return: zero_status
    notes: >-
      Raw bytes decode as ldr r1,[r0]; rsb r1,r1,#0; str r1,[r0]; mov
      r0,#0; bx lr: negate the aligned signed word in place using
      two's-complement wrapping arithmetic, then return zero. The next
      separately entered function starts at 0x082e9b54, so Ghidra's
      20-byte extent is exact. Decoding every ARM B/BL word in osos.dec
      finds exactly 21 callers: all are unconditional bl instructions,
      with no predicated bl forms and no plain-b tail calls. No NULL or
      alignment guard exists in the original or port. The Rust
      wrapping_neg preserves the ARM result for INT_MIN; host tests
      cover zero, both signs, representative bits, both extrema, and
      double negation restoring the original word.

  # --- tagged-pointer initializer ---
  - address: 0x08166a2c
    ghidra: FUN_08166a2c
    name: tagged_pointer_init
    size: 12
    sig: "u8 *tagged_pointer_init(u8 *this, u32 tag)"
    module: cxx/tagged_pointer_init
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: tag
      r0 return: this
    notes: >-
      Raw ARM is `strb r1,[r0]; mov r1,#0; str r1,[r0,#4]; bx lr`: write
      tag's low byte at +0, preserve bytes +1..+3, clear the aligned word
      at +4, and return this unchanged. True code extent is 12 bytes,
      0x08166a2c..0x08166a38; Ghidra's reported 16 bytes swallows the
      separately linked 4-byte empty destructor at 0x08166a3c. Decoding
      every ARM B/BL word in osos.dec finds exactly 21 direct callers, all
      unconditional plain bl, with no predicated forms or plain-B tail
      calls. No aligned image data word equals 0x08166a2c, so binding is
      static rather than virtual. The Rust port uses a u32 tag to accept
      the complete ARM argument register while intentionally storing only
      its low byte. Its own target text section prevents accidental
      identical-code folding; host tests prove r0 pass-through, high-bit
      tag truncation, the pointer-word clear, and preservation of adjacent
      sentinel and padding bytes. No deliberate behavioral deviations.

  # --- UI flag-update tail forwarder (app/ui_flag_update_forwarder) ---
  - address: 0x082879b0
    ghidra: thunk_FUN_0819f604
    name: ui_flag_update_forwarder
    size: 4
    sig: "u32 ui_flag_update_forwarder(u8 *object, u32 flag)"
    module: app/ui_flag_update_forwarder
    status: ported
    renames:
      param_1/r0: object
      param_2/r1: flag
      FUN_0819f604: ui_flag_update_body
    notes: >-
      Raw extent is exactly four bytes at 0x082879b0..0x082879b4: the
      sole instruction eafc5f13 is an unconditional direct tail b to
      0x0819f604, whose separately entered body spans
      0x0819f604..0x0819f734 (304 bytes including its three literal
      words). Ghidra's size is exact for the thunk, not its target. A
      complete ARM B/BL word scan finds 21 direct bl callers to this
      entry: 19 unconditional and two blne at 0x081ecb7c and
      0x081ecf74. It also has seven tail-b callers: six unconditional
      and bne 0x08289c10. The predicate gates reside in callers; this
      thunk has no NULL or flag guard and forwards r0, r1, and its body
      result unchanged. The target body conditionally copies r1's low
      byte to a selected UI object's +0x64 field then emits two
      framework notifications, but no identity is inferred for that
      body. Deviation: the relocated payload replaces the source-relative
      branch with an ARM literal veneer to the same load address; host
      tests inject a volatile seam and prove NULL, a noncanonical
      all-ones flag word, one dispatch per call, argument preservation,
      and return propagation. match.py on the worktree archive reports the
      expected one-instruction b versus the relocation-safe ldr pc plus
      target word veneer; no original instruction prefix remains by design.

  # --- vendored OpenSSL: the EVP digest entry points @ 0x0804a564..0x0804a734 ---
  # Identity is nailed down by OpenSSL's own assertion string, verbatim at
  # 0x0804a694: "ctx->digest->md_size <= EVP_MAX_MD_SIZE" (crypto/evp/digest.c).
  # The cluster is EVP_DigestInit_ex @ 0x0804a6bc (clears flag bit 2, releases
  # the old md_data, stores the type at ctx+0x00, allocates type->ctx_size
  # (+0x44) into ctx+0x0c via traced_alloc, tail-calls type->init (+0x10)),
  # EVP_DigestUpdate @ 0x0804a728 (below), EVP_DigestFinal_ex @ 0x0804a604
  # (md_size assert, type->final (+0x18), optional size out-parameter,
  # type->cleanup (+0x20), flag bit 2, iram_memzero of md_data) and the
  # one-shot wrapper @ 0x0804a564 that runs all four over a 16-byte stack
  # EVP_MD_CTX. This is the SSLeay-era 0.9.x layout - update hangs off
  # ctx->digest, not off the context - and is a different animal from the
  # proprietary MBA-obfuscated digest ported as crypto/digest_init.
  - address: 0x0804a6bc
    ghidra: FUN_0804a6bc
    name: evp_digest_init_ex
    size: 108
    sig: "int evp_digest_init_ex(EvpMdCtx *ctx, const EvpMd *digest, void *impl)"
    module: crypto/evp_digest_init_ex
    status: ported
    renames:
      param_1/r0: ctx
      param_2/r1: digest
      param_3/r2: impl (ignored)
      param_1_plus_0x00: digest
      param_1_plus_0x08: flags
      param_1_plus_0x0c: md_data
      param_2_plus_0x10: init
      param_2_plus_0x44: ctx_size
      FUN_08043994: traced_free
      FUN_08043c18: traced_alloc
    notes: >-
      OpenSSL EVP_DigestInit_ex from crypto/evp/digest.c. Raw ARM spans
      exactly 0x0804a6bc through the tail bx r1 at 0x0804a724 (27 words);
      the separately linked EVP_DigestUpdate begins at 0x0804a728, so
      Ghidra's 108-byte extent is correct. A complete raw-image decode of
      every ARM B/BL word finds exactly ten inbound calls: nine unconditional
      bl at 0x0803b530, 0x0804a5ac, 0x0805fd8c, 0x080607dc, 0x08092660,
      0x080a40d0, 0x080a41a4, 0x080e7488, and 0x080ebf18, plus blne at
      0x080ebe90; no tail b or aligned image data word targets this entry.
      The blne caller gates the call itself: this callee has no ctx or digest
      NULL guard. It clears ctx flags bit 1, and if the descriptor changed,
      frees ctx->md_data only when the old descriptor is non-NULL with
      nonzero ctx_size, stores the new descriptor, then allocates its nonzero
      ctx_size through traced_alloc(ctx_size, 0, 0). It leaves md_data
      unchanged for a zero-sized new descriptor, even after a descriptor
      replacement. Finally it tail-dispatches digest->init(ctx), including
      when digest was unchanged, and returns the callback's result. The third
      ABI argument impl/r2 is never read and engine (+0x04) is never written:
      intentional retail behavior that differs from upstream ENGINE-aware
      OpenSSL. Host tests prove release/allocation/init order, exact allocator
      arguments, result propagation, cleaned-bit clear, ignored impl,
      same-descriptor reinitialization without heap calls, and zero-sized
      descriptor preservation of md_data. Deliberate host deviation:
      #[repr(C)] named fields preserve target offsets only where pointers are
      four bytes; the Rust callback is an ordinary call rather than bx tail
      dispatch, with identical observable arguments and result.
  - address: 0x0804a604
    ghidra: FUN_0804a604
    name: evp_digest_final_ex
    size: 144
    sig: "int evp_digest_final_ex(EvpMdCtx *ctx, u8 *md, u32 *size)"
    module: crypto/evp_digest_final_ex
    status: ported
    renames:
      param_1/r0: ctx
      param_2/r1: md
      param_3/r2: size
      uVar1/r6: finish_result
      pcVar2/r2: finish
      pcVar3/r1: cleanup
      FUN_0805f500: heap_panic_entry
      thunk_EXT_FUN_220002d4: iram_memzero_veneer
    notes: >-
      OpenSSL EVP_DigestFinal_ex from crypto/evp/digest.c. Raw ARM code
      spans exactly 0x0804a604..0x0804a694 (36 words); its following bytes
      0x0804a694..0x0804a6bc are the assertion string
      "ctx->digest->md_size <= EVP_MAX_MD_SIZE", referenced by the
      predicated fatal call, and the next independently linked function,
      EVP_DigestInit_ex, starts at 0x0804a6bc. A complete raw-image decode
      of every ARM B/BL word finds exactly 11 inbound calls, all
      unconditional bl at 0x0804a5dc, 0x0804afc0, 0x0804b0fc, 0x0805fd50,
      0x0806077c, 0x080926dc, 0x080a4148, 0x080a41e4, 0x080e74c4,
      0x080e74dc, and 0x080ebf88, with no predicated calls and no tail b
      calls. No aligned word in osos.dec equals this entry address, so no
      indirect dispatch is discovered. The function dereferences ctx and
      digest without guards; signed md_size values through 36, including
      negative values, proceed while values above 36 enter heap_panic_entry.
      It calls digest->finish (+0x18), returns that value unchanged, reloads
      ctx->digest for optional size output (+0x08), reloads again for the
      optional cleanup (+0x20), sets flags bit 1 only after cleanup, then
      reloads digest and md_data before clearing ctx_size (+0x44) bytes via
      the IRAM memzero veneer 0x08037dc8 -> 0x220002d4. Callback-driven
      replacements of descriptor or state therefore affect later steps.
      Host tests prove return propagation, optional size and cleanup paths,
      signed raw size storage, flag gating, state clearing, and a finish
      callback that changes the descriptor and md_data before the reloads.
      Deliberate host deviation: shared #[repr(C)] named fields preserve the
      target offsets only on ARM where pointers are four bytes. The port
      otherwise calls the already-ported IRAM memzero veneer directly.
  - address: 0x0804a728
    ghidra: FUN_0804a728
    name: evp_digest_update
    size: 12
    sig: "int evp_digest_update(EvpMdCtx *ctx, const u8 *data, u32 count)"
    module: crypto/evp_digest_update
    status: ported
    renames:
      param_1/r0: ctx
      r1: data
      r2: count
      FUN_0804a6bc: evp_digest_init_ex (ported)
      FUN_0804a604: evp_digest_final_ex (ported)
      FUN_0804ac2c: evp_md_ctx_cleanup (ported)
      FUN_0804b214: evp_sha1 (the EVP_MD getter; not ported)
    notes: >-
      OpenSSL's `EVP_DigestUpdate` from crypto/evp/digest.c, three words
      and nothing else - `ldr r3,[r0,#0x0]` / `ldr r3,[r3,#0x14]` /
      `bx r3` - i.e. `return ctx->digest->update(ctx, data, count)` as a
      tail call. Extent verified from the raw image rather than trusted:
      the previous function ends at 0x0804a724 with `bx r1` and the next
      one (the base64 encoder) starts at 0x0804a734 with
      `stmdb sp!,{r4,lr}`, so there is no literal pool and Ghidra's 12
      bytes are exact for once. r0, r1 and r2 pass through untouched;
      only r3 - the unused fourth argument register - is clobbered, which
      is why every Ghidra call site shows at most three arguments.
      Call count binary-verified by decoding every B/BL word in osos.dec
      exactly 21 sites, 19 unconditional `bl` and 2 `blgt` (0x080ebfdc
      and 0x080ef6c4), zero tail `b`, and NO data word in the image holds
      0x0804a728, so it is never dispatched indirectly. The predication
      is a real behavioural fact - the body contains no `cmp` at all, so
      there is neither a NULL guard nor a zero-length guard, and both
      predicated callers test the buffer pointer and the byte count
      themselves (`cmp r0,#0x0` / `cmpne r5,#0x0` / `movgt` the three
      arguments / `blgt`) before deciding to call. A zero count that does
      arrive is still forwarded to the algorithm, and this port keeps
      that. The EVP_MD slots the port declares are the ones the four
      sibling entry points actually load - md_size +0x08, init +0x10,
      update +0x14, final +0x18, cleanup +0x20, and ctx_size +0x44;
      +0x1c and the eight words through +0x40 remain opaque because this
      cluster does not inspect them. Deviation - host only - host pointers
      are twice the target's 32-bit width, so EvpMd and EvpMdCtx place their
      slots as `#[repr(C)]` named fields instead of addressing raw bytes; on
      armv5te-none-eabi the fields land on the original offsets, and the
      pinned body words plus the decoded `#0x14` immediate are asserted in a
      host test so the declared slot stays tied to the encoding.
      match.py against the worktree archive shows
      the same two loads and the same `bx r3` tail branch, with a
      `push {fp, lr}` / `pop {fp, lr}` pair the release profile's frame
      pointers add around them; the dispatch structure is identical.
  # --- vendored OpenSSL: EVP_MD_CTX teardown ---
  - address: 0x0804ac2c
    ghidra: FUN_0804ac2c
    name: evp_md_ctx_cleanup
    size: 136
    sig: "int evp_md_ctx_cleanup(EvpMdCtx *ctx)"
    module: crypto/evp_md_ctx_cleanup
    status: ported
    renames:
      param_1/r0: ctx
      iVar1/r0: digest
      pcVar2/r1: cleanup
      FUN_0805f440: heap_poison
      FUN_08043994: traced_free
    notes: >-
      Raw ARM extent is exactly 0x0804ac2c..0x0804acb4 (34 instruction
      words): the preceding 0x0804ac0c..0x0804ac28 are the tail of a string
      literal, and the next independently linked function starts at
      0x0804acb4 with `push {r3,r4,r5,r6,r7,lr}`. A complete raw-image
      decode of every ARM B/BL immediate finds exactly 11 inbound sites, all
      unconditional `bl` (0x0803b598, 0x0804a5f4, 0x0804ad0c, 0x0804adb4,
      0x0804afc8, 0x0804b104, 0x0805fed0, 0x08060870, 0x08092738,
      0x080a41ec, and 0x080e7504), with no predicated calls and no direct
      tail `b`; no image word equals this entry address, so no indirect
      dispatch is discovered. Algorithm: ctx itself is dereferenced
      unconditionally; if ctx->digest is non-NULL, its optional cleanup slot
      (+0x20) runs unless ctx->flags bit 1 is set. The code RELOADS digest
      after that arbitrary callback, then if its ctx_size (+0x44) and
      ctx->md_data are nonzero and flags bit 2 is clear, calls heap_poison
      (ctx->md_data, ctx_size) followed by traced_free on a RELOADED md_data
      pointer. It finally stores zero to all four context words and returns
      1. Host tests prove callback replacement of digest/md_data is honored,
      poison precedes free, the bit-1 and bit-2 gates affect their distinct
      actions, and a NULL digest still clears every word. Deliberate
      host/codegen deviation: target offsets use `#[repr(C)]` named fields
      because host pointers are 8 bytes; the final stores are volatile to
      retain the ARM word clears and avoid a memclear helper, with identical
      memory effects. Both callees are already ported and are called
      directly.

  # --- vendored OpenSSL: the SHA-1 EVP_MD descriptor getter ---
  - address: 0x0804b214
    ghidra: FUN_0804b214
    name: evp_sha1
    size: 8
    sig: "const EvpMd *evp_sha1(void)"
    module: crypto/evp_sha1
    status: ported
    renames:
      r0: sha1_descriptor
      DAT_0804b21c: evp_sha1_descriptor
      FUN_0804a6bc: evp_digest_init_ex (not ported)
      FUN_0804a728: evp_digest_update (ported)
    notes: >-
      OpenSSL's `EVP_sha1` getter: exactly two instruction words
      (`ldr r0,[pc]` / `bx lr`) return the EVP_MD descriptor literal
      0x08906620. The immediate successor begins at 0x0804b220, so this
      getter's true extent is eight instruction bytes plus its four-byte
      literal pool at 0x0804b21c; Ghidra's eight-byte code size is exact.
      A complete raw-image scan decoding every ARM B/BL word found exactly
      11 direct callers, all unconditional `bl` at 0x0805f540,
      0x080625f0, 0x08062a2c, 0x08078938, 0x08092650, 0x080a40c0,
      0x080a4194, 0x080ce234, 0x080e7478, 0x082d4268, and 0x082d4360;
      it has no predicated callers and no tail `b` sites. No word in the
      image equals 0x0804b214, so it has no discovered indirect dispatch
      reference. The descriptor address is firmware RAM and cannot be
      dereferenced by a host test; this port deliberately returns the
      same numeric pointer on every target, preserving the firmware ABI.

  # --- vendored OpenSSL: the EVP_PKEY wrapper lifecycle @ 0x0804ae6c..0x0804af2f ---
  # The p_lib.c cluster: EVP_PKEY_free @ 0x0804ae6c (below), the 8-byte
  # return-0 stub @ 0x0804aeac, EVP_PKEY_new @ 0x0804aeb4 (traced_alloc(24),
  # stamps {type=0, references=1, pkey=NULL, save_parameters=1,
  # attributes=NULL} - the 0.9.6-era evp_pkey_st - and reports failure with
  # 0x08049a84(ERR_LIB_EVP=6, EVP_F_EVP_PKEY_NEW=106, ERR_R_MALLOC_FAILURE=
  # 65)), EVP_PKEY_size @ 0x0804af10 (type==EVP_PKEY_RSA ? RSA_size : 0, the
  # callee 0x08063040 being (BN_num_bits(rsa->n @ +0x10) + 7) / 8) and
  # EVP_PKEY_type @ 0x0804af30 ({6,19}->6, 28 kept, {66,67,70,113,116}->116,
  # else 0 - EVP_PKEY_RSA/RSA2, EVP_PKEY_DH, EVP_PKEY_DSA1..4/DSA, NID_undef).
  - address: 0x0804ae6c
    ghidra: FUN_0804ae6c
    name: evp_pkey_free
    size: 64
    sig: "void evp_pkey_free(EvpPkey *pkey)"
    module: crypto/evp_pkey
    status: ported
    renames:
      param_1/r0: pkey
      FUN_08043828: crypto_add_lock (ported, called directly)
      FUN_08093cbc: evp_pkey_free_it (unported; EVP_PKEY_OPS.free_it seam)
      FUN_08043994: traced_free (ported, called directly - OPENSSL_free)
      FUN_0804aeb4: evp_pkey_new (unported; constructor sibling)
      FUN_0804af10: evp_pkey_size (unported)
      FUN_0804af30: evp_pkey_type (unported)
      FUN_08062374: rsa_free (unported; the tag-6/19 payload release)
    notes: >-
      OpenSSL's `EVP_PKEY_free` from crypto/evp/p_lib.c, sixteen
      instruction words with no literal pool; the next separately linked
      function (the 8-byte `mov r0,#0; bx lr` stub) starts at 0x0804aeac,
      so Ghidra's 64-byte extent is exactly right. Algorithm: NULL returns
      untouched; otherwise CRYPTO_add_lock(&x->references @ +0x08, -1,
      CRYPTO_LOCK_EVP_PKEY=10, NULL, 0); a decremented count still > 0
      (signed bgt) returns; the final drop runs EVP_PKEY_free_it @
      0x08093cbc (tags 6/19 only: RSA_free(x->pkey.rsa) @ 0x08062374,
      whose layout checks out - references @ +0x38 under CRYPTO_LOCK_RSA=
      9, rsa_meth_st.finish @ meth+0x20, CRYPTO_free_ex_data class 6, eight
      BN_frees of n..iqmp @ +0x10..+0x2c) and hands the 24-byte wrapper to
      traced_free. Call count binary-verified by decoding every B/BL word
      in osos.dec: exactly 15 sites - 12 unconditional `bl`, 2 `bleq`
      (0x080e8424, 0x080f4f68) and 1 `blne` (0x082d4938) - no tail `b`, no
      data word holds the address, so it is never dispatched virtually. The
      predicated sites are caller-side error-path gates, NOT NULL guards:
      the body carries its own `movs r4,r0 / beq`. Deviation:
      EVP_PKEY_free_it remains on the EVP_PKEY_OPS seam (the
      crypto/bn_num_bits.rs shape) - target calls its stock body in place
      while host tests install a recorder. CRYPTO_add_lock is now the direct
      port at 0x08043828, whose shared resource-operation hook table stays
      at its stock NULL defaults during these tests. Host tests: NULL is a
      no-op; a shared key drops its reference without teardown; the last
      reference runs free_it BEFORE traced_free on the same pointer; and
      nonpositive counts (0 -> -1, -7 -> -8) still take the teardown path,
      proving the signed gate. match.py against the worktree archive: same
      NULL guard, same signed >0 gate, same three-call teardown sequence;
      the diff is the remaining free_it seam and LLVM's frame-pointer setup.
      #[inline(never)] for the 15 `bl` targets.
  # --- Obfuscated table transform, fixed-selector adapter ---
  - address: 0x0802b488
    ghidra: FUN_0802b488
    name: table_transform_fixed_selector
    size: 16
    sig: "u32 table_transform_fixed_selector(u32 context, u32 table, u32 arg)"
    module: util/table_transform_fixed_selector
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: table
      param_3/r2: arg
      FUN_0802af9c: table_transform_body
    notes: >-
      Raw extent is exactly sixteen bytes at 0x0802b488..0x0802b498, byte
      confirmed by the distinct eight-byte sibling at 0x0802b498 (ldr
      r0,=0x5b7e363c; bx lr) which Ghidra omits from its listing. Four
      instructions: mov r3,r2; mov r2,r1; ldr r1,[pc,#100] (literal
      0x988a6f1f at 0x0802b4fc); b 0x0802af9c. A tail branch, so the
      body's r0 is the return value. A complete ARM B/BL word scan finds
      21 direct bl callers, 17 unconditional and four predicated; every
      caller sits in the 0x0800c3dc..0x0800daxx cluster, which is itself
      mixed-boolean-arithmetic obfuscated. The 1260-byte body at
      0x0802af9c is not ported. It rewrites the selector as r1 -
      0xdd04c3a0 = 0xbb85ab7f, the head of the opaque-constant table at
      0x0802b4a0, indexes the third argument as table[(k<<11)+(j<<10)+
      (byte<<2)], and fills a 160-byte stack buffer in a loop. No
      identity is inferred for that body. Deviation: the relocated
      payload replaces the source-relative b with a volatile seam call to
      the same load address; host tests inject a recording seam and prove
      the register shuffle, the bound selector, the single call, full
      32-bit argument fidelity, result propagation, and the selector's
      rewrite relation. match.py shows the same register shuffle and
      literal; the only difference is the source-relative b replaced by
      the seam's volatile pointer load plus bx, as designed.
  # ---- HFS B-tree node fetch (src/fs/hfs_btree_get_node.rs) ----
  - address: 0x08053d6c
    ghidra: FUN_08053d6c
    name: hfs_btree_get_node
    size: 168
    sig: "i32 hfs_btree_get_node(BTreeControlBlock *btree, u32 node_number, BTreeNodeBlock *block)"
    module: fs/hfs_btree_get_node
    status: ported
    renames:
      param_1/r0: btree
      param_2/r1: node_number
      param_3/r3: block
      FUN_08044b08: check_node
      FUN_0806b5b8: trash_node
      field 0x1c: node_size
      field 0x20: total_nodes
      field 0x3c: get_block_proc
      field 0x48: num_get_nodes
      block field 0x0c: block_size
      block field 0x10: block_read_from_disk
    notes: >-
      Apple's HFS GetNode from the classic BTreeIO.c, in the three-argument
      form that predates the flags/kGetNodeHint parameter - the options word
      handed to the get-block callback is the constant 0 (kGetBlock). The
      image carries the APPLE_HFS and HFSPLUS volume-format strings, and both
      callees come from the same source file - 0x08044b08 is CheckNode (it
      bounds fLink/bLink by totalNodes, kind by the four node kinds, height by
      the control block's treeDepth at +0x02, numRecords by (nodeSize-14)/8,
      and walks the descending record-offset table, returning 36 on any
      inconsistency) and 0x0806b5b8 is TrashNode (release-block callback with
      kReleaseBlock|kTrashBlock == 4, then bumps the release counter at +0x50
      and nils the block). Algorithm - reject node_number >= totalNodes with
      status 36 on an UNSIGNED cmp/bls, publish nodeSize into the caller's
      BlockDescriptor +0x0c, call the +0x3c callback as (fork, node_number, 0,
      block), abort on a nonzero status, bump numGetNodes at +0x48, return
      success immediately when blockReadFromDisk at +0x10 is zero (a cached
      node was validated on the way in), otherwise CheckNode the buffer; a
      clean node is kept, a bad one sets bit 0 of the u16 at
      btree->fork->volume, trashes the block, and returns the validator's own
      status rather than a re-materialised literal. Every failure exit, and
      only a failure exit, nils buffer and blockHeader; the damaged path does
      so twice (once inside TrashNode, once on the way out) exactly as the
      original does. True extent confirmed from raw bytes as
      0x08053d6c..0x08053e14, 168 bytes, matching Ghidra; the next function is
      the separately entered leaf at 0x08053e14. Decoding every ARM B/BL word
      in osos.dec finds exactly 21 direct callers, ALL unconditional plain bl
      - no predicated forms and no plain-b tail calls - so no caller guards
      this entry and every check it needs it performs itself. No aligned image
      data word equals 0x08053d6c, so binding is static rather than virtual.
      Deviation - the original reaches its three callees as raw machine
      addresses (blx through the control block's +0x3c word plus two direct
      bl), and neither callee body is ported yet, so all three go through one
      volatile BTREE_NODE_OPS table; target builds resolve it to the resident
      addresses and perform the same three transfers, while host tests install
      recorders. Struct offsets are asserted at compile time with offset_of!
      and all target pointer fields are modelled as u32 so the host layout is
      the target layout. Nine host tests cover the at-size and past-size
      rejections on an empty and a full tree, the unsigned compare at
      0x8000_0000, node-size publication and callback argument forwarding, a
      failing callback, the cache-hit skip, a validated disk read, and the
      damaged-volume path including a non-1 blockReadFromDisk byte and
      verbatim status propagation. The two tests that walk
      btree->fork->volume need a below-4-GiB fixture (hint
      HFS_BTREE_GET_NODE = 0x6a00_0000) and therefore skip on arm64 macOS,
      which reserves the low 4 GiB; they run on Linux. match.py (exit 1, diff
      expected) shows the same field offsets, the same control flow and the
      same ldr/ldr/ldrh/orr/strh damaged-volume sequence; the only differences
      are the ops-table indirection (blx r6/r7/r8 for blx ip and the two bl)
      and LLVM's larger frame.
  # --- Refcounted handle release, virtual-slot-1 instantiation ---
  - address: 0x0839d1d4
    ghidra: FUN_0839d1d4
    name: refcounted_body_release_slot1
    size: 144
    sig: "void refcounted_body_release_slot1(RefcountedBody **slot)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r4: slot
      FUN_0839d264: lock_if_mutex_helper
      FUN_0839d274: unlock_if_mutex_helper
    notes: >-
      Ghidra reports 136 bytes; the raw extent is 144 at
      0x0839d1d4..0x0839d264 because the shared str r6,[r4] / pop tail at
      0x0839d25c..0x0839d260 belongs to both exits. Two separately linked
      16-byte helpers follow at 0x0839d264 (lock) and 0x0839d274 (unlock),
      each loading body+8, NULL-checking it and tail-branching to
      mutex_lock 0x0807f5c4 / mutex_unlock 0x0807f6a0 (both ported); the
      next function starts at 0x0839d284. A complete ARM B/BL word scan
      finds 20 direct bl callers, all unconditional, in the 0x08137xxx
      and 0x08162xxx clusters; no b sites and no predicated forms. Third
      template instantiation of the refcounted-handle release over the
      RefcountedBody layout shared with refcounted_body_release
      (0x0839cd98) and refcounted_body_release_owned (0x0839d2c0); the
      only difference is the final drop's treatment of the implementation
      word at body+0: this one dispatches virtual slot 1 (+4) via ldr
      r1,[r0]; ldr r1,[r1,#4]; blx r1 and never frees the implementation.
      No identity is inferred for slot 1. Decrement is a plain wrapping
      subs on a fresh slot load, so zero underflows to -1 and takes the
      shared path; the unlock helper runs on another fresh slot load and
      guards only the mutex word; the final drop is gated on a further
      fresh load being non-NULL, then mutex_delete 0x0807f650, tag-2
      operator_delete 0x082aad24 of the reloaded mutex word, mutex field
      cleared, tag-2 delete of the body, and the slot NULLed on every path
      past the decrement. Five host tests through the module's recording
      kernel and heap seams prove final-drop event order with vtable[1]
      dispatched and vtable[7] untouched, the non-final path, a final drop
      with neither implementation nor mutex, the zero-underflow wrap, and
      the NULL-body early-out. Deviation: LLVM inlines the ported mutex
      operations where the original bl's the helpers; match.py shows the
      same guard, decrement, dispatch and delete structure in a larger
      body.
  # --- Refcounted handle copy assignment, virtual-slot-1 release ---
  - address: 0x0839f118
    ghidra: FUN_0839f118
    name: refcounted_ptr_copy_assign_slot1
    size: 48
    sig: "RefcountedBody **refcounted_ptr_copy_assign_slot1(RefcountedBody **dst, RefcountedBody *const *src)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r5: dst
      param_2/r4: src
      FUN_0839d1d4: refcounted_body_release_slot1
      FUN_0839d198: refcounted_body_attach
    notes: >-
      Raw ARM confirms Ghidra's 48-byte extent: 0x0839f118..0x0839f148, where
      the separately linked refcounted_ptr_construct_variant begins. Decoding
      every ARM B/BL word in osos.dec finds nine direct callers, all
      unconditional bl instructions at 0x0813763c, 0x081377b8, 0x081623f4,
      0x081c03a8, 0x081c03f4, 0x081c046c, 0x081c0534, 0x081c05a4, and
      0x083e0b5c, plus the unconditional tail b at 0x083e0b74; no predicated
      calls and no image data word reference this address. C++ copy-assignment
      compares the slot addresses, then on inequality releases the old body
      with the slot-1 virtual-destructor instantiation, loads *src AFTER that
      release, attaches the loaded body, and returns dst. The direct attach
      entry at 0x0839d198 is a separately entered 56-byte helper whose raw
      body is byte-identical in behavior to the ported attach at 0x0839d370
      modulo mutex branch displacements; this port deliberately calls the
      canonical ported helper. Two host tests cover the same-slot no-op and
      non-final release plus source attachment. match.py exit 1 is its
      expected structural diff output; it confirms the slot-address guard,
      release/load/attach order, and returned destination.
  # --- "crts" string-pool entry copy between pools (util/string_pool) ---
  - address: 0x080be830
    ghidra: FUN_080be830
    name: string_pool_copy_entry
    size: 176
    sig: "int string_pool_copy_entry(StringPool *src, int src_id, StringPool *dst, int *dst_id_out)"
    module: util/string_pool
    status: ported
    renames:
      param_1/r0-r5: src
      param_2/r1-r6: src_id
      param_3/r2-r7: dst
      param_4/r3-r8: dst_id_out
      FUN_080b4318: string_pool_read seam (pool blob reader, unported)
      FUN_080c5a94: string_pool_intern seam (pool interning writer, unported)
      FUN_0805d1d4: malloc_tag4
      FUN_0805d070: free_tag4
      "sp+4..sp+0x204": 512-byte stack scratch blob
      "sp+0x204": len
      "0xffffff94": MEM_FULL_ERR (-108, Mac OS memFullErr)
    notes: >-
      Copies one entry between two "crts" pools: queries the payload
      length of entry src_id in src through the unported reader
      0x080b4318 (dst NULL, max_len 0x7fffffff), stages the payload
      through a 512-byte stack buffer when it fits (unsigned cmp, so
      exactly 512 stays on the stack) or a malloc_tag4 allocation
      otherwise, reads it for real with max_len = the queried length,
      then hands the buffer to the unported interning writer 0x080c5a94
      which stores the new 1-based id through dst_id_out; a failed
      allocation returns -108 (mvn r0, #107) and a heap buffer is freed
      on every path past allocation. Extent self-verified from raw bytes:
      the body runs 0x080be830 (push r4-r8,lr) through the pop at
      0x080be8dc, and the next separately linked function (an in-place
      word byte-swap loop) starts with push r4,r5,lr at 0x080be8e0; both
      constants are mvn immediates so there is no trailing literal pool
      and Ghidra's 176 bytes is exact. Call census by decoding every ARM
      B/BL word in osos.dec: exactly 15 direct bl sites, all
      unconditional (fourteen back to back in the slot-copy chain of
      0x0806ad74, whose final +0x8a8 slot open-codes this exact sequence
      inline and confirms the semantics, plus one in 0x080d189c), and
      one tail b at 0x0806b03c; the address occurs in no data word, so
      it is never dispatched virtually. Deliberate deviations: the two
      unported pool callees dispatch through the volatile STRING_POOL_READ
      / STRING_POOL_INTERN seams whose target defaults transmute the
      retail addresses (the util/crts_object.rs precedent — no identity
      beyond the verified behaviour is invented for either), while the
      already-ported malloc_tag4/free_tag4 are called directly per
      util/inner_state.rs; the scratch buffer is MaybeUninit so no memset
      appears where the original has none, and the free condition
      (blob != stack blob) is computed from the allocation choice, which
      is equivalent. match.py structural (exit-1 diff expected): same
      query/allocate/copy/intern/free shape with the same mvn #107
      failure path; differs by the crate fp frame and blx dispatch
      through the volatile seams. Eight host tests through recording
      read/intern seams and the shared mock heap prove query-failure
      short-circuit, stack staging at 5 and exactly 512 bytes, the 513
      byte heap round trip with tag-4 alloc and free, memFullErr on
      allocation failure, copy-read and intern failure propagation with
      the heap buffer still freed, and the zero-length blob.

  # --- "crts" string-pool counted-string store (util/string_pool) ---
  - address: 0x080be81c
    ghidra: FUN_080be81c
    name: string_pool_store_counted
    size: 20
    sig: "int string_pool_store_counted(StringPool *pool, const uint16_t *counted, int *id_out)"
    module: util/string_pool
    status: ported
    renames:
      param_1/r0: pool
      param_2/r1: counted (u16-length-prefixed UTF-16 string)
      param_3/r2: id_out
      FUN_080b4e60: string_pool_store seam (replace-and-intern wrapper, unported)
      FUN_080a7714: crts_has_tag (called by the unported tail target)
      FUN_080c5efc: string_pool_release (runs inside the tail target)
      FUN_080c5a94: string_pool_intern seam (the tail target's own tail target, unported)
    notes: >-
      Stores a counted UTF-16 string into the pool, replacing the id the
      caller holds in *id_out. Extent self-verified from raw bytes: the
      body is exactly five instructions, 0x080be81c through the tail b
      0x080b4e60 at 0x080be82c, and the next separately linked function
      (string_pool_copy_entry) starts with push r4-r8,lr at 0x080be830,
      so Ghidra's 20 bytes is exact; its C, however, silently inlines
      the bodies of BOTH the tail target 0x080b4e60 and that function's
      own tail target 0x080c5a94, which is why it shows releases, pool
      locks and allocation loops that this address never executes. The
      thunk: mov r3,r2 (id_out); movs r2,r1; ldrhne r2,[r1],#2; lslne
      r2,r2,#1; b 0x080b4e60 - a non-NULL counted yields data = counted
      + 1 (the payload directly follows the length word) and len =
      *counted * 2 (the prefix counts 16-bit units, the pool stores
      bytes), while a NULL counted leaves both zero, which is how a
      caller clears a slot: the wrapper releases the old id and the
      interning writer's zero-length path zeroes *id_out and returns 0.
      The tail target 0x080b4e60 (76 bytes; the bx lr at 0x080b4eb0 is
      alignment padding, not body) was decoded from raw bytes for the
      seam ABI: it runs the 0x080a7714 tag guard, releases *id_out via
      string_pool_release (ldr r1,[r4] is unconditional, so a NULL
      id_out faults and callers never pass one), then tail-branches into
      0x080c5a94 with the same four arguments; a failed release
      short-circuits with its status. Call census by decoding every ARM
      B/BL word in osos.dec: exactly 15 direct bl sites, all
      unconditional (0x08046388, 0x0804644c, 0x0804650c, 0x08046570,
      0x080465d4, 0x080671e8, 0x080672a0, 0x08067bd8, 0x08067c04,
      0x08067c30, 0x08067c84, 0x08094d24, 0x080cb24c, 0x080cb298,
      0x080dcb2c), no predicated forms, no tail branches, and the
      address occurs in no data word, so it is never dispatched
      virtually. Deliberate deviations: the unported tail target
      dispatches through the volatile STRING_POOL_STORE seam whose
      default transmutes retail 0x080b4e60 (the util/crts_object.rs
      precedent - no identity beyond the raw-byte-verified behaviour
      documented on the StringPoolStore ABI alias is invented for it),
      and the length is widened to u32 before doubling as the original's
      ldrh + 32-bit lsl do, so a count of 0x8000 yields 0x10000 bytes,
      not 0. match.py structural (exit-1 diff expected): same NULL gate,
      halfword load, doubling shift and tail dispatch; differs by the
      crate fp frame, LLVM choosing beq over predicated execution, an
      explicit mov r1,#0 on the NULL path, and blx-equivalent indirect
      dispatch through the volatile seam word. Five host tests through a
      recording store seam prove the NULL string (data NULL, len 0,
      status and id_out untouched), the empty string (data = counted + 2,
      len 0), payload passthrough as little-endian bytes with a NULL
      id_out forwarded unchecked, the u32 widening at count 0x8000, and
      that a NULL pool and a failing status pass through with no guard
      in the thunk itself.

  # --- "crts" string-pool entry release (util/string_pool) ---
  - address: 0x080c5efc
    ghidra: FUN_080c5efc
    name: string_pool_release
    size: 176
    sig: "int string_pool_release(StringPool *pool, int id)"
    module: util/string_pool
    status: ported
    renames:
      param_1/r0-r4: pool
      param_2/r1-r5: id
      FUN_080a7714: crts_has_tag
      "*(pool+0x04)": flags (bit 0 FLAG_REFCOUNTED, bit 6 FLAG_IMMUTABLE)
      "*(pool+0x08)": entries (Handle to PoolEntry[])
      "*(pool+0x0c)": refcounts (Handle to i32[])
      "*(pool+0x1c)": entry_count
      "*(pool+0x30)": lock_depth
      "*(pool+0x34)": reclaimable_bytes
      "0x80000001": ENTRY_RECLAIMABLE
      "0xffffffce": PARAM_ERR (-50)
    notes: >-
      Releases one reference to an interned entry of the 0x58-byte
      "crts"-tagged pool that crts_object_destroy @ 0x0809e160 tears down
      and whose +0x30 counter the util/tagged_counter pair moves. Extent
      self-verified from raw bytes: the body runs 0x080c5efc (push
      r4,r5,r6,lr) through the pop at 0x080c5fa8 and the next separately
      linked function's push sits at 0x080c5fac, with no trailing literal
      pool because the function loads no constant from memory - so
      Ghidra's 176 bytes is exact. r6 is pushed and never used, purely for
      eight-byte stack alignment. Call census by decoding every ARM B/BL
      word in osos.dec - exactly 21 direct call sites, 18 plain bl and 3
      blne (0x08048fa8, 0x08048fb8, 0x08095e3c), plus 2 tail branches, b
      at 0x080caa24 and bne at 0x080da8a0; the predicated forms are
      caller-side gates on unrelated conditions, not NULL guards, since
      this function guards its own pool argument. The address occurs in no
      data word, so it is never dispatched virtually. Algorithm - reject
      NULL, a foreign tag, a non-zero lock_depth at +0x30, and flags bit 6
      with -50 (mvn r0, #0x31); id 0 returns 0 as a documented no-op,
      which is why 0x080ca934 releases fourteen stored ids back to back
      without a single zero test and why the interning allocators
      0x080b4e60 / 0x080be81c open by releasing the caller's old id; a
      negative id or one past entry_count at +0x1c returns -50 (signed
      compare). When flags bit 0 is set the parallel i32 refcount reached
      through the +0x0c handle is decremented unconditionally with
      wrapping arithmetic and only a zero result proceeds - an
      already-zero count underflows to -1 and does not free. Freeing adds
      the entry's length word to reclaimable_bytes at +0x34 and stamps
      0x80000001 over its blob offset. Both handle fields are
      Mac-Memory-Manager handles (pointer to a relocatable master
      pointer), hence the double dereference, and -50 is classic Mac OS
      paramErr. Bit 31 of a PoolEntry's first word marks a non-live entry;
      bit 0 separates this reclaimable state, whose stale bytes still
      occupy the blob, from the allocator's 0x80000000 free-list marker,
      so a released entry is not returned to the free list here - the
      compaction pass 0x080a64bc that the allocator runs before its next
      allocation consumes reclaimable_bytes instead. Deliberate deviations
      - the four-instruction tag guard at 0x080a7714 calls the ported
      crts_has_tag directly, without a dispatch seam; the two handle fields are
      typed as native double pointers rather than the u32 words
      util/crts_object.rs uses, because that module only compares and
      forwards them while this one dereferences them - native pointers
      keep the 4-byte target spacing exactly (asserted at compile time
      with offset_of under cfg(target_pointer_width = "32")) and let the
      host tests exercise every path instead of skipping on hosts that
      cannot map a fixture below 4 GiB. match.py structural review
      (exit-1 diff expected) - same shape and identical field offsets
      (+0x30, +0x04 tsteq #0x40, +0x1c, +0x0c, +0x08, +0x34) and the same
      0x80000001 store; differs only by the inlined tag compare with its
      literal-pool word, the crate fp frame, and LLVM hoisting id-1 into
      scaled register indexing in place of the original's pre-indexed
      ldr r1,[r0,#-4]!. Host tests (16) cover NULL, a foreign tag, a
      locked pool, an immutable pool, id 0, negative and out-of-range
      ids, the boundary id equal to entry_count, an empty pool,
      uncounted release, accumulation, the wrapping add at i32::MAX,
      counted release with references remaining, the last reference, an
      already-zero count, an uncounted pool never touching the refcount
      handle, and re-reading the master pointer at call time.

  - address: 0x0829dcf8
    ghidra: FUN_0829dcf8
    name: fs_error_status
    size: 40
    sig: "u32 fs_error_status(const FileAccessor *accessor)"
    module: fs/error_status
    status: ported
    vars: {param_1: accessor, iVar1: errno}
    renames:
      FUN_082d0aa4: storage_errno
      FUN_082d0ae8: ata_error_record
    notes: >-
      The file-path layer's failure-status accessor. Ghidra's 40 bytes
      cover only the entry stub at 0x0829dcf8; its out-of-line body is
      the 324 bytes at 0x0829dbb4..0x0829dcf4 that the stub tail-branches
      into with `b 0x0829dbb4`. Ghidra lists NO function at 0x0829dbb4 in
      functions.csv yet inlines that switch into the C for 0x0829dcf8, so
      the true extent is 0x0829dbb4..0x0829dd1c = 364 bytes; the 0x0829db9c
      entry (vector_size_elem2_clamped) already recorded 0x0829dbb4 as the
      next function start. A scan of every ARM B/BL word in osos.dec finds
      21 references to the entry - 20 plain `bl` plus one `beq 0x0829dcf8`
      tail-call at 0x081bc9c4 inside the sibling accessor FUN_081bc998,
      which returns 0 outright when its drive-letter check succeeds and
      only falls through to the mapping when it fails (a success
      short-circuit, not a NULL guard). The body at 0x0829dbb4 has exactly
      one reference in the image - the entry's own tail `b` - and appears
      in no DATA word, so it is not virtually dispatched. Algorithm - if
      the accessor's sticky status override at +0x20 is nonzero, return it
      verbatim (the transfer loops 0x081bdfe0 / 0x081be11c clear that word
      before each chunk); otherwise read the calling task's storage-layer
      errno (the code word ata_report_error @ 0x083690a8 stores at +0x04
      of the per-task record ata_error_record @ 0x082d0ae8 owns, fetched
      by the reader @ 0x082d0aa4) and translate it - 0..=11 through a
      pc-relative jump table, the sparse high codes through a signed
      compare tree - to 0x3f 0x40 0x07 0x42 0x43 0x44 0x45 0x46 0x47 0x32
      0x49 0x4a for 0..=11, plus 0xd 0x36, 0x11 0x35, 0x16 0x37, 0x18
      0x34, 0x1c 0x38, 0x1e 0x39, 0x1f 0x3a, 0x20 0x3b, 0x65 0x3c, 0x66
      0x3d, 0x67 0x3e; everything else, including codes the unsigned
      `cmp r1,#0xa` / `addls pc` pair reads as negative, yields 0x4b.
      Deliberate deviations - the original's `mov r0,r4` hands the mapping
      body a `this` it never reads (the body only tests r1), so the port
      drops the dead argument; the compare tree becomes a Rust `match`,
      exact because every non-default arm is an `==` test; the still
      unported 16-byte reader @ 0x082d0aa4 is open-coded over the already
      ported ata_error_record behind a FS_ERROR_HOOKS.storage_errno seam
      (the hook exists so host tests drive the mapping without racing
      ata_cmd's tests for the shared record pool - its default is the real
      record read, not a stub). Codegen deviation - LLVM replaces the
      split table-plus-tree with one dense 0x68-entry jump table covering
      0..=0x67, default in every hole; 158 instructions against 81, same
      answer for every input. match.py structural (exit-1 diff expected) -
      the entry keeps the ldr +0x20 / cmp / early-return / fetch-errno /
      tail-branch shape, differing only by the dropped dead argument, the
      crate fp frame and the `blx` through FS_ERROR_HOOKS standing in for
      `bl 0x082d0aa4`. Host tests (9) cover every mapped code through both
      the mapping and the entry, the holes and the tail past 0x67, codes
      the original reads as negative, a parked override of 1/0x4b/0x53/-1,
      errno 0 mapping to 0x3f rather than reading as success, that no
      status collides with the callers' 0 success value, that the head
      words are neither read nor written, and that status_override really
      sits at byte offset 0x20.

  - address: 0x080c4ba8
    ghidra: FUN_080c4ba8
    name: ata_pio_write_byte
    size: 28
    sig: "i32 ata_pio_write_byte(u8 *destination, u8 value)"
    module: drivers/ata_pio_write_byte
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: value
      DAT_080c4bc4: ata_controller_base
    notes: >-
      Raw ARM is ldr r3, [pc, #20]; ldr r2, [r3, #0x78]; tst r2, #2;
      beq back to the ready load; strb r1, [r0]; mov r0, #0; bx lr.
      The literal pool word at 0x080c4bc4 is 0x38700000, the S5L8702 ATA
      controller base, so +0x78 is ATA_PIO_READY. It waits for PIO write
      readiness (bit 1), stores the low byte to the caller-selected ATA
      task-file register, and returns zero without a NULL guard or timeout.
      The next function starts at 0x080c4bc8, confirming the 28-byte code
      extent plus its 4-byte literal. Decoding every ARM B/BL word in
      osos.dec finds exactly 21 unconditional bl call sites and no
      predicated form; a raw word scan finds no DATA reference to this
      entry, so it is not virtually dispatched. Host builds model only the
      fixed MMIO ready register with an atomic word so the readiness gate can
      be tested; device builds retain the volatile physical read.

  - address: 0x0821754c
    ghidra: FUN_0821754c
    name: app_controller_dispatch_layout
    size: 48
    sig: "void app_controller_dispatch_layout(AppController *controller, void *layout)"
    module: app/controller_layout_dispatch
    status: ported
    renames:
      param_1/r0: controller
      param_2/r1: layout
      iVar1: global_controller
    notes: >-
      The verified extent is 0x0821754c..0x08217578; 0x0821757c is the
      next bx-lr entry. It calls app_controller_get @ 0x0817ee04, stores
      halfword 5 at that returned object's +0x80 mode field, then tail
      dispatches the supplied controller's vtable +0x11c slot with
      (controller, layout). The input controller is retained across the
      getter in r4 and is not interchangeable with the singleton. Decoding
      every ARM B/BL word in osos.dec finds 20 unconditional bl call sites,
      no plain-b entries, and no predicated forms. The vtable slot is runtime
      data on the stale 0x0898xxxx page, so no callee identity is claimed.
      Host tests use structural 64-bit vtables; target-width offset assertions
      preserve the firmware's +0x80 and +0x11c fields.

  # --- Application boot metrics channel singleton ---
  - address: 0x08264740
    ghidra: FUN_08264740
    name: app_boot_metrics_channel_get
    size: 48
    sig: "u8 *app_boot_metrics_channel_get(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0826476c: app_boot_metrics_channel_cache
      FUN_082aadd4: operator_new
      FUN_08266990: app_boot_metrics_channel_construct
    notes: >-
      True extent is 48 bytes: 44 bytes of instructions
      (0x08264740..0x08264768) followed by the 0x089cc918 literal-pool
      word at 0x0826476c; the next function starts at 0x08264770. Raw ARM:
      push {r4,lr}; load cache; if non-NULL skip; otherwise
      operator_new(0xac), call FUN_08266990 with that raw result, cache the
      constructor return; reload and return the cache. Decoding every ARM
      B/BL word in osos.dec finds exactly 20 unconditional bl call sites,
      no predicated bl form, and no plain-b tail sites. The object's
      "Channel AppBoot" dump label and caller startup-duration records
      submitted through FUN_082664b4 establish the application-boot metrics
      channel role; the fuller class identity is not recovered. The
      constructor remains unported and is deliberately behind the existing
      singleton constructor-table seam; host builds use its documented
      zeroing default and a crate static in place of runtime cache
      0x089cc918, so this getter is not hook-ready until FUN_08266990 is
      ported.

  # --- PhotoBrowse slideshow controller singleton ---
  - address: 0x081cbbb0
    ghidra: FUN_081cbbb0
    name: photo_browse_slideshow_get
    size: 56
    sig: "u8 *photo_browse_slideshow_get(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_081cbbe0: photo_browse_slideshow_cache
      DAT_081cbbe4: photo_browse_slideshow_size
      FUN_082aadd4: operator_new
      FUN_081cd5b8: photo_browse_slideshow_construct
    notes: >-
      True extent is 56 bytes: twelve instruction words at
      0x081cbbb0..0x081cbbdc followed by the cache (0x08a09e00) and
      allocation-size (0x000008fc) pool words at 0x081cbbe0 and
      0x081cbbe4; raw bytes show the next function begins at 0x081cbbe8.
      The lazy getter reloads cache; if it is NULL it calls
      operator_new(0x8fc), invokes FUN_081cd5b8(allocation, NULL), stores
      that result, reloads and returns the cache. Decoding every ARM B/BL
      word in osos.dec finds exactly 18 direct BL call sites, all
      unconditional, with no predicated BL forms and no plain-B tails. The
      constructor routes the "PhotoBrowse_Slideshow" literal through its
      base construction path, identifying the controller without claiming a
      fuller C++ class name. Its unported two-argument constructor joins the
      existing singleton constructor-table seam with a zeroing default, and
      the port uses a crate static for the runtime cache word; it is not
      hook-ready until FUN_081cd5b8 is ported.

  # --- Two-word record initialization helper ---
  - address: 0x081b4e24
    ghidra: FUN_081b4e24
    name: store_u32_pair
    size: 8
    sig: "u32 *store_u32_pair(u32 *dst, u32 first, u32 second)"
    module: util/u32_pair_store
    status: ported
    renames:
      param_1: dst
      param_2: first
      param_3: second
    notes: >-
      True extent is 8 instruction bytes at 0x081b4e24..0x081b4e2b; the
      separately linked next function starts at 0x081b4e2c. Raw ARM is
      stm r0, {r1,r2}; bx lr: it stores first and second at dst[0] and
      dst[1] and returns the unchanged dst through r0. Ghidra's void
      signature is incomplete; callers at 0x081281a8, 0x0816de24,
      0x081dcf7c, 0x081dd024, and 0x081fadc4 consume that return. Decoding
      every ARM B/BL word in osos.dec found 20 unconditional bl call sites,
      no predicated bl forms, and one unconditional B tail branch at
      0x082ae914. There is no NULL guard. The Rust export has its own
      link section to retain a distinct hook seam if LLVM folds an identical
      body.

  # --- Query object destructor ---
  - address: 0x0813e5c4
    ghidra: FUN_0813e5c4
    name: query_object_destroy_port
    size: 48
    sig: "u8 *query_object_destroy_port(u8 *query)"
    module: fp/fp_misc
    status: ported
    renames:
      param_1/r0/r4: query
      DAT_0813e5f0: query_object_derived_vtable
      FUN_08044d58: query_inner_destroy
      FUN_08259860: query_destroy_tail
    notes: >-
      True extent is 48 bytes: 44 instruction bytes at
      0x0813e5c4..0x0813e5ec and the 0x08984e18 literal-pool word at
      0x0813e5f0; the separately linked next function begins at
      0x0813e5f4. Ghidra's claimed 192-byte body incorrectly absorbs that
      sibling and the tail target. Decoding every ARM B/BL word in osos.dec
      finds exactly 20 direct BL call sites, every one unconditional
      (no predicated BL); a word scan finds no data-word occurrence of
      0x0813e5c4, so current evidence is direct, non-virtual dispatch.
      Algorithm: write vtable 0x08984e18 to query+0, read the byte at
      query+0x45, and only for mode 2 invoke 0x08044d58 with the u32
      backend pointer at query+0x40, including NULL. It then tail-branches
      to 0x08259860, a distinct destructor target whose class identity is
      not recovered; its result returns in r0. The Rust port preserves both
      unported retail calls through target defaults and host-swappable
      seams, but uses a call/return instead of the final ARM tail branch.
      Host tests prove the non-mode-2 skip, mode-2 release-before-tail
      ordering, NULL forwarding, vtable-before-release, and r0 forwarding.
      match.py structural review against the worktree archive shows the
      vtable store, mode gate, conditional call, and result forwarding;
      extra instructions are the two-function seam dispatch.

  # --- Trace-buffer local-static accessor ---
  - address: 0x0814a08c
    ghidra: FUN_0814a08c
    name: trace_buffer_get
    size: 144
    sig: "u8 *trace_buffer_get(void)"
    module: app/trace_buffer
    status: ported
    renames:
      DAT_0814a10c: trace_static_guard
      DAT_0814a110: trace_static_words
      DAT_0814a114: __dso_handle
      DAT_0814a118: trace_static_destructor_word
      FUN_0814a2c8: trace_buffer_construct
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab1c8: cxa_atexit
      FUN_082ab338: cxa_guard_release
      FUN_0807f680: condvar_init
      FUN_08094404: mutex_lock_counted
      FUN_0809449c: mutex_unlock_counted
      FUN_082aadd4: operator_new
    notes: >-
      True raw extent is 144 bytes: 128 instruction bytes at
      0x0814a08c..0x0814a108 followed by four literal-pool words at
      0x0814a10c..0x0814a118; the separately linked next function begins
      at 0x0814a11c. This corrects the supplied/Ghidra 128-byte code-only
      size. Decoding every ARM B/BL word in osos.dec finds exactly 20 direct
      callers, all unconditional BL: 0x0807c6d0, 0x0807c6e4, 0x0807c744,
      0x08084da0, 0x0808697c, 0x080869fc, 0x0808e0f4, 0x08090b78,
      0x0809b654, 0x080a28b8, 0x080ab060, 0x080ab24c, 0x080b566c,
      0x080c6818, 0x080c6848, 0x080d3540, 0x080d9024, 0x08149f54,
      0x0818a080, and 0x08296eec; no predicated BL and no direct tail B.
      The accessor ignores incoming r0, initializes the three words at
      0x08a778e8 under the ADS guard/cache page 0x089cb1ec, then locks
      those words, allocates 0x40 through operator_new only while cache+4 is
      NULL, calls the 0x0814a2c8 constructor, caches its result including
      NULL, unlocks, and returns the cache. Constructor 0x0814a2c8 is not
      ported, so app/trace_buffer uses a documented zeroing dispatch seam.
      Literal 0x080ed318 is not a function entry: raw ARM places it on
      `cmp r4,r6` inside the function beginning 0x080ed2f0, so the
      cxa_atexit registration deliberately uses a no-op callback. Host
      builds model the three target words directly because CondVar and
      CountedMutex layouts diverge at 64-bit pointer width; target builds
      directly invoke the ported condvar and counted-mutex functions.

  - address: 0x0814a130
    ghidra: FUN_0814a130
    name: trace_buffer_slot_acquire
    size: 144
    sig: "TraceBufferEntry *trace_buffer_slot_acquire(TraceBuffer *buffer, u32 selector, CountedMutex **entry_guard)"
    module: app/trace_buffer
    status: ported
    renames:
      param_1/r0/r4: buffer (0x40-byte trace-buffer registry)
      param_2/r1/r6: selector (unsigned entry slot)
      param_3/r2/r7: entry_guard (one-word output counted-mutex guard)
      r5: initialized_mutex (buffer+0x2c)
      FUN_0807f5c4: mutex_lock
      FUN_080ed9dc: condvar_wait_forever
      FUN_0807f6a0: mutex_unlock
      FUN_0818a128: unported counted-mutex scope-guard acquire helper
      FUN_08149f68: unported selector-and-presence validator
      FUN_0814a030: default registry-root accessor
      FUN_08206e8c: unported entry-lock scope-guard acquire helper
      FUN_0809449c: mutex_unlock_counted
    notes: >-
      Raw ARM establishes the exact 144-byte instruction extent
      0x0814a130..0x0814a1c0; the separately linked next function begins at
      0x0814a1c0. Decoding every ARM B/BL word in osos.dec finds exactly 11
      direct BL call sites, all unconditional (no predicated forms):
      0x08086988, 0x08086a08, 0x0808e100, 0x0809b660, 0x080a28c4,
      0x080ab06c, 0x080ab258, 0x080b5678, 0x080c6854, 0x080d9030 and
      0x0818a08c. One additional unconditional tail B at 0x08149f64 is the
      forwarding wrapper; no aligned data word equals 0x0814a130, so the
      resolver is not a virtual-dispatch target. It locks buffer+0x2c and
      waits on buffer+0x34 while byte buffer+0x28 is zero, then takes the
      counted table mutex at +0x1c. Selectors >= 7 and NULL slots call the
      default-root ADS accessor 0x0814a030 without modifying entry_guard.
      A live slot stores entry+0x44 into entry_guard, locks it, unlocks the
      table mutex, and returns the entry; the caller owns the returned lock.
      The two unported one-word scope-guard helpers 0x0818a128 and
      0x08206e8c are inlined through the existing counted-mutex ports. The
      unported default-root accessor remains a target fixed-address boundary
      with a host-test seam. Host tests cover selectors 0 and 6, NULL slots,
      u32::MAX, output-guard preservation, and lock ownership/balancing.

  # --- Legacy printf growable byte sink ---
  - address: 0x08077bcc
    ghidra: FUN_08077bcc
    name: format_buffer_append_char
    size: 200
    sig: "void format_buffer_append_char(u8 **inline_buffer, u8 **heap_buffer, u32 *length, u32 *capacity, u32 value)"
    module: printf/format_buffer
    status: ported
    renames:
      param_1/r0/r7: inline_buffer
      param_2/r1/r4: heap_buffer
      param_3/r2/r6: length
      param_4/r3/r5: capacity
      param_5/[sp,#24]/r8: value
      FUN_08043c18: traced_alloc
      FUN_08043f3c: traced_realloc
      thunk_EXT_FUN_22000020: __rt_memcpy
    notes: >-
      True raw extent is exactly 200 bytes, 0x08077bcc..0x08077c94; the
      separately linked formatter function begins at 0x08077c94. Decoding
      every ARM B/BL word in osos.dec finds 20 direct inbound BL call sites,
      all unconditional and no predicated BL: 0x080e7b0c, 0x080e7b6c,
      0x080e7e9c, 0x080e817c, 0x080e819c, 0x080e81c0, 0x080e81e4,
      0x080e8204, 0x080e8238, 0x080e8258, 0x080e82b4, 0x080e9124,
      0x080e9148, 0x080e915c, 0x080e9188, 0x080e91a8, 0x080e91c8,
      0x080e9280, 0x080e92b8, and 0x080e92ec. With a non-NULL
      heap_buffer slot, loop while length >=u capacity: a NULL heap pointer
      first allocates capacity bytes through ported traced_alloc
      (capacity = 0x400 only when it was zero), copies length bytes from
      inline_buffer via __rt_memcpy through the 0x08037db0 IRAM veneer, then
      clears inline_buffer; a non-NULL heap pointer first adds 0x400 to
      capacity and calls ported traced_realloc 0x08043f3c. Once
      length < capacity, increment length with u32 wrap and store value's
      low byte at the old offset, choosing inline_buffer when non-NULL and
      heap_buffer otherwise. Fixed-capacity operation is selected by a NULL
      heap_buffer slot. Deviation: the raw body proceeds to an invalid copy
      or byte store after a NULL allocation/reallocation result; the port
      retains the preceding capacity/heap-slot stores but returns before
      that invalid access. Host tests cover fixed-capacity emission/full
      drop, initial allocation at 0x400, inline-byte migration, the
      mandatory second growth at an exactly full initial heap, and safe
      failed reallocation. match.py structural review compares the
      capacity loop, allocation/copy, realloc, and byte-store paths; the
      null-result guard adds control flow.

  # --- Legacy printf sprintf veneer ---
  - address: 0x080edc2c
    ghidra: FUN_080edc2c
    name: retail_sprintf
    size: 28
    sig: "i32 retail_sprintf(u8 *buf, const u8 *format, VaList args)"
    module: printf/format_buffer
    status: ported
    renames:
      param_1/r0: buf
      param_2/r1: format
      FUN_080f3c24: retail_vsprintf
    notes: >-
      The legacy format-conversion family's sprintf, named retail_sprintf
      to distinguish it from the separately ported standard-library veneer
      sprintf @ 0x0802f724. Raw ARM confirms the 28-byte extent exactly
      (0x080edc2c..0x080edc48; the distinct next function begins at
      0x080edc48): push {r0,r1,r2,r3} (ADS variadic spill), push {r4,lr},
      ldr r1,[sp,#12] (reload the spilled format), add r2,sp,#16 (va_list
      at the spilled third argument), bl 0x080f3c24, pop {r4},
      ldr pc,[sp],#20 (drop the spill and return). r0 passes through
      untouched and the callee's count survives the epilogue, so the veneer
      returns it unchanged; no NULL guard on any argument. Decoding every
      ARM B/BL word in osos.dec finds exactly 15 inbound BL call sites,
      all unconditional (no predicated BL, no tail B): 0x0806e0cc,
      0x080beed8, 0x080d34d0, 0x08112234, 0x081125cc, 0x08112850,
      0x08116864, 0x081168bc, 0x0812bacc, 0x081cb0ac, 0x081cb0f4,
      0x081eb48c, 0x081ebb5c, 0x081ef4d0, and 0x0828b73c. Call site
      0x0806e0a4 pins the shape: format literal "%d.%d.%d" unpacking a
      packed word into four version bytes, three register args plus one
      stack arg. The callee retail_vsprintf @ 0x080f3c24 is NOT ported
      (decoded: spills r2/r3, seeds a cursor at buf, calls conversion core
      0x08077c94 as (sink descriptor 0x0807ca58, &cursor, 0xffffffff,
      format, args), NUL-terminates at the final cursor, returns the core
      count), so the call dispatches through the RETAIL_VSPRINTF seam
      whose wired default is the ported vsprintf @ 0x0802f654 - same
      unbounded, NUL-terminating, count-returning contract, differing only
      in the conversion engine (0x08034374 family vs the unported
      0x08077c94 core). Deviations: the variadic ... becomes an explicit
      VaList (house convention; args IS the pointer the spill builds), and
      the bl becomes an indirect tail call through the seam. Host tests
      prove verbatim forwarding of buf/format/args, count propagation
      (including a negative count), NULL pass-through without a guard,
      that the veneer writes nothing itself, and the wired default's
      identity. match.py structural: both bodies are 7-instruction
      forward-and-return veneers; the diff is the crate's fp frame and the
      seam indirection (bx r3 through RETAIL_VSPRINTF instead of a direct
      bl), the documented deviations.

  # --- Four-word record constructor ---
  - address: 0x0803a488
    ghidra: FUN_0803a488
    name: four_word_record_create
    size: 80
    sig: "*mut FourWordRecord four_word_record_create(u32 payload)"
    module: util/four_word_record
    status: ported
    renames:
      param_1/r0/r4: payload
      puVar1/r0: record
      FUN_08043c18: traced_alloc
      FUN_08049a84: diag_ring_record
    notes: >-
      Raw ARM confirms the supplied 80-byte extent exactly:
      0x0803a488..0x0803a4d8, followed by the separately linked function at
      0x0803a4d8. Decoding every ARM B/BL word in osos.dec finds 20 direct
      inbound call sites, every one an unconditional BL; there are no
      predicated BL forms and no direct tail B. Calls traced_alloc(16, 0, 0);
      a non-NULL result receives zero, payload, zero, zero in its four
      ascending 32-bit words. NULL records diagnostic (0x0d, 0x82, 0x41, 0,
      0) through the directly ported diag_ring_record and returns NULL. No
      dispatch seam or other deliberate deviation: both callees are already
      ported. Host tests prove allocator arguments, all four success stores,
      NULL return, and the exact failure-ring tag/data.

  # --- Embedded collection item-count virtual forwarder ---
  - address: 0x082a6888
    ghidra: FUN_082a6888
    name: list_item_count
    size: 16
    sig: "uintptr_t list_item_count(void *list)"
    module: cxx/list_item_count
    status: ported
    renames:
      param_1/r0: list
      "list+0xec": collection (u32 target pointer)
      r1_after_second_ldr: collection_vtable
      r1_after_third_ldr: collection_item_count_method
    notes: >-
      True raw extent is exactly 16 bytes, 0x082a6888..0x082a6898: `ldr
      r0,[r0,#0xec]; ldr r1,[r0]; ldr r1,[r1,#0x170]; bx r1`. The next
      separately linked function starts at 0x082a6898. Decoding every ARM B/BL
      word in osos.dec finds exactly 19 direct inbound calls, all unconditional
      BL: 0x081105c8, 0x0816c19c, 0x0816c224, 0x08187ce4, 0x0818847c,
      0x081884f8, 0x08188668, 0x08286100, 0x08286258, 0x082863e0,
      0x082864f8, 0x08286af8, 0x08286b1c, 0x08286b58, 0x082872dc,
      0x08287418, 0x08287b7c, 0x08287cf8, and 0x08288c3c. There are no
      predicated direct BL forms and no direct tail B callers. It dereferences
      the collection pointer held at list+0xec, loads that object's vtable slot
      +0x170, and tail-dispatches it with the collection in r0. Callers use the
      returned word as a list-item count; the concrete class and target remain
      unrecovered, so the port uses the object's own vtable rather than
      inventing a callee identity. Deviation: Rust makes the terminal bx a
      final call whose result is returned immediately. The list's embedded
      pointer remains a u32 on host and target; host tests map its fixture
      below 4 GiB so this target layout remains exact.

  # --- StreamCache mass-storage manager singleton accessor ---
  - address: 0x08228068
    ghidra: FUN_08228068
    name: stream_cache_mass_storage_manager_get
    size: 88
    sig: "*mut u8 stream_cache_mass_storage_manager_get()"
    module: app/stream_cache_mass_storage_manager
    status: ported
    renames:
      DAT_082280b0: STREAM_CACHE_MASS_STORAGE_MANAGER_GUARD
      DAT_082280b4: STREAM_CACHE_MASS_STORAGE_MANAGER
      DAT_082280b8: dso_handle
      DAT_082280bc: mass_storage_manager_destructor
      FUN_08228270: STREAM_CACHE_MASS_STORAGE_MANAGER_CTOR
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab1c8: cxa_atexit
      FUN_082ab338: cxa_guard_release
    notes: >-
      True raw extent is 88 bytes: 72 bytes of code at
      0x08228068..0x082280b0 and the four-word literal pool at
      0x082280b0..0x082280c0; the next separately linked function begins
      at 0x082280c0. Decoding every ARM B/BL word in osos.dec finds exactly
      19 direct inbound call sites, all unconditional BL:
      0x081af590, 0x081af718, 0x081c8748, 0x081c875c, 0x08201740,
      0x08201a48, 0x08201a54, 0x08206324, 0x082063ec, 0x08206464,
      0x082064c0, 0x082064cc, 0x0820668c, 0x082066e0, 0x082068e4,
      0x08206a30, 0x08206a90, 0x08206ae8, and 0x0822813c. No predicated
      BL forms or direct tail B enter it. It is the ADS function-local-static
      sequence: fast-path test guard bit 0; cxa_guard_acquire; construct
      the fixed manager 0x08ae4b4c through unported FUN_08228270; register
      the constructor return with cxa_atexit(dtor 0x0821d440,
      __dso_handle 0x089ca09c); cxa_guard_release; then reload and return
      the fixed manager literal, never the constructor result. The embedded
      constructor at 0x08228270 initializes through manager+0x2c and the
      sibling worker at 0x082280c0 reads manager+0x30, establishing the
      port's 0x34-byte target-layout storage extent. Deliberate deviations:
      the device seam calls retained retailOS constructor 0x08228270, while
      the host default is inert until tests install a constructor. The
      registered handler word 0x0821d440 enters after the actual prologue at
      0x0821d43c and cannot safely execute standalone, so its typed Rust
      shutdown handler is a no-op rather than inventing a callee identity.

  # --- Fixed selector table-record address mapper ---
  - address: 0x08218aec
    ghidra: FUN_08218aec
    name: selector_record_address
    size: 36
    sig: "u32 selector_record_address(u32 base, u32 selector)"
    module: util/selector_record_address
    status: ported
    renames:
      param_1/r0: base
      param_2/r1: selector
      r2: range_index
    notes: >-
      Raw extent is exactly 36 bytes, 0x08218aec..0x08218b10: the following
      `push {r4,lr}` begins the separately linked function at 0x08218b10.
      Decoding every ARM B/BL word in osos.dec finds exactly 19 direct inbound
      call sites, all unconditional BL: 0x08218430, 0x08218768, 0x082189e8,
      0x082347ec, 0x08234c08, 0x08234c60, 0x08237c80, 0x08237cd8,
      0x0825d8b0, 0x0825dc44, 0x0825dc9c, 0x08260cd4, 0x08260d2c,
      0x082611c8, 0x08261220, 0x0826a810, 0x0826a868, 0x0826ad2c, and
      0x0826ad84. There are no predicated BL forms or direct tail B callers.
      For selectors 0x801c through 0x8029 inclusive, the leaf returns
      base + selector * 12 - 0x60144; every other u32 returns zero. The ARM
      subtract-and-cmp range test and additions use 32-bit wrapping, retained
      explicitly in Rust. No deliberate deviation.

  # --- Selector-item base-class constructor (PairHeaderBase embedder) ---
  - address: 0x08218b10
    ghidra: FUN_08218b10
    name: selector_item_base_construct
    size: 124
    sig: "void *selector_item_base_construct(void *this, u8 item_id, u32 descriptor, u8 item_kind, u32 context, u8 enable_flag)"
    module: cxx/selector_item_base
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: item_id
      param_3/r2: descriptor
      param_4/r3: item_kind
      param_5/[sp,#8]: context
      param_6/[sp,#12]: enable_flag
      r4: object
    notes: >-
      Raw extent is exactly 124 bytes of code, 0x08218b10..0x08218b8c, plus a
      three-word literal pool at 0x08218b8c..0x08218b98 (vtable 0x08993c60,
      selector 0x801c, impl thunk 0x08ae5444); the next function's
      push {r4, r5, r6, r7, r8, lr} begins at 0x08218b98. Ghidra's 124-byte
      size is the code extent only. Decoding every ARM B/BL word in osos.dec
      finds exactly 17 direct inbound call sites, all unconditional BL:
      0x08218da0, 0x082224d8, 0x08228e48, 0x08234b30, 0x08234f54,
      0x08237a80, 0x08237bf0, 0x08237fec, 0x0823ad74, 0x0823af28,
      0x0825db80, 0x0825dfd0, 0x08260c54, 0x08261094, 0x08261570,
      0x0826ac34, and 0x0826b084. No predicated BL forms, no direct tail B,
      and no data references (the address appears in no vtable). Every call
      site is a derived-class constructor with the same shape (push {r2, r3,
      r4, lr}; plant both stack arguments; bl; then overwrite the vtable
      word at +0x00 with its own and store derived fields at
      +0x18/+0x1c/+0x20), which is what marks this as a base class; the
      class itself is unidentified and the name is structural. The
      constructor builds a 0x180-byte object: vtable at +0x00, flags byte
      at +0x04 updated as (old & ~1 | enable_flag) & ~2 (bit0 from the
      whole low flag byte, bit1 always cleared, upper bits preserved),
      context word +0x08, caller bytes/word at +0xb4/+0xb8/+0xbc, the
      0xb8-byte PairHeaderBase subobject at +0xc0 built by the ported
      pair_header_base_construct @ 0x0810ebbc (this recovered from its
      result minus 0xc0, exactly as the ARM does), trailing words
      +0x178/+0x17c cleared, +0x0c..+0xb3 zero-filled through the IRAM
      veneer 0x08037db8 (the port calls the ported memzero_aligned
      directly, the established idiom), then the default 12-byte selector
      record {selector 0x801c, byte 8, impl 0x08ae5444} planted at
      +0x0c/+0x10/+0x14. The record ties the class into the fixed selector
      table that selector_record_address @ 0x08218aec maps
      (0x801c..=0x8029, 12-byte records). Documented anomaly, not
      resolved: the {selector, impl} pair table at 0x0826acd0 lists the
      same impl thunk 0x08ae5444 under selector 0x801d, while this
      constructor plants it next to 0x801c; 0x08ae5444 is a mid-function
      entry (mov r2, sp; mov r1, #1; mov r0, #25; ...; pop {ip, pc})
      whose owning function is unidentified. No dispatch seams: both
      callees are ported. Host tests prove the full store layout, the
      read-modify-write flags formula (including a bit-1-bearing flag
      never setting bit1), and the original's ordering (vtable and caller
      fields before the embedded base chain; trailing clears, zero-fill
      and selector record after it). match.py structural: LLVM
      reassociates the flags RMW to (old & 0xfc) | (flag & 0xfd)
      (algebraically identical), lowers the fixed-size zero fill to the
      archive-provided weak __aeabi_memclr4 over +0x10..+0xb4 with the
      record stores folded around it, and elides the sub r4, r0, #0xc0
      recovery after proving the ported base ctor returns its argument.

  # --- Four-slot artwork cache availability query ---
  - address: 0x081b6d88
    ghidra: FUN_081b6d88
    name: artwork_slot_available
    size: 80
    sig: "void *artwork_slot_available(void *cache, u32 slot_index)"
    module: app/artwork_slot_available
    status: ported
    renames:
      param_1/r0: cache
      param_2/r1: slot_index
      r4: embedded_mutex
      r5: cache
      r6: slot_index
      uVar1/r0: artwork
    notes: >-
      Raw extent is exactly 80 bytes, 0x081b6d88..0x081b6dd8: the following
      `push {r4,r5,r6,lr}` at 0x081b6dd8 begins the separately linked
      selection/update function, with no literal pool. Decoding every ARM
      B/BL word in osos.dec finds exactly 19 direct inbound call sites, all
      unconditional BL (no predicated BL forms): 0x0822e19c, 0x0822e364,
      0x0822e434, 0x0822e504, 0x0822e5d4, 0x0822e69c, 0x0822e878,
      0x0822e9e0, 0x0822eca8, 0x0822ed78, 0x0822ee98, 0x0822f544,
      0x0822f618, 0x0822f7b4, 0x0822fce0, 0x0822fdb4, 0x0822fecc,
      0x08230238, and 0x08230990. Locks the embedded mutex at cache+0x18,
      then for indexes 0..3 returns the pointer word at cache+index*24+0x24
      only when the byte at cache+index*24+0x38 is zero; otherwise it returns
      NULL and always unlocks. Callers use a non-NULL result to select
      MediaContent resources over NoArt resources, so these are artwork slots.
      The class-0x7f80 cache has no recovered concrete class name. Deliberate
      host deviation: slot pointers remain u32 words and subsequent fields use
      raw offsets because the host's embedded Mutex is wider than its 8-byte
      ARM layout.

  # --- Showcase slot queue pending-entry predicate ---
  - address: 0x081b7f9c
    ghidra: FUN_081b7f9c
    name: showcase_queue_has_pending_entry
    size: 28
    sig: "u32 showcase_queue_has_pending_entry(const u32 *queue)"
    module: app/showcase_queue_has_pending_entry
    status: ported
    renames:
      param_1/r0: queue
      r1: first_cursor
      r0 (after load): second_cursor_or_result
    notes: >-
      Raw extent is exactly 28 bytes, 0x081b7f9c..0x081b7fb8: the following
      `mov r2,#0` at 0x081b7fb8 begins a separately entered initializer, so
      the predicate has no literal pool. Decoding every ARM B/BL word in
      osos.dec finds exactly 11 direct inbound call sites, all unconditional
      BL with no predicated forms: 0x081b6d70, 0x081b6fcc, 0x081b755c,
      0x081b7720, 0x081b7858, 0x081b78d8, 0x081b7ba0, 0x081b7c20,
      0x081b7d44, 0x081b7e8c, and 0x081b7f4c. It returns one precisely when
      the aligned words at queue+4 and queue+8 differ, otherwise zero.
      Showcase initialization and all callers establish that these queues are
      embedded in its four slots. The equality-only predicate cannot establish
      which cursor is producer versus consumer, so the port deliberately uses
      neutral names and preserves the opaque u32-word layout on 64-bit hosts.

  # --- Callback helper forwarding and release ---
  - address: 0x08124c60
    ghidra: FUN_08124c60
    name: callback_dispatch_release
    size: 56
    sig: "u32 callback_dispatch_release(void *context, DispatchValue *value, u32 extra)"
    module: app/callback_dispatch_release
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: callback
      param_3/r2: extra
      r4: callback
      r5: helper_result
      FUN_08124ff4: unported_helper
    notes: >-
      Raw extent is exactly 56 bytes, 0x08124c60..0x08124c98: the following
      `push {r4,r5,r6,lr}` at 0x08124c98 begins a separately linked sibling,
      with no literal pool. Decoding every ARM B/BL word in osos.dec finds 19
      direct inbound BL call sites, all unconditional: 0x081014b0, 0x08101938,
      0x08142cc0, 0x0814713c, 0x0817edd8, 0x0817ffac, 0x0817ffd0, 0x081812e4,
      0x081814f0, 0x08181554, 0x08181648, 0x0818166c, 0x08184050, 0x081b2e34,
      0x081b354c, 0x081f9080, 0x081f9df8, 0x081f9f24, and 0x08288aec. There
      are no predicated inbound BL forms; 0x08124ccc is a tail B from the
      sibling, not a branch-and-link call. The wrapper preserves r2 as stack
      argument five while calling unported FUN_08124ff4 with fixed r2/r3 flags
      1/0, then conditionally invokes its non-NULL callback's vtable +0x04
      entry and returns the saved helper result. FUN_08124ff4 has no ledger
      entry, so no callee identity is claimed. Target builds call its fixed
      address; host tests use a volatile typed seam. The host vtable is
      pointer-width widened but retains the target slot's role.

  # --- PCF50635 register read (drivers/i2c) ---
  - address: 0x0836d3b8
    ghidra: FUN_0836d3b8
    name: pmu_i2c_read
    size: 84
    sig: "i32 pmu_i2c_read(u32 reg, i32 len, u8 *buf)"
    module: drivers/i2c
    status: ported
    renames:
      param_1/r0: reg
      param_2/r1: len
      param_3/r2: buf
      local_18: register
      iVar1/r0: status
      iVar2/r4: completed
      FUN_0836bb84: i2c_write
      FUN_0836b950: i2c_read
    notes: >-
      Raw ARM establishes the exact 84-byte extent 0x0836d3b8..0x0836d40c:
      the following ldr r0,[pc,#44] at 0x0836d40c starts the separately
      linked sibling, with no literal pool. It saves reg as a stack word,
      calls FUN_0836bb84(0x73, 1, &reg), and returns that status immediately
      on failure. A zero write status and signed-positive len call
      FUN_0836b950(0x73, len, buf) exactly once: the stock loop increases
      completed by its full remaining count, so it terminates after that read.
      Zero or negative len still write the register but skip the read; both
      raw statuses return verbatim. Decoding every ARM B/BL word in osos.dec
      verifies 18 unconditional BL call sites at 0x082e53f0, 0x082e5450,
      0x082e55b8, 0x082e55f8, 0x082e574c, 0x082e5b8c, 0x0836d130,
      0x0836d150, 0x0836d160, 0x0836d184, 0x0836d194, 0x0836d1f8,
      0x0836d318, 0x0836d33c, 0x0836d498, 0x0836d5bc, 0x0836d600, and
      0x0836d678; no predicated BL forms. The direct non-call branches are
      bcc 0x0836d3ac, beq 0x0836d690, and b 0x0836d6bc. PORT DEVIATION:
      raw S5L8702 primitives remain unported and are reached through typed
      fixed-address target calls or volatile host-test seams, yielding blx
      instead of the retail direct bl. Host tests cover write failure,
      signed non-positive lengths, single positive reads, verbatim read
      status, bank selection, and invalid-bank no-transfer behavior.

  # --- PCF50635 register write (drivers/i2c) ---
  - address: 0x0836d524
    ghidra: FUN_0836d524
    name: pmu_i2c_write
    size: 60
    sig: "i32 pmu_i2c_write(u32 reg, i32 len, const u8 *data)"
    module: drivers/i2c
    status: ported
    renames:
      param_1/r0: reg
      param_2/r1: len
      param_3/r2: data
      local_10: packet
      iVar1/r0: copied
      FUN_0836bb84: i2c_write
    notes: >-
      Raw ARM establishes the exact 60-byte extent 0x0836d524..0x0836d560:
      the following push {r3,r4,r5,lr} at 0x0836d560 starts the separately
      linked sibling (the PMU reg-0x1d/0x1b writer), with no literal pool.
      The write twin of pmu_i2c_read (FUN_0836d3b8): push {r2,r3,r4,lr}
      reserves a 16-byte stack frame, strb stores only the low byte of reg
      at [sp], a signed byte loop (cmp r0,r1 / ldrblt / strblt) copies len
      payload bytes to [sp+1+i] (non-positive signed lengths copy nothing),
      then FUN_0836bb84 is called as i2c_write(0x73, len+1, sp) — the
      PCF50635 PMU slave — and its raw status returns verbatim. A payload
      above 15 bytes would overrun the retail frame; every caller passes
      small lengths. Decoding every ARM B/BL word in osos.dec verifies 17
      unconditional BL call sites at 0x082e5a94, 0x0836d0ac, 0x0836d0f0,
      0x0836d258, 0x0836d2c8, 0x0836d2e0, 0x0836d430, 0x0836d4f8,
      0x0836d57c, 0x0836d5a0, 0x0836d5e8, 0x0836d65c, 0x0836d6dc,
      0x0836d6f8, 0x0836d7d4, 0x0836d7ec, and 0x0836d804; no predicated
      BL forms. The conditional direct branches bne 0x0836d474, bcc
      0x0836d518, beq 0x0836d710, and beq 0x0836d824 are tail-branch
      references, not calls. PORT DEVIATIONS: the raw S5L8702 I2C write
      primitive remains unported and is reached through a typed
      fixed-address target call / volatile host-test seam, yielding blx
      instead of the retail direct bl; the payload copy uses volatile byte
      accesses so LLVM cannot fold it into a memcpy; LLVM zeroes the
      packet frame with __aeabi_memclr (resolved from compiler_builtins)
      where retail leaves the unsent tail bytes as stack garbage — only
      len+1 bytes are ever transmitted and all are written, so the wire
      behaviour is identical. Host tests cover packet layout, the low-byte
      strb of reg, verbatim status passthrough, signed non-positive
      lengths (len 0 sends the lone reg byte, len -1 wraps the count to
      0), and the 15-byte maximum frame payload.

  # --- Bootstrap-to-current mutex handoff (kernel/mutex_handoff) ---
  - address: 0x081fca10
    ghidra: FUN_081fca10
    name: mutex_handoff_lock
    size: 48
    sig: "void mutex_handoff_lock(MutexHandoff *handoff)"
    module: kernel/mutex_handoff
    status: ported
    renames:
      param_1/r4: handoff
      FUN_0807f5c4: mutex_lock
      FUN_0807f6a0: mutex_unlock
    notes: >-
      Raw ARM establishes the exact 48-byte extent 0x081fca10..0x081fca40:
      the following ldr r0,[r0,#12] at 0x081fca40 starts the separately
      entered companion unlock helper. It locks the embedded Mutex at
      handoff+0x04, reads the selected Mutex pointer at handoff+0x0c, and
      leaves the embedded mutex held when that pointer is identical. Otherwise
      it locks the selected mutex before releasing the embedded mutex, handing
      the caller from bootstrap protection to the selected lock. The companion
      at 0x081fca40 releases handoff+0x0c directly. Decoding every ARM B/BL
      word in osos.dec finds exactly 18 direct inbound calls, all
      unconditional plain BL at 0x081fcb78, 0x08208204, 0x0820823c,
      0x0820839c, 0x0820845c, 0x08208494, 0x082084f4, 0x08208580,
      0x082085ac, 0x08214620, 0x08214658, 0x0821499c, 0x08214a2c,
      0x08214ae4, 0x08214b1c, 0x08214b64, 0x08214dc4, and 0x08214e88.
      There are no predicated BL forms or direct B tail callers, and no
      aligned image data word equals the entry address. The port directly calls
      the already-ported mutex_lock/mutex_unlock rather than inventing a
      dispatch seam; LLVM may inline their ROM-dispatch guards on target.


  - address: 0x081fca40
    ghidra: FUN_081fca40
    name: mutex_handoff_unlock
    size: 8
    sig: "void mutex_handoff_unlock(MutexHandoff *handoff)"
    module: kernel/mutex_handoff
    status: ported
    renames:
      param_1/r0: handoff
      param_1_plus_0xc/r0: current_mutex
      FUN_0807f6a0: mutex_unlock
    notes: >-
      Raw ARM establishes the exact eight-byte extent
      0x081fca40..0x081fca48: `ldr r0,[r0,#0x0c]; b 0x0807f6a0`; the
      separately linked next function opens with `ldrb r1,[r0,#8]` at
      0x081fca48. It loads handoff.current_mutex at +0x0c and tail-branches
      directly to the already-ported mutex_unlock without any pointer guard;
      current_mutex must name a valid Mutex. mutex_unlock then guards its
      semaphore-cell and zero-handle accesses.
      Decoding every ARM B/BL word in osos.dec finds exactly 13 direct inbound
      calls, all unconditional plain BL at 0x08208218, 0x08208380, 0x082084bc,
      0x08208564, 0x08208590, 0x08208608, 0x08214634, 0x082147dc, 0x08214a78,
      0x08214b3c, 0x08214bac, 0x08214dd4, and 0x08214ea4. Four unconditional
      direct B tail callers are 0x082083d8, 0x0820847c, 0x082149d8, and
      0x08214b04; no predicated forms or aligned image data word equals the
      entry address. The Rust port calls the existing mutex_unlock directly,
      so LLVM may retain a call instead of the retail tail branch while
      preserving the selected-mutex release behavior. Host tests cover a
      distinct selected mutex and a selected Mutex with no semaphore cell.
  # --- volume controller opaque byte +0x90 accessor ---
  - address: 0x081fa044
    ghidra: FUN_081fa044
    name: volume_controller_byte_at_90
    size: 8
    sig: "u8 volume_controller_byte_at_90(const u8 *volume_controller)"
    module: app/volume_controller_byte_at_90
    status: ported
    renames:
      param_1/r0: volume_controller
      param_1_plus_0x90: byte_at_0x90
      r0: byte_at_0x90
    notes: >-
      Raw ARM establishes the full eight-byte extent
      0x081fa044..0x081fa04c: `ldrb r0,[r0,#0x90]; bx lr`; the separately
      linked next function opens with `push {r4,lr}` at 0x081fa04c. It returns
      the raw unsigned byte at volume_controller+0x90, with no NULL guard or
      mutation. Decoding every ARM B/BL word in osos.dec finds exactly 18
      direct inbound calls, all unconditional plain BL at 0x081a19fc,
      0x081a1ab8, 0x081a2178, 0x081a21ec, 0x081a22a4, 0x081a5a3c,
      0x081a651c, 0x081a6c80, 0x082170bc, 0x08217410, 0x08217dd8,
      0x08217e2c, 0x08217f14, 0x0821c50c, 0x0821d0c4, 0x0821def4,
      0x0821f11c, and 0x0821fb1c; there are no predicated BL forms or direct
      B tail callers. The volume-controller constructor FUN_081fa070 zeros
      this byte at 0x081fa0b4, and direct callers distinguish zero, nonzero,
      and one, but neither proves the field's domain meaning. The
      offset-based name deliberately preserves that uncertainty. No
      deviations. Host tests cover every u8 result, neighboring-byte guards,
      unaligned input, and absence of mutation. Reference C:
      decomp/c/021/081fa044_FUN_081fa044.c; raw assembly:
      osos.dec 0x081fa044.

  # --- Global application tagged-string registry getter ---
  - address: 0x0819fdb0
    ghidra: FUN_0819fdb0
    name: application_string_registry
    size: 36
    sig: "void *application_string_registry(void)"
    module: app/application_string_registry
    status: ported
    renames:
      FUN_0819fe10: application_resource_provider
      DAT_0819fdd0: application_resource_provider_global (0x089d0188)
      r0_after_call: application_resource_provider
      r0_return: application_string_registry
      application_resource_provider_plus_4: tagged_string_registry
    notes: >-
      Raw ARM establishes the true 36-byte extent: `push {lr}; bl
      0x0819fe10; cmp r0,#0; ldrne r0,[pc,#12]; ldrne r0,[r0]; ldrne
      r0,[r0,#4]; moveq r0,#0; pop {pc}` at 0x0819fdb0..0x0819fdd0,
      plus its 0x089d0188 literal-pool word at 0x0819fdd0; the separately
      linked next function opens with `push {r4,r5,r6,lr}` at 0x0819fdd4.
      Ghidra's reported 32-byte size omits that owned literal-pool word.
      It obtains the global application resource provider through
      application_resource_provider, returns NULL when that provider is NULL,
      and otherwise reloads the global provider word before returning its raw
      32-bit +0x04 tagged-string-registry field; the field itself may be NULL
      and is not validated. Decoding every ARM B/BL word in osos.dec finds
      exactly 11 inbound calls, all unconditional BL at 0x0814745c,
      0x08184e5c, 0x0819f410, 0x0819fddc, 0x081b9888, 0x081b9b14,
      0x081b9e28, 0x081cdb74, 0x081d22ac, 0x081e028c, and 0x0829a4b0;
      there are no predicated BL forms or direct B tail callers. No deliberate
      deviations. Host tests cover an absent provider, a NULL registry field,
      and replacing the live target-width +0x04 field.

  # --- Global application resource-provider getter ---
  - address: 0x0819fe10
    ghidra: FUN_0819fe10
    name: application_resource_provider
    size: 20
    sig: "void *application_resource_provider(void)"
    module: app/application_resource_provider
    status: ported
    renames:
      DAT_0819fe20: application_resource_provider_global (0x089d0188)
      r0: application_resource_provider
    notes: >-
      Raw ARM establishes a 20-byte extent: `ldr r0,[pc,#8] ;
      ldr r0,[r0] ; cmp r0,#0 ; bx lr` at 0x0819fe10..0x0819fe20 plus
      its 0x089d0188 literal pool word at 0x0819fe20; the separately linked
      next function opens with `push {r2,r3,r4,r5,r6,lr}` at 0x0819fe24.
      The cmp only updates flags, so this leaf volatile-loads and returns the
      global application resource-provider pointer verbatim, including NULL
      before initialization. Its resource-list lookup callers and the sibling
      that walks returned+0x20 establish the provider role. Decoding every
      ARM B/BL word in osos.dec finds exactly 18 inbound calls, all
      unconditional BL at 0x081472bc, 0x08182824, 0x08184b2c, 0x08184f24,
      0x081852fc, 0x0818564c, 0x08185674, 0x0819f340, 0x0819f430,
      0x0819fdb4, 0x081b9b48, 0x081d0c04, 0x081d2300, 0x081e05a8,
      0x081e0704, 0x081e96c8, 0x081e9ae8, and 0x0829a3f8. There are no
      predicated BL forms or direct B tail callers. Firmware builds
      volatile-load the fixed runtime-RAM word at 0x089d0188; host builds use
      a zero-initialized private backing word because that image location holds
      stale runtime data.


  # --- registration-handle wrapper destructor ---
  - address: 0x081c87c4
    ghidra: FUN_081c87c4
    name: registration_handle_wrapper_destroy
    size: 24
    sig: "RegistrationHandleWrapper *registration_handle_wrapper_destroy(RegistrationHandleWrapper *wrapper)"
    module: app/registration_handle_wrapper
    status: ported
    renames:
      param_1/r0: wrapper
      DAT_081c87dc: registration_handle_wrapper_vtable (0x089a74ac)
      param_1_plus_4/r0: registration
      FUN_081d9734: registration_handle_destroy
      iVar1/r0: registration
      r0: wrapper
    notes: >-
      Raw ARM confirms the 24-byte code extent 0x081c87c4..0x081c87dc; its
      0x089a74ac vtable literal is the following word at 0x081c87dc, and the
      separately linked sibling starts at 0x081c87e0. It stores that wrapper
      vtable at +0, calls the ported registration_handle_destroy on its
      registration-handle base at +4, subtracts four from the returned base
      address, and returns the wrapper. Decoding every ARM B/BL word in
      osos.dec finds exactly 15 direct inbound calls, all unconditional plain
      BL at 0x0820cff4, 0x0820d04c, 0x0820d05c, 0x0820d338, 0x0820d350,
      0x0820d360, 0x0820d3b8, 0x0820d3cc, 0x0820d42c, 0x0820d47c,
      0x0820d4e0, 0x0820d58c, 0x0820d5dc, 0x0820d620, and 0x0820d66c;
      there are no predicated BL forms or direct B tail callers, and no
      aligned image data word equals this entry. The class identity does not
      survive the zero-filled vtable words at 0x089a74ac..0x089a74b4, so the
      port names it only for the observed registration-handle wrapper role.
      PORT DEVIATION: the wrapper models the embedded base as a named
      #[repr(C)] field rather than applying a literal byte offset; this is
      +4 on target while keeping host pointer widening self-consistent. Host
      tests prove the -1 sentinel path restores both vtables without needing
      a host slot-release seam and returns the outer wrapper.

  # --- manager-table registration handle initializer ---
  - address: 0x081d96a0
    ghidra: FUN_081d96a0
    name: registration_handle_init
    size: 144
    sig: "RegistrationHandle *registration_handle_init(RegistrationHandle *registration, void *owner, unsigned int selector_kind, const unsigned int *selector)"
    module: app/registration_handle
    status: ported
    renames:
      param_1/r4: registration
      param_2/r0: owner
      param_3/r2: selector_kind
      param_4/ip: selector
      DAT_081d9730: registration_handle_vtable (0x089a74bc)
      local_c: slot_index
      FUN_081d95e4: registration_slot_find
      FUN_081d98b8: registration_slot_acquire
      r0: registration
    notes: >-
      Raw ARM confirms the 144-byte code extent 0x081d96a0..0x081d9730; its
      0x089a74bc vtable literal is the following word at 0x081d9730, and the
      separately linked sibling begins at 0x081d9734. It writes the vtable,
      owner, and -1 slot sentinel. A NULL owner returns immediately; selector
      kind 1 calls FUN_081d95e4(owner, -1, selector[0], selector[1], &slot)
      and stops unless it returns zero, while kinds 2 and 3 take selector[0]
      directly. Only unsigned slots below 32 are offered to FUN_081d98b8; a
      non-NULL return causes the slot to be stored. FUN_081d95e4 and
      FUN_081d98b8 remain unported: firmware uses their fixed retailOS
      addresses and host tests install volatile seams. Deliberate deviation:
      the #[repr(C)] registration field names retain its three target words
      while letting the host owner pointer widen. Decoding every ARM B/BL
      word in osos.dec finds exactly 10 direct inbound BL callers, all
      unconditional: 0x081af5d0, 0x081af628, 0x081c8364, 0x081c84ec,
      0x081c8574, 0x081c85f4, 0x081c86e0, 0x081c87b4, 0x081c8818, and
      0x081c8900. There are no predicated BL forms or direct B tail callers.

  # --- manager-table registration handle ---
  - address: 0x081d9734
    ghidra: FUN_081d9734
    name: registration_handle_destroy
    size: 40
    sig: "RegistrationHandle *registration_handle_destroy(RegistrationHandle *registration)"
    module: app/registration_handle
    status: ported
    renames:
      param_1/r4: registration
      DAT_081d975c: registration_handle_vtable (0x089a74bc)
      param_1_plus_4: owner
      param_1_plus_8/r1: slot_index
      FUN_081d9918: registration_slot_release
      r0: registration
    notes: >-
      Raw ARM confirms the 40-byte extent 0x081d9734..0x081d975c; its vtable
      literal is the following word at 0x081d975c, and the separately linked
      sibling starts at 0x081d9760. It stores vtable 0x089a74bc, then calls
      FUN_081d9918(owner, slot_index) unless slot_index is exactly -1, and
      returns registration. Decoding every ARM B/BL word in osos.dec finds 18
      direct inbound BL callers, all unconditional: 0x081af5e8, 0x081af5f8,
      0x081af6c8, 0x081af6e4, 0x081c8394, 0x081c83a8, 0x081c851c,
      0x081c8538, 0x081c85a4, 0x081c85b8, 0x081c8624, 0x081c8638,
      0x081c8730, 0x081c87d0, 0x081c8830, 0x081c8854, 0x081c8934, and
      0x081c8948; there are no predicated BL or direct B tail callers. No
      aligned data word equals this function address, so it is not directly
      vtable-dispatched. Deliberate deviation: unported FUN_081d9918 is a
      volatile host seam and a fixed retailOS-address call on firmware. Host
      tests prove zero invokes it, -1 skips it even with a NULL owner, other
      negative values retain their u32 bit pattern, the vtable store precedes
      either path, and this is returned.

  # --- service-handler slot bit update ---
  - address: 0x08193f50
    ghidra: FUN_08193f50
    name: service_handler_slot_bit_update
    size: 76
    sig: "void service_handler_slot_bit_update(u32 *slot_table, i32 slot, u32 bit, u32 enabled)"
    module: app/service_handler_slot_bit_update
    status: ported
    renames:
      param_1/r0: slot_table
      param_2/r1: slot
      param_3/r2: bit
      param_4/r3: enabled
      param_1_plus_slot_times_0x20: slot_record
      slot_record_plus_0x0c: active_status
      slot_record_plus_0x18: bitset
      FUN_08030f44: heap_panic
      FUN_08193fec: unidentified_service_handler_slot_bit_update_tail
    notes: >-
      Raw ARM establishes the exact 76-byte extent
      0x08193f50..0x08193f9c: `push {r4,lr}` through `pop {r4,lr}; b
      0x08193fec`; the separately linked next function starts at 0x08193f9c
      (`cmp r1,#3`). Ghidra incorrectly folds the tail target at 0x08193fec
      into this entry. The function asserts only slot >= 3, selects an
      eight-word record at slot_table + slot*0x20, clears active_status bit
      zero, and conditionally updates bitset's bit. The ARM register shift
      takes bit & 0xff, producing a zero mask for counts 32 through 255;
      bitset nonzero restores active_status bit zero, while every other
      active_status bit is preserved. It then tail-branches to the unported
      0x08193fec routine; that routine's identity is deliberately not
      invented. Decoding every ARM B/BL word in osos.dec finds exactly 18
      inbound branches: 18 unconditional plain BL at 0x0818e364, 0x0818ea4c,
      0x0818f424, 0x0818fce4, 0x081908d8, 0x08192870, 0x08192b40,
      0x08194528, 0x08194834, 0x08194aec, 0x081d7758, 0x081d7810,
      0x082010e8, 0x082011b8, 0x08208ff8, 0x082091a8, 0x08209238, and
      0x08209368; no predicated BL forms or direct B tail callers. Deliberate
      deviation: firmware makes an ordinary call to the verified unported
      tail address rather than emitting the stock tail B; host tests route it
      through a volatile seam. Tests cover set, clear-to-empty while
      preserving status bits, the ARM shift counts 31, 32, and 0x100, and
      tail invocation after the writes.

  # --- buffered stream discard skip ---
  - address: 0x080bbb00
    ghidra: FUN_080bbb00
    name: buffered_stream_skip
    size: 124
    sig: "i32 buffered_stream_skip(void *owner, i32 distance)"
    module: ft/buffer_skip
    status: ported
    renames:
      param_1: owner
      param_1_plus_0x0: stream
      param_1_plus_0x14: discard_buffer
      param_2: distance
      local_18: position
      local_14: chunk
      FUN_08042e70: buffered_stream_tell
      FUN_08043124: buffered_stream_seek
      FUN_08042fd4: buffered_stream_read
    notes: >-
      Raw ARM confirms the exact 124-byte extent 0x080bbb00..0x080bbb7c:
      the next separately linked function starts with `push {r3,r4,r5,r6,r7,lr}`
      at 0x080bbb7c. Negative distance obtains the 64-bit current position
      through 0x08042e70, adds the sign-extended i32 with carry, then calls
      0x08043124 with the result in the aligned r2/r3 pair. Nonnegative
      distance repeatedly subtracts min(distance, 0x8000) before calling
      0x08042fd4 to read into owner+0x14; a callee-modified count slot cannot
      alter later chunk requests. Zero returns 0 without a call, and every
      callee error propagates unchanged. Decoding every ARM B/BL word in
      osos.dec found exactly 18 inbound branches, all unconditional BL at
      0x0806403c, 0x08064098, 0x080640e4, 0x0807f938, 0x0807f990,
      0x0807fa5c, 0x0807fac8, 0x0807fae0, 0x0807fb6c, 0x0807fc50,
      0x0807fd54, 0x0809641c, 0x08096448, 0x08096480, 0x0809ed40,
      0x0809ee34, 0x0809ee88, and 0x0809eea0; no predicated BL or plain B
      callers. The three unported buffered-stream callees use volatile,
      host-replaceable seams; target builds transmute their verified retailOS
      entry addresses directly. Tests cover zero, 32 KiB chunking, read
      failure, negative wraparound, and tell failure.

  # --- zeroing big-endian stream word reader ---
  - address: 0x080575cc
    ghidra: FUN_080575cc
    name: stream_read_be32_or_zero
    size: 36
    sig: "i32 stream_read_be32_or_zero(u32 ctx, u32 *out)"
    module: util/stream_read_be32_or_zero
    status: ported
    renames:
      param_1/r0: ctx
      param_2/r1: out
      local_10: value
      FUN_08057874: stream_read_be32
    notes: >-
      Raw ARM confirms Ghidra's 36-byte extent: this function runs from
      0x080575cc through 0x080575ec, and its u16-reading sibling begins at
      0x080575f0. It initializes a stack-local u32 to zero, calls the already
      ported stream_read_be32 @ 0x08057874 with that local, unconditionally
      stores the local through out, and returns the callee's 0/1 status
      unchanged. Consequently, unlike stream_read_be32, failure writes zero
      to out. Decoding every ARM B/BL word in osos.dec finds exactly 18
      inbound calls, all plain unconditional BL with no predicated BL forms.
      The function has no NULL guard. Host tests cover a successful big-endian
      read and failure's zeroing store; tests sharing the mutable
      stream-read-core seam use one crate-global lock. Deliberate deviations:
      Rust uses a local u32 in its ABI-selected stack frame rather than the
      retail stack slot; initialized value, call ABI, output store, and status
      are identical. match.py structural review: same initialize -> call ->
      unconditional store -> return shape; LLVM adds a frame pointer and
      stack adjustment.

  # --- query object virtual release wrapper ---
  - address: 0x083e75d0
    ghidra: FUN_083e75d0
    name: query_object_release_slot
    size: 36
    sig: "void **query_object_release_slot(void **slot)"
    module: util/query_object_release
    status: ported
    renames:
      param_1/r0: slot
      param_1_deref: query_object
      query_object_plus_0x0: vtable
      vtable_plus_0x1c: unresolved_release_method
    notes: >-
      Raw ARM confirms the 36-byte extent 0x083e75d0..0x083e75f0; the
      separately linked sibling FUN_083e75f4 begins at 0x083e75f4. The wrapper
      reads query_object from the caller-owned slot, skips the dispatch only
      when that object is NULL, then loads its vtable and calls entry +0x1c
      with query_object itself in r0. After the indirect call it returns slot,
      not the method's return register. Decoding every ARM B/BL word in osos.dec finds
      exactly 17 inbound direct calls, all unconditional plain BL (zero
      predicated BL forms and zero plain B tails); every call site supplies a
      stack-local query object created by query_object_create (0x082597a0) and
      releases it on scope exit. The vtable target cannot be decoded to a real
      function entry from the static image, so its identity is deliberately not
      invented; the port performs the same vtable dispatch directly, with no
      added seam. Deliberate deviation: Rust returns after the indirect method
      rather than emitting `blx; mov r0,r4`; both discard its return value and
      return the original slot. Host tests cover the NULL guard and verify slot
      +0x1c, object forwarding, unchanged slot contents, and returned address.

  - address: 0x083863d4
    ghidra: FUN_083863d4
    name: sqlite_utf8_read
    size: 120
    sig: "u32 sqlite_utf8_read(const u8 *text, const u8 *terminator, const u8 **next)"
    module: sqlite/utf8_read
    status: ported
    renames:
      param_1/r0: text
      param_2/r1: terminator
      param_3/r2: next
      uVar1/r0: codepoint
      uVar2/r12: continuation_or_table_base
      DAT_0838644c: sqlite_utf8_trans1_runtime_address
      DAT_08386450: SQLITE_REPLACEMENT_CHARACTER
    notes: >-
      SQLite 3.5.9's sqlite3Utf8Read (utf.c), recovered from the raw 120-byte
      ARM body 0x083863d4..0x0838644c. The following two words are its literal
      pool (0x0838644c..0x08386454); sqlite_vm_printf opens at 0x08386454, so
      they are not another function. Decoding every ARM B/BL word in osos.dec
      finds exactly 17 direct call sites: all 17 are unconditional BL and
      there are no predicated or tail-B references (15 lexer/parser calls in
      0x082d7f4c..0x082dfd8c, plus 0x0838c4f4 and 0x0838c55c).
      Consumes one byte unconditionally; a byte below 0xc0 returns itself.
      Otherwise SQLite's 64-byte sqlite3UtfTrans1 seed table is folded through
      consecutive continuation bytes until terminator, a non-continuation, or
      the end. Overlong ASCII, U+D800..U+DFFF, U+FFFE, and U+FFFF become
      U+FFFD, while all other historical overlong and 4-to-6-byte encodings
      remain accepted. The raw literal says 0x088faa4b, whose table is at
      image 0x08905923 under the SQLite +0xaed8 image/runtime skew documented
      in sqlite/mod.rs; its 64 recovered bytes exactly match upstream.
  - address: 0x08378f8c
    ghidra: FUN_08378f8c
    name: find_index
    size: 152
    sig: "Index *find_index(sqlite3 *db, const char *name, const char *database)"
    module: sqlite/find_index
    status: ported
    renames:
      param_1/r0: db
      param_2/r1: name
      param_3/r2: database
      uVar3/r4: i
      uVar2/r1: j
      iVar4/r5: schema
      iVar5/r6: result
      DAT_schema_plus_0x18: idx_hash
    notes: >-
      SQLite 3.4.x/3.5.x util.c's sqlite3FindIndex. Raw ARM confirms the
      exact 152-byte extent 0x08378f8c..0x08379024; the separately linked
      sqlite3FindTable starts immediately at 0x08379024. Decoding every ARM
      B/BL word in osos.dec finds exactly 8 inbound direct call sites, all
      unconditional BL (no predicated BL forms or tail-B references):
      0x082b3d20, 0x0836ef48, 0x08373f3c, 0x083754e4, 0x0837b5c4,
      0x0838002c, 0x08381f1c, and 0x0838477c. For i in 0..nDb using the
      signed bgt loop guard, it probes entries in 1,0,2,3,... order; a
      non-NULL database filters with str_icmp(database, aDb[j].zName), then
      hash_find(aDb[j].pSchema + 0x18, name, strlen(name)+1) returns the first
      Index hit. The three direct helper callees are already ported: str_icmp
      @ 0x08384f14, strlen @ 0x08392478, and hash_find @ 0x0837ad88.
      Deliberate deviations: str_icmp and hash_find use module-local volatile
      hooks (defaulting to those real ports) so host fixtures observe their
      arguments without sharing mutable state with another port; the original
      direct BL instructions therefore become indirect calls. The raw u32
      target-pointer layout is preserved rather than using host-width fields.
  - address: 0x08379024
    ghidra: FUN_08379024
    name: find_table
    size: 148
    sig: "Table *find_table(sqlite3 *db, const char *name, const char *database)"
    module: sqlite/find_table
    status: ported
    vars: {param_1: db, param_2: name, param_3: database, uVar2: i, uVar3: j,
      iVar4: result}
    notes: >-
      SQLite 3.4.x/3.5.x util.c's sqlite3FindTable, identified by the
      (i<2) ? i^1 : i index swap (cmp #2 / movge / eorlt #1) that searches
      TEMP before MAIN, the str_icmp schema-name filter, and the
      strlen(zName)+1 key into hash_find. Ghidra's 148-byte extent is
      exact: the next function starts at 0x083790b8. 17 bl call sites,
      binary-scanned over every B/BL word in osos.dec, matching Ghidra's
      count; all unconditional, no predicated forms (0x082dc140,
      0x0836ec3c, 0x0836ef30, 0x0836f0f4, 0x0836f2bc, 0x08373f10,
      0x0837560c, 0x08375970, 0x0837b678, 0x0837d280, 0x0837fd10,
      0x0838017c, 0x08380534, 0x08381ee8, 0x0838474c, 0x0838a914,
      0x0838a9a8 - the parser/resolver half of the SQLite unit; the
      hash_find notes already list this function's own probe at
      0x08379094). Body: for i in 0..nDb (signed bgt guard, nDb reloaded
      per iteration), j = i<2 ? i^1 : i; skip aDb[j] when database is
      non-NULL and str_icmp(database, aDb[j].zName) != 0 (database is the
      LEFT argument); else hash_find(aDb[j].pSchema + 4, name,
      strlen(name)+1) - Schema.schema_cookie at +0x00, tblHash at +0x04,
      the layout hash_init/hash_clear pin - and return the first non-NULL
      hit. Db layout is schema_to_index's: 24-byte stride, zName @ +0x00,
      pSchema @ +0x14. All three callees were already ported (strlen @
      0x08392478, str_icmp @ 0x08384f14, hash_find @ 0x0837ad88).
      Deviations: str_icmp and hash_find go through this module's own
      volatile FIND_TABLE_HOOKS seam (defaults are the real ports), so
      the shipped image's indirect blx replaces two direct bls - a
      module-local seam, not hash_find's HASH_FIND_HOOKS, because
      concurrent test threads swapping one static would race; strlen is
      called directly (the strdup/set_string precedent) and LLVM inlines
      it into a byte loop. match.py structural: same loop shape, same
      i^1 swap (LLVM emits eorcc where ADS emits eorlt after cmp #2 -
      identical for the reachable i >= 0), same reloads of db->nDb/aDb,
      same +0x14/+4 addressing. Host tests (7): TEMP-before-MAIN probe
      order, full 1,0,2,3 scan order on miss, database filter argument
      order and skip behaviour, filter rejecting every schema (hash never
      probed), the real ported str_icmp folding "MAIN" onto "main",
      nDb==0 and nDb<0 finding nothing, and the default hooks resolving
      to the real ports. Fixture: u32-word slab via
      testing::hints::SQLITE_FIND_TABLE.

  # --- app: the demo mode's canned calendar record ---
  - address: 0x08284e4c
    ghidra: FUN_08284e4c
    name: demo_mode_datetime
    size: 44
    sig: "DateTime *demo_mode_datetime(DateTime *out)"
    module: app/demo_mode_datetime
    status: ported
    renames:
      param_1/r0/r4: out
      FUN_081883fc: demo_mode_instance
      FUN_0827216c: resource_chain_find
      DAT_08284e78: RESOURCE_ID_DEMO_MODE_DATETIME (0x000080aa)
      DAT_08284e7c: RESOURCE_KIND_DTTM (0x4474546d, "DtTm" big-endian)
      0x08037db0: __rt_memcpy (ROM thunk table entry 0)
    notes: >-
      Copies the demo mode's canned ten-byte calendar record into the
      caller's DateTime: demo_mode_instance() (TCDemoMode singleton,
      class 0x8080) -> resource_chain_find(head, "DtTm", 0x80aa) ->
      tail __rt_memcpy(out, record, 10) through thunk 0x08037db0 (ROM
      0x22000020 == osos 0x08000020, ported in libc/rt_memcpy.rs). The
      record is the time/datetime.rs ten-byte DateTime - pinned by the
      callers: FUN_082851d0 round-trips the buffer through
      datetime_to_unix_seconds 0x08093c38 / unix_seconds_to_datetime
      0x080964cc, and FUN_082ca58c fills two adjacent fields at +0x08
      and +0x12 (a 10-byte stride). functions.csv's 44 is the exact
      code size (eleven instructions, 0x08284e4c..0x08284e78); the
      two-word literal pool (id @ 0x08284e78, kind @ 0x08284e7c) makes
      the true extent 52 bytes, ending where the next function pushes
      {r0-r4,lr} @ 0x08284e80. 17 call sites, binary-scanned over
      every B/BL word in osos.dec: 16 bl plus one bleq @ 0x08203a1c
      that gates the fill of an object's field +0x1a on its flag byte
      +0x18 being clear - caller-side gating, no callee contract. No
      DATA word in the image holds 0x08284e4c, so it is never
      dispatched virtually. All three callees were already ported and
      are called directly, no seam. Deviations: none structural - the
      original has NO NULL guard (a missing singleton or unanswered
      entry copies ten bytes from address 0) and the port keeps that;
      the tail b propagates __rt_memcpy's r0, so the port returns out.
      match.py structural: same call/load order and tail branch; LLVM
      adds the usual {fp,lr} frame and forms 0x80aa with mov/orr
      instead of a second literal-pool load. Host tests (4): full
      ten-byte copy of a distinct fixture record with out returned,
      size_of::<DateTime>() == 10 (the mov r2,#10 literal), the find
      hitting the demo-mode object itself with exactly (kind
      0x4474546d, id 0x80aa), and the walk continuing past a declining
      first provider to a second node.

  - address: 0x0827c9c8
    ghidra: FUN_0827c9c8
    name: query_param_list_push
    size: 60
    sig: "void query_param_list_push(ParamList *list, u32 value)"
    module: app/query_param_list
    vars: {param_1: list, param_2: value}
    status: ported
    notes: >-
      Extent byte-verified: fifteen ARM words @ 0x0827c9c8..0x0827ca00,
      ending where the separately linked list initializer sibling pushes
      {r4, lr} @ 0x0827ca04; Ghidra's 60 bytes is correct. Binary scan
      of every B/BL word in osos.dec: exactly 17 call sites, all
      unconditional `bl`, and all seventeen live inside the 18-slot
      binder FUN_0827c8a8 @ 0x0827c8a8 (one per register/stack
      argument; the 18th push is inlined there verbatim). No predicated
      forms, no tail branches, no DATA-word references, so the function
      is never vtable-dispatched. The receiver is the media library SQL
      query layer's fixed 18-slot bound-parameter list (callers build
      statements such as "SELECT version ... FROM genius"; the
      initializer @ 0x0827ca04 chains string_default_construct @
      0x08277440 for the base sub-object, then fills all 18 slots with
      -1 and clears all 18 tag bytes). Layout: +0x00/+0x04 base
      sub-object, +0x08 count, +0x0c values u32[18], +0x54 tagged
      u8[18], +0x68 heap token (operator_delete @ 0x082aad24'd by the
      binder when it changes). Algorithm: value 0xffffffff is the
      slot-absent sentinel and returns with no store; otherwise a set
      bit31 sets tagged[count] = 1 and is stripped from the word stored
      at values[count]; count then increments. No NULL guard and no
      capacity guard - the firmware relies on the sole caller never
      pushing more than 18. All three observed binder invocations push
      small ints (0, 1, 2) and -1 sentinels, so the bit31 tag path is
      contract-only, not exercised by any caller in the image.
      Deviation: the stock code reloads count from +0x08 three times;
      the port reads it once (unobservable - nothing it touches can
      alias the receiver). match.py structural: 15 instructions on both
      sides with the same blocks (sentinel early-out, tag strip + flag
      byte store, value store, count increment); LLVM adds its usual
      {fp, lr} frame and tests bit31 as a signed bgt instead of
      predicated tst. Host tests (6): sentinel dropped with zero
      stores, plain value stored at count with count increment and
      later slots untouched, bit31 tag sets the flag byte and strips
      the bit, 0x80000000 is not the sentinel, 0x7fffffff stored
      unchanged, and sequential pushes including an interleaved
      sentinel fill slots in order with per-slot tags.

  - address: 0x081d66b8
    ghidra: FUN_081d66b8
    name: iap_incoming_process_thread_slot_wait
    size: 68
    sig: "u32 iap_incoming_process_thread_slot_wait(u8 *this, u32 index)"
    module: app/iap_incoming_process_thread
    status: ported
    renames:
      param_1: this (the 0x240-byte iAP incoming-process context, NOT
        the 0x44-byte CIapIncomingProcessThread wrapper - see the
        module header's correction)
      param_2: index (registration slot, 0..28)
      FUN_08030f44: heap_panic (direct, ported in heap/veneers)
      FUN_08257cbc: slot_object_condvar_timed_wait (unported; timed
        condition wait on the slot object's condvar at +0x10 with the
        {sec,nsec} deadline stored at its +0x1c, behind the
        IAP_THREAD_SLOT_WAIT_OPS seam)
    notes: >-
      68 bytes, 0x081d66b8..0x081d66fc, all code, no literal pool;
      Ghidra's 68 is exact (the next function opens at 0x081d66fc with
      `push {r4,r5,r6,r7,r8,lr}` - a same-shape sibling that calls the
      zero-timeout poll veneer 0x08257cb4). Byte-decoded from osos.dec.
      Call count binary-verified by decoding every B/BL word in
      osos.dec: 17 unconditional `bl`, zero predicated forms, and no
      DATA word anywhere references the address - only ever
      direct-called, never virtual. Sites: 0x081395e0, 0x081642a4,
      0x081649f8, 0x08190404, 0x08190b4c, 0x081962dc, 0x081a9664,
      0x081a96c8, 0x081aeccc, 0x081f1e24, 0x081f2340, 0x081f25b8,
      0x081f3058, 0x081f3524, 0x081fd6e8, 0x081fdef8, 0x08200fa4.
      The TIMED-WAIT sibling of iap_incoming_process_thread_slot_poll
      @ 0x081d7270 on the same 0x240-byte context: identical register
      frame, same `add r4,r0,#0x114` registry mutex (veneers
      0x08261e20/0x08261e24, resolved to the canonical ported
      posix_mutex_lock/unlock), same `cmp r6,#29; bcs heap_panic`
      bound, same `add r0,r5,r6,lsl #3; ldr r0,[r0,#0x154]` slot
      resolution with NULL fatal via heap_panic WITH THE MUTEX HELD
      (0x08030f44, non-returning), same `mov r0,r4; pop; b unlock`
      tail so the return IS the unlock status. The ONLY difference is
      the callee: 0x08257cbc is a 4-instruction veneer
      (`add r1,r0,#0x1c; add r0,r0,#0x10; b 0x8261f28`) running a
      TIMED condition wait on the slot object's condvar at +0x10 with
      the {sec,nsec} deadline stored at the object's +0x1c..+0x23
      (zeroed by its ctor @ 0x08257cc8: str at +0x1c and +0x20);
      0x8261f28 stacks that pair and chains 0x8261f94 -> the posix
      timed-wait body @ 0x0826269c (fp/fp_misc cond_wait_attr_clock
      notes). The wait status is discarded. Callers pair it with the
      poll: @ 0x081f1e18 the session runs instance-veneer ->
      `ldr r1,[r5,#0x2c]` -> slot_poll, then repeats getter+index and
      calls this function - drain a pending signal, then block until
      the slot's deadline.
      Deviations - 0x08257cbc is unported and rides the
      IAP_THREAD_SLOT_WAIT_OPS seam: target default transmutes the ROM
      address (hook-ready), host default is inert and tests install a
      recording model; the fatal paths are not host-tested
      (heap_panic is `-> !`, the pending_event_take precedent).
      Host tests (5, app::iap_incoming_process_thread::slot_wait_tests,
      low-slab fixture at hints::IAP_THREAD_SLOT_WAIT): the wait runs
      under the held mutex with the slot's +0 pointer verbatim and the
      unlock status 0 returned; edge indices 0 and 28 both resolve;
      the index strides by 8 and the slot's +4 context word is never
      read (poison-pinned); the unlock status is forwarded as the
      return value (mock drops the owner word, the function's own
      unlock then reports 0x05 non-owner, surfaced verbatim); and the
      symbol is address-distinct from the poll sibling (LLVM must not
      fold the near-identical bodies).
      match.py structural (exit 1, diff expected): same lock /
      bounds-check / lsl-#3 + 0x154 slot load / NULL-check / wait /
      mov r0,r4 / tail-unlock skeleton as the original; deltas are the
      crate fp frame, `cmp #28/bhi` for `cmp #29/bcs`, a predicated
      `bleq` heap_panic, and the seam's literal-load + blx for the
      unported wait callee - the same four deltas the ported poll
      sibling records.

  - address: 0x081b36e0
    ghidra: FUN_081b36e0
    name: string_view_array_construct
    size: 176
    sig: "StringViewArray *string_view_array_construct(StringViewArray *this, ResourceProvider *resources, void *controller, void *parent, const StringViewArraySpec *spec)"
    module: ui/string_view_array
    status: ported
    renames:
      param_1: this
      param_2: resources
      param_3: controller
      param_4: parent
      in_stack_00000000: spec
      DAT_081b3788: STRING_VIEW_ARRAY_VTABLE_ADDRESS
      DAT_081b378c: ARRAY_ELEMENT_CONSTRUCTOR_ADDRESS
      FUN_08291c24: string_view_construct (direct, ported in ui/string_view)
      FUN_08277440: string_default_construct (direct, ported in cxx/string_object)
      FUN_082ab234: cpp_array_construct (direct, ported in runtime/cpp_array_construct)
      FUN_0827639c: string_object_assign_cstr (direct, ported in cxx/string_object)
      FUN_0811108c: timer_schedule_shim (direct, ported in drivers/timer)
    notes: >-
      Raw ARM establishes a 176-byte extent: 168 bytes of code from
      0x081b36e0 through the return at 0x081b3784, followed by its two
      literal-pool words at 0x081b3788 and 0x081b378c; the separately
      linked next function begins at 0x081b3790. Decoding every B/BL word
      in osos.dec finds exactly 17 direct call sites, all unconditional
      plain bl, with zero predicated BL forms and no B tail callers. The
      five-argument derived StringView constructor forwards its register
      arguments and stacked spec to string_view_construct, overwrites the
      base vtable with 0x0898b884, default-constructs then clears the
      trailing StringObject at +0x220, and asks cpp_array_construct to
      initialize five 0x6c-byte elements at +0x248. It copies spec+0x6c,
      +0x68, +0x74 and +0x78 into +0x22c, +0x230, +0x238 and +0x23c,
      stores saturating unsigned spec+0x70 minus spec+0x78 at +0x234,
      clears +0x228 and +0x244, then builds a timer at +0x468 with this
      as config word. The raw constructor word 0x081a87b0 is not a decoded
      function entry and remains deliberately unnamed. Ghidra's no-argument
      int signature drops all inputs and the pointer return. The host port
      plants the derived StringObject's two target-width words directly,
      because its required host alignment conflicts with the embedded base
      StringView strings; ARM calls string_default_construct directly. Host
      test records the base constructor, nested array construction, resource
      resolve, both timers, empty-string clear, derived array ABI, returned
      this, copied words, the underflow clamp, and untouched padding.

  - address: 0x0814d230
    ghidra: FUN_0814d230
    name: mov_atom_node_get_offset
    size: 16
    sig: "u64 mov_atom_node_get_offset(const MovAtomNode *node)"
    module: mov/atom_node
    status: ported
    renames:
      param_1/r0: node
      param_1_plus_0x10: offset_lo
      param_1_plus_0x14: offset_hi
    notes: >-
      Raw ARM establishes the exact 16-byte extent
      0x0814d230..0x0814d240: mov r1,r0; ldr r1,[r1,#20];
      ldr r0,[r0,#16]; bx lr. The separately linked next function begins
      at 0x0814d240. Decoding every ARM B/BL word in osos.dec finds 11
      direct call sites, all unconditional plain bl: 0x08121144,
      0x08121174, 0x081c21a8, 0x081c3f50, 0x081c7e38, 0x081c807c,
      0x081e6014, 0x081e6054, 0x081f3ab4, 0x0820c71c, 0x0820c77c;
      zero predicated BL forms and no aligned DATA word reference this
      address. Returns the MOV atom node's payload offset as the low
      word at +0x10 and high word at +0x14, loaded high then low and
      returned in AAPCS r0:r1. No NULL guard: stock faults on the first
      load. No deliberate deviations: Rust performs the two volatile
      target-width word reads in the same order. mov_atom_info now calls
      this direct port rather than its former ROM dispatch seam. Host
      tests cover zero, all-ones, and distinct high/low halves while
      proving every node field is preserved.

  - address: 0x0814d21c
    ghidra: FUN_0814d21c
    name: mov_atom_node_set_child_b_if_present
    size: 20
    sig: "u32 mov_atom_node_set_child_b_if_present(MovAtomNode *node, MovAtomNode *child_b)"
    module: mov/atom_node
    status: ported
    renames:
      param_1/r0: node
      param_2/r1: child_b
      param_1_plus_0x04: child_b_link
    notes: >-
      Raw ARM establishes the exact 20-byte extent
      0x0814d21c..0x0814d230: cmp r1,#0; strne r1,[r0,#4];
      moveq r0,#1; movne r0,#0; bx lr. The separately linked next
      function begins at 0x0814d230. Decoding every ARM B/BL word in
      osos.dec finds 13 direct call sites, all unconditional plain bl:
      0x081c1dd8, 0x081c1de4, 0x081c1df8, 0x081c1e04, 0x081c1e6c,
      0x081c7db8, 0x081c7ddc, 0x081c7de8, 0x081e5fd8, and
      0x0820c6c0..0x0820c6e4; there are zero predicated BL forms and
      no tail branches. No aligned data word in osos.dec holds
      0x0814d21c. The leaf stores the supplied non-NULL node pointer
      in child-B (+0x04) and returns 0; a NULL child returns 1 without
      dereferencing node, so even a NULL node is harmless on that path.
      This is the companion of child-A setter 0x0814d28c.
      No deliberate deviations: the Rust export preserves the target-width
      pointer word, conditional store, and u32 return. Host tests prove
      the non-NULL store, exact preservation of every other field, and the
      no-dereference NULL-child path.

  - address: 0x0814d28c
    ghidra: FUN_0814d28c
    name: mov_atom_node_set_child_a_if_present
    size: 20
    sig: "u32 mov_atom_node_set_child_a_if_present(MovAtomNode *node, MovAtomNode *child_a)"
    module: mov/atom_node
    status: ported
    renames:
      param_1/r0: node
      param_2/r1: child_a
      param_1_plus_0x00: child_a_link
    notes: >-
      Raw ARM establishes the exact 20-byte extent
      0x0814d28c..0x0814d2a0: cmp r1,#0; strne r1,[r0];
      moveq r0,#1; movne r0,#0; bx lr. The separately linked next
      function begins at 0x0814d2a0. Decoding every ARM B/BL word in
      osos.dec finds 9 direct call sites, all unconditional plain bl:
      0x081c1dec, 0x081c1e10, 0x081c1e1c, 0x081c1e28, 0x081c1e34,
      0x081c1e40, 0x081c1e4c, 0x081c7dc4, and 0x081c7dd0; there are
      zero predicated BL forms and no tail branches. No aligned data
      word in osos.dec holds 0x0814d28c. The leaf stores the supplied
      non-NULL node pointer in child-A (+0x00) and returns 0; a NULL
      child returns 1 without dereferencing node, so even a NULL node is
      harmless on that path. This is the child-A counterpart to the
      child-B setter at 0x0814d21c. No deliberate deviations: the Rust
      export preserves the target-width pointer word, conditional store,
      and u32 return. Host tests prove the non-NULL store, exact
      preservation of every other field, and the no-dereference
      NULL-child path.

  - address: 0x0814d2e0
    ghidra: FUN_0814d2e0
    name: mov_atom_node_clear_links
    size: 20
    sig: "MovAtomNode *mov_atom_node_clear_links(MovAtomNode *node)"
    module: mov/atom_node
    status: ported
    renames:
      param_1/r0: node
      param_1_plus_0x00: child_a
      param_1_plus_0x04: child_b
      param_1_plus_0x08: dup_chain
    notes: >-
      Raw ARM establishes the exact 20-byte extent
      0x0814d2e0..0x0814d2f4: mov r1,#0; str r1,[r0,#8];
      str r1,[r0]; str r1,[r0,#4]; bx lr. The separately linked next
      function begins at 0x0814d2f4. Decoding every ARM B/BL word in
      osos.dec finds 17 direct call sites, all unconditional plain bl:
      0x080c60a8, 0x08121168, 0x08121194, 0x081e6038, 0x081e6040,
      0x081e6074, 0x081e607c, 0x0820c740..0x0820c760, and
      0x0820c7bc..0x0820c7dc; there are zero predicated BL forms.
      The leaf clears only a MOV atom node's duplicate-fourcc chain,
      child-A, and child-B link fields in that store order, preserving
      offset, size, kind, flag, and fourcc. The low-level code leaves r0
      unchanged on return, despite Ghidra inferring void; the Rust export
      returns node to preserve this ABI-visible result used by
      FUN_080c6080 after its recursive child teardown. No null guard:
      stock faults on the first store. No aligned data word in osos.dec
      holds 0x0814d2e0. Host test poisons every field, proves all three
      links clear, all other fields survive, and the input pointer returns.

  - address: 0x0820c8a0
    ghidra: FUN_0820c8a0
    name: mov_chain_table_next
    size: 56
    sig: "i32 mov_chain_table_next(const MovChainTable *table, u32 index, u32 *out)"
    module: mov/chain_table
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: index
      param_3/r2: out
    notes: >-
      Raw ARM establishes the exact 56-byte extent 0x0820c8a0..0x0820c8d8
      (14 instructions, no literal pool): the separately linked sibling
      setter opens at 0x0820c8d8 with cmp r1,#128 / addcc, so Ghidra's
      56 is exact for once. Decoding every ARM B/BL word in osos.dec
      finds 16 direct call sites, all unconditional plain bl
      (0x081e39cc, 0x081e3b28, 0x081e4004, 0x081e4ad0, 0x081e4c04,
      0x081e4f50, 0x081e4fbc, 0x081e5358, 0x0820c838, 0x0820c928,
      0x0820ca10, 0x0820cb54, 0x0829e3e8, 0x0829e458, 0x0829e468,
      0x0829e480), zero predicated forms, zero tail branches; no DATA
      word references the address, so it is never dispatched virtually.
      The zero predicated count is behavioural: no caller NULL-guards the
      table or pre-checks the index, every caller relies on this
      function's own bounds check.
      The function is the next-link accessor of a chained slot table
      embedded at the head of the MOV playback manager object: 128
      entries of 20 bytes (mutex at this+0xa00 = exactly 128*20, lock
      service at +0xa08; outer callers reach the table via
      *(obj+0x1060)). Entry word +0x0c is the next-slot link: < 0x80
      continues the chain, 0xffffffff ends it, anything else is corrupt.
      The chain walkers @ 0x0820c8f4 and 0x0820c9ec feed each returned
      link back in as the next index, making both sentinels observable
      contract; the predicated twin @ 0x0820c8d8 (strcc r2,[r0,#12]) is
      the matching setter, and the scan @ 0x0820c86c finds entries by
      the tag byte at +0x08. The atom-path string
      moov/udta/meta/ilst sits at 0x0820c7e8 directly above the cluster.
      Observable subtleties preserved: the *out store is UNCONDITIONAL
      (str r0,[r2] precedes the validity branches), so a corrupt link is
      still reported through *out alongside the error return, while an
      out-of-range index skips the store entirely. No NULL guards, no
      other deviations. Return codes are bare 0/2 as in stock.
      match.py structural: LLVM emits an extra push {fp,lr} frame and
      folds the two validity tests (next < 128 || next == 0xffffffff)
      into one unsigned range check (next+1 <u 129), which is provably
      equivalent; block order, the *5*4 entry addressing, the +0x0c
      load, and the store-before-check sequence all match. Own
      link_section guards against identical-code folding (none
      occurred; symbol resolves in librustypod.a).
      Host tests (6): valid link read, all-entries distinct-word field
      and index selection plus table immutability, the 0xffffffff end
      marker succeeding verbatim at slots 0 and 127, the 0x7f/0x80
      link boundary with the corrupt values still stored on the error
      path, out-of-range indexes (128, 129, 0x80000000, 0xffffffff)
      storing nothing, and a full 10->20->30->40->end chain walk as
      the 0x0820c9ec caller performs it.

  - address: 0x0835206c
    ghidra: FUN_0835206c
    name: xor_f6_in_place
    size: 48
    sig: "void xor_f6_in_place(u8 *bytes, i32 len)"
    module: crypto/xor_f6_in_place
    status: ported
    renames:
      param_1/r0: bytes
      param_2/r1: len
      iVar1/r2: index
    notes: >-
      Raw ARM establishes the exact 48-byte extent
      0x0835206c..0x0835209c: the next separately entered function begins
      at 0x0835209c. It starts index at zero; while signed index < signed
      len, it loads bytes[index], XORs it with 0xf6, stores it back, and
      increments index. Non-positive lengths perform no dereference; for a
      positive length there is no NULL guard. Decoding every ARM B/BL word
      in osos.dec finds exactly 16 direct call sites, all unconditional
      plain bl instructions at 0x0804b264..0x0804b31c; there are zero
      predicated BL forms and no tail branches. Those sixteen calls decode
      consecutive 0x100-byte pages of a lookup table before
      FUN_0804b220 indexes it. Deliberate deviations: none. Host tests
      cover lengths 0..64 at byte alignments 0..3, boundary preservation,
      and zero/negative lengths including a NULL pointer. match.py
      structural review (exit 1 expected) finds the same signed
      non-positive early return and bytewise XOR-F6 loop; LLVM differs only
      by counting down len and post-incrementing the pointer.

  - address: 0x082045ac
    ghidra: FUN_082045ac
    name: screen_base_construct
    size: 128
    sig: "ScreenBase *screen_base_construct(ScreenBase *storage, u32 initial_target, u32 create_link, u32 resource_id)"
    module: app/screen_base
    status: ported
    renames:
      param_1/r0: storage
      param_2/r1: initial_target
      param_3/r2: create_link
      param_4/r3: resource_id
      FUN_0812468c: ScreenBaseOps::construct_base (seam, unported base-ctor chain)
      FUN_0827233c: task_ctx_field_0x30 (direct, ported)
      FUN_082722a0: ScreenBaseOps::set_next_link (seam, unported refcounted link setter)
      FUN_081d2204: framework_root_instance (direct, ported)
      DAT_08204624: SCREEN_BASE_VTABLE_ADDRESS (0x089917d4)
      DAT_08204628: ROOT_RESOURCE_ID (0x0dad0167)
    notes: >-
      16 bl + 0 predicated bl + 0 b call sites, binary-scanned by decoding
      every B/BL word in osos.dec (matches Ghidra's 16). True extent is
      128 bytes: Ghidra's 120 bytes of code plus the two literal-pool
      words @ 0x08204624 (vtable 0x089917d4) and 0x08204628 (id
      0x0dad0167); the next separately linked function - the class's
      deleting destructor - begins at 0x0820462c, confirmed from raw
      bytes. The shared base constructor of the Silver screen classes:
      every caller does `mov r2,#1; bl 0x082045ac` then re-plants its own
      vtable over +0x00/+0x1c (the derived-ctor idiom); caller
      0x081d25ac builds the framework root object itself (it ends by
      storing the object into the framework_root_instance holder @
      0x089cc858) and passes r3 = 0x0dad0167, and caller 0x0815e934
      registers the TCSportTimer* screen classes. Algorithm: this =
      <base chain @ 0x0812468c>(storage, initial_target, create_link) -
      the chain bottoms out in the ported framework_base_construct @
      0x081110d0 and registers the object at task-ctx +0x34; this is the
      base ctor's RETURN (mov r4,r0), not the incoming storage. Plant
      vtable 0x089917d4 at +0x00 and primary+0xdc at +0x1c (the
      secondary virtual-base vtable 0x089918b0: 17 words of
      pure-virtual thunks 0x082a0464). Store resource_id at +0x20, clear
      the owned sub-object at +0x24 (the dtor releases it through its
      vtable slot +0x04). Link into the current task's
      resource-provider chain: head = task_ctx_field_0x30() (task-ctx
      +0x30, cf. app/resource_chain.rs), installed at +0x14 through the
      refcounted setter @ 0x082722a0 (release old @ 0x08124dbc, retain
      new @ 0x08124af4). Then, only when initial_target == 0 AND the
      +0x20 value re-read from the object (ldr r0,[r4,#32], not the
      saved r6) is not 0x0dad0167, adopt the framework root instance as
      the target via vtable slot +0x2c (the "adopt this target" slot,
      same as framework_base_initialize's). 0x0dad0167 sits in
      retailOS's private 0x0dad0000..0x0dad0fff resource-id namespace
      (cf. app/registry.rs STOCK_KEY) and is passed only by the root's
      own constructor - the root cannot adopt itself, and its holder
      slot is stored only at the very end of that constructor. No NULL
      guards anywhere: NULL base-ctor return faults at the vtable store,
      no current task faults inside task_ctx_field_0x30, a pre-init NULL
      root is dispatched to slot +0x2c verbatim. Deviations: the vtable
      address is static image data, so firmware builds plant the
      original 0x089917d4 (the application_resource_provider precedent)
      and host builds plant a modeled static with a swappable slot +0x2c;
      the secondary store computes primary + size_of::<ScreenBaseVtable>
      which 32-bit layout asserts pin to exactly 0xdc (the original's
      add r0,r0,#220). The two unported direct callees sit behind
      SCREEN_BASE_OPS seams (the app/class_6800.rs precedent):
      construct_base's default stub returns storage verbatim (only the
      return-value contract is modeled), set_next_link's default stub
      models the store (`if old != new { next = new }`) and omits the
      retain/release calls. +0x20 is named resource_id from the single
      observed nonzero writer; its readers live in the class's unported
      methods. Host tests (8 in the module): explicit target skips
      adoption; zero target adopts the holder's root instance; a NULL
      root is still dispatched; the root id suppresses adoption;
      id+-1 and an out-of-namespace id still adopt; a relocating base
      ctor's return is honored (storage untouched); both default stubs'
      modeled behavior. match.py structural: identical store sequence
      (vtable, add #220 secondary, +0x20, +0x24), same cmp r5,#0 /
      re-read [r4,#32] / literal 0x0dad0167 gate, same vtable +0x2c
      re-load and blx; the diffs are the crate fp prologue and the two
      ldr/blx seam calls standing in for bl 0x0812468c / bl 0x082722a0,
      plus the inlined CURRENT_TASK_CTX_BLOCK slot for bl 0x0827233c -
      all documented deviations.
  - address: 0x08143990
    ghidra: FUN_08143990
    name: screen_base_parent_construct_with_next
    size: 20
    sig: "ScreenBase *screen_base_parent_construct_with_next(ScreenBase *storage, void *next_provider, u32 initial_target, u32 create_link)"
    module: app/screen_base
    status: ported
    renames:
      param_1/r0: storage
      param_2/r1: next_provider
      param_3/r2: initial_target
      param_4/r3: create_link
      FUN_08272438: ScreenBaseParentConstructWithNext (seam, unported base constructor)
      DAT_081439a4: SCREEN_BASE_PARENT_VTABLE_ADDRESS (0x08985da8)
    notes: >-
      Extent verified from raw ARM: exactly 20 bytes
      (0x08143990..0x081439a4), with four instruction words followed by
      literal vtable word 0x08985da8 at 0x081439a4; the sibling constructor
      begins at 0x081439a8. A complete osos.dec B/BL decode finds 16 direct
      inbound calls at 0x0811c818, 0x08133e30, 0x08159560, 0x0816f664,
      0x08177a94, 0x0819c56c, 0x0819e07c, 0x0819f930, 0x081a4f70,
      0x081cae40, 0x081cd5fc, 0x081e8f04, 0x081ee0dc, 0x081fa08c,
      0x08204188, and 0x0822c824; all are unconditional plain bl, with no
      predicated calls, tail branches, or DATA-word references. The
      four-argument intermediate parent constructor delegates to
      FUN_08272438(storage, next_provider, initial_target, create_link),
      accepts its r0 return as this, installs vtable 0x08985da8 at +0x00,
      then returns this. The delegated constructor passes r2/r3 to the
      framework base, clears the provider-chain +0x14 link, and installs r1
      through the refcounted next-link setter 0x082722a0. Its concrete class
      identity is not recoverable; the screen-base parent role is established
      by the 0x0812468c chain calling sibling overload 0x08143978 and
      overwriting its vtable with 0x08983020. Deliberate deviations: the
      unported direct callee is a fixed-address call on device and a volatile
      host seam; the host vtable is an opaque aligned marker because no slot
      identity is decoded. Host coverage verifies all four arguments, a
      relocating callee result, the vtable replacement, and preservation of
      every other modeled field. match.py structural review shows the same
      delegated call then literal vtable store; LLVM differs only by its fp
      frame and literal-loaded blx seam.


  # --- Block-backed list cursor current-index reader ---
  - address: 0x081f0354
    ghidra: FUN_081f0354
    name: list_cursor_index
    size: 24
    sig: "i32 list_cursor_index(const void *list)"
    module: cxx/list_cursor_index
    status: ported
    renames:
      param_1/r0: list
      "list+0x30": cursor (u32 target pointer)
      "cursor+0x2b4": current_index (signed, -1 = no position)
    notes: >-
      True raw extent is exactly 24 bytes, 0x081f0354..0x081f036c: `mov
      r1,r0; ldr r1,[r1,#0x30]; mvn r0,#0; cmp r1,#0; ldrne r0,[r1,#0x2b4];
      bx lr`. 0x081f036c opens the next separately linked function's push
      {r4,r5,r6,lr} prologue, confirming the extent. Decoding every ARM B/BL
      word in osos.dec finds exactly 16 direct inbound calls, all plain
      unconditional BL (0x0821aa00, 0x0821aa28, 0x0821add8, 0x0821b140,
      0x0821b15c, 0x0821b17c, 0x0821b51c, 0x0821b620, 0x0821b6b4,
      0x0821b7b0, 0x0821b7e4, 0x0821b83c, 0x0821b8a4, 0x0821b8bc,
      0x0821b904, 0x0821b91c); no predicated forms and no tail-branch
      transfers. Algorithm: read the cursor pointer at list+0x30; NULL
      returns the mvn r0,#0 sentinel (-1) with no further dereference,
      otherwise return the signed word at cursor+0x2b4. Field identities are
      sibling-grounded: the cursor's setter 0x081fcc64 treats +0x2b4 as a
      signed current index clamped against the count at +0x2b8, the element
      accessor 0x081f0700 forwards (cursor, index) to the cursor's indexed
      lookup 0x081fcab0, and the adjacent getter 0x081f0a84 reads +0x2b8
      with a 0 default. The list object is constructed by 0x081f13b8 with
      the cursor word born NULL; writers 0x081f07bc/0x081f0bb8/0x081f10c4/
      0x081f1430 attach it later. All 16 callers (the 0x0821a9e0 family of
      list widgets) consume the result as a signed position. Deviation: the
      result is typed i32, bit-identical to the original r0 including the
      0xFFFFFFFF sentinel. No dispatch seam: the function is a leaf. Host
      tests (4): NULL cursor returns -1 without touching the record region,
      index words 0/1/42/399/0x7fffffff come back verbatim, negative index
      words (0xffffffff/0xfffffffe/0x80000000) pass through ungated, and a
      detach/reattach transition re-reads the cursor word. match.py
      structural: same two loads (+0x30, +0x2b4), NULL guard, mvn sentinel;
      the diff is LLVM rotating the NULL path into a mvneq/bxeq early-out
      plus the crate fp frame — the accepted ADS-vs-LLVM noise. The symbol
      resolves standalone in librustypod.a (no LLVM body fold observed).

  # --- notifier-class notification dispatch (own +0x14 target) ---
  - address: 0x08167028
    ghidra: FUN_08167028
    name: own_target_notification_post
    size: 36
    sig: "void own_target_notification_post(OwnTargetNotifier *this, u32 message_code, const u8 *bytes, u32 byte_count, usize reply_queue)"
    module: app/own_target_notification_post
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: message_code
      param_3/r2: bytes
      param_4/r3: byte_count
      param_5/[sp]: reply_queue
      FUN_08166d3c: arena_message_post (unported, ARENA_MESSAGE_POST seam)
    notes: >-
      The notification dispatch of the notifier class constructed @
      0x081671f4 (vtable literal 0x08987f18, the class immediately after
      the UI animation class): all fifteen sibling methods @
      0x08166d14..0x081671e4 raise their 0x638000xx codes through this one
      forwarder. EXTENT: exactly 36 bytes, 0x08167028..0x0816704b - nine
      instruction words, no literal pool; the next function opens its own
      push {r2,r3,r4,lr} @ 0x0816704c, so Ghidra's 36 is right for once.
      CALL COUNT: binary-verified by decoding every B/BL word in osos.dec
      - exactly 16 sites, all plain unconditional bl, no predicated forms,
      no tail b, no DATA-word references. Ghidra's listing shows only 15:
      the site @ 0x08166ea0 is absent from decomp/osos.asm. ALGORITHM:
      pure ABI adapter into the shared arena-message poster FUN_08166d3c
      @ 0x08166d3c (unported, 76 bytes, fully decoded): push {r2,r3,r4,lr};
      ip=r2(bytes); r2=r3(byte_count); r3=[sp,#16](reply_queue);
      strd r2,[sp] (callee stack args byte_count/reply_queue);
      r2=[r0,#20] (this->target +0x14); r3=ip; bl; pop. The callee never
      reads its r0 - this passes through only because it occupies the
      register; it arena-allocates a 12-byte envelope (message_arena_pool
      + fixed_block_pool_alloc(..,0xc), both ported), builds it with
      queued_message_construct (ported), and posts fire-and-forget
      (no_wait=1, flags=0) through queued_message_post (ported). BEHAVIOR:
      no NULL guards anywhere - this is dereferenced unconditionally and a
      NULL target word is forwarded as-is to a chain that dereferences it
      before any test; the zero predicated-bl count matches (callers never
      guard). The stock pop returns with r0 holding queued_message_post's
      status even though the C type is void; callers ignore it, the port
      is typed void and leaves r0 as the seam call left it. DEVIATIONS:
      the unported callee rides the ARENA_MESSAGE_POST seam (transmuted
      firmware address on target, documenting panic on host), keeping the
      six-argument ABI including the unread this in r0; the notifier
      struct models the +0x14 target as a u32 wire word (PostedTaskMessage
      precedent) so the offset is exact on both pointer widths. Host tests
      (5): full six-word forwarding with the own-target inserted third,
      NULL target/bytes/reply forwarded unguarded, u32::MAX/usize::MAX
      extremes pass through, per-call re-read of the target word, and the
      +0x14 layout assertion. match.py structural: same argument shuffle
      (mov ip,r2 / ldr r2,[r0,#20] / two stack-arg stores / mov r3,ip /
      call); the diffs are the seam blx-indirection, stm-vs-strd stack
      stores, and the crate fp frame - accepted ADS-vs-LLVM noise. Symbol
      resolves standalone in librustypod.a under its own link_section (no
      LLVM body fold observed).

  - address: 0x080f10b8
    ghidra: FUN_080f10b8
    name: list_pop_front
    size: 52
    sig: "ListNode *list_pop_front(ListHead *list)"
    module: kernel/condvar
    status: ported
    renames:
      r0: list (then node/return value)
      r1: list (saved)
      r2: node->next, then list->tail
      r3: zero
    notes: >-
      Intrusive singly-linked list pop-front over a {head, tail} anchor
      pair: load head, return NULL when empty (r0 already holds the NULL
      head, bxeq lr); else head = head->next; when the popped node was
      also the tail (single-element list) BOTH anchor words are cleared
      (two streq on the tail==node compare); the popped node's next is
      always zeroed; returns the popped node. 16 bl call sites,
      binary-verified (every B/BL word in osos.dec decoded), ALL plain
      unconditional bl - zero predicated forms, so callers never guard
      the call (the callee's own empty-list early-out is the only
      guard). Ghidra's 52-byte extent is CORRECT here: the next function
      (list_remove @ 0x080f10ec) starts immediately after the trailing
      bx lr. Already ported as the exported `list_pop_front` in
      kernel/condvar.rs by the condvar-layer region port (0x0807f5cc
      entry); this record adds the per-address mapping, like the
      task_yield commit (9ac77aa) did. Doc-header size fixed 48 -> 52
      bytes. match.py structural: same instruction sequence modulo the
      crate fp frame (push/pop {fp,lr}) and LLVM materializing the zero
      in r2/r1 instead of the original's shared r3 - accepted ADS-vs-
      LLVM noise. Host coverage already in kernel::condvar::tests:
      list_pop_empty_returns_null, list_push_pop_fifo_order (FIFO order
      + next zeroed + anchors cleared), list_single_element_clears_
      both_anchors.

  - address: 0x080f1158
    ghidra: FUN_080f1158
    name: list_push_back
    size: 36
    sig: "void list_push_back(ListHead *list, ListNode *node)"
    module: kernel/condvar
    status: ported
    renames:
      r0: list
      r1: node
      r2: list->head, then list->tail
    notes: >-
      Intrusive singly-linked list push-back over a {head, tail} anchor
      pair: load list->head; when NULL (streq path) node becomes the head,
      else the store goes to list->tail->next (ldrne/strne pair); then
      list->tail = node unconditionally and node->next = 0. The trailing
      `mov r0,#0; str r0,[r1]; bx lr` uses r0 purely as a zero SCRATCH
      for the node->next store - NOT a return value; every one of the 15
      callers discards r0, so the port is void (deliberate deviation
      documented in the doc header). 15 bl call sites, binary-verified
      (every B/BL word in osos.dec decoded): 0x0807f6d4, 0x0809eb24,
      0x0809ec54, 0x0809ec74, 0x080ed99c, 0x080ed9f8, 0x08100210,
      0x0812be8c, 0x0812c1cc, 0x0816624c, 0x0819c014, 0x0839e528,
      0x0839e628, 0x0839e74c, 0x0839e7ac - ALL plain unconditional bl,
      zero predicated forms. Ghidra's 36-byte extent is CORRECT: the next
      function starts with push {r2-r4,lr} at 0x080f117c. Already ported
      as the exported `list_push_back` in kernel/condvar.rs by the
      condvar-layer region port (0x0807f5cc entry) and re-used by
      kernel/mqueue.rs; this record adds the per-address mapping, like
      the list_pop_front entry above (0x080f10b8). Doc header updated
      with the verified call count and the r0-scratch note. Host
      coverage already in kernel::condvar::tests:
      list_push_pop_fifo_order (empty-list head push, tail append across
      a 4-node chain, next zeroed on every node, head/tail anchors).
      match.py structural: same compare/branch/store sequence modulo the
      crate fp frame - accepted ADS-vs-LLVM noise.

  # --- Named attribute-record lookup (util/attr_record) ---
  - address: 0x080bfb8c
    ghidra: FUN_080bfb8c
    name: attr_record_lookup
    size: 72
    sig: "u8 *attr_record_lookup(const NamedAttributeTable *attribute_table, const u8 *attribute_name)"
    module: util/attr_record
    status: ported
    renames:
      param_1/r4: attribute_table
      param_2/r0: attribute_name
      attribute_table_plus_0x48/r1: nonzero_guard
      attribute_table_plus_0x50/r1: attribute_records
      attribute_table_plus_0x80/r1: global_state_table
      FUN_080781b0: global_state_get
      iVar1/r0: state_record
      state_record_plus_0x04/r0: attribute_record_index
    notes: >-
      EXTENT: raw osos.dec is exactly 72 bytes, 0x080bfb8c..0x080bfbd3
      (18 instruction words, no literal pool); the distinct next function
      starts with push {r4,lr} at 0x080bfbd4, confirming Ghidra's size. CALL
      COUNT: decoding every B/BL word in osos.dec found exactly 16 direct
      references: 0x0803c9d0, 0x0803ca1c, 0x0803cafc, 0x0803cb2c,
      0x0803cb60, 0x0803cb84, 0x0803cba0, 0x0803cd6c, 0x0803cd7c,
      0x080ae140, 0x080ae1b8, 0x080ae214, 0x080ae260, 0x080b674c,
      0x080befb4, and 0x080bf02c. Every reference is a plain unconditional
      bl; there are no predicated calls or tail b branches. ALGORITHM: return
      NULL unless attribute_table, its +0x48 guard, attribute_name, and
      attribute_name[0] are nonzero; call the already-ported
      global_state_get(attribute_name, attribute_table +0x80); return NULL
      for a NULL state record; otherwise return attribute_table +0x50 plus
      state_record[1] << 4. The +0x48 field's identity is unknown and is
      named only for its verified nonzero-guard role; the state-record index
      is intentionally unchecked and u32-wrapping. Host tests cover a
      low-u32 mapped object with exact name/table forwarding and record
      address, all four early-out conditions without calling the seam, a
      NULL returned state record, and u32::MAX index wrapping. The shared
      GLOBAL_STATE_SLOT_FIND test lock serializes this port and
      global_state_get's existing mock tests. match.py against this
      worktree's ARM archive reports the same field loads (+0x48, +0x80,
      +0x50), global_state_get call, state-record +0x4 load, and lsl #4
      address calculation; its extra frame and register moves are
      ADS-versus-LLVM codegen differences. Deliberate deviations: none.

  # --- Opaque typed-allocation release ---
  - address: 0x08060460
    ghidra: FUN_08060460
    name: release_typed_allocation
    size: 12
    sig: "void release_typed_allocation(void *allocation)"
    module: cxx/typed_allocation_release
    status: ported
    renames:
      param_1/r0: allocation
      DAT_08060468/r1: opaque_allocation_descriptor_0x0890b608
      FUN_0803b3a4: typed_allocation_release_helper
      FUN_080c85bc: type_erased_release_engine
    notes: >-
      EXTENT: raw osos.dec is 12 bytes, not Ghidra's reported 8:
      ldr r1,[pc]; b 0x0803b3a4; literal 0x0890b608. The next separate
      function begins at 0x0806046c (ldr r0,[r0,#12]), proving the
      trailing literal belongs here. CALL COUNT: decoding every ARM B/BL
      word in osos.dec found 16 direct call sites: 6 unconditional bl and
      10 blne, with no direct b sites. The predominance of caller-predicated
      calls is intentional: this wrapper has no NULL guard and forwards even
      NULL into 0x0803b3a4. ALGORITHM: load the fixed opaque descriptor in
      r1 and tail-branch to 0x0803b3a4; that helper makes a temporary
      {allocation, descriptor} frame and calls the unported type-erased
      release engine 0x080c85bc with r2=0. The sibling allocator wrapper
      0x080604ac supplies the same descriptor to 0x0803b468, but the
      descriptor's concrete type is not recoverable, so no identity was
      invented. No aligned data word in osos.dec equals 0x08060460, ruling
      out virtual dispatch. ARM global assembly preserves the three raw
      words and the tail return edge exactly. Host tests install a recording
      seam and prove both NULL (no accidental guard) and non-NULL allocation
      forwarding with the exact descriptor. match.py reviewed.

  # --- Opaque allocation release ---
  - address: 0x08070c04
    ghidra: FUN_08070c04
    name: release_opaque_allocation
    size: 12
    sig: "void release_opaque_allocation(void *allocation)"
    module: cxx/opaque_allocation_release
    status: ported
    renames:
      param_1/r0: allocation
      DAT_08070c0c/r1: opaque_allocation_descriptor_0x089063e8
      FUN_0803b3a4: typed_allocation_release_helper
      FUN_080c85bc: type_erased_release_engine
    notes: >-
      EXTENT: raw osos.dec is 12 bytes, not Ghidra's reported 8:
      ldr r1,[pc]; b 0x0803b3a4; literal 0x089063e8. The next separate
      function begins at 0x08070c10 (ldr r0,[r0]), proving the trailing
      literal belongs here. CALL COUNT: decoding every ARM B/BL word in
      osos.dec found 12 direct references: 11 bl calls (6 unconditional bl,
      2 bleq, and 3 blne) and one beq tail transfer at 0x0806fb60. The five
      predicated bl calls establish their own gates; this wrapper has no NULL
      guard and forwards NULL into 0x0803b3a4. ALGORITHM: load the fixed
      opaque descriptor in r1 and tail-branch to 0x0803b3a4; that helper
      makes a temporary {allocation, descriptor} frame and calls the
      unported type-erased release engine 0x080c85bc with r2=0. The
      descriptor's concrete type is not recoverable, so no identity was
      invented. ARM global assembly preserves the three raw words and tail
      return edge exactly. Host tests share the existing helper seam and
      prove NULL and non-NULL allocation forwarding with the exact
      descriptor. match.py reviewed. Deliberate deviations: none.

  # --- opaque object packed two-bit field accessor ---
  - address: 0x08054468
    ghidra: FUN_08054468
    name: object_packed_field
    size: 24
    sig: "u32 object_packed_field(const void *object, const void *selector)"
    module: ui/object_packed_field
    status: ported
    renames:
      param_1/r0: object
      param_2/r1: selector
      object_plus_0x1c/r0: packed_fields
      selector_plus_0x01/r1: packed_field_index
      uVar1/r0: packed_field
    notes: >-
      EXTENT: raw osos.dec is exactly 24 bytes at 0x08054468..0x0805447c;
      the next separately linked function begins at 0x08054480. CALL COUNT:
      decoding every ARM B/BL word in osos.dec finds 16 direct call sites:
      0x080616ac, 0x080616cc, 0x080e28dc, 0x080e2954, 0x080e29bc,
      0x080e2a18, 0x080e2a74, 0x080e2ad0, 0x080e2b00, 0x080e2b2c,
      0x080e2b80, 0x080e2be8, 0x080e2c2c, 0x080e2c6c, 0x080e2cc8, and
      0x080e2d08. Every call is a plain unconditional bl; there are no
      predicated calls or tail b branches. No aligned DATA word in osos.dec
      contains this address. ALGORITHM: load the byte at object+0x1c, shift it
      right by ((selector[1] & 0x7f) << 1), and mask the low two bits. The
      four low selector values choose the byte's four packed fields; larger
      masked values shift at least eight positions and return zero. The
      selector high bit is ignored by ARM's register-shift count after lsl #1.
      The object and selector identities are unidentified, so names describe
      only verified roles. The ARM leaf has no NULL or alignment guard.
      Exhaustive host tests cover every packed byte and every selector byte,
      including high-bit aliasing and out-of-range zero results. Deliberate
      deviations: none.

  # --- Lazy Tdat payload allocator ---
  - address: 0x0803c328
    ghidra: FUN_0803c328
    name: ensure_tdat_payload
    size: 104
    sig: "u32 ensure_tdat_payload(TdatPayloadOwner *owner)"
    module: ui/tdat_payload
    status: ported
    vars:
      param_1/r0: owner
      param_1[0]: element
      param_1[4]: payload
      element_plus_0x48: pool
    notes: >-
      Raw ARM extent is exactly 104 bytes: push {r4,lr} begins at
      0x0803c328 and the next independently linked function begins with
      push {r4-r9,lr} at 0x0803c390. The function returns 1 immediately when
      payload[0] has bit 0 set. Otherwise it calls the ported
      ui_element_is_tdat_class at 0x0806aa3c on owner->element. A matching
      'tdat' element supplies its pool descriptor from +0x48 to unported
      FUN_0805de1c; its result replaces owner->payload, gets byte 0 set to
      1, and returns success only if non-NULL. Allocation failure stores and
      returns 0; a non-'tdat' element leaves the old payload untouched. Call
      count verified by decoding every ARM B/BL word in osos.dec: 16 direct
      calls, all unconditional bl and zero predicated forms. Deliberate
      deviation: dispatches through TDAT_PAYLOAD_OPS to the still-unported
      0x0805de1c pool allocator, while directly calling the already-ported
      class predicate; target default calls the original allocator.

  # --- Four-word owned buffer destructor ---
  - address: 0x0803a2ec
    ghidra: FUN_0803a2ec
    name: owned_buffer_destroy
    size: 36
    sig: "void owned_buffer_destroy(OwnedBuffer *owner)"
    module: heap/owned_buffer
    status: ported
    renames:
      param_1/r0/r4: owner
      owner_plus_0x08/r0: data
      FUN_08043994: traced_free
    notes: >-
      Raw ARM extent is exactly 36 bytes, 0x0803a2ec..0x0803a310: the next
      independently linked function starts with push {r4-r9,lr} at
      0x0803a310, with no literal pool. Decoding every ARM B/BL immediate in
      osos.dec finds exactly 16 direct BL-form inbound calls: 15 unconditional
      bl at 0x0803a2e4, 0x0803b6ac, 0x0805f72c, 0x0805fbf0, 0x08075170,
      0x0807898c, 0x080889d8, 0x080a96bc, 0x080b22d0, 0x080b2560,
      0x080b2568, 0x082b4f7c, 0x082be134, 0x082be218, and 0x083684f0; plus
      blne at 0x08040ca0. The separate tail b at 0x08091334 is not a call.
      The predicated caller proves callers sometimes gate destruction
      themselves; the callee still NULL-guards owner. For non-NULL owner,
      ldr r0,[owner,#8] conditionally frees the data allocation through the
      already ported traced_free, then moves owner into r0 and tail-branches
      to traced_free. Ghidra's C misses that final owner release. Deliberate
      deviation: Rust returns after an ordinary second traced_free call rather
      than tail-branching; traced_free returns normally, so observable calls
      and order are unchanged. Host tests prove NULL owner makes no allocator
      call, NULL data releases only owner, and non-NULL data releases data
      before owner.

  # --- Four-word owned buffer assignment ---
  - address: 0x0803a3cc
    ghidra: FUN_0803a3cc
    name: owned_buffer_assign
    size: 188
    sig: "u32 owned_buffer_assign(OwnedBuffer *owner, const u8 *source, i32 requested_length)"
    module: heap/owned_buffer
    status: ported
    renames:
      param_1/r0/r4: owner
      param_2/r1/r6: source
      param_3/r2/r5: requested_length
      owner_plus_0x00/r0: length
      owner_plus_0x08/r0/r7: data
      FUN_08392478: strlen
      FUN_08043c18: traced_alloc
      FUN_08043f3c: traced_realloc
      FUN_08037db0: __rt_memcpy
    notes: >-
      Raw ARM extent is exactly 188 bytes, 0x0803a3cc..0x0803a488; the next
      separately linked function begins with push {r3,r4,r5,lr} at
      0x0803a488. Decoding every ARM B/BL immediate in osos.dec finds 12
      inbound BL-form calls, all unconditional: 0x0803a2c8, 0x0805fd68,
      0x0805fdf4, 0x08060254, 0x08065ad0, 0x08078964, 0x0808a248,
      0x0808a974, 0x080a96ac, 0x082b4f54, 0x08396bf4, and 0x08396d50.
      There are no predicated forms; a separate unconditional tail b at
      0x0803a258 is thunk_FUN_0803a3cc. Negative requested_length returns 0
      for NULL source or measures a NUL-terminated source through strlen.
      Signed length < requested_length or NULL data requests requested_length
      + 1 bytes, wrapped, from traced_alloc or traced_realloc. Failure restores
      the old data word and leaves length unchanged. Success stores the length;
      non-NULL source is copied through __rt_memcpy and receives a NUL at
      data[length], while NULL source deliberately skips both writes. The
      signed compare makes a high-bit length force a grow even for requested
      length zero. traced_realloc FUN_08043f3c is now called directly,
      preserving its signed positive-size gate and its underlying
      allocation-failure result. Host tests cover NUL-measured
      allocation/copy, negative NULL rejection, realloc failure
      preservation, signed high-bit grow, and NULL-source no-terminator
      behavior.

  # --- I2C slave 0x39 one-byte register read ---
  - address: 0x0836e36c
    ghidra: FUN_0836e36c
    name: i2c_0x39_read_register
    size: 92
    sig: "i32 i2c_0x39_read_register(u32 reg, u8 *out)"
    module: drivers/i2c
    status: ported
    renames:
      param_1/r0: reg
      param_2/r1/r5: out
      local_1c/sp+4: register
      local_20/sp: value
      FUN_0806a4a0: kernel_sem5_wait
      FUN_0836bb84: i2c_write
      FUN_0836b950: i2c_read
      FUN_080645a8: kernel_sem5_signal
    notes: >-
      Raw ARM extent is exactly 92 bytes, 0x0836e36c..0x0836e3c8: push
      {r2-r8,lr}; kernel_sem5_wait(); i2c_write(0x39,1,&reg); on zero status
      i2c_read(0x39,1,out); kernel_sem5_signal(); return status. The next
      independently linked function begins at 0x0836e3c8, with no literal
      pool. The one-byte write transfers only the low byte of reg. The I2C
      device at slave address 0x39 has no verified identity in the retail
      image, so the name deliberately describes the wire-visible operation.
      Decoding every ARM B/BL immediate in osos.dec finds 15 direct calls,
      all unconditional bl and zero predicated forms: 0x08084564, 0x08084570,
      0x080845e4, 0x080846ac, 0x080846d8, 0x08084b94, 0x08084ba0,
      0x080f4bf0, 0x080f4bfc, 0x080f4c10, 0x0836e1fc, 0x0836e21c,
      0x0836e248, 0x0836e358, and 0x08393610. No deliberate semantic
      deviation: the existing kernel semaphore ports preserve the unconditional
      lock-release order; the already-established raw I2C target/host seam
      uses indirect blx calls in place of retail direct bl calls.

  # --- Encoded word block signed-shift dispatcher ---
  - address: 0x08342d70
    ghidra: FUN_08342d70
    name: encoded_word_block_shift
    size: 52
    sig: "u32 encoded_word_block_shift(EncodedWordBlock *block, i32 signed_distance)"
    module: util/encoded_word_block_shift
    status: ported
    renames:
      param_1/r0: block
      param_2/r1: signed_distance
      iVar1/r0: magnitude
      FUN_08322c48: encoded_word_block_shift_right
      FUN_08341910: encoded_word_block_shift_left
    notes: >-
      Raw ARM confirms the exact 52-byte extent 0x08342d70..0x08342da4:
      `movs r2,r1` tests signed_distance, zero sets r0 to 0 and returns
      without dereferencing block, a non-negative distance tail-branches to
      0x08322c48 with r0 unchanged, and a negative distance uses `rsb r0,r2,#0`
      before tail-branching to 0x08341910. The independently linked next
      function starts with `push {r0,r4-r11,lr}` at 0x08342da4, so there is no
      literal pool. Disassembly of both branch targets identifies 0x08322c48
      as the encoded-word-block right shift and 0x08341910 as its left-shift
      sibling; neither target has an existing port ledger entry. Decoding every
      ARM B/BL immediate in osos.dec finds exactly 15 direct inbound calls, all
      unconditional bl (0 predicated), and no tail-b callers; the address
      occurs in no aligned image word, so it is not a known virtual-dispatch
      target. i32::MIN retains the ARM wrapping-negation magnitude
      0x80000000. Deliberate deviation: none on target; release codegen
      restores its frame and tail-branches through the selected fixed entry.
      Host builds replace the address with a volatile callback seam.

  - address: 0x0826d834
    ghidra: FUN_0826d834
    name: view_base_set_word_44
    size: 24
    sig: "void view_base_set_word_44(ViewBase *view, u32 word_44)"
    module: ui/view_base
    status: ported
    renames:
      param_1/r0: view
      param_2/r1: word_44
      DAT_vtable_plus_d4/r1: view vtable slot +0xd4 dynamic callback
    notes: >-
      Raw bytes correct Ghidra's reported 28-byte extent: ldr r2,[r0,#0x44]
      at 0x0826d834 through bx lr at 0x0826d84c are 24 bytes, and the
      separately linked sibling FUN_0826d850 begins at 0x0826d850 with
      push {r4,r5,r6,lr}. The setter returns without a store or call when
      view+0x44 already equals word_44. Otherwise it stores word_44 first,
      reloads the view vtable, and tail-dispatches its +0xd4 slot with view
      as r0. Decoding every ARM B/BL immediate in osos.dec finds exactly 15
      direct inbound calls: all unconditional bl, zero predicated and zero
      direct tail-b forms. The dynamic slot target is runtime vtable data,
      not a separately identified firmware entry, so the port does not
      invent a callee identity. Target builds retain that direct vtable
      dispatch; host builds use the same ABI through a volatile slot seam
      because the target u32 vtable word cannot hold a host-width function
      pointer. Host tests cover equal values, zero-to-u32::MAX and
      u32::MAX-to-zero changes, store-before-callback order, and the
      untouched neighboring +0x4c field.

  - address: 0x0817e2e0
    ghidra: FUN_0817e2e0
    name: registry_element_secondary_dispatch
    size: 28
    sig: "void registry_element_secondary_dispatch(RegistryElementSecondaryDispatch *element, void *value)"
    module: app/element_registry
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: value
      iVar1/r1: secondary_collection (element + 0x34)
      iVar2/r2: secondary_collection vtable slot +0x1c
      local_8/sp: value_slot
    notes: >-
      Raw ARM is exactly 28 bytes, from 0x0817e2e0 through the pop {ip,pc} at
      0x0817e2f8; the separately linked next function starts at 0x0817e2fc.
      It stores value in a stack word, derives the embedded secondary collection
      at element +0x34, and blx-dispatches that object's runtime vtable slot
      +0x1c with (collection, &value_slot). The slot target is runtime data, not
      a statically identifiable function entry, so the port performs the
      supplied vtable dispatch and does not invent a callee identity or seam.
      Decoding every ARM B/BL immediate in osos.dec finds 15 inbound direct
      calls: all unconditional bl, zero predicated bl, zero direct tail b, and
      no aligned data-word occurrence of 0x0817e2e0. The sibling constructor
      FUN_0817e3b0 initializes an observable-array-derived subobject at +0x34.
      The raw body has no NULL guard; host tests prove both a real pointer and
      NULL are forwarded. Deliberate host-only representation deviation: the
      dynamic vtable is structural so it can retain a native-width callback,
      while the target field's +0x34 layout is asserted on 32-bit builds.

  - address: 0x0811e2e4
    ghidra: FUN_0811e2e4
    name: ui_current_window
    size: 20
    sig: "void *ui_current_window(void)"
    module: ui/current_window
    status: ported
    renames:
      iVar1/r0: session (active-session accessor result)
      uVar2/r0: current_window (return value)
    notes: >-
      Returns the WindowManager active session's current-window word at
      +0x1c, or NULL when no session is active: bl 0x08148b98 (active
      session), cmp r0,#0, ldrne r0,[r0,#28], pop {r4,pc}. Extent
      binary-verified: the sibling flag setter 0x0811e2f8 opens push
      {r4, lr} immediately after the pop {r4, pc}. Exactly 15 direct bl
      call sites, all unconditional, none predicated, no data-word
      references (verified by decoding every B/BL word and scanning every
      word of osos.dec) - never dispatched virtually. The callee
      0x08148b98 stays in retailOS behind a one-pointer seam: it takes
      the scheduler lock (0x08148cc8), loads the WindowManager singleton's
      (0x089cb2ec; "WindowManager" string at 0x08148b30) active-session
      word at +0x10, and unlocks (0x08148dd4); teardown 0x08148e20 clears
      that field to NULL. Caller evidence that the result is the current
      window object: 0x0828d9b4 tests its title byte at +0x101, 0x081ec31c
      virtual-calls vtable +0xd8, and 0x08180ce0/0x08182ce0/0x081843d4/
      0x0817f7a4/0x0828e3d4 pass its +0xe8 handle to the window-list
      lookup 0x082775f4. match.py diff is the expected seam shape
      (ldr/blx through the ACTIVE_SESSION static plus LLVM's fp frame);
      the core cmp/ldrne at +28 matches. Host fixtures use the dedicated
      CURRENT_WINDOW hint (0x43000000); the +0x1c field is read as a u32
      target word and widened.

  - address: 0x082ab3ec
    ghidra: FUN_082ab3ec
    name: cpp_finalise_dtor_guard
    size: 20
    sig: "void *cpp_finalise_dtor_guard(void *array, u32 count, u32 elem_size, void (*dtor)(void *))"
    module: heap/veneers
    status: ported
    renames:
      param_1/r0: array
      param_2/r1: count
      param_3/r2: elem_size
      param_4/r3: dtor
      ip/r12: saved_count
    notes: >-
      Raw ARM extent is exactly 20 bytes: 0x082ab3ec..0x082ab3fc, followed
      immediately by the separately linked 0x082ab400. It saves count in ip,
      tests dtor with movs, and either returns array unchanged when dtor is
      NULL or moves count to r3 and tail-branches to __cpp_finalise
      (0x080336d8) with (array, dtor, elem_size, count); that walker returns
      array-8. Ghidra's loop body is not present in the raw bytes. Decoding
      every ARM B/BL immediate in osos.dec finds 17 inbound direct branches:
      14 unconditional bl and three unconditional tail b
      (0x08038e24/0x08038e90/0x08038eb0), with no predicated branches.
      Scanning aligned osos.dec words finds no 0x082ab3ec data reference, so
      this is not dispatched virtually. The Rust call cannot guarantee the
      ARM tail-call shape but preserves both observable return paths.

  - address: 0x08248698
    ghidra: FUN_08248698
    name: copy_four_words
    size: 36
    sig: "u32 copy_four_words(const u32 *source, u32 *destination)"
    module: util/copy_four_words
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: destination
      r2: copied_word
      r0 (after final ldr): final_word
    notes: >-
      Raw ARM is exactly nine instructions at 0x08248698..0x082486b8; the
      next separately linked function opens at 0x082486bc. It copies four
      words from source (r0) to destination (r1), preserving each ordered
      ldr/str pair. The final ldr writes r0, so its observed value is stored
      at destination+12 and returned; preceding stores can alter that final
      read for overlapping ranges. Decoding every ARM B/BL word in osos.dec
      found exactly 14 inbound direct bl sites, all unconditional, with no
      predicated BL forms or tail branches. The body has no callee or dispatch
      seam; its only deliberate Rust choice is to expose the otherwise
      untyped r0 result as u32.


  # --- Silver controller secondary-interface base constructor ---
  - address: 0x0821599c
    ghidra: FUN_0821599c
    name: controller_secondary_interface_construct
    size: 12
    sig: "u32 *controller_secondary_interface_construct(u32 *secondary_interface)"
    module: app/controller_secondary_interface
    status: ported
    renames:
      param_1/r0: secondary_interface
      DAT_082159a8: controller_secondary_interface_vtable (0x08993258)
    notes: >-
      Raw extent is exactly 12 bytes, 0x0821599c..0x082159a8: ldr r1,[pc,#4];
      str r1,[r0]; bx lr; literal 0x08993258. The next separately linked
      function starts at 0x082159ac with bx lr, so Ghidra's 12-byte size is
      correct and includes the literal-pool word. Decoding every ARM B/BL
      immediate in osos.dec finds exactly 14 inbound direct calls: all are
      unconditional bl, with zero predicated bl and zero tail b; scanning
      aligned words finds no data reference to 0x0821599c, so it is never
      virtually dispatched. This is the base constructor for the secondary
      interface subobject at +0xd0 in the kinded-controller leaves: it plants
      vtable word 0x08993258 and returns the unchanged input pointer because
      r0 is not overwritten. Each caller immediately replaces that word with
      its leaf-specific secondary vtable (for example, 0x0822519c stores
      this + 0x164), but the base store is still the function's behavior.
      Deliberate Rust deviation: the runtime vtable remains a raw u32 target
      address rather than a Rust vtable because the decrypted static page is
      not trustworthy; the original's unguarded aligned str is modeled as an
      unguarded aligned u32 store.

  # --- Strided buffer entry address accessor ---
  - address: 0x08209768
    ghidra: FUN_08209768
    name: strided_buffer_entry_at
    size: 32
    sig: "u32 strided_buffer_entry_at(const StridedBuffer *table, u32 index)"
    module: app/strided_buffer_entry
    status: ported
    renames:
      param_1/r0: table
      param_2/r1: index
      FUN_081d8614: layout_entry_size_dispatch
      field_+0x04: entry_buffer
      field_+0x0c: layout
    notes: >-
      Raw extent is exactly 32 bytes, 0x08209768..0x08209788: it calls
      FUN_081d8614, then returns table->entry_buffer + (index + 1) * stride
      through add/mla word arithmetic. The next separately linked function
      starts at 0x08209788. Decoding every ARM B/BL immediate in osos.dec
      finds exactly 14 inbound direct calls, all unconditional bl, with zero
      predicated bl and zero tail b; scanning aligned words finds no DATA
      reference. FUN_081d8614 is an unported three-load virtual dispatcher:
      table+0x0c -> layout, layout->vtable slot +0x2c -> stride. The port
      performs that decoded vtable dispatch directly rather than inventing a
      callee identity or adding a redundant seam. Host coverage checks index
      0, a later entry, and index 0xffffffff's modulo-32-bit header result.

  - address: 0x082570ec
    ghidra: FUN_082570ec
    name: fixed_matrix_identity_init
    size: 84
    sig: "void fixed_matrix_identity_init(FixedMatrix4x4 *matrix)"
    module: util/fixed_matrix_identity
    status: ported
    renames:
      param_1/r0: matrix (aligned writable 0x44-byte Q16.16 4x4 matrix)
      r1: zero (shared by the twelve zero element stores)
      r2: q16_one (0x00010000, shared by the four diagonal stores)
      r3: identity_marker (one-byte value stored at matrix +0x40)
    notes: >-
      Raw ARM extent is exactly 84 bytes, 0x082570ec..0x08257140: the next
      separately linked function begins at 0x08257140. It initializes all
      sixteen signed Q16.16 matrix elements to the 4x4 identity, storing
      0x00010000 at word indices 15, 10, 5, and 0 and zero at every other
      index in that decoded order, then stores byte one at +0x40. Decoding
      every ARM B/BL immediate in osos.dec found exactly 14 direct inbound bl
      sites and one unconditional tail b at 0x08242ec0; there are zero
      predicated forms. The tail-b caller and the matrix multiplication caller
      at 0x08242da4 establish the +0x40 byte as the known-identity marker:
      products propagate it only when both input matrices carry one. The
      original is an unguarded leaf with no callees. No deliberate Rust
      deviations: the port keeps the exact element store order with volatile
      aligned accesses and leaves the three trailing ABI-padding bytes alone.

  - address: 0x081d6380
    ghidra: FUN_081d6380
    name: vtable_object_base_construct
    size: 40
    sig: "void *vtable_object_base_construct(void *this, u32 input_at_8, u32 input_at_12)"
    module: cxx/vtable_object_base_construct
    status: ported
    renames:
      param_1/r0: this (aligned writable 28-byte base object)
      param_2/r1: input_at_8 (copied to this +0x08)
      param_3/r2: input_at_12 (copied to this +0x0c)
      r3: zero (clears object fields)
    notes: >-
      Raw extent is exactly 40 bytes, 0x081d6380..0x081d63a8: the literal
      vtable word at 0x081d63a8 is part of the function and the next
      separately linked function starts at 0x081d63ac. The unguarded leaf
      installs vtable 0x0898e004, copies r1 and r2 to +0x08 and +0x0c,
      clears the word at +0x04, byte at +0x10, and words at +0x14 and +0x18,
      then returns this in r0; bytes +0x11..+0x13 are preserved. Decoding
      every ARM B/BL immediate in osos.dec found exactly 14 inbound direct
      bl sites, all unconditional, with zero predicated bl and zero tail b.
      An aligned whole-image word scan found no data reference, so callers
      statically bind this base constructor. No deliberate Rust deviations.

  - address: 0x081b1068
    ghidra: FUN_081b1068
    name: viewer_no_content_layout_default_resource_name
    size: 100
    sig: "char *viewer_no_content_layout_default_resource_name(void)"
    module: app/no_content_layout_default
    status: ported
    renames:
      DAT_081b10bc: no_content_layout_default_storage (0x089ca630 resource-data prefix)
      DAT_081b10c0: __dso_handle (0x089ca09c)
      DAT_081b10c4: no_content_layout_default_destructor (0x081a631c)
      DAT_081b10c8: viewer_no_content_layout_default_resource_name (0x089ca631)
    notes: >-
      Raw ARM code runs from 0x081b1068 through 0x081b10bc; the four
      literal-pool words at 0x081b10bc..0x081b10c8 extend the true function
      extent to 100 bytes, and the next separately linked function begins at
      0x081b10cc. It is an ADS function-local-static accessor over the
      resource-data prefix 0x089ca630 = "ssViewer_No_Content_Layout_Default".
      The actual return is base + 1, the resource name
      "sViewer_No_Content_Layout_Default". Retail's aligned guard word at
      base + 4 is 0x72657765 ("ewer"), so bit 0 is set and normal execution
      skips initialization. A clear guard calls cxa_guard_acquire
      (0x082ab31c), registers base + 1 with the real function entry
      0x081a631c through cxa_atexit (0x082ab1c8) and __dso_handle, releases
      the guard (0x082ab338), then writes one to base only if it was zero.
      Decoding every ARM B/BL word in osos.dec found exactly 14 direct
      callers: all 14 are unconditional bl (0x0803c300, 0x0805bb6c,
      0x08076b60, 0x081896b8, 0x08189ddc, 0x08206f84, 0x0820709c,
      0x082c6d54, 0x082c9a6c, 0x082c9e68, 0x082c9eac, 0x082c9ef8,
      0x082ca394, 0x082ca3d8); no predicated bl or tail b reaches it.
      The destructor is not assigned an invented identity: raw ARM at its
      real entry calls 0x081de270(object, 11) then stores one at object+0xd6.
      Device builds retain the fixed data pointer and target; host tests use
      aligned private storage and an inert handler because retail code cannot
      execute on the host.

  - address: 0x0813e9ec
    ghidra: FUN_0813e9ec
    name: shared_interface_tree_mutex_construct
    size: 184
    sig: "SharedInterfaceTreeMutex *shared_interface_tree_mutex_construct(SharedInterfaceTreeMutex *this, u8 mode, u32 kind)"
    module: app/root_context_f9c_bound
    status: ported
    renames:
      param_1/r0: this (writable 0x48-byte shared-interface base object)
      param_2/r1: mode (stored as a byte at this +0x44)
      param_3/r2: kind (selects root-context resource words 0 or 1)
      DAT_0813ea98: shared_interface_base_vtable (0x089a75c8)
      DAT_0813ea9c: shared_interface_tree_mutex_vtable (0x089850c4)
      DAT_0813eaa0: app_root_object_global (0x089ca674)
      FUN_083c00dc: word_key_set_allocate_node
      FUN_08261e28: cxx_mutex_construct
    notes: >-
      Raw ARM has 172 bytes of code from 0x0813e9ec through 0x0813ea98,
      followed by the three literal-pool words at 0x0813ea98/0x0813ea9c/
      0x0813eaa0; the distinct next function starts with cmp r0,#0 at
      0x0813eaa4, so the true extent is 184 bytes. It first installs shared
      interface-base vtable 0x089a75c8, clears the embedded word-key tree at
      +0x04, obtains its sentinel through word_key_set_allocate_node, makes
      the sentinel parent NULL and its left/right links self-pointing,
      constructs the mutex at +0x20, stores 2 at +0x3c, replaces the vtable
      with 0x089850c4, conditionally copies root-context resource kind 0 or 1
      to +0x40, and stores mode at +0x44.
      A complete ARM B/BL immediate decode found exactly ten direct inbound
      bl sites, all unconditional (0x080fe348, 0x0811ee1c, 0x081436f8,
      0x0814a938, 0x08168a9c, 0x081aa35c, 0x081b7610, 0x081cc7d4,
      0x0829df40, 0x082c86e8), with zero predicated bl and zero plain-b
      tails. An aligned whole-image word scan found no entry-address data
      reference. The vtable is a shared interface with no recovered class
      identity, so the name records only the verified tree/mutex shape.
      Host layout widens tree pointer fields to keep them disjoint; target
      layout assertions retain the ARM offsets. The original leaves the
      mutex constructor's unused ABI-scratch scope inputs unspecified after
      node allocation; the port passes zeroes, which its attr initializer
      overwrites before use.

  - address: 0x08168a8c
    ghidra: FUN_08168a8c
    name: root_context_f9c_bound_construct
    size: 56
    sig: "RootContextF9cBound *root_context_f9c_bound_construct(RootContextF9cBound *this, u8 mode)"
    module: app/root_context_f9c_bound
    status: ported
    renames:
      param_1/r0: this (writable 0x48-byte base-derived object)
      param_2/r1: mode (stored as a byte at this +0x44)
      FUN_0813e9ec: shared_interface_tree_mutex_construct
      DAT_08168ac4: root_context_f9c_bound_vtable (0x089880f0)
      DAT_08168ac8: app_root_object_global (0x089ca674)
    notes: >-
      Raw ARM establishes exactly 56 bytes: code 0x08168a8c..0x08168ac4
      plus the two literal-pool words at 0x08168ac4/0x08168ac8; the distinct
      next function begins at 0x08168acc with cmp r0,#0. It calls the ported
      shared_interface_tree_mutex_construct 0x0813e9ec(this, 0, 0), uses that
      returned base object, stores vtable 0x089880f0 at +0x00, copies
      *(*0x089ca674 + 0x30 + 0xf9c) to +0x40, and stores r1's low byte at
      +0x44. The literal vtable address reads C++ name-blob text in osos.dec,
      not a valid static table, so no class or virtual-callee identity is
      invented. A complete ARM B/BL immediate scan finds 14 direct inbound
      bl sites, all unconditional (0x0812f954, 0x08142568, 0x0814a9b4,
      0x081b57bc, 0x081b5c4c, 0x081f89fc, 0x081f8ae4, 0x081f8e60,
      0x081f8f04, 0x08208c38, 0x0820ba60, 0x08210ac8, 0x08211510, and
      0x082c76b0), zero predicated bl, plus the unconditional tail b at
      0x0825a024. The derived constructor calls the ported base directly;
      host tests use the genuine tree allocator through a dedicated writable
      arena and app/context_scope.rs's existing APP_ROOT_OBJECT model of
      0x089ca674 through a dedicated below-4-GiB raw-u32 root/context fixture.

  - address: 0x08168ae4
    ghidra: thunk_FUN_0825a028
    name: root_context_f9c_bound_destruct
    size: 4
    sig: "RootContextF9cBound *root_context_f9c_bound_destruct(RootContextF9cBound *this)"
    module: app/root_context_f9c_bound
    status: ported
    renames:
      param_1/r0: this (root-context +0xf9c bound object; forwarded without a NULL guard)
      thunk_FUN_0813eabc: root_context_f9c_bound_destruct_first_veneer
      FUN_0825a028: root_context_f9c_bound_destruct_target (unported)
    notes: >-
      Raw ARM confirms Ghidra's 4-byte extent: 0x08168ae4 is the lone
      `b 0x0813eabc`; the next separately linked body starts at 0x08168ae8.
      That first veneer is itself the lone `b 0x0825a028`, so this port
      forwards this and the final target return unchanged through a volatile
      seam. The preceding 0x08168acc deleting-destructor path tests this for
      NULL, calls 0x0813eabc, then tail-branches to operator_delete
      (0x082aad24), identifying this as the non-deleting counterpart for the
      adjacent root_context_f9c_bound constructor. Full-image ARM B/BL decode
      finds exactly 12 inbound direct calls, all unconditional bl:
      0x0812fa24, 0x081425f8, 0x08142614, 0x081b58b4, 0x081b5d8c,
      0x081f8aa0, 0x081f8b78, 0x081f8e90, 0x081f8ed8, 0x081f8fe4,
      0x08210b4c, and 0x08211870. There are no predicated forms and aligned
      whole-image word scans find no DATA reference to either veneer or the
      unported body. Device builds call the fixed 0x0825a028 address; host
      tests replace that boundary with a recorder and prove both NULL
      forwarding and return propagation.
  - address: 0x08139dec
    ghidra: FUN_08139dec
    name: iap_incoming_client_base_construct
    size: 144
    sig: "u8 *iap_incoming_client_base_construct(u8 *this, usize incoming_r1, usize scope_word0, usize scope_word1)"
    module: app/iap_incoming_client_base
    status: ported
    renames:
      param_1/r0/r4: this (writable 0x2d0-byte iAP incoming-client base object)
      DAT_08139e78: IAP_INCOMING_CLIENT_BASE_VTABLE (0x08984cb8; the 0x0898xxxx
        page is stale RW data in the image, so only the address constant
        survives - the util/resource_list precedent)
      FUN_08261e28: cxx_mutex_construct (ported, called directly; its r1 is the
        vtable literal, its r2/r3 are the caller's register-shape seeds)
      thunk_EXT_FUN_2200027c: memzero_aligned via the IRAM veneer 0x08037db8
        (ported; called through a read_volatile fn-pointer load so LLVM does
        not inline it and re-lower to __aeabi_memclr4)
      FUN_081d71c0: iap_incoming_process_thread_instance (ported, called
        directly, twice - the second read is a genuine re-read)
      FUN_081d6e38: register_client (unported; IAP_INCOMING_CLIENT_BASE_OPS
        seam, target default transmutes 0x081d6e38)
      FUN_081d7270: iap_incoming_process_thread_slot_poll (ported, called
        directly)
      FUN_08030f44: heap_panic (ported, direct; the slot == -1 path is fatal
        and not host-testable)
    notes: >-
      The shared base-class constructor of every object that registers as a
      client of the CIapIncomingProcessThread incoming-packet worker. Verified
      extent 0x08139dec..0x08139e7c: Ghidra's 140 covers exactly the
      instructions; the vtable literal 0x08984cb8 at 0x08139e78 (reached by
      the first ldr's [pc,#128]) extends the span to 144, and the next
      function at 0x08139e7c is the matching base DESTRUCTOR (replants the
      same vtable, polls then unregisters the slot via 0x081d7270/0x081d66fc,
      destroys the mutex via 0x08261e54, returns this) - a separate port.
      Call count binary-verified by decoding every B/BL word in osos.dec: 14
      bl sites, ALL unconditional (0x0813a2e8, 0x081650b0, 0x08193cbc,
      0x08193e3c, 0x08195338, 0x08196e94, 0x081af24c, 0x081d7c30, 0x081e30b8,
      0x081f25f0, 0x081f38ec, 0x08200c64, 0x08201224, 0x08209548), zero
      predicated forms, zero tail b, and no DATA word references the address -
      only ever direct-called, never virtual. Every site is a derived
      constructor that immediately overwrites the vtable word (0x081650ac
      plants 0x08987e04 and extends the object at +0x2d0, proving the base
      size 0x2d0). Algorithm: plant vtable at +0x00, cxx_mutex_construct the
      0x1c-byte pool-guard mutex at +0x2ac, slot word +0x2c8 = -1, self
      pointer +0x2cc = this, memzero(this+4, 0x2a8), then thread the 32
      20-byte pool nodes at +0x2c..+0x2ac newest-first onto the head word at
      +0x18 (afterwards head = this+0x298, node at +0x2c terminates NULL; the
      loop's bic r0,r0,#0x10000 is dead for the 1..32 count and preserved),
      then register this with the thread context (0x081d6e38 wraps
      0x081d6dbc: free slot of the 29-slot table at context+0x154, a
      0x24-byte wait-registration object into the slot's +0 word, the client
      into the +4 context word, returns the index or -1), store the slot at
      +0x2c8 even when it is -1 BEFORE the fatal check, and finish with one
      zero-timeout poll of the new slot after a second, genuine instance
      re-read. Of the constant-1 registration arguments, r1 seeds the scoped
      object built inside 0x081d6e38 (bl 0x08261e94); r3 is carried into
      0x081d6dbc but never read there. Deviations: the registration wrapper
      rides the IAP_INCOMING_CLIENT_BASE_OPS seam (host default inert,
      returning slot 0; tests install recording mocks) - hook-ready for the
      registration call only once 0x081d6e38 is ported, the target default
      reaching the stock body until then; incoming_r1/scope_word0/
      scope_word1 keep the register shape only (the original overwrites r1
      with the vtable literal and forwards its caller's r2/r3 into
      cxx_mutex_construct's stack-scope seeds - the ADS uninitialized-frame
      idiom that port documents). LLVM fully unrolls the 32-iteration
      threading loop and keeps the head in registers (intermediate head
      values never land in memory) - unobservable in a single-threaded
      constructor; match.py structural (exit 1, diff expected): same vtable
      plant / mutex bl / -1 store / zero-fill / chain shape / register blx /
      cmn-store-beq panic / re-read / poll / return-this. Host tests (serial
      suite): full layout + registration + poll of slot 3, the instance
      re-read proven by re-publishing a second thread fixture inside the
      register mock, and edge slot 28 flowing verbatim to the +0x2c8 word and
      the poll; fixtures map one slab at the dedicated
      hints::IAP_INCOMING_CLIENT_BASE (0xca000000) with the client object at
      +0x000 and two 0x240-byte thread contexts at +0x400/+0x800.

  # --- handler-list activation facade ---
  - address: 0x08134720
    ghidra: FUN_08134720
    name: handler_list_activate
    size: 124
    sig: "void handler_list_activate(HandlerListOwner *owner, HandlerList *handlers)"
    module: app/handler_list_activate
    status: ported
    renames:
      param_1/r0: owner (this - the screen/handler object whose vtable
        slot +0xd0 receives the dispatch result)
      param_2/r1: handlers (the caller's 24-byte stack HandlerList)
      DAT_08134798: HANDLER_LIST_RESOURCE (0x0dad0195, the pool word)
      FUN_081e8ca0: handler_context_get (HANDLER_LIST_ACTIVATE_OPS seam;
        unported lazy singleton - 0x34-byte context object, cache word
        0x089cfe38, ctor 0x081e8ee8)
      FUN_081e8da0: handler_context_install_list (seam; unported)
      FUN_081e8cd0: handler_context_active_state (seam; unported)
      FUN_0817ee04: app_controller_get (direct, ported in app/singletons)
      FUN_08181110: app_controller_begin_command (seam; unported - 48
        bytes, fills a 16-byte record from FUN_08182c78 and mirrors it at
        controller +0x88)
      FUN_081dfa20: command_dispatcher_get (direct, ported)
      FUN_081dfab0: command_dispatch_by_resource (direct, ported)
    notes: >-
      EXTENT: Ghidra's 120 bytes is the code only; the function owns one
      trailing literal-pool word (0x0dad0195 @ 0x08134798, reached by ldr
      r4, [pc, #56]), so the true extent is 124 bytes, 0x08134720..
      0x0813479c, where the next function opens push {r4-r10, lr}. CALL
      COUNT verified by decoding every B/BL word in osos.dec: exactly 14
      bl sites, 11 unconditional plus 3 predicated blne @ 0x08223084,
      0x08226d04, 0x08239650, each immediately after cmn r0, #1 - the
      CALLER skips activation when its preceding virtual query answered
      -1; this function carries no such guard. No plain-b tail-call
      sites. ALGORITHM: the consumer half of the handler-list protocol
      (callers stack-build a HandlerList with handler_list_construct
      0x0816cbfc and vector_push_back_elem12 pushes like
      {"HandleAddToOTG" + 1, 1}): install the list into the
      handler-context singleton, read the context's active-state word,
      post command 0x0dad0195 (private 0x0dad0000 namespace) with that
      state to the app controller's pending-command record, dispatch
      command 0x0dad0196 (resource = command - 1, aux = 0) through the
      command dispatcher, and tail-dispatch the owner's vtable slot
      +0xd0 with the dispatch result (bx r2). The context getter is
      genuinely called twice, bracketing the install. Ghidra's C is
      accurate this time (its jumptable warning is just the tail bx).
      Deviations: the four unported callees ride the
      HANDLER_LIST_ACTIVATE_OPS read_volatile seam (command_dispatch.rs
      pattern) - target defaults transmute 0x081e8ca0 / 0x081e8da0 /
      0x081e8cd0 / 0x08181110 so the port is hook-ready on device, host
      defaults panic; the owner vtable keeps a structural pointer
      representation with a target-width +0xd0 offset assertion (the
      controller_layout_dispatch.rs pattern). Host tests (serial, own
      OPS_LOCK then SINGLETON_LOCK then the heap mock lock): the full
      dependency order (context-get, install, context-get, state,
      begin-command, ctor alloc/registry/resolve, dispatch, dtor,
      owner-slot) with every argument asserted, plus a zero-state run
      proving the state word and dispatch result forward verbatim.
      match.py --lib <worktree archive>: same skeleton - install pair,
      state read, controller post with (0x0dad0195, 0, 0, state, 0),
      dispatch with (resource, resource + 1, 0), vtable +0xd0 tail bx;
      deltas are the fp prologue, the seam blx indirection, and
      relocation placeholders for the three ported bl targets, the
      accepted deviation classes. 37 instructions vs 30.

  # --- controller pending-command post ---
  - address: 0x08181110
    ghidra: FUN_08181110
    name: app_controller_begin_command
    size: 48
    sig: "void app_controller_begin_command(AppControllerPendingCommand *controller, u32 command, u32 arg2, u32 arg3, u32 state, u32 aux)"
    module: app/controller_pending_command
    status: ported
    renames:
      param_1/r0: controller (application controller; pending command mirror at +0x88)
      param_2/r1: command (record lookup key's first word)
      param_3/r2: arg2 (record and controller +0x88 word 0)
      param_4/r3: arg3 (record and controller +0x8c word 1)
      param_5/[sp,#0x18]: state (record and controller +0x90 word 2)
      param_6/[sp,#0x1c]: aux (record and controller +0x94 word 3)
      FUN_08182c78: command_record_resolve_or_allocate (unported seam)
    notes: >-
      EXTENT verified against osos.dec: exactly 48 bytes,
      0x08181110..0x08181140; the next sibling begins push {r0-r12, lr} at
      0x08181140. CALL COUNT verified by decoding every ARM B/BL word in
      osos.dec: exactly 9 unconditional bl sites at 0x08134764, 0x08225498,
      0x082290f4, 0x08230824, 0x082319d0, 0x08231aa4, 0x08231b78,
      0x08231c38, and 0x082323bc; no predicated bl and no plain-B tail-call
      sites. ALGORITHM: call FUN_08182c78(controller, command, 0, 1), write
      arg2/arg3/state/aux to the returned 16-byte record, then mirror the
      same four words at controller +0x88..+0x94. Neither the returned record
      nor controller is NULL-guarded. DEVIATION: the unported resolver is a
      read_volatile seam: target defaults reach its stock 0x08182c78 body,
      host defaults panic and tests install a recording resolver. The
      controller prefix and record have repr(C) named fields with the
      +0x88 target-width offset assertion rather than host-unsafe byte
      offsets. The ported handler_list_activate caller now calls this
      function directly; its old target/default seam has been removed. Host
      tests prove resolver arguments (command, 0, 1), every high-bit and zero
      payload word, record/controller mirroring, prior-record overwrite, and
      +0x00..+0x87 preservation.

  - address: 0x080a1d7c
    ghidra: FUN_080a1d7c
    name: string_object_construct_game_path
    size: 292
    sig: "i32 string_object_construct_game_path(StringObject *this, u32 mode, const StringObject *id_string, i32 games_base, const char *path)"
    module: cxx/string_object
    status: ported
    renames:
      param_1/r0: this (the StringObject under construction)
      param_2/r1: mode (0 = games_RO, 3 = gamedata_ShareRW, else dir_name(mode))
      param_3/r2: id_string (StringObject whose raw payload becomes the
        third path component; unread in mode 3)
      param_4/r3: games_base (mode-0 only - 0 -> "iPod_Control", nonzero
        -> "Resources/Games"; ignored by every other branch, a stock quirk)
      in_stack_00000000: path (the fifth, stacked argument; one leading
        '/' stripped)
      DAT_080a1e50: STRING_OBJECT_VTABLE_ADDRESS (0x089a6044)
      FUN_082a538c: string_object_raw_payload (direct, ported)
      FUN_08097e00: game_storage_dir_name (GAME_STORAGE_DIR_NAME seam)
      FUN_082769d4: string_object_format (direct, ported)
    notes: >-
      EXTENT: Ghidra's 212 bytes is the code only; the function owns an
      80-byte literal pool @ 0x080a1e50..0x080a1ea0 (vtable word plus the
      seven path strings "/", "", "iPod_Control", "Resources/Games",
      "iPod_Control/%s%s%s", "%s/%s/%s%s%s"), so the true extent is 292
      bytes, 0x080a1d7c..0x080a1ea0, where the next function starts (no
      prologue; a helper tail-branching to 0x0809a798). CALL COUNT
      verified by decoding every B/BL word in osos.dec: exactly 14 bl
      sites, all unconditional, no predicated forms, no tail branches.
      Ghidra's C drops param_4 and the stacked path argument and every
      format argument - the asm is authoritative. ALGORITHM: the
      StringObject game-storage path constructor. Plants the vtable and
      a NULL payload, reads the first path byte UNGUARDED (a NULL path
      faults there, before the later NULL checks can matter), strips one
      leading '/', then composes and assigns a printf path through
      string_object_format @ 0x082769d4: mode 0 -> "%s/%s/%s%s%s" with
      base = (games_base == 0 ? "iPod_Control" : "Resources/Games"),
      dir_name(0) = "games_RO", the id payload, "/" (or "" when the
      post-strip path pointer is NULL), and the stripped path; mode 3 ->
      "iPod_Control/%s%s%s" with dir_name(3) = "gamedata_ShareRW", the
      separator and the path (id_string never touched); any other mode
      -> "%s/%s/%s%s%s" with base ALWAYS "iPod_Control" (games_base
      ignored), dir_name(mode), id payload, separator, path. The
      original leaves the formatter's character count in r0 (not this);
      all 14 callers discard it. Deviations: modeled static vtable and
      path strings (STRING_OBJECT_EMPTY_CSTR pattern); the variadic
      arguments are restacked as a [u32] word array passed as VaList
      (house convention - stable Rust cannot define C-variadic
      functions, and the original's r2/r3/stack argument words ARE a
      va_list); the directory selector @ 0x08097e00 rides the
      read_volatile GAME_STORAGE_DIR_NAME seam whose default
      game_storage_dir_name_model is an exact behavioral copy of the ROM
      jump table (pure string selection, safe on device and host).
      Host tests (assign/format bench + word-snapshotting engine):
      mode-0 composition with both bases, slash stripping (exactly one
      leading '/'), NULL id payload passthrough, mode-3 never
      dereferencing id_string (passed as 0x1), games_base ignored
      outside mode 0, seam indirection receiving the mode verbatim, and
      the formatter count returned. match.py --lib <worktree archive>:
      65 instructions vs 53; same three-way dispatch, guarded strip,
      vtable/payload init and per-branch vararg stacking; deltas are the
      fp frame, the seam blx indirection, and the VaList marshaling -
      the accepted deviation classes.

  - address: 0x08097e00
    ghidra: FUN_08097e00
    name: game_storage_dir_name
    size: 132
    sig: "const char * game_storage_dir_name(u32 mode)"
    module: cxx/string_object
    status: identified
    notes: >-
      EXTENT: Ghidra's 72 bytes is the code only; five serialized
      strings at 0x08097e48..0x08097e84 bring the true extent to 132
      bytes, ending where the next function opens push {r0-r12, lr}.
      Decoded from raw bytes: a 5-way addls pc, pc, r0, lsl #2 jump
      table returning "games_RO" (mode 0 or 4), "gamedata_RW" (1),
      "gamestats_WO" (2), "gamedata_ShareRW" (3), and the empty string
      for any other mode. The r1 argument is never read (callers pass
      the id payload or a path byte; the body preserves r1, which
      0x080a1d7c silently relies on across the call). Not yet ported as
      an exported symbol: its sole known caller
      string_object_construct_game_path @ 0x080a1d7c reaches it through
      the GAME_STORAGE_DIR_NAME seam, whose default
      game_storage_dir_name_model is this exact behavior. A future
      porter only needs to lift the model into an exported function and
      rewire the seam default.

  - address: 0x08087554
    ghidra: FUN_08087554
    name: piezo_note_post
    size: 64
    sig: "void piezo_note_post(u32 period_ticks, u32 duration_ticks)"
    module: drivers/piezo
    status: ported
    renames:
      param_1: period_ticks
      param_2: duration_ticks
    notes: >-
      Extent verified from raw bytes: 64 bytes, 0x08087554..0x08087594;
      the sibling at 0x08087594 opens its own push prologue. Piezo
      (clicker) note post helper: malloc(24) @ 0x0802edac (ported,
      runtime/malloc_rt), zero-fill through IRAM veneer 0x08037db8 ->
      0x2200027c = memzero_aligned @ 0x0800027c (ported, libc/memzero;
      the port calls memzero_aligned directly through a read_volatile
      fn-pointer load so LLVM does not inline it and re-lower the fixed
      24-byte fill to __aeabi_memclr4), stores period_ticks at +0x10 and
      duration_ticks at +0x14, then tail-branches through veneer
      0x08037f30 -> IRAM 0x22004154 to mailbox_send_gateway @ 0x08004154
      (ported, heap/mailbox_send_gateway): KS_send(mailbox 2, block,
      priority 5, semaphore 0). Call sites binary-verified by decoding
      every B/BL word in osos.dec: 19 total (13 bl, 4 tail b, 1 tail
      bne @ 0x0805bc14, 1 blne @ 0x081b1178); Ghidra's "14 bl" misses
      the tail branches and double-counts the thunk 0x08061054 (a lone
      b 0x08087554). The predicated sites are callers gating the post on
      their own state; the callee itself is unconditional. Semantics
      pinned by the mailbox-2 receiver task @ 0x083938f4: it reads
      +0x10/+0x14 as tick counts for two S5L8702 timer channels
      (0x3c700020 + channel * 0x20) - +0x10 arms the tone channel via
      timer_channel_arm @ 0x0836dc14 only when nonzero (0 = rest, e.g.
      the (0, 10) post @ 0x080495a4), +0x14 arms the note-length channel
      (0x0836dbb0: prescaler 749, control 0x240) whose expiry callback
      0x080dea34 ends the note. Caller pairs are (period, duration)
      notes: boot chime (540,200),(676,400) @ 0x08392f10/0x08044ec4;
      click (223,50),(250,50) @ 0x0804958c. freemyipod RetailOS names
      the mailbox set M_PIEZOMGR/PIEZOMGRSEND. Deviation: the original
      has no malloc-failure check (a NULL block would be zero-filled at
      address 0 and posted); the port returns early without posting, the
      documented stdio-slot-append precedent. Host tests mock HEAP_OPS
      and ROM_GATEWAY_OPS to prove the 24-byte zero-fill, payload
      stores, the (2, block, 5, 0) send, and the failure path. match.py
      --lib <worktree archive>: 26 vs 16 instructions; same
      alloc/zero/store/tail-post structure, deltas are the HEAP_OPS and
      volatile-fn blx indirection, the fp frame, and the null-check
      branch (the documented deviation).

  # --- Counted wide-string equality under collation flags ---
  - address: 0x08059b60
    ghidra: FUN_08059b60
    name: counted_wstr_eq
    size: 40
    sig: "u32 counted_wstr_eq(const u16 *a, const u16 *b, u32 flags)"
    module: util/counted_wstr_eq
    status: ported
    vars:
      param_1/r0: a
      param_2/r1: b
      param_3/r2: flags
    notes: >-
      Extent verified from raw bytes: exactly 40 bytes (10
      instructions), the next word at 0x08059b88 begins an unrelated
      body (ldr r3,[r0,#0x5c] counter bump). Both operands are counted
      UTF-16 strings: a u16 element count followed by that many
      elements. The wrapper reads both counts, then calls the counted
      wide-string compare FUN_0804529c @ 0x0804529c (396 bytes, not
      ported) as compare(a+1, a[0], b+1, b[0], flags) with flags as the
      fifth AAPCS stack argument (the original's str r2,[sp] over the
      pushed r3 slot; the callee reads it at [sp,#0x40] after its
      9-reg push and 28-byte frame). rsbs r0,r0,#1 + movcc r0,#0 maps
      the tri-state to strict equality: 1 iff compare returned exactly
      0, else 0 (negatives are huge unsigned, so they map to 0 too).
      Call count verified by decoding every ARM B/BL word in osos.dec:
      14 direct calls, all unconditional bl, zero predicated forms.
      Callee semantics from its raw bytes: flags bit 0 skips leading
      elements <= 0x20 on both sides and folds a..z to uppercase, bit 2
      routes straight to the generic collate engine at 0x08045c24, bit
      3 is forwarded to it, bit 4 bails to it when both current
      elements are ASCII digits, and any element > 0x7a or with a zero
      weight-table entry also bails; all observed callers pass flags =
      0. Deliberate deviation: 0x0804529c is not ported, so its call is
      an explicit COUNTED_WSTR_COMPARE dispatch seam (debug_printf
      precedent) - target builds transmute 0x0804529c, host default is
      a placeholder returning 1 and tests substitute a recorder.
      match.py --lib <worktree archive>: 16 vs 10 instructions; same
      ldrh header loads, stack-passed flags and 0/1 result mapping,
      deltas are the seam blx indirection, the fp frame, and clz/lsr
      instead of rsbs/movcc for the equality map.

  - address: 0x08053bd0
    ghidra: FUN_08053bd0
    name: ui_plst_next
    size: 80
    sig: "u32 ui_plst_next(const u8 *element)"
    module: ui/plst_next
    status: ported
    renames:
      param_1/r0: element
      param_1_plus_0x24: successor
      param_1_plus_0x14: linked
      param_1_plus_0x10: chain_node
      FUN_080613e0: ui_element_is_plst_class
    notes: >-
      Successor selector for the linked 'plst' sequence hanging off a
      'tdat' UI element (first link: ui_tdat_first_plst 0x080522d4 at
      element+0x34). Validates the element with the already-ported
      'plst' class predicate, then picks in strict order: the word at
      +0x24, else the word at +0x14, else the first nonzero +0x14 word
      along the auxiliary chain of link nodes starting at +0x10 (each
      node: candidate at +0x14, next node at +0x10); returns 0 when the
      class check fails or no successor exists. This is the iteration
      step of the sequence walk in callers 0x08050ae0, 0x08066854,
      0x08054724 and 0x080561b4, and of the plst teardown in 0x0806a994
      (saves the successor before destroying each element). Extent
      verified from raw bytes: exactly 80 bytes; the sibling function's
      push {r4-r8,lr} prologue starts at 0x08053c20. Call count
      verified by decoding every B/BL word in osos.dec: 14
      unconditional bl (0x08050b24, 0x08054750, 0x080561e0, 0x0806126c,
      0x08066890, 0x08066b2c, 0x080689e8, 0x0806a9d0, 0x0806aa28,
      0x0806c1e4, 0x0806c2fc, 0x0806c348, 0x080ca77c, 0x080ce5f4), zero
      predicated - the internal class guard is what every caller relies
      on. No data word in osos references 0x08053bd0 (never dispatched
      virtually). Ghidra's decompiled C wrongly declares a void return
      and drops the result; every caller depends on r0. Deviation: the
      port calls ui_element_is_plst_class directly instead of branching
      to stock 0x080613e0. match.py --lib <worktree archive>: 17 vs 20
      instructions; same guard order and +0x24/+0x14/+0x10 selection
      chain, deltas are the crate fp frame, the intra-crate bl to the
      ported predicate, and LLVM's unpredicated reload of +0x14 vs ADS
      ldreq/cmpeq/popne predication.

  # --- Filesystem mapped allocation-bitmap block completion ---
  - address: 0x0806448c
    ghidra: FUN_0806448c
    name: mapped_block_window_finish
    size: 84
    sig: "i32 mapped_block_window_finish(void)"
    module: fs/block_window
    status: ported
    renames:
      DAT_080644e0/r4: mapped_block_window (fixed object at 0x08adc510)
      DAT_080644e0_plus_0x200: block_number
      DAT_080644e0_plus_0x204: owner
      DAT_080644e0_plus_0x208: dirty
      DAT_080644e0_plus_0x209: mapped
      FUN_08149e10: mapped_block_finish_vtable_slot3_thunk
      iVar1/r1/r0: callback_status
    notes: >-
      Raw decoding gives exactly 21 instructions (84 bytes) from
      0x0806448c through 0x080644dc; the following word at 0x080644e0 is
      the 0x08adc510 global literal and 0x080644e4 starts the next function.
      It follows owner+0x15c, then +0x08, then +0x04; thunk 0x08149e10
      follows one more +0x04 handle word and calls vtable slot 3 with
      (block_number, 1, window, 0). The callback result is truncated to i16
      and sign-extended after all bookkeeping is cleared: block number and
      owner words, then dirty and mapped bytes. The begin sibling
      0x08051578 records the same owner/block pair, and 0x0805cdd0 sets the
      dirty byte, supporting the mapped-block-window name without inventing
      the concrete owner type. Decoding every ARM B/BL word in osos.dec finds
      14 direct callers: 10 unconditional bl and 4 blne (0x08042648,
      0x080429c4, 0x08042b3c, 0x08042cac); no internal NULL guard exists, so
      those predicated callers materially protect their own teardown paths.
      Target code performs the recovered dynamic vtable load directly rather
      than branching through unported thunk 0x08149e10; host tests substitute
      only that dynamic call because target-width u32 vtable entries cannot
      hold native host function pointers. match.py --lib <worktree archive>:
      28 vs 21 instructions; the full owner/link/vtable-slot-3 chain and four
      clears remain, while the expected differences are the direct thunk
      expansion, LLVM frame, literal placement, and sign extension scheduled
      before the clears.

  # --- Generic descriptor value wrapper ---
  - address: 0x0803b468
    ghidra: FUN_0803b468
    name: parameter_descriptor_value
    size: 44
    sig: "u32 parameter_descriptor_value(const u8 *descriptor) [r1/r2 ignored; incoming r3 forwarded]"
    module: runtime/parameter_descriptor_value
    status: ported
    renames:
      param_1/r0: descriptor
      local_8: value
      iVar1/r0: resolver_status
      r3: opaque_context
      FUN_080d3a78: parameter_descriptor_resolve
    notes: >-
      Raw decoding gives exactly 44 bytes from 0x0803b468 through
      0x0803b490; the next sibling starts at 0x0803b494 with
      push {r4-r9,lr}. The wrapper clears its stack output word, invokes
      the unported generic descriptor resolver with that word, descriptor,
      and NULL r2 callback, then returns the output only when the resolver
      status is signed-positive; zero and every negative status return zero,
      even after an output store. r3 is deliberately not written before the
      call, so the incoming live r3 is forwarded as the resolver's fourth
      argument. Decoding every ARM B/BL word in osos.dec finds exactly 14
      call sites, all unconditional plain bl and none predicated:
      0x08065a5c, 0x08065aa4, 0x0806f0bc, 0x08078800, 0x0808a170,
      0x0808a18c, 0x0808a210, 0x0808a288, 0x0808a89c, 0x0808a928,
      0x080c5960, 0x080d7dc4, 0x080f0108, and 0x080f013c. The target
      resolver at 0x080d3a78 is not ported, so release builds make its
      direct firmware call; the test-only callback seam is the deliberate
      deviation used to prove status gating and argument forwarding.
      match.py --lib <worktree archive>: 17 versus 11 instructions; both
      preserve r1=descriptor, r2=NULL, the untouched r3 fourth argument,
      signed status gate, and zero fallback. LLVM adds a frame, literal-load
      plus blx direct-target call, and an unconditional output reload.

  # --- Allocation-bitmap mapped block acquisition ---
  - address: 0x08063918
    ghidra: FUN_08063918
    name: allocation_bitmap_block_map
    size: 128
    sig: "i32 allocation_bitmap_block_map(MappedBlockWindowOwner *owner, u32 bit_index, MappedBlockWindow **out_window)"
    module: fs/block_window
    status: ported
    renames:
      param_1/r0: owner
      param_2/r1: bit_index
      param_3/r2: out_window
      param_1_plus_0x2: owner_kind
      param_1_plus_0x10: direct_block_base
      param_1_plus_0x15c: dispatch_context
      local_10: block_number
      FUN_0805cb18: allocation_bitmap_block_index_resolve
      FUN_08051578: mapped_block_window_begin
    notes: >-
      Raw decoding gives exactly 128 bytes (32 ARM instructions) from
      0x08063918 through 0x08063994; 0x08063998 begins the distinct next
      function with push {r0-r12,lr}. The 0x4244 owner kind reads the signed
      i16 direct block base at +0x10 and adds bit_index >> 12. Other kinds
      call 0x0805cb18 with owner, the word at *(owner+0x15c), request kind 1,
      two stack scratch pointers, byte index bit_index >> 3, and zero; a
      nonzero resolver status returns immediately. On success both paths call
      0x08051578 as (0, block_number, out_window, 0, owner), which begins the
      shared mapped-block window. Decoding every ARM B/BL word in osos.dec
      finds exactly 14 direct callers, all unconditional bl and none
      predicated: 0x080424b4, 0x0804253c, 0x080425dc, 0x080426d8,
      0x0804279c, 0x08042810, 0x080428d8, 0x08042990, 0x080429f4,
      0x08042a90, 0x08042b04, 0x08042b6c, 0x08042c04, and 0x08042c74.
      Neither direct callee is ported, so target code calls their stock
      addresses while host tests use explicit resolver and begin dispatch
      seams. match.py --lib <worktree archive>: 41 vs 32 instructions; the
      same kind branch, signed direct-base computation, resolver status
      return, and begin call remain. The additions are the target-address
      loads and blx seam calls plus LLVM's larger frame and split cleanup.


  # --- SQLite expression visitor ---
  - address: 0x0839866c
    ghidra: FUN_0839866c
    name: sqlite_expr_walk
    size: 140
    sig: "i32 sqlite_expr_walk(Expr *expr, ExprVisit visit, void *context)"
    module: sqlite/walk_expr
    status: ported
    renames:
      param_1/r0: expr
      param_2/r1: visit
      param_3/r2: context
      FUN_08398614: sqlite_expr_list_walk
    notes: >-
      Raw ARM confirms a 140-byte extent from 0x0839866c through
      0x083986f4; the separately linked next walker starts at 0x083986f8.
      This SQLite expression visitor invokes visit(context, expr) before
      descending through p_left (+0x08), p_right (+0x0c), then p_list
      (+0x10). A visitor status of zero descends; any nonzero signed status
      at most one prunes its descendants and returns success, while a status
      greater than one aborts with one. Any descendant abort propagates as
      one. A NULL expr returns zero without calling visit. Exactly 13 direct
      B/BL words target this entry: 12 unconditional bl and one blne at
      0x08383394, whose call is gated by the caller's condition. The
      distinct list helper FUN_08398614 is unported, so the target default
      calls its verified stock address while host tests install a recording
      volatile seam; names.yaml contains no ported entry for that helper.
      The raw body does not inspect Expr::p_select. match.py is structural:
      LLVM's recursive call and list-helper slot call need not reproduce ADS
      instruction selection exactly.


  # --- Four-slot disk block-read gate ---
  - address: 0x082c6244
    ghidra: FUN_082c6244
    name: disk_block_read
    size: 120
    sig: "u32 disk_block_read(u32 device, u32 block_index, void *destination, u32 block_count, u32 wait_for_ready)"
    module: fs/disk_block
    status: ported
    renames:
      param_1/r0: device
      param_2/r1: block_index
      param_3/r2: destination
      param_4/r3: block_count
      param_5/[sp]: wait_for_ready
      uVar2: valid_device
      iVar1: dispatch_result
    notes: >-
      Raw ARM confirms the 120-byte extent from 0x082c6244 through
      0x082c62b8; the separately linked next function opens at 0x082c62bc.
      Exactly nine direct B/BL words target this entry, all unconditional bl
      (0x082b1b48, 0x082c5170, 0x082d0a50, 0x082e1540, 0x082e1ac4,
      0x082e1c58, 0x082e3e2c, 0x082e3f50, and 0x082e3ff0); there are no
      predicated calls or tail branches. The body calls unported stock helper
      0x082c3174 with (device, wait_for_ready) before it checks that device is
      below four. A false helper result, or device >= 4, returns zero without
      dispatch. Otherwise it calls unported wrapper 0x083653cc with
      (device, block_index, destination, block_count, 1) and normalizes any
      nonzero result to one. The direct callee identities are not asserted
      beyond their observed readiness-gate and dispatch roles. Target code
      invokes their verified retailOS addresses; host tests install recording
      seams for both.

  # --- Filesystem cache-backed block acquisition ---
  - address: 0x082e3f98
    ghidra: FUN_082e3f98
    name: cache_block_acquire
    size: 128
    sig: "u8 *cache_block_acquire(CacheContext *context, u32 block_index, u32 ignored, u32 cache_key)"
    module: fs/cache_block
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: block_index
      param_3/r2: ignored
      param_4/r3: cache_key
      local_14: entry
      iVar2: cache_hit_or_read_result
      FUN_082dfdf0: cache_entry_allocate
      FUN_082c6244: disk_block_read
      FUN_082e18bc: cache_entry_release
    notes: >-
      Raw ARM confirms the 128-byte extent from 0x082e3f98 through
      0x082e4014; the separately entered next function begins at 0x082e4018.
      A NULL context or block_index greater than or equal to the context's
      +0x1d8 exclusive limit returns NULL without allocation. The incoming r2
      is overwritten before the allocator call, while r3 is retained in the
      pushed stack slot and forwarded as the cache key. A cache hit returns
      its retained descriptor; a miss reads one block from the context's
      +0x78 device into descriptor +0x18. A zero read result detaches and
      releases the descriptor, then returns NULL. The allocator and disk-read
      targets are not ported, so target code calls their verified retailOS
      addresses and host tests install recording seams. The already ported
      cache_entry_release replaces its retailOS address deliberately. Exactly
      13 direct B/BL words target this entry, all unconditional bl: no
      predicated forms and no tail branches. Host tests cover NULL and
      exclusive-limit rejection, cache-hit bypass, miss read arguments, and
      failed-miss detach/release.

  # --- Filesystem cache-block slot preparation ---
  - address: 0x082e48bc
    ghidra: FUN_082e48bc
    name: cache_block_prepare
    size: 180
    sig: "u32 cache_block_prepare(CacheBlockPrepareRequest *request, u32 set_dirty, u32 set_timestamp)"
    module: fs/cache_block_prepare
    status: ported
    renames:
      param_1/r0: request
      param_2/r1: set_dirty
      param_3/r2: set_timestamp
      uVar3: slot
      iVar1: entry
      iVar2: flush_result
      local_18: timestamp_halves
      FUN_082d7924: cache_lock_wait
      FUN_082e2264: timestamp_halves
      FUN_082d7944: cache_lock_signal
      FUN_082e3f98: cache_block_acquire
      FUN_082e26e8: copy_cache_header
      FUN_082e4b4c: cache_entry_flush
      FUN_082e18bc: cache_entry_release
    notes: >-
      Raw ARM confirms the 180-byte extent from 0x082e48bc through
      0x082e4968; the separately entered next function begins at 0x082e4970.
      Exactly nine direct ARM B/BL words target this entry, all unconditional
      bl: 0x082b1750, 0x082e33f8, 0x082e3654, 0x082e4280, 0x082e45dc,
      0x082e468c, 0x082e61f0, 0x082e649c, and 0x082e6750. There are no
      predicated calls or tail branches, so callers do not flag-gate this
      operation. A request slot at +0x10 greater than or equal to 16 returns
      zero before touching the cache lock or request output. Otherwise the
      function brackets the optional dirty-bit (+0x0b bit 5) and timestamp
      writes (+0x16 low half, +0x18 high half) with the cache lock; it
      acquires request context/block words +0x00/+0x0c, stores the entry in
      request +0x18, copies request header +0x04 into entry
      +0x18 + slot * 32, flushes it, then releases it with detach_owner when
      flush returns zero. FUN_082e2264 and FUN_082e26e8 remain unported, so
      target builds call their verified addresses and host tests install
      recording seams. The raw body never reloads input r3 after saving it
      as the stack timestamp scratch word; the cache-acquire r2/r3 slots are
      caller-clobbered after the lock boundary. The port deliberately passes
      zero for those unspecified slots. Existing ports replace the remaining
      helpers. Host coverage exercises the early slot rejection, a NULL
      acquire after dirty/timestamp mutation, successful slot placement,
      flush return propagation, and reference release.

  # --- Filesystem cache-entry writeback ---
  - address: 0x082e4b4c
    ghidra: FUN_082e4b4c
    name: cache_entry_flush
    size: 56
    sig: "u32 cache_entry_flush(CacheEntry *entry)"
    module: fs/cache_entry_flush
    status: ported
    renames:
      param_1/r0: entry
      iVar1/r0: context
      FUN_082c62f0: disk_block_write
    notes: >-
      Raw ARM confirms the 56-byte extent from 0x082e4b4c through 0x082e4b80;
      the separately entered next function begins at 0x082e4b84. The movs r2,
      r0 NULL guard returns zero for a NULL entry. A non-NULL entry with a
      NULL word +0x08 context link also returns zero. Otherwise it reads the
      context +0x78 low halfword device selector, entry +0x0c block index, and
      entry +0x18 payload, then calls the verified but unported
      FUN_082c62f0(device, block_index, payload, 1, 0). The callee result
      remains in r0 after bl, so this port returns it despite Ghidra's
      incorrect void signature. Exactly nine direct B/BL words target this
      entry: all are unconditional bl at 0x082b1e38, 0x082e048c, 0x082e0574,
      0x082e05d4, 0x082e28b8, 0x082e33b8, 0x082e441c, 0x082e4494, and
      0x082e4948; no predicated forms or tail branches exist. The disk writer
      has no ported names.yaml entry, so target builds call its verified
      retailOS address while host tests install a recording seam. Tests cover
      both NULL exits, low-halfword device selection, argument forwarding, and
      return propagation.

  # --- Filesystem cache-page halfword transfer ---
  - address: 0x082e1a34
    ghidra: FUN_082e1a34
    name: cache_position_halfword_access
    size: 68
    sig: "u32 cache_position_halfword_access(CacheContext *cache, u32 position, u16 *value, u32 write)"
    module: fs/cache_page
    status: ported
    renames:
      param_1/r0: cache
      param_2/r1: position
      param_3/r2: value
      param_4/r3: write
      FUN_082e3d78: cache_page_resolve
    notes: >-
      Raw ARM confirms the 68-byte extent from 0x082e1a34 through
      0x082e1a74; the next separately linked function begins at 0x082e1a78.
      Every ARM B/BL word in osos.dec was decoded: exactly 10 direct call
      sites, all unconditional plain bl at 0x082e0cf0, 0x082e0d40,
      0x082e0d74, 0x082e0dec, 0x082e3a34, 0x082e3aac, 0x082e3ac8,
      0x082e3ae0, 0x082e3b0c, and 0x082e3b28; there are no predicated forms
      or tail branches. It preserves value and write across a call to the
      unported cache-page resolver at 0x082e3d78 as (cache, position, write).
      A NULL result returns zero without dereferencing value. Otherwise,
      position & 0xff indexes an aligned u16 within the returned 512-byte
      page; write == 0 reads that halfword to value and any nonzero write
      stores value into it, returning one. The resolver is not present in
      names.yaml as ported, so target builds call its retailOS load address
      directly and host tests install a recording resolver seam. This is the
      sole deliberate deviation; caching and dirty-page bookkeeping remain
      resolver-owned rather than inferred by this wrapper.

  # --- Path-resolution utility byte fill ---
  - address: 0x082e2ef0
    ghidra: FUN_082e2ef0
    name: byte_fill
    size: 20
    sig: "u8 *byte_fill(u8 *dst, u32 len, u8 value)"
    module: libc/byte_fill
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: len
      param_3/r2: value
    notes: >-
      Raw ARM establishes the 20-byte extent: the loop runs from
      0x082e2ef0 through its bx lr at 0x082e2f00, and the next distinct
      function begins at 0x082e2f04. Decoding every ARM B/BL word in
      osos.dec finds 13 direct callers, all plain unconditional bl; no
      predicated bl forms target this entry. The bne at 0x082e2efc is the
      function's own loop back-edge, not a call. It pre-decrements the
      32-bit length, byte-stores r2 through post-incremented r0 until the
      counter reaches -1, and therefore leaves dst + len in r0; zero length
      dereferences nothing. Ghidra's void signature misses that observable
      r0 result. No deliberate deviations: volatile stores only prevent LLVM
      from lowering the freestanding byte loop to an unavailable memset
      libcall.

  # --- StringObject concatenation helper ---
  - address: 0x082aade4
    ghidra: FUN_082aade4
    name: string_object_concatenate
    size: 56
    sig: "void string_object_concatenate(StringObject *out, const StringObject *first, const StringObject *second)"
    module: cxx/string_object
    status: ported
    renames:
      param_1/r0: out
      param_2/r1: first
      param_3/r2: second
      local_stack: temporary
      FUN_082773e0: string_object_copy_construct
      FUN_082774c8: string_object_append
      FUN_08277484: string_object_destroy
    notes: >-
      Raw ARM confirms the exact 56-byte extent from the push at 0x082aade4
      through the pop at 0x082aae18; 0x082aae1c is the next separately
      linked function and there is no literal pool. Decoding every B/BL
      instruction in osos.dec finds exactly 13 direct callers, all
      unconditional bl (zero predicated forms) and no tail branches:
      0x081041bc, 0x081041f4, 0x08104354, 0x08104634, 0x08104718,
      0x08104728, 0x08118038, 0x08118690, 0x0815e558, 0x08173c64,
      0x08173c74, 0x081d12b4, 0x081d12c4. It copy-constructs an 8-byte
      stack StringObject from first, appends second, copy-constructs out
      from that temporary, then destroys the temporary. The nominal return
      is void; callers ignore r0, which happens to retain the destructor's
      temporary pointer. All three direct callees are already ported, so
      there is no dispatch seam or deliberate deviation. Host tests prove
      the left-plus-right result, preserving-allocation shape, source
      immutability, output construction, and exactly one temporary destroy.

  # --- Application boot metrics submission ---
  - address: 0x082664b4
    ghidra: FUN_082664b4
    name: app_boot_metrics_submit
    size: 116
    sig: "void app_boot_metrics_submit(void *channel, u32 event_id, u32 value)"
    module: app/app_boot_metrics_submit
    status: ported
    renames:
      param_1/r0: channel
      param_2/r1: event_id
      param_3/r2: value
      local_10: record
      "channel+0x18": accepting_gate
      "channel+0x19": enabled_gate
      "record+0x08": initializer_result
      FUN_0815a554: record_initializer_unidentified
      FUN_082aadd4: operator_new
      FUN_082aad24: operator_delete
      vtable_plus_0x1c: record_slot_dispatch_unidentified
    notes: >-
      Raw ARM establishes the exact 116-byte extent
      0x082664b4..0x08266524; the next separately linked function starts at
      0x08266528. An independent scan of every ARM B/BL word in osos.dec
      finds exactly 13 incoming BL calls, all unconditional (cond=AL), and
      zero predicated BL forms. The function gates first on channel+0x19,
      then channel+0x18; it then allocates a 12-byte record with the ported
      operator_new, calls unported FUN_0815a554, and accepts when record+8 is
      nonzero OR event_id is inclusively 0x16..0x21. Acceptance stores
      event_id/value at +0/+4, places the record pointer in a stack slot, and
      BLX-dispatches channel's vtable slot +0x1c as (channel, &record).
      Otherwise it restores its frame and conditional-tail-branches to the
      ported operator_delete. FUN_0815a554 is only known to zero +0/+4, call
      FUN_0815a50c, store its answer at +8, and return its record; neither it
      nor the virtual target is assigned an invented identity. Target Rust
      calls 0x0815a554 directly and reads the physical 32-bit vtable slot;
      host tests use its deterministic zeroing seam and a pointer-width
      fixture to avoid 64-bit offset/pointer truncation. Rust uses a normal
      call rather than the final conditional tail branch to operator_delete.

  # --- Lazy shared-handle manager singleton ---
  - address: 0x081bbf98
    ghidra: FUN_081bbf98
    name: lazy_handle_manager_get
    size: 88
    sig: "LazyHandleManager *lazy_handle_manager_get(void)"
    module: app/lazy_handle_manager
    status: ported
    renames:
      DAT_081bbfe0: lazy_handle_manager_guard
      DAT_081bbfe4: lazy_handle_manager
      DAT_081bbfe8: __dso_handle
      DAT_081bbfec: lazy_handle_manager_destructor_unidentified
      FUN_081bc0c4: lazy_handle_manager_construct_unported
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab1c8: cxa_atexit
      FUN_082ab338: cxa_guard_release
    notes: >-
      True raw extent is 88 bytes: 72 instruction bytes at
      0x081bbf98..0x081bbfe0 plus the four-word literal pool through
      0x081bbff0; the separately linked next function opens at 0x081bbff0.
      Ghidra's stated 72-byte size excludes the pool. Decoding every ARM B/BL
      word in osos.dec finds exactly 13 direct callers: all are unconditional
      plain bl, with zero predicated forms and zero tail b sites. The accessor
      tests bit 0 of guard 0x089cc9fc, calls cxa_guard_acquire, invokes the
      unported manager constructor 0x081bc0c4 on the fixed 16-byte object
      0x08ac8890, registers the constructor return with cxa_atexit using
      handler 0x081b1210 and __dso_handle 0x089ca09c, releases the guard, and
      reloads/returns the fixed object rather than the constructor result. The
      handler is a real raw ARM entry (starts mov ip,r0) but has no assigned
      identity; target Rust dispatches to its verified address and host uses a
      no-op only for shutdown-chain test teardown. Target Rust likewise
      dispatches the unported constructor to its verified address; host tests
      substitute a recording constructor. This makes the accessor hook-ready
      on target without inventing either callee.

  # --- Media-player interface slot +0x11c dispatcher ---
  - address: 0x081f9d78
    ghidra: FUN_081f9d78
    name: media_player_interface_slot_11c
    size: 24
    sig: "u32 media_player_interface_slot_11c(void)"
    module: app/media_player_interface_slot_11c
    status: ported
    renames:
      FUN_08259594: media_player_interface_get
      "vtable+0x11c": media_player_state_query_unidentified
      r0: media_player_interface_then_slot_result
      r1: media_player_interface_vtable_then_slot
    notes: >-
      Raw ARM proves the 24-byte extent: push {r4,lr}, BL
      media_player_interface_get, load the returned interface's vtable word,
      load vtable +0x11c, restore r4/lr, then BX the loaded slot. The next
      separately linked function begins at 0x081f9d90 with
      strb r1,[r0,#0x90]; bx lr. Decoding every ARM B/BL word in osos.dec
      finds exactly 13 incoming direct calls, all unconditional BL (cond=AL),
      with zero predicated forms and zero plain-B tail sites. Every recovered
      caller uses the tail result as a state predicate, establishing an
      observable u32 result despite Ghidra's void signature. The incoming r0
      is discarded before the getter call; the slot receives only the global
      media-player interface. The +0x11c target is dynamic vtable data with
      no recoverable static identity, so no callee identity is invented.
      Target Rust preserves the direct getter BL and tail BX in assembly;
      host tests use a native-width structural vtable fixture because firmware
      vtable words are 32-bit pointers.

  # --- Context-scope assignment from selector ---
  - address: 0x08173878
    ghidra: FUN_08173878
    name: context_scope_assign_selector
    size: 208
    sig: "void context_scope_assign_selector(void *scope, void *source, i32 selector)"
    module: app/context_scope_selector
    status: ported
    renames:
      param_1: scope
      param_2: source
      param_3: selector
      FUN_08177604: selector_map_refresh_unported
      FUN_08284118: context_scope_init_from_handle
      FUN_0828414c: context_scope_assign
      FUN_08284188: context_scope_drop
      FUN_083d604c: handle_deref_or_null
      FUN_083d7a48: vector_size_elem4_alias_7a48
      "vtable+0x1bc": current_selector_unidentified
      "vtable+0x1c0": make_context_handle_unidentified
    notes: >-
      Raw ARM establishes the 208-byte extent exactly:
      0x08173878..0x08173944 are instructions and the next separately
      entered function begins at 0x08173948 with push {r4-r11,lr}; there
      is no literal pool. Decoding every ARM B/BL word in osos.dec finds
      exactly 13 direct incoming calls, all unconditional plain BL
      (cond=AL), with zero predicated forms and zero plain-B tail callers:
      0x0817697c, 0x08176ab4, 0x08176e1c, 0x08232738, 0x08232844,
      0x08232a8c, 0x08232c20, 0x08232cc4, 0x082375ec, 0x082376e0,
      0x08237744, 0x0823787c, and 0x08239b4c. Selector -1 tail-calls
      context_scope_init(scope, NULL, 0). Otherwise source+0x18 is
      unwrapped; when byte source+0x84c is set, virtual slot +0x1bc is
      compared with vector_size(source+0x840), a mismatch calls the
      unported map refresher 0x08177604, the handle is reloaded, and
      vector[selector] becomes the argument to virtual slot +0x1c0.
      The 16-byte slot result initializes a stack context scope that is
      assigned into scope, then receives its empty destructor. The slots
      have no recoverable concrete identity, so the port names only their
      verified roles. On host, their 32-bit target vtable words become a
      native-width structural fixture and only 0x08177604 is a volatile
      direct-call seam; target builds invoke that verified address. The
      ordinary selector path leaves scope+0x00 and the upper three bytes of +0x10
      unchanged, as context_scope_assign writes only its payload fields
      and flag byte. Its ARM r0 is the dead stack temporary after drop,
      while the -1 tail path returns scope; every caller discards it, so
      Rust deliberately exports void rather than a dangling result.
  - address: 0x08054664
    ghidra: FUN_08054664
    name: ui_plst_slot_item_position
    size: 172
    sig: "u32 ui_plst_slot_item_position(u8 *handle, u32 selector, u32 reverse_flag)"
    module: ui/plst_slot_position
    status: ported
    renames:
      param_1/r0: handle
      param_2/r1: selector
      param_3/r2: reverse_flag
      FUN_080b48dc: normalize_selector_unported
      FUN_080df1b8: materialize_slot_unported
      FUN_080ca00c: build_position_map_unported
      FUN_080daad4: slot_index_of_unported
    notes: >-
      Reverse slot lookup on the same element family ui/plst_slot_item
      (0x08052728) reads forward: where that port maps (selector, index)
      to an item word, this one maps (selector, item word) back to its
      index. The handle is a two-word object {element @ +0x0, peer @
      +0x4}; the peer is only NULL-gated, never dereferenced. Guard chain
      (handle, element, peer, selector all non-NULL) is entirely
      internal, matching the verified call profile: decoding every B/BL
      word in osos.dec finds exactly 13 incoming calls, all unconditional
      plain bl, 0 predicated. Ghidra's 172-byte extent is exact (the next
      function's body starts at 0x08054710). The element carries a SECOND
      49-word table at +0x470 (0x3ac + 49*4), a per-selector cache of
      sorted key->index maps, parallel to the +0x3ac slot table. Selector
      and flag-byte pass through the shared stock normalizer 0x080b48dc
      by pointer. On a cache miss with normalized selector in 1..=48 the
      slot is materialized via 0x80df1b8 and, ONLY when that returns 0
      (unlike the sibling, this caller checks), the map is built from the
      slot via 0x080ca00c (alloc 16+count*8, {key = slot item word @
      +0x10+i*4, value = i} pairs, count @ +4, sort by key via 0x8061ecc)
      and cached. The cache word is then re-read; a non-NULL map is
      searched via 0x80daad4 (binary search + linear fallback, -1 on
      miss, reverse-adjusts a hit to count-index-1 when the flag byte is
      nonzero) with the handle ITSELF as key. All failure paths return
      -1 (0xffffffff). All four callees are unported and sit behind the
      PLST_SLOT_POSITION_OPS seam (stock addresses on target; host
      defaults identity-normalize / never-materialize / panic-on-reach);
      the normalize/materialize fn-pointer types are reused from
      ui/plst_slot_item. The handle's pointer fields are read as u32
      target words and widened (native-width reads faulted the first
      host-test run). Host fixture hint PLST_SLOT_POSITION 0xd3000000,
      far clear of the sequential run. match.py structural: LLVM fp
      frame, ldrd/blx dispatch of the ops quartet, and the sel-1 u< 49
      lowering of the sel!=0 && sel<49 bound; guard chain, +0x470/+0x3ac
      addressing, materialize-return check, cache store/re-read, and the
      handle-as-key search all match the original's structure.
  - address: 0x080547b0
    ghidra: FUN_080547b0
    name: firmware_query_low_byte
    size: 16
    sig: "u8 firmware_query_low_byte(void)"
    module: app/firmware_query_low_byte
    status: ported
    renames:
      FUN_080547b0: firmware_query_low_byte
      FUN_080b4eb4: unported_firmware_query
    notes: >-
      Raw osos.dec words e92d4010 eb0181be e20000ff e8bd8010 establish the
      whole 16-byte body: push {r4,lr}; bl 0x080b4eb4; and r0,r0,#0xff;
      pop {r4,pc}. The following push {r4-r9,sl,lr} at 0x080547c0 begins
      the distinct next function, so Ghidra's extent is exact. Decoding
      every ARM B/BL word in osos.dec finds exactly ten direct incoming
      calls, all unconditional bl (cond=AL): 0x08058860, 0x0812f2cc,
      0x081a5ec4, 0x081a5fe0, 0x081a62c8, 0x081a6724, 0x081a6f74,
      0x0825b8e4, 0x082a1ad4, and 0x082ca3f4; there are no predicated bl
      or direct-b tail callers. The sole callee 0x080b4eb4 has no recovered
      identity and is not already ported, so target builds dispatch there
      directly and host tests expose only a replacement seam. Its raw body
      returns zero or one, but this wrapper preserves the explicit low-byte
      mask for arbitrary r0 values.
  - address: 0x083d63cc
    ghidra: FUN_083d63cc
    name: wang_hash_64_to_32
    size: 148
    sig: "u32 wang_hash_64_to_32(u32 dead0, u32 dead1, u32 key_lo, u32 key_hi)"
    module: util/wang_hash
    status: ported
    renames:
      param_3/r2: key_lo
      param_4/r3: key_hi
    notes: >-
      Thomas Wang's 64->32 integer hash (inthash.htm), instruction-exact
      semantics: key += ~(key << 32); key ^= key >> 22; key += ~(key << 13);
      key ^= key >> 8; key += key << 3 (x9); key ^= key >> 15; key += ~(key
      << 27); key ^= key >> 31; return low 32 bits. Pure leaf mixer for a
      hash table with 64-bit keys: every caller feeds the result through
      __rt_udiv (0x08036f14) by the bucket count. Verified 12 bl call sites,
      all unconditional (cond=e), by decoding every B/BL word in osos.dec;
      Ghidra agrees. Ghidra's 148-byte extent is exact (next function starts
      at 0x083d6460). ABI quirk: callers pass four register args, but the
      first instruction overwrites r0 and r1 is never read -- only r2/r3
      (key low/high) are inputs; the port keeps two dead leading parameters
      to match the retail calling convention. The u64 formula was proven
      equal to a word-level simulation of the exact adds/adc carry-chained
      instruction stream on 20000 random inputs; host tests pin golden
      vectors from that simulation plus edge cases (single bits, holes,
      carry boundaries, 10000-key deterministic sweep). match.py structural:
      37 instructions both sides; LLVM frame-pointer prologue (crate-wide),
      subs+adc instead of mvn#0+adds+mvn (carry-equivalent: borrow of r2-1
      == no-carry of 0xffffffff+r2), and umull for the *9 where ADS
      shift-adds; all mixing blocks and xor-shift pairs line up.
  - address: 0x082b4964
    ghidra: FUN_082b4964
    name: cg_two_bit_code_or_zero
    size: 40
    sig: "u32 cg_two_bit_code_or_zero(u32 code)"
    module: codegen/two_bit_code
    status: ported
    renames:
      param_1/r0: code
    notes: >-
      Leaf normalizer for a code-generator field subsequently packed into
      bits 5..6: preserve exactly the nonzero two-bit codes 1, 2, and 3;
      return zero for zero and every other u32 representation. Raw ARM
      decoding confirms Ghidra's 40-byte extent: the next function begins
      at 0x082b498c. Decoding every ARM B/BL word in osos.dec finds exactly
      12 incoming plain bl calls, all unconditional (cond=e), at
      0x082cb61c, 0x082cb6ac, 0x082cb868, 0x082cb940, 0x082cb998,
      0x082cb9f0, 0x082cbd00, 0x082cbde4, 0x082cc684, 0x082cc718,
      0x082cc758, and 0x082cc79c; no predicated calls. The original
      compares in the sequence zero, one, two, three and returns early for
      each valid nonzero code. Rust's inclusive unsigned bound has identical
      C-ABI input/output behavior; no deliberate deviations. Host tests
      cover every u8 input plus u32::MAX, including the sign-extended -1
      representation passed by byte-field callers.

  # --- opaque collection indexed accessor ---
  - address: 0x08299d18
    ghidra: FUN_08299d18
    name: opaque_collection_item_at
    size: 8
    sig: "void *opaque_collection_item_at(void *collection, usize index)"
    module: app/opaque_collection_item_at
    status: ported
    renames:
      param_1/r0: collection
      param_2/r1: index
      "collection+0x4": embedded_container
    notes: >-
      Raw ARM extent is exactly 8 bytes, 0x08299d18..0x08299d20: `add
      r0,r0,#4; b 0x083d5fbc`; the separately entered next function begins
      at 0x08299d20. The tail target is a real 24-byte
      container_element_at instantiation (`push {r4,lr}; ldr r2,[r0]; ldr
      r2,[r2,#0x40]; blx r2; ldr r0,[r0]; pop {r4,pc}`), so this is not a
      veneer: it forwards indexed lookup to the C++ container embedded at
      target word 1. Decoding every ARM B/BL immediate in osos.dec found
      exactly eight incoming plain unconditional bl calls, at 0x08126c98,
      0x081277a4, 0x08299a08, 0x08299ab0, 0x08299b54, 0x08299bac,
      0x08299c30, and 0x08299cc0; no predicated calls. Callers use this in
      indexed loops alongside opaque_collection_item_count at 0x08299d78.
      No target-side behavioral deviation: Rust advances one target-width
      word and calls the existing container_element_at port. The host-only
      implementation uses unaligned reads after that four-byte advance,
      because host vtable pointers are eight bytes; this preserves the
      firmware object's target layout. Host test constructs that exact
      packed layout, proves the second word is selected, passes index 1 to
      the virtual slot-0x40 method, and returns a NULL element unchanged.

  # --- opaque collection item-count accessor ---
  - address: 0x08299d78
    ghidra: FUN_08299d78
    name: opaque_collection_item_count
    size: 8
    sig: "u32 opaque_collection_item_count(const void *collection)"
    module: app/opaque_collection_item_count
    status: ported
    renames:
      param_1/r0: collection
      "collection+0x8": item_count
    notes: >-
      True raw extent is exactly 8 bytes, 0x08299d78..0x08299d80: `ldr
      r0,[r0,#8]; bx lr`; the next function starts at 0x08299d80. It returns
      the aligned raw u32 at collection+8, with no NULL guard, validation, or
      writes. Decoding every ARM B/BL immediate in osos.dec found exactly 12
      incoming plain unconditional bl calls, at 0x08126c84, 0x08127be8,
      0x08127d00, 0x08127d48, 0x08127d78, 0x08127f50, 0x08299728,
      0x08299974, 0x082999a0, 0x082999b4, 0x082999f0, and 0x08299a98; no
      predicated bl calls. There is also one non-call tail `bne` at
      0x082996f8, following an owner+0xbc NULL check, and no literal
      data-word reference to 0x08299d78. Direct callers use the value as a
      count, including item-loop bounds, while sibling indexed accessors
      operate on the same opaque object; its concrete class is not recovered.
      No deliberate behavioral deviation: the port retains the aligned word load and no
      NULL guard. Its private .text section prevents LLVM from folding this
      tiny body onto an identical accessor. Host tests cover ordinary, zero,
      high-bit, and u32::MAX values and prove no object word is written.
      match.py structural (exit 1, diff expected): the common ldr r0,[r0,#8]
      remains, with the crate-wide frame-pointer push/mov/pop replacing bx
      lr.

  # --- Four-word sentinel record initialization (util/four_word_sentinel_init) ---
  - address: 0x08287278
    ghidra: FUN_08287278
    name: four_word_sentinel_init
    size: 28
    sig: "void *four_word_sentinel_init(void *target)"
    module: util/four_word_sentinel_init
    status: ported
    renames:
      param_1/r0: target
      "target+0x0": first
      "target+0x4": second
      "target+0x8": third
      "target+0xc": invalid_sentinel
    notes: >-
      Raw ARM confirms the 28-byte extent exactly: four word stores plus bx lr
      from 0x08287278 through 0x08287290, with the separately linked next
      function opening at 0x08287294. It writes zero at target+0x0, +0x4, and
      +0x8, then 0xffffffff at target+0xc. The incoming target in r0 is never
      overwritten and returns to callers unchanged; the allocation call site
      at 0x08182cb8 proves this otherwise absent Ghidra return value. Decoding
      every ARM B/BL word in osos.dec finds exactly 12 direct calls, all plain
      unconditional bl: 0x08182cb8, 0x08184928, 0x08225464, 0x082290c0,
      0x082307f0, 0x0823199c, 0x08231a70, 0x08231b44, 0x08231c04,
      0x08232390, 0x08286b3c, and 0x08288d50; none are predicated and there
      are no direct tail branches. No NULL guard or reads. No deliberate
      deviations; the target-only unique text section prevents LLVM from
      folding this small body with a byte-identical port. Host tests prove all
      four stores, unchanged receiver return, and no neighboring-word writes.
      match.py structural (exit 1, diff expected): LLVM uses its standard
      frame-pointer prologue/epilogue, allocates r2 for the zero literal, and
      otherwise preserves the retail store order and receiver return.

  # --- Q16.16 point-coordinate store ---
  - address: 0x08280184
    ghidra: FUN_08280184
    name: fixed_point_set
    size: 8
    sig: "void fixed_point_set(FixedPoint *point, i32 x, i32 y)"
    module: app/fixed_point
    status: ported
    renames:
      param_1/r0: point
      param_2/r1: x
      param_3/r2: y
    notes: >-
      Raw ARM confirms the exact extent 0x08280184..0x0828018c: `stm
      r0,{r1,r2}; bx lr`; 0x0828018c is the separately linked three-word
      sibling. It writes the x and y Q16.16 coordinate words in ascending
      order with no reads, NULL check, or alignment guard. Decoding every ARM
      B/BL immediate in osos.dec finds exactly 12 direct calls, all plain
      unconditional bl: 0x08142e64, 0x08142e78, 0x08142e88, 0x08142e98,
      0x0828c8f8, 0x0828c934, 0x0828c970, 0x0828c9a8, 0x0828c9c4,
      0x0828c9d8, 0x0828c9e8, and 0x0828c9f8; none are predicated and there
      are no tail branches. The first caller builds the unit-square corners;
      the second builds two Q16.16 quads. No aligned image word contains
      0x08280184, so it is statically called, not vtable-dispatched. No
      deliberate behavioral deviations: the port uses the same two ordered
      field stores and its target-only text section keeps this tiny hook
      boundary distinct. Host tests cover signed/high-bit coordinate words,
      overwrite behavior, and untouched neighboring words.

  # --- 0xa0 lazy singleton getter (app/singletons) ---
  - address: 0x08259740
    ghidra: FUN_08259740
    name: lazy_singleton_0xa0
    size: 48
    sig: "void *lazy_singleton_0xa0(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0825976c: singleton_0xa0_globals (pool literal holding 0x089cc954)
      "singleton_0xa0_globals+0x4": singleton_0xa0_cache (runtime cache word 0x089cc958)
      FUN_082aadd4: operator_new (ported direct callee)
      FUN_08259278: singleton_0xa0_ctor (unported; SINGLETON_CTORS.singleton_0xa0 slot)
    notes: >-
      Raw ARM establishes a 48-byte true extent: 44 bytes of instructions
      from 0x08259740 through 0x08259768 plus pool word 0x089cc954 at
      0x0825976c; the separately linked sibling begins at 0x08259770.
      `push {r4,lr}; ldr r4,[pc,#32]; ldr r0,[r4,#4]; cmp r0,#0; bne;
      mov r0,#0xa0; bl operator_new; bl FUN_08259278; str r0,[r4,#4];
      ldr r0,[r4,#4]; pop {r4,pc}` returns the cached 0xa0-byte object,
      allocating and constructing it only when the cache is NULL. The
      post-construction reload is intentional: the constructor's return is
      stored unconditionally, and a NULL return retries on the next call.
      Full-image decode of every ARM B/BL word found exactly 12 direct call
      sites, all unconditional `bl` (zero predicated calls and zero direct
      plain-B tails): 0x081174b0, 0x0811755c, 0x081356fc, 0x0813ec34,
      0x08172190, 0x081737bc, 0x081c8f94, 0x081c9354, 0x081c939c,
      0x08258040, 0x08289874, and 0x082aa4cc. Callers use vtable slots but
      the constructor exposes no class-name literal or registry id, so the
      name remains allocation-extent based. Deliberate deviations: the
      runtime-initialized 0x089cc958 cache is represented by
      app/singletons.rs static SINGLETON_0XA0, and unported constructor
      FUN_08259278 is dispatched through SINGLETON_CTORS.singleton_0xa0
      with the established zeroing default. The getter is not hook-ready
      until that constructor is ported. Host tests prove the exact 0xa0
      allocation, raw-block constructor argument, cache fast path, and
      NULL-return retry behavior.

  # --- Four-component pixel average (util/four_component_pixel_average) ---
  - address: 0x0824bd98
    ghidra: FUN_0824bd98
    name: average_four_component_pixel
    size: 84
    sig: "void average_four_component_pixel(u8 *dst, const u8 *first, const u8 *second)"
    module: util/four_component_pixel_average
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: first
      param_3/r2: second
      FUN_0824bf5c: store_four_components (unported trivial store helper)
    notes: >-
      Raw ARM establishes the true 84-byte extent 0x0824bd98..0x0824bdec:
      Ghidra's 88-byte report absorbs the first instruction of the separately
      linked four-input sibling at 0x0824bdf0. The function loads all four
      bytes from first and second before its `bl 0x0824bf5c`, computes each
      `(first[i] + second[i]) >> 1` in u32 registers, then the helper stores
      the four components in ascending index order. This proves both floor
      rounding and that dst may alias either source; there is no NULL or
      alignment guard. Full-image decoding of every ARM B/BL immediate finds
      exactly 12 direct calls, all unconditional `bl`, at 0x08250e9c,
      0x08250f30, 0x08250fb8, 0x0825104c, 0x082510dc, 0x0825116c,
      0x082512a8, 0x08251334, 0x082513b4, 0x08251440, 0x082514c8, and
      0x08251550; there are zero predicated calls and zero direct plain-B
      tails. Deliberate deviation: the port retains every source byte, then
      stores the equivalent four results directly rather than adding a
      dispatch seam for the unported 24-byte helper FUN_0824bf5c. Host tests
      cover floor rounding at byte extremes, every 8-bit input pair, and dst
      aliasing each source. match.py against this worktree archive (exit 1,
      structural diff expected) finds the same source-load/floor-average/store
      structure; LLVM emits the independent stores in descending component
      order after all loads.

  # --- Fixed fourteen-byte copy helper (util/copy_fourteen_bytes) ---
  - address: 0x082231c0
    ghidra: FUN_082231c0
    name: copy_fourteen_bytes
    size: 36
    sig: "u8 *copy_fourteen_bytes(u8 *dst, const u8 *src)"
    module: util/copy_fourteen_bytes
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: src
      r2: initial_copy_length
      thunk_EXT_FUN_22000188: iram_memcpy_veneer
    notes: >-
      Raw osos.dec is exactly nine ARM instructions (36 bytes) from
      0x082231c0 through 0x082231e0; the next separately linked function
      opens at 0x082231e4. It copies the first 13 bytes from src to dst by
      calling the 0x08037df8 IRAM memcpy veneer, then copies byte 13 after
      that forward grouped copy completes and returns dst. Decoding every ARM
      B/BL immediate in the image finds exactly nine inbound direct bl calls,
      all unconditional and none predicated: 0x080f9d38, 0x080fa8d0,
      0x080fa9e8, 0x080fcc08, 0x080fcd60, 0x080fd28c, 0x080fd3c8,
      0x080fdf58, and 0x080fe004. No direct plain-B tail or aligned DATA word
      targets this entry. The veneer is already ported; the Rust body calls
      memcpy_forward_words directly, which LLVM inlines as the same 8-byte,
      4-byte, 1-byte, and trailing-byte transfer sequence. match.py against
      the worktree archive printed the expected structural diff, retaining
      those grouped loads and stores. Host tests cover disjoint copy, return
      value, untouched bytes, and every word-aligned overlap. No deliberate
      deviations.

  # --- Fixed four-byte copy helper (util/copy_four_bytes) ---
  - address: 0x0824bf8c
    ghidra: FUN_0824bf8c
    name: copy_four_bytes
    size: 36
    sig: "void copy_four_bytes(u8 *dst, const u8 *src)"
    module: util/copy_four_bytes
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: src
      r2: copied_byte
    notes: >-
      Raw ARM is exactly nine instructions at 0x0824bf8c..0x0824bfac; the
      next separately linked four-word copy helper begins at 0x0824bfb0. It
      performs four ordered ldrb/strb pairs from src (r1) to dst (r0), so each
      later source load observes the preceding destination stores for
      overlapping ranges. Decoding every ARM B/BL immediate in osos.dec finds
      exactly ten inbound direct bl sites, all unconditional, at 0x08249420,
      0x082494e8, 0x082495c8, 0x0824974c, 0x0825519c, 0x0826031c,
      0x08260354, 0x0826038c, 0x082603c4, and 0x082603fc; there are no
      predicated BL forms or direct plain-B tails. The function has no callee
      or dispatch seam. match.py against this worktree archive (exit 1,
      structural diff expected) preserves the four ldrb/strb pairs, with only
      LLVM's prologue and epilogue differing. Deliberate deviations: none.

  # --- Conditional member release (app/member_release) ---
  - address: 0x082201f4
    ghidra: thunk_FUN_08214348
    name: member_release_if_present
    size: 4
    sig: "void member_release_if_present(MemberReleaseOwner *owner)"
    module: app/member_release
    status: ported
    renames:
      param_1/r0: owner
      "owner+0x14": member
      FUN_08214348: conditional_member_release_tail_target
      FUN_081f0530: member_release (unported stock callee)
    notes: >-
      Raw osos.dec bytes are one ARM `b 0x0822aa34`; that trampoline in turn
      tail-branches to FUN_08214348 at 0x08214348. Its 32-byte body reads
      owner+0x14, calls FUN_081f0530 only for a nonzero word, and otherwise
      returns without a call. The next separately linked function after this
      4-byte veneer begins at 0x082201f8. Full-image decoding of every ARM
      B/BL immediate finds exactly 12 direct callers, all unconditional bl
      (zero predicated forms): 0x081426c0, 0x081792d0, 0x081798cc,
      0x08179a08, 0x0817a308, 0x0817a850, 0x0817bc94, 0x0817c71c,
      0x0817d110, 0x0817d3fc, 0x0817d600, and 0x082a9c80. No image word
      equals 0x082201f4, so it is not directly dispatched as data. Deliberate
      deviations: the two stock tail branches are flattened into one Rust
      body; the unported member-release callee is a volatile seam that calls
      its fixed retailOS address on target and a test recorder on host. The
      retail tail target leaves an otherwise unspecified r4 in r0 after a
      non-NULL release; every verified caller ignores it, so the Rust ABI is
      void. Host tests prove both the exact nonzero word forwarding and that
      zero suppresses the release call.

  # --- Default navigation request (ui/default_navigation_request) ---
  - address: 0x08219ed4
    ghidra: FUN_08219ed4
    name: ui_request_default_navigation
    size: 16
    sig: "void ui_request_default_navigation(void *controller)"
    module: ui/default_navigation_request
    status: ported
    renames:
      param_1/r0: controller
      r1: default_selection
      r2: default_flags
      r3: default_item
      FUN_08218e0c: navigation_dispatcher (unported stock callee)
    notes: >-
      Raw osos.dec extent is exactly 16 bytes: mvn r3,#0; mov r2,#0;
      mvn r1,#0; b 0x08218e0c. The next separately linked function begins at
      0x08219ee4. Full-image decoding of every ARM B/BL immediate finds
      exactly 12 direct call sites, all predicated blne (zero unconditional
      bl and zero direct plain-B tails), at 0x08131474, 0x08225a18,
      0x0822719c, 0x082296dc, 0x0822a2dc, 0x0822a8c4, 0x082311bc,
      0x082321ec, 0x08232c68, 0x0823305c, 0x082370b0, and 0x082393c4.
      No aligned osos.dec word equals 0x08219ed4, so it is not directly
      dispatched as data. The wrapper passes its controller unguarded to the
      unported navigation dispatcher with selector tuple (-1, 0, -1).
      Deliberate deviation: Rust makes an ordinary indirect call through a
      volatile seam instead of the retail tail branch; firmware reaches
      0x08218e0c while host tests record the exact forwarded tuple.


  # --- Timed iAP packet completion (app/iap_packet_completion) ---
  - address: 0x081ac4fc
    ghidra: FUN_081ac4fc
    name: complete_iap_packet_with_timestamp
    size: 100
    sig: "u32 complete_iap_packet_with_timestamp(void *unused_context, IapPacket *packet, u32 completion_state, u32 unused)"
    module: app/iap_packet_completion
    status: ported
    renames:
      param_1/r0: unused_context
      param_2/r1: packet
      param_3/r2: completion_state
      param_4/r3: unused
      DAT_081ac560: iap_packet_completion_state (0x089ccb68)
      DAT_081ac560_plus_0x02: completion_active
      DAT_081ac560_plus_0x18: completion_timestamp_ms
      FUN_08262958: clock_source_construct (ported)
      FUN_082a1c30: timespec_to_milliseconds (ported)
      FUN_081d7f14: packet_completion_submit (unported)
      FUN_08262908: clock_source_destroy (ported)
    notes: >-
      Raw extent is 100 bytes, not merely Ghidra's code: 24 ARM
      instructions run from 0x081ac4fc through pop {r4,r5,r6,pc} at
      0x081ac55c, followed by the function-owned literal 0x089ccb68 at
      0x081ac560. The next separately linked function starts at 0x081ac564.
      Decoding every ARM B/BL immediate in osos.dec finds exactly 12 inbound
      calls, all unconditional plain bl at 0x081a9340, 0x081a9e2c,
      0x081a9e9c, 0x081a9fc8, 0x081aa048, 0x081aa0a0, 0x081acd7c,
      0x081acf14, 0x081ad0f0, 0x081ad3fc, 0x081ade50, and 0x081ae624;
      there are no predicated BL forms, direct B tail callers, or aligned
      data-word references. The raw prologue stacks four ABI words, then
      overwrites the saved r2 slot with an 8-byte kind-1 clock. It reads that
      clock through vtable +0x0c into the saved r0/r1 pair, converts the
      timespec to wrapping milliseconds, writes global +0x18 then byte +0x02
      = 1, calls unported 0x081d7f14(packet, completion_state), destroys the
      clock, and returns that status. r0 and r3 are never read. The global's
      concrete type is unrecovered, so its name records only this observed
      packet-completion role. Deliberate deviations: 0x081d7f14 uses the
      existing volatile IAP_PACKET_EVENT_SCHEDULE_OPS completion seam, and
      the clock vtable read uses the existing PENDING_EVENT_INSERT_OPS model;
      target defaults still re-resolve vtable +0x0c. The global has a host
      fixture. Host tests prove timestamp/active stores occur before both a
      successful and failing completion, including sub-millisecond truncation.

  # --- Signed three-way clamp (util/signed_clamp_i32) ---
  - address: 0x080f0f84
    ghidra: FUN_080f0f84
    name: signed_clamp_i32
    size: 32
    sig: "i32 signed_clamp_i32(i32 value, i32 lower, i32 upper)"
    module: util/signed_clamp_i32
    status: ported
    renames:
      param_1/r0/r3: value
      param_2/r1: lower
      param_3/r2: upper
    notes: >-
      Raw ARM establishes the exact 32-byte extent
      0x080f0f84..0x080f0fa4: mov r3,r0; cmp r3,r1; movlt r0,r1; bxlt lr;
      cmp r3,r2; movle r0,r3; movgt r0,r2; bx lr. The separately linked
      predecessors at 0x080f0f44 and 0x080f0f64 are byte-identical
      signed-clamp siblings, while smull at 0x080f0fa4 begins the next
      function. The lower compare happens first: value < lower returns lower;
      otherwise value <= upper returns value, and only then does value > upper
      return upper. Thus inverted bounds are observable (value below lower
      returns lower; values at or above it return upper). Full-image decoding
      of every ARM B/BL immediate finds exactly twelve inbound calls, all
      unconditional plain bl (zero predicated): 0x082485f0, 0x08248604,
      0x08248618, 0x0824862c, 0x08248650, 0x08248664, 0x08248678,
      0x0824868c, 0x08253ec4, 0x08253edc, 0x08253ef4, 0x08253f0c. One
      unconditional direct B at 0x082a05d0 tail-calls this body. No deliberate
      deviations; the Rust branch order exactly preserves signed comparisons
      and invalid-interval behavior. Host tests cover signed extremes,
      sign-boundary comparisons, inclusive endpoints, and inverted bounds.

  # --- XOR two-word exchange helper ---
  - address: 0x080e75ac
    ghidra: FUN_080e75ac
    name: swap_u32_words
    size: 44
    sig: "void swap_u32_words(u32 *first, u32 *second)"
    module: util/u32_word_swap
    status: ported
    renames:
      param_1/r0: first
      param_2/r1: second
      r2: xor
      r3: second_word
    notes: >-
      Raw osos.dec confirms ten operational instructions from 0x080e75ac
      through 0x080e75d0, followed by `bx lr` at 0x080e75d4; the separately linked next function
      starts at 0x080e75d8 (`cmp r0,#0`). The function reads both aligned
      words, stores their XOR through first, rereads second, then uses two
      XOR stores to exchange them. It has no NULL guard. Decoding every ARM
      B/BL word in osos.dec finds exactly 12 direct calls, all unconditional
      bl and no predicated bl: 0x080ea524, 0x080ea530, 0x080eabb4,
      0x080eabd8, 0x080eabfc, 0x080eac24, 0x080eac4c, 0x080eb3ac,
      0x080eb3d0, 0x080eb3f4, 0x080eb41c, and 0x080eb444. Aliased pointers
      deliberately clear the word, as required by the original XOR sequence;
      this is intentionally unlike a conventional swap. Deviations: none.

  # --- LCD controller register write ---
  - address: 0x080d4b14
    ghidra: FUN_080d4b14
    name: lcd_write_register
    size: 24
    sig: "void lcd_write_register(u32 register_index, u32 value)"
    module: drivers/lcd_write_register
    status: ported
    renames:
      param_1/r0: register_index
      param_2/r1/r4: value
    notes: >-
      Raw ARM has six instructions from 0x080d4b14 through 0x080d4b28:
      preserve r1 in r4, bl 0x080d7b60 with r0 intact, restore r0 = r4,
      then tail-b 0x080bb5f4. The next separately linked function starts at
      0x080d4b2c, so Ghidra's 24-byte extent is correct and there is no
      literal pool. The two helpers each poll 0x3830001c until busy bit 4
      clears; the first stores its input at 0x38300004, and the second stores
      its input at 0x38300040. Full-image decoding of every ARM B/BL immediate
      finds exactly 12 inbound calls: all are unconditional bl, at
      0x080ddd50, 0x080ddd68, 0x080ddd7c, 0x080ddd94, 0x080ddda0,
      0x080dddac, 0x080e3364, 0x080e3370, 0x080e337c, 0x080e3398,
      0x080e33a4, and 0x080e33b0; there are zero predicated bl and zero direct
      B call sites. No osos data word equals the entry address, so it is not
      a virtual-dispatch target. The 0x0200..0x0213 register-index sequences
      at 0x080ddd24 and the 0x0002/0x000b/0x0010 sequences at 0x080e3338
      identify this as the LCD register writer. Deliberate deviation: inlines
      unported helper 0x080d7b60, while routing the tail-call helper through
      lcd_write_value; device loads/stores are volatile and host builds use
      atomic MMIO models.
      Reference C: decomp/c/007/080d4b14_FUN_080d4b14.c.
  # --- LCD controller data-port write ---
  - address: 0x080bb5f4
    ghidra: FUN_080bb5f4
    name: lcd_write_value
    size: 28
    sig: "void lcd_write_value(u32 value)"
    module: drivers/lcd_write_register
    status: ported
    renames:
      param_1/r0/r2: value
      DAT_080bb60c: LCD_CONTROLLER_BASE
    notes: >-
      Raw osos.dec confirms six instructions at 0x080bb5f4..0x080bb608:
      mov r2,r0; push {lr}; bl 0x080ce0c8; ldr r0,[pc,#4]; str r2,[r0,#0x40];
      then pop {pc}. The 0x38300000 literal at 0x080bb60c belongs to this
      28-byte extent; the separately linked next function starts at
      0x080bb610. Thus value survives the readiness call and is written
      without validation or timeout to LCD controller +0x40. Decoding every
      ARM B/BL immediate in osos.dec finds nine direct BL call sites, all
      unconditional and no predicated forms: 0x080bb538, 0x080dbef0,
      0x080dbef8, 0x080dbf00, 0x080dbf08, 0x0836b3f8, 0x0836b404,
      0x0836b41c, and 0x0836b428. The sole direct B is the tail call from
      lcd_write_register at 0x080d4b28; no data word equals the entry address.
      Deliberate deviation: the local lcd_wait_ready implementation reproduces
      the unported 0x080ce0c8 status-bit-4 poll; device accesses are volatile,
      while host tests use atomic MMIO registers. Reference C:
      decomp/c/007/080bb5f4_FUN_080bb5f4.c.

  # --- Scoped path-component query wrapper ---
  - address: 0x0809b678
    ghidra: FUN_0809b678
    name: path_component_query
    size: 48
    sig: "u32 path_component_query(const u8 *path, u32 unused)"
    module: app/path_component_query
    status: ported
    renames:
      param_1/r0: path (NUL-terminated path C string)
      param_2/r1/r4: unused (forwarded to 0x0809b6a8, then discarded)
      r4: query status across path-object destruction
      r2/r3 spill: StringObject stack storage
    notes: >-
      Raw osos.dec decoding confirms Ghidra's 48-byte extent: twelve
      instructions occupy 0x0809b678..0x0809b6a4, and the separately linked
      0x0809b6a8 helper begins immediately afterward. The wrapper constructs
      the derived path object at its r2/r3 spill storage through ported
      path_object_construct @ 0x08279284, calls the 0x0809b6a8 component
      helper with the constructor return and arg2, preserves its status in r4,
      then destroys the original stack storage through ported
      string_object_destroy_veneer @ 0x082792fc. Full-image decoding of every
      ARM B/BL immediate finds exactly 12 inbound direct calls, all
      unconditional plain bl at 0x08047464, 0x080474a4, 0x08047588,
      0x0809db88, 0x080b3d68, 0x080b5384, 0x080bd678, 0x0811c59c,
      0x08195754, 0x081967ac, 0x081b0004, and 0x0826631c; there are zero
      predicated bl and zero direct B tail calls. Every recovered caller passes
      a path string and zero as arg2, and branches on the returned status.
      Raw decoding of helper 0x0809b6a8 shows it overwrites saved r4 from r0
      and never reads r1, so arg2 is dead in the complete retail composition.
      Its 0x08149e38 callee's exact facade-slot identity is not established;
      the port therefore names only the observed component-query role.
      Deliberate deviation: that unresolved helper is a volatile dispatch
      boundary, calling its fixed retailOS address on device and failing closed
      to 0 on host. Host tests prove construction/query/destruction ordering,
      argument forwarding for 0, 1, and 0x5a5af00d, derived/base vtable
      transitions, and verbatim statuses 0, 13, and 53.

  # --- Scoped facade path slot +0x5c operation ---
  - address: 0x08084d58
    ghidra: FUN_08084d58
    name: path_facade_slot_5c
    size: 68
    sig: "u32 path_facade_slot_5c(const u8 *path, u32 base_hint)"
    module: app/path_probe
    status: ported
    renames:
      param_1/r0/r4: path (passed unchanged to facade vtable slot +0x5c)
      param_2/r1: base_hint (live through mov r0,sp into guard construction)
      r0 after 0x0818a0bc: facade
      r2: facade vtable slot +0x5c operation
      r4 after blx: operation status preserved across guard destruction
    notes: >-
      Raw osos.dec decoding confirms the 68-byte extent: seventeen ARM
      instructions from 0x08084d58 through 0x08084d98, followed immediately
      by a distinct function at 0x08084d9c. Full-image decoding of every ARM
      B/BL immediate finds exactly 12 inbound direct calls: eight
      unconditional bl at 0x08084d40, 0x0811d744, 0x0812fc50, 0x0813a554,
      0x0813a610, 0x081ef29c, 0x082737dc and 0x082dcfc8; four predicated
      blne at 0x0827ef74, 0x0827ef90, 0x0827f7a8 and 0x0827f7c4; no direct
      B tail calls. The r0-r3 spill is a 16-byte InterfaceGuard: construct
      it through path_probe_guard_construct @ 0x08206e40 with the live r1
      base_hint, fetch facade selector 1 through facade_for_selector @
      0x0818a0bc, invoke its raw vtable slot +0x5c with the saved path in r1,
      preserve r0 in r4, then destroy the guard through
      path_probe_guard_destroy @ 0x08206e6c and return the saved status.
      Write-mode file-open invokes it before recreation; four callers gate it
      on nonzero status. The precise semantic identity of slot +0x5c is not
      established, so the name records only the verified dispatch role.
      Deliberate host deviation: reuses the existing path-probe guard/facade
      seams, whose host defaults fail closed because the shared interface
      graph remains a retailOS boundary. Host tests prove base-hint forwarding,
      slot selection, path identity, guard ordering, and verbatim statuses.

  - address: 0x0806ea5c
    ghidra: FUN_0806ea5c
    name: x509v3_add_value
    size: 220
    sig: "u32 x509v3_add_value(const char *name, const char *value, usize **values)"
    module: crypto/x509v3_add_value
    status: ported
    renames:
      param_1/r0: name
      param_2/r1: value
      param_3/r2: values
      r4: entry
      r5: name_copy
      r7: value_copy
    notes: >-
      Raw extent is exactly 220 bytes, 0x0806ea5c..0x0806eb38: the immediate
      sibling FUN_0806eb38 starts at 0x0806eb38. Decoding every ARM B/BL word
      in osos.dec finds 12 direct call sites: 9 unconditional bl and 3 blne;
      the predicated callers gate their configuration assembly themselves.
      This is OpenSSL X509V3_add_value: duplicate non-NULL name/value through
      unported BUF_strdup (0x08042220), allocate the 12-byte
      {section=NULL,name,value} CONF_VALUE record through ported traced_alloc,
      lazily create the stack through ported ata_call_with_zero, then append
      through ported namespace_provider_push. A zero append, duplication,
      allocation, or factory result logs (0x22,0x69,0x41,0,0), frees entry,
      name copy, and value copy in that order, and returns 0; append success
      transfers ownership and returns 1. Deliberate deviation: BUF_strdup is
      the CSTR_DUPLICATE volatile seam, defaulting to NULL until ported; the
      existing ata_call_with_zero factory seam likewise defaults to NULL until
      FUN_083696f4 is ported. Host tests use usize word indices to retain the
      target's 12-byte record layout without overlapping 64-bit host pointers.

  # --- StringObject owner destruction ---
  - address: 0x0804bd88
    ghidra: FUN_0804bd88
    name: string_object_owner_destroy
    size: 40
    sig: "i32 string_object_owner_destroy(StringObjectOwner *owner)"
    module: cxx/string_object_owner_destroy
    status: ported
    renames:
      param_1/r0: owner
      param_1_word_1: embedded StringObject member
      thunk_FUN_082792fc: string_object_destroy_veneer
      FUN_082aad24: operator_delete
    notes: >-
      Raw ARM establishes a 40-byte extent: ten words from 0x0804bd88 through
      pop {r4,pc} at 0x0804bdac, then the separately linked sibling
      FUN_0804bdb0 begins at 0x0804bdb0. Ghidra's 32-byte extent omits the
      leading cmp/mvneq null-status pair. Decoding every ARM B/BL immediate in
      osos.dec finds exactly 12 inbound calls, all unconditional bl at
      0x0806da90, 0x0806dad4, 0x0806dbfc, 0x0806dc10, 0x0806dd04,
      0x0806dda0, 0x080718b4, 0x080718c4, 0x080718d0, 0x080718dc,
      0x080718e8, and 0x080718f4; no predicated forms, direct B calls, or
      image data words target this entry. NULL returns 0xffffffce (-50) with
      no calls. Non-NULL input denotes an allocation with an opaque first word
      and a StringObject member at +4: it calls the already ported
      string_object_destroy_veneer with that member, subtracts one word from
      its returned pointer, calls already ported tag-2 operator_delete, then
      returns zero. The generic owner name deliberately records only this
      verified member-ownership relation; no class identity is invented.

  # --- Default UTF-16 trim-and-compare wrapper ---
  - address: 0x0804544c
    ghidra: FUN_0804544c
    name: wstr_default_trim_compare
    size: 60
    sig: "i32 wstr_default_trim_compare(void *context, const u16 *left, u32 left_len, const u16 *right, u32 right_len)"
    module: util/wstr_default_trim_compare
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: left
      param_3/r2: left_len
      param_4/r3: right
      param_5/[sp]: right_len
      DAT_08045488: default_collation_flags
      FUN_0804548c: trim_compare_helper
    notes: >-
      Raw ARM establishes a 60-byte instruction extent from 0x0804544c
      through pop {r2,r3,r4,pc} at 0x08045484; the following word at
      0x08045488 is its literal-pool pointer 0x089cb1a4, and the separately
      linked helper FUN_0804548c begins at 0x0804548c. The wrapper reads the
      live word at 0x089cb1a4, stores 29 only when it was zero, then calls
      FUN_0804548c(context, left, left_len, right, right_len, flags) and
      preserves its i32 tri-state in r0. Ghidra incorrectly reports void.
      Decoding every ARM B/BL immediate in osos.dec finds exactly 12 inbound
      calls: all unconditional bl at 0x080456f0, 0x0808d218, 0x080d1aec,
      0x080d1bfc, 0x080dc650, 0x080e2994, 0x080e2a04, 0x080e2a60,
      0x080e2abc, 0x080e2bd4, 0x080e2cb4, and 0x08229068; zero predicated
      forms. The helper's raw body trims leading u16 elements <= 0x20 from
      both ranges and calls unported counted compare FUN_0804529c with this
      default word. Deliberate deviation: FUN_0804548c is not ported, so the
      port has a volatile WSTR_DEFAULT_TRIM_COMPARE dispatch seam; target
      builds transmute its retail address and host tests install a recorder.
      The host-only flags word models the live target word. Host tests prove
      zero-to-29 initialization, nonzero preservation, exact forwarding of
      null and maximum-length inputs, and unchanged negative return values.

  # --- Tagged object bidirectional link ---
  - address: 0x0805bc34
    ghidra: FUN_0805bc34
    name: tagged_object_link
    size: 196
    sig: "i32 tagged_object_link(TaggedLinkSource *source, TaggedLinkTarget *target)"
    module: ui/tagged_object_link
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: target
      FUN_080b49a4: source_tag_guard
      FUN_080c67b8: target_tag_and_storage_guard
      FUN_080e2d70: source_contains_target
      FUN_0803bbfc: list_insert
      FUN_080646b8: list_remove
    notes: >-
      Raw ARM establishes a 196-byte extent, from 0x0805bc34 through pop
      {r1,r2,r3,r4,r5,r6,r7,pc} at 0x0805bcf4; separately entered sibling
      FUN_0805bcf8 starts at 0x0805bcf8. Decoding every ARM B/BL immediate
      in osos.dec finds exactly 12 inbound calls: 11 unconditional bl at
      0x08047c80, 0x0805e4a8, 0x0805e4bc, 0x0806171c, 0x08063518,
      0x0806360c, 0x08065ca8, 0x080b4a44, 0x0812cbe0, 0x0816ed80, and
      0x0817c108 plus one blne at 0x080e5c24. That predicated caller checks
      its optional target itself; this routine's own tag checks return -50.
      It rejects a source without tag 0x4d536e64 or a target without tag
      0x4d526376 and nonzero word +0x30, then skips an existing link. It
      inserts {target,0} into the source list and {source} into the target
      list; source failure returns -108, target failure rolls back the source
      insertion and returns -108, while two successes increment both +0x0c
      and +0x08 counters. The object-family and list identities remain
      unknown. The source membership scan (0x080e2d70), insertion
      (0x0803bbfc), and removal (0x080646b8) are volatile target-address
      seams with host recorders. Rust initializes the two temporary records
      directly rather than retaining the stock bzero calls that only clear
      them before their pointer words are stored.

  # --- Halfword-aligned little-endian word load ---
  - address: 0x080539d4
    ghidra: FUN_080539d4
    name: load_u32_halfword_aligned
    size: 28
    sig: "u32 load_u32_halfword_aligned(const u8 *p)"
    module: libc/halfword_load
    status: ported
    renames:
      param_1/r0: p (two-byte-aligned source)
      r1: low_halfword
      r0(after second ldrh): high_halfword
    notes: >-
      Raw osos.dec establishes the exact 28-byte extent: seven ARM words
      from 0x080539d4 through pop {ip,pc} at 0x080539ec; the separately
      linked next function starts at 0x080539f0. Ghidra's body is
      semantically correct but its undefined4 * signature conceals the
      two-byte alignment contract. Decoding every ARM B/BL immediate finds
      exactly 12 inbound calls, all unconditional bl at 0x080450e8,
      0x080450f4, 0x08045114, 0x08045120, 0x08045134, 0x08045140,
      0x08045164, 0x0804761c, 0x08050330, 0x08066090, 0x0806c958, and
      0x0806c968; no predicated forms or direct B entries. No image data
      word equals this address. The ARM reads ldrh [p] and ldrh [p,#2],
      spills the halves adjacently, then ldr reloads their little-endian
      u32 composition. The port retains two aligned u16 loads rather than
      read_unaligned, matching the original's accepted two-byte alignment
      and avoiding its four-ldrb ARMv5 sequence. match.py confirms that LLVM
      retains the paired ldrh/orr algorithm while folding the temporary stack
      word and reordering the independent loads. No NULL or bounds guard is
      added; p must point to four readable bytes at a two-byte-aligned
      address.

  # --- ASCII lowercase-to-uppercase conversion ---
  - address: 0x082e0180
    ghidra: FUN_082e0180
    name: ascii_to_uppercase
    size: 20
    sig: "u32 ascii_to_uppercase(u32 value)"
    module: util/ascii_to_uppercase
    status: ported
    renames:
      param_1/r0: value
      r1: lowercase_offset (value - 'a')
      r0(after subls): uppercase_value
    notes: >-
      Raw osos.dec establishes the exact five-word, 20-byte extent from
      0x082e0180 through bx lr at 0x082e0190; the next separately entered
      function begins at 0x082e0194. The ARM computes value - 0x61 with
      u32 wraparound and applies sub #0x20 followed by and #0xff only when
      that difference is unsigned <= 25, so exactly the full-u32 values
      'a' through 'z' map to ASCII uppercase. Inputs with a lowercase low
      byte but nonzero upper bits remain unchanged. Decoding every ARM B/BL
      immediate in osos.dec finds exactly eight inbound calls, all
      unconditional bl at 0x081bc9a4, 0x081bd384, 0x081bd9f0, 0x081bdb04,
      0x081bdd9c, 0x082e37a0, 0x082e39ac, and 0x082e39b8; there are zero
      predicated call forms and zero direct B entries. No image data word
      equals 0x082e0180, so it is not directly dispatched virtually. No
      deliberate deviations; host tests exhaust all u8 values and prove
      high-bit word values do not receive byte-style folding.

  # --- FAT data-cluster to cache-block-index conversion ---
  - address: 0x082e01cc
    ghidra: FUN_082e01cc
    name: fat_cluster_to_block_index
    size: 56
    sig: "u32 fat_cluster_to_block_index(const FatVolume *volume, u32 cluster)"
    module: fs/fat_cluster_to_block
    status: ported
    renames:
      param_1/r0: volume
      param_2/r1: cluster
      "volume+0x74": first_data_block
      "volume+0x1ce": blocks_per_cluster_shift
      "volume+0x1d8": cache_block_limit
    notes: >-
      Raw osos.dec confirms the 56-byte code extent from 0x082e01cc through
      bxhi lr at 0x082e01f8 and the zero-return mov/bx pair at
      0x082e01fc..0x082e0200. The next separately entered function starts
      at 0x082e0208; the intervening word at 0x082e0204 is the
      PC-relative literal 0x000001ce used to address the shift halfword.
      Decoding every ARM B/BL immediate finds 11 direct callers, all plain
      unconditional bl (0x082b18dc, 0x082b1b94, 0x082b1d88, 0x082b1f34,
      0x082b2050, 0x082b2090, 0x082e0454, 0x082e2130, 0x082e2994,
      0x082e3314, 0x082e66fc), with zero predicated bl forms. Two further
      entries are tail branches: unconditional b at 0x082e1480 and bne at
      0x082e2bb4. For cluster >= 2, it calculates first_data_block +
      ((cluster - 2) << (blocks_per_cluster_shift & 0xff)) with ARM u32
      wraparound, returning the candidate only when it is strictly less than
      cache_block_limit. ARM register LSL amounts >= 32 yield zero; the port
      retains that rule. Clusters 0 and 1 return zero before dereferencing
      volume; all other paths have no NULL or bounds guard. No deliberate
      deviations. Host tests cover reserved-cluster short-circuiting, shifted
      data-cluster mapping, strict limit rejection, addition wraparound, and
      the >=32 shift rule.

  - address: 0x082cda48
    ghidra: FUN_082cda48
    name: cg_expression_dependency_mask
    size: 128
    sig: "u64 cg_expression_dependency_mask(const u32 *context, const u8 *node)"
    module: codegen/expression_dependency_mask
    status: ported
    renames:
      param_1/r0/r5: context
      param_2/r1/r4: node
      r6:r7: accumulated_mask
      "node+0x08": second_child
      "node+0x0c": first_child
      "node+0x10": field_10_collection
      "node+0x24": leaf_identifier
      "node+0x38": field_38_collection
    notes: >-
      Raw osos.dec extent is exactly 128 code bytes from 0x082cda48 through
      pop {r4-r8,pc} at 0x082cdac4; the next separately linked function
      begins at 0x082cdac8. Decoding every ARM B/BL immediate finds 11 direct
      call sites, all unconditional bl (0x082ccfec, 0x082cd048, 0x082cd058,
      0x082cd7a0, 0x082cd7b4, 0x082cd8f4, 0x082cda10, 0x082cda24,
      0x082cda7c, 0x082cda90, 0x08367324), with zero predicated forms.
      NULL returns zero. Tag 0x95 tail-calls FUN_082d07f8(context,
      node+0x24); every other tag recursively combines node+0x0c then
      node+0x08, then ORs FUN_082cd8c4(context,node+0x10) and
      FUN_082cd9b4(context,node+0x38). The three unported callees retain
      their literal device addresses on ARM and use the documented host ops
      seam; their identities are intentionally not invented. Tests prove the
      NULL path, leaf short-circuit, 32-bit node links, recursive call order,
      and low/high-half mask OR.

  - address: 0x08242ec4
    ghidra: FUN_08242ec4
    name: fixed_matrix_cursor_current
    size: 20
    sig: "FixedMatrix4x4 *fixed_matrix_cursor_current(const FixedMatrixCursor *cursor)"
    module: util/fixed_matrix_cursor
    status: ported
    renames:
      param_1/r0: cursor
      "[cursor+0x00]": matrix_base
      "[cursor+0x04]": index
      r0: current_matrix
    notes: >-
      Raw ARM extent is exactly five instruction words from 0x08242ec4 through
      0x08242ed4; the next separately linked function starts at 0x08242ed8.
      Decoding every ARM B/BL immediate in osos.dec found exactly 11 inbound
      direct bl calls, all unconditional and none predicated: 0x0824faf8,
      0x08252144, 0x08252168, 0x08252198, 0x082521a4, 0x082521ec,
      0x08252590, 0x082527bc, 0x082527dc, 0x082527ec, and 0x08253838.
      No direct tail branch or aligned DATA word targets this entry. It loads
      cursor->matrix_base and cursor->index, then returns matrix_base +
      index * 0x44, the target-width size of FixedMatrix4x4; both add
      instructions wrap modulo 2^32. There is no NULL, bounds, or alignment
      guard and no callee. No deliberate deviations. Host tests cover the
      first entry, a later entry, and 32-bit address wraparound.

  # --- Mode-selected state byte ---
  - address: 0x0822b684
    ghidra: FUN_0822b684
    name: mode_selected_byte
    size: 20
    sig: "u8 mode_selected_byte(const void *state)"
    module: app/mode_selected_byte
    status: ported
    renames:
      param_1/r0: state
      "state+0x2f4": mode_byte
      "state+0x5ec": default_byte
      "state+0x5f8": mode_flags
    notes: >-
      Raw osos.dec confirms the exact five-instruction, 20-byte extent:
      ldrb r1,[r0,#0x5f8]; tst r1,#1; ldrbeq r0,[r0,#0x5ec]; ldrbne
      r0,[r0,#0x2f4]; bx lr from 0x0822b684 through 0x0822b694. The next
      separately linked function opens at 0x0822b698. It returns the
      zero-extended +0x5ec byte for flags with bit 0 clear and the
      zero-extended +0x2f4 byte when bit 0 is set; no NULL or bounds guard
      exists. The state class, byte semantics, and other flag bits remain
      unidentified. Decoding every ARM B/BL immediate finds exactly nine
      direct bl callers, all unconditional and no predicated forms:
      0x0822aa40, 0x0822ac08, 0x0822aef0, 0x0822af70, 0x0822b158,
      0x0822b2ec, 0x0822b554, 0x0822b6dc, and 0x0822b800. No direct tail
      branch reaches this entry. No deliberate deviations. Host tests cover
      both bytes, every meaningful flag-bit state, byte extremes, and
      read-only behavior.

  # --- Mode-selected stream position ---
  - address: 0x0822aecc
    ghidra: FUN_0822aecc
    name: mode_selected_position
    size: 20
    sig: "u32 mode_selected_position(const void *state)"
    module: app/mode_selected_position
    status: ported
    renames:
      param_1/r0: state
      "state+0x2ec": mode_position
      "state+0x5e4": default_position
      "state+0x5f8": mode_flags
    notes: >-
      Raw osos.dec confirms the exact five-instruction, 20-byte extent:
      ldrb r1,[r0,#0x5f8]; tst r1,#1; ldreq r0,[r0,#0x5e4]; ldrne
      r0,[r0,#0x2ec]; bx lr from 0x0822aecc through 0x0822aedc. The next
      separately linked function opens at 0x0822aee0. It returns the
      +0x5e4 position for flags with bit 0 clear and the +0x2ec position
      when bit 0 is set; no NULL, bounds, or alignment guard exists. The
      state class and other flag bits remain unidentified. Decoding every
      ARM B/BL immediate finds exactly 11 direct bl callers, all
      unconditional and no predicated forms: 0x081cbc30, 0x081cc090,
      0x081cc5e4, 0x081cd0c4, 0x081cd0d8, 0x08220330, 0x082203c8,
      0x0822b43c, 0x0822b524, 0x0822b618, and 0x0822b62c. The separately
      linked thunk at 0x0820a49c reaches this entry through an unconditional
      tail b, not a call. No deliberate deviations. Host tests cover both
      selected words, every meaningful flag-bit state, full-width values,
      and read-only behavior.

  # --- Playlist-pointer selection marker ---
  - address: 0x08219eec
    ghidra: FUN_08219eec
    name: set_playlist_pointer_marker_if
    size: 72
    sig: "u32 set_playlist_pointer_marker_if(void *ignored_r0, void *ignored_r1, u32 enabled, void *unobserved_r3)"
    module: app/playlist_pointer_marker
    status: ported
    renames:
      r0: ignored_r0
      r1: ignored_r1
      r2: enabled
      r3: unobserved_r3
      DAT_08219f34: playlist_pointer_key_bundle
      DAT_08219f38: playlist_pointer_string_table
    notes: >-
      Raw osos.dec confirms the exact 72-byte extent from 0x08219eec through
      the two literal words at 0x08219f34 and 0x08219f38; the next separately
      linked function opens at 0x08219f3c. Decoding every ARM B/BL immediate
      finds exactly 11 direct bl callers, all unconditional and none
      predicated: 0x0822584c, 0x0822703c, 0x082294fc, 0x0822a208, 0x0822a768,
      0x08231060, 0x08232050, 0x08232fe4, 0x0823420c, 0x08236fdc, and
      0x0823904c. Three additional unconditional b tail transfers enter at
      0x08130fc8, 0x08232998, and 0x08239c14; no aligned DATA word targets
      this entry. When r2 is nonzero, it builds a temporary COW string for
      the literal at DAT_08219f34 + 8, "PlaylistPtr", writes decimal 1 through
      string_table_set_decimal(DAT_08219f38, key, &1), releases the temporary,
      and returns 1. A zero r2 bypasses all calls but still returns 1. r0 and
      r1 are saved only to stack slots later overwritten by the decimal value;
      r3 is saved into the COW result slot but direct callers do not establish
      it, so it is intentionally not assigned a semantic role. The string
      table object at 0x08a79c10 is outside the decrypted body and is passed
      unchanged; no identity beyond its observed marker write is inferred.

  # --- Range-state constructor ---
  - address: 0x081f7278
    ghidra: FUN_081f7278
    name: range_state_construct
    size: 88
    sig: "RangeState *range_state_construct(RangeState *state, const u32 *initial_value, u32 resolution)"
    module: util/range_state
    status: ported
    renames:
      param_1/r0: state
      param_2/r1: initial_value
      param_3/r2: resolution
      FUN_080fffcc: pair_zero_initialize
      FUN_081f7194: range_state_configure
    notes: >-
      Raw osos.dec confirms the exact 88-byte extent, sixteen ARM words from
      0x081f7278 through bx lr at 0x081f72cc; the separately linked sibling
      constructor opens at 0x081f72d0. Ghidra omitted the live r2 argument:
      after the four pair-zero calls the nonzero path passes it unchanged as
      the third argument to FUN_081f7194. Decoding every ARM B/BL immediate
      finds exactly 11 direct callers, all plain unconditional bl and none
      predicated or tail branches: 0x081272d8, 0x08140390, 0x08140570,
      0x0817e588, 0x081b4e58, 0x081bb1e0, 0x081bb204, 0x081dc5b0,
      0x0827dfac, 0x0829b574, and 0x0829b59c. The constructor sets words
      0 and 1 to zero and u32::MAX respectively, clears four two-word
      records at words 5..12, leaves words 2..4 and 13..15 intact, and
      configures only when *initial_value is nonzero. The configuration
      callee remains stock on firmware; host builds deliberately use an
      inert test seam because FUN_081f7194 is not ported. No identity beyond
      the observed range-state initialization is inferred.

  # --- Image-format descriptor slot update ---
  - address: 0x081d5f98
    ghidra: FUN_081d5f98
    name: image_format_descriptor_slot_set
    size: 60
    sig: "u32 image_format_descriptor_slot_set(u32 *slots, u32 slot, const u8 *descriptor)"
    module: app/image_format_descriptor_slot
    status: ported
    renames:
      param_1/r0: slots
      param_2/r1: slot
      param_3/r2: descriptor
      "slots+0x290": sequence
      "slots+(slot+1)*0x24": record
      thunk_EXT_FUN_22000188: iram_memcpy_veneer
    notes: >-
      Raw osos.dec confirms the exact 60-byte extent from 0x081d5f98 through
      bx lr at 0x081d5fd0; the next separately linked function begins at
      0x081d5fd4. The body calculates slots + (slot + 1) * 0x24, stores the
      current sequence word at record + 0, increments the sequence word at
      slots + 0x290, then calls the IRAM memcpy veneer at 0x08037df8 to copy
      the aligned 32-byte descriptor to record + 4, and returns 1. Decoding
      every ARM B/BL immediate finds exactly 11 direct callers, all
      unconditional plain bl and none predicated: 0x081b7d98, 0x081cd228,
      0x081cd244, 0x081cd260, 0x081cd27c, 0x081cd298, 0x0820a4fc,
      0x0820a518, 0x0820a534, 0x08220228, and 0x08220244. No tail b or
      aligned DATA word targets this entry. The callers build a 32-byte
      descriptor with image_format_descriptor_for_kind before this call,
      establishing the descriptor's image-format role. There are no NULL,
      bounds, or alignment checks. Slot 17 has its descriptor bytes overlap
      the sequence word, so the later copy overwrites the incremented word;
      this port preserves that instruction order. No deliberate deviations.

  # --- iAP packet event callback dispatch (app/iap_packet_event_dispatch) ---
  - address: 0x081a9728
    ghidra: FUN_081a9728
    name: dispatch_iap_packet_event
    size: 176
    sig: "IapPacket *dispatch_iap_packet_event(void *context, IapPacket *packet, u32 event)"
    module: app/iap_packet_event_dispatch
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: packet
      param_3/r2: event
      "*packet": packet_vtable
      packet_vtable+0xd8..+0xfc: unrecovered_iap_packet_event_callbacks
    notes: >-
      Raw osos.dec confirms the 176-byte extent: compare event with 9,
      move packet into r0, jump-table branches, and ten vtable loads through
      bx r1 at 0x081a97d4; the separately linked next function begins at
      0x081a97d8. The event-to-slot map is {0:+0xe0, 1:+0xd8, 2:+0xe4,
      3:+0xdc, 4:+0xec, 5:+0xf0, 6:+0xfc, 7:+0xf8, 8:+0xf4, 9:+0xe8}.
      An event above 9 returns packet directly without dereference. Decoding
      every ARM B/BL immediate in osos.dec finds exactly 11 direct inbound
      calls: ten predicated blne at 0x081aa964, 0x081aa98c, 0x081aa9c8,
      0x081aa9f0, 0x081aaa18, 0x081aaa44, 0x081aaa6c, 0x081aaa94,
      0x081aaabc, and 0x081abc1c, plus one unconditional bl at 0x081ac090;
      there are no direct plain-B tails. The ten callback identities and
      return contracts are unrecovered indirect targets, so target Rust reads
      their verified 32-bit vtable slots while host tests use a slot-index
      dispatch seam rather than inventing callee identities.

  # --- UTF-8 tail character clear ---
  - address: 0x08139b90
    ghidra: FUN_08139b90
    name: utf8_clear_tail_character
    size: 88
    sig: "void utf8_clear_tail_character(void *context, u8 *bytes, u32 length)"
    module: app/utf8_clear_tail_character
    status: ported
    renames:
      param_1/r0: context (dead)
      param_2/r1: bytes
      param_3/r2: length
      FUN_08030f44: heap_panic
    notes: >-
      Raw osos.dec confirms the 88-byte, all-code extent from 0x08139b90
      through bx lr at 0x08139be4; the separately linked next function starts
      at 0x08139be8. The body panics when bytes is NULL or length is zero,
      then starts at (length - 1) & 0xffff. It clears backward through bytes
      0x80..0xbf and 0xf8..0xff, stopping after clearing the first ASCII byte
      or UTF-8 lead byte in 0xc0..0xf7; if no such byte occurs, it clears
      index zero too. Ghidra incorrectly omits that final index-zero clear.
      Whole-image ARM B/BL-immediate decoding found exactly 11 inbound plain,
      unconditional bl sites (0x081aceb8, 0x081ad3b4, 0x081ade14, 0x081ae528,
      0x081ae948, 0x081aec28, 0x081aee30, 0x081af088, 0x081e2ac8,
      0x081fd914, and 0x081fe174), with no predicated calls, tail branches,
      or direct data-word references. Every caller overwrites r0 immediately,
      so the incidental ARM index return is deliberately not exposed. No
      behavioral deviations.

  - address: 0x08197a30
    ghidra: FUN_08197a30
    name: scoped_string_id_record_set_destroy
    size: 68
    sig: "ScopedStringIdRecordSet *scoped_string_id_record_set_destroy(ScopedStringIdRecordSet *this)"
    module: app/scoped_string_id_record_set
    status: ported
    renames:
      param_1/r0: this
      DAT_08197a70: SCOPED_STRING_ID_RECORD_SET_DESCRIPTOR_ADDRESS
    notes: >-
      Raw osos.dec extent is 68 bytes: sixteen code words at
      0x08197a30..0x08197a6c plus the literal-pool word at 0x08197a70
      (= 0x08989b60); the next separately linked function starts at
      0x08197a74. Decoding every ARM B/BL word finds 11 direct call sites,
      all unconditional BL, with no predicated forms. This is the plain
      non-deleting destructor of an otherwise unidentified 0x6c-byte
      aggregate: it stores the descriptor literal at +0x00, destroys five
      StringIdRecord members in reverse order at +0x58, +0x48, +0x38, +0x28,
      and +0x18 through the already ported string_id_record_destroy
      (0x08258c80), drops the 20-byte ContextScope member at +0x04 through
      the already ported context_scope_drop (0x08284188), then returns this.
      The +0x68 trailing word is not touched. The raw descriptor target is
      the NUL-terminated string
      TransitionAddonI28TSilverMediaListCntlr_GeniusE; it does not establish
      a semantic class identity, so the port uses a structural name and models
      that string with a static rather than storing an absolute host pointer.
      No replaceable dispatch seam was introduced. match.py structural review
      found reverse member calls at +0x58/+0x48/+0x38/+0x28/+0x18 and the
      retained scope call; the latter is a volatile function-pointer read and
      `blx` only because LLVM would otherwise remove the direct call to its
      proven-empty body.

  - address: 0x080d8f64
    ghidra: FUN_080d8f64
    name: magic_tagged_object_validate
    size: 36
    sig: "i32 magic_tagged_object_validate(const MagicTaggedObject *object)"
    module: cxx/magic_tagged_object
    status: ported
    renames:
      param_1/r0: object
      DAT_080d8f88: MAGIC_TAGGED_OBJECT_TAG
    notes: >-
      Raw osos.dec confirms the 36-byte code extent at 0x080d8f64..0x080d8f84,
      followed by literal-pool word 0x534c7374 at 0x080d8f88; the next
      separately linked function starts at 0x080d8f8c. The function returns
      zero only when object is non-NULL and its first word matches that opaque
      tag; NULL or any other first word returns -0x32. Whole-image ARM
      B/BL-immediate decoding found exactly 11 inbound direct callers, all
      unconditional plain bl, with no predicated forms or tail branches. The
      tag does not recover a class identity, so the port uses a structural name
      and introduces no dispatch seam. No behavioral deviations.

  # --- "crts" first-word tag guard ---
  - address: 0x080a7714
    ghidra: FUN_080a7714
    name: crts_has_tag
    size: 36
    sig: "u32 crts_has_tag(const u32 *object)"
    module: util/crts_tag
    status: ported
    renames:
      param_1/r0: object
      DAT_080a7738: CRTS_TAG (0x73747263, in-memory bytes "crts")
      r0: is_crts_tag
    notes: >-
      Raw osos.dec confirms the 36-byte code body: nine code words from
      0x080a7714 through bx lr at 0x080a7734, followed by the owned 4-byte
      0x73747263 literal-pool word at 0x080a7738; the separately linked next
      function starts at 0x080a773c. It returns one only when object is
      non-NULL and its first aligned word equals the literal, otherwise zero;
      neither path writes object. Whole-image ARM B/BL-immediate decoding
      found exactly 11 inbound direct call sites, all unconditional plain bl:
      0x080760b4, 0x0808e174, 0x0809e168, 0x0809f74c, 0x080b4340,
      0x080b4e74, 0x080c5ab4, 0x080c5f08, 0x080d6dc8, 0x080d86bc, and
      0x080e27f0. There are no predicated BL forms or direct B tail callers,
      and no aligned image word equals the entry address, so it is not
      directly dispatched virtually. No behavioral deviations. Host tests
      cover NULL, exact and adjacent non-matching tags, preservation of the
      second word, and absence of mutation.

  # --- recursive tree flag-bit propagation ---
  - address: 0x08157ea0
    ghidra: FUN_08157ea0
    name: propagate_tree_flag_bit_3
    size: 120
    sig: "void propagate_tree_flag_bit_3(TreeNode *node, u32 mode)"
    module: app/tree_flag_propagation
    status: ported
    renames:
      param_1/r0: node
      param_2/r1: mode
      param_1_plus_0x48: flags
      param_1_plus_0xa8: children
      "vtable_plus_0x14": child_walk_mode_query
      FUN_0826d80c: set_tree_node_flag_bit_3
    notes: >-
      Raw osos.dec confirms the 120-byte body from 0x08157ea0 through
      0x08157f18, where the separately linked next function begins. It updates
      bit 3 (0x08) in node+0x48 via 0x0826d80c, then cursor-walks the
      collection at node+0xa8. Each child gets vtable+0x14(child, 0x1100);
      a zero result applies the bit directly, while any nonzero result recurses.
      Whole-image ARM B/BL-immediate decoding finds 11 direct bl callers: ten
      unconditional and blne at 0x081412c8; the recursive bl at 0x08157ee8 is
      included. There is also bne tail caller 0x081412f4 and no aligned image
      word containing the entry address. The child vtable query is not assigned
      a stronger identity. The original has no guard before helper calls, so a
      null yielded child reaches the helper's flag load just as in retailOS.
      Deliberate deviation: the helper is an unported 40-byte leaf, but its
      raw body is exactly the recovered compare-and-set bit operation; it is
      private inline code here rather than a second port or dispatch seam.
      Host tests cover nonzero (including 7) and zero mode, leaf and recursive
      children, preservation of unrelated flag bits, query selector, cursor
      indices, and an empty collection.

  # --- UTF-16 range signed-decimal parse wrapper ---
  - address: 0x080f01c4
    ghidra: FUN_080f01c4
    name: parse_i32_utf16_range
    size: 72
    sig: "i32 parse_i32_utf16_range(const u8 *range)"
    module: strto/range_i32
    status: ported
    renames:
      param_1/r0: range
      local_sp_plus_8: converted
      local_sp: text
      FUN_080f020c: string_from_range
      FUN_082773e0: string_object_copy_construct
      FUN_08277484: string_object_destroy
      FUN_082a50b0: string_object_c_str
      FUN_080e7904: parse_decimal
    notes: >-
      Raw osos.dec confirms exactly 72 code bytes from 0x080f01c4 through the
      pop at 0x080f0208; the separately linked string-from-range converter
      begins at 0x080f020c. Whole-image ARM B/BL-immediate decoding found 11
      inbound call sites, all unconditional plain bl with zero predicated
      forms: 0x080e28bc, 0x081baa20, 0x081baa54, 0x081baa8c, 0x081dca6c,
      0x081fa8b0, 0x081fac44, 0x081fac68, 0x0820b464, 0x0820b4ac, and
      0x0820b6a4. The raw sequence converts range into a StringObject at
      sp+8, copy-constructs a second object at sp, destroys the conversion
      temporary, passes the copy's C string to the signed decimal parser
      0x080e7904, saves that return, destroys the copy, and returns the saved
      i32. The converter and decimal parser remain unported and are the two
      volatile RANGE_I32_OPS defaults, which transmute their retail addresses
      on target and are injected by host tests. The copy constructor, C-string
      accessor, and destructors are direct calls to their existing ports.
      Host tests cover whitespace and signs, empty and non-digit text,
      mid-text termination, wrapping overflow, exact range forwarding,
      copied C-string input, allocation size, and both destructor calls.

  # --- aligned opaque storage transfer ---
  - address: 0x08077444
    ghidra: FUN_08077444
    name: storage_transfer_aligned_blocks
    size: 76
    sig: "i32 storage_transfer_aligned_blocks(const StorageTransferGeometry *geometry, u32 unused, u32 relative_block, u32 relative_block_high, u32 byte_count, void *buffer)"
    module: fs/storage_transfer
    status: ported
    renames:
      param_1/r0: geometry
      param_2/r1: unused
      param_3/r2: relative_block
      param_4/r3: relative_block_high
      param_5/stack: byte_count
      param_6/stack: buffer
      param_1_plus_0x0: block_size
      param_1_plus_0x4: base_block
      FUN_08036f14: __rt_udiv
      FUN_0807dd68: opaque_storage_block_transfer
      DAT_0807dda0: opaque_storage_object
      FUN_08149e10: storage_vtable_dispatch_thunk
    notes: >-
      Raw osos.dec confirms exactly 76 code bytes (19 ARM words) at
      0x08077444..0x0807748c; 0x08077490 is the independently linked next
      function, so Ghidra's 132-byte extent is wrong. Whole-image ARM
      B/BL-immediate decoding finds exactly 11 inbound sites, all plain
      unconditional bl (cond=e) at 0x080773e8, 0x08077438, 0x0807cdf8,
      0x0807ce40, 0x0807cfac, 0x0807cfd8, 0x08081a84, 0x08098230,
      0x0809851c, 0x080985c0, and 0x080f08dc; there are no predicated
      forms. The body divides byte_count by geometry->block_size through
      __rt_udiv and returns 0x50 when its r1 remainder is nonzero. On an
      exact multiple it adds geometry->base_block to relative_block, then
      tail-branches to 0x0807dd68 with (absolute_block, quotient, buffer).
      That recovered 52-byte helper loads its opaque object from
      0x089cfd04, returns 2 when absent, otherwise invokes the virtual thunk
      0x08149e10 with (absolute_block, block_count, buffer, 0). No stronger
      backend identity is inferred. The original also calculates the high
      half of base_block + relative_block but overwrites it before the tail
      branch; the port preserves the ABI input but deliberately omits this
      dead arithmetic. Host tests prove misaligned-byte rejection without a
      dispatch, zero and multi-block dispatch, wrapping base addition,
      return-status forwarding, and the ignored high word.

  # --- Nested object +0x0c value accessor ---
  - address: 0x08070c40
    ghidra: FUN_08070c40
    name: nested_object_value
    size: 12
    sig: "u32 nested_object_value(const NestedObjectOwner *owner)"
    module: cxx/nested_object_value
    status: ported
    renames:
      param_1/r0: owner
      "owner+0x0": nested (u32 target pointer)
      "nested+0xc": value
      r0_after_first_ldr: nested
      r0_after_second_ldr: value
    notes: >-
      True raw extent is exactly 12 bytes, 0x08070c40..0x08070c4c: `ldr
      r0,[r0]; ldr r0,[r0,#0xc]; bx lr`. The separately linked next function
      begins with `cmp r0,#0` at 0x08070c4c. Decoding every ARM
      B/BL-immediate word in osos.dec finds exactly 11 direct inbound calls,
      all unconditional plain BL at 0x0805f714, 0x0806fed0, 0x08070754,
      0x08070840, 0x08070f7c, 0x08071140, 0x080711c8, 0x080a2938,
      0x080b24c4, 0x080ce250, and 0x080ebc84; there are no predicated direct
      BL forms and no direct tail B callers. It follows owner+0x00 as a
      32-bit target pointer, then returns that nested object's word at +0x0c.
      Neither load checks for NULL. The concrete class and field identities
      remain unrecovered, so the name describes the observed nested value
      access rather than inventing an object identity. Host tests use a
      dedicated below-4-GiB fixture and prove both the +0x0c word selection
      and following a changed owner pointer.

  # --- C string dotted-prefix comparison ---
  - address: 0x082dacdc
    ghidra: FUN_082dacdc
    name: cstr_dotted_prefix_compare
    size: 72
    sig: "i32 cstr_dotted_prefix_compare(const char *candidate, const char *prefix)"
    module: util/cstr_dotted_prefix_compare
    status: ported
    renames:
      param_1/r0/r4: candidate
      param_2/r1/r5: prefix
      iVar1/r0/r6: prefix_len
      uVar2/r0: comparison_result
      FUN_08392478: strlen
      FUN_0803105c: strncmp
    notes: >-
      Raw ARM extent is exactly 72 bytes, 0x082dacdc..0x082dad24: the next
      separately linked function starts with `push {r4,r5,lr}` at 0x082dad24.
      Decoding every ARM B/BL-immediate word in osos.dec finds exactly 10
      direct inbound calls, all unconditional plain BL at 0x0808a904,
      0x0808a984, 0x0808cc20, 0x080934bc, 0x080934f0, 0x08396b8c,
      0x08396ba4, 0x08396bbc, 0x08396c24, and 0x08396c64; there are no
      predicated forms. It measures prefix through the unguarded strlen,
      bounded-compares candidate against it, forwards a nonzero strncmp
      difference, and otherwise accepts only NUL or '.' immediately after
      the prefix. Neither input has a NULL guard. Rust uses volatile
      function-pointer loads solely to retain the two original call
      boundaries against LLVM inlining; host tests cover complete and dotted
      matches, continuation rejection, comparison-result forwarding, an
      empty prefix, and a prefix with trailing bytes after its NUL.

  # --- Synchronized opaque-pointer FIFO enqueue ---
  - address: 0x0839e5f8
    ghidra: FUN_0839e5f8
    name: pointer_queue_enqueue
    size: 72
    sig: "void pointer_queue_enqueue(PointerQueue *queue, void *value)"
    module: app/pointer_queue
    status: ported
    renames:
      param_1/r4: queue
      param_2/r7: value
      iVar2/r0: wrapper_node
      "queue+0x4": pending_list
      "queue+0xc": mutex
      "queue+0x14": ready_condvar
      FUN_082aadd4: operator_new
      FUN_080f1158: list_push_back
      FUN_080744d8: condvar_signal
      FUN_0807f5c4: mutex_lock
      FUN_0807f6a0: mutex_unlock
    notes: >-
      Raw ARM confirms the exact 72-byte extent 0x0839e5f8..0x0839e640:
      eighteen instruction words end in `pop {r4-r8,lr}; b mutex_unlock`,
      and the distinct next function begins at 0x0839e640 with
      `push {r4,r5,r6,lr}`. Under queue+0xc it allocates 8 bytes through the
      already ported operator_new, stores the incoming opaque value at node+4,
      appends its node+0 link to the list at queue+4, signals the condvar at
      queue+0x14, then unlocks. All ten direct inbound B/BL words are plain
      unconditional BL at 0x081decfc, 0x081df084, 0x081df0bc, 0x081df10c,
      0x081df14c, 0x081df184, 0x081df1bc, 0x081df1f4, 0x081df230, and
      0x081df26c; no predicated direct calls or tail branches target it. A
      full data-word scan finds no reference to 0x0839e5f8. The callers build
      12-byte command records and pass their pointers, but this function only
      owns the 8-byte wrapper. No semantic deviation: Rust calls the ported
      mutex_unlock instead of reproducing the final ARM tail branch, and
      discards its scratch return.

  # --- FAT directory cursor synchronizer ---
  - address: 0x082b2014
    ghidra: FUN_082b2014
    name: fat_cursor_synchronize
    size: 140
    sig: "void fat_cursor_synchronize(FatCursor *cursor)"
    module: fs/fat_cursor
    status: ported
    renames:
      param_1/r0: cursor
      "cursor+0x0": state
      "cursor+0xc": current_cluster
      "cursor+0x10": current_block
      "cursor+0x1c": advance
      "state+0x0": volume
      "state+0x4": entry
      FUN_082e1378: fat_dirent_start_cluster (ported, fs/fat_dirent)
      FUN_082e01cc: fat_cluster_to_block_index (ported, fs/fat_cluster_to_block)
      FUN_082e0378: fat_next_cluster (ported, fs/fat_next_cluster)
    notes: >-
      Raw ARM confirms the exact 140-byte extent 0x082b2014..0x082b20a0:
      35 instruction words end in `pop {r4-r6,pc}` and the separately entered
      next function begins at 0x082b20a0 (`mov r0,sp; mov r1,#8; b
      0x080441e4`). The decoded body ignores Ghidra's second parameter. It
      initializes a zero current cluster from state volume/entry through
      fat_dirent_start_cluster, translates a nonzero cluster through
      fat_cluster_to_block_index, and optionally reads fat_next_cluster. A
      nonzero next cluster updates the block, clears advance, then clears the
      current cluster for reinitialization; zero preserves the cursor. A decode
      of every ARM B/BL-immediate word finds eight direct inbound branches, all
      unconditional BL at 0x082b19b4, 0x082b1c64, 0x082b1ccc, 0x082e5ebc,
      0x082e620c, 0x082e62d0, 0x082e65b8, and 0x082e6650. No predicated
      branch, tail branch, or aligned data word targets the entry. No semantic
      deviation: the port directly calls all three already ported FAT helpers;
      host tests use a dedicated below-4-GiB target-pointer fixture and the
      shared FAT cache-value seam to prove initialization, cached no-op,
      successful advance, and unavailable-next preservation.

  # --- Mounted-volume descriptor cursor seek wrapper ---
  - address: 0x082e628c
    ghidra: FUN_082e628c
    name: volume_seek
    size: 128
    sig: "u64 volume_seek(i32 index, u32 unused, u32 offset_low, i32 offset_high, u32 origin)"
    module: fs/volume_seek
    status: ported
    renames:
      param_1/r0: index
      param_2/r1: unused
      param_3/r2: offset_low
      param_4/r3: offset_high
      param_5/[sp]: origin
      r4: descriptor
      r5: ata_device
      r6: ata_error
      r7: result_high
      r8: saved_offset_high
      r9: saved_origin
      FUN_082e0e1c: volume_table_lookup (ported, fs/volume_table)
      FUN_082d7934: ata_semaphore_wait (ported, drivers/ata_semaphore)
      FUN_082b2014: fat_cursor_synchronize (ported, fs/fat_cursor)
      FUN_082b1784: resident cursor seek (unrecovered)
      FUN_082d7954: ata_semaphore_signal (ported, drivers/ata_semaphore)
      FUN_083690a8: ata_report_error (ported, drivers/ata_cmd)
    notes: >-
      Raw ARM extent is exactly 128 bytes, 0x082e628c..0x082e630c: 32
      instruction words, ending in `mov r0,r4; mov r1,r7; pop
      {r3-r9,pc}`. The next independently entered entry begins at
      0x082e630c (`mvn r3,#0; mov r0,r0`) before its shared body at
      0x082e6314. The wrapper overwrites r1 with zero for
      volume_table_lookup(index, 0); on NULL it forms the full -1 r0/r1
      result and reports ATA error 9. Otherwise it follows descriptor+0
      then that object's +0 word to load the ATA u16 semaphore index at
      +0x78, waits, calls ported fat_cursor_synchronize(descriptor), and
      calls unrecovered FUN_082b1784 with descriptor, r2/r3 offset, and stack
      origin. It always signals after a successful lookup, reports error 0,
      then restores the cursor seek's r0/r1 result pair. A decode of every ARM
      B/BL-immediate word in osos.dec finds 10 direct inbound branches: nine
      unconditional BL at 0x081bca9c, 0x081bcb40, 0x081bd24c, 0x081bd2b8,
      0x081be02c, 0x081be168, 0x082e5c44, 0x082e5c74, and 0x082e5dc8; plus
      predicated BLLT at 0x082e5cf4. That conditional reach is gated by its
      caller's signed comparison; this wrapper has no added guard. No tail B
      branch and no aligned data word in the full decrypted image targets this
      address. Target builds call the ported cursor synchronizer, volume-table,
      ATA semaphore, and ATA error ports; only the cursor-seek entry remains a
      typed fixed-address boundary. Host tests install volatile recorders and
      use a dedicated below-4-GiB raw-u32 fixture to prove nested
      +0/+0/+0x78 selection, ported cursor synchronization, lock/seek/unlock/
      status order, complete seek argument forwarding, and missing-descriptor
      early failure.

  # --- Opaque descriptor lookup veneer ---
  - address: 0x082c5814
    ghidra: FUN_082c5814
    name: lookup_opaque_descriptor
    size: 12
    sig: "void *lookup_opaque_descriptor(void **output, void *lookup_context, usize lookup_key)"
    module: cxx/opaque_descriptor_lookup
    status: ported
    renames:
      param_1/r0: output
      param_2/r1: lookup_context
      param_3/r2: lookup_key
      DAT_082c581c/r3: opaque_descriptor_0x0890b608
      FUN_0803a7f0: generic_descriptor_lookup_helper
    notes: >-
      EXTENT: raw osos.dec is 12 bytes, not Ghidra's reported 8:
      ldr r3,[pc]; b 0x0803a7f0; literal 0x0890b608. The next separately
      linked function starts at 0x082c5820 (mov r2,r1), proving the literal
      belongs here. CALL COUNT: decoding every ARM B/BL word in osos.dec
      found exactly 10 direct inbound calls, all unconditional plain bl at
      0x080aef00, 0x080aefa4, 0x080af034, 0x080af0f8, 0x080af20c,
      0x08272a28, 0x082d42c8, 0x082d462c, 0x082d48e0, and 0x082d49f0;
      there are no predicated calls or direct tail branches. ALGORITHM:
      retain r0-r2, load the fixed opaque descriptor in r3, and tail-branch
      to 0x0803a7f0. Its raw entry delegates to 0x0803a99c and returns its
      output pointer only when that call is positive, otherwise NULL. The
      descriptor has no recoverable concrete identity, despite other wrappers
      also loading 0x0890b608, so no type was invented. A complete aligned
      data-word scan finds no reference to 0x082c5814, ruling out virtual
      dispatch. ARM global assembly preserves the raw tail transfer. Host
      tests install a recording seam and prove NULL output forwarding,
      non-NULL argument forwarding, the fixed descriptor, and result
      forwarding. Deliberate deviations: none.

  # --- SQLite prepared statement finalizer ---
  - address: 0x083906d4
    ghidra: FUN_083906d4
    name: sqlite3_finalize
    size: 32
    sig: "i32 sqlite3_finalize(Vdbe *statement)"
    module: sqlite/finalize
    status: ported
    renames:
      param_1/r0/r4: statement
      FUN_08391d64: stmt_lru_remove
      FUN_0838af84: sqlite3VdbeFinalize
    notes: >-
      Raw ARM confirms the exact 32-byte extent 0x083906d4..0x083906f4:
      cmp r0,#0; push {r4,lr}; popeq {r4,pc}; mov r4,r0; bl 0x08391d64;
      mov r0,r4; pop {r4,lr}; b 0x0838af84. The next separately linked
      function begins at 0x083906f4. SQLite 3.5.9's vdbeapi.c identifies this
      composition as sqlite3_finalize: NULL returns SQLITE_OK (zero); otherwise
      stmtLruRemove detaches the Vdbe from the statement-memory LRU, then
      sqlite3VdbeFinalize halts/destroys it and supplies the result code. The
      LRU helper's raw +0x150/+0x154 prev/next unlink verifies that identity.
      Decoding every ARM B/BL-immediate word in osos.dec finds 10 direct
      inbound BL sites: eight unconditional at 0x082ccf10, 0x082ccf30,
      0x082ccf8c, 0x08381544, 0x0838f170, 0x0838f440, 0x083901d8, and
      0x083904e0; two BLNE at 0x0838f648 and 0x08390558. The predicated sites
      prove non-NULL before the call; the remaining eight depend on this
      function's own guard. Neither callee is ported, so target builds retain
      exact typed retail-address calls through volatile seams and host tests
      install recorders to prove NULL suppression plus non-NULL
      unlink-then-finalize ordering and status forwarding. No mutex operation
      exists in the verified retail body and none is added. Host verification:
      cargo test --target x86_64-unknown-linux-gnu (6435 passed).

  # --- application byte source priority accessor ---
  - address: 0x082725c0
    ghidra: FUN_082725c0
    name: byte_source_at
    size: 44
    sig: "u8 byte_source_at(const ByteSource *source, usize index)"
    module: app/byte_source
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: index
      "source+0x00..+0x04": inline_bytes
      "source+0x08": fallback_bytes
      "source+0x0c": override_bytes
      cVar1/r2: override_byte
    notes: >-
      Raw binary confirms the 44-byte extent, eleven ARM words from
      0x082725c0 through 0x082725e8; the next separately linked function
      starts with push {r4,r5,lr} at 0x082725ec. Decoding every ARM
      B/BL-immediate word in osos.dec finds exactly 10 inbound call sites,
      all unconditional bl: 0x08103204, 0x08117f20, 0x08118fc8, 0x08119178,
      0x081198b0, 0x08119bb4, 0x0827261c, 0x0828a3c0, 0x0828a3dc, and
      0x0828b304. No predicated or tail calls occur; the callee has no
      receiver, bounds, or alignment guard beyond checking the two optional
      pointer fields. It reads override_bytes[index] first and returns it
      only when nonzero; a zero override defers to fallback_bytes[index] when
      fallback_bytes is non-NULL, otherwise to inline_bytes[index]. A complete
      aligned data-word scan finds no 0x082725c0 reference, so no virtual
      dispatch seam is needed. Deviation: none. Host tests prove nonzero
      override precedence at every embedded index, zero override fallback
      including a zero fallback byte, NULL override fallback, and the
      all-NULL embedded-byte path.

  # --- opaque object masked-word refresh ---
  - address: 0x082c43d0
    ghidra: FUN_082c43d0
    name: object_masked_word_refresh
    size: 32
    sig: "void object_masked_word_refresh(u32 *object)"
    module: util/object_masked_word_refresh
    status: ported
    renames:
      param_1/r0/r4: object
      "object+0x00": sqlite_connection
      "object+0x74": result_code
      FUN_0836f468: sqlite_api_exit
    notes: >-
      Raw osos.dec confirms the 32-byte extent, eight ARM words from
      0x082c43d0 through pop {r4,pc} at 0x082c43ec; the next separately
      linked function starts at 0x082c43f0. Decoding every ARM B/BL-immediate
      word finds exactly 10 direct inbound call sites, all unconditional plain
      bl (none predicated): 0x0838f9c8, 0x0838f9ec, 0x0838fa10, 0x0838fa64,
      0x0838fa8c, 0x0838fab4, 0x0838fafc, 0x0838fb20, 0x0838fb40, and
      0x0838fb60. NULL returns immediately. Otherwise it passes the raw SQLite
      connection pointer in object word zero and object word 29 (+0x74) to the
      directly ported `sqlite_api_exit`, then replaces word 29 with its result.
      Both the direct callee at 0x0836f468 and its `sqlite_error` dependency at
      0x083766f4 are ported, so target and host builds call the Rust
      implementation without a retail-address seam. Deliberate deviation: host
      fixtures and accesses use target-width u32 words so +0x74 remains word 29
      despite 64-bit host pointers. Host tests prove NULL suppression, raw
      connection forwarding, healthy result masking, and the `mallocFailed`
      result replacement.

  # --- opaque keyed collection item count ---
  - address: 0x0829e2e0
    ghidra: FUN_0829e2e0
    name: opaque_keyed_collection_item_count
    size: 24
    sig: "u32 opaque_keyed_collection_item_count(const void *collection, u32 selector)"
    module: app/opaque_keyed_collection_item_count
    status: ported
    renames:
      param_1/r0: collection
      param_2/r1: selector
      FUN_0829e310: opaque_keyed_collection_vector_select
      "vector+0x00": begin
      "vector+0x04": end
      "end-begin": byte_span
    notes: >-
      Raw osos.dec confirms the 24-byte extent: push {r4,lr}; bl 0x0829e310;
      ldm r0,{r0,r1}; sub r0,r1,r0; asr r0,r0,#2; pop {r4,pc}, from
      0x0829e2e0 through 0x0829e2f4. The next separately entered function
      begins at 0x0829e2f8; no literal-pool word belongs to this body.
      Decoding every ARM B/BL immediate in osos.dec found exactly eight direct,
      unconditional plain bl callers — 0x0817eadc, 0x0817ec04, 0x0817fb40,
      0x0817fb50, 0x0817fe1c, 0x0817fe2c, 0x0818225c, and 0x081836dc — with
      no predicated calls. No 32-bit data word in osos.dec equals 0x0829e2e0.
      The wrapper passes collection and selector unchanged to unported
      FUN_0829e310, then returns the signed target-width `(end - begin) >> 2`
      from that vector head; it has no NULL guard or writes. No concrete
      collection or selector identity is claimed. Deliberate deviation:
      instead of a new stub, the port reuses opaque_keyed_collection_item_at's
      existing retail-address/host-test selector seam; names.yaml has no port
      for 0x0829e310. Host tests prove selector forwarding, normal counts,
      zero count, signed underflow, and read-only vector-head access. match.py
      structural review is expected to differ because Rust loads the seam
      through a static rather than issuing the retail bl.

  # --- opaque keyed collection indexed accessor ---
  - address: 0x0829e2f8
    ghidra: FUN_0829e2f8
    name: opaque_keyed_collection_item_at
    size: 24
    sig: "u32 opaque_keyed_collection_item_at(const void *collection, u32 selector, u32 index)"
    module: app/opaque_keyed_collection_item_at
    status: ported
    renames:
      param_1/r0: collection
      param_2/r1: selector
      param_3/r2/r4: index
      FUN_0829e310: opaque_keyed_collection_vector_select
      "vector+0x00": begin
      "begin+index*4": entry
    notes: >-
      Raw osos.dec confirms the 24-byte extent: push {r4,lr}; mov r4,r2; bl
      0x0829e310; ldr r0,[r0]; ldr r0,[r0,r4,lsl #2]; pop {r4,pc}, from
      0x0829e2f8 through 0x0829e30c. The next separately entered function
      begins at 0x0829e310; no literal-pool word belongs to this body.
      Decoding every ARM B/BL immediate in osos.dec found exactly 10 direct,
      unconditional plain bl callers — 0x0817eaf8, 0x0817ec2c, 0x0817fb68,
      0x0817fe48, 0x0817fec0, 0x0817ff48, 0x0817ffec, 0x08180070,
      0x08182280, and 0x08183650 — with no predicated calls. The wrapper
      passes collection and selector unchanged to unported FUN_0829e310,
      then reads the returned vector head's begin word and the unchecked
      indexed entry. No concrete collection or selector identity is claimed.
      Deliberate deviation: the unported selector is a target retail-address
      call behind a recording host seam, not a new stub; names.yaml had no
      existing port for 0x0829e310. Host tests prove index scaling, raw
      selector forwarding, no selector normalization, and that end is neither
      consulted nor written. match.py structural review is expected to differ:
      Rust loads the seam through a static rather than issuing the retail bl.

  - address: 0x0829b490
    ghidra: FUN_0829b490
    name: object_word_payload_resolve
    size: 48
    sig: "u32 *object_word_payload_resolve(u32 *destination, u32 *object)"
    module: util/object_word_payload_resolve
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: object
      local_20: words
    notes: >-
      Raw osos.dec extent is 48 bytes: 12 ARM words from 0x0829b490 through
      the pop {...,pc} at 0x0829b4c0; the next separately linked function
      begins at 0x0829b4c4. Decoding every ARM B/BL word finds exactly 10
      direct inbound call sites, all unconditional bl (zero predicated forms
      and zero direct tail branches). It copies the four-word payload at
      object + 0x1c to a stack record, calls the unported object payload
      processor at 0x0829b804, then copies that record to destination and
      returns destination by restoring r0 from its saved register set.
      The 0x0829b804 call remains an explicit target-only dispatch seam
      because no names.yaml entry ports that callee; host tests model it.

  # --- four-entry runtime key-table index ---
  - address: 0x08293048
    ghidra: FUN_08293048
    name: four_slot_key_index
    size: 48
    sig: "i32 four_slot_key_index(void *context, u32 key)"
    module: app/four_slot_key_index
    status: ported
    renames:
      param_1/r0: context (unused)
      param_2/r1: key
      DAT_08293074: slot_key_table
      iVar1/r0: index
    notes: >-
      Raw osos.dec establishes a 48-byte extent: 44 bytes of ARM code from
      0x08293048 through the `bx lr` at 0x08293070, then its PC-relative
      table-address literal 0x089d04c4 at 0x08293074; the separately linked
      next function starts at 0x08293078. Decoding every ARM B/BL-immediate
      word found exactly 10 inbound direct call sites — 0x08292fb8,
      0x08293208, 0x08293328, 0x082933a0, 0x08293408, 0x082937dc,
      0x08293984, 0x08293bb8, 0x08293d50, and 0x08294dc0 — all unconditional
      plain `bl`, with no predicated forms or direct tail branches. Algorithm:
      ignore context, search the leading byte of four 24-byte runtime table
      records for a key equal to the full r1 value, and return its first index;
      return -1 after the fourth mismatch. Since ARM `ldrb` zero-extends
      before `cmp r3,r1`, keys above 0xff never match. The table's concrete
      record meaning is unrecovered and is not claimed. Deliberate deviation:
      ARM reads the original fixed runtime table at 0x089d04c4; host tests use
      equivalent private backing storage. The Rust read is volatile to retain
      the table's runtime-mutability across calls. Host tests cover first,
      middle duplicate, final, absent, and full-width nonmatching keys.

  # --- global callback unregistration ---
  - address: 0x082928d8
    ghidra: FUN_082928d8
    name: global_callback_unregister
    size: 172
    sig: "void global_callback_unregister(u32 callback)"
    module: app/global_callback_unregister
    status: ported
    renames:
      param_1/r0: callback
      DAT_08292984: callback_list_mutex
      DAT_08292988: callback_dispatch_active
      DAT_0829298c: callback_list
      DAT_08292990: deferred_callback_removals
      local_1c: cursor
      local_14: end
    notes: >-
      Raw osos.dec establishes 172 bytes of code from 0x082928d8 through
      pop {r4,r5,pc} at 0x08292880, followed by the four-word literal pool
      at 0x08292884..0x08292990; the next independently linked function
      starts at 0x08292994. It locks the C++ mutex at 0x08ad2c4c through the
      already ported 0x082621a8 alias, then branches on byte 0x089cfd54.
      A clear byte walks the circular std::list headed by the sentinel word
      at 0x08ad2c68 + 0x10 and erases only the first node whose +0x08 callback
      word equals r0; a set byte does not read that list and appends the raw
      callback word, including zero, to the deferred-removal list at
      0x08ad2c80. It always unlocks through the ported 0x082621ac alias and
      ignores both mutex statuses. Decoding every ARM B/BL immediate in
      osos.dec finds exactly 10 direct inbound bl sites: six unconditional
      (0x081426b8, 0x0816e49c, 0x081b8954, 0x081dae74, 0x081dbfd4,
      0x08292734), two bleq (0x08198f04, 0x081b8d44), and two blne
      (0x081a13d4, 0x081eaa7c). Thus the two caller-side predicates gate
      invocation; this helper carries no NULL guard. List erase
      0x083dd5f4 and append 0x083dd660 have no ported ledger entries, so
      target builds retain direct fixed-address calls and host tests use a
      recording seam. Tests cover first-match-only removal with duplicates,
      absent callback preservation, zero callback deferral without touching
      a poisoned main-list sentinel, and lock/unlock completion. match.py
      structurally differs at the host-test seam indirection, as expected.

  # --- JPEG stream byte reader ---
  - address: 0x082105b0
    ghidra: FUN_082105b0
    name: jpeg_stream_read_byte
    size: 76
    sig: "u32 jpeg_stream_read_byte(JpegStream *stream)"
    module: jpeg/stream_read_byte
    status: ported
    renames:
      param_1/r0: stream
      "*(u32 *)(stream + 0x04)": stream->error_status
      "*(u32 *)(stream + 0x08)": stream->file_handle
      "*(u32 *)(stream + 0x14)": stream->bytes_read
      local_08: transferred
      local_04: byte
    notes: >-
      Raw osos.dec establishes the exact 76-byte extent: sixteen ARM words
      from 0x082105b0 through pop {r2,r3,r4,pc} at 0x082105f8; the next
      separately linked function begins at 0x082105fc. Decoding every ARM
      B/BL immediate in the image found exactly 10 direct inbound bl sites,
      all unconditional (0x0821063c, 0x08210648, 0x08210654, 0x08210660,
      0x08210688, 0x082106b0, 0x082106f0, 0x08210704, 0x0821071c, and
      0x0821072c), with no predicated call or tail B. A null handle at +0x08
      stores -7 at +0x04 and returns zero. Otherwise it initializes the
      transferred local to zero, calls the already ported retail_file_read
      (0x082784b8) as (handle, 1, &byte, &transferred), ignores its status
      and transferred result, increments +0x14 with u32 wraparound, and
      returns the zero-extended byte. The direct callers consume JPEG markers
      and big-endian word values, which identifies this as the JPEG parser's
      stream byte primitive. No new dispatch seam was added: the preexisting
      file-read wrapper preserves the stock zero control word and its host
      test boundary. Host tests cover the null-handle error latch, ignored
      file-read failure/short transfer with a high-bit byte, and counter
      wraparound. match.py reports the same null-handle branch, one-byte
      read setup/call, post-call counter increment, byte load, and return;
      its larger Rust frame and different local-byte alignment are expected.

  # --- draw-state text-width adapter ---
  - address: 0x08263384
    ghidra: FUN_08263384
    name: draw_state_text_width
    size: 20
    sig: "u32 draw_state_text_width(DrawStateTextMeasureFields *draw_state, const u8 *text)"
    module: cxx/draw_state_text_width
    status: ported
    renames:
      param_1/r0: draw_state
      param_2/r1: text
      "*(u32 *)(draw_state + 0x1c)": renderer
      "draw_state + 0x20": layout_state
      "*(u8 *)(draw_state + 0x28)": layout_flags
      FUN_080878d8: text_width_accumulator
    notes: >-
      Raw ARM establishes the complete five-word extent:
      mov r2,r0; ldrb r3,[r0,#0x28]; ldr r0,[r0,#0x1c];
      add r2,r2,#0x20; b 0x080878d8. The next separately linked entry begins
      at 0x08263398, so Ghidra's 20-byte extent has neither a trailing literal
      nor swallowed sibling. This is a draw-state adapter, not a veneer: it
      preserves the caller's NUL-terminated text pointer in r1 while selecting
      the draw-state renderer word, embedded two-word layout state, and
      zero-extended layout-flags byte before tail-calling the shared text-width
      accumulator. The unported 0x080878d8 accumulator initializes its layout
      iterator, decodes permissive UTF-8 codepoints through the already ported
      0x0807a0c8 decoder, and sums their glyph widths through 0x08080f18.
      Decoding every ARM B/BL immediate in osos.dec finds exactly 10 inbound
      direct bl instructions, all unconditional and no plain B:
      0x08140f48, 0x081b2f84, 0x081b3028, 0x081ea820, 0x081f42f0,
      0x0820d71c, 0x08263a80, 0x08263a90, 0x08290cb8, and 0x082911ac.
      No DATA word references 0x08263384, so this adapter is not virtually
      dispatched. Deliberate implementation deviation: the tail branch becomes
      a volatile typed boundary to the resident unported accumulator
      (0x080878d8), which keeps the production ABI and permits host recording.
      Tests prove the renderer/text/layout-state forwarding, returned width,
      byte widening of flags (including high bytes discarded by ldrb), and
      forwarding of a null text pointer without inventing a guard. match.py
      structurally differs only at the Rust boundary and frame, as expected.

  # --- UI resource C-string value adapter ---
  - address: 0x08290450
    ghidra: FUN_08290450
    name: ui_resource_value_from_cstr
    size: 44
    sig: "u32 ui_resource_value_from_cstr(const u8 *resource_name)"
    module: ui/resource_value
    status: ported
    renames:
      param_1/r0: resource_name
      uStack_c: temporary_cow_string
      FUN_083d8b5c: cxx_string_from_cstr
      FUN_083d8b04: cxx_string_release
    notes: >-
      Raw osos.dec establishes the exact 44-byte extent
      0x08290450..0x0829047c: push {r2,r3,r4,lr}; move resource_name from r0
      into r1; place the one-word COW-string temporary at sp+4; call
      cxx_string_from_cstr @ 0x083d8b5c; call the separately entered helper at
      0x0829047c; retain its r0 return in r4; release the temporary through
      cxx_string_release @ 0x083d8b04; and return the saved word. The next
      function begins at 0x0829047c with `mov r1,r0; push {r4,lr}`, so Ghidra's
      44-byte extent has no literal-pool tail or swallowed sibling. Decoding
      every ARM B/BL immediate in osos.dec finds exactly 10 inbound direct bl
      sites, all unconditional with no predicated bl or direct b: 0x08159318,
      0x0815ea04, 0x0815ea10, 0x0815ea1c, 0x081a3324, 0x081a3434,
      0x081a3460, 0x081a3470, 0x081a34a4, and 0x081a4a54. Callers pass UI
      resource names (including Stopwatch and Voice Memo image names) and
      consume the mapped word. The 0x0829047c helper is deliberately retained
      as a fixed-address target boundary: raw code loads its map global from
      0x08ad7c6c and calls the separately unported 0x083db75c, whose exact map
      class is not established. The Rust port uses the existing COW-string
      constructor/release ports and reaches 0x0829047c through a typed
      fixed-address boundary on ARM; host tests install the same boundary.
      Tests cover exact non-empty byte forwarding through the temporary, its
      heap release, and the empty name with a zero lookup result. match.py
      structurally differs in the boundary call and Rust frame but retains
      construct/call/release order.

  - address: 0x081fbb24
    ghidra: FUN_081fbb24
    name: callback_queue_instance_get
    size: 12
    sig: "void *callback_queue_instance_get(void)"
    module: app/callback_queue
    status: ported
    renames: {r0: callback_queue_instance}
    notes: >-
      Raw ARM is ldr r0,[pc,#4]; ldr r0,[r0]; bx lr. The 12-byte body is
      followed by its separate literal-pool word 0x089cfcc8; the next
      function starts at 0x081fbb34. Decoding every ARM B/BL word in
      osos.dec finds exactly 10 direct callers, all unconditional bl and no
      predicated forms. The word is initialized by FUN_081fb5e0 with its
      0x6c-byte callback-queue object. Target builds load the runtime-owned
      global word directly; host builds use a native-width static pointer
      because the firmware's 32-bit global cannot hold a 64-bit host pointer.

  # --- UI indexed-element selection ---
  - address: 0x081ea758
    ghidra: FUN_081ea758
    name: ui_element_set_selection_index
    size: 16
    sig: "void *ui_element_set_selection_index(void *element, u32 selection_index)"
    module: ui/selection_index
    status: ported
    renames:
      param_1/r0: element
      param_2/r1: selection_index
      FUN_0826ec9c: ui_element_invalidate
    notes: >-
      Raw osos.dec establishes the exact 16-byte extent from 0x081ea758
      through the tail b to ui_element_invalidate at 0x081ea764; the next
      separately linked function opens at 0x081ea768 with mov r3,#0. The
      body stores selection_index to element+0x124, sets the element+0x120
      change byte to one, then invalidates its whole bounds and returns that
      function's restored element pointer. Decoding every ARM B/BL word finds
      exactly 10 direct call sites: two unconditional bl, seven blne, and one
      blcs; no direct b reaches this entry. The seven blne callers guard
      nullable lookup results, while the blcs caller at 0x081ea7c4 clamps the
      new selection index to its element-owned count. Ghidra's purported body
      includes the independently entered 0x081ea768 sibling; the port follows
      raw ARM instead. No deviations: the existing ui_element_invalidate port
      is the verified target of the retail tail branch.

  # --- Identified vtable-object constructor ---
  - address: 0x081f6080
    ghidra: FUN_081f6080
    name: identified_vtable_object_construct
    size: 68
    sig: "void *identified_vtable_object_construct(void *this, u32 input_at_16, u32 input_at_20, u8 flag_at_48, u8 flag_at_49)"
    module: cxx/identified_vtable_object_construct
    status: ported
    renames:
      param_1/r0: this (aligned writable derived-object prefix, returned through base r0)
      param_2/r1: input_at_16
      param_3/r2: input_at_20
      param_4/r3: flag_at_48
      param_5/stack: flag_at_49
      DAT_081f60c0: derived_vtable_08990364
      FUN_08274f10: identified_vtable_base_construct (unported exact default behind ops seam)
      DAT_08274f3c: base_vtable_089a5f98
      DAT_08274f40: base_id_counter_08a09f00
    notes: >-
      Raw osos.dec establishes a 68-byte extent: sixteen instruction words at
      0x081f6080..0x081f60bc followed by the derived-vtable literal
      0x08990364 at 0x081f60c0; the next separately linked function begins
      at 0x081f60c4 with cmp r0,#0. Ghidra's reported 64 bytes omit only that
      literal-pool word. Decoding every ARM B/BL immediate finds exactly ten
      inbound direct calls, all unconditional bl (0x0810dd8c, 0x0810ed58,
      0x0811029c, 0x0811e7f0, 0x081307a0, 0x081443b8, 0x08158b24,
      0x0816df30, 0x081df6f8, 0x08205d5c), with zero predicated forms, zero
      direct tail b, and zero aligned data-word references. The unguarded
      constructor calls 0x08274f10, then addresses every derived write from
      its r0 return: it installs 0x08990364, clears +0x40/+0x44, stores r3
      and the fifth stack byte at +0x48/+0x49, and copies r1/r2 to
      +0x10/+0x14. The fully decoded but independently unported base installs
      0x089a5f98, clears +8/+12, assigns this+4 from 0x08a09f00, and
      increments that counter. The port's default ops-seam callee reproduces
      that sequence exactly; the seam is the deliberate deviation required
      until 0x08274f10 is ported. The ten callers are derived constructors
      that overwrite the vtable again and initialize fields beginning at
      +0x50, so no class identity beyond this verified base behavior is
      invented. Host tests cover all stores, preserved gaps, counter wrapping,
      and the base-return-derived write target. match.py reports the expected
      dispatch-seam frame/call diff while retaining the base call followed by
      the same derived store order.

  # --- Fixed settings-dispatcher singleton accessor ---
  - address: 0x0820a008
    ghidra: FUN_0820a008
    name: setting_dispatcher_get
    size: 88
    sig: "void *setting_dispatcher_get(void)"
    module: drivers/setting_dispatcher
    status: ported
    renames:
      DAT_0820a050: setting_dispatcher_guard_08a09d68
      DAT_0820a054: setting_dispatcher_08ae4978
      DAT_0820a058: __dso_handle_089ca09c
      DAT_0820a05c: invalid_setting_dispatcher_destructor_082112fc
      FUN_0821c198: setting_dispatcher_construct (unported fixed-address seam)
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab1c8: cxa_atexit
      FUN_082ab338: cxa_guard_release
    notes: >-
      Raw osos.dec establishes an 88-byte extent: the 72-byte instruction
      body at 0x0820a008..0x0820a050 followed by four literal-pool words
      through 0x0820a060; the next separately entered function starts at
      0x0820a060. The body tests guard bit 0, calls cxa_guard_acquire, calls
      the unported in-place constructor 0x0821c198 on fixed storage
      0x08ae4978, registers the constructor result with cxa_atexit using
      word 0x082112fc and __dso_handle 0x089ca09c, releases the guard, then
      reloads and returns the fixed object literal rather than the
      constructor result. Decoding every ARM B/BL word in osos.dec finds
      exactly 10 inbound direct call sites, all unconditional bl
      (0x08093614, 0x0809367c, 0x080936b4, 0x08093738, 0x080d9ba0,
      0x081af8d8, 0x081afb2c, 0x081afb78, 0x081c174c, 0x08202238), with
      zero predicated bl and no direct tail b. The vtable installed by the
      unported constructor has a +0x10 method that dispatches selector
      values 0..5 into fixed hardware-setting words, so the port names the
      object for verified behavior rather than inventing its class identity.
      Constructor 0x0821bd10 writes through object+0x1d1, establishing the
      0x1d4-byte storage extent. Word 0x082112fc is not a callable entry:
      its first instruction is bne 0x082112c0 after another function's frame
      setup. The port therefore retains 0x0821c198 as an explicit target
      seam and uses an inert shutdown handler instead of fabricating a
      destructor identity. Host tests cover initialization, repeat calls,
      bit-0-set and bit-0-clear guard paths, constructor-return versus
      getter-return distinction, storage extent, and shutdown preservation.

  # --- Registry-class-0x8e80 singleton accessor ---
  - address: 0x082040ec
    ghidra: FUN_082040ec
    name: singleton_class_8e80
    size: 44
    sig: "void *singleton_class_8e80(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_08204118: class_8e80_cache_089cc388
      FUN_0820416c: class_8e80_construct (unported singleton constructor)
      FUN_082aadd4: operator_new
    notes: >-
      Raw osos.dec establishes a 48-byte true extent: eleven instruction
      words at 0x082040ec..0x08204114 followed by the cache-word literal
      0x089cc388 at 0x08204118; the next separately linked function starts
      at 0x0820411c. The 44-byte code body loads and tests that cache, then
      on NULL allocates 0x48 bytes through the already-ported operator_new,
      invokes the unported constructor 0x0820416c, stores its return, reloads
      the cache, and returns it. The constructor registers the object through
      registry_register with literal class id 0x8e80, so the accessor uses
      that verified identity rather than inventing a class name. Decoding
      every ARM B/BL word in osos.dec finds exactly 10 inbound direct call
      sites, all unconditional bl (0x08172218, 0x0817379c, 0x08199464,
      0x081994e0, 0x0819ae48, 0x0819afc0, 0x0819b0a8, 0x08227670,
      0x08227698, 0x082276c0), with zero predicated forms and no direct tail
      b. Deliberate deviations: the unported constructor uses the existing
      SINGLETON_CTORS class_8e80 slot with its family zeroing default, and
      the runtime-initialized cache word is represented by the crate static
      CLASS_8E80_INSTANCE. It is not hook-ready until 0x0820416c is ported.

  # --- Clamped mode-selected position ---
  - address: 0x081cc5b8
    ghidra: FUN_081cc5b8
    name: set_clamped_mode_position
    size: 96
    sig: "void set_clamped_mode_position(void *state, u32 requested_position)"
    module: app/clamped_mode_position
    status: ported
    renames:
      param_1/r0: state
      param_2/r1: requested_position
      "state+0x1c": mode_backend
      "state+0x8b8": cached_position
      "state+0x8bc": lower_position
      "state+0x8c0": upper_position
      FUN_0822aecc: mode_selected_position
      FUN_0822ba50: mode_selected_position_set (unported fixed-address seam)
      FUN_081cccac: mode_position_changed (unported fixed-address seam)
    notes: >-
      Raw osos.dec confirms the exact 96-byte extent: 24 ARM instruction
      words from 0x081cc5b8 through bx lr at 0x081cc614; the next separately
      linked function begins at 0x081cc618. The code applies signed ARM
      comparisons to clamp r1 to state+0x8bc..state+0x8c0, reads the active
      position with the already-ported mode_selected_position on state+0x1c,
      calls unported 0x0822ba50 with r1=1 and r2=the clamped position only
      when that active position differs, then updates state+0x8b8 and tail
      calls unported 0x081cccac only when the cached position differs.
      Decoding every ARM B/BL immediate finds exactly 10 direct bl callers:
      nine unconditional at 0x0819dbb4, 0x0819dcac, 0x081cba70, 0x081cbc3c,
      0x081cc944, 0x081cca64, 0x081cca78, 0x081cce74, and 0x081ccfec, plus
      one predicated blne at 0x081cbfdc. Two unconditional tail b transfers
      enter at 0x081cbbf0 and 0x081cd5b4; no aligned DATA word targets this
      entry. Deliberate deviations: fixed-address stock continuations have
      replaceable host seams, while ARM invokes their verified addresses.
      Host tests cover signed lower and upper clamping, backend equality
      suppression, cached-position update, and independent notification
      suppression.

  # --- Registry-class-0x8780 dispatch state ---
  - address: 0x081a3ed4
    ghidra: FUN_081a3ed4
    name: class_8780_dispatch_state
    size: 52
    sig: "u32 class_8780_dispatch_state(const void *object)"
    module: app/class_8780_dispatch_state
    status: ported
    renames:
      param_1/r0: object
      "object+0x1c": priority_gate
      "object+0x8a": secondary_gate
      "object+0x8c": secondary_value
    notes: >-
      Raw osos.dec confirms Ghidra's 52-byte extent exactly: thirteen ARM
      instructions from 0x081a3ed4 through bx lr at 0x081a3f04, with the
      separately linked next function beginning at 0x081a3f08 and no literal
      pool. Decoding every ARM B/BL immediate in osos.dec finds exactly ten
      inbound direct call sites, all unconditional plain bl: 0x08126378,
      0x0819b2f8, 0x081a2ebc, 0x081a2ef0, 0x081a38a0, 0x081a3bfc,
      0x081a4c2c, 0x081a4e9c, 0x081a4eec, and 0x081a4f3c. There are no
      predicated bl forms, direct tail b callers, or aligned DATA-word
      references. Selects a four-value dispatch state: a nonzero priority
      gate returns 3; otherwise a zero secondary gate returns 0, and a
      nonzero secondary gate returns 1 or 2 according to whether its
      associated value is zero. Callers use 0..=2 for alternate dispatches
      and state 3 as their common readiness gate. The constructor at
      0x081a4f64 zeroes all three observed bytes. Deliberate deviations:
      none; the field names express only this selector's verified precedence,
      not unrecovered business-level state meanings.

  # --- Two-pair derived vtable constructor ---
  - address: 0x081d0d98
    ghidra: FUN_081d0d98
    name: vtable_two_pair_derived_construct
    size: 20
    sig: "void *vtable_two_pair_derived_construct(void *this, const void *src_pair_at_4, const void *src_pair_at_12)"
    module: cxx/vtable_two_pair_derived_construct
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: src_pair_at_4
      param_3/r2: src_pair_at_12
      FUN_0811050c: vtable_two_pair_base_construct
    notes: >-
      Raw osos.dec has exactly 20 instruction bytes at 0x081d0d98 through
      0x081d0da8, then the literal-pool derived-vtable word 0x0898dcb0 at
      0x081d0dac; the next separately linked function starts at 0x081d0db0.
      It forwards r0-r2 to the already ported 0x0811050c base constructor,
      then stores the derived vtable at the returned r0 and returns it.
      Decoding every ARM B/BL immediate found exactly ten inbound calls, all
      unconditional bl (0x08135c74, 0x08135de8, 0x08136130, 0x0813634c,
      0x0816c8f0, 0x0816c9d8, 0x08186f38, 0x081dcb34, 0x081dcc20,
      0x081dcd08); zero predicated bl forms, zero tail b transfers, and zero
      aligned DATA-word references target the entry. Ghidra drops the
      pair-source arguments, but the ARM ABI passes their stale r1/r2 values
      into the base constructor. The concrete class identity is not
      established, so the name describes only the verified construction.
      Deliberate deviation: calls the existing direct base port, with a
      volatile final vtable store retaining the decoded post-call ordering.

  # --- Materialize provider range into StringObject ---
  - address: 0x081d0d4c
    ghidra: FUN_081d0d4c
    name: string_from_range_provider
    size: 32
    sig: "void string_from_range_provider(StringObject *out, Utf16RangeProvider *provider)"
    module: cxx/string_from_range_provider
    status: ported
    renames:
      param_1/r0: out
      param_2/r1: provider
      "provider+0x0": vtable
      "provider+0xc": utf16_range
      "vtable+0x8": materialize_range
      FUN_081d0d6c: materialize_and_copy_range (inlined)
      FUN_080f020c: string_from_range (RANGE_I32_OPS seam)
    notes: >-
      Raw osos.dec confirms the exact 32-byte extent from 0x081d0d4c through
      the pop at 0x081d0d68; the separately linked 44-byte helper begins at
      0x081d0d6c. The wrapper passes a private 8-byte stack range and provider
      to that helper, which dispatches provider vtable slot +0x08 with the
      provider, then copies provider+0x0c/+0x10 into the range. The wrapper
      passes that range directly to the unported UTF-16 converter 0x080f020c
      with the caller's StringObject output. Whole-image ARM B/BL-immediate
      decoding found exactly ten inbound calls, all unconditional plain bl at
      0x08135c80, 0x08135df8, 0x08136140, 0x0813635c, 0x0816c900,
      0x0816c9e8, 0x08186f50, 0x081dcb40, 0x081dcc2c, and 0x081dcd14;
      no predicated forms or aligned DATA words target the entry. The plain
      calls agree with the original's absent NULL guards: invalid pointers
      reach a direct dereference or indirect call. Deliberate deviation: the
      uniquely owned helper 0x081d0d6c is inlined, and the existing
      RANGE_I32_OPS string_from_range seam is reused rather than duplicated.
      Host test proves slot +0x08 dispatch, observes the post-materialization
      [0, 0xffffffff] range, and confirms direct forwarding of the output.

  # --- Layout-state deactivation ---
  - address: 0x081a221c
    ghidra: FUN_081a221c
    name: deactivate_layout_state
    size: 56
    sig: "void deactivate_layout_state(void *this)"
    module: app/layout_state
    status: ported
    renames:
      param_1/r0: this
      "this+0x30": embedded_timer
      "this+0x70": active
      "this+0x74": mutex
      FUN_081a2158: set_layout_state_enabled
      FUN_0812c6b0: timer_stop
      FUN_0807f5c4: mutex_lock
      FUN_0807f6a0: mutex_unlock
    notes: >-
      Raw osos.dec confirms Ghidra's exact 56-byte extent: fourteen ARM
      instructions from push {r4,lr} at 0x081a221c through the tail b
      mutex_unlock at 0x081a2250; the separately linked sibling starts with
      push {r4,lr} at 0x081a2254 and there is no literal pool. Whole-image
      decoding of every ARM B/BL immediate finds ten direct bl callers: one
      unconditional bl at 0x0821c668 and nine caller-gated blne sites at
      0x08216818, 0x08216878, 0x08217650, 0x082176d4, 0x0821cfd4,
      0x0821db50, 0x0821deec, 0x0821fa28, and 0x0821fce0. One additional
      bne tail transfer at 0x0821f494 reaches the entry. No aligned DATA word
      contains the entry address, so it is not a virtual-dispatch target.
      The method locks this+0x74, calls the unported 0x081a2158 with
      (this, 0), stops the embedded timer at this+0x30, clears this+0x70, and
      tail-unlocks; no NULL or active-state guard exists. The callee's wider
      identity is not inferred. Deliberate deviation: existing mutex_lock,
      timer_stop, and mutex_unlock ports are direct target calls; the
      unported propagation callee uses a volatile seam with its firmware
      address on target and a host recorder. Host outer-mutex calls are no-ops
      because the target's 32-bit mutex at +0x74 is misaligned for Rust's
      64-bit-host Mutex representation.

  # --- Layout-state activation ---
  - address: 0x081a2254
    ghidra: FUN_081a2254
    name: activate_layout_state
    size: 132
    sig: "void activate_layout_state(void *this)"
    module: app/layout_state
    status: ported
    renames:
      param_1/r0: this
      "this+0x14": profile_when_volume_byte_is_zero
      "this+0x18": profile_when_volume_byte_is_nonzero
      "this+0x30": embedded_timer
      "this+0x5c": transition_sentinel
      "this+0x68": prepared
      "this+0x70": active
      "this+0x74": mutex
      FUN_081a2158: set_layout_state_enabled (unported direct callee)
      FUN_081eda10: class_8900_path_selector (unported direct callee)
      FUN_0812af10: reset_selected_profile (unported direct callee)
      FUN_081a1848: primary_layout_path (unported direct callee)
      FUN_081a1a90: fallback_layout_path (unported direct callee)
      FUN_081eb0c4: singleton_class_8900
      FUN_081f77a4: volume_controller_get
      FUN_081fa044: volume_controller_byte_at_90
      FUN_0812c6b0: timer_stop
      FUN_0807f5c4: mutex_lock
      FUN_0807f6a0: mutex_unlock
    notes: >-
      Raw osos.dec confirms the exact 132-byte extent: push {r4,lr} at
      0x081a2254 through tail b mutex_unlock at 0x081a22d4; separately linked
      sibling FUN_081a22d8 opens with push {r4,lr}, and there is no literal
      pool. Whole-image decoding of every ARM B/BL immediate finds exactly
      nine inbound direct bl calls: plain bl at 0x0821c660, 0x0821db44, and
      0x0821dee0; caller-gated blne at 0x0821c628, 0x0821e4a0, 0x0821e764,
      0x0821e7bc, 0x0821f710, and 0x0821ff5c. There are no inbound direct b
      transfers or aligned DATA words holding the entry, so it is not a
      virtual-dispatch target. The predicated majority is caller-side gating;
      the body itself has no NULL or active-state guard.

      The method locks this+0x74, stops this+0x30, propagates enabled=1, writes
      -1 to +0x5c, clears +0x68, and sets +0x70. It initializes the ported
      class-0x8900 singleton, calls unported 0x081eda10 with its return, and
      takes the primary path only for an exact result of 1. That path fetches
      the ported volume-controller singleton and selects this+0x14 when its
      raw +0x90 byte is zero or this+0x18 otherwise. The raw r0 return from
      unported 0x0812af10 is passed directly to unported 0x081a1848; every
      other selector calls unported 0x081a1a90 with this. It tail-unlocks in
      either case. Existing mutex, timer, singleton, and volume ports are
      direct calls. The unported direct callees use volatile target-address
      slots; their broader identities are deliberately not inferred. Host
      tests use a unique low u32 fixture to prove both volume-byte profiles,
      the exact selector==1 split, field writes, timer stop, and r0 forwarding.

  # --- TTrackExtrasCache fixed-static accessor ---
  - address: 0x081ba3ac
    ghidra: FUN_081ba3ac
    name: track_extras_cache_get
    size: 192
    sig: "TTrackExtrasCache *track_extras_cache_get(void)"
    module: app/track_extras_cache
    status: ported
    renames:
      DAT_081ba46c: track_extras_cache_guard (0x089cb1ac)
      DAT_081ba470: track_extras_cache (0x08a77828)
      DAT_081ba474: dso_handle (0x089ca09c)
      DAT_081ba478: invalid_shutdown_target (0x081af920)
      FUN_081ba710: track_extras_cache_constructor
      "track_extras_cache+0x38": registration_node
      "track_extras_cache+0x44": client_handle_slot
      "track_extras_cache+0x48": cached_resource
    notes: >-
      Raw osos.dec establishes the exact 192-byte extent: 44 instruction words
      from 0x081ba3ac through the return at 0x081ba468, followed by the four
      literal-pool words at 0x081ba46c..0x081ba478; the distinct next function
      starts at 0x081ba47c. Whole-image decoding of every ARM B/BL immediate
      found exactly ten inbound transfers, all unconditional plain bl at
      0x08066d40, 0x08066d4c, 0x08068f5c, 0x08084e40, 0x08114c80,
      0x08116378, 0x0817c928, 0x081df040, 0x08211444, and 0x08212124; there
      are no predicated forms, tail branches, or aligned data-word references.
      The accessor guard-initializes the fixed TTrackExtrasCache, registers its
      constructor result, then when the two-level client slot at +0x44 resolves
      NULL registers the +0x38 node, lazily caches a 24-byte resource at +0x48,
      and attaches the cache to the root returned by 0x0814b460. Existing
      cxa_guard, cxa_atexit, cxa_guard_release, operator_new, block_manager_get,
      handle_deref_or_null, shared_cell_release_direct, and the existing
      MANAGER_CLIENT_REGISTER seam are reused. The unresolved direct targets
      0x081ba710, 0x083b51f4, 0x081f7450, 0x0814b460, and 0x08124af4 use
      volatile fixed-address target calls and host recorders; no wider callee
      identity is claimed. Literal 0x081af920 is demonstrably inside an extant
      ARM frame (its first word is add sl, sp, #44), not a function entry, so
      the port intentionally registers a no-op shutdown handler instead.

  # --- Vtable flag/payload prefix constructor ---
  - address: 0x081b6968
    ghidra: FUN_081b6968
    name: vtable_flag_payload_construct
    size: 28
    sig: "void *vtable_flag_payload_construct(void *this, u32 payload)"
    module: cxx/vtable_flag_payload_construct
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: payload
      r2: saved_payload
      FUN_08135788: vtable_flag_base_construct
      DAT_081b6984: derived_vtable (0x0898c2a4)
    notes: >-
      Raw osos.dec confirms Ghidra's 28 instruction bytes: mov r2,r1 at
      0x081b6968 through pop {pc} at 0x081b6980, followed by the
      0x0898c2a4 literal-pool word at 0x081b6984; the separately linked
      next function begins at 0x081b6988. Whole-image decoding of every ARM
      B/BL immediate finds exactly ten direct calls, all unconditional plain
      bl at 0x08135ba8, 0x0813ee48, 0x08143a6c, 0x08148790, 0x08177f0c,
      0x08187080, 0x0819cd3c, 0x081ba988, 0x081deb68, and 0x0820b210; no
      predicated bl or plain-b transfer reaches the entry. No aligned DATA
      word references it. The function preserves r1 in r2 across unported
      0x08135788, overwrites the returned object's base vtable with
      0x0898c2a4, writes the preserved payload at +8, and returns the base
      result. The fully decoded 0x08135788 has no existing ledger port, so a
      volatile seam models its stores (base vtable 0x08984948 at +0 and zero
      byte at +4) until it is ported separately; no wider class identity is
      inferred. Host tests cover the literal fields, sentinels, and a base
      return distinct from its input. match.py is structural review only.

  # --- ATA command flag-word getter ---
  - address: 0x081660d8
    ghidra: FUN_081660d8
    name: ata_cmd_get_flags
    size: 8
    sig: "u32 ata_cmd_get_flags(const u8 *cmd)"
    module: drivers/ata_cmd
    status: ported
    renames:
      param_1: cmd
      "cmd+0x10": flags
    notes: >-
      Raw osos.dec establishes the exact 8-byte extent: ldr r0,[r0,#0x10]
      at 0x081660d8 followed by bx lr at 0x081660dc; the separately linked
      next accessor begins at 0x081660e0 with ldr r0,[r0,#0x28]. Whole-image
      decoding of every ARM B/BL immediate finds exactly 10 direct calls, all
      unconditional plain bl at 0x0813b624, 0x08166294, 0x08283274,
      0x08283284, 0x08283324, 0x08283458, 0x08283468, 0x08283798,
      0x08283cac and 0x08283e24; there are no predicated bl forms or
      tail-b transfers. No aligned DATA word contains the entry address, so
      this is not a virtual-dispatch target. Returns the complete u32 ATA
      command flag word at cmd+0x10 without a NULL guard, precisely as the
      raw ldr does; all direct callers invoke it unconditionally. The
      pre-existing ata_cmd_set_flags owns the matching word store. Host tests
      cover zero, individual observed flags, the high status bit and all
      bits set. No deliberate deviations; match.py is structural review only.

  # --- Framework-root global accessor ---
  - address: 0x0814b460
    ghidra: FUN_0814b460
    name: framework_root_get
    size: 16
    sig: "void *framework_root_get(void)"
    module: app/framework_root
    status: ported
    renames:
      DAT_0814b46c: framework_root_global (0x089cb1e0)
      "*framework_root_global": framework_root
    notes: >-
      Raw osos.dec establishes the exact 16-byte extent: ldr r0,[pc,#4] at
      0x0814b460, ldr r0,[r0] at 0x0814b464, bx lr at 0x0814b468, then the
      0x089cb1e0 literal-pool word at 0x0814b46c. The separately linked next
      function begins at 0x0814b470. Whole-image decoding of every ARM B/BL
      immediate finds exactly 10 direct inbound branches, all unconditional
      plain bl at 0x081ba458, 0x081f0738, 0x081f0898, 0x081f1050,
      0x081f1580, 0x08208798, 0x082143a4, 0x082143b0, 0x08280bac, and
      0x08280f1c. There are no predicated forms, tail-b transfers, or aligned
      DATA words that target this entry. It returns the runtime framework-root
      pointer verbatim, including NULL; no initialization or NULL guard exists.
      The decrypted image's 0x089cxxxx RW page contains stale runtime state,
      so the target port reads 0x089cb1e0 directly while host tests model that
      word with a crate static. The dependent track_extras_cache port now calls
      this function directly instead of retaining its fixed-address seam.
      match.py is structural review only.

  # --- UI first collection-item bounds accessor ---
  - address: 0x08146c94
    ghidra: FUN_08146c94
    name: ui_element_copy_first_collection_item_bounds
    size: 96
    sig: "void ui_element_copy_first_collection_item_bounds(void *element, Rect *out)"
    module: ui/first_collection_item_bounds
    status: ported
    renames:
      param_1: element
      param_2: out
      local_20: first_collection_item
      auStack_1c: iterator_state
      FUN_08155e80: iterator_state_construct
      FUN_08155d6c: iterator_state_next
      FUN_08155ec0: iterator_state_cleanup
    notes: >-
      Raw osos.dec establishes the exact 96-byte extent:
      0x08146c94..0x08146cf4, with the separately linked next function
      beginning at 0x08146cf4. Whole-image decoding of every ARM B/BL
      immediate finds exactly ten inbound branches, all unconditional plain
      bl at 0x08146a58, 0x08146bd4, 0x0815c2ec, 0x0815c5ac, 0x08187ebc,
      0x08187f3c, 0x081b8428, 0x081b8668, 0x081b87f0, and 0x081b890c;
      there are no predicated calls or tail-b transfers. The function clears
      all four output rectangle words, constructs the already-ported
      iterator_state at element+0xa8 with start -2, advances it once, then
      copies item+0x80..+0x8c to out only when the yielded pointer is nonzero,
      and always releases the iterator. No dispatch seam was added: all three
      direct callees are already ported in app/vtable_set. Deliberate safety
      deviation: raw ARM leaves the yielded-item stack word uninitialized
      before iterator_state_next; if an empty iterator does not write its out
      pointer the subsequent non-NULL test is indeterminate. The Rust port
      initializes it to NULL, preserving the cleared rectangle for that empty
      case without undefined behavior. Host tests cover a non-NULL item's four
      signed bounds words and an empty traversal that writes no item pointer.

  # --- UI current collection-item word accessor ---
  - address: 0x0815d888
    ghidra: FUN_0815d888
    name: ui_collection_current_item_word
    size: 36
    sig: "u32 ui_collection_current_item_word(void *owner)"
    module: ui/collection_current_item_word
    status: ported
    renames:
      param_1: owner
      FUN_081d0e00: collection_current_item_word_dispatch
    notes: >-
      Raw osos.dec establishes the exact 36-byte extent:
      0x0815d888..0x0815d8ac. The separately linked next function begins at
      0x0815d8ac with push {r4,r5,r6,lr}. Whole-image decoding of every ARM
      B/BL immediate finds exactly ten direct inbound calls, all unconditional
      plain bl at 0x0815d048, 0x0815d338, 0x0815d35c, 0x0815d37c,
      0x0815d39c, 0x0815d40c, 0x0815d42c, 0x0815d44c, 0x0815d46c, and
      0x0815e010; there are no predicated bl forms or tail-b transfers. The
      nonzero owner+0x34 and signed-nonnegative owner+0x80 gates are preserved
      before the b tail transfer to the unported FUN_081d0e00 with owner+0x2c.
      That target increments the handle to +0x04, dispatches its object's
      vtable slot +0x40, and dereferences the returned word pointer. No
      names.yaml entry exists for FUN_081d0e00, so this one-function port
      models it as a volatile seam with that exact default body rather than
      inventing its callee identity. Host tests cover both short-circuit gates,
      the zero boundary of the signed state, the embedded-handle argument, and
      returned-word propagation. match.py is structural review only.

  # --- UI element nonzero flag-word query ---
  - address: 0x0815d7d4
    ghidra: FUN_0815d7d4
    name: ui_element_has_nonzero_flags
    size: 16
    sig: "u32 ui_element_has_nonzero_flags(const void *element)"
    module: ui/nonzero_flags
    status: ported
    renames:
      param_1: element
      DAT_48: flags
    notes: >-
      Raw osos.dec establishes the exact 16-byte extent:
      0x0815d7d4..0x0815d7e4. Its four instructions load the aligned UI
      element flag word at element+0x48, compare it with zero, and return
      1 for every nonzero word or 0 for zero. Whole-image decoding of every
      ARM B/BL immediate finds exactly ten inbound direct calls, all
      unconditional plain bl at 0x08125e90, 0x0815cae8, 0x0815d074,
      0x0815d19c, 0x0815d230, 0x0815d4f0, 0x0815dcd4, 0x0819ab4c,
      0x0819ab60, and 0x0819ac70; there are no predicated bl forms or
      direct tail-b transfers. A whole-image aligned-word scan finds no
      data-word reference to this address. The helper dereferences its
      argument immediately and therefore has no NULL or alignment guard.
      No dispatch seam is needed because it is a pure leaf. Deliberate
      deviations: none. Host tests cover zero, representative low/high-bit
      nonzero words, and isolation of the +0x48 field. match.py is
      structural review only.

  # --- Text buffer constructor ---
  - address: 0x08186380
    ghidra: FUN_08186380
    name: text_buffer_construct_with_capacity
    size: 60
    sig: "TextBuffer *text_buffer_construct_with_capacity(TextBuffer *buffer, u32 minimum_capacity)"
    module: cxx/text_buffer
    status: ported
    renames:
      param_1/r0: buffer
      param_2/r1: minimum_capacity
      DAT_081863bc: TEXT_BUFFER_TYPE_MARKER_ADDRESS
      FUN_08277440: string_default_construct
      FUN_08186344: text_buffer_reserve
    notes: >-
      Raw osos.dec establishes 56 code bytes at 0x08186380..0x081863bc and
      the 4-byte literal-pool word at 0x081863bc; the next separately linked
      function begins at 0x081863c0. Whole-image decoding of every ARM B/BL
      immediate finds ten inbound direct calls, every one an unconditional
      plain bl at 0x080abfc4, 0x080ac000, 0x080be7c8, 0x081318a0,
      0x0813283c, 0x0815f364, 0x0815f604, 0x0818cac8, 0x08210054, and
      0x0822348c; no predicated form or tail b reaches it. It chains the
      ported StringObject default constructor, overwrites word +0 with the
      0x081863bc literal, clears word +8, then conditionally reserves the
      nonzero requested capacity through FUN_08186344. The literal value
      0x089896bc reads as the static string 4TC_VolumeLimitLockScreenE, not
      a decodable vtable; its runtime role is unknown and modeled only as a
      type marker. FUN_08186344 is not separately ported, but its decoded
      reserve behavior is the volatile TextBufferOps default: realloc_wrapper
      @ 0x080edbf0 with tag 0x34/copy-on-move 1, NULL-or-one prior-text
      initialization, and capacity storage only after success. The ROM marker
      pointer is modeled by static host text. Host tests prove the zero-capacity
      no-dispatch path and nonzero post-initialization reserve dispatch.

  # --- Media-player queue refresh (app/media_player_queue_refresh) ---
  - address: 0x0817a914
    ghidra: FUN_0817a914
    name: media_player_queue_refresh
    size: 204
    sig: "u32 media_player_queue_refresh(MediaPlayerQueueOwner *player)"
    module: app/media_player_queue_refresh
    status: ported
    renames:
      param_1/r0: player
      "player+0x54": source_link
      "source_link+0x04": source_interface
      "source_interface vtable+0x138": source_ready
      "source_interface vtable+0x0e4": source_usable
      "player vtable+0x144": queue_refresh_notification
      "player+0x5c": fallback_state
      "player+0x60": fallback_descriptor
      "player+0x88": queue
      FUN_0822040c: queue_prepare_from_source
      FUN_082204c0: queue_prepare_fallback
      thunk_FUN_0822b5e0: queue_complete
      thunk_FUN_0822ba50: queue_finalize
    notes: >-
      Raw ARM extent is exactly 204 bytes at 0x0817a914..0x0817a9df: the
      next separately linked function starts at 0x0817a9e0. Decoding every
      ARM B/BL immediate in osos.dec finds exactly 10 direct inbound bl
      sites, all unconditional and with zero predicated forms or direct
      plain-B tails: 0x081792e8, 0x081798d4, 0x08179bd8, 0x0817a8c8,
      0x0817bc9c, 0x0817c724, 0x0817d118, 0x0817d404, 0x0817d630, and
      0x082a9c88. A nonzero source_link is adjusted by four and tested
      through its vtable slots +0x138 and +0x0e4. Both successes notify the
      player through +0x144 with (15,16), prepare queue+0x00 from that
      interface, complete it, and finalize it with state zero. Missing or
      rejected input instead notifies (15,-1), prepares from player+0x60,
      completes, and uses player+0x5c as the final signed state. Either
      failed prepare returns zero before completion/finalization. The four
      fixed helper addresses and dynamic vtable entries were absent from the
      ledger at port time, so they remain target transfers and host seams;
      their semantic identities are not invented. Deliberate deviation:
      stock's two final tail paths are one Rust return call after the shared
      state selection; arguments and result are preserved.

  # --- Two-word zero initializer (util/zero_u32_pair) ---
  - address: 0x080fffcc
    ghidra: FUN_080fffcc
    name: zero_u32_pair
    size: 16
    sig: "u32 *zero_u32_pair(u32 *dst)"
    module: util/zero_u32_pair
    status: ported
    renames:
      param_1/r0: dst
    notes: >-
      Raw osos.dec confirms the exact 16-byte extent, four ARM words from
      0x080fffcc through bx lr at 0x080fffd8; the separately linked next
      function starts at 0x080fffdc. Decoding every ARM B/BL immediate finds
      exactly 10 direct inbound bl sites, all unconditional and none
      predicated: 0x08155f54, 0x081dc5e4, 0x081f7294, 0x081f729c,
      0x081f72a4, 0x081f72ac, 0x081f72e8, 0x081f72f0, 0x081f72f8, and
      0x081f7300. No direct plain-B tail transfer targets the entry. The
      function stores zero at dst+0 and dst+4, then returns the unchanged dst
      in r0. Ghidra incorrectly reports void, but its chained range-state
      constructor callers consume this result. No NULL guard exists; callers
      must supply two aligned writable u32 words. Deliberate deviations: none.

  # --- Silver list resource processor (app/silver_list_table_resource) ---
  - address: 0x0812d7bc
    ghidra: FUN_0812d7bc
    name: silver_list_table_process_resource
    size: 68
    sig: "SilverListTable *silver_list_table_process_resource(void *context, u32 resource_id)"
    module: app/silver_list_table_resource
    status: ported
    renames:
      param_1/r0: context (unused)
      param_2/r1: resource_id
      r4: table
      FUN_082aadd4: operator_new
      FUN_081473d8: silver_list_table_ctor
      FUN_08147360: silver_list_table_item_process_all
      "table vtable+0x04": virtual_destructor
    notes: >-
      Raw osos.dec confirms the exact 68-byte extent: seventeen ARM words
      from 0x0812d7bc through pop {r4, r5, r6, pc} at 0x0812d7fc; the
      conditional virtual tail transfer is bxne r1 at 0x0812d7f8, and the next
      separately linked function's push starts at 0x0812d800. Decoding every ARM
      B/BL immediate finds exactly ten direct inbound bl sites, all
      unconditional and none predicated: 0x08126640, 0x0812664c,
      0x08126658, 0x08126664, 0x08126670, 0x0812667c, 0x081b1394,
      0x081b13a0, 0x0828a1e0, and 0x0828a1ec; no direct B tail targets
      this entry. context is dead: r0 is overwritten with 0x30 for
      operator_new before any use. The body allocates a SilverListTable,
      constructs it with (table, resource_id, 1), calls
      FUN_08147360(table, resource_id) while r2 remains 1, then only
      guards the virtual vtable+4 destructor. Thus a NULL allocation faults
      in construction/item processing before the final compare, as stock.
      The vtable +4 word at 0x08986438 is 0x4015622b in this image, not a
      decodable osos function entry, so its identity is deliberately not
      invented; the port retains dynamic dispatch. Deliberate deviation:
      host-replaceable operations model the unported item processor and
      virtual destructor, while target allocation and construction call the
      existing Rust ports directly. Host test proves the 0x30 allocation,
      construct(resource_id, 1) -> process(resource_id) -> destroy order,
      ignored context, and propagation of the destructor result.

  # --- Range append with boundary newline preservation (app/range_append_boundary_newline) ---
  - address: 0x0811853c
    ghidra: FUN_0811853c
    name: range_append_and_preserve_boundary_newline
    size: 52
    sig: "u32 range_append_and_preserve_boundary_newline(void *context, const TextRange *range, u32 metadata)"
    module: app/range_append_boundary_newline
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: range
      param_3/r2: metadata
      "range+0x04": start
      "range+0x08": end
      FUN_081195c4: range_append_helper (unported)
      FUN_08119160: boundary_newline_helper (unported)
    notes: >-
      Raw osos.dec confirms the exact 52-byte extent: thirteen ARM words from
      0x0811853c through the plain-B tail to 0x08119160 at 0x0811856c; the
      separately linked next function starts with push at 0x08118570. Ghidra's
      184-byte extent therefore incorrectly swallows that sibling. Decoding
      every ARM B/BL immediate finds exactly ten direct inbound bl sites:
      nine unconditional at 0x0811814c, 0x08118190, 0x081183fc, 0x0811840c,
      0x081189a0, 0x08119004, 0x081196cc, 0x08119734, and 0x08119970; plus
      blne at 0x081189b4. The predicated caller gates the invocation itself;
      this body has no range NULL guard. It computes wrapping end - start,
      calls unported 0x081195c4(context,start,length,metadata), then
      tail-transfers to unported 0x08119160(context,range), returning that
      result. Both targets were absent from the ledger at port time, so their
      semantic identities are not invented; target builds retain their fixed
      addresses and host tests install seams. Deliberate deviation: the first
      target transfer is an indirect payload-safe blx instead of stock's
      PC-relative bl; match.py confirms the final target transfer remains a
      tail bx, with arguments and result preserved.

  # --- Little-endian word store with final-byte return (util/u32_le_store_last_byte) ---
  - address: 0x080eda38
    ghidra: FUN_080eda38
    name: store_u32_le_last_byte
    size: 28
    sig: "u8 *store_u32_le_last_byte(u8 *dst, u32 value)"
    module: util/u32_le_store_last_byte
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: value
      r2: shifted_byte
    notes: >-
      Raw osos.dec confirms the exact 28-byte extent, seven ARM words from
      0x080eda38 through bx lr at 0x080eda54; the separately linked next
      function starts at 0x080eda58. Ghidra's 32-byte size incorrectly includes
      that sibling's first word. Decoding every ARM B/BL immediate finds exactly
      ten direct inbound calls, all unconditional plain bl and none predicated:
      0x0805afc8, 0x0805b494, 0x0805b520, 0x0805b540, 0x0805b560, 0x0805b588,
      0x0805b6a0, 0x0805b6b4, 0x0805b6c8, and 0x0809bed0. No direct plain-B
      tail transfer targets this entry. The body uses strb with post-index +1
      for the first three bytes, then a final strb, so it stores value in
      little-endian order and returns dst+3 in r0. No NULL, alignment, or bounds
      guard exists. Deliberate deviations: none.

  # --- Static descriptor word store (util/store_static_descriptor) ---
  - address: 0x080f81ac
    ghidra: FUN_080f81ac
    name: store_static_descriptor
    size: 12
    sig: "u32 *store_static_descriptor(u32 *descriptor_slot)"
    module: util/store_static_descriptor
    status: ported
    renames:
      param_1/r0: descriptor_slot
      DAT_080f81b8: static_descriptor
    notes: >-
      Raw osos.dec confirms the exact 12-byte instruction extent: ldr r1,
      [pc,#4]; str r1,[r0]; bx lr at 0x080f81ac..0x080f81b4, followed by its
      0x089800ac literal at 0x080f81b8. The separately linked next function
      starts at 0x080f81bc. Decoding every ARM B/BL immediate finds exactly
      ten direct inbound calls, all unconditional plain bl at 0x0812646c,
      0x0819b3b4, 0x0819b4e0, 0x081b4b54, 0x081b4c58, 0x081e7e10,
      0x081e7fd0, 0x081e8440, 0x0820abc8, and 0x0820ac54; no predicated bl
      forms or direct plain-B tails target it. Callers apply it to the tail
      word of freshly allocated objects. It stores the opaque static-data
      address 0x089800ac and preserves descriptor_slot in r0; Ghidra's void
      signature drops that return, though callers immediately recover their
      enclosing object from it. The literal's target is not a function entry
      and its semantic identity is not inferred. No NULL, alignment, or
      bounds guard exists. Deliberate deviations: none.

  # --- Opaque global identifier slot initializer (app/global_identifier_slot) ---
  - address: 0x081235dc
    ghidra: FUN_081235dc
    name: initialize_global_identifier_slot
    size: 32
    sig: "u32 *initialize_global_identifier_slot(u32 *slot, u32 identifier)"
    module: app/global_identifier_slot
    status: ported
    renames:
      param_1/r0: slot
      param_2/r1: identifier
      r4: slot
      r5: identifier
      FUN_081f4e44: identifier_registry_get (unported)
      FUN_081f4e9c: identifier_registry_initialize (unported)
    notes: >-
      Raw osos.dec establishes an exact 32-byte extent: eight ARM words from
      0x081235dc through pop {r4,r5,r6,pc} at 0x081235fc; the separately linked
      next function begins with push at 0x08123600. Ghidra's 36-byte size
      includes that sibling's first word. Decoding every ARM B/BL immediate
      finds ten direct inbound plain bl calls, all unconditional, at 0x082b07a0,
      0x082b07ac, 0x082b07b8, 0x082b07c4, 0x082b07d0, 0x082b07dc, 0x082b07e8,
      0x082b07f4, 0x082b0800, and 0x082b080c; there are no predicated forms.
      One additional direct plain-B tail enters at 0x082b081c. The body calls
      unported 0x081f4e44, forwards its result and identifier to unported
      0x081f4e9c, stores identifier through slot, and returns slot. The opaque
      shared object's type and the callees' semantic identities are not
      inferred. Target builds make payload-safe indirect calls to their fixed
      retailOS addresses; host tests replace them through a seam, preserving
      call order and arguments while changing direct bl to indirect blx.

  # --- Buffered FreeType stream position (ft/buffer) ---
  - address: 0x08042e70
    ghidra: FUN_08042e70
    name: buffered_stream_tell
    size: 140
    sig: "i32 buffered_stream_tell(const FtBufferedStream *stream, u64 *position)"
    module: ft/buffer
    status: ported
    renames:
      param_1/r0: stream
      param_2/r1: position
      r4: stream
      r5: position
      FUN_0805b73c: backing_stream_tell
    notes: >-
      Raw osos.dec confirms the exact 140-byte extent, from push
      {r4,r5,r6,lr} at 0x08042e70 through pop {r4,r5,r6,pc} at
      0x08042ef8; the separately linked next function begins with push at
      0x08042efc. The function clears the output first. Output mode calls the
      unported backing-position helper at 0x0805b73c with io_context and then
      adds the signed 32-bit cursor-buffer_start difference to its 64-bit
      result; input mode copies cached_position and subtracts the signed,
      inclusive buffer_end-cursor+1 span. These signed adjustments preserve
      ARM's adds/adc and subs/sbc behavior when bit 31 is set. Decoding every
      ARM B/BL immediate in osos.dec finds exactly 10 direct inbound calls:
      unconditional plain bl at 0x08042d88, 0x08043138, 0x0807fc6c,
      0x08087804, 0x08094960, 0x0809509c, 0x08097bec, 0x0809d84c,
      0x080ab0f0, and 0x080bbb20; there are no predicated forms or direct
      plain-B tail callers. Deliberate deviation: the unported 0x0805b73c
      helper is called through a volatile seam for host replacement and through
      its verified retailOS address on target, rather than by the stock direct
      bl. Its broader identity is not inferred.

  # --- COW string C-string equality (cxx/string) ---
  - address: 0x083eab9c
    ghidra: FUN_083eab9c
    name: cxx_string_equals_cstr
    size: 108
    sig: "int cxx_string_equals_cstr(const char *const *string, const char *cstr)"
    module: cxx/string
    status: ported
    renames:
      param_1/r0/r6: string
      param_2/r1/r7: cstr
      r4: captured_length
      r5: cstr_length
      r0: ordering
      FUN_08392478: strlen
      FUN_08030f64: memcmp
    notes: >-
      Raw osos.dec confirms the exact 108-byte extent from push
      {r4,r5,r6,r7,r8,lr} at 0x083eab9c through pop {r4,r5,r6,r7,r8,pc} at
      0x083eac04; the separately linked next function begins with push at
      0x083eac08. Decoding every ARM B/BL immediate finds exactly nine direct
      inbound calls, all unconditional plain bl at 0x0815af14, 0x0815af60,
      0x0815af74, 0x0815b33c, 0x0815b4a4, 0x0816764c, 0x08167670,
      0x0829c0e0, and 0x0829c274; there are no predicated forms or direct
      plain-B tail callers. It reads the COW `_Rep` length at data-4 before
      strlen(cstr), reloads data and length, memcmps the smaller captured
      length, then applies a length tie-break. Only zero comparison returns
      1. The two direct callees are already ported as strlen @ 0x08392478
      and memcmp @ 0x08030f64; no new dispatch seam is introduced. Deliberate
      deviations: none.

  # --- SQLite API-boundary result-code filter (sqlite/api_exit) ---
  - address: 0x0836f468
    ghidra: FUN_0836f468
    name: sqlite_api_exit
    size: 68
    sig: "i32 sqlite_api_exit(*mut u8 db, i32 result_code)"
    module: sqlite/api_exit
    status: ported
    renames:
      param_1/r0/r4: db
      param_2/r1: result_code
      "db+0x18": err_mask
      "db+0x1e": malloc_failed
      FUN_083766f4: sqlite_error
      uVar1/r0: selected_result_code
    notes: >-
      Raw osos.dec confirms the exact 68-byte extent: push {r4,lr} at
      0x0836f468 through pop {r4,pc} at 0x0836f4a8; the separately linked
      next function begins with push {r4-r11,lr} at 0x0836f4ac. Decoding every
      ARM B/BL-immediate word in osos.dec finds exactly nine direct inbound
      plain bl calls, all unconditional, at 0x082b795c, 0x082c43e4,
      0x08381604, 0x083816c4, 0x08384c90, 0x0838f668, 0x083901f0,
      0x08390334, and 0x08390570. There are no predicated bl calls; 13
      additional unconditional plain-B tail transfers target this entry. NULL
      db preserves the incoming result code only through the default 0xff
      errMask. A non-NULL db with mallocFailed nonzero calls ported
      sqlite_error(db, SQLITE_NOMEM, NULL), clears the sticky byte, selects
      SQLITE_NOMEM, then masks the selected code with db->errMask. Rust passes
      an explicit NULL variadic-home pointer to sqlite_error; its NULL format
      makes that otherwise-unused ABI argument unobservable. Host tests prove
      NULL default masking, healthy masking without writes, and nonzero
      mallocFailed reporting, clearing, SQLITE_NOMEM replacement, and masking.

  - address: 0x0837d250
    ghidra: FUN_0837d250
    name: sqlite_locate_table
    size: 120
    sig: "Table *sqlite_locate_table(Parse *parse, u32 is_view, const char *name, const char *database)"
    module: sqlite/locate_table
    status: ported
    renames:
      param_1/r0: parse
      param_2/r1: is_view
      param_3/r2: name
      param_4/r3: database
      iVar1/r5: table
    notes: >-
      Raw ARM fixes the 120-byte code extent at
      0x0837d250..0x0837d2c4: the following bytes
      0x0837d2c8..0x0837d2f8 are the "no such table", "no such view",
      "%s: %s", and "%s: %s.%s" literal pool, and the separately entered
      next function starts at 0x0837d2fc. Decoding every ARM B/BL word in
      osos.dec finds nine direct call sites — 0x082e6cec, 0x0836ea1c,
      0x0836eee4, 0x0836f44c, 0x08373e08, 0x0837574c, 0x0838459c,
      0x0838f1dc, and 0x08398e94 — all unconditional plain bl, with zero
      predicated BL forms and no tail branches. No aligned data word in
      osos.dec holds 0x0837d250. This is SQLite's sqlite3LocateTable
      helper: sqlite_read_schema @ 0x083817ec gates all work; a successful
      schema load calls find_table @ 0x08379024 with (parse->db, name,
      database). A miss calls sqlite_error_msg @ 0x083767a0 using "no such
      table" for is_view == 0 and "no such view" otherwise, with the
      qualified format when database is non-NULL, then sets
      parse->checkSchema at +0x12 to 1. The parse layout's formerly opaque
      byte is now named check_schema. All three callees are already ported,
      so the port adds no dispatch seam. Deliberate deviation: C variadic
      arguments are an explicit three-word VaList stack array, the existing
      sqlite_error_msg convention; its unused trailing word for an
      unqualified name cannot be observed by the format parser. Host tests
      cover unqualified table and qualified view misses, preservation of a
      preexisting error result, ARM wrapping of nErr, checkSchema latching,
      and exact literal/vararg selection.

  # --- FPLib signed multi-limb shift wrapper ---
  - address: 0x08340ee0
    ghidra: FUN_08340ee0
    name: bigint_shift_signed
    size: 36
    sig: "void bigint_shift_signed(u32 *value, i32 shift)"
    module: fp/fp_bigint_shift
    status: ported
    renames:
      param_1/r0: value
      param_2/r1: shift
      FUN_0833bde8: bigint_right_shift_core
      FUN_08360b88: bigint_left_shift_core
    notes: >-
      Raw osos.dec extent is exactly 36 bytes (0x08340ee0..0x08340f00):
      nine ARM instructions ending in an unconditional tail branch; the next
      separately linked function begins at 0x08340f04. Decoding every ARM
      B/BL word finds exactly 9 direct call sites, all unconditional bl
      (0x08314c6c, 0x0832857c, 0x0833d248, 0x08343318, 0x0834337c,
      0x083433e8, 0x08343474, 0x083434e0, 0x0834354c), with zero predicated
      calls and no aligned raw data-word reference. Signed shift > 0
      tail-branches to the unported 0x08360b88 core as (shift, value);
      shift <= 0 wraps its ARM rsb negation, then tail-branches to the
      unported 0x0833bde8 core as (value, magnitude). Raw helper prologues
      confirm their opposite argument orders and normalized multi-limb shift
      behavior, but their semantic identities are not invented. The port
      preserves the split and i32::MIN magnitude through volatile dispatch
      slots; target defaults call the verified retailOS addresses, while host
      defaults are inert solely to permit isolated recorder tests. match.py
      (worktree archive) reviewed the expected 9-vs-17 instruction diff:
      LLVM materializes volatile hook loads and indirect tail branches.

  # --- Stream-buffer slot lookahead comparison ---
  - address: 0x083d6504
    ghidra: FUN_083d6504
    name: streambuf_slot_peek_equal
    size: 76
    sig: "bool streambuf_slot_peek_equal(StreambufSlotPairContext *context)"
    module: cxx/streambuf_slot_peek_equal
    status: ported
    renames:
      param_1/r0: context
      "context+0x6c": left_slot
      "context+0x70": right_slot
      iVar2/r4: left_character
      iVar1/r0: right_character
      FUN_083da5a8: streambuf_sgetc_raw
    notes: >-
      Raw osos.dec fixes the exact extent at 0x083d6504..0x083d654c: the
      final pop {r4,r5,r6,pc} is at 0x083d654c and a separately linked sibling
      opens at 0x083d6550. Decoding every ARM B/BL-immediate word finds exactly
      nine inbound calls, all unconditional plain bl (no predicated forms):
      0x083b6450, 0x083b64a4, 0x083b6518, 0x083b6560, 0x083b6690,
      0x083b6750, 0x083b67fc, 0x083b694c, and 0x083d7b5c. No aligned image
      data word holds 0x083d6504. The body reads two non-NULL-REQUIRED slot
      pointers from context +0x6c/+0x70, maps a NULL stream-buffer word in
      either slot to -1, otherwise calls 0x083da5a8, and returns whether the
      two i32 results match. Raw 0x083da5a8 confirms only its behavior here:
      it returns the byte at streambuf +0x18 when that unsigned cursor is below
      +0x1c, otherwise it dispatches through the vtable word at +0x20; no
      callee identity is invented. The ARM bridge tail-branches directly to
      that unported address. The host-only recorder models it because a
      target-width vtable callback word cannot hold an x86-64 callback. Tests
      prove both NULL slots compare as EOF without a helper call, equal
      non-NULL lookaheads call left then right, and a NULL left vs non-EOF
      right does not compare equal. match.py against the worktree archive
      reports the expected 19-vs-20 instruction structural diff: LLVM uses a
      larger frame and branchy bool normalization around the same ordered
      slot loads and two direct helper calls.

  # --- Stream-buffer slot one-byte lookahead ---
  - address: 0x083d7158
    ghidra: FUN_083d7158
    name: streambuf_slot_peek_byte
    size: 32
    sig: "u8 streambuf_slot_peek_byte(const u32 *streambuf_slot)"
    module: cxx/streambuf_slot_peek_byte
    status: ported
    renames:
      param_1/r0: streambuf_slot
      "*streambuf_slot/r0": streambuf
      uVar1/r0: character
      FUN_083da5a8: streambuf_sgetc_raw
    notes: >-
      Raw osos.dec confirms the exact 32-byte extent from 0x083d7158 through
      0x083d7174; a separately linked sibling begins at 0x083d7178. Decoding
      every ARM B/BL-immediate word finds exactly eight inbound calls, all
      unconditional plain bl (zero predicated): 0x083b646c, 0x083b64b4,
      0x083b6528, 0x083b6570, 0x083b66d0, 0x083b6760, 0x083b680c, and
      0x083b685c. The body dereferences its non-NULL-REQUIRED target-width
      stream-buffer slot, maps a NULL word to signed EOF (-1), otherwise calls
      0x083da5a8, then masks either i32 result to its low byte. The ARM port
      reuses the existing direct bridge to that unported helper without a
      behavioral deviation; host tests replace it with a recorder to prove
      NULL avoids the call, the pointed buffer is forwarded, and both a wide
      result and EOF truncate to their expected byte values. match.py using the
      worktree archive reports the expected 8-vs-11 instruction diff: LLVM
      returns the NULL case early and saves fp instead of retailOS's unused r4.

  # --- SQLite adaptive Bitvec destructor ---
  - address: 0x08370588
    ghidra: FUN_08370588
    name: sqlite3_bitvec_destroy
    size: 64
    sig: "void sqlite3BitvecDestroy(Bitvec *bitvec)"
    module: sqlite/bitvec
    status: ported
    renames:
      param_1/r0/r5: bitvec
      iVar1/r4: child_index
      "bitvec+0x08": i_divisor
      "bitvec+0x0c": children
      FUN_083906f4: tracked_free
    notes: >-
      Raw osos.dec fixes the exact extent at 0x08370588..0x083705c8: 16 ARM
      instructions ending in `b 0x083906f4` (the ported tracked_free), and the
      separately linked sibling starts at 0x083705c8. Decoding every ARM B/BL
      immediate in the image finds exactly 9 inbound direct bl call sites:
      0x082dd7f0, 0x082ddb9c, 0x082ddeec, 0x082de870, 0x082de8ac,
      0x083704a4, 0x083705ac, 0x0837de1c, and 0x0837ecdc. All are
      unconditional; there are no predicated BL forms, tail branches, or
      aligned data-word references to this entry. This is SQLite 3.5.9's
      sqlite3BitvecDestroy: NULL returns immediately; iDivisor == 0 selects
      the inline 500-byte bitmap, while nonzero selects 125 child pointers,
      recursively destroyed in ascending slot order before the parent is freed.
      The Rust port directly calls tracked_free rather than tail-branching and
      uses named repr(C) fields for the target union, avoiding host pointer
      width overlap. Host tests prove NULL handling, inline-bitmap child
      suppression, and sparse child post-order destruction through tag-57
      tracked-free recording.

  # --- SQLite adaptive Bitvec membership test ---
  - address: 0x08370738
    ghidra: FUN_08370738
    name: sqlite3_bitvec_test
    size: 184
    sig: "u32 sqlite3BitvecTest(const Bitvec *bitvec, u32 bit)"
    module: sqlite/bitvec
    status: ported
    renames:
      param_1/r0/r4: bitvec
      param_2/r1/r5: bit
      "bitvec+0x00": size
      "bitvec+0x08": i_divisor
      "bitvec+0x0c": storage (inline bitmap, hash slots, or child pointers)
      FUN_08036f14: __rt_udiv
    notes: >-
      Raw osos.dec fixes the exact 184-byte extent at
      0x08370738..0x083707f0: the final hash-table empty-slot branch returns
      through the pop at 0x0837078c, and the separately linked next function
      opens at 0x083707f0. Decoding every ARM B/BL immediate in the image
      finds exactly eight direct BL callers, all unconditional: 0x082dd0f4,
      0x08370428, 0x08370438, 0x08370448, 0x0837045c, 0x0837df0c,
      0x0837e244, and 0x0837f010; no predicated BL forms reach it. The sole
      direct B is caller-side BEQ at 0x082dd020, selecting this function when
      a cursor flag is zero. No aligned image data word holds this entry.
      This is SQLite 3.5.9's sqlite3BitvecTest: NULL, bit zero, and bit >
      iSize return false; iSize <= 4000 tests bit-1 in the inline 500-byte
      bitmap. Larger vectors with iDivisor descend to child
      [(bit-1)/iDivisor] at remainder (bit-1)%iDivisor+1. A zero divisor
      instead linearly probes the 125 u32 hash slots from (bit*37)%125,
      wrapping at the final slot, and stops at zero. The existing
      __rt_udivmod wrapper preserves the retailOS __rt_udiv quotient/remainder
      ABI without adding a dispatch seam. Deliberate deviation: the target
      union is named BitvecStorage so its bitmap/hash target-width fields do
      not overlap widened host child pointers. Host tests cover NULL/zero/range
      rejection, first and final inline bits, quotient/remainder child descent
      and NULL child rejection, and a wrapped two-collision hash probe.
  - address: 0x082e406c
    ghidra: FUN_082e406c
    name: ata_set_error
    size: 20
    sig: "void ata_set_error(u32 error)"
    module: drivers/ata_cmd
    status: ported
    renames:
      param_1/r0: error
      iVar1: record
      FUN_082d0ae8: ata_error_record
    notes: >-
      Raw ARM confirms the 20-byte extent from 0x082e406c through
      0x082e407f; 0x082e4080 is a separately entered 404-byte function.
      The body saves error in r4, calls ata_error_record (0x082d0ae8), and
      stores it at record +0x04. Exactly nine direct BL words target this
      entry: eight unconditional bl at 0x082dfeb4, 0x082e07d4, 0x082e0b0c,
      0x082e1514, 0x082e2f64, 0x082e2fd0, 0x082e3ca4, and 0x082e4254; one
      blne at 0x082e17e4 is gated by its caller's condition. The port uses
      the existing ata_error_record seam and an aligned volatile store,
      preserving no NULL guard and no returned status. Its own text section
      prevents LLVM from folding it with ata_report_error, which has the
      same store but returns 0xffffffff.
  - address: 0x082bcb40
    ghidra: FUN_082bcb40
    name: storage_device_operations_get
    size: 36
    sig: "u32 *storage_device_operations_get(void)"
    module: drivers/storage_device_operations
    status: ported
    renames:
      DAT_082bcb5c: storage_device_operations_guard
      DAT_082bcb60: storage_device_operations
      FUN_082bca98: initialize_storage_device_operations
    notes: >-
      Raw osos.dec fixes the true 36-byte extent at 0x082bcb40..0x082bcb63:
      seven instruction words through pop {r4,pc} at 0x082bcb58 plus the two
      literal-pool words at 0x082bcb5c and 0x082bcb60; 0x082bcb64 is the
      next separately entered function. Ghidra's 28-byte extent omits that
      pool. It volatile-loads the guard at 0x08a09f70, calls 0x082bca98 only
      when the whole guard word is zero, then returns the fixed four-word
      table at 0x08ae54f0. The initializer fills that table with raw function
      entries 0x080c8b68, 0x080cc700, 0x080cea0c, and 0x080c17e0; their
      identities are intentionally not inferred here. Decoding every ARM B/BL
      word finds exactly nine direct inbound calls, all unconditional bl:
      0x080c88b8, 0x080d9938, 0x080dd2ec, 0x080e3b18, 0x082bd074,
      0x082bd0d8, 0x082bd17c, 0x082bd238, and 0x082bd33c. There are no
      predicated callers or tail branches. Firmware builds retain the original
      RAM addresses and direct initializer call; host builds model those
      runtime objects and inject the unported initializer through a seam.

  # --- tagged payload address helper ---
  - address: 0x082b513c
    ghidra: FUN_082b513c
    name: tagged_payload_address
    size: 24
    sig: "u8 *tagged_payload_address(u8 **context_slot, const TaggedPayloadDescriptor *descriptor)"
    module: util/tagged_payload_address
    status: ported
    renames:
      param_1/r0: context_slot
      param_2/r1: descriptor
      r2: descriptor_flags
    notes: >-
      Raw osos.dec fixes the extent at 0x082b513c..0x082b5154: ldr r2,[r1];
      tst r2,#0x400; ldreq r0,[r0]; ldreq r1,[r1,#8]; addeq r0,r0,r1; bx
      lr. Bit 0x400 returns context_slot itself without dereferencing it;
      otherwise it returns *context_slot + descriptor +0x08's u32 offset.
      9 direct inbound calls, all plain unconditional bl: 0x0803ac8c,
      0x0803ae54, 0x0803afc0, 0x0803b19c, 0x0803b2b4, 0x0803b348,
      0x080c86b0, 0x080c879c, and 0x080d3c30. No predicated calls and no
      aligned osos.dec data-word references. The distinct sibling begins at
      0x082b5154. No deliberate deviations; repr(C) u32 descriptor fields
      preserve its +0x00 flags and +0x08 offset layout on hosts.

  # --- SQLite VDBE memory-cell affinity application ---
  - address: 0x082b4694
    ghidra: FUN_082b4694
    name: vdbe_mem_apply_affinity
    size: 100
    sig: "void sqlite3VdbeMemApplyAffinity(Mem *p_mem, u8 affinity, u8 enc)"
    module: sqlite/vdbe_mem_apply_affinity
    status: ported
    renames:
      param_1/r0/r4: p_mem
      param_2/r1: affinity
      param_3/r2: enc
      FUN_082b46f8: sqlite3VdbeMemApplyNumericAffinity
      FUN_0838b644: sqlite3VdbeIntegerAffinity
      FUN_0838c32c: sqlite3VdbeMemStringify
    notes: >-
      Raw osos.dec confirms Ghidra's 100-byte extent: the final pop {r4,pc}
      is at 0x082b46f4 and the independently entered numeric-affinity helper
      begins with stmdb at 0x082b46f8; there is no literal pool. Decoding every
      ARM B/BL-immediate word in the image finds exactly nine inbound BL words:
      eight unconditional bl at 0x083865dc, 0x08387c14, 0x08387cdc,
      0x08387d40, 0x08387ea4, 0x08387eb4, 0x083885c8, and 0x08388658, plus
      one bleq at 0x08387d88. No aligned image-data word contains the entry
      address. This is SQLite 3.5.9's sqlite3VdbeMemApplyAffinity: TEXT ('a')
      stringifies an otherwise numeric cell through the ported
      sqlite3VdbeMemStringify @ 0x0838c32c, then clears MEM_Int|MEM_Real;
      NONE ('b') is a no-op; all other affinity codes apply numeric affinity,
      then integer affinity only if the result still has MEM_Real. The two
      unported helper bodies @ 0x082b46f8 and @ 0x0838b644 use a volatile
      dispatch seam: target defaults call retailOS and host tests supply
      recorders. No deliberate behavioral deviations. Tests cover the numeric
      text-render path, already-text suppression, NONE preservation, and both
      post-numeric REAL branches.
  - address: 0x082a014c
    ghidra: FUN_082a014c
    name: fixed16_dot3
    size: 64
    sig: "i32 fixed16_dot3(const i32 *a, const i32 *b)"
    module: util/fixed
    status: ported
    renames:
      param_1/r0: a
      param_2/r1: b
      r4: a
      r5: b
      r6: first_two_products
      FUN_080e9878: fixed16_mul
    notes: >-
      Raw osos.dec establishes the exact 64-byte body
      0x082a014c..0x082a018b: three word pairs at a[0..2]/b[0..2] are
      multiplied by fixed16_mul (0x080e9878), then added in order with
      wrapping 32-bit add. The ldr at 0x082a018c begins the separately
      entered vector-negation sibling; no literal pool belongs to this body.
      Decoding every ARM B/BL immediate in osos.dec finds exactly nine
      inbound calls, all plain unconditional bl (no predicated bl or tail b):
      0x0824c060, 0x0824c090, 0x0824c134, 0x0824c1ac, 0x0824c210,
      0x0824c240, 0x0824c2f4, 0x0824c360, and 0x0824c688. There are no
      aligned osos.dec data words containing this entry. The original has no
      NULL or bounds guard; the Rust port likewise directly reads all six
      aligned words. No deliberate deviations. Host tests cover ordinary
      signed Q16.16 products, fractional truncation, and overflow of an
      intermediate dot-product sum.

  # --- rendering request descriptor conversion ---
  - address: 0x0826b2a8
    ghidra: FUN_0826b2a8
    name: render_request_from_descriptor
    size: 112
    sig: "void render_request_from_descriptor(const RenderRequestDescriptor *descriptor, void *request)"
    module: ui/render_request_from_descriptor
    status: ported
    renames:
      param_1/r0/r12: descriptor
      param_2/r1/lr/r0: request
      param_1[0]/r2: source
      param_1[4]/r3: source_parameter
      param_1[8]/[sp]: options
      "*(u16 *)(param_1 + 0x24)/[sp,#4]": mode
      local_14/sp+0x14: bounds.top
      local_10/sp+0x18: bounds.left
      local_c/sp+0x1c: bounds.bottom
      local_8/sp+0x20: bounds.right
    notes: >-
      Raw osos.dec confirms the 112-byte extent: `mov ip,r0` at 0x0826b2a8
      through `pop {pc}` at 0x0826b314; the next independently linked body
      begins with `push {r4,r5,r6,lr}` at 0x0826b318. Decoding every ARM
      B/BL-immediate word in the decrypted image finds exactly nine inbound
      calls, all plain unconditional bl at 0x08101378, 0x08175200,
      0x081b7380, 0x081cbfac, 0x081cc240, 0x081ccc3c, 0x081f8104,
      0x081f81d0, and 0x08211378; there are no predicated bl forms or tail
      b references. It constructs the QuickDraw bounds {top, left,
      top + height, left + width}, with wrapping ARM addition, and calls
      0x0810e7d0 with that rectangle, descriptor words +0x00/+0x10/+0x20,
      the +0x24 low halfword, and two zero words. The callee's identity is
      intentionally not inferred; target builds dispatch to the retail entry
      and host tests install a recording seam. No behavioral deviation.
  - address: 0x082485d8
    ghidra: FUN_082485d8
    name: fixed16_clamp_unit4
    size: 100
    sig: "void fixed16_clamp_unit4(i32 *destination, const i32 *source)"
    module: util/fixed
    status: ported
    renames:
      param_1/r0/r4: destination
      param_2/r1/r5: source
      r6: clamped_component0
      r7: clamped_component1
      r8: clamped_component2
      FUN_080f0f84: signed_clamp_i32
    notes: >-
      Raw ARM confirms the exact 100-byte extent
      0x082485d8..0x0824863c: it snapshots source[0..3], invokes the ported
      signed_clamp_i32(value, 0, 0x10000) at 0x080f0f84 for each component,
      then writes the four saved results through destination[0..3]. The
      source snapshot precedes every destination store, so overlapping ranges
      preserve the original source quartet. Decoding every ARM B/BL immediate
      in osos.dec finds exactly nine inbound direct bl instructions, all
      unconditional and unpredicated: 0x0824d470, 0x0824d488, 0x0824d4a4,
      0x0824d4c0, 0x0824d4dc, 0x0824f95c, 0x082545e8, 0x08254608, and
      0x08254624. There are no direct tail branches and no aligned osos.dec
      data word containing the entry, so it is not dispatched virtually. No
      deliberate deviations. Host tests cover signed extremes, both Q16.16
      bounds, values immediately outside them, and an overlapping destination.

  - address: 0x08242d60
    ghidra: FUN_08242d60
    name: fixed28_lerp
    size: 48
    sig: "i32 fixed28_lerp(i32 start, i32 end, i32 factor_q4_28)"
    module: util/fixed
    status: ported
    renames:
      param_1/r0/r4: start
      param_2/r1: end
      param_3/r2: factor_q4_28
      r12: product_low
      r1: product_high
    notes: >-
      Raw ARM confirms the exact 48-byte extent, from push {r4,lr} at
      0x08242d60 through pop {r4,pc} at 0x08242d8c; the ldr r2,[r0] at
      0x08242d90 begins the separately linked next body. It forms the
      wrapped signed delta end - start, multiplies it by the Q4.28 factor
      with smull, adds 0x08000000 with adds/adc carry propagation, extracts
      bits [59:28] through lsr#28/lsl#4/orr, and wraps that rounded result
      into start. This is round-half-up (toward positive infinity), including
      negative half ties, with no saturation or input guard. Decoding every
      ARM B/BL immediate in osos.dec finds exactly nine inbound direct bl
      calls, all plain unconditional and none predicated: 0x08242cd8,
      0x08242cf0, 0x08242d08, 0x08242d20, 0x08242d44, 0x08249874,
      0x08249888, 0x0824989c, and 0x082498b0. It is an unguarded leaf.
      No deliberate Rust deviations. Host tests compare the literal ARM
      register funnel across signed extremes and Q4.28 edge factors, then
      prove endpoint identity, positive/negative half ties, and final-add
      wrap. match.py structural review finds the same wrapped subtraction,
      signed widening multiply, half-ulp bias, Q4.28 funnel, and final
      wrapping add; LLVM contracts adds/adc into smlal and uses fp rather
      than r4 for its frame.


  # --- Q16.16 two-by-two determinant (util/fixed) ---
  - address: 0x0823627c
    ghidra: FUN_0823627c
    name: fixed16_det2
    size: 36
    sig: "i32 fixed16_det2(i32 a, i32 b, i32 c, i32 d)"
    module: util/fixed
    status: ported
    renames:
      param_1/r0: a
      param_2/r1: b
      param_3/r2: c
      param_4/r3/r12: d
      r0: truncated_ad
      r1: truncated_bc
      r2: bc_product_low
      r3: product_high
    notes: >-
      Raw osos.dec confirms the exact 36-byte body at 0x0823627c..0x0823629c:
      mov ip,r3 through bx lr; cmp r1,#0 at 0x082362a0 starts the separately
      linked next function, and there is no literal pool. It computes the
      Q16.16 two-by-two determinant by signed-multiplying a*d and b*c,
      independently extracting each product's bits [47:16] with the
      lsl#16/lsr#16 funnel, then subtracting those truncated i32 results with
      wrapping ARM semantics. Decoding every ARM B/BL immediate in osos.dec
      finds exactly nine inbound direct calls, all plain unconditional bl:
      0x082a0718, 0x082a0740, 0x082a0760, 0x082a0788, 0x082a07a8,
      0x082a07c4, 0x082a07e4, 0x082a0804, and 0x082a0824. No predicated
      branches, tail branches, or aligned data words refer to this entry.
      No deliberate Rust deviations. Host tests prove Q16.16 identity,
      signed fractional cross-zero truncation (which distinguishes this from
      shifting the full determinant once), and signed-extreme arithmetic.
      match.py finds the same two signed widening multiplies, two Q16.16
      funnels, and wrapped subtraction; LLVM reverses product scheduling and
      saves fp/lr, so the instruction sequence is structurally rather than
      byte-for-byte identical.

  # --- Default selector dispatcher (app/default_selector_dispatch) ---
  - address: 0x0821a11c
    ghidra: FUN_0821a11c
    name: dispatch_default_selector
    size: 12
    sig: "void dispatch_default_selector(void *context, u32 notify)"
    module: app/default_selector_dispatch
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: notify
      r2: notify_forwarded
      r1: default_selector
    notes: >-
      Raw osos.dec confirms Ghidra's 12-byte extent: mov r2,r1 at
      0x0821a11c, mvn r1,#0 at 0x0821a120, and the tail branch to unported
      FUN_082193d8 at 0x0821a124; mov r0,#1 at 0x0821a128 begins the
      separately linked next function. The wrapper forwards context and notify
      unchanged to that dispatcher while fixing its signed selector argument
      to -1. Decoding every ARM B/BL immediate finds exactly nine inbound
      direct calls, all plain unconditional bl and none predicated:
      0x08131368, 0x08225948, 0x082270ec, 0x0822962c, 0x0822a814,
      0x0823110c, 0x0823213c, 0x08232ac0, and 0x082392b4. The retail callee
      remains unported, so target builds call its fixed 0x082193d8 address and
      host tests use a volatile seam. Tests prove the fixed -1 selector and
      forwarding for zero and all-set notification values. match.py finds the
      same argument move and sentinel write; LLVM materializes the absolute
      target, restores its frame, and bx dispatches instead of using the
      retail tail-branch encoding.

  # --- Validated 0x89c singleton (app/validated_singleton_0x89c) ---
  - address: 0x0820a570
    ghidra: FUN_0820a570
    name: validated_singleton_0x89c_get
    size: 100
    sig: "void *validated_singleton_0x89c_get(void)"
    module: app/validated_singleton_0x89c
    status: ported
    renames:
      DAT_0820a5cc: validated_singleton_0x89c_cache
      DAT_0820a5d0: validated_singleton_0x89c_size
      FUN_0820a610: validated_singleton_construct_veneer
      FUN_0820a4c0: validated_singleton_validate
      FUN_0820a61c: validated_singleton_destroy
    notes: >-
      Raw osos.dec establishes 92 instruction bytes at 0x0820a570..0x0820a5c8
      followed by cache and size pool words at 0x0820a5cc/0x0820a5d0, so the
      true extent is 100 bytes; the separate next entry at 0x0820a5d4 is a
      tail veneer. A complete ARM B/BL decode finds nine direct callers, all
      plain unconditional bl and no predicated forms or tail branches:
      0x081007dc, 0x08100874, 0x08101340, 0x081015f4, 0x08174380,
      0x08174518, 0x081746b4, 0x081751c8, and 0x08177958. It loads the cache
      word at 0x089cb1dc; on NULL, calls the ported operator_new(0x89c), the
      unknown construction veneer 0x0820a610, and stores its return before
      calling the unknown 0x0820a4c0 validator. A nonzero validator result
      retains and returns the cache; zero reloads the cache, passes
      0x0820a61c's destructor return to the ported operator_delete, clears the
      cache, and returns NULL. NULL construction results bypass validation and
      retry next call. The constructor veneer supplies literal 0x083e9314 in
      r1, but it does not prove an object identity. Target code uses original
      cache and retail lifecycle addresses; host tests use a cache and three
      volatile lifecycle operations to prove cache-hit, acceptance, rollback
      ordering including the destructor-return deallocation pointer, and NULL
      construction retry. match.py structural review: expected LLVM frame and
      direct/indirect call-shape differences only.

  # --- Internal LCD layer setup ---
  - address: 0x081f5c2c
    ghidra: FUN_081f5c2c
    name: configure_internal_display_layers
    size: 176
    sig: "void configure_internal_display_layers(InternalDisplayLayerSetup *setup)"
    module: drivers/internal_display_layers
    status: ported
    renames:
      param_1/r4: setup
      param_1_plus_0x18: layer5_input
      param_1_plus_0x1c: layer1_input
      param_1_plus_0x20: layer1
      param_1_plus_0x24: layer5
      FUN_081d8870: display_get
      FUN_081d9064: display_get_layer
      FUN_08120604: surface_config_init
      FUN_08120978: layer_apply_config
      FUN_081d8cfc: display_set_clear_color
      FUN_081d9138: display_pending_parameters
      FUN_08120880: layer_set_global_alpha_blend
    notes: >-
      Raw osos.dec confirms the 176-byte extent 0x081f5c2c..0x081f5cdc:
      44 code words through pop {r4,r5,pc}; the separately linked
      configuration copier opens at 0x081f5cdc. Decoding every ARM BL
      immediate finds exactly nine direct callers, all unconditional:
      0x0810dcfc, 0x0810ecf4, 0x08110218, 0x0811e6a0, 0x08130728,
      0x08144350, 0x081589d8, 0x0816dedc, and 0x081df66c. It obtains
      internal display layers 1 and 5, stores them at setup+0x20/+0x24,
      initializes two 64-byte stack surface descriptors, and copies the
      layer-1 input (+0x1c) then layer-5 input (+0x18) into them. The
      copier carries input byte +8 to descriptor +0, writes 3 at +1,
      maps input +0x0c to descriptor +0x10/+0x18 and input +0x10 to
      +0x0c/+0x14, clears +4/+8/+0x24/+0x28, and conditionally sets the
      +0x30 opaque word (format 0 -> 0x00ff0010, 2 -> 0x0000001f,
      3 -> 0). It applies each descriptor, clears the display to zero,
      calls unported 0x081d9138 with (3,5), then selects global-alpha
      blending on layers 5 and 1. The unported callee's exact stores are
      +0x54=3, +0x55=5, and +0x24=1, but its broader identity is not
      established; target code reaches its verified address through a
      volatile function pointer, while host tests use a recorder. Every
      other callee is already ported and is called directly. The temporary
      descriptors remain MaybeUninit stack storage so their initializer's
      deliberately unwritten bytes retain the retail stack-local behavior.

  # --- Stream-write owner constructor ---
  - address: 0x081f542c
    ghidra: FUN_081f542c
    name: stream_write_owner_construct
    size: 52
    sig: "StreamWriteOwnerPrefix *stream_write_owner_construct(StreamWriteOwnerPrefix *owner, const char *name, u32 buffer_capacity, u32 ignored_input)"
    module: cxx/stream_write_owner_construct
    status: ported
    renames:
      param_1/r0: owner
      param_2/r1: name
      param_3/r2: buffer_capacity
      param_4/r3: ignored_input
      DAT_081f545c: stream_write_owner_vtable
      FUN_08030ff4: strcpy
    notes: >-
      Raw osos.dec confirms twelve instruction words at
      0x081f542c..0x081f5458 followed by the 0x0899020c vtable literal at
      0x081f545c, so the true extent is 52 bytes despite Ghidra's 48-byte
      report; the separately linked next function begins at 0x081f5460.
      Decoding every ARM B/BL immediate finds nine direct inbound callers,
      all unconditional bl (0x0805e730, 0x080f672c, 0x08121558, 0x0816156c,
      0x081c7f48, 0x081e3210, 0x082828d4, 0x08282f50, 0x08295844), with no
      predicated forms, tail branches, or aligned data-word references. It
      writes the vtable at +0, strcpy-copies r1 to inline name storage +0x0c,
      writes r2 at +0x10c, zeroes +8 then +4, and returns r0; r3 is never
      read. The complete allocation is 0x114 bytes, but its final word is not
      touched here. The Rust port calls the existing strcpy port and otherwise
      reproduces every decoded store; no deliberate deviations.
  - address: 0x081d068c
    ghidra: FUN_081d068c
    name: stop_controller_timer_pair
    size: 28
    sig: "void stop_controller_timer_pair(u8 *controller)"
    module: app/controller_timer_pair
    status: ported
    renames:
      param_1/r0: controller
      "controller+0xb4": first_timer
      "controller+0xb0": second_timer
      FUN_0812c6b0: timer_stop
    notes: >-
      Raw osos.dec establishes the exact 28-byte extent from 0x081d068c
      through the tail branch at 0x081d06a4; the separately linked next
      function begins with push {r4,lr} at 0x081d06a8. Decoding every ARM
      B/BL immediate in osos.dec finds nine direct inbound bl call sites:
      eight unconditional at 0x081cf878, 0x081cfad4, 0x081d005c, 0x081d04c0,
      0x081d07a8, 0x081d07cc, 0x081d080c, and 0x081d08b4, plus the predicated
      blne at 0x0810d5a8. It loads controller+0xb4 and calls the already
      ported timer_stop, then reloads controller+0xb0 and tail-branches to
      timer_stop. Neither timer pointer is NULL-guarded. Ghidra incorrectly
      absorbs the separately linked sibling into this function. Deliberate
      deviation: Rust represents the final tail branch as a direct call.
      The host test changes controller+0xb0 from the first stop's trace
      callback, proving the second pointer is loaded after the first call;
      it also proves that controller+0xb8 remains untouched.
  - address: 0x081cda64
    ghidra: FUN_081cda64
    name: lazy_slot_table_entry
    size: 24
    sig: "void *lazy_slot_table_entry(u32 slot)"
    module: util/lazy_slot_table_entry
    status: ported
    renames:
      param_1/r0: slot
      FUN_081cda30: lazy_slot_table_getter (unported fixed-address target call)
      FUN_081cd9b8: indexed_slot_word_load
    notes: >-
      Raw osos.dec confirms that Ghidra's 32-byte extent swallows the next
      sibling: the six instructions at 0x081cda64..0x081cda78 are 24 bytes,
      ending with b 0x081cd9b8; the next separately linked function begins
      with push at 0x081cda7c. It saves slot across the lazy getter call,
      then tail-branches to the two-instruction indexed word load, so it
      returns table[slot] with neither bounds nor NULL checks. Decoding every
      ARM B/BL immediate in osos.dec finds exactly nine direct inbound calls,
      all unconditional bl (0x0829c874, 0x0829c944, 0x0829da30, 0x0829da44,
      0x0829da78, 0x0829da8c, 0x0829ee8c, 0x0829eeb4, 0x0829eecc), with no
      predicated forms or direct tail branches. The callee initializes a
      15-word table of 0x2000-byte-buffer records, but no concrete record
      identity is established. Deliberate deviations: ARM calls the verified
      unported getter at 0x081cda30; host tests install a getter seam, and
      Rust performs the tail helper's indexed load directly.

  # --- String-and-word record copy constructor ---
  - address: 0x081f4ffc
    ghidra: FUN_081f4ffc
    name: string_word_record_copy_construct
    size: 24
    sig: "StringWordRecord *string_word_record_copy_construct(StringWordRecord *this, const StringWordRecord *source)"
    module: cxx/string_word_record
    status: ported
    renames:
      param_1/r0: this
      param_2/r1: source
      iVar1/r0: constructed_string
      "source+0x08": value
      FUN_082773e0: string_object_copy_construct
    notes: >-
      Raw osos.dec confirms the six instructions from 0x081f4ffc through
      pop {r4,pc} at 0x081f5010: it saves source in r4, calls the ported
      StringObject copy constructor at 0x082773e0 on the embedded +0
      subobject, then loads source+8 and stores it at result+8. The next
      separately linked function begins with push {r4,r5,r6,lr} at
      0x081f5014, confirming Ghidra's 24-byte extent. Decoding every ARM
      B/BL immediate in osos.dec finds exactly nine direct inbound calls:
      four unconditional bl at 0x083e82a4, 0x083e82f4, 0x083e97f8, and
      0x083eaa10; five blne at 0x0817770c, 0x083e1870, 0x083e1900,
      0x083e80b0, and 0x083e8b58. The callee has no NULL guard: the
      predicated callers gate the construction before entry, while the four
      remaining callers enter unconditionally. Callers at 0x08177604 and in
      the 0x083eXXXX vector helpers establish only a StringObject plus an
      opaque u32, not a concrete record identity. The Rust repr(C) record
      uses named fields so the ARM +8 word does not overlap widened host
      pointers. No deliberate deviations; the embedded constructor is
      already ported and called directly. Focused host tests cover deep
      payload copying and self-construction, including the embedded
      constructor's address-based no-allocation path.

  # --- paired four-word copy helper ---
  - address: 0x08158c94
    ghidra: FUN_08158c94
    name: copy_four_words_paired
    size: 28
    sig: "u32 *copy_four_words_paired(u32 *destination, const u32 *source)"
    module: util/copy_four_words
    status: ported
    renames:
      param_1/r0: destination
      param_2/r1: source
      uVar1/r2/r3: word
    notes: >-
      Raw osos.dec establishes the exact 28-byte extent from 0x08158c94
      through bx lr at 0x08158cac; the separately linked next function
      begins at 0x08158cb0. Decoding every ARM B/BL immediate finds exactly
      nine direct inbound call sites, all unconditional bl at 0x0829b208,
      0x0829b398, 0x0829b3b0, 0x0829b4a4, 0x0829b63c, 0x0829b670,
      0x0829b750, 0x0829b8a0, and 0x0829b8b8; there are no predicated forms
      or direct tail branches. It loads words 0 and 1 before storing either,
      then loads word 3 before word 2 and stores word 2 before word 3.
      r0 is unchanged and returned as destination. No deliberate deviations;
      host tests cover all relative four-word overlaps and the return pointer.

  # --- file-backed property-store destructor ---
  - address: 0x08149d60
    ghidra: FUN_08149d60
    name: vtable_file_store_destruct
    size: 64
    sig: "u8 *vtable_file_store_destruct(u8 *this)"
    module: app/vtable_set
    status: ported
    renames:
      param_1/r4: this
      param_1_plus_0x00: vtable
      param_1_plus_0x0c: aligned_buffer
      param_1_plus_0x30: inner_file_object
      DAT_08149d9c: store_destruct_vtable
      FUN_0816c044: store_base_destructor_tail
      FUN_081a8204: aligned_buffer_reset (direct, ported in heap/aligned_buffer)
    notes: >-
      Raw osos.dec establishes 60 executable bytes at 0x08149d60..0x08149d9b
      plus the literal-pool word at 0x08149d9c, with the next independent
      entry at 0x08149da0; Ghidra's 92-byte size is wrong. Decoding every
      ARM B/BL immediate finds nine direct inbound calls, all unconditional
      bl: 0x0808e5c4, 0x0808e5e4, 0x080c6c9c, 0x080c6d3c, 0x08103174,
      0x08149d54, 0x081a36d8, 0x081a3700, and 0x0825c230; no predicated
      direct calls or direct b callers. It installs literal 0x0898665c,
      conditionally invokes the non-NULL inner object's dynamic vtable slot
      +0x04 with the inner object in r0, then clears this+0x30. Its tail
      branch reaches 0x0816c044, whose raw-decoded body installs
      0x0898846c, directly resets the aligned buffer at this+0x0c through
      ported aligned_buffer_reset, and eventually reaches the empty
      0x08275bc8 base tail; the Rust port inlines that complete tail chain
      rather than adding an unfaithful seam. The vtable literals remain raw
      target words because their class identities are not independently
      established. Host test
      file_store_destruct_releases_inner_resets_base_and_returns_this covers
      populated and NULL inner fields, order-visible intermediate vtable
      installation, ownership clearing, base reset, and the returned this.
  # --- channel control enable state ---
  - address: 0x081085d0
    ghidra: FUN_081085d0
    name: channel_control_apply_enable
    size: 96
    sig: "void channel_control_apply_enable(void *context, u32 selector, i32 enable)"
    module: drivers/channel_control_enable
    status: ported
    renames:
      param_1/r0: context
      param_2/r1: selector
      param_3/r2: enable
      FUN_081073f8: unported_channel_state_dispatch
      DAT_38400900: secondary_channel_control_bank
      DAT_38400b00: primary_channel_control_bank
    notes: >-
      Raw osos.dec establishes a 96-byte body from 0x081085d0 through the
      return at 0x0810862c; the next independently linked function begins
      at 0x08108630. Decoding every ARM B/BL immediate finds exactly nine
      direct inbound calls, all unconditional bl at 0x08292fac, 0x08293ab0,
      0x08293ac0, 0x08293be0, 0x08293c0c, 0x08293d34, 0x08294388,
      0x082947b4, and 0x082947c4; no predicated call forms target this
      entry. It is a no-op only when `(selector & 0x7f) == 0 && enable == 0`;
      otherwise it calls the unported target at 0x081073f8, then updates
      control word 0x38400b00 or 0x38400900 plus `(selector & 0x7f) * 0x20`.
      The update clears bits 31..26 and 17; zero enable clears bit 21 and
      sets bit 28, while nonzero enable sets bit 21. The target port reaches
      0x081073f8 by its verified address without assigning that callee an
      identity; host tests install a recorder that mutates the register
      before the read, proving call-before-read ordering, both banks, all
      nonzero enable values, and the two bank-zero no-op cases.
  - address: 0x080f3d3c
    ghidra: FUN_080f3d3c
    name: parse_i16_prefix
    size: 200
    sig: "i32 parse_i16_prefix(const u8 *s, u8 **endptr, i32 base)"
    module: strto/parse_i16_prefix
    status: ported
    renames:
      param_1/r0: s
      param_2/r1: endptr
      param_3/r2: base
      iVar3/r3: i16_accumulator
      DAT_080f3df4: radix_8_digit_bitmap
      DAT_080f3df8: radix_16_digit_bitmap
      DAT_080f3dfc: radix_10_digit_bitmap
      DAT_080f3e00: ascii_digit_value_map
    notes: >-
      Raw osos.dec establishes the 200-byte extent 0x080f3d3c..0x080f3e03:
      184 executable bytes followed by its four-word literal pool, before the
      distinct next entry at 0x080f3e04. Decoding every ARM B/BL immediate
      finds exactly nine direct inbound call sites, all unconditional bl at
      0x08098a3c, 0x08098a58, 0x08098a74, 0x08098a90, 0x08098cf8,
      0x080a07f8, 0x080a0810, 0x080a0828, and 0x080a0840; no predicated
      forms or direct tail branches exist. It parses an optional '-' and a
      forced 0x/0X hexadecimal prefix without whitespace or '+' handling,
      truncating the signed 16-bit accumulator after every multiply-add.
      The digit-map addresses point to zero-filled runtime storage in
      osos.dec and their initializer is not identified; the port deliberately
      uses their implied C-locale ASCII digit sets (0..7, 0..9, and
      0..9/A..F/a..f). Host tests cover radix normalization, prefix/endptr
      placement, sign, null/empty input, and per-step 16-bit wrapping.

  # --- refcounted body acquire through owner ---
  - address: 0x08131fb8
    ghidra: FUN_08131fb8
    name: refcounted_body_acquire_from_owner
    size: 8
    sig: "void refcounted_body_acquire_from_owner(RefcountedBody **dst, const RefcountedBodyOwner *owner)"
    module: cxx/handle
    status: ported
    renames:
      param_1/r0: dst
      param_2/r1: owner
      owner+0x28/r1: body
      FUN_0839cd5c: refcounted_body_acquire
    notes: >-
      Raw ARM confirms the complete two-word body: `ldr r1,[r1,#0x28]` at
      0x08131fb8 then unconditional `b 0x0839cd5c` at 0x08131fbc. The prior
      sibling ends with `bx lr` at 0x08131fb4; 0x08131fc0 begins the next
      independently linked function, so Ghidra's 8-byte extent is exact even
      though its C output incorrectly absorbs the tail target's implementation.
      The target is the already ported refcounted_body_acquire, so this port
      directly delegates rather than inventing a duplicate dispatch seam.
      Binary-scanning every aligned ARM B/BL word in osos.dec finds exactly
      nine direct inbound callers, all unconditional bl at 0x08130fe4,
      0x08131040, 0x0813108c, 0x0813110c, 0x08131158, 0x081311e4,
      0x08131230, 0x08131418, and 0x08131664; it finds no predicated or
      direct-tail inbound branches. No aligned image word equals 0x08131fb8,
      so it is not virtually dispatched. The source owner type remains
      unidentified: the port exposes only its verified ten-word opaque prefix
      and refcounted-body field at +0x28. It preserves the original's lack of
      NULL, alignment, and bounds guards; host tests prove field selection,
      destination overwrite on a NULL body, and delegated wrapping increment.
  - address: 0x0807f1e8
    ghidra: FUN_0807f1e8
    name: keyed_record_pair_lookup
    size: 76
    sig: "u32 keyed_record_pair_lookup(const KeyedRecordPair *records, u32 record_count, u32 key, u32 *first_value_out, u32 *second_value_out)"
    module: app/keyed_record_pair_lookup
    status: ported
    renames:
      param_1/r0: records
      param_2/r1: record_count
      param_3/r2: key
      param_4/r3: first_value_out
      stack_0/r5: second_value_out
      uVar1/r12: record_index
      piVar2/lr: record
    notes: >-
      Raw ARM establishes the exact 76-byte extent
      0x0807f1e8..0x0807f234: the next separately entered function starts
      with cmp r0,#0 at 0x0807f234. Decoding every ARM B/BL immediate in
      osos.dec finds exactly nine direct inbound calls, all unconditional
      plain bl in FUN_0828fbac at 0x0828fd2c, 0x0828fe84, 0x0828fea8,
      0x0828fed0, 0x0828fef0, 0x0828ff10, 0x0828fff4, 0x08290050, and
      0x08290078; no predicated calls or tail branches. The helper linearly
      scans 16-byte records for the first matching u32 key, then copies
      words +8 and +12 to its two outputs and returns 1; a miss returns 0
      without modifying either output. The +4 word is not read.
  - address: 0x0804ea74
    ghidra: FUN_0804ea74
    name: fixed16_round_to_integer
    size: 28
    sig: "i32 fixed16_round_to_integer(i32 value)"
    module: util/fixed
    status: ported
    renames:
      param_1/r0: value
    notes: >-
      Raw ARM confirms the complete 28-byte leaf from cmp r0,#0 at
      0x0804ea74 through bx lr at 0x0804ea8c; cmp r0,#0 at 0x0804ea90 begins
      a separately linked sibling. Positive Q16.16 inputs form
      (value + 0x8000) & 0xffff0000. Negative inputs form
      -((0x8000 - value) & 0xffff0000): the cmp flags survive the two
      flag-preserving shifts, so half ties round away from zero. All
      arithmetic wraps in the ARM register, including i32::MIN negation.
      Decoding every ARM B/BL immediate in osos.dec finds exactly nine
      inbound direct calls, all unpredicated unconditional bl:
      0x0809f4c0, 0x080ae484, 0x080ae498, 0x080ae4ac, 0x080ae4bc,
      0x080b81d4, 0x080b81e0, 0x080b81ec, and 0x080b81f8. There are no
      predicated calls, direct tail branches, or aligned image words equal to
      the entry, so it is an unguarded leaf and not virtually dispatched. No
      deliberate Rust deviations. Host tests cover fractional boundaries,
      signed half ties, and both signed overflow endpoints.
  # --- Filesystem mapped allocation-bitmap dirty marker ---
  - address: 0x0805cdd0
    ghidra: FUN_0805cdd0
    name: mapped_block_window_mark_dirty
    size: 16
    sig: "void mapped_block_window_mark_dirty(void)"
    module: fs/block_window
    status: ported
    renames:
      DAT_0805cde0/r1: mapped_block_window (fixed object at 0x08adc510)
      DAT_0805cde0_plus_0x208: dirty
      param_1/r0: ignored_window_pointer
    notes: >-
      Raw ARM confirms four instructions (16 bytes) from 0x0805cdd0 through
      bx lr at 0x0805cddc. The fixed mapped-window address 0x08adc510 is the
      following literal word at 0x0805cde0, and the next separately entered
      function begins with push {r4,lr} at 0x0805cde4. The routine stores one
      to the shared window's dirty byte at +0x208 without reading the incoming
      r0 pointer supplied by its callers. Decoding every ARM B/BL-immediate
      word in osos.dec finds exactly nine inbound calls, all unconditional
      plain bl at 0x080424c8, 0x08042550, 0x080425f0, 0x08042a04,
      0x08042aa0, 0x08042b14, 0x08042b7c, 0x08042c14, and 0x08042c84; no
      predicated form or direct tail branch targets it. No deliberate Rust
      deviations: target code stores through the fixed address, while host
      tests install the established mapped-window fixture.

  # --- Unsigned ASCII integer-prefix parser ---
  - address: 0x08074c18
    ghidra: FUN_08074c18
    name: parse_u32_prefix
    size: 160
    sig: "u32 parse_u32_prefix(const u8 *s, u8 **endptr, i32 base)"
    module: strto/parse_u32_prefix
    status: ported
    renames:
      param_1/r0: s
      param_2/r1: endptr
      param_3/r2: base
      iVar2/r3: u32_accumulator
      DAT_08074ca8: radix_8_digit_bitmap
      DAT_08074cac: radix_16_digit_bitmap
      DAT_08074cb0: radix_10_digit_bitmap
      DAT_08074cb4: ascii_digit_value_map
    notes: >-
      Raw osos.dec establishes a 160-byte entry extent
      0x08074c18..0x08074cb7: the 144-byte instruction body ends at the
      pop {r4,r5,r6,r7,pc} at 0x08074ca4, its four-word literal pool fills
      0x08074ca8..0x08074cb4, and push {r4,r5,r6,lr} at 0x08074cb8 begins a
      separately linked function. Decoding every ARM B/BL immediate finds
      exactly nine direct inbound call sites, all unconditional bl at
      0x0809897c, 0x08098c8c, 0x08098ca8, 0x08098cc4, 0x0809fcdc,
      0x0809fe90, 0x080a0130, 0x080a0700, and 0x080a075c; no predicated
      forms or direct tail branches exist. NULL and empty input return zero
      without writing endptr. Other inputs select radix 8 or 16 only when
      requested, otherwise decimal; 0x/0X forces hexadecimal. It accumulates
      accepted digits in a wrapping u32 and writes the stopping cursor when
      supplied. The four digit-map locations point to zero-filled runtime
      storage in osos.dec and their initializer is not identified; the port
      deliberately uses their implied C-locale ASCII digit sets (0..7, 0..9,
      and 0..9/A..F/a..f). Host tests cover radix normalization, forced
      prefix/endptr placement, null/empty input, null endptr, and u32
      wrapping.
  - address: 0x08051ce4
    ghidra: FUN_08051ce4
    name: object_selected_item
    size: 56
    sig: "const void *object_selected_item(const void *object)"
    module: ui/object_state
    status: ported
    renames:
      param_1/r0: object
      r1: backend
      r2: object_saved_before_backend_resolve
      r3: explicit_item
      r2_after_ldr: primary_item
      r0_after_ldreq: secondary_item
    notes: >-
      Raw ARM confirms the 56-byte extent 0x08051ce4..0x08051d1c: the
      separately entered sibling begins with push {r0-r4,lr} at 0x08051d1c.
      The routine first calls the already ported kind-1/self, kind-2/proxy
      backend resolver 0x08051dc4, then loads the original object's +0x04
      explicit item. A non-null explicit item wins; otherwise it returns the
      resolved backend's non-null +0xf44 primary item, then its +0xf48
      secondary item (including NULL). Decoding every ARM B/BL immediate in
      osos.dec finds exactly nine inbound direct calls, all unconditional
      plain bl at 0x08059adc, 0x08059b28, 0x08061d04, 0x080866d8,
      0x08087694, 0x0808cf10, 0x080e28f4, 0x0813d8b8, and 0x0817b364;
      there are no predicated calls or direct tail branches. The concrete item
      class is not recovered; callers use the result as a selected item with
      a +0x40 subobject. No deliberate Rust deviations: raw u32 pointer-word
      reads preserve the target's 4-byte field spacing on 64-bit hosts.

  - address: 0x0804202c
    ghidra: FUN_0804202c
    name: zeroing_buffer_destroy
    size: 52
    sig: "void zeroing_buffer_destroy(ZeroingBuffer *buffer)"
    module: heap/zeroing_buffer
    status: ported
    vars:
      param_1/r0: buffer
      param_1_plus_0x4: data
      param_1_plus_0x8: byte_len
      FUN_08037dc8: iram_memzero_veneer
      FUN_08043994: traced_free
    notes: >-
      Raw ARM extent is exactly 0x0804202c..0x08042060; the independently
      linked sibling begins at 0x08042060 with push {r3,r4-r7,lr}. For a
      NULL buffer movs/popeq returns without a free. Otherwise a non-NULL
      data word (+0x04) is zeroed for byte_len (+0x08) through the ported
      IRAM veneer 0x08037dc8 -> 0x220002d4, then released through
      traced_free; the record is always released last through a tail b to
      traced_free. Decoding every ARM B/BL word in osos.dec finds nine
      direct inbound bl calls: four unconditional (0x0807c258, 0x080a53f8,
      0x080ef850, 0x080effb8) and five blne (0x0803a8ac, 0x0805fedc,
      0x0806f914, 0x0807c230, 0x080a1410); no tail branch or data word
      targets this destructor. ARM release compilation retains the final tail
      branch to traced_free; its extra prologue only preserves the record
      across the payload release.

  - address: 0x080403dc
    ghidra: FUN_080403dc
    name: tagged_word_buffer_create
    size: 92
    sig: "TaggedWordBuffer *tagged_word_buffer_create(void)"
    module: heap/tagged_word_buffer
    status: ported
    renames:
      r0: buffer
      r1: zero
      r2: zero
      r3: zero
      FUN_08043c18: traced_alloc
      FUN_08049a84: diag_ring_record
    notes: >-
      Raw ARM confirms the 92-byte extent exactly: 0x080403dc through
      0x08040438, where the next separately linked function starts. Decoding
      every ARM B/BL immediate in osos.dec finds exactly nine inbound direct
      calls, all unconditional plain bl at 0x0803dca0, 0x0803dcb0,
      0x0803e2f0, 0x0803e824, 0x0803eeb0, 0x0803f218, 0x0803fa00,
      0x080e8b3c, and 0x08368390; no predicated BL forms or direct tail
      branches target it. Calls ported traced_alloc(20, 0, 0); allocation
      failure records ported diag_ring_record(3, 0x71, 0x41, 0, 0) then
      returns NULL. Success initializes the five target words
      {data, len, capacity, tag, flags} as {0, 0, 0, 0, 1}; bit 0 makes the
      returned TaggedWordBuffer self-deleting under tagged_word_buffer_destroy.
      No deliberate Rust deviations: both callees are direct ports.
  - address: 0x0803b3a4
    ghidra: FUN_0803b3a4
    name: typed_allocation_release_helper
    size: 20
    sig: "void typed_allocation_release_helper(void *allocation, const void *descriptor)"
    module: cxx/typed_allocation_release
    status: ported
    renames:
      param_1/r0: allocation
      param_2/r1: descriptor
      uStack_10/sp: allocation_release_frame.allocation
      uStack_c/sp: allocation_release_frame.descriptor
      FUN_080c85bc: type_erased_release_engine
    notes: >-
      EXTENT: raw osos.dec is exactly 20 bytes at
      0x0803b3a4..0x0803b3b7; the next separately linked function begins at
      0x0803b3b8 with push {r0,r1,r2,r4,r5,r6,r7,lr}. CALL COUNT: decoding
      every ARM B/BL immediate in osos.dec finds 27 direct references: 9
      unconditional plain bl calls at 0x08065b18, 0x08065b24, 0x0806f12c,
      0x0806fb28, 0x0808a3f4, 0x0808aa9c, 0x080c5a2c, 0x080f0130, and
      0x082c5a50, plus 18 unconditional b tail transfers; no reference is
      predicated. ALGORITHM: push allocation and descriptor as the first two
      frame words, call the unported type-erased release engine 0x080c85bc
      with the frame, descriptor, and zero third argument, then return its
      r0 value to the caller even though the recovered signature is void.
      The allocation and descriptor have no NULL guard. Rust uses a repr(C)
      named two-pointer frame, which preserves the target's +0 and +4 layout;
      host tests record both frame words, the separate descriptor argument,
      the zero third argument, and NULL forwarding. Target builds call the
      fixed engine address; hosts use a volatile replaceable seam because the
      retail address is unmapped. match.py structural review found the same
      saved two-word frame, r0=sp, r2=0 setup, and engine call; LLVM's frame
      pointer and indirect fixed-address call are deliberate codegen
      differences.
  - address: 0x080034d0
    ghidra: FUN_080034d0
    name: non_abi_continuation_veneer
    size: 8
    sig: "void non_abi_continuation_veneer(void)"
    module: sqlite/vdbe
    status: identified
    renames:
      DAT_080034d4: continuation_address
      FUN_0838b67c: containing_routine
    notes: >-
      Literal veneer: ldr pc, [pc, #-4] at 0x080034d0 tail-branches through
      0x0838b6d4 at 0x080034d4. Ghidra reports 4 bytes; the true extent is
      8 (instruction plus literal), because the next separately linked veneer
      opens at 0x080034d8. The literal is not a function entry: raw ARM at
      0x0838b6d4 begins `cmp r2, #2; bne 0x0838b6f0`, within the routine that
      starts at 0x0838b67c with `push {r3,r4-r9,sl,fp,lr}`. Its immediately
      preceding instruction is `ldrb r2, [r4,#0xfc]`, so this transfer relies
      on live r0-r6 state from the caller rather than any recoverable C ABI.
      Decoding every ARM B/BL word in osos.dec finds exactly nine inbound
      direct calls, all `blne` (0x08000fac, 0x080012d8, 0x080013b4,
      0x08001508, 0x08001664, 0x08001688, 0x08001800, 0x08001818, and
      0x0800192c); callers gate the transfer on their live r0 being nonzero.
      There are no direct B transfers or data-word references. A Rust
      function cannot preserve and consume this non-ABI register continuation
      faithfully, so no dispatch seam or guessed callee is added. Retain
      status identified until the containing continuation's required live
      register contract can be ported as one coherent routine.
  - address: 0x080034c0
    ghidra: FUN_080034c0
    name: non_abi_state_machine_continuation_veneer
    size: 8
    sig: "void non_abi_state_machine_continuation_veneer(void)"
    module: sqlite/vdbe
    status: identified
    renames:
      DAT_080034c4: continuation_address
      FUN_0838b084: containing_routine
    notes: >-
      Literal veneer: ldr pc, [pc, #-4] at 0x080034c0 tail-branches through
      0x0838b198 at 0x080034c4. Ghidra reports 4 bytes; the true extent is
      8 (instruction plus literal), because the next separately linked veneer
      opens at 0x080034c8. The literal is not a function entry: raw ARM at
      0x0838b198 begins `cmp r0, #1; bne 0x0838b218`, within the routine that
      starts at 0x0838b084 with `push {r3,r4,r5,r6,r7,r8,r9,sl,fp,lr}`. This
      transfer skips that routine's setup of live r4-r8 and therefore depends
      on the caller's non-ABI register frame; no callee identity is guessed
      and no Rust dispatch seam is added. Decoding every ARM B/BL word in
      osos.dec finds exactly nine inbound direct calls: seven unconditional
      `bl` at 0x08000b64, 0x08000d28, 0x08000e38, 0x08001030, 0x0800124c,
      0x08001590, and 0x08001860, plus two `bleq` calls at 0x08000c94 and
      0x08001b50. The predicated calls are caller-side state gates; the veneer
      itself has no guard. There are no direct B transfers and no additional
      data-word references (the literal at 0x080034c4 is the sole occurrence).
      A Rust function cannot preserve and consume this continuation contract
      faithfully, so retain status identified until the containing routine can
      be ported as one coherent state machine.
  - address: 0x083e8ba0
    ghidra: FUN_083e8ba0
    name: vector_copy_range_elem24
    size: 60
    sig: "u8 *vector_copy_range_elem24(const u8 *first, const u8 *last, u8 *output)"
    module: cxx/templates
    status: ported
    renames:
      param_1/r0: first
      param_2/r1: last
      param_3/r2: output
      param_4/r3: unused
      r4: output_cursor
      r5: source_cursor
      r6: last
      return/r0: output_cursor
    notes: >-
      Raw osos.dec extent is exactly 0x083e8ba0..0x083e8bdc; the separately
      linked next function begins at 0x083e8bdc. It walks [first, last) in
      24-byte steps, conditionally calling the IRAM memcpy veneer
      (blne 0x08037df8) only when the current output cursor is nonzero, then
      advances both cursors and returns the advanced output pointer. An
      initially NULL output therefore skips only its first record before
      becoming address 0x18. Decoding every ARM B/BL immediate in osos.dec
      finds exactly eight inbound direct calls, all unconditional plain bl at
      0x083e1bd0, 0x083e1c20, 0x083e1d08, 0x083e1d50, 0x083e1d64,
      0x083e1de8, 0x083e1e10, and 0x083e1e3c; no predicated calls or direct
      tail branches exist. r3 is never read. The Rust port calls the
      already-ported memcpy_forward_words body through a volatile function
      pointer rather than the veneer, preserving its forward-copy semantics
      without LLVM inlining it. Host tests cover three records and guards, an
      empty range, and the one-record NULL-output path. match.py structural
      review finds the same cursor loop, conditional copy, and 24-byte step;
      its extra setup and indirect call are the deliberate veneer deviation.

  - address: 0x083e78b4
    ghidra: FUN_083e78b4
    name: vtable_shared_handle_construct
    size: 68
    sig: "VtableSharedHandle *vtable_shared_handle_construct(VtableSharedHandle *storage)"
    module: cxx/vtable_shared_handle_construct
    status: ported
    renames:
      param_1/r0: storage
      DAT_083e78f0: shared_handle_base_descriptor
      DAT_083e78f4: shared_handle_descriptor
      FUN_082a8b68: shared_handle_initialize
      return/r0: storage
    notes: >-
      Raw ARM gives a 68-byte extent: 60 instruction bytes at
      0x083e78b4..0x083e78ec plus literal-pool descriptors 0x089a8aac and
      0x089a8b04 at 0x083e78f0/0x083e78f4; the separate next function starts
      at 0x083e78f8. It writes the base descriptor, calls unported
      FUN_082a8b68 on storage+0x18, clears six words at +0x1c..+0x30, writes
      the derived descriptor, and returns the original storage (Ghidra
      incorrectly gives void). Decoding every ARM B/BL immediate in
      osos.dec verifies exactly eight inbound direct calls, all unconditional
      plain bl at 0x082a782c, 0x082a7dc4, 0x082a8f80, 0x083d8044,
      0x083d8074, 0x083d80d4, 0x083dae68, and 0x083dae9c; no predicated
      forms or direct B transfers exist. FUN_082a8b68 is absent from the
      ledger, so target builds call its verified fixed address and host tests
      use a volatile seam. The host default intentionally cannot model that
      function's target-runtime global shared object and is a no-op. Host
      tests prove the exact call argument, transient base descriptor, retained
      shared-handle write, six-word clear, preserved prefix, ignored callee
      return, and returned storage. match.py structural review completed.

  - address: 0x083d8008
    ghidra: FUN_083d8008
    name: empty_destructor_083d8008
    size: 4
    sig: "void *empty_destructor_083d8008(void *object)"
    module: cxx/empty_destructor_083d8008
    status: ported
    renames:
      param_1/r0: object
      return/r0: object
    notes: >-
      Raw osos.dec extent is exactly 0x083d8008..0x083d800c: the one word
      is mov pc,lr (0xe1a0f00e), after a separately linked function ending at
      0x083d8008 and before the next function beginning at 0x083d800c.
      Decoding every immediate ARM B/BL word verifies exactly eight inbound
      direct calls, all unconditional plain bl at 0x08038e6c, 0x08266dd4,
      0x08266e10, 0x08266e78, 0x08266fcc, 0x082a7550, 0x082a7564, and
      0x083d7c38; there are no predicated forms or direct B tail calls.
      The direct callers pass stack objects and use r0 after the call, so
      this is an empty non-deleting C++ destructor whose return is object,
      not Ghidra's void prototype. The one occurrence as an image data word
      at 0x0895e4bc has no established table or object identity. Rust
      returns object unchanged, reads and writes nothing, and uses a distinct
      text section to avoid identical-code folding. Host tests cover NULL,
      odd, high, and maximal pointer values plus a writable object proving
      no byte changes. match.py structural review completed.

  - address: 0x0836be8c
    ghidra: FUN_0836be8c
    name: ata_command_submit_wait
    size: 156
    sig: "u32 ata_command_submit_wait(AtaCommandDevice *device, u8 *command)"
    module: drivers/ata_command_submit_wait
    status: ported
    renames:
      param_1/r0: device
      param_2/r1: command
      DAT_0836bf1c: ata_device_signature
      DAT_0836bf20: ata_controller_base
      DAT_0836bf24: ata_command_timeout_ms
      FUN_080b1ae8: ata_command_prepare
      FUN_0836c6c8: ata_taskfile_program
      FUN_0836ce80: ata_operation_state_set
      FUN_080d7b7c: ata_command_execute
      return/r0: status
    notes: >-
      Raw osos.dec has 144 instruction bytes at 0x0836be8c..0x0836bf1b and
      three literal-pool words through 0x0836bf27, for a 156-byte extent; the
      separate next function begins at 0x0836bf28. It validates device != NULL,
      device[0] == 0x31656449 ("Ide1"), device +0x44 != 0, and command != NULL,
      returning 7 on failure. It propagates the 0x080b1ae8 preparation status,
      performs a volatile-equivalent read/write of ATA MMIO 0x38700010, then
      propagates task-file programming 0x0836c6c8(device, command, 0), ignores
      0x0836ce80(device, 4), and maps 0x080d7b7c(device, 5000) status 3 to 0
      and every other status to 0x59. All four callees are real direct function
      entries but absent from the ledger; their names describe only their
      verified local roles. Decoding every ARM B/BL immediate in osos.dec finds
      exactly eight inbound direct calls, all unconditional plain bl at
      0x0836bd58, 0x0836bdc8, 0x0836be58, 0x0836be74, 0x0836cd20, 0x0836cd98,
      0x0836ce28, and 0x0836ce68, with no predicated form or direct tail
      branch; a raw aligned word scan finds no DATA word containing this entry.
      Target code calls the unported dependencies through literal fixed-address
      veneers because patch-payload code cannot reach their stock PC-relative
      bl targets; host tests supply volatile callback seams. Four host tests
      cover all validation gates, propagated preparation/task-file errors,
      operation ordering and the ignored state-set result, plus success and
      non-3 execution-status mapping. match.py structural review (exit-1 diff
      expected) retains the validation, early-return, MMIO read/write, four
      call stages, and final status mapping.

  - address: 0x0839b864
    ghidra: FUN_0839b864
    name: record_stream_begin
    size: 156
    sig: "void record_stream_begin(RecordStream *stream, u32 flags)"
    module: util/record_stream_begin
    status: ported
    renames:
      param_1/r4: stream (opaque record stream; position +0x08, record offset +0x0e, remaining +0x12, count +0x14, info +0x40, buffer +0x44)
      param_2/r8: flags (record header byte; bit 3 selects compact offset)
      r5: saved buffer position
      r6: current record header (buffer + saved position)
      r7: stream info (capacity halfword +0x1e)
      r9: record buffer
      FUN_082c5d1c: unported stream flag configuration helper
    notes: >-
      Raw extent is exactly 156 bytes, 0x0839b864..0x0839b8fc; the
      distinct next function starts at 0x0839b900. Decoding every ARM B/BL
      word in osos.dec finds eight direct inbound calls: seven plain bl at
      0x082b5c28, 0x082b641c, 0x082b68b4, 0x082b6ac0, 0x082bdb80,
      0x082bde90, and 0x082dae70; one bleq at 0x082c3688. The predicated
      caller provides the equality gate; this body has no NULL checks.
      It saves byte position +0x08, clears buffer[position..capacity),
      writes an eight-byte header `{flags, 0, 0, 0, 0, capacity_hi,
      capacity_lo, 0}`, chooses next offset position + 8 + (bit3 clear ? 4
      : 0), stores capacity - offset modulo 2^16, invokes
      FUN_082c5d1c(stream, flags), then restores position and publishes
      offset, zero flag bytes +0x01/+0x02 and count +0x14, and state 1 at
      +0x00 in that order. The target reaches still-stock FUN_082c5d1c
      through its literal address; host tests use a recording replacement
      solely for the unported helper, proving its arguments and that it sees
      remaining before final state publication. Host tests also cover both
      bit-3 forms, post-capacity header writes, and u16 remaining underflow.
      match.py structural review exits 1 as expected: the two stock IRAM
      veneer calls resolve through the ported `iram_memzero_veneer`, and the
      retained stock configuration call is an address-loaded `blx`; the
      buffer/header and final field-store ordering remain.

  - address: 0x082e0378
    ghidra: FUN_082e0378
    name: fat_next_cluster
    size: 124
    sig: "u32 fat_next_cluster(FatVolume *volume, u32 cluster)"
    module: fs/fat_next_cluster
    status: ported
    renames:
      param_1/r0: volume
      param_2/r1: cluster
      r4: saved volume
      local_10/[sp]: raw next-cluster value
      FUN_082e0cac: read_cache_position_value (unported resident boundary)
      return/r0: validated next cluster (zero on read failure or reserved marker)
    notes: >-
      Raw extent is exactly 124 instruction bytes, 0x082e0378 through the
      `pop {r3,r4,r5,pc}` at 0x082e03f0; the next separately entered function
      begins with `push {r4,r5,r6,lr}` at 0x082e03f4. Ghidra's four-argument
      prototype is wrong: decoded ARM consumes only r0 volume and r1 cluster,
      then passes r2 = its stack-local output word to unported FUN_082e0cac.
      A zero reader result returns zero before the volume format load. Format
      3 preserves raw values except 0xff8..0xfff; format 8 masks to 28 bits
      then maps exactly 0x0fffffff to zero; every other format preserves raw
      values except 0xfff7..0xffff. Decoding every ARM B/BL immediate in
      osos.dec finds exactly eight direct inbound calls, all unconditional
      plain bl at 0x082b2074, 0x082e0258, 0x082e031c, 0x082e1920,
      0x082e2ba0, 0x082e5f60, 0x082e66d0, and 0x082e6774; no predicated
      forms or direct B tail calls reach the entry. FUN_082e0cac has no
      names.yaml entry, but the existing fs/zero_cache_position resident
      boundary was centralized as fs/cache_position_value and reused rather
      than creating a second host stub. Host tests cover reader failure before
      the volume dereference, ABI forwarding, FAT12 endpoints, FAT32 high-nibble
      masking plus its exact sentinel, and FAT16-style reserved endpoints.
  - address: 0x082d38f4
    ghidra: FUN_082d38f4
    name: opaque_descriptor_convert
    size: 12
    sig: "i32 opaque_descriptor_convert(void *source, void **output_slot)"
    module: cxx/opaque_descriptor_convert
    status: ported
    renames:
      param_1/r0: source
      param_2/r1: output_slot
      DAT_082d38fc/r2: opaque_descriptor_0x089063e8
      FUN_0803b3b8: generic_descriptor_convert_engine
      return/r0: conversion_result
    notes: >-
      Raw osos.dec proves a 12-byte extent, not Ghidra's reported 8:
      0x082d38f4 ldr r2,[pc]; 0x082d38f8 b 0x0803b3b8; and the
      function-local literal 0x089063e8 at 0x082d38fc. The separately linked
      sibling begins at 0x082d3900. Decoding every ARM B/BL immediate in
      osos.dec finds exactly eight inbound direct calls, all unconditional
      plain bl at 0x080af15c, 0x080af16c, 0x080af270, 0x080af280,
      0x08272a84, 0x08272aac, 0x082d4694, and 0x082d46a4; there are no
      predicated BL forms or direct tail branches. No aligned DATA word in
      osos.dec contains this entry. It replaces incoming r2 with the opaque
      descriptor then tail-branches to unported generic engine 0x0803b3b8,
      forwarding source and the optional output-slot unchanged and returning
      that engine's r0 result. The concrete descriptor and engine identities
      remain unrecovered, so none were invented. ARM global assembly preserves
      the three raw words and tail edge; host tests use a volatile callback
      seam because the retail engine is unmapped. match.py reviewed.
  - address: 0x082c4e84
    ghidra: FUN_082c4e84
    name: context_malloc
    size: 104
    sig: "void *context_malloc(sqlite3_context *context, i64 n_byte)"
    module: sqlite/context_malloc
    status: ported
    renames:
      param_1/r0: context
      param_3/r2: n_byte low word
      param_4/r3: n_byte high word
      FUN_083911d0: sqlite3_result_error_toobig
      FUN_083911a8: sqlite3_result_error_nomem
      FUN_08390b14: sqlite3_malloc
      return/r0: allocation
    notes: >-
      Raw osos.dec extent is exactly 104 bytes,
      0x082c4e84..0x082c4eec: the return is pop {r4-r8,pc} at 0x082c4ee8,
      and the distinct next function starts with push {r4-r6,lr} at
      0x082c4eec. Decoding every immediate ARM B/BL word finds exactly eight
      direct inbound calls, all unconditional plain bl at 0x082d2b40,
      0x082d8dd8, 0x082e8b24, 0x082e8bf8, 0x08365410, 0x08367c88,
      0x083944cc, and 0x08396648; there are no predicated forms or direct
      tail branches. This is SQLite 3.5's private contextMalloc: obtain the
      scalar callback context's embedded Mem.db at context+0x18, sign-extend
      its aLimit[SQLITE_LIMIT_LENGTH] word at db+0x50, and compare that full
      signed i64 ceiling with n_byte. A request strictly over the limit calls
      sqlite3_result_error_toobig and returns NULL. Otherwise it calls
      sqlite3_malloc with the low word; a NULL result calls
      sqlite3_result_error_nomem only for a strictly positive i64 request.
      The error helpers are verified real entries but absent from the ledger,
      so the target uses fixed-address callback defaults for
      0x083911d0/0x083911a8 while host tests install recorders; sqlite3_malloc
      @ 0x08390b14 is already ported and called directly. Rust models the
      AAPCS r2:r3 request as a natural i64, omitting only the ABI's unused r1
      padding word. Host tests cover strict-limit rejection, equality at the
      limit, and zero/negative including i64::MIN requests. match.py
      structural review completed.
  - address: 0x082a8ba0
    ghidra: FUN_082a8ba0
    name: opaque_shared_array_handle_release
    size: 96
    sig: "void **opaque_shared_array_handle_release(void **handle)"
    module: cxx/handle
    status: identified
    renames:
      param_1/r0: handle
      puVar3/r4: shared_array_body
      DAT_082a8c00: opaque_array_element_callback
      FUN_0803170c: cxx_vec_delete
      FUN_082aad24: operator_delete
    notes: >-
      Raw osos.dec gives an exact 96-byte extent: 92 instruction bytes from
      0x082a8ba0 through the pop {r4,r5,r6,pc} at 0x082a8bfc, followed by
      the literal word 0x083cddb4 at 0x082a8c00; the next separately linked
      function opens at 0x082a8c04. It returns handle. A NULL *handle is
      unchanged; otherwise it decrements the body word at +0x1c with wrapping
      subs semantics. Only a 1-to-0 transition C++-deletes arrays at body+0
      and body+8 through 0x0803170c using the literal callback, then
      operator-deletes the body at 0x082aad24. The handle word is never
      cleared. Decoding every ARM B/BL immediate in osos.dec finds exactly
      eight inbound direct calls, all unconditional plain bl at 0x08266e54,
      0x082a7174, 0x082a7430, 0x082a7d80, 0x082a950c, 0x083da7a4,
      0x083e7880, and 0x083e7974; there are no predicated or tail-B callers.
      This cannot be ported faithfully in isolation: the array finalizer calls
      0x083cddb4 as an element destructor, but raw ARM there begins
      `str r6,[sp,#8]` inside the separately linked routine that starts at
      0x083cdd24. Its immediately preceding execution relies on live r4-r9
      and stack-frame state, so it is not a real function entry or a
      recoverable C ABI. No callee identity or host seam is invented. Retain
      identified status until that containing routine and its non-ABI callback
      contract can be ported together.

  # --- UI: passkey mask-indicator refresh ---
  - address: 0x0827f098
    ghidra: FUN_0827f098
    name: refresh_passkey_mask_indicators
    size: 128
    sig: "void refresh_passkey_mask_indicators(void *passkey_mask_view)"
    module: ui/passkey_mask_indicators
    status: ported
    renames:
      param_1/r0: passkey_mask_view
      DAT_0827f10c: masked_digit_word
      DAT_0827f108: first_mask_indicator_selector
      DAT_0827f110: second_mask_indicator_selector
      DAT_0827f114: third_mask_indicator_selector
      DAT_0827f118: fourth_mask_indicator_selector
      "(*passkey_mask_view->vtable)[0x58]": opaque_mask_indicator_dispatch
    notes: >-
      Raw osos.dec gives 112 instruction bytes from push {r4-r6,lr} at
      0x0827f098 through bx r3 at 0x0827f104, then a 16-byte literal pool
      at 0x0827f108..0x0827f118 (0x5799, 0x2a2a2a2a, 0x579a, 0x579b,
      0x579c); true extent is 128 bytes and the separately linked next
      function begins at 0x0827f11c. Decoding every immediate ARM B/BL word
      in osos.dec finds exactly eight inbound direct calls, all unconditional
      plain bl at 0x0813f860, 0x0820e880, 0x0827ebcc, 0x0827f2a0,
      0x0827f2c4, 0x0827f97c, 0x0827fa08 and 0x0827fa80; there are no
      predicated direct calls or direct tail branches. Algorithm: load the
      passkey view's vtable slot +0x58 anew for each selector, then call it
      with the view, the four-asterisk word 0x2a2a2a2a and selectors 0x5799
      through 0x579c in ascending order. The virtual entry's concrete
      identity is not recovered, so the port preserves only its verified ABI
      and reload-before-each-call behavior rather than inventing a callee.
      Host tests use a native-width structural vtable; the target build
      asserts its dispatched field remains at ARM byte offset +0x58.

  # --- current indexed-record handle accessor ---
  - address: 0x0829e1b4
    ghidra: FUN_0829e1b4
    name: current_record_handle
    size: 36
    sig: "u32 current_record_handle(const CurrentRecordCursor *cursor)"
    module: app/current_record_handle
    status: ported
    renames:
      param_1/r0: cursor
      param_1+0x04: records
      param_1+0x08: current_index
      field_+0x04: handle
    notes: >-
      Raw osos.dec gives the exact 36-byte extent 0x0829e1b4..0x0829e1d8:
      the next separately linked function starts at 0x0829e1d8. The routine
      returns zero only when cursor->current_index is -1; otherwise it returns
      the word at cursor->records + current_index * 0x14 + 4, with modulo-32-bit
      add arithmetic and no NULL or bounds guards. Decoding every ARM B/BL
      immediate in osos.dec finds exactly eight inbound direct calls, all
      unconditional plain bl at 0x081af640, 0x081c836c, 0x081c84f4,
      0x081c857c, 0x081c85fc, 0x081c86e8, 0x081c8820, and 0x081c8908; there
      are zero predicated bl calls. A ninth inbound branch is the unconditional
      tail b at 0x0829dd2c, whose preceding add r0,r0,#4 adapts a containing
      object to this cursor layout. No aligned DATA word names this entry.

  # --- application resource-reference value lookup ---
  - address: 0x082a449c
    ghidra: FUN_082a449c
    name: resource_reference_value
    size: 120
    sig: "u32 resource_reference_value(ResourceReference *reference)"
    module: app/resource_reference_value
    status: ported
    renames:
      param_1/r0: reference
      param_1+0x00: vtable
      param_1+0x04: indirect_source
      param_1+0x08: direct_entry
      param_1+0x0c: resource_context
      "(*reference->vtable)[0x08]": resource_reference_is_valid
      FUN_082a2cac: resolve_indirect_resource_entry
      FUN_080506bc: resource_index_lookup
    notes: >-
      Raw osos.dec proves the Ghidra 120-byte extent is exact: the body runs
      from 0x082a449c through pop {r4,r5,r6,pc} at 0x082a4510 and the next
      separately linked function opens at 0x082a4514, with no literal pool.
      Decoding every immediate ARM B/BL word finds exactly eight inbound
      direct calls, all unconditional plain bl at 0x0812f918, 0x081f79a4,
      0x081fd178, 0x08208b5c, 0x0821dc8c, 0x0821efc8, 0x0823a570 and
      0x0823a57c; there are no predicated forms or direct tail branches.
      It calls vtable slot +0x08 to validate the reference, selects its
      direct +0x08 entry or resolves a non-null +0x04 source through the
      still-unported 0x082a2cac, then queries the resource index at
      context+0xf9c through still-unported 0x080506bc. A matched record
      returns its +0x0c value, otherwise zero. Target builds call both
      verified retailOS addresses; host tests inject their ABI through a
      private seam. The vtable target has no recovered concrete identity, so
      only its binary-verified ABI and slot are represented.
  - address: 0x0829cf40
    ghidra: FUN_0829cf40
    name: byte_is_three_or_four
    size: 24
    sig: "u32 byte_is_three_or_four(const u8 *value)"
    module: util/value_predicate
    status: ported
    renames:
      param_1/r0: value
      r0 (return): predicate_result
    notes: >-
      Raw osos.dec establishes the exact 24-byte extent from 0x0829cf40
      through 0x0829cf54; the separately linked next function begins at
      0x0829cf58. It performs `ldrb r0,[r0]`, compares that byte with 3 and,
      only if unequal, 4, then returns normalized one or zero in r0. Decoding
      every immediate ARM B/BL word in osos.dec finds exactly eight inbound
      direct calls, all unconditional plain bl at 0x0817dc1c, 0x0817ddd8,
      0x0817de34, 0x0817de94, 0x0817df90, 0x0817dfb4, 0x0817e088, and
      0x0817e168; there are no predicated direct calls or direct tail
      branches. No aligned DATA word names this entry. No deviations: the
      port preserves the single byte read and intentionally has no NULL guard.
      Host tests exhaust all 256 input byte values, accepting only 3 and 4.
  - address: 0x0827e48c
    ghidra: FUN_0827e48c
    name: singleton_class_7a00_instance
    size: 12
    sig: "void *singleton_class_7a00_instance(void)"
    module: app/singletons
    status: ported
    renames:
      DAT_0827e498: singleton_class_7a00_holder
      DAT_089cc604: singleton_class_7a00_instance
    notes: >-
      Raw osos.dec gives the exact 12-byte extent 0x0827e48c..0x0827e498:
      `ldr r0,[pc,#4]; ldr r0,[r0,#4]; bx lr`; the literal at 0x0827e498 is
      0x089cc600 and the separately linked next function begins at
      0x0827e49c. The accessor returns the holder's +4 slot unconditionally,
      including NULL. Decoding every immediate ARM B/BL word in osos.dec finds
      exactly eight inbound direct calls, all unconditional plain bl at
      0x08113fc4, 0x0816cfc4, 0x0816d3bc, 0x0816d838, 0x0816da48, 0x0816daac,
      0x0816daf8, and 0x0816db64; there are no predicated bl forms, direct
      tail b callers, or aligned DATA words that name this entry. The holder
      is filled by FUN_0827e76c after allocating 0x16c bytes and constructing
      FUN_0827e7fc, which registers the result under class id 0x7a00. No
      concrete class name survives, so the symbol records only that verified
      identity. Target code reads the live runtime slot at 0x089cc604; host
      builds use a NULL-initialized static because the decrypted 0x089cxxxx
      RW page holds stale string data rather than runtime globals.

  # --- Opaque application-object interior-address accessor ---
  - address: 0x0829a06c
    ghidra: FUN_0829a06c
    name: object_offset_48
    size: 8
    sig: "u8 *object_offset_48(u8 *object)"
    module: app/object_offset_48
    status: ported
    renames:
      param_1/r0: object
      r0 (return): object_plus_0x48
    notes: >-
      Raw osos.dec is exactly `add r0,r0,#0x48; bx lr` at 0x0829a06c..0x0829a070;
      the separately linked next function starts at 0x0829a074, confirming the
      eight-byte extent. Decoding every aligned ARM B/BL immediate in osos.dec
      finds exactly eight inbound direct calls, all unconditional plain bl at
      0x080ac9ec, 0x08109300, 0x08109534, 0x0810a48c, 0x08128130, 0x0817544c,
      0x081dd9c8, and 0x081e92a0; there are no predicated forms or direct tail
      branches. No aligned DATA word names the entry, so it is not virtually
      dispatched. Each caller gets the object through task_ctx_field_0x30 and
      object_cast_to_class(..., 0x80), then uses object+0x48 as an opaque
      payload base; the recovered callers do not establish a concrete class or
      field layout, so the port deliberately retains an offset-based name. No
      behavioral deviations: wrapping_add preserves the raw address addition,
      including NULL -> 0x48 and target-width wraparound. Host tests check the
      exact interior address, surrounding-byte non-mutation, and NULL case.
  - address: 0x0827d6e4
    ghidra: FUN_0827d6e4
    name: recording_controller_get
    size: 116
    sig: "void *recording_controller_get(void)"
    module: app/recording_controller
    status: ported
    renames:
      DAT_0827d748: recording_controller_state
      DAT_0827d74c: recording_controller
      DAT_0827d750: dso_handle
      DAT_0827d754: recording_controller_destructor_word
      FUN_0827de04: recording_controller_construct
      FUN_0827ddb8: recording_controller_one_time_setup
      FUN_082ab31c: cxa_guard_acquire
      FUN_082ab1c8: cxa_atexit
      FUN_082ab338: cxa_guard_release
    notes: >-
      Raw osos.dec corrects Ghidra's 100-byte code-only extent: instructions
      run from 0x0827d6e4 through pop {r4,pc} at 0x0827d744, then the four
      literal words at 0x0827d748..0x0827d754 make the true extent 116 bytes;
      the next separately linked function begins at 0x0827d758. Decoding every
      immediate ARM B/BL word finds exactly eight inbound direct calls, all
      unconditional plain bl at 0x08085ff8, 0x0816803c, 0x08168080,
      0x081b0b1c, 0x081b0b30, 0x081b0b84, 0x081b0bec, and 0x081b0de4;
      there are no predicated forms or direct tail branches. It tests bit 0 of
      the guard at state+8, conditionally constructs and registers the fixed
      0x08ad17c0 controller, then independently runs one-time setup while the
      state byte is clear before returning the fixed literal. Recording-buffer
      producers call it before the paired recording sync path, so
      recording_controller is the observed role; no C++ class-name literal
      survives, and none is invented. The unported constructor 0x0827de04 and
      setup 0x0827ddb8 remain target-address seams with host models. The
      registered word 0x08272fac is not a function entry: raw bytes are ASCII
      URL data beginning g/00/9/xmlsig#, so the Rust shutdown handler is
      deliberately inert rather than inventing a callee.

  # --- Scoped-context chapter count ---
  - address: 0x082a2c34
    ghidra: FUN_082a2c34
    name: scoped_context_chapter_count_or_zero
    size: 76
    sig: "u32 scoped_context_chapter_count_or_zero(const ScopedContext *this)"
    module: app/scoped_context
    status: ported
    renames:
      param_1/r0: this
      FUN_080448a0: owner_name_value_lookup
      r1@0x082a2c60: source
      r1@0x082a2c68: value
    notes: >-
      Raw bytes establish a 76-byte extent, correcting Ghidra's 156-byte
      report: nineteen ARM instructions run from 0x082a2c34 through the final
      pop {r4,pc} at 0x082a2c7c, and the separately linked next function
      begins with its own push {r4,lr} at 0x082a2c80. Decoding every aligned
      immediate ARM B/BL word in osos.dec finds eight inbound direct calls,
      all unconditional plain bl at 0x08112580, 0x08173b10, 0x081798a4,
      0x0817bec4, 0x081f8fb8, 0x0821dc7c, 0x0821efb8, and 0x0823419c; there
      are zero predicated forms and zero direct tail branches to this entry.
      It dispatches this->vtable slot +0x08, returns zero without reading the
      owner on a zero validity result, then reads owner +0x10. Bit 0 of that
      source's first byte gates a non-NULL +0x14 value pointer, which it passes
      by tail branch to 0x080448a0; every other path returns zero. Callers
      establish that the returned u32 is a chapter count: at 0x0823419c,
      values 0 and 1 select AudiobookHasNoChapters while an unsigned value
      greater than 1 selects AudiobookHasMultipleChapters. A valid token with
      a NULL owner or source faults just as in ARM. The target's raw body
      carries the literal name, but no concrete identity for 0x080448a0 is
      recovered or invented. It has no ledger entry, so target builds call its
      verified ROM address while host tests replace it with a recording callback.
      No aligned data word in osos.dec holds 0x082a2c34,
      so this entry is not virtual-dispatched. Deliberate deviations: repr(C)
      fragments replace literal offsets and source expresses the stock direct
      tail branch as a typed call; ARM codegen retains the transfer as
      literal-loaded bx rather than direct b. The validity gate, source
      selection, pointer argument, and u32 result are preserved. Host tests
      prove invalid short-circuiting, every disabled-bit case, NULL value
      rejection, and full-result delegation after all gates pass. match.py
      structural review with --size 76 finds the same vtable +0x08 blx,
      validity compare, gated owner +0x10/source byte/source +0x14 chain, and
      tail transfer. The 22-versus-19 instruction diff is the crate frame,
      branch layout in place of ADS predicates, and literal-loaded bx target.
  - address: 0x08276fbc
    ghidra: FUN_08276fbc
    name: unicode_casefold_byte
    size: 20
    sig: "u32 unicode_casefold_byte(u32 codepoint)"
    module: cxx/string_object
    status: identified
    renames:
      param_1/r0: codepoint
      DAT_08276fd0: unicode_casefold_byte_table
    notes: >-
      Raw osos.dec is exactly five ARM words from 0x08276fbc through bx lr at
      0x08276fcc; the literal word at 0x08276fd0 (0x083ed1dd) belongs to this
      function's pool and the separately linked next function begins at
      0x08276fd4. It returns codepoint unchanged when codepoint >= 0x100;
      otherwise it returns unicode_casefold_byte_table[codepoint & 0xff].
      Decoding every aligned immediate ARM B/BL word in osos.dec finds exactly
      eight inbound direct calls, all unconditional plain bl at 0x080c6d74,
      0x080c6d80, 0x081326bc, 0x082760cc, 0x082760dc, 0x08276140,
      0x08276150, and 0x082761a8; there are no predicated forms or direct tail
      branches. This is deliberately identified rather than ported: the
      literal table base is in static-image code, whose first byte is 0xf0,
      making fold(0) nonzero and contradicting the case-insensitive UTF-8
      comparison callers' terminator behavior. The firmware must replace or
      relocate this 256-byte table at runtime, but that mechanism and its
      contents are unresolved; modeling the static bytes would be fabricated
      behavior.
  - address: 0x0826ed98
    ghidra: FUN_0826ed98
    name: ui_element_set_render_owner_clip
    size: 68
    sig: "void ui_element_set_render_owner_clip(UiElement *element, const Rect *region)"
    module: ui/render_owner_clip
    status: ported
    renames:
      param_1/r4: element
      param_2/r5: region
      iVar1/r6: render_owner
      local_20: clipped_region
      "element+0x80": bounds
      "render_owner+0xec": pending_clip
      "vtable+0x5c": render_owner
      FUN_0826c24c: rect_intersect_into
    notes: >-
      Raw osos.dec establishes the true 68-byte extent: push {r0-r6,lr} at
      0x0826ed98 through pop {r0-r6,pc} at 0x0826edd8; the separately linked
      next function is the bx lr at 0x0826eddc. Decoding every immediate ARM
      B/BL word finds exactly eight inbound direct calls, all unconditional
      plain bl at 0x08140df4, 0x08158434, 0x081b8d14, 0x081baee0,
      0x0826ea6c, 0x08285d9c, 0x08285ef0, and 0x08287dac; there are no
      predicated forms, tail-branch callers, or aligned data-word references.
      The body calls element's vtable +0x5c method before intersecting region
      with element+0x80 through already ported rect_intersect_into, then
      copies the four-word result to returned render_owner+0xec. It has no
      NULL guard for any input, vtable, returned owner, or accessed field.
      The render owner's concrete class is not inferred. Target builds load the
      existing rect_intersect_into port through a volatile function pointer to
      retain the original call boundary; that makes an indirect blx where ADS
      used direct bl but changes no behavior. Host fixtures use packed
      native-pointer views solely to retain retailOS byte offsets. Tests cover
      clipping plus the vtable-selected owner, and empty/disjoint rectangles.
  - address: 0x08292788
    ghidra: FUN_08292788
    name: global_callback_register
    size: 128
    sig: "void global_callback_register(u32 callback)"
    module: app/global_callback_register
    status: ported
    renames:
      param_1/r4: callback
      DAT_08292808: callback_list_mutex
      DAT_0829280c: callback_list
      local_10: cursor
      local_0c: end
      FUN_083d6f94: not_equal_deref_alias_6f94
      FUN_083dd660: callback_list_append
    notes: >-
      Raw osos.dec establishes exactly 128 bytes from push {r0,r4,r5,r6,lr}
      at 0x08292788 through the branch to the common unlock at 0x08292804;
      the two literal words at 0x08292808 and 0x0829280c are its pool, and
      the separately linked next function starts at 0x08292810. It locks the
      callback-list mutex at 0x08ad2c4c through the ported 0x082621a8 alias,
      then walks the circular list headed by the sentinel word at
      0x08ad2c68 + 0x10. It compares each node's callback word at +0x08 with
      r0; an equal word exits without mutation, while reaching the sentinel
      appends r0 through 0x083dd660. It always unlocks through the ported
      0x082621ac alias and ignores both mutex statuses. Decoding every aligned
      immediate ARM B/BL word in osos.dec finds exactly eight inbound direct
      calls, all unconditional plain bl at 0x0816e3fc, 0x0819e888,
      0x081a0fd4, 0x081b8304, 0x081b8bb0, 0x081db82c, 0x081ea538, and
      0x081ea5dc; there are no predicated forms. The unported list append has
      no names.yaml port entry, so target builds call its fixed retail address
      through a typed function pointer and host tests install a recording seam.
      Tests cover an absent zero word,
      duplicate suppression, complete-list traversal, and mutex unlock.

  - address: 0x08275b40
    ghidra: FUN_08275b40
    name: event_payload_message_construct
    size: 48
    sig: "EventPayloadMessage *event_payload_message_construct(EventPayloadMessage *storage, u32 kind, const u32 *payload_words, u32 event_code)"
    module: app/event_payload_message
    status: ported
    renames:
      param_1/r0: storage
      param_2/r1: kind
      param_3/r2/r4: payload_words
      param_4/r3/r5: event_code
      puVar1/r0: this
      DAT_08275b70: EVENT_PAYLOAD_MESSAGE_VTABLE
      FUN_08266a48: message_kind_construct
    notes: >-
      Raw osos.dec has 48 bytes of code from the push {r4,r5,r6,lr} at
      0x08275b40 through pop {r4,r5,r6,pc} at 0x08275b6c, then the 4-byte
      literal-pool word 0x089a5fb4 at 0x08275b70; the separately linked
      next function opens at 0x08275b74, making the true extent 52 bytes.
      Decoding every aligned immediate ARM B/BL word in osos.dec finds eight
      inbound direct calls, all unconditional plain bl at 0x08100044,
      0x08110678, 0x08110990, 0x08110c98, 0x08110cd8, 0x08110d04,
      0x08110d54, and 0x08110d84; there are no predicated bl forms or tail
      b callers. It calls the already-ported message_kind_construct with r0
      and r1, stores its own vtable at +0x00, copies the two words addressed
      by r2 to +0x08/+0x0c in order, stores r3 at +0x10, and returns the
      constructor result in r0. Ghidra's void signature loses this returned
      pointer, which all eight callers consume. The 0x089a5fb4 vtable is
      retained as a raw address constant: this constructor never dispatches
      through it, so no identity is inferred for its unresolved slots. Host
      tests prove the five exact stores, guard-word preservation, returned
      pointer, and source/storage alias ordering.

  - address: 0x08272800
    ghidra: FUN_08272800
    name: video_engine_release_one_handle_and_delete
    size: 28
    sig: "void video_engine_release_one_handle_and_delete(void *unused, u32 *handle)"
    module: util/video_engine
    status: ported
    renames:
      param_1/r0: unused
      param_2/r1/r4: handle
      FUN_082d1134: video_engine_release_one_handle_unported
      FUN_082aad24: operator_delete
    notes: >-
      Raw osos.dec fixes the complete 28-byte extent: `push {r4,lr}; mov
      r4,r1; mov r0,#1; bl 0x082d1134; mov r0,r4; pop {r4,lr}; b
      0x082aad24`, followed immediately by the distinct function at
      0x0827281c. It ignores r0, calls the unported video-engine helper
      0x082d1134 with count 1 and the r1 handle, then tail-branches to the
      already ported tag-2 operator_delete with that unchanged handle. There
      is no NULL guard before the helper; operator_delete alone guards its
      free path. A whole-image ARM B/BL decode finds 8 inbound direct calls:
      4 unconditional bl at 0x0827ba6c, 0x0827ba84, 0x0827bc88, and
      0x0828e6a8; 4 blne at 0x0827bd38, 0x0827bd48, 0x0827bd58, and
      0x0827bd68, each gated by its caller's non-NULL handle load. No aligned
      data word references this entry. Target builds transfer directly to
      0x082d1134; host tests replace only that unported boundary and record
      both the exact count/handle forwarding and the tag-2 deletion.
